{{!--
templates/actor/parts/weapon-range.hbs
Purpose: Contains conditional display logic.
How it fits: Used by rendering code to produce UI fragments.
--}}


{{#if system.area}}{{#unless (eq system.area '-')}}
{{system.area}},
{{/unless}}{{/if}}
{{either system.range.short 'OK'}}/
{{~#if (eq system.range.max "short")}}-
{{~else}}{{either system.range.medium 'OK'}}
{{/if~}}/
{{~#if (eq system.range.max "long")~}}
{{either system.range.long 'OK'}}
{{~else~}}-
{{/if}}

