{{!--
templates/actor/parts/description.hbs
Purpose: Contains styled HTML nodes for UI layout.
How it fits: Used by rendering code to produce UI fragments.
--}}


<div class="section-group column">
  <h2 class="section-group-header">{{ANARCHY.common.description}}</h2>
  <div class="editor actor-editor">
    {{editor 
      system.description 
      target="system.description"
      owner=options.owner
      button=true
      editable=options.editable}}
  </div>
</div>

