{{!--
templates/chat/parts/glitch.hbs
Purpose: Renders collections with `each` blocks. Contains conditional display logic.
How it fits: Used by rendering code to produce UI fragments.
--}}


<div class="risk-glitch risk-glitch-{{@root.roll.glitchOutcome}}">
  <label>
    {{> 'systems/mwd/templates/common/label.hbs' this}}:
  </label>
  {{#each @root.roll.subrolls.glitch.terms.[0].results as |dice|}}
    {{#if (eq dice.result 1)}}
      <span class="risk-glitch-dice {{@root.roll.glitchOutcome}}">
        {{{iconD6 dice.result}}}
      </span>
    {{/if}}
  {{/each}}
  {{#if @root.roll.subrolls.glitch.total}}
    <label>
      {{@root.roll.subrolls.glitch.total}}
    </label>
  {{/if}}
</div>
