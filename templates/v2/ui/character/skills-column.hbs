{{!--
templates/v2/ui/character/skills-column.hbs
Purpose: Renders collections with `each` blocks. Contains styled HTML nodes for UI layout.
How it fits: Used by rendering code to produce UI fragments.
--}}

<div class="mwd-skills-col">
  {{#each (lookup @root.skillsDisplay context.skills) as |s|}}
    {{> "mwd.v2.ui.character.skill-row" s=s}}
  {{/each}}
</div>
