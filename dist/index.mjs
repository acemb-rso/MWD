var cy = Object.defineProperty;
var uy = Object.getPrototypeOf;
var dy = Reflect.get;
var ed = (i) => {
  throw TypeError(i);
};
var my = (i, e, t) => e in i ? cy(i, e, { enumerable: !0, configurable: !0, writable: !0, value: t }) : i[e] = t;
var z = (i, e, t) => my(i, typeof e != "symbol" ? e + "" : e, t), _o = (i, e, t) => e.has(i) || ed("Cannot " + t);
var Y = (i, e, t) => (_o(i, e, "read from private field"), t ? t.call(i) : e.get(i)), Ce = (i, e, t) => e.has(i) ? ed("Cannot add the same private member more than once") : e instanceof WeakSet ? e.add(i) : e.set(i, t), Ie = (i, e, t, n) => (_o(i, e, "write to private field"), n ? n.call(i, t) : e.set(i, t), t), v = (i, e, t) => (_o(i, e, "access private method"), t);
var Oi = (i, e, t) => dy(uy(i), t, e);
const q = {
  TYPES: {
    Actor: {
      character: "Character",
      vehicle: "Vehicle/drone",
      battlemech: "Battlemech"
    },
    Item: {
      contact: "Contact",
      gear: "Gear",
      consumable: "Consumable",
      quality: "Trait",
      assetModule: "Asset Module",
      skill: "Skill",
      mechWeapon: "Mech-Scale Weapon",
      personalWeapon: "Personal Weapon",
      armor: "Armor"
    }
  },
  settings: {
    defaultCssClass: {
      name: "Style of Destiny UI",
      hint: "Select the style used for actors, items and GM Manager"
    },
    skillSet: {
      name: "Skill set",
      hint: "Select the set of skills to use"
    },
    gmDifficulty: {
      name: "Default difficulty pools",
      hint: "The default difficulty pools, represented as 'Trivial:4,Easy:6' ...",
      default: "Trivial:4,Easy:6,Average:8,Hard:10,Very hard:12",
      chatMessage: "The difficulty {difficulty} ({pool}d6) is {success}"
    },
    damageMode: {
      name: "Damage application",
      hint: "Determine how Armor / Damage Resistance (RD) applies",
      values: {
        resistanceArmorMonitor: "MechWarrior: Destiny: Resistance, Armor, then Monitor",
        armorResistanceMonitor: "Armor, Resistance, Monitor",
        armorGivesResistance: "RD = Armor/3 + Str/4, damaged per blow, AA ignores armor",
        armorGiveResistanceHitsAvoid: "RD = Armor/3, damaged per blow, AA reduces resistance"
      }
    },
    useDestinyMechanics: {
      name: "Use Destiny opposed roll mechanics",
      hint: "Roll 2D6 and add the computed pool as a modifier instead of counting successes."
    }
  },
  chat: {
    blindMessageToGM: "Blind message from {user}:<br>{message}",
    sufferedDrain: "{actor} suffered a drain of {drain}",
    noDrain: "{actor} did not suffer any drain",
    defendAttack: "Defense against {success} success",
    partiallyDefended: "Attack exceeds defense of {success} success",
    fullyDefended: "The attack is fully defended",
    applyDamage: "Apply {damage}"
  },
  chat_actions: {
    rollDice: {
      title: "Roll Destiny dice",
      instruction: "Number of dice to roll: ",
      error: "Veuillez entrer un nombre valide de dés.",
      result: "Roll of {count}d6, {success} success! {ones} dice with a value of 1"
    }
  },
  user: {
    selectedTokenActors: "Selected actor tokens"
  },
  common: {
    newEntry: "New entry...",
    newName: "New {type}",
    cancel: "Cancel",
    add: "Add",
    resize: "Resize",
    say: "Say:",
    edit: "Edit",
    activate: "Activate",
    del: "Remove",
    favorite: "Favorites",
    addFavorite: "Add to favorites",
    delFavorite: "Remove from favorites",
    viewMode: "View mode",
    editMode: "Edit mode",
    attach: "Change owner",
    attachCopy: "Give a copy",
    roll: {
      button: "Roll",
      title: "Roll {name} {specialization}",
      attribute: "Attribute",
      attribute2: "Second attribute",
      modifiers: {
        edge: "Use edge",
        edgePool: "Spend from",
        specialization: "Specialization",
        poolModifiers: "Asset Modules",
        social: {
          credibility: "Credibility",
          rumor: "Rumor"
        },
        anarchyDisposition: "Anarchy - Dispositions",
        anarchyRisk: "Anarchy - Take risks",
        glitch: "Glitch dice",
        convergence: "GOD Convergence",
        drain: "Drain",
        wounds: "Wounds modifiers",
        weaponRange: "Range",
        weaponArea: "Multiple targets",
        other: "Other modifiers",
        virtualReality: "Virtual Reality",
        reduced: "Pool reduced",
        reroll: "Rerolls",
        rerollForced: "Rerolls successes",
        opponentReroll: "Force opponent rerolls",
        opponentPool: "Reduce opponent pool"
      },
      rollTheme: {
        dicePool: "Dice pool",
        reroll: "Failure rerolls",
        removed: "Success to reroll",
        rerollRemoved: "Reroll removed success",
        glitch: "Glitch dice",
        drain: "Drain",
        convergence: "Convergence",
        anarchyRisk: "Anarchy risk dice"
      },
      opponentRoll: "Opponent roll",
      totalSuccess: "Total successes",
      success: "Successes",
      risk: {
        prowess: "Prowess",
        mixed: "Prowess and Glitch",
        nothing: "no effect",
        glitch: "Glitch"
      },
      rerollSuccess: "Successes after reroll",
      rerollForcedLoss: "Forced reroll",
      rerollForcedSuccess: "Forced reroll successes"
    },
    monitorValue: "Monitor value",
    confirmation: {
      del: "Confirm removal",
      delItem: "Confirm removal of {name} ({type})",
      delowner: "Confirm detach from owner {name}",
      attach: "Attach actor to owner",
      attachOrCopy: "Attach {ownedType} {ownedName} to {ownerType} {ownerName}, or create a copy first"
    },
    selection: {
      actorSettingMarks: "Select actor setting Marks on {name}"
    },
    errors: {
      insufficient: "Not enough {resource}: required {required}, available {available}",
      outOfRange: "Cannot set {resource} to {value}, out of range [{min} , {max}]",
      onlyGM: "Only allowed for GM",
      noEdgeForActor: "{actor} is a {actorType}, so it cannot use Edge",
      expectedType: "Item is of type {type} instead of expected type {expectedType}",
      ignoredTargets: "This action cannot target {targets} due to its damage type",
      noTargetSelected: "No valid target is selected, consider selecting a valid target before using this {weapon}",
      maxTargetsExceedeed: "{weapon} has a {area} area of effect, with a maximum of {max} targets. You are currently targetting {count} targets",
      noDefenseOnWeapon: "No defense configured for {actor} weapon: {weapon}.<br>Configure defense against this weapon to be able to attack with it.",
      weaponNotFound: "No weapon could be found for this roll.",
      noTokenActor: "Token is not attached to an Actor!",
      cannotUseEdgeAnymore: "Too late to use edge. A defender already rolled his defense!",
      actorCannotApplyDamage: "Actor {actor} cannot apply {damageType} : maybe check the decker has connected his cyberdeck",
      actorCannotReceiveDamage: "Cannot apply {damageType} to actor {actor} : it does not have a condition monitor for this type of damage",
      actorDoesNotHaveDefense: "Actor {actor} does not have a {defense}, the attack should not to target {actorType}.<br>Or it may be a bug, please report and manage manually"
    },
    sourceReference: "Source reference",
    sourceReferenceHelp: "rulebook, page, ...",
    description: "Description"
  },
  actor: {
    characterSheet: "Character sheet",
    characterTabbedSheet: "Character sheet (tabs)",
    characterEnhancedSheet: "Character enhanced sheet (tabs)",
    vehicleSheet: "Vehicle sheet",
    battlemechSheet: "Battlemech sheet",
    characterNPCSheet: "NPC sheet",
    actorName: "Name",
    celebrity: "Legend",
    famous: "Fame",
    edgePools: {
      title: "Edge Pools",
      physical: "Physical",
      mental: "Mental",
      social: "Social",
      grit: "Grit",
      insight: "Insight",
      rumor: "Rumor",
      legend: "Legend",
      credibility: "Credibility",
      chaos: "Chaos",
      rating: "Rating",
      current: "Current"
    },
    tabs: {
      main: "Character",
      equipment: "Equipment",
      biography: "Biography"
    },
    words: {
      keywords: "Keywords",
      cues: "Cues",
      dispositions: "Dispositions"
    },
    counters: {
      xp: "XP",
      xpUnused: "Unspent XP",
      xpTotal: "Lifetime XP",
      current: "Current",
      lifetime: "Lifetime",
      edge: "Edge",
      edgePools: {
        physical: "Physical",
        mental: "Mental",
        social: "Social",
        grit: "Grit",
        insight: "Insight",
        rumor: "Rumor",
        legend: "Legend",
        credibility: "Credibility",
        chaos: "Chaos",
        rating: "Rating",
        current: "Current"
      },
      mental: {
        insight: "Insight",
        rumor: "Rumor"
      },
      social: {
        legend: "Legend",
        credibility: "Credibility"
      }
    },
    monitors: {
      conditionMonitors: "Condition monitors",
      overflow: "{actor}: Overflow of {monitor} condition monitor, transfering {overflow} to {overflowMonitor} condition monitor",
      physical: "Physical",
      fatigue: "Fatigue",
      armor: "Armor",
      structure: "Structure",
      heat: "Heat",
      effect: "Effect",
      grit: "Grit",
      insight: "Insight",
      rumor: "Rumor",
      legend: "Legend",
      credibility: "Credibility",
      chaos: "Chaos",
      resistance: "Resistance",
      resistanceBase: "Base resistance",
      resistanceByType: "By damage type",
      resistanceByTypeButton: "Resist by type",
      resistanceByTypeTitle: "Type-specific resistance",
      resistanceBonusLabel: "Bonuses",
      resistanceTotal: "Total",
      resistanceFallback: "Falls back to base",
      damageType: "Damage type",
      resistancePresets: {
        label: "Presets",
        biological: "Biological",
        armoredVehicle: "Armored vehicle",
        energyShielded: "Energy shielded"
      },
      resistanceApplied: "{actor} resisted {value} vs {damageType} on {monitor} ({source})",
      resistanceSources: {
        default: "base",
        type: "type-specific"
      }
    },
    vehicle: {
      moves: "Moves",
      attacks: "Attacks",
      stealth: "Stealth",
      category: "Category",
      skill: "Skill",
      weapons: "Vehicle Weapons",
      heatDissipation: "Heat dissipation",
      criticalTrack: "Criticals",
      locationFront: "Front effects",
      locationSide: "Side effects",
      locationRear: "Rear effects",
      locationCore: "Core effects",
      crew: {
        label: "Crew",
        placeholder: "Crew names or notes"
      },
      quickActions: {
        title: "Quick Actions",
        rangedAttack: "Ranged Attack",
        meleeAttack: "Melee Attack",
        pilotingCheck: "Piloting Check",
        sensorSweep: "EW",
        emergencyRepair: "Emergency Repair",
        primaryWeapons: "Primary Weapons",
        allWeapons: "All Weapons",
        primaryLabel: "Primary",
        unarmed: "Unarmed (Punch/Kick)",
        unarmedNotes: "Basic unarmed strike.",
        selectWeaponGroup: "Select Weapon Group",
        selectMeleeProfile: "Select Melee Profile",
        selectSensorSkill: "Select Sensor Sweep Skill",
        weaponGroup: "Weapon Group",
        weaponsUsed: "Weapons",
        meleeProfile: "Melee Profile",
        meleeDamage: "Damage",
        skillUsed: "Skill",
        tooltips: {
          ranged: "Roll an attack using any Weapon Group or Primary Weapon",
          melee: "Roll a melee attack using fists, kicks, or installed melee weapons",
          piloting: "Piloting roll for movement, jumping, stability, or hazard checks",
          sensorSweep: "Acquire contact or generate targeting data using the machine sensor suite",
          emergencyRepair: "Choose a crit or repairable status and attempt the matching remedy"
        },
        errors: {
          noRanged: "No weapon groups available for ranged attack.",
          noMelee: "No melee attacks available.",
          noSensorSweep: "EW actions require an eligible targeted token and an available sensor action."
        }
      }
    },
    battlemech: {
      chassis: "Chassis",
      tonnage: "Tonnage",
      heat: {
        thresholds: "Heat thresholds",
        runningHot: "Hot",
        overheated: "Overheat",
        shutdown: "Danger",
        statusLabel: "Current heat state",
        status: {
          safe: "Safe",
          hot: "Hot",
          overheat: "Overheat",
          danger: "Danger",
          runningHot: "Hot",
          overheated: "Overheat",
          shutdown: "Danger"
        }
      },
      hardpoints: {
        title: "Hardpoint summary",
        type: "Type",
        size: "Size",
        location: "Location",
        assigned: "Assigned to",
        none: "No hardpoints configured."
      },
      weaponGroups: {
        title: "Weapon groups",
        mountPoints: "Mount points used: {used} / {total}",
        group: "Group",
        weapons: "Weapons",
        empty: "No weapons assigned to this group.",
        none: "No weapon groups configured.",
        missingWeapon: "Weapon {missingId} is missing from the actor."
      },
      weapons: {
        title: "Battlemech weapons",
        weapon: "Weapon",
        category: "Category",
        mount: "Mount",
        hardpoint: "Hardpoint",
        heat: "Heat",
        none: "No mech weapons equipped.",
        mountUnknown: "Unspecified"
      }
    },
    ownership: {
      owner: "Owner",
      unknown: "Unknown",
      owned: "Owned"
    }
  },
  actorType: {
    character: "Character",
    npc: "NPC",
    vehicle: "Vehicle",
    battlemech: "Battlemech"
  },
  item: {
    sheet: "Sheet for ",
    tabs: {
      main: "Details",
      modifiers: "Modifiers"
    },
    skill: {
      code: "Internal code",
      copyDefault: "Configure skill",
      isKnowledge: "Knowledge",
      attribute: "Attribute",
      value: "Level",
      specialization: "Specialisation",
      specializationHelp: "Type the name to choose a specialization",
      isSocial: "Social skill",
      hasDrain: "Drain",
      hasConvergence: "Convergence"
    },
    quality: {
      positive: "Legacy positive flag",
      category: "Category",
      tier: "Tier",
      activation: "Activation",
      tags: "Tags",
      effects: "Effects",
      prerequisites: "Prerequisites",
      limits: "Limits",
      categoryOptions: {
        positive: "Positive",
        negative: "Negative",
        narrative: "Narrative"
      },
      tierOptions: {
        minor: "Minor",
        major: "Major"
      },
      activationOptions: {
        passive: "Passive",
        triggered: "Triggered"
      }
    },
    assetModule: {
      category: "Category",
      level: "Level",
      levelShort: "Lvl",
      jumping: {
        enabled: "Grants Jumping",
        movement: "Jump Move",
        heat: "Jump Heat",
        attackRatingBonus: "Jump AR Bonus",
        defenseRatingBonus: "Jump DR Bonus",
        dfaEnabled: "Enable Death From Above"
      }
    },
    gear: {
      quantity: "Quantity",
      quantityShort: "Qty",
      rating: "Rating",
      category: "Category",
      tags: "Tags",
      categoryLabels: {
        audiovisual: "Audiovisual Gear",
        communication: "Communication Gear",
        computing: "Computing Gear",
        espionage: "Espionage Gear",
        hostileEnvironment: "Hostile Environment Gear",
        medical: "Medical Gear",
        optical: "Optical Gear",
        power: "Power Gear",
        repairSalvage: "Repair/Salvage Gear",
        survival: "Survival Gear",
        surveillance: "Surveillance Gear"
      }
    },
    consumable: {
      quantity: "Quantity",
      quantityShort: "Qty",
      rating: "Potency",
      category: "Consumable Type",
      tags: "Tags",
      categoryLabels: {
        ammo: "Ammunition",
        explosive: "Explosive",
        medical: "Medical",
        repair: "Repair",
        fuel: "Fuel / Power Cell",
        utility: "Utility"
      }
    },
    lifeModule: {
      moduleType: "Module Type",
      type: {
        faction: "Faction",
        childhood: "Childhood",
        higherEducation: "Higher Education",
        realLife: "Real Life"
      }
    },
    mechWeapon: {
      category: "Weapon Category",
      size: "Size",
      hardpoint: "Hardpoint",
      damage: "Damage Value",
      damageType: "Damage Type",
      heat: "Heat",
      area: "Area of effect",
      range: {
        max: "Maximum range"
      }
    },
    personalWeapon: {
      skill: "Skill",
      category: "Weapon Category",
      damage: "Damage Value",
      ap: "Armor Piercing",
      damageType: "Damage Type",
      damageShort: "DV",
      apShort: "AP",
      weaponWithoutActor: "No Actor",
      equipped: "Equipped",
      primary: "Primary",
      attack: "Attack",
      traits: "Traits",
      notes: "Notes",
      attackRatingBand: {
        label: "Attack Rating Modifiers",
        close: "Close",
        near: "Near",
        far: "Far",
        extreme: "Extreme"
      },
      range: {
        max: "Maximum range"
      },
      withArmor: "Armor protects"
    },
    armor: {
      equipped: "Equipped",
      primary: "Primary",
      rating: "Armor Rating",
      defenseBonus: "Defense Bonus",
      mitigation: "Type Modifiers",
      durability: "Durability",
      tags: "Armor Tags",
      traits: "Traits",
      notes: "Notes"
    }
  },
  itemType: {
    singular: {
      skill: "Skill",
      quality: "Trait",
      assetModule: "Asset Module",
      gear: "Gear",
      consumable: "Consumable",
      contact: "Contact",
      lifeModule: "Life Module",
      mechWeapon: "Mech-Scale Weapon",
      personalWeapon: "Personal Weapon",
      armor: "Armor"
    },
    plural: {
      skill: "Skills",
      quality: "Qualities",
      assetModule: "Asset Modules",
      gear: "Gears",
      consumable: "Consumables",
      contact: "Contacts",
      lifeModule: "Life Modules",
      action: "Actions",
      monitor: "Monitors",
      mechWeapon: "Mech-Scale Weapons",
      personalWeapon: "Personal Weapons",
      armor: "Armor"
    }
  },
  monitor: {
    physical: "Physical",
    fatigue: "Fatigue"
  },
  monitorLetter: {
    physical: "P",
    fatigue: "F"
  },
  assetModuleCategory: {
    faction: "Faction",
    logistics: "Logistics",
    mobility: "Mobility",
    training: "Training",
    influence: "Influence",
    personal: "Personal",
    operations: "Operations",
    special: "Special"
  },
  attributes: {
    strength: "Strength",
    reflexes: "Reflexes",
    willpower: "Guts",
    intelligence: "Intelligence",
    charisma: "Charisma",
    edge: "Edge",
    // Legacy synonyms retained for migration safety:
    agility: "Reflexes",
    logic: "Intelligence",
    knowledge: "Knowledge",
    noAttribute: "No attribute chosen",
    // Legacy vehicle attributes retained (if still referenced somewhere):
    autopilot: "Autopilot",
    handling: "Handling",
    firewall: "Firewall",
    system: "System",
    // MWD vehicle attributes:
    chassis: "Chassis",
    reliability: "Reliability",
    condition: "Condition"
  },
  attributeAction: {
    defense: "Defense",
    judgeIntentions: "Judge intentions",
    perception: "Perception / Mental resistance",
    resistTorture: "Resist torture / Physical resistance",
    composure: "Composure / Social resistance",
    memory: "Memory",
    catch: "Catch object",
    lift: "Lift/carry"
  },
  defense: {
    physicalDefense: "Physical defense",
    physicalResistance: "Physical resistance",
    socialDefense: "Social defense",
    mentalResistance: "Mental resistance"
  },
  skill: {
    athletics: "Athletics",
    heavyWeapons: "Heavy Weapons",
    escapeArtist: "Escape Artist",
    gunnery: "Gunnery",
    meleeCombat: "Melee Combat",
    piloting: "Piloting",
    projectileWeapons: "Projectile Weapons",
    firearms: "Firearms",
    stealth: "Stealth",
    zeroGOps: "Zero-G Operations",
    art: "Art",
    artillery: "Artillery",
    systemOps: "System Operations",
    computers: "Computers",
    demolitions: "Demolitions",
    knowledge: "Knowledge",
    medTech: "MedTech",
    science: "Science",
    perception: "Perception",
    tactics: "Tactics",
    technician: "Technician",
    tracking: "Tracking",
    navigation: "Navigation",
    animalHandling: "Animal Handling",
    survival: "Survival",
    acting: "Acting",
    disguise: "Disguise",
    leadership: "Leadership",
    negotiation: "Negotiation",
    etiquette: "Etiquette",
    streetwise: "Streetwise",
    intimidation: "Intimidation",
    Administration: "Administration"
  },
  area: {
    none: "None",
    shotgun: "Shotgun",
    circle: "Circle",
    cone: "Cone",
    rect: "Rectangle",
    ray: "Ray"
  },
  range: {
    contact: "Contact",
    short: "Short",
    medium: "Medium",
    far: "Far",
    extreme: "Extreme"
  },
  vehicleCategory: {
    drone: "Drone",
    personal: "Personal",
    combat: "Combat",
    aerospace: "Aerospace",
    mech: "Mech"
  },
  mwd: {
    armorMitigationType: {
      penetrating: "Penetrating",
      concussive: "Concussive",
      energy: "Energy",
      thermal: "Thermal",
      electrical: "Electrical"
    },
    weightClass: {
      label: "Weight class",
      light: "Light",
      medium: "Medium",
      heavy: "Heavy",
      assault: "Assault"
    },
    hardpoint: {
      type: {
        penetrating: "Penetrating",
        concussive: "Concussive",
        energy: "Energy",
        thermal: "Thermal",
        electrical: "Electrical",
        support: "Support",
        omni: "Omni"
      },
      size: {
        small: "Small",
        medium: "Medium",
        large: "Large"
      },
      location: {
        head: "Head",
        torso: "Torso",
        arms: "Arms",
        turret: "Turret"
      }
    },
    hardpointType: {
      penetrating: "Penetrating",
      concussive: "Concussive",
      energy: "Energy",
      thermal: "Thermal",
      electrical: "Electrical",
      support: "Support",
      omni: "Omni"
    },
    hardpointSize: {
      small: "Small",
      medium: "Medium",
      large: "Large"
    },
    hardpointLocation: {
      head: "Head",
      torso: "Torso",
      arms: "Arms",
      turret: "Turret"
    },
    primarySlot: {
      mode: {
        normal: "Large hardpoint",
        converted: "Converted slot"
      }
    },
    weaponCategory: {
      ranged: "Ranged",
      melee: "Melee"
    },
    melee: {
      title: "Melee options",
      baseProfile: "Unarmed",
      baseProfileLabel: "Base melee profile",
      damagePlaceholder: "Damage code",
      notesPlaceholder: "Notes",
      maxWeapons: "Maximum equipped melee weapons",
      allowedLocations: "Allowed locations",
      availableProfiles: "Available melee profiles",
      location: {
        head: "Head",
        torso: "Torso",
        arm: "Arm",
        leg: "Leg"
      },
      locationAny: "Any location"
    },
    loadout: {
      title: "Weapon loadout",
      mountPoints: "Mount points used",
      primarySlot: {
        label: "Primary weapon slot",
        noRestriction: "No type restriction",
        allowedWeapons: "Allowed primary weapons"
      },
      hardpoints: "Hardpoints",
      weaponGroups: "Weapon groups",
      primaryTag: "Primary",
      occupied: "{{weaponGroup}} assigned",
      emptyHardpoint: "Empty",
      errors: {
        label: "Errors",
        multiplePrimary: "Only one primary weapon group is allowed.",
        mountPointsExceeded: "Loadout uses {used} mount points but only {total} are available.",
        hardpointUnavailable: "No matching hardpoint for {weapon} ({type}, {size}).",
        primaryNeedsLarge: "{weapon} must use a large hardpoint to be primary.",
        primaryWithoutWeapon: "Primary group needs at least one weapon.",
        weaponAlreadyGrouped: "{weapon} is already assigned to another group.",
        primaryNotAllowedWeapon: "{weapon} is not allowed in the converted primary slot.",
        primaryTypeRestriction: "{weapon} does not match the converted primary slot restriction ({type}).",
        meleeLimitExceeded: "Equipped melee weapons {equipped} exceed limit {limit}.",
        meleeLocationRestricted: "{weapon} cannot be mounted at that location."
      },
      warnings: {
        label: "Warnings",
        weaponMissing: "Weapon with id {weapon} is missing."
      },
      newGroup: "New weapon group"
    },
    weapon: {
      damageType: {
        energy: "Energy",
        kinetic: "Kinetic",
        ballistic: "Ballistic",
        explosive: "Explosive",
        plasma: "Plasma",
        electrical: "Electrical",
        melee: "Melee",
        none: "None"
      }
    },
    personalWeapon: {
      damageType: {
        penetrating: "Penetrating",
        concussive: "Concussive",
        energy: "Energy",
        thermal: "Thermal",
        electrical: "Electrical"
      }
    }
  },
  modifier: {
    column: {
      group: "Group",
      effect: "Effect",
      value: "Value",
      category: "",
      subCategory: "",
      condition: "When"
    },
    group: {
      roll: "Roll",
      attribute: "Attribute",
      monitor: "Monitor",
      other: "Other"
    },
    roll: {
      effect: {
        pool: "Pool bonus",
        reroll: "Rerolls",
        rerollMax: "Reroll allowance cap",
        glitch: "Glitch dice",
        successReroll: "Reroll own successes",
        opponentPool: "Opponent pool malus",
        opponentReroll: "Opponent rerolls"
      },
      category: {
        attribute: "Attribute",
        skill: "Skill",
        attributeAction: "Attribute action"
      }
    },
    monitor: {
      effect: {
        armor: "Armor",
        structure: "Structure",
        fatigue: "Fatigue",
        physical: "Physical"
      },
      category: {
        max: "Increased max",
        resistance: "Resistance",
        resistanceByType: "Damage-type resistance"
      }
    },
    other: {
      effect: {
        ignoreWounds: "Ignore wounds",
        damageArmor: "Damage to armor",
        initiative: "Initiative bonus",
        celebrity: "Adjust legend"
      },
      category: {}
    },
    condition: {
      always: "Always"
    }
  }
}, N = q, M = "mwd", fy = "MechWarrior: Destiny", Al = `system.${M}`, py = M, Fr = `systems/${M}`, af = `${Fr}/style`, sr = `${Fr}/third-party/style`, se = `systems/${M}/templates`, to = `${Fr}/img/icons`, Ae = `${to}/skills`, Le = "MWD | ", rf = "enableBattlemechTokenHeatFx", gy = 2, hy = 5, yy = 4, sf = 8, bn = {
  reflexes: "reflexes",
  strength: "strength",
  willpower: "willpower",
  intelligence: "intelligence",
  charisma: "charisma",
  edge: "edge",
  handling: "handling",
  system: "system",
  chassis: "chassis",
  reliability: "reliability",
  condition: "condition"
}, wl = {
  autopilot: "autopilot",
  firewall: "firewall",
  knowledge: "knowledge"
}, ei = {
  grit: "grit",
  chaos: "chaos",
  insight: "insight",
  rumor: "rumor",
  legend: "legend",
  credibility: "credibility"
}, Hc = {
  physical: [ei.grit, ei.chaos],
  mental: [ei.insight, ei.rumor],
  social: [ei.legend, ei.credibility]
}, A = {
  actorTypes: {
    character: "character",
    npc: "npc",
    vehicle: "vehicle",
    battlemech: "battlemech"
  },
  itemType: {
    skill: "skill",
    quality: "quality",
    assetModule: "assetModule",
    mechWeapon: "mechWeapon",
    personalWeapon: "personalWeapon",
    armor: "armor",
    gear: "gear",
    consumable: "consumable",
    contact: "contact",
    lifeModule: "lifeModule"
  },
  actorAttributes: bn,
  itemAttributes: wl,
  attributes: { ...bn, ...wl },
  monitors: {
    fatigue: "fatigue",
    armor: "armor",
    physical: "physical",
    structure: "structure",
    heat: "heat"
  },
  counters: {
    xp: "xp",
    xpTotal: "xpTotal",
    xpUnused: "xpUnused",
    edge: "edge",
    edgePools: ei,
    edgePoolGroups: Hc,
    physical: {
      grit: ei.grit,
      chaos: ei.chaos
    },
    mental: {
      insight: ei.insight,
      rumor: ei.rumor
    },
    social: {
      legend: ei.legend,
      credibility: ei.credibility
    },
    chaos: ei.chaos
  },
  area: {
    none: "none",
    shotgun: "shotgun",
    circle: "circle",
    cone: "cone",
    rect: "rect",
    ray: "ray"
  }
}, by = Object.freeze({
  physical: "physical",
  mental: "mental",
  social: "social"
});
Object.freeze(Object.values(by));
function oe(i = "") {
  return String(i ?? "").replace(/([a-z0-9])([A-Z])/g, "$1 $2").replace(/[-_]+/g, " ").replace(/\s+/g, " ").trim().replace(/\b\w/g, (e) => e.toUpperCase());
}
const _a = {
  [A.actorTypes.character]: [
    A.actorAttributes.strength,
    A.actorAttributes.reflexes,
    A.actorAttributes.willpower,
    A.actorAttributes.intelligence,
    A.actorAttributes.charisma,
    A.actorAttributes.edge
  ],
  [A.actorTypes.npc]: [
    A.actorAttributes.strength,
    A.actorAttributes.reflexes,
    A.actorAttributes.willpower,
    A.actorAttributes.intelligence,
    A.actorAttributes.charisma,
    A.actorAttributes.edge
  ],
  [A.actorTypes.vehicle]: [
    A.actorAttributes.handling,
    A.actorAttributes.system,
    A.actorAttributes.chassis,
    A.actorAttributes.reliability
  ],
  [A.actorTypes.battlemech]: [
    A.actorAttributes.handling,
    A.actorAttributes.system,
    A.actorAttributes.chassis,
    A.actorAttributes.reliability
  ]
}, xo = {
  character: {
    physical: {
      status: { label: "Penalty", path: "derived.penalty" },
      derived: {
        penalty: { fn: "penaltyPer3Damage", source: "value" }
      }
    },
    fatigue: {
      status: { label: "Penalty", path: "derived.penalty" },
      derived: {
        penalty: { fn: "penaltyPer3Damage", source: "value" }
      }
    },
    armor: {
      status: { label: "Resist", path: "derived.resistance" },
      derived: {
        resistance: { fn: "resistancePerQuarter", source: "armorPersonalBase" }
      }
    }
  },
  battlemech: {
    armor: {
      status: { label: "Resist", path: "derived.resistance" },
      derived: {
        resistance: { fn: "resistancePerQuarter", source: "mechArmorBase" }
      }
    },
    structure: {
      status: null
    },
    heat: {
      status: { label: "Penalty", path: "derived.penalty" },
      derived: {
        penalty: { fn: "penaltyPer3Damage", source: "value" }
      }
    }
  },
  vehicle: {
    armor: {
      status: { label: "Resist", path: "derived.resistance" },
      derived: {
        resistance: { fn: "resistancePerQuarter", source: "vehicleArmorBase" }
      }
    },
    durability: {
      status: { label: "Penalty", path: "derived.penalty" },
      derived: {
        penalty: { fn: "penaltyPer3Damage", source: "value" }
      }
    }
  }
}, jt = {
  rollType: {
    attributeAction: "attributeAction",
    defense: "defense",
    attribute: "attribute",
    skill: "skill",
    weapon: "weapon"
  },
  actions: {
    defense: "defense",
    resistTorture: "resistTorture",
    judgeIntentions: "judgeIntentions",
    perception: "perception",
    composure: "composure",
    memory: "memory",
    catch: "catch",
    lift: "lift"
  },
  defenses: {
    physicalDefense: "physicalDefense",
    physicalResistance: "physicalResistance",
    socialDefense: "socialDefense",
    mentalResistance: "mentalResistance"
  },
  fixedDefenseCode: {
    // fix for old incorrect defense codes
    mentalDefense: "physicalResistance"
  }
};
globalThis.ANARCHY_CONSTANTS = {
  SYSTEM_NAME: M,
  SYSTEM_DESCRIPTION: fy,
  SYSTEM_SOCKET: Al,
  SYSTEM_SCOPE: py,
  SYSTEM_PATH: Fr,
  STYLE_PATH: af,
  THIRD_PARTY_STYLE_PATH: sr,
  TEMPLATES_PATH: se,
  ICONS_PATH: to,
  ICONS_SKILLS_PATH: Ae,
  LOG_HEAD: Le,
  SPECIALIZATION_BONUS: gy,
  TARGET_SUCCESS: hy,
  TARGET_SUCCESS_EDGE: yy,
  BASE_MONITOR: sf,
  ACTOR_ATTRIBUTES: bn,
  ITEM_ATTRIBUTES: wl,
  EDGE_POOL_GROUPS: Hc,
  TEMPLATE: A,
  ANARCHY_SYSTEM: jt
};
const xi = class xi {
  static ascending(e = (t) => t) {
    return (t, n) => xi.sortingBy(e(t), e(n));
  }
  static descending(e = (t) => t) {
    return (t, n) => xi.sortingBy(e(n), e(t));
  }
  static sortingBy(e, t) {
    return e > t ? 1 : e < t ? -1 : 0;
  }
  static bySortedArray(e) {
    return (t) => e.indexOf(t);
  }
  static ascendingBySortedArray(e) {
    return xi.ascending(xi.bySortedArray(e));
  }
  static sortedMap(e, t = (n, a) => 0) {
    return Object.keys(e).sort(t).reduce(
      (n, a) => (n[a] = e[a], n),
      {}
    );
  }
  static reindexIds(e) {
    let t = 1;
    return e.forEach((n) => n.id = t++), e;
  }
  static distinct(e) {
    return [...new Set(e)];
  }
  static sum() {
    return (e, t) => e + t;
  }
  static sumValues(e, t = (n) => n) {
    return e.map(t).filter((n) => n != null).reduce(xi.sum(), 0);
  }
  static divint(e, t) {
    return Math.floor(e / t);
  }
  static divup(e, t) {
    return Math.ceil(e / t);
  }
  static join(e, t = "") {
    return e.reduce(xi.joiner(t));
  }
  static joiner(e = "") {
    return (t, n) => t + e + n;
  }
  static classify(e, t = (n) => n.type) {
    let n = {};
    return xi.classifyInto(n, e, t), n;
  }
  static classifyFirst(e, t) {
    let n = {};
    for (const a of e) {
      const r = t(a);
      n[r] || (n[r] = a);
    }
    return n;
  }
  static classifyInto(e, t, n = (a) => a.type) {
    for (const a of t) {
      const r = n(a);
      let s = e[r];
      s || (s = [], e[r] = s), s.push(a);
    }
  }
  static showControlWhen(e, t) {
    t ? e.show() : e.hide();
  }
  static minmax(e, t, n) {
    return Math.max(t, Math.min(e, n));
  }
};
z(xi, "isString", (e) => typeof e == "string" || e instanceof String);
let be = xi;
const Sy = {
  keyword: "keywords",
  disposition: "dispositions",
  cue: "cues"
}, j = class j {
  /**
   * Initialize enum caches.
   * Call once during system init/setup after config/constants are available.
   */
  static init() {
    var t, n, a, r, s, o, l, c, u, d, m, f;
    j.hbsAttributes = j.mapObjectToKeyValue(N.attributes).filter((p) => p.value !== "knowledge" && p.value !== "noAttribute"), j.hbsItemTypes = j.mapObjectToKeyValue(N.itemType), j.hbsMonitors = j.mapObjectToKeyValue(N.monitor), j.hbsMonitorLetters = j.mapObjectToKeyValue(N.monitorLetter), j.hbsAssetModuleCategories = j.mapObjectToKeyValue(N.assetModuleCategory), (n = (t = N.item) == null ? void 0 : t.lifeModule) != null && n.type ? j.hbsLifeModuleTypes = j.mapObjectToKeyValue(N.item.lifeModule.type) : (console.warn("MWD | MWD.item.lifeModule.type is missing; life module enums disabled."), j.hbsLifeModuleTypes = []), j.hbsAreas = j.mapObjectToKeyValue(N.area), j.hbsRanges = j.mapObjectToKeyValue(N.range), j.hbsVehicleCategories = j.mapObjectToKeyValue(N.vehicleCategory), j.hbsMwdWeightClasses = j.mapObjectToKeyValue((a = N.mwd) == null ? void 0 : a.weightClass), j.hbsMwdHardpointTypes = j.mapObjectToKeyValue((r = N.mwd) == null ? void 0 : r.hardpointType), j.hbsMwdHardpointSizes = j.mapObjectToKeyValue((s = N.mwd) == null ? void 0 : s.hardpointSize), j.hbsMwdHardpointLocations = j.mapObjectToKeyValue((o = N.mwd) == null ? void 0 : o.hardpointLocation), j.hbsMwdPrimaryModes = j.mapObjectToKeyValue((l = N.mwd) == null ? void 0 : l.primarySlotMode), j.hbsMwdWeaponCategories = j.mapObjectToKeyValue((c = N.mwd) == null ? void 0 : c.weaponCategory), j.hbsMwdWeaponDamageTypes = j.mapObjectToKeyValue((u = N.mwd) == null ? void 0 : u.weaponDamageType), j.hbsPersonalWeaponDamageTypes = j.mapObjectToKeyValue((d = N.mwd) == null ? void 0 : d.personalDamageType), j.hbsPersonalWeaponDamageCategories = j.mapObjectToKeyValue((m = N.mwd) == null ? void 0 : m.personalDamageCategory), j.hbsMwdMeleeLocations = j.mapObjectToKeyValue((f = N.mwd) == null ? void 0 : f.meleeLocation), j.hbsDamageTypes = be.distinct(
      (j.hbsMwdWeaponDamageTypes ?? []).concat(j.hbsPersonalWeaponDamageTypes ?? []),
      (p) => p.value
    );
    const e = Object.values(_a).flat();
    j.sortedAttributeKeys = be.distinct(
      e.concat(Object.keys(N.attributes ?? {}))
    ), j.registerHandleBarHelpers(), j.ENUMS = j.getEnums();
  }
  static registerHandleBarHelpers() {
    typeof Handlebars > "u" || Handlebars.registerHelper("sortedAttributes", (e) => {
      if (!e || typeof e != "object") return [];
      const t = Object.keys(e), n = j.sortedAttributeKeys ?? [], a = new Map(n.map((r, s) => [r, s]));
      return t.sort((r, s) => {
        const o = a.has(r) ? a.get(r) : 9999, l = a.has(s) ? a.get(s) : 9999;
        return o !== l ? o - l : String(r).localeCompare(String(s));
      }), t.map((r) => {
        const s = e[r];
        return s && typeof s == "object" ? { key: r, ...s } : { key: r, value: s };
      });
    });
  }
  static getDamageTypes() {
    return j.hbsDamageTypes ?? [];
  }
  static getAttributes(e = () => !0) {
    return (j.hbsAttributes ?? []).filter((t) => e(t.value));
  }
  static getActorWordTypes() {
    return Sy;
  }
  static getMonitors() {
    return j.hbsMonitors ?? [];
  }
  /**
   * Returns a big blob of enums suitable for sheet contexts.
   * `withKnowledge` is passed through to the skills service.
   */
  static getEnums(e = () => !0, t = !1) {
    return {
      attributes: j.getAttributes(e),
      itemTypes: j.hbsItemTypes ?? [],
      monitors: j.hbsMonitors ?? [],
      monitorLetters: j.hbsMonitorLetters ?? [],
      assetModuleCategories: j.hbsAssetModuleCategories ?? [],
      lifeModuleTypes: j.hbsLifeModuleTypes ?? [],
      areas: j.hbsAreas ?? [],
      ranges: j.hbsRanges ?? [],
      vehicleCategories: j.hbsVehicleCategories ?? [],
      // Skills: now sourced from MWD namespace (with safe fallback)
      skills: j.getSkillsEnum({ withKnowledge: t }),
      // MWD enums
      mwdWeightClasses: j.hbsMwdWeightClasses ?? [],
      mwdHardpointTypes: j.hbsMwdHardpointTypes ?? [],
      mwdHardpointSizes: j.hbsMwdHardpointSizes ?? [],
      mwdHardpointLocations: j.hbsMwdHardpointLocations ?? [],
      mwdPrimaryModes: j.hbsMwdPrimaryModes ?? [],
      mwdWeaponCategories: j.hbsMwdWeaponCategories ?? [],
      mwdWeaponDamageTypes: j.hbsMwdWeaponDamageTypes ?? [],
      personalWeaponDamageTypes: j.hbsPersonalWeaponDamageTypes ?? [],
      personalWeaponDamageCategories: j.hbsPersonalWeaponDamageCategories ?? [],
      damageTypes: j.hbsDamageTypes ?? [],
      mwdMeleeLocations: j.hbsMwdMeleeLocations ?? []
    };
  }
  static getSkillsEnum({ withKnowledge: e = !1 } = {}) {
    var a, r, s, o, l;
    const t = ((r = (a = game == null ? void 0 : game.system) == null ? void 0 : a.mwd) == null ? void 0 : r.skills) ?? ((o = (s = game == null ? void 0 : game.system) == null ? void 0 : s.anarchy) == null ? void 0 : o.skills);
    return (((l = t == null ? void 0 : t.getSkills) == null ? void 0 : l.call(t, { withKnowledge: e })) ?? []).map((c) => ({
      value: c.code,
      label: c.label ?? c.code
    }));
  }
  /**
   * Convert an object map into an array like [{ value, label }, ...]
   * Accepts:
   * - { key: "Label" }
   * - { key: { label: "Label" } }
   */
  static mapObjectToKeyValue(e, t = "value", n = "label") {
    return !e || typeof e != "object" ? [] : Object.keys(e).map((a) => {
      const r = e[a];
      let s;
      return r && typeof r == "object" ? s = r.label ?? r.name ?? r.value ?? String(a) : r != null ? s = String(r) : s = String(a), {
        [t]: a,
        [n]: s
      };
    });
  }
  // Backwards-compat alias (the legacy file misspelled this)
  static mapObjetToKeyValue(e, t = "value", n = "label") {
    return j.mapObjectToKeyValue(e, t, n);
  }
};
z(j, "ENUMS"), // HBS-friendly arrays of { value, label } (or key/value depending on caller)
z(j, "hbsAttributes"), z(j, "hbsItemTypes"), z(j, "hbsMonitors"), z(j, "hbsMonitorLetters"), z(j, "hbsAssetModuleCategories"), z(j, "hbsLifeModuleTypes"), z(j, "hbsAreas"), z(j, "hbsRanges"), z(j, "hbsVehicleCategories"), // MWD-specific enum groups
z(j, "hbsMwdWeightClasses"), z(j, "hbsMwdHardpointTypes"), z(j, "hbsMwdHardpointSizes"), z(j, "hbsMwdHardpointLocations"), z(j, "hbsMwdPrimaryModes"), z(j, "hbsMwdWeaponCategories"), z(j, "hbsMwdWeaponDamageTypes"), z(j, "hbsPersonalWeaponDamageTypes"), z(j, "hbsPersonalWeaponDamageCategories"), z(j, "hbsDamageTypes"), z(j, "hbsMwdMeleeLocations"), z(j, "sortedAttributeKeys");
let Oe = j;
class Ay {
  static monitor(e) {
    return Oe.getFromList(Oe.getMonitors(), e) ?? "";
  }
  static letter(e) {
    return Oe.getFromList(Oe.getMonitorLetters(), e) ?? "";
  }
}
class wy {
  static toLowerCaseNoAccent(e) {
    return e == null ? void 0 : e.normalize("NFD").toLowerCase().replace(/[\u0300-\u036f]/g, "");
  }
  static toUpperCaseNoAccent(e) {
    return e == null ? void 0 : e.normalize("NFD").toUpperCase().replace(/[\u0300-\u036f]/g, "");
  }
}
const Ty = [
  "fas fa-dice",
  "fas fa-dice-one",
  "fas fa-dice-two",
  "fas fa-dice-three",
  "fas fa-dice-four",
  "fas fa-dice-five",
  "fas fa-dice-six"
];
class re {
  static fontAwesome(e) {
    return `<i class="${e}"></i>`;
  }
  static iconSystemPath(e, t) {
    return re.iconPath(`${af}/${e}`, t);
  }
  static iconPath(e, t) {
    return `<img class="${t}" src="${e}" />`;
  }
  static iconD6(e) {
    if (e < 0 || e > 6)
      throw `Dice ${e} is out of dice range [1..6] or 0 for multidice`;
    return re.fontAwesome(Ty[e]);
  }
}
globalThis.ANARCHY_ICONS = re;
const Ke = (i, e = {}) => i.replace(/\{(.*?)\}/g, (t, n) => e[n] ?? "");
function Uc(i) {
  return i && typeof i == "object" && !Array.isArray(i) ? Object.values(i).flatMap((e) => Uc(e)) : Array.isArray(i) ? i.map((e) => String(e ?? "").trim()).filter(Boolean) : String(i ?? "").split(",").map((e) => e.trim()).filter(Boolean);
}
function wr(i = []) {
  return Array.from(new Set(i.map((e) => String(e ?? "").trim()).filter(Boolean)));
}
function os(i, e = "standard") {
  return String(i ?? "").trim() || e;
}
function ky(i, e = {}) {
  i && (i.movedToKeywords ?? (i.movedToKeywords = []), i.movedToKeywords.push(e));
}
function My(i, e, t = {}) {
  i && (i.errors ?? (i.errors = []), i.errors.push({ message: e, ...t }));
}
const jc = Object.freeze(["templated"]), Cy = Object.freeze([
  "burstCapable",
  "fullAutoCapable",
  "salvoCapable"
]), vy = Object.freeze([
  "clustered",
  "mineLayer",
  "smoke",
  "incendiary",
  "emp"
]), Ey = Object.freeze([
  { value: "burstCapable", label: "Burst Capable" },
  { value: "fullAutoCapable", label: "Full Auto Capable" },
  { value: "salvoCapable", label: "Salvo Capable" }
]), Iy = Object.freeze([
  { value: "templated", label: "Templated" },
  { value: "clustered", label: "Clustered" },
  { value: "mineLayer", label: "Mine Layer" },
  { value: "smoke", label: "Smoke" },
  { value: "incendiary", label: "Incendiary" },
  { value: "emp", label: "EMP" }
]), of = Object.freeze([
  { value: "blast", label: "Blast" },
  { value: "cone", label: "Cone" },
  { value: "line", label: "Line" },
  { value: "cloud", label: "Cloud" },
  { value: "minefield", label: "Minefield" }
]), lf = Object.freeze([
  { value: "targeted", label: "Targeted (start on target)" },
  { value: "origin", label: "Origin (start on attacker)" },
  { value: "placed", label: "Placed (start free)" }
]), Ny = Object.freeze(["blast", "cone", "line"]);
new Set(jc);
const Ry = /* @__PURE__ */ new Set([
  ...jc,
  ...Cy
]), Py = /* @__PURE__ */ new Set([
  ...jc,
  ...vy
]);
function Kc() {
  return {
    movedToKeywords: [],
    errors: []
  };
}
function Ps(i) {
  return wr(Uc(i));
}
function cf({
  traits: i = [],
  keywords: e = [],
  recognized: t = /* @__PURE__ */ new Set(),
  report: n = null,
  owner: a = "weapon",
  path: r = ""
} = {}) {
  const s = Uc(i), o = Ps(e), l = [], c = [...o];
  for (const u of s) {
    if (t.has(u)) {
      l.push(u);
      continue;
    }
    c.push(u), ky(n, {
      owner: a,
      from: r || "traits",
      to: r ? r.replace(/traits$/u, "keywords") : "keywords",
      value: u
    });
  }
  return {
    traits: wr(l),
    keywords: wr(c)
  };
}
function uf({
  traits: i = [],
  keywords: e = [],
  report: t = null,
  path: n = "system.traits"
} = {}) {
  return cf({
    traits: i,
    keywords: e,
    recognized: Ry,
    report: t,
    owner: "weapon",
    path: n
  });
}
function df({
  traits: i = [],
  keywords: e = [],
  report: t = null,
  path: n = "system.payloads[].traits"
} = {}) {
  return cf({
    traits: i,
    keywords: e,
    recognized: Py,
    report: t,
    owner: "payload",
    path: n
  });
}
function mf(i = {}, e = "standard") {
  const t = i ?? {}, n = os(
    t.resolverKey ?? t.damageModel ?? t.resolver,
    e
  ), a = String(t.damageModel ?? "").trim(), r = t.onHitEffect;
  return {
    resolverKey: n,
    damageModel: a,
    onHitEffect: r === null ? null : String(r ?? "").trim() || null
  };
}
function $o(i = {}) {
  const e = i ?? {}, t = !!e.enabled, n = e.shots, a = e.accuracyMod, r = e.addHeat, s = e.consumption;
  return {
    enabled: t,
    ...n !== void 0 ? { shots: Math.max(0, Number(n ?? 0) || 0) } : {},
    ...a !== void 0 ? { accuracyMod: Number(a ?? 0) || 0 } : {},
    ...r !== void 0 ? { addHeat: Number(r ?? 0) || 0 } : {},
    ...s !== void 0 ? { consumption: Math.max(0, Number(s ?? 0) || 0) } : {}
  };
}
function Dy(i = {}) {
  const e = i ?? {};
  return {
    single: $o(e.single),
    burst: $o(e.burst),
    fullAuto: $o(e.fullAuto)
  };
}
function Oy(i) {
  const e = String(i ?? "").trim().toLowerCase();
  return e === "circle" ? "blast" : e === "ray" ? "line" : of.some((t) => t.value === e) ? e : "";
}
function Ly(i) {
  const e = String(i ?? "").trim().toLowerCase();
  return ["target", "targeted"].includes(e) ? "targeted" : lf.some((t) => t.value === e) ? e : "";
}
function _y(i = null) {
  const e = i ?? {}, t = Oy(e.shape ?? e.t ?? e.type), n = e.size ?? e.distance ?? e.radius ?? e.length, a = Ly(e.placement ?? e.origin ?? e.mode);
  return !t && n === void 0 && !a ? null : {
    shape: t,
    size: Math.max(0, Number(n ?? 0) || 0),
    placement: a || "targeted"
  };
}
function xy({
  weapon: i = {},
  payload: e = {},
  effectiveTraits: t = [],
  effectiveResolution: n = null,
  report: a = null
} = {}) {
  var h, y;
  const r = wr((i == null ? void 0 : i.traits) ?? []), s = wr((e == null ? void 0 : e.traits) ?? []), o = t.includes("templated"), l = r.includes("templated"), c = s.includes("templated"), u = (e == null ? void 0 : e.template) ?? null, d = (i == null ? void 0 : i.template) ?? null, m = os((h = e == null ? void 0 : e.resolution) == null ? void 0 : h.resolverKey, ""), f = os((y = i == null ? void 0 : i.resolution) == null ? void 0 : y.resolverKey, ""), p = os(n == null ? void 0 : n.resolverKey, "standard"), g = [];
  if (!o)
    return {
      errors: g,
      liveCapabilities: [],
      template: null,
      resolverKey: p,
      isTemplated: !1
    };
  l && g.push("Weapon-authored templated attacks are not supported in personal weapon capability v1."), l && c && g.push("Templated capability cannot be authored on both weapon and payload."), d && g.push("Template configuration must be authored on the payload for templated attacks."), c || g.push("Templated attacks require the active payload to author the templated capability."), (!(u != null && u.shape) || !(Number(u == null ? void 0 : u.size) > 0)) && g.push("Templated payloads require a valid template shape and size."), u != null && u.placement || g.push("Templated payloads require a template placement mode."), p !== "template" && g.push("Templated attacks require resolution.resolverKey to be template."), m && m !== "template" && g.push("Payload templated attacks must author resolution.resolverKey as template."), f === "template" && g.push("Weapon-level template resolver routing is not supported for personal weapon capability v1.");
  for (const b of g)
    My(a, b, { capability: "templated" });
  return {
    errors: g,
    liveCapabilities: ["templated"],
    template: u,
    resolverKey: p,
    isTemplated: !0
  };
}
const Tr = Object.freeze(["none", "minor", "major", "full"]), $y = Object.freeze(["blast", "cone", "line", "rect"]), By = Object.freeze({
  blast: "circle",
  cone: "cone",
  line: "ray",
  rect: "rect"
}), zy = Object.freeze({
  circle: "blast",
  cone: "cone",
  ray: "line",
  rect: "rect",
  rectangle: "rect"
}), pe = Object.freeze({
  none: "none",
  minor: "minor",
  major: "major",
  full: "full"
}), Fy = Object.freeze({
  none: 0,
  minor: 0.25,
  major: 0.5,
  full: 1
}), ai = Object.freeze({
  discrete: "discrete",
  persistent: "persistent"
});
function W(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? t : e;
}
function Hy(i, e = !1) {
  if (typeof i == "boolean") return i;
  const t = String(i ?? "").trim().toLowerCase();
  return ["true", "1", "yes", "y", "on"].includes(t) ? !0 : ["false", "0", "no", "n", "off"].includes(t) ? !1 : e;
}
function ff(i) {
  return foundry.utils.deepClone(i);
}
function lt(i, e = pe.none) {
  const t = String(i ?? "").trim().toLowerCase();
  return Tr.includes(t) ? t : e;
}
function Tl(i) {
  return Number(Fy[lt(i)] ?? 0) || 0;
}
function Cn(i) {
  return Tr.indexOf(lt(i));
}
function kl(i, e = 1) {
  const t = Math.max(0, Cn(i)), n = Math.max(0, t - Math.max(0, Math.trunc(W(e, 1))));
  return Tr[n] ?? pe.none;
}
function Uy(i, e = 1) {
  const t = Math.max(0, Cn(i)), n = Math.min(Tr.length - 1, t + Math.max(0, Math.trunc(W(e, 1))));
  return Tr[n] ?? pe.full;
}
function bi(i) {
  return lt(i).toUpperCase();
}
function Gc(i = {}) {
  var a, r, s, o, l;
  const e = i ?? {}, t = Math.max(1, Math.trunc(W(
    ((a = e == null ? void 0 : e.escalation) == null ? void 0 : a.intervalTurns) ?? ((r = e == null ? void 0 : e.escalation) == null ? void 0 : r.interval) ?? 1,
    1
  ))), n = Math.max(0, Math.trunc(W(((s = e == null ? void 0 : e.escalation) == null ? void 0 : s.rate) ?? 1, 1)));
  return {
    startExposure: lt(e.startExposure, pe.minor),
    escalation: {
      rate: n,
      intervalTurns: t,
      max: lt((o = e == null ? void 0 : e.escalation) == null ? void 0 : o.max, pe.full)
    },
    onFull: {
      burnDelta: Math.max(0, Math.trunc(W(((l = e == null ? void 0 : e.onFull) == null ? void 0 : l.burnDelta) ?? 0, 0)))
    },
    clearOnExit: Hy(e.clearOnExit, !0)
  };
}
function on(i = {}) {
  const e = i ?? {}, t = String(e.kind ?? ai.discrete).trim().toLowerCase() === ai.persistent ? ai.persistent : ai.discrete;
  return {
    kind: t,
    hazard: t === ai.persistent ? Gc(e.hazard ?? e) : null
  };
}
function pf(i = {}) {
  return on(i).kind === ai.persistent;
}
function Zn(i, e) {
  return Math.max(0, Math.ceil(W(i, 0) * Tl(e)));
}
function io(i, e = "") {
  const t = String(i ?? "").trim().toLowerCase();
  return $y.includes(t) ? t : e;
}
function gf(i, e = "circle") {
  return By[io(i)] ?? e;
}
function jy(i, e = "") {
  const t = String(i ?? "").trim().toLowerCase();
  return zy[t] ?? e;
}
function Wc(i) {
  let e = W(i, 0);
  for (; e < 0; ) e += 360;
  for (; e >= 360; ) e -= 360;
  return e;
}
function ln() {
  var i, e, t;
  return W(((e = (i = canvas == null ? void 0 : canvas.scene) == null ? void 0 : i.grid) == null ? void 0 : e.distance) ?? ((t = canvas == null ? void 0 : canvas.dimensions) == null ? void 0 : t.distance), 1) || 1;
}
function Ds() {
  var i, e;
  return W(((i = canvas == null ? void 0 : canvas.grid) == null ? void 0 : i.size) ?? ((e = canvas == null ? void 0 : canvas.dimensions) == null ? void 0 : e.size), 100) || 100;
}
function Qt(i = 0) {
  return W(i, 0) * (Ds() / ln());
}
function Ja(i = 0) {
  return W(i, 0) * (ln() / Ds());
}
function Ml(i = {}, e = {}) {
  return Math.hypot(W(i.x, 0) - W(e.x, 0), W(i.y, 0) - W(e.y, 0));
}
function Hr(i) {
  return W(i, 0) * Math.PI / 180;
}
function Ky({ geometry: i = {}, tokenCenter: e = {} } = {}) {
  const t = W(e.x, 0) - W(i.x, 0), n = W(e.y, 0) - W(i.y, 0), a = Hr(i.direction ?? 0), r = Math.cos(a), s = Math.sin(a);
  return Math.max(0, t * r + n * s);
}
function Bo(i = 0, e = 0) {
  if (!(e > 0)) return pe.none;
  const t = Math.max(0, Math.min(1, i / e));
  return t <= 1 / 3 ? pe.full : t <= 2 / 3 ? pe.major : t <= 1 ? pe.minor : pe.none;
}
function td({ template: i = {}, placement: e = {} } = {}) {
  var l, c;
  const t = io((e == null ? void 0 : e.shape) ?? (i == null ? void 0 : i.shape), "");
  if (!t) return null;
  const n = W(
    (e == null ? void 0 : e.distance) ?? (i == null ? void 0 : i.distance) ?? (i == null ? void 0 : i.size),
    0
  );
  if (!(n > 0)) return null;
  const a = t === "cone" ? W((e == null ? void 0 : e.angle) ?? 90, 90) : null, r = t === "line" ? W((e == null ? void 0 : e.width) ?? ln(), ln()) : null, s = t === "rect" ? W((e == null ? void 0 : e.width) ?? (i == null ? void 0 : i.width) ?? (e == null ? void 0 : e.distance) ?? (i == null ? void 0 : i.distance) ?? (i == null ? void 0 : i.size), 0) : null, o = t === "rect" ? W((e == null ? void 0 : e.height) ?? (i == null ? void 0 : i.height) ?? (e == null ? void 0 : e.distance) ?? (i == null ? void 0 : i.distance) ?? (i == null ? void 0 : i.size), 0) : null;
  return t === "rect" && (!(s > 0) || !(o > 0)) ? null : {
    shape: t,
    measuredTemplateType: gf(t),
    x: W((l = e == null ? void 0 : e.anchor) == null ? void 0 : l.x, 0),
    y: W((c = e == null ? void 0 : e.anchor) == null ? void 0 : c.y, 0),
    direction: Wc((e == null ? void 0 : e.direction) ?? 0),
    distance: t === "rect" ? Math.max(s, o) : n,
    angle: a,
    width: r,
    height: t === "rect" ? o : null,
    anchorX: t === "rect" ? W((e == null ? void 0 : e.anchorX) ?? (i == null ? void 0 : i.anchorX) ?? 0, 0) : null,
    anchorY: t === "rect" ? W((e == null ? void 0 : e.anchorY) ?? (i == null ? void 0 : i.anchorY) ?? 0, 0) : null,
    placementMode: String((i == null ? void 0 : i.placement) ?? (e == null ? void 0 : e.placementMode) ?? "").trim() || null
  };
}
function ct(i = null, { template: e = null, placement: t = null } = {}) {
  var u, d, m, f;
  const n = i && typeof i == "object" ? i : {};
  if ((!n || !Object.keys(n).length) && (e || t))
    return td({ template: e, placement: t });
  const a = String(
    n.measuredTemplateType ?? n.t ?? n.type ?? ""
  ).trim().toLowerCase(), r = io(
    n.shape ?? jy(a) ?? "",
    ""
  );
  if (!r)
    return e || t ? td({ template: e, placement: t }) : null;
  const s = r === "rect" ? W(n.width ?? (t == null ? void 0 : t.width) ?? (e == null ? void 0 : e.width) ?? n.distance ?? n.size, 0) : 0, o = r === "rect" ? W(n.height ?? (t == null ? void 0 : t.height) ?? (e == null ? void 0 : e.height) ?? n.distance ?? n.size, 0) : 0, l = W(
    n.distance ?? n.size ?? n.templateDistance ?? (t == null ? void 0 : t.distance) ?? (e == null ? void 0 : e.distance) ?? (e == null ? void 0 : e.size),
    0
  );
  if (r === "rect") {
    if (!(s > 0) || !(o > 0)) return null;
  } else if (!(l > 0)) return null;
  return {
    shape: r,
    measuredTemplateType: a || gf(r),
    x: W(n.x ?? ((u = n.anchor) == null ? void 0 : u.x) ?? ((d = t == null ? void 0 : t.anchor) == null ? void 0 : d.x), 0),
    y: W(n.y ?? ((m = n.anchor) == null ? void 0 : m.y) ?? ((f = t == null ? void 0 : t.anchor) == null ? void 0 : f.y), 0),
    direction: Wc(n.direction ?? n.rotation ?? (t == null ? void 0 : t.direction) ?? 0),
    distance: r === "rect" ? Math.max(s, o) : l,
    angle: r === "cone" ? W(n.angle ?? (t == null ? void 0 : t.angle) ?? 90, 90) : null,
    width: r === "line" ? W(n.width ?? (t == null ? void 0 : t.width) ?? ln(), ln()) : r === "rect" ? s : null,
    height: r === "rect" ? o : null,
    anchorX: r === "rect" ? W(n.anchorX ?? (t == null ? void 0 : t.anchorX) ?? (e == null ? void 0 : e.anchorX) ?? 0, 0) : null,
    anchorY: r === "rect" ? W(n.anchorY ?? (t == null ? void 0 : t.anchorY) ?? (e == null ? void 0 : e.anchorY) ?? 0, 0) : null,
    placementMode: String(n.placementMode ?? (e == null ? void 0 : e.placement) ?? "").trim() || null
  };
}
function Gy(i = null) {
  return i ? typeof (i == null ? void 0 : i.toObject) == "function" ? i.toObject() : typeof (i == null ? void 0 : i.toJSON) == "function" ? i.toJSON() : i && typeof i == "object" ? ff(i) : null : null;
}
function Wy(i = []) {
  const e = [];
  for (const t of i) {
    const n = Number(t);
    n > 0 && (e.some((a) => Math.abs(a - n) < 1e-3) || e.push(n));
  }
  return e;
}
function qy(i = null, { placementMode: e = "region", shapeHint: t = "" } = {}) {
  const n = Gy(i);
  if (!n || typeof n != "object") return null;
  const a = String(n.type ?? "").trim().toLowerCase(), r = io(t, "");
  if (a === "circle")
    return ct({
      shape: r || "blast",
      measuredTemplateType: "circle",
      x: n.x,
      y: n.y,
      distance: Ja(n.radius),
      placementMode: e
    });
  if (a === "ellipse") {
    const s = W(n.radiusX, 0), o = W(n.radiusY, 0);
    return !(s > 0) || Math.abs(s - o) > 1e-3 ? null : ct({
      shape: r || "blast",
      measuredTemplateType: "circle",
      x: W(n.x, 0) + s,
      y: W(n.y, 0) + o,
      distance: Ja(s),
      placementMode: e
    });
  }
  if (a === "cone")
    return ct({
      shape: r || "cone",
      measuredTemplateType: "cone",
      x: n.x,
      y: n.y,
      direction: n.rotation ?? n.direction,
      distance: Ja(n.radius),
      angle: n.angle,
      placementMode: e
    });
  if (a === "line") {
    const s = Array.from((i == null ? void 0 : i.measuredSegments) ?? []), o = Wy(s.map((f) => f == null ? void 0 : f.distance)), l = Math.max(
      W(n.distance, 0),
      W(n.length, 0),
      W(n.radius, 0),
      ...o,
      0
    ), c = o.filter((f) => Math.abs(f - l) >= 1e-3), u = Math.max(
      0,
      W(n.width, 0),
      W(n.thickness, 0),
      c.length ? Math.min(...c) : 0
    ) || ln(), d = (i == null ? void 0 : i.origin) ?? n.origin ?? {}, m = s.reduce((f, p) => {
      const g = Number((p == null ? void 0 : p.distance) ?? 0), h = Number((f == null ? void 0 : f.distance) ?? 0);
      return g > h ? p : f;
    }, null);
    return ct({
      shape: r || "line",
      measuredTemplateType: "ray",
      x: d.x ?? n.x,
      y: d.y ?? n.y,
      direction: (m == null ? void 0 : m.angle) ?? n.rotation ?? n.direction,
      distance: l,
      width: u,
      placementMode: e
    });
  }
  return a === "rectangle" || a === "rect" ? ct({
    shape: r || "rect",
    measuredTemplateType: "rect",
    x: n.x,
    y: n.y,
    direction: n.rotation ?? n.direction,
    width: Ja(n.width),
    height: Ja(n.height),
    anchorX: n.anchorX,
    anchorY: n.anchorY,
    placementMode: e
  }) : null;
}
function hf(i = null, { placementMode: e = "region", shapeHint: t = "" } = {}) {
  const n = (i == null ? void 0 : i.document) ?? i ?? null, a = Array.from((n == null ? void 0 : n.shapes) ?? []);
  return a.length !== 1 ? null : qy(a[0], { placementMode: e, shapeHint: t });
}
function Vy(i = null, e = null) {
  const t = ct(i);
  return t ? {
    template: {
      shape: t.shape,
      placement: t.placementMode ?? (e == null ? void 0 : e.placement) ?? null,
      distance: t.distance,
      size: t.distance
    },
    placement: {
      shape: t.shape,
      anchor: {
        x: t.x,
        y: t.y
      },
      distance: t.distance,
      direction: t.direction,
      angle: t.angle ?? void 0,
      width: t.width ?? void 0,
      placementMode: t.placementMode ?? null
    }
  } : null;
}
function yf(i) {
  var o, l, c, u, d, m, f, p, g;
  const e = (i == null ? void 0 : i.center) ?? ((o = i == null ? void 0 : i.object) == null ? void 0 : o.center) ?? null;
  if (e)
    return {
      x: W(e.x, 0),
      y: W(e.y, 0)
    };
  const t = W((i == null ? void 0 : i.x) ?? ((l = i == null ? void 0 : i.document) == null ? void 0 : l.x), 0), n = W((i == null ? void 0 : i.y) ?? ((c = i == null ? void 0 : i.document) == null ? void 0 : c.y), 0), a = W((i == null ? void 0 : i.w) ?? ((u = i == null ? void 0 : i.object) == null ? void 0 : u.w) ?? ((d = i == null ? void 0 : i.document) == null ? void 0 : d.width), 1), r = W((i == null ? void 0 : i.h) ?? ((m = i == null ? void 0 : i.object) == null ? void 0 : m.h) ?? ((f = i == null ? void 0 : i.document) == null ? void 0 : f.height), 1), s = W(((p = canvas == null ? void 0 : canvas.grid) == null ? void 0 : p.size) ?? ((g = canvas == null ? void 0 : canvas.dimensions) == null ? void 0 : g.size), 100);
  return {
    x: t + a * s / 2,
    y: n + r * s / 2
  };
}
function Yy(i) {
  var n, a, r, s;
  const e = W((i == null ? void 0 : i.w) ?? ((n = i == null ? void 0 : i.object) == null ? void 0 : n.w) ?? ((a = i == null ? void 0 : i.document) == null ? void 0 : a.width), 1) * Ds(), t = W((i == null ? void 0 : i.h) ?? ((r = i == null ? void 0 : i.object) == null ? void 0 : r.h) ?? ((s = i == null ? void 0 : i.document) == null ? void 0 : s.height), 1) * Ds();
  return Math.max(e, t) / 2;
}
function Jy({ geometry: i, tokenCenter: e, tokenRadius: t }) {
  const n = Qt(i.distance);
  return Ml({ x: i.x, y: i.y }, e) <= n + t;
}
function Qy({ geometry: i, tokenCenter: e, tokenRadius: t }) {
  const n = Qt(i.distance), a = Qt(i.width ?? ln()), r = Hr(i.direction), s = e.x - i.x, o = e.y - i.y, l = Math.cos(r), c = Math.sin(r), u = s * l + o * c;
  if (u < -t || u > n + t) return !1;
  const d = Math.max(0, Math.min(n, u)), m = i.x + d * l, f = i.y + d * c;
  return Math.hypot(e.x - m, e.y - f) <= t + a / 2;
}
function Xy({ geometry: i, tokenCenter: e, tokenRadius: t }) {
  const n = Qt(i.distance), a = e.x - i.x, r = e.y - i.y, s = Math.hypot(a, r);
  if (s > n + t) return !1;
  if (s === 0) return !0;
  let l = Math.atan2(r, a) * 180 / Math.PI - i.direction;
  for (; l <= -180; ) l += 360;
  for (; l > 180; ) l -= 360;
  const c = W(i.angle, 90) / 2, u = Math.asin(Math.min(1, t / Math.max(s, 1))) * 180 / Math.PI;
  return Math.abs(l) <= c + u;
}
function Zy({ geometry: i, tokenCenter: e, tokenRadius: t }) {
  const n = Qt(W(i.width, 0)), a = Qt(W(i.height, 0));
  if (!(n > 0) || !(a > 0)) return !1;
  const r = W(i.anchorX, 0), s = W(i.anchorY, 0), o = W(i.x, 0), l = W(i.y, 0), c = o + n * (0.5 - r), u = l + a * (0.5 - s), d = -Hr(i.direction ?? 0), m = Math.cos(d), f = Math.sin(d), p = e.x - c, g = e.y - u, h = p * m - g * f, y = p * f + g * m;
  return Math.abs(h) <= n / 2 + t && Math.abs(y) <= a / 2 + t;
}
function no(i = null, e = null) {
  const t = ct(i);
  if (!t || !e) return !1;
  const n = yf(e), a = Yy(e);
  return t.shape === "blast" ? Jy({ geometry: t, tokenCenter: n, tokenRadius: a }) : t.shape === "line" ? Qy({ geometry: t, tokenCenter: n, tokenRadius: a }) : t.shape === "cone" ? Xy({ geometry: t, tokenCenter: n, tokenRadius: a }) : t.shape === "rect" ? Zy({ geometry: t, tokenCenter: n, tokenRadius: a }) : !1;
}
function bf({ template: i = {}, placement: e = {}, geometry: t = null, token: n = null } = {}) {
  if (!n) return pe.none;
  const a = ct(t, { template: i, placement: e });
  if (!a || !no(a, n))
    return pe.none;
  const r = yf(n), s = Qt(a.distance);
  if (!(s > 0)) return pe.none;
  if (a.shape === "line" || a.shape === "cone") {
    const l = Ky({ geometry: a, tokenCenter: r });
    return Bo(l, s);
  }
  if (a.shape === "rect") {
    const l = {
      x: W(a.x, 0) + Qt(W(a.width, 0)) * (0.5 - W(a.anchorX, 0)),
      y: W(a.y, 0) + Qt(W(a.height, 0)) * (0.5 - W(a.anchorY, 0))
    }, c = Ml(l, r);
    return Bo(c, s);
  }
  const o = Ml({ x: a.x, y: a.y }, r);
  return Bo(o, s);
}
function Rn({ tier: i = pe.none, appliedTier: e = null, evadeUsed: t = !1, evadeLocked: n = !1 } = {}) {
  const a = lt(i, pe.none), r = lt(e ?? a, a);
  return {
    initialTier: a,
    initialLabel: bi(a),
    initialMultiplier: Tl(a),
    finalTier: r,
    finalLabel: bi(r),
    finalMultiplier: Tl(r),
    evadeUsed: !!t,
    evadeLocked: !!n
  };
}
function qc(i = {}, { locked: e = !1, active: t = !1 } = {}) {
  const n = lt((i == null ? void 0 : i.initialTier) ?? (i == null ? void 0 : i.tier), pe.none);
  if (!t || e || n === pe.none)
    return Rn({
      tier: n,
      appliedTier: n,
      evadeUsed: !1,
      evadeLocked: !!e
    });
  const a = kl(n, 1);
  return Rn({
    tier: n,
    appliedTier: a,
    evadeUsed: n !== a,
    evadeLocked: !!e
  });
}
function Sf(i = []) {
  return i.map((e) => ({
    x: Math.round(W(e.x, 0)),
    y: Math.round(W(e.y, 0))
  }));
}
function eb(i = {}) {
  const e = Qt(W(i.distance, 0)), t = Qt(W(i.width, ln())) / 2, n = Hr(i.direction ?? 0), a = Math.cos(n), r = Math.sin(n), s = -r, o = a, l = {
    x: W(i.x, 0) + e * a,
    y: W(i.y, 0) + e * r
  };
  return {
    type: "polygon",
    points: Sf([
      { x: i.x + s * t, y: i.y + o * t },
      { x: l.x + s * t, y: l.y + o * t },
      { x: l.x - s * t, y: l.y - o * t },
      { x: i.x - s * t, y: i.y - o * t }
    ])
  };
}
function tb(i = {}) {
  const e = W(i.angle, 90), t = Qt(W(i.distance, 0)), n = W(i.direction, 0), a = e / 2, r = [{ x: i.x, y: i.y }];
  for (let s = 0; s <= 8; s += 1) {
    const o = -a + e / 8 * s, l = Hr(n + o);
    r.push({
      x: W(i.x, 0) + Math.cos(l) * t,
      y: W(i.y, 0) + Math.sin(l) * t
    });
  }
  return {
    type: "polygon",
    points: Sf(r)
  };
}
function ib(i = {}) {
  return {
    type: "rectangle",
    x: Math.round(W(i.x, 0)),
    y: Math.round(W(i.y, 0)),
    width: Math.round(Qt(W(i.width, 0))),
    height: Math.round(Qt(W(i.height, 0))),
    rotation: Wc(i.direction ?? 0),
    anchorX: W(i.anchorX, 0),
    anchorY: W(i.anchorY, 0)
  };
}
function ao(i = null) {
  const e = ct(i);
  if (!e) return [];
  if (e.shape === "blast") {
    const t = Qt(W(e.distance, 0));
    return [{
      type: "ellipse",
      x: Math.round(W(e.x, 0) - t),
      y: Math.round(W(e.y, 0) - t),
      radiusX: Math.round(t),
      radiusY: Math.round(t),
      rotation: 0
    }];
  }
  return e.shape === "line" ? [eb(e)] : e.shape === "cone" ? [tb(e)] : e.shape === "rect" ? [ib(e)] : [];
}
function ji(i = null) {
  const e = ct(i);
  return e ? ff(e) : null;
}
const Af = Object.freeze({
  penetrating: "Penetrating",
  concussive: "Concussive",
  energy: "Energy",
  thermal: "Thermal",
  electrical: "Electrical"
}), Os = Object.freeze(
  Object.entries(Af).map(([i, e]) => ({ value: i, label: e }))
), nb = Object.freeze({
  ballistic: "penetrating",
  kinetic: "concussive",
  explosive: "concussive",
  laser: "energy",
  plasma: "thermal",
  electrical: "electrical",
  melee: "penetrating",
  corrosive: "thermal",
  poison: "concussive",
  none: "concussive",
  penetrating: "penetrating",
  concussive: "concussive",
  energy: "energy",
  thermal: "thermal"
}), ab = Object.freeze(
  Os.map((i) => i.value)
), Cl = Object.freeze({}), ro = Object.freeze({
  ablative: Object.freeze({
    key: "ablative",
    label: "Ablative",
    rated: !1,
    aliases: ["ablative"],
    resolve: () => ({ mitigationByType: { energy: 2 } })
  }),
  flak: Object.freeze({
    key: "flak",
    label: "Flak",
    rated: !1,
    aliases: ["flak"],
    resolve: () => ({ mitigationByType: { penetrating: 1 } })
  }),
  reinforced: Object.freeze({
    key: "reinforced",
    label: "Reinforced",
    rated: !0,
    aliases: ["reinforced"],
    resolve: (i) => ({ reinforced: Math.max(0, Number((i == null ? void 0 : i.rating) ?? 0) || 0) })
  }),
  padded: Object.freeze({
    key: "padded",
    label: "Padded",
    rated: !1,
    aliases: ["padded"],
    resolve: () => ({ mitigationByType: { concussive: 1 } })
  }),
  insulated: Object.freeze({
    key: "insulated",
    label: "Insulated",
    rated: !1,
    aliases: ["insulated"],
    resolve: () => ({ mitigationByType: { thermal: 2 } })
  })
}), rb = Object.freeze(
  Object.values(ro).map((i) => ({
    value: i.key,
    label: i.label,
    rated: i.rated
  }))
), wf = Cf(Cl), Tf = Cf(ro);
function so(i) {
  return i && typeof i == "object" && !Array.isArray(i) ? Object.values(i).flatMap((e) => so(e)) : Array.isArray(i) ? i.map((e) => String(e ?? "").trim()).filter(Boolean) : String(i ?? "").split(",").map((e) => e.trim()).filter(Boolean);
}
function ci(i, e = "penetrating") {
  const t = String(i ?? "").trim().toLowerCase();
  return nb[t] ?? e;
}
function kf(i) {
  const e = String(i ?? "").trim();
  return e ? ci(e, "") : "";
}
function Mf(i) {
  const e = String(i ?? "").trim().toLowerCase();
  return ab.includes(e);
}
function Pi(i) {
  const e = ci(i, "");
  return Af[e] ?? String(i ?? "").trim();
}
function Ki(i) {
  const e = i ?? {}, t = Number(e.ballistic ?? 0) || 0, n = Number(e.melee ?? 0) || 0;
  return {
    penetrating: e.penetrating !== void 0 ? Number(e.penetrating ?? 0) || 0 : Math.max(t, n),
    concussive: e.concussive !== void 0 ? Number(e.concussive ?? 0) || 0 : Number(e.explosive ?? 0) || 0,
    energy: Number(e.energy ?? 0) || 0,
    thermal: Number(e.thermal ?? 0) || 0,
    electrical: Number(e.electrical ?? 0) || 0
  };
}
function ls(i) {
  return so(i);
}
function ja(i) {
  return so(i);
}
function sb(i) {
  return Ps(i);
}
function cs(i = {}, e = "standard") {
  return mf(i, e);
}
function us(i = {}) {
  return Dy(i);
}
function ob(i = null) {
  return _y(i);
}
function xa(i = "id") {
  var t, n;
  const e = (n = (t = globalThis.foundry) == null ? void 0 : t.utils) == null ? void 0 : n.randomID;
  return typeof e == "function" ? e() : `${i}-${Math.random().toString(36).slice(2, 10)}`;
}
function Cf(i) {
  const e = {};
  return Object.values(i).forEach((t) => {
    [t.key, ...t.aliases ?? []].forEach((n) => {
      e[kr(n)] = t.key;
    });
  }), Object.freeze(e);
}
function kr(i) {
  return String(i ?? "").trim().toLowerCase().replace(/[^a-z0-9]+/g, "");
}
function Ka(i) {
  return Array.isArray(i) ? i : i && typeof i == "object" ? Object.values(i) : i == null || i === "" ? [] : [i];
}
function vf(i, e) {
  return Ka(i).map((t) => lb(t, e)).filter(Boolean);
}
function lb(i, e) {
  if (typeof i == "string" || typeof i == "number") {
    const n = e[kr(i)];
    return n ? { id: xa("trait"), key: n, rating: 1 } : null;
  }
  if (!i || typeof i != "object") return null;
  const t = e[kr(i.key ?? i.value ?? i.name)];
  return t ? {
    id: String(i.id ?? "").trim() || xa("trait"),
    key: t,
    rating: Math.max(0, Number(i.rating ?? 0) || 0)
  } : null;
}
function Un(i) {
  return vf(i, wf);
}
function Xi(i) {
  return vf(i, Tf);
}
function Ls(i) {
  return {
    close: Number((i == null ? void 0 : i.close) ?? (i == null ? void 0 : i.short) ?? 0) || 0,
    near: Number((i == null ? void 0 : i.near) ?? (i == null ? void 0 : i.medium) ?? 0) || 0,
    far: Number((i == null ? void 0 : i.far) ?? (i == null ? void 0 : i.long) ?? 0) || 0,
    extreme: Number((i == null ? void 0 : i.extreme) ?? 0) || 0
  };
}
function cb(i = {}, e = {}) {
  const t = Ls(i), n = Ls(e);
  return {
    close: t.close + n.close,
    near: t.near + n.near,
    far: t.far + n.far,
    extreme: t.extreme + n.extreme
  };
}
function ub(i, e) {
  var t;
  return ((t = e[i]) == null ? void 0 : t.label) ?? i;
}
function Ef(i, e) {
  var a;
  const t = ub(i == null ? void 0 : i.key, e), n = Math.max(0, Number((i == null ? void 0 : i.rating) ?? 0) || 0);
  return (a = e[i == null ? void 0 : i.key]) != null && a.rated && n > 0 ? `${t} ${n}` : t;
}
function If(i, e) {
  return Ka(i).map((t) => {
    const n = t == null ? void 0 : t.key, a = e[n];
    return a != null && a.resolve ? {
      entry: t,
      effect: a.resolve(t),
      label: Ef(t, e)
    } : null;
  }).filter(Boolean);
}
function db(i, e) {
  const t = { ...i ?? {} };
  return Object.entries(e ?? {}).forEach(([n, a]) => {
    t[n] = (Number(t[n] ?? 0) || 0) + (Number(a ?? 0) || 0);
  }), t;
}
function mb(i = []) {
  const e = {}, t = /* @__PURE__ */ new Set();
  for (const n of i.filter(Boolean)) {
    n.accuracyMod !== void 0 && (e.accuracyMod = (Number(e.accuracyMod ?? 0) || 0) + (Number(n.accuracyMod ?? 0) || 0)), n.ap !== void 0 && (e.ap = (Number(e.ap ?? 0) || 0) + (Number(n.ap ?? 0) || 0)), n.addHeat !== void 0 && (e.addHeat = (Number(e.addHeat ?? 0) || 0) + (Number(n.addHeat ?? 0) || 0)), n.bonusVsArmorTag && (e.bonusVsArmorTag = db(e.bonusVsArmorTag, n.bonusVsArmorTag));
    for (const a of n.flags ?? []) {
      const r = String(a ?? "").trim();
      r && t.add(r);
    }
  }
  return t.size > 0 && (e.flags = Array.from(t)), e;
}
function fb(i = [], e = []) {
  const t = Array.isArray(i) || typeof i == "string" ? { traits: i, standardTraits: e } : i ?? {}, n = ja(t.traits), a = Un(t.standardTraits), r = If(a, Cl), s = n.map((o) => {
    var u;
    const l = wf[kr(o)];
    if (!l) return null;
    const c = (u = Cl[l]) == null ? void 0 : u.resolve;
    return typeof c == "function" ? c({ key: l, rating: 1 }) : null;
  });
  return mb([
    ...r.map((o) => o.effect),
    ...s
  ]);
}
function pb(i) {
  const e = i ?? {}, t = Kc(), n = df({
    traits: e.traits,
    keywords: e.keywords,
    report: t,
    path: "ammo.types[].traits"
  });
  return {
    id: String(e.id ?? "").trim() || xa("ammo"),
    name: String(e.name ?? "").trim() || "Ammo",
    damageType: kf(e.damageType),
    apMod: Number(e.apMod ?? e.ap ?? 0) || 0,
    attackRatingBandMod: Ls(e.attackRatingBandMod ?? e.attackRatingBand),
    traits: n.traits,
    keywords: n.keywords,
    migration: t
  };
}
function gb(i) {
  var l;
  const e = i ?? {}, t = Math.max(0, Number(e.max ?? 0) || 0), n = Number(e.current), a = Number.isFinite(n) ? Math.max(0, Math.min(n, t > 0 ? t : n)) : Math.max(0, t), r = Ka(e.types).map(pb), s = String(e.activeTypeId ?? "").trim(), o = r.some((c) => c.id === s) ? s : ((l = r[0]) == null ? void 0 : l.id) ?? "";
  return {
    current: a,
    max: t,
    consumePerAttack: Math.max(1, Number(e.consumePerAttack ?? 1) || 1),
    activeTypeId: o,
    types: r
  };
}
function hb(i, e = "untracked") {
  const t = String(i ?? "").trim();
  return t ? t === "linked" ? "internal" : t === "perAttack" ? e : ["untracked", "internal", "actorResource", "itemRef"].includes(t) ? t : e : e;
}
function vl(i = {}) {
  const e = i ?? {};
  return {
    amount: Math.max(1, Number(e.amount ?? e.consumePerUse ?? e.consumePerAttack ?? 1) || 1),
    sourceId: String(e.sourceId ?? "").trim()
  };
}
function id(i = {}) {
  const e = i ?? {};
  return {
    damageType: kf(e.damageType),
    ap: Number(e.ap ?? e.apMod ?? 0) || 0,
    clusteringDice: Math.max(0, Number(e.clusteringDice ?? e.clusterDice ?? 0) || 0),
    attackRatingBand: Ls(e.attackRatingBand ?? e.attackRatingBandMod)
  };
}
function El(i = {}) {
  return mf(i, "standard");
}
function yb(i) {
  return String(i ?? "").trim().toLowerCase() === "unloaded";
}
function ti(i, { report: e = null, path: t = "system.payloads[]" } = {}) {
  var l;
  const n = i ?? {}, a = String(n.id ?? "").trim() || xa("payload"), r = df({
    traits: n.traits ?? ((l = n.modifies) == null ? void 0 : l.traits),
    keywords: n.keywords,
    report: e,
    path: `${t}.traits`
  }), s = so(n.compatibleWith ?? n.compatible), o = ob(n.template);
  return yb(a) ? {
    id: "unloaded",
    label: "Unloaded",
    compatibleWith: [],
    modifies: id({}),
    traits: [],
    keywords: [],
    template: null,
    areaEffect: on({ kind: "discrete" }),
    resolution: El({ resolverKey: "standard" }),
    consumption: vl({ amount: 1, sourceId: "" })
  } : {
    id: a,
    label: String(n.label ?? n.name ?? "").trim() || "Payload",
    compatibleWith: s,
    modifies: id(n.modifies ?? n),
    traits: r.traits,
    keywords: r.keywords,
    template: o,
    areaEffect: on(n.areaEffect ?? {}),
    resolution: El(n.resolution ?? n),
    consumption: vl(n.consumption ?? n)
  };
}
function $i(i) {
  var o, l, c, u, d, m;
  const e = i ?? {}, t = hb(
    e.kind || e.type || ((o = e.link) != null && o.actorPath || e.actorPath ? "actorResource" : "") || ((l = e.link) != null && l.itemId || e.itemId || (c = e.link) != null && c.itemPath || e.itemPath ? "itemRef" : "") || (e.tracking || e.current !== void 0 || e.max !== void 0 ? "internal" : "") || "untracked",
    "untracked"
  ), n = e.tracking ?? e, a = Math.max(0, Number(n.max ?? 0) || 0), r = Number(n.current), s = Number.isFinite(r) ? Math.max(0, Math.min(r, a > 0 ? a : r)) : Math.max(0, a);
  return {
    id: String(e.id ?? "").trim() || xa("source"),
    label: String(e.label ?? e.name ?? "").trim() || "Source",
    kind: t,
    tracking: {
      current: s,
      max: a
    },
    link: {
      actorPath: String(((u = e.link) == null ? void 0 : u.actorPath) ?? e.actorPath ?? "").trim(),
      itemId: String(((d = e.link) == null ? void 0 : d.itemId) ?? e.itemId ?? "").trim(),
      itemPath: String(((m = e.link) == null ? void 0 : m.itemPath) ?? e.itemPath ?? "").trim()
    }
  };
}
function Nf({ report: i = null, path: e = "system.payloads" } = {}) {
  return {
    payloads: [ti({
      id: "unloaded",
      label: "Unloaded",
      resolution: { resolverKey: "standard" },
      consumption: { amount: 1, sourceId: "" }
    }, { report: i, path: `${e}[0]` })],
    selectedPayloadId: "unloaded",
    consumptionSources: [$i({
      id: "untracked",
      label: "Untracked",
      kind: "untracked"
    })]
  };
}
function Rf(i) {
  return String(i ?? "").trim().toLowerCase() === "melee";
}
function Il(i = [], { report: e = null, path: t = "system.payloads" } = {}) {
  const n = Ka(i).map((a, r) => ti(a, { report: e, path: `${t}[${r}]` })).filter(Boolean);
  return n.some((a) => a.id === "unloaded") ? n : [
    ti({
      id: "unloaded",
      label: "Unloaded",
      resolution: { resolverKey: "standard" },
      consumption: { amount: 1, sourceId: "" }
    }, { report: e, path: `${t}[0]` }),
    ...n
  ];
}
function oo(i = {}, { report: e = null, path: t = "system.payloads" } = {}) {
  var d;
  const n = gb(i), a = Math.max(1, Number(n.consumePerAttack ?? 1) || 1), r = n.max > 0, s = r ? "internal-magazine" : "untracked", o = [$i(r ? {
    id: s,
    label: "Internal Source",
    kind: "internal",
    tracking: {
      current: n.current,
      max: n.max
    }
  } : {
    id: s,
    label: "Untracked",
    kind: "untracked",
    tracking: {}
  })], l = n.types.length ? n.types.map((m, f) => ti({
    id: m.id,
    label: m.name,
    modifies: {
      damageType: m.damageType,
      ap: m.apMod,
      attackRatingBand: m.attackRatingBandMod,
      traits: m.traits
    },
    keywords: m.keywords,
    resolution: { resolverKey: "standard" },
    consumption: {
      amount: a,
      sourceId: r ? s : ""
    }
  }, { report: e, path: `${t}[${f}]` })) : [ti({
    id: "unloaded",
    label: "Unloaded",
    resolution: { resolverKey: "standard" },
    consumption: {
      amount: a,
      sourceId: r ? s : ""
    }
  }, { report: e, path: `${t}[0]` })], c = Il(l, { report: e, path: t }), u = c.some((m) => m.id === n.activeTypeId) ? n.activeTypeId : ((d = c[0]) == null ? void 0 : d.id) ?? "unloaded";
  return {
    payloads: c,
    selectedPayloadId: u,
    consumptionSources: o
  };
}
function Ti(i, { legacyAmmo: e = null, category: t = "", report: n = null, path: a = "system.payloads" } = {}) {
  if (Rf(t)) return [];
  const r = Ka(i).map((s, o) => ti(s, { report: n, path: `${a}[${o}]` })).filter(Boolean);
  return r.length > 0 ? Il(r, { report: n, path: a }) : e ? Il(oo(e, { report: n, path: a }).payloads, { report: n, path: a }) : Nf({ report: n, path: a }).payloads;
}
function Fn(i, { legacyAmmo: e = null } = {}) {
  const t = Ka(i).map($i).filter(Boolean);
  return t.length > 0 ? t : e ? oo(e).consumptionSources : Nf().consumptionSources;
}
function pn(i, e = [], { legacyAmmo: t = null, category: n = "" } = {}) {
  var s;
  if (Rf(n)) return "";
  const a = Ti(e, { legacyAmmo: t, category: n }), r = String(i ?? "").trim();
  if (a.some((o) => o.id === r)) return r;
  if (t) {
    const o = oo(t).selectedPayloadId;
    if (a.some((l) => l.id === o)) return o;
  }
  return ((s = a[0]) == null ? void 0 : s.id) ?? "unloaded";
}
function nd({ root: i = null, path: e = "", fallback: t = {} } = {}) {
  const n = String(e ?? "").trim();
  if (!i || !n)
    return {
      current: Math.max(0, Number(t.current ?? 0) || 0),
      max: Math.max(0, Number(t.max ?? 0) || 0),
      currentPath: n
    };
  const a = foundry.utils.getProperty(i, n);
  if (a && typeof a == "object") {
    const o = Math.max(0, Number(a.max ?? t.max ?? 0) || 0), l = Number(a.current);
    return {
      current: Number.isFinite(l) ? Math.max(0, Math.min(l, o > 0 ? o : l)) : Math.max(0, o),
      max: o,
      currentPath: `${n}.current`
    };
  }
  const r = Math.max(0, Number(a ?? t.current ?? 0) || 0), s = Math.max(r, Math.max(0, Number(t.max ?? 0) || 0));
  return {
    current: s > 0 ? Math.min(r, s) : r,
    max: s,
    currentPath: n
  };
}
function Pf({ source: i = null, actor: e = null } = {}) {
  var n, a, r, s, o, l, c;
  if (!i)
    return {
      id: "",
      label: "",
      kind: "untracked",
      isTracked: !1,
      current: 0,
      max: 0,
      consumePerUse: 1,
      actorPath: "",
      itemId: "",
      itemPath: ""
    };
  const t = {
    id: i.id,
    label: i.label,
    kind: i.kind,
    actorPath: String(((n = i.link) == null ? void 0 : n.actorPath) ?? "").trim(),
    itemId: String(((a = i.link) == null ? void 0 : a.itemId) ?? "").trim(),
    itemPath: String(((r = i.link) == null ? void 0 : r.itemPath) ?? "").trim()
  };
  if (i.kind === "internal") {
    const u = Math.max(0, Number(((s = i.tracking) == null ? void 0 : s.current) ?? 0) || 0), d = Math.max(0, Number(((o = i.tracking) == null ? void 0 : o.max) ?? 0) || 0);
    return {
      ...t,
      isTracked: d > 0 || u > 0,
      current: u,
      max: d,
      currentPath: ""
    };
  }
  if (i.kind === "actorResource") {
    const u = nd({
      root: (e == null ? void 0 : e.system) ?? null,
      path: t.actorPath,
      fallback: i.tracking
    });
    return {
      ...t,
      isTracked: !0,
      current: u.current,
      max: u.max,
      currentPath: u.currentPath
    };
  }
  if (i.kind === "itemRef") {
    const u = ((c = (l = e == null ? void 0 : e.items) == null ? void 0 : l.get) == null ? void 0 : c.call(l, t.itemId)) ?? null, d = nd({
      root: (u == null ? void 0 : u.system) ?? null,
      path: t.itemPath,
      fallback: i.tracking
    });
    return {
      ...t,
      isTracked: !0,
      current: d.current,
      max: d.max,
      currentPath: d.currentPath,
      sourceItem: u
    };
  }
  return {
    ...t,
    isTracked: !1,
    current: 0,
    max: 0,
    currentPath: ""
  };
}
function bb({ source: i = null, actor: e = null } = {}) {
  return Pf({ source: i, actor: e });
}
function Nl({
  payloads: i = [],
  selectedPayloadId: e = "",
  consumptionSources: t = [],
  actor: n = null,
  payloadId: a = "",
  category: r = ""
} = {}) {
  const s = Ti(i, { category: r }), o = Fn(t), l = pn(a || e, s, { category: r }), c = s.find((f) => f.id === l) ?? s[0] ?? null, u = (c == null ? void 0 : c.consumption) ?? vl(), d = u.sourceId ? o.find((f) => f.id === u.sourceId) ?? null : o.find((f) => f.kind === "untracked") ?? $i({
    id: "untracked",
    label: "Untracked",
    kind: "untracked"
  }), m = Pf({ source: d, actor: n });
  return {
    payloads: s,
    activePayload: c,
    activePayloadId: (c == null ? void 0 : c.id) ?? "",
    payloadLabel: (c == null ? void 0 : c.label) ?? "",
    source: d,
    sourceState: {
      ...m,
      consumePerUse: Math.max(1, Number(u.amount ?? 1) || 1),
      sourceId: (d == null ? void 0 : d.id) ?? ""
    }
  };
}
function Sb({
  damageType: i = "penetrating",
  ap: e = 0,
  attackRatingBand: t = {},
  traits: n = [],
  keywords: a = [],
  standardTraits: r = [],
  resolution: s = {},
  fireModes: o = {},
  payloads: l = [],
  selectedPayloadId: c = "",
  consumptionSources: u = [],
  payloadId: d = "",
  actor: m = null,
  ammo: f = null,
  ammoTypeId: p = "",
  category: g = ""
} = {}) {
  var G, V, Z, E, X, H;
  const h = Nl({
    payloads: l != null && l.length ? l : void 0,
    selectedPayloadId: c || p,
    consumptionSources: u,
    actor: m,
    payloadId: d || p,
    category: g
  }), b = ((!l || l.length === 0) && f ? Nl({
    ...oo(f),
    actor: m,
    payloadId: d || p,
    category: g
  }) : null) ?? h, S = b.activePayload, w = uf({
    traits: n,
    keywords: a
  }), T = Array.from(/* @__PURE__ */ new Set([
    ...w.traits,
    ...ja(S == null ? void 0 : S.traits)
  ])), k = Ps([
    ...w.keywords,
    ...Ps(S == null ? void 0 : S.keywords)
  ]), C = cs(s, "standard"), I = (G = S == null ? void 0 : S.resolution) != null && G.resolverKey ? El(S.resolution) : C, O = us(o), x = Kc(), B = xy({
    weapon: {
      traits: w.traits,
      resolution: C
    },
    payload: S,
    effectiveTraits: T,
    effectiveResolution: I,
    report: x
  }), P = Un(r), L = fb({
    traits: [],
    standardTraits: P
  }), D = {
    ...b.sourceState
  };
  return delete D.sourceItem, {
    damageType: ((V = S == null ? void 0 : S.modifies) == null ? void 0 : V.damageType) || ci(i),
    ap: (Number(e ?? 0) || 0) + (Number(((Z = S == null ? void 0 : S.modifies) == null ? void 0 : Z.ap) ?? 0) || 0),
    clusteringDice: Number(((E = S == null ? void 0 : S.modifies) == null ? void 0 : E.clusteringDice) ?? 0) || 0,
    attackRatingBand: cb(
      t,
      ((X = S == null ? void 0 : S.modifies) == null ? void 0 : X.attackRatingBand) ?? {}
    ),
    effects: L,
    traits: T,
    keywords: k,
    standardTraits: P,
    payloadLabel: b.payloadLabel,
    payload: S ? foundry.utils.deepClone(S) : null,
    payloadState: {
      payloads: b.payloads.map((he) => foundry.utils.deepClone(he)),
      activePayloadId: b.activePayloadId,
      payloadLabel: b.payloadLabel,
      sourceId: ((H = b.source) == null ? void 0 : H.id) ?? "",
      sourceLabel: b.sourceState.label ?? "",
      sourceKind: b.sourceState.kind ?? "untracked",
      isTracked: b.sourceState.isTracked,
      current: b.sourceState.current,
      max: b.sourceState.max,
      consumePerUse: b.sourceState.consumePerUse
    },
    source: b.source ? foundry.utils.deepClone(b.source) : null,
    sourceState: foundry.utils.deepClone(D),
    template: B.template ? foundry.utils.deepClone(B.template) : null,
    areaEffect: on((S == null ? void 0 : S.areaEffect) ?? {}),
    resolution: foundry.utils.deepClone(I),
    resolverKey: String((I == null ? void 0 : I.resolverKey) ?? "standard").trim() || "standard",
    fireModes: foundry.utils.deepClone(O),
    capabilityReport: {
      ...x,
      liveCapabilities: B.liveCapabilities,
      isTemplated: B.isTemplated,
      template: B.template ? foundry.utils.deepClone(B.template) : null,
      resolverKey: String((I == null ? void 0 : I.resolverKey) ?? "standard").trim() || "standard"
    },
    ammoLabel: b.payloadLabel,
    ammoType: S ? foundry.utils.deepClone(S) : null,
    ammoState: {
      current: D.current,
      max: D.max,
      consumePerAttack: D.consumePerUse,
      activeTypeId: b.activePayloadId,
      types: b.payloads.map((he) => {
        var ie, le;
        return {
          id: he.id,
          name: he.label,
          damageType: ((ie = he.modifies) == null ? void 0 : ie.damageType) ?? "",
          clusteringDice: Number(((le = he.modifies) == null ? void 0 : le.clusteringDice) ?? 0) || 0,
          traits: he.traits ?? [],
          keywords: he.keywords ?? []
        };
      }),
      isTracked: D.isTracked,
      ammoLabel: b.payloadLabel
    }
  };
}
function Df(i = {}, e = {}) {
  const t = Ki(i), n = Ki(e);
  return {
    penetrating: t.penetrating + n.penetrating,
    concussive: t.concussive + n.concussive,
    energy: t.energy + n.energy,
    thermal: t.thermal + n.thermal,
    electrical: t.electrical + n.electrical
  };
}
function zo({ standardTraits: i = [], traits: e = [], traitState: t = {} } = {}) {
  var m, f;
  const n = Xi(i), r = ja(e).map((p) => {
    const g = Tf[kr(p)];
    return g ? { id: xa("trait"), key: g, rating: g === "reinforced" ? 1 : 0 } : null;
  }).filter(Boolean), s = If(
    [...n, ...r],
    ro
  ), o = s.reduce((p, g) => {
    var h;
    return Df(p, ((h = g.effect) == null ? void 0 : h.mitigationByType) ?? {});
  }, Ki({})), l = s.reduce(
    (p, g) => {
      var h;
      return p + Math.max(0, Number(((h = g.effect) == null ? void 0 : h.reinforced) ?? 0) || 0);
    },
    0
  ), c = Number((m = t == null ? void 0 : t.reinforced) == null ? void 0 : m.current), u = Number((f = t == null ? void 0 : t.reinforced) == null ? void 0 : f.max), d = Number.isFinite(c) ? c : Number.isFinite(u) ? u : l;
  return {
    mitigationByType: o,
    reinforcedMax: l,
    traitState: {
      reinforced: {
        current: Math.min(l, Math.max(0, d || 0)),
        max: l
      }
    },
    labels: s.map((p) => p.label),
    standardTraits: n
  };
}
function Ab({ traits: i = [], standardTraits: e = [] } = {}) {
  return [
    ...ja(i),
    ...Xi(e).map((n) => Ef(n, ro))
  ].filter(Boolean);
}
function Vc(i) {
  const e = Math.max(0, Number(i ?? 0) || 0);
  return e <= 0 ? 0 : Math.ceil(e / 4);
}
function wb({
  currentArmorRating: i = 0,
  mitigationByType: e = {},
  damageType: t
} = {}) {
  const n = Math.max(0, Number(i ?? 0) || 0);
  if (n <= 0)
    return {
      currentArmorRating: 0,
      baseMitigation: 0,
      typeMitigationMod: 0,
      totalMitigation: 0,
      isDestroyed: !0
    };
  const a = ci(t, "penetrating"), r = Ki(e), s = Vc(n), o = Number(r[a] ?? 0) || 0;
  return {
    currentArmorRating: n,
    baseMitigation: s,
    typeMitigationMod: o,
    totalMitigation: s + o,
    isDestroyed: !1
  };
}
function Tb({ damageIncoming: i = 0, armorTags: e = [], effects: t = {} } = {}) {
  const n = new Set(ls(e));
  let a = Number(i ?? 0) || 0;
  const r = [];
  return Object.entries((t == null ? void 0 : t.bonusVsArmorTag) ?? {}).forEach(([s, o]) => {
    if (!n.has(s)) return;
    const l = Number(o ?? 0) || 0;
    l && (a *= 1 + l, r.push({ tag: s, bonus: l }));
  }), {
    damageIncoming: a,
    applied: r
  };
}
class la {
  static checkSufficient(e, t, n) {
    if (t > n) {
      const a = Ke(N.common.errors.insufficient, {
        resource: e,
        required: t,
        available: n
      });
      throw ui.notifications.error(a), a;
    }
  }
  static checkOutOfRange(e, t, n, a) {
    if (t < n || t > a) {
      const r = Ke(N.common.errors.outOfRange, {
        resource: e,
        value: t,
        min: n,
        max: a
      });
      throw ui.notifications.error(r), r;
    }
  }
  static checkUserGM() {
    if (!game.user.isGM) {
      const e = N.common.errors.onlyGM;
      throw ui.notifications.error(e), e;
    }
  }
  static checkItemType(e, t) {
    if (e.type != t) {
      const n = Ke(N.common.errors.expectedType, {
        type: e.type ? N.itemType.singular[e.type] : e.type,
        expectedType: t
      });
      throw ui.notifications.error(n), n;
    }
  }
  static checkActorCanReceiveDamage(e, t, n) {
    if (!t) {
      const a = Ke(N.common.errors.actorCannotReceiveDamage, {
        actor: n.name,
        damageType: Mf(e) ? Pi(e) : N.actor.monitors[e] ?? N.mwd.weaponDamageType[e] ?? N.mwd.personalDamageType[e] ?? e
      });
      throw ui.notifications.error(a), a;
    }
  }
  static checkWeaponDefense(e, t) {
    var a;
    const n = e.getDefense();
    if ((((a = e.isPersonalWeapon) == null ? void 0 : a.call(e)) ?? e.type === A.itemType.personalWeapon) && !n) {
      const r = Ke(N.common.errors.noDefenseOnWeapon, { actor: t.name, weapon: e.name });
      throw ui.notifications.error(r), r;
    }
  }
  static checkTargetsCount(e, t, n) {
    if (e > 0 && t.length > e) {
      const a = Ke(N.common.errors.maxTargetsExceedeed, {
        weapon: this.name,
        area: N.area[n],
        count: t.length,
        max: e
      });
      throw ui.notifications.error(a), a;
    }
  }
  static checkActorDefenseAction(e, t, n) {
    if (!e) {
      const a = Ke(N.common.errors.actorDoesNotHaveDefense, {
        actor: t.name,
        defense: n.labelkey,
        actorType: N.actorType[t.type]
      });
      throw ui.notifications.error(a), a;
    }
  }
}
function Yi(i, e, t, n, a, r = (s) => !0) {
  return {
    code: i,
    labelkey: N.attributeAction[i],
    label: N.attributeAction[i],
    attributeFunction1: e ?? ((s) => {
    }),
    attributeFunction2: t ?? ((s) => {
    }),
    icon: n,
    actorTypes: a,
    condition: r
  };
}
function qr(i, e) {
  return {
    code: i,
    labelkey: N.defense[i],
    label: N.defense[i],
    actionCode: e
  };
}
const Et = A.actorAttributes, It = A.actorTypes, oi = jt.actions, Vr = jt.defenses, Fo = [
  Yi(oi.defense, (i) => Et.reflexes, (i) => Et.intelligence, re.fontAwesome("fas fa-shield-alt"), [It.character, It.npc]),
  Yi(oi.resistTorture, (i) => Et.strength, (i) => Et.willpower, re.fontAwesome("fas fa-angry"), [It.character, It.npc]),
  Yi(oi.perception, (i) => Et.logic, (i) => Et.willpower, re.fontAwesome("fas fa-eye"), [It.character, It.npc]),
  Yi(oi.perception, (i) => Et.system, (i) => Et.handling, re.fontAwesome("fas fa-video"), [It.vehicle, It.battlemech]),
  Yi(oi.composure, (i) => Et.charisma, (i) => Et.willpower, re.fontAwesome("fas fa-meh"), [It.character, It.npc]),
  Yi(oi.judgeIntentions, (i) => Et.charisma, (i) => Et.charisma, re.fontAwesome("fas fa-theater-masks"), [It.character, It.npc]),
  Yi(oi.memory, (i) => Et.logic, (i) => Et.logic, re.fontAwesome("fas fa-brain"), [It.character, It.npc]),
  Yi(oi.catch, (i) => Et.reflexes, (i) => Et.reflexes, re.fontAwesome("fas fa-baseball-ball"), [It.character, It.npc]),
  Yi(oi.lift, (i) => Et.strength, (i) => Et.strength, re.fontAwesome("fas fa-dumbbell"), [It.character, It.npc])
], Yr = [
  qr(Vr.physicalDefense, oi.defense),
  qr(Vr.physicalResistance, oi.resistTorture),
  qr(Vr.socialDefense, oi.composure),
  qr(Vr.mentalResistance, oi.perception)
];
class it {
  static init() {
    Handlebars.registerHelper("fixedDefenseCode", (e) => it.fixedDefenseCode(e));
  }
  static all(e = void 0) {
    return e ? Fo.filter(e) : Fo;
  }
  static getActorActions(e) {
    return Fo.filter((t) => t.actorTypes.includes(e.type) && t.condition(e));
  }
  static fixedDefenseCode(e) {
    return jt.fixedDefenseCode[e] ?? e;
  }
  static getActorDefenses(e) {
    return Yr.map((t) => {
      const n = it.getActorAction(e, t.actionCode);
      return it._convertToDefense(n, t);
    }).filter((t) => t == null ? void 0 : t.code);
  }
  static getDefenseAttributeAction(e) {
    var t;
    return (t = Yr.find((n) => n.code == e)) == null ? void 0 : t.actionCode;
  }
  static getActorAction(e, t) {
    return it.getActorActions(e).find((n) => n.code == t);
  }
  static getActorDefense(e, t) {
    t = it.fixedDefenseCode(t);
    const n = Yr.find((r) => r.code == t), a = it.getActorAction(e, n.actionCode);
    return la.checkActorDefenseAction(a, e, n), it._convertToDefense(a, n);
  }
  static _convertToDefense(e, t) {
    return e ? foundry.utils.mergeObject(
      t,
      e ?? {},
      { overwrite: !1, inplace: !1 }
    ) : void 0;
  }
  static getDefenses() {
    return Yr;
  }
  static prepareShortcut(e, t) {
    const n = it.getActorActions(e).find((a) => a.code == t);
    if (n)
      return {
        icon: n.icon,
        label: n.labelkey,
        callback: (a) => a.actor.rollAttributeAction(t)
      };
  }
}
class Rl {
  constructor() {
    this.remoteCalls = {}, game.socket.on(Al, async (e) => this.onSocketMessage(e));
  }
  static async register(e, t) {
    game.system.anarchy.remoteCall._register(e, t);
  }
  async _register(e, t) {
    if (this.remoteCalls[e])
      throw `RemoteCall msg ${e} is already registered`;
    foundry.utils.mergeObject(t, {
      callback: (n) => {
        console.log(Le + "RemoteCall [", e, "] (", n, ")");
      },
      condition: (n) => !0,
      multiple: !1
      /* true if multiple users should handle the message */
    }, { overwrite: !1 }), this.remoteCalls[e] = t, console.log(Le + "RemoteCall registered", e);
  }
  static call(e, t) {
    return game.system.anarchy.remoteCall._remoteCall(e, t);
  }
  _remoteCall(e, t) {
    const n = this.remoteCalls[e];
    return !n || n.condition(game.user) || !n.multiple && Ei.isUniqueConnectedGM() ? !1 : (game.socket.emit(Al, { msg: e, data: t }), !0);
  }
  async onSocketMessage(e) {
    const t = this.remoteCalls[e.msg];
    if (t) {
      const n = t.condition(game.user), a = t.multiple, r = Ei.isUniqueConnectedGM();
      n && (a || r) ? t.callback(e.data) : console.log(Le + "RemoteCall.onSocketMessage(", e, ") ignored :", n, a, r);
    } else
      console.log(Le + "RemoteCall: No callback registered for", e);
  }
}
const ad = "Users.blindMessageToGM";
class Ei {
  static init() {
    Rl.register(ad, {
      callback: (e) => Ei.blindMessageToGM(e),
      condition: (e) => e.isGM
    });
  }
  static blindMessageToGM(e) {
    Rl.call(ad, e) || ChatMessage.create({
      user: e.user,
      whisper: ChatMessage.getWhisperRecipients("GM"),
      blind: !0,
      content: Ke(N.chat.blindMessageToGM, {
        user: game.user.name,
        message: e.content
      })
    });
  }
  static getUsers(e = (t) => !0) {
    return (game.version ? game.users : game.users.entities).filter(e);
  }
  static firstConnectedGM() {
    return Ei.getUsers((e) => e.isGM && e.active).sort(be.ascending((e) => e.id)).at(0) ?? {};
  }
  /**
   * @returns true pour un seul utilisateur: le premier GM connecté par ordre d'id
   */
  static isUniqueConnectedGM(e = game.user) {
    return e.id == Ei.firstConnectedGM().id;
  }
  static firstResponsible(e) {
    if (!(e != null && e.testUserPermission))
      return;
    const t = Ei.getUsers(
      (n) => n.active && e.testUserPermission(n, CONST.DOCUMENT_OWNERSHIP_LEVELS.OWNER)
    ).sort(be.ascending((n) => n.id)).at(0);
    return (t == null ? void 0 : t.id) === game.user.id ? e : void 0;
  }
  static getTargetTokens(e) {
    return Array.from(e.targets);
  }
  static getSelectedTokens(e) {
    return Array.from(canvas.tokens.controlled);
  }
  static getSelectedActors() {
    return Array.from(canvas.tokens.controlled).map((e) => e.actor);
  }
  static getPlayerActor() {
    return game.user.character;
  }
}
const ha = N.actor.monitors, Ji = N.actor.counters, Of = {
  armor: {
    path: "system.monitors.armor.value",
    monitor: (i) => i.system.monitors.armor,
    iconChecked: re.fontAwesome("fas fa-shield-slash"),
    iconUnchecked: re.fontAwesome("fas fa-shield-alt"),
    iconHit: re.fontAwesome("fas fa-bahai"),
    resource: ha.armor
  },
  fatigue: {
    path: "system.monitors.fatigue.value",
    monitor: (i) => i.system.monitors.fatigue,
    iconChecked: re.fontAwesome("fas fa-grimace"),
    iconUnchecked: re.fontAwesome("far fa-smile"),
    iconHit: re.fontAwesome("fas fa-bahai"),
    resource: ha.fatigue,
    overflow: (i) => A.monitors.physical,
    useArmor: !0
  },
  physical: {
    path: "system.monitors.physical.value",
    monitor: (i) => i.system.monitors.physical,
    iconChecked: re.fontAwesome("fas fa-heartbeat"),
    iconUnchecked: re.fontAwesome("far fa-heart"),
    iconHit: re.fontAwesome("fas fa-bahai"),
    resource: ha.physical,
    useArmor: !0
  },
  structure: {
    path: "system.monitors.structure.value",
    monitor: (i) => i.system.monitors.structure,
    iconChecked: re.fontAwesome("fas fa-car-crash"),
    iconUnchecked: re.fontAwesome("fas fa-car-alt"),
    iconHit: re.fontAwesome("fas fa-bahai"),
    resource: ha.structure
  },
  heat: {
    path: "system.monitors.heat.value",
    monitor: (i) => i.system.monitors.heat,
    iconChecked: re.fontAwesome("fas fa-fire"),
    iconUnchecked: re.fontAwesome("far fa-sun"),
    iconHit: re.fontAwesome("fas fa-temperature-high"),
    resource: ha.heat
  },
  criticals: {
    path: "system.hybrid.criticals.value",
    monitor: (i) => {
      var e;
      return ((e = i.system.hybrid) == null ? void 0 : e.criticals) ?? { value: 0, max: 0 };
    },
    iconChecked: re.fontAwesome("fas fa-bolt"),
    iconUnchecked: re.fontAwesome("far fa-dot-circle"),
    iconHit: re.fontAwesome("fas fa-exclamation-triangle"),
    resource: ha.structure
  },
  anarchy: {
    path: "system.counters.anarchy.value",
    monitor: (i) => ({
      value: i.system.counters.anarchy.value,
      max: 6
    }),
    iconChecked: re.iconPath(`${sr}/anarchy-point.webp`, "checkbar-img"),
    iconUnchecked: re.iconPath(`${sr}/anarchy-point-off.webp`, "checkbar-img"),
    resource: Ji.anarchy
  },
  plot: {
    path: "system.counters.anarchy.value",
    monitor: (i) => {
      const e = i.system.counters.anarchy.value;
      return { value: e, max: e + 1 };
    },
    iconChecked: re.iconPath(`${sr}/danger-point.webp`, "checkbar-img"),
    iconUnchecked: re.iconPath(`${sr}/danger-point-off.webp`, "checkbar-img"),
    resource: Ji.anarchy
  },
  sceneAnarchy: {
    path: "system.counters.edgePools.chaos.value",
    monitor: (i) => {
      const e = i.getEdgePoolValue(A.counters.edgePools.chaos), t = i.getAttributeValue(A.actorAttributes.edge);
      return { value: e, max: t };
    },
    iconChecked: re.iconPath("systems/mwd/icons/default/explosion.svg", "checkbar-img"),
    iconUnchecked: re.iconPath("systems/mwd/icons/default/explosion.svg", "checkbar-img"),
    resource: Ji.edgePools.chaos
  },
  grit: {
    path: "system.counters.edgePools.grit.value",
    monitor: (i) => ({ value: i.getEdgePoolValue(A.counters.edgePools.grit), max: i.getAttributeValue(A.actorAttributes.edge) }),
    iconChecked: re.iconPath("systems/mwd/icons/default/shield.svg", "checkbar-img"),
    iconUnchecked: re.iconPath("systems/mwd/icons/default/shield.svg", "checkbar-img"),
    resource: Ji.edgePools.grit
  },
  insight: {
    path: "system.counters.edgePools.insight.value",
    monitor: (i) => ({ value: i.getEdgePoolValue(A.counters.edgePools.insight), max: i.getAttributeValue(A.actorAttributes.edge) }),
    iconChecked: re.iconPath("systems/mwd/icons/default/eye.svg", "checkbar-img"),
    iconUnchecked: re.iconPath("systems/mwd/icons/default/eye.svg", "checkbar-img"),
    resource: Ji.edgePools.insight
  },
  legend: {
    path: "system.counters.edgePools.legend.value",
    monitor: (i) => ({ value: i.getEdgePoolValue(A.counters.edgePools.legend), max: i.getAttributeValue(A.actorAttributes.edge) }),
    iconChecked: re.iconPath("systems/mwd/icons/default/tower-flag.svg", "checkbar-img"),
    iconUnchecked: re.iconPath("systems/mwd/icons/default/tower-flag.svg", "checkbar-img"),
    resource: Ji.edgePools.legend
  },
  credibility: {
    path: "system.counters.edgePools.credibility.value",
    monitor: (i) => ({ value: i.getEdgePoolValue(A.counters.edgePools.credibility), max: i.getAttributeValue(A.actorAttributes.edge) }),
    iconChecked: re.iconPath("systems/mwd/icons/misc/hand.svg", "checkbar-img"),
    iconUnchecked: re.iconPath("systems/mwd/icons/misc/hand.svg", "checkbar-img"),
    resource: Ji.edgePools.credibility
  },
  rumor: {
    path: "system.counters.edgePools.rumor.value",
    monitor: (i) => ({ value: i.getEdgePoolValue(A.counters.edgePools.rumor), max: i.getAttributeValue(A.actorAttributes.edge) }),
    iconChecked: re.iconPath("systems/mwd/icons/default/mystery-man.svg", "checkbar-img"),
    iconUnchecked: re.iconPath("systems/mwd/icons/default/mystery-man.svg", "checkbar-img"),
    resource: Ji.edgePools.rumor
  }
}, Li = foundry.utils.mergeObject(Of, {});
class te {
  static init() {
    Handlebars.registerHelper("iconCheckbar", te.iconCheckbar), Handlebars.registerHelper("iconCheckbarHit", te.iconHit);
  }
  static hackCheckbars(e) {
    if (e) {
      const t = foundry.utils.mergeObject(Of, {});
      foundry.utils.mergeObject(t, e, { recursive: !0 }), foundry.utils.mergeObject(Li, t, { overwrite: !0 });
    }
  }
  static iconCheckbar(e, t) {
    return t ? te.iconChecked(e) : te.iconUnchecked(e);
  }
  static iconChecked(e) {
    var t;
    return (t = Li[e]) == null ? void 0 : t.iconChecked;
  }
  static iconUnchecked(e) {
    var t;
    return (t = Li[e]) == null ? void 0 : t.iconUnchecked;
  }
  static iconHit(e) {
    var t, n;
    return ((t = Li[e]) == null ? void 0 : t.iconHit) ?? ((n = Li[e]) == null ? void 0 : n.iconChecked);
  }
  static useArmor(e) {
    var t;
    return (t = Li[e]) == null ? void 0 : t.useArmor;
  }
  static max(e, t) {
    var a;
    const n = (a = Li[t]) == null ? void 0 : a.monitor(e);
    return ((n == null ? void 0 : n.max) ?? 0) + ((n == null ? void 0 : n.maxBonus) ?? 0);
  }
  static value(e, t) {
    var a;
    const n = (a = Li[t]) == null ? void 0 : a.monitor(e);
    return (n == null ? void 0 : n.value) ?? 0;
  }
  static resistance(e, t, n = void 0) {
    return te.resistanceDetail(e, t, n).value;
  }
  static resistanceDetail(e, t, n = void 0) {
    var l, c;
    const a = (l = Li[t]) == null ? void 0 : l.monitor(e), r = te._resolveResistance(a == null ? void 0 : a.resistance, n), s = te._resolveResistance(a == null ? void 0 : a.resistanceBonus, n), o = n === void 0 ? 0 : Number(((c = a == null ? void 0 : a.resistanceBonusByType) == null ? void 0 : c[n]) ?? 0);
    return {
      value: r.value + s.value + o,
      damageType: n,
      source: r.source,
      bonusSource: s.source,
      bonusByType: o,
      usedType: r.source === "type" || s.source === "type" || o !== 0
    };
  }
  static _resolveResistance(e, t = void 0) {
    var r;
    if (typeof e == "number")
      return { value: e ?? 0, source: "legacy" };
    const n = t !== void 0 ? (r = e == null ? void 0 : e.byType) == null ? void 0 : r[t] : void 0;
    return n !== void 0 ? { value: Number(n) || 0, source: "type" } : { value: Number((e == null ? void 0 : e.default) ?? 0) || 0, source: "default" };
  }
  static newValue(e, t) {
    return e + (t ? 0 : 1);
  }
  static async switchMonitorCheck(e, t, n, a, r = void 0, s = void 0) {
    await te.setCounter(e, t, te.newValue(n, a), r, s);
  }
  static async addCounter(e, t, n, a = void 0) {
    if (n != 0) {
      const r = te.getCounterValue(e, t, a) ?? 0;
      await te.setCounter(e, t, r + n, a);
    }
  }
  static async setCounter(e, t, n, a = void 0, r = void 0) {
    switch (t) {
      case A.monitors.anarchy:
        return await te.setAnarchy(e, n);
      case A.monitors.sceneAnarchy:
        return await te.setSceneAnarchy(e, n);
    }
    return await te.setCheckbar(e, t, n);
  }
  static getCounterValue(e, t, n) {
    switch (t) {
      case A.monitors.anarchy:
        return te.getAnarchy(e, t);
    }
    return te.value(e, t);
  }
  static async setCheckbar(e, t, n) {
    if (n == te.getCounterValue(e, t))
      return;
    const a = Li[t];
    if (a.path) {
      const r = te.max(e, t);
      if (r <= 0)
        return;
      await te._manageOverflow(a, e, t, n, r), n = Math.min(n, r), la.checkOutOfRange(a.resource, n, 0, r), await e.setCheckbarValue(a.path, n);
    }
  }
  static async _manageOverflow(e, t, n, a, r) {
    if (a > r) {
      const s = e.overflow ? e.overflow(t) : void 0, o = e.recomputeOverflow ? e.recomputeOverflow(a - r) : a - r;
      s && o > 0 && (te._notifyOverflow(t, n, o, s), await te.addCounter(t, s, o));
    }
  }
  static _notifyOverflow(e, t, n, a) {
    const r = Ke(N.actor.monitors.overflow, {
      actor: e.name,
      monitor: N.actor.monitors[t],
      overflow: n,
      overflowMonitor: N.actor.monitors[a]
    });
    ui.notifications.warn(r);
  }
  static async _manageFatigueOverflow(e, t, n) {
    await te.addCounter(e, A.monitors.physical, t - n);
  }
  static async setAnarchy(e, t) {
    if (e.hasOwnAnarchy()) {
      if (e.hasGMAnarchy()) {
        await game.system.anarchy.gmAnarchy.setAnarchy(t), e.render();
        return;
      }
      await te._setAnarchyMonitor(e, A.monitors.anarchy, t);
    }
  }
  static async setSceneAnarchy(e, t) {
    await te._setAnarchyMonitor(e, A.monitors.sceneAnarchy, t);
  }
  static async _setAnarchyMonitor(e, t, n) {
    const a = te.value(e, t);
    await te.setCheckbar(e, t, n), game.user.isGM || te.notifyAnarchyChange(e, t, a, n);
  }
  static getAnarchy(e, t) {
    return !game.user.isGM && (!e.hasOwnAnarchy() || e.hasGMAnarchy()) || t == Ji.anarchy && (!e.hasOwnAnarchy() || e.hasGMAnarchy()) ? 0 : te.value(e, t);
  }
  static notifyAnarchyChange(e, t, n, a) {
    Ei.blindMessageToGM({
      from: game.user.id,
      content: Ke(
        N.gmManager.playerChangedAnarchy,
        {
          user: game.user.name,
          actor: e.name,
          monitor: N.actor.counters[t],
          from: n,
          to: a
        }
      )
    });
  }
}
const { loadTemplates: kb, renderTemplate: Mb } = foundry.applications.handlebars, rd = {
  highlighted: ["far fa-times-circle", "fas fa-dice-one", "fas fa-dice-two", "fas fa-dice-three", "fas fa-dice-four", "fas fa-dice-five", "fas fa-dice-six"],
  dimmed: ["far fa-times-circle", "far fa-dice-one", "far fa-dice-two", "far fa-dice-three", "far fa-dice-four", "far fa-dice-five", "far fa-dice-six"]
};
class Zi {
  static init() {
    Hooks.once("ready", async () => await this.onReady()), Handlebars.registerHelper("dice-cursor-array", (e, t) => Zi.array(e ?? 0, t ?? 5)), Handlebars.registerHelper("dice-cursor-fas", (e, t) => Zi.fasClass(e, t)), Handlebars.registerHelper("dice-cursor-active", (e, t) => Zi.activeClass(e, t)), Handlebars.registerHelper("dice-cursor-color", (e, t) => Zi.colorClass(e, t));
  }
  static async onReady() {
    await kb([
      "systems/mwd/templates/roll/parts/dice-cursor.hbs"
    ]);
  }
  static array(e, t) {
    if (e > t) throw `min>max: ${e} > ${t}`;
    return Array(t - e + 1).fill().map((n, a) => e + a);
  }
  static isActive(e, t) {
    return t <= e && e < 0 || 0 < e && e <= t;
  }
  static activeClass(e, t) {
    return Zi.isActive(e, t) ? "active" : "inactive";
  }
  static fasClass(e, t) {
    const n = Zi.isActive(e, t) ? rd.highlighted : rd.dimmed;
    return Zi.$getFas(n, Math.abs(e));
  }
  static colorClass(e, t) {
    return e == 0 || !t ? e < 0 ? "fixed-dice-malus" : "fixed-dice-bonus" : e < 0 ? "variable-dice-malus" : "variable-dice-bonus";
  }
  static $getFas(e, t) {
    return e[t > 6 ? t % 6 : t];
  }
  static async diceCursor({ value: e, min: t, max: n, editable: a }) {
    return await Mb("systems/mwd/templates/roll/parts/dice-cursor.hbs", {
      value: e,
      min: t,
      max: n,
      editable: a
    });
  }
}
const ii = {
  /**
   * Hook to declare template data migrations
   */
  DECLARE_MIGRATIONS: "anarchy-declareMigration",
  /**
   * Hook used to declare additional styles available
   */
  REGISTER_STYLES: "anarchy-registerStyles",
  /**
   * Hook allowing to register additional roll parameters
   */
  REGISTER_ROLL_PARAMETERS: "anarchy-registerRollParameters",
  /**
   * Hook allowing to modify some parameters (from Anarchy hacks modules).
   * Setting property ignore=true allows to remove the parameter.
   */
  MODIFY_ROLL_PARAMETER: "anarchy-forbidRollParameter",
  /**
   * Hook allowing to provide alternate skill sets for Anarchy hack modules
   */
  PROVIDE_SKILL_SET: "anarchy-provideSkillSet",
  /**
   * Hook allowing to provide alternate way to apply damages for Anarchy hack modules
   */
  PROVIDE_DAMAGE_MODE: "anarchy-provideDamageMode"
}, sd = "anarchy-";
globalThis.ANARCHY_HOOKS = ii;
class ea {
  constructor() {
    this.hooks = [], Hooks.on("getSceneControlButtons", (e) => {
      var n;
      if (!((n = game.user) != null && n.isGM)) return;
      const t = Array.isArray(e) ? e.find((a) => a.name === "notes") : e == null ? void 0 : e.notes;
      if (!t) {
        const a = Array.isArray(e) ? e.map((r) => r.name) : Object.keys(e ?? {});
        console.warn("MWD: token controls not found. Available:", a);
        return;
      }
      t.tools = t.tools ?? {}, !t.tools["mwd-gm-gadget"] && (t.tools["mwd-gm-gadget"] = {
        name: "mwd-gm-gadget",
        title: "Open GM Gadget",
        icon: "fa-solid fa-sliders",
        order: 990,
        button: !0,
        visible: !0,
        onChange: () => {
          var a, r;
          return (r = (a = game.mwd) == null ? void 0 : a.gmGadget) == null ? void 0 : r.call(a);
        }
      });
    });
  }
  static instance() {
    return game.system.anarchy.hooks;
  }
  static register(e) {
    ea.instance()._register(e);
  }
  _register(e) {
    if (console.log(Le + "HooksManager.register", e), !e.startsWith(sd))
      throw `For safety Anarchy Hooks names must be prefixed by '${sd}'`;
    this.hooks.push(e);
  }
}
const od = [
  A.itemType.assetModule,
  A.itemType.mechWeapon,
  A.itemType.personalWeapon,
  "weapon"
];
class Te {
  constructor() {
    this.modifiers = {
      groups: Oe.mapObjetToKeyValue(N.modifier.group, "key", "label"),
      roll: Te._buildGroupOptions("roll"),
      attribute: Te._buildGroupOptions("attribute"),
      monitor: Te._buildGroupOptions("monitor"),
      other: Te._buildGroupOptions("other")
    }, Hooks.once("ready", () => this.onReady());
  }
  static _buildGroupOptions(e) {
    switch (e) {
      case "attribute":
        return {
          label: N.modifier.group[e],
          effects: Oe.hbsAttributes.map((t) => ({ key: t.value, label: t.labelkey })),
          categories: []
        };
    }
    return {
      label: N.modifier.group[e],
      effects: Oe.mapObjetToKeyValue(N.modifier[e].effect, "key", "label"),
      categories: Oe.mapObjetToKeyValue(N.modifier[e].category, "key", "label")
    };
  }
  async onReady() {
    Handlebars.registerHelper("modifierHasSubCategory", (e, t, n) => this.hasSubCategory(e, t, n)), Handlebars.registerHelper("modifierSelectOption", (e, t) => this.getSelectOptions(e, t));
  }
  hasSubCategory(e, t, n) {
    switch (e) {
      case "roll":
        return !0;
      case "monitor":
        return n === "resistanceByType";
    }
    return !1;
  }
  getSelectOptions(e, t) {
    var n, a;
    switch (e) {
      case "group":
        return this.modifiers.groups;
      case "effect":
        return (n = this.modifiers[t.hash.group]) == null ? void 0 : n.effects;
      case "category":
        return (a = this.modifiers[t.hash.group]) == null ? void 0 : a.categories;
      case "subCategory":
        switch (t.hash.group) {
          case "roll":
            return this.getSelectRollSubCategories(t.hash.category);
          case "monitor": {
            switch (t.hash.category) {
              case "resistanceByType":
                return Oe.getDamageTypes().map((r) => ({ key: r.value, label: r.labelkey }));
            }
            return [];
          }
        }
        return [];
    }
    return [];
  }
  getSelectRollSubCategories(e) {
    switch (e) {
      case "attribute":
        return Oe.getAttributes().map((n) => ({ key: n.value, label: n.labelkey }));
      case "skill":
        return game.system.anarchy.skills.getSkills().map((n) => ({ key: n.code, label: n.labelkey }));
      case "attributeAction":
        const t = it.all().map((n) => ({ key: n.code, label: n.labelkey }));
        return be.distinct(t.map((n) => n.key)).map((n) => t.find((a) => a.key == n));
    }
    return [];
  }
  getEnums() {
    return { modifiers: this.modifiers };
  }
  static buildRollModifiersFilter(e, t) {
    return (n) => {
      var a;
      if (n.group == "roll" && n.effect == t)
        switch (n.category) {
          case "attribute":
            return [e.attribute1, e.attribute2].includes(n.subCategory);
          case "skill":
            return n.subCategory == ((a = e.skill) == null ? void 0 : a.system.code);
          case "attributeAction":
            return n.subCategory == e.attributeAction || n.subCategory == it.getDefenseAttributeAction(e.defenseAction);
        }
      return !1;
    };
  }
  static computeRollModifiers(e, t, n) {
    const a = Te.buildRollModifiersFilter(t, n), r = (c) => c.group == "roll" && c.effect == n && a(c), s = Te._activeItems(e).map((c) => Te.itemModifiers(c, r)).reduce((c, u) => c.concat(u), []).sort(be.descending((c) => c.modifier.value)), o = Te.$sumAssetModuleModifiers(s.filter((c) => od.includes(c.item.type)).map((c) => c.modifier.value)), l = be.sumValues(s.filter((c) => !od.includes(c.item.type)).map((c) => c.modifier.value));
    return {
      value: o + l,
      sources: s
    };
  }
  static $sumAssetModuleModifiers(e) {
    const t = e.find((r) => r > 3) ?? 0, n = be.sumValues(e.filter((r) => r < 0)), a = Math.min(3, be.sumValues(e.filter((r) => r > 0 && r <= 3)));
    return n + Math.max(a, t);
  }
  static computeModifiers(e, t, n = void 0, a = void 0) {
    const r = Te._createFilter(t, n, a), s = Te._activeItems(e).map((l) => Te.itemModifiers(l, r)).reduce((l, c) => l.concat(c), []);
    return {
      value: be.sumValues(s, (l) => l.modifier.value),
      sources: s
    };
  }
  static sumMonitorModifiers(e, t, n, a = void 0) {
    return Te.sumModifiers(Te._activeItems(e), "monitor", t, n, a);
  }
  static sumModifiers(e, t, n, a, r = void 0) {
    const s = Te._createFilter(t, n, a, r), o = Te._activeItems(e).map((l) => Te.itemModifiers(l, s)).reduce((l, c) => l.concat(c), []);
    return be.sumValues(o, (l) => l.modifier.value);
  }
  static _createFilter(e, t, n, a = void 0) {
    return (r) => r.group == e && r.effect == (t ?? r.effect) && r.category == (n ?? r.category) && (a == null ? !0 : r.subCategory == a);
  }
  static countModifiers(e, t, n = void 0, a = void 0) {
    const r = Te._createFilter(t, n, a);
    return Te._activeItems(e).map((o) => Te.itemModifiers(o, r)).reduce((o, l) => o.concat(l), []).count;
  }
  static itemModifiers(e, t) {
    return Te._listItemModifiers(e, t).map((n) => Te._itemModifier(e, n));
  }
  static _listItemModifiers(e, t = (n) => !0) {
    return (e.system.modifiers ?? []).filter(t);
  }
  static _itemModifier(e, t) {
    return {
      item: e,
      modifier: t
    };
  }
  static _activeItems(e) {
    return e;
  }
}
const { loadTemplates: Ho, renderTemplate: kR } = foundry.applications.handlebars, Pe = {
  pool: "pool",
  reroll: "reroll",
  rerollMax: "rerollMax",
  rerollForced: "rerollForced",
  successReroll: "successReroll",
  glitch: "glitch",
  edge: "edge",
  risk: "risk",
  opponentPool: "opponentPool",
  opponentReroll: "opponentReroll"
}, ld = 4, Cb = [
  // attribute1
  {
    code: "attribute1",
    options: {
      order: 1,
      category: Pe.pool,
      hbsTemplateRoll: `${se}/roll/parts/select-attribute.hbs`
    },
    condition: (i) => Object.values(jt.rollType).includes(i.mode),
    isUsed: (i) => !0,
    factory: (i) => {
      var t;
      const e = i.attribute1 ?? ((t = i.skill) == null ? void 0 : t.system.attribute);
      return {
        labelkey: e ? N.attributes[e] : N.attributes.noAttributes,
        value: i.actor.getAttributeValue(e, i.activeItem),
        flags: { editable: i.skill },
        selected: e,
        choices: Oe.getAttributes((n) => i.attributes.includes(n))
      };
    }
  },
  // attribute2
  {
    code: "attribute2",
    options: {
      order: 1,
      category: Pe.pool,
      hbsTemplateRoll: `${se}/roll/parts/select-attribute.hbs`,
      hbsTemplateChat: `${se}/chat/parts/pool-attribute2.hbs`
    },
    condition: (i) => [jt.rollType.attribute, jt.rollType.attributeAction, jt.rollType.defense].includes(i.mode),
    isUsed: (i) => i.used,
    onChecked: (i, e) => i.used = !!e,
    factory: (i) => {
      const e = i.attribute2;
      return {
        labelkey: e ? N.attributes[e] : N.attributes.noAttributes,
        value: i.actor.getAttributeValue(e, i.activeItem),
        flags: { editable: jt.rollType.attribute == i.mode },
        selected: e,
        choices: Oe.getAttributes((t) => i.attributes.includes(t))
      };
    }
  },
  // skill
  {
    code: "skill",
    options: {
      flags: {},
      order: 3,
      category: Pe.pool,
      hbsTemplateRoll: `${se}/roll/parts/input-numeric.hbs`
    },
    condition: (i) => ["skill", "weapon"].includes(i.mode),
    factory: (i) => {
      var t, n, a, r;
      const e = (t = i.actor) != null && t.getSkillRating ? i.actor.getSkillRating(i.skill) : ((a = (n = i.skill) == null ? void 0 : n.system) == null ? void 0 : a.value) ?? 0;
      return {
        label: (r = i.skill) == null ? void 0 : r.name,
        value: e
      };
    }
  },
  // specialization
  {
    code: "specialization",
    options: {
      flags: { optional: !0 },
      value: 2,
      order: 4,
      category: Pe.pool,
      hbsTemplateRoll: `${se}/roll/parts/check-option.hbs`
    },
    isUsed: (i) => i.used,
    condition: (i) => !!i.specialization,
    onChecked: (i, e) => {
      i.used = e, i.value = e ? 2 : 0;
    },
    factory: (i) => ({
      label: i.specialization,
      used: i.specialization != null,
      value: 2
    })
  },
  // credibility usage
  {
    code: "credibility",
    options: {
      flags: { editDice: !0, editable: !0 },
      order: 5,
      category: Pe.pool,
      value: 0,
      labelkey: N.common.roll.modifiers.social.credibility,
      hbsTemplateRoll: `${se}/roll/parts/input-numeric.hbs`
    },
    condition: (i) => {
      var e;
      return ((e = i.skill) == null ? void 0 : e.system.isSocial) && i.actor.getCredibilityValue() > 0;
    },
    factory: (i) => ({
      min: 0,
      max: i.actor.getCredibilityValue()
    })
  },
  // modifiers bonus
  {
    code: "poolModifiers",
    options: {
      flags: { editDice: !0, editable: !0 },
      labelkey: N.common.roll.modifiers.poolModifiers,
      order: 5,
      category: Pe.pool,
      hbsTemplateRoll: `${se}/roll/parts/input-numeric.hbs`,
      min: -4,
      max: 4
    },
    factory: (i) => Bn.computeRollModifiers(Pe.pool, i)
  },
  // wounds
  {
    code: "wounds",
    options: {
      flags: { optional: !0 },
      order: 10,
      category: Pe.pool,
      labelkey: N.common.roll.modifiers.wounds,
      hbsTemplateRoll: `${se}/roll/parts/input-numeric.hbs`
    },
    isUsed: (i) => i.used,
    condition: (i) => i.actor.getWounds(),
    onChecked: (i, e) => {
      i.used = e, i.value = e ? -i.wounds : 0;
    },
    factory: (i) => {
      const e = i.actor.getWounds();
      return {
        wounds: e,
        min: -e,
        max: 0,
        value: -e,
        used: !0
      };
    }
  },
  // other modifiers
  {
    code: "other",
    options: {
      flags: { editDice: !0, editable: !0 },
      order: 25,
      category: Pe.pool,
      value: 0,
      labelkey: N.common.roll.modifiers.other,
      hbsTemplateRoll: `${se}/roll/parts/input-numeric.hbs`,
      min: -5,
      max: 5
    }
  },
  // glitch
  {
    code: "glitch",
    options: {
      flags: { editDice: !0, editable: !0 },
      order: 40,
      category: Pe.glitch,
      value: 0,
      labelkey: N.common.roll.modifiers.glitch,
      hbsTemplateRoll: `${se}/roll/parts/input-numeric.hbs`,
      hbsTemplateChat: `${se}/chat/parts/glitch.hbs`,
      min: 0,
      max: 5
    },
    isUsed: (i) => i.value > 0,
    factory: (i) => {
      const e = i.actor.getWounds(), t = Bn.computeRollModifiers(Pe.glitch, i);
      return {
        value: (e == 0 ? 0 : 1) + (i.glitch ?? 0) + t.value
      };
    }
  },
  // social rumor
  {
    code: "rumor",
    options: {
      flags: { editDice: !0, editable: !0 },
      order: 50,
      category: Pe.glitch,
      value: 0,
      labelkey: N.common.roll.modifiers.social.rumor,
      hbsTemplateRoll: `${se}/roll/parts/input-numeric.hbs`,
      hbsTemplateChat: `${se}/chat/parts/glitch.hbs`,
      min: 0,
      max: 1
    },
    condition: (i) => {
      var e;
      return ((e = i.skill) == null ? void 0 : e.system.isSocial) && i.actor.getRumorValue() > 0;
    },
    factory: (i) => ({
      max: i.actor.getRumorValue()
    })
  },
  // rerolls
  {
    code: "reroll",
    options: {
      flags: { editDice: !0, editable: !0 },
      order: 30,
      category: Pe.reroll,
      labelkey: N.common.roll.modifiers.reroll,
      hbsTemplateRoll: `${se}/roll/parts/input-numeric.hbs`,
      min: 0,
      max: ld
    },
    factory: (i) => {
      const e = Bn.computeRollModifiers(Pe.reroll, i), t = Bn.computeRollModifiers(Pe.rerollMax, i);
      return foundry.utils.mergeObject(e, {
        max: ld + Math.max(0, t.value ?? 0)
      });
    }
  },
  // reduction from opponent
  {
    code: "reduced",
    options: {
      order: 29,
      category: Pe.pool,
      labelkey: N.common.roll.modifiers.reduced,
      hbsTemplateRoll: `${se}/roll/parts/input-numeric.hbs`,
      min: -4,
      max: 0
    },
    condition: (i) => {
      var e;
      return (((e = i.attackRoll) == null ? void 0 : e.param.opponentPool) ?? 0) != 0;
    },
    factory: (i) => {
      var t;
      const e = -(((t = i.attackRoll) == null ? void 0 : t.param.opponentPool) ?? 0);
      return {
        flags: { editDice: !0, used: !0 },
        value: e
      };
    }
  },
  // forced success rerolls
  {
    code: "rerollForced",
    options: {
      order: 31,
      category: Pe.rerollForced,
      labelkey: N.common.roll.modifiers.rerollForced,
      hbsTemplateRoll: `${se}/roll/parts/input-numeric.hbs`,
      min: -5,
      max: 0
    },
    factory: (i) => {
      var t;
      const e = Bn.computeRollModifiers(Pe.successReroll, i);
      return e.value = -e.value - (((t = i.attackRoll) == null ? void 0 : t.param.opponentReroll) ?? 0), foundry.utils.mergeObject(e, {
        flags: { editDice: !0, used: !0, editable: !0 }
      });
    }
  },
  // anarchy dispositions
  {
    code: "anarchyDisposition",
    options: {
      flags: { optional: !0, isAnarchy: !0, forceDisplay: !0 },
      order: 70,
      category: Pe.pool,
      value: 0,
      min: 0,
      max: 3,
      labelkey: N.common.roll.modifiers.anarchyDisposition,
      hbsTemplateRoll: `${se}/roll/parts/check-option.hbs`
    },
    isUsed: (i) => i.used,
    condition: (i) => i.actor.getAnarchyValue() > 0,
    onChecked: (i, e) => {
      i.used = e, i.value = e ? 3 : 0;
    }
  },
  // anarchy take risks
  {
    code: "anarchyRisk",
    options: {
      flags: { optional: !0, isAnarchy: !0, forceDisplay: !0 },
      order: 70,
      category: Pe.risk,
      value: 0,
      labelkey: N.common.roll.modifiers.anarchyRisk,
      hbsTemplateRoll: `${se}/roll/parts/check-option.hbs`,
      hbsTemplateChat: `${se}/chat/parts/anarchy-risk.hbs`
    },
    isUsed: (i) => i.used,
    condition: (i) => i.actor.getAnarchyValue() > 0,
    onChecked: (i, e) => {
      i.used = e, i.value = e ? 1 : 0;
    }
  },
  // edge
  {
    code: "edge",
    options: {
      flags: { optional: !0, forceDisplay: !0 },
      value: 0,
      order: 70,
      category: Pe.edge,
      labelkey: N.common.roll.modifiers.edge,
      hbsTemplateRoll: `${se}/roll/parts/check-option.hbs`
    },
    isUsed: (i) => i.used,
    condition: (i) => i.options.canUseEdge && i.actor.getRemainingEdge(),
    onChecked: (i, e) => {
      i.used = e, i.value = e ? 1 : 0;
    },
    factory: (i) => {
      var a;
      const t = [
        A.counters.edgePools.grit,
        A.counters.edgePools.chaos,
        A.counters.edgePools.insight,
        A.counters.edgePools.rumor,
        A.counters.edgePools.legend,
        A.counters.edgePools.credibility
      ].map((r) => {
        const s = i.actor.getEdgePoolValue(r);
        return {
          code: r,
          label: N.actor.counters.edgePools[r] ?? r,
          value: s
        };
      }), n = ((a = t.find((r) => r.value > 0)) == null ? void 0 : a.code) ?? A.counters.edgePools.grit;
      return {
        edgePools: t,
        pool: n
      };
    }
  },
  // reduce opponent pool
  {
    code: "opponentPool",
    options: {
      flags: { editDice: !0, editable: !0, forceDisplay: !0 },
      order: 100,
      category: Pe.opponentPool,
      labelkey: N.common.roll.modifiers.opponentPool,
      hbsTemplateRoll: `${se}/roll/parts/input-numeric.hbs`,
      min: 0,
      max: 4
    },
    factory: (i) => Bn.computeRollModifiers(Pe.opponentPool, i),
    condition: (i) => !i.attributeAction
  },
  // force opponent rerolls
  {
    code: "opponentReroll",
    options: {
      flags: { editDice: !0, editable: !0, forceDisplay: !0 },
      order: 100,
      category: Pe.opponentReroll,
      value: 0,
      labelkey: N.common.roll.modifiers.opponentReroll,
      hbsTemplateRoll: `${se}/roll/parts/input-numeric.hbs`,
      min: 0,
      max: 4
    },
    factory: (i) => Bn.computeRollModifiers(Pe.opponentReroll, i),
    condition: (i) => !i.attributeAction
  }
];
class Bn {
  constructor() {
    this.registeredParameters = {}, ea.register(ii.REGISTER_ROLL_PARAMETERS), ea.register(ii.MODIFY_ROLL_PARAMETER), Hooks.on(ii.MODIFY_ROLL_PARAMETER, (e) => this._validate(e)), Hooks.once(ii.REGISTER_ROLL_PARAMETERS, (e) => Cb.forEach(
      (t) => e(t)
    )), Hooks.once("ready", () => this.onReady());
  }
  async onReady() {
    Hooks.callAll(ii.REGISTER_ROLL_PARAMETERS, async (t) => {
      Hooks.callAll(ii.MODIFY_ROLL_PARAMETER, t), t.ignore || await this._register(t);
    });
    const e = be.distinct([].concat(Object.values(this.registeredParameters).map((t) => t.options.hbsTemplateRoll)).concat(Object.values(this.registeredParameters).map((t) => t.options.hbsTemplateChat)).filter((t) => typeof t == "string" && t.length > 0));
    await Ho(be.distinct(e)), await Ho([`${se}/roll/parts/parameter-label.hbs`]);
  }
  _validate(e) {
    e.code || (console.error(`${Le} RollParameter does not have a code`, e), e.ignore = !0);
  }
  async _register(e) {
    if (this.registeredParameters[e.code]) {
      console.error(`${Le} RollParameter ${e.code} is already registered`, e);
      return;
    }
    e.onChecked || (e.onChecked = (t, n) => t.used = n), e.onValue = (t, n) => t.value = n, this.registeredParameters[e.code] = e;
  }
  async _optionalLoadTemplate(e) {
    e && await Ho([e]);
  }
  build(e) {
    return Object.values(this.registeredParameters).filter((t) => !t.condition || t.condition(e)).map((t) => this._computeParameter(t, e));
  }
  compute(e) {
    const t = e.filter((r) => this.isParameterUsed(r)), n = be.classify(t, (r) => r.category), a = {};
    return Object.values(n).forEach((r) => a[r[0].category] = be.sumValues(r, (s) => s.value ?? (s.optional ? 1 : 0))), a;
  }
  isParameterUsed(e) {
    const t = this.findParameter(e.code);
    return (t == null ? void 0 : t.isUsed) != null ? t.isUsed(e) : e.value != null ? e.value != 0 : (console.error(`registered parameter ${t.code} does not have isUsed method`, t), !1);
  }
  findParameter(e) {
    return this.registeredParameters[e];
  }
  _computeParameter(e, t) {
    const n = {
      code: e.code,
      onChecked: e.onChecked,
      onValue: e.onValue,
      isUsed: e.isUsed
    };
    return foundry.utils.mergeObject(n, e.options), e.factory && foundry.utils.mergeObject(n, e.factory(t, e.options)), foundry.utils.mergeObject(n, {
      used: n.used || n.value,
      min: n.min ?? 0,
      max: n.max ?? n.value ?? 0
    }), n;
  }
  static computeRollModifiers(e, t) {
    const n = (r) => {
      var s;
      return !((s = r.isWeapon) != null && s.call(r)) || t.weapon && r.id == t.weapon.id;
    }, a = t.actor.items.filter(n);
    return Te.computeRollModifiers(a, t, e);
  }
}
const { ApplicationV2: vb, HandlebarsApplicationMixin: Eb } = foundry.applications.api, { loadTemplates: Ib, renderTemplate: Nb } = foundry.applications.handlebars;
var Zs, Lf;
const Tt = class Tt extends Eb(vb) {
  static get DEFAULT_OPTIONS() {
    return foundry.utils.mergeObject(super.DEFAULT_OPTIONS, {
      id: "roll-dialog",
      classes: ["anarchy-dialog"],
      position: { width: 500, height: "auto" },
      window: {
        resizable: !0,
        minimizable: !0
      }
    }, { inplace: !1 });
  }
  static init() {
    Hooks.once("ready", async () => await this.onReady());
  }
  static async onReady() {
    await Ib([
      "systems/mwd/templates/roll/roll-parameters-category.hbs",
      "systems/mwd/templates/roll/parts/generic.hbs",
      "systems/mwd/templates/roll/parts/image-attribute.hbs",
      "systems/mwd/templates/roll/parts/image-attributeAction.hbs",
      "systems/mwd/templates/roll/parts/image-defense.hbs",
      "systems/mwd/templates/roll/parts/image-skill.hbs",
      "systems/mwd/templates/roll/parts/image-weapon.hbs"
    ]);
  }
  static prepareActorRoll(e, t = void 0) {
    var n;
    return {
      actor: e,
      tokenId: (n = e.token) == null ? void 0 : n.id,
      attributes: e.getUsableAttributes(t),
      options: {
        canUseEdge: e.canUseEdge()
      }
    };
  }
  static async rollAttribute(e, t) {
    const n = foundry.utils.mergeObject(Tt.prepareActorRoll(e), {
      mode: jt.rollType.attribute,
      attribute1: t
    });
    await Tt.create(n);
  }
  static async rollAttributeAction(e, t) {
    const n = foundry.utils.mergeObject(Tt.prepareActorRoll(e), {
      mode: jt.rollType.attributeAction,
      attributeAction: t.code,
      attribute1: t.attributeFunction1(e),
      attribute2: t.attributeFunction2(e)
    });
    await Tt.create(n);
  }
  static async rollSkill(e, t, n) {
    const a = foundry.utils.mergeObject(Tt.prepareActorRoll(e), {
      mode: jt.rollType.skill,
      skill: t,
      attribute1: (t == null ? void 0 : t.system.attribute) ?? A.actorAttributes.reflexes,
      specialization: n
    });
    await Tt.create(a);
  }
  static async rollWeapon(e, t, n, a) {
    const r = foundry.utils.mergeObject(Tt.prepareActorRoll(e), {
      mode: jt.rollType.weapon,
      weapon: n,
      skill: t,
      attribute1: (t == null ? void 0 : t.system.attribute) ?? e.getPhysicalAgility(),
      specialization: void 0,
      targeting: a
    });
    await Tt.create(r);
  }
  static async rollDefense(e, t, n) {
    const a = foundry.utils.mergeObject(Tt.prepareActorRoll(e), {
      mode: jt.rollType.defense,
      attribute1: t.attributeFunction1(e),
      attribute2: t.attributeFunction2(e),
      defenseAction: t.code,
      attackRoll: n.attackRoll,
      tokenId: n.defenderTokenId,
      choiceChatMessageId: n.choiceChatMessageId
    });
    await Tt.create(a);
  }
  static async itemAttributeRoll(e, t) {
    const n = foundry.utils.mergeObject(Tt.prepareActorRoll(e.actor), {
      mode: jt.rollType.attribute,
      item: e,
      attribute1: t,
      attributes: e.actor.getUsableAttributes(e)
    });
    await Tt.create(n);
  }
  static async create(e) {
    var s;
    const t = v(s = Tt, Zs, Lf).call(s, e), n = await Nb(`${se}/roll/roll-dialog-title.hbs`, t), a = {
      id: `roll-dialog-${foundry.utils.randomID()}`,
      classes: [game.system.anarchy.styles.selectCssClass(), ...Tt.DEFAULT_OPTIONS.classes],
      window: { title: n }
    };
    return new Tt({ roll: t }, a).render({ force: !0 });
  }
  constructor(e = {}, t = {}) {
    super(e, t), this.roll = e.roll;
  }
  async _prepareContext() {
    return this.roll;
  }
  async activateListeners(e) {
    const t = e instanceof HTMLElement ? e : e[0];
    await super.activateListeners(t), this.html = t instanceof HTMLElement ? $(t) : e, this.html.find(".select-attribute-parameter").change(async (n) => {
      const a = this._getRollParameter(n), r = this._getEventItem(n, this.roll.actor), s = n.currentTarget.value, o = this.roll.actor.getAttributeValue(s, r);
      this.roll[a.code] = s, await this._setParameterSelectedOption(a, s, o);
    }), this.html.find(".check-optional").click(async (n) => {
      const a = this._getRollParameter(n);
      a.onChecked(a, n.currentTarget.checked), a.category == Pe.pool && await this._updateParameterValue(a, a.value), a.code == "edge" && this.html.find(`.parameter[data-parameter-code='${a.code}'] .edge-pool-select`).prop("disabled", !a.used);
    }), this.activateDiceParameterClick(), this.html.find("input.parameter-value:not(:disabled)").on("input", async (n) => {
      const a = this._getRollParameter(n), r = Number.parseInt(n.currentTarget.value) ?? 0;
      await this._updateParameterValue(a, r);
    }), this.html.find(".select-option-parameter").change(async (n) => {
      const a = this._getRollParameter(n), r = n.currentTarget.value, s = Number.parseInt(r);
      await this._setParameterSelectedOption(a, r, s);
    }), this.html.find(".edge-pool-select").change(async (n) => {
      const a = this._getRollParameter(n);
      a.pool = n.currentTarget.value;
    }), this.html.find('[data-action="roll"]').on("click", async (n) => {
      n.preventDefault(), await game.system.anarchy.rollManager.roll(this.roll), await this.close();
    }), this.html.find('[data-action="cancel"]').on("click", async (n) => {
      n.preventDefault(), await this.close();
    });
  }
  activateDiceParameterClick() {
    this.html.find(".input-cursor-parameter a").click(async (e) => {
      var n;
      const t = this._getRollParameter(e);
      if ((n = t.flags) != null && n.editDice) {
        const a = Number.parseInt(this.html.find(e.currentTarget).attr("data-dice")) ?? 0, r = t.value != a || a == 0 ? a : a > 0 ? a - 1 : a + 1;
        await this._updateParameterValue(t, r);
      }
    });
  }
  async _setParameterSelectedOption(e, t, n) {
    e.onChecked(e, t), e.max = n, await this._updateParameterValue(e, n);
  }
  async _updateParameterValue(e, t) {
    e.onValue(e, t), this.html.find(`.parameter[data-parameter-code='${e.code}'] .parameter-value`).text(t);
    const n = await this.renderDiceCursor(e);
    this.html.find(`.parameter[data-parameter-code='${e.code}'] .input-cursor-parameter`).empty().append(n), this.activateDiceParameterClick(), this.html.find(`.parameter[data-parameter-code='${e.code}'] input.parameter-value`).val(e.value);
  }
  async renderDiceCursor(e) {
    var t;
    return await Zi.diceCursor({
      value: e.value,
      min: e.min,
      max: e.max,
      editable: (t = e.flags) == null ? void 0 : t.editDice
    });
  }
  _getSelectedOption(e) {
    return this.html.find(`.parameter[data-parameter-code='${e.code}'] select.select-option-parameter option:selected`).text();
  }
  _getEventItem(e, t) {
    const n = this.html.find(e.currentTarget).closest(".parameter").attr("data-item-id");
    return n ? t.items.get(n) : void 0;
  }
  _getRollParameter(e) {
    const t = this.html.find(e.currentTarget).closest(".parameter").attr("data-parameter-code");
    return this.roll.parameters.find((n) => n.code == t);
  }
};
Zs = new WeakSet(), Lf = function(e) {
  const t = game.system.anarchy.rollParameters.build(e).sort(be.ascending((n) => n.order ?? 200));
  return foundry.utils.mergeObject(e, {
    ENUMS: Oe.getEnums((n) => e.attributes.includes(n)),
    ANARCHY: N,
    parameters: t
  });
}, Ce(Tt, Zs), z(Tt, "PARTS", {
  body: {
    template: `${se}/roll/roll-dialog.hbs`
  }
});
let Bi = Tt;
const Yc = 2, Pl = "skillSpecializationCatalog", Rb = [
  { key: "running", label: "Running" },
  { key: "jumping", label: "Jumping" },
  { key: "swimming", label: "Swimming" },
  { key: "climbing", label: "Climbing" },
  { key: "acrobatics", label: "Acrobatics" }
], _f = /* @__PURE__ */ new Set(), Vi = [
  // Strength
  { code: "athletics", label: "Athletics", attribute: "strength", icon: `${Ae}/athletics.svg`, domains: ["physical"], specializations: Rb },
  { code: "heavyWeapons", label: "Heavy Weapons", attribute: "strength", icon: `${Ae}/heavy-weapons.svg`, defense: "physicalDefense", domains: ["physical"] },
  // Reflexes
  { code: "escapeArtist", label: "Escape Artist", attribute: "reflexes", icon: `${Ae}/escape-artist.svg`, domains: ["physical"] },
  { code: "gunnery", label: "Gunnery", attribute: "reflexes", icon: `${Ae}/vehicle-weapons.svg`, defense: "physicalDefense", domains: ["physical"] },
  { code: "meleeCombat", label: "Melee Combat", attribute: "reflexes", icon: `${Ae}/close-combat.svg`, defense: "physicalDefense", domains: ["physical"] },
  { code: "piloting", label: "Piloting", attribute: "reflexes", icon: `${Ae}/piloting-ground-steering-wheel.svg`, domains: ["physical"] },
  { code: "projectileWeapons", label: "Projectile Weapons", attribute: "reflexes", icon: `${Ae}/projectile-weapons.svg`, defense: "physicalDefense", domains: ["physical"] },
  { code: "firearms", label: "Firearms", attribute: "reflexes", icon: `${Ae}/firearms.svg`, defense: "physicalDefense", domains: ["physical"] },
  { code: "stealth", label: "Stealth", attribute: "reflexes", icon: `${Ae}/stealth.svg`, domains: ["physical"] },
  { code: "zeroGOps", label: "Zero-G Operations", attribute: "reflexes", icon: `${Ae}/fly.svg`, domains: ["physical"] },
  // Intelligence
  { code: "art", label: "Art", attribute: "intelligence", icon: `${Ae}/art.svg`, domains: ["mental"] },
  { code: "artillery", label: "Artillery", attribute: "intelligence", icon: `${Ae}/artillery.svg`, domains: ["mental"] },
  { code: "systemOps", label: "System Operations", attribute: "intelligence", icon: `${Ae}/electronics.svg`, domains: ["mental"] },
  { code: "computers", label: "Computers", attribute: "intelligence", icon: `${Ae}/hacking.svg`, domains: ["mental"] },
  { code: "demolitions", label: "Demolitions", attribute: "intelligence", icon: `${Ae}/demolition.svg`, domains: ["mental"] },
  { code: "medTech", label: "Medtech", attribute: "intelligence", icon: `${Ae}/biotech.svg`, domains: ["mental"] },
  { code: "science", label: "Science", attribute: "intelligence", icon: `${Ae}/skills.svg`, domains: ["mental"] },
  { code: "perception", label: "Perception", attribute: "intelligence", icon: `${Ae}/skills.svg`, domains: ["mental"] },
  { code: "tactics", label: "Tactics", attribute: "intelligence", icon: `${Ae}/skills.svg`, domains: ["mental"] },
  { code: "technician", label: "Technician", attribute: "intelligence", icon: `${Ae}/engineering.svg`, domains: ["mental"] },
  { code: "tracking", label: "Tracking", attribute: "intelligence", icon: `${Ae}/tracking.svg`, domains: ["physical", "mental"] },
  { code: "navigation", label: "Navigation", attribute: "intelligence", icon: `${Ae}/piloting-other.svg`, domains: ["mental"] },
  // Guts
  { code: "administration", label: "Administration", attribute: "willpower", icon: `${Ae}/knowledge.svg`, domains: ["social", "mental"] },
  { code: "animalHandling", label: "Animal Handling", attribute: "willpower", icon: `${Ae}/animals.svg`, domains: ["physical", "mental"] },
  { code: "survival", label: "Survival", attribute: "willpower", icon: `${Ae}/survival.svg`, domains: ["physical", "mental"] },
  // Charisma
  { code: "acting", label: "Acting", attribute: "charisma", icon: `${Ae}/con-art.svg`, domains: ["social"] },
  { code: "disguise", label: "Disguise", attribute: "charisma", icon: `${Ae}/disguise.svg`, domains: ["social", "mental"] },
  { code: "leadership", label: "Leadership", attribute: "charisma", icon: `${Ae}/psychology.svg`, domains: ["social"] },
  { code: "negotiation", label: "Negotiation", attribute: "charisma", icon: `${Ae}/negotiation.svg`, domains: ["social"] },
  { code: "etiquette", label: "Etiquette", attribute: "charisma", icon: `${Ae}/etiquette.svg`, domains: ["social"] },
  { code: "streetwise", label: "Streetwise", attribute: "charisma", icon: `${Ae}/etiquette2.svg`, domains: ["social"] },
  { code: "intimidation", label: "Intimidation", attribute: "charisma", icon: `${Ae}/intimidation.svg`, domains: ["social", "mental"] }
].map(Pb);
for (const i of Vi)
  _f.add(i.code);
function Pb(i) {
  return {
    ...i,
    label: i.label ?? i.code,
    icon: i.icon ?? `${Fr}/icons/skills/skills.svg`,
    specializations: Qc(i.specializations)
  };
}
function Jc(i) {
  return String(i ?? "").trim().toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, "");
}
function Qc(i = []) {
  const e = /* @__PURE__ */ new Set();
  return (Array.isArray(i) ? i : []).map((t) => {
    const n = Jc((t == null ? void 0 : t.key) ?? (t == null ? void 0 : t.label) ?? t);
    return !n || e.has(n) ? null : (e.add(n), {
      key: n,
      label: String((t == null ? void 0 : t.label) ?? (t == null ? void 0 : t.key) ?? t ?? n).trim() || n
    });
  }).filter(Boolean);
}
function Db(i = []) {
  const e = new Error(i[0] ?? "Invalid skill specialization data.");
  return e.validationErrors = i, e;
}
function Ob() {
  const i = {};
  for (const e of Vi) {
    const t = (Array.isArray(e.specializations) ? e.specializations : []).map((n) => String((n == null ? void 0 : n.label) ?? "").trim()).filter(Boolean);
    t.length && (i[e.code] = t);
  }
  return i;
}
const Lb = Object.freeze(Ob());
function _b(i, e = [], { strict: t = !1, errors: n = [] } = {}) {
  var r, s;
  if (!Array.isArray(e)) {
    if (t) {
      const o = ((r = Dl(i)) == null ? void 0 : r.label) ?? i;
      n.push(`${o}: expected an array of specialization labels.`);
    }
    return [];
  }
  const a = [];
  for (const o of e) {
    const l = String(o ?? "").trim();
    if (!l) {
      if (t) {
        const c = ((s = Dl(i)) == null ? void 0 : s.label) ?? i;
        n.push(`${c}: specialization labels cannot be blank.`);
      }
      continue;
    }
    a.push(l);
  }
  return Qc(a).map((o) => o.label);
}
function Dl(i) {
  return Vi.find((e) => e.code === i);
}
function xf(i, { strict: e = !1 } = {}) {
  const t = i && typeof i == "object" && !Array.isArray(i) ? i : {}, n = [], a = {};
  for (const [r, s] of Object.entries(t)) {
    if (!_f.has(r)) {
      e && n.push(`Unknown skill code "${r}".`);
      continue;
    }
    const o = _b(r, s, { strict: e, errors: n });
    o.length && (a[r] = o);
  }
  if (e && n.length) throw Db(n);
  return Object.fromEntries(
    Vi.map((r) => [r.code, a[r.code]]).filter(([, r]) => Array.isArray(r) && r.length)
  );
}
function xb() {
  var i, e, t;
  try {
    if ((t = (e = (i = game == null ? void 0 : game.settings) == null ? void 0 : i.settings) == null ? void 0 : e.has) != null && t.call(e, `${M}.${Pl}`))
      return game.settings.get(M, Pl);
  } catch {
  }
  return zf();
}
function $f() {
  const i = xf(xb(), { strict: !1 });
  return Object.fromEntries(
    Object.entries(i).map(([e, t]) => [
      e,
      Qc(t)
    ])
  );
}
function Bf(i = [], { allowedKeys: e = null } = {}) {
  const t = /* @__PURE__ */ new Set();
  return (Array.isArray(i) ? i : []).map((n) => Jc(n)).filter((n) => !n || t.has(n) || e && !e.has(n) ? !1 : (t.add(n), !0));
}
function Lt(i) {
  const e = Dl(i);
  if (e)
    return {
      ...e,
      specializations: ta(e.code)
    };
}
function _s() {
  const i = $f();
  return [...Vi].map((e) => ({
    ...e,
    specializations: [...i[e.code] ?? []]
  })).sort((e, t) => e.label.localeCompare(t.label));
}
function ta(i) {
  return [...$f()[i] ?? []];
}
function Xc(i, e) {
  const t = Jc(e);
  if (t)
    return ta(i).find((n) => n.key === t);
}
function $b(i, e) {
  var t;
  return ((t = Xc(i, e)) == null ? void 0 : t.label) ?? "";
}
function zf() {
  return foundry.utils.deepClone(Lb);
}
function lo(i, { strict: e = !1 } = {}) {
  return xf(i, { strict: e });
}
function xs(i = []) {
  return Bf(i);
}
function Bb(i, e = []) {
  const t = new Set(ta(i).map((a) => a.key)), n = new Set(Bf(e, { allowedKeys: t }));
  return ta(i).filter((a) => n.has(a.key)).map((a) => a.key);
}
function Ol(i, e) {
  var t, n;
  return xs(
    ((n = (t = i == null ? void 0 : i.skills) == null ? void 0 : t[e]) == null ? void 0 : n.specializations) ?? []
  );
}
function co(i, e) {
  return Bb(
    e,
    Ol(i, e)
  );
}
function Ff(i, e) {
  const t = new Set(co(i, e));
  return ta(e).filter((n) => t.has(n.key));
}
function zb(i) {
  const e = Math.ceil(i.length / 2);
  return { left: i.slice(0, e), right: i.slice(e) };
}
function Fb(i) {
  var e, t;
  i.skills ?? (i.skills = {});
  for (const n of Vi) {
    const a = (e = i.skills)[t = n.code] ?? (e[t] = {});
    a.rating == null && (a.rating = 0), a.bonus == null && (a.bonus = 0), a.specializations = xs(a.specializations);
  }
}
function Hf(i, { bonusBySkill: e = null } = {}) {
  const t = _s(), { left: n, right: a } = zb(t), r = (s) => {
    var y, b, S, w, T, k;
    const o = s.code, l = s.attribute, c = Number(((b = (y = i == null ? void 0 : i.skills) == null ? void 0 : y[o]) == null ? void 0 : b.rating) ?? 0), u = Number(((w = (S = i == null ? void 0 : i.attributes) == null ? void 0 : S[l]) == null ? void 0 : w.value) ?? 0), d = Number(((k = (T = i == null ? void 0 : i.skills) == null ? void 0 : T[o]) == null ? void 0 : k.bonus) ?? 0), m = Number((e == null ? void 0 : e[o]) ?? 0), f = d + m, p = Ff(i, o), g = ta(o).filter((C) => !p.some((I) => I.key === C.key)), h = u + c + f;
    return {
      code: o,
      label: s.label,
      icon: s.icon,
      attribute: l,
      attributeLabel: Oe != null && Oe.localizeAttribute ? Oe.localizeAttribute(l) : l,
      rating: c,
      base: u,
      bonus: f,
      total: h,
      rollPayload: JSON.stringify({ intent: "skill", key: o }),
      canAddSpecialization: g.length > 0,
      specializations: p.map((C) => ({
        ...C,
        bonus: Yc,
        rollPayload: JSON.stringify({
          intent: "skill",
          key: o,
          specializationKey: C.key,
          specializationLabel: C.label
        })
      })),
      // Input wiring paths (so templates don’t concat strings themselves)
      pathRating: `system.skills.${o}.rating`,
      pathBonus: `system.skills.${o}.bonus`
    };
  };
  return {
    left: n.map(r),
    right: a.map(r)
  };
}
const Ia = {
  prone: {
    label: "Prone",
    mods: [
      { domains: ["physical", "combat"], value: -2 }
    ]
  },
  blinded: {
    label: "Blinded",
    mods: [
      { domains: ["physical", "combat"], value: -3 },
      { domains: ["social"], value: -1 }
    ]
  },
  frightened: {
    label: "Frightened",
    mods: [
      { domains: ["mental", "social"], value: -1 }
    ]
  },
  overloaded: {
    label: "Overloaded",
    mods: [
      { domains: ["mental"], value: -2 }
    ]
  }
}, Uf = "statusConditionCatalog", Hb = Object.freeze([
  { value: "person", label: "Person" },
  { value: "machine", label: "Machine" },
  { value: "all", label: "All Actors" },
  { value: "character", label: "Character" },
  { value: "npc", label: "NPC" },
  { value: "vehicle", label: "Vehicle" },
  { value: "battlemech", label: "BattleMech" }
]), cd = Object.freeze([
  { value: "false", label: "No" },
  { value: "true", label: "Yes" }
]), jf = Object.freeze([
  A.actorTypes.character,
  A.actorTypes.npc
]), Kf = Object.freeze([
  A.actorTypes.vehicle,
  A.actorTypes.battlemech
]), Ub = Object.freeze([...jf, ...Kf]), $s = Object.freeze({
  person: jf,
  machine: Kf,
  all: Ub,
  character: [A.actorTypes.character],
  npc: [A.actorTypes.npc],
  vehicle: [A.actorTypes.vehicle],
  battlemech: [A.actorTypes.battlemech]
}), Gf = "systems/mwd/img/icons/status", jb = Object.freeze([
  // Person conditions: lightly filtered from the existing status/icon pool.
  ae("prone", "Prone", "person", "physical", ["movement", "posture"], "prone.svg", { modifierKey: "prone", order: 10 }),
  ae("blinded", "Blinded", "person", "sensory", ["vision"], "blinded.svg", { modifierKey: "blinded", order: 20 }),
  ae("frightened", "Frightened", "person", "mental", ["morale"], "brain_injury.svg", { modifierKey: "frightened", order: 30 }),
  ae("deafened", "Deafened", "person", "sensory", ["hearing"], "deafened.svg", { order: 40 }),
  ae("hidden", "Hidden", "person", "tactical", ["stealth"], "hidden.svg", { order: 50 }),
  ae("suppressed", "Suppressed", "person", "tactical", ["offense"], "suppressed.svg", { order: 60 }),
  ae("grappled", "Grappled", "person", "physical", ["movement"], "grappled.svg", { order: 70 }),
  ae("stunned", "Stunned", "person", "physical", ["action"], "concussion.svg", { order: 80 }),
  ae("knockedOut", "Knocked Out", "person", "physical", ["unconscious"], "knockout.svg", { order: 90 }),
  ae("onFire", "On Fire", "all", "hazard", ["fire", "heat", "escalating"], "on_fire.svg", { order: 100 }),
  ae("drugged", "Drugged", "person", "chemical", ["impairment"], "drugged.svg", { order: 110 }),
  ae("radiation", "Radiation", "person", "hazard", ["radiation"], "radiation_low.svg", { order: 120 }),
  ae("overloaded", "Overloaded", "all", "reactor", ["heat", "actionRestriction"], "surge.svg", { managed: !0, modifierKey: "overloaded", order: 130 }),
  ae("preparedInterrupt", "Prepared", "person", "tactical", ["reaction", "prepared"], "readied_action.svg", { manual: !1, managed: !0, order: 140 }),
  ae("machineCritical", "Machine Critical", "machine", "damage", ["critical", "system"], "surge.svg", { manual: !1, managed: !0, order: 150 }),
  // Machine stability and movement.
  ae("unstable", "Unstable", "machine", "stability", ["movement", "piloting", "knockdown"], "falling.svg", { order: 1e3 }),
  ae("staggeredMechanical", "Staggered (Mechanical)", "machine", "stability", ["movement", "actionRestriction"], "falling.svg", { order: 1010 }),
  ae("proneMechFall", "Prone", "battlemech", "stability", ["movement", "posture", "standUp"], "prone.svg", { order: 1020 }),
  ae("skidding", "Skidding", "machine", "movement", ["forcedMovement", "tracking"], "falling.svg", { order: 1030 }),
  ae("stalled", "Stalled", "machine", "movement", ["movement", "actionRestriction"], "emp.svg", { order: 1040 }),
  ae("limping", "Limping", "machine", "movement", ["movement", "location"], "broken_leg.svg", { order: 1050 }),
  ae("jumpJetFailure", "Jump Jet Failure", "battlemech", "movement", ["jump", "equipment"], "surge.svg", { order: 1060 }),
  ae("actuatorFailure", "Actuator Failure", "machine", "movement", ["movement", "handling"], "broken_leg.svg", { order: 1070 }),
  ae("gyroDamage", "Gyro Damage", "battlemech", "movement", ["movement", "piloting"], "falling.svg", { order: 1080 }),
  // Machine weapons.
  ae("weaponFailure", "Weapon Failure", "machine", "weapon", ["weapon", "mountScoped"], "broken_weapon.svg", { order: 1100 }),
  ae("jammedBallistic", "Jammed (Ballistic)", "machine", "weapon", ["weapon", "ballistic", "clearAction"], "broken_weapon.svg", { order: 1110 }),
  ae("armDestroyed", "Arm Destroyed", "battlemech", "damage", ["location", "weapon", "arm"], "dismembered_arm.svg", { order: 1120 }),
  // Sensors and electronics.
  ae("sensorDegraded", "Sensor Degraded", "machine", "sensor", ["sensor", "perception"], "all-seeing-eye.webp", { order: 1200 }),
  ae("sensorBlind", "Sensor Blind", "machine", "sensor", ["sensor", "targeting", "rangeLimit"], "damaged_eye.svg", { order: 1210 }),
  ae("ecmJamming", "ECM Jamming", "machine", "electronicWarfare", ["ecm", "tracking"], "emp.svg", { order: 1220 }),
  ae("ecmShrouded", "ECM Shrouded", "machine", "electronicWarfare", ["ecm", "defense"], "hidden.svg", { order: 1230 }),
  ae("epmBoosted", "EPM Boosted", "machine", "electronicWarfare", ["ecm", "sensor"], "all-seeing-eye.webp", { order: 1240 }),
  ae("eccmBoosted", "EPM Boosted (Legacy)", "machine", "electronicWarfare", ["ecm", "sensor", "legacy"], "all-seeing-eye.webp", { order: 1241 }),
  ae("sensorLocked", "Sensor Locked", "machine", "sensor", ["sensor", "targeted"], "all-seeing-eye.webp", { order: 1250 }),
  ae("trackingLost", "Tracking Lost", "machine", "sensor", ["sensor", "targetingData"], "damaged_eye.svg", { order: 1260 }),
  // Reactor and heat.
  ae("reactorInstability", "Reactor Instability", "machine", "reactor", ["heat", "reactor", "escalating"], "surge.svg", { order: 1300 }),
  ae("shutdown", "Shutdown", "machine", "reactor", ["heat", "actionRestriction"], "emp.svg", { order: 1310 }),
  ae("overheating", "Thermal Surge", "machine", "reactor", ["heat", "escalating"], "on_fire_mild.svg", { order: 1320 }),
  ae("coolingFailure", "Cooling Failure", "machine", "reactor", ["heat", "cooling"], "surge.svg", { order: 1325 }),
  ae("reactorBreach", "Reactor Breach", "machine", "reactor", ["reactor", "catastrophic", "countdown"], "radiation_high.svg", { order: 1330 }),
  // Machine damage and battlefield exposure.
  ae("legDestroyed", "Leg Destroyed", "battlemech", "damage", ["location", "movement", "leg"], "dismembered_leg.svg", { order: 1400 }),
  ae("exposed", "Exposed", "machine", "tactical", ["defense", "vulnerable"], "target.svg", { icon: `${Gf}/falling.svg`, order: 1410 }),
  ae("entrenchedHullDown", "Entrenched / Hull Down", "machine", "tactical", ["defense", "cover"], "cover.svg", { order: 1420 }),
  ae("obscured", "Obscured (Smoke/Dust)", "machine", "visibility", ["visibility", "cover"], "hidden.svg", { order: 1430 }),
  ae("obscuredLight", "Obscured (Light)", "machine", "visibility", ["visibility", "cover", "light"], "hidden.svg", { order: 1431 }),
  ae("obscuredHeavy", "Obscured (Heavy)", "machine", "visibility", ["visibility", "cover", "heavy"], "hidden.svg", { order: 1432 }),
  // Tactical markers.
  ae("evasiveWeave", "Evasive", "machine", "tactical", ["defense", "attackPenalty", "selfInduced"], "falling.svg", { order: 1500 }),
  ae("braced", "Braced", "machine", "tactical", ["defense", "mobilityPenalty"], "cover.svg", { order: 1510 }),
  ae("overextended", "Overextended", "machine", "tactical", ["attack", "defensePenalty"], "surge.svg", { order: 1520 }),
  ae("targetFocused", "Target Focused", "machine", "tactical", ["targeted", "attack"], "all-seeing-eye.webp", { order: 1530 }),
  ae("suppressedMechanical", "Suppressed", "machine", "tactical", ["offense", "suppressed"], "suppressed.svg", { order: 1540 })
]);
function ae(i, e, t, n, a, r, s = {}) {
  return {
    id: i,
    label: e,
    actorGroup: t,
    category: n,
    tags: a,
    icon: s.icon ?? `${Gf}/${r}`,
    manual: s.manual ?? !0,
    managed: s.managed ?? !1,
    modifierKey: s.modifierKey ?? "",
    order: s.order ?? 0
  };
}
function Kb() {
  return typeof foundry < "u" && (foundry == null ? void 0 : foundry.utils);
}
function Gb(i) {
  return Kb() && typeof foundry.utils.deepClone == "function" ? foundry.utils.deepClone(i) : JSON.parse(JSON.stringify(i));
}
function ud(i, e = !1) {
  if (typeof i == "boolean") return i;
  const t = String(i ?? "").trim().toLowerCase();
  return t ? ["true", "1", "yes", "y", "on"].includes(t) : e;
}
function Wf() {
  return Gb(jb);
}
function Zc(i) {
  const e = String(i ?? "").trim();
  if (!e) return "";
  const t = e.replace(/([a-z0-9])([A-Z])/g, "$1 $2").split(/[^A-Za-z0-9]+/).map((n) => n.trim()).filter(Boolean);
  return t.length ? t.map((n, a) => {
    const r = n.toLowerCase();
    return a === 0 ? r : `${r.charAt(0).toUpperCase()}${r.slice(1)}`;
  }).join("") : "";
}
function eu(i = []) {
  const e = Array.isArray(i) ? i : String(i ?? "").split(","), t = /* @__PURE__ */ new Set(), n = [];
  for (const a of e) {
    const r = Zc(a);
    !r || t.has(r) || (t.add(r), n.push(r));
  }
  return n;
}
function Wb(i = []) {
  return eu(i).join(", ");
}
function qf(i, e = "person") {
  const n = String(i ?? "").trim().toLowerCase();
  return n === "battlemech" ? "battlemech" : Object.prototype.hasOwnProperty.call($s, n) ? n : e;
}
function qb(i) {
  return [...$s[qf(i)] ?? []];
}
function Vb(i = {}, { strict: e = !1, index: t = 0 } = {}) {
  const n = [], a = `Row ${t + 1}`, r = String((i == null ? void 0 : i.id) ?? "").trim(), s = String((i == null ? void 0 : i.label) ?? "").trim(), o = String((i == null ? void 0 : i.actorGroup) ?? "person").trim(), l = qf(o, ""), c = Zc((i == null ? void 0 : i.category) ?? "general") || "general", u = eu(i == null ? void 0 : i.tags), d = String((i == null ? void 0 : i.icon) ?? "").trim(), m = String((i == null ? void 0 : i.modifierKey) ?? "").trim(), f = Number((i == null ? void 0 : i.order) ?? 0);
  if (r || n.push(`${a}: id cannot be blank.`), s || n.push(`${a}: label cannot be blank.`), (!l || o && !Object.prototype.hasOwnProperty.call($s, l)) && n.push(`${a}: actorGroup must be one of ${Object.keys($s).join(", ")}.`), m && !(Ia != null && Ia[m]) && n.push(`${a}: modifierKey "${m}" is not a known mechanics-backed status.`), Number.isFinite(f) || n.push(`${a}: order must be numeric.`), e && n.length) {
    const p = new Error(n[0]);
    throw p.validationErrors = n, p;
  }
  return {
    id: r,
    label: s || r || "Status",
    actorGroup: l || "person",
    category: c,
    tags: u,
    icon: d,
    manual: ud(i == null ? void 0 : i.manual, !0),
    managed: ud(i == null ? void 0 : i.managed, !1),
    modifierKey: m,
    order: Number.isFinite(f) ? Math.trunc(f) : 0
  };
}
function ia(i = [], { strict: e = !1 } = {}) {
  const t = Array.isArray(i) ? i : [], n = [], a = /* @__PURE__ */ new Set(), r = [];
  if (t.forEach((s, o) => {
    try {
      const l = Vb(s, { strict: e, index: o });
      if (!l.id) return;
      const c = l.id.toLowerCase();
      if (a.has(c)) {
        e && n.push(`Row ${o + 1}: duplicate id "${l.id}".`);
        return;
      }
      a.add(c), r.push(l);
    } catch (l) {
      e && n.push(...Array.isArray(l.validationErrors) ? l.validationErrors : [l.message]);
    }
  }), e && n.length) {
    const s = new Error(n[0]);
    throw s.validationErrors = n, s;
  }
  return r.sort((s, o) => s.order !== o.order ? s.order - o.order : s.label.localeCompare(o.label));
}
function uo(i = void 0) {
  var n, a;
  if (i !== void 0) return ia(i, { strict: !1 });
  const e = (n = globalThis.game) == null ? void 0 : n.settings, t = (a = e == null ? void 0 : e.get) == null ? void 0 : a.call(e, M, Uf);
  return ia(
    Array.isArray(t) ? t : Wf(),
    { strict: !1 }
  );
}
function Ur(i, e = uo()) {
  const t = String(i ?? "").trim();
  return t ? e.find((n) => String(n.id ?? "").trim() === t) ?? null : null;
}
function Vf(i = null) {
  return String(
    typeof i == "string" ? i : (i == null ? void 0 : i.type) ?? ""
  ).trim();
}
function mo(i, e = null) {
  const t = Vf(e);
  return !t || !i ? !1 : qb(i.actorGroup).includes(t);
}
function Yb({ statusId: i = "", actor: e = null, metadata: t = {}, catalogEntry: n = null } = {}) {
  const a = n ?? Ur(i), r = eu((t == null ? void 0 : t.tags) ?? (a == null ? void 0 : a.tags) ?? []);
  return {
    id: String(i || (a == null ? void 0 : a.id) || "").trim(),
    category: Zc((t == null ? void 0 : t.category) ?? (a == null ? void 0 : a.category) ?? "general") || "general",
    tags: r,
    actorGroup: String((t == null ? void 0 : t.actorGroup) ?? (a == null ? void 0 : a.actorGroup) ?? "").trim(),
    actorType: Vf(e),
    scope: String((t == null ? void 0 : t.scope) ?? "").trim(),
    location: String((t == null ? void 0 : t.location) ?? "").trim(),
    itemUuid: String((t == null ? void 0 : t.itemUuid) ?? "").trim(),
    targetUuid: String((t == null ? void 0 : t.targetUuid) ?? "").trim(),
    severity: String((t == null ? void 0 : t.severity) ?? "").trim(),
    notes: String((t == null ? void 0 : t.notes) ?? "").trim()
  };
}
function Jb(i = uo()) {
  return ia(i, { strict: !1 }).map((e) => ({
    id: e.id,
    name: e.label,
    label: e.label,
    img: e.icon,
    icon: e.icon
  }));
}
function dd() {
  if (typeof CONFIG > "u") return [];
  const i = Jb();
  return CONFIG.statusEffects = i, i;
}
const Qb = /* @__PURE__ */ new Set(["overloaded", "preparedInterrupt"]);
function md(i) {
  return i ? (i == null ? void 0 : i.document) ?? i : null;
}
function Xb(i, e) {
  var n, a, r;
  if (!i) return null;
  const t = md(e) ?? md(i == null ? void 0 : i.token);
  return t ? t.isLinked ? t.baseActor ?? ((r = (n = game.actors) == null ? void 0 : n.get) == null ? void 0 : r.call(n, ((a = t == null ? void 0 : t.baseActor) == null ? void 0 : a.id) ?? "")) ?? t.actor ?? i : t.actor ?? i : i;
}
function tu(i) {
  const e = String(i ?? "").trim();
  if (!e) return "Status";
  const a = (e.includes(".") ? e.split(".").at(-1) : e).replace(/^status/i, "").replace(/([a-z0-9])([A-Z])/g, "$1 $2").replace(/[_-]+/g, " ").trim();
  return a ? a.replace(/\b\w/g, (r) => r.toUpperCase()) : e;
}
function Zb(i) {
  const e = String((i == null ? void 0 : i.name) ?? (i == null ? void 0 : i.label) ?? (i == null ? void 0 : i.id) ?? "Status").trim();
  return e ? tu(e) : "Status";
}
function eS(i) {
  const e = typeof (i == null ? void 0 : i.img) == "string" ? i.img.trim() : "";
  if (e) return e;
  const t = i ? Object.getOwnPropertyDescriptor(i, "icon") : null;
  return "value" in (t ?? {}) ? String(t.value ?? "").trim() : "";
}
function tS(i) {
  var e, t;
  return ((t = (e = globalThis.CSS) == null ? void 0 : e.escape) == null ? void 0 : t.call(e, String(i ?? ""))) ?? String(i ?? "").replace(/["\\]/g, "\\$&");
}
function Yf(i) {
  var e;
  return Object.prototype.hasOwnProperty.call(((e = i == null ? void 0 : i.system) == null ? void 0 : e.burn) ?? {}, "overloaded");
}
function Jf(i, e) {
  var a;
  const t = String(e ?? "").trim();
  return !i || !t ? null : Array.from(((a = i.effects) == null ? void 0 : a.contents) ?? i.effects ?? []).find((r) => {
    var s, o, l, c, u, d, m;
    return (o = (s = r == null ? void 0 : r.statuses) == null ? void 0 : s.has) != null && o.call(s, t) || Array.isArray(r == null ? void 0 : r.statuses) && r.statuses.includes(t) || ((c = (l = r == null ? void 0 : r.getFlag) == null ? void 0 : l.call(r, M, "status")) == null ? void 0 : c.id) === t || ((m = (d = (u = r == null ? void 0 : r.flags) == null ? void 0 : u[M]) == null ? void 0 : d.status) == null ? void 0 : m.id) === t ? !0 : String((r == null ? void 0 : r.statusId) ?? (r == null ? void 0 : r.id) ?? "").trim() === t;
  }) ?? null;
}
function Qf(i, e) {
  var n, a, r;
  const t = Jf(i, e);
  return ((n = t == null ? void 0 : t.getFlag) == null ? void 0 : n.call(t, M, "status")) ?? ((r = (a = t == null ? void 0 : t.flags) == null ? void 0 : a[M]) == null ? void 0 : r.status) ?? null;
}
function Gi(i, e) {
  var t, n, a, r, s, o;
  return e === "overloaded" && Yf(i) ? !!((n = (t = i == null ? void 0 : i.system) == null ? void 0 : t.burn) != null && n.overloaded) || !!((r = (a = i == null ? void 0 : i.statuses) == null ? void 0 : a.has) != null && r.call(a, e)) : ((o = (s = i == null ? void 0 : i.statuses) == null ? void 0 : s.has) == null ? void 0 : o.call(s, e)) ?? !1;
}
function iS(i, e) {
  const t = Gi(e, i.id), n = Qf(e, i.id) ?? {};
  return {
    id: i.id,
    label: i.label,
    icon: i.icon,
    active: t,
    managed: !!i.managed || Qb.has(i.id),
    manual: !!i.manual,
    legacy: !1,
    category: i.category,
    tags: [...i.tags ?? []],
    scope: String(n.scope ?? "").trim(),
    notes: String(n.notes ?? "").trim()
  };
}
function nS(i, e) {
  const t = (CONFIG.statusEffects ?? []).find((a) => String((a == null ? void 0 : a.id) ?? "").trim() === i) ?? null, n = Qf(e, i) ?? {};
  return {
    id: i,
    label: t ? Zb(t) : tu(i),
    icon: t ? eS(t) : "",
    active: Gi(e, i),
    managed: !1,
    manual: !1,
    legacy: !0,
    category: "",
    tags: [],
    scope: String(n.scope ?? "").trim(),
    notes: String(n.notes ?? "").trim()
  };
}
function iu(i) {
  const e = /* @__PURE__ */ new Set(), t = uo(), n = [];
  for (const a of t) {
    const r = String((a == null ? void 0 : a.id) ?? "").trim();
    if (!r || e.has(r)) continue;
    const s = Gi(i, r), o = mo(a, i);
    !s && (!o || !a.manual) || (e.add(r), n.push(iS(a, i)));
  }
  for (const a of Array.from((i == null ? void 0 : i.statuses) ?? [])) {
    const r = String(a ?? "").trim();
    !r || e.has(r) || (e.add(r), n.push(nS(r, i)));
  }
  return n.sort((a, r) => a.active !== r.active ? a.active ? -1 : 1 : a.legacy !== r.legacy ? a.legacy ? 1 : -1 : a.label.localeCompare(r.label));
}
function aS(i) {
  if (!i.length)
    return "<p>No token statuses are configured.</p>";
  const e = foundry.utils.escapeHTML;
  return `
    <div class="mwd-token-status-dialog">
      <p style="margin-top: 0;">Toggle the statuses shown on this token.</p>
      <div style="display: grid; gap: 0.2rem; max-height: 20rem; overflow-y: auto; padding-right: 0.25rem;">
        ${i.map((n) => {
    const a = n.active ? "checked" : "", r = n.icon ? `<img src="${e(n.icon)}" alt="" width="20" height="20" style="flex: 0 0 20px; border: 0;" />` : "", s = n.managed ? '<small style="opacity: 0.7;">Managed by system state</small>' : "", o = n.legacy ? '<small style="opacity: 0.7;">Legacy / uncataloged</small>' : "";
    return `
      <div class="mwd-token-status-dialog__row" data-status-id="${e(n.id)}" style="display: grid; gap: 0.2rem; padding: 0.35rem 0; border-bottom: 1px solid rgba(255,255,255,0.08);">
        <label style="display: flex; align-items: center; gap: 0.5rem;">
          <input type="checkbox" name="status.${e(n.id)}.active" value="1" ${a} />
          ${r}
          <span style="flex: 1 1 auto;">${e(n.label)}</span>
          ${s}
          ${o}
        </label>
        ${n.legacy ? "" : `
          <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 0.35rem; padding-left: 1.85rem;">
            <input type="text" name="status.${e(n.id)}.scope" value="${e(n.scope ?? "")}" placeholder="Scope" />
            <input type="text" name="status.${e(n.id)}.notes" value="${e(n.notes ?? "")}" placeholder="Notes" />
          </div>
        `}
      </div>
    `;
  }).join("")}
      </div>
    </div>
  `;
}
async function rS({ actor: i, effects: e, selectedStatusIds: t }) {
  const n = new Map(t.map((a) => [a.id, a]));
  for (const a of e) {
    const r = n.get(a.id), s = !!(r != null && r.active);
    await Wi({
      actor: i,
      statusId: a.id,
      active: s,
      metadata: (r == null ? void 0 : r.metadata) ?? {}
    });
  }
}
async function fd(i, e, t = {}) {
  const n = Ur(e);
  if (!n) return !1;
  const a = Jf(i, e);
  if (!a) return !1;
  const r = Yb({
    actor: i,
    statusId: e,
    metadata: t,
    catalogEntry: n
  }), s = { [`flags.${M}.status`]: r };
  return typeof a.update == "function" ? (await a.update(s), !0) : a.id && typeof i.updateEmbeddedDocuments == "function" ? (await i.updateEmbeddedDocuments("ActiveEffect", [{ _id: a.id, ...s }]), !0) : !1;
}
async function Wi({ actor: i, statusId: e, active: t, metadata: n = {} }) {
  if (!i || !e) return !1;
  const a = Gi(i, e);
  if (!!t === a)
    return t ? fd(i, e, n) : !1;
  const r = Ur(e), s = r ? mo(r, i) : !1;
  return t && r && !s ? !1 : e === "overloaded" && Yf(i) ? (await i.update({ "system.burn.overloaded": !!t }), !0) : (await i.toggleStatusEffect(e, { active: !!t, overlay: !1 }), t && await fd(i, e, n), !0);
}
function sS(i) {
  var n, a, r, s, o;
  const e = /* @__PURE__ */ new Map(), t = Array.from(((n = i == null ? void 0 : i.querySelectorAll) == null ? void 0 : n.call(i, "[data-status-id]")) ?? []);
  for (const l of t) {
    const c = String(((a = l == null ? void 0 : l.dataset) == null ? void 0 : a.statusId) ?? "").trim();
    if (!c) continue;
    const u = tS(c), d = !!((r = l.querySelector(`input[name="status.${u}.active"]`)) != null && r.checked), m = String(((s = l.querySelector(`input[name="status.${u}.scope"]`)) == null ? void 0 : s.value) ?? "").trim(), f = String(((o = l.querySelector(`input[name="status.${u}.notes"]`)) == null ? void 0 : o.value) ?? "").trim();
    e.set(c, {
      id: c,
      active: d,
      metadata: { scope: m, notes: f }
    });
  }
  return Array.from(e.values());
}
async function Xf({ actor: i, token: e } = {}) {
  var a;
  if (!i || !e) return !1;
  const t = Xb(i, e), n = iu(t);
  return n.length ? foundry.applications.api.DialogV2.wait({
    window: {
      title: `Token Statuses: ${e.name ?? i.name ?? "Token"}`
    },
    position: {
      width: 420
    },
    content: aS(n),
    buttons: [
      {
        action: "apply",
        label: "Apply",
        icon: "fa-solid fa-check",
        default: !0,
        callback: async (r, s) => {
          var o;
          try {
            const l = sS(s.form);
            return await rS({ actor: t, effects: n, selectedStatusIds: l }), !0;
          } catch (l) {
            return console.error("MWD | Failed to update token statuses", l), (o = ui.notifications) == null || o.error("Unable to update token statuses."), !1;
          }
        }
      },
      {
        action: "cancel",
        label: "Cancel",
        icon: "fa-solid fa-xmark",
        callback: () => !1
      }
    ],
    close: () => !1
  }) : ((a = ui.notifications) == null || a.warn("No token statuses are configured."), !1);
}
function oS() {
  typeof Hooks > "u" || Hooks.on("renderTokenHUD", (i, e, t = {}) => {
    var d, m, f, p, g;
    const n = (t == null ? void 0 : t._id) ?? (t == null ? void 0 : t.id) ?? "", a = ((m = (d = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : d.get) == null ? void 0 : m.call(d, n)) ?? null, r = (a == null ? void 0 : a.actor) ?? null;
    if (!r) return;
    const s = uo(), o = new Map(s.map((h) => [h.id, h])), c = typeof jQuery < "u" && e instanceof jQuery ? e[0] : e;
    if (!(c instanceof HTMLElement)) return;
    const u = c.querySelectorAll("[data-status-id], [data-statusId], [data-effect-id]");
    for (const h of u) {
      const y = String(
        ((f = h.dataset) == null ? void 0 : f.statusId) ?? ((p = h.dataset) == null ? void 0 : p.statusid) ?? ((g = h.dataset) == null ? void 0 : g.effectId) ?? ""
      ).trim();
      if (!y) continue;
      const b = o.get(y);
      if (!b) continue;
      !Gi(r, y) && !mo(b, r) && (h.hidden = !0, h.style.display = "none");
    }
  });
}
const lS = Object.freeze({
  STR: bn.strength,
  REF: bn.reflexes,
  WIL: bn.willpower,
  INT: bn.intelligence,
  CHA: bn.charisma
}), cS = Object.freeze({
  STR: "Strength",
  REF: "Reflexes",
  WIL: "Willpower",
  INT: "Intelligence",
  CHA: "Charisma"
}), uS = Object.freeze({
  composure: {
    id: "composure",
    label: "Composure",
    formula: ["WIL", "CHA"],
    tags: ["combat", "utility", "mental"],
    domains: ["mental"]
  },
  judgeIntent: {
    id: "judgeIntent",
    label: "Judge Intent",
    formula: ["INT", "CHA"],
    tags: ["combat", "utility", "social", "mental"],
    domains: ["social", "mental"]
  },
  memory: {
    id: "memory",
    label: "Memory",
    formula: ["INT", "INT"],
    tags: ["combat", "utility", "mental"],
    domains: ["mental"]
  },
  lift: {
    id: "lift",
    label: "Lift",
    formula: ["STR", "STR"],
    tags: ["combat", "utility", "physical"],
    domains: ["physical"]
  },
  endure: {
    id: "endure",
    label: "Endure",
    formula: ["STR", "WIL"],
    tags: ["combat", "utility", "physical", "mental"],
    domains: ["physical", "mental"]
  }
});
function nu(i) {
  const e = String(i ?? "").trim();
  return e ? uS[e] ?? null : null;
}
function dS(i) {
  const e = nu(i);
  return e ? {
    intent: "common",
    id: e.id
  } : null;
}
function mS(i) {
  return lS[String(i ?? "").trim().toUpperCase()] ?? null;
}
function fS(i) {
  return cS[String(i ?? "").trim().toUpperCase()] ?? String(i ?? "").trim().toUpperCase();
}
function pS(i = []) {
  return (Array.isArray(i) ? i : []).map((e) => String(e ?? "").trim().toUpperCase()).filter(Boolean).join(" + ");
}
const au = Object.freeze([
  { value: "positive", label: "Positive" },
  { value: "negative", label: "Negative" },
  { value: "narrative", label: "Narrative" }
]), ru = Object.freeze([
  { value: "minor", label: "Minor" },
  { value: "major", label: "Major" }
]), Zf = Object.freeze([
  { value: "passive", label: "Passive" },
  { value: "triggered", label: "Triggered" }
]), ep = Object.freeze([
  { value: "rollMod", label: "Roll Modifier" },
  { value: "burnAdjust", label: "Burn Adjust" },
  { value: "actionCostMod", label: "Action Cost Mod" },
  { value: "initiativeMod", label: "Initiative Mod" },
  { value: "damageMod", label: "Damage Intake Mod" },
  { value: "edgeEvent", label: "Edge Event" }
]), tp = Object.freeze([
  { value: "onBuildRoll", label: "Build Roll" },
  { value: "onBeforeBurnApplied", label: "Before Burn Applied" },
  { value: "onBeforeActionCostFinalized", label: "Before Action Cost Finalized" },
  { value: "onInitiativeResolved", label: "Initiative Resolved" },
  { value: "onDamageResolved", label: "Damage Resolved" },
  { value: "onEndOfActivation", label: "End of Activation" },
  { value: "onEdgeSpend", label: "Edge Spend" },
  { value: "onEdgeGain", label: "Edge Gain" }
]), su = Object.freeze([
  { value: "eq", label: "=" },
  { value: "neq", label: "!=" },
  { value: "gt", label: ">" },
  { value: "gte", label: ">=" },
  { value: "lt", label: "<" },
  { value: "lte", label: "<=" },
  { value: "includes", label: "Includes" },
  { value: "notIncludes", label: "Excludes" },
  { value: "truthy", label: "Is True" },
  { value: "falsy", label: "Is False" }
]), ip = Object.freeze([
  { value: "adjustAmount", label: "Adjust Amount" },
  { value: "grantPool", label: "Grant Pool" }
]), gS = new Set(au.map((i) => i.value)), hS = new Set(ru.map((i) => i.value)), yS = new Set(Zf.map((i) => i.value)), bS = new Set(ep.map((i) => i.value)), np = new Set(tp.map((i) => i.value)), SS = new Set(su.map((i) => i.value)), AS = new Set(ip.map((i) => i.value));
function Se(i, e = "") {
  return String(i ?? "").trim() || e;
}
function Ee(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? t : e;
}
function Bs(i) {
  return foundry.utils.deepClone(i);
}
function ap(i = []) {
  return (Array.isArray(i) ? i : typeof i == "string" ? i.split(",") : []).map((t) => String(t ?? "").trim()).filter(Boolean);
}
function wS(i) {
  if (typeof i != "string") return i;
  const e = i.trim();
  if (!e) return "";
  if (e === "true") return !0;
  if (e === "false") return !1;
  if (/^-?\d+(\.\d+)?$/.test(e)) return Number(e);
  if (e.startsWith("[") && e.endsWith("]") || e.startsWith("{") && e.endsWith("}"))
    try {
      return JSON.parse(e);
    } catch {
      return e;
    }
  return e;
}
function Uo(i) {
  const e = Math.max(0, Math.trunc(Ee(i, 0)));
  return e > 0 ? e : 0;
}
function vn(i = {}) {
  const e = i && typeof i == "object" ? i : {};
  return {
    perActivation: Uo(e.perActivation),
    perRound: Uo(e.perRound),
    perScene: Uo(e.perScene)
  };
}
function TS(i = {}) {
  const e = i && typeof i == "object" ? i : {}, t = {
    id: Se(e.id, foundry.utils.randomID()),
    fact: Se(e.fact)
  }, n = su.find((r) => e[r.value] !== void 0 && e[r.value] !== null), a = (n == null ? void 0 : n.value) ?? (SS.has(String(e.comparator ?? "").trim()) ? String(e.comparator).trim() : "eq");
  return t.comparator = a, a !== "truthy" && a !== "falsy" && (t.value = wS(e[a] ?? e.value ?? "")), t;
}
function en(i = []) {
  return (Array.isArray(i) ? i : []).map(TS);
}
function kS(i = {}) {
  const e = i && typeof i == "object" ? i : {}, t = bS.has(String(e.type ?? "").trim()) ? String(e.type).trim() : "rollMod", n = MS(t), a = np.has(String(e.phase ?? "").trim()) ? String(e.phase).trim() : n, r = AS.has(String(e.operation ?? "").trim()) ? String(e.operation).trim() : "adjustAmount";
  return {
    id: Se(e.id, foundry.utils.randomID()),
    type: t,
    phase: a,
    selector: Se(e.selector),
    skillKeys: ap(e.skillKeys),
    label: Se(e.label),
    value: Ee(e.value, 0),
    min: e.min === void 0 || e.min === null || e.min === "" ? null : Ee(e.min, 0),
    max: e.max === void 0 || e.max === null || e.max === "" ? null : Ee(e.max, 0),
    pool: Se(e.pool),
    operation: r,
    conditions: en(e.conditions),
    limit: vn(e.limit)
  };
}
function rp(i = {}) {
  const e = Se(i == null ? void 0 : i.selector);
  return e === "intent.skill" || e.startsWith("intent.skill.");
}
function Hn(i = []) {
  return (Array.isArray(i) ? i : []).map(kS).filter((t) => t.phase && t.type);
}
function Ii(i = {}) {
  const e = i && typeof i == "object" ? Bs(i) : {}, t = e.positive === !1 ? "negative" : "positive", n = gS.has(String(e.category ?? "").trim()) ? String(e.category).trim() : t, a = hS.has(String(e.tier ?? "").trim()) ? String(e.tier).trim() : "minor", r = yS.has(String(e.activation ?? "").trim()) ? String(e.activation).trim() : "passive";
  return {
    ...e,
    positive: n === "positive",
    category: n,
    tier: a,
    activation: r,
    tags: ap(e.tags),
    effects: Hn(e.effects),
    prerequisites: en(e.prerequisites),
    limits: vn(e.limits)
  };
}
function sp() {
  return {
    categories: [...au],
    tiers: [...ru],
    activations: [...Zf],
    effectTypes: [...ep],
    phases: [...tp],
    comparators: [...su],
    edgeOperations: [...ip]
  };
}
function ds(i = "") {
  var e;
  return ((e = au.find((t) => t.value === i)) == null ? void 0 : e.label) ?? "Positive";
}
function ms(i = "") {
  var e;
  return ((e = ru.find((t) => t.value === i)) == null ? void 0 : e.label) ?? "Minor";
}
function MS(i = "") {
  switch (i) {
    case "burnAdjust":
      return "onBeforeBurnApplied";
    case "actionCostMod":
      return "onBeforeActionCostFinalized";
    case "initiativeMod":
      return "onInitiativeResolved";
    case "damageMod":
      return "onDamageResolved";
    case "edgeEvent":
      return "onEdgeGain";
    default:
      return "onBuildRoll";
  }
}
function CS(i) {
  return Array.from((i == null ? void 0 : i.items) ?? []).filter((e) => ((e == null ? void 0 : e.canonicalType) ?? (e == null ? void 0 : e.type)) === "quality").map((e) => ({
    item: e,
    system: Ii(e.system ?? {})
  }));
}
function vS(i = {}, e = {}) {
  const t = vn(i), n = vn(e);
  return {
    perActivation: n.perActivation || t.perActivation,
    perRound: n.perRound || t.perRound,
    perScene: n.perScene || t.perScene
  };
}
function op(i = {}) {
  var a, r, s;
  const e = Se(i.combatId ?? ((a = i.combat) == null ? void 0 : a.id)), t = Math.max(0, Math.trunc(Ee(i.round ?? ((r = i.combat) == null ? void 0 : r.round), 0))), n = Se(i.sceneId ?? ((s = canvas == null ? void 0 : canvas.scene) == null ? void 0 : s.id));
  return {
    activationKey: Se(i.activationKey),
    roundKey: e ? `${e}:${t}` : "",
    sceneKey: n
  };
}
function ES(i, e = {}) {
  var r, s, o, l;
  const t = ((r = i == null ? void 0 : i.flags) == null ? void 0 : r[M]) ?? {}, n = ((s = t == null ? void 0 : t.traitUsage) == null ? void 0 : s.scene) ?? {}, a = e.state ?? {};
  return {
    activation: ((o = a == null ? void 0 : a.traitUsage) == null ? void 0 : o.activation) ?? {},
    round: ((l = a == null ? void 0 : a.traitUsage) == null ? void 0 : l.round) ?? {},
    scene: n
  };
}
function IS(i, e, t, n) {
  var a, r, s, o, l;
  switch (t) {
    case "perActivation":
      return Math.max(0, Math.trunc(Ee((a = i.activation) == null ? void 0 : a[n], 0)));
    case "perRound":
      return Math.max(0, Math.trunc(Ee((s = (r = i.round) == null ? void 0 : r[e.roundKey]) == null ? void 0 : s[n], 0)));
    case "perScene":
      return Math.max(0, Math.trunc(Ee((l = (o = i.scene) == null ? void 0 : o[e.sceneKey]) == null ? void 0 : l[n], 0)));
    default:
      return 0;
  }
}
function NS(i, e, t, n) {
  const a = [];
  for (const r of ["perActivation", "perRound", "perScene"]) {
    const s = Math.max(0, Math.trunc(Ee(t == null ? void 0 : t[r], 0)));
    if (!s) continue;
    IS(i, e, r, n) >= s && a.push(`${r} limit reached`);
  }
  return a;
}
function RS(i, e, t) {
  switch (e) {
    case "truthy":
      return !!i;
    case "falsy":
      return !i;
    case "neq":
      return i !== t;
    case "gt":
      return Number(i) > Number(t);
    case "gte":
      return Number(i) >= Number(t);
    case "lt":
      return Number(i) < Number(t);
    case "lte":
      return Number(i) <= Number(t);
    case "includes":
      return Array.isArray(i) ? i.includes(t) : String(i ?? "").includes(String(t ?? ""));
    case "notIncludes":
      return Array.isArray(i) ? !i.includes(t) : !String(i ?? "").includes(String(t ?? ""));
    case "eq":
    default:
      return i === t;
  }
}
function pd(i, e) {
  if (!Se(i == null ? void 0 : i.fact)) return !0;
  const t = foundry.utils.getProperty(e, i.fact);
  return RS(t, i.comparator, i.value);
}
function PS(i = "", e = {}) {
  const t = Se(i);
  return t ? (Array.isArray(e == null ? void 0 : e.selectors) ? e.selectors : []).some(
    (a) => a === t || a.startsWith(`${t}.`)
  ) : !0;
}
function lp(i, e) {
  return `${i.id}:${e.id}`;
}
function DS(i, e) {
  var t;
  return !!((t = i.system) != null && t.inactive) || !!(e != null && e.inactive);
}
function gd(i = []) {
  return i.map((e) => e.fact).filter(Boolean).join(", ");
}
function ya(i, e, t) {
  const n = Ee(i[e], 0);
  let a = n;
  return typeof t.value == "number" && (a += t.value), typeof t.min == "number" && (a = Math.max(t.min, a)), typeof t.max == "number" && (a = Math.min(t.max, a)), i[e] = a, a - n;
}
function fn(i, e, t, n, a) {
  n && i.push({
    id: `trait:${a}:${e.id}:${t.id}`,
    label: t.label || e.name,
    value: n,
    source: e.name,
    traitItemId: e.id,
    traitEffectId: t.id
  });
}
function OS({ item: i, effect: e, phase: t, packet: n, result: a }) {
  switch (e.type) {
    case "rollMod": {
      const r = Ee(e.value, 0);
      return fn(a.modifiers, i, e, r, t), r;
    }
    case "burnAdjust": {
      if (t === "onEndOfActivation") {
        const s = ya(n, "burnDelta", e);
        return fn(a.modifiers, i, e, s, t), s;
      }
      const r = ya(n, "amount", e);
      return fn(a.modifiers, i, e, r, t), r;
    }
    case "actionCostMod": {
      const r = ya(n, "cost", e);
      return fn(a.modifiers, i, e, r, t), r;
    }
    case "initiativeMod": {
      const r = ya(n, "total", e);
      return fn(a.modifiers, i, e, r, t), r;
    }
    case "damageMod": {
      const r = ya(n, "amount", e);
      return fn(a.modifiers, i, e, r, t), r;
    }
    case "edgeEvent": {
      if (t === "onEndOfActivation" && e.operation === "grantPool" && e.pool)
        return n.edgeAdjustments ?? (n.edgeAdjustments = []), n.edgeAdjustments.push({
          poolKey: e.pool,
          amount: Ee(e.value, 0),
          label: e.label || i.name,
          source: i.name
        }), fn(a.modifiers, i, e, Ee(e.value, 0), t), Ee(e.value, 0);
      const r = ya(n, "amount", e);
      return fn(a.modifiers, i, e, r, t), r;
    }
    default:
      return 0;
  }
}
function LS(i, e, t) {
  const n = lp(i, e), a = [];
  return t.perActivation > 0 && a.push({ kind: "usage", scope: "perActivation", key: n, delta: 1 }), t.perRound > 0 && a.push({ kind: "usage", scope: "perRound", key: n, delta: 1 }), t.perScene > 0 && a.push({ kind: "usage", scope: "perScene", key: n, delta: 1 }), a;
}
function cp(i = "") {
  const e = Se(i);
  return e ? [`action.${e}`] : [];
}
function ca(i, e = {}) {
  var o, l, c, u, d;
  const t = e.snapshot ?? null, n = e.state ?? (t == null ? void 0 : t.state) ?? {}, a = Array.isArray(n == null ? void 0 : n.actionLog) ? n.actionLog.map((m) => Se(m == null ? void 0 : m.id)).filter(Boolean) : [], r = (n == null ? void 0 : n.actionState) ?? {}, s = [];
  return r != null && r.aim && s.push("state.aim"), r != null && r.preparedInterrupt && s.push("state.preparedInterrupt"), {
    activation: {
      moved: a.includes("move") || !!((o = r == null ? void 0 : r.move) != null && o.moved),
      saSpent: Math.max(0, Math.trunc(Ee(n == null ? void 0 : n.saSpentThisActivation, 0))),
      attacksThisActivation: Math.max(0, Math.trunc(Ee(n == null ? void 0 : n.attacksThisActivation, 0))),
      burnThisActivation: Math.max(0, Math.trunc(Ee(n == null ? void 0 : n.burnThisActivation, 0)))
    },
    actionState: {
      aim: (r == null ? void 0 : r.aim) ?? null,
      move: (r == null ? void 0 : r.move) ?? null,
      preparedInterrupt: (r == null ? void 0 : r.preparedInterrupt) ?? null
    },
    burn: {
      current: Math.max(0, Math.trunc(Ee((c = (l = i == null ? void 0 : i.system) == null ? void 0 : l.burn) == null ? void 0 : c.value, 0))),
      overloaded: !!((d = (u = i == null ? void 0 : i.system) == null ? void 0 : u.burn) != null && d.overloaded)
    },
    timing: {
      firstAttackThisActivation: Math.max(0, Math.trunc(Ee(n == null ? void 0 : n.attacksThisActivation, 0))) === 0,
      firstExtraSAThisActivation: Math.max(0, Math.trunc(Ee(n == null ? void 0 : n.saSpentThisActivation, 0))) <= 3
    },
    selectors: s
  };
}
function ou({ actor: i, resolved: e, payload: t, runtime: n = {} } = {}) {
  var d, m, f, p, g, h, y, b, S, w, T, k;
  const a = ca(i, n), r = Se((e == null ? void 0 : e.intent) ?? (t == null ? void 0 : t.intent), "skill"), s = Array.isArray(e == null ? void 0 : e.domains) ? e.domains : [], o = Se(((d = e == null ? void 0 : e.attack) == null ? void 0 : d.rangeBand) ?? (t == null ? void 0 : t.rangeBand)), l = Se(((f = (m = t == null ? void 0 : t.edge) == null ? void 0 : m.pre) == null ? void 0 : f.poolKey) ?? ((p = t == null ? void 0 : t.edge) == null ? void 0 : p.poolKey) ?? ""), c = Se(
    ((g = e == null ? void 0 : e.data) == null ? void 0 : g.skillKey) ?? ((h = e == null ? void 0 : e.specialization) == null ? void 0 : h.skillKey) ?? (r === "skill" ? t == null ? void 0 : t.key : "")
  ), u = Se(
    ((S = (b = (y = e == null ? void 0 : e.breakdown) == null ? void 0 : y.find) == null ? void 0 : b.call(y, (C) => (C == null ? void 0 : C.id) === "skill")) == null ? void 0 : S.label) ?? (e == null ? void 0 : e.title)
  );
  return a.intent = r, a.domains = s, a.rangeBand = o, a.skill = {
    key: c,
    label: u
  }, a.edge = {
    stage: (w = t == null ? void 0 : t.toggles) != null && w.useEdge ? "pre" : "",
    pool: l,
    spent: !!((T = t == null ? void 0 : t.toggles) != null && T.useEdge)
  }, a.selectors.push(`intent.${r}`), s.forEach((C) => a.selectors.push(`domain.${C}`)), o && a.selectors.push(`range.${o}`), r === "skill" && c && a.selectors.push(`skill.${c}`), (k = t == null ? void 0 : t.toggles) != null && k.useEdge && a.selectors.push("edge.pre"), a;
}
function up({ actor: i, packet: e = {}, runtime: t = {} } = {}) {
  const n = ca(i, t);
  return n.action = {
    id: Se(e.actionId),
    category: Se(e.category),
    resource: Se(e.resource),
    cost: Ee(e.cost, 0),
    effectiveCost: Ee(e.effectiveCost ?? e.cost, 0)
  }, n.selectors.push(...cp(e.actionId)), n.action.category && n.selectors.push(`actionCategory.${n.action.category}`), n.action.resource && n.selectors.push(`actionResource.${n.action.resource}`), n;
}
function fs({ actor: i, packet: e = {}, runtime: t = {} } = {}) {
  const n = ca(i, t);
  return n.action = {
    id: Se(e.actionId),
    category: Se(e.category),
    resource: Se(e.resource)
  }, n.burn = {
    ...n.burn,
    amount: Ee(e.amount, 0),
    source: Se(e.source)
  }, e.source === "extraSA" && e.extraSaIndex === 1 && n.selectors.push("activation.extraSA:first"), e.source && n.selectors.push(`burn.${e.source}`), n.action.id && n.selectors.push(...cp(n.action.id)), n.action.category && n.selectors.push(`actionCategory.${n.action.category}`), n.action.category === "reaction" && n.selectors.push(`reaction.${n.action.id}`), n;
}
function dp({ actor: i, packet: e = {}, runtime: t = {} } = {}) {
  const n = ca(i, t);
  return n.intent = "initiative", n.domains = ["combat"], n.initiative = {
    total: Ee(e.total, 0)
  }, n.selectors.push("intent.initiative"), n;
}
function mp({ actor: i, packet: e = {}, runtime: t = {} } = {}) {
  const n = ca(i, t);
  return n.damage = {
    amount: Ee(e.amount, 0),
    track: Se(e.track),
    damageType: Se(e.damageType)
  }, n.selectors.push("incoming"), n;
}
function Ll({ actor: i, packet: e = {}, phase: t = "onEdgeGain", runtime: n = {} } = {}) {
  const a = ca(i, n);
  return a.edge = {
    pool: Se(e.poolKey),
    amount: Ee(e.amount, 0),
    eventKey: Se(e.eventKey),
    source: Se(e.source)
  }, a.selectors.push(t === "onEdgeSpend" ? "edge.spend" : "edge.gain"), a.edge.eventKey && a.selectors.push(`event.${a.edge.eventKey}`), a;
}
function fp({ actor: i, packet: e = {}, runtime: t = {} } = {}) {
  const n = ca(i, t);
  return n.event = {
    phase: "endOfActivation"
  }, n.selectors.push("endOfActivation"), Ee(e.burnDelta, 0) !== 0 && n.selectors.push("burn.adjust"), n;
}
function hi({ actor: i, phase: e, facts: t = {}, packet: n = {}, options: a = {} } = {}) {
  var u;
  const r = {
    packet: Bs(n),
    modifiers: [],
    mutations: [],
    applied: [],
    skipped: []
  };
  if (!i || !np.has(String(e ?? "").trim()))
    return r;
  const s = a.runtime ?? {}, o = ES(i, s), l = op(s), c = CS(i);
  for (const { item: d, system: m } of c) {
    if (DS(d, m)) {
      r.skipped.push({
        traitItemId: d.id,
        traitEffectId: "",
        label: d.name,
        reason: "Trait is inactive"
      });
      continue;
    }
    const f = m.prerequisites.filter((p) => Se(p == null ? void 0 : p.fact)).filter((p) => !pd(p, t));
    if (f.length) {
      r.skipped.push({
        traitItemId: d.id,
        traitEffectId: "",
        label: d.name,
        reason: `Prerequisites not met: ${gd(f)}`
      });
      continue;
    }
    for (const p of m.effects.filter((g) => g.phase === e)) {
      if (!PS(p.selector, t)) {
        r.skipped.push({
          traitItemId: d.id,
          traitEffectId: p.id,
          label: p.label || d.name,
          reason: `Selector did not match (${p.selector || "any"})`
        });
        continue;
      }
      if (rp(p) && p.skillKeys.length) {
        const w = Se((u = t == null ? void 0 : t.skill) == null ? void 0 : u.key);
        if (!w || !p.skillKeys.includes(w)) {
          r.skipped.push({
            traitItemId: d.id,
            traitEffectId: p.id,
            label: p.label || d.name,
            reason: `Skill did not match (${p.skillKeys.join(", ")})`
          });
          continue;
        }
      }
      const g = p.conditions.filter((w) => Se(w == null ? void 0 : w.fact)).filter((w) => !pd(w, t));
      if (g.length) {
        r.skipped.push({
          traitItemId: d.id,
          traitEffectId: p.id,
          label: p.label || d.name,
          reason: `Conditions not met: ${gd(g)}`
        });
        continue;
      }
      const h = vS(m.limits, p.limit), y = lp(d, p), b = NS(o, l, h, y);
      if (b.length) {
        r.skipped.push({
          traitItemId: d.id,
          traitEffectId: p.id,
          label: p.label || d.name,
          reason: b.join(", ")
        });
        continue;
      }
      const S = OS({
        item: d,
        effect: p,
        phase: e,
        packet: r.packet,
        result: r
      });
      r.applied.push({
        traitItemId: d.id,
        traitEffectId: p.id,
        label: p.label || d.name,
        value: S,
        phase: e,
        source: d.name
      }), a.consumeUsage && r.mutations.push(...LS(d, p, h));
    }
  }
  return r;
}
async function rn({ actor: i, mutations: e = [], runtime: t = {} } = {}) {
  var o, l, c, u, d, m, f, p, g;
  if (!i || !Array.isArray(e) || !e.length) return;
  const n = e.filter((h) => (h == null ? void 0 : h.kind) === "usage");
  if (!n.length) return;
  const a = Bs(((c = (l = (o = i.flags) == null ? void 0 : o[M]) == null ? void 0 : l.traitUsage) == null ? void 0 : c.scene) ?? {}), r = t.state ? Bs(t.state) : null, s = op(t);
  for (const h of n) {
    const y = Se(h.key), b = Math.max(0, Math.trunc(Ee(h.delta, 0)));
    if (!(!y || !b))
      switch (h.scope) {
        case "perActivation": {
          if (!r) break;
          r.traitUsage ?? (r.traitUsage = {}), (u = r.traitUsage).activation ?? (u.activation = {}), r.traitUsage.activation[y] = Math.max(0, Ee(r.traitUsage.activation[y], 0) + b);
          break;
        }
        case "perRound": {
          if (!r || !s.roundKey) break;
          r.traitUsage ?? (r.traitUsage = {}), (d = r.traitUsage).round ?? (d.round = {}), (m = r.traitUsage.round)[f = s.roundKey] ?? (m[f] = {}), r.traitUsage.round[s.roundKey][y] = Math.max(
            0,
            Ee(r.traitUsage.round[s.roundKey][y], 0) + b
          );
          break;
        }
        case "perScene": {
          if (!s.sceneKey) break;
          a[p = s.sceneKey] ?? (a[p] = {}), a[s.sceneKey][y] = Math.max(0, Ee(a[s.sceneKey][y], 0) + b);
          break;
        }
      }
  }
  r && ((g = t.combatant) != null && g.id) && await t.combatant.setFlag(M, "personalCombat", r), await i.setFlag(M, "traitUsage", { scene: a });
}
const pp = "personalActionCatalog", tt = Object.freeze({
  standard: "standard",
  complex: "complex",
  free: "free",
  reaction: "reaction",
  recovery: "recovery"
}), _l = Object.freeze([
  { value: tt.standard, label: "Standard" },
  { value: tt.complex, label: "Complex" },
  { value: tt.free, label: "Free" },
  { value: tt.reaction, label: "Reaction" },
  { value: tt.recovery, label: "Burn & Recovery" }
]), gp = Object.freeze([
  { value: "", label: "Not Implemented / Placeholder" },
  { value: "combatAction", label: "Generic Action" },
  { value: "combatAttack", label: "Attack Pipeline" },
  { value: "combatEvade", label: "Evade" },
  { value: "combatAssist", label: "Assist" },
  { value: "combatInterrupt", label: "Interrupt" },
  { value: "combatReduceBurn", label: "Reduce Burn" },
  { value: "combatOverloadCheck", label: "Overload Check" }
]), hd = new Set(_l.map((i) => i.value)), yd = new Set(gp.map((i) => i.value)), hp = Object.freeze([
  { id: "move", label: "Move", category: "standard", cost: 1, handler: "combatAction", state: "move", description: "Reposition within the scene and establish your location." },
  { id: "aim", label: "Aim", category: "standard", cost: 1, handler: "combatAction", state: "aim", description: "Line up your next attack for a stronger single-target shot." },
  { id: "interact", label: "Interact", category: "standard", cost: 1, handler: "combatAction", description: "Manipulate an object or the environment with intent." },
  { id: "assess", label: "Assess", category: "standard", cost: 1, handler: "combatAction", description: "Read the situation and gather useful tactical information." },
  { id: "attack", label: "Attack", category: "complex", cost: 2, handler: "combatAttack", prominent: !0, description: "Make an offensive action and resolve it through the attack pipeline." },
  { id: "firstAid", label: "First Aid", category: "complex", cost: 2, handler: "", reason: "Recovery resolver not yet implemented.", description: "Stabilize or recover harm through a focused treatment action." },
  { id: "readyItem", label: "Ready Item", category: "free", cost: 0, handler: "combatAction", description: "Draw, stow, or ready a piece of gear for use." },
  { id: "prepare", label: "Prepare", category: "free", cost: 0, handler: "combatAction", state: "preparedInterrupt", description: "Declare a trigger now so you can interrupt later." },
  { id: "drop", label: "Drop", category: "free", cost: 0, handler: "combatAction", description: "Release or discard something you are holding." },
  { id: "communicate", label: "Communicate", category: "free", cost: 0, handler: "combatAction", description: "Speak, signal, or coordinate without changing the mechanics." },
  { id: "adjust", label: "Adjust", category: "free", cost: 0, handler: "combatAction", description: "Make a small physical adjustment or quick correction." },
  { id: "activateItem", label: "Activate Item", category: "free", cost: 0, handler: "combatAction", description: "Switch on or initialize an item without resolving its full effect." },
  { id: "react", label: "React", category: "reaction", cost: 0, handler: "combatAction", description: "Take a generic response to an outside trigger." },
  { id: "evade", label: "Evade", category: "reaction", cost: 0, handler: "combatEvade", description: "Avoid or soften incoming non-direct danger." },
  { id: "opportunity", label: "Opportunity", category: "reaction", cost: 0, handler: "combatAttack", description: "Exploit an opening and make a reactive attack." },
  { id: "assist", label: "Assist", category: "reaction", cost: 0, handler: "combatAssist", description: "Support another combatant when their moment comes." },
  { id: "interrupt", label: "Interrupt", category: "reaction", cost: 0, handler: "combatInterrupt", description: "Resolve a prepared response when its trigger is met." },
  { id: "reduceBurn", label: "Reduce Burn", category: "standard", cost: 1, handler: "combatReduceBurn", prominentWhenBurning: !0, description: "Take a breather and bring your Burn down by one." },
  { id: "overloadCheck", label: "Overload Check", category: "recovery", cost: 0, handler: "combatOverloadCheck", roll: { intent: "overload" }, prominentWhenBurning: !0, description: "Roll to see whether mounting Burn pushes you into overload." }
].map((i) => Object.freeze(fo(i)))), _S = new Map(hp.map((i) => [i.id, i]));
function fo(i) {
  return JSON.parse(JSON.stringify(i ?? null));
}
function bd(i, e = !1) {
  if (typeof i == "boolean") return i;
  const t = String(i ?? "").trim().toLowerCase();
  return ["true", "1", "yes", "y", "on"].includes(t) ? !0 : ["false", "0", "no", "n", "off", ""].includes(t) ? !1 : e;
}
function xS(i, e = null) {
  var n;
  const t = String((i == null ? void 0 : i.rollIntent) ?? ((n = i == null ? void 0 : i.roll) == null ? void 0 : n.intent) ?? (e == null ? void 0 : e.intent) ?? "").trim();
  return t ? { intent: t } : null;
}
function $S(i, { strict: e = !1, index: t = 0 } = {}) {
  const n = String((i == null ? void 0 : i.id) ?? "").trim(), a = _S.get(n) ?? {}, r = `Row ${t + 1}`, s = [];
  n || s.push(`${r}: id cannot be blank.`);
  const o = String((i == null ? void 0 : i.category) ?? a.category ?? "").trim();
  hd.has(o) || s.push(`${r}: category must be one of ${Array.from(hd).join(", ")}.`);
  const l = String((i == null ? void 0 : i.label) ?? a.label ?? "").trim();
  l || s.push(`${r}: label cannot be blank.`);
  const c = (i == null ? void 0 : i.cost) ?? a.cost ?? 0, u = Number(c);
  (!Number.isFinite(u) || u < 0) && s.push(`${r}: cost must be a non-negative number.`);
  const d = String((i == null ? void 0 : i.handler) ?? a.handler ?? "").trim();
  if (yd.has(d) || s.push(`${r}: handler must be one of ${Array.from(yd).map((p) => p || "(blank)").join(", ")}.`), s.length) {
    if (e) {
      const p = new Error(s[0]);
      throw p.validationErrors = s, p;
    }
    return null;
  }
  const m = {
    ...fo(a),
    id: n,
    label: l,
    category: o,
    cost: Math.trunc(u),
    handler: d,
    description: String((i == null ? void 0 : i.description) ?? a.description ?? "").trim(),
    reason: String((i == null ? void 0 : i.reason) ?? a.reason ?? "").trim(),
    prominent: bd(i == null ? void 0 : i.prominent, !!a.prominent),
    prominentWhenBurning: bd(i == null ? void 0 : i.prominentWhenBurning, !!a.prominentWhenBurning)
  };
  m.id === "opportunity" && !m.handler && (m.handler = "combatAttack", m.reason = ""), m.id === "opportunity" && m.handler === "combatAttack" && m.reason === "Opportunity attacks are not yet implemented." && (m.reason = ""), m.id === "assist" && !m.handler && (m.handler = "combatAssist", m.reason = ""), m.id === "assist" && m.handler === "combatAssist" && m.reason === "Reaction assist effects are not yet implemented." && (m.reason = ""), m.id === "interrupt" && !m.handler && (m.handler = "combatInterrupt", m.reason = ""), m.id === "interrupt" && m.handler === "combatInterrupt" && m.reason === "Prepared interrupt resolution is not yet implemented." && (m.reason = "");
  const f = xS(i, a.roll ?? null);
  return f ? m.roll = f : delete m.roll, m.reason || delete m.reason, m.description || delete m.description, m.prominent || delete m.prominent, m.prominentWhenBurning || delete m.prominentWhenBurning, m;
}
function lu() {
  return fo(hp);
}
function jr(i, { strict: e = !1 } = {}) {
  if (!Array.isArray(i)) {
    if (e) {
      const r = new Error("Action catalog must be an array.");
      throw r.validationErrors = [r.message], r;
    }
    return lu();
  }
  const t = [], n = /* @__PURE__ */ new Set(), a = [];
  if (i.forEach((r, s) => {
    try {
      const o = $S(r, { strict: e, index: s });
      if (!o) return;
      const l = o.id.toLowerCase();
      if (n.has(l)) {
        const c = `Row ${s + 1}: duplicate action id "${o.id}".`;
        e && a.push(c);
        return;
      }
      n.add(l), t.push(o);
    } catch (o) {
      e && a.push(...Array.isArray(o.validationErrors) ? o.validationErrors : [o.message]);
    }
  }), a.length) {
    const r = new Error(a[0]);
    throw r.validationErrors = a, r;
  }
  return t;
}
function yp() {
  var i, e;
  try {
    const t = (e = (i = game == null ? void 0 : game.settings) == null ? void 0 : i.get) == null ? void 0 : e.call(i, M, pp);
    return jr(t, { strict: !1 });
  } catch {
    return lu();
  }
}
function ps(i) {
  const e = String(i ?? "").trim();
  return yp().find((t) => t.id === e) ?? null;
}
function BS(i) {
  return yp().filter((e) => e.category === i).map((e) => Object.freeze(fo(e)));
}
const Qn = "hazard";
function zS(i) {
  return i && typeof i == "object" ? i : {};
}
function In(i) {
  var a, r, s;
  const e = ((a = i == null ? void 0 : i.getFlag) == null ? void 0 : a.call(i, "mwd", Qn)) ?? ((s = (r = i == null ? void 0 : i.flags) == null ? void 0 : r.mwd) == null ? void 0 : s[Qn]) ?? null;
  if (!e || typeof e != "object") return null;
  const t = on(e.areaEffect ?? { kind: ai.persistent, hazard: e.hazardDef }), n = ct(
    e.templateGeometry,
    {
      template: e.template,
      placement: e.templatePlacement
    }
  ) ?? null;
  return {
    ...foundry.utils.deepClone(zS(e)),
    areaEffect: t,
    hazardDef: Gc(e.hazardDef ?? t.hazard ?? {}),
    templateGeometry: n
  };
}
function FS(i) {
  return !!In(i);
}
async function jo(i) {
  var n, a, r;
  const e = ((n = i == null ? void 0 : i.getFlag) == null ? void 0 : n.call(i, "mwd", Qn)) ?? ((r = (a = i == null ? void 0 : i.flags) == null ? void 0 : a.mwd) == null ? void 0 : r[Qn]) ?? null;
  if (!e || typeof e != "object") return null;
  if (e != null && e.templateGeometry) return In(i);
  const t = In(i);
  return !(t != null && t.templateGeometry) || !(i != null && i.setFlag) ? t : (await i.setFlag("mwd", Qn, {
    ...foundry.utils.deepClone(e),
    templateGeometry: ji(t.templateGeometry)
  }), In(i));
}
async function HS({ attacker: i = null, attack: e = {}, targetResult: t = null } = {}) {
  var c, u, d, m, f, p, g, h, y, b, S, w;
  const n = (canvas == null ? void 0 : canvas.scene) ?? null;
  if (!n) return null;
  const a = ct(
    e == null ? void 0 : e.templateGeometry,
    {
      template: e == null ? void 0 : e.template,
      placement: e == null ? void 0 : e.templatePlacement
    }
  ), r = on((e == null ? void 0 : e.areaEffect) ?? ((c = e == null ? void 0 : e.payload) == null ? void 0 : c.areaEffect) ?? {});
  if (r.kind !== ai.persistent || !a) return null;
  const s = ao(a);
  if (!s.length) return null;
  const o = {
    sourceActorUuid: (i == null ? void 0 : i.uuid) ?? null,
    sourceItemUuid: ((u = e == null ? void 0 : e.weapon) == null ? void 0 : u.uuid) ?? null,
    payloadId: ((d = e == null ? void 0 : e.payloadState) == null ? void 0 : d.activePayloadId) ?? ((m = e == null ? void 0 : e.payload) == null ? void 0 : m.id) ?? "",
    templateGeometry: ji(a),
    templatePlacement: foundry.utils.deepClone((e == null ? void 0 : e.templatePlacement) ?? null),
    template: foundry.utils.deepClone((e == null ? void 0 : e.template) ?? null),
    damage: Number(((f = t == null ? void 0 : t.damage) == null ? void 0 : f.effectiveWeaponDamage) ?? ((p = e == null ? void 0 : e.weapon) == null ? void 0 : p.damage) ?? 0) || 0,
    ap: Number((e == null ? void 0 : e.totalAp) ?? ((g = e == null ? void 0 : e.weapon) == null ? void 0 : g.ap) ?? 0) || 0,
    damageType: String(((h = t == null ? void 0 : t.damage) == null ? void 0 : h.damageType) ?? ((y = e == null ? void 0 : e.weapon) == null ? void 0 : y.damageType) ?? "concussive").trim() || "concussive",
    label: `${String(((b = e == null ? void 0 : e.weapon) == null ? void 0 : b.name) ?? "Hazard").trim() || "Hazard"} (${bi(((S = r.hazard) == null ? void 0 : S.startExposure) ?? "minor")})`,
    areaEffect: r,
    hazardDef: r.hazard
  }, [l] = await n.createEmbeddedDocuments("Region", [{
    name: o.label,
    color: ((w = game.user) == null ? void 0 : w.color) ?? "#d86a2c",
    shapes: s,
    flags: {
      mwd: {
        [Qn]: o
      }
    }
  }]);
  return l ?? null;
}
function Sd(i = null) {
  var r, s, o, l;
  const e = (i == null ? void 0 : i.document) ?? i ?? null;
  if (!e) return [];
  const t = (e == null ? void 0 : e.parent) ?? (canvas == null ? void 0 : canvas.scene) ?? null;
  if (!t) return [];
  const n = ((r = e == null ? void 0 : e.object) == null ? void 0 : r.center) ?? (e == null ? void 0 : e.center) ?? {
    x: Number((e == null ? void 0 : e.x) ?? 0) + (Number((e == null ? void 0 : e.width) ?? 1) || 1) * (Number(((s = canvas == null ? void 0 : canvas.grid) == null ? void 0 : s.size) ?? 100) || 100) / 2,
    y: Number((e == null ? void 0 : e.y) ?? 0) + (Number((e == null ? void 0 : e.height) ?? 1) || 1) * (Number(((o = canvas == null ? void 0 : canvas.grid) == null ? void 0 : o.size) ?? 100) || 100) / 2
  }, a = {
    x: Number((n == null ? void 0 : n.x) ?? 0) || 0,
    y: Number((n == null ? void 0 : n.y) ?? 0) || 0,
    elevation: Number((e == null ? void 0 : e.elevation) ?? ((l = e == null ? void 0 : e.object) == null ? void 0 : l.elevation) ?? 0) || 0
  };
  return Array.from(t.regions ?? []).filter(FS).filter((c) => {
    var u, d, m;
    if ((d = (u = c == null ? void 0 : c.tokens) == null ? void 0 : u.has) != null && d.call(u, e)) return !0;
    try {
      return ((m = c == null ? void 0 : c.testPoint) == null ? void 0 : m.call(c, a)) ?? !1;
    } catch {
      return !1;
    }
  });
}
function gn(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? t : e;
}
function US({ actor: i = null, token: e = null } = {}) {
  var r;
  const t = (e == null ? void 0 : e.document) ?? e ?? null, n = String(((r = t == null ? void 0 : t.texture) == null ? void 0 : r.src) ?? "").trim(), a = String((i == null ? void 0 : i.img) ?? "").trim();
  return n || a || "icons/svg/mystery-man.svg";
}
function jS(i = []) {
  return Array.isArray(i) ? i.map((e) => ({
    key: String((e == null ? void 0 : e.key) ?? "").trim(),
    label: String((e == null ? void 0 : e.label) ?? (e == null ? void 0 : e.key) ?? "").trim(),
    value: gn(e == null ? void 0 : e.value, 0)
  })).filter((e) => e.key) : [];
}
function cu(i = {}) {
  var c, u, d;
  const e = (i == null ? void 0 : i.preview) ?? {}, t = lt(((c = i == null ? void 0 : i.exposure) == null ? void 0 : c.initialTier) ?? (i == null ? void 0 : i.exposureTier) ?? (i == null ? void 0 : i.tier), "none"), n = lt(
    ((u = i == null ? void 0 : i.exposure) == null ? void 0 : u.finalTier) ?? (e.evadeActive ? e.finalTier : t),
    t
  ), a = Rn({
    tier: t,
    appliedTier: n,
    evadeUsed: !!(e.evadeActive && t !== n),
    evadeLocked: !!((i == null ? void 0 : i.evadeLocked) ?? ((d = i == null ? void 0 : i.exposure) == null ? void 0 : d.evadeLocked))
  }), r = Math.max(0, gn(i == null ? void 0 : i.baseDamage, 0)), s = Math.max(0, gn(i == null ? void 0 : i.damageBefore, Zn(r, a.initialTier))), o = Math.max(0, gn(i == null ? void 0 : i.damageAfter, Zn(r, a.finalTier))), l = (e == null ? void 0 : e.reactionPreview) ?? {};
  return {
    kind: String((i == null ? void 0 : i.kind) ?? "hazard").trim() || "hazard",
    eventType: String((i == null ? void 0 : i.eventType) ?? "entry").trim() || "entry",
    regionId: String((i == null ? void 0 : i.regionId) ?? "").trim(),
    regionName: String((i == null ? void 0 : i.regionName) ?? (i == null ? void 0 : i.label) ?? "Hazard").trim() || "Hazard",
    actorUuid: String((i == null ? void 0 : i.actorUuid) ?? "").trim() || null,
    tokenUuid: String((i == null ? void 0 : i.tokenUuid) ?? "").trim() || null,
    actorName: String((i == null ? void 0 : i.actorName) ?? "Target").trim() || "Target",
    turnsExposed: Math.max(0, gn(i == null ? void 0 : i.turnsExposed, 0)),
    baseDamage: r,
    ap: Math.max(0, gn(i == null ? void 0 : i.ap, 0)),
    damageType: ci(i == null ? void 0 : i.damageType, "concussive"),
    damageTypeLabel: Pi((i == null ? void 0 : i.damageType) ?? "concussive") || "Damage",
    source: String((i == null ? void 0 : i.source) ?? (i == null ? void 0 : i.regionName) ?? "Hazard").trim() || "Hazard",
    applied: !!(i != null && i.applied),
    applyReason: String((i == null ? void 0 : i.applyReason) ?? "").trim(),
    onFullBurnDelta: Math.max(0, gn(i == null ? void 0 : i.onFullBurnDelta, 0)),
    exposure: a,
    damageBefore: s,
    damageAfter: o,
    nextTier: lt(i == null ? void 0 : i.nextTier, a.finalTier),
    nextLabel: bi((i == null ? void 0 : i.nextTier) ?? a.finalTier),
    preview: {
      evadeActive: !!e.evadeActive,
      edgePoolKey: String(e.edgePoolKey ?? "").trim() || null,
      finalTier: a.finalTier,
      burnDelta: gn(l == null ? void 0 : l.burnDelta, 0),
      canSpendEdge: !!(l != null && l.canSpendEdge),
      edgePools: jS(l == null ? void 0 : l.edgePools)
    }
  };
}
function KS(i = {}, { actor: e = null, token: t = null } = {}) {
  const n = cu(i), a = n.eventType === "tick" ? "Hazard Tick" : n.eventType === "exit" ? "Hazard Exit" : "Hazard Entry", r = n.exposure.initialLabel === n.exposure.finalLabel ? n.exposure.initialLabel : `${n.exposure.initialLabel} -> ${n.exposure.finalLabel}`, s = n.damageBefore === n.damageAfter ? String(n.damageAfter) : `${n.damageBefore} -> ${n.damageAfter}`;
  return {
    classes: [
      "mwd-chat-card",
      "mwd-hazard-card",
      `is-${n.exposure.finalTier}`,
      n.applied ? "is-applied" : ""
    ].filter(Boolean).join(" "),
    header: {
      left: n.regionName,
      right: a
    },
    target: {
      name: n.actorName,
      image: US({ actor: e, token: t })
    },
    rows: [
      { label: "Exposure", value: r },
      { label: "Damage", value: s },
      { label: "Next Tier", value: n.nextLabel },
      { label: "Turns Exposed", value: String(n.turnsExposed) }
    ],
    reactionHint: n.exposure.evadeLocked ? "Evade is locked for this hazard." : n.preview.evadeActive ? n.preview.edgePoolKey ? "Evade active. Reaction Burn canceled by Edge." : n.preview.burnDelta > 0 ? `Evade active. This reaction adds +${n.preview.burnDelta} Burn.` : "Evade active." : "",
    actions: n.applied ? [] : GS(n)
  };
}
function GS(i = {}) {
  const e = [];
  if (!i.exposure.evadeLocked && i.exposure.initialTier !== "none" && e.push({
    action: "toggleHazardEvade",
    label: i.preview.evadeActive ? "Clear Evade" : "Use Reaction",
    cssClass: `mwd-target-row__action ${i.preview.evadeActive ? "is-active" : ""}`
  }), i.preview.evadeActive && i.preview.canSpendEdge)
    for (const t of i.preview.edgePools)
      e.push({
        action: "toggleHazardEvadeEdge",
        label: i.preview.edgePoolKey === t.key ? `Edge: ${t.label}` : `Use ${t.label}`,
        cssClass: `mwd-target-row__action ${i.preview.edgePoolKey === t.key ? "is-active" : ""}`,
        dataset: { "pool-key": t.key }
      });
  return e.push({
    action: "applyHazardTick",
    label: "Apply",
    cssClass: "mwd-target-row__action mwd-apply-attack-damage"
  }), e;
}
async function bp(i = {}, { actor: e = null, token: t = null } = {}) {
  return foundry.applications.handlebars.renderTemplate(
    "mwd.v2.roll.mwd-hazard-card",
    KS(i, { actor: e, token: t })
  );
}
function WS(i, e) {
  const t = Math.max(0, e.runningHot ?? e.hot ?? 0), n = Math.max(0, e.overheated ?? e.overheat ?? 0), a = Math.max(0, e.shutdown ?? e.danger ?? 0), r = Math.max(0, i);
  if (t === 0 || n === 0 || a === 0)
    return { levelsInHot: 0, levelsInOverheat: 0, levelsInDanger: 0 };
  const s = Math.max(0, Math.min(r, n - 1) - (t - 1)), o = Math.max(0, Math.min(r, a - 1) - (n - 1)), l = Math.max(0, r - (a - 1));
  return { levelsInHot: s, levelsInOverheat: o, levelsInDanger: l };
}
function uu(i, e) {
  const { levelsInHot: t, levelsInOverheat: n, levelsInDanger: a } = WS(i, e);
  return {
    movementPenalty: t,
    rangedDicePenalty: n,
    dangerLevel: a
  };
}
function qS(i, e, t, n = 1 / 0) {
  return Math.max(0, i + e - t);
}
function Sp(i, e, t) {
  return {
    shutdownDN: i,
    shutdownPool: Math.max(0, e + t),
    explosionDN: 1,
    explosionPool: Math.max(1, e + t - i)
  };
}
function VS(i = {}) {
  return Object.values(i).some(
    (e) => (e == null ? void 0 : e.enabled) && !(e != null && e.destroyed) && Array.isArray(e == null ? void 0 : e.tags) && e.tags.includes("ammoStore")
  );
}
const Ad = Object.freeze({
  safe: "Safe",
  hot: "Hot",
  overheat: "Overheat",
  danger: "Danger",
  runningHot: "Hot",
  overheated: "Overheat",
  shutdown: "Danger"
});
function or(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? t : e;
}
function du(i = {}, e = 0) {
  const t = Math.max(0, or(e, 0)), n = Math.max(0, or(i.hot ?? i.runningHot, 2)), a = Math.max(0, or(i.overheat ?? i.overheated, 3)), r = Math.max(0, or(i.danger ?? i.shutdown, t || 4));
  return {
    hot: n,
    overheat: a,
    danger: r,
    runningHot: n,
    overheated: a,
    shutdown: r
  };
}
function Ap(i = 0, e = {}, t = 0) {
  const n = Math.max(0, or(i, 0)), a = du(e, t);
  return n >= a.danger ? "danger" : n >= a.overheat ? "overheat" : n >= a.hot ? "hot" : "safe";
}
function mu(i = "safe") {
  return Ad[String(i ?? "").trim()] ?? Ad.safe;
}
const YS = /* @__PURE__ */ new Set([A.actorTypes.vehicle, A.actorTypes.battlemech]), JS = Object.freeze({
  head: "Head",
  torso: "Torso",
  arms: "Arms",
  legs: "Legs",
  core: "Core",
  front: "Front",
  side: "Side",
  rear: "Rear",
  turret: "Turret",
  rotor: "Rotor"
});
function QS(i) {
  const e = Math.trunc(Number(i ?? 0));
  return Number.isFinite(e) ? Math.min(18, Math.max(3, e)) : 10;
}
function wp(i = null) {
  return String((i == null ? void 0 : i.type) ?? i ?? "").trim();
}
function XS(i = null) {
  var t, n;
  const e = ((n = (t = i == null ? void 0 : i.system) == null ? void 0 : t.mwd) == null ? void 0 : n.locations) ?? {};
  return Object.entries(e).filter(([, a]) => (a == null ? void 0 : a.enabled) !== !1).map(([a]) => a);
}
function je(i, e = [], t = "core") {
  const n = new Set(XS(i));
  return e.find((a) => n.has(a)) ?? e[0] ?? t;
}
function ZS(i = "") {
  return i === "head" ? "head" : i === "arms" ? "arms" : i === "legs" ? "legs" : ["front", "side", "rear", "rotor"].includes(i) ? "motive" : i === "turret" ? "weapon" : i === "torso" ? "torso" : "core";
}
function eA(i, e) {
  return e <= 4 ? { locationKey: je(i, ["torso", "head"]), family: "critical" } : e === 5 ? { locationKey: je(i, ["legs", "torso"]), family: "legs" } : e === 6 ? { locationKey: je(i, ["legs", "torso"]), family: "legs" } : e === 7 ? { locationKey: je(i, ["arms", "torso"]), family: "arms" } : e === 8 ? { locationKey: je(i, ["arms", "torso"]), family: "arms" } : e <= 13 ? { locationKey: je(i, ["torso", "head"]), family: "torso" } : e === 14 ? { locationKey: je(i, ["arms", "torso"]), family: "arms" } : e === 15 ? { locationKey: je(i, ["arms", "torso"]), family: "arms" } : e === 16 ? { locationKey: je(i, ["arms", "torso"]), family: "arms" } : e === 17 ? { locationKey: je(i, ["legs", "torso"]), family: "legs" } : { locationKey: je(i, ["head", "torso"]), family: "head" };
}
function tA(i, e) {
  return e <= 4 ? { locationKey: je(i, ["core", "front"]), family: "critical" } : e === 5 ? { locationKey: je(i, ["front", "core"]), family: "motive" } : e <= 7 ? { locationKey: je(i, ["side", "front"]), family: "motive" } : e === 8 ? { locationKey: je(i, ["rear", "side"]), family: "motive" } : e === 9 ? { locationKey: je(i, ["front", "core"]), family: "motive" } : e === 10 ? { locationKey: je(i, ["core", "front"]), family: "core" } : e === 11 ? { locationKey: je(i, ["turret", "core"]), family: "weapon" } : e === 12 ? { locationKey: je(i, ["side", "front"]), family: "motive" } : e === 13 ? { locationKey: je(i, ["rear", "side"]), family: "motive" } : e === 14 ? { locationKey: je(i, ["front", "side"]), family: "motive" } : e === 15 ? { locationKey: je(i, ["core", "rear"]), family: "core" } : e === 16 ? { locationKey: je(i, ["turret", "core"]), family: "weapon" } : e === 17 ? { locationKey: je(i, ["side", "front", "rotor"]), family: "motive" } : { locationKey: je(i, ["core", "front"]), family: "core" };
}
function Mr(i = "") {
  return JS[i] ?? (String(i ?? "").trim() || "Location");
}
function Cr(i = null) {
  return YS.has(wp(i));
}
function Tp() {
  if (typeof Roll == "function")
    try {
      const i = new Roll("3d6"), e = i.evaluate({ async: !1 });
      return Number((e == null ? void 0 : e.total) ?? i.total ?? 10) || 10;
    } catch {
    }
  return Array.from({ length: 3 }, () => 1 + Math.floor(Math.random() * 6)).reduce((i, e) => i + e, 0);
}
function kp({
  actor: i = null,
  rollTotal: e = Tp(),
  armorBefore: t = 0,
  structureBefore: n = 0
} = {}) {
  const a = wp(i), r = QS(e), s = Math.max(0, Number(t ?? 0) || 0) <= 0, o = a === A.actorTypes.battlemech ? eA(i, r) : tA(i, r), l = r <= 4, c = s && r >= 16, u = l || c, d = !u && r >= 16, m = r === 18 && a === A.actorTypes.battlemech ? je(i, ["torso", "head"]) : o.locationKey, f = o.family || ZS(o.locationKey);
  return {
    rollTotal: r,
    actorType: a,
    locationKey: o.locationKey,
    locationLabel: Mr(o.locationKey),
    locationFamily: f,
    isForcedCritical: l,
    isStructureCritical: c,
    isAutomaticCritical: u,
    chaosCriticalOption: d,
    chaosTargetLocationKey: m,
    chaosTargetLocationLabel: Mr(m),
    descriptiveOnly: !u,
    pureStructureHit: s,
    armorBefore: Math.max(0, Number(t ?? 0) || 0),
    structureBefore: Math.max(0, Number(n ?? 0) || 0)
  };
}
function We(i, e = {}) {
  return Object.freeze({
    key: i,
    label: e.label ?? i,
    attributeKey: String(e.attributeKey ?? "").trim(),
    skillKey: String(e.skillKey ?? "").trim(),
    resource: String(e.resource ?? "sa").trim() || "sa",
    cost: Math.max(0, Number(e.cost ?? 0) || 0),
    category: String(e.category ?? "simple").trim() || "simple",
    remediable: e.remediable !== !1,
    intent: String(e.intent ?? "").trim(),
    use: String(e.use ?? "").trim(),
    notes: String(e.notes ?? "").trim()
  });
}
const wd = Object.freeze({
  none: We("none", {
    label: "No Field Remedy",
    remediable: !1,
    resource: "sa",
    cost: 0,
    category: "none"
  }),
  emergencyRepair: We("emergencyRepair", {
    label: "Emergency Repair",
    attributeKey: "reliability",
    skillKey: "technician",
    cost: 2,
    category: "complex"
  }),
  systemReset: We("systemReset", {
    label: "System Reset",
    attributeKey: "reliability",
    skillKey: "systemOps",
    cost: 1,
    category: "simple"
  }),
  reboot: We("reboot", {
    label: "Reboot",
    attributeKey: "reliability",
    skillKey: "computers",
    cost: 1,
    category: "simple"
  }),
  feedReset: We("feedReset", {
    label: "Feed Reset",
    attributeKey: "reliability",
    skillKey: "gunnery",
    cost: 1,
    category: "simple"
  }),
  pilotRecovery: We("pilotRecovery", {
    label: "Pilot Recovery",
    attributeKey: "reliability",
    skillKey: "piloting",
    cost: 1,
    category: "simple"
  }),
  coolantDump: We("coolantDump", {
    label: "Coolant Dump",
    attributeKey: "reliability",
    skillKey: "systemOps",
    cost: 1,
    category: "simple",
    notes: "Restore the heat-management system."
  }),
  stand: We("stand", {
    label: "Stand",
    attributeKey: "handling",
    skillKey: "piloting",
    cost: 1,
    category: "simple"
  }),
  stabalize: We("stabalize", {
    label: "Stabilize",
    attributeKey: "handling",
    skillKey: "piloting",
    cost: 2,
    category: "complex"
  }),
  stabilize: We("stabilize", {
    label: "Stabilize",
    attributeKey: "handling",
    skillKey: "piloting",
    cost: 2,
    category: "complex"
  }),
  powerReroute: We("powerReroute", {
    label: "Power Reroute",
    attributeKey: "system",
    skillKey: "technician",
    cost: 1,
    category: "simple"
  }),
  epmFilter: We("epmFilter", {
    label: "EPM Filter",
    attributeKey: "system",
    skillKey: "perception",
    cost: 2,
    category: "complex"
  }),
  powerCycle: We("powerCycle", {
    label: "Power Cycle",
    attributeKey: "system",
    skillKey: "computers",
    cost: 2,
    category: "complex"
  }),
  acquireTarget: We("acquireTarget", {
    label: "Acquire Target",
    attributeKey: "system",
    skillKey: "perception",
    cost: 1,
    category: "simple",
    intent: "acquire"
  }),
  generateFireSolution: We("generateFireSolution", {
    label: "Generate Fire Solution",
    attributeKey: "system",
    skillKey: "gunnery",
    cost: 1,
    category: "simple",
    intent: "targeting"
  }),
  breakLock: We("breakLock", {
    label: "Break Lock",
    attributeKey: "handling",
    skillKey: "systemOps",
    cost: 0,
    category: "reaction",
    notes: "Degrade detection state from an attacker."
  }),
  ecmSpike: We("ecmSpike", {
    label: "ECM Spike",
    attributeKey: "system",
    skillKey: "systemOps",
    cost: 1,
    category: "simple",
    notes: "Apply ECM Jamming."
  }),
  sprint: We("sprint", {
    label: "Sprint",
    attributeKey: "handling",
    skillKey: "piloting",
    cost: 1,
    category: "simple",
    notes: "Third move action in the activation."
  }),
  extinguish: We("extinguish", {
    label: "Extinguish",
    attributeKey: "system",
    skillKey: "piloting",
    cost: 2,
    category: "complex"
  }),
  swat: We("swat", {
    label: "Swat",
    attributeKey: "handling",
    skillKey: "piloting",
    cost: 2,
    category: "complex",
    notes: "Remove BattleArmor or NARC."
  }),
  emergencyJettison: We("emergencyJettison", {
    label: "Emergency Jettison",
    attributeKey: "system",
    skillKey: "gunnery",
    cost: 0,
    category: "reaction"
  }),
  jettisonCore: We("jettisonCore", {
    label: "Jettison Core",
    attributeKey: "system",
    skillKey: "technician",
    cost: 2,
    category: "complex"
  }),
  reposition: We("reposition", {
    label: "Reposition",
    attributeKey: "handling",
    skillKey: "piloting",
    cost: 1,
    category: "simple"
  }),
  toggle: We("toggle", {
    label: "Toggle",
    attributeKey: "",
    skillKey: "",
    cost: 0,
    category: "free"
  }),
  majorRepair: We("majorRepair", {
    label: "Major Repair",
    attributeKey: "reliability",
    skillKey: "technician",
    cost: 0,
    category: "narrative",
    remediable: !1
  })
}), iA = Object.freeze({
  stabilize: "stabalize"
});
function Mp(i = "") {
  const e = String(i ?? "").trim(), t = iA[e] ?? e;
  return wd[t] ?? wd.none;
}
function wt(i, e = {}) {
  const t = Mp(i);
  return Object.freeze({
    key: t.key,
    label: e.label ?? t.label,
    actionId: e.actionId ?? `machineCrit${t.key.charAt(0).toUpperCase()}${t.key.slice(1)}`,
    actionLabel: e.actionLabel ?? t.label,
    resource: t.resource,
    cost: t.cost,
    category: t.category,
    remediable: e.remediable ?? t.remediable,
    skillKey: e.skillKey ?? t.skillKey,
    baseDn: Number(e.baseDn ?? t.cost ?? 0)
  });
}
const xl = Object.freeze(Object.fromEntries(
  [
    ["none", wt("none", { baseDn: 0 })],
    ["emergencyRepair", wt("emergencyRepair", { baseDn: 2 })],
    ["systemReset", wt("systemReset", { baseDn: 2 })],
    ["coolantDump", wt("coolantDump", { baseDn: 1 })],
    ["reboot", wt("reboot", { baseDn: 2 })],
    ["feedReset", wt("feedReset", { label: "Feed Reset", baseDn: 1 })],
    ["pilotRecovery", wt("pilotRecovery", { baseDn: 2 })],
    ["stand", wt("stand", { baseDn: 1 })],
    ["stabalize", wt("stabalize", { baseDn: 2 })],
    ["powerReroute", wt("powerReroute", { baseDn: 2 })],
    ["epmFilter", wt("epmFilter", { baseDn: 2 })],
    ["powerCycle", wt("powerCycle", { baseDn: 2 })],
    ["acquireTarget", wt("acquireTarget", { baseDn: 2 })],
    ["jettisonCore", wt("jettisonCore", { baseDn: 3 })],
    ["breakLock", wt("breakLock", { baseDn: 2 })],
    ["extinguish", wt("extinguish", { baseDn: 2 })],
    ["reposition", wt("reposition", { baseDn: 0 })],
    ["toggle", wt("toggle", { baseDn: 0, remediable: !0 })],
    ["majorRepair", wt("majorRepair", { baseDn: 0, remediable: !1 })]
  ]
));
function Pn(i = "") {
  const e = String(i ?? "").trim();
  return xl[e] ?? xl.emergencyRepair;
}
function nA(i = "") {
  return Object.prototype.hasOwnProperty.call(xl, String(i ?? "").trim());
}
function aA(i = {}) {
  return [
    i == null ? void 0 : i.key,
    i == null ? void 0 : i.label,
    i == null ? void 0 : i.locationLabel,
    ...Array.isArray(i == null ? void 0 : i.gates) ? i.gates : [],
    ...Array.isArray(i == null ? void 0 : i.mods) ? i.mods : []
  ].map((e) => String(e ?? "").trim()).filter(Boolean).join(" ").toLowerCase();
}
function po(i = {}, e = null) {
  const t = e ?? Pn((i == null ? void 0 : i.remedyKey) ?? ""), n = String((i == null ? void 0 : i.remedySkillKey) ?? "").trim();
  if (n) return n;
  if (t.key === "systemReset") {
    const a = aA(i);
    if (/(sensor|optic|target|tracking|communications|comms|processor|computer|fire-control|fire control)/i.test(a))
      return "computers";
  }
  return String(t.skillKey ?? "").trim();
}
function go(i = {}, e = null) {
  const t = e ?? Pn((i == null ? void 0 : i.remedyKey) ?? ""), n = Number(i == null ? void 0 : i.remedyBaseDn);
  if (Number.isFinite(n) && n >= 0) return n;
  const a = Number(t.baseDn ?? t.cost ?? 1);
  return Number.isFinite(a) ? Math.max(0, a) : 1;
}
function fu(i = {}) {
  const e = (i == null ? void 0 : i.remedyEffect) ?? {}, t = String((e == null ? void 0 : e.onSuccess) ?? "clear").trim() || "clear", n = String((e == null ? void 0 : e.onFailure) ?? "noChange").trim() || "noChange";
  return { onSuccess: t, onFailure: n };
}
const Ot = Object.freeze({
  intact: 0,
  impaired: 1,
  damaged: 2,
  crippled: 3,
  disabled: 4
}), Td = Object.freeze({
  [Ot.intact]: "Intact",
  [Ot.impaired]: "Impaired",
  [Ot.damaged]: "Damaged",
  [Ot.crippled]: "Crippled",
  [Ot.disabled]: "Disabled"
}), rA = Object.freeze({
  [Ot.intact]: 0,
  [Ot.impaired]: 1,
  [Ot.damaged]: 2,
  [Ot.crippled]: 3,
  [Ot.disabled]: 4
}), sA = Object.freeze({
  0: 1,
  1: 2,
  2: 3,
  3: 4,
  4: 6,
  5: 8
}), oA = Object.freeze([
  "head",
  "torso",
  "arms",
  "legs"
]), lA = Object.freeze([
  "front",
  "side",
  "rear",
  "core",
  "turret",
  "rotor"
]), cA = Object.freeze({
  head: Object.freeze({ enabled: !0, stress: 0, condition: 0, tags: ["cockpit", "sensor"], destroyed: !1 }),
  torso: Object.freeze({ enabled: !0, stress: 0, condition: 0, tags: ["weaponGroup", "engine", "gyro", "ammoStore"], destroyed: !1 }),
  arms: Object.freeze({ enabled: !0, stress: 0, condition: 0, tags: ["weaponGroup"], destroyed: !1 }),
  legs: Object.freeze({ enabled: !0, stress: 0, condition: 0, tags: ["motiveSystem"], destroyed: !1 })
}), uA = Object.freeze({
  front: Object.freeze({ enabled: !0, stress: 0, condition: 0, tags: ["weaponGroup", "motiveSystem"], destroyed: !1 }),
  side: Object.freeze({ enabled: !0, stress: 0, condition: 0, tags: ["weaponGroup", "motiveSystem"], destroyed: !1 }),
  rear: Object.freeze({ enabled: !0, stress: 0, condition: 0, tags: ["weaponGroup", "motiveSystem", "ammoStore"], destroyed: !1 }),
  turret: Object.freeze({ enabled: !0, stress: 0, condition: 0, tags: ["turret", "weaponGroup"], destroyed: !1 }),
  rotor: Object.freeze({ enabled: !1, stress: 0, condition: 0, tags: ["rotor"], destroyed: !1 }),
  core: Object.freeze({ enabled: !0, stress: 0, condition: 0, tags: ["crewCompartment", "engine", "ammoStore"], destroyed: !1 })
}), dA = Object.freeze({
  [A.actorTypes.battlemech]: Object.freeze({
    head: Object.freeze({ type: "cockpitCatastrophe", destroyed: !0, statusState: "destroyed" }),
    torso: Object.freeze({ type: "torsoCollapse", destroyed: !0, statusState: "destroyed" }),
    arms: Object.freeze({ type: "armSystemCollapse", destroyed: !0, statusState: "" }),
    legs: Object.freeze({ type: "legCollapse", destroyed: !0, statusState: "immobilized" })
  }),
  [A.actorTypes.vehicle]: Object.freeze({
    front: Object.freeze({ type: "hullCollapse", destroyed: !0, statusState: "destroyed" }),
    side: Object.freeze({ type: "hullCollapse", destroyed: !0, statusState: "destroyed" }),
    rear: Object.freeze({ type: "hullCollapse", destroyed: !0, statusState: "destroyed" }),
    core: Object.freeze({ type: "coreFailure", destroyed: !0, statusState: "destroyed" }),
    turret: Object.freeze({ type: "turretDestroyed", destroyed: !0, statusState: "" }),
    rotor: Object.freeze({ type: "rotorFailure", destroyed: !0, statusState: "destroyed" })
  })
});
function Xn(i) {
  var e;
  if (typeof foundry < "u" && typeof ((e = foundry == null ? void 0 : foundry.utils) == null ? void 0 : e.deepClone) == "function")
    try {
      return foundry.utils.deepClone(i);
    } catch {
    }
  return JSON.parse(JSON.stringify(i ?? null));
}
function Ve(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? t : e;
}
function pu(i, e, t) {
  return Math.min(t, Math.max(e, i));
}
function ua(i = null) {
  const e = String((i == null ? void 0 : i.type) ?? i ?? "").trim();
  return e === A.actorTypes.battlemech || e === "mech" ? A.actorTypes.battlemech : A.actorTypes.vehicle;
}
function mA(i = A.actorTypes.vehicle) {
  return i === A.actorTypes.battlemech ? cA : uA;
}
function Cp(i = A.actorTypes.vehicle) {
  return i === A.actorTypes.battlemech ? oA : lA;
}
function Dn(i = 0) {
  return pu(Math.trunc(Ve(i, 0)), 0, Ot.disabled);
}
function kd(i = {}, e = {}) {
  return {
    enabled: (i == null ? void 0 : i.enabled) !== void 0 ? !!i.enabled : !!e.enabled,
    stress: Math.max(0, Ve((i == null ? void 0 : i.stress) ?? (e == null ? void 0 : e.stress), 0)),
    condition: Dn((i == null ? void 0 : i.condition) ?? (e == null ? void 0 : e.condition) ?? 0),
    tags: Array.isArray(i == null ? void 0 : i.tags) ? i.tags.slice() : Array.isArray(e == null ? void 0 : e.tags) ? e.tags.slice() : [],
    destroyed: !!((i == null ? void 0 : i.destroyed) ?? (e == null ? void 0 : e.destroyed))
  };
}
function fA(i = {}) {
  const e = {
    head: ["head"],
    torso: ["torso", "torsoFront", "torsoRear", "core"],
    arms: ["arms", "leftArm", "rightArm"],
    legs: ["legs", "leftLeg", "rightLeg"]
  }, t = {};
  for (const [n, a] of Object.entries(e)) {
    const r = a.map((d) => [d, i == null ? void 0 : i[d]]).filter(([, d]) => d && typeof d == "object");
    if (!r.length) continue;
    const s = r.reduce((d, [, m]) => d + Math.max(0, Ve(m == null ? void 0 : m.stress, 0)), 0), o = r.reduce((d, [, m]) => Math.max(d, Dn(m == null ? void 0 : m.condition)), 0), l = r.some(([, d]) => (d == null ? void 0 : d.enabled) !== !1), c = r.some(([, d]) => (d == null ? void 0 : d.destroyed) === !0), u = Array.from(new Set(r.flatMap(([, d]) => Array.isArray(d == null ? void 0 : d.tags) ? d.tags : [])));
    t[n] = {
      enabled: l,
      stress: s,
      condition: o,
      destroyed: c,
      tags: u
    };
  }
  for (const [n, a] of Object.entries(i ?? {}))
    Object.hasOwn(t, n) || ["torsoFront", "torsoRear", "core", "leftArm", "rightArm", "leftLeg", "rightLeg"].includes(n) || (t[n] = a);
  return t;
}
function pA(i = {}, e = A.actorTypes.vehicle) {
  const t = mA(e), n = e === A.actorTypes.battlemech ? fA(i ?? {}) : i ?? {}, a = {};
  for (const [r, s] of Object.entries(t))
    a[r] = kd((n == null ? void 0 : n[r]) ?? {}, s);
  for (const [r, s] of Object.entries(n ?? {}))
    a[r] || (a[r] = kd(s, {}));
  return a;
}
function gA(i = {}, e = A.actorTypes.vehicle) {
  const t = i.mwd = i.mwd ?? {};
  return t.unitType = String(t.unitType ?? (e === A.actorTypes.battlemech ? "mech" : "vehicle")).trim() || (e === A.actorTypes.battlemech ? "mech" : "vehicle"), t.status = t.status ?? { state: "operational", reasons: [] }, t.crits = Array.isArray(t.crits) ? t.crits : [], t;
}
function vp(i = {}) {
  var a;
  const e = ((a = i == null ? void 0 : i.monitors) == null ? void 0 : a.structure) ?? {}, t = Math.max(0, Ve(e == null ? void 0 : e.max, 0));
  if (t <= 0) return null;
  const n = pu(Ve(e == null ? void 0 : e.value, 0), 0, t);
  return Math.max(0, t - n);
}
function hA(i = {}, e = A.actorTypes.vehicle) {
  var n;
  if (ua(e) !== A.actorTypes.vehicle || (vp(i) ?? 1) > 0) return i;
  const t = ((n = i == null ? void 0 : i.mwd) == null ? void 0 : n.locations) ?? {};
  for (const a of Object.values(t))
    !a || a.enabled !== !0 || (a.condition = Ot.disabled);
  return i;
}
function yA(i = {}) {
  var a, r;
  const e = i.attributes = i.attributes ?? {}, t = (a = e.reliability) == null ? void 0 : a.value, n = (r = e.condition) == null ? void 0 : r.value;
  return Math.max(0, Ve(t ?? n, 0));
}
function bA(i = [], e = -1) {
  return !Array.isArray(i) || e < 0 ? !1 : i.some((t) => Math.trunc(Ve(t, -1)) === e);
}
function Ep(i, e, t) {
  !e || !Number.isFinite(t) || t === 0 || (i.stressDelta[e] = Number(i.stressDelta[e] ?? 0) + t);
}
function SA(i = "") {
  const e = String(i ?? "").trim();
  return e === "highMargin" ? 3 : e === "hit" ? 2 : e === "graze" ? 1 : 0;
}
function AA(i, e, t) {
  if (e.stress !== i.stress) return e.stress - i.stress;
  if (e.condition !== i.condition) return e.condition - i.condition;
  if (i.condition >= Ot.disabled && e.condition < Ot.disabled) return 1;
  if (e.condition >= Ot.disabled && i.condition < Ot.disabled) return -1;
  const n = Cp(t), a = n.indexOf(i.key), r = n.indexOf(e.key);
  if (a !== r) {
    const s = a >= 0 ? a : Number.MAX_SAFE_INTEGER, o = r >= 0 ? r : Number.MAX_SAFE_INTEGER;
    return s - o;
  }
  return String(i.key).localeCompare(String(e.key));
}
function wA(i = {}, e = A.actorTypes.vehicle) {
  return Object.entries(i).filter(([, t]) => t && t.enabled === !0 && t.destroyed !== !0).map(([t, n]) => ({
    key: t,
    stress: Math.max(0, Ve(n == null ? void 0 : n.stress, 0)),
    condition: Dn(n == null ? void 0 : n.condition)
  })).sort((t, n) => AA(t, n, e));
}
function TA(i = {}, e = A.actorTypes.vehicle, t = "") {
  var a;
  const n = String(t ?? "").trim();
  if (n) {
    const r = i == null ? void 0 : i[n];
    if (r && r.enabled === !0 && r.destroyed !== !0) return n;
  }
  return ((a = wA(i, e)[0]) == null ? void 0 : a.key) ?? "";
}
function kA(i, e, t, n) {
  !t || !e || (i.fallbackEvents.push({
    location: t,
    type: e.type,
    destroyed: !!e.destroyed,
    statusState: String(e.statusState ?? "").trim()
  }), e.destroyed && (n[t].destroyed = !0), e.statusState && (i.statusState = e.statusState));
}
function MA({ result: i, locations: e, locationKey: t, reliability: n, threshold: a, currentShock: r }) {
  var c;
  const s = Math.max(0, n), o = Math.max(0, Ve((c = e == null ? void 0 : e[t]) == null ? void 0 : c.stress, 0)), l = Math.max(0, o - s);
  return e[t].stress = l, Ep(i, t, l - o), Math.max(0, r - Math.max(1, a));
}
function CA({ locations: i = {}, shockBefore: e = 0, shockGain: t = 0, threshold: n = 1, reliability: a = 0, spendableBefore: r = 0 } = {}) {
  return {
    stressDelta: {},
    shockDelta: 0,
    conditionAdvancements: [],
    reliabilitySpends: [],
    fallbackEvents: [],
    spendOpportunities: [],
    statusState: "",
    summary: {
      shockBefore: e,
      shockGain: t,
      shockAfter: e + t,
      threshold: n,
      reliability: a,
      reliabilitySpendableBefore: r,
      reliabilitySpendableAfter: r,
      selectedLocations: [],
      locationsBefore: Xn(i),
      locationsAfter: null
    }
  };
}
function vA(i, e = "critical") {
  if (typeof i == "string") {
    const n = String(i ?? "").trim();
    return n ? { locationKey: n, source: e, applyReductions: !0, allowSpend: !0 } : null;
  }
  if (!i || typeof i != "object") return null;
  const t = String(i.locationKey ?? i.location ?? "").trim();
  return t ? {
    locationKey: t,
    source: String(i.source ?? e).trim() || e,
    applyReductions: i.applyReductions !== !1,
    allowSpend: i.allowSpend !== !1
  } : null;
}
function Ip(i = 0) {
  const e = pu(Math.trunc(Ve(i, 0)), 0, 5);
  return Math.max(1, Number(sA[e] ?? 1));
}
function gu(i = 0) {
  return Td[Dn(i)] ?? Td[0];
}
function hu(i = 0) {
  return rA[Dn(i)] ?? 0;
}
function EA(i = A.actorTypes.vehicle) {
  return Cp(ua(i)).slice();
}
function da(i = {}, e = A.actorTypes.vehicle) {
  var o, l, c;
  const t = ua(e), n = i.attributes = i.attributes ?? {}, a = yA(i);
  n.reliability = n.reliability ?? {}, n.reliability.value = a, n.condition = n.condition ?? {}, n.condition.value = Math.max(0, Ve(((o = n.condition) == null ? void 0 : o.value) ?? a, a));
  const r = gA(i, t);
  r.shock = r.shock ?? {}, r.shock.value = Math.max(0, Ve((l = r.shock) == null ? void 0 : l.value, 0)), r.reliabilitySpendable = r.reliabilitySpendable ?? {};
  const s = (c = r.reliabilitySpendable) == null ? void 0 : c.value;
  return r.reliabilitySpendable.value = Math.max(0, Ve(s ?? a, a)), r.locations = pA(r.locations ?? {}, t), hA(i, t), i;
}
function Np(i = null, e = null) {
  var s;
  const t = ua(i);
  if (t !== A.actorTypes.vehicle) return {};
  const n = da(Xn((i == null ? void 0 : i.system) ?? {}), t), a = Number.isFinite(Number(e)) ? Math.max(0, Number(e)) : vp(n);
  if (a === null || a > 0) return {};
  const r = {};
  for (const [o, l] of Object.entries(((s = n.mwd) == null ? void 0 : s.locations) ?? {}))
    !l || l.enabled !== !0 || (r[`system.mwd.locations.${o}.condition`] = Ot.disabled);
  return r;
}
function IA({ actorSnapshot: i = null, unitType: e = "", locationKey: t = "" } = {}) {
  var s;
  const n = ua(e || (i == null ? void 0 : i.type) || (i == null ? void 0 : i.actorType)), a = String(t ?? "").trim(), r = ((s = dA[n]) == null ? void 0 : s[a]) ?? {
    type: "catastrophicFailure",
    destroyed: !0,
    statusState: ""
  };
  return {
    location: a,
    type: r.type,
    destroyed: !!r.destroyed,
    statusState: String(r.statusState ?? "").trim()
  };
}
function NA({
  actorSnapshot: i = null,
  locationKey: e = "",
  machineDamageDealt: t = 0,
  attackQuality: n = "",
  allowReliabilitySpend: a = !1,
  reliabilitySpendSelections: r = [],
  directConditionLocations: s = [],
  maxIterations: o = 10
} = {}) {
  var I, O, x, B, P, L, D, G;
  const l = Xn(i ?? {}), c = ua((l == null ? void 0 : l.type) ?? (l == null ? void 0 : l.actorType)), u = da(l.system ?? {}, c), d = Xn(((I = u.mwd) == null ? void 0 : I.locations) ?? {}), m = Math.max(0, Ve((x = (O = u.attributes) == null ? void 0 : O.reliability) == null ? void 0 : x.value, 0));
  let f = Math.max(0, Ve((P = (B = u.mwd) == null ? void 0 : B.reliabilitySpendable) == null ? void 0 : P.value, m));
  const p = Ip(m), g = Math.max(0, Ve(t, 0)), h = Math.max(0, Ve((D = (L = u.mwd) == null ? void 0 : L.shock) == null ? void 0 : D.value, 0)), y = SA(n);
  let b = Math.max(0, h + y);
  const S = CA({
    locations: d,
    shockBefore: h,
    shockGain: y,
    threshold: p,
    reliability: m,
    spendableBefore: f
  }), w = String(e ?? "").trim();
  if (w && d[w] && g > 0) {
    const Z = Math.max(0, Ve((G = d[w]) == null ? void 0 : G.stress, 0)) + g;
    d[w].stress = Z, Ep(S, w, g);
  }
  let T = 0, k = 0;
  const C = ({
    forcedLocationKey: V = "",
    source: Z = "shock",
    applyReductions: E = !0,
    allowSpendForThisAdvancement: X = !0
  } = {}) => {
    const H = TA(d, c, V);
    if (!H) return !1;
    const he = d[H];
    S.summary.selectedLocations.push({ source: Z, location: H });
    const ie = !!a && !!X && f > 0, le = ie && bA(r, T);
    if (S.spendOpportunities.push({
      index: T,
      location: H,
      source: Z,
      canSpend: ie,
      selected: le
    }), T += 1, le)
      f = Math.max(0, f - 1), S.reliabilitySpends.push({ location: H, prevented: !0, source: Z });
    else {
      const ze = Dn(he.condition);
      if (ze >= Ot.disabled)
        kA(
          S,
          IA({ actorSnapshot: l, unitType: c, locationKey: H }),
          H,
          d
        );
      else {
        const Xe = Dn(ze + 1);
        d[H].condition = Xe, S.conditionAdvancements.push({ location: H, from: ze, to: Xe, source: Z });
      }
    }
    return E && (b = MA({
      result: S,
      locations: d,
      locationKey: H,
      reliability: m,
      threshold: p,
      currentShock: b
    }), b = Math.max(0, b)), !0;
  };
  for (; b >= p && k < Math.max(1, Math.trunc(Ve(o, 10))); ) {
    const V = C({ source: "shock" });
    if (k += 1, !V) break;
  }
  k >= Math.max(1, Math.trunc(Ve(o, 10))) && b >= p && (S.loopGuardTriggered = !0);
  for (const V of Array.isArray(s) ? s : []) {
    const Z = vA(V, "critical");
    Z && C({
      forcedLocationKey: Z.locationKey,
      source: Z.source,
      applyReductions: Z.applyReductions,
      allowSpendForThisAdvancement: Z.allowSpend
    });
  }
  return S.shockDelta = Math.max(0, b) - h, S.summary.shockAfter = Math.max(0, b), S.summary.reliabilitySpendableAfter = f, S.summary.locationsAfter = Xn(d), S;
}
function RA(i = null, e = null) {
  var l, c, u, d, m;
  if (!i || !e) return {};
  const t = ua(i), n = da(Xn(i.system ?? {}), t), a = Xn(((l = n.mwd) == null ? void 0 : l.locations) ?? {}), r = {};
  for (const [f, p] of Object.entries(e.stressDelta ?? {})) {
    const g = a[f];
    g && (g.stress = Math.max(0, Math.max(0, Ve(g.stress, 0)) + Ve(p, 0)), r[`system.mwd.locations.${f}.stress`] = g.stress);
  }
  for (const f of Array.from(e.conditionAdvancements ?? [])) {
    const p = String((f == null ? void 0 : f.location) ?? "").trim(), g = a[p];
    g && (g.condition = Dn((f == null ? void 0 : f.to) ?? g.condition), r[`system.mwd.locations.${p}.condition`] = g.condition);
  }
  for (const f of Array.from(e.fallbackEvents ?? [])) {
    const p = String((f == null ? void 0 : f.location) ?? "").trim(), g = a[p];
    g && f.destroyed && (g.destroyed = !0, r[`system.mwd.locations.${p}.destroyed`] = !0);
  }
  const s = Array.isArray(e.reliabilitySpends) ? e.reliabilitySpends.length : 0, o = Math.max(0, Ve((u = (c = n.mwd) == null ? void 0 : c.reliabilitySpendable) == null ? void 0 : u.value, 0));
  return r["system.mwd.reliabilitySpendable.value"] = Math.max(0, o - s), r["system.mwd.shock.value"] = Math.max(0, Ve((m = (d = n.mwd) == null ? void 0 : d.shock) == null ? void 0 : m.value, 0) + Ve(e.shockDelta, 0)), Object.assign(r, Np(i)), e.statusState && (r["system.mwd.status.state"] = e.statusState), r;
}
const PA = Object.freeze(["small", "medium", "large"]), DA = Object.freeze(/* @__PURE__ */ new Set(["energy", "thermal", "electrical", "electric"])), Aa = Object.freeze({
  type: "energy",
  size: "small",
  location: "",
  itemId: ""
});
function cn(i, e = "small") {
  const t = String(i ?? "").trim().toLowerCase();
  return PA.includes(t) ? t : e;
}
function gs(i = null) {
  var t, n;
  const e = (n = (t = i == null ? void 0 : i.system) == null ? void 0 : t.mwd) == null ? void 0 : n.hardpoints;
  return e ? Array.isArray(e) ? [...e] : Object.values(e) : [];
}
function un(i = [], { defaultLocation: e = "", idFactory: t = null } = {}) {
  return (Array.isArray(i) ? i : Object.values(i && typeof i == "object" ? i : {})).map((a, r) => ({
    id: String((a == null ? void 0 : a.id) ?? `hardpoint-${r + 1}`).trim(),
    type: String((a == null ? void 0 : a.type) ?? Aa.type).trim() || Aa.type,
    size: cn((a == null ? void 0 : a.size) ?? Aa.size),
    location: String((a == null ? void 0 : a.location) ?? e ?? Aa.location).trim(),
    itemId: String((a == null ? void 0 : a.itemId) ?? Aa.itemId).trim()
  })).map((a, r) => ({
    ...a,
    id: a.id || (typeof t == "function" ? t(r) : `hardpoint-${r + 1}`)
  }));
}
function zi(i = null) {
  return un(gs(i));
}
function OA(i = [], e = [], { defaultLocation: t = "" } = {}) {
  const n = un(i, { defaultLocation: t }), a = new Map(
    un(e, { defaultLocation: t }).map((r) => [r.id, r]).filter(([r]) => r)
  );
  return n.map((r) => {
    const s = a.get(r.id);
    return s ? {
      ...r,
      type: s.type,
      size: s.size,
      location: s.location || t,
      itemId: r.itemId
    } : r;
  });
}
function LA(i = [], e = {}, { defaultLocation: t = "", idFactory: n = null } = {}) {
  const a = un(i, { defaultLocation: t }), r = String((e == null ? void 0 : e.id) ?? "").trim() || (typeof n == "function" ? n(a.length) : `hardpoint-${a.length + 1}`);
  return [
    ...a,
    ...un([{
      ...Aa,
      location: t,
      ...e,
      id: r
    }], { defaultLocation: t })
  ];
}
function _A(i = [], e = "", { defaultLocation: t = "" } = {}) {
  const n = String(e ?? "").trim(), a = un(i, { defaultLocation: t });
  let r = null;
  return { hardpoints: a.filter((o) => o.id !== n ? !0 : (r = o, !1)), removed: r };
}
function xA(i = [], e = "", t = "", { defaultLocation: n = "" } = {}) {
  const a = String(e ?? "").trim(), r = String(t ?? "").trim(), s = un(i, { defaultLocation: n });
  let o = !1;
  return { hardpoints: s.map((c) => {
    let u = c.itemId;
    return r && c.id !== a && c.itemId === r && (u = ""), c.id === a && (u = r), u !== c.itemId && (o = !0), {
      ...c,
      itemId: u
    };
  }), changed: o };
}
function $A(i = [], e = "", t = {}, { defaultLocation: n = "" } = {}) {
  const a = String(e ?? "").trim(), r = un(i, { defaultLocation: n });
  let s = !1;
  return { hardpoints: r.map((l) => {
    if (l.id !== a) return l;
    const c = {
      ...l,
      type: String((t == null ? void 0 : t.type) ?? l.type).trim() || l.type,
      size: cn((t == null ? void 0 : t.size) ?? l.size, l.size),
      location: String((t == null ? void 0 : t.location) ?? l.location ?? n).trim() || n,
      itemId: l.itemId
    };
    return s || (s = JSON.stringify(c) !== JSON.stringify(l)), c;
  }), changed: s };
}
function vr(i = null, e = "") {
  const t = String(e ?? "").trim();
  return t ? zi(i).find((n) => n.itemId === t) ?? null : null;
}
function BA(i = null) {
  return new Set(
    zi(i).map((e) => e.itemId).filter(Boolean)
  );
}
function $l(i = null, { canonicalType: e = "" } = {}) {
  const t = String(e ?? "").trim(), n = new Map(
    Array.from((i == null ? void 0 : i.items) ?? []).map((r) => [String((r == null ? void 0 : r.id) ?? "").trim(), r]).filter(([r, s]) => r && s)
  ), a = /* @__PURE__ */ new Set();
  return zi(i).map((r) => {
    const s = String((r == null ? void 0 : r.itemId) ?? "").trim();
    return !s || a.has(s) ? null : (a.add(s), n.get(s) ?? null);
  }).filter((r) => r ? t ? String((r == null ? void 0 : r.canonicalType) ?? (r == null ? void 0 : r.type) ?? "").trim() === t : !0 : !1);
}
function Rp(i = null) {
  var e;
  return String(((e = i == null ? void 0 : i.system) == null ? void 0 : e.damageType) ?? "").trim() || "energy";
}
function zs(i = "") {
  const e = String(i ?? "").trim().toLowerCase();
  return DA.has(e) ? "energy" : e || "energy";
}
function Pp(i = null) {
  var e;
  return cn(((e = i == null ? void 0 : i.system) == null ? void 0 : e.size) ?? "small");
}
function yu(i = {}, e = null) {
  if (String((e == null ? void 0 : e.canonicalType) ?? (e == null ? void 0 : e.type) ?? "").trim() !== A.itemType.mechWeapon) return !1;
  const n = String((i == null ? void 0 : i.type) ?? "").trim() || "energy", a = cn((i == null ? void 0 : i.size) ?? "small"), r = Rp(e), s = Pp(e);
  return (n === "omni" || zs(n) === zs(r)) && a === s;
}
function zA(i = {}, e = null) {
  if (String((e == null ? void 0 : e.canonicalType) ?? (e == null ? void 0 : e.type) ?? "").trim() !== A.itemType.mechWeapon)
    return "Only mech weapons can be mounted in hardpoint slots.";
  const n = String((i == null ? void 0 : i.type) ?? "").trim() || "energy", a = cn((i == null ? void 0 : i.size) ?? "small"), r = Rp(e), s = Pp(e);
  return n === "omni" || zs(n) === zs(r) ? a !== s ? `${(e == null ? void 0 : e.name) ?? "That weapon"} is ${oe(s)} and cannot fit a ${oe(a)} slot.` : "" : `${(e == null ? void 0 : e.name) ?? "That weapon"} is ${oe(r)} and cannot fit a ${oe(n)} slot.`;
}
function Er(i) {
  return Array.isArray(i) ? i : [];
}
function _e(i = "") {
  return String(i ?? "").trim();
}
function Fs(i = "") {
  const e = String(i ?? "").trim().toLowerCase();
  return e ? ["head", "cockpit"].includes(e) ? "head" : ["arm", "arms", "leftarm", "rightarm", "left-arm", "right-arm"].includes(e) ? "arms" : ["leg", "legs", "leftleg", "rightleg", "left-leg", "right-leg"].includes(e) ? "legs" : ["torso", "centertorso", "lefttorso", "righttorso", "center-torso", "left-torso", "right-torso", "core"].includes(e) ? "torso" : ["turret", "weapon", "weapons"].includes(e) ? "turret" : ["mobility", "motive", "drive", "wheel", "track", "tracks", "rotor"].includes(e) ? "mobility" : ["body", "front", "side", "rear"].includes(e) ? "body" : e : "";
}
function ho(i = null) {
  var t, n, a;
  const e = Array.isArray((t = i == null ? void 0 : i.system) == null ? void 0 : t.weaponGroups) ? i.system.weaponGroups : [];
  return e.length ? e : Array.isArray((a = (n = i == null ? void 0 : i.system) == null ? void 0 : n.mwd) == null ? void 0 : a.weaponGroupDetails) ? i.system.mwd.weaponGroupDetails : [];
}
function Ir(i = null, e = null) {
  const t = Er(e == null ? void 0 : e.weaponIds).map(_e).filter(Boolean);
  return t.length ? t.map((n) => {
    var a, r;
    return (r = (a = i == null ? void 0 : i.items) == null ? void 0 : a.get) == null ? void 0 : r.call(a, n);
  }).filter(Boolean) : Er(e == null ? void 0 : e.weapons).filter(Boolean);
}
function Dp(i = {}) {
  const e = (i == null ? void 0 : i.system) ?? i ?? {}, t = [
    e.damageType,
    e.category,
    e.weaponCategory,
    e.attackType
  ].map((a) => String(a ?? "").trim().toLowerCase()).filter(Boolean), n = Er(e.traits).map((a) => String(a ?? "").trim().toLowerCase());
  return t.includes("energy") || n.includes("energy");
}
function Op(i = {}) {
  const e = (i == null ? void 0 : i.system) ?? i ?? {}, t = [
    e.damageType,
    e.category,
    e.weaponCategory,
    e.attackType
  ].map((a) => String(a ?? "").trim().toLowerCase()).filter(Boolean), n = Er(e.traits).map((a) => String(a ?? "").trim().toLowerCase());
  return t.includes("ballistic") || n.includes("ballistic");
}
function FA(i = {}, e = null, t = "") {
  const n = Fs(t);
  return n ? Ir(e, i).some(
    (a) => {
      var r;
      return Fs((r = vr(e, a == null ? void 0 : a.id)) == null ? void 0 : r.location) === n;
    }
  ) : !1;
}
function Lp(i = null, { statusId: e = "", locationFamily: t = "" } = {}) {
  const n = ho(i);
  if (!n.length)
    return {
      weaponGroupId: "",
      weaponGroupName: "",
      weaponIds: [],
      automationMode: "callout"
    };
  const a = e === "jammedBallistic" ? n.filter((o) => Ir(i, o).some(Op)) : n;
  if (!a.length)
    return {
      weaponGroupId: "",
      weaponGroupName: "",
      weaponIds: [],
      automationMode: "callout"
    };
  const r = a.find((o) => !!(o != null && o.isPrimary)) ?? null, s = a.find((o) => FA(o, i, t)) ?? r ?? a[0] ?? null;
  return {
    weaponGroupId: _e(s == null ? void 0 : s.id),
    weaponGroupName: String((s == null ? void 0 : s.name) ?? "").trim(),
    weaponIds: Ir(i, s).map((o) => _e(o == null ? void 0 : o.id)).filter(Boolean),
    automationMode: s ? "engine" : "callout"
  };
}
function bu(i = {}) {
  const e = String((i == null ? void 0 : i.statusLabel) ?? "").trim();
  if (e) return e;
  const t = _e(i == null ? void 0 : i.statusId);
  return t === "staggeredMechanical" ? "Staggered" : t === "proneMechFall" ? "Prone" : oe(t || (i == null ? void 0 : i.key) || "Critical");
}
function yo(i = {}) {
  const e = String((i == null ? void 0 : i.effectText) ?? "").trim();
  if (e) return e;
  const t = _e(i == null ? void 0 : i.key), n = _e(i == null ? void 0 : i.statusId), a = String((i == null ? void 0 : i.weaponGroupName) ?? "").trim(), r = a ? ` (${a})` : "";
  return t === "targetingProcessorLock" ? "All fire modes require +1 SA to attack." : n === "staggeredMechanical" ? "At the start of activation, immediately spend 1 Simple Action fighting the controls." : n === "stalled" ? "-1 Handling and -1 System." : n === "unstable" ? "-2 dice to Piloting tests. Each move action is followed by a piloting roll to avoid falling prone." : n === "overheating" ? "At the start of the turn, gain 2 Heat." : n === "reactorInstability" ? "Energy weapon groups produce +1 Heat and -1 damage." : n === "weaponFailure" ? `Weapon group unusable for attacks${r}.` : n === "jammedBallistic" ? `Ballistic attacks from this weapon group cannot be used${r}.` : n === "skidding" ? "At the start of the turn, make an immediate full move in the last direction of travel; collision ends the skid and leaves the machine prone." : n === "limping" ? "-1 movement and +1 Piloting DN until repaired." : n === "proneMechFall" ? "The machine immediately falls prone." : t === "sensorOverload" ? "Contacts are capped at Track until repaired." : t === "opticsFracture" ? "All targets are treated as Obscured until repaired." : t === "commsSensorSuiteOut" ? "No sensor or ECM actions may be taken until repaired." : t === "balanceTimingFault" ? "Movement actions require manual piloting-fall follow-up." : t === "mobilityOverstress" ? "Call out the mobility overstress rider after movement actions." : t === "legStabilizerDamage" ? "Advanced maneuver penalties remain reminder-only for now." : "";
}
function Su(i = {}) {
  const e = String((i == null ? void 0 : i.automationMode) ?? "").trim();
  if (e === "engine" || e === "callout") return e;
  const t = _e(i == null ? void 0 : i.key), n = _e(i == null ? void 0 : i.statusId);
  return t === "targetingProcessorLock" || ["staggeredMechanical", "stalled", "unstable", "overheating", "reactorInstability", "weaponFailure", "jammedBallistic"].includes(n) ? "engine" : "callout";
}
function HA(i = {}) {
  const e = String((i == null ? void 0 : i.weaponGroupName) ?? "").trim();
  if (e) return `Weapon Group: ${e}`;
  const t = Fs(i == null ? void 0 : i.locationFamily);
  return t ? `Location: ${oe(t)}` : "";
}
function UA(i = null, e = {}, t = {}) {
  const n = String((e == null ? void 0 : e.key) ?? "").trim(), a = String((e == null ? void 0 : e.statusId) ?? "").trim(), r = ["weaponFailure", "jammedBallistic"].includes(a) ? Lp(i, { statusId: a, locationFamily: (t == null ? void 0 : t.locationFamily) ?? "" }) : { weaponGroupId: "", weaponGroupName: "", weaponIds: [], automationMode: "" };
  return {
    statusLabel: String((e == null ? void 0 : e.statusLabel) ?? "").trim() || bu({ key: n, statusId: a }),
    effectText: String((e == null ? void 0 : e.effectText) ?? "").trim() || yo({
      key: n,
      statusId: a,
      weaponGroupName: r.weaponGroupName
    }),
    automationMode: String((e == null ? void 0 : e.automationMode) ?? "").trim() || r.automationMode || Su({ key: n, statusId: a }),
    weaponGroupId: r.weaponGroupId,
    weaponGroupName: r.weaponGroupName,
    weaponIds: r.weaponIds
  };
}
function _p(i = {}, e = null) {
  const t = ["weaponFailure", "jammedBallistic"].includes(String((i == null ? void 0 : i.statusId) ?? "").trim()) && !String((i == null ? void 0 : i.weaponGroupId) ?? "").trim() ? Lp(e, {
    statusId: i == null ? void 0 : i.statusId,
    locationFamily: i == null ? void 0 : i.locationFamily
  }) : {
    weaponGroupId: String((i == null ? void 0 : i.weaponGroupId) ?? "").trim(),
    weaponGroupName: String((i == null ? void 0 : i.weaponGroupName) ?? "").trim(),
    weaponIds: Er(i == null ? void 0 : i.weaponIds).map((n) => String(n ?? "").trim()).filter(Boolean),
    automationMode: ""
  };
  return {
    ...i,
    statusLabel: String((i == null ? void 0 : i.statusLabel) ?? "").trim() || bu(i),
    effectText: String((i == null ? void 0 : i.effectText) ?? "").trim() || yo({
      ...i,
      weaponGroupName: t.weaponGroupName
    }),
    automationMode: String((i == null ? void 0 : i.automationMode) ?? "").trim() || t.automationMode || Su(i),
    weaponGroupId: t.weaponGroupId,
    weaponGroupName: t.weaponGroupName,
    weaponIds: t.weaponIds
  };
}
const xp = "machineCritical", $p = "machineCriticalTableGeneralUuid", jA = "machineCriticalTableBattlemechUuid", KA = "machineCriticalTableVehicleUuid", Bp = "machineCriticalTableBattlemechHeadUuid", zp = "machineCriticalTableBattlemechTorsoUuid", Fp = "machineCriticalTableBattlemechArmsUuid", Hp = "machineCriticalTableBattlemechLegsUuid", Up = "machineCriticalTableVehicleBodyUuid", jp = "machineCriticalTableVehicleTurretUuid", Kp = "machineCriticalTableVehicleMobilityUuid", Rt = Object.freeze({
  general: "Compendium.mwd.critical-hit-tables.RollTable.MWDGeneralCrit01",
  battlemech: "Compendium.mwd.critical-hit-tables.RollTable.MWDMechTorsoCrit01",
  vehicle: "Compendium.mwd.critical-hit-tables.RollTable.MWDVehicleBodyCrit1",
  mechHead: "Compendium.mwd.critical-hit-tables.RollTable.MWDMechHeadCrit01",
  mechTorso: "Compendium.mwd.critical-hit-tables.RollTable.MWDMechTorsoCrit01",
  mechArms: "Compendium.mwd.critical-hit-tables.RollTable.MWDMechArmsCrit01",
  mechLegs: "Compendium.mwd.critical-hit-tables.RollTable.MWDMechLegsCrit01",
  vehicleBody: "Compendium.mwd.critical-hit-tables.RollTable.MWDVehicleBodyCrit1",
  vehicleTurret: "Compendium.mwd.critical-hit-tables.RollTable.MWDVehicleTurretCrit",
  vehicleMobility: "Compendium.mwd.critical-hit-tables.RollTable.MWDVehicleMobility"
}), GA = /* @__PURE__ */ new Set(["physical", "fatigue", ""]), Bl = Object.freeze({
  2: Object.freeze({ key: "catastrophicCascade", remedyKey: "none", gates: [], mods: [], resourceEffects: {}, pilotDamage: {}, escalationKey: "cascade" }),
  3: Object.freeze({ key: "hardLock", remedyKey: "emergencyRepair", gates: [], mods: [], resourceEffects: {}, pilotDamage: {}, escalationKey: "lockout" }),
  4: Object.freeze({ key: "powerSurge", remedyKey: "coolantDump", gates: [], mods: [], resourceEffects: {}, pilotDamage: {}, escalationKey: "surge" }),
  5: Object.freeze({ key: "feedFlowDisruption", remedyKey: "feedReset", gates: [], mods: [], resourceEffects: {}, pilotDamage: {}, escalationKey: "feed" }),
  6: Object.freeze({ key: "controlFault", remedyKey: "systemReset", gates: [], mods: [], resourceEffects: {}, pilotDamage: {}, escalationKey: "control" }),
  7: Object.freeze({ key: "systemDesync", remedyKey: "systemReset", gates: [], mods: [], resourceEffects: {}, pilotDamage: {}, escalationKey: "desync" }),
  8: Object.freeze({ key: "structuralShock", remedyKey: "emergencyRepair", gates: [], mods: [], resourceEffects: {}, pilotDamage: {}, escalationKey: "shock" }),
  9: Object.freeze({ key: "overload", remedyKey: "coolantDump", gates: [], mods: [], resourceEffects: {}, pilotDamage: {}, escalationKey: "overload" }),
  10: Object.freeze({ key: "degradationSpike", remedyKey: "emergencyRepair", gates: [], mods: [], resourceEffects: {}, pilotDamage: {}, escalationKey: "degradation" }),
  11: Object.freeze({ key: "partialOutage", remedyKey: "emergencyRepair", gates: [], mods: [], resourceEffects: {}, pilotDamage: {}, escalationKey: "outage" }),
  12: Object.freeze({ key: "criticalBreach", remedyKey: "none", gates: [], mods: [], resourceEffects: {}, pilotDamage: {}, escalationKey: "conditionAdvance" })
}), Md = Object.freeze({
  battlemech: Object.freeze({
    head: Object.freeze({
      2: J("cockpitShock", "Cockpit Shock", "none", ["sensor"], ["sensorBlind"], {}, { track: "physical", amount: 3 }, "cascade", "sensorBlind"),
      3: J("targetingProcessorLock", "Targeting Processor Lock", "reboot", ["attack"], [], { extraAttackCost: 1 }, { track: "physical", amount: 2 }, "lockout", "", "All fire modes require +1 SA to attack.", "engine"),
      4: J("neuralFeedback", "Neural Feedback", "systemReset", [], [], {}, { track: "fatigue", amount: 1 }, "surge", "staggeredMechanical"),
      5: J("opticsCoolantFog", "Optics Coolant Fog / View Obstruction", "systemReset", ["attack"], ["rangeLimitClose"], {}, {}, "feed", "sensorDegraded"),
      6: J("commandInputDelay", "Command Input Delay", "reboot", [], [], {}, { track: "fatigue", amount: 2 }, "control", "stalled"),
      7: J("fireControlDesyncHead", "Fire-Control Desync", "systemReset", ["attack"], ["noCqBonus"], {}, {}, "desync", "sensorDegraded"),
      8: J("cockpitImpact", "Cockpit Impact", "pilotRecovery", ["piloting"], ["stabilityCheck"], {}, { track: "physical", amount: 2 }, "shock", "unstable"),
      9: J("sensorOverload", "Sensor Overload", "systemReset", ["sensor"], [], {}, { track: "fatigue", amount: 2 }, "overload", "", "Contacts are capped at Track until repaired.", "callout"),
      10: J("opticsFracture", "Optics Fracture", "emergencyRepair", ["attack"], [], {}, {}, "degradation", "", "All targets are treated as Obscured until repaired.", "callout"),
      11: J("commsSensorSuiteOut", "Communications / Sensor Suite Out", "systemReset", ["sensor"], [], {}, {}, "outage", "", "No sensor or ECM actions may be taken until repaired.", "callout"),
      12: J("headCriticalBreach", "Head condition +1", "none", [], [], {}, { track: "physical", amount: 2 }, "conditionAdvance", "")
    }),
    torso: Object.freeze({
      2: J("reactorGyroCascade", "Reactor / Gyro Cascade", "none", ["piloting"], ["stabilityCheck"], {}, { track: "fatigue", amount: 3 }, "cascade", "unstable"),
      3: J("gyroLock", "Gyro Lock", "emergencyRepair", ["move", "jump"], ["pilotingPenalty"], {}, {}, "lockout", "stalled"),
      4: J("reactorUnstable", "Reactor Unstable", "coolantDump", ["energyWeapon"], [], { heatPerEnergyAttack: 1 }, { track: "fatigue", amount: 2 }, "heat", "reactorInstability"),
      5: J("powerRoutingFault", "Power Routing Fault", "emergencyRepair", ["weaponGroup"], [], {}, {}, "feed", "weaponFailure"),
      6: J("coreResponseDelay", "Core Response Delay", "reboot", [], [], {}, { track: "fatigue", amount: 2 }, "control", "staggeredMechanical"),
      7: J("targetingMovementSyncFault", "Targeting / Movement Sync Fault", "systemReset", ["attack", "move"], ["noMovementFireAdvantage"], {}, {}, "desync", ""),
      8: J("internalShock", "Internal Shock", "emergencyRepair", ["piloting"], ["stabilityCheck"], {}, { track: "physical", amount: 2, condition: "failedFallImpact" }, "shock", "unstable"),
      9: J("heatSinkSaturation", "Heat Sink Saturation", "coolantDump", ["attack"], [], {}, {}, "heat", "overheating"),
      10: J("gyroDrift", "Gyro Drift", "emergencyRepair", ["move"], ["highMobilityBlocked"], {}, {}, "degradation", "limping"),
      11: J("powerBusOutage", "Power Bus Outage", "emergencyRepair", ["subsystem"], [], {}, {}, "outage", ""),
      12: J("torsoCriticalBreach", "Torso condition +1", "none", [], [], {}, {}, "conditionAdvance", "")
    }),
    arms: Object.freeze({
      2: J("weaponMountCascade", "Weapon Mount Cascade", "emergencyRepair", ["attack"], [], {}, {}, "cascade", "weaponFailure"),
      3: J("actuatorLockArm", "Actuator Lock", "emergencyRepair", ["attack"], [], {}, {}, "lockout", "weaponFailure"),
      4: J("weaponFeedback", "Weapon Feedback", "emergencyRepair", ["attack"], [], {}, {}, "surge", "", "Arm-specific recoil and feedback effects remain reminder-only.", "callout"),
      5: J("ammoFeedFaultArm", "Ammo / Feed Fault", "feedReset", ["attack"], [], {}, {}, "feed", "jammedBallistic"),
      6: J("fineActuationError", "Fine Actuation Error", "emergencyRepair", ["attack"], [], {}, {}, "control", "", "Fine-control arm effects remain reminder-only.", "callout"),
      7: J("targetingMisalignmentArm", "Targeting Misalignment", "systemReset", ["attack"], [], {}, {}, "desync", "", "Arm-mounted targeting issues remain reminder-only.", "callout"),
      8: J("recoilShock", "Recoil Shock", "emergencyRepair", ["attack"], [], {}, {}, "shock", "", "Recoil shock remains reminder-only for the affected arm.", "callout"),
      9: J("servoStrainArm", "Servo Strain", "emergencyRepair", ["attack"], [], {}, {}, "overload", "", "Servo strain remains reminder-only for the affected arm.", "callout"),
      10: J("stabilizerDamageArm", "Stabilizer Damage", "emergencyRepair", ["attack"], [], {}, {}, "degradation", "", "Stabilizer damage remains reminder-only for the affected arm.", "callout"),
      11: J("localPowerLossArm", "Local Power Loss", "emergencyRepair", ["subsystem"], [], {}, {}, "outage", "", "Local arm power-loss effects remain reminder-only.", "callout"),
      12: J("armsCriticalBreach", "Arms condition +1", "none", [], [], {}, {}, "conditionAdvance", "")
    }),
    legs: Object.freeze({
      2: J("mobilityCascadeLegs", "Mobility Cascade", "emergencyRepair", ["move"], [], {}, { track: "physical", amount: 2, condition: "resultingFallImpact" }, "cascade", "proneMechFall"),
      3: J("legActuatorLock", "Leg Actuator Lock", "emergencyRepair", ["move", "jump"], [], {}, {}, "lockout", "limping"),
      4: J("myomerSurge", "Myomer Surge", "coolantDump", ["move"], [], { heatOrStrainOnMove: 1 }, {}, "surge", ""),
      5: J("jumpJetMobilityFeedFault", "Jump Jet / Mobility Feed Fault", "emergencyRepair", ["move", "jump"], [], {}, {}, "feed", "jumpJetFailure"),
      6: J("gaitFault", "Gait Fault", "emergencyRepair", ["move"], [], {}, {}, "control", "unstable"),
      7: J("balanceTimingFault", "Balance Timing Fault", "systemReset", ["move"], [], {}, {}, "desync", "", "Movement actions require manual piloting-fall follow-up.", "callout"),
      8: J("forcedStabilityTest", "Forced Stability Test", "emergencyRepair", ["move", "piloting"], ["stabilityCheck"], {}, { track: "physical", amount: 2, condition: "resultingFallImpact" }, "shock", "unstable"),
      9: J("mobilityOverstress", "Mobility Overstress", "emergencyRepair", ["move"], [], {}, {}, "overload", "", "Call out the mobility overstress rider after movement actions.", "callout"),
      10: J("legStabilizerDamage", "Leg Stabilizer Damage", "emergencyRepair", ["move"], [], {}, {}, "degradation", "", "Advanced maneuver penalties remain reminder-only for now.", "callout"),
      11: J("partialMobilityOutageLegs", "Partial Mobility Outage", "emergencyRepair", ["move"], [], {}, {}, "outage", "limping"),
      12: J("legsCriticalBreach", "Legs condition +1", "none", [], [], {}, {}, "conditionAdvance", "")
    })
  }),
  vehicle: Object.freeze({
    body: Object.freeze({
      2: J("internalSystemsCascade", "Internal Systems Cascade", "none", [], [], {}, { track: "physical", amount: 2, condition: "openToppedOrCatastrophic" }, "cascade", ""),
      3: J("coreSystemsLock", "Core Systems Lock", "systemReset", ["subsystem"], [], {}, {}, "lockout", ""),
      4: J("enginePowerSurge", "Engine / Power Surge", "coolantDump", [], [], {}, { track: "fatigue", amount: 2, condition: "crewApplicable" }, "surge", ""),
      5: J("fuelFeedDisruption", "Fuel / Feed Disruption", "emergencyRepair", ["subsystem"], [], {}, {}, "feed", ""),
      6: J("controlFaultBody", "Control Fault", "systemReset", ["move"], [], {}, {}, "control", "stalled"),
      7: J("systemsDesyncBody", "Systems Desync", "systemReset", ["attack", "move"], [], {}, {}, "desync", "stalled"),
      8: J("structuralShockBody", "Structural Shock", "emergencyRepair", ["piloting"], ["controlTest"], {}, { track: "physical", amount: 2, condition: "crashImpact" }, "shock", "unstable"),
      9: J("overloadBody", "Overload", "coolantDump", [], [], {}, {}, "overload", ""),
      10: J("hullStressSpike", "Hull Stress Spike", "emergencyRepair", [], [], {}, {}, "degradation", ""),
      11: J("partialOutageBody", "Partial Outage", "emergencyRepair", ["subsystem"], [], {}, {}, "outage", ""),
      12: J("bodyCriticalBreach", "Body condition +1", "none", [], [], {}, {}, "conditionAdvance", "")
    }),
    turret: Object.freeze({
      2: J("turretWeaponCascade", "Turret Weapon Cascade", "emergencyRepair", ["attack"], [], {}, {}, "cascade", "weaponFailure"),
      3: J("traverseLock", "Traverse Lock", "emergencyRepair", ["attack"], [], {}, {}, "lockout", "weaponFailure"),
      4: J("fireControlSurgeTurret", "Fire-Control Surge", "systemReset", ["attack"], [], { heatOrStrainOnTurretAttack: 1 }, {}, "surge", "weaponFailure"),
      5: J("ammoFeedFaultTurret", "Ammo / Feed Fault", "feedReset", ["attack"], [], {}, {}, "feed", "jammedBallistic"),
      6: J("controlFaultTurret", "Control Fault", "systemReset", ["attack"], [], { extraAttackCost: 1 }, {}, "control", "weaponFailure"),
      7: J("trackingDesyncTurret", "Tracking Desync", "systemReset", ["attack"], ["noCqBonus"], {}, {}, "desync", "sensorDegraded"),
      8: J("mountShockTurret", "Mount Shock", "emergencyRepair", ["attack"], ["nextTurretAttackBlocked"], {}, {}, "shock", "weaponFailure"),
      9: J("overloadTurret", "Overload", "emergencyRepair", ["attack"], [], { turretAttackStress: 1 }, {}, "overload", "weaponFailure"),
      10: J("stabilizerDamageTurret", "Stabilizer Damage", "emergencyRepair", ["attack"], ["limitedArcFire"], {}, {}, "degradation", "weaponFailure"),
      11: J("turretSubsystemOutage", "Turret Subsystem Outage", "emergencyRepair", ["subsystem"], [], {}, {}, "outage", ""),
      12: J("turretCriticalBreach", "Turret condition +1", "none", [], [], {}, {}, "conditionAdvance", "")
    }),
    mobility: Object.freeze({
      2: J("mobilityCascadeVehicle", "Mobility Cascade", "emergencyRepair", ["move"], ["skidStallCrashRisk"], {}, {}, "cascade", "stalled"),
      3: J("driveLock", "Drive / Track / Wheel Lock", "emergencyRepair", ["move"], [], {}, {}, "lockout", "stalled"),
      4: J("powertrainSurge", "Powertrain Surge", "coolantDump", ["move"], [], { heatOrStrainOnMove: 1 }, {}, "surge", ""),
      5: J("transmissionRotorFeedFault", "Transmission / Rotor Feed Fault", "emergencyRepair", ["move"], [], {}, {}, "feed", "stalled"),
      6: J("steeringFault", "Steering Fault", "emergencyRepair", ["move"], [], {}, {}, "control", "stalled"),
      7: J("handlingDesync", "Handling Desync", "systemReset", ["move"], ["noMobilityCqBonus"], {}, {}, "desync", "unstable"),
      8: J("chassisShock", "Chassis Shock", "emergencyRepair", ["move", "piloting"], ["controlTest"], {}, {}, "shock", "unstable"),
      9: J("overloadMobility", "Overload", "emergencyRepair", ["move"], [], { repeatedMoveCost: 1 }, {}, "overload", "limping"),
      10: J("suspensionLiftDamage", "Suspension / Lift Damage", "emergencyRepair", ["move"], ["majorHandlingImpairment"], {}, {}, "degradation", "stalled"),
      11: J("partialMobilityOutageVehicle", "Partial Mobility Outage", "emergencyRepair", ["move"], [], {}, {}, "outage", "stalled"),
      12: J("mobilityCriticalBreach", "Mobility condition +1", "none", [], [], {}, {}, "conditionAdvance", "")
    })
  })
});
function Gp() {
  return typeof foundry < "u" && (foundry == null ? void 0 : foundry.utils);
}
function Kr(i) {
  return Gp() && typeof foundry.utils.deepClone == "function" ? foundry.utils.deepClone(i) : JSON.parse(JSON.stringify(i ?? null));
}
function J(i, e, t, n = [], a = [], r = {}, s = {}, o = "", l = "", c = "", u = "", d = "") {
  return Object.freeze({
    label: e,
    signal: Object.freeze({
      key: i,
      remedyKey: t,
      gates: n,
      mods: a,
      resourceEffects: r,
      pilotDamage: s,
      escalationKey: o,
      statusId: l,
      effectText: c,
      automationMode: u,
      statusLabel: d
    })
  });
}
function WA() {
  return Gp() && typeof foundry.utils.randomID == "function" ? foundry.utils.randomID() : Math.random().toString(36).slice(2, 18).padEnd(16, "0").slice(0, 16);
}
function qA() {
  try {
    return (/* @__PURE__ */ new Date()).toISOString();
  } catch {
    return "";
  }
}
function Cd(i) {
  return Array.isArray(i) ? i : typeof i == "string" ? i.split(",").map((e) => e.trim()).filter(Boolean) : [];
}
function vd(i) {
  return i && typeof i == "object" && !Array.isArray(i) ? i : {};
}
function VA(i = {}) {
  var e, t, n, a, r, s, o, l;
  return ((t = (e = i == null ? void 0 : i.flags) == null ? void 0 : e.mwd) == null ? void 0 : t.crit) ?? ((r = (a = (n = i == null ? void 0 : i.document) == null ? void 0 : n.flags) == null ? void 0 : a.mwd) == null ? void 0 : r.crit) ?? ((l = (o = (s = i == null ? void 0 : i.data) == null ? void 0 : s.flags) == null ? void 0 : o.mwd) == null ? void 0 : l.crit) ?? i;
}
function YA(i, e, t) {
  if (!t) return null;
  const n = new Error(i);
  throw n.validationErrors = e.length ? e : [i], n;
}
function Ed(i = 7) {
  const e = Math.min(12, Math.max(2, Math.trunc(Number(i ?? 7)) || 7));
  return Kr(Bl[e] ?? Bl[7]);
}
function na(i = {}, { strict: e = !1 } = {}) {
  const t = VA(i), n = [], a = String((t == null ? void 0 : t.key) ?? "").trim(), r = String((t == null ? void 0 : t.remedyKey) ?? "emergencyRepair").trim() || "emergencyRepair", s = Cd(t == null ? void 0 : t.gates).map((b) => String(b ?? "").trim()).filter(Boolean), o = Cd(t == null ? void 0 : t.mods).map((b) => String(b ?? "").trim()).filter(Boolean), l = vd(t == null ? void 0 : t.resourceEffects), c = vd(t == null ? void 0 : t.pilotDamage), u = String((t == null ? void 0 : t.escalationKey) ?? "").trim(), d = String((t == null ? void 0 : t.statusId) ?? "").trim(), m = String((t == null ? void 0 : t.statusLabel) ?? "").trim(), f = String((t == null ? void 0 : t.effectText) ?? "").trim(), p = String((t == null ? void 0 : t.automationMode) ?? "").trim();
  a || n.push("Critical signal key cannot be blank."), nA(r) || n.push(`Unknown machine critical remedy "${r}".`);
  for (const [b, S] of Object.entries(l))
    Number.isFinite(Number(S)) || n.push(`Resource effect "${b}" must be numeric.`);
  const g = String((c == null ? void 0 : c.track) ?? "").trim(), h = Number((c == null ? void 0 : c.amount) ?? 0), y = String((c == null ? void 0 : c.condition) ?? "").trim();
  return GA.has(g) || n.push(`Pilot damage track "${g}" is invalid.`), (!Number.isFinite(h) || h < 0) && n.push("Pilot damage amount must be non-negative."), n.length ? (YA(n[0], n, e), null) : {
    key: a,
    remedyKey: r,
    gates: s,
    mods: o,
    resourceEffects: Object.fromEntries(
      Object.entries(l).map(([b, S]) => [String(b), Number(S)])
    ),
    pilotDamage: g || h || y ? {
      track: g || "fatigue",
      amount: Math.trunc(h),
      ...y ? { condition: y } : {}
    } : {},
    escalationKey: u,
    statusId: d,
    statusLabel: m,
    effectText: f,
    automationMode: p
  };
}
function _n(i, e = {}) {
  var n, a;
  return (Array.isArray((a = (n = i == null ? void 0 : i.system) == null ? void 0 : n.mwd) == null ? void 0 : a.crits) ? i.system.mwd.crits : []).map((r) => _p(r, i)).filter((r) => r && r.active !== !1).filter((r) => !e.key || r.key === e.key).filter((r) => !e.locationKey || r.locationKey === e.locationKey).filter((r) => !e.locationFamily || r.locationFamily === e.locationFamily).filter((r) => !e.gate || Array.isArray(r.gates) && r.gates.includes(e.gate)).filter((r) => !e.mod || Array.isArray(r.mods) && r.mods.includes(e.mod));
}
function Id(i, e) {
  var r, s;
  const t = ((s = (r = i == null ? void 0 : i.system) == null ? void 0 : r.monitors) == null ? void 0 : s[e]) ?? {}, n = Math.max(0, Number(t.max ?? 0) || 0), a = Math.min(n, Math.max(0, Number(t.value ?? 0) || 0));
  return {
    max: n,
    value: a,
    remaining: Math.max(0, n - a)
  };
}
function JA(i) {
  return (i == null ? void 0 : i.type) === A.actorTypes.vehicle || (i == null ? void 0 : i.type) === A.actorTypes.battlemech;
}
function QA(i, e, t, n) {
  return {
    ...e != null && e.hitLocation && typeof e.hitLocation == "object" ? e.hitLocation : kp({
      actor: i,
      rollTotal: e == null ? void 0 : e.hitLocationRollTotal,
      armorBefore: t,
      structureBefore: n
    }),
    armorBefore: t,
    structureBefore: n,
    pureStructureHit: t <= 0
  };
}
function Wp(i = {}, e = !1) {
  return e && i.chaosTargetLocationKey ? {
    locationKey: i.chaosTargetLocationKey,
    locationFamily: i.locationFamily === "head" ? "torso" : i.locationFamily,
    locationLabel: i.chaosTargetLocationLabel ?? Mr(i.chaosTargetLocationKey)
  } : {
    locationKey: i.locationKey,
    locationFamily: i.locationFamily,
    locationLabel: i.locationLabel ?? Mr(i.locationKey)
  };
}
function XA(i = {}, e = !1) {
  return !!(i.isAutomaticCritical || i.chaosCriticalOption && e);
}
function qp(i = {}) {
  const e = String((i == null ? void 0 : i.attackQuality) ?? "").trim();
  if (["graze", "hit", "highMargin"].includes(e)) return e;
  const t = String((i == null ? void 0 : i.outcome) ?? "").trim(), n = Math.max(0, Number((i == null ? void 0 : i.netHits) ?? 0) || 0);
  return t === "graze" ? "graze" : t === "hit" && n >= 4 ? "highMargin" : t === "hit" ? "hit" : "";
}
function ZA({
  actor: i = null,
  payload: e = {},
  hitLocation: t = null,
  chaosCriticalSelected: n = !1
} = {}) {
  if (!JA(i)) return { ok: !1, reason: "Machine damage requires a vehicle or BattleMech actor." };
  const a = Math.max(0, Math.ceil(Number((e == null ? void 0 : e.damage) ?? (e == null ? void 0 : e.amount) ?? 0) || 0)), r = Id(i, A.monitors.armor), s = Id(i, A.monitors.structure), o = t ? { ...t, armorBefore: r.remaining, structureBefore: s.remaining, pureStructureHit: r.remaining <= 0 } : QA(i, e, r.remaining, s.remaining), l = Math.min(a, i.type === A.actorTypes.vehicle && r.max <= 0 ? 0 : r.remaining), c = Math.min(s.remaining, Math.max(0, a - l)), u = Math.min(r.max, r.value + l), d = Math.min(s.max, s.value + c), m = XA(o, n), f = Wp(o, n), p = qp(e), g = c;
  return {
    ok: !0,
    mode: "machineAttackDamage",
    actorName: i.name ?? "Machine",
    damageIncoming: a,
    adjustedIncoming: a,
    finalDamage: c,
    requestedDelta: a,
    appliedDelta: c,
    usedArmor: l > 0,
    damageType: String((e == null ? void 0 : e.damageType) ?? "kinetic").trim() || "kinetic",
    effectiveAp: Math.max(0, Number((e == null ? void 0 : e.ap) ?? 0) || 0),
    hitLocation: o,
    critical: {
      automatic: !!o.isAutomaticCritical,
      optional: !!o.chaosCriticalOption,
      selected: m,
      chaosCriticalSelected: !!n,
      locationKey: f.locationKey,
      locationFamily: f.locationFamily,
      locationLabel: f.locationLabel
    },
    machine: {
      armorBefore: r.remaining,
      armorAfter: Math.max(0, r.max - u),
      armorDamageBefore: r.value,
      armorDamageAfter: u,
      armorMax: r.max,
      armorAbsorbed: l,
      structureBefore: s.remaining,
      structureAfter: Math.max(0, s.max - d),
      structureDamageBefore: s.value,
      structureDamageAfter: d,
      structureMax: s.max,
      structureDamage: c,
      pureStructureHit: r.remaining <= 0,
      locationStressGain: g,
      locationTakesStress: g > 0
    },
    degradation: {
      attackQuality: p,
      locationStressGain: g
    },
    beforeLabel: `Armor ${r.remaining}/${r.max}, Structure ${s.remaining}/${s.max}`,
    afterLabel: `Armor ${Math.max(0, r.max - u)}/${r.max}, Structure ${Math.max(0, s.max - d)}/${s.max}`,
    source: String((e == null ? void 0 : e.source) ?? "").trim(),
    notes: String((e == null ? void 0 : e.notes) ?? "").trim()
  };
}
function _i(i, e = "") {
  var t, n;
  try {
    return ((n = (t = game == null ? void 0 : game.settings) == null ? void 0 : t.get) == null ? void 0 : n.call(t, M, i)) || e;
  } catch {
    return e;
  }
}
function ew(i = null) {
  return _i($p, Rt.general);
}
async function Vp(i = null, e = "") {
  const t = String(e || ew(i)).trim();
  if (!t || typeof fromUuid != "function") return null;
  try {
    return await fromUuid(t);
  } catch (n) {
    return console.warn("MWD | Unable to resolve machine critical table", t, n), null;
  }
}
function Yp(i = null, e = {}) {
  const t = String((e == null ? void 0 : e.locationFamily) ?? (e == null ? void 0 : e.locationKey) ?? "").trim();
  return (i == null ? void 0 : i.type) === A.actorTypes.battlemech ? t === "head" ? "head" : t === "arms" || t === "arm" || /arm/i.test(String((e == null ? void 0 : e.locationKey) ?? "")) ? "arms" : t === "legs" || t === "leg" || /leg/i.test(String((e == null ? void 0 : e.locationKey) ?? "")) ? "legs" : "torso" : t === "turret" || t === "weapon" || /turret|weapon/i.test(String((e == null ? void 0 : e.locationKey) ?? "")) ? "turret" : t === "mobility" || t === "motive" || /mobility|motive|drive|wheel|track/i.test(String((e == null ? void 0 : e.locationKey) ?? "")) ? "mobility" : "body";
}
function tw(i = null, e = {}) {
  const t = Yp(i, e);
  return (i == null ? void 0 : i.type) === A.actorTypes.battlemech ? t === "head" ? _i(Bp, Rt.mechHead) : t === "arms" ? _i(Fp, Rt.mechArms) : t === "legs" ? _i(Hp, Rt.mechLegs) : _i(zp, Rt.mechTorso) || _i(jA, Rt.battlemech) : t === "turret" ? _i(jp, Rt.vehicleTurret) : t === "mobility" ? _i(Kp, Rt.vehicleMobility) : _i(Up, Rt.vehicleBody) || _i(KA, Rt.vehicle);
}
function iw(i = null, e = {}, t = 7) {
  var s, o, l, c;
  const n = (i == null ? void 0 : i.type) === A.actorTypes.vehicle ? "vehicle" : "battlemech", a = Yp(i, e), r = Math.min(12, Math.max(2, Math.trunc(Number(t ?? 7)) || 7));
  return Kr(((o = (s = Md[n]) == null ? void 0 : s[a]) == null ? void 0 : o[r]) ?? ((c = (l = Md[n]) == null ? void 0 : l[a]) == null ? void 0 : c[7]));
}
function Jr(i = {}, e = {}) {
  const t = Number((i == null ? void 0 : i.rollTotal) ?? 0);
  if (Number.isFinite(t) && t >= 2 && t <= 12) return Math.trunc(t);
  const n = String((e == null ? void 0 : e.key) ?? "").trim();
  for (const [a, r] of Object.entries(Bl))
    if (r.key === n) return Number(a);
  return n === "cascade" || (e == null ? void 0 : e.escalationKey) === "cascade" ? 2 : (e == null ? void 0 : e.escalationKey) === "conditionAdvance" ? 12 : 7;
}
function Ko(i = {}, e = 0) {
  return e === 2 || (i == null ? void 0 : i.key) === "catastrophicCascade" || (i == null ? void 0 : i.key) === "cascade" || (i == null ? void 0 : i.escalationKey) === "cascade";
}
function nw(i, e) {
  var n;
  return Array.from((i == null ? void 0 : i.results) ?? ((n = i == null ? void 0 : i.results) == null ? void 0 : n.contents) ?? []).find((a) => {
    const r = Array.isArray(a == null ? void 0 : a.range) ? a.range : [], s = Number(r[0] ?? 0), o = Number(r[1] ?? r[0] ?? 0);
    return e >= s && e <= o;
  }) ?? null;
}
async function Nd({ actor: i = null, hitLocation: e = {}, rollTotal: t = 7, tableUuid: n = "" } = {}) {
  const a = iw(i, e, t), r = String(n || tw(i, e)).trim();
  if (!a) return { error: "No location critical table is defined for this hit location." };
  if (!r || typeof fromUuid != "function")
    return {
      signal: na(a.signal, { strict: !0 }),
      label: a.label,
      tableUuid: r,
      resultId: "",
      rollTotal: t
    };
  const s = await Vp(i, r);
  if (!s) return { error: `Machine location critical table could not be resolved: ${r}` };
  const o = nw(s, t);
  if (!o) return { error: `Machine location critical table has no result for ${t}: ${r}` };
  const l = na(o, { strict: !0 });
  return {
    signal: l,
    label: String((o == null ? void 0 : o.text) ?? (o == null ? void 0 : o.name) ?? l.key).trim() || l.key,
    tableUuid: s.uuid ?? r,
    resultId: o.id ?? o._id ?? "",
    rollTotal: t
  };
}
async function Rd({ actor: i = null, drawFn: e = null, tableUuid: t = "", recursiveCascade: n = !1 } = {}) {
  var l;
  if (typeof e == "function") {
    const c = await e({ actor: i, recursiveCascade: n }), u = na((c == null ? void 0 : c.signal) ?? c, { strict: !0 });
    return {
      signal: u,
      label: String((c == null ? void 0 : c.label) ?? u.key).trim() || u.key,
      tableUuid: String((c == null ? void 0 : c.tableUuid) ?? t ?? "").trim(),
      resultId: String((c == null ? void 0 : c.resultId) ?? "").trim(),
      rollTotal: Number((c == null ? void 0 : c.rollTotal) ?? 0) || null
    };
  }
  const a = await Vp(i, t);
  if (!(a != null && a.draw)) return { error: "Machine critical table is not configured." };
  const r = await a.draw({ displayChat: !1 }), s = Array.from((r == null ? void 0 : r.results) ?? [])[0] ?? null;
  if (!s) return { error: "Machine critical table returned no result." };
  const o = na(s, { strict: !0 });
  return {
    signal: o,
    label: String((s == null ? void 0 : s.text) ?? (s == null ? void 0 : s.name) ?? o.key).trim() || o.key,
    tableUuid: a.uuid ?? t,
    resultId: s.id ?? s._id ?? "",
    rollTotal: Number(((l = r == null ? void 0 : r.roll) == null ? void 0 : l.total) ?? 0) || null
  };
}
function Go({ actor: i, drawn: e, hitLocation: t, source: n = {}, cascade: a = !1 } = {}) {
  var d, m, f, p, g, h, y;
  const r = na((e == null ? void 0 : e.signal) ?? e, { strict: !0 }), s = Pn(r.remedyKey), o = Wp(t, !1), l = String((e == null ? void 0 : e.label) ?? r.key).trim() || r.key, c = UA(i, r, o), u = po({
    key: r.key,
    label: l,
    locationLabel: o.locationLabel,
    gates: r.gates,
    mods: r.mods,
    remedyKey: r.remedyKey
  }, s);
  return {
    id: WA(),
    key: r.key,
    label: l,
    tableUuid: String((e == null ? void 0 : e.tableUuid) ?? "").trim(),
    resultId: String((e == null ? void 0 : e.resultId) ?? "").trim(),
    generalKey: String(((d = e == null ? void 0 : e.general) == null ? void 0 : d.key) ?? "").trim(),
    generalLabel: String(((m = e == null ? void 0 : e.general) == null ? void 0 : m.label) ?? "").trim(),
    generalRollTotal: Number(((f = e == null ? void 0 : e.general) == null ? void 0 : f.rollTotal) ?? (e == null ? void 0 : e.rollTotal) ?? 0) || null,
    generalTableUuid: String(((p = e == null ? void 0 : e.general) == null ? void 0 : p.tableUuid) ?? "").trim(),
    generalResultId: String(((g = e == null ? void 0 : e.general) == null ? void 0 : g.resultId) ?? "").trim(),
    locationKey: o.locationKey,
    locationFamily: o.locationFamily,
    locationLabel: o.locationLabel,
    gates: r.gates,
    mods: r.mods,
    resourceEffects: r.resourceEffects,
    pilotDamage: r.pilotDamage,
    remedyKey: r.remedyKey,
    remedyLabel: s.label,
    remedySkillKey: u,
    remedyBaseDn: go({ remedyKey: r.remedyKey }, s),
    remedyEffect: fu({}),
    escalationKey: r.escalationKey,
    statusId: r.statusId,
    statusLabel: c.statusLabel,
    effectText: c.effectText,
    automationMode: c.automationMode,
    weaponGroupId: c.weaponGroupId,
    weaponGroupName: c.weaponGroupName,
    weaponIds: c.weaponIds,
    active: !0,
    cascade: !!a,
    createdRound: Number(((y = (h = globalThis.game) == null ? void 0 : h.combat) == null ? void 0 : y.round) ?? 0) || 0,
    createdAt: qA(),
    source: Kr(n ?? {}),
    actorType: (i == null ? void 0 : i.type) ?? ""
  };
}
async function aw({
  actor: i = null,
  hitLocation: e = {},
  source: t = {},
  drawFn: n = null,
  tableUuid: a = ""
} = {}) {
  try {
    const r = await Rd({ actor: i, drawFn: n, tableUuid: a, recursiveCascade: !1 });
    if (r != null && r.error) return { ok: !1, reason: r.error, crits: [] };
    const s = na(r.signal, { strict: !0 }), o = Jr(r, s), l = await Nd({ actor: i, hitLocation: e, rollTotal: o });
    if (l != null && l.error) return { ok: !1, reason: l.error, crits: [] };
    const c = {
      ...l,
      general: {
        key: s.key,
        label: String((r == null ? void 0 : r.label) ?? s.key).trim() || s.key,
        rollTotal: o,
        tableUuid: String((r == null ? void 0 : r.tableUuid) ?? "").trim(),
        resultId: String((r == null ? void 0 : r.resultId) ?? "").trim()
      }
    };
    if (!Ko(s, o))
      return { ok: !0, crits: [Go({ actor: i, drawn: c, hitLocation: e, source: t })], cascade: !1 };
    const u = await Rd({ actor: i, drawFn: n, tableUuid: a, recursiveCascade: !0 }), d = u != null && u.error ? Ed(12) : na(u.signal, { strict: !0 }), m = Ko(d, Jr(u, d)) ? 12 : Jr(u, d), f = m === 12 && Ko(d, Jr(u, d)) ? Ed(12) : d, p = await Nd({ actor: i, hitLocation: e, rollTotal: m });
    if (p != null && p.error) return { ok: !1, reason: p.error, crits: [] };
    const g = {
      ...p,
      general: {
        key: f.key,
        label: String((u == null ? void 0 : u.label) ?? f.key).trim() || f.key,
        rollTotal: m,
        tableUuid: String((u == null ? void 0 : u.tableUuid) ?? "").trim(),
        resultId: String((u == null ? void 0 : u.resultId) ?? "").trim()
      }
    };
    return {
      ok: !0,
      cascade: !0,
      crits: [
        Go({ actor: i, drawn: c, hitLocation: e, source: t, cascade: !0 }),
        Go({ actor: i, drawn: g, hitLocation: e, source: t })
      ]
    };
  } catch (r) {
    return { ok: !1, reason: (r == null ? void 0 : r.message) ?? "Unable to draw machine critical.", crits: [] };
  }
}
function rw(i = {}, e = null) {
  return Array.isArray(i == null ? void 0 : i.preparedCriticalRecords) ? i.preparedCriticalRecords.map((t) => _p(Kr(t), e)) : [];
}
function sw(i = []) {
  return Array.from(i ?? []).filter((e) => String((e == null ? void 0 : e.escalationKey) ?? "").trim() === "conditionAdvance").map((e) => String((e == null ? void 0 : e.locationKey) ?? "").trim()).filter(Boolean);
}
function ow(i = {}) {
  var t, n, a;
  if (!((t = i == null ? void 0 : i.machine) != null && t.pureStructureHit)) return [];
  if (Math.max(0, Number(((n = i == null ? void 0 : i.machine) == null ? void 0 : n.structureDamage) ?? 0) || 0) <= 0) return [];
  const e = String(((a = i == null ? void 0 : i.hitLocation) == null ? void 0 : a.locationKey) ?? "").trim();
  return e ? [{ locationKey: e, source: "pureStructure", applyReductions: !1, allowSpend: !1 }] : [];
}
async function lw(i, e) {
  if (!(!(i != null && i.toggleStatusEffect) || !e))
    try {
      await Wi({
        actor: i,
        statusId: xp,
        active: !0,
        metadata: {
          scope: "Machine critical effects",
          notes: "Visual marker for active system.mwd.crits entries."
        }
      });
    } catch (t) {
      console.warn("MWD | Unable to sync machine critical status", t);
    }
}
async function cw(i, e) {
  var t, n;
  for (const a of e) {
    const r = String((a == null ? void 0 : a.statusId) ?? "").trim();
    if (!(!r || (((n = (t = i.statuses) == null ? void 0 : t.has) == null ? void 0 : n.call(t, r)) ?? !1)))
      try {
        await Wi({ actor: i, statusId: r, active: !0 });
      } catch (s) {
        console.warn(`MWD | Unable to apply crit status "${r}"`, s);
      }
  }
}
async function uw(i, e) {
  var n, a, r, s;
  const t = new Set(Dw(i));
  for (const o of t)
    if (!(!o || (((a = (n = i.statuses) == null ? void 0 : n.has) == null ? void 0 : a.call(n, o)) ?? !1)))
      try {
        await Wi({ actor: i, statusId: o, active: !0 });
      } catch (l) {
        console.warn(`MWD | Unable to apply degradation-derived status "${o}"`, l);
      }
  for (const o of Array.from((e == null ? void 0 : e.fallbackEvents) ?? [])) {
    if (!(o != null && o.destroyed)) continue;
    const l = String(o.location ?? "").trim(), c = l === "legs" ? "legDestroyed" : l === "arms" ? "armDestroyed" : "";
    if (!(!c || (((s = (r = i.statuses) == null ? void 0 : r.has) == null ? void 0 : s.call(r, c)) ?? !1)))
      try {
        await Wi({ actor: i, statusId: c, active: !0 });
      } catch (u) {
        console.warn(`MWD | Unable to apply degradation status "${c}"`, u);
      }
  }
}
async function dw({
  actor: i = null,
  token: e = null,
  payload: t = {},
  options: n = {}
} = {}) {
  var d, m, f;
  const a = ZA({
    actor: i,
    payload: t,
    chaosCriticalSelected: !!(t != null && t.chaosCriticalSelected)
  });
  if (!a.ok) return a;
  const r = !!n.dryRun, s = rw(t, i);
  let o = s.length ? { ok: !0, crits: s, cascade: s.length > 1 } : { ok: !0, crits: [] };
  !s.length && a.critical.selected && (o = await aw({
    actor: i,
    hitLocation: {
      ...a.hitLocation,
      locationKey: a.critical.locationKey,
      locationFamily: a.critical.locationFamily,
      locationLabel: a.critical.locationLabel
    },
    source: {
      ...(t == null ? void 0 : t.sourceData) ?? {},
      source: (t == null ? void 0 : t.source) ?? "",
      tokenUuid: (e == null ? void 0 : e.uuid) ?? (t == null ? void 0 : t.targetTokenUuid) ?? ""
    },
    drawFn: n.drawCritical,
    tableUuid: (t == null ? void 0 : t.criticalTableUuid) ?? ""
  }));
  const l = NA({
    actorSnapshot: i,
    locationKey: a.critical.locationKey || a.hitLocation.locationKey,
    machineDamageDealt: a.machine.structureDamage,
    attackQuality: ((d = a.degradation) == null ? void 0 : d.attackQuality) ?? qp(t),
    allowReliabilitySpend: !0,
    reliabilitySpendSelections: Array.isArray(t == null ? void 0 : t.reliabilitySpendSelections) ? t.reliabilitySpendSelections : [],
    directConditionLocations: [
      ...ow(a),
      ...o.ok ? sw(o.crits) : []
    ]
  });
  l.loopGuardTriggered && console.warn("MWD | Machine degradation loop guard triggered", {
    actor: (i == null ? void 0 : i.name) ?? (i == null ? void 0 : i.id) ?? "Machine",
    payload: t,
    degradation: l
  });
  const c = Array.isArray((f = (m = i == null ? void 0 : i.system) == null ? void 0 : m.mwd) == null ? void 0 : f.crits) ? Kr(i.system.mwd.crits) : [], u = o.ok && o.crits.length ? c.concat(o.crits) : c;
  if (!r) {
    const p = {
      "system.monitors.armor.value": a.machine.armorDamageAfter,
      "system.monitors.structure.value": a.machine.structureDamageAfter,
      ...RA(i, l),
      ...Np(i, a.machine.structureAfter)
    };
    o.ok && o.crits.length && (p["system.mwd.crits"] = u), await i.update(p);
    const g = u.some((h) => (h == null ? void 0 : h.active) !== !1);
    await lw(i, g), o.ok && o.crits.length && await cw(i, o.crits), await uw(i, l);
  }
  return {
    ...a,
    dryRun: r,
    appliedDelta: a.machine.structureDamage,
    critical: {
      ...a.critical,
      drawOk: !!o.ok,
      reason: o.ok ? "" : o.reason,
      records: o.ok ? o.crits : [],
      cascade: !!o.cascade
    },
    degradation: l
  };
}
const Qr = Object.freeze({
  contact: 1,
  track: 2,
  lock: 3
});
function Jp(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? t : e;
}
function Qp(i = null) {
  return (i == null ? void 0 : i.type) === A.actorTypes.battlemech ? A.actorTypes.battlemech : A.actorTypes.vehicle;
}
function Nt(i = null, e = "") {
  var t, n;
  return ((n = (t = i == null ? void 0 : i.statuses) == null ? void 0 : t.has) == null ? void 0 : n.call(t, e)) ?? !1;
}
function Pd(i = "lock", e = "lock") {
  const t = Qr[i] ?? Qr.lock;
  return (Qr[e] ?? Qr.lock) < t ? e : i;
}
function Mt(i = null, e = "") {
  var t, n, a;
  return ((a = (n = (t = i == null ? void 0 : i.system) == null ? void 0 : t.mwd) == null ? void 0 : n.locations) == null ? void 0 : a[e]) ?? {};
}
function Dt(i = {}) {
  return Math.max(0, Math.trunc(Jp(i == null ? void 0 : i.condition, 0)));
}
function Xp(i = null, { weaponGroupId: e = "", weaponId: t = "", weapon: n = null, rangeBand: a = "" } = {}) {
  var f, p;
  const r = _e(e), s = _e(t), o = ho(i), l = o.find((g) => _e(g == null ? void 0 : g.id) === r) ?? null, c = n ?? (s ? ((p = (f = i == null ? void 0 : i.items) == null ? void 0 : f.get) == null ? void 0 : p.call(f, s)) ?? null : null), u = l ?? o.find(
    (g) => (Array.isArray(g == null ? void 0 : g.weaponIds) ? g.weaponIds : []).some((h) => _e(h) === s)
  ) ?? null, d = u ? Ir(i, u) : c ? [c] : [], m = new Set(d.map(
    (g) => {
      var h;
      return Fs((h = vr(i, g == null ? void 0 : g.id)) == null ? void 0 : h.location);
    }
  ).filter(Boolean));
  return {
    weaponGroupId: _e(u == null ? void 0 : u.id),
    weapons: d,
    families: m,
    isArmMounted: m.has("arms"),
    isTurretMounted: m.has("turret"),
    hasEnergy: d.some(Dp),
    rangeBand: String(a ?? "").trim().toLowerCase()
  };
}
function ye(i, e = "") {
  const t = String(e ?? "").trim();
  t && (i.effectTexts.includes(t) || i.effectTexts.push(t));
}
function mw(i, e = null) {
  for (const t of _n(e)) {
    const n = String((t == null ? void 0 : t.statusId) ?? "").trim();
    n === "unstable" ? (i.pilotingDice += -2, ye(i, "Unstable: -2 dice to Piloting; movement risks a fall.")) : n === "staggeredMechanical" ? i.startTurnSaLoss += 1 : n === "overheating" ? i.startTurnHeat += 2 : n === "reactorInstability" ? (i.energyAttackHeat += 1, i.energyAttackDamage += -1) : n === "stalled" ? (i.handling += -1, i.system += -1, i.noSprint = !0, i.noJump = !0) : n === "limping" ? (i.movementPenalty += 1, i.pilotingDn += 1) : n === "jumpJetFailure" && (i.noJump = !0);
  }
}
function fw(i, e = null) {
  Nt(e, "sensorDegraded") && (i.acquireDice += -2, ye(i, "Sensor Degraded: -2 dice to Acquire Target.")), Nt(e, "sensorBlind") && (i.rangeCapClose = !0, i.noTargetingDataGeneration = !0, i.noTargetingDataUse = !0, ye(i, "Sensor Blind: no Targeting Data and attacks beyond Close are blocked.")), Nt(e, "trackingLost") && (i.noTargetingDataUse = !0, ye(i, "Tracking Lost: stored Targeting Data is unusable.")), (Nt(e, "epmBoosted") || Nt(e, "eccmBoosted")) && (i.ecmSpikeDefenseDice += 2, ye(i, "EPM Boosted: -2 dice against incoming ECM Spike attempts.")), Nt(e, "coolingFailure") && (i.coolingImpaired = !0, ye(i, "Cooling Failure: heat dissipation reduced by 2.")), Nt(e, "shutdown") && (i.cannotAct = !0, ye(i, "Shutdown: the machine cannot act until it is power-cycled.")), Nt(e, "proneMechFall") && (i.prone = !0, i.noSprint = !0, i.noJump = !0, ye(i, "Prone: crawl only; Close attacks are impaired and Close defense is compromised.")), Nt(e, "onFire") && (i.startTurnHeat += 1, ye(i, "On Fire: gain 1 Heat at the start of the turn.")), Nt(e, "actuatorFailure") && (i.handling += -1), Nt(e, "gyroDamage") && (i.pilotingDn += 1), Nt(e, "suppressedMechanical") && (i.attackAr += -2, i.noTargetingDataGeneration = !0), Nt(e, "evasiveWeave") && (i.attackAr += -2, i.defenseDr += 2), Nt(e, "braced") && (i.attackAr += 1), Nt(e, "targetFocused") && (i.defenseDr += -2, i.targetFocused = !0), Nt(e, "entrenchedHullDown") && (i.defenseDr += 5), Nt(e, "exposed") && (i.defenseDr += -2);
}
function pw(i, e = null) {
  const t = Mt(e, "head"), n = Mt(e, "torso"), a = Mt(e, "arms"), r = Mt(e, "legs"), s = Dt(t);
  s >= 1 && (i.trackingPenaltyAsTarget += 1, ye(i, "Head impaired: attackers gain +1 tracking penalty bonus against this machine.")), s >= 2 && (i.detectionStateCap = Pd(i.detectionStateCap, "track"), ye(i, "Head damaged: detection state against this machine is capped at Track.")), s >= 3 && (i.noSensorActions = !0, ye(i, "Head crippled: no sensor or ECM actions.")), s >= 4 && (i.detectionStateCap = Pd(i.detectionStateCap, "contact"), ye(i, "Head disabled: detection state against this machine is capped at Contact."));
  const o = Dt(n);
  o >= 1 && (i.generalAttackDice += -1, ye(i, "Torso impaired: -1 die on attacks.")), o >= 2 && (i.attackHeat += 1, ye(i, "Torso damaged: attacks generate +1 Heat.")), o >= 3 && (i.pilotingDn += 1, ye(i, "Torso crippled: Gyro Damage (+1 Piloting DN).")), o >= 4 && (i.reactorBreach = !0, ye(i, "Torso disabled: Reactor Breach risk is active."));
  const l = Dt(a);
  l >= 1 && (i.armAttackDice += -1), l >= 2 && (i.armAttackDice += -1), l >= 3 && (i.armAttackDice += -1), l >= 4 && (i.armMountedOffline = !0, ye(i, "Arms disabled: all arm-mounted weapon groups are offline.")), l > 0 && ye(i, `Arms ${oe(["", "impaired", "damaged", "crippled", "disabled"][Math.min(l, 4)])}: arm-mounted attacks are degraded.`);
  const c = Dt(r);
  c >= 1 && (i.movementPenalty += 1, ye(i, "Legs impaired: -1 movement.")), c >= 2 && (i.noSprint = !0, ye(i, "Legs damaged: cannot Sprint.")), c >= 3 && (i.handling += -1, i.noSprint = !0, ye(i, "Legs crippled: Actuator Failure (-1 Handling).")), c >= 4 && (i.immobile = !0, ye(i, "Legs disabled: immobile.")), r != null && r.destroyed && (i.immobile = !0, i.forcedProne = !0, ye(i, "Legs destroyed: prone and immobilized.")), a != null && a.destroyed && ye(i, "Arms destroyed: arm-mounted weapon groups are destroyed."), t != null && t.destroyed && ye(i, "Head destroyed: pilot survival is unlikely."), n != null && n.destroyed && ye(i, "Torso destroyed: reactor detonation is imminent.");
}
function gw(i, e = null) {
  const t = Mt(e, "front"), n = Mt(e, "turret"), a = Mt(e, "rear"), r = Mt(e, "core"), s = Math.max(
    Dt(t),
    Dt(Mt(e, "side")),
    Dt(Mt(e, "rear")),
    Dt(r)
  );
  s >= 1 && (i.system += -1, i.reliability += -1, ye(i, "Body impaired: -1 System and -1 Reliability.")), s >= 2 && (i.attackHeat += 1, ye(i, "Body damaged: attacks generate +1 Heat.")), s >= 3 && (i.system += -2, i.handling += -2, ye(i, "Body crippled: -2 System and -2 Handling.")), s >= 4 && (i.cannotAct = !0, ye(i, "Body disabled: the vehicle is disabled."));
  const o = Dt(n);
  o >= 1 && (i.turretAttackDice += -1), o >= 2 && (i.turretAttackDice += -1), o >= 3 && (i.trackingPenaltyAsTarget += 2, ye(i, "Turret crippled: +2 tracking penalty against the vehicle.")), o >= 4 && (i.turretOffline = !0, ye(i, "Turret disabled: turret weapons are offline."));
  const l = Math.max(
    Dt(a),
    Dt(Mt(e, "rotor"))
  );
  l >= 1 && (i.movementPenalty += 1), l >= 2 && (i.movementPenalty += 1), l >= 3 && (i.noSprint = !0, ye(i, "Mobility crippled: cannot Sprint.")), l >= 4 && (i.immobile = !0, ye(i, "Mobility disabled: immobile."));
}
function zt(i = null) {
  const e = {
    handling: 0,
    system: 0,
    reliability: 0,
    pilotingDn: 0,
    pilotingDice: 0,
    acquireDice: 0,
    targetingDice: 0,
    generalAttackDice: 0,
    armAttackDice: 0,
    turretAttackDice: 0,
    attackAr: 0,
    defenseDr: 0,
    movementPenalty: 0,
    trackingPenaltyAsTarget: 0,
    ecmSpikeDefenseDice: 0,
    startTurnHeat: 0,
    startTurnSaLoss: 0,
    attackHeat: 0,
    energyAttackHeat: 0,
    energyAttackDamage: 0,
    noSprint: !1,
    noJump: !1,
    noSensorActions: !1,
    noTargetingDataGeneration: !1,
    noTargetingDataUse: !1,
    cannotAct: !1,
    immobile: !1,
    rangeCapClose: !1,
    coolingImpaired: !1,
    detectionStateCap: "lock",
    armMountedOffline: !1,
    turretOffline: !1,
    targetFocused: !1,
    reactorBreach: !1,
    forcedProne: !1,
    prone: !1,
    effectTexts: []
  };
  return mw(e, i), fw(e, i), Qp(i) === A.actorTypes.battlemech ? pw(e, i) : gw(e, i), e;
}
function hw(i = null) {
  const e = zt(i);
  return {
    handling: e.handling,
    system: e.system,
    reliability: e.reliability
  };
}
function yw(i = null) {
  return zt(i).pilotingDn;
}
function bw(i = null) {
  return zt(i).pilotingDice;
}
function Sw(i = null) {
  return zt(i).acquireDice;
}
function Aw(i = null) {
  return zt(i).targetingDice;
}
function ww(i = null, e = {}) {
  const t = zt(i), n = Xp(i, e);
  let a = t.generalAttackDice;
  return n.isArmMounted && (a += t.armAttackDice), n.isTurretMounted && (a += t.turretAttackDice), t.prone && n.rangeBand === "close" && (a += -3), a;
}
function Dd(i = null, e = {}) {
  const t = zt(i), n = String((e == null ? void 0 : e.rangeBand) ?? "").trim().toLowerCase();
  return {
    ar: t.attackAr,
    dr: t.defenseDr + (t.prone && n === "close" ? -5 : 0)
  };
}
function Tw(i = null, e = {}) {
  const t = zt(i), n = Xp(i, e);
  return t.cannotAct ? { blocked: !0, reason: "Shutdown prevents the machine from acting." } : t.armMountedOffline && n.isArmMounted ? { blocked: !0, reason: "Arm-mounted weapon groups are offline from degradation." } : t.turretOffline && n.isTurretMounted ? { blocked: !0, reason: "Turret weapons are offline from degradation." } : { blocked: !1, reason: "" };
}
function kw(i = null) {
  const e = zt(i);
  return e.cannotAct || e.noSensorActions;
}
function Mw(i = null) {
  const e = zt(i);
  return e.cannotAct || e.noSensorActions || e.noTargetingDataGeneration;
}
function Cw(i = null) {
  const e = zt(i);
  return e.noTargetingDataUse || e.cannotAct;
}
function vw(i = null) {
  return zt(i).rangeCapClose;
}
function Ew(i = null) {
  return zt(i).trackingPenaltyAsTarget;
}
function Iw(i = null) {
  return zt(i).detectionStateCap;
}
function Nw({ attacker: i = null, targetActor: e = null, value: t = 0 } = {}) {
  let n = Math.max(0, Jp(t, 0));
  return Cw(i) ? 0 : (Nt(e, "ecmJamming") && (n = Math.max(0, n - 2)), n);
}
function Au(i = null) {
  const e = zt(i);
  return {
    startTurnHeat: e.startTurnHeat,
    coolingImpaired: e.coolingImpaired,
    attackHeat: e.attackHeat,
    energyAttackHeat: e.energyAttackHeat,
    energyAttackDamage: e.energyAttackDamage
  };
}
function Rw(i = null) {
  const e = zt(i);
  return {
    movementPenalty: e.movementPenalty,
    noSprint: e.noSprint,
    noJump: e.noJump,
    immobile: e.immobile,
    forcedProne: e.forcedProne
  };
}
function Pw(i = "", e = "", t = {}) {
  const n = Dt(t), a = !!(t != null && t.destroyed);
  if (i === A.actorTypes.battlemech) {
    if (e === "head") {
      if (a) return "Pilot dead.";
      if (n >= 4) return "Contact state capped at Contact; pilot incapacitation risk.";
      if (n >= 3) return "No sensor or ECM actions.";
      if (n >= 2) return "Contact state capped at Track.";
      if (n >= 1) return "+1 tracking penalty.";
    }
    if (e === "torso") {
      if (a) return "Reactor detonation; pilot dead.";
      if (n >= 4) return "Reactor Breach.";
      if (n >= 3) return "Gyro Damage.";
      if (n >= 2) return "+1 Heat to all actions.";
      if (n >= 1) return "-1 die on attacks.";
    }
    if (e === "arms") {
      if (a) return "All arm-mounted weapon groups destroyed.";
      if (n >= 4) return "All arm-mounted weapon groups offline.";
      if (n >= 3) return "-3 dice on attacks with arm-mounted weapon groups.";
      if (n >= 2) return "-2 dice on attacks with arm-mounted weapon groups.";
      if (n >= 1) return "-1 die on attacks with arm-mounted weapon groups.";
    }
    if (e === "legs") {
      if (a) return "Prone and immobilized.";
      if (n >= 4) return "Immobile.";
      if (n >= 3) return "Actuator Failure; cannot run.";
      if (n >= 2) return "Cannot Sprint.";
      if (n >= 1) return "-1 movement.";
    }
  } else {
    if (["front", "side", "rear", "core"].includes(e)) {
      if (a) return "Vehicle destroyed; crew dead.";
      if (n >= 4) return "Vehicle disabled; crew incapacitation risk.";
      if (n >= 3) return "-2 System and -2 Handling.";
      if (n >= 2) return "+1 Heat for all actions.";
      if (n >= 1) return "-1 System and -1 Reliability.";
    }
    if (e === "turret") {
      if (a) return "Weapons destroyed.";
      if (n >= 4) return "Weapons offline.";
      if (n >= 3) return "+2 tracking penalty.";
      if (n >= 2) return "-2 dice on attacks.";
      if (n >= 1) return "-1 die on attacks.";
    }
    if (e === "rotor") {
      if (a) return "Immobile; advance body condition by 1.";
      if (n >= 4) return "Immobile.";
      if (n >= 3) return "Cannot Sprint.";
      if (n >= 2) return "-2 movement.";
      if (n >= 1) return "-1 movement.";
    }
  }
  return "";
}
function Dw(i = null) {
  var n, a;
  const e = Qp(i), t = /* @__PURE__ */ new Set();
  return e === A.actorTypes.battlemech && (Dt(Mt(i, "head")) >= 1 && t.add("sensorDegraded"), Dt(Mt(i, "head")) >= 3 && t.add("sensorBlind"), Dt(Mt(i, "torso")) >= 3 && t.add("gyroDamage"), Dt(Mt(i, "torso")) >= 4 && t.add("reactorBreach"), Dt(Mt(i, "legs")) >= 3 && t.add("actuatorFailure"), (n = Mt(i, "arms")) != null && n.destroyed && t.add("armDestroyed"), (Dt(Mt(i, "legs")) >= 4 || (a = Mt(i, "legs")) != null && a.destroyed) && t.add("legDestroyed")), Array.from(t);
}
function Zp(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? t : e;
}
function Yt(i, e = 0) {
  return Math.max(e, Zp(i, e));
}
function Nr(i) {
  return Array.isArray(i) ? i : [];
}
function wu(i = {}) {
  return (i == null ? void 0 : i.system) ?? i ?? {};
}
function Ow(i) {
  return String((i == null ? void 0 : i.name) ?? "BattleMech").trim() || "BattleMech";
}
function eg(i = {}) {
  var e;
  return Nr((e = i == null ? void 0 : i.mwd) == null ? void 0 : e.crits).filter((t) => (t == null ? void 0 : t.active) !== !1);
}
function Hs(i = {}) {
  var e, t, n;
  return Yt((n = (t = (e = wu(i)) == null ? void 0 : e.mwd) == null ? void 0 : t.heat) == null ? void 0 : n.pendingGenerated, 0);
}
function Lw(i = null) {
  if (!i) return "";
  const e = String((i == null ? void 0 : i.combatId) ?? "").trim(), t = String((i == null ? void 0 : i.combatantId) ?? "").trim(), n = Number((i == null ? void 0 : i.round) ?? NaN), a = Number((i == null ? void 0 : i.turn) ?? NaN);
  return !e || !t || !Number.isFinite(n) || !Number.isFinite(a) ? "" : `${e}:${t}:${n}:${a}`;
}
function zl(i = {}) {
  const e = (i == null ? void 0 : i.system) ?? i ?? {}, t = [
    e.damageType,
    e.category,
    e.weaponCategory,
    e.attackType
  ].map((a) => String(a ?? "").trim().toLowerCase()).filter(Boolean), n = Nr(e.traits).map((a) => String(a ?? "").trim().toLowerCase());
  return t.includes("energy") || n.includes("energy");
}
function _w({ weapons: i = [], crits: e = [] } = {}) {
  const t = Nr(i).filter(Boolean), n = Nr(e).filter((c) => (c == null ? void 0 : c.active) !== !1), a = t.reduce((c, u) => {
    var d;
    return c + Yt(((d = u == null ? void 0 : u.system) == null ? void 0 : d.heat) ?? (u == null ? void 0 : u.heat), 0);
  }, 0), r = t.some(zl), s = n.map((c) => (c == null ? void 0 : c.resourceEffects) ?? {}).filter((c) => c && typeof c == "object"), o = s.reduce((c, u) => c + Yt(u.heatPerAttack, 0), 0), l = r ? s.reduce((c, u) => c + Yt(u.heatPerEnergyAttack, 0), 0) : 0;
  return {
    weaponCount: t.length,
    baseHeat: a,
    extraAttackHeat: o,
    extraEnergyHeat: l,
    total: a + o + l
  };
}
function an(i = {}) {
  var I, O, x, B, P, L, D, G;
  const e = wu(i), t = ((I = e == null ? void 0 : e.monitors) == null ? void 0 : I.heat) ?? {}, n = ((O = e == null ? void 0 : e.mwd) == null ? void 0 : O.heat) ?? {}, a = ((x = e == null ? void 0 : e.hybrid) == null ? void 0 : x.heat) ?? {}, r = eg(e), s = Au(i), o = Yt(t.value ?? n.current, 0), l = Yt(t.max ?? n.max ?? n.hardMax, 0), c = du(n.thresholds ?? {}, l), u = Math.max(
    l,
    o,
    Yt(c.shutdown ?? c.danger, 0)
  ), d = Yt(a.dissipation ?? n.ventPerTurn, 1), m = r.some((V) => (V == null ? void 0 : V.escalationKey) === "heat"), f = !!(n.coolingImpaired || m || s.coolingImpaired), p = f ? Math.max(1, d - 2) : d, g = Hs(e), h = String(n.lastResolvedActivationKey ?? "").trim(), y = Ap(o, c, l), b = uu(o, c), S = b.dangerLevel > 0, w = Yt((P = (B = e == null ? void 0 : e.attributes) == null ? void 0 : B.chassis) == null ? void 0 : P.value, 0), T = Yt((D = (L = e == null ? void 0 : e.attributes) == null ? void 0 : L.reliability) == null ? void 0 : D.value, 0), k = S ? Sp(b.dangerLevel, w, T) : null, C = VS(((G = e == null ? void 0 : e.mwd) == null ? void 0 : G.locations) ?? {});
  return {
    current: o,
    max: l,
    trackLength: l,
    displayMax: u,
    dissipation: d,
    effectiveDissipation: p,
    coolingImpaired: f,
    pendingGenerated: g,
    lastResolvedActivationKey: h,
    thresholds: c,
    statusCode: y,
    status: mu(y),
    penalties: b,
    inDanger: S,
    dangerChecks: k,
    volatile: C
  };
}
function xw(i = {}, { pendingGenerated: e = null } = {}) {
  var d, m, f, p;
  const t = wu(i), n = an(t), a = e === null ? n.pendingGenerated : Yt(e, 0), r = qS(n.current, a, n.effectiveDissipation, n.trackLength), s = Ap(r, n.thresholds, n.trackLength), o = uu(r, n.thresholds), l = o.dangerLevel > 0, c = Yt((m = (d = t == null ? void 0 : t.attributes) == null ? void 0 : d.chassis) == null ? void 0 : m.value, 0), u = Yt((p = (f = t == null ? void 0 : t.attributes) == null ? void 0 : f.reliability) == null ? void 0 : p.value, 0);
  return {
    ...n,
    previousHeat: n.current,
    generated: a,
    newHeat: r,
    statusCode: s,
    status: mu(s),
    penalties: o,
    inDanger: l,
    dangerChecks: l ? Sp(o.dangerLevel, c, u) : null,
    pendingGeneratedAfter: 0
  };
}
async function Tu(i, e, { reason: t = "" } = {}) {
  if (!i || i.type !== "battlemech") return { ok: !1, reason: "BattleMech actor required." };
  const n = Yt(e, 0);
  return await i.update({
    "system.mwd.heat.pendingGenerated": n,
    "system.mwd.heat.lastResolvedActivationKey": ""
  }), {
    ok: !0,
    actor: i,
    pendingGenerated: n,
    reason: t
  };
}
async function $w(i, e, { reason: t = "" } = {}) {
  if (!i || i.type !== "battlemech") return { ok: !1, reason: "BattleMech actor required." };
  const n = Hs(i);
  return Tu(i, n + Zp(e, 0), { reason: t });
}
async function tg(i, { weaponIds: e = [], attackProfile: t = null, reason: n = "" } = {}) {
  var m;
  if (!i || i.type !== "battlemech") return { ok: !1, reason: "BattleMech actor required." };
  const a = Array.from(new Set(Nr(e).map((f) => String(f ?? "").trim()).filter(Boolean))), r = a.map((f) => {
    var p, g;
    return (g = (p = i.items) == null ? void 0 : p.get) == null ? void 0 : g.call(p, f);
  }).filter(Boolean), s = Yt((t == null ? void 0 : t.heat) ?? ((m = t == null ? void 0 : t.attackSummary) == null ? void 0 : m.heat), 0), o = _w({
    weapons: r,
    crits: eg(i.system)
  });
  o.weaponCount = Math.max(o.weaponCount, a.length, s > 0 ? 1 : 0), o.baseHeat = Math.max(o.baseHeat, s);
  const l = Au(i), c = r.some(zl) || zl(t);
  if (o.extraAttackHeat += Math.max(0, Number(l.attackHeat ?? 0)), o.extraEnergyHeat += c ? Math.max(0, Number(l.energyAttackHeat ?? 0)) : 0, o.total = o.baseHeat + o.extraAttackHeat + o.extraEnergyHeat, o.total <= 0)
    return { ok: !0, actor: i, pendingGenerated: Hs(i), contribution: o };
  const d = Hs(i) + o.total;
  return await i.update({
    "system.mwd.heat.pendingGenerated": d,
    "system.mwd.heat.lastResolvedActivationKey": ""
  }), {
    ok: !0,
    actor: i,
    reason: n,
    pendingGenerated: d,
    contribution: o
  };
}
async function Bw(i, e, { source: t = "" } = {}) {
  if (!i || !(e != null && e.inDanger) || !(e != null && e.dangerChecks)) return;
  const n = Ow(i), a = String(t ?? "").trim(), r = [
    `<h3>${n} - Danger Zone (${e.penalties.dangerLevel} level${e.penalties.dangerLevel > 1 ? "s" : ""})</h3>`,
    a ? `<p><em>Heat resolved via ${a}.</em></p>` : "",
    `<p><strong>Shutdown Check:</strong> Roll <strong>${e.dangerChecks.shutdownPool}d6</strong> vs DN <strong>${e.dangerChecks.shutdownDN}</strong> (Chassis + Reliability). On failure, compare margin to pilot's System Operations: if margin &lt; System Operations the pilot may override; otherwise the mech shuts down.</p>`,
    e.volatile ? `<p><strong>Explosion Check:</strong> Roll <strong>${e.dangerChecks.explosionPool}d6</strong> vs DN <strong>${e.dangerChecks.explosionDN}</strong> (Chassis + Reliability - ${e.penalties.dangerLevel}). Failure detonates volatile components.</p>` : "<p><em>No volatile components detected - Explosion check skipped.</em></p>"
  ].filter(Boolean);
  await ChatMessage.create({
    speaker: ChatMessage.getSpeaker({ actor: i }),
    content: r.join("")
  });
}
async function ku(i, { source: e = "", postDangerCard: t = !0, activation: n = null, force: a = !1 } = {}) {
  if (!i || i.type !== "battlemech") return { ok: !1, reason: "BattleMech actor required." };
  const r = xw(i), s = Lw(n);
  return !a && s && r.lastResolvedActivationKey === s ? {
    ok: !0,
    actor: i,
    source: e,
    skipped: !0,
    resolution: r
  } : (await i.update({
    "system.monitors.heat.value": r.newHeat,
    "system.mwd.heat.current": r.newHeat,
    "system.mwd.heat.pendingGenerated": r.pendingGeneratedAfter,
    "system.mwd.heat.lastResolvedActivationKey": s
  }), t && r.inDanger && await Bw(i, r, { source: e }), {
    ok: !0,
    actor: i,
    source: e,
    resolution: r
  });
}
function zw(i = null, e = "") {
  const t = _e(e);
  return t ? ho(i).find((n) => _e(n == null ? void 0 : n.id) === t) ?? null : null;
}
function Fw(i = {}, e = {}) {
  const t = _e(i == null ? void 0 : i.weaponGroupId);
  if (t && t === _e(e.weaponGroupId)) return !0;
  const n = new Set((Array.isArray(e.weaponIds) ? e.weaponIds : []).map(_e).filter(Boolean));
  return n.size > 0 && (Array.isArray(i == null ? void 0 : i.weaponIds) ? i.weaponIds : []).some((a) => n.has(_e(a)));
}
function ig(i = null, { weaponGroupId: e = "", weaponId: t = "", weapon: n = null } = {}) {
  var d, m;
  const a = _e(e), r = _e(t), s = zw(i, a), o = n ?? (r ? ((m = (d = i == null ? void 0 : i.items) == null ? void 0 : d.get) == null ? void 0 : m.call(d, r)) ?? null : null), l = ho(i);
  let c = s;
  !c && r && (c = l.find((f) => (Array.isArray(f == null ? void 0 : f.weaponIds) ? f.weaponIds : []).map(_e).includes(r)) ?? null);
  const u = c ? Ir(i, c) : o ? [o] : [];
  return {
    weaponGroupId: _e(c == null ? void 0 : c.id),
    weaponGroupName: String((c == null ? void 0 : c.name) ?? "").trim(),
    weaponIds: u.map((f) => _e(f == null ? void 0 : f.id)).filter(Boolean),
    weapons: u,
    hasEnergy: u.some(Dp),
    hasBallistic: u.some(Op)
  };
}
function Hw(i = null) {
  const t = _n(i).reduce((n, a) => _e(a == null ? void 0 : a.key) === "targetingProcessorLock" ? n + 1 : n, 0);
  return {
    baseCost: 2,
    extraCost: t,
    totalCost: 2 + t
  };
}
function Uw(i = null, { weaponGroupId: e = "", weaponId: t = "", weapon: n = null } = {}) {
  const a = ig(i, { weaponGroupId: e, weaponId: t, weapon: n }), r = _n(i);
  for (const s of r) {
    if (!Fw(s, a)) continue;
    const o = _e(s == null ? void 0 : s.statusId);
    if (o === "weaponFailure")
      return {
        blocked: !0,
        crit: s,
        reason: s != null && s.weaponGroupName ? `${s.weaponGroupName} is unusable because of ${s.label}.` : `${s.label} prevents this weapon group from attacking.`
      };
    if (o === "jammedBallistic" && a.hasBallistic)
      return {
        blocked: !0,
        crit: s,
        reason: s != null && s.weaponGroupName ? `${s.weaponGroupName} is jammed and cannot make ballistic attacks.` : `${s.label} prevents this ballistic attack.`
      };
  }
  return { blocked: !1, crit: null, reason: "" };
}
function jw(i = null, { weaponGroupId: e = "", weaponId: t = "", weapon: n = null } = {}) {
  return ig(i, { weaponGroupId: e, weaponId: t, weapon: n }).hasEnergy ? _n(i).reduce((r, s) => _e(s == null ? void 0 : s.statusId) === "reactorInstability" ? r - 1 : r, 0) : 0;
}
function Kw(i = null) {
  const e = _n(i), t = {
    saCost: 0,
    heatDelta: 0,
    automated: [],
    reminders: []
  };
  for (const n of e) {
    const a = _e(n == null ? void 0 : n.statusId);
    if (a === "staggeredMechanical") {
      t.saCost += 1, t.automated.push({
        crit: n,
        label: "Staggered",
        text: "Spent 1 SA at the start of activation."
      });
      continue;
    }
    if (a === "overheating") {
      t.heatDelta += 2, t.automated.push({
        crit: n,
        label: "Overheating",
        text: "Gained 2 Heat at the start of activation."
      });
      continue;
    }
    a === "skidding" && t.reminders.push({
      crit: n,
      label: "Skidding",
      text: yo(n)
    });
  }
  return t;
}
function Mu(i = {}) {
  return {
    statusLabel: bu(i),
    effectText: yo(i),
    automationMode: Su(i),
    scopeSummary: HA(i)
  };
}
function ng(i = {}) {
  const e = Mu(i), t = e.automationMode === "engine" ? "Automated" : "Reminder";
  return [
    e.statusLabel ? `${e.statusLabel}: ${e.effectText}` : e.effectText,
    e.scopeSummary,
    t
  ].filter(Boolean).join(" | ");
}
function aa(i = null) {
  return (i == null ? void 0 : i.type) === A.actorTypes.vehicle || (i == null ? void 0 : i.type) === A.actorTypes.battlemech;
}
const Rr = ["close", "near", "far", "extreme"], Gw = ["near", "close", "far", "extreme"];
function $a(i) {
  return Array.isArray(i) ? i : [];
}
function li(i = "") {
  return String(i ?? "").trim();
}
function Pt(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? t : e;
}
function ag(i = {}) {
  const e = String((i == null ? void 0 : i.max) ?? (i == null ? void 0 : i.bandCap) ?? "near").trim().toLowerCase();
  return Rr.includes(e) ? e : "near";
}
function Us(i = "near") {
  const e = Rr.indexOf(String(i ?? "").trim().toLowerCase());
  return e >= 0 ? e : Rr.indexOf("near");
}
function Ww(i = "near", e = "near") {
  return Us(i) <= Us(e) ? i : e;
}
function Fl(i = {}) {
  return {
    max: ag(i),
    close: Pt((i == null ? void 0 : i.close) ?? (i == null ? void 0 : i.short), 0),
    near: Pt((i == null ? void 0 : i.near) ?? (i == null ? void 0 : i.medium), 0),
    far: Pt((i == null ? void 0 : i.far) ?? (i == null ? void 0 : i.long), 0),
    extreme: Pt(i == null ? void 0 : i.extreme, 0)
  };
}
function qw(i = null, e = null) {
  var t, n;
  return String(
    (e == null ? void 0 : e.category) ?? ((t = i == null ? void 0 : i.system) == null ? void 0 : t.category) ?? ((n = i == null ? void 0 : i.system) == null ? void 0 : n.weaponCategory) ?? "ranged"
  ).trim().toLowerCase() || "ranged";
}
function rg(i = null, e = null) {
  var t, n, a, r;
  return String(
    (e == null ? void 0 : e.resolverKey) ?? ((t = e == null ? void 0 : e.resolution) == null ? void 0 : t.resolverKey) ?? ((a = (n = i == null ? void 0 : i.system) == null ? void 0 : n.resolution) == null ? void 0 : a.resolverKey) ?? ((r = i == null ? void 0 : i.system) == null ? void 0 : r.resolverKey) ?? "standard"
  ).trim().toLowerCase() || "standard";
}
function Vw(i = null, e = null) {
  var r, s, o, l;
  if (rg(i, e) !== "standard" || String(
    ((r = e == null ? void 0 : e.areaEffect) == null ? void 0 : r.kind) ?? ((o = (s = i == null ? void 0 : i.system) == null ? void 0 : s.areaEffect) == null ? void 0 : o.kind) ?? ""
  ).trim().toLowerCase() === "persistent") return !0;
  const a = String(
    (e == null ? void 0 : e.area) ?? ((l = i == null ? void 0 : i.system) == null ? void 0 : l.area) ?? "none"
  ).trim().toLowerCase();
  return !!(a && a !== "none");
}
function Hl(i = {}) {
  return Rr.reduce((e, t) => (e[t] = Pt(i == null ? void 0 : i[t], 0), e), {});
}
function Yw(i = null) {
  var n, a, r;
  const e = (a = (n = i == null ? void 0 : i.system) == null ? void 0 : n.mwd) == null ? void 0 : a.weaponGroups;
  if (e) {
    const s = Array.isArray(e) ? e : Object.values(e);
    if (s.length) return s;
  }
  const t = (r = i == null ? void 0 : i.system) == null ? void 0 : r.weaponGroups;
  return Array.isArray(t) ? t : [];
}
function Jw(i = null) {
  return Yw(i).map((e, t) => ({
    id: li(e == null ? void 0 : e.id) || `group-${t + 1}`,
    index: Number.isInteger(Number(e == null ? void 0 : e.index)) ? Number(e.index) : t,
    name: String((e == null ? void 0 : e.name) ?? `Weapon Group ${t + 1}`).trim() || `Weapon Group ${t + 1}`,
    weaponIds: $a(e == null ? void 0 : e.weaponIds).map(li).filter(Boolean),
    isPrimary: !!(e != null && e.isPrimary)
  }));
}
function sg(i = null) {
  var t;
  const e = (i == null ? void 0 : i.state) ?? i ?? {};
  return $a((t = e == null ? void 0 : e.actionState) == null ? void 0 : t.usedWeaponGroupIds).map(li).filter(Boolean);
}
function Qw(i = {}, e = "") {
  const t = li(e);
  if (!t) return i;
  i.actionState ?? (i.actionState = {});
  const n = new Set(sg(i));
  return n.add(t), i.actionState.usedWeaponGroupIds = Array.from(n), i;
}
function Xw(i = null, e = null) {
  var S, w, T, k, C, I, O, x, B, P, L, D, G, V, Z;
  const t = $a(e == null ? void 0 : e.weaponIds).map(li).filter(Boolean), n = t.map((E) => {
    var X, H;
    return ((H = (X = i == null ? void 0 : i.items) == null ? void 0 : X.get) == null ? void 0 : H.call(X, E)) ?? null;
  }).filter(Boolean), a = t.filter((E) => !n.some((X) => li(X == null ? void 0 : X.id) === E)), r = [], s = [], o = [], l = [];
  for (const E of n) {
    if (((E == null ? void 0 : E.canonicalType) ?? (E == null ? void 0 : E.type)) !== A.itemType.mechWeapon) {
      l.push(`${(E == null ? void 0 : E.name) ?? "Grouped item"} is not a BattleMech weapon.`);
      continue;
    }
    if (((S = E == null ? void 0 : E.isActive) == null ? void 0 : S.call(E)) === !1) {
      o.push(`${E.name ?? "Weapon"} is inactive and does not contribute to ranged fire.`);
      continue;
    }
    const H = ((w = E.getCombatProfile) == null ? void 0 : w.call(E)) ?? null;
    if (qw(E, H) === "melee") {
      o.push(`${E.name ?? "Weapon"} is melee-only and is ignored for ranged fire.`);
      continue;
    }
    if (Vw(E, H)) {
      l.push(`${E.name ?? "Weapon"} uses a special attack mode and cannot be grouped in v1.`);
      continue;
    }
    const ie = vr(i, E == null ? void 0 : E.id);
    if (!ie) {
      l.push(`${E.name ?? "Weapon"} is not mounted in a loaded hardpoint.`);
      continue;
    }
    if (!yu(ie, E)) {
      l.push(`${E.name ?? "Weapon"} no longer matches its assigned hardpoint.`);
      continue;
    }
    r.push({
      id: li(E == null ? void 0 : E.id),
      name: String((E == null ? void 0 : E.name) ?? "Weapon").trim() || "Weapon",
      img: (E == null ? void 0 : E.img) ?? "",
      skill: String((H == null ? void 0 : H.skill) ?? ((T = E == null ? void 0 : E.system) == null ? void 0 : T.skill) ?? "gunnery").trim() || "gunnery",
      damageType: String((H == null ? void 0 : H.damageType) ?? ((k = E == null ? void 0 : E.system) == null ? void 0 : k.damageType) ?? "kinetic").trim() || "kinetic",
      damageTypeLabel: String(
        (H == null ? void 0 : H.damageTypeLabel) ?? ((C = E == null ? void 0 : E.getDamageTypeLabel) == null ? void 0 : C.call(E)) ?? (H == null ? void 0 : H.damageType) ?? ((I = E == null ? void 0 : E.system) == null ? void 0 : I.damageType) ?? "kinetic"
      ).trim() || "kinetic",
      resolverKey: rg(E, H),
      range: Fl((H == null ? void 0 : H.range) ?? ((O = E == null ? void 0 : E.system) == null ? void 0 : O.range) ?? {}),
      rangeCap: ag((H == null ? void 0 : H.range) ?? ((x = E == null ? void 0 : E.system) == null ? void 0 : x.range) ?? {}),
      attackRatingBand: Hl((H == null ? void 0 : H.attackRatingBand) ?? ((B = E == null ? void 0 : E.system) == null ? void 0 : B.attackRatingBand) ?? {}),
      damage: Pt((H == null ? void 0 : H.damage) ?? ((P = E == null ? void 0 : E.system) == null ? void 0 : P.damage), 0),
      clusteringDice: Math.max(0, Pt(H == null ? void 0 : H.clusteringDice, 0)),
      ap: Pt((H == null ? void 0 : H.ap) ?? ((L = E == null ? void 0 : E.system) == null ? void 0 : L.ap) ?? ((D = E == null ? void 0 : E.system) == null ? void 0 : D.armorPiercing), 0),
      heat: Pt(((G = E == null ? void 0 : E.system) == null ? void 0 : G.heat) ?? (H == null ? void 0 : H.heat), 0),
      notes: String((H == null ? void 0 : H.notes) ?? ((V = E == null ? void 0 : E.system) == null ? void 0 : V.notes) ?? ((Z = E == null ? void 0 : E.system) == null ? void 0 : Z.description) ?? "").trim(),
      skillDef: (H == null ? void 0 : H.skillDef) ?? null,
      hardpointId: li(ie == null ? void 0 : ie.id),
      hardpointType: String((ie == null ? void 0 : ie.type) ?? "").trim(),
      hardpointSize: String((ie == null ? void 0 : ie.size) ?? "").trim(),
      hardpointLocation: String((ie == null ? void 0 : ie.location) ?? "").trim(),
      sourceWeapon: E
    }), s.push({
      id: li(ie == null ? void 0 : ie.id),
      type: String((ie == null ? void 0 : ie.type) ?? "").trim(),
      size: String((ie == null ? void 0 : ie.size) ?? "").trim(),
      location: String((ie == null ? void 0 : ie.location) ?? "").trim(),
      itemId: li(E == null ? void 0 : E.id),
      weaponName: String((E == null ? void 0 : E.name) ?? "Weapon").trim() || "Weapon"
    });
  }
  a.length && l.push(`Missing weapon ids: ${a.join(", ")}.`), r.length === 0 && l.length === 0 && l.push("No active ranged weapons in this group."), new Set(r.map((E) => E.damageType)).size > 1 && l.push("All grouped ranged weapons must share the same damage type."), new Set(r.map((E) => E.skill)).size > 1 && l.push("All grouped ranged weapons must use the same attack skill."), new Set(r.map((E) => E.resolverKey)).size > 1 && l.push("All grouped ranged weapons must share the same resolver family.");
  const m = r[0] ?? null, f = r.reduce(
    (E, X) => Ww(E, X.rangeCap),
    (m == null ? void 0 : m.rangeCap) ?? "near"
  ), p = Us(f), g = Rr.reduce((E, X, H) => (E[X] = H <= p ? r.reduce((he, ie) => {
    var le;
    return Math.min(he, Pt((le = ie.attackRatingBand) == null ? void 0 : le[X], 0));
  }, Number.POSITIVE_INFINITY) : 0, Number.isFinite(E[X]) || (E[X] = 0), E), {}), h = {
    ...(m == null ? void 0 : m.range) ?? Fl({ max: f }),
    max: f
  }, y = Gw.find((E) => Us(E) <= p) ?? "close", b = r.length > 0 ? {
    damage: r.reduce((E, X) => E + X.damage, 0),
    clusteringDice: r.reduce((E, X) => E + X.clusteringDice, 0),
    ap: Math.max(0, ...r.map((E) => E.ap)),
    heat: r.reduce((E, X) => E + X.heat, 0),
    damageType: (m == null ? void 0 : m.damageType) ?? "kinetic",
    damageTypeLabel: (m == null ? void 0 : m.damageTypeLabel) ?? oe((m == null ? void 0 : m.damageType) ?? "kinetic"),
    rangeCap: f,
    range: h,
    defaultRangeBand: y,
    attackRatings: g
  } : null;
  return {
    id: li(e == null ? void 0 : e.id),
    index: Number.isInteger(Number(e == null ? void 0 : e.index)) ? Number(e.index) : 0,
    name: String((e == null ? void 0 : e.name) ?? "Weapon Group").trim() || "Weapon Group",
    weaponIds: t,
    isPrimary: !!(e != null && e.isPrimary),
    missingWeaponIds: a,
    memberWeapons: r.map((E) => ({
      id: E.id,
      name: E.name,
      img: E.img,
      skill: E.skill,
      damageType: E.damageType,
      damageTypeLabel: E.damageTypeLabel,
      clusteringDice: E.clusteringDice,
      heat: E.heat
    })),
    memberHardpoints: s,
    compatibilityWarnings: o,
    baseDisableReason: l[0] ?? "",
    isAttackLegal: l.length === 0 && r.length > 0,
    attackSummary: b,
    _memberProfiles: r
  };
}
function js(i = null, { usedWeaponGroupIds: e = [] } = {}) {
  const t = new Set($a(e).map(li).filter(Boolean));
  return Jw(i).map((n) => {
    const a = Xw(i, n), r = t.has(a.id), s = !r, o = r ? "Already fired this activation." : a.baseDisableReason;
    return {
      id: a.id,
      index: a.index,
      name: a.name,
      weaponIds: a.weaponIds,
      isPrimary: a.isPrimary,
      missingWeaponIds: a.missingWeaponIds,
      memberWeapons: a.memberWeapons,
      memberHardpoints: a.memberHardpoints,
      compatibilityWarnings: a.compatibilityWarnings,
      isAttackLegal: a.isAttackLegal,
      isAvailableThisActivation: s,
      disableReason: o,
      attackSummary: a.attackSummary,
      _memberProfiles: a._memberProfiles
    };
  });
}
function Zw(i = null, e = "", { usedWeaponGroupIds: t = [] } = {}) {
  const n = li(e);
  return n ? js(i, { usedWeaponGroupIds: t }).find((a) => a.id === n) ?? null : null;
}
function eT(i = null, e = "", { usedWeaponGroupIds: t = [] } = {}) {
  var o;
  const n = Zw(i, e, { usedWeaponGroupIds: t });
  if (!n)
    return { ok: !1, reason: "That weapon group no longer exists.", group: null, profile: null };
  if (!n.isAttackLegal)
    return { ok: !1, reason: n.disableReason || "That weapon group cannot be fired.", group: n, profile: null };
  if (!n.isAvailableThisActivation)
    return { ok: !1, reason: n.disableReason || "That weapon group has already fired this activation.", group: n, profile: null };
  const a = ((o = n._memberProfiles) == null ? void 0 : o[0]) ?? null, r = n.attackSummary ?? {}, s = [
    ...$a(n.compatibilityWarnings),
    ...$a(n._memberProfiles).map((l) => l.notes).filter(Boolean)
  ].join(`
`);
  return {
    ok: !0,
    reason: "",
    group: n,
    profile: {
      id: n.id,
      uuid: (i == null ? void 0 : i.uuid) ?? null,
      name: n.name || "Weapon Group",
      img: (a == null ? void 0 : a.img) ?? "",
      item: null,
      type: "mechWeaponGroup",
      equipped: !0,
      isPrimary: !!n.isPrimary,
      category: "ranged",
      skill: (a == null ? void 0 : a.skill) ?? "gunnery",
      skillDef: (a == null ? void 0 : a.skillDef) ?? null,
      damage: Pt(r.damage, 0),
      clusteringDice: Math.max(0, Pt(r.clusteringDice, 0)),
      ap: Pt(r.ap, 0),
      heat: Pt(r.heat, 0),
      damageType: String(r.damageType ?? "kinetic").trim() || "kinetic",
      damageTypeLabel: String(r.damageTypeLabel ?? r.damageType ?? "kinetic").trim() || "kinetic",
      attackRatingBand: Hl(r.attackRatings ?? {}),
      range: Fl(r.range ?? { max: r.rangeCap ?? "near" }),
      defaultRangeBand: String(r.defaultRangeBand ?? "near").trim() || "near",
      traits: [],
      effects: {},
      notes: s,
      resolverKey: "standard",
      capabilityReport: {
        isTemplated: !1,
        errors: []
      },
      attackSummary: {
        damage: Pt(r.damage, 0),
        clusteringDice: Math.max(0, Pt(r.clusteringDice, 0)),
        ap: Pt(r.ap, 0),
        heat: Pt(r.heat, 0),
        damageType: String(r.damageType ?? "kinetic").trim() || "kinetic",
        damageTypeLabel: String(r.damageTypeLabel ?? r.damageType ?? "kinetic").trim() || "kinetic",
        rangeCap: String(r.rangeCap ?? "near").trim() || "near",
        attackRatings: Hl(r.attackRatings ?? {})
      },
      machineWeaponGroup: {
        id: n.id,
        weaponIds: n.memberWeapons.map((l) => l.id),
        weaponNames: n.memberWeapons.map((l) => l.name),
        memberWeapons: n.memberWeapons.map((l) => ({ id: l.id, name: l.name })),
        hardpointIds: n.memberHardpoints.map((l) => l.id),
        memberHardpoints: n.memberHardpoints.map((l) => ({
          id: l.id,
          type: l.type,
          size: l.size,
          location: l.location,
          itemId: l.itemId,
          weaponName: l.weaponName
        }))
      },
      compatibilityWarnings: [...n.compatibilityWarnings]
    }
  };
}
const Xt = "mwd", pi = "personalCombat", jn = "preparedInterrupt", tT = "systems/mwd/img/icons/status/readied_action.svg", yn = 3, iT = 1, nT = 1;
function Xr(i, e) {
  return !(i != null && i.activation) || !e ? !1 : i.activation.combatId === e.combatId && Number(i.activation.round ?? -1) === Number(e.round ?? -1) && Number(i.activation.turn ?? -1) === Number(e.turn ?? -1) && i.activation.combatantId === e.combatantId;
}
function Cu(i = null) {
  return {
    saRemaining: yn,
    faRemaining: iT,
    raRemaining: nT,
    saSpentThisActivation: 0,
    burnThisActivation: 0,
    attacksThisActivation: 0,
    reactionBurnSinceLastActivation: 0,
    traitUsage: {
      activation: {},
      round: {}
    },
    actionState: {
      aim: null,
      move: null,
      preparedInterrupt: null,
      usedWeaponGroupIds: []
    },
    hazards: {},
    pendingReaction: null,
    machineCritsProcessed: !1,
    actionLog: [],
    activation: i
  };
}
function hs(i, e = null) {
  return foundry.utils.mergeObject(
    Cu(e),
    foundry.utils.deepClone(i ?? {}),
    { inplace: !1, overwrite: !0 }
  );
}
function Ul(i, e = null) {
  const t = hs(i ?? {}, (i == null ? void 0 : i.activation) ?? e);
  return t.actionLog = gr(t.actionLog), t.hazards = Ks(t.hazards), t.pendingReaction = ys(t.pendingReaction), t;
}
function Ks(i) {
  return !i || typeof i != "object" ? {} : Object.fromEntries(
    Object.entries(i).map(([e, t]) => {
      const n = String(e ?? "").trim();
      return !n || !t || typeof t != "object" ? null : [n, {
        tier: lt(t.tier, pe.none),
        turnsExposed: Math.max(0, Number(t.turnsExposed ?? 0) || 0),
        evadeLocked: !!t.evadeLocked,
        lastProcessedRound: Number(t.lastProcessedRound ?? 0) || 0
      }];
    }).filter(Boolean)
  );
}
function ys(i) {
  if (!i || typeof i != "object") return null;
  const e = String(i.type ?? "").trim();
  return e ? {
    type: e,
    sourceKind: String(i.sourceKind ?? "").trim() || null,
    sourceId: String(i.sourceId ?? "").trim() || null,
    messageId: String(i.messageId ?? "").trim() || null,
    resultIndex: Number.isInteger(Number(i.resultIndex)) ? Number(i.resultIndex) : null,
    exposureBefore: lt(i.exposureBefore, pe.none),
    exposureAfterPreview: lt(i.exposureAfterPreview, pe.none),
    edgePoolKey: String(i.edgePoolKey ?? "").trim() || null,
    allowCurrentTurn: !!i.allowCurrentTurn
  } : null;
}
function gr(i) {
  return Array.isArray(i) ? i.map((e) => {
    const t = String((e == null ? void 0 : e.label) ?? "").trim();
    return t ? {
      id: String((e == null ? void 0 : e.id) ?? "").trim(),
      label: t,
      costLabel: String((e == null ? void 0 : e.costLabel) ?? "").trim()
    } : null;
  }).filter(Boolean) : [];
}
function aT(i = []) {
  return gr(i).filter((e) => {
    const t = ps(e == null ? void 0 : e.id);
    return (t == null ? void 0 : t.category) === tt.reaction;
  });
}
function Od(i = null, e = null) {
  const t = Cu(e);
  return t.reactionBurnSinceLastActivation = Math.max(0, Number((i == null ? void 0 : i.reactionBurnSinceLastActivation) ?? 0) || 0), t.actionLog = aT(i == null ? void 0 : i.actionLog), t.hazards = Ks(i == null ? void 0 : i.hazards), t;
}
function Zr(i = null) {
  var e;
  return Ul(i == null ? void 0 : i.state, ((e = i == null ? void 0 : i.state) == null ? void 0 : e.activation) ?? null);
}
function rT(i, e) {
  return i === "free" ? "Free" : i === "burn" ? `+${e} Burn` : `${e} ${String(i).toUpperCase()}`;
}
function sT(i = {}, e = "", { snapshot: t = null, metadata: n = {} } = {}) {
  var s, o, l, c;
  const a = foundry.utils.deepClone(i ?? {});
  a.actionState ?? (a.actionState = {});
  const r = {
    actionId: e,
    round: Number(((s = t == null ? void 0 : t.combat) == null ? void 0 : s.round) ?? 0),
    turn: Number(((o = t == null ? void 0 : t.combat) == null ? void 0 : o.turn) ?? 0),
    combatantId: ((l = t == null ? void 0 : t.combatant) == null ? void 0 : l.id) ?? null
  };
  return e === "aim" && (a.actionState.aim = {
    ...r,
    target: ((c = t == null ? void 0 : t.targeting) == null ? void 0 : c.target) ?? null
  }), e === "move" && (a.actionState.move = {
    ...r,
    moved: !0
  }), e === "prepare" && (a.actionState.preparedInterrupt = {
    ...r,
    condition: String((n == null ? void 0 : n.condition) ?? "").trim(),
    scope: String((n == null ? void 0 : n.scope) ?? "").trim()
  }), a;
}
function Qa(i = {}) {
  var a;
  const e = ((a = i == null ? void 0 : i.actionState) == null ? void 0 : a.preparedInterrupt) ?? null;
  if (!e) return null;
  const t = String((e == null ? void 0 : e.condition) ?? "").trim(), n = String((e == null ? void 0 : e.scope) ?? "").trim();
  return !t && !n ? null : {
    ...e,
    condition: t,
    scope: n
  };
}
function oT(i = null) {
  if (!i) return "";
  const e = [];
  return i.condition && e.push(`Trigger: ${i.condition}`), i.scope && e.push(`Scope: ${i.scope}`), e.join(" | ");
}
function Ld() {
  return (CONFIG.statusEffects ?? []).find((i) => String((i == null ? void 0 : i.id) ?? "").trim() === jn) ?? {
    id: jn,
    name: "Prepared",
    icon: tT
  };
}
function lT(i) {
  const e = (CONFIG.statusEffects ?? []).find((n) => String((n == null ? void 0 : n.id) ?? "").trim() === i), t = String((e == null ? void 0 : e.name) ?? (e == null ? void 0 : e.label) ?? i ?? "").trim();
  return tu(t);
}
function ba(i) {
  const e = Number(i);
  return !Number.isFinite(e) || e === 0 ? "0" : e > 0 ? `+${e}` : String(e);
}
function cT(i) {
  if (typeof i == "number") return Number.isFinite(i) ? i : 0;
  const e = String(i ?? "").trim().match(/[-+]?\d+(\.\d+)?/);
  return e ? Number(e[0]) : 0;
}
function _d(i) {
  var o;
  const e = (i == null ? void 0 : i.document) ?? i ?? null, t = (i == null ? void 0 : i.object) ?? (e == null ? void 0 : e.object) ?? i ?? null, n = String((e == null ? void 0 : e.id) ?? "").trim(), a = F._pendingTokenPositions.get(n) ?? null, r = Number((a == null ? void 0 : a.x) ?? (e == null ? void 0 : e.x)), s = Number((a == null ? void 0 : a.y) ?? (e == null ? void 0 : e.y));
  if (t && Number.isFinite(r) && Number.isFinite(s)) {
    if (typeof t.getCenterPoint == "function")
      return t.getCenterPoint({ x: r, y: s });
    if (typeof t.getCenter == "function")
      return t.getCenter(r, s);
  }
  return (t == null ? void 0 : t.center) ?? ((o = e == null ? void 0 : e.object) == null ? void 0 : o.center) ?? null;
}
function uT(i, e = "") {
  if (!Number.isFinite(i)) return "";
  const t = Math.round(i * 10) / 10, n = Number.isInteger(t) ? String(t) : t.toFixed(1);
  return e ? `${n} ${e}` : n;
}
function Wo(i) {
  var t, n;
  const e = (n = (t = game.settings) == null ? void 0 : t.get) == null ? void 0 : n.call(t, "core", "rollMode");
  return typeof ChatMessage.applyRollMode == "function" && ChatMessage.applyRollMode(i, e), i;
}
function qo(i) {
  return !!In(i);
}
function dT(i = []) {
  const e = Array.isArray(i) ? i.filter(Boolean) : [];
  if (!e.length) return "";
  const t = e.slice().sort((a, r) => Cn(r == null ? void 0 : r.tier) - Cn(a == null ? void 0 : a.tier))[0] ?? null;
  if (!t) return "";
  const n = `HAZARD ${bi(t.tier)} (${Math.max(0, Number(t.turnsExposed ?? 0) || 0)})`;
  return t.evadeLocked ? `${n} LOCK` : n;
}
function jl(i = null) {
  var n, a, r, s;
  const e = /* @__PURE__ */ new Set(), t = (o) => {
    const l = String(o ?? "").trim();
    l && e.add(l);
  };
  return t(i == null ? void 0 : i.id), t(i == null ? void 0 : i._id), t(i == null ? void 0 : i.uuid), t((n = i == null ? void 0 : i.actor) == null ? void 0 : n.id), t((a = i == null ? void 0 : i.actor) == null ? void 0 : a.uuid), t((r = i == null ? void 0 : i.baseActor) == null ? void 0 : r.id), t((s = i == null ? void 0 : i.baseActor) == null ? void 0 : s.uuid), e;
}
function mT(i = "") {
  var n, a, r;
  const e = String(i ?? "").trim();
  if (!e) return null;
  if (typeof fromUuidSync == "function")
    try {
      const s = fromUuidSync(e);
      if (s) return (s == null ? void 0 : s.actor) ?? (s == null ? void 0 : s.baseActor) ?? s;
    } catch {
    }
  const t = e.startsWith("Actor.") ? e.slice(6).split(".")[0] : e;
  return ((a = (n = game == null ? void 0 : game.actors) == null ? void 0 : n.get) == null ? void 0 : a.call(n, t)) ?? (((r = game == null ? void 0 : game.actors) == null ? void 0 : r.contents) ?? []).find((s) => jl(s).has(e)) ?? null;
}
function og(i = null) {
  var t, n, a, r, s, o, l, c, u, d, m;
  if (!aa(i)) return null;
  const e = String(
    ((n = (t = i == null ? void 0 : i.system) == null ? void 0 : t.pilot) == null ? void 0 : n.uuid) ?? ((s = (r = (a = i == null ? void 0 : i.system) == null ? void 0 : a.mwd) == null ? void 0 : r.pilot) == null ? void 0 : s.uuid) ?? ((c = (l = (o = i == null ? void 0 : i.system) == null ? void 0 : o.mwd) == null ? void 0 : l.crew) == null ? void 0 : c.operatorActorUuid) ?? ((m = (d = (u = i == null ? void 0 : i.system) == null ? void 0 : u.mwd) == null ? void 0 : d.crew) == null ? void 0 : m.pilotActorUuid) ?? ""
  ).trim();
  return mT(e);
}
function xd(i = null, e = null) {
  var r, s, o, l, c, u, d, m, f, p, g;
  if (!aa(i) || !e) return !1;
  const t = String(
    ((s = (r = i == null ? void 0 : i.system) == null ? void 0 : r.pilot) == null ? void 0 : s.uuid) ?? ((c = (l = (o = i == null ? void 0 : i.system) == null ? void 0 : o.mwd) == null ? void 0 : l.pilot) == null ? void 0 : c.uuid) ?? ((m = (d = (u = i == null ? void 0 : i.system) == null ? void 0 : u.mwd) == null ? void 0 : d.crew) == null ? void 0 : m.operatorActorUuid) ?? ((g = (p = (f = i == null ? void 0 : i.system) == null ? void 0 : f.mwd) == null ? void 0 : p.crew) == null ? void 0 : g.pilotActorUuid) ?? ""
  ).trim(), n = jl(e);
  if (n.has(t)) return !0;
  const a = og(i);
  if (!a) return !1;
  for (const h of jl(a))
    if (n.has(h)) return !0;
  return !1;
}
const Ca = class Ca {
  static init() {
    var e;
    Hooks.on("updateCombat", (t, n) => this._onUpdateCombat(t, n)), Hooks.on("updateCombatant", (t, n) => this._onUpdateCombatant(t, n)), Hooks.on("updateToken", (t, n) => this._onUpdateToken(t, n)), Hooks.on("refreshToken", (t) => this._onRefreshToken(t)), Hooks.on("createCombatant", (t) => this._onCreateCombatant(t)), Hooks.on("deleteCombatant", (t) => this._onDeleteCombatant(t)), Hooks.on("deleteCombat", (t) => this._onDeleteCombat(t)), Hooks.on("createRegion", (t) => this._onCreateRegion(t)), Hooks.on("updateRegion", (t) => this._onUpdateRegion(t)), Hooks.on("deleteRegion", (t) => this._onDeleteRegion(t)), Hooks.on("targetToken", (t, n, a) => this._onTargetToken(t, n, a));
    for (const t of ["TOKEN_ENTER", "TOKEN_EXIT", "TOKEN_MOVE_IN", "TOKEN_MOVE_OUT"]) {
      const n = (e = CONST == null ? void 0 : CONST.REGION_EVENTS) == null ? void 0 : e[t];
      n && Hooks.on(n, (...a) => this._onRegionTokenEvent(...a));
    }
  }
  static async onReady() {
    var e;
    await this.ensureCurrentCombatantState(), await this.syncPreparedIndicators(), await this._syncAllSceneHazards(), await this._processCurrentCombatantMachineCrits(), (e = game.combat) != null && e.id && this._lastActivationByCombat.set(
      game.combat.id,
      this.getActivationIdentity(game.combat, game.combat.combatant)
    ), this.renderOpenActorSheets();
  }
  static _asTokenDocument(e) {
    return e ? (e == null ? void 0 : e.document) ?? e : null;
  }
  static _getTokenSceneId(e) {
    var n, a, r, s;
    const t = this._asTokenDocument(e);
    return ((n = t == null ? void 0 : t.parent) == null ? void 0 : n.id) ?? ((a = t == null ? void 0 : t.scene) == null ? void 0 : a.id) ?? ((s = (r = t == null ? void 0 : t.object) == null ? void 0 : r.scene) == null ? void 0 : s.id) ?? null;
  }
  static _getSceneTokenDocumentById(e, t = ((n) => (n = canvas == null ? void 0 : canvas.scene) == null ? void 0 : n.id)()) {
    var s, o, l, c, u;
    const a = String(e ?? "").trim();
    if (!a || !t) return null;
    const r = ((s = canvas == null ? void 0 : canvas.scene) == null ? void 0 : s.id) === t ? canvas.scene : (l = (o = game.scenes) == null ? void 0 : o.get) == null ? void 0 : l.call(o, t);
    return ((u = (c = r == null ? void 0 : r.tokens) == null ? void 0 : c.get) == null ? void 0 : u.call(c, a)) ?? null;
  }
  static _getCombatantTokenDocument(e, t = ((n) => (n = canvas == null ? void 0 : canvas.scene) == null ? void 0 : n.id)()) {
    const a = this._asTokenDocument(e == null ? void 0 : e.token);
    return a && typeof a == "object" ? a : this._getSceneTokenDocumentById(this._getCombatantTokenId(e), t);
  }
  static _getCombatantTokenId(e) {
    var t, n, a, r, s;
    return String(
      (e == null ? void 0 : e.tokenId) ?? ((t = e == null ? void 0 : e.token) == null ? void 0 : t.id) ?? ((n = e == null ? void 0 : e.token) == null ? void 0 : n._id) ?? ((r = (a = e == null ? void 0 : e.token) == null ? void 0 : a.document) == null ? void 0 : r.id) ?? ((s = e == null ? void 0 : e._source) == null ? void 0 : s.tokenId) ?? ""
    ).trim();
  }
  static _getCombatantActorId(e) {
    var n, a, r, s;
    const t = this._asTokenDocument(e == null ? void 0 : e.token);
    return String(
      (e == null ? void 0 : e.actorId) ?? ((n = e == null ? void 0 : e.actor) == null ? void 0 : n.id) ?? ((a = e == null ? void 0 : e._source) == null ? void 0 : a.actorId) ?? (t == null ? void 0 : t.actorId) ?? ((r = t == null ? void 0 : t.actor) == null ? void 0 : r.id) ?? ((s = t == null ? void 0 : t.baseActor) == null ? void 0 : s.id) ?? ""
    ).trim();
  }
  static _getCombatants(e) {
    return e != null && e.combatants ? typeof e.combatants.values == "function" ? Array.from(e.combatants.values()) : Array.from(e.combatants ?? []) : [];
  }
  static _getCombatantActor(e, t = ((n) => (n = canvas == null ? void 0 : canvas.scene) == null ? void 0 : n.id)()) {
    const a = this._getCombatantTokenDocument(e, t);
    return (a == null ? void 0 : a.actor) ?? (e == null ? void 0 : e.actor) ?? null;
  }
  static getActionEconomyActorForCombatant(e, t = ((n) => (n = canvas == null ? void 0 : canvas.scene) == null ? void 0 : n.id)()) {
    const a = this._getCombatantActor(e, t);
    return aa(a) ? og(a) ?? a : a;
  }
  static rememberActivation(e, t) {
    !(e != null && e.id) || !(t != null && t.id) || this._lastActivationByCombat.set(e.id, this.getActivationIdentity(e, t));
  }
  static _getCombatSceneId(e) {
    var t, n, a;
    return String(
      ((t = e == null ? void 0 : e.scene) == null ? void 0 : t.id) ?? (e == null ? void 0 : e.sceneId) ?? ((n = e == null ? void 0 : e._source) == null ? void 0 : n.scene) ?? ((a = e == null ? void 0 : e._source) == null ? void 0 : a.sceneId) ?? ""
    ).trim();
  }
  static _getCombatantSceneId(e) {
    var n, a, r, s, o;
    const t = this._asTokenDocument(e == null ? void 0 : e.token);
    return String(
      (e == null ? void 0 : e.sceneId) ?? ((n = e == null ? void 0 : e._source) == null ? void 0 : n.sceneId) ?? ((a = t == null ? void 0 : t.parent) == null ? void 0 : a.id) ?? ((r = t == null ? void 0 : t.scene) == null ? void 0 : r.id) ?? ((o = (s = t == null ? void 0 : t.object) == null ? void 0 : s.scene) == null ? void 0 : o.id) ?? ""
    ).trim();
  }
  static _findCombatantForToken(e, t = null, n = ((a) => (a = canvas == null ? void 0 : canvas.scene) == null ? void 0 : a.id)()) {
    var l, c, u;
    const r = this._asTokenDocument(t), s = String((r == null ? void 0 : r.id) ?? "").trim();
    if (!e || !s) return null;
    if (((c = (l = r == null ? void 0 : r.combatant) == null ? void 0 : l.combat) == null ? void 0 : c.id) === e.id) return r.combatant;
    let o = null;
    if (typeof e.getCombatantsByToken == "function")
      try {
        o = ((u = e.getCombatantsByToken(s)) == null ? void 0 : u[0]) ?? null;
      } catch {
        o = null;
      }
    else if (typeof e.getCombatantByToken == "function")
      try {
        o = e.getCombatantByToken(s) ?? null;
      } catch {
        o = null;
      }
    return o || (this._getCombatants(e).find((d) => {
      const m = this._getCombatantTokenDocument(d, n), f = this._getCombatantTokenId(d) || String((m == null ? void 0 : m.id) ?? "").trim(), p = this._getCombatantSceneId(d) || n;
      return f === s && (!n || !p || p === n);
    }) ?? null);
  }
  static _collectActorIds(e, t = null) {
    var s, o;
    const n = /* @__PURE__ */ new Set(), a = (l) => {
      const c = String(l ?? "").trim();
      c && n.add(c);
    };
    a(e == null ? void 0 : e.id), a(e == null ? void 0 : e._id);
    const r = this._asTokenDocument(t) ?? this._asTokenDocument(e == null ? void 0 : e.token);
    return a((s = r == null ? void 0 : r.actor) == null ? void 0 : s.id), a((o = r == null ? void 0 : r.baseActor) == null ? void 0 : o.id), a(r == null ? void 0 : r.actorId), n;
  }
  static _tokenDocumentMatchesActor(e, t, n = null) {
    var s, o;
    const a = this._asTokenDocument(e);
    if (!a || !t) return !1;
    const r = n ?? this._collectActorIds(t, a);
    return [
      (s = a == null ? void 0 : a.actor) == null ? void 0 : s.id,
      (o = a == null ? void 0 : a.baseActor) == null ? void 0 : o.id,
      a == null ? void 0 : a.actorId
    ].some((l) => r.has(String(l ?? "").trim()));
  }
  static getPreferredTokenDocument(e) {
    var a, r;
    if (!e) return null;
    const t = this._asTokenDocument(e == null ? void 0 : e.token);
    return t || (((r = (((a = e.getActiveTokens) == null ? void 0 : a.call(e, !0, !0)) ?? [])[0]) == null ? void 0 : r.document) ?? null);
  }
  static getPreferredToken(e) {
    const t = this.getPreferredTokenDocument(e);
    return t ? t.object ?? this._getSceneTokenById(t.id) : null;
  }
  static getCurrentSceneTokenDocument(e, t = null) {
    var p, g, h, y;
    const n = (p = canvas == null ? void 0 : canvas.scene) == null ? void 0 : p.id, a = this._asTokenDocument(t);
    if (this._getTokenSceneId(a) === n) return a;
    const r = String((a == null ? void 0 : a.id) ?? (t == null ? void 0 : t.id) ?? "").trim();
    if (r) {
      const b = this._getSceneTokenDocumentById(r, n);
      if (b) return b;
    }
    const s = this.getPreferredTokenDocument(e);
    if (this._getTokenSceneId(s) === n) return s;
    const o = String((s == null ? void 0 : s.id) ?? "").trim();
    if (o) {
      const b = this._getSceneTokenDocumentById(o, n);
      if (b) return b;
    }
    const c = ((h = (((g = e == null ? void 0 : e.getActiveTokens) == null ? void 0 : g.call(e, !0, !0)) ?? []).find((b) => {
      var S, w;
      return ((w = (S = b == null ? void 0 : b.document) == null ? void 0 : S.parent) == null ? void 0 : w.id) === n;
    })) == null ? void 0 : h.document) ?? null;
    if (c) return c;
    const u = Array.from(((y = canvas == null ? void 0 : canvas.scene) == null ? void 0 : y.tokens) ?? []), d = this._collectActorIds(e, s), m = u.filter((b) => this._tokenDocumentMatchesActor(b, e, d));
    return m.find((b) => {
      var S, w, T;
      return ((S = b == null ? void 0 : b.combatant) == null ? void 0 : S.id) === ((T = (w = game.combat) == null ? void 0 : w.combatant) == null ? void 0 : T.id);
    }) ?? null ?? m[0] ?? null;
  }
  static getCurrentSceneToken(e, t = null) {
    const n = this.getCurrentSceneTokenDocument(e, t);
    return n ? n.object ?? this._getSceneTokenById(n.id) : null;
  }
  static _getSceneTokenById(e) {
    var t, n, a, r;
    return e ? ((n = (t = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : t.get) == null ? void 0 : n.call(t, e)) ?? ((r = (a = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : a.placeables) == null ? void 0 : r.find((s) => s.id === e)) ?? null : null;
  }
  static _measureTokenDistance(e, t) {
    var o, l;
    const n = canvas == null ? void 0 : canvas.grid, a = _d(e), r = _d(t);
    if (!n || !a || !r) return null;
    if (typeof n.measurePath == "function")
      try {
        const c = n.measurePath([a, r], { gridSpaces: !0 }), u = Number(
          (c == null ? void 0 : c.distance) ?? (c == null ? void 0 : c.cost) ?? (c == null ? void 0 : c.totalDistance) ?? (c == null ? void 0 : c.totalCost) ?? NaN
        );
        if (Number.isFinite(u)) return u;
      } catch {
      }
    const s = ((l = (o = foundry == null ? void 0 : foundry.canvas) == null ? void 0 : o.geometry) == null ? void 0 : l.Ray) ?? globalThis.Ray;
    if (typeof n.measureDistances == "function" && typeof s == "function")
      try {
        const c = n.measureDistances([{ ray: new s(a, r) }], { gridSpaces: !0 }), u = Number(Array.isArray(c) ? c[0] : NaN);
        if (Number.isFinite(u)) return u;
      } catch {
        return null;
      }
    return null;
  }
  static getUserTargetTokens(e = game.user) {
    var a;
    const n = (Array.isArray((a = e == null ? void 0 : e.targets) == null ? void 0 : a.ids) ? e.targets.ids : []).map((r) => this._getSceneTokenById(r)).filter(Boolean);
    return n.length ? n : Array.from((e == null ? void 0 : e.targets) ?? []).map((r) => (r == null ? void 0 : r.object) ?? r).filter(Boolean);
  }
  static getTargetingSnapshot(e = null, t = game.user) {
    var u, d, m, f, p, g, h, y;
    const n = this.getUserTargetTokens(t), a = n.length;
    if (a === 0)
      return {
        count: 0,
        none: !0,
        single: !1,
        multiple: !1,
        heading: "Target",
        primaryLabel: "No target selected",
        detailRows: [],
        target: null
      };
    if (a > 1)
      return {
        count: a,
        none: !1,
        single: !1,
        multiple: !0,
        heading: "Targets",
        primaryLabel: `${a} selected`,
        detailRows: [],
        target: null
      };
    const r = n[0], s = this._measureTokenDistance(e, r), o = String(((d = (u = canvas == null ? void 0 : canvas.scene) == null ? void 0 : u.grid) == null ? void 0 : d.units) ?? ((f = (m = game.system) == null ? void 0 : m.grid) == null ? void 0 : f.units) ?? "").trim(), l = uT(s, o), c = String((r == null ? void 0 : r.name) ?? ((p = r == null ? void 0 : r.actor) == null ? void 0 : p.name) ?? "Target").trim() || "Target";
    return {
      count: a,
      none: !1,
      single: !0,
      multiple: !1,
      heading: "Target",
      primaryLabel: c,
      detailRows: l ? [{ label: "Distance", value: l }] : [],
      target: {
        id: (r == null ? void 0 : r.id) ?? null,
        name: c,
        img: ((h = (g = r == null ? void 0 : r.document) == null ? void 0 : g.texture) == null ? void 0 : h.src) ?? ((y = r == null ? void 0 : r.texture) == null ? void 0 : y.src) ?? "",
        distance: Number.isFinite(s) ? s : null,
        distanceLabel: l
      }
    };
  }
  static getRollImpact(e = []) {
    const t = (Array.isArray(e) ? e : []).map((a) => {
      const r = cT((a == null ? void 0 : a.numericValue) ?? (a == null ? void 0 : a.value) ?? 0);
      return {
        label: String((a == null ? void 0 : a.label) ?? "").trim() || "Modifier",
        numericValue: r,
        value: String((a == null ? void 0 : a.value) ?? ba(r)).trim() || ba(r)
      };
    }), n = t.reduce((a, r) => a + r.numericValue, 0);
    return {
      total: n,
      totalLabel: ba(n),
      entries: t
    };
  }
  static getCombat(e, t = null) {
    var g;
    const n = (g = canvas == null ? void 0 : canvas.scene) == null ? void 0 : g.id, a = game.combat, r = this._getCombatSceneId(a), o = !!this._asTokenDocument(t), l = this.getCurrentSceneTokenDocument(e, t), c = (l == null ? void 0 : l.object) ?? this._getSceneTokenById((l == null ? void 0 : l.id) ?? null);
    if (!a || r && n && r !== n)
      return {
        combat: null,
        combatant: null,
        token: c,
        tokenDocument: l
      };
    let u = this._findCombatantForToken(a, l, n);
    if (!o) {
      const h = (a == null ? void 0 : a.combatant) ?? null, y = this._getCombatantActor(h, n);
      xd(y, e) && (u = h);
    }
    const d = this._getCombatants(a);
    if (!u) {
      const h = this._collectActorIds(e, l), y = String((l == null ? void 0 : l.id) ?? "").trim(), b = d.filter((T) => {
        const k = this._getCombatantTokenId(T), C = this._getCombatantTokenDocument(T, n), I = k || String((C == null ? void 0 : C.id) ?? "").trim();
        if (o && y) return I === y;
        if (h.has(this._getCombatantActorId(T))) return !0;
        const O = (C == null ? void 0 : C.actor) ?? (T == null ? void 0 : T.actor) ?? null;
        return this._tokenDocumentMatchesActor(C, e, h) || !o && xd(O, e);
      }), S = b.find((T) => {
        var k;
        return T.id === ((k = a == null ? void 0 : a.combatant) == null ? void 0 : k.id);
      }) ?? null;
      u = b.find(
        (T) => {
          var k;
          return y && (this._getCombatantTokenId(T) || String(((k = this._getCombatantTokenDocument(T, n)) == null ? void 0 : k.id) ?? "").trim()) === y;
        }
      ) ?? null ?? S ?? b[0] ?? null;
    }
    const m = this._getCombatantTokenDocument(u, n), f = l ?? m ?? null, p = c ?? (m == null ? void 0 : m.object) ?? this._getSceneTokenById(this._getCombatantTokenId(u)) ?? null;
    return {
      combat: a,
      combatant: u,
      token: p,
      tokenDocument: f
    };
  }
  static getSnapshot(e, { token: t = null } = {}) {
    var C, I, O, x, B;
    const {
      combat: n,
      combatant: a,
      token: r,
      tokenDocument: s
    } = this.getCombat(e, t), o = !!a && ((C = n == null ? void 0 : n.combatant) == null ? void 0 : C.id) === a.id, l = a ? this.getActivationIdentity(n, a) : null;
    o && (n != null && n.id) && !this._lastActivationByCombat.has(n.id) && this.rememberActivation(n, a);
    const c = a ? a.getFlag(Xt, pi) : null, u = a ? o ? Xr(c, l) ? Ul(c, l) : Od(c, l) : Ul(c, l) : Cu(l);
    u.actionLog = gr(u.actionLog);
    const d = Math.max(0, Number(((O = (I = e == null ? void 0 : e.system) == null ? void 0 : I.burn) == null ? void 0 : O.value) ?? 0)), m = Math.floor(d / 2), f = !!((B = (x = e == null ? void 0 : e.system) == null ? void 0 : x.burn) != null && B.overloaded), p = Qa(u), g = this.getActiveStatuses(e), h = g.filter(
      (P) => !(f && P.id === "overloaded") && P.id !== jn
    ), y = this.getModifierSummary(e, m), b = this.getRollImpact(y), S = Math.max(0, Number(u.burnThisActivation ?? 0)), w = a ? o ? "" : "Waiting for this combatant's activation." : "No combatant on the current scene.", T = [];
    f && T.push({ id: "overloaded", label: "Overloaded" }), p && T.push({
      id: "preparedInterrupt",
      label: "Prepared",
      hint: oT(p)
    });
    const k = Object.entries(u.hazards ?? {});
    if (k.length) {
      const P = k.map(([, L]) => L).sort((L, D) => Cn(D == null ? void 0 : D.tier) - Cn(L == null ? void 0 : L.tier))[0] ?? null;
      P && T.push({
        id: "hazard",
        label: `Hazard ${bi(P.tier)}`,
        hint: `${k.length} active hazard${k.length === 1 ? "" : "s"}`
      });
    }
    return {
      token: r,
      tokenDocument: s,
      combat: n,
      combatant: a,
      hasCombatant: !!a,
      isCurrentTurn: o,
      overloaded: f,
      burn: {
        value: d,
        penalty: m,
        canOverloadCheck: d >= 6 && !f
      },
      state: u,
      hazards: u.hazards ?? {},
      pendingReaction: u.pendingReaction ?? null,
      preparedInterrupt: p,
      targeting: this.getTargetingSnapshot(r),
      states: T,
      effects: h,
      statuses: g,
      rollImpact: b,
      summaryText: `SA: ${u.saRemaining} / ${yn}   FA: ${u.faRemaining}   RA: ${u.raRemaining}`,
      activation: {
        burnThisActivation: S,
        burnThisActivationLabel: `+${S}`,
        items: [
          { label: "SA", value: `${u.saRemaining}/${yn}` },
          { label: "FA", value: String(u.faRemaining) },
          { label: "RA", value: String(u.raRemaining) },
          { label: "Burn", value: `+${S}`, detail: "this activation" }
        ]
      },
      inactiveReason: w,
      modifierSummary: y
    };
  }
  static getAvailableReactionEdgePools(e) {
    var t, n;
    return (t = e == null ? void 0 : e.hasEdgePools) != null && t.call(e) ? (((n = e.getEdgePoolSummary) == null ? void 0 : n.call(e).pools) ?? []).filter((a) => Number((a == null ? void 0 : a.effectiveValue) ?? 0) > 0).map((a) => ({
      key: String(a.key ?? "").trim(),
      label: String(a.key ?? "").trim(),
      value: Number(a.effectiveValue ?? 0)
    })).filter((a) => a.key) : [];
  }
  static getReactionSpendPreview(e, { token: t = null, edgePoolKey: n = "" } = {}) {
    var u;
    const a = this.getSnapshot(e, { token: t }), r = Number(((u = a.state) == null ? void 0 : u.raRemaining) ?? 0) > 0, s = this.getAvailableReactionEdgePools(e), o = String(n ?? "").trim(), l = !r && s.some((d) => d.key === o);
    return {
      snapshot: a,
      usesReaction: r,
      burnDelta: r || l ? 0 : 2,
      canSpendEdge: !r && s.length > 0,
      edgePools: s,
      edgePoolKey: l ? o : null,
      costLabel: r ? "1 RA" : l ? `1 Edge (${o})` : "+2 Burn"
    };
  }
  static async commitReactionSpend(e, {
    token: t = null,
    actionId: n = "",
    actionLabel: a = "",
    actionCategory: r = tt.reaction,
    logLabel: s = "",
    edgePoolKey: o = "",
    allowCurrentTurn: l = !1
  } = {}) {
    var g, h, y, b;
    const c = this.getReactionSpendPreview(e, { token: t, edgePoolKey: o }), u = c.snapshot;
    if (!u.hasCombatant) return { ok: !1, reason: "No combatant on the current scene." };
    if (!l && u.isCurrentTurn) return { ok: !1, reason: "Only outside your activation." };
    const d = Zr(u), m = {
      combat: u.combat,
      combatant: u.combatant,
      state: d,
      sceneId: ((g = canvas == null ? void 0 : canvas.scene) == null ? void 0 : g.id) ?? "",
      snapshot: { ...u, state: d }
    };
    let f = 0, p = null;
    if (c.usesReaction)
      d.raRemaining = Math.max(0, Number(d.raRemaining ?? 0) - 1);
    else {
      const S = c.edgePoolKey ? 0 : 2, w = hi({
        actor: e,
        phase: "onBeforeBurnApplied",
        facts: fs({
          actor: e,
          packet: {
            actionId: n,
            category: r,
            resource: "reaction",
            amount: S,
            source: "reaction"
          },
          runtime: m
        }),
        packet: {
          actionId: n,
          category: r,
          resource: "reaction",
          amount: S,
          source: "reaction"
        },
        options: { runtime: m, consumeUsage: !0 }
      });
      m.pendingMutations = (m.pendingMutations ?? []).concat(w.mutations), f = Math.max(0, Number(w.packet.amount ?? S) || 0), c.edgePoolKey ? (await e.spendEdge(c.edgePoolKey, 1, { source: "reactionBurnCancel" }), p = c.edgePoolKey) : f > 0 && (d.reactionBurnSinceLastActivation = Math.max(
        0,
        Number(d.reactionBurnSinceLastActivation ?? 0) + f
      ));
    }
    return this._appendActionLog(d, {
      id: n,
      label: s || a,
      costLabel: c.costLabel
    }), (h = m.pendingMutations) != null && h.length ? await rn({ actor: e, mutations: m.pendingMutations, runtime: m }) : await u.combatant.setFlag(Xt, pi, d), f > 0 && await e.update({ "system.burn.value": Math.max(0, Number(((b = (y = e.system) == null ? void 0 : y.burn) == null ? void 0 : b.value) ?? 0) + f) }), {
      ok: !0,
      snapshot: this.getSnapshot(e, { token: t }),
      costLabel: c.costLabel,
      burnDelta: f,
      spentEdgePoolKey: p,
      usedReaction: c.usesReaction
    };
  }
  static async updateCombatantState(e, { token: t = null, mutate: n = null } = {}) {
    const a = this.getSnapshot(e, { token: t });
    if (!(a != null && a.combatant)) return { ok: !1, reason: "No combatant on the current scene." };
    const r = Zr(a), s = typeof n == "function" ? n(r, a) ?? r : r;
    return await a.combatant.setFlag(Xt, pi, s), { ok: !0, snapshot: this.getSnapshot(e, { token: t }) };
  }
  static getUsedWeaponGroupIds(e, { token: t = null, snapshot: n = null, currentTurnOnly: a = !0 } = {}) {
    const r = n ?? this.getSnapshot(e, { token: t });
    return r != null && r.hasCombatant ? a && !r.isCurrentTurn ? [] : sg(r) : [];
  }
  static async markWeaponGroupUsed(e, { token: t = null, groupId: n = "" } = {}) {
    const a = String(n ?? "").trim();
    return a ? this.updateCombatantState(e, {
      token: t,
      mutate: (r) => Qw(r, a)
    }) : { ok: !1, reason: "Weapon group id is required." };
  }
  static async setPendingReaction(e, { token: t = null, pendingReaction: n = null } = {}) {
    return this.updateCombatantState(e, {
      token: t,
      mutate: (a) => (a.pendingReaction = ys(n), a)
    });
  }
  static async clearPendingReaction(e, { token: t = null } = {}) {
    return this.setPendingReaction(e, { token: t, pendingReaction: null });
  }
  static async setHazardState(e, { token: t = null, regionId: n = "", hazardState: a = null } = {}) {
    const r = String(n ?? "").trim();
    return r ? this.updateCombatantState(e, {
      token: t,
      mutate: (s) => (s.hazards ?? (s.hazards = {}), a ? s.hazards[r] = Ks({ [r]: a })[r] : delete s.hazards[r], s)
    }) : { ok: !1, reason: "Hazard region id is required." };
  }
  static getModifierSummary(e, t = Math.floor(Number(((a) => (a = ((n) => (n = e == null ? void 0 : e.system) == null ? void 0 : n.burn)()) == null ? void 0 : a.value)() ?? 0) / 2)) {
    var c, u;
    const r = ((u = (c = e == null ? void 0 : e.system) == null ? void 0 : c.derived) == null ? void 0 : u.condition) ?? {}, s = [];
    t > 0 && s.push({
      label: "Burn Penalty",
      numericValue: -t,
      value: ba(-t)
    });
    const o = Number(r.fatiguePenalty ?? 0);
    o && s.push({
      label: "Fatigue",
      numericValue: o,
      value: ba(o)
    });
    const l = Number(r.physicalPenalty ?? 0);
    return l && s.push({
      label: "Physical",
      numericValue: l,
      value: ba(l)
    }), s.length || s.push({
      label: "Current Modifiers",
      numericValue: 0,
      value: "0"
    }), s;
  }
  static getActiveStatuses(e) {
    return Array.from((e == null ? void 0 : e.statuses) ?? []).map((n) => ({
      id: n,
      label: lT(n)
    })).sort((n, a) => n.label.localeCompare(a.label));
  }
  static buildActionModel(e, t) {
    var o, l, c, u;
    const n = (d) => {
      const m = nu(d), f = dS(d);
      return !f || !m ? null : {
        id: d,
        label: m.label,
        handler: "roll",
        roll: JSON.stringify(f),
        disabled: !1,
        reason: ""
      };
    }, a = (d) => {
      const m = BS(d).filter((f) => f.id !== "overloadCheck").filter((f) => !(d === tt.recovery && f.id === "reduceBurn"));
      if (d === tt.standard) {
        const f = ps("reduceBurn");
        f && !m.some((p) => p.id === "reduceBurn") && m.push(f);
      }
      return m.map((f) => this._buildCatalogAction(e, t, f));
    }, r = (d) => {
      const m = ps(d);
      if (!m) return null;
      const f = this._buildCatalogAction(e, t, m);
      return f.disabled ? null : f;
    }, s = (o = t.burn) != null && o.canOverloadCheck ? r("overloadCheck") : null;
    return {
      utilityButtons: [
        {
          id: "initiative",
          label: "Initiative",
          handler: "roll",
          roll: JSON.stringify({ intent: "initiative" }),
          disabled: !1,
          reason: ""
        },
        {
          id: "statuses",
          label: "Statuses",
          handler: "toggleStatuses",
          disabled: !1,
          reason: t.token ? "" : "Requires a token on the current scene."
        },
        n("composure"),
        n("judgeIntent"),
        n("memory"),
        n("lift"),
        n("endure")
      ].filter(Boolean),
      summaryPills: [
        { label: "SA", value: `${t.state.saRemaining}/${yn}` },
        { label: "Cap", value: `${Math.max(0, Number(((l = t.state) == null ? void 0 : l.saSpentThisActivation) ?? 0))}/${Kl(e)}` },
        { label: "FA", value: `${t.state.faRemaining}` },
        { label: "RA", value: `${t.state.raRemaining}` },
        {
          label: "Burn/Turn",
          value: `+${Math.max(0, Number(((c = t.state) == null ? void 0 : c.burnThisActivation) ?? 0))}`,
          action: s
        }
      ],
      activationLog: gr((u = t.state) == null ? void 0 : u.actionLog).map((d, m) => ({
        ...d,
        index: m + 1
      })),
      menus: [
        {
          id: "standard",
          label: "Standard Actions",
          actions: a(tt.standard)
        },
        {
          id: "complex",
          label: "Complex Actions",
          actions: a(tt.complex)
        },
        {
          id: "free",
          label: "Free Actions",
          actions: a(tt.free)
        },
        {
          id: "reaction",
          label: "Reactions",
          actions: a(tt.reaction)
        },
        {
          id: "burn",
          label: "Burn & Recovery",
          actions: a(tt.recovery)
        }
      ].filter((d) => d.actions.length)
    };
  }
  static _buildCatalogAction(e, t, n) {
    const a = t.hasCombatant ? "" : "No current-scene combatant.", r = t.isCurrentTurn ? "" : "Only during your activation.", s = t.overloaded ? "Overloaded actors can only recover Burn." : "", o = es(e, t), l = t.state ?? {}, c = n.category;
    let u = "sa", d = Number(n.cost ?? 0) || 0, m = rT(u, d), f = "";
    if (n.id === "reduceBurn")
      u = "sa", d = 1, m = "1 SA", f = a || r || (o <= 0 ? "Activation SA cap reached." : "") || (t.burn.value <= 0 ? "Burn is already at 0." : "");
    else if (n.id === "overloadCheck")
      u = "check", d = 0, m = "Check", f = a || r || (t.burn.canOverloadCheck ? "" : t.overloaded ? "Already Overloaded." : "Burn below 6.");
    else if (n.id === "interrupt") {
      const p = Qa(l);
      u = Number(l.raRemaining ?? 0) > 0 ? "ra" : "burn", d = u === "ra" ? 1 : 2, m = u === "ra" ? "1 RA" : "+2 Burn", f = a || (t.isCurrentTurn ? "Only outside your activation." : "") || (p ? "" : "Prepare an interrupt first.");
    } else if (n.id === "evade") {
      const p = ys(l.pendingReaction), g = t.isCurrentTurn && !(p != null && p.allowCurrentTurn) ? "Only outside your activation." : "";
      u = Number(l.raRemaining ?? 0) > 0 ? "ra" : "burn", d = u === "ra" ? 1 : 2, m = u === "ra" ? "1 RA" : "+2 Burn", f = a || g || (p ? "" : "Use an area effect or hazard card to trigger Evade.");
    } else if (c === tt.standard)
      f = a || r || s || (o < d ? "Activation SA cap reached." : "");
    else if (c === tt.complex)
      f = a || r || s || (o < d ? "Activation SA cap reached." : "");
    else if (c === tt.free) {
      const p = Number(l.faRemaining ?? 0) > 0;
      u = p ? "fa" : "sa", d = 1, m = p ? "Free" : "1 SA", f = a || r || !p && s || (!p && o < 1 ? "Activation SA cap reached." : "");
    } else if (c === tt.reaction) {
      const p = Number(l.raRemaining ?? 0) > 0;
      u = p ? "ra" : "burn", d = p ? 1 : 2, m = p ? "1 RA" : "+2 Burn", f = a || (t.isCurrentTurn ? "Only outside your activation." : "");
    } else c === tt.recovery && (f = a || r);
    return n.handler || (f = n.reason || "Not yet implemented."), {
      id: n.id,
      label: n.label,
      category: c,
      handler: n.handler,
      description: String(n.description ?? "").trim(),
      resource: u,
      cost: d,
      costLabel: m,
      disabled: !!f,
      reason: f,
      roll: n.roll ? JSON.stringify(n.roll) : "",
      prominent: !!(n.prominent || n.prominentWhenBurning && t.burn.value >= 6)
    };
  }
  static async executeAction(e, { token: t = null, actionId: n = "", metadata: a = {} } = {}) {
    const r = ps(n);
    return r ? r.handler ? r.category === tt.standard ? this._executeStandardAction(e, { token: t, action: r, metadata: a }) : r.category === tt.free ? this._executeFreeAction(e, { token: t, action: r, metadata: a }) : r.category === tt.reaction ? this._executeReactionAction(e, { token: t, action: r, metadata: a }) : { ok: !1, reason: r.reason || "That action is not implemented yet." } : { ok: !1, reason: r.reason || "That action is not implemented yet." } : { ok: !1, reason: "Unknown combat action." };
  }
  static async _executeStandardAction(e, { token: t = null, action: n, metadata: a = {} } = {}) {
    const r = this.getSnapshot(e, { token: t });
    if (!r.hasCombatant) return { ok: !1, reason: "No combatant on the current scene." };
    if (!r.isCurrentTurn) return { ok: !1, reason: "Only available during your activation." };
    if (r.overloaded) return { ok: !1, reason: "Overloaded actors can only recover Burn." };
    if (es(e, r) < Number(n.cost ?? 1))
      return { ok: !1, reason: "Activation SA cap reached." };
    const s = await this.spendResource(e, {
      token: t,
      resource: "sa",
      cost: Number(n.cost ?? 1) || 1,
      actionId: n.id,
      actionLabel: n.label,
      actionCostLabel: `${Number(n.cost ?? 1) || 1} SA`,
      actionCategory: n.category
    });
    return s != null && s.ok ? (await this._applyActionState(e, {
      token: t,
      actionId: n.id,
      metadata: a,
      snapshot: s.snapshot
    }), { ok: !0, snapshot: this.getSnapshot(e, { token: t }) }) : s;
  }
  static async _executeFreeAction(e, { token: t = null, action: n, metadata: a = {} } = {}) {
    var l;
    const r = this.getSnapshot(e, { token: t });
    if (!r.hasCombatant) return { ok: !1, reason: "No combatant on the current scene." };
    if (!r.isCurrentTurn) return { ok: !1, reason: "Only available during your activation." };
    const s = Number(((l = r.state) == null ? void 0 : l.faRemaining) ?? 0) > 0;
    if (!s && r.overloaded)
      return { ok: !1, reason: "Overloaded actors can only recover Burn." };
    if (!s && es(e, r) < 1)
      return { ok: !1, reason: "Activation SA cap reached." };
    const o = await this.spendResource(e, {
      token: t,
      resource: s ? "fa" : "sa",
      cost: 1,
      actionId: n.id,
      actionLabel: n.label,
      actionCostLabel: s ? "Free" : "1 SA",
      actionCategory: n.category
    });
    return o != null && o.ok ? (await this._applyActionState(e, {
      token: t,
      actionId: n.id,
      metadata: a,
      snapshot: o.snapshot
    }), { ok: !0, snapshot: this.getSnapshot(e, { token: t }) }) : o;
  }
  static async _promptSpendEdgeForReaction(e) {
    var o, l, c;
    if (!((o = e.hasEdgePools) != null && o.call(e))) return null;
    const n = Object.keys(((c = (l = e.system) == null ? void 0 : l.counters) == null ? void 0 : c.edgePools) ?? {}).map((u) => e.getEdgePool(u)).filter((u) => u.hasPools && u.effectiveValue > 0);
    if (!n.length) return null;
    const a = (u) => String(u).charAt(0).toUpperCase() + String(u).slice(1);
    let r;
    n.length === 1 ? r = `<input type="hidden" name="poolKey" value="${n[0].key}">
        <p>from <strong>${a(n[0].key)}</strong> (${n[0].effectiveValue} available)</p>` : r = n.map((u, d) => `
        <label style="display:block">
          <input type="radio" name="poolKey" value="${u.key}" ${d === 0 ? "checked" : ""}>
          ${a(u.key)} &mdash; ${u.effectiveValue} available
        </label>
      `).join("");
    const s = `<p>This reaction costs <strong>+2 Burn</strong>. Spend 1 Edge to ignore it?</p><form>${r}</form>`;
    return Dialog.confirm({
      title: "Reaction: Spend Edge?",
      content: s,
      yes: (u) => {
        const d = u.find("[name='poolKey']:checked, [name='poolKey'][type='hidden']").first().val();
        return String(d ?? n[0].key).trim() || n[0].key;
      },
      no: () => null,
      defaultYes: !1
    });
  }
  static async _executeReactionAction(e, { token: t = null, action: n, metadata: a = {} } = {}) {
    var d, m;
    const r = this.getSnapshot(e, { token: t });
    if (!r.hasCombatant) return { ok: !1, reason: "No combatant on the current scene." };
    const s = ys((d = r.state) == null ? void 0 : d.pendingReaction), o = n.id === "evade" && (s == null ? void 0 : s.allowCurrentTurn);
    if (r.isCurrentTurn && !o) return { ok: !1, reason: "Only outside your activation." };
    if (n.id === "interrupt" && !Qa(r.state))
      return { ok: !1, reason: "Prepare an interrupt first." };
    const l = n.id === "assist" && (a != null && a.targetName) ? `${n.label}: ${a.targetName}` : n.id === "interrupt" && (a != null && a.scope) ? `${n.label}: ${String(a.scope).trim()}` : n.label;
    let c = String((a == null ? void 0 : a.edgePoolKey) ?? "").trim();
    !c && Number(((m = r.state) == null ? void 0 : m.raRemaining) ?? 0) <= 0 && (c = await Ca._promptSpendEdgeForReaction(e) ?? "");
    const u = await this.commitReactionSpend(e, {
      token: t,
      actionId: n.id,
      actionLabel: n.label,
      actionCategory: n.category,
      logLabel: l,
      edgePoolKey: c,
      allowCurrentTurn: o
    });
    return u != null && u.ok ? { ...u, actionLabel: l } : u;
  }
  static async _applyActionState(e, { token: t = null, actionId: n = "", metadata: a = {}, snapshot: r = null } = {}) {
    const s = r ?? this.getSnapshot(e, { token: t });
    if (!(s != null && s.combatant)) return { ok: !1, reason: "No combatant on the current scene." };
    const o = sT(s.state, n, {
      snapshot: s,
      metadata: a
    });
    return await s.combatant.setFlag(Xt, pi, o), { ok: !0, snapshot: this.getSnapshot(e, { token: t }) };
  }
  static async clearAim(e, { token: t = null } = {}) {
    const n = this.getSnapshot(e, { token: t });
    if (!(n != null && n.combatant)) return { ok: !1, reason: "No combatant on the current scene." };
    const a = Zr(n);
    return a.actionState ?? (a.actionState = {}), a.actionState.aim ? (a.actionState.aim = null, await n.combatant.setFlag(Xt, pi, a), { ok: !0, snapshot: this.getSnapshot(e, { token: t }) }) : { ok: !0, snapshot: n };
  }
  static getPreparedInterrupt(e = null) {
    const t = (e == null ? void 0 : e.state) ?? e ?? {};
    return Qa(t);
  }
  static async clearPreparedInterrupt(e, { token: t = null } = {}) {
    const n = this.getSnapshot(e, { token: t });
    if (!(n != null && n.combatant)) return { ok: !1, reason: "No combatant on the current scene." };
    const a = Zr(n);
    return a.actionState ?? (a.actionState = {}), a.actionState.preparedInterrupt ? (a.actionState.preparedInterrupt = null, await n.combatant.setFlag(Xt, pi, a), { ok: !0, snapshot: this.getSnapshot(e, { token: t }) }) : { ok: !0, snapshot: n };
  }
  static async _syncPreparedIndicatorForCombatant(e) {
    var u, d, m;
    if (!game.user.isGM || !e) return;
    const t = this._getCombatantSceneId(e) || ((u = canvas == null ? void 0 : canvas.scene) == null ? void 0 : u.id), n = this._getCombatantTokenDocument(e, t), a = (n == null ? void 0 : n.actor) ?? (e == null ? void 0 : e.actor) ?? null;
    if (!n || !a) return;
    const r = e.getFlag(Xt, pi), s = !!Qa(r), o = Ld(), l = String((o == null ? void 0 : o.id) ?? jn).trim() || jn;
    (((m = (d = a == null ? void 0 : a.statuses) == null ? void 0 : d.has) == null ? void 0 : m.call(d, l)) ?? !1) !== s && await a.toggleStatusEffect(l, { active: s, overlay: !1 });
  }
  static async syncPreparedIndicators(e = game.combat) {
    if (!(!game.user.isGM || !e))
      for (const t of this._getCombatants(e))
        await this._syncPreparedIndicatorForCombatant(t);
  }
  static async clearPreparedIndicatorForCombatant(e) {
    var o, l, c;
    if (!game.user.isGM || !e) return;
    const t = this._getCombatantSceneId(e) || ((o = canvas == null ? void 0 : canvas.scene) == null ? void 0 : o.id), n = this._getCombatantTokenDocument(e, t), a = (n == null ? void 0 : n.actor) ?? (e == null ? void 0 : e.actor) ?? null;
    if (!n || !a) return;
    const r = Ld(), s = String((r == null ? void 0 : r.id) ?? jn).trim() || jn;
    (((c = (l = a == null ? void 0 : a.statuses) == null ? void 0 : l.has) == null ? void 0 : c.call(l, s)) ?? !1) && await a.toggleStatusEffect(s, { active: !1, overlay: !1 });
  }
  static _buildSpendAction(e, t, n = "") {
    var l;
    const a = Number(((l = e.state) == null ? void 0 : l[`${t.resource}Remaining`]) ?? 0), r = t.resource === "sa" ? "" : a < t.cost ? `No ${String(t.resource).toUpperCase()} remaining.` : "", s = n || r, o = this._formatCostLabel(t.resource, t.cost);
    return {
      id: t.id,
      label: t.label,
      costLabel: o,
      handler: "combatSpend",
      resource: t.resource,
      cost: t.cost,
      disabled: !!s,
      reason: s,
      prominent: !1
    };
  }
  static _buildStubAction(e) {
    return {
      ...e,
      handler: "",
      disabled: !0,
      reason: "Not yet implemented."
    };
  }
  static _formatCostLabel(e, t) {
    return `${t} ${String(e).toUpperCase()}`;
  }
  static _appendActionLog(e, { id: t = "", label: n = "", costLabel: a = "" } = {}) {
    const r = String(n ?? "").trim();
    if (!r) return;
    const s = gr(e == null ? void 0 : e.actionLog);
    s.push({
      id: String(t ?? "").trim(),
      label: r,
      costLabel: String(a ?? "").trim()
    }), e.actionLog = s;
  }
  static getActivationIdentity(e, t) {
    return {
      combatId: (e == null ? void 0 : e.id) ?? null,
      combatantId: (t == null ? void 0 : t.id) ?? null,
      round: Number((e == null ? void 0 : e.round) ?? 0),
      turn: Number((e == null ? void 0 : e.turn) ?? 0)
    };
  }
  static async ensureCurrentCombatantState() {
    var r, s;
    if (!game.user.isGM) return;
    const e = game.combat, t = e == null ? void 0 : e.combatant;
    if (!e || !t || ((r = e.scene) == null ? void 0 : r.id) !== ((s = canvas == null ? void 0 : canvas.scene) == null ? void 0 : s.id)) return;
    this.rememberActivation(e, t);
    const n = this.getActivationIdentity(e, t), a = t.getFlag(Xt, pi);
    Xr(a, n) || await t.setFlag(Xt, pi, Od(a, n));
  }
  static async spendResource(e, {
    token: t = null,
    resource: n = "sa",
    cost: a = 1,
    actionId: r = "",
    actionLabel: s = "",
    actionCostLabel: o = "",
    actionCategory: l = ""
  } = {}) {
    var S, w, T, k, C, I, O;
    const c = this.getSnapshot(e, { token: t });
    if (!c.hasCombatant)
      return { ok: !1, reason: "No combatant on the current scene." };
    if (!c.isCurrentTurn)
      return { ok: !1, reason: "Only available during your activation." };
    this.rememberActivation(c.combat, c.combatant);
    const u = {
      combat: c.combat,
      combatant: c.combatant,
      state: hs(c.state, this.getActivationIdentity(c.combat, c.combatant)),
      sceneId: ((S = canvas == null ? void 0 : canvas.scene) == null ? void 0 : S.id) ?? "",
      snapshot: c
    };
    let d = Math.max(0, Number(a ?? 0) || 0);
    const m = hi({
      actor: e,
      phase: "onBeforeActionCostFinalized",
      facts: up({
        actor: e,
        packet: { actionId: r, category: l, resource: n, cost: d, effectiveCost: d },
        runtime: u
      }),
      packet: { actionId: r, category: l, resource: n, cost: d, effectiveCost: d },
      options: { runtime: u, consumeUsage: !0 }
    });
    d = Math.max(0, Number(m.packet.cost ?? d) || 0), u.pendingMutations = (u.pendingMutations ?? []).concat(m.mutations);
    const f = `${n}Remaining`, p = Number(((w = c.state) == null ? void 0 : w[f]) ?? 0);
    if (n !== "sa" && p < d)
      return { ok: !1, reason: `No ${String(n).toUpperCase()} remaining.` };
    const g = u.state, h = n === "sa" ? Kl(e) : 0, y = Math.max(0, Number(((T = c.state) == null ? void 0 : T.saSpentThisActivation) ?? 0) || 0);
    if (n === "sa" && y + d > h)
      return { ok: !1, reason: "Activation SA cap reached." };
    g[f] = Math.max(0, p - d), n === "sa" && (g.saSpentThisActivation = y + d, r === "attack" && (g.attacksThisActivation = Number(g.attacksThisActivation ?? 0) + 1)), this._appendActionLog(g, {
      id: r,
      label: s,
      costLabel: o || this._formatCostLabel(n, d)
    });
    let b = 0;
    if (n === "sa") {
      const x = Math.max(0, y - yn), B = Math.max(0, g.saSpentThisActivation - yn), P = Math.max(0, Number(((k = c.state) == null ? void 0 : k.attacksThisActivation) ?? 0) || 0), L = Math.max(0, Number(g.attacksThisActivation ?? 0) || 0);
      for (let D = x + 1; D <= B; D += 1) {
        const G = hi({
          actor: e,
          phase: "onBeforeBurnApplied",
          facts: fs({
            actor: e,
            packet: {
              actionId: r,
              category: l,
              resource: n,
              amount: 1,
              source: "extraSA",
              extraSaIndex: D
            },
            runtime: u
          }),
          packet: {
            actionId: r,
            category: l,
            resource: n,
            amount: 1,
            source: "extraSA",
            extraSaIndex: D
          },
          options: { runtime: u, consumeUsage: !0 }
        });
        u.pendingMutations = (u.pendingMutations ?? []).concat(G.mutations), b += Math.max(0, Number(G.packet.amount ?? 0) || 0);
      }
      for (let D = P + 1; D <= L; D += 1) {
        if (D <= 1) continue;
        const G = hi({
          actor: e,
          phase: "onBeforeBurnApplied",
          facts: fs({
            actor: e,
            packet: {
              actionId: r,
              category: l,
              resource: n,
              amount: 1,
              source: "attack"
            },
            runtime: u
          }),
          packet: {
            actionId: r,
            category: l,
            resource: n,
            amount: 1,
            source: "attack",
            attackIndex: D
          },
          options: { runtime: u, consumeUsage: !0 }
        });
        u.pendingMutations = (u.pendingMutations ?? []).concat(G.mutations), b += Math.max(0, Number(G.packet.amount ?? 0) || 0);
      }
      g.burnThisActivation = Math.max(0, Number(g.burnThisActivation ?? 0) + b);
    }
    return (C = u.pendingMutations) != null && C.length ? await rn({
      actor: e,
      mutations: u.pendingMutations,
      runtime: {
        ...u,
        state: g
      }
    }) : await c.combatant.setFlag(Xt, pi, g), b > 0 && await e.update({ "system.burn.value": Math.max(0, Number(((O = (I = e.system) == null ? void 0 : I.burn) == null ? void 0 : O.value) ?? 0) + b) }), { ok: !0, snapshot: this.getSnapshot(e, { token: c.token }) };
  }
  static async reduceBurn(e, { token: t = null } = {}) {
    var o, l, c, u;
    const n = this.getSnapshot(e, { token: t });
    if (!n.hasCombatant) return { ok: !1, reason: "No combatant on the current scene." };
    if (!n.isCurrentTurn) return { ok: !1, reason: "Only available during your activation." };
    if (es(e, n) <= 0) return { ok: !1, reason: "Activation SA cap reached." };
    if (n.burn.value <= 0) return { ok: !1, reason: "Burn is already at 0." };
    const a = await this.spendResource(e, {
      token: n.token,
      resource: "sa",
      cost: 1,
      actionId: "reduceBurn",
      actionLabel: "Reduce Burn",
      actionCostLabel: "1 SA",
      actionCategory: tt.standard
    });
    if (!a.ok) return a;
    const r = Math.max(0, Number(((l = (o = e.system) == null ? void 0 : o.burn) == null ? void 0 : l.value) ?? 0) - 1), s = { "system.burn.value": r };
    return r === 0 && ((u = (c = e.system) == null ? void 0 : c.burn) != null && u.overloaded) && (s["system.burn.overloaded"] = !1), await e.update(s), { ok: !0, snapshot: this.getSnapshot(e, { token: n.token }) };
  }
  static async finalizeActivation(e, t) {
    var f, p, g, h, y, b, S, w, T, k, C, I;
    if (!game.user.isGM || !t || !e) return;
    const n = ((p = (f = e.combatants) == null ? void 0 : f.get) == null ? void 0 : p.call(f, t)) ?? null, a = this._getCombatantActor(n, ((g = e == null ? void 0 : e.scene) == null ? void 0 : g.id) ?? ((h = canvas == null ? void 0 : canvas.scene) == null ? void 0 : h.id)), r = this.getActionEconomyActorForCombatant(n, ((y = e == null ? void 0 : e.scene) == null ? void 0 : y.id) ?? ((b = canvas == null ? void 0 : canvas.scene) == null ? void 0 : b.id));
    if (!n || !r) return;
    const s = n.getFlag(Xt, pi), o = Xr(s, this.getActivationIdentity(e, n)) ? hs(s, this.getActivationIdentity(e, n)) : hs(s), c = {
      burnDelta: Number(o.saSpentThisActivation ?? 0) <= yn && Number(o.burnThisActivation ?? 0) <= 0 && Number(o.reactionBurnSinceLastActivation ?? 0) <= 0 ? -2 : 0,
      edgeAdjustments: []
    }, u = {
      combat: e,
      combatant: n,
      state: o,
      sceneId: ((S = e.scene) == null ? void 0 : S.id) ?? ((w = canvas == null ? void 0 : canvas.scene) == null ? void 0 : w.id) ?? ""
    }, d = hi({
      actor: r,
      phase: "onEndOfActivation",
      facts: fp({ actor: r, packet: c, runtime: u }),
      packet: c,
      options: { runtime: u, consumeUsage: !0 }
    });
    await rn({ actor: r, mutations: d.mutations, runtime: u });
    const m = Number(d.packet.burnDelta ?? c.burnDelta) || 0;
    if (m) {
      const O = Math.max(0, Number(((k = (T = r.system) == null ? void 0 : T.burn) == null ? void 0 : k.value) ?? 0) + m), x = { "system.burn.value": O };
      O === 0 && ((I = (C = r.system) == null ? void 0 : C.burn) != null && I.overloaded) && (x["system.burn.overloaded"] = !1), await r.update(x);
    }
    for (const O of d.packet.edgeAdjustments ?? []) {
      const x = Number((O == null ? void 0 : O.amount) ?? 0) || 0;
      !x || !(O != null && O.poolKey) || (x > 0 ? await r.gainEdge(O.poolKey, x, { skipTraitHooks: !0, source: "endOfActivationTrait" }) : await r.spendEdge(O.poolKey, Math.abs(x), { skipTraitHooks: !0, source: "endOfActivationTrait" }));
    }
    (a == null ? void 0 : a.type) === "battlemech" && await ku(a, {
      source: "combat turn advance",
      postDangerCard: !0,
      activation: this.getActivationIdentity(e, n)
    });
  }
  static async _onUpdateCombat(e, t) {
    if (Object.prototype.hasOwnProperty.call(t ?? {}, "turn") || Object.prototype.hasOwnProperty.call(t ?? {}, "round")) {
      const a = this._lastActivationByCombat.get(e == null ? void 0 : e.id) ?? null, r = typeof a == "string" ? a : (a == null ? void 0 : a.combatantId) ?? null, s = this.getActivationIdentity(e, e == null ? void 0 : e.combatant), o = a && typeof a == "object" ? !Xr(a, s) : r && r !== s.combatantId;
      r && o && await this.finalizeActivation(e, r), await this.ensureCurrentCombatantState(), await this._processCurrentCombatantHazards(e), await this._processCurrentCombatantMachineCrits(e), e != null && e.id && this._lastActivationByCombat.set(e.id, s);
    }
    this.renderOpenActorSheets();
  }
  static async _onCreateCombatant(e) {
    var a, r, s;
    const t = e == null ? void 0 : e.combat;
    ((a = t == null ? void 0 : t.combatant) == null ? void 0 : a.id) === (e == null ? void 0 : e.id) && await this.ensureCurrentCombatantState(), await this._syncPreparedIndicatorForCombatant(e);
    const n = this._getCombatantTokenDocument(e, ((r = t == null ? void 0 : t.scene) == null ? void 0 : r.id) ?? ((s = canvas == null ? void 0 : canvas.scene) == null ? void 0 : s.id));
    n && await this._syncHazardPresenceForToken(n), this.renderOpenActorSheets();
  }
  static async _onDeleteCombatant(e) {
    await this.clearPreparedIndicatorForCombatant(e), this.renderOpenActorSheets();
  }
  static async _onDeleteCombat(e) {
    e != null && e.id && this._lastActivationByCombat.delete(e.id);
    for (const t of this._getCombatants(e))
      await this.clearPreparedIndicatorForCombatant(t);
    this.renderOpenActorSheets();
  }
  static _onUpdateCombatant(e, t) {
    var r, s, o, l;
    const n = foundry.utils.hasProperty(t, `flags.${Xt}.${pi}`), a = foundry.utils.hasProperty(t, `flags.${Xt}.targeting`) || foundry.utils.hasProperty(t, `flags.${Xt}.ewState`);
    if (n) {
      this._syncPreparedIndicatorForCombatant(e);
      const c = this._getCombatantTokenDocument(e, this._getCombatantSceneId(e) || ((r = canvas == null ? void 0 : canvas.scene) == null ? void 0 : r.id));
      c && this._queueHazardOverlayRefresh(c);
    }
    if (n || a) {
      this.renderOpenActorSheets((s = e == null ? void 0 : e.actor) == null ? void 0 : s.id);
      const c = this.getActionEconomyActorForCombatant(
        e,
        this._getCombatantSceneId(e) || ((o = canvas == null ? void 0 : canvas.scene) == null ? void 0 : o.id)
      );
      c != null && c.id && c.id !== ((l = e == null ? void 0 : e.actor) == null ? void 0 : l.id) && this.renderOpenActorSheets(c.id);
    }
  }
  static _onTargetToken(e, t, n) {
    var a;
    (e == null ? void 0 : e.id) === ((a = game.user) == null ? void 0 : a.id) && this.queueCharacterSheetRefresh();
  }
  static _onUpdateToken(e, t) {
    var r, s;
    if (!["x", "y", "elevation"].some(
      (o) => Object.prototype.hasOwnProperty.call(t ?? {}, o)
    ) || ((r = e == null ? void 0 : e.parent) == null ? void 0 : r.id) !== ((s = canvas == null ? void 0 : canvas.scene) == null ? void 0 : s.id)) return;
    const a = String((e == null ? void 0 : e.id) ?? "").trim();
    if (a) {
      const o = Object.prototype.hasOwnProperty.call(t ?? {}, "x") ? Number(t.x) : Number(e == null ? void 0 : e.x), l = Object.prototype.hasOwnProperty.call(t ?? {}, "y") ? Number(t.y) : Number(e == null ? void 0 : e.y);
      Number.isFinite(o) && Number.isFinite(l) && this._pendingTokenPositions.set(a, { x: o, y: l });
    }
    this._syncHazardPresenceForToken(e), this.queueCharacterSheetRefresh();
  }
  static _onRefreshToken(e) {
    this._refreshHazardOverlay(e);
  }
  static _getTokenDocumentFromRegionEvent(e = []) {
    var t, n, a, r, s, o;
    for (const l of e) {
      if (!l) continue;
      const c = [
        l == null ? void 0 : l.document,
        l == null ? void 0 : l.token,
        l == null ? void 0 : l.tokenDocument,
        (t = l == null ? void 0 : l.object) == null ? void 0 : t.document,
        (n = l == null ? void 0 : l.data) == null ? void 0 : n.token,
        (a = l == null ? void 0 : l.data) == null ? void 0 : a.tokenDocument,
        (r = l == null ? void 0 : l.eventData) == null ? void 0 : r.token,
        (s = l == null ? void 0 : l.eventData) == null ? void 0 : s.tokenDocument
      ];
      for (const u of c) {
        const d = (u == null ? void 0 : u.document) ?? u ?? null;
        if ((d == null ? void 0 : d.documentName) === "Token" || ((o = d == null ? void 0 : d.constructor) == null ? void 0 : o.documentName) === "Token")
          return d;
      }
    }
    return null;
  }
  static _onRegionTokenEvent(...e) {
    const t = this._getTokenDocumentFromRegionEvent(e);
    t && this._syncHazardPresenceForToken(t);
  }
  static async _onCreateRegion(e) {
    qo(e) && (await jo(e), await this._syncAllSceneHazards((e == null ? void 0 : e.parent) ?? (canvas == null ? void 0 : canvas.scene) ?? null));
  }
  static async _onUpdateRegion(e) {
    qo(e) && (await jo(e), await this._syncAllSceneHazards((e == null ? void 0 : e.parent) ?? (canvas == null ? void 0 : canvas.scene) ?? null));
  }
  static async _onDeleteRegion(e) {
    var r, s, o;
    const t = String((e == null ? void 0 : e.id) ?? "").trim();
    if (!t) return;
    const n = (e == null ? void 0 : e.parent) ?? (canvas == null ? void 0 : canvas.scene) ?? null, a = game.combat;
    for (const l of this._getCombatants(a)) {
      const c = this._getCombatantTokenDocument(l, (n == null ? void 0 : n.id) ?? ((r = canvas == null ? void 0 : canvas.scene) == null ? void 0 : r.id)), u = (c == null ? void 0 : c.actor) ?? (l == null ? void 0 : l.actor) ?? null;
      if (!u || !c) continue;
      const d = this.getSnapshot(u, { token: c });
      (s = d == null ? void 0 : d.hazards) != null && s[t] && (await this.setHazardState(u, { token: c, regionId: t, hazardState: null }), ((o = d == null ? void 0 : d.pendingReaction) == null ? void 0 : o.sourceKind) === "hazard" && d.pendingReaction.sourceId === t && await this.clearPendingReaction(u, { token: c }), this._queueHazardOverlayRefresh(c));
    }
  }
  static async _syncAllSceneHazards(e = (canvas == null ? void 0 : canvas.scene) ?? null) {
    if (e) {
      for (const t of Array.from(e.regions ?? []))
        qo(t) && await jo(t);
      for (const t of Array.from(e.tokens ?? []))
        await this._syncHazardPresenceForToken(t), this._queueHazardOverlayRefresh(t);
    }
  }
  static async _syncHazardPresenceForToken(e) {
    var c, u, d, m, f, p;
    const t = this._asTokenDocument(e), n = (t == null ? void 0 : t.actor) ?? null;
    if (!this._supportsHazardActor(n) || !t) {
      this._queueHazardOverlayRefresh(t);
      return;
    }
    const a = this.getSnapshot(n, { token: t });
    if (!(a != null && a.hasCombatant)) {
      this._queueHazardOverlayRefresh(t);
      return;
    }
    Ks(a.hazards);
    const r = Sd(t), s = new Map(
      r.map((g) => {
        const h = In(g);
        return h ? [String(g.id ?? "").trim(), { region: g, flag: h }] : null;
      }).filter(Boolean)
    ), o = [], l = [];
    await this.updateCombatantState(n, {
      token: t,
      mutate: (g) => {
        var h, y, b, S, w;
        g.hazards ?? (g.hazards = {});
        for (const [T, { flag: k }] of s.entries()) {
          if (g.hazards[T]) continue;
          const C = {
            tier: lt((h = k == null ? void 0 : k.hazardDef) == null ? void 0 : h.startExposure, pe.minor),
            turnsExposed: 0,
            evadeLocked: !1,
            lastProcessedRound: 0
          };
          g.hazards[T] = C, o.push({ regionId: T, flag: k, hazardState: C });
        }
        for (const [T, k] of Object.entries(g.hazards ?? {})) {
          if (s.has(T)) continue;
          const C = In((S = (b = (y = canvas == null ? void 0 : canvas.scene) == null ? void 0 : y.regions) == null ? void 0 : b.get) == null ? void 0 : S.call(b, T)) ?? null;
          ((w = C == null ? void 0 : C.hazardDef) == null ? void 0 : w.clearOnExit) !== !1 && (delete g.hazards[T], l.push({ regionId: T, hazardState: k, flag: C }));
        }
        return g;
      }
    });
    for (const g of o) {
      const h = ((c = s.get(g.regionId)) == null ? void 0 : c.region) ?? ((m = (d = (u = canvas == null ? void 0 : canvas.scene) == null ? void 0 : u.regions) == null ? void 0 : d.get) == null ? void 0 : m.call(d, g.regionId)) ?? null;
      await this._createHazardEventChatCard({
        actor: n,
        token: t,
        region: h,
        hazardFlag: g.flag,
        hazardState: g.hazardState,
        eventType: "entry",
        nextTier: g.hazardState.tier,
        allowEvade: !g.hazardState.evadeLocked
      });
    }
    for (const g of l) {
      ((f = a == null ? void 0 : a.pendingReaction) == null ? void 0 : f.sourceKind) === "hazard" && a.pendingReaction.sourceId === g.regionId && await this.clearPendingReaction(n, { token: t });
      const h = String(((p = g == null ? void 0 : g.flag) == null ? void 0 : p.label) ?? "Hazard").trim() || "Hazard", y = `<div class="mwd-gm-notice"><b>${foundry.utils.escapeHTML(h)}:</b> ${foundry.utils.escapeHTML(n.name ?? "Target")} leaves the zone.</div>`;
      await ChatMessage.create(Wo({
        speaker: ChatMessage.getSpeaker({ actor: n, token: t }),
        content: y
      }));
    }
    this._queueHazardOverlayRefresh(t);
  }
  static async _processCurrentCombatantHazards(e = game.combat) {
    var l, c, u;
    const t = (e == null ? void 0 : e.combatant) ?? null, n = this._getCombatantTokenDocument(t, ((l = e == null ? void 0 : e.scene) == null ? void 0 : l.id) ?? ((c = canvas == null ? void 0 : canvas.scene) == null ? void 0 : c.id)), a = (n == null ? void 0 : n.actor) ?? (t == null ? void 0 : t.actor) ?? null;
    if (!t || !n || !this._supportsHazardActor(a)) return;
    const r = this.getSnapshot(a, { token: n }), s = Number((e == null ? void 0 : e.round) ?? 0) || 0, o = new Map(
      Sd(n).map((d) => {
        const m = In(d);
        return m ? [String(d.id ?? "").trim(), { region: d, flag: m }] : null;
      }).filter(Boolean)
    );
    for (const [d, m] of Object.entries(r.hazards ?? {})) {
      if ((Number((m == null ? void 0 : m.lastProcessedRound) ?? 0) || 0) >= s) continue;
      const f = o.get(d);
      if (!f) continue;
      const p = this._getHazardNextTier(m, ((u = f.flag) == null ? void 0 : u.hazardDef) ?? {});
      await this._createHazardEventChatCard({
        actor: a,
        token: n,
        region: f.region,
        hazardFlag: f.flag,
        hazardState: m,
        eventType: "tick",
        nextTier: p,
        allowEvade: !m.evadeLocked
      }), await this.setHazardState(a, {
        token: n,
        regionId: d,
        hazardState: {
          ...m,
          lastProcessedRound: s
        }
      });
    }
    this._queueHazardOverlayRefresh(n);
  }
  static async _processCurrentCombatantMachineCrits(e = game.combat) {
    var l, c, u, d, m, f, p, g, h;
    const t = (e == null ? void 0 : e.combatant) ?? null, n = this._getCombatantTokenDocument(t, ((l = e == null ? void 0 : e.scene) == null ? void 0 : l.id) ?? ((c = canvas == null ? void 0 : canvas.scene) == null ? void 0 : c.id)), a = (n == null ? void 0 : n.actor) ?? (t == null ? void 0 : t.actor) ?? null;
    if (!t || !n || !a || !aa(a)) return;
    const r = this.getSnapshot(a, { token: n });
    if (!(r != null && r.hasCombatant) || !(r != null && r.isCurrentTurn) || (u = r == null ? void 0 : r.state) != null && u.machineCritsProcessed) return;
    const s = Kw(a), o = [];
    if (s.saCost > 0) {
      const y = await this.spendResource(a, {
        token: n,
        resource: "sa",
        cost: s.saCost,
        actionId: "machineCritStaggered",
        actionLabel: "Staggered",
        actionCostLabel: `${s.saCost} SA`,
        actionCategory: "simple"
      });
      o.push(y != null && y.ok ? `Staggered: spent ${s.saCost} SA at the start of activation.` : `Staggered: unable to auto-spend ${s.saCost} SA (${(y == null ? void 0 : y.reason) ?? "manual resolution required"}).`);
    }
    if (s.heatDelta > 0 && a.type === "battlemech") {
      const b = Math.max(0, Number(((f = (m = (d = a.system) == null ? void 0 : d.monitors) == null ? void 0 : m.heat) == null ? void 0 : f.value) ?? ((h = (g = (p = a.system) == null ? void 0 : p.mwd) == null ? void 0 : g.heat) == null ? void 0 : h.current) ?? 0) || 0) + s.heatDelta;
      await a.update({
        "system.monitors.heat.value": b,
        "system.mwd.heat.current": b
      }), o.push(`Overheating: gained ${s.heatDelta} Heat at the start of activation.`);
    }
    if (o.push(...s.reminders.map((y) => `${y.label}: ${y.text}`)), o.length) {
      const y = [
        '<div class="mwd-gm-notice">',
        `<strong>${foundry.utils.escapeHTML(a.name ?? "Machine")} - Critical Effects</strong>`,
        `<ul>${o.map((b) => `<li>${foundry.utils.escapeHTML(b)}</li>`).join("")}</ul>`,
        "</div>"
      ].join("");
      await ChatMessage.create(Wo({
        speaker: ChatMessage.getSpeaker({ actor: a, token: n }),
        content: y
      }));
    }
    await this.updateCombatantState(a, {
      token: n,
      mutate: (y) => (y.machineCritsProcessed = !0, y)
    });
  }
  static _getHazardNextTier(e = {}, t = {}) {
    var l, c, u, d;
    const n = Math.max(0, Number((e == null ? void 0 : e.turnsExposed) ?? 0) || 0), a = Math.max(1, Number(((l = t == null ? void 0 : t.escalation) == null ? void 0 : l.intervalTurns) ?? 1) || 1), r = Math.max(0, Number(((c = t == null ? void 0 : t.escalation) == null ? void 0 : c.rate) ?? 1) || 0);
    if (!(r > 0 && (n + 1) % a === 0)) return lt(e == null ? void 0 : e.tier, pe.none);
    let o = lt(e == null ? void 0 : e.tier, pe.none);
    for (let m = 0; m < r; m += 1)
      if (o = Uy(o, 1), Cn(o) >= Cn(((u = t == null ? void 0 : t.escalation) == null ? void 0 : u.max) ?? pe.full)) {
        o = lt((d = t == null ? void 0 : t.escalation) == null ? void 0 : d.max, pe.full);
        break;
      }
    return o;
  }
  static async _createHazardEventChatCard({
    actor: e = null,
    token: t = null,
    region: n = null,
    hazardFlag: a = {},
    hazardState: r = {},
    eventType: s = "entry",
    nextTier: o = null,
    allowEvade: l = !1
  } = {}) {
    var g, h, y;
    if (!e) return null;
    const c = lt(r == null ? void 0 : r.tier, pe.none), u = lt(o, c), d = l && c !== pe.none && !(r != null && r.evadeLocked) ? this.getReactionSpendPreview(e, { token: t }) : null, m = {
      kind: "hazard",
      eventType: s,
      regionId: String((n == null ? void 0 : n.id) ?? "").trim(),
      regionName: String((a == null ? void 0 : a.label) ?? (n == null ? void 0 : n.name) ?? "Hazard").trim() || "Hazard",
      actorUuid: e.uuid,
      tokenUuid: (t == null ? void 0 : t.uuid) ?? ((g = t == null ? void 0 : t.document) == null ? void 0 : g.uuid) ?? null,
      actorName: e.name ?? "Target",
      turnsExposed: Math.max(0, Number((r == null ? void 0 : r.turnsExposed) ?? 0) || 0),
      baseDamage: Math.max(0, Number((a == null ? void 0 : a.damage) ?? 0) || 0),
      damageBefore: Zn(Number((a == null ? void 0 : a.damage) ?? 0) || 0, c),
      damageAfter: Zn(
        Number((a == null ? void 0 : a.damage) ?? 0) || 0,
        l && !(r != null && r.evadeLocked) ? kl(c, 1) : c
      ),
      damageType: String((a == null ? void 0 : a.damageType) ?? "concussive").trim() || "concussive",
      ap: Math.max(0, Number((a == null ? void 0 : a.ap) ?? 0) || 0),
      onFullBurnDelta: Math.max(0, Number(((y = (h = a == null ? void 0 : a.hazardDef) == null ? void 0 : h.onFull) == null ? void 0 : y.burnDelta) ?? 0) || 0),
      source: String((a == null ? void 0 : a.label) ?? (n == null ? void 0 : n.name) ?? "Hazard").trim() || "Hazard",
      nextTier: u,
      exposure: {
        initialTier: c,
        finalTier: c,
        initialLabel: bi(c),
        finalLabel: bi(c),
        evadeLocked: !!(r != null && r.evadeLocked)
      },
      preview: {
        evadeActive: !1,
        edgePoolKey: null,
        finalTier: c,
        reactionPreview: d ? {
          burnDelta: Number(d.burnDelta ?? 0),
          canSpendEdge: !!d.canSpendEdge,
          edgePools: Array.isArray(d.edgePools) ? d.edgePools : []
        } : {}
      }
    }, f = await bp(m, { actor: e, token: t }), p = await ChatMessage.create(Wo({
      speaker: ChatMessage.getSpeaker({ actor: e, token: t }),
      content: f,
      flags: {
        mwd: {
          hazardCard: m
        }
      }
    }));
    return p && d && c !== pe.none && !(r != null && r.evadeLocked) && await this.setPendingReaction(e, {
      token: t,
      pendingReaction: {
        type: "evade",
        sourceKind: "hazard",
        sourceId: String((n == null ? void 0 : n.id) ?? "").trim() || null,
        messageId: p.id,
        exposureBefore: c,
        exposureAfterPreview: kl(c, 1),
        edgePoolKey: null,
        allowCurrentTurn: !0
      }
    }), p;
  }
  static _supportsHazardActor(e) {
    return (e == null ? void 0 : e.type) === "character" || (e == null ? void 0 : e.type) === "npc";
  }
  static _queueHazardOverlayRefresh(e) {
    var n;
    const t = (e == null ? void 0 : e.object) ?? e ?? null;
    (n = t == null ? void 0 : t.refresh) == null || n.call(t);
  }
  static _refreshHazardOverlay(e) {
    var c, u, d;
    const t = (e == null ? void 0 : e.object) ?? e ?? null, n = (t == null ? void 0 : t.document) ?? e ?? null;
    if (!t || !n) return;
    const a = (n == null ? void 0 : n.actor) ?? null, r = a ? this.getSnapshot(a, { token: n }) : null, s = Object.values((r == null ? void 0 : r.hazards) ?? {}), o = dT(s);
    let l = t.mwdHazardOverlay ?? null;
    if (!o) {
      l != null && l.parent && l.parent.removeChild(l), (c = l == null ? void 0 : l.destroy) == null || c.call(l), t.mwdHazardOverlay = null;
      return;
    }
    l || (l = new PIXI.Text(o, {
      fontFamily: "MWD UI",
      fontSize: 14,
      fontWeight: "700",
      fill: "#fff2d5",
      stroke: "#23150d",
      strokeThickness: 4,
      align: "center"
    }), (d = (u = l.anchor) == null ? void 0 : u.set) == null || d.call(u, 0, 1), t.addChild(l), t.mwdHazardOverlay = l), l.text = o, l.x = 6, l.y = Math.max(18, Number(t.h ?? 0) - 4);
  }
  static queueCharacterSheetRefresh(e = null) {
    this._targetRefreshTimeout && clearTimeout(this._targetRefreshTimeout), this._targetRefreshTimeout = setTimeout(() => {
      this._targetRefreshTimeout = null, this.renderOpenActorSheets(e);
    }, 0);
  }
  static _collectOpenActorSheetApps(e = null) {
    var r, s;
    const t = Array.isArray(e) && e.length ? new Set(e.map((o) => String(o ?? "").trim()).filter(Boolean)) : null, n = /* @__PURE__ */ new Set(), a = (o) => {
      var l;
      for (const c of Object.values((o == null ? void 0 : o.apps) ?? {})) {
        const u = String(((l = c == null ? void 0 : c.actor) == null ? void 0 : l.type) ?? "").trim();
        u && (t && !t.has(u) || n.add(c));
      }
    };
    for (const o of Array.from(game.actors ?? []))
      a(o);
    for (const o of Array.from(((r = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : r.placeables) ?? []))
      a(o == null ? void 0 : o.actor);
    for (const o of Object.values(ui.windows ?? {})) {
      const l = String(((s = o == null ? void 0 : o.actor) == null ? void 0 : s.type) ?? "").trim();
      l && (t && !t.has(l) || n.add(o));
    }
    return Array.from(n);
  }
  static renderOpenActorSheets(e = null, t = null) {
    var a;
    const n = this._collectOpenActorSheetApps(t);
    for (const r of n)
      if (!(e && ((a = r.actor) == null ? void 0 : a.id) !== e)) {
        if (typeof r.requestCombatDashboardRefresh == "function") {
          r.requestCombatDashboardRefresh();
          continue;
        }
        r.render({ force: !0 });
      }
  }
  static renderOpenCharacterSheets(e = null) {
    this.renderOpenActorSheets(e, ["character"]);
  }
};
z(Ca, "_targetRefreshTimeout", null), z(Ca, "_pendingTokenPositions", /* @__PURE__ */ new Map()), z(Ca, "_lastActivationByCombat", /* @__PURE__ */ new Map());
let F = Ca;
function Kl(i) {
  var n, a, r, s, o, l;
  const e = Math.max(0, Number(((r = (a = (n = i == null ? void 0 : i.system) == null ? void 0 : n.attributes) == null ? void 0 : a.reflexes) == null ? void 0 : r.value) ?? 0) || 0), t = Math.max(0, Number(((l = (o = (s = i == null ? void 0 : i.system) == null ? void 0 : s.attributes) == null ? void 0 : o.willpower) == null ? void 0 : l.value) ?? 0) || 0);
  return yn + Math.floor((e + t) / 2);
}
function es(i, e) {
  var t;
  return Math.max(0, Kl(i) - Math.max(0, Number(((t = e == null ? void 0 : e.state) == null ? void 0 : t.saSpentThisActivation) ?? 0) || 0));
}
const Gl = Object.freeze([
  Object.freeze({ key: "close", label: "Close", min: 0, max: 5, baseDn: 2 }),
  Object.freeze({ key: "near", label: "Near", min: 6, max: 26, baseDn: 3 }),
  Object.freeze({ key: "far", label: "Far", min: 27, max: 62, baseDn: 4 }),
  Object.freeze({ key: "extreme", label: "Extreme", min: 63, max: 120, baseDn: 5 })
]), bs = Object.freeze([
  Object.freeze({ key: "close", label: "Close", min: 0, max: 59, baseDn: 2 }),
  Object.freeze({ key: "near", label: "Near", min: 60, max: 269, baseDn: 3 }),
  Object.freeze({ key: "far", label: "Far", min: 270, max: 629, baseDn: 4 }),
  Object.freeze({ key: "extreme", label: "Extreme", min: 630, max: 1200, baseDn: 5 })
]), fT = new Map(Gl.map((i) => [i.key, i])), pT = new Map(bs.map((i) => [i.key, i]));
function ts(i, e) {
  const t = Number(i);
  return !Number.isFinite(t) || t <= 0 ? e : Math.max(0, Math.trunc(t));
}
function bo(i = "") {
  return fT.get(String(i ?? "").trim().toLowerCase()) ?? null;
}
function lg(i = "") {
  return pT.get(String(i ?? "").trim().toLowerCase()) ?? null;
}
function Gs(i = "") {
  if (String(i ?? "").trim().toLowerCase() === "outofrange") return "Out of Range";
  const e = bo(i);
  return e ? `${e.label} ${e.min}-${e.max} m` : String(i ?? "").trim() || "Range";
}
function So(i = "") {
  if (String(i ?? "").trim().toLowerCase() === "outofrange") return "Out of Range";
  const e = bo(i);
  return e ? e.label : String(i ?? "").trim() || "Range";
}
function cg(i = "") {
  if (String(i ?? "").trim().toLowerCase() === "outofrange") return "Out of Range";
  const e = lg(i);
  return e ? e.label : String(i ?? "").trim() || "Range";
}
function gT(i = "", e = 1) {
  if (String(i ?? "").trim().toLowerCase() === "outofrange") return 6;
  const t = bo(i);
  return Number.isFinite(Number(t == null ? void 0 : t.baseDn)) ? Number(t.baseDn) : e;
}
function hT(i = "", e = 1) {
  if (String(i ?? "").trim().toLowerCase() === "outofrange") return 6;
  const t = lg(i);
  return Number.isFinite(Number(t == null ? void 0 : t.baseDn)) ? Number(t.baseDn) : e;
}
function vu(i = {}) {
  return {
    max: String((i == null ? void 0 : i.max) ?? "").trim().toLowerCase() || "extreme",
    close: ts((i == null ? void 0 : i.close) ?? (i == null ? void 0 : i.short), 5),
    near: ts((i == null ? void 0 : i.near) ?? (i == null ? void 0 : i.medium), 26),
    far: ts((i == null ? void 0 : i.far) ?? (i == null ? void 0 : i.long), 62),
    extreme: ts(i == null ? void 0 : i.extreme, 120)
  };
}
function yT(i, e = {}, t = "close") {
  var u;
  const n = Number(i);
  if (!Number.isFinite(n) || n < 0)
    return String(t ?? "close").trim().toLowerCase() || "close";
  const a = vu(e), r = ((u = bo(a.max)) == null ? void 0 : u.key) ?? "extreme", s = Gl.findIndex((d) => d.key === r), o = Number((a == null ? void 0 : a[r]) ?? NaN);
  if (Number.isFinite(o) && n > o)
    return "outOfRange";
  let l = "extreme";
  n <= a.close ? l = "close" : n <= a.near ? l = "near" : n <= a.far && (l = "far");
  const c = Gl.findIndex((d) => d.key === l);
  return s >= 0 && c > s ? r : l;
}
function ug(i, e = "close") {
  const t = Number(i);
  if (!Number.isFinite(t) || t < 0)
    return String(e ?? "close").trim().toLowerCase() || "close";
  const n = String(e ?? "close").trim().toLowerCase() || "close", a = bs[bs.length - 1] ?? null;
  if (a && t > Number(a.max ?? NaN))
    return "outOfRange";
  for (const r of bs)
    if (t <= r.max) return r.key;
  return n;
}
const Na = "lifeModuleCatalog", Ao = Object.freeze([
  { moduleType: "faction", label: "Faction" },
  { moduleType: "childhood", label: "Childhood" },
  { moduleType: "higherEducation", label: "Higher Education" },
  { moduleType: "realLife", label: "Real Life" }
]), bT = Object.freeze(
  Object.fromEntries(Ao.map((i) => [i.moduleType, i.label]))
), ST = new Set(Ao.map((i) => i.moduleType)), AT = /* @__PURE__ */ new Set(["skill", "edgePool"]), Eu = Object.freeze({
  grit: "Grit",
  chaos: "Chaos",
  insight: "Insight",
  rumor: "Rumor",
  legend: "Legend",
  credibility: "Credibility"
}), dg = Object.freeze(Object.keys(Eu)), wT = Object.freeze({
  skill: "Skill",
  edgePool: "Edge Pool"
}), TT = Object.freeze(IT()), kT = Object.freeze(NT()), MT = /* @__PURE__ */ new Set(["artillery", "gunnery", "piloting", "heavyWeapons"]), CT = /* @__PURE__ */ new Set([
  "artillery",
  "gunnery",
  "heavyWeapons",
  "meleeCombat",
  "piloting",
  "projectileWeapons",
  "firearms"
]), vT = Object.freeze(
  Vi.map((i) => i.code).filter((i) => !CT.has(i))
), ET = Object.freeze(ma([
  {
    id: "faction-capellan-confederation",
    label: "Capellan Confederation",
    moduleType: "faction",
    grants: [
      { id: "skill", choices: [{ type: "skill", value: "disguise" }] },
      { id: "edge-pool", choices: [{ type: "edgePool", value: "rumor" }] }
    ]
  },
  {
    id: "faction-draconis-combine",
    label: "Draconis Combine",
    moduleType: "faction",
    grants: [
      { id: "edge-pool", choices: [{ type: "edgePool", value: "legend" }] },
      { id: "skill", choices: [{ type: "skill", value: "meleeCombat" }] }
    ]
  },
  {
    id: "faction-federated-suns",
    label: "Federated Suns",
    moduleType: "faction",
    grants: [
      { id: "edge-pool", choices: [{ type: "edgePool", value: "credibility" }] },
      { id: "skill", choices: [{ type: "skill", value: "firearms" }] }
    ]
  },
  {
    id: "faction-free-worlds-league",
    label: "Free Worlds League",
    moduleType: "faction",
    grants: [
      { id: "skill", choices: [{ type: "skill", value: "negotiation" }] },
      { id: "edge-pool", choices: [{ type: "edgePool", value: "rumor" }] }
    ]
  },
  {
    id: "faction-lyran-commonwealth",
    label: "Lyran Commonwealth",
    moduleType: "faction",
    grants: [
      { id: "skill", choices: [{ type: "skill", value: "administration" }] },
      { id: "edge-pool", choices: [{ type: "edgePool", value: "legend" }] }
    ]
  },
  {
    id: "faction-taurian-concordat",
    label: "Taurian Concordat",
    moduleType: "faction",
    grants: [
      { id: "skill", choices: [{ type: "skill", value: "survival" }] },
      { id: "edge-pool", choices: [{ type: "edgePool", value: "grit" }] }
    ]
  },
  {
    id: "faction-magistracy-of-canopus",
    label: "Magistracy of Canopus",
    moduleType: "faction",
    grants: [
      { id: "edge-pool", choices: [{ type: "edgePool", value: "chaos" }] },
      { id: "skill", choices: [{ type: "skill", value: "medTech" }] }
    ]
  },
  {
    id: "faction-outworlds-alliance",
    label: "Outworlds Alliance",
    moduleType: "faction",
    grants: [
      { id: "edge-pool", choices: [{ type: "edgePool", value: "insight" }] },
      { id: "skill", choices: [{ type: "skill", value: "navigation" }] }
    ]
  },
  {
    id: "faction-pirate",
    label: "Pirate",
    moduleType: "faction",
    grants: [
      { id: "skill", choices: [{ type: "skill", value: "streetwise" }] },
      { id: "edge-pool", choices: [{ type: "edgePool", value: "chaos" }] }
    ]
  },
  {
    id: "faction-comstar",
    label: "ComStar",
    moduleType: "faction",
    grants: [
      { id: "skill", choices: [{ type: "skill", value: "systemOps" }] },
      { id: "edge-pool", choices: [{ type: "edgePool", value: "rumor" }] }
    ]
  },
  {
    id: "faction-mercenary",
    label: "Mercenary",
    moduleType: "faction",
    grants: [
      { id: "skill", choices: [{ type: "skill", value: "tactics" }] },
      { id: "edge-pool", choices: [{ type: "edgePool", value: "grit" }] }
    ]
  },
  {
    id: "faction-unaffiliated",
    label: "Unaffiliated",
    moduleType: "faction",
    grants: [{
      id: "choice",
      choices: [
        ...vT.map((i) => ({ type: "skill", value: i })),
        ...dg.map((i) => ({ type: "edgePool", value: i }))
      ]
    }]
  },
  { id: "childhood-backwoods", label: "Backwoods", moduleType: "childhood", skillChoices: ["tracking", "projectileWeapons"] },
  { id: "childhood-blue-collar", label: "Blue Collar", moduleType: "childhood", skillChoices: ["art", "technician", "zeroGOps"] },
  { id: "childhood-mercenary-brat", label: "Mercenary Brat", moduleType: "childhood", skillChoices: ["firearms"] },
  { id: "childhood-farm", label: "Farm", moduleType: "childhood", skillChoices: ["animalHandling"] },
  { id: "childhood-fugitives", label: "Fugitives", moduleType: "childhood", skillChoices: ["escapeArtist", "disguise"] },
  { id: "childhood-nobility", label: "Nobility", moduleType: "childhood", skillChoices: ["etiquette"] },
  { id: "childhood-slave", label: "Slave", moduleType: "childhood", skillChoices: ["athletics"] },
  { id: "childhood-spacer-family", label: "Spacer Family", moduleType: "childhood", skillChoices: ["zeroGOps"] },
  { id: "childhood-street", label: "Street", moduleType: "childhood", skillChoices: ["streetwise"] },
  { id: "childhood-war-orphan", label: "War Orphan", moduleType: "childhood", skillChoices: ["survival"] },
  { id: "childhood-white-collar", label: "White Collar", moduleType: "childhood", skillChoices: ["etiquette"] },
  { id: "higher-education-technical-college", label: "Technical College", moduleType: "higherEducation", skillChoices: ["technician", "systemOps"] },
  { id: "higher-education-trade-school", label: "Trade School", moduleType: "higherEducation", skillChoices: ["computers", "administration"] },
  { id: "higher-education-university", label: "University", moduleType: "higherEducation", skillChoices: ["science", "medTech"] },
  { id: "higher-education-solaris-vii-internship", label: "Solaris VII Internship", moduleType: "higherEducation", skillChoices: ["streetwise"] },
  { id: "higher-education-police-academy", label: "Police Academy", moduleType: "higherEducation", skillChoices: ["negotiation", "perception"] },
  { id: "higher-education-intelligence-operative-training", label: "Intelligence Operative Training", moduleType: "higherEducation", skillChoices: ["intimidation", "tracking"] },
  { id: "higher-education-military-academy", label: "Military Academy", moduleType: "higherEducation", skillChoices: ["perception", "administration"] },
  { id: "higher-education-military-enlistment", label: "Military Enlistment", moduleType: "higherEducation", skillChoices: ["heavyWeapons", "artillery"] },
  { id: "higher-education-family-training", label: "Family Training", moduleType: "higherEducation", skillChoices: ["leadership", "etiquette"], requiresAny: ["childhood-nobility"] },
  { id: "higher-education-officer-candidate-school", label: "Officer Candidate School", moduleType: "higherEducation", skillChoices: ["tactics", "leadership"] },
  { id: "real-life-agitator", label: "Agitator", moduleType: "realLife", skillChoices: ["meleeCombat"] },
  { id: "real-life-civilian-job", label: "Civilian Job", moduleType: "realLife", skillChoices: ["art", "administration"] },
  { id: "real-life-combat-correspondent", label: "Combat Correspondent", moduleType: "realLife", skillChoices: ["perception"] },
  { id: "real-life-comstar-service", label: "ComStar Service", moduleType: "realLife", skillChoices: ["systemOps", "computers"] },
  { id: "real-life-covert-operations", label: "Covert Operations", moduleType: "realLife", skillChoices: ["stealth", "demolitions"] },
  { id: "real-life-explorer", label: "Explorer", moduleType: "realLife", skillChoices: ["navigation"] },
  { id: "real-life-guerrilla-insurgent", label: "Guerrilla Insurgent", moduleType: "realLife", skillChoices: ["stealth", "survival"] },
  { id: "real-life-merchant", label: "Merchant", moduleType: "realLife", skillChoices: ["negotiation"] },
  { id: "real-life-neer-do-well", label: "Ne'er-Do-Well", moduleType: "realLife", skillChoices: ["acting"] },
  { id: "real-life-organized-crime", label: "Organized Crime", moduleType: "realLife", skillChoices: ["intimidation"] },
  { id: "real-life-postgraduate-studies", label: "Postgraduate Studies", moduleType: "realLife", skillChoices: ["science", "technician"] },
  { id: "real-life-solaris-insider", label: "Solaris Insider", moduleType: "realLife", skillChoices: ["negotiation"] },
  { id: "real-life-solaris-vii-games", label: "Solaris VII Games", moduleType: "realLife", skillChoices: ["piloting", "gunnery"] },
  { id: "real-life-think-tank", label: "Think Tank", moduleType: "realLife", skillChoices: ["science", "tactics"] },
  {
    id: "real-life-tour-of-duty",
    label: "Tour of Duty",
    moduleType: "realLife",
    skillChoices: ["artillery", "gunnery", "meleeCombat", "piloting", "projectileWeapons", "firearms", "heavyWeapons"],
    requiresAny: [
      "higher-education-military-enlistment",
      "higher-education-military-academy",
      "higher-education-family-training",
      "higher-education-officer-candidate-school"
    ]
  },
  {
    id: "real-life-to-serve-and-protect",
    label: "To Serve and Protect",
    moduleType: "realLife",
    skillChoices: ["firearms", "intimidation"],
    requiresAny: ["higher-education-police-academy"]
  },
  {
    id: "real-life-vagabond",
    label: "Vagabond",
    moduleType: "realLife",
    skillChoices: Vi.map((i) => i.code).filter((i) => !MT.has(i)),
    excludesAny: [
      "higher-education-military-enlistment",
      "higher-education-military-academy",
      "higher-education-family-training",
      "higher-education-officer-candidate-school"
    ]
  }
], { strict: !1 }));
function IT() {
  const i = /* @__PURE__ */ new Map();
  for (const e of Vi) {
    const t = String(e.code ?? "").trim(), n = String(e.label ?? "").trim();
    t && (i.set(t.toLowerCase(), t), n && i.set(n.toLowerCase(), t));
  }
  return i;
}
function NT() {
  const i = /* @__PURE__ */ new Map();
  for (const [e, t] of Object.entries(Eu))
    i.set(e.toLowerCase(), e), i.set(t.toLowerCase(), e), i.set(`${t.toLowerCase()} pool`, e);
  return i;
}
function RT(i = []) {
  const e = Array.isArray(i) ? i.filter(Boolean) : [String(i ?? "").trim()].filter(Boolean), t = new Error(e[0] ?? "Invalid life module data.");
  return t.validationErrors = e, t;
}
function mg(i) {
  return Array.isArray(i) ? i : typeof i == "string" ? i.split(",").map((e) => e.trim()).filter(Boolean) : [];
}
function Gr(i) {
  return String(i ?? "").trim().toLowerCase().replace(/['\u2019]/g, "").replace(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, "");
}
function wo(i) {
  const e = String(i ?? "").trim();
  return ST.has(e) ? e : "";
}
function To(i) {
  const e = String(i ?? "").trim();
  return e ? TT.get(e.toLowerCase()) ?? "" : "";
}
function PT(i) {
  const e = String(i ?? "").trim();
  return e ? kT.get(e.toLowerCase()) ?? "" : "";
}
function DT(i, { strict: e = !1, errors: t = [], prefix: n = "Entry" } = {}) {
  const a = /* @__PURE__ */ new Set(), r = [];
  for (const s of mg(i)) {
    const o = To(s);
    if (!o) {
      e && t.push(`${n}: unknown skill "${s}".`);
      continue;
    }
    a.has(o) || (a.add(o), r.push(o));
  }
  return r;
}
function $d(i) {
  const e = /* @__PURE__ */ new Set();
  return mg(i).map(Gr).filter((t) => !t || e.has(t) ? !1 : (e.add(t), !0));
}
function Bd(i = [], e = /* @__PURE__ */ new Map()) {
  return i.map((t) => {
    var n;
    return ((n = e.get(t)) == null ? void 0 : n.label) ?? t;
  });
}
function Ba(i = {}) {
  return `${i.type}:${i.value}`;
}
function OT(i) {
  var e;
  return ((e = Lt(i)) == null ? void 0 : e.label) ?? i;
}
function fg(i) {
  return Eu[i] ?? i;
}
function LT(i) {
  return wT[i] ?? i;
}
function _T(i = {}, { includeTypePrefix: e = !1 } = {}) {
  const t = String((i == null ? void 0 : i.type) ?? "").trim(), n = String((i == null ? void 0 : i.value) ?? "").trim();
  if (!t || !n) return "";
  const a = t === "skill" ? OT(n) : `${fg(n)} Pool`;
  return e ? `${LT(t)}: ${a}` : a;
}
function Pr(i = {}, { includeBonusText: e = !1, includeTypePrefix: t = !1 } = {}) {
  const n = _T(i, { includeTypePrefix: t });
  return n ? e ? i.type === "skill" ? `+1 ${n} rolls` : `+1 ${n}` : n : "";
}
function xT(i) {
  const e = String(i ?? "").trim(), t = e.indexOf(":");
  return t < 0 ? null : {
    type: e.slice(0, t).trim(),
    value: e.slice(t + 1).trim()
  };
}
function $T(i, { strict: e = !1, errors: t = [], prefix: n = "Entry", grantLabel: a = "Bonus" } = {}) {
  const r = typeof i == "string" ? xT(i) : i, s = String((r == null ? void 0 : r.type) ?? "").trim(), o = String((r == null ? void 0 : r.value) ?? "").trim();
  if (!AT.has(s))
    return e && t.push(`${n} ${a}: unknown bonus type "${s || i}".`), null;
  const l = s === "skill" ? To(o) : PT(o);
  return l ? {
    type: s,
    value: l
  } : (e && t.push(`${n} ${a}: unknown ${s === "skill" ? "skill" : "edge pool"} "${o}".`), null);
}
function Wl(i, { strict: e = !1, errors: t = [], prefix: n = "Entry", grantLabel: a = "Bonus" } = {}) {
  const r = /* @__PURE__ */ new Set(), s = [], o = Array.isArray(i) ? i : [];
  for (const l of o) {
    const c = $T(l, { strict: e, errors: t, prefix: n, grantLabel: a });
    if (!c) continue;
    const u = Ba(c);
    r.has(u) || (r.add(u), s.push(c));
  }
  return s;
}
function pg(i, { strict: e = !1, errors: t = [], prefix: n = "Entry" } = {}) {
  const a = DT(i, { strict: e, errors: t, prefix: n });
  return a.length ? [{
    id: "skill",
    label: "",
    choices: a.map((r) => ({ type: "skill", value: r }))
  }] : [];
}
function BT(i, { strict: e = !1, errors: t = [], prefix: n = "Entry" } = {}) {
  const a = String(i ?? "").trim();
  return a ? a.split(";").map((s) => s.trim()).filter(Boolean).map((s, o) => {
    const l = `Bonus ${o + 1}`, c = Wl(
      s.split("|").map((u) => u.trim()).filter(Boolean),
      { strict: e, errors: t, prefix: n, grantLabel: l }
    );
    return {
      id: `grant-${o + 1}`,
      label: "",
      choices: c
    };
  }).filter((s) => s.choices.length) : [];
}
function gg(i, e = "grant") {
  return Gr(i) || e;
}
function zT(i, e, { strict: t = !1, errors: n = [], prefix: a = "Entry" } = {}) {
  const r = `grant-${e + 1}`, s = `Bonus ${e + 1}`;
  if (typeof i == "string") {
    const u = Wl(
      i.split("|").map((d) => d.trim()).filter(Boolean),
      { strict: t, errors: n, prefix: a, grantLabel: s }
    );
    return u.length ? { id: r, label: "", choices: u } : null;
  }
  const o = gg(i == null ? void 0 : i.id, r), l = String((i == null ? void 0 : i.label) ?? "").trim(), c = Wl(i == null ? void 0 : i.choices, { strict: t, errors: n, prefix: a, grantLabel: s });
  return c.length ? { id: o, label: l, choices: c } : (t && n.push(`${a} ${s}: define at least one bonus choice.`), null);
}
function FT(i, { strict: e = !1, errors: t = [], prefix: n = "Entry" } = {}) {
  if (Array.isArray(i)) {
    if (i.every((r) => typeof r == "string" && !String(r).includes(":")))
      return pg(i, { strict: e, errors: t, prefix: n });
    const a = /* @__PURE__ */ new Set();
    return i.map((r, s) => zT(r, s, { strict: e, errors: t, prefix: n })).filter((r) => r ? a.has(r.id) ? (e && t.push(`${n}: duplicate bonus id "${r.id}".`), !1) : (a.add(r.id), !0) : !1);
  }
  return typeof i == "string" ? BT(i, { strict: e, errors: t, prefix: n }) : [];
}
function HT(i = []) {
  return (Array.isArray(i) ? i : []).map((e) => (Array.isArray(e == null ? void 0 : e.choices) ? e.choices : []).map((n) => `${n.type}:${n.value}`).join("|")).filter(Boolean).join("; ");
}
function Iu() {
  return foundry.utils.deepClone(ET);
}
function za(i) {
  return bT[i] ?? (String(i ?? "").trim() || "Life Module");
}
function hg() {
  return Ao.map((i) => ({
    value: i.moduleType,
    label: i.label
  }));
}
function ma(i = [], { strict: e = !1 } = {}) {
  const t = Array.isArray(i) ? i : [], n = [], a = /* @__PURE__ */ new Set(), r = t.map((o, l) => {
    const c = `Entry ${l + 1}`, u = String((o == null ? void 0 : o.label) ?? "").trim(), d = Gr((o == null ? void 0 : o.id) ?? u), m = wo(o == null ? void 0 : o.moduleType), f = (o == null ? void 0 : o.grants) != null ? FT(o.grants, { strict: e, errors: n, prefix: c }) : pg(o == null ? void 0 : o.skillChoices, { strict: e, errors: n, prefix: c }), p = $d(o == null ? void 0 : o.requiresAny), g = $d(o == null ? void 0 : o.excludesAny);
    return !d && e && n.push(`${c}: id cannot be blank.`), !u && e && n.push(`${c}: label cannot be blank.`), !m && e && n.push(`${c}: choose a valid module type.`), !f.length && e && n.push(`${c}: choose at least one bonus.`), d && a.has(d) && e && n.push(`${c}: duplicate id "${d}".`), d && a.add(d), {
      id: d,
      label: u,
      moduleType: m,
      grants: f,
      requiresAny: p,
      excludesAny: g
    };
  }), s = new Map(r.map((o) => [o.id, o]));
  for (const o of r) {
    for (const l of o.requiresAny)
      l === o.id && e && n.push(`${o.label || o.id}: cannot require itself.`), !s.has(l) && e && n.push(`${o.label || o.id}: unknown requirement "${l}".`);
    for (const l of o.excludesAny)
      l === o.id && e && n.push(`${o.label || o.id}: cannot exclude itself.`), !s.has(l) && e && n.push(`${o.label || o.id}: unknown exclusion "${l}".`);
  }
  if (e && n.length) throw RT(n);
  return r.filter((o) => o.id && o.label && o.moduleType && o.grants.length).map((o) => ({
    id: o.id,
    label: o.label,
    moduleType: o.moduleType,
    grants: o.grants.map((l) => ({
      id: l.id,
      label: l.label,
      choices: l.choices.map((c) => ({
        type: c.type,
        value: c.value
      }))
    })),
    requiresAny: [...o.requiresAny],
    excludesAny: [...o.excludesAny]
  }));
}
function yg(i = []) {
  const e = new Map(Iu().map((r) => [r.id, r])), t = ma(i, { strict: !1 }), n = [...t], a = new Set(t.map((r) => r.id));
  for (const [r, s] of e.entries())
    a.has(r) || n.push(foundry.utils.deepClone(s));
  return n;
}
async function UT() {
  var i, e, t;
  try {
    if (!((t = (e = (i = game == null ? void 0 : game.settings) == null ? void 0 : i.settings) == null ? void 0 : e.has) != null && t.call(e, `${M}.${Na}`))) return;
    const n = game.settings.get(M, Na), a = yg(n);
    JSON.stringify(n) !== JSON.stringify(a) && await game.settings.set(M, Na, a);
  } catch {
  }
}
function jT() {
  var i, e, t;
  try {
    if ((t = (e = (i = game == null ? void 0 : game.settings) == null ? void 0 : i.settings) == null ? void 0 : e.has) != null && t.call(e, `${M}.${Na}`))
      return yg(game.settings.get(M, Na));
  } catch {
  }
  return Iu();
}
function ko() {
  return ma(jT(), { strict: !1 });
}
function En(i) {
  const e = Gr(i);
  return e ? ko().find((t) => t.id === e) ?? null : null;
}
function Nu(i) {
  const e = wo(i);
  return ko().filter((t) => t.moduleType === e);
}
function bg(i) {
  return !i || typeof i != "object" || Array.isArray(i) ? {} : Object.fromEntries(
    Object.entries(i).map(([e, t]) => [gg(e, ""), String(t ?? "").trim()]).filter(([e]) => !!e)
  );
}
function Sg(i, e = "", { legacySelectedSkill: t = "" } = {}) {
  const n = new Set((Array.isArray(i == null ? void 0 : i.choices) ? i.choices : []).map(Ba)), a = String(e ?? "").trim();
  if (n.has(a)) return a;
  if (t) {
    const r = To(t), s = r ? `skill:${r}` : "";
    if (s && n.has(s)) return s;
  }
  return n.size === 1 ? Array.from(n)[0] : "";
}
function Ag(i, e = {}, { legacySelectedSkill: t = "" } = {}) {
  const n = Array.isArray(i == null ? void 0 : i.grants) ? i.grants : [], a = bg(e);
  return Object.fromEntries(
    n.map((r) => [
      r.id,
      Sg(r, a[r.id], { legacySelectedSkill: t })
    ])
  );
}
function Mo(i, e = {}, { legacySelectedSkill: t = "" } = {}) {
  const n = Array.isArray(i == null ? void 0 : i.grants) ? i.grants : [], a = Ag(i, e, { legacySelectedSkill: t });
  return n.map((r, s) => {
    const o = Sg(r, a[r.id], { legacySelectedSkill: t }), l = (Array.isArray(r.choices) ? r.choices : []).find((c) => Ba(c) === o) ?? null;
    return {
      id: r.id,
      index: s,
      label: String((r == null ? void 0 : r.label) ?? "").trim() || (n.length > 1 ? `Bonus ${s + 1}` : "Granted Bonus"),
      selectedKey: o,
      choice: l,
      isResolved: !!l,
      requiresSelection: (Array.isArray(r == null ? void 0 : r.choices) ? r.choices : []).length > 1
    };
  });
}
function KT(i, e = {}, { legacySelectedSkill: t = "" } = {}) {
  var n;
  return ((n = Mo(i, e, { legacySelectedSkill: t }).map((a) => a.choice).find((a) => (a == null ? void 0 : a.type) === "skill")) == null ? void 0 : n.value) ?? "";
}
function Dr(i = {}) {
  const e = foundry.utils.deepClone(i ?? {}), t = Gr(e.catalogId), n = t ? En(t) : null, a = wo(e.moduleType || (n == null ? void 0 : n.moduleType)), r = n ? Ag(n, e.selectedGrants, { legacySelectedSkill: e.selectedSkill }) : bg(e.selectedGrants);
  return e.moduleType = a, e.catalogId = t, e.selectedGrants = r, e.selectedSkill = n ? KT(n, r, { legacySelectedSkill: e.selectedSkill }) : To(e.selectedSkill), e;
}
function wg(i, e = {}, { legacySelectedSkill: t = "" } = {}) {
  return Mo(i, e, { legacySelectedSkill: t }).map((n) => {
    var l, c;
    const a = Array.isArray((c = (l = i == null ? void 0 : i.grants) == null ? void 0 : l[n.index]) == null ? void 0 : c.choices) ? i.grants[n.index].choices : [], r = new Set(a.map((u) => u.type)).size > 1, s = a.map((u) => ({
      value: Ba(u),
      label: Pr(u, { includeTypePrefix: r }),
      selected: Ba(u) === n.selectedKey
    })), o = s.length === 1 ? {
      value: s[0].value,
      label: s[0].label,
      displayLabel: Pr(a[0], { includeBonusText: !0 })
    } : null;
    return {
      id: n.id,
      label: n.label,
      selectionPath: `system.selectedGrants.${n.id}`,
      selectedKey: n.selectedKey,
      options: s,
      singleOption: o,
      hasMultipleChoices: s.length > 1
    };
  });
}
function GT(i, e) {
  return i.isDuplicate ? `Duplicate ${za(i.moduleType)} slot item.` : i.catalog ? i.unresolvedGrantCount > 0 ? "Choose valid bonus options." : i.excludedBy.length ? `Blocked by ${Bd(i.excludedBy, e).join(", ")}.` : i.requiresAny.length && !i.matchedRequirementIds.length ? `Requires ${Bd(i.requiresAny, e).join(" or ")}.` : "" : "Catalog entry is missing or unlinked.";
}
function WT(i, e = [], t = {}) {
  var a, r, s;
  if (!i || !Array.isArray(e) || !e.length) return [];
  const n = Math.max(0, Number(((s = (r = (a = i.system) == null ? void 0 : a.attributes) == null ? void 0 : r.edge) == null ? void 0 : s.value) ?? 0));
  return e.filter((o) => (o == null ? void 0 : o.type) === "edgePool").map((o) => {
    var p, g, h, y;
    const l = String(o.value ?? "").trim(), c = fg(l), u = Math.max(0, Number(((y = (h = (g = (p = i.system) == null ? void 0 : p.counters) == null ? void 0 : g.edgePools) == null ? void 0 : h[l]) == null ? void 0 : y.rating) ?? 0)), d = Math.max(0, Number((t == null ? void 0 : t[l]) ?? 0)), m = Math.max(0, u + d - n);
    return m ? `${c} Pool bonus loses ${m} ${m === 1 ? "point" : "points"} to the Edge cap.` : "";
  }).filter(Boolean);
}
function Nn(i) {
  var m;
  const e = ko(), t = new Map(e.map((f) => [f.id, f])), n = Array.from((i == null ? void 0 : i.items) ?? []).filter((f) => f.type === A.itemType.lifeModule), a = /* @__PURE__ */ new Map();
  for (const f of n) {
    const p = wo((m = f.system) == null ? void 0 : m.moduleType);
    !p || a.has(p) || a.set(p, f.id);
  }
  const r = n.map((f) => {
    var T;
    const p = Dr(f.system ?? {}), g = t.get(p.catalogId) ?? null, h = p.moduleType || (g == null ? void 0 : g.moduleType) || "", y = g ? Mo(g, p.selectedGrants, { legacySelectedSkill: p.selectedSkill }) : [], b = y.map((k) => k.choice).filter(Boolean), S = ((T = b.find((k) => k.type === "skill")) == null ? void 0 : T.value) ?? "", w = S ? Lt(S) : null;
    return {
      item: f,
      itemId: f.id,
      moduleType: h,
      catalogId: (g == null ? void 0 : g.id) ?? p.catalogId,
      catalog: g,
      label: (g == null ? void 0 : g.label) ?? f.name,
      selectedGrants: p.selectedGrants,
      resolvedGrants: y,
      unresolvedGrantCount: y.filter((k) => !k.isResolved).length,
      selectedChoices: b,
      selectedChoiceLabels: b.map((k) => Pr(k, { includeBonusText: !0 })),
      selectedSkill: S,
      selectedSkillLabel: (w == null ? void 0 : w.label) ?? S,
      requiresAny: [...(g == null ? void 0 : g.requiresAny) ?? []],
      excludesAny: [...(g == null ? void 0 : g.excludesAny) ?? []],
      matchedRequirementIds: [],
      excludedBy: [],
      isDuplicate: h ? a.get(h) !== f.id : !1,
      isActive: !1,
      inactiveReason: "",
      bonus: 0
    };
  }), s = /* @__PURE__ */ new Map();
  for (const f of r) {
    if (!f.catalogId) continue;
    const p = s.get(f.catalogId) ?? [];
    p.push(f), s.set(f.catalogId, p);
  }
  for (const f of r)
    f.excludedBy = f.excludesAny.filter((p) => (s.get(p) ?? []).length > 0);
  let o = !0;
  for (; o; ) {
    o = !1;
    for (const f of r) {
      const p = f.requiresAny.filter(
        (h) => (s.get(h) ?? []).some((y) => y.isActive)
      ), g = !f.isDuplicate && !!f.catalog && f.unresolvedGrantCount === 0 && f.excludedBy.length === 0 && (f.requiresAny.length === 0 || p.length > 0);
      f.isActive !== g && (f.isActive = g, o = !0), f.matchedRequirementIds.join("|") !== p.join("|") && (f.matchedRequirementIds = p);
    }
  }
  const l = Object.fromEntries(Vi.map((f) => [f.code, 0])), c = Object.fromEntries(dg.map((f) => [f, 0])), u = /* @__PURE__ */ new Map();
  for (const f of r) {
    const p = f.isActive ? f.selectedChoices : [], g = p.filter((y) => y.type === "skill"), h = p.filter((y) => y.type === "edgePool");
    f.bonus = g.length;
    for (const y of g)
      l[y.value] = Number(l[y.value] ?? 0) + 1;
    for (const y of h)
      c[y.value] = Number(c[y.value] ?? 0) + 1;
    f.inactiveReason = f.isActive ? "" : GT(f, t), u.set(f.itemId, f);
  }
  for (const f of r)
    f.warningLabels = f.isActive ? WT(i, f.selectedChoices, c) : [];
  const d = Ao.map((f) => {
    const p = r.find((g) => g.moduleType === f.moduleType && !g.isDuplicate) ?? null;
    return {
      moduleType: f.moduleType,
      label: f.label,
      availableEntries: e.filter((g) => g.moduleType === f.moduleType),
      state: p
    };
  });
  return {
    catalog: e,
    states: r,
    stateByItemId: u,
    slotStates: d,
    bonusBySkill: l,
    bonusByEdgePool: c
  };
}
function qT(i = {}) {
  var t, n, a;
  const e = String((i == null ? void 0 : i.intent) ?? "").trim();
  return e === "skill" ? String(((t = i == null ? void 0 : i.data) == null ? void 0 : t.skillKey) ?? "").trim() : e === "attack" ? String(((a = (n = i == null ? void 0 : i.attack) == null ? void 0 : n.skill) == null ? void 0 : a.code) ?? "").trim() : "";
}
function VT({ actor: i, resolved: e } = {}) {
  const t = qT(e);
  return !i || !t ? [] : Nn(i).states.flatMap(
    (n) => n.isActive ? n.selectedChoices.filter((a) => a.type === "skill" && a.value === t).map((a) => ({
      id: `life-module:${n.itemId}:${Ba(a)}`,
      label: n.label,
      value: 1,
      source: "Life Module",
      tooltip: `${n.label}: +1 to ${Pr(a)} rolls`
    })) : []
  );
}
function Fa(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? Math.trunc(t) : e;
}
function zd(i, e = 0) {
  return Math.max(0, Fa(i, e));
}
function YT(i, e = "") {
  return String(i ?? "").trim() || e;
}
function JT(i = {}) {
  const e = i && typeof i == "object" ? i : {}, t = !!e.enabled;
  return {
    enabled: t,
    movement: zd(e.movement, 0),
    heat: zd(e.heat, 0),
    attackRatingBonus: Fa(e.attackRatingBonus, 0),
    defenseRatingBonus: Fa(e.defenseRatingBonus, 0),
    dfaEnabled: !!(e.dfaEnabled ?? t)
  };
}
function QT(i = {}) {
  const e = i && typeof i == "object" ? i : {};
  return {
    diceModifier: Fa(e.diceModifier, 0),
    targetNumberModifier: Fa(e.targetNumberModifier, 0)
  };
}
function Ws(i = {}) {
  const e = i && typeof i == "object" ? i : {}, t = e.mobility && typeof e.mobility == "object" ? e.mobility : {}, n = e.targeting && typeof e.targeting == "object" ? e.targeting : {};
  return {
    ...e,
    category: YT(e.category, "special"),
    level: Math.max(1, Fa(e.level, 1)),
    mobility: {
      ...t,
      jumping: JT(t.jumping)
    },
    targeting: {
      ...n,
      clustering: QT(n.clustering)
    }
  };
}
function XT(i = {}) {
  const e = (i == null ? void 0 : i.system) ?? i;
  return Ws(e).mobility.jumping;
}
function ZT(i = {}) {
  const e = (i == null ? void 0 : i.system) ?? i;
  return Ws(e).targeting.clustering;
}
const e0 = {
  types: [
    "character",
    "npc",
    "vehicle",
    "battlemech"
  ],
  templates: {
    description: {
      ownerId: "",
      description: "",
      gmnotes: "",
      favorites: [],
      state: {
        physical: {
          value: 0,
          max: 0
        },
        fatigue: {
          value: 0,
          max: 0
        }
      }
    },
    "matrix-monitor": {
      monitors: {
        matrix: {
          canMark: !0,
          marks: [],
          value: 0,
          max: 6,
          resistance: {
            default: 0,
            byType: {}
          }
        }
      }
    },
    "attribute-reflexes": {
      attributes: {
        reflexes: {
          value: 1
        }
      }
    },
    "attribute-strength": {
      attributes: {
        strength: {
          value: 1
        }
      }
    },
    "attribute-willpower": {
      attributes: {
        willpower: {
          value: 1
        }
      }
    },
    "attribute-charisma": {
      attributes: {
        charisma: {
          value: 1
        }
      }
    },
    "attribute-intelligence": {
      attributes: {
        intelligence: {
          value: 1
        }
      }
    },
    "attribute-edge": {
      attributes: {
        edge: {
          value: 1
        }
      },
      counters: {
        edgePools: {
          grit: {
            value: null
          },
          insight: {
            value: null
          },
          rumor: {
            value: null
          },
          legend: {
            value: null
          },
          credibility: {
            value: null
          },
          chaos: {
            value: null
          }
        }
      }
    },
    "attribute-autopilot": {
      attributes: {
        autopilot: {
          value: 6
        }
      }
    },
    "attribute-handling": {
      attributes: {
        handling: {
          value: 0
        }
      }
    },
    "mwd-base": {
      mwd: {
        unitType: "vehicle",
        model: "",
        cbillCost: 0,
        heat: {
          current: 0,
          safeMax: 1,
          hardMax: 10,
          coolingImpaired: !1
        },
        shock: {
          value: 0
        },
        reliabilitySpendable: {
          value: 0
        },
        locations: {},
        crits: [],
        crew: {
          count: 1,
          effectiveCount: 1,
          injuryLevel: 0,
          bailedOut: !1
        },
        status: {
          state: "operational",
          reasons: []
        },
        config: {
          critTargetNumber: 8,
          critOnSnakeEyes: !0,
          maxLocationStress: 3,
          heatBands: {
            safe: 1,
            runningHot: 2,
            overheated: 3,
            shutdown: 4
          }
        }
      }
    },
    "mwd-vehicle": {
      templates: [
        "mwd-base"
      ],
      attributes: {
        handling: {
          value: 3
        },
        system: {
          value: 3
        },
        chassis: {
          value: 3
        },
        reliability: {
          value: 3
        }
      },
      mwd: {
        hardpoints: [],
        locations: {
          front: {
            enabled: !0,
            stress: 0,
            condition: 0,
            tags: [
              "weaponGroup",
              "motiveSystem"
            ],
            destroyed: !1
          },
          side: {
            enabled: !0,
            stress: 0,
            condition: 0,
            tags: [
              "weaponGroup",
              "motiveSystem"
            ],
            destroyed: !1
          },
          rear: {
            enabled: !0,
            stress: 0,
            condition: 0,
            tags: [
              "weaponGroup",
              "motiveSystem",
              "ammoStore"
            ],
            destroyed: !1
          },
          turret: {
            enabled: !0,
            stress: 0,
            condition: 0,
            tags: [
              "turret",
              "weaponGroup"
            ],
            destroyed: !1
          },
          rotor: {
            enabled: !1,
            stress: 0,
            condition: 0,
            tags: [
              "rotor"
            ],
            destroyed: !1
          },
          core: {
            enabled: !0,
            stress: 0,
            condition: 0,
            tags: [
              "crewCompartment",
              "engine",
              "ammoStore"
            ],
            destroyed: !1
          }
        }
      },
      movement: {
        ground: 0,
        flight: 0
      }
    },
    "mwd-battlemech": {
      templates: [
        "mwd-base"
      ],
      attributes: {
        handling: {
          value: 4
        },
        system: {
          value: 3
        },
        chassis: {
          value: 4
        },
        reliability: {
          value: 3
        }
      },
      mwd: {
        unitType: "mech",
        locations: {
          head: {
            enabled: !0,
            stress: 0,
            condition: 0,
            tags: [
              "cockpit",
              "sensor"
            ],
            destroyed: !1
          },
          torso: {
            enabled: !0,
            stress: 0,
            condition: 0,
            tags: [
              "weaponGroup",
              "engine",
              "gyro",
              "ammoStore"
            ],
            destroyed: !1
          },
          arms: {
            enabled: !0,
            stress: 0,
            condition: 0,
            tags: [
              "weaponGroup"
            ],
            destroyed: !1
          },
          legs: {
            enabled: !0,
            stress: 0,
            condition: 0,
            tags: [
              "motiveSystem"
            ],
            destroyed: !1
          }
        },
        crew: {
          count: 1,
          effectiveCount: 1,
          injuryLevel: 0,
          bailedOut: !1
        },
        heat: {
          current: 0,
          max: 10,
          thresholds: {
            runningHot: 2,
            overheated: 3,
            shutdown: 4
          }
        },
        chassis: "",
        tonnage: 0,
        weightClass: "medium",
        hardpoints: [],
        weaponGroups: [],
        primarySlot: {
          mode: "normal",
          allowedWeaponIds: [],
          typeRestriction: ""
        },
        melee: {
          baseProfile: {
            name: "Unarmed",
            damage: "",
            notes: ""
          },
          maxWeapons: 0,
          allowedLocations: []
        }
      }
    },
    "machine-movement-battlemech": {
      movement: {
        ground: 0,
        flight: 0
      }
    }
  },
  character: {
    templates: [
      "description",
      "counters",
      "ownership",
      "attribute-reflexes",
      "attribute-strength",
      "attribute-willpower",
      "attribute-intelligence",
      "attribute-charisma",
      "attribute-edge"
    ],
    monitors: {
      physical: {
        value: 1,
        max: 10,
        resistance: {
          default: 0,
          byType: {}
        }
      },
      fatigue: {
        value: 1,
        max: 10,
        resistance: {
          default: 0,
          byType: {}
        }
      },
      armor: {
        label: "Armor",
        value: 9,
        max: 9,
        effect: "",
        resistance: ""
      }
    },
    counters: {
      xp: {
        value: 0,
        total: 0
      },
      edgePools: {
        grit: {
          value: 2,
          rating: 2
        },
        insight: {
          value: 0,
          rating: 1
        },
        rumor: {
          value: 1,
          rating: 1
        },
        legend: {
          value: 0,
          rating: 1
        },
        credibility: {
          value: 1,
          rating: 1
        },
        chaos: {
          value: 1,
          rating: 1
        }
      }
    },
    prototypeToken: {
      actorLink: !0,
      disposition: 1,
      displayName: 20,
      displayBars: 40
    },
    style: "",
    speed: 12,
    keywords: [],
    dispositions: [],
    cues: [],
    burn: {
      value: 0,
      overloaded: !1
    },
    biography: {
      faction: "",
      age: 0,
      rank: "",
      height: 0,
      weight: 0,
      history: "",
      experienceLevel: "green"
    }
  },
  npc: {
    templates: [
      "description",
      "ownership"
    ],
    attributes: {
      strength: {
        value: 1
      },
      reflexes: {
        value: 1
      },
      intelligence: {
        value: 1
      },
      willpower: {
        value: 1
      },
      charisma: {
        value: 1
      },
      edge: {
        value: 1
      }
    },
    monitors: {
      physical: {
        value: 0,
        max: 10,
        resistance: {
          default: 0,
          byType: {}
        }
      },
      fatigue: {
        value: 0,
        max: 10,
        resistance: {
          default: 0,
          byType: {}
        }
      },
      armor: {
        label: "Armor",
        value: 0,
        max: 9,
        effect: "",
        resistance: ""
      }
    },
    role: "",
    biography: "",
    style: "sra-enhanced"
  },
  vehicle: {
    templates: [
      "description",
      "matrix-monitor",
      "mwd-vehicle"
    ],
    attributes: {},
    monitors: {
      structure: {
        value: 0,
        max: 15,
        resistance: {
          default: 2,
          byType: {}
        }
      },
      armor: {
        value: 0,
        max: 12,
        resistance: {
          default: 1,
          byType: {}
        }
      }
    },
    weaponGroups: [],
    meleeProfiles: [],
    skills: {},
    moves: 0,
    attacks: 0,
    stealth: 0,
    category: "",
    skill: "piloting",
    passengers: 4,
    pilot: {
      uuid: ""
    },
    crew: ""
  },
  battlemech: {
    templates: [
      "description",
      "mwd-battlemech"
    ],
    attributes: {},
    monitors: {
      structure: {
        value: 0,
        max: 18,
        resistance: {
          default: 1,
          byType: {}
        }
      },
      armor: {
        value: 0,
        max: 15,
        resistance: {
          default: 1,
          byType: {}
        }
      },
      heat: {
        value: 0,
        max: 10,
        resistance: {
          default: 0,
          byType: {}
        }
      }
    },
    hybrid: {
      heat: {
        dissipation: 1
      },
      criticals: {
        value: 0,
        max: 4,
        notes: ""
      },
      locations: {
        front: "",
        sides: "",
        rear: "",
        core: ""
      }
    },
    weaponGroups: [],
    meleeProfiles: [],
    skills: {},
    moves: 0,
    attacks: 0,
    stealth: 0,
    category: "mech",
    skill: "gunnery",
    passengers: 1,
    pilot: {
      uuid: ""
    },
    crew: ""
  }
}, t0 = {
  types: [
    "contact",
    "gear",
    "consumable",
    "quality",
    "assetModule",
    "skill",
    "lifeModule",
    "mechWeapon",
    "personalWeapon",
    "armor"
  ],
  templates: {
    modifiers: {
      modifiers: []
    },
    inactive: {
      inactive: !1
    },
    references: {
      sourceReference: "",
      description: "",
      gmnotes: ""
    }
  },
  skill: {
    templates: [
      "inactive",
      "references"
    ],
    code: "",
    attribute: "knowledge",
    value: 0,
    hasDrain: !1,
    hasConvergence: !1,
    isSocial: !1
  },
  quality: {
    templates: [
      "modifiers",
      "inactive",
      "references"
    ],
    positive: !0,
    category: "positive",
    tier: "minor",
    tags: [],
    activation: "passive",
    effects: [],
    prerequisites: [],
    limits: {
      perActivation: 0,
      perRound: 0,
      perScene: 0
    }
  },
  assetModule: {
    templates: [
      "modifiers",
      "inactive",
      "references"
    ],
    category: "special",
    level: 1,
    mobility: {
      jumping: {
        enabled: !1,
        movement: 0,
        heat: 0,
        attackRatingBonus: 0,
        defenseRatingBonus: 0,
        dfaEnabled: !1
      }
    }
  },
  lifeModule: {
    templates: [
      "inactive",
      "references"
    ],
    moduleType: "faction"
  },
  mechWeapon: {
    templates: [
      "modifiers",
      "inactive",
      "references"
    ],
    category: "ranged",
    weaponCategory: "ranged",
    skill: "gunnery",
    size: "small",
    damage: 0,
    ap: 0,
    damageType: "penetrating",
    attackRatingBand: {
      close: 0,
      near: 0,
      far: 0,
      extreme: 0
    },
    payloads: [],
    selectedPayloadId: "",
    consumptionSources: [],
    heat: 0,
    volatile: !1,
    area: "none",
    notes: "",
    range: {
      max: "close",
      close: 0,
      near: 0,
      far: 0,
      extreme: 0
    }
  },
  personalWeapon: {
    templates: [
      "modifiers",
      "inactive",
      "references"
    ],
    equipped: !1,
    isPrimary: !1,
    category: "ranged",
    skill: "firearms",
    damage: 0,
    ap: 0,
    damageType: "penetrating",
    attackRatingBand: {
      close: 0,
      near: 0,
      far: 0,
      extreme: 0
    },
    range: {
      max: "extreme",
      close: 5,
      near: 26,
      far: 62,
      extreme: 120
    },
    standardTraits: [],
    ammo: {
      current: 0,
      max: 0,
      consumePerAttack: 1,
      activeTypeId: "",
      types: []
    },
    traits: [],
    notes: ""
  },
  armor: {
    templates: [
      "modifiers",
      "inactive",
      "references"
    ],
    equipped: !1,
    isPrimary: !1,
    rating: 0,
    defenseBonus: 0,
    mitigationByType: {
      penetrating: 0,
      concussive: 0,
      energy: 0,
      thermal: 0,
      electrical: 0
    },
    durability: {
      current: 0,
      max: 0
    },
    standardTraits: [],
    traitState: {
      reinforced: {
        current: 0,
        max: 0
      }
    },
    tags: [],
    traits: [],
    notes: ""
  },
  gear: {
    templates: [
      "inactive",
      "references"
    ],
    quantity: 1,
    rating: 0,
    category: "",
    tags: []
  },
  consumable: {
    templates: [
      "inactive",
      "references"
    ],
    quantity: 1,
    rating: 0,
    category: "ammo",
    tags: []
  },
  contact: {
    templates: [
      "inactive",
      "references"
    ]
  }
}, Co = {
  Actor: e0,
  Item: t0
}, Fd = Object.freeze({
  Actor: /* @__PURE__ */ new Set(["prototypeToken"]),
  Item: /* @__PURE__ */ new Set()
});
function Or(i) {
  return !!i && typeof i == "object" && !Array.isArray(i);
}
function Ha(i) {
  return typeof structuredClone == "function" ? structuredClone(i) : JSON.parse(JSON.stringify(i));
}
function Lr(i = {}, e = {}) {
  const t = Ha(i);
  for (const [n, a] of Object.entries(e ?? {})) {
    if (Or(a) && Or(t[n])) {
      t[n] = Lr(t[n], a);
      continue;
    }
    t[n] = Ha(a);
  }
  return t;
}
function Tg(i = "", e = Co) {
  const t = e == null ? void 0 : e[i];
  return Or(t) ? t : {};
}
function kg(i = Co, e = "", t = "", n = /* @__PURE__ */ new Set()) {
  var c;
  const a = String(t ?? "").trim();
  if (!a || n.has(a)) return {};
  const r = Tg(e, i), s = (c = r == null ? void 0 : r.templates) == null ? void 0 : c[a];
  if (!Or(s)) return {};
  n.add(a);
  let o = {};
  for (const u of Array.from(s.templates ?? []))
    o = Lr(
      o,
      kg(i, e, u, n)
    );
  const l = Ha(s);
  return delete l.templates, Lr(o, l);
}
function i0(i = Co, e = "", t = "") {
  const n = String(t ?? "").trim();
  if (!n) return {};
  const a = Tg(e, i), r = a == null ? void 0 : a[n];
  if (!Or(r)) return {};
  let s = {};
  for (const l of Array.from(r.templates ?? []))
    s = Lr(
      s,
      kg(i, e, l)
    );
  const o = Ha(r);
  return delete o.templates, Lr(s, o);
}
function n0(i = "", e = "", t = Co) {
  const n = i0(t, i, e), a = Fd[i] ?? Fd.Item, r = { system: {} };
  for (const [s, o] of Object.entries(n))
    a.has(s) ? r[s] = Ha(o) : r.system[s] = Ha(o);
  return r;
}
async function Mg(i = "", e = "") {
  return n0(i, e);
}
const Cg = Object.freeze({
  weapon: A.itemType.personalWeapon,
  vehicleWeapon: A.itemType.mechWeapon,
  shadowamp: A.itemType.assetModule
}), a0 = Object.freeze({
  contact: "systems/mwd/img/default/mystery-man.svg",
  gear: "systems/mwd/img/default/Default_Gear.svg",
  consumable: "systems/mwd/img/default/Default_Gear.svg",
  quality: "systems/mwd/img/default/card-joker.svg",
  assetModule: "systems/mwd/img/default/upgrade.svg",
  skill: "systems/mwd/img/default/Default_Skill.svg",
  lifeModule: "systems/mwd/img/default/book.svg",
  mechWeapon: "systems/mwd/img/icons/systems/upgrades/grenade_launcher_underbarrel.svg",
  personalWeapon: "systems/mwd/img/colt-m1911.svg",
  armor: "systems/mwd/img/default/Default_Armor.svg"
});
function vg(i) {
  return Cg[i] ?? i;
}
function r0(i) {
  return a0[vg(i)];
}
function s0(i) {
  return Object.prototype.hasOwnProperty.call(Cg, i);
}
const ra = Object.freeze(["close", "near", "far", "extreme"]), Hd = Object.freeze({
  none: { targets: 1, adjust: [0] },
  shotgun: { targets: 2, adjust: [0, -2] },
  circle: { targets: void 0 },
  cone: { targets: void 0 },
  rect: { targets: void 0 },
  ray: { targets: void 0 }
});
function qt() {
  return foundry.data.operators.ForcedDeletion;
}
function o0(i, e) {
  const t = String(e ?? "").split(".").map((a) => a.trim()).filter(Boolean);
  if (!i || typeof i != "object" || t.length < 2) return i;
  let n = i;
  for (let a = 0; a < t.length - 1; a += 1) {
    const r = t[a], s = n == null ? void 0 : n[r];
    (!s || typeof s != "object" || Array.isArray(s)) && (n[r] = {}), n = n[r];
  }
  return i;
}
function l0(i, e) {
  const t = String(e ?? "").trim();
  if (!i || typeof i != "object" || !t) return !1;
  if (t.startsWith("areaEffect.hazard.")) {
    (!i.areaEffect || typeof i.areaEffect != "object" || Array.isArray(i.areaEffect)) && (i.areaEffect = {});
    const n = String(i.areaEffect.kind ?? "").trim().toLowerCase();
    if (n && n !== "persistent")
      return !1;
    i.areaEffect.kind = "persistent";
  }
  return o0(i, t), !0;
}
function Xa(i) {
  return ja(i);
}
function Ud(i = {}) {
  const e = uf({
    traits: i.traits,
    keywords: i.keywords,
    report: Kc(),
    path: "system.traits"
  });
  return {
    traits: e.traits,
    keywords: e.keywords
  };
}
function Eg(i) {
  return i === "long" ? "extreme" : i === "short" ? "close" : i === "medium" ? "near" : ra.includes(i) ? i : "near";
}
function Sn(i) {
  const e = vu(i);
  return e.max = Eg(e.max ?? (i == null ? void 0 : i.max) ?? "extreme"), e;
}
function hr(i) {
  return String(i ?? "").trim().toLowerCase() === "melee" ? "melee" : "ranged";
}
function is(i, e = "") {
  return hr(i) === "melee" ? "meleeCombat" : "gunnery";
}
function Vo(i, e) {
  return hr(e) === "melee" ? Sn({
    max: "close",
    close: 0,
    near: 0,
    far: 0,
    extreme: 0
  }) : Sn(i);
}
function Za(i) {
  return {
    close: Number((i == null ? void 0 : i.close) ?? (i == null ? void 0 : i.short) ?? 0) || 0,
    near: Number((i == null ? void 0 : i.near) ?? (i == null ? void 0 : i.medium) ?? 0) || 0,
    far: Number((i == null ? void 0 : i.far) ?? (i == null ? void 0 : i.long) ?? 0) || 0,
    extreme: Number((i == null ? void 0 : i.extreme) ?? 0) || 0
  };
}
function jd(i, e = 1) {
  const t = Number(i);
  return Number.isFinite(t) ? Math.max(0, Math.trunc(t)) : Math.max(0, Math.trunc(Number(e) || 0));
}
function Kd(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? Math.max(0, Math.trunc(t)) : Math.max(0, Math.trunc(Number(e) || 0));
}
function Gd(i) {
  return String(i ?? "").trim();
}
function Wd(i) {
  return (Array.isArray(i) ? i : typeof i == "string" ? i.split(",") : []).map((t) => String(t ?? "").trim()).filter(Boolean);
}
function c0(i) {
  const e = ra.indexOf(i);
  return e >= 0 ? e : ra.indexOf("near");
}
function u0(i = Sn({})) {
  const e = ["near", "close", "far", "extreme"], t = c0(i.max);
  return e.find((n) => ra.indexOf(n) <= t) ?? "close";
}
function d0(i) {
  const e = Eg(i == null ? void 0 : i.max), t = ra.indexOf(e);
  return ra.map((n, a) => ({
    key: n,
    allowed: t >= 0 ? a <= t : a === 0,
    value: (i == null ? void 0 : i[n]) ?? void 0,
    labelkey: Gs(n)
  }));
}
function m0(i, e, t, n) {
  let a = Number(e);
  if (t)
    if (n !== void 0)
      a += Math.ceil(Number(n) / 2);
    else
      return console.warn("Weapon not attached to an actor"), q.item.personalWeapon.weaponWithoutActor;
  return a;
}
function f0(i, e, t) {
  let n = "";
  return t && q.attributes[t] && (n += q.attributes[t].substring(0, 3).toUpperCase() + "/2 + "), n += String(e), n;
}
function p0(i, e) {
  return te.useArmor(i) ? e ? "noArmor" : "withArmor" : "";
}
function qd(i) {
  const e = game.system.mwd.skills.get(i);
  if (!e)
    return {
      img: DEFAULT_ITEM_ICONS.skill,
      system: {
        code: i,
        attribute: ""
      }
    };
  const t = {
    img: e.icon,
    system: {
      code: e.code,
      attribute: e.attribute
    }
  };
  return e.code !== "knowledge" && (t.name = e.label), t;
}
function Yo(i = {}) {
  const e = Dr(i), t = En(e.catalogId);
  return {
    system: e,
    ...t ? { name: t.label } : {}
  };
}
function g0(i) {
  const e = String(i ?? "").trim();
  return !!(!e || e.startsWith("icons/svg/") || e === "icons/mystery-man.svg");
}
var Br, Ri, ql, Ig, Ss;
const Ut = class Ut extends Item {
  static init() {
    Y(this, Br) || (Ie(this, Br, !0), Hooks.on("createItem", (e, t, n) => {
      var a, r;
      Promise.resolve((a = e.onCreateItem) == null ? void 0 : a.call(e, t, n)).catch((s) => {
        console.error(`${Le}Item create hook failed`, s);
      }), v(r = Ut, Ri, ql).call(r, e);
    }), Hooks.on("updateItem", (e) => {
      var t;
      v(t = Ut, Ri, ql).call(t, e);
    }), Hooks.on("deleteItem", (e) => {
      var t;
      v(t = Ut, Ri, Ig).call(t, e);
    }), Hooks.on("createActiveEffect", (e) => {
      var t;
      v(t = Ut, Ri, Ss).call(t, e);
    }), Hooks.on("updateActiveEffect", (e) => {
      var t;
      v(t = Ut, Ri, Ss).call(t, e);
    }), Hooks.on("deleteActiveEffect", (e) => {
      var t;
      v(t = Ut, Ri, Ss).call(t, e);
    }));
  }
  static canonicalType(e) {
    return vg(e);
  }
  static defaultIconForType(e) {
    return r0(e);
  }
  get canonicalType() {
    return this.constructor.canonicalType(this.type);
  }
  async onCreateItem(e, t) {
  }
  async _preCreate(e, t, n) {
    super._preCreate && await super._preCreate(e, t, n);
    const a = (e == null ? void 0 : e.type) ?? this.type, r = this.constructor.canonicalType(a), s = {}, o = await Mg("Item", r);
    if (o.system && Object.keys(o.system).length && (s.system = foundry.utils.mergeObject(
      foundry.utils.deepClone(o.system),
      foundry.utils.deepClone((e == null ? void 0 : e.system) ?? this.system ?? {}),
      { inplace: !1, recursive: !0, overwrite: !0 }
    )), a !== r && s0(a) && (s.type = r), g0((e == null ? void 0 : e.img) ?? this.img)) {
      const l = this.constructor.defaultIconForType(r);
      l && (s.img = l);
    }
    if (r === A.itemType.lifeModule && (!(e != null && e.name) || e.name === "DOCUMENT.Item") && (s.name = "MWD.itemType.singular.lifeModule"), r === A.itemType.lifeModule) {
      const l = Yo(s.system ?? (e == null ? void 0 : e.system) ?? this.system ?? {});
      s.system = l.system, l.name && (!(e != null && e.name) || e.name === "DOCUMENT.Item") && (s.name = l.name);
    }
    Object.keys(s).length && this.updateSource(s);
  }
  async _preUpdate(e, t, n) {
    var l, c;
    super._preUpdate && await super._preUpdate(e, t, n);
    const a = e != null && e.system ? foundry.utils.mergeObject(foundry.utils.deepClone(this.system ?? {}), foundry.utils.deepClone(e.system), { inplace: !1 }) : null;
    if (a && this.isPersonalWeapon()) {
      e.system ?? (e.system = {});
      const u = a.ammo, d = Ud(a);
      e.system.standardTraits = [], e.system.payloads = Ti(a.payloads, { legacyAmmo: u, category: a.category }), e.system.consumptionSources = Fn(a.consumptionSources, { legacyAmmo: u }), e.system.selectedPayloadId = pn(
        a.selectedPayloadId,
        e.system.payloads,
        { legacyAmmo: u, category: a.category }
      ), e.system.traits = d.traits, e.system.keywords = d.keywords, e.system.resolution = cs(a.resolution, "standard"), e.system.fireModes = us(a.fireModes), e.system.attackRatingBand = Za(a.attackRatingBand), e.system.range = Sn(a.range), e.system.damageType = ci(a.damageType), e.system.ammo = qt();
    }
    if (a && this.isMechWeapon()) {
      e.system ?? (e.system = {});
      const u = a.ammo, d = hr(a.weaponCategory ?? a.category);
      e.system.category = d, e.system.weaponCategory = d, e.system.skill = is(d, a.skill), e.system.size = cn(a.size ?? a.hardpointSize ?? "small"), e.system.ap = Number(a.ap ?? a.armorPiercing ?? 0) || 0, e.system.damage = Math.max(0, Number(a.damage ?? 0) || 0), e.system.damageType = ci(a.damageType), e.system.attackRatingBand = Za(a.attackRatingBand), e.system.range = Vo(a.range, d), e.system.payloads = Ti(a.payloads, { legacyAmmo: u, category: d }), e.system.consumptionSources = Fn(a.consumptionSources, { legacyAmmo: u }), e.system.selectedPayloadId = pn(
        a.selectedPayloadId,
        e.system.payloads,
        { legacyAmmo: u, category: d }
      ), e.system.heat = Math.max(0, Number(a.heat ?? 0) || 0), e.system.area = String(a.area ?? "none").trim() || "none", e.system.volatile = !!a.volatile, e.system.ammo = qt(), e.system.hardpointId = qt(), e.system.hardpointType = qt(), e.system.hardpointSize = qt(), e.system.mountLocation = qt(), e.system.damageAttribute = qt(), e.system.monitor = qt(), e.system.noArmor = qt(), e.system.armorAvoidance = qt(), e.system.defense = qt();
    }
    if (a && this.isArmor() && (e.system ?? (e.system = {}), e.system.mitigationByType = Ki(a.mitigationByType ?? a.mitigation), e.system.tags = ls(a.tags), e.system.traits = Xa(a.traits), e.system.standardTraits = Xi(a.standardTraits), e.system.traitState = zo({
      standardTraits: e.system.standardTraits,
      traits: e.system.traits,
      traitState: a.traitState
    }).traitState), a && this.isLifeModule()) {
      const u = Yo(a);
      e.system ?? (e.system = {}), foundry.utils.mergeObject(e.system, u.system, { inplace: !0, overwrite: !0 }), u.name && (e.name = u.name);
      return;
    }
    if (a && this.isQuality()) {
      e.system ?? (e.system = {});
      const u = Ii(a);
      foundry.utils.mergeObject(e.system, u, { inplace: !0, overwrite: !0 });
      return;
    }
    if (a && this.canonicalType === A.itemType.assetModule) {
      e.system ?? (e.system = {});
      const u = Ws(a);
      foundry.utils.mergeObject(e.system, u, { inplace: !0, overwrite: !0 });
      return;
    }
    if (a && this.isQuantityTrackedInventoryItem()) {
      e.system ?? (e.system = {}), e.system.quantity = jd(a.quantity, 1), e.system.rating = Kd(a.rating, 0), e.system.category = Gd(a.category), e.system.tags = Wd(a.tags);
      return;
    }
    if (!this.isSkill()) return;
    const r = (l = e == null ? void 0 : e.system) == null ? void 0 : l.code;
    if (r === void 0) return;
    const s = this.system.code;
    if (r === s) return;
    const o = qd(r);
    o && ((c = o == null ? void 0 : o.system) == null || delete c.code, foundry.utils.mergeObject(e, o, { inplace: !0 }));
  }
  prepareBaseData() {
    super.prepareBaseData();
    const e = this.canonicalType;
    e === A.itemType.personalWeapon ? this._preparePersonalWeaponBaseData() : e === A.itemType.mechWeapon ? this._prepareMechWeaponBaseData() : e === A.itemType.armor ? this._prepareArmorBaseData() : e === A.itemType.lifeModule ? this._prepareLifeModuleBaseData() : e === A.itemType.quality ? this._prepareQualityBaseData() : e === A.itemType.assetModule ? this._prepareAssetModuleBaseData() : [A.itemType.gear, A.itemType.consumable].includes(e) && this._prepareGearBaseData();
  }
  _prepareAssetModuleBaseData() {
    const e = this.system ?? {}, t = Ws(e);
    Object.assign(e, t);
  }
  _preparePersonalWeaponBaseData() {
    const e = this.system ?? {}, t = e.ammo;
    e.equipped = !!e.equipped, e.isPrimary = !!e.isPrimary, e.category = String(e.category ?? e.weaponCategory ?? "ranged").trim() || "ranged", e.skill = String(e.skill ?? "firearms").trim() || "firearms", e.ap = Number(e.ap ?? e.armorPiercing ?? 0) || 0, e.damage = Number(e.damage ?? 0) || 0, e.damageType = ci(e.damageType), e.attackRatingBand = Za(e.attackRatingBand), e.range = Sn(e.range);
    const n = Ud(e);
    e.standardTraits = [], e.traits = n.traits, e.keywords = n.keywords, e.resolution = cs(e.resolution, "standard"), e.fireModes = us(e.fireModes), e.payloads = Ti(e.payloads, { legacyAmmo: t, category: e.category }), e.consumptionSources = Fn(e.consumptionSources, { legacyAmmo: t }), e.selectedPayloadId = pn(e.selectedPayloadId, e.payloads, { legacyAmmo: t, category: e.category }), delete e.ammo, e.notes = String(e.notes ?? "").trim();
  }
  _prepareMechWeaponBaseData() {
    const e = this.system ?? {}, t = e.ammo, n = hr(e.weaponCategory ?? e.category);
    e.category = n, e.weaponCategory = n, e.skill = is(n, e.skill), e.size = cn(e.size ?? e.hardpointSize ?? "small"), e.ap = Number(e.ap ?? e.armorPiercing ?? 0) || 0, e.damage = Math.max(0, Number(e.damage ?? 0) || 0), e.damageType = ci(e.damageType), e.attackRatingBand = Za(e.attackRatingBand), e.range = Vo(e.range, n), e.payloads = Ti(e.payloads, { legacyAmmo: t, category: n }), e.consumptionSources = Fn(e.consumptionSources, { legacyAmmo: t }), e.selectedPayloadId = pn(e.selectedPayloadId, e.payloads, { legacyAmmo: t, category: n }), e.heat = Math.max(0, Number(e.heat ?? 0) || 0), e.area = String(e.area ?? "none").trim() || "none", e.volatile = !!e.volatile, e.notes = String(e.notes ?? "").trim(), delete e.ammo, delete e.hardpointId, delete e.hardpointType, delete e.hardpointSize, delete e.mountLocation, delete e.damageAttribute, delete e.monitor, delete e.noArmor, delete e.armorAvoidance, delete e.defense;
  }
  _prepareArmorBaseData() {
    const e = this.system ?? {};
    e.equipped = !!e.equipped, e.isPrimary = !!e.isPrimary, e.rating = Math.max(0, Number(e.rating ?? 0)), e.defenseBonus = Number(e.defenseBonus ?? 0) || 0, e.mitigationByType = Ki(e.mitigationByType ?? e.mitigation), delete e.mitigation, e.durability ?? (e.durability = {}), e.durability.max = Math.max(0, Number(e.durability.max ?? e.rating ?? 0)), e.durability.current = Math.min(
      e.durability.max,
      Math.max(0, Number(e.durability.current ?? e.durability.max ?? e.rating ?? 0))
    ), e.standardTraits = Xi(e.standardTraits), e.tags = ls(e.tags), e.traits = Xa(e.traits), e.traitState = zo({
      standardTraits: e.standardTraits,
      traits: e.traits,
      traitState: e.traitState
    }).traitState, e.notes = String(e.notes ?? "").trim();
  }
  _prepareLifeModuleBaseData() {
    const e = Yo(this.system ?? {});
    foundry.utils.mergeObject(this.system, e.system, { inplace: !0, overwrite: !0 });
  }
  _prepareQualityBaseData() {
    const e = Ii(this.system ?? {});
    foundry.utils.mergeObject(this.system, e, { inplace: !0, overwrite: !0 });
  }
  _prepareGearBaseData() {
    const e = this.system ?? {};
    e.quantity = jd(e.quantity, 1), e.rating = Kd(e.rating, 0), e.category = Gd(e.category), e.tags = Wd(e.tags);
  }
  getAttributes() {
    return [];
  }
  getUsableAttributes() {
    return this.getAttributes();
  }
  getAttributeValue(e) {
    var t;
    return this.system.attributes ? ((t = this.system.attributes[e]) == null ? void 0 : t.value) ?? 0 : 0;
  }
  hasOwnAnarchy() {
    return !1;
  }
  hasGMAnarchy() {
    return !1;
  }
  async nextConnectionMode() {
  }
  async setCheckbarValue(e, t) {
    return this.update({ [e]: t });
  }
  isWeapon() {
    return [A.itemType.mechWeapon, A.itemType.personalWeapon].includes(this.canonicalType);
  }
  isPersonalWeapon() {
    return this.canonicalType === A.itemType.personalWeapon;
  }
  isMechWeapon() {
    return this.canonicalType === A.itemType.mechWeapon;
  }
  isArmor() {
    return this.canonicalType === A.itemType.armor;
  }
  isLifeModule() {
    return this.canonicalType === A.itemType.lifeModule;
  }
  isQuality() {
    return this.canonicalType === A.itemType.quality;
  }
  isGear() {
    return this.canonicalType === A.itemType.gear;
  }
  isConsumable() {
    return this.canonicalType === A.itemType.consumable;
  }
  isQuantityTrackedInventoryItem() {
    return this.isGear() || this.isConsumable();
  }
  supportsEquippedEffectSync() {
    return this.isPersonalWeapon() || this.isArmor();
  }
  shouldApplyEquippedEffects() {
    var e;
    return this.supportsEquippedEffectSync() && !!this.actor && !!((e = this.system) != null && e.equipped);
  }
  getSyncedActorEffects({ actor: e = this.actor } = {}) {
    return e != null && e.effects ? e.effects.contents.filter((t) => {
      var a, r;
      const n = (r = (a = t.flags) == null ? void 0 : a[M]) == null ? void 0 : r[Ut.EQUIPPED_EFFECT_FLAG];
      return (n == null ? void 0 : n.sourceItemId) === this.id;
    }) : [];
  }
  async removeSyncedActorEffects({ actor: e = this.actor } = {}) {
    const t = this.getSyncedActorEffects({ actor: e });
    return !t.length || !e ? [] : e.deleteEmbeddedDocuments("ActiveEffect", t.map((n) => n.id));
  }
  async syncEquippedActorEffects({ actor: e = this.actor } = {}) {
    var m, f, p, g;
    if (!e || !this.supportsEquippedEffectSync()) return { created: [], updated: [], deleted: [] };
    const t = this.getSyncedActorEffects({ actor: e }), n = Array.from(((m = this.effects) == null ? void 0 : m.contents) ?? []);
    if (!this.shouldApplyEquippedEffects()) {
      if (!t.length) return { created: [], updated: [], deleted: [] };
      const h = await e.deleteEmbeddedDocuments("ActiveEffect", t.map((y) => y.id));
      return { created: [], updated: [], deleted: h };
    }
    const a = /* @__PURE__ */ new Map();
    for (const h of t) {
      const y = (g = (p = (f = h.flags) == null ? void 0 : f[M]) == null ? void 0 : p[Ut.EQUIPPED_EFFECT_FLAG]) == null ? void 0 : g.sourceEffectId;
      if (!y) continue;
      const b = a.get(y) ?? [];
      b.push(h), a.set(y, b);
    }
    const r = [], s = [], o = [], l = new Set(n.map((h) => h.id));
    for (const [h, y] of a.entries()) {
      if (!l.has(h)) {
        o.push(...y.map((b) => b.id));
        continue;
      }
      y.length > 1 && o.push(...y.slice(1).map((b) => b.id));
    }
    for (const h of n) {
      const b = (a.get(h.id) ?? [])[0] ?? null, S = this._prepareSyncedActorEffectData(h);
      b ? s.push({ _id: b.id, ...S }) : r.push(S);
    }
    const c = o.length ? await e.deleteEmbeddedDocuments("ActiveEffect", o) : [], u = s.length ? await e.updateEmbeddedDocuments("ActiveEffect", s) : [];
    return { created: r.length ? await e.createEmbeddedDocuments("ActiveEffect", r) : [], updated: u, deleted: c };
  }
  _prepareSyncedActorEffectData(e) {
    const t = e.toObject();
    delete t._id;
    const n = String(e.name ?? "Effect").trim() || "Effect", a = String(this.name ?? "Item").trim() || "Item", r = n.startsWith(a) ? n : `${a}: ${n}`;
    return t.name = r, t.transfer = !1, t.origin = e.uuid ?? this.uuid ?? t.origin ?? null, t.flags = foundry.utils.mergeObject(t.flags ?? {}, {
      [M]: {
        [Ut.EQUIPPED_EFFECT_FLAG]: {
          synced: !0,
          sourceItemId: this.id,
          sourceItemUuid: this.uuid ?? null,
          sourceEffectId: e.id,
          sourceEffectUuid: e.uuid ?? null
        }
      }
    }), t;
  }
  isSkill() {
    return this.canonicalType === A.itemType.skill;
  }
  async rollAttribute(e) {
    this.parent && await Bi.itemAttributeRoll(this, e);
  }
  async switchMonitorCheck(e, t, n, a = void 0) {
    await te.switchMonitorCheck(this.parent, e, t, n, a, this);
  }
  async setCounter(e, t) {
    await te.setCounter(this, e, t);
  }
  async createModifier(e = {}) {
    e = foundry.utils.mergeObject(e, {
      group: "roll",
      effect: "pool",
      category: "skill",
      subCategory: "",
      value: 0,
      condition: ""
    }), this._mutateModifiers((t) => t.concat([e]));
  }
  async deleteModifier(e) {
    await this._mutateModifiers((t) => t.filter((n) => n.id !== e));
  }
  async changeModifierSelection(e, t, n) {
    const a = this._computeModifierImpact(t, n);
    this._applyModifierUpdate(e, a);
  }
  _computeModifierImpact(e, t) {
    switch (e) {
      case "group":
        return (n) => {
          n.group !== t && (n.group = t, n.effect = "", n.category = "", n.subCategory = "");
        };
      case "effect":
        return (n) => n.effect = t;
      case "category":
        return (n) => {
          n.category !== t && (n.category = t, n.subCategory = "");
        };
      case "subCategory":
        return (n) => n.subCategory = t;
    }
    return (n) => {
    };
  }
  async changeModifierValue(e, t) {
    this._applyModifierUpdate(e, (n) => n.value = Number(t));
  }
  async changeModifierCondition(e, t) {
    this._applyModifierUpdate(e, (n) => n.condition = t);
  }
  async _applyModifierUpdate(e, t = (n) => {
  }) {
    await this._mutateModifiers((n) => n.map((a) => (a.id === e && t(a), a)));
  }
  async _mutateModifiers(e = (t) => t) {
    const t = e(this.system.modifiers);
    be.reindexIds(t), await this.update({ "system.modifiers": t });
  }
  async _mutateQualitySystem(e = (t) => t) {
    const t = e(foundry.utils.deepClone(Ii(this.system ?? {})));
    await this.update({ system: Ii(t) });
  }
  async createQualityPrerequisite(e = {}) {
    await this._mutateQualitySystem((t) => (t.prerequisites = en(t.prerequisites).concat([{
      id: e.id ?? foundry.utils.randomID(),
      fact: e.fact ?? "",
      comparator: e.comparator ?? "eq",
      value: e.value ?? ""
    }]), t));
  }
  async deleteQualityPrerequisite(e) {
    await this._mutateQualitySystem((t) => (t.prerequisites = en(t.prerequisites).filter((n) => n.id !== e), t));
  }
  async updateQualityPrerequisite(e, t, n) {
    await this._mutateQualitySystem((a) => (a.prerequisites = en(a.prerequisites).map((r) => (r.id !== e || (t === "fact" && (r.fact = n), t === "comparator" && (r.comparator = n), t === "value" && (r.value = n)), r)), a));
  }
  async createQualityEffect(e = {}) {
    await this._mutateQualitySystem((t) => (t.effects = Hn(t.effects).concat([{
      id: e.id ?? foundry.utils.randomID(),
      type: e.type ?? "rollMod",
      phase: e.phase ?? "onBuildRoll",
      selector: e.selector ?? "",
      skillKeys: e.skillKeys ?? [],
      label: e.label ?? "",
      value: Number(e.value ?? 0) || 0,
      min: e.min ?? null,
      max: e.max ?? null,
      pool: e.pool ?? "",
      operation: e.operation ?? "adjustAmount",
      conditions: en(e.conditions ?? []),
      limit: vn(e.limit ?? {})
    }]), t));
  }
  async deleteQualityEffect(e) {
    await this._mutateQualitySystem((t) => (t.effects = Hn(t.effects).filter((n) => n.id !== e), t));
  }
  async updateQualityEffect(e, t, n) {
    await this._mutateQualitySystem((a) => (a.effects = Hn(a.effects).map((r) => (r.id !== e || (t === "type" && (r.type = n), t === "phase" && (r.phase = n), t === "selector" && (r.selector = n), t === "skillKeys" && (r.skillKeys = Array.isArray(n) ? n : []), t === "label" && (r.label = n), t === "value" && (r.value = Number(n ?? 0) || 0), t === "min" && (r.min = n === "" ? null : Number(n ?? 0)), t === "max" && (r.max = n === "" ? null : Number(n ?? 0)), t === "pool" && (r.pool = n), t === "operation" && (r.operation = n), t === "limit.perActivation" && (r.limit = vn({ ...r.limit ?? {}, perActivation: n })), t === "limit.perRound" && (r.limit = vn({ ...r.limit ?? {}, perRound: n })), t === "limit.perScene" && (r.limit = vn({ ...r.limit ?? {}, perScene: n }))), r)), a));
  }
  async createQualityEffectCondition(e, t = {}) {
    await this._mutateQualitySystem((n) => (n.effects = Hn(n.effects).map((a) => (a.id !== e || (a.conditions = en(a.conditions).concat([{
      id: t.id ?? foundry.utils.randomID(),
      fact: t.fact ?? "",
      comparator: t.comparator ?? "eq",
      value: t.value ?? ""
    }])), a)), n));
  }
  async deleteQualityEffectCondition(e, t) {
    await this._mutateQualitySystem((n) => (n.effects = Hn(n.effects).map((a) => (a.id !== e || (a.conditions = en(a.conditions).filter((r) => r.id !== t)), a)), n));
  }
  async updateQualityEffectCondition(e, t, n, a) {
    await this._mutateQualitySystem((r) => (r.effects = Hn(r.effects).map((s) => (s.id !== e || (s.conditions = en(s.conditions).map((o) => (o.id !== t || (n === "fact" && (o.fact = a), n === "comparator" && (o.comparator = a), n === "value" && (o.value = a)), o))), s)), r));
  }
  async _mutateWeaponStandardTraits(e = (t) => t) {
    var n;
    const t = e(foundry.utils.deepClone(Un((n = this.system) == null ? void 0 : n.standardTraits)));
    await this.update({ "system.standardTraits": Un(t) });
  }
  async createWeaponStandardTrait(e = {}) {
    await this._mutateWeaponStandardTraits((t) => t.concat([{
      id: e.id ?? foundry.utils.randomID(),
      key: e.key ?? "armorPiercing",
      rating: Math.max(0, Number(e.rating ?? 0) || 0)
    }]));
  }
  async deleteWeaponStandardTrait(e) {
    await this._mutateWeaponStandardTraits((t) => t.filter((n) => n.id !== e));
  }
  async updateWeaponStandardTrait(e, t, n) {
    await this._mutateWeaponStandardTraits((a) => a.map((r) => (r.id !== e || (t === "key" && (r.key = n), t === "rating" && (r.rating = Math.max(0, Number(n ?? 0) || 0))), r)));
  }
  async _mutateArmorStandardTraits(e = (t) => t) {
    var n;
    const t = e(foundry.utils.deepClone(Xi((n = this.system) == null ? void 0 : n.standardTraits)));
    await this.update({ "system.standardTraits": Xi(t) });
  }
  async createArmorStandardTrait(e = {}) {
    await this._mutateArmorStandardTraits((t) => t.concat([{
      id: e.id ?? foundry.utils.randomID(),
      key: e.key ?? "ablative",
      rating: Math.max(0, Number(e.rating ?? 0) || 0)
    }]));
  }
  async deleteArmorStandardTrait(e) {
    await this._mutateArmorStandardTraits((t) => t.filter((n) => n.id !== e));
  }
  async updateArmorStandardTrait(e, t, n) {
    await this._mutateArmorStandardTraits((a) => a.map((r) => (r.id !== e || (t === "key" && (r.key = n), t === "rating" && (r.rating = Math.max(0, Number(n ?? 0) || 0))), r)));
  }
  async _mutatePayloads(e = (t) => t) {
    var a, r, s, o, l, c, u;
    const t = e(foundry.utils.deepClone(
      Ti((a = this.system) == null ? void 0 : a.payloads, {
        legacyAmmo: (r = this.system) == null ? void 0 : r.ammo,
        category: ((s = this.system) == null ? void 0 : s.category) ?? ((o = this.system) == null ? void 0 : o.weaponCategory)
      })
    )).map(ti), n = pn((l = this.system) == null ? void 0 : l.selectedPayloadId, t, {
      category: ((c = this.system) == null ? void 0 : c.category) ?? ((u = this.system) == null ? void 0 : u.weaponCategory)
    });
    await this.update({
      "system.payloads": t,
      "system.selectedPayloadId": n,
      "system.ammo": qt()
    });
  }
  async _mutateConsumptionSources(e = (t) => t) {
    var n, a;
    const t = e(foundry.utils.deepClone(
      Fn((n = this.system) == null ? void 0 : n.consumptionSources, { legacyAmmo: (a = this.system) == null ? void 0 : a.ammo })
    )).map($i);
    await this.update({
      "system.consumptionSources": t,
      "system.ammo": qt()
    });
  }
  async updatePayloadField(e, t, n) {
    String(e ?? "").trim() !== "unloaded" && await this._mutatePayloads((a) => a.map((r) => r.id !== e ? r : (l0(r, t) && foundry.utils.setProperty(r, t, n), ti(r))));
  }
  async createPayload(e = {}) {
    await this._mutatePayloads((t) => t.concat([ti({
      id: e.id ?? foundry.utils.randomID(),
      label: e.label ?? e.name ?? "Payload",
      compatibleWith: e.compatibleWith ?? [],
      modifies: e.modifies ?? {},
      traits: e.traits ?? [],
      keywords: e.keywords ?? [],
      template: e.template ?? null,
      areaEffect: e.areaEffect ?? { kind: "discrete" },
      resolution: e.resolution ?? { resolverKey: "standard" },
      consumption: e.consumption ?? { amount: 1, sourceId: "" }
    })]));
  }
  async deletePayload(e) {
    var r, s, o, l, c;
    if (String(e ?? "").trim() === "unloaded") return;
    const t = ((r = this.system) == null ? void 0 : r.category) ?? ((s = this.system) == null ? void 0 : s.weaponCategory), n = Ti((o = this.system) == null ? void 0 : o.payloads, {
      legacyAmmo: (l = this.system) == null ? void 0 : l.ammo,
      category: t
    }).filter((u) => u.id !== e), a = ((c = n[0]) == null ? void 0 : c.id) ?? "unloaded";
    await this.update({
      "system.payloads": n.length ? n : Ti([], { category: t }),
      "system.selectedPayloadId": n.some((u) => {
        var d;
        return u.id === ((d = this.system) == null ? void 0 : d.selectedPayloadId);
      }) ? this.system.selectedPayloadId : n.length ? a : "",
      "system.ammo": qt()
    });
  }
  async createPayloadStandardTrait(e, t = {}) {
    String(e ?? "").trim() !== "unloaded" && await this._mutatePayloads((n) => n.map((a) => a.id !== e ? a : (a.modifies ?? (a.modifies = {}), a.modifies.standardTraits = Un(a.modifies.standardTraits).concat([{
      id: t.id ?? foundry.utils.randomID(),
      key: t.key ?? "armorPiercing",
      rating: Math.max(0, Number(t.rating ?? 0) || 0)
    }]), ti(a))));
  }
  async deletePayloadStandardTrait(e, t) {
    String(e ?? "").trim() !== "unloaded" && await this._mutatePayloads((n) => n.map((a) => a.id !== e ? a : (a.modifies ?? (a.modifies = {}), a.modifies.standardTraits = Un(a.modifies.standardTraits).filter((r) => r.id !== t), ti(a))));
  }
  async updatePayloadStandardTrait(e, t, n, a) {
    String(e ?? "").trim() !== "unloaded" && await this._mutatePayloads((r) => r.map((s) => s.id !== e ? s : (s.modifies ?? (s.modifies = {}), s.modifies.standardTraits = Un(s.modifies.standardTraits).map((o) => (o.id !== t || (n === "key" && (o.key = a), n === "rating" && (o.rating = Math.max(0, Number(a ?? 0) || 0))), o)), ti(s))));
  }
  async createConsumptionSource(e = {}) {
    await this._mutateConsumptionSources((t) => t.concat([$i({
      id: e.id ?? foundry.utils.randomID(),
      label: e.label ?? "Source",
      kind: e.kind ?? "internal",
      tracking: e.tracking ?? { current: 0, max: 0 },
      link: e.link ?? {}
    })]));
  }
  async deleteConsumptionSource(e) {
    await this._mutateConsumptionSources((t) => t.filter((n) => n.id !== e)), await this._mutatePayloads((t) => t.map((n) => {
      var a;
      return ((a = n == null ? void 0 : n.consumption) == null ? void 0 : a.sourceId) !== e ? n : (n.consumption.sourceId = "", ti(n));
    }));
  }
  async updateConsumptionSourceField(e, t, n) {
    await this._mutateConsumptionSources((a) => a.map((r) => {
      var s, o, l;
      if (r.id !== e) return r;
      if (foundry.utils.setProperty(r, t, n), t === "kind" && r.kind === "itemRef" && (r.link ?? (r.link = {}), String(r.link.itemPath ?? "").trim() || (r.link.itemPath = "quantity"), (!String(r.label ?? "").trim() || String(r.label ?? "").trim() === "Source") && (r.label = "Linked Item")), t === "link.itemId" && r.kind === "itemRef") {
        r.link ?? (r.link = {}), String(r.link.itemPath ?? "").trim() || (r.link.itemPath = "quantity");
        const c = ((l = (o = (s = this.actor) == null ? void 0 : s.items) == null ? void 0 : o.get) == null ? void 0 : l.call(o, String(r.link.itemId ?? "").trim())) ?? null;
        c && (!String(r.label ?? "").trim() || ["Source", "Linked Item"].includes(String(r.label ?? "").trim())) && (r.label = c.name ?? r.label);
      }
      return $i(r);
    }));
  }
  getPayloadState({ payloadId: e = "", ammoTypeId: t = "" } = {}) {
    var n, a, r, s, o;
    return Nl({
      payloads: (n = this.system) == null ? void 0 : n.payloads,
      selectedPayloadId: (a = this.system) == null ? void 0 : a.selectedPayloadId,
      consumptionSources: (r = this.system) == null ? void 0 : r.consumptionSources,
      actor: this.actor ?? null,
      payloadId: e || t,
      category: ((s = this.system) == null ? void 0 : s.category) ?? ((o = this.system) == null ? void 0 : o.weaponCategory)
    });
  }
  getActivePayloadReloadState({ payloadId: e = "", ammoTypeId: t = "", user: n = game.user } = {}) {
    var p, g, h;
    const a = String(((p = this.system) == null ? void 0 : p.category) ?? ((g = this.system) == null ? void 0 : g.weaponCategory) ?? "").trim().toLowerCase(), r = {
      canReload: !1,
      reason: "",
      payloadLabel: "",
      activePayloadId: "",
      current: 0,
      max: 0,
      inCombat: !1,
      source: null,
      sourceState: null,
      payloadState: null
    };
    if (!this.isPersonalWeapon())
      return { ...r, reason: "Only personal weapons can be reloaded from this sheet." };
    if (!this.actor)
      return { ...r, reason: "Reload is only available for weapons owned by an actor." };
    if (a === "melee")
      return { ...r, reason: "Melee weapons do not use reloadable payloads." };
    const s = this.getPayloadState({ payloadId: e || t }), o = (s == null ? void 0 : s.sourceState) ?? null, l = (s == null ? void 0 : s.source) ?? null, c = String((s == null ? void 0 : s.activePayloadId) ?? "").trim(), u = String((s == null ? void 0 : s.payloadLabel) ?? "").trim() || "Unloaded", d = Math.max(0, Number((o == null ? void 0 : o.current) ?? 0) || 0), m = Math.max(0, Number((o == null ? void 0 : o.max) ?? 0) || 0), f = !!((h = F.getCombat(this.actor)) != null && h.combatant);
    return !c || c === "unloaded" ? {
      ...r,
      reason: "Select a payload before reloading.",
      payloadLabel: u,
      activePayloadId: c,
      payloadState: s,
      source: l,
      sourceState: o,
      current: d,
      max: m,
      inCombat: f
    } : o != null && o.isTracked ? o.kind !== "internal" ? {
      ...r,
      reason: "Linked ammo sources are read-only from the weapon sheet.",
      payloadLabel: u,
      activePayloadId: c,
      payloadState: s,
      source: l,
      sourceState: o,
      current: d,
      max: m,
      inCombat: f
    } : m <= 0 ? {
      ...r,
      reason: "This payload source has no reloadable capacity.",
      payloadLabel: u,
      activePayloadId: c,
      payloadState: s,
      source: l,
      sourceState: o,
      current: d,
      max: m,
      inCombat: f
    } : f && !(n != null && n.isGM) ? {
      ...r,
      reason: "Only a GM can reload from the weapon sheet during combat.",
      payloadLabel: u,
      activePayloadId: c,
      payloadState: s,
      source: l,
      sourceState: o,
      current: d,
      max: m,
      inCombat: f
    } : d >= m ? {
      ...r,
      reason: "Magazine already full.",
      payloadLabel: u,
      activePayloadId: c,
      payloadState: s,
      source: l,
      sourceState: o,
      current: d,
      max: m,
      inCombat: f
    } : {
      canReload: !0,
      reason: "",
      payloadLabel: u,
      activePayloadId: c,
      payloadState: s,
      source: l,
      sourceState: o,
      current: d,
      max: m,
      inCombat: f
    } : {
      ...r,
      reason: "This payload is untracked and does not need to be reloaded.",
      payloadLabel: u,
      activePayloadId: c,
      payloadState: s,
      source: l,
      sourceState: o,
      current: d,
      max: m,
      inCombat: f
    };
  }
  canReloadActivePayload({ detailed: e = !1, ...t } = {}) {
    const n = this.getActivePayloadReloadState(t);
    return e ? n : n.canReload;
  }
  async reloadActivePayload({ payloadId: e = "", ammoTypeId: t = "" } = {}) {
    var a;
    const n = this.getActivePayloadReloadState({ payloadId: e, ammoTypeId: t });
    return !n.canReload || !((a = n.source) != null && a.id) ? { ok: !1, ...n } : (await this._mutateConsumptionSources((r) => r.map((s) => {
      var o;
      return s.id !== n.source.id ? s : (s.tracking ?? (s.tracking = {}), s.tracking.max = Math.max(0, Number(((o = s.tracking) == null ? void 0 : o.max) ?? n.max) || n.max), s.tracking.current = n.max, $i(s));
    })), {
      ok: !0,
      payloadLabel: n.payloadLabel,
      activePayloadId: n.activePayloadId,
      current: n.max,
      max: n.max,
      reloadedAmount: Math.max(0, n.max - n.current),
      sourceId: n.source.id
    });
  }
  async setActivePayload(e) {
    var n, a, r, s, o, l;
    const t = pn(
      e,
      Ti((n = this.system) == null ? void 0 : n.payloads, {
        legacyAmmo: (a = this.system) == null ? void 0 : a.ammo,
        category: ((r = this.system) == null ? void 0 : r.category) ?? ((s = this.system) == null ? void 0 : s.weaponCategory)
      }),
      {
        category: ((o = this.system) == null ? void 0 : o.category) ?? ((l = this.system) == null ? void 0 : l.weaponCategory)
      }
    );
    await this.update({
      "system.selectedPayloadId": t,
      "system.ammo": qt()
    });
  }
  canConsumePayload({ payloadId: e = "", ammoTypeId: t = "" } = {}) {
    var a;
    const n = this.getPayloadState({ payloadId: e || t });
    return (a = n == null ? void 0 : n.sourceState) != null && a.isTracked ? Number(n.sourceState.current ?? 0) >= Number(n.sourceState.consumePerUse ?? 1) : !0;
  }
  async consumePayload({ payloadId: e = "", ammoTypeId: t = "" } = {}) {
    var s;
    const n = this.getPayloadState({ payloadId: e || t });
    if (!((s = n == null ? void 0 : n.sourceState) != null && s.isTracked)) return !0;
    const a = Math.max(1, Number(n.sourceState.consumePerUse ?? 1) || 1), r = Math.max(0, Number(n.sourceState.current ?? 0) || 0);
    return r < a ? !1 : n.sourceState.kind === "internal" ? (await this._mutateConsumptionSources((o) => o.map((l) => {
      var c;
      return l.id !== ((c = n.source) == null ? void 0 : c.id) ? l : (l.tracking ?? (l.tracking = {}), l.tracking.current = Math.max(0, r - a), $i(l));
    })), !0) : n.sourceState.kind === "actorResource" && this.actor && n.sourceState.currentPath ? (await this.actor.update({
      [n.sourceState.currentPath]: Math.max(0, r - a)
    }), !0) : n.sourceState.kind === "itemRef" && n.sourceState.sourceItem && n.sourceState.currentPath ? (await n.sourceState.sourceItem.update({
      [n.sourceState.currentPath]: Math.max(0, r - a)
    }), !0) : !1;
  }
  getAmmoState({ ammoTypeId: e = "" } = {}) {
    return this.getPayloadState({ payloadId: e });
  }
  async setActiveAmmoType(e) {
    await this.setActivePayload(e);
  }
  canConsumeAmmo({ ammoTypeId: e = "" } = {}) {
    return this.canConsumePayload({ payloadId: e });
  }
  async consumeAmmo({ ammoTypeId: e = "" } = {}) {
    return this.consumePayload({ payloadId: e });
  }
  async createAmmoType(e = {}) {
    await this.createPayload(e);
  }
  async deleteAmmoType(e) {
    await this.deletePayload(e);
  }
  async updateAmmoType(e, t, n) {
    const a = t === "name" ? "label" : t === "damageType" ? "modifies.damageType" : t === "apMod" ? "modifies.ap" : t.startsWith("attackRatingBandMod.") ? `modifies.attackRatingBand.${t.split(".")[1]}` : t === "traits" ? "traits" : t === "keywords" ? "keywords" : t;
    await this.updatePayloadField(e, a, n);
  }
  async createAmmoTypeStandardTrait(e, t = {}) {
    await this.createPayloadStandardTrait(e, t);
  }
  async deleteAmmoTypeStandardTrait(e, t) {
    await this.deletePayloadStandardTrait(e, t);
  }
  async updateAmmoTypeStandardTrait(e, t, n, a) {
    await this.updatePayloadStandardTrait(e, t, n, a);
  }
  getCombatProfile({ payloadId: e = "", ammoTypeId: t = "" } = {}) {
    if (!this.isWeapon()) return null;
    const n = this.system ?? {}, a = this.isMechWeapon() ? hr(n.weaponCategory ?? n.category) : String(n.category ?? n.weaponCategory ?? "ranged").trim() || "ranged", r = this.isMechWeapon() ? Vo(n.range, a) : Sn(n.range), s = this.isMechWeapon() ? is(a, n.skill) : String(n.skill ?? "").trim() || "firearms", o = Lt(s), l = Number(n.damage ?? 0) || 0, c = Sb({
      damageType: n.damageType,
      ap: Number(n.ap ?? n.armorPiercing ?? 0) || 0,
      attackRatingBand: Za(n.attackRatingBand),
      traits: Xa(n.traits),
      keywords: sb(n.keywords),
      standardTraits: [],
      resolution: cs(n.resolution, "standard"),
      fireModes: us(n.fireModes),
      payloads: Ti(n.payloads, { legacyAmmo: n.ammo, category: a }),
      selectedPayloadId: pn(n.selectedPayloadId, n.payloads, { legacyAmmo: n.ammo, category: a }),
      consumptionSources: Fn(n.consumptionSources, { legacyAmmo: n.ammo }),
      payloadId: e || t,
      actor: this.actor ?? null,
      category: a
    });
    return {
      id: this.id ?? "weapon",
      uuid: this.uuid ?? null,
      name: this.name ?? "Weapon",
      img: this.img,
      item: this,
      type: this.canonicalType,
      equipped: !!n.equipped,
      isPrimary: !!n.isPrimary,
      category: a,
      skill: s,
      skillDef: o,
      damage: l,
      clusteringDice: Number(c.clusteringDice ?? 0) || 0,
      ap: c.ap,
      damageType: c.damageType,
      damageTypeLabel: Pi(c.damageType),
      attackRatingBand: c.attackRatingBand,
      range: r,
      defaultRangeBand: this.getDefaultRangeBand(r),
      traits: c.traits,
      keywords: c.keywords,
      standardTraits: c.standardTraits,
      effects: c.effects,
      payloadLabel: c.payloadLabel,
      payload: c.payload,
      payloadState: c.payloadState,
      source: c.source,
      sourceState: c.sourceState,
      template: c.template,
      areaEffect: c.areaEffect,
      resolution: c.resolution,
      resolverKey: c.resolverKey,
      fireModes: c.fireModes,
      capabilityReport: c.capabilityReport,
      ammoLabel: c.payloadLabel,
      ammoType: c.payload,
      ammoState: c.ammoState,
      heat: Math.max(0, Number(n.heat ?? 0) || 0),
      area: String(n.area ?? "none").trim() || "none",
      volatile: !!n.volatile,
      notes: String(n.notes ?? n.description ?? "").trim()
    };
  }
  getArmorProfile({ actor: e = this.actor } = {}) {
    var d, m;
    if (!this.isArmor()) return null;
    const t = this.system ?? {}, n = Math.max(0, Number(t.rating ?? 0)), a = Math.max(0, Number(((d = t == null ? void 0 : t.durability) == null ? void 0 : d.max) ?? n)), r = Math.min(
      a,
      Math.max(0, Number(((m = t == null ? void 0 : t.durability) == null ? void 0 : m.current) ?? a))
    ), s = Math.min(n, r), o = Ki((t == null ? void 0 : t.mitigationByType) ?? (t == null ? void 0 : t.mitigation)), l = zo({
      standardTraits: Xi(t == null ? void 0 : t.standardTraits),
      traits: Xa(t == null ? void 0 : t.traits),
      traitState: t == null ? void 0 : t.traitState
    }), c = ls(t == null ? void 0 : t.tags), u = Vc(s);
    return {
      id: this.id ?? "armor",
      uuid: this.uuid ?? null,
      name: this.name ?? "Armor",
      img: this.img,
      type: this.canonicalType,
      item: this,
      actor: e,
      equipped: !!t.equipped,
      isPrimary: !!t.isPrimary,
      rating: n,
      defenseBonus: Number(t.defenseBonus ?? 0) || 0,
      currentArmorRating: s,
      ratingCurrent: s,
      remainingDurability: r,
      baseMitigation: u,
      baseResistance: u,
      mitigationByType: Df(o, l.mitigationByType),
      tags: c,
      isDestroyed: r <= 0,
      durability: {
        current: r,
        max: a
      },
      traitState: l.traitState,
      standardTraits: Xi(t.standardTraits),
      traits: Ab({
        traits: Xa(t.traits),
        standardTraits: Xi(t.standardTraits)
      }),
      notes: String(t.notes ?? "").trim()
    };
  }
  getDefaultRangeBand(e = Sn(((t) => (t = this.system) == null ? void 0 : t.range)())) {
    return u0(e);
  }
  isWeaponSkill(e) {
    return ((e == null ? void 0 : e.canonicalType) ?? (e == null ? void 0 : e.type)) === A.itemType.skill && e.system.code === this.system.skill;
  }
  getWeaponSkill() {
    var n;
    const e = (n = this.actor) == null ? void 0 : n.items.find((a) => this.isWeaponSkill(a));
    if (e) return e;
    const t = game.items.find((a) => this.isWeaponSkill(a));
    return t || qd(this.system.skill);
  }
  getDefense() {
    var t, n, a;
    if (this.isMechWeapon()) {
      const r = Lt(is(((t = this.system) == null ? void 0 : t.weaponCategory) ?? ((n = this.system) == null ? void 0 : n.category), (a = this.system) == null ? void 0 : a.skill));
      return r != null && r.defense ? it.fixedDefenseCode(r.defense) : void 0;
    }
    if (this.system.defense)
      return it.fixedDefenseCode(this.system.defense);
    const e = Lt(String(this.system.skill ?? "").trim());
    return e != null && e.defense ? it.fixedDefenseCode(e.defense) : void 0;
  }
  getDamage() {
    if (!this.parent) return;
    const e = this.isWeapon() ? this.getCombatProfile() : null, t = this._getMonitor();
    if (this.isMechWeapon())
      return {
        value: Math.max(0, Number((e == null ? void 0 : e.damage) ?? this.system.damage ?? 0) || 0),
        monitor: t,
        damageType: (e == null ? void 0 : e.damageType) ?? this.system.damageType,
        damageTypeLabel: (e == null ? void 0 : e.damageTypeLabel) ?? this.getDamageTypeLabel(),
        noArmor: !1,
        armorMode: "",
        clusteringDice: Number((e == null ? void 0 : e.clusteringDice) ?? 0) || 0
      };
    const n = this.system.damageAttribute ? this.parent.getAttributeValue(this.system.damageAttribute) ?? 0 : 0;
    return {
      value: m0(
        t,
        this.system.damage,
        this.system.damageAttribute,
        n
      ),
      monitor: t,
      damageType: (e == null ? void 0 : e.damageType) ?? this.system.damageType,
      damageTypeLabel: (e == null ? void 0 : e.damageTypeLabel) ?? this.getDamageTypeLabel(),
      noArmor: this.system.noArmor ?? this.system.armorAvoidance,
      armorMode: p0(t, this.system.noArmor ?? this.system.armorAvoidance)
    };
  }
  getDamageCode() {
    var e, t;
    if (this.isMechWeapon()) {
      const n = Math.max(0, Number(((e = this.system) == null ? void 0 : e.damage) ?? 0) || 0), a = Math.max(0, Number(((t = this.getCombatProfile()) == null ? void 0 : t.clusteringDice) ?? 0) || 0);
      return a > 0 ? `${n} + ${a}d6 cluster` : String(n);
    }
    return f0(
      this._getMonitor(),
      this.system.damage,
      this.system.damageAttribute
    );
  }
  getDamageTypeLabel() {
    var t, n, a;
    if (this.isWeapon())
      return Pi(((t = this.getCombatProfile()) == null ? void 0 : t.damageType) ?? this.system.damageType);
    const e = ((n = q.mwd.weaponDamageType) == null ? void 0 : n[this.system.damageType]) ?? ((a = q.mwd.personalDamageType) == null ? void 0 : a[this.system.damageType]);
    return e || this.system.damageType;
  }
  getRanges() {
    return d0(Sn(this.system.range)).filter((e) => e.allowed).map((e) => ({ value: e.value, labelkey: e.labelkey }));
  }
  validateTargets(e) {
    var s;
    const t = (s = this.getDamage()) == null ? void 0 : s.monitor, n = Ei.getTargetTokens(game.user), a = n.filter((o) => {
      var l;
      return (l = o.actor) == null ? void 0 : l.canReceiveDamage(t);
    }), r = n.filter((o) => {
      var l;
      return !((l = o.actor) != null && l.canReceiveDamage(t));
    }).map((o) => o.name);
    if (r.length > 0) {
      const o = Ke(q.common.errors.ignoredTargets, {
        targets: r.reduce(be.joiner(", "))
      });
      ui.notifications.info(o);
    }
    if (a.length === 0) {
      const o = Ke(q.common.errors.noTargetSelected, {
        weapon: this.name ?? q.itemType.singular.weapon
      });
      ui.notifications.info(o);
    } else
      this.checkWeaponTargetsCount(a);
    return a;
  }
  checkWeaponTargetsCount(e) {
    const t = this.system.area, n = Hd[t] ?? {};
    la.checkTargetsCount(n.targets ?? 0, e, t);
  }
  getAreaModifier(e) {
    const t = this.getArea(), n = Hd[t] ?? {};
    return n.targets && n.adjust && e <= n.targets ? n.adjust[e - 1] ?? 0 : 0;
  }
  getArea() {
    return this.system.area === "" ? A.area.none : this.system.area ?? A.area.none;
  }
  _getMonitor() {
    return this.isWeapon() ? A.monitors.physical : this.system.monitor || A.monitors.physical;
  }
  isKnowledgeSkill() {
    return this.isSkill() && this.system.code === "knowledge";
  }
  isGeneralSkill() {
    return this.isSkill() && this.system.code !== "knowledge";
  }
  prepareShortcut() {
    if (this.isSkill())
      return {
        img: this.img,
        label: this.name,
        callback: (e) => e.actor.rollSkill(this)
      };
    if (this.isWeapon())
      return {
        img: this.img,
        label: this.name,
        callback: (e) => e.actor.rollWeapon(this)
      };
  }
  prepateShortcut() {
    return this.prepareShortcut();
  }
};
Br = new WeakMap(), Ri = new WeakSet(), ql = async function(e) {
  if (typeof (e == null ? void 0 : e.syncEquippedActorEffects) == "function")
    try {
      await e.syncEquippedActorEffects();
    } catch (t) {
      console.error(`${Le}Failed to sync equipped item effects`, { item: e, error: t });
    }
}, Ig = async function(e) {
  if (typeof (e == null ? void 0 : e.removeSyncedActorEffects) == "function")
    try {
      await e.removeSyncedActorEffects({ actor: e.actor ?? e.parent ?? null });
    } catch (t) {
      console.error(`${Le}Failed to remove synced item effects`, { item: e, error: t });
    }
}, Ss = async function(e) {
  const t = e == null ? void 0 : e.parent;
  if (typeof (t == null ? void 0 : t.syncEquippedActorEffects) == "function")
    try {
      await t.syncEquippedActorEffects();
    } catch (n) {
      console.error(`${Le}Failed to sync parent item effects`, { effect: e, error: n });
    }
}, Ce(Ut, Ri), Ce(Ut, Br, !1), z(Ut, "RANGE_ORDER", ra), z(Ut, "EQUIPPED_EFFECT_FLAG", "equippedItemSync"), z(Ut, "DEFAULT_UNARMED", Object.freeze({
  id: "unarmed",
  name: "Unarmed",
  category: "melee",
  skill: "meleeCombat",
  damage: 1,
  ap: 0,
  damageType: "concussive",
  attackRatingBand: { close: 0, near: 0, far: 0, extreme: 0 },
  range: { max: "close", close: 0, near: 0, far: 0, extreme: 0 },
  standardTraits: [],
  keywords: [],
  resolution: { resolverKey: "standard", damageModel: "", onHitEffect: null },
  fireModes: {
    single: { enabled: !1 },
    burst: { enabled: !1 },
    fullAuto: { enabled: !1 }
  },
  payloads: [],
  selectedPayloadId: "",
  consumptionSources: [{
    id: "untracked",
    label: "Untracked",
    kind: "untracked",
    tracking: { current: 0, max: 0 },
    link: { actorPath: "", itemId: "", itemPath: "" }
  }],
  payloadState: {
    payloads: [],
    activePayloadId: "",
    payloadLabel: "",
    sourceId: "",
    sourceLabel: "",
    sourceKind: "",
    current: 0,
    max: 0,
    consumePerUse: 1,
    isTracked: !1
  },
  payloadLabel: "",
  traits: [],
  notes: ""
}));
let Ua = Ut;
const Vd = {
  none: { targets: 1, adjust: [0] },
  shotgun: { targets: 2, adjust: [0, -2] },
  circle: { targets: void 0 },
  cone: { targets: void 0 },
  rect: { targets: void 0 },
  ray: { targets: void 0 }
}, h0 = {
  code: "weapon-range",
  options: {
    flags: { editable: !0 },
    order: 20,
    category: Pe.pool,
    labelkey: q.common.roll.modifiers.weaponRange,
    hbsTemplateRoll: `${se}/roll/parts/select-option.hbs`,
    hbsTemplateChat: void 0
    //``
  },
  isUsed: (i) => !0,
  condition: (i) => i.weapon,
  factory: (i) => {
    const e = i.weapon.getRanges(), t = e.map((n) => n.value);
    return {
      value: e[0].value,
      min: Math.min(...t),
      max: Math.max(...t),
      choices: e,
      selected: e[0].labelkey
    };
  }
}, y0 = {
  code: "weapon-area",
  options: {
    used: !0,
    order: 20,
    category: Pe.pool,
    labelkey: q.common.roll.modifiers.weaponArea,
    hbsTemplateRoll: `${se}/roll/parts/input-numeric.hbs`,
    hbsTemplateChat: void 0
    //``
  },
  isUsed: (i) => i.used,
  condition: (i) => i.weapon && i.weapon.getArea() != A.area.none,
  factory: (i) => {
    var n;
    const e = ((n = i.targeting.targetedTokenIds) == null ? void 0 : n.length) ?? 1, t = i.weapon.getAreaModifier(e);
    return {
      value: t,
      min: Math.min(0, t),
      max: Math.max(0, t),
      used: e > 1
    };
  }
}, Ne = class Ne extends Ua {
  static buildDefaultUnarmedProfile(e = null) {
    var a, r, s, o, l, c, u, d;
    const t = Math.max(0, Number(
      ((a = e == null ? void 0 : e.getAttributeValue) == null ? void 0 : a.call(e, A.actorAttributes.strength)) ?? ((o = (s = (r = e == null ? void 0 : e.system) == null ? void 0 : r.attributes) == null ? void 0 : s.strength) == null ? void 0 : o.value) ?? 0
    ) || 0), n = Math.max(0, Number(
      ((l = e == null ? void 0 : e.getAttributeValue) == null ? void 0 : l.call(e, A.actorAttributes.reflexes)) ?? ((d = (u = (c = e == null ? void 0 : e.system) == null ? void 0 : c.attributes) == null ? void 0 : u.reflexes) == null ? void 0 : d.value) ?? 0
    ) || 0);
    return {
      ...foundry.utils.deepClone(this.DEFAULT_UNARMED),
      damage: Math.ceil(t / 2),
      attackRatingBand: {
        ...this.DEFAULT_UNARMED.attackRatingBand,
        close: n
      },
      range: {
        ...this.DEFAULT_UNARMED.range,
        max: "close"
      },
      uuid: null,
      img: null,
      item: null,
      equipped: !0,
      isPrimary: !1,
      defaultRangeBand: "close",
      isSynthetic: !0
    };
  }
  static init() {
    Hooks.once(ii.REGISTER_ROLL_PARAMETERS, (e) => {
      e(y0), e(h0);
    });
  }
  prepareBaseData() {
    if (super.prepareBaseData(), (this.canonicalType ?? this.type) !== A.itemType.personalWeapon) return;
    const e = this.system ?? {};
    e.equipped = !!e.equipped, e.isPrimary = !!e.isPrimary, e.category = String(e.category ?? e.weaponCategory ?? "ranged").trim() || "ranged", e.skill = String(e.skill ?? "firearms").trim() || "firearms", e.ap = Number(e.ap ?? e.armorPiercing ?? 0) || 0, e.damage = Number(e.damage ?? 0) || 0, e.damageType = ci(e.damageType), e.attackRatingBand = Ne.normalizeAttackRatingBand(e.attackRatingBand), e.range = Ne.normalizePersonalRangeData(e.range), e.traits = Ne.normalizeTraits(e.traits), e.notes = String(e.notes ?? "").trim();
  }
  static maxIndex(e) {
    const t = Ne.RANGE_ORDER.indexOf(e);
    return t >= 0 ? t : Ne.RANGE_ORDER.indexOf("near");
  }
  /**
    * Compute UI-friendly range band data:
    * - cap: normalized max band
    * - bands: [{key, allowed, value}]
    * - optimalKey: highest value among allowed (tie -> closest)
    */
  static getRangeBands(e) {
    const t = e ?? {}, n = Ne.normalizeRangeKey(t.max ?? "near"), a = Ne.maxIndex(n), r = Ne.RANGE_ORDER.map((l, c) => ({
      key: l,
      allowed: c <= a,
      value: Number(t[l] ?? (l === "extreme" && t.long !== void 0 ? t.long : 0))
    }));
    let s = "close", o = -1 / 0;
    for (const l of r)
      l.allowed && l.value > o && (o = l.value, s = l.key);
    return { cap: n, bands: r, optimalKey: s };
  }
  static get defaultIcon() {
    return "systems/mwd/img/colt-m1911.svg";
  }
  static defaultIconForType(e) {
    return e === A.itemType.mechWeapon ? "systems/mwd/img/icons/systems/upgrades/grenade_launcher_underbarrel.svg" : this.defaultIcon;
  }
  static normalizeTraits(e) {
    return ja(e);
  }
  static normalizePersonalRangeData(e) {
    const t = vu(e);
    return t.max = Ne.normalizeRangeKey(t.max ?? (e == null ? void 0 : e.max) ?? "extreme"), t;
  }
  static normalizeRangeData(e) {
    return {
      max: Ne.normalizeRangeKey((e == null ? void 0 : e.max) ?? "near"),
      close: Number((e == null ? void 0 : e.close) ?? (e == null ? void 0 : e.short) ?? 0) || 0,
      near: Number((e == null ? void 0 : e.near) ?? (e == null ? void 0 : e.medium) ?? 0) || 0,
      far: Number((e == null ? void 0 : e.far) ?? (e == null ? void 0 : e.long) ?? 0) || 0,
      extreme: Number((e == null ? void 0 : e.extreme) ?? 0) || 0
    };
  }
  static normalizeAttackRatingBand(e) {
    return {
      close: Number((e == null ? void 0 : e.close) ?? (e == null ? void 0 : e.short) ?? 0) || 0,
      near: Number((e == null ? void 0 : e.near) ?? (e == null ? void 0 : e.medium) ?? 0) || 0,
      far: Number((e == null ? void 0 : e.far) ?? (e == null ? void 0 : e.long) ?? 0) || 0,
      extreme: Number((e == null ? void 0 : e.extreme) ?? 0) || 0
    };
  }
  getCombatProfile(e = {}) {
    if ((this.canonicalType ?? this.type) === A.itemType.personalWeapon)
      return super.getCombatProfile(e);
    const t = this.system ?? {}, n = this.canonicalType ?? this.type, a = n === A.itemType.personalWeapon ? Ne.normalizePersonalRangeData(t.range) : Ne.normalizeRangeData(t.range), r = String(t.skill ?? "").trim(), s = Lt(r), o = Number(t.damage ?? 0) || 0, l = Number(t.ap ?? t.armorPiercing ?? 0) || 0, c = String(t.category ?? t.weaponCategory ?? "ranged").trim() || "ranged", u = Ne.normalizeTraits(t.traits);
    return {
      id: this.id ?? "weapon",
      uuid: this.uuid ?? null,
      name: this.name ?? "Weapon",
      img: this.img,
      item: this,
      type: n,
      equipped: !!t.equipped,
      isPrimary: !!t.isPrimary,
      category: c,
      skill: r || "firearms",
      skillDef: s,
      damage: o,
      ap: l,
      damageType: n === A.itemType.personalWeapon ? ci(t.damageType) : String(t.damageType ?? "kinetic").trim() || "kinetic",
      attackRatingBand: Ne.normalizeAttackRatingBand(t.attackRatingBand),
      range: a,
      defaultRangeBand: this.getDefaultRangeBand(a),
      traits: u,
      effects: {},
      notes: String(t.notes ?? t.description ?? "").trim()
    };
  }
  getDefaultRangeBand(e = Ne.normalizeRangeData(((t) => (t = this.system) == null ? void 0 : t.range)())) {
    const n = ["near", "close", "far", "extreme"], a = Ne.maxIndex(e.max);
    return n.find((r) => Ne.RANGE_ORDER.indexOf(r) <= a) ?? "close";
  }
  getWeaponSkill() {
    var n;
    const e = (n = this.actor) == null ? void 0 : n.items.find(
      (a) => a.type === A.itemType.skill && a.system.code === this.system.skill
    );
    if (e) return e;
    const t = Lt(String(this.system.skill ?? "").trim());
    return t ? {
      name: t.label,
      system: {
        code: t.code,
        attribute: t.attribute,
        value: 0
      }
    } : null;
  }
  getDefense() {
    if ((this.canonicalType ?? this.type) !== A.itemType.personalWeapon)
      return this.system.defense ? it.fixedDefenseCode(this.system.defense) : void 0;
    if (this.system.defense)
      return it.fixedDefenseCode(this.system.defense);
    const e = Lt(String(this.system.skill ?? "").trim());
    return e != null && e.defense ? it.fixedDefenseCode(e.defense) : void 0;
  }
  getDamage() {
    if (!this.parent)
      return;
    const e = this._getMonitor(), t = this.system.damageAttribute ? this.parent.getAttributeValue(this.system.damageAttribute) ?? 0 : 0;
    return {
      value: Ne.damageValue(
        e,
        this.system.damage,
        this.system.damageAttribute,
        t
      ),
      monitor: e,
      damageType: this.system.damageType,
      damageTypeLabel: this.getDamageTypeLabel(),
      noArmor: this.system.noArmor ?? this.system.armorAvoidance,
      armorMode: Ne.armorMode(e, this.system.noArmor ?? this.system.armorAvoidance)
    };
  }
  static damageValue(e, t, n, a) {
    if (t = Number(t), n)
      if (a !== void 0)
        t = t + Math.ceil(Number(a) / 2);
      else
        return console.warn("Weapon not attached to an actor"), q.item.personalWeapon.weaponWithoutActor;
    return t;
  }
  getDamageCode() {
    return Ne.damageCode(
      this._getMonitor(),
      this.system.damage,
      this.system.damageAttribute
    );
  }
  static damageCode(e, t, n) {
    let a = "";
    return n && q.attributes[n] && (a += q.attributes[n].substring(0, 3).toUpperCase() + "/2 + "), a += String(t), a;
  }
  static armorMode(e, t) {
    return te.useArmor(e) ? t ? "noArmor" : "withArmor" : "";
  }
  getDamageTypeLabel() {
    if ((this.canonicalType ?? this.type) === A.itemType.personalWeapon)
      return Pi(this.system.damageType);
    const e = q.mwd.weaponDamageType[this.system.damageType] ?? q.mwd.personalDamageType[this.system.damageType];
    return e || this.system.damageType;
  }
  getRanges() {
    const e = (this.canonicalType ?? this.type) === A.itemType.personalWeapon, t = e ? Ne.normalizePersonalRangeData(this.system.range) : Ne.normalizeRangeData(this.system.range);
    return Ne.getRangeList(t, {
      personalScale: e
    }).filter((n) => n.allowed).map((n) => ({ value: n.value, labelkey: n.labelkey }));
  }
  _getRange(e) {
    return { value: this.system.range[e], labelkey: Oe.getFromList(Oe.getEnums().ranges, e) };
  }
  static getRangeList(e, { personalScale: t = !1 } = {}) {
    const n = Ne.normalizeRangeKey(e == null ? void 0 : e.max), a = Ne.RANGE_ORDER.indexOf(n);
    return Ne.RANGE_ORDER.map((r, s) => ({
      key: r,
      allowed: a >= 0 ? s <= a : s === 0,
      value: (e == null ? void 0 : e[r]) ?? (r === "extreme" && (e == null ? void 0 : e.long) !== void 0 ? e.long : void 0),
      labelkey: t ? Gs(r) : Oe.getFromList(Oe.getEnums().ranges, r)
    }));
  }
  static normalizeRangeKey(e) {
    return e === "long" ? "extreme" : e;
  }
  prepareShortcut() {
    return {
      img: this.img,
      label: this.name,
      callback: (e) => e.actor.rollWeapon(this)
    };
  }
  validateTargets(e) {
    var s;
    const t = (s = this.getDamage()) == null ? void 0 : s.monitor, n = Ei.getTargetTokens(game.user), a = n.filter((o) => {
      var l;
      return (l = o.actor) == null ? void 0 : l.canReceiveDamage(t);
    }), r = n.filter((o) => {
      var l;
      return !((l = o.actor) != null && l.canReceiveDamage(t));
    }).map((o) => o.name);
    if (r.length > 0) {
      const o = Ke(q.common.errors.ignoredTargets, {
        targets: r.reduce(be.joiner(", "))
      });
      ui.notifications.info(o);
    }
    if (a.length == 0) {
      const o = Ke(q.common.errors.noTargetSelected, {
        weapon: this.name ?? q.itemType.singular.weapon
      });
      ui.notifications.info(o);
    } else
      this.checkWeaponTargetsCount(a);
    return a;
  }
  checkWeaponTargetsCount(e) {
    const t = this.system.area, n = Vd[t] ?? {};
    la.checkTargetsCount(n.targets ?? 0, e, t);
  }
  getAreaModifier(e) {
    const t = this.getArea(), n = Vd[t] ?? {};
    return n.targets && n.adjust && e <= n.targets ? n.adjust[e - 1] ?? 0 : 0;
  }
  getArea() {
    return this.system.area == "" ? A.area.none : this.system.area ?? A.area.none;
  }
  _getMonitor() {
    return (this.canonicalType ?? this.type) === A.itemType.personalWeapon ? A.monitors.physical : this.system.monitor || A.monitors.physical;
  }
};
z(Ne, "RANGE_ORDER", ["close", "near", "far", "extreme"]), z(Ne, "DEFAULT_UNARMED", Ua.DEFAULT_UNARMED);
let yi = Ne;
function b0(i) {
  const e = [];
  for (let [t, n] of Object.entries(i ?? {}))
    n !== void 0 && (t = t.replace(/[A-Z]+(?![a-z])|[A-Z]/g, (a, r) => (r ? "-" : "") + a.toLowerCase()), e.push(`data-${t}="${Handlebars.escapeExpression(n)}"`));
  return new Handlebars.SafeString(e.join(" "));
}
function S0({ hash: i }) {
  return i;
}
function A0() {
  var i, e;
  return ((e = (i = foundry == null ? void 0 : foundry.applications) == null ? void 0 : i.handlebars) == null ? void 0 : e.Handlebars) ?? Handlebars;
}
class Ru {
  constructor() {
    this.registerHelpers(), Hooks.once("init", () => {
      this.registerHelpers(), console.log(`${Le}Handlebars helpers registered (init)`);
    }), console.log(`${Le}Handlebars helpers registered (ctor)`);
  }
  registerHelpers() {
    const e = A0(), t = {
      // Foundry utils
      getProperty: foundry.utils.getProperty,
      // DND5E-inspired utilities
      "mwd-dataset": b0,
      "mwd-object": S0,
      // Simple comparisons
      eq: (n, a) => n === a,
      ne: (n, a) => n !== a,
      // Strings/arrays
      concat: (...n) => be.join(n.slice(0, -1)),
      join: (n, a = " ") => Array.isArray(n) ? n.join(a) : "",
      includes: (n, a) => n == null ? void 0 : n.includes(a),
      length: (n) => (n == null ? void 0 : n.length) || 0,
      substring: (n, a, r) => n == null ? void 0 : n.substring(a, r),
      toUpperCase: wy.toUpperCaseNoAccent,
      // Math
      modulo: (n, a) => n % a,
      divint: be.divint,
      divup: be.divup,
      sum: (n, a) => n + a,
      diff: (n, a) => n - a,
      times: (n, a) => n * a,
      min: (n, a) => Math.min(n, a),
      max: (n, a) => Math.max(n, a),
      // Utility blocks
      for: Ru.hbsForLoop,
      // fixes “Missing helper: for”
      range: (n, a) => Array.from({ length: a - n + 1 }, (r, s) => n + s),
      ifGte: (n, a, r) => n >= a ? r.fn(this) : r.inverse(this),
      // Damage / weapons (legacy-compatible)
      weaponDamageLetter: Ay.letter,
      weaponDamageCode: yi.damageCode,
      weaponDamageValue: yi.damageValue,
      weaponArmorMode: yi.armorMode,
      weaponRangeList: yi.getRangeList,
      // Icons
      iconFA: re.fontAwesome,
      iconSrc: re.iconSystemPath,
      iconPath: re.iconPath,
      iconD6: re.iconD6,
      // Enums
      localizeAttribute: Oe.localizeAttribute
    };
    e.registerHelper(t), e !== Handlebars && Handlebars.registerHelper(t);
  }
  static hbsForLoop(e, t, n) {
    let a = "";
    for (let r = e; r < t; ++r) a += n.fn(r);
    return a;
  }
}
const Yd = "sheetTheme", Vl = "mwd-theme-default", w0 = "mwd-theme-sra", T0 = [
  { name: "Default (CSB)", cssClass: Vl },
  { name: "SRA", cssClass: w0 }
];
class k0 {
  constructor() {
    this.availableStyles = {}, ea.register(ii.REGISTER_STYLES), Hooks.once(ii.REGISTER_STYLES, (e) => T0.forEach((t) => e(t.cssClass, t.name))), Hooks.once("ready", () => this.onReady());
  }
  async onReady() {
    Hooks.callAll(ii.REGISTER_STYLES, (e, t) => this.availableStyles[e] = t), console.log(Le + "Loaded styles", this.availableStyles), game.settings.register(M, Yd, {
      scope: "world",
      name: "Sheet Theme",
      hint: "Select the visual theme used by MWD sheets.",
      config: !0,
      default: Vl,
      choices: this.availableStyles,
      type: String,
      onChange: () => {
        setTimeout(() => {
          var e, t;
          for (const n of Object.values(ui.windows ?? {})) {
            if (typeof (n == null ? void 0 : n.render) != "function") continue;
            const a = n.element instanceof HTMLElement ? n.element : (e = n.element) == null ? void 0 : e[0];
            (t = a == null ? void 0 : a.classList) != null && t.contains("actor-sheet-v2") && n.render(!1);
          }
        }, 0);
      }
    });
  }
  selectCssClass() {
    const e = game.settings.get(M, Yd);
    return this.availableStyles[e] ? e : Vl;
  }
}
function Jd(i) {
  const e = Number(i ?? 0);
  return Number.isFinite(e) ? Math.trunc(e) : 0;
}
function lr(i) {
  return i === A.monitors.physical ? "Physical" : i === A.monitors.fatigue ? "Fatigue" : String(i ?? "").trim() || "Track";
}
function M0({
  incomingDamage: i = 0,
  armorBefore: e = 0,
  reinforcedBefore: t = 0,
  reinforcedMax: n = 0,
  hasArmorItem: a = !1
} = {}) {
  const r = {
    armorBefore: Math.max(0, Number(e ?? 0) || 0),
    armorAfter: Math.max(0, Number(e ?? 0) || 0),
    reinforcedBefore: Math.max(0, Number(t ?? 0) || 0),
    reinforcedAfter: Math.max(0, Number(t ?? 0) || 0),
    reinforcedMax: Math.max(0, Number(n ?? 0) || 0),
    update: {}
  };
  return !a || Math.max(0, Number(i ?? 0) || 0) <= 0 ? r : r.reinforcedBefore > 0 ? (r.reinforcedAfter = Math.max(0, r.reinforcedBefore - 1), r.reinforcedAfter !== r.reinforcedBefore && (r.update["system.traitState.reinforced.current"] = r.reinforcedAfter), r) : (r.armorAfter = Math.max(0, r.armorBefore - 1), r.armorAfter !== r.armorBefore && (r.update["system.durability.current"] = r.armorAfter), r);
}
function yr(i) {
  return i ? (i == null ? void 0 : i.document) ?? i : null;
}
function Jo(i, e) {
  var n, a, r;
  if (!i) return null;
  const t = yr(e) ?? yr(i == null ? void 0 : i.token);
  return t ? t.isLinked ? t.baseActor ?? ((r = (n = game.actors) == null ? void 0 : n.get) == null ? void 0 : r.call(n, ((a = t == null ? void 0 : t.baseActor) == null ? void 0 : a.id) ?? "")) ?? t.actor ?? i : t.actor ?? i : i;
}
function ns(i, e) {
  var t, n, a;
  return Math.max(0, Number(((a = (n = (t = i == null ? void 0 : i.system) == null ? void 0 : t.monitors) == null ? void 0 : n[e]) == null ? void 0 : a.value) ?? 0) || 0);
}
function Qd(i) {
  var e, t;
  return Math.max(0, Number(((t = (e = i == null ? void 0 : i.system) == null ? void 0 : e.burn) == null ? void 0 : t.value) ?? 0) || 0);
}
function C0(i) {
  return (i == null ? void 0 : i.type) === A.actorTypes.character || (i == null ? void 0 : i.type) === A.actorTypes.npc;
}
function v0(i) {
  return (i == null ? void 0 : i.type) === A.actorTypes.vehicle || (i == null ? void 0 : i.type) === A.actorTypes.battlemech;
}
function E0(i) {
  return [
    A.actorTypes.character,
    A.actorTypes.npc,
    A.actorTypes.vehicle,
    A.actorTypes.battlemech
  ].includes(i == null ? void 0 : i.type);
}
function I0(i, e) {
  const t = String(i ?? "").trim();
  return t === "status" ? E0(e) : t === "machineAttackDamage" ? v0(e) : C0(e);
}
function N0(i, e) {
  var t;
  return ((t = iu(e).find((n) => n.id === i)) == null ? void 0 : t.label) ?? i;
}
function R0(i) {
  var n, a, r, s;
  const e = foundry.utils.escapeHTML, t = [];
  if (i.mode === "machineAttackDamage") {
    const o = i.appliedDelta >= 0 ? "Applied" : "Recovered";
    t.push(`<div><b>${o}:</b> ${Number(i.damageIncoming ?? i.requestedDelta ?? 0)} machine damage</div>`), (n = i.hitLocation) != null && n.locationLabel && t.push(`<div><b>Location:</b> ${e(i.hitLocation.locationLabel)} (${Number(i.hitLocation.rollTotal ?? 0)})</div>`), i.machine && (t.push(`<div><b>Armor:</b> ${Number(i.machine.armorBefore ?? 0)} -> ${Number(i.machine.armorAfter ?? 0)}</div>`), t.push(`<div><b>Structure:</b> ${Number(i.machine.structureBefore ?? 0)} -> ${Number(i.machine.structureAfter ?? 0)}</div>`)), (r = (a = i.critical) == null ? void 0 : a.records) != null && r.length ? t.push(`<div><b>Critical:</b> ${e(i.critical.records.map((l) => l.label).join(", "))}</div>`) : (s = i.critical) != null && s.reason && t.push(`<div><b>Critical:</b> ${e(i.critical.reason)}</div>`);
  }
  if (i.mode === "attackDamage" || i.mode === "trackDelta") {
    const o = i.appliedDelta >= 0 ? "Applied" : "Recovered", l = Math.abs(Number(i.appliedDelta ?? 0)), c = l === 1 ? "point" : "points", u = i.usedArmor ? ` via armor-aware ${e(Pi(i.damageType))}` : "";
    t.push(`<div><b>${o}:</b> ${l} ${c} to ${e(lr(i.track))}${u}</div>`), i.usedArmor && i.mitigation && (t.push(
      `<div><b>Mitigation:</b> base ${Number(i.mitigation.baseMitigation ?? 0)} + type ${Number(i.mitigation.typeMitigationMod ?? 0)} - AP ${Number(i.effectiveAp ?? 0)} = ${Number(i.mitigation.netResistance ?? 0)}</div>`
    ), Number(i.mitigation.reinforcedMax ?? 0) > 0 && t.push(
      `<div><b>Reinforced:</b> ${Number(i.mitigation.reinforcedAfter ?? 0)}/${Number(i.mitigation.reinforcedMax ?? 0)}</div>`
    ));
  }
  if (i.mode === "burnDelta") {
    const o = i.appliedDelta >= 0 ? "Adjusted Burn +" : "Adjusted Burn -";
    t.push(`<div><b>${o}</b>${Math.abs(Number(i.appliedDelta ?? 0))}</div>`);
  }
  return i.mode === "status" && t.push(
    `<div><b>Status:</b> ${i.active ? "Applied" : "Removed"} ${e(i.statusLabel ?? i.statusId ?? "Status")}</div>`
  ), t.push(`<div><b>Target:</b> ${e(i.actorName ?? "Actor")}</div>`), i.beforeLabel && i.afterLabel && t.push(`<div><b>Result:</b> ${e(i.beforeLabel)} -> ${e(i.afterLabel)}</div>`), i.source && t.push(`<div><b>Source:</b> ${e(i.source)}</div>`), i.notes && t.push(`<div><b>Notes:</b> ${e(i.notes)}</div>`), `<div class="mwd-gm-notice"><b>GM Harm:</b>${t.join("")}</div>`;
}
function P0(i) {
  var t, n;
  const e = (n = (t = game.settings) == null ? void 0 : t.get) == null ? void 0 : n.call(t, "core", "rollMode");
  return typeof ChatMessage.applyRollMode == "function" && ChatMessage.applyRollMode(i, e), i;
}
class Jt {
  static supportsActor(e, { mode: t = "" } = {}) {
    return I0(t, e);
  }
  static getActorOptions({ mode: e = "" } = {}) {
    return Array.from(game.actors ?? []).filter((t) => this.supportsActor(t, { mode: e })).sort((t, n) => String(t.name ?? "").localeCompare(String(n.name ?? ""))).map((t) => ({
      id: t.id,
      name: t.name || "Character"
    }));
  }
  static getStatusOptions(e = null) {
    return iu(e).map((t) => ({
      value: t.id,
      label: t.label,
      active: t.active
    }));
  }
  static getSceneTarget({ mode: e = "" } = {}) {
    var a, r;
    const t = Array.from(((a = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : a.controlled) ?? []);
    if (t.length > 1)
      return { actor: null, token: null, reason: "Select only one controlled token." };
    if (t.length === 1) {
      const s = yr(t[0]), o = Jo((s == null ? void 0 : s.actor) ?? null, s);
      return this._resolveSceneTargetResult(o, s, { mode: e });
    }
    const n = Array.from(((r = game.user) == null ? void 0 : r.targets) ?? []);
    if (n.length > 1)
      return { actor: null, token: null, reason: "Target only one token." };
    if (n.length === 1) {
      const s = yr(n[0]), o = Jo((s == null ? void 0 : s.actor) ?? null, s);
      return this._resolveSceneTargetResult(o, s, { mode: e });
    }
    return { actor: null, token: null, reason: "No controlled or targeted token." };
  }
  static _resolveSceneTargetResult(e, t, { mode: n = "" } = {}) {
    return !t || !e ? { actor: null, token: null, reason: "No controlled or targeted token." } : this.supportsActor(e, { mode: n }) ? {
      actor: e,
      token: t,
      reason: ""
    } : {
      actor: null,
      token: t,
      reason: `${e.name || "Token actor"} is not supported by the GM harm tool.`
    };
  }
  static resolveTarget({ actor: e = null, token: t = null, actorId: n = "", preferSceneTarget: a = !1, mode: r = "" } = {}) {
    var l, c;
    const s = yr(t);
    if (s) {
      const u = Jo((s == null ? void 0 : s.actor) ?? e, s), d = this._resolveSceneTargetResult(u, s, { mode: r });
      if (d.actor) return { ...d, source: "token" };
    }
    if (a) {
      const u = this.getSceneTarget({ mode: r });
      if (u.actor) return { ...u, source: "scene" };
    }
    if (e && this.supportsActor(e, { mode: r }))
      return { actor: e, token: s, reason: "", source: "actor" };
    const o = n ? ((c = (l = game.actors) == null ? void 0 : l.get) == null ? void 0 : c.call(l, n)) ?? null : null;
    return o && this.supportsActor(o, { mode: r }) ? { actor: o, token: null, reason: "", source: "fallback" } : {
      actor: null,
      token: s,
      source: null,
      reason: a && this.getSceneTarget({ mode: r }).reason || "Choose a supported target."
    };
  }
  static async apply({ actor: e = null, token: t = null, payload: n = {}, options: a = {} } = {}) {
    var c;
    const r = String((n == null ? void 0 : n.mode) ?? "").trim(), s = this.resolveTarget({
      actor: e,
      token: t,
      actorId: a.actorId ?? "",
      preferSceneTarget: !!a.preferSceneTarget,
      mode: r
    });
    if (!s.actor)
      return { ok: !1, reason: s.reason || "Choose a supported target." };
    let o;
    switch (String((n == null ? void 0 : n.mode) ?? "").trim()) {
      case "attackDamage":
        o = await this._applyAttackDamage(s.actor, n, a);
        break;
      case "machineAttackDamage":
        o = await this._applyMachineAttackDamage(s.actor, s.token, n, a);
        break;
      case "trackDelta":
        o = await this._applyTrackDelta(s.actor, n, a);
        break;
      case "burnDelta":
        o = await this._applyBurnDelta(s.actor, n);
        break;
      case "status":
        o = await this._applyStatus(s.actor, n);
        break;
      default:
        return { ok: !1, reason: "Unsupported harm mode." };
    }
    const l = {
      ok: !0,
      actor: s.actor,
      token: s.token,
      actorName: s.actor.name || "Character",
      sourceType: s.source,
      dryRun: !!a.dryRun,
      ...o
    };
    if (a.logToChat && !a.dryRun) {
      const u = R0(l), d = P0({
        speaker: ChatMessage.getSpeaker({ actor: s.actor, token: s.token }),
        content: u
      });
      await ChatMessage.create(d);
    }
    return a.dryRun || (c = F.renderOpenCharacterSheets) == null || c.call(F, s.actor.id), l;
  }
  static async _applyTrackDelta(e, t, n = {}) {
    const a = (t == null ? void 0 : t.track) === A.monitors.fatigue ? A.monitors.fatigue : A.monitors.physical, r = Jd((t == null ? void 0 : t.delta) ?? (t == null ? void 0 : t.amount) ?? 0);
    if (!!(t != null && t.useArmor) && r > 0)
      return this._applyPersonalArmorAwareDamage(e, {
        mode: "trackDelta",
        track: a,
        damage: r,
        damageType: t == null ? void 0 : t.damageType,
        ap: (t == null ? void 0 : t.ap) ?? 0,
        effects: (t == null ? void 0 : t.effects) ?? {},
        source: t == null ? void 0 : t.source,
        notes: t == null ? void 0 : t.notes
      }, n);
    const o = ns(e, a);
    n.dryRun || await te.addCounter(e, a, r);
    const l = n.dryRun ? Math.max(0, o + r) : ns(e, a);
    return {
      mode: "trackDelta",
      track: a,
      requestedDelta: r,
      appliedDelta: l - o,
      usedArmor: !1,
      beforeLabel: `${lr(a)} ${o}`,
      afterLabel: `${lr(a)} ${l}`,
      source: String((t == null ? void 0 : t.source) ?? "").trim(),
      notes: String((t == null ? void 0 : t.notes) ?? "").trim()
    };
  }
  static async _applyBurnDelta(e, t) {
    var l, c;
    const n = Jd((t == null ? void 0 : t.delta) ?? (t == null ? void 0 : t.amount) ?? 0), a = Qd(e), r = Math.max(0, a + n), s = { "system.burn.value": r };
    r === 0 && ((c = (l = e.system) == null ? void 0 : l.burn) != null && c.overloaded) && (s["system.burn.overloaded"] = !1), await e.update(s);
    const o = Qd(e);
    return {
      mode: "burnDelta",
      requestedDelta: n,
      appliedDelta: o - a,
      beforeLabel: `Burn ${a}`,
      afterLabel: `Burn ${o}`,
      source: String((t == null ? void 0 : t.source) ?? "").trim(),
      notes: String((t == null ? void 0 : t.notes) ?? "").trim()
    };
  }
  static async _applyStatus(e, t) {
    const n = String((t == null ? void 0 : t.statusId) ?? (t == null ? void 0 : t.status) ?? "").trim();
    if (!n)
      return { mode: "status", statusId: "", active: !1, statusLabel: "Status", beforeLabel: "", afterLabel: "" };
    const a = Gi(e, n), r = !!(t != null && t.active);
    await Wi({
      actor: e,
      statusId: n,
      active: r,
      metadata: {
        scope: t == null ? void 0 : t.scope,
        notes: t == null ? void 0 : t.notes,
        location: t == null ? void 0 : t.location,
        itemUuid: t == null ? void 0 : t.itemUuid,
        targetUuid: t == null ? void 0 : t.targetUuid,
        severity: t == null ? void 0 : t.severity
      }
    });
    const s = Gi(e, n);
    return {
      mode: "status",
      statusId: n,
      statusLabel: N0(n, e),
      active: s,
      beforeLabel: a ? "Active" : "Inactive",
      afterLabel: s ? "Active" : "Inactive",
      source: String((t == null ? void 0 : t.source) ?? "").trim(),
      notes: String((t == null ? void 0 : t.notes) ?? "").trim()
    };
  }
  static async _applyAttackDamage(e, t, n = {}) {
    return this._applyPersonalArmorAwareDamage(e, {
      mode: "attackDamage",
      track: (t == null ? void 0 : t.track) ?? A.monitors.physical,
      damage: (t == null ? void 0 : t.damage) ?? 0,
      netHits: (t == null ? void 0 : t.netHits) ?? 0,
      damageType: t == null ? void 0 : t.damageType,
      ap: (t == null ? void 0 : t.ap) ?? 0,
      effects: (t == null ? void 0 : t.effects) ?? {},
      source: t == null ? void 0 : t.source,
      notes: t == null ? void 0 : t.notes
    }, n);
  }
  static async _applyMachineAttackDamage(e, t, n, a = {}) {
    return dw({ actor: e, token: t, payload: n, options: a });
  }
  static async _applyPersonalArmorAwareDamage(e, t, n = {}) {
    var I, O, x, B, P, L, D, G, V;
    const a = !!n.dryRun, r = (t == null ? void 0 : t.track) === A.monitors.fatigue ? A.monitors.fatigue : A.monitors.physical, s = Math.max(0, Number((t == null ? void 0 : t.damage) ?? 0) || 0), o = Math.max(0, Number((t == null ? void 0 : t.netHits) ?? 0) || 0), l = (t == null ? void 0 : t.effects) ?? {}, c = ((I = e.getPersonalCombatLoadout) == null ? void 0 : I.call(e, { refresh: !0 })) ?? null, u = (c == null ? void 0 : c.activeArmor) ?? null, d = Math.max(0, Number((u == null ? void 0 : u.currentArmorRating) ?? ((O = u == null ? void 0 : u.durability) == null ? void 0 : O.current) ?? 0) || 0), m = ci(t == null ? void 0 : t.damageType, "concussive"), f = ns(e, r);
    let p = s + o;
    const g = d > 0 ? Tb({
      damageIncoming: p,
      armorTags: (u == null ? void 0 : u.tags) ?? [],
      effects: l
    }) : { damageIncoming: p, applied: [] };
    p = g.damageIncoming;
    const h = wb({
      currentArmorRating: d,
      mitigationByType: (u == null ? void 0 : u.mitigationByType) ?? {},
      damageType: m
    }), y = Math.max(
      0,
      (Number((t == null ? void 0 : t.ap) ?? 0) || 0) + (Number((l == null ? void 0 : l.ap) ?? 0) || 0)
    ), b = h.isDestroyed ? 0 : Math.max(0, h.baseMitigation + h.typeMitigationMod - y);
    let S = Math.max(0, Math.ceil(p - b));
    const w = {
      snapshot: ((x = F.getSnapshot) == null ? void 0 : x.call(F, e)) ?? null
    }, T = hi({
      actor: e,
      phase: "onDamageResolved",
      facts: mp({
        actor: e,
        packet: {
          amount: S,
          track: r,
          damageType: m
        },
        runtime: w
      }),
      packet: {
        amount: S,
        track: r,
        damageType: m
      },
      options: { runtime: w, consumeUsage: !0 }
    });
    a || await rn({ actor: e, mutations: T.mutations, runtime: w }), S = Math.max(0, Number(T.packet.amount ?? S) || 0), !a && S > 0 && await te.addCounter(e, r, S);
    const k = M0({
      incomingDamage: s + o,
      armorBefore: ((B = u == null ? void 0 : u.durability) == null ? void 0 : B.current) ?? 0,
      reinforcedBefore: ((L = (P = u == null ? void 0 : u.traitState) == null ? void 0 : P.reinforced) == null ? void 0 : L.current) ?? 0,
      reinforcedMax: ((G = (D = u == null ? void 0 : u.traitState) == null ? void 0 : D.reinforced) == null ? void 0 : G.max) ?? 0,
      hasArmorItem: !!((V = u == null ? void 0 : u.item) != null && V.id)
    });
    !a && Object.keys(k.update).length > 0 && await u.item.update(k.update);
    const C = a ? Math.max(0, f + S) : ns(e, r);
    return {
      mode: (t == null ? void 0 : t.mode) ?? "attackDamage",
      track: r,
      requestedDelta: s + o,
      appliedDelta: C - f,
      usedArmor: !0,
      damageType: m,
      effectiveAp: y,
      mitigation: {
        ...h,
        netResistance: b,
        armorBefore: k.armorBefore,
        armorAfter: k.armorAfter,
        reinforcedBefore: k.reinforcedBefore,
        reinforcedAfter: k.reinforcedAfter,
        reinforcedMax: k.reinforcedMax
      },
      damageIncoming: p,
      adjustedIncoming: p,
      finalDamage: S,
      tagEffectResult: g,
      beforeLabel: `${lr(r)} ${f}`,
      afterLabel: `${lr(r)} ${C}`,
      source: String((t == null ? void 0 : t.source) ?? "").trim(),
      notes: String((t == null ? void 0 : t.notes) ?? "").trim()
    };
  }
}
z(Jt, "MODE_OPTIONS", Object.freeze([
  { value: A.monitors.physical, label: "Physical" },
  { value: A.monitors.fatigue, label: "Fatigue" },
  { value: "burn", label: "Burn" },
  { value: "status", label: "Status" }
]));
const Xd = Os, Yl = "damage-mode", D0 = `${M}.${Yl}`, as = {}, Qo = {};
class ve {
  static init() {
    ea.register(ii.PROVIDE_DAMAGE_MODE), Hooks.on("updateSetting", async (e, t, n, a) => ve.onUpdateSetting(e, t, n, a)), Hooks.on(ii.PROVIDE_DAMAGE_MODE, (e) => {
      e("resistanceArmorMonitor", N.settings.damageMode.values.resistanceArmorMonitor, ve.sufferDamageResistanceArmorMonitor), e("armorResistanceMonitor", N.settings.damageMode.values.armorResistanceMonitor, ve.sufferDamageArmorResistanceMonitor), e("armorGivesResistance", N.settings.damageMode.values.armorGivesResistance, ve.sufferDamageArmorAsResistance_Earthdawn), e("armorGiveResistanceHitsAvoid", N.settings.damageMode.values.armorGiveResistanceHitsAvoid, ve.sufferDamageArmorAsResistance_Cyberpunk);
    }), Hooks.once("ready", () => ve.onReady());
  }
  static onReady() {
    ve._registerDamageModeSetting(), ve._selectDamageMode();
  }
  static _registerDamageModeSetting() {
    Hooks.callAll(ii.PROVIDE_DAMAGE_MODE, (e, t, n) => {
      as[e] = t, Qo[e] = n;
    }), game.settings.register(M, Yl, {
      scope: "world",
      name: N.settings.damageMode.name,
      hint: N.settings.damageMode.hint,
      config: !0,
      default: Object.keys(as)[0],
      choices: as,
      type: String
    });
  }
  static async onUpdateSetting(e, t, n, a) {
    e.key == D0 && ve._selectDamageMode();
  }
  static _selectDamageMode() {
    let e = game.settings.get(M, Yl);
    Qo[e] || (e = Object.keys(as)[0]), ve.damageModeCode = e, ve.damageModeMethod = Qo[e];
  }
  static async sufferDamage(e, t, n, a, r, s, o) {
    const { monitor: l, damageType: c } = ve._resolveDamageContext(e, t, o);
    if (la.checkActorCanReceiveDamage(c ?? l, l, e), ve._shouldUsePersonalDamageV2(e, l, o)) {
      await ve.sufferPersonalDamageV2(e, l, c, n, a, r, s, o);
      return;
    }
    await (ve.damageModeMethod ?? ve.sufferDamageResistanceArmorMonitor)(e, l, c, n, a, r, s), await e.applyArmorDamage(l, c, Te.sumModifiers([o], "other", "damageArmor"));
  }
  static _shouldUsePersonalDamageV2(e, t, n) {
    var a, r;
    return !((a = e == null ? void 0 : e.isCharacterLike) != null && a.call(e)) || ![A.monitors.physical, A.monitors.fatigue].includes(t) ? !1 : !!((r = n == null ? void 0 : n.isPersonalWeapon) != null && r.call(n) || (n == null ? void 0 : n.canonicalType) === A.itemType.personalWeapon || (n == null ? void 0 : n.type) === A.itemType.personalWeapon);
  }
  static async sufferPersonalDamageV2(e, t, n, a, r, s, o, l) {
    var d;
    const c = ((d = l == null ? void 0 : l.getCombatProfile) == null ? void 0 : d.call(l)) ?? l ?? null, u = await Jt.apply({
      actor: e,
      payload: {
        mode: "attackDamage",
        track: t,
        damage: Number(a ?? (c == null ? void 0 : c.damage) ?? 0) || 0,
        netHits: Number(r ?? 0) || 0,
        damageType: n ?? (c == null ? void 0 : c.damageType),
        ap: Number((c == null ? void 0 : c.ap) ?? 0) || 0,
        effects: (c == null ? void 0 : c.effects) ?? {}
      },
      options: {
        logToChat: !1
      }
    });
    u != null && u.ok && ve._notifyPersonalArmorMitigation(e, {
      damageType: u.damageType,
      baseIncoming: Number(u.requestedDelta ?? 0),
      adjustedIncoming: Number(u.adjustedIncoming ?? u.damageIncoming ?? 0),
      finalDamage: Number(u.finalDamage ?? 0),
      armorMitigation: u.mitigation ?? {},
      effectiveAp: Number(u.effectiveAp ?? 0),
      tagEffectResult: u.tagEffectResult ?? { applied: [] }
    });
  }
  static _notifyPersonalArmorMitigation(e, t = {}) {
    var u;
    const n = t.armorMitigation ?? {}, a = ve._localizeDamageType(t.damageType), r = n.isDestroyed ? "Armor destroyed" : `Base ${Number(n.baseMitigation ?? 0)} + Type ${Number(n.typeMitigationMod ?? 0)} - AP ${Number(t.effectiveAp ?? 0)}`, s = Number(t.adjustedIncoming ?? t.baseIncoming ?? 0), o = Number(t.finalDamage ?? 0), l = (((u = t.tagEffectResult) == null ? void 0 : u.applied) ?? []).map((d) => `${d.tag} +${Math.round((Number(d.bonus ?? 0) || 0) * 100)}%`).join(", "), c = l ? ` [${l}]` : "";
    ui.notifications.info(
      `${e.name} mitigated ${a}: ${r}${c}. Incoming ${s}, final ${o}.`
    );
  }
  static async sufferDamageResistanceArmorMonitor(e, t, n, a, r, s, o) {
    const l = te.resistanceDetail(e, t, n), c = l.value;
    let u = 0;
    if (s) {
      const d = Math.min(c, a), m = Math.min(c - d, r);
      u = a - d, te.useArmor(t) && (u -= await ve.damageToArmor(e, n, u)), u += r - m;
    } else
      u = a + r - c, te.useArmor(t) && (u -= await ve.damageToArmor(e, n, u));
    u > 0 && await te.addCounter(e, t, u), ve._notifyResistanceUsage(e, t, n, l);
  }
  static async sufferDamageArmorResistanceMonitor(e, t, n, a, r, s, o) {
    let l = 0;
    te.useArmor(t) ? s ? (a -= await ve.damageToArmor(e, n, a), l = r + a) : (l = r + a, l -= await ve.damageToArmor(e, n, l)) : l = a + r;
    const c = te.resistanceDetail(e, t, n);
    return l -= c.value, l > 0 && await te.addCounter(e, t, l), ve._notifyResistanceUsage(e, t, n, c), l;
  }
  static async sufferDamageArmorAsResistance_Cyberpunk(e, t, n, a, r, s, o) {
    let l = a + r;
    if (te.useArmor(t) && l > 0) {
      const u = s ? r : 0, d = Math.max(0, ve._computeArmorResistance(e) - u);
      d > 0 && (await te.addCounter(e, "armor", 1), l -= d);
    }
    const c = te.resistanceDetail(e, t, n);
    return l -= c.value, l > 0 && await te.addCounter(e, t, l), ve._notifyResistanceUsage(e, t, n, c), Math.max(l, 0);
  }
  static async sufferDamageArmorAsResistance_Earthdawn(e, t, n, a, r, s, o) {
    let l = a + r;
    if (te.useArmor(t) && !s && l > 0) {
      const u = ve._computeArmorResistance(e);
      u > 0 && (await te.addCounter(e, "armor", 1), l -= u);
    }
    l -= ve._computeStrengthResistance(e, t);
    const c = te.resistanceDetail(e, t, n);
    return l -= c.value, l > 0 && await te.addCounter(e, t, l), ve._notifyResistanceUsage(e, t, n, c), l;
  }
  static async damageToArmor(e, t, n) {
    if (n > 0) {
      const a = te.max(e, A.monitors.armor), r = te.getCounterValue(e, A.monitors.armor), s = Math.min(a - r, n), o = te.resistance(e, A.monitors.armor, t), l = Math.max(0, s - o);
      return l > 0 && await te.addCounter(e, A.monitors.armor, l), s;
    } else
      return 0;
  }
  static _resolveDamageContext(e, t, n) {
    var o;
    const a = (typeof t == "object" ? (t == null ? void 0 : t.damageType) ?? (t == null ? void 0 : t.type) : t) ?? ((o = n == null ? void 0 : n.system) == null ? void 0 : o.damageType), r = typeof t == "object" ? (t == null ? void 0 : t.monitor) ?? a : a;
    return { monitor: e.getDamageMonitor(r), damageType: a };
  }
  static _notifyResistanceUsage(e, t, n, a) {
    var u;
    if (!a || t === void 0)
      return;
    const r = N.actor.monitors[t] ?? t, s = ve._localizeDamageType(n) ?? r, o = a.usedType ? "type" : "default", l = ((u = N.actor.monitors.resistanceSources) == null ? void 0 : u[o]) ?? o, c = Ke(N.actor.monitors.resistanceApplied, {
      actor: e.name,
      monitor: r,
      damageType: s,
      value: a.value,
      source: l
    });
    ui.notifications.info(c);
  }
  static _localizeDamageType(e) {
    if (e)
      return Mf(e) ? Pi(e) : N.mwd.weaponDamageType[e] ?? N.mwd.personalDamageType[e] ?? N.actor.monitors[e] ?? e;
  }
  static _computeArmorResistance(e) {
    const t = te.max(e, "armor"), n = te.getCounterValue(e, "armor"), a = Math.max(0, t - n);
    return Math.max(0, Math.ceil(a / 3));
  }
  static _computeStrengthResistance(e, t) {
    const n = e.getAttributeValue(A.actorAttributes.strength);
    return Math.max(0, Math.floor(n / 4));
  }
}
class ni extends Actor {
  static init() {
    Hooks.on("updateActor", (e, t, n, a) => {
      var r;
      return (r = Ei.firstResponsible(e)) == null ? void 0 : r.onUpdateActor(t, n);
    });
  }
  constructor(e, t = {}) {
    var n;
    if (!((n = t.anarchy) != null && n.ready)) {
      const a = game.system.anarchy.actorClasses[e.type];
      if (foundry.utils.mergeObject(t, { anarchy: { ready: !0 } }), a)
        return e.img || (e.img = a.defaultIcon), new a(e, t);
    }
    t.anarchy = void 0, super(e, t);
  }
  static get initiative() {
    return "2d6 + @modifiers.initiative";
  }
  static get defaultIcon() {
  }
  static padWordListToMin(e, t) {
    for (let n = e.length; n < t; n++)
      e.push({
        word: "",
        id: n + 1,
        audio: "",
        no_delete: !1
      });
    for (let n = 0; n < t; n++)
      e[n].no_delete = !0;
    return e;
  }
  static sortSkills(e, t) {
    return t ? t.sort((n, a) => {
      const r = n.system.code === "knowledge" || n.system.attribute === "knowledge", s = a.system.code === "knowledge" || a.system.attribute === "knowledge";
      if (r && !s) return 1;
      if (!s && r) return -1;
      if (r && s)
        return n.name > a.name ? 1 : n.name > a.name ? -1 : 0;
      const o = e.getAttributeValue(n.system.attribute) + n.system.value, l = e.getAttributeValue(a.system.attribute) + a.system.value;
      return o > l ? -1 : o < l ? 1 : 0;
    }) : [];
  }
  static sortQualities(e) {
    if (!e)
      return [];
    const t = ["positive", "negative", "narrative"], n = ["major", "minor"];
    return e.sort((a, r) => {
      var m, f, p, g, h, y;
      const s = String(((m = a.system) == null ? void 0 : m.category) ?? (((f = a.system) == null ? void 0 : f.positive) === !1 ? "negative" : "positive")).trim() || "positive", o = String(((p = r.system) == null ? void 0 : p.category) ?? (((g = r.system) == null ? void 0 : g.positive) === !1 ? "negative" : "positive")).trim() || "positive", l = t.indexOf(s) - t.indexOf(o);
      if (l !== 0) return l;
      const c = String(((h = a.system) == null ? void 0 : h.tier) ?? "minor").trim() || "minor", u = String(((y = r.system) == null ? void 0 : y.tier) ?? "minor").trim() || "minor", d = n.indexOf(c) - n.indexOf(u);
      return d !== 0 ? d : String(a.name ?? "").localeCompare(String(r.name ?? ""));
    });
  }
  static sortAssetModules(e) {
    return e ? e.sort((t, n) => t.system.level > n.system.level ? -1 : t.system.level < n.system.level || t.name > n.name ? 1 : t.name < n.name ? -1 : 0) : [];
  }
  static sortAttributeButton(e) {
    return e ? e.sort((t, n) => t.labelkey > n.labelkey ? 1 : t.labelkey < n.labelkey ? -1 : 0) : [];
  }
  getAllowedUsers(e = CONST.DOCUMENT_OWNERSHIP_LEVELS.OWNER) {
    return game.users.filter((t) => this.testUserPermission(t, e));
  }
  getAllowedUserIds(e = CONST.DOCUMENT_OWNERSHIP_LEVELS.OWNER) {
    return this.getAllowedUsers(e).map((t) => t.id);
  }
  getRightToDefend() {
    return CONST.DOCUMENT_OWNERSHIP_LEVELS.OWNER;
  }
  hasOwnAnarchy() {
    return !1;
  }
  hasGMAnarchy() {
    return !this.hasPlayerOwner;
  }
  isVehicle() {
    return [A.actorTypes.vehicle, A.actorTypes.battlemech].includes(this.type);
  }
  prepareData() {
    super.prepareData(), this._prepareEdgePools(), this.cleanupFavorites();
  }
  prepareDerivedData() {
    if (this.system.modifiers = {
      initiative: Te.sumModifiers(this.items, "other", "initiative")
    }, this.system.monitors) {
      const e = Oe.getDamageTypes();
      Object.entries(this.system.monitors).forEach((t) => {
        t[1].resistance = ni.normalizeResistance(t[1].resistance), t[1].maxBonus = Te.sumMonitorModifiers(this.items, t[0], "max"), t[1].resistanceBonus = Te.sumMonitorModifiers(this.items, t[0], "resistance"), t[1].resistanceBonusByType = Object.fromEntries(
          e.map((n) => [n.value, Te.sumMonitorModifiers(this.items, t[0], "resistanceByType", n.value)]).filter(([, n]) => n)
        );
      });
    }
    this.system.attributes && Object.entries(this.system.attributes).forEach((e) => e[1].total = this.getAttributeValue(e[0])), this.system.state = this.computeState();
  }
  static normalizeResistance(e) {
    const t = typeof e == "number" ? { default: e, byType: {} } : foundry.utils.mergeObject({ default: 0, byType: {} }, e ?? {}, { inplace: !1, recursive: !0 });
    return t.default = Number(t.default ?? 0), t.byType = t.byType ?? {}, t;
  }
  getAttributes() {
    return _a[this.type] ?? [];
  }
  getPhysicalAgility() {
  }
  getCorrespondingAttribute(e) {
    if (this.getAttributes().includes(e))
      return e;
  }
  async onUpdateActor(e, t) {
    var n, a;
    ((n = e.system) == null ? void 0 : n.monitors) != null && ((a = e.system) == null ? void 0 : a.state) == null && this.update({ "system.state": this.computeState() });
  }
  computeState() {
    return {
      physical: this.computePhysicalState(),
      fatigue: this.computeFatigueState()
    };
  }
  computePhysicalState() {
    return { value: 0, max: 0 };
  }
  computeFatigueState() {
    var t;
    const e = (t = this.system.monitors) == null ? void 0 : t.fatigue;
    return e ? { value: e.max - e.value, max: e.max } : { value: 0, max: 0 };
  }
  _prepareEdgePools() {
    var n;
    if (!((n = this.system) != null && n.counters))
      return;
    const e = this.getAttributeValue(A.actorAttributes.edge), t = foundry.utils.getProperty(this.system, "counters.edgePools") ?? {};
    Object.values(A.counters.edgePools).forEach((a) => {
      const r = t[a] ?? {}, s = r.value;
      r.value = s ?? e ?? 0, r.value = Math.min(r.value, e ?? r.value ?? 0), r.max = e ?? r.max ?? 0, t[a] = r;
    }), foundry.utils.setProperty(this.system, "counters.edgePools", t);
  }
  async setCheckbarValue(e, t) {
    return await this.update({ [e]: t });
  }
  _getMonitorMax(e) {
    const t = this.getAttributeValue(e);
    return t == 0 ? 0 : sf + be.divup(t, 2);
  }
  getAttributeActions() {
    return it.getActorActions(this);
  }
  getUsableAttributes(e = void 0) {
    const t = (e ? [e] : this.items).map((a) => a.getAttributes()).reduce((a, r) => a.concat(r), []), n = be.distinct(this.getAttributes().concat(t));
    return n.sort(be.ascendingBySortedArray(Oe.sortedAttributeKeys)), n;
  }
  getAttributeValue(e, t = void 0) {
    let n = 0;
    if (e = this.getCorrespondingAttribute(e), e) {
      if (this.getAttributes().includes(e))
        n = this.system.attributes[e].value;
      else if (t)
        n = (t == null ? void 0 : t.getAttributeValue(e)) ?? 0;
      else {
        const a = this.items.filter((r) => r.getAttributes().includes(e));
        if (a.length > 0) {
          const r = a.map((s) => s.getAttributeValue(e) ?? 0);
          n = Math.max(...r);
        }
      }
      n += Te.sumModifiers(this.items, "attribute", e);
    }
    return n;
  }
  resolveDamageType(e) {
    switch (e) {
      case "stun":
        return A.monitors.fatigue;
    }
    return e;
  }
  getDamageMonitor(e) {
    e = this.resolveDamageType(e);
  }
  async applyArmorDamage(e, t, n = 0) {
    switch (e = this.resolveDamageType(e), e) {
      case A.monitors.physical:
      case A.monitors.fatigue:
        await ve.damageToArmor(this, t, n);
    }
  }
  async rollAttribute(e) {
    await Bi.rollAttribute(this, e);
  }
  async rollAttributeAction(e) {
    const t = it.getActorAction(this, e);
    await Bi.rollAttributeAction(this, t);
  }
  async rollSkill(e, t) {
    await Bi.rollSkill(this, e, t);
  }
  async rollWeapon(e) {
    var r, s, o;
    la.checkWeaponDefense(e, this);
    const t = (r = e.validateTargets(this)) == null ? void 0 : r.map((l) => l.id), n = {
      attackerTokenId: (o = (s = game.scenes.current) == null ? void 0 : s.tokens.find((l) => {
        var c;
        return ((c = l.actor) == null ? void 0 : c.id) == this.id;
      })) == null ? void 0 : o.id,
      targetedTokenIds: t
    }, a = this.items.find((l) => e.isWeaponSkill(l));
    await Bi.rollWeapon(this, a, e, n);
  }
  async rollDefense(e) {
    const t = e.attack.defense, n = it.getActorDefense(this, t);
    await Bi.rollDefense(this, n, e);
  }
  async switchMonitorCheck(e, t, n, a = void 0) {
    await te.switchMonitorCheck(this, e, t, n, a);
  }
  async addCounter(e, t, n = void 0) {
    await te.addCounter(this, e, t, n);
  }
  async setCounter(e, t, n = void 0) {
    await te.setCounter(this, e, t, n);
  }
  canSetMarks() {
    return !1;
  }
  getCyberdeck() {
  }
  canApplyDamage(e) {
    switch (e) {
      case A.monitors.physical:
      case A.monitors.fatigue:
        return this.getDamageMonitor(e) != null;
    }
    return !1;
  }
  canReceiveDamage(e) {
    return this.canApplyDamage(e);
  }
  async onEnterCombat() {
    const e = Te.sumModifiers(this.items, "other", "sceneAnarchy");
    e > 0 && await te.setCounter(this, A.monitors.sceneAnarchy, e);
  }
  async onLeaveCombat() {
    await te.setCounter(this, A.monitors.sceneAnarchy, 0);
  }
  getCelebrityValue() {
    return 0;
  }
  getCredibilityValue() {
    return 0;
  }
  getRumorValue() {
    return 0;
  }
  getAnarchy() {
    var a, r;
    const e = this.hasGMAnarchy(), t = (r = (a = game.system) == null ? void 0 : a.anarchy) == null ? void 0 : r.gmAnarchy, n = e && t && typeof t.getAnarchy == "function" ? t.getAnarchy() : {
      isGM: !1,
      value: 0,
      max: 0
    };
    return n.scene = this.getAnarchyScene(), n;
  }
  getAnarchyScene() {
    return 0;
  }
  getAnarchyValue() {
    return this.getAnarchy().value ?? 0;
  }
  async spendCredibility(e) {
    await this.spendEdgePool(A.counters.social.credibility, e);
  }
  async spendRumor(e) {
    await this.spendEdgePool(A.counters.mental.rumor, e);
  }
  async spendAnarchy(e) {
    var t, n;
    if (e && !this.hasPlayerOwner) {
      const a = (n = (t = game.system) == null ? void 0 : t.anarchy) == null ? void 0 : n.gmAnarchy;
      a != null && a.npcConsumesAnarchy && await a.npcConsumesAnarchy(this, e);
      return;
    }
  }
  getEdgePools() {
    var e;
    return ((e = this.system.counters) == null ? void 0 : e.edgePools) ?? {};
  }
  getEdgePoolValue(e) {
    var r, s;
    const t = this.getAttributeValue(A.actorAttributes.edge), a = ((s = (r = this.getEdgePools()) == null ? void 0 : r[e]) == null ? void 0 : s.value) ?? t ?? 0;
    return Math.min(a, t ?? a ?? 0);
  }
  getRemainingEdge(e = void 0) {
    return e ? this.getEdgePoolValue(e) : Math.max(0, ...Object.values(A.counters.edgePools).map((t) => this.getEdgePoolValue(t)));
  }
  canUseEdge() {
    return this.getAttributes().includes(A.actorAttributes.edge);
  }
  async spendEdgePool(e, t) {
    t != 0 && await te.addCounter(this, e, -t);
  }
  async spendEdge(e, t = A.counters.edgePools.grit) {
    if (e != 0) {
      if (!this.canUseEdge()) {
        const n = N.actorType[this.type] ?? this.type, a = `${this.name} (${n}) cannot use Edge`;
        throw ui.notifications.warn(a), a;
      }
      await this.spendEdgePool(t, e);
    }
  }
  getSkillRating(e) {
    var n;
    const t = typeof e == "string" ? this.items.get(e) : e;
    return ((n = t == null ? void 0 : t.system) == null ? void 0 : n.value) ?? 0;
  }
  getSkillValue(e, t = void 0) {
    const n = typeof e == "string" ? this.items.get(e) : e;
    if (!n)
      return 0;
    const a = this.getAttributeValue(n.system.attribute);
    return this.getSkillRating(n) + a + (t ? 2 : 0);
  }
  getWounds() {
    return 0;
  }
  /**
   * @param ownerActor the Actor who becomes the owner of this Actor
   */
  async attachToOwnerActor(e = void 0, t = "attach") {
    if ((e == null ? void 0 : e.id) == this.id)
      return;
    e != null && e.hasPlayerOwner;
    let n = this;
    if (t == "copy") {
      const a = this.clone();
      n = (await Actor.createDocuments([a]))[0];
    }
    await n.update({ "system.ownerId": (e == null ? void 0 : e.id) ?? "" }), e == null || e.render(), this.render();
  }
  getOwnerActor() {
    if (this.system.ownerId)
      return game.actors.get(this.system.ownerId);
  }
  getOwnedActors() {
    return game.actors.filter((e) => e.system.ownerId == this.id);
  }
  hasFavorite(e, t) {
    const n = ni._prepareFavorite(e, t);
    return !!this.system.favorites.find((a) => ni._isSameFavorite(n, a));
  }
  static _prepareFavorite(e, t) {
    return { type: e, id: t };
  }
  static _isSameFavorite(e, t) {
    return e.id == t.id && e.type == t.type;
  }
  async switchFavorite(e, t, n) {
    const a = ni._prepareFavorite(t, n), r = this.system.favorites.filter((s) => !ni._isSameFavorite(a, s));
    e && r.push(a), this.update({ "system.favorites": r });
  }
  async cleanupFavorites() {
    const e = this.computeShortcuts().filter((t) => !t.callback);
    e.length < this.system.favorites && this.update({ "system.favorites": e });
  }
  getShortcuts() {
    return this.computeShortcuts().filter((e) => e.label && e.callback);
  }
  computeShortcuts() {
    return this.system.favorites ? this.system.favorites.map((e) => this.getShortcut(e.type, e.id)) : [];
  }
  getShortcut(e, t) {
    var a;
    const n = ni._prepareFavorite(e, t);
    if (e == "attributeAction") {
      const r = it.prepareShortcut(this, t);
      if (r)
        return foundry.utils.mergeObject(r, n);
    } else if (Object.values(A.itemType).includes(e)) {
      const r = (a = this.items.get(t)) == null ? void 0 : a.prepareShortcut();
      if (r)
        return foundry.utils.mergeObject(r, n);
    }
    return n;
  }
  async _onSetManualStepper(e, t) {
    var s, o;
    e == null || e.preventDefault();
    const n = (s = t == null ? void 0 : t.dataset) == null ? void 0 : s.id, a = Number((o = t == null ? void 0 : t.dataset) == null ? void 0 : o.value);
    if (!n || Number.isNaN(a)) return;
    const r = this._mwd.state.manual.find((l) => l.id === n);
    if (r)
      return r.value = a, this.render(!1);
  }
}
const { ApplicationV2: O0, HandlebarsApplicationMixin: L0 } = foundry.applications.api, { renderTemplate: Zd } = foundry.applications.handlebars, _0 = `${se}/chat/celebrity-roll.hbs`, va = class va extends L0(O0) {
  static get DEFAULT_OPTIONS() {
    return foundry.utils.mergeObject(super.DEFAULT_OPTIONS, {
      id: "roll-celebrity",
      classes: ["anarchy-dialog"],
      position: { width: 400, height: "auto" },
      window: {
        resizable: !0
      }
    }, { inplace: !1 });
  }
  static async create(e) {
    const t = {
      actor: e,
      celebrity: {
        label: N.actor.counters.edgePools.legend,
        value: e.getCelebrityValue()
      },
      modifiers: foundry.utils.mergeObject(
        { label: N.item.tabs.modifiers },
        Te.computeModifiers(e.items, "other", "celebrity")
      ),
      other: {
        label: N.common.roll.modifiers.other,
        value: 0
      },
      ANARCHY: N
    }, n = await Zd(`${se}/dialog/roll-celebrite-title.hbs`, t), a = {
      id: `roll-celebrity-${foundry.utils.randomID()}`,
      classes: [game.system.anarchy.styles.selectCssClass(), ...va.DEFAULT_OPTIONS.classes],
      window: { title: n }
    };
    return new va({ roll: t }, a).render({ force: !0 });
  }
  constructor(e = {}, t = {}) {
    super(e, t), this.roll = e.roll;
  }
  async _prepareContext() {
    return this.roll;
  }
  async activateListeners(e) {
    const t = e instanceof HTMLElement ? e : e[0];
    await super.activateListeners(t);
    const n = $(t);
    n.find(".input-celebrity-other").on("input", (a) => {
      this.roll.other.value = Number.parseInt(a.currentTarget.value) ?? 0;
    }), n.find('[data-action="roll"]').on("click", async () => {
      await va.doRoll(this.roll), await this.close();
    }), n.find('[data-action="cancel"]').on("click", async () => {
      await this.close();
    });
  }
  static async doRoll(e) {
    const t = [
      e.celebrity,
      e.modifiers,
      e.other
    ], n = be.sumValues(t, (o) => o.value), a = {
      actor: e.actor,
      parameters: t,
      pool: n,
      options: {
        classes: [game.system.anarchy.styles.selectCssClass()]
      },
      ANARCHY: N
    }, r = new Roll(`${n}d6cs>=5`);
    await r.evaluate();
    const s = await Zd(_0, a);
    await r.toMessage({ flavor: s });
  }
  // async roll() {
  //   const parameters = [
  //     this.roll.celebrity,
  //     this.roll.modifiers,
  //     this.roll.other
  //   ];
  //   const pool = Misc.sumValues(parameters, it => it.value);
  //   const hbsCelebrityRoll = {
  //     actor: this.roll.actor,
  //     parameters: parameters,
  //     pool: pool,
  //     options: {
  //       classes: [game.system.anarchy.styles.selectCssClass()]
  //     },
  //     ANARCHY: ANARCHY
  //   }
  //   const roll = new Roll(`${pool}d6cs>=5`);
  //   await roll.evaluate();
  //   const flavor = await renderTemplate(HBS_TEMPLATE_CHAT_CELEBRITY_ROLL, hbsCelebrityRoll);
  //   await roll.toMessage({ flavor: flavor });
  // }
};
z(va, "PARTS", {
  body: {
    template: `${se}/dialog/roll-celebrite.hbs`
  }
});
let Jl = va;
const { renderTemplate: x0 } = foundry.applications.handlebars, $0 = `${se}/chat/actor-say-word.hbs`;
class em extends ni {
  static get initiative() {
    return ni.initiative + " + max(@attributes.reflexes.value, @attributes.intelligence.value)";
  }
  hasOwnAnarchy() {
    return this.hasPlayerOwner;
  }
  prepareDerivedData() {
    !this.system.monitors.fatigue && this.system.monitors.stun && (this.system.monitors.fatigue = foundry.utils.duplicate(this.system.monitors.stun)), this.system.monitors.physical.max = this._getMonitorMax(A.actorAttributes.strength), this.system.monitors.fatigue.max = this._getMonitorMax(A.actorAttributes.willpower), super.prepareDerivedData(), this.system.ignoreWounds = Te.sumModifiers(this.items, "other", "ignoreWounds");
  }
  computePhysicalState() {
    var l, c;
    const e = Math.max(0, Number(((l = this.system.monitors.armor) == null ? void 0 : l.max) ?? 0)), t = Math.min(
      e,
      Math.max(0, Number(((c = this.system.monitors.armor) == null ? void 0 : c.value) ?? 0))
    ), n = Math.max(0, e - t), a = Math.max(this.system.monitors.physical.max, this.system.monitors.fatigue.max) + e, r = this.system.monitors.physical.value == this.system.monitors.physical.max, s = this.system.monitors.fatigue.max == this.system.monitors.fatigue.value, o = r || s ? a : Math.max(this.system.monitors.physical.value, this.system.monitors.fatigue.value) + n;
    return {
      max: a,
      value: a - o
    };
  }
  getAttributes() {
    return _a[this.type] ?? _a[A.actorTypes.character];
  }
  getPhysicalAgility() {
    return A.actorAttributes.reflexes;
  }
  getCorrespondingAttribute(e) {
    return A.itemAttributes.firewall == e ? A.itemAttributes.firewall : super.getCorrespondingAttribute(e);
  }
  getDamageMonitor(e) {
    switch (e = this.resolveDamageType(e), e) {
      case A.monitors.fatigue:
      case A.monitors.physical:
        return e;
    }
    return super.getDamageMonitor(e);
  }
  async createWord(e, t) {
    this._mutateWords(e, (n) => n.concat([{ word: t, audio: "" }]));
  }
  async sayWord(e, t) {
    var a, r;
    const n = (a = this.getWord(e, t)) == null ? void 0 : a.word;
    n && ChatMessage.create({
      speaker: { alias: ((r = this.token) == null ? void 0 : r.name) ?? this.name },
      content: await x0(
        $0,
        {
          actor: this,
          wordsToSay: n
        }
      )
    });
  }
  getWord(e, t) {
    return e ? this.system[e].find((n) => n.id == t) : void 0;
  }
  async updateWord(e, t, n) {
    this._applyWordUpdate(e, t, (a) => foundry.utils.mergeObject(a, { word: n }, { overwrite: !0 }));
  }
  async _applyWordUpdate(e, t, n) {
    this._mutateWords(e, (a) => a.map((r) => (r.id == t && n(r), r)));
  }
  async deleteWord(e, t) {
    this._mutateWords(e, (n) => n.filter((a) => a.id != t));
  }
  async _mutateWords(e, t = (n) => n) {
    if (!e)
      return;
    let n = t(this.system[e]);
    be.reindexIds(n), await this.update({ [`system.${e}`]: n });
  }
  getCelebrityValue() {
    return this.getEdgePoolValue(A.counters.social.legend);
  }
  getCredibilityValue() {
    return this.getEdgePoolValue(A.counters.social.credibility);
  }
  getRumorValue() {
    return this.getEdgePoolValue(A.counters.mental.rumor);
  }
  getAnarchy() {
    return this.hasOwnAnarchy() ? {
      value: this.system.counters.anarchy.value,
      max: this.system.counters.anarchy.max,
      scene: this.getAnarchyScene()
    } : super.getAnarchy();
  }
  getAnarchyScene() {
    return this.getEdgePoolValue(A.counters.edgePools.chaos);
  }
  async spendAnarchy(e) {
    if (e > 0) {
      const t = this.getAnarchyScene(), n = this.getAnarchyValue();
      la.checkSufficient(N.actor.counters.anarchy, e, n + t);
      const a = Math.min(t, e), r = e - a;
      a > 0 && te.addCounter(this, A.monitors.sceneAnarchy, -a), this.hasPlayerOwner ? (await game.system.anarchy.gmAnarchy.actorGivesAnarchyToGM(this, e), te.addCounter(this, A.monitors.anarchy, -r)) : r > 0 && super.spendAnarchy(r);
    }
  }
  canUseEdge() {
    return !0;
  }
  getWounds() {
    const e = be.divint(this.system.monitors.fatigue.value, 3) + be.divint(this.system.monitors.physical.value, 3);
    return Math.max(0, e - this.system.ignoreWounds);
  }
  async rollCelebrity() {
    await Jl.create(this);
  }
}
function B0(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? t : e;
}
function z0(i = {}) {
  return !i || typeof i != "object" || Array.isArray(i) ? {} : Object.fromEntries(
    Object.entries(i).map(([e, t]) => [String(e ?? "").trim(), B0(t, 0)]).filter(([e, t]) => e && t !== 0)
  );
}
function tm(i = {}) {
  return {
    default: 0,
    byType: z0(i == null ? void 0 : i.byType)
  };
}
const Ng = Object.freeze({
  ground: "Ground",
  flight: "Flight",
  jump: "Jump"
});
function Kn(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? Math.max(0, Math.trunc(t)) : e;
}
function F0(i = "") {
  return i === A.actorTypes.vehicle || i === A.actorTypes.battlemech;
}
function H0(i = "") {
  return i === A.actorTypes.battlemech ? ["ground", "flight"] : i === A.actorTypes.vehicle ? ["ground", "flight"] : [];
}
function vo(i = {}, { actorType: e = "", legacyMoves: t = 0 } = {}) {
  if (!F0(e)) return {};
  const n = i && typeof i == "object" ? i : {}, a = Kn(t, 0), r = {
    ground: Kn(n.ground ?? n.move ?? n.run, a),
    flight: Kn(n.flight ?? n.fly, 0)
  };
  return e === A.actorTypes.battlemech && n.jump !== void 0 && (r.jump = Kn(n.jump, 0)), r;
}
function U0(i = null) {
  if (!(i != null && i.enabled)) return null;
  const e = Kn(i.movement, 0);
  if (e <= 0) return null;
  const t = [
    i.heat > 0 ? `Heat +${Kn(i.heat, 0)}` : "",
    Number(i.attackRatingBonus ?? 0) ? `AR ${Number(i.attackRatingBonus ?? 0) >= 0 ? "+" : ""}${Number(i.attackRatingBonus ?? 0)}` : "",
    Number(i.defenseRatingBonus ?? 0) ? `DR ${Number(i.defenseRatingBonus ?? 0) >= 0 ? "+" : ""}${Number(i.defenseRatingBonus ?? 0)}` : "",
    i.dfaEnabled ? "DFA" : ""
  ].filter(Boolean);
  return {
    key: "jump",
    label: Ng.jump ?? "Jump",
    value: e,
    displayValue: String(e),
    path: "",
    visible: !0,
    editable: !1,
    derived: !0,
    title: String(i.sourceLabel ?? "").trim(),
    detail: t.join(" | "),
    blocked: !!i.blocked,
    blockedReason: String(i.blockedReason ?? "").trim()
  };
}
function Rg({
  actorType: i = "",
  movement: e = {},
  legacyMoves: t = 0,
  editing: n = !1,
  basePath: a = "system.movement",
  jumpProfile: r = null
} = {}) {
  const s = vo(e, { actorType: i, legacyMoves: t }), o = H0(i).map((c) => {
    const u = Kn(s[c], 0), d = c === "flight";
    return {
      key: c,
      label: Ng[c] ?? c,
      value: u,
      displayValue: String(u),
      path: `${a}.${c}`,
      visible: n || !d || u > 0,
      editable: !0,
      derived: !1,
      title: "",
      detail: ""
    };
  }).filter((c) => c.visible), l = i === A.actorTypes.battlemech ? U0(r) : null;
  return l && o.push(l), o;
}
function Pg({ actorType: i = "", movement: e = {}, legacyMoves: t = 0, jumpProfile: n = null } = {}) {
  const a = vo(e, { actorType: i, legacyMoves: t });
  return Rg({
    actorType: i,
    movement: a,
    legacyMoves: t,
    editing: !1,
    jumpProfile: n
  }).map((r) => ({
    label: r.label,
    value: String(r.value)
  }));
}
function j0() {
  return foundry.data.operators.ForcedDeletion;
}
class Dg extends ni {
  prepareDerivedData() {
    this._prepareMwdAttributes(), this._prepareMwdDegradation(), this._prepareMwdMovement(), this._prepareMwdMonitors(), this._prepareMwdItems(), super.prepareDerivedData();
  }
  static get defaultIcon() {
    return `${to}/default/Default_Vehicle.svg`;
  }
  static get initiative() {
    return ni.initiative;
  }
  computePhysicalState() {
    return {
      max: this.system.monitors.structure.max,
      value: this.system.monitors.structure.max - this.system.monitors.structure.value
    };
  }
  getAttributes() {
    return _a[this.type] ?? _a[A.actorTypes.vehicle];
  }
  getPhysicalAgility() {
    return A.actorAttributes.handling;
  }
  getDamageMonitor(e) {
    switch (e = this.resolveDamageType(e), e) {
      case A.monitors.physical:
        return A.monitors.structure;
      case A.monitors.fatigue:
        return;
    }
    return super.getDamageMonitor(e);
  }
  getRightToDefend() {
    return CONST.DOCUMENT_OWNERSHIP_LEVELS.OBSERVER;
  }
  async _migrateHandlingToAttribute(e) {
    var a;
    const t = ((a = this.system.attributes.handling) == null ? void 0 : a.value) ?? 0, n = this.system.handling;
    n && t < n && await this.update({
      "system.handling": j0(),
      "system.attributes.handling.value": n
    });
  }
  _prepareMwdAttributes() {
    var r;
    const e = this.system.mwd = this.system.mwd ?? {}, t = {
      [A.actorAttributes.handling]: { value: 0 },
      [A.actorAttributes.system]: { value: 0 },
      [A.actorAttributes.reliability]: { value: 0 },
      [A.actorAttributes.condition]: { value: 0 },
      [A.actorAttributes.chassis]: { value: 0 }
    }, n = foundry.utils.mergeObject(
      foundry.utils.duplicate(t),
      this.system.attributes ?? {},
      { inplace: !1, recursive: !0 }
    );
    this.system.attributes = n, e.attributes = foundry.utils.mergeObject(
      foundry.utils.duplicate(t),
      e.attributes ?? {},
      { inplace: !1, recursive: !0 }
    ), Object.entries(e.attributes).forEach(([s, o]) => {
      var l;
      ((l = n[s]) == null ? void 0 : l.value) === void 0 && (n[s] = n[s] ?? {}, n[s].value = (o == null ? void 0 : o.value) ?? 0);
    });
    const a = hw(this);
    for (const [s, o] of Object.entries(a))
      o && (n[s] = n[s] ?? { value: 0 }, n[s].value = Math.max(0, Number(((r = n[s]) == null ? void 0 : r.value) ?? 0) + Number(o ?? 0)), e.attributes[s] = e.attributes[s] ?? {}, e.attributes[s].value = n[s].value);
  }
  _prepareMwdDegradation() {
    da(this.system, this.type);
  }
  _prepareMwdMovement() {
    const e = vo(this.system.movement, {
      actorType: this.type,
      legacyMoves: this.system.moves
    });
    this.system.movement = e, this.system.moves = e.ground ?? Math.max(0, Number(this.system.moves ?? 0) || 0);
  }
  _prepareMwdMonitors() {
    var s, o, l, c, u, d, m, f, p, g;
    const e = this.system.mwd = this.system.mwd ?? {}, t = this.system.monitors = this.system.monitors ?? {}, n = this.type === A.actorTypes.battlemech ? 15 : 12, a = Math.max(0, Number(((s = t.armor) == null ? void 0 : s.max) ?? n));
    t.armor = foundry.utils.mergeObject(
      { value: 0, max: a, resistance: ni.normalizeResistance((o = t.armor) == null ? void 0 : o.resistance) },
      t.armor ?? {},
      { inplace: !1, recursive: !0 }
    ), t.armor.resistance = tm(t.armor.resistance);
    const r = {
      value: ((l = t.structure) == null ? void 0 : l.value) ?? 0,
      max: ((c = t.structure) == null ? void 0 : c.max) ?? (this.type === A.actorTypes.battlemech ? 18 : 15),
      resistance: ni.normalizeResistance((u = t.structure) == null ? void 0 : u.resistance)
    };
    if (t.structure = foundry.utils.mergeObject(
      foundry.utils.duplicate(r),
      t.structure ?? {},
      { inplace: !1, recursive: !0 }
    ), t.structure.resistance = tm(t.structure.resistance), e.monitors = e.monitors ?? {}, e.monitors.structure = foundry.utils.mergeObject(
      foundry.utils.duplicate(r),
      e.monitors.structure ?? {},
      { inplace: !1, recursive: !0 }
    ), this.type === A.actorTypes.battlemech) {
      const h = {
        value: ((d = t.heat) == null ? void 0 : d.value) ?? ((m = e.heat) == null ? void 0 : m.current) ?? 0,
        max: ((f = t.heat) == null ? void 0 : f.max) ?? ((p = e.heat) == null ? void 0 : p.hardMax) ?? 10,
        resistance: ni.normalizeResistance((g = t.heat) == null ? void 0 : g.resistance)
      };
      t.heat = foundry.utils.mergeObject(
        foundry.utils.duplicate(h),
        t.heat ?? {},
        { inplace: !1, recursive: !0 }
      ), e.monitors.heat = foundry.utils.mergeObject(
        foundry.utils.duplicate(h),
        e.monitors.heat ?? {},
        { inplace: !1, recursive: !0 }
      );
    }
  }
  _prepareMwdItems() {
    const e = this.system.mwd = this.system.mwd ?? {}, t = {
      skills: ["skill"],
      traits: ["trait", A.itemType.quality],
      lifeModules: ["lifeModule"],
      cues: ["cue"],
      dispositions: ["disposition"],
      // Consumables ride the same inventory rail as general gear for the
      // legacy actor-side item catalog until those callers move to explicit
      // canonical buckets.
      gear: ["gear", "consumable"],
      assetModules: ["assetModule"],
      vehicleUpgrades: ["vehicleUpgrade"],
      mechEquipment: ["mechEquipment"],
      personalWeapons: ["personalWeapon", "weapon"],
      vehicleWeapons: ["vehicleWeapon"],
      mechWeapons: ["mechWeapon"],
      weaponGroups: ["weaponGroup"]
    };
    e.items = Object.fromEntries(
      Object.entries(t).map(([n, a]) => [
        n,
        this.items.filter((r) => a.includes(r.type))
      ])
    );
  }
}
const im = {
  light: 4,
  medium: 5,
  heavy: 6,
  assault: 7
}, K0 = { mode: "normal", allowedWeaponIds: [], typeRestriction: "" }, G0 = {
  baseProfile: { name: "Unarmed", damage: "", notes: "" },
  maxWeapons: 0,
  allowedLocations: []
};
function Xo() {
  var i, e, t;
  return ((i = N == null ? void 0 : N.mwd) == null ? void 0 : i.hardpointType) ?? ((t = (e = N == null ? void 0 : N.mwd) == null ? void 0 : e.hardpoint) == null ? void 0 : t.type) ?? {};
}
function nm() {
  var i, e, t;
  return ((i = N == null ? void 0 : N.mwd) == null ? void 0 : i.hardpointSize) ?? ((t = (e = N == null ? void 0 : N.mwd) == null ? void 0 : e.hardpoint) == null ? void 0 : t.size) ?? {};
}
class Og {
  constructor(e) {
    this.actor = e, this.mwd = e.system.mwd ?? {};
  }
  compute() {
    const e = this.mwd.weightClass ?? "medium", t = im[e] ?? im.medium, n = this._normalizeHardpoints(), a = this._normalizeWeaponGroups(), r = a.find((y) => y.isPrimary), s = a.filter((y) => y.isPrimary), o = this._primarySlot(), l = [], c = [];
    s.length > 1 && l.push(N.mwd.loadout.errors.multiplePrimary);
    const u = r ? t - 1 : t, d = a.length + (r ? 1 : 0);
    a.length > u && l.push(Ke(N.mwd.loadout.errors.mountPointsExceeded, {
      used: d,
      total: t
    }));
    const m = this._getWeapons((y) => (y.system.weaponCategory ?? "ranged") !== "melee"), f = new Map(m.map((y) => [y.id, y])), p = /* @__PURE__ */ new Set(), g = n.map((y) => ({ ...y, occupiedBy: null, occupiedByName: void 0 }));
    for (const y of a)
      for (const b of y.weaponIds ?? []) {
        const S = f.get(b);
        if (!S) {
          c.push(Ke(N.mwd.loadout.warnings.weaponMissing, { weapon: b }));
          continue;
        }
        const w = S.system.damageType ?? "energy", T = cn(S.system.size ?? "small");
        if (p.has(b)) {
          l.push(Ke(N.mwd.loadout.errors.weaponAlreadyGrouped, { weapon: S.name }));
          continue;
        }
        if (p.add(b), y.isPrimary && this._validatePrimaryWeapon(S, w, T, o, l), (S.system.weaponCategory ?? "ranged") === "melee")
          continue;
        const k = vr(this.actor, S.id), C = g.find((I) => I.id === (k == null ? void 0 : k.id));
        C ? yu(C, S) ? (C.occupiedBy = y.id, C.occupiedByName = y.name) : l.push(Ke(N.mwd.loadout.errors.hardpointUnavailable, {
          weapon: S.name,
          type: Xo()[w] ?? w,
          size: nm()[T] ?? T
        })) : l.push(Ke(N.mwd.loadout.errors.hardpointUnavailable, {
          weapon: S.name,
          type: Xo()[w] ?? w,
          size: nm()[T] ?? T
        }));
      }
    r && (!r.weaponIds || r.weaponIds.length === 0) && l.push(N.mwd.loadout.errors.primaryWithoutWeapon);
    const h = this._computeMeleeState(l);
    return {
      mountPoints: {
        total: t,
        used: d,
        remaining: Math.max(0, t - d)
      },
      weightClass: e,
      hardpoints: g,
      weaponGroups: a,
      primaryGroupId: r == null ? void 0 : r.id,
      errors: l,
      warnings: c,
      meleeProfiles: h.profiles,
      meleeLimit: h.limit
    };
  }
  _normalizeWeaponGroups() {
    return this._toCollection(this.mwd.weaponGroups).map((e, t) => ({
      id: e.id ?? `group-${t + 1}`,
      name: e.name || Ke(N.common.newName, { type: N.itemType.singular.weapon }),
      weaponIds: this._asArray(e.weaponIds),
      isPrimary: e.isPrimary ?? !1
    }));
  }
  _normalizeHardpoints() {
    return this._toCollection(this.mwd.hardpoints).map((e, t) => ({
      id: e.id ?? `hardpoint-${t + 1}`,
      type: e.type ?? "energy",
      size: cn(e.size ?? "small"),
      location: e.location ?? "arms",
      itemId: String(e.itemId ?? "").trim()
    }));
  }
  _primarySlot() {
    const e = foundry.utils.mergeObject(foundry.utils.duplicate(K0), this.mwd.primarySlot ?? {});
    return e.allowedWeaponIds = this._asArray(e.allowedWeaponIds), e;
  }
  _computeMeleeState(e) {
    var o, l, c;
    const t = foundry.utils.mergeObject(foundry.utils.duplicate(G0), this.mwd.melee ?? {}), n = this._getWeapons((u) => (u.system.weaponCategory ?? "ranged") === "melee"), a = [], r = Number(t.maxWeapons ?? 0);
    n.length > r && e.push(Ke(N.mwd.loadout.errors.meleeLimitExceeded, {
      equipped: n.length,
      limit: r
    }));
    const s = this._asArray(t.allowedLocations);
    return a.push({
      name: ((o = t.baseProfile) == null ? void 0 : o.name) || N.mwd.melee.baseProfile,
      damage: ((l = t.baseProfile) == null ? void 0 : l.damage) ?? "",
      notes: ((c = t.baseProfile) == null ? void 0 : c.notes) ?? ""
    }), n.forEach((u) => {
      var m, f;
      const d = ((m = vr(this.actor, u.id)) == null ? void 0 : m.location) ?? "";
      s.length > 0 && d && !s.includes(d) && e.push(Ke(N.mwd.loadout.errors.meleeLocationRestricted, {
        weapon: u.name,
        location: N.mwd.meleeLocation[d] ?? d
      })), a.push({
        name: u.name,
        damage: u.getDamageCode(),
        notes: ((f = u.system.references) == null ? void 0 : f.description) ?? ""
      });
    }), { profiles: a, limit: r };
  }
  _validatePrimaryWeapon(e, t, n, a, r) {
    var s;
    a.mode === "converted" ? (((s = a.allowedWeaponIds) == null ? void 0 : s.length) > 0 && !a.allowedWeaponIds.includes(e.id) && r.push(Ke(N.mwd.loadout.errors.primaryNotAllowedWeapon, { weapon: e.name })), a.typeRestriction && t !== a.typeRestriction && r.push(Ke(N.mwd.loadout.errors.primaryTypeRestriction, {
      weapon: e.name,
      type: Xo()[a.typeRestriction] ?? a.typeRestriction
    }))) : n !== "large" && r.push(Ke(N.mwd.loadout.errors.primaryNeedsLarge, { weapon: e.name }));
  }
  _getWeapons(e) {
    return $l(this.actor, { canonicalType: A.itemType.mechWeapon }).filter((t) => {
      var n;
      return (n = t.isActive) == null ? void 0 : n.call(t);
    }).filter(e);
  }
  _asArray(e) {
    return this._toCollection(e);
  }
  _toCollection(e) {
    return Array.isArray(e) ? e : e == null || e === "" ? [] : typeof e == "object" ? Object.values(e) : [e];
  }
}
function Ql(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? Math.max(0, Math.trunc(t)) : e;
}
function W0(i) {
  return Array.isArray(i) ? i : Array.isArray(i == null ? void 0 : i.contents) ? i.contents : i && typeof i[Symbol.iterator] == "function" ? Array.from(i) : [];
}
function q0(i = {}) {
  var e;
  return String((i == null ? void 0 : i.type) ?? ((e = i == null ? void 0 : i.actor) == null ? void 0 : e.type) ?? "").trim();
}
function Xl(i = {}) {
  var e;
  return (i == null ? void 0 : i.system) ?? ((e = i == null ? void 0 : i.actor) == null ? void 0 : e.system) ?? {};
}
function V0(i = {}) {
  var t;
  const e = (i == null ? void 0 : i.assetModules) ?? (i == null ? void 0 : i.items) ?? ((t = i == null ? void 0 : i.actor) == null ? void 0 : t.items) ?? [];
  return W0(e).filter((n) => ((n == null ? void 0 : n.canonicalType) ?? (n == null ? void 0 : n.type)) === A.itemType.assetModule);
}
function Y0(i = {}) {
  var t;
  const e = Xl(i);
  return Ql((t = e == null ? void 0 : e.movement) == null ? void 0 : t.jump, 0);
}
function J0(i = {}) {
  var n, a, r;
  const e = (i == null ? void 0 : i.statuses) ?? ((n = i == null ? void 0 : i.actor) == null ? void 0 : n.statuses);
  return (a = e == null ? void 0 : e.has) != null && a.call(e, "jumpJetFailure") ? !0 : (Array.isArray(i == null ? void 0 : i.effects) ? i.effects : Array.isArray((r = i == null ? void 0 : i.actor) == null ? void 0 : r.effects) ? i.actor.effects : []).some((s) => {
    var c;
    if (String((s == null ? void 0 : s.statusId) ?? (s == null ? void 0 : s.id) ?? "").trim() === "jumpJetFailure") return !0;
    const l = s == null ? void 0 : s.statuses;
    return Array.isArray(l) ? l.includes("jumpJetFailure") : !!((c = l == null ? void 0 : l.has) != null && c.call(l, "jumpJetFailure"));
  });
}
function Q0(i = {}) {
  var t, n;
  return (Array.isArray((n = (t = Xl(i)) == null ? void 0 : t.mwd) == null ? void 0 : n.crits) ? Xl(i).mwd.crits : []).some((a) => a && a.active !== !1 && Array.isArray(a.gates) && a.gates.includes("jump"));
}
function am(i = "") {
  return {
    enabled: !1,
    available: !1,
    blocked: !1,
    blockedReason: "",
    legacy: !1,
    movement: 0,
    heat: 0,
    attackRatingBonus: 0,
    defenseRatingBonus: 0,
    dfaEnabled: !1,
    sourceIds: [],
    sourceNames: [],
    sourceLabel: i
  };
}
function X0(i = {}) {
  const e = q0(i);
  if (e && e !== A.actorTypes.battlemech)
    return { jumping: am() };
  const t = V0(i).map((h) => ({
    item: h,
    profile: XT(h)
  })).filter((h) => h.profile.enabled), n = Y0(i), a = t.length === 0 && n > 0;
  if (!(t.length > 0 || a))
    return { jumping: am() };
  const s = Q0(i) || J0(i), o = s ? "Jump capability is blocked by current machine damage or status effects." : "", l = t.reduce((h, y) => {
    var b, S;
    return {
      movement: h.movement + Ql(y.profile.movement, 0),
      heat: h.heat + Ql(y.profile.heat, 0),
      attackRatingBonus: h.attackRatingBonus + Number(y.profile.attackRatingBonus ?? 0),
      defenseRatingBonus: h.defenseRatingBonus + Number(y.profile.defenseRatingBonus ?? 0),
      dfaEnabled: h.dfaEnabled || !!y.profile.dfaEnabled,
      sourceIds: h.sourceIds.concat((b = y.item) != null && b.id ? [y.item.id] : []),
      sourceNames: h.sourceNames.concat((S = y.item) != null && S.name ? [y.item.name] : [])
    };
  }, {
    movement: 0,
    heat: 0,
    attackRatingBonus: 0,
    defenseRatingBonus: 0,
    dfaEnabled: !1,
    sourceIds: [],
    sourceNames: []
  }), c = a ? ["Legacy Jump Movement"] : l.sourceNames, u = a ? [] : l.sourceIds, d = a ? n : l.movement, m = a ? 0 : l.heat, f = a ? 0 : l.attackRatingBonus, p = a ? 0 : l.defenseRatingBonus, g = a ? !0 : l.dfaEnabled;
  return {
    jumping: {
      enabled: !0,
      available: !s,
      blocked: s,
      blockedReason: o,
      legacy: a,
      movement: d,
      heat: m,
      attackRatingBonus: f,
      defenseRatingBonus: p,
      dfaEnabled: !s && g,
      sourceIds: u,
      sourceNames: c,
      sourceLabel: c.join(", ")
    }
  };
}
const Ni = Object.freeze(["blind", "contact", "track", "lock"]), Z0 = Object.freeze({
  blind: "Blind",
  contact: "Contact",
  track: "Track",
  lock: "Lock"
});
function ek(i) {
  const e = Ni.indexOf(i);
  return e < 0 || e >= Ni.length - 1 ? Ni.includes(i) ? i : "blind" : Ni[e + 1];
}
function Pu(i, e) {
  const t = Math.max(0, Number(i) || 0);
  return e === "lock" ? t + 1 : e === "track" ? t : 0;
}
function vi(i) {
  return Z0[i] ?? String(i ?? "Unknown");
}
function tk(i) {
  return i === "blind" ? 1 : i === "contact" ? 2 : i === "track" ? 3 : 1;
}
const Lg = /* @__PURE__ */ new Map();
function rm(i = null) {
  var o;
  const e = (i == null ? void 0 : i.document) ?? i ?? null, t = (i == null ? void 0 : i.object) ?? (e == null ? void 0 : e.object) ?? i ?? null, n = String((e == null ? void 0 : e.id) ?? (i == null ? void 0 : i.id) ?? "").trim(), a = Lg.get(n) ?? null, r = Number((a == null ? void 0 : a.x) ?? (e == null ? void 0 : e.x) ?? (i == null ? void 0 : i.x)), s = Number((a == null ? void 0 : a.y) ?? (e == null ? void 0 : e.y) ?? (i == null ? void 0 : i.y));
  if (t && Number.isFinite(r) && Number.isFinite(s)) {
    if (typeof t.getCenterPoint == "function")
      return t.getCenterPoint({ x: r, y: s });
    if (typeof t.getCenter == "function")
      return t.getCenter(r, s);
  }
  return (t == null ? void 0 : t.center) ?? ((o = e == null ? void 0 : e.object) == null ? void 0 : o.center) ?? null;
}
function ik(i = null, e = {}) {
  var r, s, o;
  const t = String((i == null ? void 0 : i.id) ?? ((r = i == null ? void 0 : i.document) == null ? void 0 : r.id) ?? "").trim();
  if (!t) return !1;
  const n = Object.prototype.hasOwnProperty.call(e ?? {}, "x") ? Number(e.x) : Number((i == null ? void 0 : i.x) ?? ((s = i == null ? void 0 : i.document) == null ? void 0 : s.x)), a = Object.prototype.hasOwnProperty.call(e ?? {}, "y") ? Number(e.y) : Number((i == null ? void 0 : i.y) ?? ((o = i == null ? void 0 : i.document) == null ? void 0 : o.y));
  return !Number.isFinite(n) || !Number.isFinite(a) ? !1 : (Lg.set(t, { x: n, y: a }), !0);
}
function nk(i = null, e = null) {
  var s, o, l, c;
  const t = (s = globalThis.canvas) == null ? void 0 : s.grid, n = rm(i), a = rm(e);
  if (!t || !n || !a) return null;
  if (typeof t.measurePath == "function")
    try {
      const u = t.measurePath([n, a], { gridSpaces: !0 }), d = Number(
        (u == null ? void 0 : u.distance) ?? (u == null ? void 0 : u.cost) ?? (u == null ? void 0 : u.totalDistance) ?? (u == null ? void 0 : u.totalCost) ?? NaN
      );
      if (Number.isFinite(d)) return d;
    } catch {
    }
  const r = ((c = (l = (o = globalThis.foundry) == null ? void 0 : o.canvas) == null ? void 0 : l.geometry) == null ? void 0 : c.Ray) ?? globalThis.Ray;
  if (typeof t.measureDistances == "function" && typeof r == "function")
    try {
      const u = t.measureDistances([{ ray: new r(n, a) }], { gridSpaces: !0 }), d = Number(Array.isArray(u) ? u[0] : NaN);
      if (Number.isFinite(d)) return d;
    } catch {
      return null;
    }
  return null;
}
function ak(i, e = "") {
  if (!Number.isFinite(i)) return "";
  const t = Math.round(Number(i) * 10) / 10, n = Number.isInteger(t) ? String(t) : t.toFixed(1);
  return e ? `${n} ${e}` : n;
}
const qs = "mwd", _g = "targeting", rk = "ewState";
function sk(i = "") {
  var t, n, a, r;
  const e = String(i ?? "").trim();
  return e ? ((r = (a = (n = (t = globalThis.canvas) == null ? void 0 : t.tokens) == null ? void 0 : n.placeables) == null ? void 0 : a.find((s) => {
    var o;
    return (((o = s.document) == null ? void 0 : o.uuid) ?? s.uuid) === e;
  })) == null ? void 0 : r.actor) ?? null : null;
}
function fa(i = "") {
  return String(i ?? "").trim();
}
function xg(i) {
  return i && typeof i == "object" ? i : {};
}
function ok(i = {}, e = "") {
  var n;
  const t = fa(e);
  if (t) {
    if (Object.prototype.hasOwnProperty.call(i, t)) return i[t];
    if (typeof ((n = foundry == null ? void 0 : foundry.utils) == null ? void 0 : n.getProperty) == "function") {
      const a = foundry.utils.getProperty(i, t);
      if (a !== void 0) return a;
    }
    return t.split(".").reduce((a, r) => {
      if (!(!a || typeof a != "object"))
        return a[r];
    }, i);
  }
}
function Du(i = {}, e = "", t = null) {
  var s;
  const n = fa(e);
  if (!n) return i;
  if (Object.prototype.hasOwnProperty.call(i, n))
    return i[n] = t, i;
  if (typeof ((s = foundry == null ? void 0 : foundry.utils) == null ? void 0 : s.setProperty) == "function")
    return foundry.utils.setProperty(i, n, t), i;
  const a = n.split(".").filter(Boolean);
  let r = i;
  for (const o of a.slice(0, -1))
    (!r[o] || typeof r[o] != "object") && (r[o] = {}), r = r[o];
  return r[a.at(-1)] = t, i;
}
function sa(i) {
  var n;
  const e = game.combat;
  if (!e || !i) return null;
  const t = (i == null ? void 0 : i.id) ?? ((n = i == null ? void 0 : i.document) == null ? void 0 : n.id) ?? String(i ?? "").trim();
  return t ? e.combatants.find((a) => a.tokenId === t) ?? null : null;
}
function lk(i) {
  var t, n;
  const e = String(i ?? "").trim();
  return e ? ((n = (t = game.combat) == null ? void 0 : t.combatants) == null ? void 0 : n.find((a) => a.tokenId === e)) ?? null : null;
}
function Ou(i) {
  if (!i || typeof i != "object") return null;
  const e = String(i.id ?? "").trim();
  if (!e) return null;
  const t = Number.isFinite(Number(i.round)) ? Number(i.round) : null, n = Number.isFinite(Number(i.expiresAfterRound)) ? Number(i.expiresAfterRound) : t;
  return {
    id: e,
    value: Math.max(0, Number(i.value ?? 0) || 0),
    sourceTokenUuid: String(i.sourceTokenUuid ?? "").trim(),
    type: String(i.type ?? "self").trim() || "self",
    shareable: !!i.shareable,
    persistent: !!i.persistent,
    suppressedBy: i.suppressedBy ?? null,
    round: t,
    expiresAfterRound: n
  };
}
function ck(i = null) {
  return i && typeof i == "object" && !Array.isArray(i) ? Ou(i) : null;
}
function uk(i = []) {
  const e = (Array.isArray(i) ? i : []).map(Ou).filter(Boolean);
  return e.length ? e.sort((t, n) => {
    const a = Number(t.round ?? -1), r = Number(n.round ?? -1);
    return a !== r ? r - a : Number(n.value ?? 0) - Number(t.value ?? 0);
  })[0] ?? null : null;
}
function dk(i) {
  const e = Ni.includes(i == null ? void 0 : i.detectionState) ? i.detectionState : "blind", t = ck(i == null ? void 0 : i.packet) ?? uk(i == null ? void 0 : i.packets);
  return {
    detectionState: e,
    packet: t,
    packets: t ? [t] : []
  };
}
function $g(i) {
  if (!i) return {};
  const e = i.getFlag(qs, _g);
  return e && typeof e == "object" && Object.keys(e).length ? e : i.getFlag(qs, rk) ?? {};
}
function Lu(i, e = {}) {
  return i ? i.setFlag(qs, _g, e) : Promise.resolve();
}
function pa(i, e) {
  const t = fa(e), n = xg($g(i));
  return dk(ok(n, t));
}
function Eo(i, e) {
  return pa(i, e).detectionState;
}
function Bg(i, e) {
  var a;
  let t = 0;
  const n = (i == null ? void 0 : i.statuses) ?? /* @__PURE__ */ new Set();
  if (n.has("ecmJamming") && (t += 2), n.has("ecmShrouded") && (t += 1), n.has("obscuredLight") && (t += 1), n.has("obscuredHeavy") && (t += 3), n.has("obscured") && (t += 1), e) {
    const r = ((a = e.getFlag(qs, "personalCombat")) == null ? void 0 : a.actionState) ?? {};
    r.move !== null && r.move !== void 0 && (t += 1);
  }
  return t += Ew(i), t;
}
function mk(i) {
  return ((i == null ? void 0 : i.statuses) ?? /* @__PURE__ */ new Set()).has("ecmShrouded") ? 1 : 0;
}
function zg(i) {
  const t = ((i == null ? void 0 : i.statuses) ?? /* @__PURE__ */ new Set()).has("ecmJamming") ? "track" : "lock", n = Iw(i), a = Ni.indexOf(t), r = Ni.indexOf(n);
  return r >= 0 && r < a ? n : t;
}
function Fg(i, e, t, n, a) {
  if (n !== "track" && n !== "lock") return null;
  const { packet: r } = pa(i, e);
  if (!r || r.suppressedBy) return null;
  const s = Number.isFinite(Number(a)) ? Number(a) : null;
  if (!r.persistent && r.expiresAfterRound !== null && s !== null && s > r.expiresAfterRound)
    return null;
  const o = Pu(t, n), l = sk(e), c = Nw({
    attacker: (i == null ? void 0 : i.actor) ?? null,
    targetActor: l,
    value: Math.min(r.value, o)
  });
  return {
    id: r.id,
    value: c,
    sourceTokenUuid: r.sourceTokenUuid,
    round: r.round,
    expiresAfterRound: r.expiresAfterRound
  };
}
function _u(i) {
  return foundry.utils.deepClone(xg($g(i)));
}
async function Zl(i, e, t) {
  const n = fa(e);
  if (!n || !i) return;
  const a = Ni.includes(t) ? t : "blind", r = _u(i), s = pa(i, n);
  Du(r, n, {
    detectionState: a,
    packet: s.packet ? foundry.utils.deepClone(s.packet) : null
  }), await Lu(i, r);
}
async function fk(i, e, t) {
  const n = fa(e), a = Ou(t);
  if (!n || !i || !a) return;
  const r = _u(i), s = pa(i, n);
  Du(r, n, {
    detectionState: s.detectionState,
    packet: a
  }), await Lu(i, r);
}
async function ec(i, e) {
  const t = fa(e);
  if (!t || !i) return;
  const n = _u(i), a = pa(i, t);
  Du(n, t, {
    detectionState: a.detectionState,
    packet: null
  }), await Lu(i, n);
}
async function pk(i, e, t = "") {
  const n = fa(e), a = String(t ?? "").trim();
  if (!n || !i) return;
  const r = pa(i, n);
  r.packet && (a && r.packet.id !== a || await ec(i, n));
}
function gk({ value: i, sourceToken: e, round: t } = {}) {
  var a;
  const n = Number.isFinite(Number(t)) ? Number(t) : null;
  return {
    id: foundry.utils.randomID(),
    value: Math.max(0, Number(i) || 0),
    sourceTokenUuid: ((a = e == null ? void 0 : e.document) == null ? void 0 : a.uuid) ?? (e == null ? void 0 : e.uuid) ?? "",
    type: "self",
    shareable: !1,
    persistent: !1,
    suppressedBy: null,
    round: n,
    expiresAfterRound: n
  };
}
function hk(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? t : e;
}
function yk(i) {
  var e, t;
  return Array.from(i ?? ((t = (e = globalThis.game) == null ? void 0 : e.user) == null ? void 0 : t.targets) ?? []).filter((n) => n == null ? void 0 : n.actor);
}
function bk({ detectionState: i = "blind", canAcquire: e = !1, ceiling: t = "lock" } = {}) {
  return i === "blind" ? "Acquire can establish Contact (DN 1)." : i === "contact" ? "Acquire can upgrade to Track." : i === "track" && e ? "Acquire can upgrade to Lock." : i === "track" && t === "track" ? "ECM prevents advancing beyond Track." : i === "lock" ? "Targeting solution optimized." : "Review target state before acquiring.";
}
function Sk({ detectionState: i = "blind", lockGated: e = !1 } = {}) {
  return i === "blind" || i === "contact" ? "Targeting Data unavailable until Track." : i === "track" ? "Targeting Data available." : i === "lock" && e ? "Targeting solution optimized." : "Targeting solution available.";
}
function Ak({ canAcquire: i = !1, detectionState: e = "blind", acquireHint: t = "" } = {}) {
  return i ? {
    enabled: !0,
    label: "Acquire",
    title: t
  } : e === "lock" ? {
    enabled: !1,
    label: "Acquire",
    title: "Target is already at Lock."
  } : {
    enabled: !1,
    label: "Acquire",
    title: "Acquire is not available for this target right now."
  };
}
function wk({ canTarget: i = !1, targetHint: e = "" } = {}) {
  return {
    enabled: i,
    label: "Target",
    title: i ? e : "Track or Lock is required before generating targeting data."
  };
}
function Tk(i = null, e = null) {
  var r, s, o, l, c, u;
  const t = nk(i, e);
  if (!Number.isFinite(t))
    return {
      distance: null,
      distanceLabel: "",
      band: "",
      bandLabel: "",
      hasRange: !1
    };
  const n = String(((o = (s = (r = globalThis.canvas) == null ? void 0 : r.scene) == null ? void 0 : s.grid) == null ? void 0 : o.units) ?? ((u = (c = (l = globalThis.game) == null ? void 0 : l.system) == null ? void 0 : c.grid) == null ? void 0 : u.units) ?? "").trim(), a = ug(t, "close");
  return {
    distance: t,
    distanceLabel: ak(t, n),
    band: a,
    bandLabel: cg(a),
    hasRange: !0
  };
}
function kk({
  targetToken: i,
  sourceToken: e = null,
  combatant: t,
  systemAttr: n = 0,
  currentRound: a = null
} = {}) {
  var S, w;
  if (!(i != null && i.actor)) return null;
  const r = ((S = i.document) == null ? void 0 : S.uuid) ?? i.uuid ?? "", s = String(i.id ?? ((w = i.document) == null ? void 0 : w.id) ?? "").trim(), o = Eo(t, r), l = zg(i.actor), c = lk(s), u = Bg(i.actor, c), d = o === "track" || o === "lock" ? Pu(n, o) : null, m = o === "track" || o === "lock" ? Fg(t, r, n, o, a) : null, f = o === "blind" || o === "contact" || o === "track" && l === "lock", p = o === "track" || o === "lock", g = o === "lock", h = bk({ detectionState: o, canAcquire: f, ceiling: l }), y = Sk({ detectionState: o, lockGated: g }), b = Tk(e, i);
  return {
    tokenName: i.name ?? "Target",
    targetTokenId: s,
    targetTokenUuid: r,
    detectionState: o,
    detectionStateLabel: vi(o),
    packetValue: (m == null ? void 0 : m.value) ?? 0,
    hasPacket: !!m,
    packetCap: d,
    hasPacketCap: d !== null,
    trackingPenalty: u,
    hasTrackingPenalty: u > 0,
    rangeBand: b.band,
    rangeBandLabel: b.bandLabel,
    distance: b.distance,
    distanceLabel: b.distanceLabel,
    hasRange: b.hasRange,
    canAcquire: f,
    canTarget: p,
    acquireHint: h,
    targetHint: y,
    acquireAction: Ak({ canAcquire: f, detectionState: o, acquireHint: h }),
    targetAction: wk({ canTarget: p, targetHint: y })
  };
}
function Hg(i, e = "") {
  const t = Array.isArray(i == null ? void 0 : i.rows) ? i.rows : [], n = String(e ?? "").trim() === "targeting" ? "canTarget" : "canAcquire";
  return t.find((a) => !!(a != null && a[n])) ?? null;
}
function tc({
  actor: i,
  token: e,
  targets: t,
  currentRound: n = null
} = {}) {
  var c, u, d, m, f;
  const a = sa(e), r = hk((d = (u = (c = i == null ? void 0 : i.system) == null ? void 0 : c.attributes) == null ? void 0 : u.system) == null ? void 0 : d.value, 0), s = n ?? ((f = (m = globalThis.game) == null ? void 0 : m.combat) == null ? void 0 : f.round) ?? null, o = yk(t).map((p) => kk({
    targetToken: p,
    sourceToken: e,
    combatant: a,
    systemAttr: r,
    currentRound: s
  })).filter(Boolean), l = o.length > 0;
  return {
    hasTargets: l,
    rows: o,
    canAcquireAny: o.some((p) => p.canAcquire),
    canTargetAny: o.some((p) => p.canTarget),
    emptyState: "Target one or more tokens on the canvas to review EW status.",
    helpText: l ? "Detection state is tracked per targeted machine. Use each row's Acquire or Target button." : "Select targets on the canvas to review EW state and launch Acquire or Target per machine."
  };
}
const Mk = Object.freeze({
  sensorDegraded: Object.freeze({
    remedyKey: "systemReset",
    skillKey: "computers",
    locationKey: "head",
    effectSummary: "Sensor sweeps and targeting suffer degraded optics and processing until reset."
  }),
  sensorBlind: Object.freeze({
    remedyKey: "systemReset",
    skillKey: "computers",
    locationKey: "head",
    effectSummary: "Sensor and targeting actions are severely impaired until reset."
  }),
  stalled: Object.freeze({
    remedyKey: "emergencyRepair",
    effectSummary: "The machine cannot move normally until the fault is repaired."
  }),
  unstable: Object.freeze({
    remedyKey: "emergencyRepair",
    effectSummary: "Piloting and movement remain unstable until the machine is repaired."
  }),
  limping: Object.freeze({
    remedyKey: "emergencyRepair",
    locationKey: "legs",
    effectSummary: "Mobility remains impaired until the damaged leg assembly is repaired."
  }),
  jumpJetFailure: Object.freeze({
    remedyKey: "emergencyRepair",
    locationKey: "legs",
    effectSummary: "Jump movement is unavailable until the jump system is repaired."
  }),
  overheating: Object.freeze({
    remedyKey: "coolantDump",
    locationKey: "torso",
    effectSummary: "Heat continues to spike until the cooling issue is cleared."
  }),
  reactorInstability: Object.freeze({
    remedyKey: "coolantDump",
    locationKey: "torso",
    effectSummary: "The reactor remains unstable until coolant routing is restored."
  }),
  proneMechFall: Object.freeze({
    remedyKey: "stand",
    effectSummary: "The machine must recover from a prone fall before normal movement resumes."
  })
});
function Ck(i = []) {
  return i.map((e) => String(e ?? "").trim()).filter(Boolean);
}
function Ug(i, e = "") {
  var s, o;
  const t = String(e ?? "").trim();
  if (!t)
    return {
      locationKey: "",
      conditionLabel: "",
      conditionModifier: 0
    };
  const n = da(
    foundry.utils.deepClone((i == null ? void 0 : i.system) ?? {}),
    i == null ? void 0 : i.type
  ), a = ((o = (s = n == null ? void 0 : n.mwd) == null ? void 0 : s.locations) == null ? void 0 : o[t]) ?? {}, r = Number((a == null ? void 0 : a.condition) ?? 0) || 0;
  return {
    locationKey: t,
    conditionLabel: gu(r),
    conditionModifier: hu(r)
  };
}
function jg({ remedySkillKey: i = "", totalDn: e = 0, conditionLabel: t = "" } = {}) {
  var a;
  const n = ((a = Lt(i)) == null ? void 0 : a.label) ?? oe(i);
  return n ? `Reliability + ${n} vs DN ${e}${t ? ` (${t})` : ""}` : "";
}
function vk(i, e = {}) {
  const t = Pn(e.remedyKey), n = Mu(e), a = Ug(i, e.locationKey), r = String(e.remedySkillKey ?? po(e, t) ?? "").trim(), s = go(e, t), o = Math.max(0, s + Number(a.conditionModifier ?? 0));
  return {
    issueKind: "crit",
    issueId: String(e.id ?? "").trim(),
    label: String(e.label ?? oe(e.key)).trim(),
    sortLabel: String(e.label ?? oe(e.key)).trim().toLowerCase(),
    locationLabel: String(e.locationLabel ?? oe(e.locationKey)).trim(),
    detail: Ck([
      n.statusLabel ? `Status: ${n.statusLabel}` : "",
      n.scopeSummary,
      n.automationMode === "engine" ? "Automated" : "Reminder Only",
      e.escalationKey ? `Escalates: ${e.escalationKey}` : ""
    ]).join(" | "),
    effectSummary: n.effectText,
    remedyKey: t.key,
    remedyLabel: t.label,
    remedySkillKey: r,
    remedyBaseDn: s,
    totalDn: o,
    remedySummary: jg({
      remedySkillKey: r,
      totalDn: o,
      conditionLabel: a.conditionLabel
    }),
    remediable: t.remediable !== !1,
    statusId: String(e.statusId ?? "").trim()
  };
}
function Kg(i, e = "") {
  var u, d;
  const t = String(e ?? "").trim(), n = Mk[t] ?? null;
  if (!n || !(((d = (u = i == null ? void 0 : i.statuses) == null ? void 0 : u.has) == null ? void 0 : d.call(u, t)) ?? !1)) return null;
  const a = Ur(t), r = Pn(n.remedyKey), s = Ug(i, n.locationKey), o = String(n.skillKey ?? "").trim() || po({ remedyKey: n.remedyKey }, r), l = go({ remedyKey: n.remedyKey }, r), c = Math.max(0, l + Number(s.conditionModifier ?? 0));
  return {
    issueKind: "status",
    issueId: t,
    label: String((a == null ? void 0 : a.label) ?? oe(t)).trim() || oe(t),
    sortLabel: String((a == null ? void 0 : a.label) ?? oe(t)).trim().toLowerCase(),
    locationLabel: s.locationKey ? oe(s.locationKey) : "",
    detail: "Status-backed repair issue",
    effectSummary: String(n.effectSummary ?? "").trim(),
    remedyKey: r.key,
    remedyLabel: r.label,
    remedySkillKey: o,
    remedyBaseDn: l,
    totalDn: c,
    remedySummary: jg({
      remedySkillKey: o,
      totalDn: c,
      conditionLabel: s.conditionLabel
    }),
    remediable: r.remediable !== !1,
    statusId: t
  };
}
function Ek(i) {
  const e = _n(i).map((a) => vk(i, a)), t = new Set(
    e.map((a) => String(a.statusId ?? "").trim()).filter(Boolean)
  ), n = Array.from((i == null ? void 0 : i.statuses) ?? []).map((a) => String(a ?? "").trim()).filter(Boolean).filter((a) => !t.has(a)).map((a) => Kg(i, a)).filter((a) => (a == null ? void 0 : a.remediable) !== !1).sort((a, r) => a.sortLabel.localeCompare(r.sortLabel));
  return [...e, ...n];
}
async function sm(i = "") {
  const e = String(i ?? "").trim();
  if (!e || typeof fromUuid != "function") return null;
  try {
    return await fromUuid(e);
  } catch {
    return null;
  }
}
async function xn({
  machineActor: i = null,
  operatorActorUuid: e = ""
} = {}) {
  var s, o, l, c, u, d, m, f, p, g, h, y, b;
  const t = await sm(e);
  if (t)
    return { actor: t, uuid: t.uuid ?? e, source: "explicit", reason: "" };
  const n = String(
    ((o = (s = i == null ? void 0 : i.system) == null ? void 0 : s.pilot) == null ? void 0 : o.uuid) ?? ((u = (c = (l = i == null ? void 0 : i.system) == null ? void 0 : l.mwd) == null ? void 0 : c.pilot) == null ? void 0 : u.uuid) ?? ((f = (m = (d = i == null ? void 0 : i.system) == null ? void 0 : d.mwd) == null ? void 0 : m.crew) == null ? void 0 : f.operatorActorUuid) ?? ((h = (g = (p = i == null ? void 0 : i.system) == null ? void 0 : p.mwd) == null ? void 0 : g.crew) == null ? void 0 : h.pilotActorUuid) ?? ""
  ).trim(), a = await sm(n);
  if (a)
    return { actor: a, uuid: a.uuid ?? n, source: "pilot", reason: "" };
  const r = ((b = (y = i == null ? void 0 : i.system) == null ? void 0 : y.mwd) == null ? void 0 : b.crew) ?? {};
  return Number((r == null ? void 0 : r.effectiveCount) ?? (r == null ? void 0 : r.count) ?? 0) > 0 ? {
    actor: null,
    uuid: "",
    source: "crew",
    reason: "Crew exists, but no operator actor is linked."
  } : {
    actor: null,
    uuid: "",
    source: "",
    reason: "No linked operator or pilot actor."
  };
}
async function Ik(i, e, t) {
  const n = t.filter((s) => (s == null ? void 0 : s.active) !== !1);
  if (!n.length)
    try {
      await Wi({ actor: i, statusId: xp, active: !1 });
    } catch (s) {
      console.warn("MWD | Unable to clear machine critical status", s);
    }
  const a = String((e == null ? void 0 : e.statusId) ?? "").trim();
  if (!(!a || n.some((s) => String((s == null ? void 0 : s.statusId) ?? "").trim() === a)))
    try {
      await Wi({ actor: i, statusId: a, active: !1 });
    } catch (s) {
      console.warn(`MWD | Unable to remove crit status "${a}"`, s);
    }
}
async function Nk(i = "") {
  const e = String(i ?? "").trim();
  if (!e || typeof fromUuid != "function") return null;
  try {
    return await fromUuid(e);
  } catch {
    return null;
  }
}
function Rk(i, { passed: e = !1, actorUuid: t = "", gmOverride: n = !1 } = {}) {
  return e ? {
    ...i,
    active: !1,
    resolvedAt: (/* @__PURE__ */ new Date()).toISOString(),
    resolvedBy: t,
    resolvedByOverride: n && !t,
    lastRemedyAttemptAt: (/* @__PURE__ */ new Date()).toISOString(),
    lastRemedyPassed: !0,
    lastRemedyActorUuid: t,
    lastRemedyOverride: n && !t
  } : {
    ...i,
    lastRemedyAttemptAt: (/* @__PURE__ */ new Date()).toISOString(),
    lastRemedyPassed: !1,
    lastRemedyActorUuid: t,
    lastRemedyOverride: n && !t
  };
}
async function Pk(i, e, {
  passed: t = !1,
  actorUuid: n = "",
  gmOverride: a = !1
} = {}) {
  const r = String(e ?? "").trim();
  if (!i || !r) return null;
  const s = foundry.utils.deepClone(i.getFlag(M, "statusRemedyMeta") ?? {});
  return s[r] = {
    ...s[r] ?? {},
    lastRemedyAttemptAt: (/* @__PURE__ */ new Date()).toISOString(),
    lastRemedyPassed: t,
    lastRemedyActorUuid: n,
    lastRemedyOverride: a && !n,
    ...t ? {
      resolvedAt: (/* @__PURE__ */ new Date()).toISOString(),
      resolvedBy: n,
      resolvedByOverride: a && !n
    } : {}
  }, await i.setFlag(M, "statusRemedyMeta", s), s[r];
}
async function Io(i = {}, e = {}) {
  var S, w, T, k, C, I, O, x, B;
  const t = String(
    i.issueKind ?? (i.statusId ? "status" : "crit")
  ).trim() || "crit", n = String(
    i.issueId ?? (t === "status" ? i.statusId : i.critId) ?? ""
  ).trim(), a = !!(e.gmOverride ?? ((w = (S = globalThis.game) == null ? void 0 : S.user) == null ? void 0 : w.isGM)), r = await Nk(i.machineActorUuid);
  if (!r) return { ok: !1, reason: "Machine actor could not be resolved." };
  const s = await xn({
    machineActor: r,
    operatorActorUuid: i.operatorActorUuid
  });
  if (!s.actor && !a)
    return { ok: !1, reason: s.reason || "No linked operator or pilot actor." };
  if (t === "status") {
    const P = Kg(r, n);
    if (!P) return { ok: !1, reason: "That status is not currently repairable." };
    const L = Pn(i.remedyKey || P.remedyKey);
    return L.remediable === !1 ? { ok: !1, reason: "That status has no field remedy." } : {
      ok: !0,
      issueKind: t,
      issueId: P.issueId,
      machineActor: r,
      statusId: P.statusId,
      issue: P,
      remedy: L,
      operatorActor: s.actor ?? null,
      operator: s,
      gmOverride: a,
      rollingActor: s.actor ?? r,
      locationKey: "",
      locationCondition: 0,
      locationConditionLabel: "",
      locationConditionModifier: 0,
      skillKey: P.remedySkillKey,
      baseDn: P.remedyBaseDn,
      totalDn: P.totalDn,
      remedyEffect: { onSuccess: "clear", onFailure: "noChange" }
    };
  }
  const o = Array.isArray((k = (T = r.system) == null ? void 0 : T.mwd) == null ? void 0 : k.crits) ? r.system.mwd.crits.slice() : [], l = o.findIndex((P) => String((P == null ? void 0 : P.id) ?? "") === n && (P == null ? void 0 : P.active) !== !1);
  if (l < 0) return { ok: !1, reason: "That critical effect is no longer active." };
  const c = o[l], u = Pn(i.remedyKey || c.remedyKey);
  if (u.remediable === !1)
    return { ok: !1, reason: "That critical effect has no field remedy." };
  const d = da(
    ((O = (I = (C = globalThis.foundry) == null ? void 0 : C.utils) == null ? void 0 : I.deepClone) == null ? void 0 : O.call(I, r.system ?? {})) ?? structuredClone(r.system ?? {}),
    r.type
  ), m = String(c.locationKey ?? "").trim(), f = ((B = (x = d.mwd) == null ? void 0 : x.locations) == null ? void 0 : B[m]) ?? {}, p = Number((f == null ? void 0 : f.condition) ?? 0) || 0, g = hu(p), h = po(c, u), y = go(c, u), b = fu(c);
  return {
    ok: !0,
    issueKind: "crit",
    issueId: c.id,
    machineActor: r,
    crit: c,
    critIndex: l,
    remedy: u,
    operatorActor: s.actor ?? null,
    operator: s,
    gmOverride: a,
    rollingActor: s.actor ?? r,
    locationKey: m,
    locationCondition: p,
    locationConditionLabel: gu(p),
    locationConditionModifier: g,
    skillKey: h,
    baseDn: y,
    totalDn: y + g,
    remedyEffect: b
  };
}
async function No(i = {}, e = {}) {
  var n, a, r;
  const t = await Io(i, e);
  return t.ok ? {
    ok: !0,
    actor: t.rollingActor,
    payload: {
      intent: "machineRemedy",
      machineActorUuid: t.machineActor.uuid ?? i.machineActorUuid ?? "",
      issueKind: t.issueKind ?? "crit",
      issueId: t.issueId ?? ((n = t.crit) == null ? void 0 : n.id) ?? t.statusId ?? "",
      critId: ((a = t.crit) == null ? void 0 : a.id) ?? "",
      statusId: t.statusId ?? "",
      remedyKey: t.remedy.key,
      operatorActorUuid: ((r = t.operatorActor) == null ? void 0 : r.uuid) ?? "",
      gmOverride: t.gmOverride,
      tags: ["machine", "remedy"],
      edge: { allowed: ["pre", "post"] }
    },
    context: t
  } : t;
}
async function Dk(i = {}, e = {}) {
  return !(i != null && i.ok) && !(i != null && i.machineActor) ? { ok: !1, reason: "Machine remedy context is not available." } : !i.operatorActor || i.gmOverride ? { ok: !0, skipped: !0 } : await (e.spendResource ?? F.spendResource.bind(F))(i.operatorActor, {
    resource: i.remedy.resource,
    cost: i.remedy.cost,
    actionId: i.remedy.actionId,
    actionLabel: i.remedy.actionLabel,
    actionCostLabel: `${i.remedy.cost} SA`,
    actionCategory: i.remedy.category
  });
}
async function Ok(i = {}, e = {}) {
  var l, c, u, d;
  const t = await Io(i, e);
  if (!t.ok) return t;
  const n = !!e.passed, a = t.remedyEffect ?? fu(t.crit);
  if (t.issueKind === "status") {
    const m = n && a.onSuccess === "clear", f = await Pk(t.machineActor, t.statusId, {
      passed: m,
      actorUuid: ((l = t.operatorActor) == null ? void 0 : l.uuid) ?? "",
      gmOverride: t.gmOverride
    });
    return m && await Wi({
      actor: t.machineActor,
      statusId: t.statusId,
      active: !1
    }), {
      ok: !0,
      passed: n,
      applied: m,
      machineActor: t.machineActor,
      operatorActor: t.operatorActor,
      statusId: t.statusId,
      remedy: t.remedy,
      context: t,
      metadata: f
    };
  }
  const r = Array.isArray((u = (c = t.machineActor.system) == null ? void 0 : c.mwd) == null ? void 0 : u.crits) ? t.machineActor.system.mwd.crits.slice() : [], s = r[t.critIndex];
  if (!s || s.active === !1)
    return { ok: !1, reason: "That critical effect is no longer active." };
  const o = Rk(s, {
    passed: n && a.onSuccess === "clear",
    actorUuid: ((d = t.operatorActor) == null ? void 0 : d.uuid) ?? "",
    gmOverride: t.gmOverride
  });
  return r[t.critIndex] = o, await t.machineActor.update({ "system.mwd.crits": r }), await Ik(t.machineActor, o, r), {
    ok: !0,
    passed: n,
    applied: n && a.onSuccess === "clear",
    machineActor: t.machineActor,
    operatorActor: t.operatorActor,
    crit: o,
    remedy: t.remedy,
    context: t
  };
}
function Gg(i = null, { sheetToken: e = null } = {}) {
  var s, o, l, c;
  const t = (e == null ? void 0 : e.document) ?? e ?? null;
  if (t) return t;
  const a = (((s = i == null ? void 0 : i.getActiveTokens) == null ? void 0 : s.call(i, !0, !0)) ?? [])[0] ?? null;
  if (a) return (a == null ? void 0 : a.document) ?? a;
  const r = Array.from(((l = (o = globalThis.canvas) == null ? void 0 : o.tokens) == null ? void 0 : l.placeables) ?? []).find((u) => {
    var d;
    return ((d = u == null ? void 0 : u.actor) == null ? void 0 : d.id) && u.actor.id === (i == null ? void 0 : i.id);
  }) ?? null;
  return r ? (r == null ? void 0 : r.document) ?? r : ((c = i == null ? void 0 : i.token) == null ? void 0 : c.document) ?? (i == null ? void 0 : i.token) ?? null;
}
function er() {
  var i, e, t;
  return ((i = game.mwd) == null ? void 0 : i.roll) ?? ((t = (e = game.system) == null ? void 0 : e.mwd) == null ? void 0 : t.roll) ?? null;
}
function Zo(i) {
  return Gg(i);
}
class Lk extends Dg {
  static get defaultIcon() {
    return `${to}/vehicles/apc.svg`;
  }
  prepareDerivedData() {
    var t, n;
    super.prepareDerivedData(), this.system.mwd = this.system.mwd ?? {}, this.system.mwd.model = this.system.mwd.model ?? "", this.system.mwd.chassis = this.system.mwd.chassis ?? "", this.system.mwd.tonnage = this.system.mwd.tonnage ?? 0, this.system.mwd.mobility = X0(this), this.system.mwd.loadout = new Og(this).compute(), this.system.mwd.weaponGroupDetails = this._prepareConfiguredWeaponGroups(), this.system.mwd.heat = this._prepareHeatTrack(), this.system.mwd.primaryGroupName = ((t = this.system.mwd.weaponGroupDetails.find((a) => a.isPrimary)) == null ? void 0 : t.name) ?? "", this.system.skills = this._prepareSkillMap(), this.system.weaponGroups = this._prepareWeaponGroups(), this.system.meleeProfiles = this._prepareMeleeProfiles();
    const e = this.system.weaponGroups.find((a) => a.isPrimary && a.isAttackLegal) ?? null;
    this.system.quickActions = {
      primaryWeaponGroup: e,
      hasLegalRangedGroups: this.system.weaponGroups.some((a) => a.isAttackLegal),
      hasSensorSweep: !!(this.system.skills.perception || this.system.skills.technician),
      jumping: ((n = this.system.mwd.mobility) == null ? void 0 : n.jumping) ?? null
    };
  }
  async rollRangedAttack({ groupId: e = "", operatorActorUuid: t = "" } = {}) {
    var d, m, f;
    const n = Zo(this), a = ((d = F.getSnapshot) == null ? void 0 : d.call(F, this, { token: n })) ?? null, r = a != null && a.isCurrentTurn ? ((m = F.getUsedWeaponGroupIds) == null ? void 0 : m.call(F, this, { token: n, snapshot: a })) ?? [] : [], s = js(this, { usedWeaponGroupIds: r }).filter((p) => (p == null ? void 0 : p.isAttackLegal) && (p == null ? void 0 : p.isAvailableThisActivation));
    if (s.length === 0) {
      ui.notifications.warn(N.actor.vehicle.quickActions.errors.noRanged);
      return;
    }
    const o = String(e ?? "").trim(), c = (o ? s.find((p) => String((p == null ? void 0 : p.id) ?? "").trim() === o) ?? null : null) ?? await this._promptWeaponGroup(s);
    if (!c)
      return;
    const u = er();
    if (!(u != null && u.execute)) {
      (f = ui.notifications) == null || f.error("MWD roll system not initialized.");
      return;
    }
    await u.execute({
      actor: this,
      payload: {
        intent: "attack",
        sourceType: "weaponGroup",
        sourceId: c.id,
        weaponGroupId: c.id,
        edge: { pool: "physical.grit", allowed: ["pre", "post"] },
        tags: ["combat", "attack", "machine", "groupFire"],
        sourceTokenId: (n == null ? void 0 : n.id) ?? null,
        operatorActorUuid: String(t ?? "").trim()
      }
    });
  }
  async rollMeleeAttack({ operatorActorUuid: e = "" } = {}) {
    var s;
    const t = this.system.meleeProfiles ?? [];
    if (t.length === 0) {
      ui.notifications.warn(N.actor.vehicle.quickActions.errors.noMelee);
      return;
    }
    const n = await this._promptMeleeProfile(t);
    if (!n)
      return;
    if (!n.weaponId) {
      await this._rollQuickSkill(this.system.skills.melee, {
        operatorActorUuid: e,
        quickAction: {
          title: N.actor.vehicle.quickActions.meleeAttack,
          meleeProfile: n
        }
      });
      return;
    }
    const a = er();
    if (!(a != null && a.execute)) {
      (s = ui.notifications) == null || s.error("MWD roll system not initialized.");
      return;
    }
    const r = Zo(this);
    await a.execute({
      actor: this,
      payload: {
        intent: "attack",
        sourceType: "mechWeapon",
        sourceId: n.weaponId,
        weaponId: n.weaponId,
        edge: { pool: "physical.grit", allowed: ["pre", "post"] },
        tags: ["combat", "attack", "machine"],
        sourceTokenId: (r == null ? void 0 : r.id) ?? null,
        operatorActorUuid: String(e ?? "").trim()
      }
    });
  }
  async rollPilotingCheck({ operatorActorUuid: e = "" } = {}) {
    await this._rollQuickSkill(this.system.skills.piloting, {
      operatorActorUuid: e,
      machineAttributeKey: A.actorAttributes.handling,
      quickAction: { title: N.actor.vehicle.quickActions.pilotingCheck }
    });
  }
  async rollElectronicWarfare({ operatorActorUuid: e = "" } = {}) {
    var l, c;
    const t = Zo(this), n = tc({ actor: this, token: t }), a = [
      {
        id: "acquire",
        intent: "acquire",
        label: "Acquire Target",
        hint: "Advance detection state on the first eligible targeted token.",
        disabled: !n.canAcquireAny
      },
      {
        id: "targeting",
        intent: "targeting",
        label: "Generate Fire Solution",
        hint: "Create targeting data for the first eligible targeted token.",
        disabled: !n.canTargetAny
      }
    ].filter((u) => !u.disabled);
    if (!a.length) {
      ui.notifications.warn(N.actor.vehicle.quickActions.errors.noSensorSweep);
      return;
    }
    const r = a.length === 1 ? a[0] : await this._promptElectronicWarfareAction(a);
    if (!r) return;
    const s = Hg(n, r.intent);
    if (!s) {
      (l = ui.notifications) == null || l.warn("No targeted token is ready for that EW action.");
      return;
    }
    const o = er();
    if (!(o != null && o.execute)) {
      (c = ui.notifications) == null || c.error("MWD roll system not initialized.");
      return;
    }
    await o.execute({
      actor: this,
      payload: {
        intent: r.intent,
        sourceTokenId: (t == null ? void 0 : t.id) ?? null,
        targetTokenId: s.targetTokenId,
        targetTokenUuid: s.targetTokenUuid,
        operatorActorUuid: String(e ?? "").trim()
      }
    });
  }
  async rollSensorSweep(e = {}) {
    return this.rollElectronicWarfare(e);
  }
  async rollEmergencyRepair({ operatorActorUuid: e = "" } = {}) {
    var s, o, l, c;
    const t = Ek(this);
    if (!t.length) {
      (s = ui.notifications) == null || s.warn("No active criticals or repairable statuses are available.");
      return;
    }
    const n = t.length === 1 ? t[0] : await this._promptRepairIssue(t);
    if (!n) return;
    const a = await No({
      machineActorUuid: this.uuid,
      issueKind: n.issueKind,
      issueId: n.issueId,
      critId: n.issueKind === "crit" ? n.issueId : "",
      statusId: n.issueKind === "status" ? n.issueId : "",
      remedyKey: n.remedyKey,
      operatorActorUuid: String(e ?? "").trim()
    }, {
      gmOverride: !!((o = game.user) != null && o.isGM)
    });
    if (!a.ok) {
      (l = ui.notifications) == null || l.warn(a.reason ?? "Unable to launch the repair action.");
      return;
    }
    const r = er();
    if (!(r != null && r.execute)) {
      (c = ui.notifications) == null || c.error("MWD roll system not initialized.");
      return;
    }
    await r.execute({
      actor: a.actor,
      payload: a.payload
    });
  }
  _prepareSkillMap() {
    return {
      gunnery: this._resolveSkill("gunnery"),
      melee: this._resolveSkill("meleeCombat"),
      piloting: this._resolveSkill("piloting"),
      perception: this._resolveSkill("perception"),
      technician: this._resolveSkill("technician")
    };
  }
  _prepareHeatTrack() {
    var r;
    const e = this.system ?? {}, t = an(e), n = foundry.utils.mergeObject(((r = e.mwd) == null ? void 0 : r.heat) ?? {}, {
      current: t.current,
      max: t.max,
      dissipation: t.dissipation,
      effectiveDissipation: t.effectiveDissipation,
      coolingImpaired: t.coolingImpaired,
      pendingGenerated: t.pendingGenerated,
      thresholds: {
        runningHot: t.thresholds.runningHot,
        overheated: t.thresholds.overheated,
        shutdown: t.thresholds.shutdown
      },
      penalties: {
        movementPenalty: t.penalties.movementPenalty,
        rangedDicePenalty: t.penalties.rangedDicePenalty,
        dangerLevel: t.penalties.dangerLevel
      },
      statusCode: t.statusCode,
      status: t.status,
      inDanger: t.inDanger,
      volatile: t.volatile
    }, { inplace: !1 }), a = t.statusCode;
    return this.system.mwd.heatStatus = {
      code: a,
      label: N.actor.battlemech.heat.status[a] ?? mu(a)
    }, n;
  }
  _prepareConfiguredWeaponGroups() {
    return js(this);
  }
  _resolveSkill(e) {
    var a;
    const t = this.items.find((r) => r.type === A.itemType.skill && r.system.code === e);
    if (t)
      return t;
    const n = Lt(e);
    if (n)
      return {
        name: n.label ?? ((a = N.skill) == null ? void 0 : a[e]) ?? e,
        system: {
          code: e,
          attribute: n.attribute,
          value: 0
        }
      };
  }
  _prepareWeaponGroups() {
    var r;
    const e = (((r = this.system.mwd) == null ? void 0 : r.weaponGroupDetails) ?? []).map((s) => ({
      ...s,
      weapons: s.memberWeapons ?? []
    })).filter((s) => s.weaponIds.length > 0);
    if (e.length > 0)
      return e.map((s) => ({
        id: s.id,
        name: s.name,
        weaponIds: s.weaponIds,
        isPrimary: s.isPrimary ?? !1,
        missingWeaponIds: s.missingWeaponIds ?? [],
        memberWeapons: s.memberWeapons ?? [],
        compatibilityWarnings: s.compatibilityWarnings ?? [],
        isAttackLegal: s.isAttackLegal !== !1,
        isAvailableThisActivation: s.isAvailableThisActivation !== !1,
        disableReason: s.disableReason ?? "",
        attackSummary: s.attackSummary ?? null
      }));
    const t = $l(this, { canonicalType: A.itemType.mechWeapon });
    if (t.length === 0)
      return [];
    const n = t.filter((s) => this.hasFavorite(A.itemType.mechWeapon, s.id)), a = [];
    return n.length > 0 && a.push({
      id: "favorite",
      name: N.actor.vehicle.quickActions.primaryWeapons,
      weaponIds: n.map((s) => s.id),
      isPrimary: !0
    }), a.push({
      id: "all",
      name: N.actor.vehicle.quickActions.allWeapons,
      weaponIds: t.map((s) => s.id),
      isPrimary: a.length === 0
    }), a;
  }
  _prepareMeleeProfiles() {
    const e = [{
      id: "unarmed",
      name: N.actor.vehicle.quickActions.unarmed,
      weaponId: null,
      damage: 1,
      notes: N.actor.vehicle.quickActions.unarmedNotes
    }], t = $l(this, { canonicalType: A.itemType.mechWeapon }).filter((n) => n.system.skill === "meleeCombat");
    return e.push(...t.map((n) => {
      var a;
      return {
        id: n.id,
        name: n.name,
        weaponId: n.id,
        damage: ((a = n.getDamage()) == null ? void 0 : a.value) ?? n.system.damage,
        notes: n.system.description ?? ""
      };
    })), e;
  }
  async _rollQuickSkill(e, t = {}) {
    var s, o, l;
    const n = er();
    if (n != null && n.execute) {
      await n.execute({
        actor: this,
        payload: {
          intent: "skill",
          key: ((s = e == null ? void 0 : e.system) == null ? void 0 : s.code) ?? "",
          attrKey: ((o = e == null ? void 0 : e.system) == null ? void 0 : o.attribute) ?? this.getPhysicalAgility(),
          ...t.machineAttributeKey ? { machineAttributeKey: t.machineAttributeKey } : {},
          ...t.operatorActorUuid ? { operatorActorUuid: String(t.operatorActorUuid).trim() } : {},
          quickAction: t.quickAction ?? null,
          edge: { allowed: ["pre", "post"] },
          tags: ["machine", "skill"]
        }
      });
      return;
    }
    const a = ((l = e == null ? void 0 : e.system) == null ? void 0 : l.attribute) ?? this.getPhysicalAgility(), r = foundry.utils.mergeObject(Bi.prepareActorRoll(this), {
      mode: jt.rollType.skill,
      skill: e,
      attribute1: a,
      specialization: void 0
    });
    t.quickAction && (r.quickAction = t.quickAction), await Bi.create(r);
  }
  async _promptWeaponGroup(e) {
    const t = Array.isArray(e) ? e.filter((s) => (s == null ? void 0 : s.isAttackLegal) !== !1) : [];
    if (t.length === 0) return null;
    if (t.length === 1) return t[0];
    const n = t.find((s) => s.isPrimary) ?? t[0], a = `<form class="mwd-quick-select">${t.map((s) => `
      <label class="quick-select-option">
        <input type="radio" name="weapon-group" value="${s.id}" ${s.id === n.id ? "checked" : ""}>
        <span>${s.name}${s.isPrimary ? ` (${N.actor.vehicle.quickActions.primaryLabel})` : ""}</span>
      </label>`).join("")}</form>`, r = await Dialog.prompt({
      title: N.actor.vehicle.quickActions.selectWeaponGroup,
      content: a,
      label: N.common.roll.button,
      callback: (s) => s.find('input[name="weapon-group"]:checked').val() ?? n.id
    });
    return t.find((s) => s.id === r) ?? n;
  }
  async _promptMeleeProfile(e) {
    if (e.length === 1)
      return e[0];
    const t = e[0], n = `<form class="mwd-quick-select">${e.map((r) => `
      <label class="quick-select-option">
        <input type="radio" name="melee-profile" value="${r.id}" ${r.id === t.id ? "checked" : ""}>
        <span>${r.name}</span>
      </label>`).join("")}</form>`, a = await Dialog.prompt({
      title: N.actor.vehicle.quickActions.selectMeleeProfile,
      content: n,
      label: N.common.roll.button,
      callback: (r) => r.find('input[name="melee-profile"]:checked').val() ?? t.id
    });
    return e.find((r) => r.id === a) ?? t;
  }
  async _promptSensorSweepSkill(e) {
    if (e.length === 1)
      return e[0];
    const t = `<form class="mwd-quick-select">${e.map((a) => `
      <label class="quick-select-option">
        <input type="radio" name="sensor-skill" value="${a.system.code}">
        <span>${a.name}</span>
      </label>`).join("")}</form>`, n = await Dialog.prompt({
      title: N.actor.vehicle.quickActions.selectSensorSkill,
      content: t,
      label: N.common.roll.button,
      callback: (a) => a.find('input[name="sensor-skill"]:checked').val()
    });
    return e.find((a) => a.system.code === n) ?? e[0];
  }
  async _promptElectronicWarfareAction(e) {
    const t = e[0], n = `<form class="mwd-quick-select">${e.map((r) => `
      <label class="quick-select-option">
        <input type="radio" name="ew-action" value="${r.id}" ${r.id === t.id ? "checked" : ""}>
        <span>${r.label}</span>
        <small>${r.hint}</small>
      </label>`).join("")}</form>`, a = await Dialog.prompt({
      title: "Electronic Warfare",
      content: n,
      label: N.common.roll.button,
      callback: (r) => r.find('input[name="ew-action"]:checked').val() ?? t.id
    });
    return e.find((r) => r.id === a) ?? t;
  }
  async _promptRepairIssue(e) {
    const t = e[0], n = `<form class="mwd-quick-select">${e.map((r) => `
      <label class="quick-select-option">
        <input type="radio" name="repair-issue" value="${r.issueKind}:${r.issueId}" ${r.issueKind === t.issueKind && r.issueId === t.issueId ? "checked" : ""}>
        <span>${r.label}</span>
        <small>${r.remedyLabel} | ${r.remedySummary || `DN ${r.totalDn}`}</small>
      </label>`).join("")}</form>`, a = await Dialog.prompt({
      title: N.actor.vehicle.quickActions.emergencyRepair,
      content: n,
      label: N.common.roll.button,
      callback: (r) => r.find('input[name="repair-issue"]:checked').val() ?? `${t.issueKind}:${t.issueId}`
    });
    return e.find((r) => `${r.issueKind}:${r.issueId}` === a) ?? t;
  }
  _serializeWeaponGroup(e, t) {
    return {
      id: e.id,
      name: e.name,
      isPrimary: e.isPrimary,
      weaponNames: t.map((n) => n.name)
    };
  }
}
const As = "activeModifiers", xu = [
  { value: "", label: "All Attributes" },
  { value: "reflexes", label: "Reflexes" },
  { value: "strength", label: "Strength" },
  { value: "willpower", label: "Willpower" },
  { value: "charisma", label: "Charisma" },
  { value: "intelligence", label: "Intelligence" },
  { value: "edge", label: "Edge" }
], $u = [
  { value: "", label: "All Intents" },
  { value: "skill", label: "Skill" },
  { value: "attack", label: "Attack" },
  { value: "defense", label: "Defense" },
  { value: "common", label: "Common" },
  { value: "attribute", label: "Attribute" },
  { value: "edge", label: "Edge" },
  { value: "initiative", label: "Initiative" }
];
function om(i) {
  const e = String(i ?? "").trim();
  return e === "" ? null : e;
}
function _k(i) {
  return String((i == null ? void 0 : i.intent) ?? "").trim() || null;
}
function xk(i) {
  var e, t, n;
  return ((e = i == null ? void 0 : i.data) == null ? void 0 : e.attrKey) ?? // skill rolls
  ((n = (t = i == null ? void 0 : i.attack) == null ? void 0 : t.skill) == null ? void 0 : n.attribute) ?? // attack rolls
  null;
}
function lm(i, e) {
  return i ? e ? i === e : !1 : !0;
}
function Wg(i) {
  return {
    id: String((i == null ? void 0 : i.id) ?? ""),
    label: String((i == null ? void 0 : i.label) ?? "").trim(),
    value: Math.trunc(Number((i == null ? void 0 : i.value) ?? 0)) || 0,
    enabled: (i == null ? void 0 : i.enabled) !== !1,
    attributeFilter: om(i == null ? void 0 : i.attributeFilter),
    intentFilter: om(i == null ? void 0 : i.intentFilter),
    source: (i == null ? void 0 : i.source) === "preset" ? "preset" : "adhoc"
  };
}
class $k {
  constructor() {
    z(this, "id", "mwd.sceneModifiers");
  }
  collect({ resolved: e } = {}) {
    var s;
    const t = (s = canvas == null ? void 0 : canvas.scene) == null ? void 0 : s.getFlag("mwd", As);
    if (!Array.isArray(t) || !t.length) return [];
    const n = _k(e), a = xk(e), r = [];
    for (const o of t) {
      const l = Wg(o);
      l.enabled && lm(l.intentFilter, n) && lm(l.attributeFilter, a) && r.push({
        id: l.id || `scene:${l.label}`,
        label: l.label,
        value: l.value,
        source: "Scene"
      });
    }
    return r;
  }
}
const Bk = `systems/${M}/templates/settings/collection-editor.hbs`, qg = /* @__PURE__ */ new Map(), el = /* @__PURE__ */ new Map();
function di(i = []) {
  const e = Array.isArray(i) ? i.filter(Boolean) : [String(i ?? "").trim()].filter(Boolean), t = new Error(e[0] ?? "Invalid settings data.");
  return t.validationErrors = e, t;
}
function Ga(i) {
  Fk(i), qg.set(i.id, i), game.settings.register(M, i.settingKey, {
    scope: "world",
    config: !1,
    type: i.settingType ?? Object,
    default: i.defaultData()
  }), game.settings.registerMenu(M, i.menuKey, {
    name: i.menu.name,
    label: i.menu.label,
    hint: i.menu.hint,
    icon: i.menu.icon,
    type: Hk(i.id),
    restricted: i.menu.restricted ?? !0
  });
}
function zk(i) {
  return qg.get(i) ?? null;
}
function Fk(i) {
  var e, t;
  if (!(i != null && i.id)) throw new Error("Settings collection definition requires an id.");
  if (!(i != null && i.settingKey)) throw new Error(`Settings collection definition "${i.id}" requires a settingKey.`);
  if (!(i != null && i.menuKey)) throw new Error(`Settings collection definition "${i.id}" requires a menuKey.`);
  if (!((e = i == null ? void 0 : i.menu) != null && e.name) || !((t = i == null ? void 0 : i.menu) != null && t.label))
    throw new Error(`Settings collection definition "${i.id}" requires menu metadata.`);
  if (typeof i.defaultData != "function")
    throw new Error(`Settings collection definition "${i.id}" requires defaultData().`);
  if (typeof i.toRows != "function")
    throw new Error(`Settings collection definition "${i.id}" requires toRows(value).`);
  if (typeof i.rowsToValue != "function")
    throw new Error(`Settings collection definition "${i.id}" requires rowsToValue(rows).`);
  if (typeof i.serializeBulk != "function" || typeof i.parseBulk != "function")
    throw new Error(`Settings collection definition "${i.id}" requires bulk serialization helpers.`);
  if (!Array.isArray(i.rowSchema) || !i.rowSchema.length)
    throw new Error(`Settings collection definition "${i.id}" requires a non-empty rowSchema.`);
}
function Hk(i) {
  if (el.has(i))
    return el.get(i);
  class e extends Vg {
  }
  return z(e, "definitionId", i), el.set(i, e), e;
}
var ue, Yg, ic, ws, Ts, wa, nc, cr, Jg, Qg, Vt;
class Vg extends FormApplication {
  constructor(t = {}, n = {}) {
    super(t, n);
    Ce(this, ue);
    const a = v(this, ue, Ts).call(this);
    this.editorState = {
      tab: "rows",
      rows: this.definition.toRows(a),
      bulkText: this.definition.serializeBulk(a),
      errors: []
    };
  }
  static get defaultOptions() {
    return foundry.utils.mergeObject(super.defaultOptions, {
      id: `${M}-${this.definitionId}-editor`,
      classes: ["mwd", "mwd-settings-editor"],
      template: Bk,
      width: 880,
      height: 760,
      resizable: !0,
      submitOnChange: !1,
      closeOnSubmit: !1
    }, { inplace: !1 });
  }
  get definition() {
    const t = zk(this.constructor.definitionId);
    if (!t)
      throw new Error(`Missing settings collection definition: ${this.constructor.definitionId}`);
    return t;
  }
  get title() {
    var t;
    return this.definition.title ?? ((t = this.definition.menu) == null ? void 0 : t.name) ?? "Settings Editor";
  }
  getData(t = {}) {
    const n = v(this, ue, Qg).call(this), a = this.editorState.rows.map((r, s, o) => ({
      index: s,
      fields: n.map((l) => v(this, ue, Jg).call(this, l, r, s)),
      canMoveUp: s > 0,
      canMoveDown: s < o.length - 1
    }));
    return foundry.utils.mergeObject(super.getData(t), {
      definitionId: this.definition.id,
      title: this.title,
      description: this.definition.description ?? "",
      helpText: this.definition.helpText ?? "",
      bulkHelpText: this.definition.bulkHelpText ?? "",
      currentTab: this.editorState.tab,
      isRowsTab: this.editorState.tab === "rows",
      isBulkTab: this.editorState.tab === "bulk",
      errors: [...this.editorState.errors ?? []],
      columns: n.map((r) => ({ key: r.key, label: r.label })),
      rows: a,
      hasRows: a.length > 0,
      bulkText: this.editorState.bulkText ?? "",
      addRowLabel: this.definition.addRowLabel ?? "Add Row",
      saveLabel: this.definition.saveLabel ?? "Save",
      cancelLabel: this.definition.cancelLabel ?? "Cancel",
      resetLabel: this.definition.resetLabel ?? "Reset to Saved",
      defaultsLabel: this.definition.defaultsLabel ?? "Restore Defaults",
      emptyStateText: this.definition.emptyStateText ?? "No rows yet. Add one to start this collection."
    }, { inplace: !1, overwrite: !0 });
  }
  activateListeners(t) {
    super.activateListeners(t), t.find("[data-action]").each((n, a) => {
      a.addEventListener("click", (r) => {
        var l;
        const s = r.currentTarget, o = String(((l = s == null ? void 0 : s.dataset) == null ? void 0 : l.action) ?? "").trim();
        o && v(this, ue, Yg).call(this, o, r, s);
      });
    });
  }
  async _onSubmit(t, { updateData: n = null, preventClose: a = !0, preventRender: r = !0 } = {}) {
    return super._onSubmit(t, { updateData: n, preventClose: a, preventRender: r });
  }
  async _updateObject(t, n) {
    var a;
    v(this, ue, Vt).call(this, []);
    try {
      const r = this.editorState.tab === "bulk" ? this.definition.parseBulk(v(this, ue, cr).call(this)) : this.definition.rowsToValue(v(this, ue, nc).call(this));
      await game.settings.set(M, this.definition.settingKey, r);
      const s = v(this, ue, Ts).call(this);
      v(this, ue, ws).call(this, s), await this.close();
    } catch (r) {
      v(this, ue, Vt).call(this, rs(r)), this.editorState.errors.length && ((a = ui.notifications) == null || a.error(this.editorState.errors[0])), this.render(!1);
    }
  }
}
ue = new WeakSet(), Yg = async function(t, n, a) {
  var r, s, o, l, c, u, d, m;
  switch (n.preventDefault(), n.stopPropagation(), t) {
    case "switchRows":
      v(this, ue, cr).call(this), this.editorState.tab = "rows", v(this, ue, Vt).call(this, []), this.render(!1);
      return;
    case "switchBulk":
      v(this, ue, wa).call(this);
      try {
        const f = this.definition.rowsToValue(this.editorState.rows);
        this.editorState.bulkText = this.definition.serializeBulk(f), this.editorState.tab = "bulk", v(this, ue, Vt).call(this, []);
      } catch (f) {
        v(this, ue, Vt).call(this, rs(f)), this.editorState.errors.length && ((r = ui.notifications) == null || r.warn(this.editorState.errors[0]));
      }
      this.render(!1);
      return;
    case "addRow":
      v(this, ue, wa).call(this), this.editorState.rows.push(((o = (s = this.definition).createEmptyRow) == null ? void 0 : o.call(s)) ?? {}), v(this, ue, Vt).call(this, []), this.render(!1);
      return;
    case "removeRow":
      v(this, ue, wa).call(this), this.editorState.rows.splice(Number(((l = a == null ? void 0 : a.dataset) == null ? void 0 : l.index) ?? -1), 1), v(this, ue, Vt).call(this, []), this.render(!1);
      return;
    case "moveRowUp":
      v(this, ue, wa).call(this), v(this, ue, ic).call(this, Number(((c = a == null ? void 0 : a.dataset) == null ? void 0 : c.index) ?? -1), -1), v(this, ue, Vt).call(this, []), this.render(!1);
      return;
    case "moveRowDown":
      v(this, ue, wa).call(this), v(this, ue, ic).call(this, Number(((u = a == null ? void 0 : a.dataset) == null ? void 0 : u.index) ?? -1), 1), v(this, ue, Vt).call(this, []), this.render(!1);
      return;
    case "loadBulk":
      try {
        const f = this.definition.parseBulk(v(this, ue, cr).call(this));
        this.editorState.rows = this.definition.toRows(f), this.editorState.bulkText = this.definition.serializeBulk(f), this.editorState.tab = "rows", v(this, ue, Vt).call(this, []);
      } catch (f) {
        v(this, ue, Vt).call(this, rs(f)), this.editorState.errors.length && ((d = ui.notifications) == null || d.warn(this.editorState.errors[0]));
      }
      this.render(!1);
      return;
    case "formatBulk":
      try {
        const f = this.definition.parseBulk(v(this, ue, cr).call(this));
        this.editorState.bulkText = this.definition.serializeBulk(f), v(this, ue, Vt).call(this, []);
      } catch (f) {
        v(this, ue, Vt).call(this, rs(f)), this.editorState.errors.length && ((m = ui.notifications) == null || m.warn(this.editorState.errors[0]));
      }
      this.render(!1);
      return;
    case "resetSetting":
      v(this, ue, ws).call(this, v(this, ue, Ts).call(this)), this.render(!1);
      return;
    case "restoreDefaults":
      v(this, ue, ws).call(this, this.definition.defaultData()), this.render(!1);
      return;
    case "cancel":
      await this.close();
      return;
    default:
      return;
  }
}, ic = function(t, n) {
  if (!Number.isInteger(t)) return;
  const a = t + n;
  if (t < 0 || a < 0 || a >= this.editorState.rows.length) return;
  const r = [...this.editorState.rows], [s] = r.splice(t, 1);
  r.splice(a, 0, s), this.editorState.rows = r;
}, ws = function(t) {
  this.editorState.rows = this.definition.toRows(t), this.editorState.bulkText = this.definition.serializeBulk(t), this.editorState.tab = "rows", v(this, ue, Vt).call(this, []);
}, Ts = function() {
  const t = game.settings.get(M, this.definition.settingKey);
  return foundry.utils.deepClone(t ?? this.definition.defaultData());
}, wa = function() {
  this.editorState.rows = v(this, ue, nc).call(this);
}, nc = function() {
  const t = foundry.utils.expandObject(this._getSubmitData()), n = (t == null ? void 0 : t.rows) ?? {};
  return Object.keys(n).sort((a, r) => Number(a) - Number(r)).map((a) => {
    const r = n[a] ?? {};
    return Object.fromEntries(
      this.definition.rowSchema.map((s) => [
        s.key,
        String((r == null ? void 0 : r[s.key]) ?? "")
      ])
    );
  });
}, cr = function() {
  var a;
  const t = this.form, n = (a = t == null ? void 0 : t.querySelector) == null ? void 0 : a.call(t, 'textarea[name="bulkText"]');
  return n instanceof HTMLTextAreaElement && (this.editorState.bulkText = n.value), this.editorState.bulkText ?? "";
}, Jg = function(t, n, a) {
  const r = t.type ?? "text", s = String((n == null ? void 0 : n[t.key]) ?? t.default ?? ""), o = r === "select" ? Uk(t).map((l) => ({
    value: String(l.value ?? ""),
    label: String(l.label ?? l.value ?? ""),
    selected: String(l.value ?? "") === s
  })) : [];
  return {
    key: t.key,
    label: t.label,
    type: r,
    inputType: r === "select" ? "text" : r,
    name: `rows.${a}.${t.key}`,
    value: s,
    placeholder: t.placeholder ?? "",
    min: t.min ?? "",
    max: t.max ?? "",
    step: t.step ?? "",
    options: o
  };
}, Qg = function() {
  return this.definition.rowSchema.map((t) => ({
    ...t,
    type: t.type ?? "text"
  }));
}, Vt = function(t = []) {
  this.editorState.errors = Array.isArray(t) ? t.filter(Boolean) : [];
}, z(Vg, "definitionId", "");
function Uk(i) {
  const e = typeof i.options == "function" ? i.options() : i.options;
  return Array.isArray(e) ? e : [];
}
function rs(i) {
  const e = Array.isArray(i == null ? void 0 : i.validationErrors) ? i.validationErrors.filter(Boolean) : [String((i == null ? void 0 : i.message) ?? "Unable to save settings.").trim()].filter(Boolean);
  return e.length ? e : ["Unable to save settings."];
}
const ac = "sceneModifierTemplates", jk = "sceneModifierTemplateEditor", Kk = Object.freeze([]);
function On(i) {
  const e = String(i ?? "").trim();
  return e === "" ? "" : e;
}
function Xg(i = []) {
  const e = [], t = [], n = /* @__PURE__ */ new Set();
  if ((Array.isArray(i) ? i : []).forEach((a, r) => {
    const s = String((a == null ? void 0 : a.label) ?? "").trim(), o = String((a == null ? void 0 : a.value) ?? "").trim(), l = `Row ${r + 1}`;
    if (!s) {
      t.push(`${l}: label cannot be blank.`);
      return;
    }
    if (n.has(s.toLowerCase())) {
      t.push(`${l}: duplicate label "${s}".`);
      return;
    }
    n.add(s.toLowerCase());
    const c = Number(o);
    if (!Number.isFinite(c)) {
      t.push(`${l}: value must be a number.`);
      return;
    }
    e.push({
      label: s,
      value: Math.trunc(c),
      attributeFilter: On(a == null ? void 0 : a.attributeFilter),
      intentFilter: On(a == null ? void 0 : a.intentFilter)
    });
  }), t.length) throw di(t);
  return e;
}
function Gk(i = []) {
  return (Array.isArray(i) ? i : []).map((e) => ({
    label: String((e == null ? void 0 : e.label) ?? ""),
    value: String((e == null ? void 0 : e.value) ?? "0"),
    attributeFilter: On(e == null ? void 0 : e.attributeFilter),
    intentFilter: On(e == null ? void 0 : e.intentFilter)
  }));
}
function Wk(i = "") {
  const e = String(i ?? "").trim();
  if (!e) return [];
  let t;
  try {
    t = JSON.parse(e);
  } catch (n) {
    throw di([
      `Bulk JSON must be valid JSON: ${n.message}`
    ]);
  }
  if (!Array.isArray(t))
    throw di(["Bulk JSON must be an array."]);
  return Xg(t.map((n) => ({
    label: String((n == null ? void 0 : n.label) ?? ""),
    value: String((n == null ? void 0 : n.value) ?? "0"),
    attributeFilter: On(n == null ? void 0 : n.attributeFilter),
    intentFilter: On(n == null ? void 0 : n.intentFilter)
  })));
}
function qk(i = []) {
  return JSON.stringify(
    (Array.isArray(i) ? i : []).map((e) => ({
      label: String((e == null ? void 0 : e.label) ?? ""),
      value: Number((e == null ? void 0 : e.value) ?? 0),
      attributeFilter: On(e == null ? void 0 : e.attributeFilter),
      intentFilter: On(e == null ? void 0 : e.intentFilter)
    })),
    null,
    2
  );
}
const Vk = {
  id: "scene-modifier-templates",
  menuKey: jk,
  settingKey: ac,
  settingType: Array,
  title: "Scene Modifier Templates",
  description: "Define reusable scene modifier presets that can be applied per scene via the GM Gadget.",
  helpText: "Labels must be unique. Value is a signed integer (+/–). Filters are optional — blank means the modifier applies to all matching rolls.",
  bulkHelpText: 'JSON shape: [{ "label": "Darkness", "value": -2, "attributeFilter": "reflexes", "intentFilter": "attack" }]',
  emptyStateText: "No templates yet. Add one to make it available in the GM Gadget Scene tab.",
  addRowLabel: "Add Template",
  rowSchema: [
    {
      key: "label",
      label: "Label",
      type: "text",
      placeholder: "Darkness"
    },
    {
      key: "value",
      label: "Value",
      type: "number",
      step: 1,
      placeholder: "-2"
    },
    {
      key: "attributeFilter",
      label: "Attribute Filter",
      type: "select",
      options: xu
    },
    {
      key: "intentFilter",
      label: "Intent Filter",
      type: "select",
      options: $u
    }
  ],
  menu: {
    name: "Scene Modifier Templates",
    label: "Configure",
    hint: "Edit the preset scene modifier templates available in the GM Gadget.",
    icon: "fas fa-cloud",
    restricted: !0
  },
  defaultData: () => foundry.utils.deepClone(Kk),
  createEmptyRow: () => ({
    label: "",
    value: "0",
    attributeFilter: "",
    intentFilter: ""
  }),
  toRows: Gk,
  rowsToValue: Xg,
  parseBulk: Wk,
  serializeBulk: qk
};
function Yk() {
  Ga(Vk);
}
const { ApplicationV2: Jk, HandlebarsApplicationMixin: Qk } = foundry.applications.api, Xk = "mwd-gmgadget", Zg = "gmDnPresets", ks = "gmNextDn", ur = "gmDnAnnounceToChat", Zk = Object.freeze([
  { label: "Standard", dn: 1 },
  { label: "Challenging", dn: 2 },
  { label: "Hard", dn: 3 },
  { label: "Extreme", dn: 4 }
]), eM = "systems/mwd/templates/v2/mwd-gmgadget.hbs", dr = Object.freeze({
  actorId: "",
  mode: "physical",
  delta: 1,
  useArmor: !1,
  damageType: "concussive",
  statusId: "",
  statusActive: !0,
  source: "",
  notes: ""
}), tM = Object.freeze({
  label: "Hazard Zone",
  startExposure: pe.minor,
  escalationRate: 1,
  escalationIntervalTurns: 1,
  escalationMax: pe.full,
  onFullBurnDelta: 0,
  clearOnExit: !0,
  damage: 6,
  ap: 0,
  damageType: "thermal",
  color: "#d86a2c"
}), iM = Object.freeze({
  attackerTokenId: "",
  bulkDetectionState: "blind"
});
function nM(i = "") {
  return String(i ?? "").split(",").map((e) => e.trim()).filter(Boolean).map((e) => {
    const [t, n] = e.split(":").map((s) => (s ?? "").trim()), a = t || "DN", r = Number.isFinite(Number(n)) ? Number(n) : Number(t);
    return {
      label: a,
      dn: Number.isFinite(r) ? Math.max(0, Math.trunc(r)) : null
    };
  }).filter((e) => Number.isFinite(e.dn));
}
function aM(i = []) {
  const e = new Error(i[0] ?? "Invalid GM DN presets.");
  return e.validationErrors = Array.isArray(i) ? i.filter(Boolean) : [], e;
}
function rM() {
  return foundry.utils.deepClone(Zk);
}
function Wr(i, { strict: e = !1 } = {}) {
  const t = typeof i == "string" ? nM(i) : Array.isArray(i) ? i : [], n = [], a = [], r = /* @__PURE__ */ new Set();
  if (t.forEach((s, o) => {
    const l = String((s == null ? void 0 : s.label) ?? "").trim(), c = s == null ? void 0 : s.dn, u = `Preset ${o + 1}`;
    if (!l) {
      e && a.push(`${u}: label cannot be blank.`);
      return;
    }
    const d = l.toLowerCase();
    if (r.has(d)) {
      e && a.push(`${u}: duplicate label "${l}".`);
      return;
    }
    const m = Number(c);
    if (!Number.isFinite(m)) {
      e && a.push(`${u}: DN must be numeric.`);
      return;
    }
    if (m < 0) {
      e && a.push(`${u}: DN cannot be negative.`);
      return;
    }
    r.add(d), n.push({
      label: l,
      dn: Math.trunc(m)
    });
  }), e && a.length) throw aM(a);
  return n;
}
function tl(i = {}) {
  return foundry.utils.mergeObject(
    foundry.utils.deepClone(dr),
    i ?? {},
    { inplace: !1, overwrite: !0 }
  );
}
function il(i = {}) {
  return foundry.utils.mergeObject(
    foundry.utils.deepClone(tM),
    i ?? {},
    { inplace: !1, overwrite: !0 }
  );
}
function nl(i = {}) {
  return foundry.utils.mergeObject(
    foundry.utils.deepClone(iM),
    i ?? {},
    { inplace: !1, overwrite: !0 }
  );
}
function eh(i) {
  return (i == null ? void 0 : i.type) === A.actorTypes.vehicle || (i == null ? void 0 : i.type) === A.actorTypes.battlemech;
}
function sM() {
  var e, t;
  const i = game.combat;
  return !i || (e = i.scene) != null && e.id && i.scene.id !== ((t = canvas == null ? void 0 : canvas.scene) == null ? void 0 : t.id) ? [] : Array.from(i.combatants ?? []).filter((n) => eh(n == null ? void 0 : n.actor)).map((n) => {
    var o, l, c, u;
    const a = n.token ?? ((c = (l = (o = canvas == null ? void 0 : canvas.scene) == null ? void 0 : o.tokens) == null ? void 0 : l.get) == null ? void 0 : c.call(l, n.tokenId)) ?? null, r = n.actor ?? (a == null ? void 0 : a.actor) ?? null, s = String((a == null ? void 0 : a.name) ?? (r == null ? void 0 : r.name) ?? "Machine").trim() || "Machine";
    return {
      tokenId: String(n.tokenId ?? (a == null ? void 0 : a.id) ?? "").trim(),
      tokenUuid: String((a == null ? void 0 : a.uuid) ?? ((u = a == null ? void 0 : a.document) == null ? void 0 : u.uuid) ?? "").trim(),
      actorId: String((r == null ? void 0 : r.id) ?? "").trim(),
      actorUuid: String((r == null ? void 0 : r.uuid) ?? "").trim(),
      label: s
    };
  }).filter((n) => n.tokenId);
}
function oM(i = [], e = "") {
  var r, s, o;
  const t = String(e ?? "").trim();
  if (t && i.some((l) => l.tokenId === t))
    return t;
  const n = Array.from(((r = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : r.controlled) ?? []).find((l) => eh(l == null ? void 0 : l.actor)), a = String((n == null ? void 0 : n.id) ?? ((s = n == null ? void 0 : n.document) == null ? void 0 : s.id) ?? "").trim();
  return a && i.some((l) => l.tokenId === a) ? a : ((o = i[0]) == null ? void 0 : o.tokenId) ?? "";
}
function tr(i = "") {
  var t, n;
  const e = String(i ?? "").trim();
  return e ? ((n = (t = game.combat) == null ? void 0 : t.combatants) == null ? void 0 : n.find((a) => String((a == null ? void 0 : a.tokenId) ?? "").trim() === e)) ?? null : null;
}
function th(i = "blind") {
  return Ni.map((e) => ({
    value: e,
    label: vi(e),
    selected: e === i
  }));
}
function lM(i = null, e = []) {
  return Array.from(e ?? []).filter((t) => t == null ? void 0 : t.actor).map((t) => {
    var s, o, l;
    const n = String(((s = t.document) == null ? void 0 : s.uuid) ?? t.uuid ?? "").trim();
    if (!n) return null;
    const a = pa(i, n), r = a.packet ?? null;
    return {
      targetTokenId: String(t.id ?? ((o = t.document) == null ? void 0 : o.id) ?? "").trim(),
      targetTokenUuid: n,
      tokenName: String(t.name ?? ((l = t.actor) == null ? void 0 : l.name) ?? "Target").trim() || "Target",
      detectionState: a.detectionState,
      detectionStateLabel: vi(a.detectionState),
      stateOptions: th(a.detectionState),
      hasPacket: !!r,
      packetValue: Number((r == null ? void 0 : r.value) ?? 0) || 0,
      packetRound: (r == null ? void 0 : r.round) ?? null,
      packetExpiry: (r == null ? void 0 : r.expiresAfterRound) ?? null
    };
  }).filter(Boolean);
}
function ih(i = null) {
  const e = (i == null ? void 0 : i.document) ?? i ?? null, t = Array.from((e == null ? void 0 : e.shapes) ?? []);
  if (t.length !== 1) return t.length > 1 ? "multiple" : "";
  const n = t[0], a = typeof (n == null ? void 0 : n.toObject) == "function" ? n.toObject() : n && typeof n == "object" ? n : null;
  return String((a == null ? void 0 : a.type) ?? "").trim().toLowerCase();
}
function cm(i = null) {
  return !!ih(i);
}
function um() {
  var n, a;
  const i = Array.from(((n = canvas == null ? void 0 : canvas.regions) == null ? void 0 : n.controlled) ?? []).map((r) => (r == null ? void 0 : r.document) ?? r ?? null).find(cm);
  if (i) return i;
  const e = ((a = canvas == null ? void 0 : canvas.regions) == null ? void 0 : a.hover) ?? null, t = (e == null ? void 0 : e.document) ?? e ?? null;
  return cm(t) ? t : null;
}
function cM(i = null) {
  var o, l;
  const e = (i == null ? void 0 : i.document) ?? i ?? null;
  if (!e)
    return {
      label: "No region selected",
      reason: "Select a Region created in Measured Template Mode on the current scene to turn it into a hazard.",
      supported: !1
    };
  const t = ih(e), n = hf(e);
  if (!n)
    return {
      label: "Unsupported region",
      reason: t === "multiple" ? "The selected Region has multiple shapes and cannot be converted into a hazard template." : `The selected Region shape "${t || "unknown"}" is not supported for hazard conversion yet.`,
      supported: !1
    };
  const a = String(n.shape ?? "").trim().toLowerCase(), r = (l = (o = canvas == null ? void 0 : canvas.scene) == null ? void 0 : o.grid) != null && l.units ? ` ${canvas.scene.grid.units}` : "";
  return {
    label: a === "rect" ? `RECT ${Number(n.width ?? 0) || 0} x ${Number(n.height ?? 0) || 0}${r}`.trim() : `${a.toUpperCase()} ${Number(n.distance ?? 0) || 0}${r}`.trim(),
    reason: "",
    supported: !0
  };
}
function uM(i) {
  var t, n;
  return !(i != null && i.token) || !(i != null && i.actor) ? {
    label: "No scene target",
    reason: String((i == null ? void 0 : i.reason) ?? "No controlled or targeted token."),
    supported: !1
  } : {
    label: String(((t = i.token) == null ? void 0 : t.name) ?? ((n = i.actor) == null ? void 0 : n.name) ?? "Token").trim(),
    reason: "",
    supported: !0
  };
}
function dM(i) {
  var t;
  if (!(i != null && i.actor))
    return {
      label: "No target selected",
      source: "",
      reason: String((i == null ? void 0 : i.reason) ?? "Choose a supported character target.")
    };
  const e = i.source === "scene" || i.source === "token" ? "Scene target" : "Actor fallback";
  return {
    label: String(((t = i.actor) == null ? void 0 : t.name) ?? "Character").trim() || "Character",
    source: e,
    reason: ""
  };
}
function mM(i) {
  return Jt.getStatusOptions(i);
}
function fM(i = "mwd") {
  game.settings.register(i, ks, {
    scope: "client",
    config: !1,
    type: Number,
    default: 1
  }), game.settings.register(i, ur, {
    scope: "client",
    config: !0,
    name: "Announce GM DN changes to chat",
    hint: "If enabled, posts a small chat notice when you change the current DN preset.",
    type: Boolean,
    default: !1
  });
}
const qe = class qe extends Qk(Jk) {
  constructor({ systemId: e = "mwd", ...t } = {}) {
    super(t), this.systemId = e, this.activeTab = "difficulty", this.harmState = tl(), this.targetingState = nl(), this.hazardState = il();
  }
  async render(e = {}) {
    var t;
    return (t = game.user) != null && t.isGM ? super.render(e) : this;
  }
  async _prepareContext(e) {
    var k, C, I, O, x, B;
    const t = await super._prepareContext(e), n = Wr(
      game.settings.get(this.systemId, Zg),
      { strict: !1 }
    ), a = Number(game.settings.get(this.systemId, ks) ?? 1), r = !!game.settings.get(this.systemId, ur), s = tl(this.harmState), o = Jt.getActorOptions({ mode: s.mode }), l = Jt.getSceneTarget({ mode: s.mode }), c = this.harmState.actorId ? ((C = (k = game.actors) == null ? void 0 : k.get) == null ? void 0 : C.call(k, this.harmState.actorId)) ?? null : null, u = Jt.resolveTarget({
      actor: c,
      actorId: this.harmState.actorId,
      preferSceneTarget: !0,
      mode: s.mode
    }), d = mM(u.actor ?? c ?? null);
    d.length && !d.some((P) => P.value === s.statusId) && (s.statusId = d[0].value, this.harmState.statusId = s.statusId);
    const m = dm(
      game.settings.get(this.systemId, ac)
    ), f = mm(
      (I = canvas == null ? void 0 : canvas.scene) == null ? void 0 : I.getFlag("mwd", As)
    ), p = um(), g = cM(p), h = il(this.hazardState), y = nl(this.targetingState), b = sM(), S = oM(b, y.attackerTokenId);
    y.attackerTokenId = S, this.targetingState.attackerTokenId = S;
    const w = tr(S), T = lM(w, ((O = game.user) == null ? void 0 : O.targets) ?? []);
    return foundry.utils.mergeObject(t, {
      presets: n,
      currentDn: a,
      currentTab: this.activeTab,
      announce: r,
      isGM: ((x = game.user) == null ? void 0 : x.isGM) ?? !1,
      scene: {
        hasScene: !!(canvas != null && canvas.scene),
        templates: m,
        activeModifiers: f,
        attributeFilterOptions: xu,
        intentFilterOptions: $u
      },
      harm: {
        state: s,
        actorOptions: o,
        modes: Jt.MODE_OPTIONS,
        damageTypes: Xd,
        statusOptions: d,
        sceneTarget: uM(l),
        effectiveTarget: dM(u),
        canApply: !!u.actor,
        applyReason: u.reason || "",
        useArmorAvailable: s.mode === "physical" || s.mode === "fatigue",
        showDamageType: (s.mode === "physical" || s.mode === "fatigue") && s.useArmor,
        showStatusFields: s.mode === "status",
        showDeltaFields: s.mode !== "status"
      },
      targeting: {
        state: y,
        attackerOptions: b.map((P) => ({
          ...P,
          selected: P.tokenId === S
        })),
        hasAttackerOptions: b.length > 0,
        hasAttacker: !!w,
        attackerLabel: ((B = b.find((P) => P.tokenId === S)) == null ? void 0 : B.label) ?? "",
        rows: T,
        hasTargets: T.length > 0,
        emptyState: "Target one or more tokens on the canvas to inspect directional targeting state.",
        bulkStateOptions: th(y.bulkDetectionState)
      },
      hazard: {
        state: h,
        template: g,
        exposureTiers: [
          { value: pe.minor, label: "Minor" },
          { value: pe.major, label: "Major" },
          { value: pe.full, label: "Full" }
        ],
        damageTypes: Xd,
        canCreate: !!(canvas != null && canvas.scene && g.supported),
        createReason: g.reason || ""
      }
    });
  }
  _getRootElement() {
    var e;
    return this.element instanceof HTMLElement ? this.element : (e = this.element) == null ? void 0 : e[0];
  }
  _captureHarmStateFromDom(e = null) {
    var r;
    const t = ((r = e == null ? void 0 : e.closest) == null ? void 0 : r.call(e, ".mwd-gmgadget__root")) ?? this._getRootElement();
    if (!(t instanceof HTMLElement)) return this.harmState;
    const n = (s, o = "") => {
      const l = t.querySelector(s);
      return l instanceof HTMLInputElement || l instanceof HTMLSelectElement || l instanceof HTMLTextAreaElement ? l.value : o;
    }, a = (s, o = !1) => {
      const l = t.querySelector(s);
      return l instanceof HTMLInputElement ? l.checked : o;
    };
    return this.harmState = tl({
      actorId: n('[name="harm-actorId"]', this.harmState.actorId),
      mode: n('[name="harm-mode"]', this.harmState.mode),
      delta: Number(n('[name="harm-delta"]', this.harmState.delta)),
      useArmor: a('[name="harm-useArmor"]', this.harmState.useArmor),
      damageType: n('[name="harm-damageType"]', this.harmState.damageType),
      statusId: n('[name="harm-statusId"]', this.harmState.statusId),
      statusActive: n('[name="harm-statusActive"]', this.harmState.statusActive ? "add" : "remove") !== "remove",
      source: n('[name="harm-source"]', this.harmState.source),
      notes: n('[name="harm-notes"]', this.harmState.notes)
    }), Number.isFinite(Number(this.harmState.delta)) || (this.harmState.delta = dr.delta), this.harmState;
  }
  _captureTargetingStateFromDom(e = null) {
    var a;
    const t = ((a = e == null ? void 0 : e.closest) == null ? void 0 : a.call(e, ".mwd-gmgadget__root")) ?? this._getRootElement();
    if (!(t instanceof HTMLElement)) return this.targetingState;
    const n = (r, s = "") => {
      const o = t.querySelector(r);
      return o instanceof HTMLInputElement || o instanceof HTMLSelectElement ? o.value : s;
    };
    return this.targetingState = nl({
      attackerTokenId: n('[name="targeting-attackerTokenId"]', this.targetingState.attackerTokenId),
      bulkDetectionState: n('[name="targeting-bulkDetectionState"]', this.targetingState.bulkDetectionState)
    }), this.targetingState;
  }
  async _onSetDn(e, t) {
    var r, s, o;
    if (e.preventDefault(), e.stopPropagation(), !((r = game.user) != null && r.isGM)) return;
    const n = Math.max(0, Math.trunc(Number(((s = t == null ? void 0 : t.dataset) == null ? void 0 : s.dn) ?? NaN)));
    if (!Number.isFinite(n)) return;
    if (await game.settings.set(this.systemId, ks, n), !!game.settings.get(this.systemId, ur)) {
      const l = String(((o = t == null ? void 0 : t.dataset) == null ? void 0 : o.label) ?? `DN ${n}`);
      await ChatMessage.create({
        speaker: ChatMessage.getSpeaker({ user: game.user }),
        content: `<div class="mwd-gm-notice"><b>GM Difficulty:</b> ${foundry.utils.escapeHTML(l)} (DN ${n} hits)</div>`
      });
    }
    return this.render({ parts: ["body"] });
  }
  async _onSwitchTab(e, t) {
    var a, r, s;
    (a = e == null ? void 0 : e.preventDefault) == null || a.call(e), (r = e == null ? void 0 : e.stopPropagation) == null || r.call(e);
    const n = String(((s = t == null ? void 0 : t.dataset) == null ? void 0 : s.tab) ?? "").trim();
    if (!(!n || n === this.activeTab))
      return this._captureHarmStateFromDom(t), this._captureTargetingStateFromDom(t), this._captureHazardStateFromDom(t), this.activeTab = n, this.render({ parts: ["body"] });
  }
  async _onClearDn(e, t) {
    var n;
    if (e.preventDefault(), e.stopPropagation(), !!((n = game.user) != null && n.isGM))
      return await game.settings.set(this.systemId, ks, 1), this.render({ parts: ["body"] });
  }
  async _onToggleAnnounce(e, t) {
    var a;
    if (e.preventDefault(), e.stopPropagation(), !((a = game.user) != null && a.isGM)) return;
    const n = !game.settings.get(this.systemId, ur);
    return await game.settings.set(this.systemId, ur, n), this.render({ parts: ["body"] });
  }
  async _onHarmInputChange(e, t) {
    var a, r;
    (a = e == null ? void 0 : e.preventDefault) == null || a.call(e), this._captureHarmStateFromDom(t);
    const n = String(((r = t == null ? void 0 : t.dataset) == null ? void 0 : r.harmKey) ?? "").trim();
    if (["actorId", "mode", "useArmor"].includes(n))
      return this.render({ parts: ["body"] });
  }
  async _onRefreshHarmTarget(e, t) {
    var n, a;
    return (n = e == null ? void 0 : e.preventDefault) == null || n.call(e), (a = e == null ? void 0 : e.stopPropagation) == null || a.call(e), this._captureHarmStateFromDom(t), this.render({ parts: ["body"] });
  }
  async _onApplyHarm(e, t) {
    var s, o, l, c, u;
    if ((s = e == null ? void 0 : e.preventDefault) == null || s.call(e), (o = e == null ? void 0 : e.stopPropagation) == null || o.call(e), !((l = game.user) != null && l.isGM)) return;
    const n = this._captureHarmStateFromDom(t), a = this._buildHarmPayload(n);
    if (!a) {
      (c = ui.notifications) == null || c.warn("Choose a valid harm action before applying it.");
      return;
    }
    const r = await Jt.apply({
      payload: a,
      options: {
        actorId: n.actorId,
        preferSceneTarget: !0,
        logToChat: !0
      }
    });
    return r != null && r.ok ? this.render({ parts: ["body"] }) : ((u = ui.notifications) == null || u.warn((r == null ? void 0 : r.reason) ?? "Unable to apply harm."), this.render({ parts: ["body"] }));
  }
  _getSelectedTargetingTargetUuids(e = null) {
    var n;
    const t = ((n = e == null ? void 0 : e.closest) == null ? void 0 : n.call(e, ".mwd-gmgadget__root")) ?? this._getRootElement();
    return t instanceof HTMLElement ? Array.from(t.querySelectorAll('input[name="targeting-selected"]:checked')).map((a) => a instanceof HTMLInputElement ? String(a.value ?? "").trim() : "").filter(Boolean) : [];
  }
  async _onTargetingInputChange(e, t) {
    var n;
    return (n = e == null ? void 0 : e.preventDefault) == null || n.call(e), this._captureTargetingStateFromDom(t), this.render({ parts: ["body"] });
  }
  async _onRefreshTargeting(e, t) {
    var n, a;
    return (n = e == null ? void 0 : e.preventDefault) == null || n.call(e), (a = e == null ? void 0 : e.stopPropagation) == null || a.call(e), this._captureTargetingStateFromDom(t), this.render({ parts: ["body"] });
  }
  async _onSetTargetingState(e, t) {
    var u, d, m, f, p, g, h, y;
    if ((u = e == null ? void 0 : e.preventDefault) == null || u.call(e), (d = e == null ? void 0 : e.stopPropagation) == null || d.call(e), !((m = game.user) != null && m.isGM)) return;
    const n = this._captureTargetingStateFromDom(t), a = tr(n.attackerTokenId), r = String(((f = t == null ? void 0 : t.dataset) == null ? void 0 : f.targetTokenUuid) ?? "").trim();
    if (!a || !r) {
      (p = ui.notifications) == null || p.warn("Select a machine attacker and a target row before setting targeting state.");
      return;
    }
    const s = ((g = t == null ? void 0 : t.closest) == null ? void 0 : g.call(t, ".mwd-gmgadget__root")) ?? this._getRootElement(), o = `targeting-row-state-${String(((h = t == null ? void 0 : t.dataset) == null ? void 0 : h.targetTokenId) ?? "").trim()}`, l = s instanceof HTMLElement ? s.querySelector(`[name="${o}"]`) : null, c = l instanceof HTMLSelectElement ? String(l.value ?? "").trim() : String(((y = t == null ? void 0 : t.dataset) == null ? void 0 : y.detectionState) ?? "").trim();
    return await Zl(a, r, c), this.render({ parts: ["body"] });
  }
  async _onClearTargetingPacket(e, t) {
    var s, o, l, c, u;
    if ((s = e == null ? void 0 : e.preventDefault) == null || s.call(e), (o = e == null ? void 0 : e.stopPropagation) == null || o.call(e), !((l = game.user) != null && l.isGM)) return;
    const n = this._captureTargetingStateFromDom(t), a = tr(n.attackerTokenId), r = String(((c = t == null ? void 0 : t.dataset) == null ? void 0 : c.targetTokenUuid) ?? "").trim();
    if (!a || !r) {
      (u = ui.notifications) == null || u.warn("Select a machine attacker and a target row before clearing packet data.");
      return;
    }
    return await ec(a, r), this.render({ parts: ["body"] });
  }
  async _onBulkSetTargetingState(e, t) {
    var s, o, l, c;
    if ((s = e == null ? void 0 : e.preventDefault) == null || s.call(e), (o = e == null ? void 0 : e.stopPropagation) == null || o.call(e), !((l = game.user) != null && l.isGM)) return;
    const n = this._captureTargetingStateFromDom(t), a = tr(n.attackerTokenId), r = this._getSelectedTargetingTargetUuids(t);
    if (!a || !r.length) {
      (c = ui.notifications) == null || c.warn("Select a machine attacker and at least one checked target row.");
      return;
    }
    for (const u of r)
      await Zl(a, u, n.bulkDetectionState);
    return this.render({ parts: ["body"] });
  }
  async _onBulkClearTargetingPackets(e, t) {
    var s, o, l, c;
    if ((s = e == null ? void 0 : e.preventDefault) == null || s.call(e), (o = e == null ? void 0 : e.stopPropagation) == null || o.call(e), !((l = game.user) != null && l.isGM)) return;
    const n = this._captureTargetingStateFromDom(t), a = tr(n.attackerTokenId), r = this._getSelectedTargetingTargetUuids(t);
    if (!a || !r.length) {
      (c = ui.notifications) == null || c.warn("Select a machine attacker and at least one checked target row.");
      return;
    }
    for (const u of r)
      await ec(a, u);
    return this.render({ parts: ["body"] });
  }
  _captureHazardStateFromDom(e = null) {
    var r;
    const t = ((r = e == null ? void 0 : e.closest) == null ? void 0 : r.call(e, ".mwd-gmgadget__root")) ?? this._getRootElement();
    if (!(t instanceof HTMLElement)) return this.hazardState;
    const n = (s, o = "") => {
      const l = t.querySelector(s);
      return l instanceof HTMLInputElement || l instanceof HTMLSelectElement || l instanceof HTMLTextAreaElement ? l.value : o;
    }, a = (s, o = !1) => {
      const l = t.querySelector(s);
      return l instanceof HTMLInputElement ? l.checked : o;
    };
    return this.hazardState = il({
      label: n('[name="hazard-label"]', this.hazardState.label),
      startExposure: n('[name="hazard-startExposure"]', this.hazardState.startExposure),
      escalationRate: Number(n('[name="hazard-escalationRate"]', this.hazardState.escalationRate)),
      escalationIntervalTurns: Number(n('[name="hazard-escalationIntervalTurns"]', this.hazardState.escalationIntervalTurns)),
      escalationMax: n('[name="hazard-escalationMax"]', this.hazardState.escalationMax),
      onFullBurnDelta: Number(n('[name="hazard-onFullBurnDelta"]', this.hazardState.onFullBurnDelta)),
      clearOnExit: a('[name="hazard-clearOnExit"]', this.hazardState.clearOnExit),
      damage: Number(n('[name="hazard-damage"]', this.hazardState.damage)),
      ap: Number(n('[name="hazard-ap"]', this.hazardState.ap)),
      damageType: n('[name="hazard-damageType"]', this.hazardState.damageType),
      color: n('[name="hazard-color"]', this.hazardState.color)
    }), this.hazardState;
  }
  async _onHazardInputChange(e, t) {
    var n;
    (n = e == null ? void 0 : e.preventDefault) == null || n.call(e), this._captureHazardStateFromDom(t);
  }
  async _onRefreshHazardTemplate(e, t) {
    var n, a;
    return (n = e == null ? void 0 : e.preventDefault) == null || n.call(e), (a = e == null ? void 0 : e.stopPropagation) == null || a.call(e), this._captureHazardStateFromDom(t), this.render({ parts: ["body"] });
  }
  async _onCreateHazard(e, t) {
    var c, u, d, m, f, p, g, h;
    if ((c = e == null ? void 0 : e.preventDefault) == null || c.call(e), (u = e == null ? void 0 : e.stopPropagation) == null || u.call(e), !((d = game.user) != null && d.isGM)) return;
    const n = this._captureHazardStateFromDom(t), a = um(), r = hf(a);
    if (!(canvas != null && canvas.scene) || !r) {
      (m = ui.notifications) == null || m.warn("Select a supported Region in Measured Template Mode before creating a hazard.");
      return;
    }
    const s = Gc({
      startExposure: n.startExposure,
      escalation: {
        rate: Number(n.escalationRate ?? 1) || 1,
        intervalTurns: Number(n.escalationIntervalTurns ?? 1) || 1,
        max: n.escalationMax
      },
      onFull: {
        burnDelta: Number(n.onFullBurnDelta ?? 0) || 0
      },
      clearOnExit: !!n.clearOnExit
    }), o = ao(r);
    if (!o.length) {
      (f = ui.notifications) == null || f.warn("Unable to convert the selected region into a hazard shape.");
      return;
    }
    const [l] = await canvas.scene.createEmbeddedDocuments("Region", [{
      name: String(n.label ?? "Hazard Zone").trim() || "Hazard Zone",
      color: String(n.color ?? "#d86a2c").trim() || "#d86a2c",
      shapes: o,
      flags: {
        mwd: {
          [Qn]: {
            sourceActorUuid: null,
            sourceItemUuid: null,
            payloadId: "gm-hazard",
            templateGeometry: ji(r),
            damage: Math.max(0, Number(n.damage ?? 0) || 0),
            ap: Math.max(0, Number(n.ap ?? 0) || 0),
            damageType: String(n.damageType ?? "thermal").trim() || "thermal",
            label: `${String(n.label ?? "Hazard Zone").trim() || "Hazard Zone"} (${bi(s.startExposure)})`,
            areaEffect: {
              kind: "persistent",
              hazard: s
            },
            hazardDef: s
          }
        }
      }
    }]);
    return (g = (p = l == null ? void 0 : l.sheet) == null ? void 0 : p.render) == null || g.call(p, !0), (h = ui.notifications) == null || h.info("Hazard region created from the selected region."), this.render({ parts: ["body"] });
  }
  _buildHarmPayload(e) {
    const t = String((e == null ? void 0 : e.source) ?? "").trim(), n = String((e == null ? void 0 : e.notes) ?? "").trim(), a = String((e == null ? void 0 : e.mode) ?? "").trim();
    if (a === "status") {
      const r = String((e == null ? void 0 : e.statusId) ?? "").trim();
      return r ? {
        mode: "status",
        statusId: r,
        active: !!(e != null && e.statusActive),
        source: t,
        notes: n
      } : null;
    }
    return a === "burn" ? {
      mode: "burnDelta",
      delta: fm(e == null ? void 0 : e.delta, dr.delta),
      source: t,
      notes: n
    } : a === "physical" || a === "fatigue" ? {
      mode: "trackDelta",
      track: a,
      delta: fm(e == null ? void 0 : e.delta, dr.delta),
      useArmor: !!(e != null && e.useArmor),
      damageType: (e == null ? void 0 : e.damageType) ?? dr.damageType,
      source: t,
      notes: n
    } : null;
  }
  // ---- Scene modifier actions ----
  async _onAddSceneModifierFromPreset(e, t) {
    var l, c, u, d;
    if ((l = e == null ? void 0 : e.preventDefault) == null || l.call(e), (c = e == null ? void 0 : e.stopPropagation) == null || c.call(e), !((u = game.user) != null && u.isGM)) return;
    const n = ((d = t == null ? void 0 : t.closest) == null ? void 0 : d.call(t, ".mwd-gmgadget__root")) ?? this._getRootElement(), a = n instanceof HTMLElement ? n.querySelector('select[name="scene-preset-index"]') : null, r = a instanceof HTMLSelectElement ? Number(a.value) : NaN, s = dm(
      game.settings.get(this.systemId, ac)
    ), o = Number.isFinite(r) ? s[r] : null;
    o && await this._mutateSceneModifiers((m) => [
      ...m,
      {
        id: foundry.utils.randomID(),
        label: o.label,
        value: o.value,
        enabled: !0,
        attributeFilter: o.attributeFilter || null,
        intentFilter: o.intentFilter || null,
        source: "preset"
      }
    ]);
  }
  async _onAddSceneModifierAdhoc(e, t) {
    var a, r, s, o;
    if ((a = e == null ? void 0 : e.preventDefault) == null || a.call(e), (r = e == null ? void 0 : e.stopPropagation) == null || r.call(e), !((s = game.user) != null && s.isGM)) return;
    const n = this._captureAdhocFormFromDom(t);
    if (!n) {
      (o = ui.notifications) == null || o.warn("Label and a numeric value are required.");
      return;
    }
    await this._mutateSceneModifiers((l) => [...l, n]);
  }
  async _onToggleSceneModifier(e, t) {
    var a, r, s, o;
    if ((a = e == null ? void 0 : e.preventDefault) == null || a.call(e), (r = e == null ? void 0 : e.stopPropagation) == null || r.call(e), !((s = game.user) != null && s.isGM)) return;
    const n = String(((o = t == null ? void 0 : t.dataset) == null ? void 0 : o.modId) ?? "").trim();
    n && await this._mutateSceneModifiers(
      (l) => l.map((c) => c.id === n ? { ...c, enabled: !c.enabled } : c)
    );
  }
  async _onRemoveSceneModifier(e, t) {
    var a, r, s, o;
    if ((a = e == null ? void 0 : e.preventDefault) == null || a.call(e), (r = e == null ? void 0 : e.stopPropagation) == null || r.call(e), !((s = game.user) != null && s.isGM)) return;
    const n = String(((o = t == null ? void 0 : t.dataset) == null ? void 0 : o.modId) ?? "").trim();
    n && await this._mutateSceneModifiers((l) => l.filter((c) => c.id !== n));
  }
  async _onClearSceneModifiers(e, t) {
    var n, a, r;
    (n = e == null ? void 0 : e.preventDefault) == null || n.call(e), (a = e == null ? void 0 : e.stopPropagation) == null || a.call(e), (r = game.user) != null && r.isGM && await this._mutateSceneModifiers(() => []);
  }
  async _mutateSceneModifiers(e) {
    const t = canvas == null ? void 0 : canvas.scene;
    if (!t) return;
    const n = mm(t.getFlag("mwd", As)), a = await e(n);
    return await t.setFlag("mwd", As, a), this.render({ parts: ["body"] });
  }
  _captureAdhocFormFromDom(e) {
    var c;
    const t = ((c = e == null ? void 0 : e.closest) == null ? void 0 : c.call(e, ".mwd-gmgadget__root")) ?? this._getRootElement();
    if (!(t instanceof HTMLElement)) return null;
    const n = (u) => {
      const d = t.querySelector(u);
      return d instanceof HTMLInputElement || d instanceof HTMLSelectElement ? d.value : "";
    }, a = n('[name="scene-adhoc-label"]').trim(), r = n('[name="scene-adhoc-value"]').trim(), s = n('[name="scene-adhoc-attributeFilter"]').trim() || null, o = n('[name="scene-adhoc-intentFilter"]').trim() || null;
    if (!a) return null;
    const l = Number(r);
    return Number.isFinite(l) ? {
      id: foundry.utils.randomID(),
      label: a,
      value: Math.trunc(l),
      enabled: !0,
      attributeFilter: s,
      intentFilter: o,
      source: "adhoc"
    } : null;
  }
};
z(qe, "DEFAULT_OPTIONS", {
  id: Xk,
  classes: ["mwd-gmgadget"],
  window: {
    title: "MWD GM Gadget",
    popOut: !0,
    resizable: !0
  },
  position: {
    width: 440,
    height: 620
  },
  actions: {
    switchTab: qe.prototype._onSwitchTab,
    setDn: qe.prototype._onSetDn,
    clearDn: qe.prototype._onClearDn,
    toggleAnnounce: qe.prototype._onToggleAnnounce,
    harmInputChange: qe.prototype._onHarmInputChange,
    refreshHarmTarget: qe.prototype._onRefreshHarmTarget,
    applyHarm: qe.prototype._onApplyHarm,
    targetingInputChange: qe.prototype._onTargetingInputChange,
    refreshTargeting: qe.prototype._onRefreshTargeting,
    setTargetingState: qe.prototype._onSetTargetingState,
    clearTargetingPacket: qe.prototype._onClearTargetingPacket,
    bulkSetTargetingState: qe.prototype._onBulkSetTargetingState,
    bulkClearTargetingPackets: qe.prototype._onBulkClearTargetingPackets,
    hazardInputChange: qe.prototype._onHazardInputChange,
    refreshHazardTemplate: qe.prototype._onRefreshHazardTemplate,
    createHazard: qe.prototype._onCreateHazard,
    addSceneModifierFromPreset: qe.prototype._onAddSceneModifierFromPreset,
    addSceneModifierAdhoc: qe.prototype._onAddSceneModifierAdhoc,
    toggleSceneModifier: qe.prototype._onToggleSceneModifier,
    removeSceneModifier: qe.prototype._onRemoveSceneModifier,
    clearSceneModifiers: qe.prototype._onClearSceneModifiers
  }
}), z(qe, "PARTS", {
  body: { template: eM }
});
let rc = qe;
function dm(i) {
  return Array.isArray(i) ? i.filter((e) => (e == null ? void 0 : e.label) && Number.isFinite(Number(e == null ? void 0 : e.value))).map((e, t) => {
    const n = Math.trunc(Number(e.value));
    return {
      index: t,
      label: String(e.label).trim(),
      value: n,
      signedValue: n >= 0 ? `+${n}` : String(n),
      attributeFilter: String(e.attributeFilter ?? "").trim() || null,
      intentFilter: String(e.intentFilter ?? "").trim() || null
    };
  }) : [];
}
function mm(i) {
  return Array.isArray(i) ? i.map((e) => {
    var r, s;
    const t = Wg(e), n = ((r = xu.find((o) => o.value === (t.attributeFilter ?? ""))) == null ? void 0 : r.label) ?? null, a = ((s = $u.find((o) => o.value === (t.intentFilter ?? ""))) == null ? void 0 : s.label) ?? null;
    return {
      ...t,
      attributeFilterLabel: t.attributeFilter ? n : null,
      intentFilterLabel: t.intentFilter ? a : null,
      signedValue: t.value >= 0 ? `+${t.value}` : String(t.value),
      isPositive: t.value > 0
    };
  }) : [];
}
function fm(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? Math.trunc(t) : e;
}
let al = null;
function pM({ systemId: i = "mwd" } = {}) {
  return al || (al = new rc({ systemId: i })), al;
}
const gM = "gmDnPresetEditor";
function hM(i = []) {
  const e = [], t = [], n = /* @__PURE__ */ new Set();
  if ((Array.isArray(i) ? i : []).forEach((a, r) => {
    const s = String((a == null ? void 0 : a.label) ?? "").trim(), o = String((a == null ? void 0 : a.dn) ?? "").trim(), l = `Row ${r + 1}`;
    if (!s) {
      t.push(`${l}: label cannot be blank.`);
      return;
    }
    if (n.has(s.toLowerCase())) {
      t.push(`${l}: duplicate label "${s}".`);
      return;
    }
    n.add(s.toLowerCase());
    const c = Number(o);
    if (!Number.isFinite(c)) {
      t.push(`${l}: DN must be a number.`);
      return;
    }
    if (c < 0) {
      t.push(`${l}: DN cannot be negative.`);
      return;
    }
    e.push({
      label: s,
      dn: Math.trunc(c)
    });
  }), t.length) throw di(t);
  return Wr(e, { strict: !0 });
}
function yM(i = []) {
  return Wr(i, { strict: !1 }).map((e) => ({
    label: e.label,
    dn: String(e.dn)
  }));
}
function bM(i = "") {
  const e = String(i ?? "").trim();
  if (!e) return [];
  let t;
  try {
    t = JSON.parse(e);
  } catch (n) {
    throw di([
      `Bulk JSON must be valid JSON: ${n.message}`
    ]);
  }
  return Wr(t, { strict: !0 });
}
function SM(i = []) {
  return JSON.stringify(
    Wr(i, { strict: !1 }),
    null,
    2
  );
}
const AM = {
  id: "gm-dn-presets",
  menuKey: gM,
  settingKey: Zg,
  settingType: Array,
  title: "GM DN Presets",
  description: "Edit the preset DN buttons shown in the GM Gadget difficulty tab.",
  helpText: "Rows are shown in order in the GM Gadget. Labels must be unique.",
  bulkHelpText: 'JSON shape: [{ "label": "Standard", "dn": 1 }]',
  emptyStateText: "No DN preset rows yet. Add one to show buttons in the GM Gadget.",
  addRowLabel: "Add Preset",
  rowSchema: [
    {
      key: "label",
      label: "Label",
      type: "text",
      placeholder: "Standard"
    },
    {
      key: "dn",
      label: "DN",
      type: "number",
      min: 0,
      step: 1,
      placeholder: "1"
    }
  ],
  menu: {
    name: "GM DN Presets",
    label: "Configure",
    hint: "Edit the preset DN buttons used by the GM Gadget.",
    icon: "fas fa-sliders-h",
    restricted: !0
  },
  defaultData: rM,
  createEmptyRow: () => ({
    label: "",
    dn: "1"
  }),
  toRows: yM,
  rowsToValue: hM,
  parseBulk: bM,
  serializeBulk: SM
};
function wM() {
  Ga(AM);
}
const TM = "lifeModuleCatalogEditor";
function kM(i = []) {
  return ma((Array.isArray(i) ? i : []).map((e) => ({
    id: String((e == null ? void 0 : e.id) ?? ""),
    moduleType: String((e == null ? void 0 : e.moduleType) ?? ""),
    label: String((e == null ? void 0 : e.label) ?? ""),
    grants: String((e == null ? void 0 : e.grants) ?? ""),
    requiresAny: String((e == null ? void 0 : e.requiresAny) ?? ""),
    excludesAny: String((e == null ? void 0 : e.excludesAny) ?? "")
  })), { strict: !0 });
}
function MM(i = []) {
  return ma(i, { strict: !1 }).map((e) => ({
    id: e.id,
    moduleType: e.moduleType,
    label: e.label,
    grants: HT(e.grants),
    requiresAny: e.requiresAny.join(", "),
    excludesAny: e.excludesAny.join(", ")
  }));
}
function CM(i = "") {
  const e = String(i ?? "").trim();
  if (!e) return [];
  let t;
  try {
    t = JSON.parse(e);
  } catch (n) {
    const a = new Error(`Bulk JSON must be valid JSON: ${n.message}`);
    throw a.validationErrors = [a.message], a;
  }
  if (!Array.isArray(t)) {
    const n = new Error("Bulk JSON must be an array.");
    throw n.validationErrors = [n.message], n;
  }
  return ma(t, { strict: !0 });
}
function vM(i = []) {
  return JSON.stringify(
    ma(i, { strict: !1 }),
    null,
    2
  );
}
const EM = {
  id: "life-module-catalog",
  menuKey: TM,
  settingKey: Na,
  settingType: Array,
  title: "Life Module Catalog",
  description: "Define the canonical life modules available for Faction, Childhood, Higher Education, and Real Life character development.",
  helpText: 'Grants use ";" to separate separate bonuses and "|" to separate choices inside one bonus. Each choice is prefixed with "skill:" or "edgePool:". Example: "skill:disguise; edgePool:rumor" or "skill:tracking|skill:projectileWeapons".',
  bulkHelpText: 'JSON shape: [{ "id": "faction-capellan-confederation", "moduleType": "faction", "label": "Capellan Confederation", "grants": [{ "id": "skill", "choices": [{ "type": "skill", "value": "disguise" }] }, { "id": "edge-pool", "choices": [{ "type": "edgePool", "value": "rumor" }] }], "requiresAny": [], "excludesAny": [] }]',
  emptyStateText: "No life modules yet. Add one to start the catalog.",
  addRowLabel: "Add Life Module",
  rowSchema: [
    {
      key: "id",
      label: "Id",
      type: "text",
      placeholder: "childhood-backwoods"
    },
    {
      key: "moduleType",
      label: "Slot",
      type: "select",
      options: hg
    },
    {
      key: "label",
      label: "Label",
      type: "text",
      placeholder: "Backwoods"
    },
    {
      key: "grants",
      label: "Grants",
      type: "text",
      placeholder: "skill:tracking|skill:projectileWeapons"
    },
    {
      key: "requiresAny",
      label: "Requires Any",
      type: "text",
      placeholder: "childhood-nobility"
    },
    {
      key: "excludesAny",
      label: "Excludes Any",
      type: "text",
      placeholder: "higher-education-military-academy"
    }
  ],
  menu: {
    name: "Life Module Catalog",
    label: "Configure",
    hint: "Edit the canonical life modules and their skill roll modifiers.",
    icon: "fas fa-book-open",
    restricted: !0
  },
  defaultData: Iu,
  createEmptyRow: () => ({
    id: "",
    moduleType: "childhood",
    label: "",
    grants: "",
    requiresAny: "",
    excludesAny: ""
  }),
  toRows: MM,
  rowsToValue: kM,
  parseBulk: CM,
  serializeBulk: vM
};
function IM() {
  Ga(EM);
}
const NM = "personalActionCatalogEditor", pm = Object.freeze([
  { value: "false", label: "No" },
  { value: "true", label: "Yes" }
]);
function RM(i = []) {
  try {
    return jr((Array.isArray(i) ? i : []).map((e) => ({
      id: String((e == null ? void 0 : e.id) ?? ""),
      label: String((e == null ? void 0 : e.label) ?? ""),
      category: String((e == null ? void 0 : e.category) ?? ""),
      cost: String((e == null ? void 0 : e.cost) ?? "0"),
      handler: String((e == null ? void 0 : e.handler) ?? ""),
      reason: String((e == null ? void 0 : e.reason) ?? ""),
      rollIntent: String((e == null ? void 0 : e.rollIntent) ?? ""),
      prominent: String((e == null ? void 0 : e.prominent) ?? "false"),
      prominentWhenBurning: String((e == null ? void 0 : e.prominentWhenBurning) ?? "false")
    })), { strict: !0 });
  } catch (e) {
    throw di(
      Array.isArray(e.validationErrors) ? e.validationErrors : [e.message]
    );
  }
}
function PM(i = []) {
  return jr(i, { strict: !1 }).map((e) => {
    var t;
    return {
      id: String(e.id ?? ""),
      label: String(e.label ?? ""),
      category: String(e.category ?? ""),
      cost: String(e.cost ?? "0"),
      handler: String(e.handler ?? ""),
      reason: String(e.reason ?? ""),
      rollIntent: String(((t = e.roll) == null ? void 0 : t.intent) ?? ""),
      prominent: e.prominent ? "true" : "false",
      prominentWhenBurning: e.prominentWhenBurning ? "true" : "false"
    };
  });
}
function DM(i = "") {
  const e = String(i ?? "").trim();
  if (!e) return [];
  let t;
  try {
    t = JSON.parse(e);
  } catch (n) {
    throw di([
      `Bulk JSON must be valid JSON: ${n.message}`
    ]);
  }
  try {
    return jr(t, { strict: !0 });
  } catch (n) {
    throw di(
      Array.isArray(n.validationErrors) ? n.validationErrors : [n.message]
    );
  }
}
function OM(i = []) {
  return JSON.stringify(
    jr(i, { strict: !1 }),
    null,
    2
  );
}
const LM = {
  id: "personal-action-catalog",
  menuKey: NM,
  settingKey: pp,
  settingType: Array,
  title: "Personal Action Catalog",
  description: "Edit the action buttons shown in the personal combat action menu.",
  helpText: "Rows are shown in menu order within their category. Handler controls what the button does; leave it as a placeholder for actions whose mechanics are not implemented yet.",
  bulkHelpText: 'JSON shape: [{ "id": "move", "label": "Move", "category": "standard", "cost": 1, "handler": "combatAction" }]',
  emptyStateText: "No actions configured. Restore defaults to rebuild the standard action catalog.",
  addRowLabel: "Add Action",
  rowSchema: [
    {
      key: "id",
      label: "Id",
      type: "text",
      placeholder: "move"
    },
    {
      key: "label",
      label: "Label",
      type: "text",
      placeholder: "Move"
    },
    {
      key: "category",
      label: "Category",
      type: "select",
      options: () => _l
    },
    {
      key: "cost",
      label: "Cost",
      type: "number",
      min: 0,
      step: 1,
      placeholder: "1"
    },
    {
      key: "handler",
      label: "Handler",
      type: "select",
      options: () => gp
    },
    {
      key: "reason",
      label: "Disabled Reason",
      type: "text",
      placeholder: "Not yet implemented."
    },
    {
      key: "rollIntent",
      label: "Roll Intent",
      type: "text",
      placeholder: "overload"
    },
    {
      key: "prominent",
      label: "Prominent",
      type: "select",
      options: () => pm
    },
    {
      key: "prominentWhenBurning",
      label: "Burn Highlight",
      type: "select",
      options: () => pm
    }
  ],
  menu: {
    name: "Personal Action Catalog",
    label: "Configure",
    hint: "Edit the personal combat action menus and first-pass action handlers.",
    icon: "fas fa-list-check",
    restricted: !0
  },
  defaultData: lu,
  createEmptyRow: () => {
    var i;
    return {
      id: "",
      label: "",
      category: ((i = _l[0]) == null ? void 0 : i.value) ?? "standard",
      cost: "1",
      handler: "combatAction",
      reason: "",
      rollIntent: "",
      prominent: "false",
      prominentWhenBurning: "false"
    };
  },
  toRows: PM,
  rowsToValue: RM,
  parseBulk: DM,
  serializeBulk: OM
};
function _M() {
  Ga(LM);
}
const xM = "skillSpecializationEditor";
function sc() {
  return _s().map((i) => ({
    value: i.code,
    label: i.label
  }));
}
function $M(i = []) {
  const e = new Set(sc().map((a) => a.value)), t = {}, n = [];
  if ((Array.isArray(i) ? i : []).forEach((a, r) => {
    const s = String((a == null ? void 0 : a.skillCode) ?? "").trim(), o = String((a == null ? void 0 : a.label) ?? "").trim(), l = `Row ${r + 1}`;
    if (!s) {
      n.push(`${l}: choose a skill.`);
      return;
    }
    if (!e.has(s)) {
      n.push(`${l}: unknown skill code "${s}".`);
      return;
    }
    if (!o) {
      n.push(`${l}: specialization label cannot be blank.`);
      return;
    }
    (t[s] ?? (t[s] = [])).push(o);
  }), n.length) throw di(n);
  return lo(t, { strict: !0 });
}
function BM(i = {}) {
  const e = lo(i, { strict: !1 });
  return Object.entries(e).flatMap(
    ([t, n]) => n.map((a) => ({ skillCode: t, label: a }))
  );
}
function zM(i = "") {
  const e = String(i ?? "").trim();
  if (!e) return {};
  let t;
  try {
    t = JSON.parse(e);
  } catch (n) {
    throw di([
      `Bulk JSON must be valid JSON: ${n.message}`
    ]);
  }
  return lo(t, { strict: !0 });
}
function FM(i = {}) {
  return JSON.stringify(
    lo(i, { strict: !1 }),
    null,
    2
  );
}
const HM = {
  id: "skill-specializations",
  menuKey: xM,
  settingKey: Pl,
  title: "Skill Specializations",
  description: "Edit the world specialization catalog for existing skills. Omitted skills have no available specializations.",
  helpText: "Use rows for normal editing. Use the bulk JSON tab for fast import/export.",
  bulkHelpText: 'JSON shape: { "athletics": ["Running", "Jumping"] }',
  emptyStateText: "No specialization rows yet. Add one to start the catalog.",
  addRowLabel: "Add Specialization",
  rowSchema: [
    {
      key: "skillCode",
      label: "Skill",
      type: "select",
      options: sc
    },
    {
      key: "label",
      label: "Specialization",
      type: "text",
      placeholder: "Running"
    }
  ],
  menu: {
    name: "Skill Specializations",
    label: "Configure",
    hint: "Edit the specialization catalog for canonical skills.",
    icon: "fas fa-list",
    restricted: !0
  },
  defaultData: zf,
  createEmptyRow: () => {
    var i;
    return {
      skillCode: ((i = sc()[0]) == null ? void 0 : i.value) ?? "",
      label: ""
    };
  },
  toRows: BM,
  rowsToValue: $M,
  parseBulk: zM,
  serializeBulk: FM
};
function UM() {
  Ga(HM);
}
const jM = "statusConditionCatalogEditor";
function KM(i = []) {
  try {
    return ia((Array.isArray(i) ? i : []).map((e) => ({
      id: String((e == null ? void 0 : e.id) ?? ""),
      label: String((e == null ? void 0 : e.label) ?? ""),
      actorGroup: String((e == null ? void 0 : e.actorGroup) ?? ""),
      category: String((e == null ? void 0 : e.category) ?? ""),
      tags: String((e == null ? void 0 : e.tags) ?? ""),
      icon: String((e == null ? void 0 : e.icon) ?? ""),
      manual: String((e == null ? void 0 : e.manual) ?? "true"),
      managed: String((e == null ? void 0 : e.managed) ?? "false"),
      modifierKey: String((e == null ? void 0 : e.modifierKey) ?? ""),
      order: String((e == null ? void 0 : e.order) ?? "0")
    })), { strict: !0 });
  } catch (e) {
    throw di(
      Array.isArray(e.validationErrors) ? e.validationErrors : [e.message]
    );
  }
}
function GM(i = []) {
  return ia(i, { strict: !1 }).map((e) => ({
    id: String(e.id ?? ""),
    label: String(e.label ?? ""),
    actorGroup: String(e.actorGroup ?? "person"),
    category: String(e.category ?? ""),
    tags: Wb(e.tags ?? []),
    icon: String(e.icon ?? ""),
    manual: e.manual ? "true" : "false",
    managed: e.managed ? "true" : "false",
    modifierKey: String(e.modifierKey ?? ""),
    order: String(e.order ?? "0")
  }));
}
function WM(i = "") {
  const e = String(i ?? "").trim();
  if (!e) return [];
  let t;
  try {
    t = JSON.parse(e);
  } catch (n) {
    throw di([
      `Bulk JSON must be valid JSON: ${n.message}`
    ]);
  }
  try {
    return ia(t, { strict: !0 });
  } catch (n) {
    throw di(
      Array.isArray(n.validationErrors) ? n.validationErrors : [n.message]
    );
  }
}
function qM(i = []) {
  return JSON.stringify(
    ia(i, { strict: !1 }),
    null,
    2
  );
}
const VM = {
  id: "status-condition-catalog",
  menuKey: jM,
  settingKey: Uf,
  settingType: Array,
  title: "Status Condition Catalog",
  description: "Edit the actor-aware statuses available in MWD status pickers and GM Harm.",
  helpText: "Actor group controls where a condition can be applied. Modifier Key is optional and only links to existing mechanics-backed status modifiers.",
  bulkHelpText: 'JSON shape: [{ "id": "unstable", "label": "Unstable", "actorGroup": "machine", "category": "stability", "tags": ["movement"] }]',
  emptyStateText: "No condition rows configured. Restore defaults to rebuild the standard MWD status catalog.",
  addRowLabel: "Add Condition",
  rowSchema: [
    { key: "id", label: "Id", type: "text", placeholder: "unstable" },
    { key: "label", label: "Label", type: "text", placeholder: "Unstable" },
    { key: "actorGroup", label: "Actor Group", type: "select", options: () => Hb },
    { key: "category", label: "Category", type: "text", placeholder: "stability" },
    { key: "tags", label: "Tags", type: "text", placeholder: "movement, piloting" },
    { key: "icon", label: "Icon", type: "text", placeholder: "systems/mwd/img/icons/status/falling.svg" },
    { key: "manual", label: "Manual", type: "select", options: () => cd },
    { key: "managed", label: "Managed", type: "select", options: () => cd },
    { key: "modifierKey", label: "Modifier Key", type: "text", placeholder: "prone" },
    { key: "order", label: "Order", type: "number", step: 1, placeholder: "1000" }
  ],
  menu: {
    name: "Status Condition Catalog",
    label: "Configure",
    hint: "Edit person, vehicle, and BattleMech status condition availability.",
    icon: "fas fa-heart-pulse",
    restricted: !0
  },
  defaultData: Wf,
  createEmptyRow: () => ({
    id: "",
    label: "",
    actorGroup: "machine",
    category: "general",
    tags: "",
    icon: "",
    manual: "true",
    managed: "false",
    modifierKey: "",
    order: "0"
  }),
  toRows: GM,
  rowsToValue: KM,
  parseBulk: WM,
  serializeBulk: qM
};
function YM() {
  Ga(VM);
}
class JM {
  static register() {
    wM(), IM(), _M(), UM(), Yk(), YM(), game.settings.register(M, "useDestinyMechanics", {
      name: N.settings.useDestinyMechanics.name,
      hint: N.settings.useDestinyMechanics.hint,
      scope: "world",
      config: !0,
      type: Boolean,
      default: !0
    }), game.settings.register(M, "enableGMGadget", {
      name: "Enable GM Gadget",
      hint: "If enabled, renders the new AppV2 GM Gadget UI on startup.",
      scope: "world",
      config: !0,
      type: Boolean,
      default: !0
    }), game.settings.register(M, rf, {
      name: "Enable BattleMech Token Heat FX",
      hint: "Show BattleMech token glow, shimmer, and subtle blur based on resolved heat.",
      scope: "client",
      config: !0,
      type: Boolean,
      default: !0
    }), game.settings.register(M, $p, {
      name: "Machine Critical Table: General",
      hint: "2d6 RollTable UUID that chooses the general type of machine critical problem.",
      scope: "world",
      config: !0,
      type: String,
      default: Rt.general
    }), game.settings.register(M, Bp, {
      name: "Machine Critical Table: BattleMech Head",
      hint: "Location interpretation table for BattleMech head criticals.",
      scope: "world",
      config: !0,
      type: String,
      default: Rt.mechHead
    }), game.settings.register(M, zp, {
      name: "Machine Critical Table: BattleMech Torso",
      hint: "Location interpretation table for BattleMech torso and forced critical hits.",
      scope: "world",
      config: !0,
      type: String,
      default: Rt.mechTorso
    }), game.settings.register(M, Fp, {
      name: "Machine Critical Table: BattleMech Arms",
      hint: "Location interpretation table for BattleMech arm criticals.",
      scope: "world",
      config: !0,
      type: String,
      default: Rt.mechArms
    }), game.settings.register(M, Hp, {
      name: "Machine Critical Table: BattleMech Legs",
      hint: "Location interpretation table for BattleMech leg criticals.",
      scope: "world",
      config: !0,
      type: String,
      default: Rt.mechLegs
    }), game.settings.register(M, Up, {
      name: "Machine Critical Table: Vehicle Body",
      hint: "Location interpretation table for vehicle body criticals.",
      scope: "world",
      config: !0,
      type: String,
      default: Rt.vehicleBody
    }), game.settings.register(M, jp, {
      name: "Machine Critical Table: Vehicle Turret",
      hint: "Location interpretation table for vehicle turret and weapon criticals.",
      scope: "world",
      config: !0,
      type: String,
      default: Rt.vehicleTurret
    }), game.settings.register(M, Kp, {
      name: "Machine Critical Table: Vehicle Mobility",
      hint: "Location interpretation table for vehicle mobility criticals.",
      scope: "world",
      config: !0,
      type: String,
      default: Rt.vehicleMobility
    });
  }
  static getSystemProperty(e, t) {
    return game.settings.get(M, e) ?? t;
  }
}
class QM extends Error {
  constructor(e, { severity: t = "error" } = {}) {
    super(e), this.name = "UserFacingRollError", this.userFacing = !0, this.severity = t === "warn" ? "warn" : "error";
  }
}
function rt(i, e = {}) {
  return new QM(i, e);
}
function sn(i, e = "Unable to execute roll.") {
  var n, a;
  const t = i != null && i.userFacing && (i == null ? void 0 : i.severity) === "warn" ? "warn" : "error";
  (a = (n = ui.notifications) == null ? void 0 : n[t]) == null || a.call(n, (i == null ? void 0 : i.message) ?? e);
}
const Vs = Symbol("SKIP_FIELD");
function XM(i) {
  return typeof i == "number" ? i : String(i ?? "").replace(/[,_\s]/g, "");
}
function nh(i) {
  return i instanceof HTMLInputElement || i instanceof HTMLSelectElement || i instanceof HTMLTextAreaElement;
}
function ZM({
  elementKind: i = "input",
  inputType: e = "",
  dtype: t = "",
  value: n = "",
  checked: a = !1
} = {}) {
  const r = String(i ?? "").trim().toLowerCase(), s = String(e ?? "").trim().toLowerCase(), o = String(t ?? "").trim().toLowerCase();
  if (!["input", "select", "textarea"].includes(r))
    return Vs;
  if (r === "input") {
    if (s === "radio")
      return a ? n : Vs;
    if (s === "checkbox")
      return !!a;
  }
  if (o === "number" || r === "input" && s === "number") {
    const l = Number(XM(n));
    return Number.isFinite(l) ? l : 0;
  }
  return o === "boolean" ? n === !0 || n === "true" : n;
}
function eC(i) {
  var e;
  return nh(i) ? ZM({
    elementKind: i instanceof HTMLSelectElement ? "select" : i instanceof HTMLTextAreaElement ? "textarea" : "input",
    inputType: i instanceof HTMLInputElement ? i.type : "",
    dtype: String(((e = i.dataset) == null ? void 0 : e.dtype) ?? ""),
    value: i.value,
    checked: i instanceof HTMLInputElement ? i.checked : !1
  }) : Vs;
}
function tC({
  root: i,
  document: e,
  selector: t = "input[name], select[name], textarea[name]",
  clampByPath: n = null,
  skipNames: a = []
} = {}) {
  if (!(i instanceof HTMLElement)) return {};
  const r = new Set(Array.isArray(a) ? a : [a]), s = {};
  for (const o of i.querySelectorAll(t)) {
    if (!nh(o) || o.closest("prose-mirror") || o.disabled) continue;
    const l = String(o.getAttribute("name") ?? o.name ?? "").trim();
    if (!l || r.has(l)) continue;
    let c = eC(o);
    c === Vs || (typeof n == "function" && (c = n(l, c)), (e ? foundry.utils.getProperty(e, l) : void 0) === c) || (s[l] = c);
  }
  return s;
}
const { HandlebarsApplicationMixin: iC } = foundry.applications.api, { HTMLField: nC } = foundry.data.fields;
function aC(i) {
  const e = new nC({ required: !1, blank: !0, initial: "" });
  return e.name = i, e;
}
var ki, zr, An, Wn, Ln, oc, lc;
const bt = class bt extends iC(foundry.applications.sheets.ActorSheetV2) {
  constructor() {
    super(...arguments);
    Ce(this, Ln);
    Ce(this, ki, !1);
    /** Track active CSB tab per group across rerenders */
    Ce(this, zr, /* @__PURE__ */ new Map());
    // group -> tabId
    Ce(this, An, /* @__PURE__ */ new Map());
    // group -> sectionId|null
    Ce(this, Wn, null);
  }
  /** @override */
  _updatePosition(t) {
    const n = super._updatePosition(t), {
      MIN_WIDTH: a,
      MAX_WIDTH: r,
      MIN_HEIGHT: s,
      MAX_HEIGHT: o
    } = this.constructor;
    return typeof n.width == "number" && (n.width = Math.min(
      r,
      Math.max(a, n.width)
    )), typeof n.height == "number" && (n.height = Math.min(
      o,
      Math.max(s, n.height)
    )), n;
  }
  // Legacy callers still probe defaultOptions directly, so keep the alias until
  // the remaining compatibility surfaces are gone.
  static get defaultOptions() {
    return this.DEFAULT_OPTIONS;
  }
  /** Editing mode flag for templates */
  get editing() {
    return Y(this, ki);
  }
  toggleEditing() {
    if (this.isEditable) {
      if (Y(this, ki)) {
        this._commitEditsToActor().finally(() => {
          Ie(this, ki, !Y(this, ki)), this.render({ force: !0 });
        });
        return;
      }
      Ie(this, ki, !Y(this, ki)), this.render({ force: !0 });
    }
  }
  /** Get the root HTMLElement for this application */
  _getRootElement() {
    var t;
    return this.element instanceof HTMLElement ? this.element : (t = this.element) == null ? void 0 : t[0];
  }
  _getPrimaryScroller() {
    const t = this._getRootElement();
    return t ? t.querySelector(".mwd-scroll-area") ?? t.querySelector(".csb-tab-panels") ?? t.querySelector(".window-content") : null;
  }
  _captureScrollPosition() {
    const t = this._getPrimaryScroller();
    if (!(t instanceof HTMLElement)) {
      Ie(this, Wn, null);
      return;
    }
    Ie(this, Wn, {
      top: t.scrollTop,
      left: t.scrollLeft
    });
  }
  _restoreScrollPosition() {
    const t = Y(this, Wn);
    if (!t) return;
    const n = () => {
      const a = this._getPrimaryScroller();
      a instanceof HTMLElement && (a.scrollTop = t.top, a.scrollLeft = t.left);
    };
    n(), requestAnimationFrame(n), Ie(this, Wn, null);
  }
  /**
   * Resolve the TokenDocument that launched this sheet when one exists.
   * This keeps linked-token behavior aligned with Foundry's token API.
   */
  getSheetTokenDocument() {
    var r, s;
    const t = this.actor ?? this.document ?? null, n = (t == null ? void 0 : t.token) ?? null, a = (r = this.document) != null && r.isToken ? ((s = this.document) == null ? void 0 : s.token) ?? n ?? null : n;
    return a ? (a == null ? void 0 : a.document) ?? a : null;
  }
  /**
   * Resolve the document that should persist actor-backed state.
   * Linked token sheets should write to the base actor document so state survives scene changes.
   */
  getPersistentActor() {
    var a, r, s;
    const t = this.actor ?? this.document ?? null;
    if (!t) return null;
    const n = this.getSheetTokenDocument();
    return n != null && n.isLinked ? n.baseActor ?? ((s = (a = game.actors) == null ? void 0 : a.get) == null ? void 0 : s.call(a, ((r = n == null ? void 0 : n.baseActor) == null ? void 0 : r.id) ?? "")) ?? n.actor ?? t : t;
  }
  /** @override */
  _initializeApplicationOptions(t) {
    var o, l, c, u, d;
    t = super._initializeApplicationOptions(t), t.classes = Array.from(t.classes ?? []);
    const n = (t == null ? void 0 : t.document) ?? this.document, a = (n == null ? void 0 : n.type) ?? ((o = this.actor) == null ? void 0 : o.type);
    a && t.classes.push(String(a));
    const r = ((d = (u = (c = (l = game.system) == null ? void 0 : l.anarchy) == null ? void 0 : c.styles) == null ? void 0 : u.selectCssClass) == null ? void 0 : d.call(u)) ?? "mwd-theme-default", s = ["mwd-theme-default", "mwd-theme-sra"];
    for (let m = t.classes.length - 1; m >= 0; m--)
      s.includes(t.classes[m]) && t.classes.splice(m, 1);
    return t.classes.push(r), t;
  }
  /**
   * No localization: provide a concrete title so Foundry doesn't show "TYPES.Actor.<type>".
   * @override
   */
  get title() {
    var s, o;
    const t = ((s = this.actor) == null ? void 0 : s.type) ?? "actor", a = {
      character: "Character",
      npc: "NPC",
      battlemech: "BattleMech",
      vehicle: "Vehicle",
      device: "Device"
    }[t] ?? String(t).replace(/(^|[-_])([a-z])/g, (l, c, u) => (c ? " " : "") + u.toUpperCase());
    return `${((o = this.actor) == null ? void 0 : o.name) ?? "Actor"} — ${a}`;
  }
  /**
   * Window actions (header/menu). Centralize here and dedupe by action key.
   * @override
   */
  _getHeaderControls() {
    var s, o;
    let t = ((s = super._getHeaderControls) == null ? void 0 : s.call(this)) ?? [];
    const n = ((o = this.document) == null ? void 0 : o.isToken) ?? !1, a = /* @__PURE__ */ new Set();
    n ? (a.add("prototypeToken"), a.add("configurePrototypeToken")) : (a.add("token"), a.add("configureToken")), t = t.filter((l) => {
      const c = (l == null ? void 0 : l.action) ?? "", u = String((l == null ? void 0 : l.label) ?? "");
      return !(a.has(c) || n && u.includes("Prototype") || !n && u === "Token");
    });
    const r = /* @__PURE__ */ new Set();
    return t = t.filter((l) => {
      const c = l == null ? void 0 : l.action, u = c ? `a:${c}` : `il:${(l == null ? void 0 : l.icon) ?? ""}|${(l == null ? void 0 : l.label) ?? ""}`;
      return r.has(u) ? !1 : (r.add(u), !0);
    }), t;
  }
  /**
   * AppV2 action handler: Edit/View toggle.
   * Note: actions mapping already routes clicks here; we keep this lean.
   */
  async _onToggleViewMode(t) {
    var n;
    (n = t == null ? void 0 : t.preventDefault) == null || n.call(t), this.toggleEditing();
  }
  /**
   * AppV2 action handler: CSB tab click.
   * Defensive: derive the tab link from target or event.
   */
  _onClickTab(t, n) {
    var l, c, u;
    const a = ((l = n == null ? void 0 : n.closest) == null ? void 0 : l.call(n, ".csb-tab-link[data-tab]")) ?? ((u = (c = t == null ? void 0 : t.target) == null ? void 0 : c.closest) == null ? void 0 : u.call(c, ".csb-tab-link[data-tab]"));
    if (!a) return;
    const r = a.dataset.tab, s = a.closest(".csb-tabs");
    if (!s || !r) return;
    const o = s.dataset.group || "default";
    Y(this, zr).set(o, r), v(this, Ln, oc).call(this, s, r);
  }
  _onClickAccordion(t, n) {
    var u, d, m;
    const a = ((u = n == null ? void 0 : n.closest) == null ? void 0 : u.call(n, ".csb-accordion__trigger[data-section]")) ?? ((m = (d = t == null ? void 0 : t.target) == null ? void 0 : d.closest) == null ? void 0 : m.call(d, ".csb-accordion__trigger[data-section]"));
    if (!a) return;
    const r = a.dataset.section, s = a.closest(".csb-accordion");
    if (!s || !r) return;
    const o = s.dataset.group || "default", c = (Y(this, An).has(o) ? Y(this, An).get(o) : s.dataset.default || null) === r ? null : r;
    Y(this, An).set(o, c), v(this, Ln, lc).call(this, s, c);
  }
  /**
   * Universal roll action: data-action="roll" + data-roll='{"intent":"skill","key":"gunnery"}'
   */
  async _onRollAction(t, n) {
    var c, u, d, m, f, p, g, h, y;
    (c = t == null ? void 0 : t.preventDefault) == null || c.call(t);
    const a = ((u = n == null ? void 0 : n.closest) == null ? void 0 : u.call(n, "[data-roll]")) ?? ((m = (d = t == null ? void 0 : t.target) == null ? void 0 : d.closest) == null ? void 0 : m.call(d, "[data-roll]")), r = (f = a == null ? void 0 : a.dataset) == null ? void 0 : f.roll;
    if (!r) return;
    let s;
    try {
      s = JSON.parse(r);
    } catch (b) {
      console.warn("MWD | Invalid data-roll JSON:", r, b);
      return;
    }
    const o = !!(t != null && t.shiftKey), l = ((p = game.mwd) == null ? void 0 : p.roll) ?? ((h = (g = game.system) == null ? void 0 : g.mwd) == null ? void 0 : h.roll);
    if (!(l != null && l.execute)) {
      (y = ui.notifications) == null || y.error("MWD roll system not initialized (game.mwd.roll.execute missing).");
      return;
    }
    try {
      return await l.execute({ actor: this.actor, payload: s, event: t, quick: o });
    } catch (b) {
      return console.error("MWD | Failed to execute roll action", b), sn(b, "Unable to execute that roll."), null;
    }
  }
  async _onEditImage(t, n) {
    var s, o, l;
    if ((s = t == null ? void 0 : t.preventDefault) == null || s.call(t), (o = t == null ? void 0 : t.stopPropagation) == null || o.call(t), !this.isEditable || !this.editing) return;
    const a = foundry.applications.apps.FilePicker.implementation;
    new a({
      type: "image",
      current: ((l = this.actor) == null ? void 0 : l.img) ?? "",
      callback: async (c) => {
        if (!c) return;
        await (this.getPersistentActor() ?? this.actor).update({ img: c });
      }
    }).render(!0);
  }
  /* -------------------------------------------- */
  /* Shared Owned Item Actions                     */
  /* -------------------------------------------- */
  // The character sheet already has richer item affordances, but NPC, vehicle,
  // and battlemech sheets only need a stable baseline: create, open, delete,
  // and the two common loadout toggles.
  _getOwnedItemFromTarget(t, n) {
    var r, s, o, l, c, u, d, m, f, p, g;
    const a = String(
      ((r = t == null ? void 0 : t.dataset) == null ? void 0 : r.itemId) ?? ((l = (o = (s = t == null ? void 0 : t.closest) == null ? void 0 : s.call(t, "[data-item-id]")) == null ? void 0 : o.dataset) == null ? void 0 : l.itemId) ?? ((m = (d = (u = (c = n == null ? void 0 : n.target) == null ? void 0 : c.closest) == null ? void 0 : u.call(c, "[data-item-id]")) == null ? void 0 : d.dataset) == null ? void 0 : m.itemId) ?? ""
    ).trim();
    return a ? ((g = (p = (f = this.actor) == null ? void 0 : f.items) == null ? void 0 : p.get) == null ? void 0 : g.call(p, a)) ?? null : null;
  }
  _getItemTypeLabel(t = "") {
    const n = String(t ?? "").trim();
    return {
      personalWeapon: "Personal Weapon",
      mechWeapon: "Mech Weapon",
      assetModule: "Asset Module",
      lifeModule: "Life Module",
      consumable: "Consumable"
    }[n] ?? n.replace(/([a-z])([A-Z])/g, "$1 $2").replace(/^./, (r) => r.toUpperCase());
  }
  async _onCreateOwnedItem(t, n) {
    var o, l, c;
    if ((o = t == null ? void 0 : t.preventDefault) == null || o.call(t), (l = t == null ? void 0 : t.stopPropagation) == null || l.call(t), !this.isEditable) return;
    const a = String(((c = n == null ? void 0 : n.dataset) == null ? void 0 : c.itemType) ?? "").trim();
    if (!a) return;
    const r = this.getPersistentActor() ?? this.actor, s = r.items.filter((u) => (u.canonicalType ?? u.type) === a).length;
    await r.createEmbeddedDocuments("Item", [{
      name: `${this._getItemTypeLabel(a)} ${s + 1}`,
      type: a
    }]), this.render({ force: !0 });
  }
  async _onEditOwnedItem(t, n) {
    var r, s, o;
    (r = t == null ? void 0 : t.preventDefault) == null || r.call(t), (s = t == null ? void 0 : t.stopPropagation) == null || s.call(t);
    const a = this._getOwnedItemFromTarget(n, t);
    (o = a == null ? void 0 : a.sheet) == null || o.render(!0);
  }
  async _onDeleteOwnedItem(t, n) {
    var s, o;
    if ((s = t == null ? void 0 : t.preventDefault) == null || s.call(t), (o = t == null ? void 0 : t.stopPropagation) == null || o.call(t), !this.isEditable) return;
    const a = this._getOwnedItemFromTarget(n, t);
    if (!a) return;
    await (this.getPersistentActor() ?? this.actor).deleteEmbeddedDocuments("Item", [a.id]), this.render({ force: !0 });
  }
  async _onToggleOwnedItemEquipped(t, n) {
    var s, o, l, c;
    if ((s = t == null ? void 0 : t.preventDefault) == null || s.call(t), (o = t == null ? void 0 : t.stopPropagation) == null || o.call(t), !this.isEditable) return;
    const a = this._getOwnedItemFromTarget(n, t);
    if (!a) return;
    const r = this.getPersistentActor() ?? this.actor;
    await ((c = r.setOwnedItemEquipped) == null ? void 0 : c.call(r, a.id, !((l = a.system) != null && l.equipped))), this.render({ force: !0 });
  }
  async _onSetOwnedItemPrimary(t, n) {
    var s, o, l, c;
    if ((s = t == null ? void 0 : t.preventDefault) == null || s.call(t), (o = t == null ? void 0 : t.stopPropagation) == null || o.call(t), !this.isEditable) return;
    const a = this._getOwnedItemFromTarget(n, t);
    if (!a) return;
    const r = this.getPersistentActor() ?? this.actor;
    await ((c = r.setOwnedItemPrimary) == null ? void 0 : c.call(r, a.id, !((l = a.system) != null && l.isPrimary))), this.render({ force: !0 });
  }
  /**
   * Post-render reconciliation hook (stable DOM).
   * Ensures every .csb-tabs group has exactly one active tab/panel:
   * - prefer remembered selection
   * - else use data-default
   * - else use first tab link
   * @override
   */
  _onRender(t, n) {
    var r, s, o;
    (r = super._onRender) == null || r.call(this, t, n);
    const a = this._getRootElement();
    if (a) {
      for (const l of a.querySelectorAll(".csb-tabs")) {
        const c = l.dataset.group || "default", u = Y(this, zr).get(c), d = l.dataset.default || ((s = l.querySelector(".csb-tab-link[data-tab]")) == null ? void 0 : s.dataset.tab), m = u || d;
        m && v(this, Ln, oc).call(this, l, m);
      }
      for (const l of a.querySelectorAll(".csb-accordion")) {
        const c = l.dataset.group || "default", u = Y(this, An).has(c) ? Y(this, An).get(c) : l.dataset.default || null;
        v(this, Ln, lc).call(this, l, u);
      }
      a.querySelectorAll(".csb-tabs").length && !a.querySelector(".csb-tab-panel.is-active") && console.warn(`${Le} CSB tabs present but no active tab applied. Check element root resolution and CSS .is-active selectors.`, {
        sheet: (o = this.constructor) == null ? void 0 : o.name
      });
      for (const l of a.querySelectorAll('prose-mirror[name="system.biography.history"]'))
        l.addEventListener("change", (c) => {
          c.preventDefault(), this._updateRichTextHistory(l);
        });
      this._restoreScrollPosition();
    }
  }
  async _updateRichTextHistory(t) {
    if (!this.isEditable || (t == null ? void 0 : t.name) !== "system.biography.history") return;
    const n = String(t.value ?? ""), a = String(foundry.utils.getProperty(this.actor, "system.biography.history") ?? "");
    if (n !== a)
      try {
        await (this.getPersistentActor() ?? this.actor).update({ "system.biography.history": n });
      } catch (r) {
        console.warn("MWD | Rich text history update failed:", r);
      }
  }
  async _commitEditsToActor() {
    var a;
    const t = this.element;
    if (!t) return;
    const n = tC({
      root: t,
      document: this.actor,
      selector: 'input[name][data-edit-field="staged"], select[name][data-edit-field="staged"], textarea[name][data-edit-field="staged"]',
      clampByPath: this._clampByPath.bind(this),
      skipNames: ["system.biography.history"]
    });
    if (["vehicle", "battlemech"].includes((a = this.actor) == null ? void 0 : a.type) && n["system.movement.ground"] !== void 0 && (n["system.moves"] = n["system.movement.ground"]), !!Object.keys(n).length)
      try {
        await (this.getPersistentActor() ?? this.actor).update(n);
      } catch (r) {
        console.warn("MWD | Commit failed (permissions or validation):", r);
      }
  }
  /** @override */
  async _prepareContext(t) {
    var o, l, c, u, d, m, f, p, g, h, y, b, S, w, T, k, C, I, O, x, B, P;
    console.log(`${Le}BaseActorSheetV2._prepareContext:start`, {
      actorName: (o = this.actor) == null ? void 0 : o.name,
      actorType: (l = this.actor) == null ? void 0 : l.type
    });
    const n = await super._prepareContext(t), a = foundry.utils.deepClone((n == null ? void 0 : n.options) ?? {});
    a.classes = Array.from(((c = this.options) == null ? void 0 : c.classes) ?? []), a.cssClass = a.classes.join(" ");
    const r = foundry.utils.mergeObject(
      n,
      {
        actor: this.actor,
        system: (u = this.actor) == null ? void 0 : u.system,
        editable: this.isEditable,
        owner: ((d = this.document) == null ? void 0 : d.isOwner) ?? !1,
        limited: !(((m = this.document) == null ? void 0 : m.isOwner) ?? !1),
        editing: Y(this, ki),
        data: this.actor,
        options: a,
        cssClass: a.cssClass
      },
      { inplace: !1 }
    );
    r.options.owner = r.owner, r.options.limited = r.limited, r.options.editable = r.editable, r.options.editing = r.editing, r.options.viewMode = !r.editing, r.skillsDisplay = Hf(((f = this.actor) == null ? void 0 : f.system) ?? {}), r.bio = {
      ...r.bio ?? {},
      fields: {
        history: aC("system.biography.history")
      }
    };
    const s = ["battlemech", "vehicle"].includes(((p = this.actor) == null ? void 0 : p.type) ?? "");
    return r.machineHeader = {
      enabled: s,
      model: String(((y = (h = (g = this.actor) == null ? void 0 : g.system) == null ? void 0 : h.mwd) == null ? void 0 : y.model) ?? "").trim(),
      path: "system.mwd.model",
      placeholder: ((b = this.actor) == null ? void 0 : b.type) === "battlemech" ? "WHM-6R Warhammer" : "Vehicle model",
      cbillCost: Number(((T = (w = (S = this.actor) == null ? void 0 : S.system) == null ? void 0 : w.mwd) == null ? void 0 : T.cbillCost) ?? 0) || 0,
      cbillDisplay: new Intl.NumberFormat("en-US").format(Number(((I = (C = (k = this.actor) == null ? void 0 : k.system) == null ? void 0 : C.mwd) == null ? void 0 : I.cbillCost) ?? 0) || 0),
      cbillPath: "system.mwd.cbillCost"
    }, r.items ?? (r.items = {}), (O = this.actor) != null && O.items && typeof (be == null ? void 0 : be.classifyInto) == "function" && (be.classifyInto(r.items, this.actor.items), r.items.weapon = [
      ...r.items.mechWeapon ?? [],
      ...r.items.personalWeapon ?? []
    ]), r.npcItems = {
      traits: r.items.quality ?? [],
      weapons: r.items.weapon ?? [],
      assetModules: r.items.assetModule ?? [],
      // Legacy partials still read npcItems.inventory, so fold consumables into
      // that alias until every remaining actor surface reads explicit buckets.
      inventory: [
        ...r.items.gear ?? [],
        ...r.items.consumable ?? []
      ]
    }, console.log(`${Le}BaseActorSheetV2._prepareContext:done`, {
      actorType: (x = this.actor) == null ? void 0 : x.type,
      cssClass: r.cssClass,
      itemCount: ((P = (B = this.actor) == null ? void 0 : B.items) == null ? void 0 : P.size) ?? 0,
      editing: Y(this, ki)
    }), r;
  }
  /** Clamp certain actor system paths to valid ranges */
  _clampByPath(t, n) {
    return typeof n != "number" ? n : ((/^system\.skills\.[^.]+\.rating$/.test(t) || /^system\.attributes\.[^.]+\.value$/.test(t)) && (n = Math.trunc(n)), /^system\.skills\.[^.]+\.rating$/.test(t) ? Math.clamp(n, 0, 12) : /^system\.attributes\.[^.]+\.value$/.test(t) ? Math.clamp(n, 0, 10) : t === "system.speed" || /^system\.movement\.(ground|flight|jump)$/.test(t) ? Math.max(0, Math.trunc(n)) : n);
  }
  /** Action handler: Condition Monitor set */
  async _onMonitorSet(t, n) {
    var p, g, h, y, b, S;
    if (t.preventDefault(), (p = t.stopPropagation) == null || p.call(t), !this.isEditable) return;
    const a = ((g = n == null ? void 0 : n.closest) == null ? void 0 : g.call(n, "[data-action='monitorSet']")) ?? ((y = (h = t == null ? void 0 : t.target) == null ? void 0 : h.closest) == null ? void 0 : y.call(h, "[data-action='monitorSet']")) ?? n, r = String(((b = a == null ? void 0 : a.dataset) == null ? void 0 : b.monitor) ?? "").trim(), s = Number((S = a == null ? void 0 : a.dataset) == null ? void 0 : S.value);
    if (!r || !Number.isFinite(s)) return;
    this._captureScrollPosition();
    const o = r === "burn" ? "system.burn.value" : `system.monitors.${r}.value`, l = Number(foundry.utils.getProperty(this.actor, o) ?? 0), c = r === "armor" ? s : l === s ? 0 : s, u = this.getPersistentActor() ?? this.actor;
    if (typeof (u == null ? void 0 : u.setMonitorValue) == "function")
      return u.setMonitorValue(r, c, { source: "sheet" });
    const d = `system.monitors.${r}`, m = Number(foundry.utils.getProperty(u, `${d}.max`)) || 0, f = Math.min(Math.max(0, c), Math.max(0, m));
    return u.update({ [`${d}.value`]: f });
  }
  /**
  * Compute -1 penalty per 3 full damage (3,6,9...)
  * Returns 0, -1, -2, ...
  */
  static _mwdPenaltyFromDamage(t) {
    const n = Math.max(0, Number(t) || 0);
    return -Math.floor(n / 3);
  }
  /**
   * Compute resistance = ceil(value / 4), with 0 -> 0
   * 1-4 => 1, 5-8 => 2, ...
   */
  static _mwdResistanceFromValue(t) {
    const n = Math.max(0, Number(t) || 0);
    return n === 0 ? 0 : Math.ceil(n / 4);
  }
};
ki = new WeakMap(), zr = new WeakMap(), An = new WeakMap(), Wn = new WeakMap(), Ln = new WeakSet(), /**
 * Apply active-state classes within a single .csb-tabs root.
 * Idempotent: safe to call every render.
 */
oc = function(t, n) {
  t.querySelectorAll(".csb-tab-link").forEach((a) => {
    a.classList.toggle("is-active", a.dataset.tab === n);
  }), t.querySelectorAll(".csb-tab-panel").forEach((a) => {
    a.classList.toggle("is-active", a.dataset.tab === n);
  });
}, lc = function(t, n) {
  t.dataset.activeSection = n ?? "", t.querySelectorAll(".csb-accordion__section").forEach((a) => {
    const r = a.dataset.section === n;
    a.classList.toggle("is-active", r);
  }), t.querySelectorAll(".csb-accordion__trigger").forEach((a) => {
    const r = a.dataset.section === n;
    a.classList.toggle("is-active", r), a.setAttribute("aria-expanded", r ? "true" : "false");
  }), t.querySelectorAll(".csb-accordion__panel").forEach((a) => {
    const r = a.closest(".csb-accordion__section"), s = (r == null ? void 0 : r.dataset.section) === n;
    a.classList.toggle("is-active", s);
  });
}, // Shared size bounds keep the V2 actor sheets visually consistent while still
// allowing each subclass to request a slightly different preferred size.
z(bt, "MIN_WIDTH", 800), z(bt, "MAX_WIDTH", 950), z(bt, "MIN_HEIGHT", 600), z(bt, "MAX_HEIGHT", 1400), /** @override */
z(bt, "DEFAULT_OPTIONS", foundry.utils.mergeObject(Oi(bt, bt, "DEFAULT_OPTIONS"), {
  classes: ["sheet", "actor", M, "appv2", "mwd-sheet", "mwd-character-sheet"],
  position: { width: 760, height: 760 },
  window: { resizable: !0, minimizable: !0 },
  actions: {
    toggleViewMode: bt.prototype._onToggleViewMode,
    tab: bt.prototype._onClickTab,
    accordion: bt.prototype._onClickAccordion,
    roll: bt.prototype._onRollAction,
    monitorSet: bt.prototype._onMonitorSet,
    editImage: bt.prototype._onEditImage,
    createOwnedItem: bt.prototype._onCreateOwnedItem,
    editOwnedItem: bt.prototype._onEditOwnedItem,
    deleteOwnedItem: bt.prototype._onDeleteOwnedItem,
    toggleOwnedItemEquipped: bt.prototype._onToggleOwnedItemEquipped,
    setOwnedItemPrimary: bt.prototype._onSetOwnedItemPrimary
  }
}, { inplace: !1 }));
let _r = bt;
function rC(i = {}) {
  switch (i.type) {
    case "stack":
      return "mwd.v2.ui.nodes.stack";
    case "hexabox":
      return "mwd.v2.ui.nodes.hexabox";
    case "panel":
      return "mwd.v2.ui.nodes.panel";
    case "include":
      return "mwd.v2.ui.nodes.include";
    case "tabs":
      return "mwd.v2.ui.nodes.tabs";
    case "accordion":
      return "mwd.v2.ui.nodes.accordion";
    default:
      return "mwd.v2.ui.nodes.unknown";
  }
}
function sC(i) {
  return Array.isArray(i) ? i : typeof i == "string" ? i.split(/\s+/).filter(Boolean) : [];
}
function Ms(i) {
  if (!i || typeof i != "object") return i;
  const e = {
    ...i,
    template: i.template ?? rC(i),
    classes: sC(i.classes),
    children: Array.isArray(i.children) ? i.children.map(Ms) : []
  };
  return i.type === "tabs" && (e.tabs = Array.isArray(i.tabs) ? i.tabs.map((t) => ({
    ...t,
    children: Array.isArray(t.children) ? t.children.map(Ms) : []
  })) : []), i.type === "accordion" && (e.sections = Array.isArray(i.sections) ? i.sections.map((t) => ({
    ...t,
    children: Array.isArray(t.children) ? t.children.map(Ms) : []
  })) : []), e;
}
function gm(i = {}) {
  return {
    ...i,
    root: Ms(i.root ?? { type: "stack", children: [] })
  };
}
var wn, eo, ah;
class oa {
  static async get(e) {
    if (Y(this, wn).has(e)) {
      const a = await Y(this, wn).get(e);
      if (Number((a == null ? void 0 : a.version) ?? 0) > 0) return a;
      Y(this, wn).delete(e);
    }
    const t = v(this, eo, ah).call(this, e);
    Y(this, wn).set(e, t);
    const n = await t;
    return Number((n == null ? void 0 : n.version) ?? 0) <= 0 && Y(this, wn).delete(e), n;
  }
}
wn = new WeakMap(), eo = new WeakSet(), ah = async function(e) {
  const t = `systems/${M}/templates/v2/layouts/${e}.layout.json`;
  try {
    const n = await fetch(t);
    if (!n.ok) throw new Error(`HTTP ${n.status} for ${t}`);
    return gm(await n.json());
  } catch (n) {
    return console.error(`${Le}LayoutRegistry.get FAILED`, { layoutId: e, url: t, error: n }), gm({
      id: e,
      version: 0,
      root: { type: "stack", children: [] }
    });
  }
}, Ce(oa, eo), Ce(oa, wn, /* @__PURE__ */ new Map());
function xr(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? t : e;
}
function Bu(i, e, t) {
  return Math.min(t, Math.max(e, i));
}
function oC(i = 0) {
  const e = Bu(xr(i, 0), 0, 100);
  return e >= 91 ? "green" : e >= 71 ? "yellow" : e >= 51 ? "orange" : e >= 31 ? "red" : "dark-red";
}
function hm(i, e = {}) {
  const t = Math.max(0, xr(e == null ? void 0 : e.max, 0)), n = Bu(xr(e == null ? void 0 : e.value, 0), 0, t), a = Math.max(0, t - n), r = t > 0 ? a / t * 100 : 0;
  return {
    label: i,
    value: String(a),
    tone: oC(r),
    remaining: a,
    max: t,
    percent: r,
    title: `${a}/${t}`
  };
}
function lC({ armor: i = {}, structure: e = {} } = {}) {
  const t = [
    hm("A", i),
    hm("S", e)
  ];
  return {
    parts: t,
    title: `Armor ${t[0].title}; Structure ${t[1].title}`
  };
}
function cc({
  id: i = "",
  label: e = "",
  kind: t = "wound",
  monitor: n = {},
  editable: a = !1
} = {}) {
  const r = Math.max(0, xr(n == null ? void 0 : n.max, 0)), s = Bu(xr(n == null ? void 0 : n.value, 0), 0, r), o = Math.max(0, r - s);
  return {
    id: i,
    label: e,
    kind: t,
    editable: !!a,
    value: o,
    max: r,
    segments: Array.from({ length: r }, (l, c) => {
      const u = c + 1;
      return {
        value: Math.max(0, r - u),
        filled: u <= o
      };
    })
  };
}
function cC(i = {}) {
  const e = String((i == null ? void 0 : i.label) ?? oe((i == null ? void 0 : i.key) ?? "Critical")).trim() || "Critical", t = String((i == null ? void 0 : i.locationLabel) ?? oe((i == null ? void 0 : i.locationKey) ?? "")).trim();
  return t ? `${e} (${t})` : e;
}
function rh(i = []) {
  const e = Array.isArray(i) ? i.filter((n) => n && n.active !== !1) : [], t = e.length;
  return {
    value: t === 0 ? "CLEAR" : t === 1 ? "1 CRIT" : `${t} CRITS`,
    title: e.map(cC).join("; "),
    count: t
  };
}
function br(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? Math.trunc(t) : e;
}
function sh(i, e = 5) {
  return br(i, e);
}
function uC({
  clusteringDice: i = 0,
  clusteringTargetNumber: e = 5,
  diceModifier: t = 0,
  targetNumberModifier: n = 0
} = {}) {
  const a = Math.max(0, br(i, 0)), r = sh(e, 5), o = a > 0 ? br(t, 0) : 0, l = Math.max(0, a + o), c = l > 0, u = c ? br(n, 0) : 0;
  return {
    active: c,
    baseDice: a,
    diceModifier: o,
    dice: l,
    baseTargetNumber: r,
    targetNumberModifier: u,
    targetNumber: r + u
  };
}
async function dC({
  clusteringDice: i = 0,
  clusteringTargetNumber: e = 5,
  RollClass: t = globalThis.Roll
} = {}) {
  const n = Math.max(0, br(i, 0)), a = sh(e, 5), r = n > 0 ? `${n}d6cs>=${a}` : "";
  if (!n || typeof t != "function")
    return {
      rolled: !1,
      dice: n,
      targetNumber: a,
      hits: 0,
      formula: r,
      results: [],
      roll: null
    };
  const s = await new t(r).evaluate(), o = Array.isArray(s == null ? void 0 : s.dice) ? s.dice[0] ?? null : null, l = Array.isArray(o == null ? void 0 : o.results) ? o.results.map((c, u) => ({
    index: u,
    result: Number((c == null ? void 0 : c.result) ?? 0) || 0,
    success: !!(c != null && c.success)
  })) : [];
  return {
    rolled: !0,
    dice: n,
    targetNumber: a,
    hits: l.filter((c) => c.success).length,
    formula: r,
    results: l,
    roll: typeof (s == null ? void 0 : s.toJSON) == "function" ? s.toJSON() : null
  };
}
function Ro(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? t : e;
}
function mC(i = null) {
  return !!((i == null ? void 0 : i.type) === A.itemType.personalWeapon || i != null && i.isSynthetic);
}
function fC(i = {}) {
  var n, a, r, s;
  const e = Array.isArray((n = i == null ? void 0 : i.attack) == null ? void 0 : n.targets) ? i.attack.targets : [], t = on(((a = i == null ? void 0 : i.attack) == null ? void 0 : a.areaEffect) ?? ((s = (r = i == null ? void 0 : i.attack) == null ? void 0 : r.payload) == null ? void 0 : s.areaEffect) ?? {});
  if (!e.length && t.kind !== ai.persistent)
    throw new Error("Attack requires at least one target.");
  return e;
}
async function oh(i = {}) {
  if (!(i != null && i.actorUuid)) return null;
  try {
    return await fromUuid(i.actorUuid);
  } catch (e) {
    return console.warn("MWD | Unable to resolve attack target actor for CQ", i, e), null;
  }
}
function pC(i = {}, e = null, t = "") {
  var n, a, r, s, o;
  return Math.max(0, Ro(
    ((n = i == null ? void 0 : i.attributes) == null ? void 0 : n[t]) ?? ((a = e == null ? void 0 : e.getAttributeValue) == null ? void 0 : a.call(e, t)) ?? ((o = (s = (r = e == null ? void 0 : e.system) == null ? void 0 : r.attributes) == null ? void 0 : s[t]) == null ? void 0 : o.value),
    0
  ));
}
function gC(i = {}, e = null, t = "") {
  var n, a, r, s, o, l;
  return Math.max(0, Ro(
    ((a = (n = i == null ? void 0 : i.skills) == null ? void 0 : n[t]) == null ? void 0 : a.rating) ?? ((r = e == null ? void 0 : e.getSkillRating) == null ? void 0 : r.call(e, t)) ?? ((l = (o = (s = e == null ? void 0 : e.system) == null ? void 0 : s.skills) == null ? void 0 : o[t]) == null ? void 0 : l.rating),
    0
  ));
}
function ym(i = []) {
  return i.reduce((e, t) => e + Ro(t == null ? void 0 : t.value, 0), 0);
}
async function hC({ attacker: i = null, ctx: e = {}, target: t = {} } = {}) {
  var I, O, x, B, P, L, D, G, V, Z, E, X, H, he, ie, le, ze, Xe, mt, ft, pt, gt, nt, ht;
  const n = await oh(t), a = Math.max(0, Number(((B = (O = (I = e == null ? void 0 : e.attack) == null ? void 0 : I.weapon) == null ? void 0 : O.attackRatingBand) == null ? void 0 : B[(x = e == null ? void 0 : e.attack) == null ? void 0 : x.rangeBand]) ?? 0) || 0), r = Cr(n), s = r ? A.actorAttributes.handling : "reflexes", o = pC(t, n, s), l = o + o, c = String(((L = (P = e == null ? void 0 : e.attack) == null ? void 0 : P.skill) == null ? void 0 : L.code) ?? ((G = (D = e == null ? void 0 : e.attack) == null ? void 0 : D.weapon) == null ? void 0 : G.skill) ?? "").trim(), u = String(((Z = (V = e == null ? void 0 : e.attack) == null ? void 0 : V.skill) == null ? void 0 : Z.label) ?? c ?? "Attack Skill").trim() || "Attack Skill", d = c ? Math.max(0, Ro(((E = i == null ? void 0 : i.getSkillRating) == null ? void 0 : E.call(i, c)) ?? ((he = (H = (X = i == null ? void 0 : i.system) == null ? void 0 : X.skills) == null ? void 0 : H[c]) == null ? void 0 : he.rating), 0)) : 0, m = r ? "piloting" : "tactics", f = r ? "Piloting" : "Tactics", p = gC(t, n, m), g = d - p, h = Math.abs(g), y = Math.max(0, Number(((ie = t == null ? void 0 : t.activeArmor) == null ? void 0 : ie.defenseBonus) ?? 0) || 0), b = String(((le = e == null ? void 0 : e.attack) == null ? void 0 : le.rangeBand) ?? "").trim() || "range", w = [{
    id: "weapon.attackRating",
    label: `Weapon AR (${((Xe = (ze = e == null ? void 0 : e.attack) == null ? void 0 : ze.weapon) == null ? void 0 : Xe.type) === "personalWeapon" || (ft = (mt = e == null ? void 0 : e.attack) == null ? void 0 : mt.weapon) != null && ft.isSynthetic ? So(b) : b})`,
    value: a
  }], T = [{
    id: r ? "target.handlingDefense" : "target.reflexesDefense",
    label: r ? "Target Handling + Handling" : "Target REF + REF",
    value: l
  }];
  if (g > 0 ? w.push({
    id: "skill.attackVsTactics",
    label: `${u} over Tactics`,
    value: h
  }) : g < 0 && T.push({
    id: "target.tacticsAdvantage",
    label: `${f} over ${u}`,
    value: h
  }), (gt = (pt = e == null ? void 0 : e.attack) == null ? void 0 : pt.aim) != null && gt.eligible && w.push({
    id: "state.aim",
    label: `Aim (${u})`,
    value: d
  }), T.push({
    id: "target.armorDefense",
    label: "Armor Defense",
    value: y
  }), Cr(i)) {
    const Ye = Dd(i, {
      rangeBand: (nt = e == null ? void 0 : e.attack) == null ? void 0 : nt.rangeBand
    });
    Ye.ar && w.push({
      id: "machineState.attackAr",
      label: "Machine State",
      value: Ye.ar
    });
  }
  if (r) {
    const Ye = Dd(n, {
      rangeBand: (ht = e == null ? void 0 : e.attack) == null ? void 0 : ht.rangeBand
    });
    Ye.dr && T.push({
      id: "machineState.defenseDr",
      label: "Machine State",
      value: Ye.dr
    });
  }
  const k = ym(w), C = ym(T);
  return {
    ar: {
      parts: w,
      total: k
    },
    dr: {
      parts: T,
      total: C
    },
    comparison: {
      attackSkillCode: c,
      attackSkillLabel: u,
      attackerSkill: d,
      defenderSkillCode: m,
      defenderSkillLabel: f,
      defenderSkill: p,
      delta: g,
      advantage: h,
      winner: g > 0 ? "attacker" : g < 0 ? "defender" : "none"
    },
    value: k - C
  };
}
async function yC(i = {}, e = {}) {
  var k, C, I, O, x, B, P, L, D, G, V, Z, E, X, H;
  const t = (i == null ? void 0 : i.attack) ?? {}, n = String(((C = (k = t == null ? void 0 : t.payload) == null ? void 0 : k.modifies) == null ? void 0 : C.damageType) ?? "").trim(), a = jw(i == null ? void 0 : i.attacker, {
    weaponGroupId: ((O = (I = t == null ? void 0 : t.weapon) == null ? void 0 : I.machineWeaponGroup) == null ? void 0 : O.id) ?? ((x = t == null ? void 0 : t.payload) == null ? void 0 : x.weaponGroupId),
    weaponId: (B = t == null ? void 0 : t.payload) == null ? void 0 : B.weaponId,
    weapon: t == null ? void 0 : t.weapon
  }), r = Au(i == null ? void 0 : i.attacker), s = String(((P = t == null ? void 0 : t.weapon) == null ? void 0 : P.damageType) ?? "").trim().toLowerCase() === "energy", o = Math.max(0, (Number(((L = t == null ? void 0 : t.weapon) == null ? void 0 : L.damage) ?? 0) || 0) + a + (s ? Number(r.energyAttackDamage ?? 0) : 0)), l = Math.max(0, Number(((D = t == null ? void 0 : t.weapon) == null ? void 0 : D.clusteringDice) ?? 0) || 0), c = Number(((G = t == null ? void 0 : t.weapon) == null ? void 0 : G.clusteringTargetNumber) ?? 5) || 5, u = e.outcome !== "miss" && l > 0 ? await dC({
    clusteringDice: l,
    clusteringTargetNumber: c
  }) : {
    rolled: !1,
    dice: l,
    targetNumber: c,
    hits: 0,
    formula: l > 0 ? `${l}d6cs>=${c}` : "",
    results: [],
    roll: null
  }, d = e.outcome === "miss" ? 0 : Number(u.hits ?? 0), m = !!(i != null && i.targetIsMachine), f = n || ((V = t == null ? void 0 : t.weapon) == null ? void 0 : V.damageType), p = m ? String(f ?? "kinetic").trim() || "kinetic" : ci(f, "concussive"), g = Math.max(0, Number((t == null ? void 0 : t.totalAp) ?? ((Z = t == null ? void 0 : t.weapon) == null ? void 0 : Z.ap) ?? 0) || 0), h = e.outcome === "graze" ? o / 2 : e.outcome === "hit" ? o : 0, y = mC(t == null ? void 0 : t.weapon) ? Number(e.netHits ?? 0) : 0, b = h + d + y, S = qc((t == null ? void 0 : t.currentExposure) ?? Rn({
    tier: ((E = t == null ? void 0 : t.currentExposure) == null ? void 0 : E.initialTier) ?? ((X = t == null ? void 0 : t.currentExposure) == null ? void 0 : X.tier) ?? "none"
  }), {
    active: !!(t != null && t.evadeActive),
    locked: !!(t != null && t.evadeLocked)
  }), w = on((t == null ? void 0 : t.areaEffect) ?? ((H = t == null ? void 0 : t.payload) == null ? void 0 : H.areaEffect) ?? {}), T = w.kind === ai.persistent ? b : Zn(b, S.finalTier);
  return {
    baseDamage: o,
    effectiveWeaponDamage: h,
    clustering: {
      ...u,
      damageBonus: d
    },
    netHits: Number(e.netHits ?? 0),
    netDamageBonus: y,
    attackQuality: e.outcome === "graze" ? "graze" : e.outcome === "hit" && Number(e.netHits ?? 0) >= 4 ? "highMargin" : e.outcome === "hit" ? "hit" : "",
    incoming: b,
    scaledIncoming: T,
    ap: g,
    damageType: p,
    damageTypeLabel: m ? p : Pi(p),
    exposure: S,
    areaEffect: w
  };
}
function bC(i = {}) {
  return String((i == null ? void 0 : i.tokenUuid) ?? (i == null ? void 0 : i.actorUuid) ?? (i == null ? void 0 : i.tokenId) ?? (i == null ? void 0 : i.actorId) ?? (i == null ? void 0 : i.name) ?? foundry.utils.randomID()).trim();
}
function bm(i, e) {
  var r, s;
  const t = ((s = (r = i == null ? void 0 : i.system) == null ? void 0 : r.monitors) == null ? void 0 : s[e]) ?? {}, n = Math.max(0, Number(t.max ?? 0) || 0), a = Math.min(n, Math.max(0, Number(t.value ?? 0) || 0));
  return Math.max(0, n - a);
}
function Sm({ attacker: i, ctx: e, damage: t, targetActor: n = null, hitLocation: a = null } = {}) {
  var r, s, o, l, c, u, d, m, f, p, g;
  return Cr(n) ? {
    mode: "machineAttackDamage",
    damage: (t == null ? void 0 : t.scaledIncoming) ?? 0,
    attackQuality: (t == null ? void 0 : t.attackQuality) ?? "",
    outcome: (t == null ? void 0 : t.attackQuality) === "highMargin" ? "hit" : (t == null ? void 0 : t.attackQuality) ?? "",
    netHits: (t == null ? void 0 : t.netHits) ?? 0,
    damageType: t == null ? void 0 : t.damageType,
    ap: (t == null ? void 0 : t.ap) ?? 0,
    hitLocation: a,
    chaosCriticalSelected: !1,
    reliabilitySpendSelections: [],
    source: `${(i == null ? void 0 : i.name) ?? "Attacker"}: ${((s = (r = e == null ? void 0 : e.attack) == null ? void 0 : r.weapon) == null ? void 0 : s.name) ?? "Attack"}`,
    sourceData: {
      attackerUuid: (i == null ? void 0 : i.uuid) ?? "",
      weaponName: ((l = (o = e == null ? void 0 : e.attack) == null ? void 0 : o.weapon) == null ? void 0 : l.name) ?? "Attack",
      weaponUuid: ((u = (c = e == null ? void 0 : e.attack) == null ? void 0 : c.weapon) == null ? void 0 : u.uuid) ?? ""
    },
    notes: ""
  } : {
    mode: "attackDamage",
    track: A.monitors.physical,
    damage: (t == null ? void 0 : t.scaledIncoming) ?? 0,
    netHits: 0,
    damageType: t == null ? void 0 : t.damageType,
    ap: (t == null ? void 0 : t.ap) ?? 0,
    effects: ((m = (d = e == null ? void 0 : e.attack) == null ? void 0 : d.weapon) == null ? void 0 : m.effects) ?? {},
    source: `${(i == null ? void 0 : i.name) ?? "Attacker"}: ${((p = (f = e == null ? void 0 : e.attack) == null ? void 0 : f.weapon) == null ? void 0 : p.name) ?? "Attack"}`,
    notes: (g = t == null ? void 0 : t.exposure) != null && g.initialTier ? `Exposure ${bi(t.exposure.initialTier)}${t.exposure.evadeUsed ? ` -> ${bi(t.exposure.finalTier)}` : ""}` : ""
  };
}
function Gn(i, e = {}, t = {}, { queued: n = !1, applied: a = !1, skipped: r = !1, reason: s = "" } = {}) {
  return r ? {
    ok: !0,
    skipped: !0,
    queued: !1,
    applied: !1,
    reason: s || "Missed target."
  } : i != null && i.ok ? {
    ok: !0,
    queued: !!n,
    applied: !!a,
    preview: !!i.dryRun,
    actorName: i.actorName ?? (e == null ? void 0 : e.name) ?? "Target",
    sourceType: i.sourceType ?? null,
    mode: i.mode ?? "attackDamage",
    track: i.track ?? A.monitors.physical,
    requestedDelta: Number(i.requestedDelta ?? 0),
    appliedDelta: Number(i.appliedDelta ?? 0),
    usedArmor: !!i.usedArmor,
    damageType: i.damageType ?? (t == null ? void 0 : t.damageType) ?? "",
    effectiveAp: Number(i.effectiveAp ?? (t == null ? void 0 : t.ap) ?? 0),
    hitLocation: i.hitLocation ?? null,
    critical: i.critical ?? null,
    machine: i.machine ?? null,
    degradation: i.degradation ?? null,
    mitigation: i.mitigation ? {
      baseMitigation: Number(i.mitigation.baseMitigation ?? 0),
      typeMitigationMod: Number(i.mitigation.typeMitigationMod ?? 0),
      netResistance: Number(i.mitigation.netResistance ?? 0),
      armorBefore: Number(i.mitigation.armorBefore ?? 0),
      armorAfter: Number(i.mitigation.armorAfter ?? 0),
      reinforcedBefore: Number(i.mitigation.reinforcedBefore ?? 0),
      reinforcedAfter: Number(i.mitigation.reinforcedAfter ?? 0),
      reinforcedMax: Number(i.mitigation.reinforcedMax ?? 0)
    } : null,
    damageIncoming: Number(i.damageIncoming ?? 0),
    adjustedIncoming: Number(i.adjustedIncoming ?? 0),
    finalDamage: Number(i.finalDamage ?? 0),
    beforeLabel: String(i.beforeLabel ?? "").trim(),
    afterLabel: String(i.afterLabel ?? "").trim(),
    source: String(i.source ?? "").trim(),
    notes: String(i.notes ?? "").trim()
  } : {
    ok: !1,
    queued: !1,
    applied: !1,
    reason: (i == null ? void 0 : i.reason) ?? s ?? "Unable to preview attack damage."
  };
}
async function SC({ attacker: i, ctx: e, target: t, outcome: n, damage: a } = {}) {
  var d, m;
  if ((n == null ? void 0 : n.outcome) === "miss")
    return Gn(null, t, a, { skipped: !0, reason: "Missed target." });
  if (((d = a == null ? void 0 : a.areaEffect) == null ? void 0 : d.kind) === ai.persistent)
    return {
      ok: !0,
      queued: !0,
      applied: !1,
      preview: !0,
      actorName: (t == null ? void 0 : t.name) ?? "Target",
      mode: "hazardEntry",
      reason: ""
    };
  let r = null, s = null;
  try {
    r = t != null && t.tokenUuid ? await fromUuid(t.tokenUuid) : null, s = t != null && t.actorUuid ? await fromUuid(t.actorUuid) : null;
  } catch (f) {
    return console.warn("MWD | Unable to resolve target for queued attack damage", t, f), Gn(null, t, a, { reason: "Unable to resolve attack target." });
  }
  const l = Cr(s) ? kp({
    actor: s,
    rollTotal: Tp(),
    armorBefore: bm(s, A.monitors.armor),
    structureBefore: bm(s, A.monitors.structure)
  }) : null, c = Sm({ attacker: i, ctx: e, damage: a, targetActor: s, hitLocation: l }), u = await Jt.apply({
    actor: s,
    token: r,
    payload: c,
    options: {
      actorId: (s == null ? void 0 : s.id) ?? "",
      dryRun: !0,
      logToChat: !1
    }
  });
  if (u != null && u.ok) {
    const f = Gn(u, t, a, { queued: !0, applied: !1 }), p = Sm({ attacker: i, ctx: e, damage: a, targetActor: s, hitLocation: l });
    return p.mode === "machineAttackDamage" && Array.isArray((m = f == null ? void 0 : f.critical) == null ? void 0 : m.records) && f.critical.records.length && (p.preparedCriticalRecords = foundry.utils.deepClone(f.critical.records)), {
      ...f,
      queuedMutation: {
        id: foundry.utils.randomID(),
        type: "attackDamage",
        applied: !1,
        target: {
          name: (t == null ? void 0 : t.name) ?? "Target",
          actorUuid: (t == null ? void 0 : t.actorUuid) ?? null,
          tokenUuid: (t == null ? void 0 : t.tokenUuid) ?? null
        },
        payload: p,
        hitLocation: l,
        preview: f
      }
    };
  }
  return Gn(u, t, a, { reason: "Unable to preview attack damage." });
}
async function AC({ attacker: i, ctx: e, outcomeModel: t, target: n, previewState: a = {} } = {}) {
  var b, S;
  const r = await hC({ attacker: i, ctx: e, target: n }), s = await oh(n), o = Number((t == null ? void 0 : t.margin) ?? 0), l = Number(r.value ?? 0), c = o;
  let u = l > 0 ? o >= 1 ? "hit" : o === 0 ? "graze" : "miss" : l < 0 ? o >= 2 ? "hit" : o === 1 ? "graze" : "miss" : o >= 1 ? "hit" : "miss";
  String(((b = e == null ? void 0 : e.attack) == null ? void 0 : b.rangeBand) ?? "").trim().toLowerCase() === "outofrange" && u === "hit" && (u = "graze");
  const d = u === "hit" ? Math.max(0, c) : 0, m = (e == null ? void 0 : e.attack) ?? {}, f = bC(n), p = (a == null ? void 0 : a[f]) ?? {}, g = (n == null ? void 0 : n.exposure) ?? Rn({ tier: "none" }), h = await yC({
    ...e,
    attacker: i,
    targetIsMachine: Cr(s),
    attack: {
      ...m,
      currentExposure: g,
      areaEffect: (m == null ? void 0 : m.areaEffect) ?? ((S = m == null ? void 0 : m.payload) == null ? void 0 : S.areaEffect) ?? null,
      evadeActive: !!(p != null && p.evadeActive),
      evadeLocked: !!(g != null && g.evadeLocked)
    }
  }, { outcome: u, netHits: d }), y = await SC({
    attacker: i,
    ctx: e,
    target: n,
    outcome: { outcome: u },
    damage: h
  });
  return {
    target: {
      name: (n == null ? void 0 : n.name) ?? "Target",
      actorUuid: (n == null ? void 0 : n.actorUuid) ?? null,
      tokenUuid: (n == null ? void 0 : n.tokenUuid) ?? null
    },
    previewKey: f,
    exposure: g,
    evadeActive: !!(p != null && p.evadeActive),
    evadeEdgePoolKey: String((p == null ? void 0 : p.edgePoolKey) ?? "").trim() || null,
    cq: r,
    margin: o,
    rawNetHits: c,
    netHits: d,
    outcome: u,
    damage: h,
    damageResult: y,
    queuedMutation: (y == null ? void 0 : y.queuedMutation) ?? null
  };
}
function wC(i = []) {
  const e = { hits: 0, grazes: 0, misses: 0 };
  for (const t of i)
    (t == null ? void 0 : t.outcome) === "hit" ? e.hits += 1 : (t == null ? void 0 : t.outcome) === "graze" ? e.grazes += 1 : e.misses += 1;
  return {
    ...e,
    overallOutcome: e.hits > 0 ? "hit" : e.grazes > 0 ? "graze" : "miss"
  };
}
async function lh({ attacker: i, ctx: e, outcomeModel: t, previewState: n = {}, existingAttackResult: a = null } = {}) {
  var c, u, d, m, f, p;
  const r = fC(e), s = [];
  for (const g of r)
    s.push(await AC({ attacker: i, ctx: e, outcomeModel: t, target: g, previewState: n }));
  const o = on(((c = e == null ? void 0 : e.attack) == null ? void 0 : c.areaEffect) ?? ((d = (u = e == null ? void 0 : e.attack) == null ? void 0 : u.payload) == null ? void 0 : d.areaEffect) ?? {});
  let l = String((a == null ? void 0 : a.persistentRegionUuid) ?? "").trim() || null;
  if (o.kind === ai.persistent && !l) {
    const g = await HS({
      attacker: i,
      attack: (e == null ? void 0 : e.attack) ?? {},
      targetResult: s[0] ?? null
    });
    l = (g == null ? void 0 : g.uuid) ?? null;
  }
  return {
    targetCount: r.length,
    results: s,
    summary: wC(s),
    areaEffect: o,
    templateGeometry: ji(ct(
      (m = e == null ? void 0 : e.attack) == null ? void 0 : m.templateGeometry,
      {
        template: (f = e == null ? void 0 : e.attack) == null ? void 0 : f.template,
        placement: (p = e == null ? void 0 : e.attack) == null ? void 0 : p.templatePlacement
      }
    )),
    persistentRegionUuid: l
  };
}
function ut(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? Math.trunc(t) : e;
}
function rl(i, e = 0, t = Number.POSITIVE_INFINITY) {
  const n = ut(i, e);
  return Math.max(e, Math.min(t, n));
}
function ch(i, e = 1) {
  var n;
  const t = ut((n = i == null ? void 0 : i.difficulty) == null ? void 0 : n.dn, ut(e, 1));
  return Math.max(0, t);
}
function TC(i, e) {
  return Math.max(0, ut(i, 0) - ut(e, 0));
}
function kC({ convert: i, remainder: e, rate: t = 4 } = {}) {
  const n = Math.max(0, ut(e, 0)), a = Math.max(1, ut(t, 4)), r = Math.max(0, ut(i, 0)), s = Math.floor(r / a) * a;
  return Math.min(n, s);
}
function zu(i, { rate: e = 4, maxPerRoll: t = Number.POSITIVE_INFINITY } = {}) {
  const n = Math.max(1, ut(e, 4)), a = Math.floor(Math.max(0, ut(i, 0)) / n), r = Number.isFinite(t) ? Math.max(0, ut(t, 0)) : Number.POSITIVE_INFINITY;
  return { amount: Math.min(a, r), rate: n };
}
function Fu(i) {
  var n;
  const e = ((n = i == null ? void 0 : i.edge) == null ? void 0 : n.earn) ?? {};
  return {
    enabled: !!(e != null && e.enabled),
    rate: Math.max(1, ut(e == null ? void 0 : e.rate, 4)),
    maxPerRoll: (e == null ? void 0 : e.maxPerRoll) ?? Number.POSITIVE_INFINITY
  };
}
function Ys(i) {
  var t;
  const e = (t = i == null ? void 0 : i.edge) == null ? void 0 : t.pool;
  return e ? String(e) : null;
}
function MC(i) {
  let e = 0, t = 0;
  const n = (a) => {
    if (!a) return;
    const r = a == null ? void 0 : a.results;
    if (Array.isArray(r))
      for (const o of r) {
        if ((o == null ? void 0 : o.active) === !1) continue;
        const l = Number(o == null ? void 0 : o.result);
        Number.isFinite(l) && (e += 1, l === 1 && (t += 1));
      }
    const s = a == null ? void 0 : a.terms;
    if (Array.isArray(s))
      for (const o of s) n(o);
    if (Array.isArray(a))
      for (const o of a) n(o);
  };
  return n(i), { dice: e, ones: t };
}
function uh(i, e) {
  if (ut(i, 0) !== 0) return !1;
  const { dice: t, ones: n } = MC(e);
  return t <= 0 ? !1 : n >= Math.ceil(t / 2);
}
function CC(i, e, t = 4) {
  return !!(i && ut(e, 0) >= ut(t, 4));
}
function Am(i, e) {
  const t = ut(e == null ? void 0 : e.successes, 0), n = ch(i, 1), a = t >= n, r = t - n, s = CC(a, r, 4), o = uh(t, e == null ? void 0 : e.raw), l = Fu(i), c = l.maxPerRoll ?? 1, u = l.enabled && r >= l.rate ? (() => {
    const { amount: m, rate: f } = zu(r, { rate: l.rate, maxPerRoll: c }), p = Ys(i);
    return m > 0 ? { amount: m, pool: p, reason: "net4", details: { margin: r, rate: f } } : null;
  })() : null;
  return {
    rollType: "simple",
    passed: a,
    successes: t,
    difficulty: { dn: n },
    margin: r,
    criticalSuccess: s,
    criticalFailure: o,
    tier: s ? "criticalSuccess" : o ? "criticalFailure" : a ? "success" : "failure",
    edgeEarned: u
  };
}
function vC(i, e, t) {
  var m, f;
  const n = ut(e == null ? void 0 : e.successes, 0), a = ut(t == null ? void 0 : t.successes, 0), r = !!((m = i == null ? void 0 : i.opposed) != null && m.net), s = String(((f = i == null ? void 0 : i.opposed) == null ? void 0 : f.dnTies) ?? "stalemate");
  let o = null, l = !1;
  r ? (o = n - a, o > 0 ? l = !0 : o < 0 ? l = !1 : s === "attackerWins" ? l = !0 : l = !1) : n > a ? l = !0 : n < a ? l = !1 : s === "attackerWins" ? l = !0 : l = !1;
  const c = Fu(i), u = c.maxPerRoll ?? 1, d = c.enabled && r && typeof o == "number" && o >= c.rate ? (() => {
    const { amount: p, rate: g } = zu(o, { rate: c.rate, maxPerRoll: u }), h = Ys(i);
    return p > 0 ? { amount: p, pool: h, reason: "net4", details: { netHits: o, rate: g } } : null;
  })() : null;
  return {
    rollType: "opposed",
    passed: l,
    successes: n,
    opposed: {
      attacker: n,
      defender: a,
      netEnabled: r,
      netHits: r ? o : void 0,
      tiePolicy: s
    },
    edgeEarned: d
  };
}
function EC(i, e) {
  var g, h, y;
  const t = ut(e == null ? void 0 : e.successes, 0), n = ch(i, 1), a = t >= n, r = uh(t, e == null ? void 0 : e.raw), s = TC(t, n), o = ((g = i == null ? void 0 : i.net) == null ? void 0 : g.convert) ?? ((h = i == null ? void 0 : i.allocation) == null ? void 0 : h.convert) ?? 0, l = Fu(i), c = l.rate, u = kC({ convert: o, remainder: s, rate: c }), d = s - u, m = l.enabled && u >= c ? (() => {
    const { amount: b } = zu(u, { rate: c, maxPerRoll: l.maxPerRoll }), S = Ys(i);
    return b > 0 ? { amount: b, pool: S, reason: "convert4", details: { converted: u, rate: c } } : null;
  })() : null, f = r ? { amount: 1, pool: Ys(i), reason: "critFail", details: { onesRule: "halfOrMoreOnes" } } : null, p = [];
  return m && p.push(m), f && p.push(f), p.length === 0 || (p.length === 1 ? p[0] : (p.reduce((b, S) => b + (Number(S == null ? void 0 : S.amount) || 0), 0), (y = p[0]) == null || y.pool)), {
    rollType: "net",
    passed: a,
    successes: t,
    difficulty: { dn: n },
    criticalFailure: r,
    tier: r ? "criticalFailure" : a ? "success" : "failure",
    net: {
      remainder: s,
      convertRequested: ut(o, 0),
      converted: u,
      value: d,
      // the actual net effect magnitude you apply (+net dice, etc.)
      rate: c,
      canConvert: s >= c
    },
    edgeEarned: m
  };
}
function IC(i, e) {
  var o, l, c, u;
  const t = ut(e == null ? void 0 : e.successes, 0), n = rl((o = i == null ? void 0 : i.extended) == null ? void 0 : o.target, 1, 1e4), a = rl((l = i == null ? void 0 : i.extended) == null ? void 0 : l.accumulated, 0, 1e4), r = rl(a + t, 0, 1e4), s = r >= n;
  return {
    rollType: "extended",
    passed: s,
    successes: t,
    extended: {
      target: n,
      accumulated: a,
      nextAccumulated: r,
      remaining: Math.max(0, n - r),
      completed: s,
      interval: ((c = i == null ? void 0 : i.extended) == null ? void 0 : c.interval) ?? null,
      failureLimit: ((u = i == null ? void 0 : i.extended) == null ? void 0 : u.failureLimit) ?? null
    },
    // NOTE: Edge earn rules for extended are intentionally omitted per your earlier constraint.
    edgeEarned: null
  };
}
function dh(i, e, t = null) {
  var a;
  switch (String((i == null ? void 0 : i.rollType) ?? "simple")) {
    case "simple":
      return Am(i, e);
    case "opposed":
      return vC(i, e, t);
    case "net":
      return EC(i, e);
    case "extended":
      return IC(i, e);
    default: {
      const r = {
        ...i,
        difficulty: { dn: Number(((a = i == null ? void 0 : i.difficulty) == null ? void 0 : a.dn) ?? 1) || 1 }
      };
      return Am(r, e);
    }
  }
}
function NC(i, e) {
  var c, u, d, m, f, p, g, h, y;
  const t = i ?? {}, n = Array.isArray((c = t == null ? void 0 : t.modifiers) == null ? void 0 : c.applied) ? t.modifiers.applied : [], a = Number(((u = t == null ? void 0 : t.modifiers) == null ? void 0 : u.total) ?? 0);
  if (n.length) {
    const b = Array.isArray(t == null ? void 0 : t.breakdownRows) ? t.breakdownRows.find((S) => S.id === "mods.total") : null;
    e.metaRows.push({
      text: `Mods: ${n.map((S) => `${S.label} ${wm(S.value)}`).join(", ")} (Total ${wm(a)})`,
      title: (b == null ? void 0 : b.tooltip) ?? ""
    });
  }
  const r = (t == null ? void 0 : t.edge) ?? null, s = Array.isArray((d = t == null ? void 0 : t.roll) == null ? void 0 : d.failureDiceRefs) ? t.roll.failureDiceRefs : [], o = !!((m = r == null ? void 0 : r.availableActions) != null && m.canPostRerollFailures), l = Array.isArray((f = r == null ? void 0 : r.allowed) == null ? void 0 : f.postPools) ? r.allowed.postPools : [];
  if (r != null && r.domain && (e.edge = {
    domain: r.domain,
    earned: ((p = t == null ? void 0 : t.outcomeModel) == null ? void 0 : p.edgeEarned) ?? null,
    preSpent: Number(((g = r == null ? void 0 : r.pre) == null ? void 0 : g.spent) ?? 0),
    postSpent: Number(((h = r == null ? void 0 : r.post) == null ? void 0 : h.spent) ?? 0),
    canPost: o && s.length > 0 && l.length > 0,
    failureCount: s.length,
    postPools: l
  }, e.metaRows.push({
    text: `Edge: ${r.domain} • pre ${e.edge.preSpent} • post ${e.edge.postSpent}`,
    title: ""
  })), (y = e.edge) != null && y.canPost) {
    e.footerRows.push({
      text: `Post-spend: Reroll ${e.edge.failureCount} failure${e.edge.failureCount === 1 ? "" : "s"}`
    });
    for (const b of e.edge.postPools)
      e.actions.push({
        action: "edgePostReroll",
        label: `Spend ${b}`,
        dataset: { "pool-key": b },
        cssClass: "mwd-edge-post"
      });
  }
}
function wm(i) {
  const e = Number(i ?? 0);
  return e >= 0 ? `+${e}` : `${e}`;
}
function RC(i, e) {
  var T, k, C, I, O, x, B, P, L, D, G, V, Z, E, X, H, he, ie, le, ze, Xe, mt, ft, pt, gt, nt, ht, Ye, at, Ct, _t, xt, St, Kt, Ft, R, U, ne, ke, Me, xe, Ze, $e, Fe, $t, yt, vt, Gt, At, Ht, st, ce, Re;
  const t = i ?? {}, n = (t == null ? void 0 : t.attackResult) ?? null;
  if (!n) return;
  const a = ((k = (T = t == null ? void 0 : t.attack) == null ? void 0 : T.weapon) == null ? void 0 : k.attackSummary) ?? null, r = ((I = (C = t == null ? void 0 : t.attack) == null ? void 0 : C.weapon) == null ? void 0 : I.machineWeaponGroup) ?? null, s = Array.isArray(n == null ? void 0 : n.results) ? n.results : [], o = (n == null ? void 0 : n.summary) ?? DC(s), l = s.some((_) => {
    var Q;
    return !!((Q = _ == null ? void 0 : _.queuedMutation) != null && Q.applied);
  }), c = s.filter(
    (_) => (_ == null ? void 0 : _.queuedMutation) && !_.queuedMutation.applied
  ), u = !!((x = (O = t == null ? void 0 : t.attack) == null ? void 0 : O.capabilityReport) != null && x.isTemplated), d = ((B = t == null ? void 0 : t.attack) == null ? void 0 : B.ewContext) ?? null;
  if (d != null && d.detectionState && d.detectionState !== "contact") {
    const _ = [`EW: ${d.detectionStateLabel ?? d.detectionState}`];
    d.targetingDataValue && _.push(`+${d.targetingDataValue} targeting`), e.metaRows.push({ text: _.join(" | "), title: "" });
  }
  const m = Math.max(0, Number(((L = (P = t == null ? void 0 : t.attack) == null ? void 0 : P.weapon) == null ? void 0 : L.clusteringDice) ?? 0) || 0), f = Number(((G = (D = t == null ? void 0 : t.attack) == null ? void 0 : D.weapon) == null ? void 0 : G.clusteringTargetNumber) ?? 5) || 5;
  if (m > 0) {
    const _ = Array.isArray((E = (Z = (V = t == null ? void 0 : t.attack) == null ? void 0 : V.weapon) == null ? void 0 : Z.clusteringModifiers) == null ? void 0 : E.sourceNames) ? t.attack.weapon.clusteringModifiers.sourceNames.filter(Boolean) : [];
    e.metaRows.push({
      text: `Clustering: ${m}d6 @ ${f}+${_.length ? ` | Control: ${_.join(", ")}` : ""}`,
      title: ""
    });
  }
  if (r != null && r.id) {
    const _ = Array.isArray(r == null ? void 0 : r.weaponNames) ? r.weaponNames.filter(Boolean) : [];
    e.metaRows.push({
      text: `Group: ${((H = (X = t == null ? void 0 : t.attack) == null ? void 0 : X.weapon) == null ? void 0 : H.name) ?? "Weapon Group"}${_.length ? ` | Members: ${_.join(", ")}` : ""}`,
      title: ""
    }), a && (e.metaRows.push({
      text: `Profile: ${a.damage ?? 0} damage${Number(a.clusteringDice ?? 0) ? ` | ${Number(a.clusteringDice ?? 0)}d6 cluster @ ${f}+` : ""} | AP ${a.ap ?? 0} | Heat ${a.heat ?? 0} | ${a.damageTypeLabel ?? a.damageType ?? "Damage"} | Range Cap ${oe(a.rangeCap ?? "")}`,
      title: ""
    }), e.footerRows.push({
      text: `Attack Ratings: ${OC(a.attackRatings ?? {})}`,
      title: ""
    }));
  }
  const p = Array.isArray((he = t == null ? void 0 : t.modifiers) == null ? void 0 : he.applied) ? t.modifiers.applied : [], g = Number(((ie = t == null ? void 0 : t.modifiers) == null ? void 0 : ie.total) ?? 0);
  if (p.length) {
    const _ = Array.isArray(t == null ? void 0 : t.breakdownRows) ? t.breakdownRows.find((Q) => Q.id === "mods.total") : null;
    e.metaRows.push({
      text: `Mods: ${p.map((Q) => `${Q.label} ${Ma(Q.value)}`).join(", ")} (Total ${Ma(g)})`,
      title: (_ == null ? void 0 : _.tooltip) ?? ""
    });
  }
  const h = (t == null ? void 0 : t.edge) ?? null, y = Array.isArray((le = t == null ? void 0 : t.roll) == null ? void 0 : le.failureDiceRefs) ? t.roll.failureDiceRefs : [], b = !!((ze = h == null ? void 0 : h.availableActions) != null && ze.canPostRerollFailures) && !l, S = Array.isArray((Xe = h == null ? void 0 : h.allowed) == null ? void 0 : Xe.postPools) ? h.allowed.postPools : [];
  if (h != null && h.domain && (e.edge = {
    domain: h.domain,
    earned: ((mt = t == null ? void 0 : t.outcomeModel) == null ? void 0 : mt.edgeEarned) ?? null,
    preSpent: Number(((ft = h == null ? void 0 : h.pre) == null ? void 0 : ft.spent) ?? 0),
    postSpent: Number(((pt = h == null ? void 0 : h.post) == null ? void 0 : pt.spent) ?? 0),
    canPost: b && y.length > 0 && S.length > 0,
    failureCount: y.length,
    postPools: S
  }, e.metaRows.push({
    text: `Edge: ${h.domain} | pre ${e.edge.preSpent} | post ${e.edge.postSpent}`,
    title: ""
  })), (gt = e.edge) != null && gt.canPost) {
    e.footerRows.push({
      text: `Post-spend: Reroll ${e.edge.failureCount} failure${e.edge.failureCount === 1 ? "" : "s"}`
    });
    for (const _ of e.edge.postPools)
      e.actions.push({
        action: "edgePostReroll",
        label: `Spend ${_}`,
        dataset: { "pool-key": _ },
        cssClass: "mwd-edge-post"
      });
  }
  const w = String((o == null ? void 0 : o.overallOutcome) ?? "").trim();
  if (e.outcomeText = s.length > 1 ? `ATTACK ${o.hits} HIT / ${o.grazes} GRAZE / ${o.misses} MISS` : w === "hit" ? "HIT!" : w === "graze" ? "GRAZE!" : "MISS!", e.metaRows.push({
    text: `Targets: ${s.length || 0}`,
    title: ""
  }), u && (e.targetRows = s.map((_, Q) => {
    var qa, Va, ee, ge, dn, fi, mn, Zu;
    const de = ((qa = t == null ? void 0 : t.areaEffectPreviewState) == null ? void 0 : qa[_ == null ? void 0 : _.previewKey]) ?? {}, fe = ((Va = _ == null ? void 0 : _.damage) == null ? void 0 : Va.exposure) ?? (_ == null ? void 0 : _.exposure) ?? null, et = String((fe == null ? void 0 : fe.initialLabel) ?? "NONE").trim() || "NONE", Wt = String((fe == null ? void 0 : fe.finalLabel) ?? et).trim() || et, Si = Number(((ee = _ == null ? void 0 : _.damage) == null ? void 0 : ee.incoming) ?? 0), Di = Number(((ge = _ == null ? void 0 : _.damage) == null ? void 0 : ge.scaledIncoming) ?? Si), ot = (_ == null ? void 0 : _.queuedMutation) ?? null, Ge = !!(ot != null && ot.applied || (dn = _ == null ? void 0 : _.damageResult) != null && dn.applied), mi = (de == null ? void 0 : de.reactionPreview) ?? null, $n = [];
    if (!Ge && et !== "NONE" && ((fi = _ == null ? void 0 : _.damageResult) != null && fi.ok) && !((mn = _ == null ? void 0 : _.damageResult) != null && mn.skipped) && $n.push({
      action: "toggleEvade",
      label: _ != null && _.evadeActive ? "Clear Evade" : "Use Reaction",
      dataset: { "preview-key": _.previewKey },
      cssClass: `mwd-target-row__action ${_ != null && _.evadeActive ? "is-active" : ""}`
    }), _ != null && _.evadeActive && (mi != null && mi.canSpendEdge) && Array.isArray(mi.edgePools))
      for (const Ya of mi.edgePools)
        $n.push({
          action: "toggleEvadeEdge",
          label: (de == null ? void 0 : de.edgePoolKey) === Ya.key ? `Edge: ${Ya.key}` : `Use ${Ya.key}`,
          dataset: {
            "preview-key": _.previewKey,
            "pool-key": Ya.key
          },
          cssClass: `mwd-target-row__action ${(de == null ? void 0 : de.edgePoolKey) === Ya.key ? "is-active" : ""}`
        });
    return ot && !Ge && $n.push({
      action: "applyAttackDamage",
      label: "Apply Damage",
      dataset: { "result-index": String(Q) },
      cssClass: "mwd-target-row__action mwd-apply-attack-damage"
    }), {
      targetName: ((Zu = _ == null ? void 0 : _.target) == null ? void 0 : Zu.name) ?? "Target",
      applied: Ge,
      outcomeLabel: String((_ == null ? void 0 : _.outcome) ?? "miss").toUpperCase(),
      exposureLabel: et === Wt ? et : `${et} -> ${Wt}`,
      damageLabel: Si === Di ? String(Di) : `${Si} -> ${Di}`,
      reactionHint: _ != null && _.evadeActive ? de != null && de.edgePoolKey ? "Evade active. Reaction Burn canceled by Edge." : (mi == null ? void 0 : mi.burnDelta) > 0 ? `Evade active. This reaction adds +${mi.burnDelta} Burn.` : "Evade active." : "",
      rowActions: $n
    };
  })), s.length > 1 && c.length > 1 && e.actions.push({
    action: "applyAllAttackDamage",
    label: `Apply All Damage (${c.length})`,
    cssClass: "mwd-apply-all-attack-damage"
  }), !u)
    for (const _ of s) {
      const Q = Number(((ht = (nt = _ == null ? void 0 : _.cq) == null ? void 0 : nt.ar) == null ? void 0 : ht.total) ?? 0), de = Number(((at = (Ye = _ == null ? void 0 : _.cq) == null ? void 0 : Ye.dr) == null ? void 0 : at.total) ?? 0);
      e.metaRows.push({
        text: `${((Ct = _ == null ? void 0 : _.target) == null ? void 0 : Ct.name) ?? "Target"}: ${String((_ == null ? void 0 : _.outcome) ?? "miss").toUpperCase()} | CQ ${Ma(((_t = _ == null ? void 0 : _.cq) == null ? void 0 : _t.value) ?? 0)} (AR ${Q} - DR ${de}) | Net ${Number((_ == null ? void 0 : _.netHits) ?? 0)}`,
        title: PC(_ == null ? void 0 : _.cq)
      });
    }
  if (!u)
    for (const [_, Q] of s.entries()) {
      const de = (Q == null ? void 0 : Q.damage) ?? null;
      if (de && (Q == null ? void 0 : Q.outcome) !== "miss") {
        const et = Number(((xt = de == null ? void 0 : de.clustering) == null ? void 0 : xt.damageBonus) ?? ((St = de == null ? void 0 : de.clustering) == null ? void 0 : St.hits) ?? 0), Wt = Number((de == null ? void 0 : de.netDamageBonus) ?? (de == null ? void 0 : de.netHits) ?? 0);
        e.footerRows.push({
          text: `${((Kt = Q == null ? void 0 : Q.target) == null ? void 0 : Kt.name) ?? "Target"}: ${de.damageTypeLabel} ${Ma(de.effectiveWeaponDamage)} weapon${et ? ` + ${et} cluster` : ""}${Wt ? ` + ${Wt} net` : ""}`,
          title: ""
        }), Number(((Ft = de == null ? void 0 : de.clustering) == null ? void 0 : Ft.dice) ?? 0) > 0 && e.footerRows.push({
          text: `${((R = Q == null ? void 0 : Q.target) == null ? void 0 : R.name) ?? "Target"}: Cluster ${Number(de.clustering.dice ?? 0)}d6 @ ${Number(de.clustering.targetNumber ?? 5) || 5}+ -> ${Number(de.clustering.hits ?? 0)} hit${Number(de.clustering.hits ?? 0) === 1 ? "" : "s"}`,
          title: ""
        });
      }
      const fe = (Q == null ? void 0 : Q.damageResult) ?? null;
      if (fe != null && fe.ok && !(fe != null && fe.skipped)) {
        const et = (Q == null ? void 0 : Q.queuedMutation) ?? (fe == null ? void 0 : fe.queuedMutation) ?? null, Wt = !!(et != null && et.applied || fe != null && fe.applied);
        if (fe.mode === "machineAttackDamage") {
          const Si = fe.machine ?? {}, Di = fe.hitLocation ?? {}, ot = fe.degradation ?? null;
          e.footerRows.push({
            text: `${((U = Q == null ? void 0 : Q.target) == null ? void 0 : U.name) ?? "Target"}: Location ${Di.locationLabel ?? "Location"}${Di.rollTotal ? ` (${Di.rollTotal})` : ""} | Armor ${Number(Si.armorBefore ?? 0)} -> ${Number(Si.armorAfter ?? 0)} | Structure ${Number(Si.structureBefore ?? 0)} -> ${Number(Si.structureAfter ?? 0)}`,
            title: ""
          }), ot != null && ot.summary && e.footerRows.push({
            text: `${((ne = Q == null ? void 0 : Q.target) == null ? void 0 : ne.name) ?? "Target"}: Shock ${Number(ot.summary.shockBefore ?? 0)} -> ${Number(ot.summary.shockAfter ?? 0)} | Threshold ${Number(ot.summary.threshold ?? 0)} | Reliability ${Number(ot.summary.reliability ?? 0)} | Reserve ${Number(ot.summary.reliabilitySpendableBefore ?? 0)} -> ${Number(ot.summary.reliabilitySpendableAfter ?? 0)}`,
            title: ""
          }), (ke = fe.critical) != null && ke.automatic ? e.footerRows.push({
            text: `${((Me = Q == null ? void 0 : Q.target) == null ? void 0 : Me.name) ?? "Target"}: Automatic critical pending`,
            title: ""
          }) : (xe = fe.critical) != null && xe.optional ? e.footerRows.push({
            text: `${((Ze = Q == null ? void 0 : Q.target) == null ? void 0 : Ze.name) ?? "Target"}: Chaos Edge can convert this location hit to a critical`,
            title: ""
          }) : e.footerRows.push({
            text: `${(($e = Q == null ? void 0 : Q.target) == null ? void 0 : $e.name) ?? "Target"}: Location hit is descriptive only`,
            title: ""
          });
          for (const Ge of ((Fe = fe.critical) == null ? void 0 : Fe.records) ?? [])
            e.footerRows.push({
              text: `${(($t = Q == null ? void 0 : Q.target) == null ? void 0 : $t.name) ?? "Target"}: Critical - ${Ge.label}${Ge.locationLabel ? ` (${Ge.locationLabel})` : ""} | ${ng(Ge)}`,
              title: ""
            }), Wt && Ge.active !== !1 && Ge.remedyKey !== "none" && e.actions.push({
              action: "machineCritRemedy",
              label: `Remedy: ${Ge.label}`,
              dataset: {
                "machine-actor-uuid": ((yt = Q == null ? void 0 : Q.target) == null ? void 0 : yt.actorUuid) ?? "",
                "crit-id": Ge.id,
                "remedy-key": Ge.remedyKey,
                "gm-override": "true"
              },
              cssClass: "mwd-machine-crit-remedy"
            });
          if (et && !Wt && Array.isArray(ot == null ? void 0 : ot.spendOpportunities))
            for (const Ge of ot.spendOpportunities)
              Ge != null && Ge.canSpend && e.actions.push({
                action: "toggleMachineReliabilitySpend",
                label: Ge.selected ? `Clear Reliability Spend: ${Ge.location}` : `Spend Reliability: ${Ge.location}`,
                dataset: {
                  "result-index": String(_),
                  "spend-index": String(Ge.index)
                },
                cssClass: `mwd-toggle-machine-reliability ${Ge.selected ? "is-active" : ""}`
              });
        }
        et && !Wt && ((vt = fe == null ? void 0 : fe.critical) != null && vt.optional) && e.actions.push({
          action: "toggleMachineChaosCrit",
          label: (Gt = et.payload) != null && Gt.chaosCriticalSelected ? `Clear Chaos Critical: ${fe.actorName ?? ((At = Q == null ? void 0 : Q.target) == null ? void 0 : At.name) ?? "Target"}` : `Spend Chaos Edge: ${fe.actorName ?? ((Ht = Q == null ? void 0 : Q.target) == null ? void 0 : Ht.name) ?? "Target"}`,
          dataset: { "result-index": String(_) },
          cssClass: `mwd-toggle-machine-chaos ${(st = et.payload) != null && st.chaosCriticalSelected ? "is-active" : ""}`
        }), et && !Wt && e.actions.push({
          action: "applyAttackDamage",
          label: `Apply Damage: ${fe.actorName ?? ((ce = Q == null ? void 0 : Q.target) == null ? void 0 : ce.name) ?? "Target"}`,
          dataset: { "result-index": String(_) },
          cssClass: "mwd-apply-attack-damage"
        });
      } else fe != null && fe.reason && e.footerRows.push({
        text: `${((Re = Q == null ? void 0 : Q.target) == null ? void 0 : Re.name) ?? "Target"}: ${fe.reason}`,
        title: ""
      });
    }
}
function PC(i = {}) {
  var n, a;
  const e = Array.isArray((n = i == null ? void 0 : i.ar) == null ? void 0 : n.parts) ? i.ar.parts : [], t = Array.isArray((a = i == null ? void 0 : i.dr) == null ? void 0 : a.parts) ? i.dr.parts : [];
  return [
    ...e.map((r) => `AR - ${r.label}: ${Ma(r.value)}`),
    ...t.map((r) => `DR - ${r.label}: ${Ma(r.value)}`)
  ].join(`
`);
}
function DC(i = []) {
  const e = { hits: 0, grazes: 0, misses: 0 };
  for (const t of i)
    (t == null ? void 0 : t.outcome) === "hit" ? e.hits += 1 : (t == null ? void 0 : t.outcome) === "graze" ? e.grazes += 1 : e.misses += 1;
  return {
    ...e,
    overallOutcome: e.hits > 0 ? "hit" : e.grazes > 0 ? "graze" : "miss"
  };
}
function Ma(i) {
  const e = Number(i ?? 0);
  return e >= 0 ? `+${e}` : `${e}`;
}
function OC(i = {}) {
  return ["close", "near", "far", "extreme"].map((e) => `${oe(e)} ${Number((i == null ? void 0 : i[e]) ?? 0) || 0}`).join(" | ");
}
function LC(i, e) {
  var c;
  const t = i ?? {}, n = (t == null ? void 0 : t.outcomeModel) ?? {}, a = (n == null ? void 0 : n.net) ?? null;
  if (!a) return;
  e.net = a;
  const r = Number((a == null ? void 0 : a.converted) ?? 0), s = Number((a == null ? void 0 : a.value) ?? 0), o = Number((a == null ? void 0 : a.rate) ?? 4);
  e.metaRows.push({
    text: `Net: ${s} • Converted: ${r} • Rate: ${o}`,
    title: ""
  });
  const l = ((c = n == null ? void 0 : n.edgeEarned) == null ? void 0 : c.amount) > 0 ? n.edgeEarned : null;
  l && e.footerRows.push({
    text: `Edge Earned: +${l.amount}${l.pool ? ` (${l.pool})` : ""}`,
    title: l.reason ?? ""
  });
}
function _C(i, e) {
  var l, c, u, d;
  const t = i ?? {}, n = (t == null ? void 0 : t.outcomeModel) ?? {}, a = Number(((l = n == null ? void 0 : n.attacker) == null ? void 0 : l.successes) ?? (n == null ? void 0 : n.attackerHits) ?? NaN), r = Number(((c = n == null ? void 0 : n.defender) == null ? void 0 : c.successes) ?? (n == null ? void 0 : n.defenderHits) ?? NaN), s = Number((n == null ? void 0 : n.netHits) ?? (n == null ? void 0 : n.net) ?? NaN);
  Number.isFinite(a) && Number.isFinite(r) && e.metaRows.push({ text: `Opposed: Att ${a} vs Def ${r} • Net ${Number.isFinite(s) ? s : a - r}` }), (u = t == null ? void 0 : t.incoming) != null && u.label && (e.incoming = { label: t.incoming.label, value: t.incoming.value ?? "" }, e.footerRows.push({ text: `Incoming: ${e.incoming.label} ${e.incoming.value}` }));
  const o = ((d = n == null ? void 0 : n.edgeEarned) == null ? void 0 : d.amount) > 0 ? n.edgeEarned : null;
  o && e.footerRows.push({ text: `Edge Earned: +${o.amount}${o.pool ? ` (${o.pool})` : ""}` });
}
function xC(i, e) {
  var c;
  const t = i ?? {}, n = (t == null ? void 0 : t.outcomeModel) ?? {}, a = (n == null ? void 0 : n.extended) ?? null;
  if (!a) return;
  e.extended = a;
  const r = Number((a == null ? void 0 : a.progress) ?? 0), s = Number((a == null ? void 0 : a.target) ?? 0), o = Number((a == null ? void 0 : a.remaining) ?? Math.max(0, s - r));
  e.metaRows.push({
    text: `Extended: ${r}/${s} (Remaining ${o})`,
    title: ""
  }), a != null && a.completed && e.footerRows.push({ text: `Completed in ${Number((a == null ? void 0 : a.rounds) ?? (a == null ? void 0 : a.attempts) ?? 0) || "?"} attempts.` });
  const l = ((c = n == null ? void 0 : n.edgeEarned) == null ? void 0 : c.amount) > 0 ? n.edgeEarned : null;
  l && e.footerRows.push({ text: `Edge Earned: +${l.amount}${l.pool ? ` (${l.pool})` : ""}` });
}
function $C(i, e) {
  const t = (i == null ? void 0 : i.machineRemedy) ?? null, n = (i == null ? void 0 : i.machineRemedyResult) ?? null;
  if (t && (e.metaRows.push({
    text: `Problem: ${t.critLabel}${t.locationLabel ? ` | ${t.locationLabel}` : ""}`,
    title: ""
  }), e.metaRows.push({
    text: `Pool: Reliability + ${t.skillLabel}`,
    title: ""
  }), e.footerRows.push({
    text: `DN ${Number(t.baseDn ?? 0)} base + ${Number(t.conditionModifier ?? 0)} condition (${t.conditionLabel})`,
    title: ""
  }), !!n)) {
    if (n.ok && n.passed && n.applied) {
      e.outcomeText = "REMEDY SUCCESS!", e.footerRows.push({
        text: `${t.remedyLabel} cleared ${t.critLabel}.`,
        title: ""
      });
      return;
    }
    if (n.ok && !n.passed) {
      e.outcomeText = "REMEDY FAILED", e.footerRows.push({
        text: `${t.critLabel} remains active.`,
        title: ""
      });
      return;
    }
    n.ok || e.footerRows.push({
      text: n.reason ?? "Machine remedy could not be applied.",
      title: ""
    });
  }
}
function BC(i, e) {
  const t = (i == null ? void 0 : i.heatDangerCheck) ?? null;
  if (!t) return;
  const n = (i == null ? void 0 : i.heatDangerResult) ?? null;
  if (e.metaRows.push({
    text: `Danger Level ${Number(t.dangerLevel ?? 0)} | Pool ${Number(t.pool ?? 0)}d6 vs DN ${Number(t.dn ?? 0)}`,
    title: ""
  }), t.kind === "shutdown" && (n == null ? void 0 : n.systemOpsRating) !== null && (n == null ? void 0 : n.systemOpsRating) !== void 0) {
    const a = n.operatorName ? `${n.operatorName} ` : "";
    e.metaRows.push({
      text: `${a}System Operations ${Number(n.systemOpsRating ?? 0)}`,
      title: ""
    });
  }
  if (t.kind === "explosion" && !t.volatile && e.metaRows.push({
    text: "No volatile components detected.",
    title: ""
  }), n) {
    zC(t, n, e);
    return;
  }
  if (e.passed) {
    e.outcomeText = t.kind === "shutdown" ? "SHUTDOWN AVOIDED" : "EXPLOSION AVOIDED", e.footerRows.push({ text: t.successText, title: "" });
    return;
  }
  e.outcomeText = t.kind === "shutdown" ? "SHUTDOWN CHECK FAILED" : "EXPLOSION CHECK FAILED", e.footerRows.push({ text: t.failureText, title: "" });
}
function zC(i, e, t) {
  if (i.kind === "shutdown") {
    FC(e, t);
    return;
  }
  HC(e, t);
}
function FC(i, e) {
  i.passed ? e.outcomeText = "SHUTDOWN AVOIDED" : i.overrideAvailable ? e.outcomeText = "SHUTDOWN OVERRIDE AVAILABLE" : e.outcomeText = "SHUTDOWN APPLIED", i.passed || e.metaRows.push({
    text: `Failure Margin ${Number(i.failureMargin ?? 0)}`,
    title: ""
  }), (i.statusApplied || i.statusAlreadyActive) && e.metaRows.push({
    text: "Shutdown status active.",
    title: ""
  }), i.effectText && e.footerRows.push({ text: i.effectText, title: "" }), i.nextStep && e.footerRows.push({ text: i.nextStep, title: "" });
}
function HC(i, e) {
  i.passed ? e.outcomeText = "EXPLOSION AVOIDED" : i.explosionTriggered ? e.outcomeText = "EXPLOSION TRIGGERED" : e.outcomeText = "EXPLOSION NOT APPLIED", i.passed || e.metaRows.push({
    text: `Failure Margin ${Number(i.failureMargin ?? 0)}`,
    title: ""
  }), i.effectText && e.footerRows.push({ text: i.effectText, title: "" }), i.nextStep && e.footerRows.push({ text: i.nextStep, title: "" });
}
function UC(i, e) {
  const t = (i == null ? void 0 : i.ewAcquireResult) ?? null, n = (i == null ? void 0 : i.acquire) ?? null;
  n != null && n.currentState && e.metaRows.push({
    text: `Target State: ${vi(n.currentState)}`,
    title: ""
  }), t && (t.ok ? (e.outcomeText = `${vi(t.previousState).toUpperCase()} → ${vi(t.newState).toUpperCase()}`, e.footerRows.push({
    text: `Detection: ${vi(t.newState)}`,
    title: ""
  }), t.hitCeiling && e.footerRows.push({
    text: `Ceiling: ${vi(t.ceiling)} (ECM active)`,
    title: ""
  })) : (e.outcomeText = "ACQUIRE FAILED", e.footerRows.push({
    text: t.reason ?? "Acquire roll failed — state unchanged.",
    title: ""
  })));
}
function jC(i, e) {
  const t = (i == null ? void 0 : i.ewTargetingResult) ?? null, n = (i == null ? void 0 : i.targeting) ?? null;
  if (n != null && n.detectionStateLabel && e.metaRows.push({
    text: `State: ${n.detectionStateLabel} | Cap: ${Number(n.cap ?? 0)}`,
    title: ""
  }), !!t)
    if (t.ok) {
      const a = t.rawHits !== t.packetValue;
      e.outcomeText = `+${t.packetValue} dice${a ? ` (capped from ${t.rawHits})` : ""}`, e.footerRows.push({
        text: `Targeting data stored: +${t.packetValue} dice`,
        title: ""
      }), t.hitCap && e.footerRows.push({
        text: `Packet capped at ${t.packetValue} (raw: ${t.rawHits})`,
        title: ""
      });
    } else
      e.outcomeText = "TARGETING FAILED", e.footerRows.push({
        text: t.reason ?? "Targeting roll failed — no data stored.",
        title: ""
      });
}
const KC = {
  skill: NC,
  attack: RC,
  net: LC,
  opposed: _C,
  extended: xC,
  machineRemedy: $C,
  heatDangerCheck: BC,
  acquire: UC,
  targeting: jC
  // defense: enhanceDefense,
  // edge: enhanceEdge,
};
async function ga({ resolved: i } = {}) {
  const e = i ?? {}, t = GC(e), n = KC[t.intent];
  return typeof n == "function" && n(e, t), await foundry.applications.handlebars.renderTemplate(
    "mwd.v2.roll.mwd-roll-card",
    t
  );
}
function GC(i) {
  var f, p, g, h, y, b, S, w, T, k, C, I, O, x, B, P, L;
  const e = i ?? {}, t = Number(((f = e == null ? void 0 : e.roll) == null ? void 0 : f.target) ?? 5), n = Number(((p = e == null ? void 0 : e.dn) == null ? void 0 : p.total) ?? ((h = (g = e == null ? void 0 : e.ctxSnapshot) == null ? void 0 : g.dn) == null ? void 0 : h.total) ?? ((b = (y = e == null ? void 0 : e.ctxSnapshot) == null ? void 0 : y.difficulty) == null ? void 0 : b.dn) ?? 0), a = Number(((S = e == null ? void 0 : e.roll) == null ? void 0 : S.pool) ?? 0), r = Number(((w = e == null ? void 0 : e.outcome) == null ? void 0 : w.hits) ?? 0), s = (e == null ? void 0 : e.outcomeModel) ?? {}, o = typeof s.passed == "boolean" ? s.passed : r >= n, l = Number.isFinite(Number(s.margin)) ? Number(s.margin) : r - n, c = s.tier ?? null, u = Array.isArray(e == null ? void 0 : e.breakdownRows) ? e.breakdownRows.map((D) => `${D.label}: ${D.value}`).join(`
`) : "", d = {
    header: { left: (e == null ? void 0 : e.title) ?? "Roll", right: (e == null ? void 0 : e.subtitle) ?? "" },
    formula: String((e == null ? void 0 : e.formula) ?? "").trim(),
    intent: (e == null ? void 0 : e.intent) ?? "unknown",
    domains: Array.isArray(e == null ? void 0 : e.domains) ? e.domains : [],
    tn: t,
    dn: n,
    pool: a,
    hits: r,
    passed: o,
    margin: l,
    tier: c,
    breakdownTooltip: u,
    metaRows: [],
    targetRows: [],
    actions: [],
    footerRows: [],
    incoming: null,
    edge: null,
    net: null,
    opposed: null,
    extended: null
  }, m = (e == null ? void 0 : e.attack) ?? null;
  if ((T = e == null ? void 0 : e.specialization) != null && T.label && d.metaRows.push({
    text: `Specialization: ${e.specialization.label} (+${Number(e.specialization.value ?? 0)})`,
    title: ""
  }), (k = m == null ? void 0 : m.weapon) != null && k.name) {
    const D = ((C = m == null ? void 0 : m.weapon) == null ? void 0 : C.type) === "personalWeapon" || (I = m == null ? void 0 : m.weapon) != null && I.isSynthetic ? So((m == null ? void 0 : m.rangeBand) ?? "") : String((m == null ? void 0 : m.rangeBand) ?? "").trim(), G = String(((O = m == null ? void 0 : m.weapon) == null ? void 0 : O.damageTypeLabel) ?? ((x = m == null ? void 0 : m.weapon) == null ? void 0 : x.damageType) ?? "").trim(), V = String(((B = m == null ? void 0 : m.payload) == null ? void 0 : B.label) ?? ((P = m == null ? void 0 : m.weapon) == null ? void 0 : P.payloadLabel) ?? "").trim();
    d.metaRows.push({
      text: `Weapon: ${m.weapon.name}${D ? ` • Range: ${D}` : ""}${G ? ` • Type: ${G}` : ""}${V ? ` • Payload: ${V}` : ""}`,
      title: ""
    }), (L = m == null ? void 0 : m.sourceState) != null && L.isTracked && d.footerRows.push({
      text: `Source: ${Number(m.sourceState.current ?? 0)}/${Number(m.sourceState.max ?? 0)}`,
      title: ""
    });
  }
  return d;
}
function WC() {
  Hooks.on("renderChatMessageHTML", (i, e) => {
    e.addEventListener("click", (t) => {
      const n = t.target.closest("[data-mwd-action]");
      if (!n) return;
      const a = String(n.dataset.mwdAction || "").trim();
      a && (a === "edgePostReroll" && pv(t, i), a === "toggleEvade" && nv(t, i), a === "toggleEvadeEdge" && av(t, i), a === "toggleHazardEvade" && uv(t, i), a === "toggleHazardEvadeEdge" && dv(t, i), a === "applyHazardTick" && mv(t, i), a === "toggleMachineChaosCrit" && ov(t, i), a === "toggleMachineReliabilitySpend" && lv(t, i), a === "machineCritRemedy" && cv(t), a === "applyAttackDamage" && tv(t, i), a === "applyAllAttackDamage" && rv(t, i));
    });
  });
}
function qC(i = {}) {
  var t;
  return (Array.isArray((t = i == null ? void 0 : i.attackResult) == null ? void 0 : t.results) ? i.attackResult.results : []).some((n) => {
    var a;
    return !!((a = n == null ? void 0 : n.queuedMutation) != null && a.applied);
  });
}
function VC(i) {
  return i === A.monitors.physical ? "Physical" : i === A.monitors.fatigue ? "Fatigue" : String(i ?? "").trim() || "Track";
}
function YC(i) {
  const e = String(i ?? "").trim().toLowerCase();
  return e === "penetrating" ? "is-penetrating" : e === "energy" ? "is-energy" : e === "thermal" ? "is-thermal" : e === "electrical" ? "is-electrical" : "is-concussive";
}
function JC(i) {
  const e = Math.max(0, Number(i ?? 0) || 0);
  return e <= 0 ? { key: "is-none", label: "No Penetration" } : e <= 2 ? { key: "is-light", label: "Light Damage" } : e <= 4 ? { key: "is-medium", label: "Moderate Damage" } : e <= 7 ? { key: "is-heavy", label: "Heavy Damage" } : { key: "is-critical", label: "Critical Damage" };
}
function QC({ actor: i = null, token: e = null } = {}) {
  var r;
  const t = (e == null ? void 0 : e.document) ?? e ?? null, n = String(((r = t == null ? void 0 : t.texture) == null ? void 0 : r.src) ?? "").trim(), a = String((i == null ? void 0 : i.img) ?? "").trim();
  return n || a || "icons/svg/mystery-man.svg";
}
function XC(i) {
  var t, n;
  const e = (n = (t = game.settings) == null ? void 0 : t.get) == null ? void 0 : n.call(t, "core", "rollMode");
  return typeof ChatMessage.applyRollMode == "function" && ChatMessage.applyRollMode(i, e), i;
}
function ZC({ summary: i = {}, actor: e = null, token: t = null } = {}) {
  var m;
  const n = String((i == null ? void 0 : i.damageType) ?? "").trim(), a = Pi(n || "concussive") || "Damage", r = VC(i == null ? void 0 : i.track), s = Math.max(0, Number((i == null ? void 0 : i.finalDamage) ?? (i == null ? void 0 : i.appliedDelta) ?? 0) || 0), o = JC(s), l = s === 1 ? "1 point" : `${s} points`, c = String((i == null ? void 0 : i.actorName) ?? (e == null ? void 0 : e.name) ?? "Target").trim() || "Target", u = [];
  i != null && i.beforeLabel && (i != null && i.afterLabel) && u.push({
    label: "Monitor",
    value: `${i.beforeLabel} -> ${i.afterLabel}`
  }), u.push({
    label: "Final Damage",
    value: l
  }), Number.isFinite(Number(i == null ? void 0 : i.damageIncoming)) && u.push({
    label: "Incoming",
    value: String(Number(i.damageIncoming ?? 0))
  }), i != null && i.usedArmor && (i != null && i.mitigation) && (u.push({
    label: "Resistance",
    value: String(Number(i.mitigation.netResistance ?? 0))
  }), u.push({
    label: "AP",
    value: String(Number(i.effectiveAp ?? 0))
  }), u.push({
    label: "Armor",
    value: `${Number(i.mitigation.armorBefore ?? 0)} -> ${Number(i.mitigation.armorAfter ?? 0)}`
  }), Number(i.mitigation.reinforcedMax ?? 0) > 0 && u.push({
    label: "Reinforced",
    value: `${Number(i.mitigation.reinforcedBefore ?? 0)} -> ${Number(i.mitigation.reinforcedAfter ?? 0)}`
  })), i != null && i.source && u.push({
    label: "Source",
    value: String(i.source).trim()
  }), i != null && i.notes && u.push({
    label: "Notes",
    value: String(i.notes).trim()
  });
  const d = Array.isArray((m = i == null ? void 0 : i.critical) == null ? void 0 : m.records) ? i.critical.records : [];
  return d.length && u.push({
    label: "Critical Effects",
    value: d.map((f) => `${f.label}${f.locationLabel ? ` (${f.locationLabel})` : ""}: ${ng(f)}`).join(" ; ")
  }), {
    classes: ["mwd-damage-card", YC(n), o.key].join(" "),
    header: {
      left: "Damage Applied",
      right: r
    },
    target: {
      name: c,
      image: QC({ actor: e, token: t })
    },
    damageTypeLabel: a,
    severityLabel: o.label,
    impactValue: s,
    impactText: s > 0 ? `${a} damage applied to ${r}.` : `${a} damage did not penetrate.`,
    rows: u
  };
}
async function Hu({ summary: i = {}, actor: e = null, token: t = null } = {}) {
  const n = await foundry.applications.handlebars.renderTemplate(
    "mwd.v2.roll.mwd-damage-application-card",
    ZC({ summary: i, actor: e, token: t })
  ), a = XC({
    speaker: ChatMessage.getSpeaker({ actor: e, token: t }),
    content: n
  });
  return ChatMessage.create(a);
}
function ev(i = {}) {
  var n, a, r;
  const e = (i == null ? void 0 : i.ctxSnapshot) ?? {}, t = Number(((n = i == null ? void 0 : i.dn) == null ? void 0 : n.total) ?? ((a = e == null ? void 0 : e.dn) == null ? void 0 : a.total) ?? ((r = e == null ? void 0 : e.difficulty) == null ? void 0 : r.dn) ?? 1);
  return {
    intent: (i == null ? void 0 : i.intent) ?? "unknown",
    rollType: (e == null ? void 0 : e.rollType) ?? "simple",
    difficulty: {
      ...e != null && e.difficulty && typeof e.difficulty == "object" ? e.difficulty : {},
      dn: Number.isFinite(t) ? t : 1
    },
    dn: (i == null ? void 0 : i.dn) ?? (e == null ? void 0 : e.dn) ?? null,
    opposed: (e == null ? void 0 : e.opposed) ?? null,
    net: (e == null ? void 0 : e.net) ?? null,
    edge: (e == null ? void 0 : e.edge) ?? null,
    domains: Array.isArray(i == null ? void 0 : i.domains) ? i.domains : [],
    attack: (i == null ? void 0 : i.attack) ?? null
  };
}
async function mh(i = {}, e = null) {
  var r, s, o;
  const t = ev(i), n = Number(((r = i == null ? void 0 : i.outcome) == null ? void 0 : r.hits) ?? 0) || 0, a = ((s = i == null ? void 0 : i.outcomeModel) == null ? void 0 : s.edgeEarned) ?? null;
  return i.outcomeModel = dh(t, { successes: n, raw: (o = i == null ? void 0 : i.roll) == null ? void 0 : o.json }, null), i.outcomeModel.edgeEarned = a, t.intent === "attack" && e && t.attack && (i.attackResult = await lh({
    attacker: e,
    ctx: t,
    outcomeModel: i.outcomeModel,
    previewState: i.areaEffectPreviewState ?? {},
    existingAttackResult: i.attackResult ?? null
  })), i;
}
async function tv(i, e) {
  var o, l, c, u, d, m, f;
  i.preventDefault();
  const t = i.target.closest("[data-mwd-action='applyAttackDamage']"), n = Number((o = t == null ? void 0 : t.dataset) == null ? void 0 : o.resultIndex);
  if (!Number.isInteger(n) || n < 0) return;
  const a = foundry.utils.deepClone((c = (l = e == null ? void 0 : e.flags) == null ? void 0 : l.mwd) == null ? void 0 : c.resolved);
  if (!a) return;
  const r = await fh(a, n);
  if (!r.ok) {
    (d = (u = ui.notifications) == null ? void 0 : u.warn) == null || d.call(u, r.reason ?? "Unable to apply attack damage.");
    return;
  }
  if (r.skipped) {
    (f = (m = ui.notifications) == null ? void 0 : m.info) == null || f.call(m, r.reason ?? "That attack damage has already been applied.");
    return;
  }
  const s = await ga({ resolved: a });
  await e.update({
    content: s,
    "flags.mwd.resolved": a
  }), await Hu({
    summary: r.summary,
    actor: r.targetActor,
    token: r.targetToken
  });
}
async function Uu(i = {}) {
  var n, a;
  const e = (n = i == null ? void 0 : i.target) != null && n.actorUuid ? await fromUuid(i.target.actorUuid) : null, t = (a = i == null ? void 0 : i.target) != null && a.tokenUuid ? await fromUuid(i.target.tokenUuid) : null;
  return Po({
    actor: e,
    token: t,
    edgePoolKey: (i == null ? void 0 : i.evadeEdgePoolKey) ?? ""
  });
}
function iv(i = "") {
  var e, t;
  return ((t = (e = game.messages) == null ? void 0 : e.get) == null ? void 0 : t.call(e, String(i ?? "").trim())) ?? null;
}
async function Po({ actor: i = null, token: e = null, actorUuid: t = "", tokenUuid: n = "", edgePoolKey: a = "" } = {}) {
  const r = i ?? (t ? await fromUuid(t) : null), s = e ?? (n ? await fromUuid(n) : null);
  return r ? {
    ...F.getReactionSpendPreview(r, { token: s, edgePoolKey: a }) ?? {},
    actor: r,
    token: s
  } : null;
}
async function ju(i, e) {
  var r, s;
  const t = foundry.utils.deepClone((s = (r = i == null ? void 0 : i.flags) == null ? void 0 : r.mwd) == null ? void 0 : s.resolved);
  if (!t) return;
  await e(t);
  const n = await fromUuid(t.actorUuid);
  if (!n) return;
  await mh(t, n);
  const a = await ga({ resolved: t });
  return await i.update({
    content: a,
    "flags.mwd.resolved": t
  }), t;
}
async function Ku(i, e, { active: t = !1, edgePoolKey: n = "" } = {}) {
  var s, o, l, c, u, d, m, f;
  const a = (s = e == null ? void 0 : e.target) != null && s.actorUuid ? await fromUuid(e.target.actorUuid) : null, r = (o = e == null ? void 0 : e.target) != null && o.tokenUuid ? await fromUuid(e.target.tokenUuid) : null;
  if (a) {
    if (!t) {
      const p = F.getSnapshot(a, { token: r }), g = (p == null ? void 0 : p.pendingReaction) ?? null;
      (g == null ? void 0 : g.sourceKind) === "attack" && (g == null ? void 0 : g.messageId) === i.id && (g == null ? void 0 : g.sourceId) === (e == null ? void 0 : e.previewKey) && await F.clearPendingReaction(a, { token: r });
      return;
    }
    await F.setPendingReaction(a, {
      token: r,
      pendingReaction: {
        type: "evade",
        sourceKind: "attack",
        sourceId: (e == null ? void 0 : e.previewKey) ?? null,
        messageId: i.id,
        resultIndex: (e == null ? void 0 : e.resultIndex) ?? null,
        exposureBefore: ((c = (l = e == null ? void 0 : e.damage) == null ? void 0 : l.exposure) == null ? void 0 : c.initialTier) ?? ((u = e == null ? void 0 : e.exposure) == null ? void 0 : u.initialTier) ?? "none",
        exposureAfterPreview: ((m = (d = e == null ? void 0 : e.damage) == null ? void 0 : d.exposure) == null ? void 0 : m.finalTier) ?? ((f = e == null ? void 0 : e.exposure) == null ? void 0 : f.initialTier) ?? "none",
        edgePoolKey: n,
        allowCurrentTurn: !1
      }
    });
  }
}
async function nv(i, e) {
  var s, o;
  i.preventDefault();
  const t = i.target.closest("[data-mwd-action='toggleEvade']"), n = String(((s = t == null ? void 0 : t.dataset) == null ? void 0 : s.previewKey) ?? "").trim();
  if (!n) return;
  const a = await ju(e, async (l) => {
    var f;
    if (l.areaEffectPreviewState ?? (l.areaEffectPreviewState = {}), !!(l.areaEffectPreviewState[n] ?? {}).evadeActive) {
      delete l.areaEffectPreviewState[n];
      return;
    }
    l.areaEffectPreviewState[n] = {
      evadeActive: !0,
      edgePoolKey: null
    };
    const d = (Array.isArray((f = l == null ? void 0 : l.attackResult) == null ? void 0 : f.results) ? l.attackResult.results : []).find((p) => (p == null ? void 0 : p.previewKey) === n) ?? null, m = d ? await Uu({ ...d, evadeEdgePoolKey: null }) : null;
    m && (l.areaEffectPreviewState[n].reactionPreview = {
      burnDelta: Number(m.burnDelta ?? 0),
      canSpendEdge: !!m.canSpendEdge,
      edgePools: (m.edgePools ?? []).map((p) => ({
        key: p.key,
        label: p.label,
        value: p.value
      }))
    });
  }), r = (Array.isArray((o = a == null ? void 0 : a.attackResult) == null ? void 0 : o.results) ? a.attackResult.results : []).find((l) => (l == null ? void 0 : l.previewKey) === n) ?? null;
  a && r && await Ku(e, r, {
    active: !!(r != null && r.evadeActive),
    edgePoolKey: String((r == null ? void 0 : r.evadeEdgePoolKey) ?? "").trim()
  });
}
async function av(i, e) {
  var o, l, c;
  i.preventDefault();
  const t = i.target.closest("[data-mwd-action='toggleEvadeEdge']"), n = String(((o = t == null ? void 0 : t.dataset) == null ? void 0 : o.previewKey) ?? "").trim(), a = String(((l = t == null ? void 0 : t.dataset) == null ? void 0 : l.poolKey) ?? "").trim();
  if (!n) return;
  const r = await ju(e, async (u) => {
    var g;
    u.areaEffectPreviewState ?? (u.areaEffectPreviewState = {});
    const d = u.areaEffectPreviewState[n] ?? {}, m = d.edgePoolKey === a ? null : a;
    u.areaEffectPreviewState[n] = {
      ...d,
      evadeActive: !0,
      edgePoolKey: m
    };
    const f = (Array.isArray((g = u == null ? void 0 : u.attackResult) == null ? void 0 : g.results) ? u.attackResult.results : []).find((h) => (h == null ? void 0 : h.previewKey) === n) ?? null, p = f ? await Uu({ ...f, evadeEdgePoolKey: m }) : null;
    p && (u.areaEffectPreviewState[n].reactionPreview = {
      burnDelta: Number(p.burnDelta ?? 0),
      canSpendEdge: !!p.canSpendEdge,
      edgePools: (p.edgePools ?? []).map((h) => ({
        key: h.key,
        label: h.label,
        value: h.value
      }))
    });
  }), s = (Array.isArray((c = r == null ? void 0 : r.attackResult) == null ? void 0 : c.results) ? r.attackResult.results : []).find((u) => (u == null ? void 0 : u.previewKey) === n) ?? null;
  r && s && await Ku(e, s, {
    active: !!(s != null && s.evadeActive),
    edgePoolKey: String((s == null ? void 0 : s.evadeEdgePoolKey) ?? "").trim()
  });
}
async function rv(i, e) {
  var c, u, d, m, f, p, g, h, y;
  i.preventDefault();
  const t = foundry.utils.deepClone((u = (c = e == null ? void 0 : e.flags) == null ? void 0 : c.mwd) == null ? void 0 : u.resolved);
  if (!t) return;
  const a = (Array.isArray((d = t == null ? void 0 : t.attackResult) == null ? void 0 : d.results) ? t.attackResult.results : []).map((b, S) => ({ result: b, index: S })).filter(({ result: b }) => (b == null ? void 0 : b.queuedMutation) && !b.queuedMutation.applied).map(({ index: b }) => b);
  if (!a.length) {
    (f = (m = ui.notifications) == null ? void 0 : m.info) == null || f.call(m, "No queued attack damage remains to apply.");
    return;
  }
  let r = 0;
  const s = [], o = [];
  for (const b of a) {
    const S = await fh(t, b);
    S.ok && S.applied ? (r += 1, o.push(S)) : S.ok || s.push(S.reason ?? `Target ${b + 1} failed.`);
  }
  if (r <= 0) {
    (g = (p = ui.notifications) == null ? void 0 : p.warn) == null || g.call(p, s[0] ?? "Unable to apply queued attack damage.");
    return;
  }
  const l = await ga({ resolved: t });
  await e.update({
    content: l,
    "flags.mwd.resolved": t
  });
  for (const b of o)
    await Hu({
      summary: b.summary,
      actor: b.targetActor,
      token: b.targetToken
    });
  s.length && ((y = (h = ui.notifications) == null ? void 0 : h.warn) == null || y.call(h, `Applied ${r} queued damage result${r === 1 ? "" : "s"}; ${s.length} failed.`));
}
async function fh(i, e) {
  var l, c, u, d, m, f, p, g, h;
  const t = ((c = (l = i == null ? void 0 : i.attackResult) == null ? void 0 : l.results) == null ? void 0 : c[e]) ?? null, n = (t == null ? void 0 : t.queuedMutation) ?? null;
  if (!n)
    return { ok: !1, reason: "No queued attack damage to apply." };
  if (n.applied)
    return { ok: !0, skipped: !0, reason: "That attack damage has already been applied." };
  let a = null, r = null, s = null;
  try {
    if (r = (u = n.target) != null && u.actorUuid ? await fromUuid(n.target.actorUuid) : null, s = (d = n.target) != null && d.tokenUuid ? await fromUuid(n.target.tokenUuid) : null, t != null && t.evadeActive && r) {
      const y = await F.commitReactionSpend(r, {
        token: s,
        actionId: "evade",
        actionLabel: "Evade",
        actionCategory: "reaction",
        logLabel: `Evade: ${((m = n.target) == null ? void 0 : m.name) ?? ((f = t == null ? void 0 : t.target) == null ? void 0 : f.name) ?? "Target"}`,
        edgePoolKey: String((t == null ? void 0 : t.evadeEdgePoolKey) ?? "").trim()
      });
      if (!(y != null && y.ok))
        return { ok: !1, reason: (y == null ? void 0 : y.reason) ?? "Unable to spend the Evade reaction." };
      await F.clearPendingReaction(r, { token: s });
    }
    if (((p = n.payload) == null ? void 0 : p.mode) === "machineAttackDamage" && ((g = n.payload) != null && g.chaosCriticalSelected)) {
      const y = await sv({
        machineActor: r,
        operatorActorUuid: (h = n.payload) == null ? void 0 : h.operatorActorUuid
      });
      if (!y.ok) return y;
    }
    a = await Jt.apply({
      actor: r,
      token: s,
      payload: n.payload ?? {},
      options: {
        actorId: (r == null ? void 0 : r.id) ?? "",
        logToChat: !1
      }
    });
  } catch (y) {
    return console.warn("MWD | Unable to apply queued attack damage", y), { ok: !1, reason: "Unable to apply attack damage to that target." };
  }
  const o = Gn(
    a,
    (t == null ? void 0 : t.target) ?? n.target ?? {},
    (t == null ? void 0 : t.damage) ?? {},
    { queued: !1, applied: !!(a != null && a.ok) }
  );
  return a != null && a.ok ? (n.applied = !0, n.appliedResult = o, t.queuedMutation = n, t.damageResult = o, t.evadeApplied = !!t.evadeActive, i.edge ?? (i.edge = {}), i.edge.availableActions = {
    ...i.edge.availableActions ?? {},
    canSpendPost: !1,
    canPostRerollFailures: !1
  }, {
    ok: !0,
    applied: !0,
    summary: o,
    targetActor: r,
    targetToken: s
  }) : { ok: !1, reason: o.reason ?? "Unable to apply attack damage." };
}
async function sv({ machineActor: i = null, operatorActorUuid: e = "" } = {}) {
  var r, s, o, l, c, u, d, m;
  const t = await xn({ machineActor: i, operatorActorUuid: e });
  if (!t.actor)
    return (r = game.user) != null && r.isGM ? { ok: !0, gmOverride: !0 } : { ok: !1, reason: t.reason || "No linked operator or pilot actor for Chaos Edge." };
  const n = A.counters.edgePools.chaos, a = Number(((o = (s = t.actor).getRemainingEdge) == null ? void 0 : o.call(s, n)) ?? ((c = (l = t.actor).getEdgePoolValue) == null ? void 0 : c.call(l, n)) ?? 0);
  return a <= 0 && !((u = game.user) != null && u.isGM) ? { ok: !1, reason: `${t.actor.name ?? "Operator"} has no Chaos Edge remaining.` } : (a > 0 && await ((m = (d = t.actor).spendEdge) == null ? void 0 : m.call(d, n, 1, { source: "machineChaosCritical" })), { ok: !0, operatorActor: t.actor });
}
async function ov(i, e) {
  var m, f, p, g, h, y;
  i.preventDefault();
  const t = i.target.closest("[data-mwd-action='toggleMachineChaosCrit']"), n = Number(((m = t == null ? void 0 : t.dataset) == null ? void 0 : m.resultIndex) ?? -1), a = foundry.utils.deepClone(e.getFlag("mwd", "resolved")), r = ((p = (f = a == null ? void 0 : a.attackResult) == null ? void 0 : f.results) == null ? void 0 : p[n]) ?? null, s = (r == null ? void 0 : r.queuedMutation) ?? null;
  if (!s || s.applied || ((g = s.payload) == null ? void 0 : g.mode) !== "machineAttackDamage") return;
  s.payload.chaosCriticalSelected = !s.payload.chaosCriticalSelected, delete s.payload.preparedCriticalRecords, s.payload.reliabilitySpendSelections = [];
  const o = (h = s.target) != null && h.actorUuid ? await fromUuid(s.target.actorUuid) : null, l = (y = s.target) != null && y.tokenUuid ? await fromUuid(s.target.tokenUuid) : null, c = await Jt.apply({
    actor: o,
    token: l,
    payload: s.payload,
    options: {
      actorId: (o == null ? void 0 : o.id) ?? "",
      dryRun: !0,
      logToChat: !1
    }
  }), u = Gn(
    c,
    (r == null ? void 0 : r.target) ?? s.target ?? {},
    (r == null ? void 0 : r.damage) ?? {},
    { queued: !0, applied: !1 }
  );
  s.preview = u, r.queuedMutation = s, r.damageResult = u;
  const d = await ga({ resolved: a });
  await e.update({
    content: d,
    "flags.mwd.resolved": a
  });
}
async function lv(i, e) {
  var p, g, h, y, b, S, w, T;
  i.preventDefault();
  const t = i.target.closest("[data-mwd-action='toggleMachineReliabilitySpend']"), n = Number(((p = t == null ? void 0 : t.dataset) == null ? void 0 : p.resultIndex) ?? -1), a = Number(((g = t == null ? void 0 : t.dataset) == null ? void 0 : g.spendIndex) ?? -1);
  if (!Number.isInteger(n) || n < 0 || !Number.isInteger(a) || a < 0) return;
  const r = foundry.utils.deepClone(e.getFlag("mwd", "resolved")), s = ((y = (h = r == null ? void 0 : r.attackResult) == null ? void 0 : h.results) == null ? void 0 : y[n]) ?? null, o = (s == null ? void 0 : s.queuedMutation) ?? null;
  if (!o || o.applied || ((b = o.payload) == null ? void 0 : b.mode) !== "machineAttackDamage") return;
  const l = new Set(
    Array.isArray((S = o.payload) == null ? void 0 : S.reliabilitySpendSelections) ? o.payload.reliabilitySpendSelections.map((k) => Number(k)).filter(Number.isInteger) : []
  );
  l.has(a) ? l.delete(a) : l.add(a), o.payload.reliabilitySpendSelections = Array.from(l).sort((k, C) => k - C);
  const c = (w = o.target) != null && w.actorUuid ? await fromUuid(o.target.actorUuid) : null, u = (T = o.target) != null && T.tokenUuid ? await fromUuid(o.target.tokenUuid) : null, d = await Jt.apply({
    actor: c,
    token: u,
    payload: o.payload,
    options: {
      actorId: (c == null ? void 0 : c.id) ?? "",
      dryRun: !0,
      logToChat: !1
    }
  }), m = Gn(
    d,
    (s == null ? void 0 : s.target) ?? o.target ?? {},
    (s == null ? void 0 : s.damage) ?? {},
    { queued: !0, applied: !1 }
  );
  o.preview = m, s.queuedMutation = o, s.damageResult = m;
  const f = await ga({ resolved: r });
  await e.update({
    content: f,
    "flags.mwd.resolved": r
  });
}
async function cv(i, e) {
  var r, s, o, l, c, u, d, m, f, p, g, h, y;
  i.preventDefault();
  const t = i.target.closest("[data-mwd-action='machineCritRemedy']"), n = await No({
    machineActorUuid: ((r = t == null ? void 0 : t.dataset) == null ? void 0 : r.machineActorUuid) ?? "",
    critId: ((s = t == null ? void 0 : t.dataset) == null ? void 0 : s.critId) ?? "",
    remedyKey: ((o = t == null ? void 0 : t.dataset) == null ? void 0 : o.remedyKey) ?? "",
    operatorActorUuid: ((l = t == null ? void 0 : t.dataset) == null ? void 0 : l.operatorActorUuid) ?? ""
  }, {
    gmOverride: !!((c = game.user) != null && c.isGM && ((u = t == null ? void 0 : t.dataset) == null ? void 0 : u.gmOverride) === "true")
  });
  if (!n.ok) {
    (m = (d = ui.notifications) == null ? void 0 : d.warn) == null || m.call(d, n.reason ?? "Unable to launch the machine remedy roll.");
    return;
  }
  const a = ((f = game.mwd) == null ? void 0 : f.roll) ?? ((g = (p = game.system) == null ? void 0 : p.mwd) == null ? void 0 : g.roll);
  if (!(a != null && a.execute)) {
    (y = (h = ui.notifications) == null ? void 0 : h.error) == null || y.call(h, "MWD roll system not initialized.");
    return;
  }
  await a.execute({
    actor: n.actor,
    payload: n.payload,
    event: i
  });
}
async function ph(i, e) {
  const t = e != null && e.actorUuid ? await fromUuid(e.actorUuid) : null, n = e != null && e.tokenUuid ? await fromUuid(e.tokenUuid) : null, a = await bp(e, { actor: t, token: n });
  return await i.update({
    content: a,
    "flags.mwd.hazardCard": e
  }), e;
}
async function Gu(i, e) {
  var n, a;
  const t = cu(foundry.utils.deepClone(((a = (n = i == null ? void 0 : i.flags) == null ? void 0 : n.mwd) == null ? void 0 : a.hazardCard) ?? {}));
  return t != null && t.actorUuid ? (await e(t), await ph(i, t), t) : null;
}
async function Wu(i, e, { active: t = !1, edgePoolKey: n = "" } = {}) {
  var s, o, l;
  const a = e != null && e.actorUuid ? await fromUuid(e.actorUuid) : null, r = e != null && e.tokenUuid ? await fromUuid(e.tokenUuid) : null;
  if (a) {
    if (!t) {
      const c = F.getSnapshot(a, { token: r }), u = (c == null ? void 0 : c.pendingReaction) ?? null;
      (u == null ? void 0 : u.sourceKind) === "hazard" && (u == null ? void 0 : u.messageId) === i.id && (u == null ? void 0 : u.sourceId) === (e == null ? void 0 : e.regionId) && await F.clearPendingReaction(a, { token: r });
      return;
    }
    await F.setPendingReaction(a, {
      token: r,
      pendingReaction: {
        type: "evade",
        sourceKind: "hazard",
        sourceId: (e == null ? void 0 : e.regionId) ?? null,
        messageId: i.id,
        exposureBefore: ((s = e == null ? void 0 : e.exposure) == null ? void 0 : s.initialTier) ?? "none",
        exposureAfterPreview: ((o = e == null ? void 0 : e.preview) == null ? void 0 : o.finalTier) ?? ((l = e == null ? void 0 : e.exposure) == null ? void 0 : l.initialTier) ?? "none",
        edgePoolKey: n,
        allowCurrentTurn: !0
      }
    });
  }
}
async function uv(i, e) {
  var n, a;
  i.preventDefault();
  const t = await Gu(e, async (r) => {
    var l, c, u;
    const s = !((l = r == null ? void 0 : r.preview) != null && l.evadeActive), o = qc(Rn({
      tier: ((c = r == null ? void 0 : r.exposure) == null ? void 0 : c.initialTier) ?? "none"
    }), {
      active: s,
      locked: !!((u = r == null ? void 0 : r.exposure) != null && u.evadeLocked)
    });
    if (r.preview ?? (r.preview = {}), r.preview.evadeActive = s, r.preview.edgePoolKey = null, r.preview.finalTier = o.finalTier, r.damageAfter = Zn(r.baseDamage ?? 0, o.finalTier), s) {
      const d = await Po({
        actorUuid: r.actorUuid,
        tokenUuid: r.tokenUuid,
        edgePoolKey: ""
      });
      r.preview.reactionPreview = d ? {
        burnDelta: Number(d.burnDelta ?? 0),
        canSpendEdge: !!d.canSpendEdge,
        edgePools: (d.edgePools ?? []).map((m) => ({
          key: m.key,
          label: m.label,
          value: m.value
        }))
      } : {};
    } else
      r.preview.reactionPreview = {};
  });
  t && await Wu(e, t, {
    active: !!((n = t == null ? void 0 : t.preview) != null && n.evadeActive),
    edgePoolKey: String(((a = t == null ? void 0 : t.preview) == null ? void 0 : a.edgePoolKey) ?? "").trim()
  });
}
async function dv(i, e) {
  var r, s, o;
  i.preventDefault();
  const t = i.target.closest("[data-mwd-action='toggleHazardEvadeEdge']"), n = String(((r = t == null ? void 0 : t.dataset) == null ? void 0 : r.poolKey) ?? "").trim(), a = await Gu(e, async (l) => {
    l.preview ?? (l.preview = {}), l.preview.evadeActive = !0, l.preview.edgePoolKey = l.preview.edgePoolKey === n ? null : n;
    const c = await Po({
      actorUuid: l.actorUuid,
      tokenUuid: l.tokenUuid,
      edgePoolKey: l.preview.edgePoolKey ?? ""
    });
    l.preview.reactionPreview = c ? {
      burnDelta: Number(c.burnDelta ?? 0),
      canSpendEdge: !!c.canSpendEdge,
      edgePools: (c.edgePools ?? []).map((u) => ({
        key: u.key,
        label: u.label,
        value: u.value
      }))
    } : {};
  });
  a && await Wu(e, a, {
    active: !!((s = a == null ? void 0 : a.preview) != null && s.evadeActive),
    edgePoolKey: String(((o = a == null ? void 0 : a.preview) == null ? void 0 : o.edgePoolKey) ?? "").trim()
  });
}
async function mv(i, e) {
  var u, d, m, f, p, g, h, y, b, S, w, T, k, C, I, O, x, B, P, L, D;
  i.preventDefault();
  const t = cu(foundry.utils.deepClone(((d = (u = e == null ? void 0 : e.flags) == null ? void 0 : u.mwd) == null ? void 0 : d.hazardCard) ?? {}));
  if (!(t != null && t.actorUuid)) return;
  if (t.applied) {
    (f = (m = ui.notifications) == null ? void 0 : m.info) == null || f.call(m, "That hazard has already been applied.");
    return;
  }
  const n = await fromUuid(t.actorUuid), a = t.tokenUuid ? await fromUuid(t.tokenUuid) : null;
  if (!n) {
    (g = (p = ui.notifications) == null ? void 0 : p.warn) == null || g.call(p, "Unable to resolve the hazard target.");
    return;
  }
  if ((h = t.preview) != null && h.evadeActive) {
    const G = await F.commitReactionSpend(n, {
      token: a,
      actionId: "evade",
      actionLabel: "Evade",
      actionCategory: "reaction",
      logLabel: `Evade: ${t.regionName}`,
      edgePoolKey: String(((y = t.preview) == null ? void 0 : y.edgePoolKey) ?? "").trim(),
      allowCurrentTurn: !0
    });
    if (!(G != null && G.ok)) {
      (S = (b = ui.notifications) == null ? void 0 : b.warn) == null || S.call(b, (G == null ? void 0 : G.reason) ?? "Unable to spend the Evade reaction.");
      return;
    }
  }
  const r = {
    mode: "attackDamage",
    track: A.monitors.physical,
    damage: Number(t.damageAfter ?? t.damageBefore ?? 0) || 0,
    netHits: 0,
    damageType: t.damageType,
    ap: Number(t.ap ?? 0) || 0,
    source: t.source,
    notes: `Hazard exposure ${t.exposure.initialLabel}${(w = t.preview) != null && w.evadeActive ? ` -> ${String(t.preview.finalTier ?? t.exposure.initialTier).toUpperCase()}` : ""}`.trim()
  }, s = await Jt.apply({
    actor: n,
    token: a,
    payload: r,
    options: {
      actorId: n.id,
      logToChat: !1
    }
  });
  if (!(s != null && s.ok)) {
    (k = (T = ui.notifications) == null ? void 0 : T.warn) == null || k.call(T, (s == null ? void 0 : s.reason) ?? "Unable to apply hazard damage.");
    return;
  }
  const o = F.getSnapshot(n, { token: a }), l = ((C = o == null ? void 0 : o.hazards) == null ? void 0 : C[t.regionId]) ?? {}, c = lt(t.nextTier, t.exposure.finalTier);
  await F.setHazardState(n, {
    token: a,
    regionId: t.regionId,
    hazardState: {
      ...l,
      tier: c,
      turnsExposed: Math.max(Number((l == null ? void 0 : l.turnsExposed) ?? 0), Number(t.turnsExposed ?? 0)) + 1,
      lastProcessedRound: Number(((I = o == null ? void 0 : o.combat) == null ? void 0 : I.round) ?? 0) || 0,
      evadeLocked: !!(l != null && l.evadeLocked) || !!(((O = t.exposure) == null ? void 0 : O.initialTier) === "full" && ((x = t.preview) == null ? void 0 : x.finalTier) === "major" && ((B = t.preview) != null && B.evadeActive))
    }
  }), c === "full" && Number((t == null ? void 0 : t.onFullBurnDelta) ?? 0) > 0 && await n.update({
    "system.burn.value": Math.max(0, Number(((L = (P = n.system) == null ? void 0 : P.burn) == null ? void 0 : L.value) ?? 0) + Number(t.onFullBurnDelta ?? 0))
  }), await F.clearPendingReaction(n, { token: a }), t.applied = !0, t.applyReason = "Applied", await ph(e, t), await Hu({
    summary: {
      actorName: n.name,
      track: s.track,
      finalDamage: Number(s.finalDamage ?? s.appliedDelta ?? 0),
      damageIncoming: Number(s.damageIncoming ?? t.damageAfter ?? 0),
      damageType: s.damageType ?? t.damageType,
      usedArmor: !!s.usedArmor,
      effectiveAp: Number(s.effectiveAp ?? t.ap ?? 0),
      mitigation: s.mitigation ?? null,
      beforeLabel: String(s.beforeLabel ?? "").trim(),
      afterLabel: String(s.afterLabel ?? "").trim(),
      source: t.source,
      notes: `Hazard exposure ${t.exposure.initialLabel}${(D = t.preview) != null && D.evadeActive ? ` -> ${String(t.preview.finalTier ?? "").toUpperCase()}` : ""}`
    },
    actor: n,
    token: a
  });
}
async function fv(i, { token: e = null } = {}) {
  var r, s;
  const t = F.getSnapshot(i, { token: e }), n = (t == null ? void 0 : t.pendingReaction) ?? null;
  if (!(n != null && n.messageId))
    return { ok: !1, reason: "Use an area effect or hazard card to trigger Evade." };
  const a = iv(n.messageId);
  if (!a)
    return await F.clearPendingReaction(i, { token: e }), { ok: !1, reason: "The pending Evade card is no longer available." };
  if (n.sourceKind === "attack") {
    const o = String(n.sourceId ?? "").trim();
    if (!o) return { ok: !1, reason: "Pending Evade target is missing." };
    const l = await ju(a, async (u) => {
      var f;
      u.areaEffectPreviewState ?? (u.areaEffectPreviewState = {}), u.areaEffectPreviewState[o] = {
        ...u.areaEffectPreviewState[o] ?? {},
        evadeActive: !0,
        edgePoolKey: n.edgePoolKey ?? null
      };
      const d = (Array.isArray((f = u == null ? void 0 : u.attackResult) == null ? void 0 : f.results) ? u.attackResult.results : []).find((p) => (p == null ? void 0 : p.previewKey) === o) ?? null, m = d ? await Uu({ ...d, evadeEdgePoolKey: n.edgePoolKey ?? "" }) : null;
      m && (u.areaEffectPreviewState[o].reactionPreview = {
        burnDelta: Number(m.burnDelta ?? 0),
        canSpendEdge: !!m.canSpendEdge,
        edgePools: (m.edgePools ?? []).map((p) => ({
          key: p.key,
          label: p.label,
          value: p.value
        }))
      });
    }), c = (Array.isArray((r = l == null ? void 0 : l.attackResult) == null ? void 0 : r.results) ? l.attackResult.results : []).find((u) => (u == null ? void 0 : u.previewKey) === o) ?? null;
    return c && await Ku(a, c, {
      active: !0,
      edgePoolKey: String((c == null ? void 0 : c.evadeEdgePoolKey) ?? n.edgePoolKey ?? "").trim()
    }), { ok: !0 };
  }
  if (n.sourceKind === "hazard") {
    const o = await Gu(a, async (l) => {
      var d, m;
      const c = qc(Rn({
        tier: ((d = l == null ? void 0 : l.exposure) == null ? void 0 : d.initialTier) ?? "none"
      }), {
        active: !0,
        locked: !!((m = l == null ? void 0 : l.exposure) != null && m.evadeLocked)
      });
      l.preview ?? (l.preview = {}), l.preview.evadeActive = !0, l.preview.edgePoolKey = l.preview.edgePoolKey ?? n.edgePoolKey ?? null, l.preview.finalTier = c.finalTier, l.damageAfter = Zn(l.baseDamage ?? 0, c.finalTier);
      const u = await Po({
        actorUuid: l.actorUuid,
        tokenUuid: l.tokenUuid,
        edgePoolKey: l.preview.edgePoolKey ?? ""
      });
      l.preview.reactionPreview = u ? {
        burnDelta: Number(u.burnDelta ?? 0),
        canSpendEdge: !!u.canSpendEdge,
        edgePools: (u.edgePools ?? []).map((f) => ({
          key: f.key,
          label: f.label,
          value: f.value
        }))
      } : {};
    });
    return o && await Wu(a, o, {
      active: !0,
      edgePoolKey: String(((s = o == null ? void 0 : o.preview) == null ? void 0 : s.edgePoolKey) ?? n.edgePoolKey ?? "").trim()
    }), { ok: !0 };
  }
  return { ok: !1, reason: "That Evade source is not supported." };
}
async function pv(i, e) {
  var p, g, h, y, b, S, w, T, k, C, I, O, x, B, P, L, D, G, V;
  i.preventDefault();
  const t = i.target.closest("[data-mwd-action='edgePostReroll']"), n = String(((p = t == null ? void 0 : t.dataset) == null ? void 0 : p.poolKey) ?? "").trim();
  if (!n) return;
  const a = foundry.utils.deepClone((h = (g = e == null ? void 0 : e.flags) == null ? void 0 : g.mwd) == null ? void 0 : h.resolved);
  if (!a) return;
  if (qC(a)) {
    (b = (y = ui.notifications) == null ? void 0 : y.warn) == null || b.call(y, "Post-roll Edge is disabled after attack damage has been applied.");
    return;
  }
  if (Number(((w = (S = a == null ? void 0 : a.edge) == null ? void 0 : S.post) == null ? void 0 : w.spent) ?? 0) === 1) return;
  if (!(Array.isArray((k = (T = a == null ? void 0 : a.edge) == null ? void 0 : T.allowed) == null ? void 0 : k.postPools) ? a.edge.allowed.postPools : []).includes(n)) {
    (I = (C = ui.notifications) == null ? void 0 : C.warn) == null || I.call(C, `Post-spend pool not allowed: ${n}`);
    return;
  }
  const s = Array.isArray((O = a == null ? void 0 : a.roll) == null ? void 0 : O.failureDiceRefs) ? a.roll.failureDiceRefs : [];
  if (s.length <= 0) {
    (B = (x = ui.notifications) == null ? void 0 : x.info) == null || B.call(x, "No failures to reroll.");
    return;
  }
  const o = await fromUuid(a.actorUuid);
  if (!o) {
    (L = (P = ui.notifications) == null ? void 0 : P.warn) == null || L.call(P, "Actor not found for this roll.");
    return;
  }
  await ((D = o.spendEdge) == null ? void 0 : D.call(o, n, 1));
  const l = Number(((G = a == null ? void 0 : a.roll) == null ? void 0 : G.target) ?? 5), u = (V = (await new Roll(`${s.length}d6cs>=${l}`).evaluate()).dice) == null ? void 0 : V[0], d = Array.isArray(u == null ? void 0 : u.results) ? u.results : [], m = d.filter((Z) => Z.success).length;
  a.outcome = a.outcome ?? {}, a.outcome.hits = Number(a.outcome.hits ?? 0) + m, a.edge = a.edge ?? {}, a.edge.post = { poolKey: n, spent: 1 }, a.edge.availableActions = {
    ...a.edge.availableActions ?? {},
    canSpendPost: !1,
    canPostRerollFailures: !1
  }, a.roll = a.roll ?? {}, a.roll.diceGroups = Array.isArray(a.roll.diceGroups) ? a.roll.diceGroups : [], a.roll.diceGroups.push({
    id: "post",
    label: "Post Reroll",
    faces: 6,
    termIndex: null,
    dice: d.map((Z, E) => {
      const X = Number(Z.result), H = !!Z.success;
      return {
        ref: `post:${E}`,
        face: X,
        isSuccess: H,
        isFailure: !H,
        tooltip: H ? `Post die ${E + 1}: ${X} (Success vs TN ${l})` : `Post die ${E + 1}: ${X} (Failure vs TN ${l})`
      };
    })
  }), await mh(a, o);
  const f = await ga({ resolved: a });
  await e.update({
    content: f,
    "flags.mwd.resolved": a,
    "flags.mwd.payload.edge.post": { poolKey: n, spent: 1 }
  });
}
const qu = `${M}.ownedWeaponAttack`;
let Tm = !1;
function gv(i, e = null) {
  var s, o, l;
  const t = (i == null ? void 0 : i.actor) ?? null, n = {
    intent: "attack",
    weaponId: (i == null ? void 0 : i.id) ?? "",
    payloadId: ((s = i == null ? void 0 : i.system) == null ? void 0 : s.selectedPayloadId) ?? "",
    edge: { pool: "physical.grit", allowed: ["pre", "post"] },
    tags: ["combat", "attack"],
    sourceTokenId: (e == null ? void 0 : e.id) ?? null
  }, a = t ? F.getSnapshot(t, { token: e }) : null, r = !!((l = (o = a == null ? void 0 : a.state) == null ? void 0 : o.actionState) != null && l.aim);
  return r && (n.aim = { active: !0 }), { payload: n, hasAim: r };
}
function hv(i, e = null) {
  return e ?? (i == null ? void 0 : i.token) ?? F.getCurrentSceneTokenDocument(i) ?? null;
}
function gh(i) {
  const e = String((i == null ? void 0 : i.uuid) ?? "").trim();
  return e ? {
    type: qu,
    uuid: e,
    name: String((i == null ? void 0 : i.name) ?? "Weapon").trim() || "Weapon",
    img: (i == null ? void 0 : i.img) ?? "icons/svg/sword.svg"
  } : null;
}
async function Do({ weapon: i, event: e = null, token: t = null } = {}) {
  var n, a, r, s, o;
  try {
    if (!((n = i == null ? void 0 : i.isPersonalWeapon) != null && n.call(i)))
      throw new Error("Attack requires an owned personal weapon.");
    const l = i.actor ?? null;
    if (!l)
      throw new Error("Attack requires an owned personal weapon.");
    const c = hv(l, t), { payload: u, hasAim: d } = gv(i, c), m = ((a = game.mwd) == null ? void 0 : a.roll) ?? ((s = (r = game.system) == null ? void 0 : r.mwd) == null ? void 0 : s.roll);
    if (!(m != null && m.execute))
      throw new Error("MWD roll system not initialized.");
    const f = await m.execute({ actor: l, payload: u, event: e });
    if (f) {
      d && await F.clearAim(l, { token: c });
      const p = F.getSnapshot(l, { token: c });
      if (p != null && p.hasCombatant) {
        const g = await F.spendResource(l, {
          token: c,
          resource: "sa",
          cost: 2,
          actionId: "attack",
          actionLabel: "Attack",
          actionCostLabel: "2 SA",
          actionCategory: "complex"
        });
        g != null && g.ok || (o = ui.notifications) == null || o.warn((g == null ? void 0 : g.reason) ?? "Unable to record attack action.");
      }
    }
    return f;
  } catch (l) {
    return console.error("MWD | Failed to launch weapon attack", l), sn(l, "Unable to attack with that weapon."), null;
  }
}
async function yv(i, { event: e = null } = {}) {
  var a, r;
  const t = String(i ?? "").trim();
  if (!t)
    return (a = ui.notifications) == null || a.warn("That weapon shortcut is missing its item reference."), null;
  const n = await fromUuid(t);
  return n ? Do({ weapon: n, event: e }) : ((r = ui.notifications) == null || r.warn("That weapon shortcut could not find its source item."), null);
}
function bv(i) {
  return `(async () => {
  const attacks = game.mwd?.attacks;
  if (!attacks?.attackWeaponByUuid) {
    return ui.notifications?.error("MWD attack shortcuts are not available right now.");
  }
  await attacks.attackWeaponByUuid(${JSON.stringify(String(i ?? "").trim())});
})();`;
}
async function Sv(i, e) {
  var s, o, l, c;
  const t = String((i == null ? void 0 : i.uuid) ?? "").trim();
  if (!t) return;
  const n = String((i == null ? void 0 : i.name) ?? "Weapon Attack").trim() || "Weapon Attack", a = bv(t);
  let r = ((o = (s = game.macros) == null ? void 0 : s.find) == null ? void 0 : o.call(
    s,
    (u) => (u == null ? void 0 : u.type) === "script" && (u == null ? void 0 : u.name) === n && (u == null ? void 0 : u.command) === a
  )) ?? null;
  r || (r = await Macro.create({
    name: n,
    type: "script",
    img: (i == null ? void 0 : i.img) ?? "icons/svg/sword.svg",
    command: a
  })), await ((c = (l = game.user) == null ? void 0 : l.assignHotbarMacro) == null ? void 0 : c.call(l, r, e));
}
function hh(i, e, t) {
  return (e == null ? void 0 : e.type) !== qu ? !0 : (Sv(e, t), !1);
}
function yh() {
  Tm || (Tm = !0, Hooks.on("hotbarDrop", hh));
}
const km = {
  HOTBAR_ATTACK_TYPE: qu,
  getOwnedWeaponAttackDragData: gh,
  launchOwnedWeaponAttack: Do,
  attackWeaponByUuid: yv,
  handleWeaponAttackHotbarDrop: hh,
  registerWeaponAttackHotbarHook: yh
};
function De(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? t : e;
}
function Av(i) {
  return String(i ?? "").replace(/<[^>]*>/g, " ").replace(/\s+/g, " ").trim();
}
function Cs(i, e = 180) {
  const t = Av(i);
  return t ? t.length <= e ? t : `${t.slice(0, Math.max(0, e - 3)).trim()}...` : "";
}
function tn(i = []) {
  return i.map((e) => String(e ?? "").trim()).filter(Boolean);
}
function mr(i = []) {
  return i.filter((e) => e && e.value !== void 0 && e.value !== null && String(e.value).trim() !== "").map((e) => ({
    label: String(e.label ?? "").trim(),
    value: String(e.value ?? "").trim(),
    emphasis: e.emphasis ?? ""
  }));
}
function vs(i = []) {
  return tn(i).map((e) => ({ label: e }));
}
function Es(i = []) {
  return i.filter((e) => e && e.value !== void 0 && e.value !== null && String(e.value).trim() !== "").map((e) => ({
    label: String(e.label ?? "").trim(),
    value: String(e.value ?? "").trim()
  }));
}
const wv = q.mwd.armorMitigationType, Tv = q.item.gear.categoryLabels, kv = q.item.consumable.categoryLabels;
function Mm(i) {
  const e = Number(i ?? 0) || 0;
  return e > 0 ? `+${e}` : `${e}`;
}
function Cm({
  item: i,
  accordionId: e,
  itemType: t,
  defaultSubtitle: n,
  categoryLabels: a = {},
  ratingLabel: r = "Rating",
  typeLabel: s = "",
  isEditable: o = !1,
  isExpanded: l = !1
} = {}) {
  var p, g, h, y, b, S, w;
  const c = Math.max(0, Math.trunc(De(((p = i == null ? void 0 : i.system) == null ? void 0 : p.quantity) ?? 1, 1))), u = Math.max(0, Math.trunc(De(((g = i == null ? void 0 : i.system) == null ? void 0 : g.rating) ?? 0, 0))), d = tn(((h = i == null ? void 0 : i.system) == null ? void 0 : h.tags) ?? []), m = String(((y = i == null ? void 0 : i.system) == null ? void 0 : y.category) ?? "").trim(), f = a[m] ?? m;
  return {
    id: i.id,
    itemType: t,
    isGear: t === "gear",
    isConsumable: t === "consumable",
    accordionId: e,
    isExpanded: l,
    name: i.name,
    img: i.img,
    subtitle: f || n,
    summaryStats: mr([
      { label: "Qty", value: c, emphasis: "strong" },
      { label: r, value: u }
    ]),
    detailTags: vs([
      s,
      ...d,
      (b = i == null ? void 0 : i.system) != null && b.inactive ? "Inactive" : ""
    ]),
    detailRows: Es([
      { label: "Quantity", value: c },
      { label: r, value: u },
      { label: "Source", value: ((S = i == null ? void 0 : i.system) == null ? void 0 : S.sourceReference) ?? "" },
      { label: "Category", value: f },
      { label: "Tags", value: d.join(", ") }
    ]),
    detailText: Cs((w = i == null ? void 0 : i.system) == null ? void 0 : w.description),
    quantity: c,
    canAdjustQuantity: o
  };
}
function Mv({ defenseBonus: i = 0, mitigationByType: e = {} } = {}) {
  const t = [], n = Number(i ?? 0) || 0;
  n !== 0 && t.push(`Defense ${Mm(n)}`);
  for (const [a, r] of Object.entries(wv)) {
    const s = Number((e == null ? void 0 : e[a]) ?? 0) || 0;
    s !== 0 && t.push(`${r} ${Mm(s)}`);
  }
  return t.join(" | ");
}
function Cv(i = {}, e = ["close", "near", "far", "extreme"]) {
  return e.map((t) => {
    const n = De(i == null ? void 0 : i[t], 0);
    return `${t.charAt(0).toUpperCase() + t.slice(1)} ${n}`;
  }).join(" | ");
}
function vv(i = {}) {
  return ["close", "near", "far", "extreme"].map((e) => `${e.charAt(0).toUpperCase()}${De(i == null ? void 0 : i[e], 0)}`).join(" ");
}
function Ev(i = "") {
  const e = String(i ?? "").trim().toLowerCase();
  return e ? e.charAt(0).toUpperCase() + e.slice(1) : "";
}
function Zt(i) {
  return String(i ?? "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#39;");
}
function uc(i = null) {
  var n, a, r, s;
  const e = /* @__PURE__ */ new Set();
  if (!i) return e;
  const t = (o) => {
    const l = String(o ?? "").trim();
    l && e.add(l);
  };
  return t(i.id), t(i.uuid), t((n = i.actor) == null ? void 0 : n.id), t((a = i.actor) == null ? void 0 : a.uuid), t((r = i.baseActor) == null ? void 0 : r.id), t((s = i.baseActor) == null ? void 0 : s.uuid), e;
}
async function Iv(i = null) {
  var t, n, a, r, s;
  const e = String(
    ((n = (t = i == null ? void 0 : i.system) == null ? void 0 : t.pilot) == null ? void 0 : n.uuid) ?? ((s = (r = (a = i == null ? void 0 : i.system) == null ? void 0 : a.mwd) == null ? void 0 : r.pilot) == null ? void 0 : s.uuid) ?? ""
  ).trim();
  if (!e || typeof fromUuid != "function") return null;
  try {
    const o = await fromUuid(e);
    return (o == null ? void 0 : o.actor) ?? (o == null ? void 0 : o.baseActor) ?? o ?? null;
  } catch {
    return null;
  }
}
async function Nv(i = null, e = null) {
  var r, s, o, l, c;
  const t = String(
    ((s = (r = i == null ? void 0 : i.system) == null ? void 0 : r.pilot) == null ? void 0 : s.uuid) ?? ((c = (l = (o = i == null ? void 0 : i.system) == null ? void 0 : o.mwd) == null ? void 0 : l.pilot) == null ? void 0 : c.uuid) ?? ""
  ).trim();
  if (!e) return !1;
  const n = uc(e);
  if (n.has(t)) return !0;
  const a = await Iv(i);
  if (!a) return !1;
  for (const u of uc(a))
    if (n.has(u)) return !0;
  return !1;
}
function Rv() {
  var n, a, r;
  const i = [], e = /* @__PURE__ */ new Set(), t = ({ actor: s = null, uuid: o = "", source: l = "actor" } = {}) => {
    const c = s ?? null, u = String(o ?? (c == null ? void 0 : c.uuid) ?? "").trim();
    !c || !u || e.has(u) || c.type !== "battlemech" && c.type !== "vehicle" || (e.add(u), i.push({ actor: c, uuid: u, source: l }));
  };
  for (const s of ((n = game.actors) == null ? void 0 : n.contents) ?? [])
    t({ actor: s, uuid: s == null ? void 0 : s.uuid, source: "actor" });
  for (const s of ((a = game.scenes) == null ? void 0 : a.contents) ?? []) {
    const o = ((r = s == null ? void 0 : s.tokens) == null ? void 0 : r.contents) ?? (s == null ? void 0 : s.tokens) ?? [];
    for (const l of o)
      l != null && l.isLinked || t({ actor: (l == null ? void 0 : l.actor) ?? null, uuid: l == null ? void 0 : l.uuid, source: "token" });
  }
  return i;
}
async function sl(i = null) {
  var n, a;
  const e = String(((n = i == null ? void 0 : i.dataset) == null ? void 0 : n.mechUuid) ?? "").trim();
  if (e && typeof fromUuid == "function")
    try {
      const r = await fromUuid(e);
      return (r == null ? void 0 : r.actor) ?? (r == null ? void 0 : r.baseActor) ?? r ?? null;
    } catch {
    }
  const t = String(((a = i == null ? void 0 : i.dataset) == null ? void 0 : a.mechId) ?? "").trim();
  return t ? game.actors.get(t) ?? null : null;
}
async function vm({ title: i, label: e, options: t = [], confirmLabel: n = "Select" } = {}) {
  const a = Array.isArray(t) ? t.filter((s) => s == null ? void 0 : s.value) : [];
  if (!a.length) return "";
  if (a.length === 1) return String(a[0].value ?? "").trim();
  const r = `<form class="mwd-quick-select"><div class="mwd-field"><label>${Zt(e)}</label><select name="selection">${a.map((s) => `<option value="${Zt(s.value)}">${Zt(s.label ?? s.value)}</option>`).join("")}</select></div></form>`;
  return await Dialog.prompt({
    title: i,
    content: r,
    label: n,
    callback: (s) => {
      var o;
      return String(s.find('select[name="selection"]').val() ?? ((o = a[0]) == null ? void 0 : o.value) ?? "").trim();
    }
  });
}
var gi, Tn, qn, Mi, Ra, kn, K, bh, Sh, mc, Is, Ah, wh, Je, Ai, hn, Th, fc, kh, Mh, Ch, vh, Eh, Ih, Nh, wi, Ta;
const we = class we extends _r {
  constructor() {
    super(...arguments);
    Ce(this, K);
    Ce(this, gi, null);
    Ce(this, Tn, null);
    Ce(this, qn, null);
    Ce(this, Mi, /* @__PURE__ */ new Set());
    Ce(this, Ra, null);
    Ce(this, kn, null);
  }
  /** @override */
  async _prepareContext(t) {
    var P, L, D, G, V, Z, E, X, H, he, ie, le, ze, Xe, mt, ft, pt, gt, nt, ht, Ye, at, Ct, _t, xt, St, Kt, Ft;
    const n = await super._prepareContext(t), a = ((P = this.getSheetTokenDocument) == null ? void 0 : P.call(this)) ?? null;
    n._mwdThemeClass = game.system.mwd.styles.selectCssClass(), n.layout = await oa.get("character");
    const r = ((D = (L = this.actor).getEdgeCap) == null ? void 0 : D.call(L)) ?? Number(((Z = (V = (G = this.actor.system) == null ? void 0 : G.attributes) == null ? void 0 : V.edge) == null ? void 0 : Z.value) ?? 0), s = !!this.isEditable, o = { physical: "Physical", mental: "Mental", social: "Social" }, l = {
      grit: "Grit",
      insight: "Insight",
      legend: "Legend",
      chaos: "Chaos",
      rumor: "Rumor",
      credibility: "Credibility"
    }, c = this.actor.getEdgePoolSummary ? this.actor.getEdgePoolSummary({ groups: Hc }) : { groups: [] };
    n.edgeConsole = {
      cap: r,
      editable: s,
      capPips: Array.from({ length: Math.max(0, r) }, (R, U) => U + 1),
      groups: (c.groups ?? []).map((R) => ({
        id: R.id,
        label: o[R.id] ?? R.id,
        pools: (R.pools ?? []).map((U) => {
          const ne = Number(U.effectiveValue ?? 0), ke = Number(U.effectiveMax ?? 0), Me = Array.from({ length: Math.max(0, ke) }, (Ze, $e) => {
            const Fe = $e + 1;
            return { n: Fe, filled: Fe <= ne };
          }), xe = String(U.key ?? "").split(".").pop();
          return {
            key: U.key,
            label: l[xe] ?? xe ?? U.key,
            value: ne,
            max: ke,
            rating: Number(U.rating ?? 0),
            ratingBonus: Number(U.ratingBonus ?? 0),
            effectiveRating: Number(U.effectiveRating ?? U.rating ?? 0),
            isCapped: Number(U.effectiveRating ?? U.rating ?? 0) > Number(U.cap ?? r),
            pips: Me,
            // Paths for edit-mode inputs
            pathRating: `system.counters.edgePools.${U.key}.rating`,
            pathValue: `system.counters.edgePools.${U.key}.value`,
            // Keep the roll payload if you want pool-name click to route through BaseActorSheetV2 roll handler
            roll: JSON.stringify({ intent: "edge", pool: U.key })
          };
        })
      }))
    };
    const u = ["grit", "insight", "legend", "chaos", "rumor", "credibility"], d = /* @__PURE__ */ new Map();
    for (const R of n.edgeConsole.groups ?? [])
      for (const U of R.pools ?? []) {
        const ne = String(U.key ?? "").split(".").pop();
        ne && d.set(ne, U), U.domain = R.id;
      }
    n.edgeConsole.poolsOrdered = u.map((R) => d.get(R)).filter(Boolean);
    const m = this.actor.system ?? {}, f = m.monitors ?? {}, p = [
      { id: "physical", label: "Physical", kind: "wound", status: { label: "Penalty", path: "derived.penalty" } },
      { id: "fatigue", label: "Fatigue", kind: "fatigue", status: { label: "Penalty", path: "derived.penalty" } },
      { id: "armor", label: "Armor", kind: "armor-personal", status: { label: "Resist", path: "derived.resistance" } }
    ], g = (R, U, ne = 0) => {
      const ke = foundry.utils.getProperty(R, U), Me = Number(ke);
      return Number.isFinite(Me) ? Me : ne;
    };
    n.conditionMonitors = p.map((R) => {
      const U = (f == null ? void 0 : f[R.id]) ?? {}, ne = Math.max(0, g(U, "max", 0)), ke = Math.min(Math.max(0, g(U, "value", 0)), ne);
      return {
        id: R.id,
        label: R.label,
        kind: R.kind,
        editable: !!this.isEditable,
        value: ke,
        max: ne,
        segments: Array.from({ length: ne }, (Me, xe) => {
          const Ze = xe + 1;
          return { value: Ze, filled: Ze <= ke };
        }),
        status: R.status ? { label: R.status.label, value: g(U, R.status.path, 0) } : null
      };
    });
    const h = Number(((X = (E = this.actor.system) == null ? void 0 : E.burn) == null ? void 0 : X.value) ?? 0), y = 10, b = 6, S = Math.min(h, y);
    n.burnOverflow = Math.max(0, h - y), n.burnPenalty = Math.floor(h / 2), n.burnPips = Array.from({ length: y }, (R, U) => {
      const ne = U + 1;
      return {
        pipValue: ne,
        filled: ne <= S,
        threshold: ne === b
      };
    }), n.combat = {
      roll: {
        initiative: JSON.stringify({ intent: "initiative" }),
        overload: JSON.stringify({ intent: "overload" })
      }
    }, n.burn = {
      value: h,
      penalty: Math.floor(h / 2),
      overflow: Math.max(0, h - 10),
      canOverloadCheck: h >= 6,
      overloaded: !!((he = (H = this.actor.system) == null ? void 0 : H.burn) != null && he.overloaded)
    };
    const w = F.getSnapshot(this.actor, { token: a });
    n.combatDashboard = {
      targeting: w.targeting,
      rollImpact: w.rollImpact,
      states: w.states,
      effects: w.effects,
      activation: w.activation,
      inactiveReason: w.inactiveReason
    };
    const T = F.buildActionModel(this.actor, w), k = new Set((T.menus ?? []).map((R) => R.id));
    Y(this, gi) && !k.has(Y(this, gi)) && Ie(this, gi, null), n.combatActions = {
      ...T,
      menus: (T.menus ?? []).map((R) => ({
        ...R,
        isOpen: R.id === Y(this, gi)
      }))
    };
    const C = ((le = (ie = this.actor).getPersonalCombatLoadout) == null ? void 0 : le.call(ie)) ?? null;
    n.personalInventory = {
      warnings: [...(C == null ? void 0 : C.warnings) ?? []],
      weapons: ((C == null ? void 0 : C.weapons) ?? []).map((R) => {
        var vt, Gt, At, Ht, st, ce, Re;
        const U = v(this, K, Ta).call(this, "weapons", R.id), ne = String((R == null ? void 0 : R.category) ?? "").trim().toLowerCase() !== "melee", ke = !!((vt = R == null ? void 0 : R.sourceState) != null && vt.isTracked), Me = String((R == null ? void 0 : R.payloadLabel) ?? "").trim() || "Unloaded", xe = ne && ke ? `${De((Gt = R == null ? void 0 : R.sourceState) == null ? void 0 : Gt.current, 0)}/${De((At = R == null ? void 0 : R.sourceState) == null ? void 0 : At.max, 0)}` : "", Ze = ne ? ke ? `${Me} ${xe}` : Me : "", $e = ne ? ke ? `Payload ${xe}` : `Payload ${Me}` : "", Fe = Cv(R.attackRatingBand), $t = vv(R.attackRatingBand), yt = Es([
          { label: "Skill", value: ((Ht = R.skillDef) == null ? void 0 : Ht.label) ?? R.skill ?? "" },
          { label: "Category", value: R.category ?? "" },
          { label: "Damage Type", value: R.damageTypeLabel ?? R.damageType ?? "" },
          { label: "Max Range", value: Ev(((st = R.range) == null ? void 0 : st.max) ?? R.defaultRangeBand ?? "") },
          { label: "CQ Bands", value: Fe },
          { label: "Payload", value: Ze },
          { label: "Traits", value: tn(R.traits ?? []).join(", ") }
        ]);
        return {
          id: R.id,
          accordionId: U,
          isExpanded: Y(this, Mi).has(U),
          name: R.name,
          img: R.img,
          subtitle: ((ce = R.skillDef) == null ? void 0 : ce.label) ?? R.category ?? "",
          summaryStats: mr([
            { label: "DV", value: De(R.damage, 0), emphasis: "strong" },
            { label: "AP", value: De(R.ap, 0) },
            { label: "Type", value: R.damageTypeLabel ?? R.damageType ?? "" },
            { label: "CQ", value: $t }
          ]),
          detailTags: vs([
            R.equipped ? "Equipped" : "",
            R.isPrimary ? "Primary" : "",
            $e,
            ...tn(R.traits ?? [])
          ]),
          detailRows: yt,
          detailText: Cs(R.notes),
          equipped: !!R.equipped,
          isPrimary: !!R.isPrimary,
          attackUuid: R.uuid ?? "",
          attackRoll: JSON.stringify({
            intent: "attack",
            weaponId: R.id,
            payloadId: ((Re = R == null ? void 0 : R.payloadState) == null ? void 0 : Re.activePayloadId) ?? "",
            edge: { pool: "physical.grit", allowed: ["pre", "post"] },
            tags: ["combat", "attack"]
          })
        };
      }),
      armor: ((C == null ? void 0 : C.armor) ?? []).map((R) => {
        var Ze, $e, Fe, $t, yt, vt, Gt, At, Ht, st, ce, Re, _, Q;
        const U = ((Ze = C == null ? void 0 : C.activeArmor) == null ? void 0 : Ze.id) === R.id ? C.activeArmor : null, ne = v(this, K, Ta).call(this, "armor", R.id), ke = De(((Fe = ($e = U == null ? void 0 : U.traitState) == null ? void 0 : $e.reinforced) == null ? void 0 : Fe.max) ?? ((yt = ($t = R == null ? void 0 : R.traitState) == null ? void 0 : $t.reinforced) == null ? void 0 : yt.max), 0), Me = ke > 0 ? `${De(((Gt = (vt = U == null ? void 0 : U.traitState) == null ? void 0 : vt.reinforced) == null ? void 0 : Gt.current) ?? ((Ht = (At = R == null ? void 0 : R.traitState) == null ? void 0 : At.reinforced) == null ? void 0 : Ht.current), 0)}/${ke}` : "", xe = Mv({
          defenseBonus: R.defenseBonus,
          mitigationByType: (U == null ? void 0 : U.mitigationByType) ?? (U == null ? void 0 : U.typedMitigation) ?? R.mitigationByType ?? {}
        });
        return {
          id: R.id,
          accordionId: ne,
          isExpanded: Y(this, Mi).has(ne),
          name: R.name,
          img: R.img,
          subtitle: (st = R.tags) != null && st.length ? R.tags.join(", ") : "Armor",
          summaryStats: mr([
            { label: "Rating", value: De((U == null ? void 0 : U.ratingCurrent) ?? R.rating, 0), emphasis: "strong" },
            { label: "Res", value: De((U == null ? void 0 : U.baseMitigation) ?? (U == null ? void 0 : U.baseResistance), 0) },
            { label: "Def", value: De(R.defenseBonus, 0) },
            { label: "Dur", value: `${De(((ce = U == null ? void 0 : U.durability) == null ? void 0 : ce.current) ?? ((Re = R.durability) == null ? void 0 : Re.current), 0)}/${De(((_ = U == null ? void 0 : U.durability) == null ? void 0 : _.max) ?? ((Q = R.durability) == null ? void 0 : Q.max), 0)}` }
          ]),
          detailTags: vs([
            R.equipped ? "Equipped" : "",
            R.isPrimary ? "Primary" : "",
            Me ? `Reinforced ${Me}` : "",
            ...tn(R.traits ?? [])
          ]),
          detailRows: Es([
            { label: "Modifiers", value: xe },
            { label: "Traits", value: tn(R.traits ?? []).join(", ") },
            { label: "Tags", value: tn(R.tags ?? []).join(", ") }
          ]),
          detailText: Cs(R.notes),
          equipped: !!R.equipped,
          isPrimary: !!R.isPrimary
        };
      }),
      gear: (((ze = n.items) == null ? void 0 : ze.gear) ?? []).map((R) => {
        const U = v(this, K, Ta).call(this, "gear", R.id);
        return Cm({
          item: R,
          accordionId: U,
          itemType: "gear",
          defaultSubtitle: "Gear",
          categoryLabels: Tv,
          ratingLabel: "Rating",
          isEditable: this.isEditable,
          isExpanded: Y(this, Mi).has(U)
        });
      }),
      // Consumables deliberately share the same quantity-tracked record model as
      // gear so stock editing and linked-source authoring stay transferable.
      consumables: (((Xe = n.items) == null ? void 0 : Xe.consumable) ?? []).map((R) => {
        const U = v(this, K, Ta).call(this, "consumables", R.id);
        return Cm({
          item: R,
          accordionId: U,
          itemType: "consumable",
          defaultSubtitle: "Consumable",
          categoryLabels: kv,
          ratingLabel: "Potency",
          typeLabel: "Consumable",
          isEditable: this.isEditable,
          isExpanded: Y(this, Mi).has(U)
        });
      })
    }, n.bio = {
      fields: ((mt = n.bio) == null ? void 0 : mt.fields) ?? {},
      faction: ((ft = m.biography) == null ? void 0 : ft.faction) ?? "",
      age: ((pt = m.biography) == null ? void 0 : pt.age) ?? "",
      rank: ((gt = m.biography) == null ? void 0 : gt.rank) ?? "",
      height: ((nt = m.biography) == null ? void 0 : nt.height) ?? "",
      weight: ((ht = m.biography) == null ? void 0 : ht.weight) ?? "",
      xpTotal: ((at = (Ye = m.counters) == null ? void 0 : Ye.xp) == null ? void 0 : at.total) ?? 0,
      xpSpent: ((_t = (Ct = m.counters) == null ? void 0 : Ct.xp) == null ? void 0 : _t.value) ?? 0,
      experienceLevel: ((xt = m.biography) == null ? void 0 : xt.experienceLevel) ?? "green",
      enrichedHistory: await foundry.applications.ux.TextEditor.implementation.enrichHTML(
        ((St = m.biography) == null ? void 0 : St.history) ?? "",
        { async: !0, secrets: this.actor.isOwner, relativeTo: this.actor }
      )
    };
    const I = Nn(this.actor);
    n.skillsDisplay = Hf(((Kt = this.actor) == null ? void 0 : Kt.system) ?? {}, {
      bonusBySkill: I.bonusBySkill
    }), n.lifeModules = I.slotStates.map((R) => {
      const U = R.state;
      return {
        moduleType: R.moduleType,
        label: R.label,
        hasCatalogEntries: R.availableEntries.length > 0,
        emptyState: R.availableEntries.length > 0 ? `Add ${R.label}` : `No ${R.label} catalog entries configured`,
        item: U ? {
          id: U.itemId,
          name: U.label,
          img: U.item.img,
          bonusLabels: [...U.selectedChoiceLabels ?? []],
          warningLabels: [...U.warningLabels ?? []],
          isActive: U.isActive,
          statusLabel: U.isActive ? "Active" : "Inactive",
          statusReason: U.inactiveReason
        } : null
      };
    });
    const O = ["positive", "negative", "narrative"], x = ["major", "minor"], B = [...((Ft = n.items) == null ? void 0 : Ft.quality) ?? []].sort((R, U) => {
      const ne = Ii(R.system ?? {}), ke = Ii(U.system ?? {}), Me = O.indexOf(ne.category) - O.indexOf(ke.category);
      if (Me !== 0) return Me;
      const xe = x.indexOf(ne.tier) - x.indexOf(ke.tier);
      return xe !== 0 ? xe : String(R.name ?? "").localeCompare(String(U.name ?? ""));
    });
    return n.qualityGroups = O.map((R) => ({
      id: R,
      label: ds(R),
      records: B.filter((U) => Ii(U.system ?? {}).category === R).map((U) => {
        var Me, xe, Ze, $e;
        const ne = Ii(U.system ?? {}), ke = v(this, K, Ta).call(this, "quality", U.id);
        return {
          id: U.id,
          accordionId: ke,
          isExpanded: Y(this, Mi).has(ke),
          name: U.name,
          img: U.img,
          subtitle: `${ms(ne.tier)} ${ds(ne.category)}`,
          summaryStats: mr([
            { label: "Tier", value: ms(ne.tier), emphasis: "strong" },
            { label: "Activation", value: ne.activation || "passive" },
            { label: "Effects", value: String(((Me = ne.effects) == null ? void 0 : Me.length) ?? 0) }
          ]),
          detailTags: vs([
            ne.inactive ? "Inactive" : "",
            ...ne.tags ?? []
          ]),
          detailRows: Es([
            { label: "Category", value: ds(ne.category) },
            { label: "Tier", value: ms(ne.tier) },
            { label: "Activation", value: ne.activation || "passive" },
            { label: "Prerequisites", value: String(((xe = ne.prerequisites) == null ? void 0 : xe.length) ?? 0) },
            { label: "Effects", value: String(((Ze = ne.effects) == null ? void 0 : Ze.length) ?? 0) },
            { label: "Tags", value: tn(ne.tags ?? []).join(", ") }
          ]),
          detailText: Cs(($e = U.system) == null ? void 0 : $e.description)
        };
      })
    })), n.assignedMech = await this._buildAssignedMech(), n;
  }
  async _buildAssignedMech() {
    var s;
    const t = ((s = this.getPersistentActor) == null ? void 0 : s.call(this)) ?? this.actor, n = q.mwd.weightClass, a = [];
    for (const o of Rv())
      await Nv(o.actor, t) && a.push(o);
    const r = a.map(({ actor: o, uuid: l }) => {
      var V, Z, E, X, H, he, ie, le, ze, Xe, mt, ft, pt, gt, nt;
      const c = o.type === "battlemech", u = ((Z = (V = o.system) == null ? void 0 : V.monitors) == null ? void 0 : Z.structure) ?? {}, d = ((X = (E = o.system) == null ? void 0 : E.monitors) == null ? void 0 : X.armor) ?? {}, m = ((he = (H = o.system) == null ? void 0 : H.mwd) == null ? void 0 : he.heat) ?? {}, f = ((le = (ie = o.system) == null ? void 0 : ie.mwd) == null ? void 0 : le.heatStatus) ?? {}, p = ((Xe = (ze = o.system) == null ? void 0 : ze.mwd) == null ? void 0 : Xe.crits) ?? [], g = ((mt = o.system) == null ? void 0 : mt.quickActions) ?? {}, h = (ht, Ye, at, Ct) => {
        var St;
        const _t = Math.max(0, De(Ct.value, 0)), xt = Math.max(0, De(Ct.max, 0));
        return {
          id: ht,
          label: Ye,
          kind: at,
          value: _t,
          max: xt,
          resistance: De((St = Ct.resistance) == null ? void 0 : St.default, 0),
          segments: Array.from({ length: xt }, (Kt, Ft) => {
            const R = Ft + 1;
            return { value: R, filled: R <= _t };
          })
        };
      }, y = Math.max(0, De(m.current, 0)), b = Math.max(0, De(m.trackLength ?? m.max, 0)), S = m.thresholds ?? {}, w = Math.max(
        b,
        y,
        De(S.shutdown ?? S.danger, 0)
      ), T = c ? {
        current: y,
        trackLength: b,
        displayMax: w,
        status: f.label ?? f.code ?? "safe",
        segments: Array.from({ length: w }, (ht, Ye) => {
          const at = Ye + 1;
          return {
            value: at,
            filled: at <= y,
            breakpoint: tn([
              at === De(S.runningHot, 0) ? "runningHot" : "",
              at === De(S.overheated, 0) ? "overheated" : "",
              at === De(S.shutdown, 0) ? "shutdown" : ""
            ]).join(" ")
          };
        })
      } : null, k = rh(p), C = c ? [h("structure", "Structure", "wound", u), h("armor", "Armor", "armor", d)] : [h("structure", "Structure", "wound", u)], I = Array.isArray((ft = o.system) == null ? void 0 : ft.weaponGroups) && o.system.weaponGroups.length > 0, O = Array.isArray((pt = o.system) == null ? void 0 : pt.meleeProfiles) && o.system.meleeProfiles.length > 0, x = g.primaryWeaponGroup ?? null, B = c ? [
        { label: "Primary", hint: (x == null ? void 0 : x.name) ?? "Primary weapon group", handler: "mechAttack", disabled: !x, dataset: { attackKind: "primary", groupId: (x == null ? void 0 : x.id) ?? "", mechUuid: l, mechId: o.id } },
        { label: "Ranged", hint: "Prompt for a weapon group", handler: "mechAttack", disabled: !I, dataset: { attackKind: "ranged", mechUuid: l, mechId: o.id } },
        { label: "Melee", hint: "Prompt for a melee profile", handler: "mechAttack", disabled: !O, dataset: { attackKind: "melee", mechUuid: l, mechId: o.id } },
        { label: "Piloting", hint: "Vehicle handling test", handler: "mechRoll", disabled: !1, dataset: { rollKind: "piloting", mechUuid: l, mechId: o.id } },
        { label: "EW", hint: "Acquire or generate fire solution", handler: "mechRoll", disabled: !g.hasSensorSweep, dataset: { rollKind: "sensor", mechUuid: l, mechId: o.id } },
        { label: "Repair", hint: "Choose a crit or repairable status", handler: "mechRoll", disabled: !1, dataset: { rollKind: "repair", mechUuid: l, mechId: o.id } }
      ] : [], P = Math.max(0, De(d.max, 0)), L = Math.max(0, P - De(d.value, 0)), D = Math.max(0, De(u.max, 0)), G = Math.max(0, D - De(u.value, 0));
      return {
        id: o.id,
        uuid: l,
        name: o.name,
        typeLabel: c ? "BattleMech" : "Vehicle",
        isMech: c,
        weightLabel: n[(nt = (gt = o.system) == null ? void 0 : gt.mwd) == null ? void 0 : nt.weightClass] ?? "",
        summaryStats: mr([
          ...c ? [{ label: "Armor", value: `${L} / ${P}` }] : [],
          { label: "Structure", value: `${G} / ${D}` },
          { label: "Heat", value: c ? String(y) : null },
          { label: "Status", value: k.count > 0 ? k.value : "OK" }
        ]),
        conditionMonitors: C,
        heat: T,
        critCount: p.length,
        quickActions: B
      };
    });
    return { mechs: r, hasMech: r.length > 0 };
  }
  async _onOpenAssignedMech(t, n) {
    const a = await sl(n);
    a && a.sheet.render(!0, { focus: !0 });
  }
  async _onMechAttack(t, n) {
    var l, c, u, d, m, f, p;
    (l = t == null ? void 0 : t.preventDefault) == null || l.call(t), (c = t == null ? void 0 : t.stopPropagation) == null || c.call(t);
    const a = await sl(n);
    if (!a) return;
    const r = String(((u = n == null ? void 0 : n.dataset) == null ? void 0 : u.attackKind) ?? "").trim(), s = String(((d = n == null ? void 0 : n.dataset) == null ? void 0 : d.groupId) ?? "").trim(), o = ((m = this.actor) == null ? void 0 : m.uuid) ?? "";
    try {
      r === "melee" ? await ((f = a.rollMeleeAttack) == null ? void 0 : f.call(a, { operatorActorUuid: o })) : await ((p = a.rollRangedAttack) == null ? void 0 : p.call(a, {
        groupId: s,
        operatorActorUuid: o
      }));
    } catch (g) {
      sn(g, "Unable to launch BattleMech attack.");
    }
  }
  async _onMechRoll(t, n) {
    var o, l, c, u, d, m, f, p;
    (o = t == null ? void 0 : t.preventDefault) == null || o.call(t), (l = t == null ? void 0 : t.stopPropagation) == null || l.call(t);
    const a = await sl(n);
    if (!a) return;
    const r = String(((c = n == null ? void 0 : n.dataset) == null ? void 0 : c.rollKind) ?? "").trim(), s = ((u = this.actor) == null ? void 0 : u.uuid) ?? "";
    try {
      r === "piloting" ? await ((d = a.rollPilotingCheck) == null ? void 0 : d.call(a, { operatorActorUuid: s })) : r === "sensor" ? await (((m = a.rollElectronicWarfare) == null ? void 0 : m.call(a, { operatorActorUuid: s })) ?? ((f = a.rollSensorSweep) == null ? void 0 : f.call(a, { operatorActorUuid: s }))) : r === "repair" && await ((p = a.rollEmergencyRepair) == null ? void 0 : p.call(a, { operatorActorUuid: s }));
    } catch (g) {
      sn(g, "Unable to launch BattleMech check.");
    }
  }
  _onRender(t, n) {
    super._onRender(t, n), v(this, K, Sh).call(this), v(this, K, wh).call(this), v(this, K, Th).call(this), v(this, K, bh).call(this);
  }
  async close(t = {}) {
    return v(this, K, mc).call(this), v(this, K, fc).call(this), Y(this, kn) !== null && (Hooks.off("updateActor", Y(this, kn)), Ie(this, kn, null)), super.close(t);
  }
  requestCombatDashboardRefresh() {
    v(this, K, Je).call(this, { force: !0 });
  }
  async _onEdgeSet(t, n) {
    var c, u, d;
    if (t.preventDefault(), t.stopPropagation(), !this.isEditable) return;
    const a = ((c = n == null ? void 0 : n.closest) == null ? void 0 : c.call(n, "[data-edge-pool][data-edge-value]")) ?? ((d = (u = t == null ? void 0 : t.target) == null ? void 0 : u.closest) == null ? void 0 : d.call(u, "[data-edge-pool][data-edge-value]"));
    if (!a) return;
    const r = String(a.dataset.edgePool ?? "").trim(), s = Number(a.dataset.edgeValue ?? NaN);
    if (!r || !Number.isFinite(s)) return;
    const o = this.actor.getEdgePool(r);
    if (!(o != null && o.hasPools)) return;
    let l = s;
    return s === o.effectiveValue && (l = s - 1), (t.button === 2 || t.type === "contextmenu") && (l = 0), t.altKey && (l = 0), t.shiftKey && (l = o.effectiveMax), this.actor.setEdgePoolValue(r, l);
  }
  async _onToggleCombatMenu(t, n) {
    var r, s, o, l, c, u, d;
    (r = t == null ? void 0 : t.preventDefault) == null || r.call(t), (s = t == null ? void 0 : t.stopPropagation) == null || s.call(t);
    const a = String(
      ((o = n == null ? void 0 : n.dataset) == null ? void 0 : o.combatMenu) ?? ((d = (u = (c = (l = t == null ? void 0 : t.target) == null ? void 0 : l.closest) == null ? void 0 : c.call(l, "[data-combat-menu]")) == null ? void 0 : u.dataset) == null ? void 0 : d.combatMenu) ?? ""
    ).trim();
    a && (Ie(this, gi, Y(this, gi) === a ? null : a), v(this, K, Je).call(this, !1));
  }
  async _onToggleStatuses(t, n) {
    var s, o, l, c, u, d, m, f;
    if ((s = t == null ? void 0 : t.preventDefault) == null || s.call(t), (o = t == null ? void 0 : t.stopPropagation) == null || o.call(t), v(this, K, wi).call(this, n, t, "Statuses are not available right now.") || !this.isEditable) return;
    const a = this.getPersistentActor() ?? this.actor, r = ((l = this.getSheetTokenDocument) == null ? void 0 : l.call(this)) ?? ((u = F.getSnapshot(a, { token: ((c = this.getSheetTokenDocument) == null ? void 0 : c.call(this)) ?? null })) == null ? void 0 : u.tokenDocument) ?? ((m = F.getSnapshot(this.actor, { token: ((d = this.getSheetTokenDocument) == null ? void 0 : d.call(this)) ?? null })) == null ? void 0 : m.tokenDocument) ?? null;
    if (!r) {
      (f = ui.notifications) == null || f.warn("Statuses require a token for this actor on the current scene.");
      return;
    }
    return Xf({
      actor: a,
      token: r
    });
  }
  async _onCombatSpend(t, n) {
    var c, u, d, m, f, p, g, h, y, b;
    if ((c = t == null ? void 0 : t.preventDefault) == null || c.call(t), (u = t == null ? void 0 : t.stopPropagation) == null || u.call(t), v(this, K, wi).call(this, n, t, "That combat action is not available right now.") || !this.isEditable) return;
    const a = String(((d = n == null ? void 0 : n.dataset) == null ? void 0 : d.resource) ?? "").trim(), r = Math.max(0, Number(((m = n == null ? void 0 : n.dataset) == null ? void 0 : m.cost) ?? 0)), s = String(((f = n == null ? void 0 : n.dataset) == null ? void 0 : f.combatAction) ?? "").trim(), o = String(((p = n == null ? void 0 : n.dataset) == null ? void 0 : p.combatLabel) ?? "").trim(), l = String(((g = n == null ? void 0 : n.dataset) == null ? void 0 : g.combatCostLabel) ?? "").trim();
    if (!(!a || !r || !s))
      try {
        const S = this.getPersistentActor() ?? this.actor, w = await F.spendResource(S, {
          token: ((h = this.getSheetTokenDocument) == null ? void 0 : h.call(this)) ?? F.getCurrentSceneTokenDocument(S) ?? F.getCurrentSceneTokenDocument(this.actor),
          resource: a,
          cost: r,
          actionId: s,
          actionLabel: o,
          actionCostLabel: l
        });
        if (!(w != null && w.ok)) {
          (y = ui.notifications) == null || y.warn((w == null ? void 0 : w.reason) ?? "Unable to spend action.");
          return;
        }
        v(this, K, Ai).call(this, { rerender: !1 }), v(this, K, Je).call(this, { force: !0 });
      } catch (S) {
        console.error("MWD | Failed to spend combat action", S), (b = ui.notifications) == null || b.error("Unable to spend action.");
      }
  }
  async _onCombatAction(t, n) {
    var r, s, o, l, c, u;
    if ((r = t == null ? void 0 : t.preventDefault) == null || r.call(t), (s = t == null ? void 0 : t.stopPropagation) == null || s.call(t), v(this, K, wi).call(this, n, t, "That combat action is not available right now.") || !this.isEditable) return;
    const a = String(((o = n == null ? void 0 : n.dataset) == null ? void 0 : o.combatAction) ?? "").trim();
    if (a)
      try {
        const d = this.getPersistentActor() ?? this.actor, m = await v(this, K, kh).call(this, a);
        if (!m) return;
        const f = await F.executeAction(d, {
          token: ((l = this.getSheetTokenDocument) == null ? void 0 : l.call(this)) ?? F.getCurrentSceneTokenDocument(d) ?? F.getCurrentSceneTokenDocument(this.actor),
          actionId: a,
          metadata: m
        });
        if (!(f != null && f.ok)) {
          (c = ui.notifications) == null || c.warn((f == null ? void 0 : f.reason) ?? "Unable to perform action.");
          return;
        }
        v(this, K, Ai).call(this, { rerender: !1 }), v(this, K, Je).call(this, { force: !0 });
      } catch (d) {
        console.error("MWD | Failed to perform combat action", d), (u = ui.notifications) == null || u.error("Unable to perform action.");
      }
  }
  async _onCombatReduceBurn(t, n) {
    var a, r, s, o, l;
    if ((a = t == null ? void 0 : t.preventDefault) == null || a.call(t), (r = t == null ? void 0 : t.stopPropagation) == null || r.call(t), !v(this, K, wi).call(this, n, t, "Burn recovery is not available right now.") && this.isEditable)
      try {
        const c = this.getPersistentActor() ?? this.actor, u = await F.reduceBurn(c, {
          token: ((s = this.getSheetTokenDocument) == null ? void 0 : s.call(this)) ?? F.getCurrentSceneTokenDocument(c) ?? F.getCurrentSceneTokenDocument(this.actor)
        });
        if (!(u != null && u.ok)) {
          (o = ui.notifications) == null || o.warn((u == null ? void 0 : u.reason) ?? "Unable to reduce Burn.");
          return;
        }
        v(this, K, Ai).call(this, { rerender: !1 }), v(this, K, Je).call(this, { force: !0 });
      } catch (c) {
        console.error("MWD | Failed to reduce Burn", c), (l = ui.notifications) == null || l.error("Unable to reduce Burn.");
      }
  }
  async _onCombatAssist(t, n) {
    var a, r, s, o, l, c, u;
    if ((a = t == null ? void 0 : t.preventDefault) == null || a.call(t), (r = t == null ? void 0 : t.stopPropagation) == null || r.call(t), !v(this, K, wi).call(this, n, t, "Assist is not available right now.") && this.isEditable)
      try {
        const d = this.getPersistentActor() ?? this.actor, m = ((s = this.getSheetTokenDocument) == null ? void 0 : s.call(this)) ?? F.getCurrentSceneTokenDocument(d) ?? F.getCurrentSceneTokenDocument(this.actor), f = F.getSnapshot(d, { token: m });
        if (!f.hasCombatant) {
          (o = ui.notifications) == null || o.warn("No combatant on the current scene.");
          return;
        }
        if (f.isCurrentTurn) {
          (l = ui.notifications) == null || l.warn("Only outside your activation.");
          return;
        }
        const p = await v(this, K, Eh).call(this, f);
        if (!p) return;
        const g = await F.executeAction(d, {
          token: m,
          actionId: "assist",
          metadata: {
            targetCombatantId: p.combatantId,
            targetActorUuid: p.actorUuid,
            targetTokenUuid: p.tokenUuid,
            targetName: p.name
          }
        });
        if (!(g != null && g.ok)) {
          (c = ui.notifications) == null || c.warn((g == null ? void 0 : g.reason) ?? "Unable to assist.");
          return;
        }
        await v(this, K, Ih).call(this, {
          actor: d,
          token: m,
          target: p,
          costLabel: g.costLabel
        }), v(this, K, Ai).call(this, { rerender: !1 }), v(this, K, Je).call(this, { force: !0 });
      } catch (d) {
        console.error("MWD | Failed to assist", d), (u = ui.notifications) == null || u.error("Unable to assist.");
      }
  }
  async _onCombatEvade(t, n) {
    var a, r, s, o, l;
    if ((a = t == null ? void 0 : t.preventDefault) == null || a.call(t), (r = t == null ? void 0 : t.stopPropagation) == null || r.call(t), !v(this, K, wi).call(this, n, t, "Evade is not available right now.") && this.isEditable)
      try {
        const c = this.getPersistentActor() ?? this.actor, u = ((s = this.getSheetTokenDocument) == null ? void 0 : s.call(this)) ?? F.getCurrentSceneTokenDocument(c) ?? F.getCurrentSceneTokenDocument(this.actor), d = await fv(c, { token: u });
        if (!(d != null && d.ok)) {
          (o = ui.notifications) == null || o.warn((d == null ? void 0 : d.reason) ?? "Unable to activate Evade.");
          return;
        }
        v(this, K, Ai).call(this, { rerender: !1 }), v(this, K, Je).call(this, { force: !0 });
      } catch (c) {
        console.error("MWD | Failed to activate Evade", c), (l = ui.notifications) == null || l.error("Unable to activate Evade.");
      }
  }
  async _onCombatInterrupt(t, n) {
    var a, r, s, o, l, c, u, d;
    if ((a = t == null ? void 0 : t.preventDefault) == null || a.call(t), (r = t == null ? void 0 : t.stopPropagation) == null || r.call(t), !v(this, K, wi).call(this, n, t, "Interrupt is not available right now.") && this.isEditable)
      try {
        const m = this.getPersistentActor() ?? this.actor, f = ((s = this.getSheetTokenDocument) == null ? void 0 : s.call(this)) ?? F.getCurrentSceneTokenDocument(m) ?? F.getCurrentSceneTokenDocument(this.actor), p = F.getSnapshot(m, { token: f }), g = F.getPreparedInterrupt(p);
        if (!p.hasCombatant) {
          (o = ui.notifications) == null || o.warn("No combatant on the current scene.");
          return;
        }
        if (p.isCurrentTurn) {
          (l = ui.notifications) == null || l.warn("Only outside your activation.");
          return;
        }
        if (!g) {
          (c = ui.notifications) == null || c.warn("Prepare an interrupt first.");
          return;
        }
        if (!await v(this, K, Mh).call(this, g)) return;
        const y = await F.executeAction(m, {
          token: f,
          actionId: "interrupt",
          metadata: g
        });
        if (!(y != null && y.ok)) {
          (u = ui.notifications) == null || u.warn((y == null ? void 0 : y.reason) ?? "Unable to interrupt.");
          return;
        }
        await F.clearPreparedInterrupt(m, { token: f }), await v(this, K, Nh).call(this, {
          actor: m,
          token: f,
          preparedInterrupt: g,
          costLabel: y.costLabel
        }), v(this, K, Ai).call(this, { rerender: !1 }), v(this, K, Je).call(this, { force: !0 });
      } catch (m) {
        console.error("MWD | Failed to interrupt", m), (d = ui.notifications) == null || d.error("Unable to interrupt.");
      }
  }
  async _onCombatOverloadCheck(t, n) {
    var s, o, l, c, u, d, m, f, p, g, h;
    if ((s = t == null ? void 0 : t.preventDefault) == null || s.call(t), (o = t == null ? void 0 : t.stopPropagation) == null || o.call(t), v(this, K, wi).call(this, n, t, "Overload check is not available right now.") || !this.isEditable) return;
    const a = ((l = n == null ? void 0 : n.dataset) == null ? void 0 : l.roll) ?? ((m = (d = (u = (c = t == null ? void 0 : t.target) == null ? void 0 : c.closest) == null ? void 0 : u.call(c, "[data-roll]")) == null ? void 0 : d.dataset) == null ? void 0 : m.roll);
    if (!a) return;
    let r;
    try {
      r = JSON.parse(a);
    } catch (y) {
      console.warn("MWD | Invalid overload payload", a, y);
      return;
    }
    try {
      const y = this.getPersistentActor() ?? this.actor, b = await ((g = (p = (f = game.mwd) == null ? void 0 : f.roll) == null ? void 0 : p.execute) == null ? void 0 : g.call(p, { actor: y, payload: r, event: t }));
      if (v(this, K, Ai).call(this, { rerender: !1 }), !b) {
        v(this, K, Je).call(this, !1);
        return;
      }
      v(this, K, Je).call(this, { force: !0 });
    } catch (y) {
      console.error("MWD | Failed to launch overload check", y), (h = ui.notifications) == null || h.error("Unable to launch overload check.");
    }
  }
  async _onCombatAttack(t, n) {
    var m, f, p, g, h, y, b, S, w, T, k, C, I, O, x, B, P, L, D, G, V, Z, E;
    if ((m = t == null ? void 0 : t.preventDefault) == null || m.call(t), (f = t == null ? void 0 : t.stopPropagation) == null || f.call(t), v(this, K, wi).call(this, n, t, "Attack is not available right now.") || !this.isEditable) return;
    const a = this.getPersistentActor() ?? this.actor, r = ((p = this.getSheetTokenDocument) == null ? void 0 : p.call(this)) ?? F.getCurrentSceneTokenDocument(a) ?? F.getCurrentSceneTokenDocument(this.actor), s = String(((g = n == null ? void 0 : n.dataset) == null ? void 0 : g.combatAction) ?? "attack").trim() || "attack", o = String(((h = n == null ? void 0 : n.dataset) == null ? void 0 : h.combatLabel) ?? (s === "opportunity" ? "Opportunity" : "Attack")).trim() || "Attack", l = s === "opportunity", c = F.getSnapshot(a, { token: r }), u = !!((b = (y = c.state) == null ? void 0 : y.actionState) != null && b.aim);
    if (!c.hasCombatant) {
      (S = ui.notifications) == null || S.warn("No combatant on the current scene.");
      return;
    }
    if (l && c.isCurrentTurn) {
      (w = ui.notifications) == null || w.warn("Only outside your activation.");
      return;
    }
    if (!l && !c.isCurrentTurn) {
      (T = ui.notifications) == null || T.warn("Only available during your activation.");
      return;
    }
    if (!l && c.overloaded) {
      (k = ui.notifications) == null || k.warn("Overloaded actors can only recover Burn.");
      return;
    }
    if (!l) {
      const X = 3 + Math.floor((Math.max(0, Number(((O = (I = (C = a.system) == null ? void 0 : C.attributes) == null ? void 0 : I.reflexes) == null ? void 0 : O.value) ?? 0)) + Math.max(0, Number(((P = (B = (x = a.system) == null ? void 0 : x.attributes) == null ? void 0 : B.willpower) == null ? void 0 : P.value) ?? 0))) / 2);
      if (Math.max(0, X - Math.max(0, Number(((L = c.state) == null ? void 0 : L.saSpentThisActivation) ?? 0))) < 2) {
        (D = ui.notifications) == null || D.warn("Activation SA cap reached.");
        return;
      }
    }
    const d = {
      intent: "attack",
      mode: "auto",
      fallback: "unarmed",
      edge: { pool: "physical.grit", allowed: ["pre", "post"] },
      tags: l ? ["combat", "attack", "reaction", "opportunity"] : ["combat", "attack"],
      aim: u ? { active: !0 } : null,
      sourceTokenId: (r == null ? void 0 : r.id) ?? null
    };
    try {
      const X = await ((Z = (V = (G = game.mwd) == null ? void 0 : G.roll) == null ? void 0 : V.execute) == null ? void 0 : Z.call(V, { actor: a, payload: d, event: t }));
      if (v(this, K, Ai).call(this, { rerender: !1 }), !X) {
        v(this, K, Je).call(this, !1);
        return;
      }
      u && await F.clearAim(a, { token: r });
      const H = l ? await F.executeAction(a, {
        token: r,
        actionId: "opportunity"
      }) : await F.spendResource(a, {
        token: r,
        resource: "sa",
        cost: 2,
        actionId: "attack",
        actionLabel: "Attack",
        actionCostLabel: "2 SA",
        actionCategory: "complex"
      });
      H != null && H.ok || (E = ui.notifications) == null || E.warn((H == null ? void 0 : H.reason) ?? `Unable to spend ${o} action.`), v(this, K, Je).call(this, { force: !0 });
    } catch (X) {
      console.error(`MWD | Failed to launch ${o}`, X), sn(X, `Unable to launch ${o}.`);
    }
  }
  async _onAddSkillSpecialization(t, n) {
    var d, m, f, p;
    if ((d = t == null ? void 0 : t.preventDefault) == null || d.call(t), (m = t == null ? void 0 : t.stopPropagation) == null || m.call(t), !this.isEditable || !this.editing) return;
    const a = String(((f = n == null ? void 0 : n.dataset) == null ? void 0 : f.skillKey) ?? "").trim();
    if (!a) return;
    const r = this.getPersistentActor() ?? this.actor, s = Ol(r.system ?? {}, a), o = co(r.system ?? {}, a), l = ta(a).filter((g) => !o.includes(g.key));
    if (l.length === 0) return;
    let c = ((p = l[0]) == null ? void 0 : p.key) ?? "";
    if (l.length > 1) {
      const g = `<form class="mwd-quick-select"><div class="mwd-field"><label>Specialization</label><select name="specialization">${l.map((h) => `<option value="${h.key}">${h.label}</option>`).join("")}</select></div></form>`;
      c = await foundry.applications.api.DialogV2.prompt({
        window: { title: "Add Skill Specialization" },
        content: g,
        ok: {
          label: "Add",
          callback: (h, y) => {
            var b, S;
            return ((b = y.form.elements.specialization) == null ? void 0 : b.value) ?? ((S = l[0]) == null ? void 0 : S.key) ?? "";
          }
        }
      });
    }
    const u = xs(
      s.concat([c])
    );
    await r.update({
      [`system.skills.${a}.specializations`]: u
    }), v(this, K, Je).call(this, { force: !0 });
  }
  async _onRemoveSkillSpecialization(t, n) {
    var l, c, u, d;
    if ((l = t == null ? void 0 : t.preventDefault) == null || l.call(t), (c = t == null ? void 0 : t.stopPropagation) == null || c.call(t), !this.isEditable || !this.editing) return;
    const a = String(((u = n == null ? void 0 : n.dataset) == null ? void 0 : u.skillKey) ?? "").trim(), r = String(((d = n == null ? void 0 : n.dataset) == null ? void 0 : d.specializationKey) ?? "").trim();
    if (!a || !r) return;
    const s = this.getPersistentActor() ?? this.actor, o = xs(
      Ol(s.system ?? {}, a).filter((m) => m !== r)
    );
    await s.update({
      [`system.skills.${a}.specializations`]: o
    }), v(this, K, Je).call(this, { force: !0 });
  }
  async _onCreateLifeModuleItem(t, n) {
    var d, m, f, p, g;
    if ((d = t == null ? void 0 : t.preventDefault) == null || d.call(t), (m = t == null ? void 0 : t.stopPropagation) == null || m.call(t), !this.isEditable) return;
    const a = String(((f = n == null ? void 0 : n.dataset) == null ? void 0 : f.moduleType) ?? "").trim();
    if (!a) return;
    const r = this.getPersistentActor() ?? this.actor, s = Nu(a);
    if (!s.length) {
      (p = ui.notifications) == null || p.warn(`No ${za(a)} life modules are configured in game settings.`);
      return;
    }
    const o = await vm({
      title: `Choose ${za(a)} Life Module`,
      label: "Life Module",
      confirmLabel: "Create",
      options: s.map((h) => ({
        value: h.id,
        label: h.label
      }))
    });
    if (!o) return;
    const l = En(o);
    if (!l) {
      (g = ui.notifications) == null || g.warn("That life module catalog entry no longer exists.");
      return;
    }
    const c = wg(l, {}), u = {};
    for (const h of c.filter((y) => y.hasMultipleChoices)) {
      const y = await vm({
        title: `Choose Bonus for ${l.label}`,
        label: h.label,
        confirmLabel: "Apply",
        options: h.options.map((b) => ({
          value: b.value,
          label: b.label
        }))
      });
      if (!y) return;
      u[h.id] = y;
    }
    await r.createEmbeddedDocuments("Item", [{
      name: l.label,
      type: "lifeModule",
      system: Dr({
        moduleType: a,
        catalogId: l.id,
        selectedGrants: u
      })
    }]), v(this, K, Je).call(this, { force: !0 });
  }
  async _onCreateOwnedItem(t, n) {
    var l, c, u;
    if ((l = t == null ? void 0 : t.preventDefault) == null || l.call(t), (c = t == null ? void 0 : t.stopPropagation) == null || c.call(t), !this.isEditable) return;
    const a = String(((u = n == null ? void 0 : n.dataset) == null ? void 0 : u.itemType) ?? "").trim();
    if (!a) return;
    const r = this.getPersistentActor() ?? this.actor, s = r.items.filter((d) => d.type === a).length, o = a === "personalWeapon" ? "Personal Weapon" : a === "armor" ? "Armor" : a === "consumable" ? "Consumable" : a.charAt(0).toUpperCase() + a.slice(1);
    await r.createEmbeddedDocuments("Item", [{
      name: `${o} ${s + 1}`,
      type: a
    }]), v(this, K, Je).call(this, { force: !0 });
  }
  async _onEditOwnedItem(t, n) {
    var r, s, o;
    (r = t == null ? void 0 : t.preventDefault) == null || r.call(t), (s = t == null ? void 0 : t.stopPropagation) == null || s.call(t);
    const a = v(this, K, hn).call(this, n, t);
    (o = a == null ? void 0 : a.sheet) == null || o.render(!0);
  }
  async _onDeleteOwnedItem(t, n) {
    var s, o;
    if ((s = t == null ? void 0 : t.preventDefault) == null || s.call(t), (o = t == null ? void 0 : t.stopPropagation) == null || o.call(t), !this.isEditable) return;
    const a = v(this, K, hn).call(this, n, t);
    if (!a) return;
    await (this.getPersistentActor() ?? this.actor).deleteEmbeddedDocuments("Item", [a.id]), v(this, K, Je).call(this, { force: !0 });
  }
  async _onToggleInventoryAccordion(t, n) {
    var r, s, o, l, c, u, d, m, f, p;
    (r = t == null ? void 0 : t.preventDefault) == null || r.call(t), (s = t == null ? void 0 : t.stopPropagation) == null || s.call(t);
    const a = String(
      ((o = n == null ? void 0 : n.dataset) == null ? void 0 : o.accordionId) ?? ((u = (c = (l = n == null ? void 0 : n.closest) == null ? void 0 : l.call(n, "[data-accordion-id]")) == null ? void 0 : c.dataset) == null ? void 0 : u.accordionId) ?? ((p = (f = (m = (d = t == null ? void 0 : t.target) == null ? void 0 : d.closest) == null ? void 0 : m.call(d, "[data-accordion-id]")) == null ? void 0 : f.dataset) == null ? void 0 : p.accordionId) ?? ""
    ).trim();
    a && (Y(this, Mi).has(a) ? Y(this, Mi).delete(a) : Y(this, Mi).add(a), v(this, K, Je).call(this, !1));
  }
  async _onToggleOwnedItemEquipped(t, n) {
    var s, o, l, c;
    if ((s = t == null ? void 0 : t.preventDefault) == null || s.call(t), (o = t == null ? void 0 : t.stopPropagation) == null || o.call(t), !this.isEditable) return;
    const a = v(this, K, hn).call(this, n, t);
    if (!a) return;
    const r = this.getPersistentActor() ?? this.actor;
    await ((c = r.setOwnedItemEquipped) == null ? void 0 : c.call(r, a.id, !((l = a.system) != null && l.equipped))), v(this, K, Je).call(this, { force: !0 });
  }
  async _onSetOwnedItemPrimary(t, n) {
    var s, o, l, c;
    if ((s = t == null ? void 0 : t.preventDefault) == null || s.call(t), (o = t == null ? void 0 : t.stopPropagation) == null || o.call(t), !this.isEditable) return;
    const a = v(this, K, hn).call(this, n, t);
    if (!a) return;
    const r = this.getPersistentActor() ?? this.actor;
    await ((c = r.setOwnedItemPrimary) == null ? void 0 : c.call(r, a.id, !((l = a.system) != null && l.isPrimary))), v(this, K, Je).call(this, { force: !0 });
  }
  async _onAdjustGearQuantity(t, n) {
    var c, u, d, m, f, p, g, h, y, b, S;
    if ((c = t == null ? void 0 : t.preventDefault) == null || c.call(t), (u = t == null ? void 0 : t.stopPropagation) == null || u.call(t), !this.isEditable) return;
    const a = v(this, K, hn).call(this, n, t);
    if (!a || !["gear", "consumable"].includes(String(a.canonicalType ?? a.type ?? "").trim())) return;
    const r = Math.trunc(Number(
      ((d = n == null ? void 0 : n.dataset) == null ? void 0 : d.delta) ?? ((p = (f = (m = n == null ? void 0 : n.closest) == null ? void 0 : m.call(n, "[data-delta]")) == null ? void 0 : f.dataset) == null ? void 0 : p.delta) ?? ((b = (y = (h = (g = t == null ? void 0 : t.target) == null ? void 0 : g.closest) == null ? void 0 : h.call(g, "[data-delta]")) == null ? void 0 : y.dataset) == null ? void 0 : b.delta) ?? 0
    ) || 0);
    if (!r) return;
    const o = (this.getPersistentActor() ?? this.actor).items.get(a.id) ?? a, l = Math.max(0, Math.trunc(Number(((S = o.system) == null ? void 0 : S.quantity) ?? 1) || 0) + r);
    await o.update({ "system.quantity": l }), v(this, K, Je).call(this, { force: !0 });
  }
  async _onAttackWeapon(t, n) {
    var l, c, u, d;
    if ((l = t == null ? void 0 : t.preventDefault) == null || l.call(t), (c = t == null ? void 0 : t.stopPropagation) == null || c.call(t), v(this, K, wi).call(this, n, t, "Equip that weapon before attacking.")) return;
    const a = v(this, K, hn).call(this, n, t);
    if (!((u = a == null ? void 0 : a.isPersonalWeapon) != null && u.call(a))) return;
    const r = this.getPersistentActor() ?? this.actor, s = ((d = this.getSheetTokenDocument) == null ? void 0 : d.call(this)) ?? F.getCurrentSceneTokenDocument(r) ?? F.getCurrentSceneTokenDocument(this.actor);
    await Do({ weapon: a, event: t, token: s }) && v(this, K, Je).call(this, { force: !0 });
  }
};
gi = new WeakMap(), Tn = new WeakMap(), qn = new WeakMap(), Mi = new WeakMap(), Ra = new WeakMap(), kn = new WeakMap(), K = new WeakSet(), bh = function() {
  var a;
  if (Y(this, kn) !== null) return;
  const t = ((a = this.getPersistentActor) == null ? void 0 : a.call(this)) ?? this.actor, n = uc(t);
  Ie(this, kn, Hooks.on("updateActor", (r, s) => {
    var m, f, p, g, h, y, b, S, w, T;
    if (r.type !== "battlemech" && r.type !== "vehicle") return;
    const o = String(((f = (m = s == null ? void 0 : s.system) == null ? void 0 : m.pilot) == null ? void 0 : f.uuid) ?? ((h = (g = (p = s == null ? void 0 : s.system) == null ? void 0 : p.mwd) == null ? void 0 : g.pilot) == null ? void 0 : h.uuid) ?? "").trim(), l = String(((b = (y = r.system) == null ? void 0 : y.pilot) == null ? void 0 : b.uuid) ?? ((T = (w = (S = r.system) == null ? void 0 : S.mwd) == null ? void 0 : w.pilot) == null ? void 0 : T.uuid) ?? "").trim(), c = o !== "", u = n.has(l), d = n.has(o);
    !c && !u || (c || u || d) && this.render();
  }));
}, Sh = function() {
  v(this, K, mc).call(this), Y(this, gi) && (Ie(this, Tn, (t) => {
    var r;
    const n = this._getRootElement();
    if (!n) return;
    const a = t.target;
    if (a instanceof Node && !((r = a.closest) != null && r.call(a, ".mwd-combat-menu"))) {
      if (!n.contains(a)) {
        v(this, K, Ai).call(this);
        return;
      }
      v(this, K, Ai).call(this);
    }
  }), document.addEventListener("click", Y(this, Tn)));
}, mc = function() {
  Y(this, Tn) && (document.removeEventListener("click", Y(this, Tn)), Ie(this, Tn, null));
}, Is = function() {
  const t = this._getRootElement();
  return t ? t.querySelector(".mwd-scroll-area") ?? t.querySelector(".csb-tab-panels") : null;
}, Ah = function() {
  const t = v(this, K, Is).call(this);
  if (!(t instanceof HTMLElement)) {
    Ie(this, qn, null);
    return;
  }
  Ie(this, qn, {
    top: t.scrollTop,
    left: t.scrollLeft
  });
}, wh = function() {
  const t = Y(this, qn);
  if (!t) return;
  const n = v(this, K, Is).call(this);
  n instanceof HTMLElement && (n.scrollTop = t.top, n.scrollLeft = t.left, requestAnimationFrame(() => {
    const a = v(this, K, Is).call(this);
    a instanceof HTMLElement && (a.scrollTop = t.top, a.scrollLeft = t.left);
  }), Ie(this, qn, null));
}, Je = function(t = !1) {
  v(this, K, Ah).call(this), this.render(t);
}, Ai = function({ rerender: t = !0 } = {}) {
  Y(this, gi) && (Ie(this, gi, null), t && v(this, K, Je).call(this, !1));
}, hn = function(t, n) {
  var r, s, o, l, c, u, d, m;
  const a = String(
    ((r = t == null ? void 0 : t.dataset) == null ? void 0 : r.itemId) ?? ((l = (o = (s = t == null ? void 0 : t.closest) == null ? void 0 : s.call(t, "[data-item-id]")) == null ? void 0 : o.dataset) == null ? void 0 : l.itemId) ?? ((m = (d = (u = (c = n == null ? void 0 : n.target) == null ? void 0 : c.closest) == null ? void 0 : u.call(c, "[data-item-id]")) == null ? void 0 : d.dataset) == null ? void 0 : m.itemId) ?? ""
  ).trim();
  return a ? this.actor.items.get(a) ?? null : null;
}, Th = function() {
  var a;
  const t = (a = this._getRootElement) == null ? void 0 : a.call(this);
  if (!t) return;
  v(this, K, fc).call(this);
  const n = new AbortController();
  Ie(this, Ra, n), t.addEventListener("dragstart", (r) => {
    var c, u, d;
    const s = (u = (c = r.target) == null ? void 0 : c.closest) == null ? void 0 : u.call(c, "[data-weapon-attack-uuid]");
    if (!s || !t.contains(s)) return;
    const o = v(this, K, hn).call(this, s, r), l = o ? gh(o) : null;
    if (!l) {
      r.preventDefault();
      return;
    }
    r.stopPropagation(), (d = r.dataTransfer) == null || d.setData("text/plain", JSON.stringify(l)), r.dataTransfer && (r.dataTransfer.effectAllowed = "copy");
  }, { signal: n.signal });
}, fc = function() {
  var t;
  (t = Y(this, Ra)) == null || t.abort(), Ie(this, Ra, null);
}, kh = async function(t) {
  if (t !== "prepare") return {};
  const a = await Dialog.prompt({
    title: "Prepare Interrupt",
    content: `
    <form class="mwd-quick-select">
      <div class="mwd-field">
        <label>Trigger</label>
        <input type="text" name="condition" placeholder="When..." />
      </div>
      <div class="mwd-field">
        <label>Scope</label>
        <input type="text" name="scope" placeholder="What you will do" />
      </div>
    </form>`,
    label: "Prepare",
    callback: (r) => ({
      condition: String(r.find('input[name="condition"]').val() ?? "").trim(),
      scope: String(r.find('input[name="scope"]').val() ?? "").trim()
    })
  });
  return a || null;
}, Mh = async function(t = {}) {
  const n = String((t == null ? void 0 : t.condition) ?? "").trim(), a = String((t == null ? void 0 : t.scope) ?? "").trim(), r = `
    <div class="mwd-quick-select">
      <p><strong>Trigger:</strong> ${Zt(n || "Unspecified trigger")}</p>
      <p><strong>Scope:</strong> ${Zt(a || "Unspecified response")}</p>
    </div>`;
  return !!await Dialog.confirm({
    title: "Resolve Interrupt",
    content: r,
    yes: () => !0,
    no: () => !1
  });
}, Ch = function(t) {
  return t != null && t.combatants ? typeof t.combatants.values == "function" ? Array.from(t.combatants.values()) : Array.from(t.combatants ?? []) : [];
}, vh = function(t) {
  var a;
  const n = String(((a = t == null ? void 0 : t.combatant) == null ? void 0 : a.id) ?? "").trim();
  return v(this, K, Ch).call(this, t == null ? void 0 : t.combat).filter((r) => r && String(r.id ?? "").trim() !== n).map((r) => {
    var c;
    const s = ((c = r.token) == null ? void 0 : c.document) ?? r.token ?? null, o = r.actor ?? (s == null ? void 0 : s.actor) ?? null, l = String(r.name ?? (s == null ? void 0 : s.name) ?? (o == null ? void 0 : o.name) ?? "Combatant").trim() || "Combatant";
    return {
      combatantId: String(r.id ?? "").trim(),
      actorUuid: (o == null ? void 0 : o.uuid) ?? null,
      tokenUuid: (s == null ? void 0 : s.uuid) ?? null,
      name: l
    };
  }).filter((r) => r.combatantId && r.name).sort((r, s) => r.name.localeCompare(s.name));
}, Eh = async function(t) {
  var s;
  const n = v(this, K, vh).call(this, t);
  if (!n.length)
    return (s = ui.notifications) == null || s.warn("No other combatants are available to assist."), null;
  const a = `
    <form class="mwd-quick-select">
      <div class="mwd-field">
        <label>Assist</label>
        <select name="combatant">
          ${n.map((o) => `<option value="${Zt(o.combatantId)}">${Zt(o.name)}</option>`).join("")}
        </select>
      </div>
    </form>`, r = await Dialog.prompt({
    title: "Assist Combatant",
    content: a,
    label: "Assist",
    callback: (o) => {
      var l;
      return String(o.find('select[name="combatant"]').val() ?? ((l = n[0]) == null ? void 0 : l.combatantId) ?? "").trim();
    }
  });
  return r ? n.find((o) => o.combatantId === r) ?? null : null;
}, Ih = async function({ actor: t, token: n = null, target: a = null, costLabel: r = "" } = {}) {
  const s = String((t == null ? void 0 : t.name) ?? "Ally").trim() || "Ally", o = String((a == null ? void 0 : a.name) ?? "an ally").trim() || "an ally", l = String(r ?? "").trim(), c = `
    <div class="mwd-chat-card mwd-chat-card--assist">
      <h3>Assist</h3>
      <p><strong>${Zt(s)}</strong> assists <strong>${Zt(o)}</strong>.</p>
      ${l ? `<p><small>Cost: ${Zt(l)}</small></p>` : ""}
    </div>`;
  await ChatMessage.create({
    speaker: ChatMessage.getSpeaker({ actor: t, token: (n == null ? void 0 : n.object) ?? n }),
    content: c
  });
}, Nh = async function({ actor: t, token: n = null, preparedInterrupt: a = null, costLabel: r = "" } = {}) {
  const s = String((t == null ? void 0 : t.name) ?? "Combatant").trim() || "Combatant", o = String((a == null ? void 0 : a.condition) ?? "").trim(), l = String((a == null ? void 0 : a.scope) ?? "").trim(), c = String(r ?? "").trim(), u = `
    <div class="mwd-chat-card mwd-chat-card--interrupt">
      <h3>Interrupt</h3>
      <p><strong>${Zt(s)}</strong> resolves a prepared interrupt.</p>
      ${o ? `<p><strong>Trigger:</strong> ${Zt(o)}</p>` : ""}
      ${l ? `<p><strong>Scope:</strong> ${Zt(l)}</p>` : ""}
      ${c ? `<p><small>Cost: ${Zt(c)}</small></p>` : ""}
    </div>`;
  await ChatMessage.create({
    speaker: ChatMessage.getSpeaker({ actor: t, token: (n == null ? void 0 : n.object) ?? n }),
    content: u
  });
}, wi = function(t, n, a = "That action is not available right now.") {
  var o, l, c, u, d;
  const r = ((o = t == null ? void 0 : t.closest) == null ? void 0 : o.call(t, "[data-action-disabled='true']")) ?? ((c = (l = n == null ? void 0 : n.target) == null ? void 0 : l.closest) == null ? void 0 : c.call(l, "[data-action-disabled='true']"));
  if (!r) return !1;
  const s = String(((u = r.dataset) == null ? void 0 : u.actionReason) ?? a).trim() || a;
  return (d = ui.notifications) == null || d.warn(s), !0;
}, Ta = function(t, n) {
  return `${String(t ?? "").trim()}:${String(n ?? "").trim()}`;
}, z(we, "PARTS", {
  sheet: {
    get template() {
      return `${se}/v2/actor/character-sheet.hbs`;
    }
  }
}), z(we, "DEFAULT_OPTIONS", foundry.utils.mergeObject(Oi(we, we, "DEFAULT_OPTIONS"), {
  classes: ["character-sheet", M, "actor-sheet-v2", "mwd-character-sheet", "mwd-sheet"],
  window: { minWidth: 450, minHeight: 740, resizable: !0 },
  position: { width: 980, height: 900 },
  actions: {
    ...Oi(we, we, "DEFAULT_OPTIONS").actions,
    edgeSet: we.prototype._onEdgeSet,
    toggleCombatMenu: we.prototype._onToggleCombatMenu,
    toggleStatuses: we.prototype._onToggleStatuses,
    combatAction: we.prototype._onCombatAction,
    combatSpend: we.prototype._onCombatSpend,
    combatAssist: we.prototype._onCombatAssist,
    combatEvade: we.prototype._onCombatEvade,
    combatInterrupt: we.prototype._onCombatInterrupt,
    combatReduceBurn: we.prototype._onCombatReduceBurn,
    combatOverloadCheck: we.prototype._onCombatOverloadCheck,
    combatAttack: we.prototype._onCombatAttack,
    createOwnedItem: we.prototype._onCreateOwnedItem,
    addSkillSpecialization: we.prototype._onAddSkillSpecialization,
    removeSkillSpecialization: we.prototype._onRemoveSkillSpecialization,
    createLifeModuleItem: we.prototype._onCreateLifeModuleItem,
    editOwnedItem: we.prototype._onEditOwnedItem,
    deleteOwnedItem: we.prototype._onDeleteOwnedItem,
    toggleInventoryAccordion: we.prototype._onToggleInventoryAccordion,
    toggleOwnedItemEquipped: we.prototype._onToggleOwnedItemEquipped,
    setOwnedItemPrimary: we.prototype._onSetOwnedItemPrimary,
    adjustGearQuantity: we.prototype._onAdjustGearQuantity,
    attackWeapon: we.prototype._onAttackWeapon,
    openAssignedMech: we.prototype._onOpenAssignedMech,
    mechAttack: we.prototype._onMechAttack,
    mechRoll: we.prototype._onMechRoll
  }
}, { inplace: !1 }));
let dc = we;
function Pv(i, e, t = "") {
  const n = foundry.utils.getProperty(i, e);
  return n === void 0 ? t : n;
}
function Vu(i, e, t = {}) {
  const {
    document: n = null,
    type: a = "text",
    value: r = Pv(n, i, a === "number" ? 0 : ""),
    displayValue: s = r,
    options: o = [],
    placeholder: l = "",
    readOnly: c = !1,
    rows: u = 4,
    help: d = ""
  } = t;
  return {
    path: i,
    label: e,
    value: r,
    displayValue: s,
    placeholder: l,
    readOnly: c,
    rows: u,
    help: d,
    options: o,
    isText: a === "text",
    isNumber: a === "number",
    isSelect: a === "select",
    isTextarea: a === "textarea"
  };
}
function Em(i, e, t, n = {}) {
  return Vu(e, t, { ...n, document: i, type: "text" });
}
function ka(i, e, t, n = {}) {
  return Vu(e, t, { ...n, document: i, type: "number" });
}
function Dv(i, e, t, n = {}) {
  return Vu(e, t, { ...n, document: i, type: "textarea" });
}
function Ov(i, e = []) {
  return e.map(
    (t) => ka(
      i,
      `system.attributes.${t.key}.value`,
      t.label
    )
  );
}
function ss(i, {
  types: e = [],
  includeTypes: t = [],
  describe: n = (o) => "",
  supportsEquip: a = !1,
  supportsPrimary: r = !1,
  supportsAttack: s = !1
} = {}) {
  const o = new Set([...e ?? [], ...t ?? []].map((l) => String(l ?? "").trim()));
  return Array.from((i == null ? void 0 : i.items) ?? []).filter((l) => o.has(String((l == null ? void 0 : l.canonicalType) ?? (l == null ? void 0 : l.type) ?? "").trim())).sort((l, c) => String(l.name ?? "").localeCompare(String(c.name ?? ""))).map((l) => {
    var c, u, d;
    return {
      id: l.id,
      name: l.name || "Item",
      subtitle: String(n(l) ?? "").trim(),
      equipped: !!((c = l.system) != null && c.equipped),
      isPrimary: !!((u = l.system) != null && u.isPrimary),
      supportsEquip: a,
      supportsPrimary: r,
      supportsAttack: s && !!((d = l.isPersonalWeapon) != null && d.call(l))
    };
  });
}
class Rh extends _r {
  static get DEFAULT_OPTIONS() {
    return foundry.utils.mergeObject(super.DEFAULT_OPTIONS, {
      classes: ["npc-sheet", M, "actor-sheet-v2"],
      position: { width: 920, height: 860 }
    }, { inplace: !1 });
  }
  async _prepareContext(e) {
    const t = await super._prepareContext(e), n = this.actor;
    return t.layout = await oa.get("npc"), t.actorSheet = {
      profileFields: [
        Em(n, "system.role", "Role / Archetype")
      ],
      attributeFields: Ov(n, [
        { key: "strength", label: "Strength" },
        { key: "reflexes", label: "Reflexes" },
        { key: "intelligence", label: "Intelligence" },
        { key: "willpower", label: "Willpower" },
        { key: "charisma", label: "Charisma" },
        { key: "edge", label: "Edge" }
      ]),
      monitorFields: [
        ka(n, "system.monitors.physical.value", "Physical"),
        ka(n, "system.monitors.physical.max", "Physical Max"),
        ka(n, "system.monitors.fatigue.value", "Fatigue"),
        ka(n, "system.monitors.fatigue.max", "Fatigue Max"),
        ka(n, "system.monitors.armor.value", "Armor"),
        Em(n, "system.monitors.armor.effect", "Armor Effect")
      ],
      itemCollections: {
        traits: ss(n, {
          types: ["quality"],
          describe: (a) => {
            var r;
            return ((r = a.system) == null ? void 0 : r.category) ?? "";
          }
        }),
        weapons: ss(n, {
          types: ["personalWeapon"],
          supportsEquip: !0,
          supportsPrimary: !0,
          describe: (a) => {
            var r, s;
            return `${((r = a.system) == null ? void 0 : r.category) ?? "ranged"} | DV ${Number(((s = a.system) == null ? void 0 : s.damage) ?? 0)}`;
          }
        }),
        assetModules: ss(n, {
          types: ["assetModule"],
          describe: (a) => {
            var r;
            return `Level ${Number(((r = a.system) == null ? void 0 : r.level) ?? 1)}`;
          }
        }),
        inventory: ss(n, {
          // Consumables share the same quantity-driven row contract as gear on
          // lightweight actor sheets, so we present them in one inventory list.
          types: ["gear", "consumable"],
          describe: (a) => {
            var r, s;
            return `Qty ${Number(((r = a.system) == null ? void 0 : r.quantity) ?? 1)} | Rating ${Number(((s = a.system) == null ? void 0 : s.rating) ?? 0)}`;
          }
        })
      },
      notesField: Dv(n, "system.biography", "Notes", { rows: 12 })
    }, t;
  }
}
z(Rh, "PARTS", {
  sheet: {
    template: `${se}/v2/actor/npc-sheet.hbs`,
    scrollable: [".sheet-body"]
  }
});
function Ph(i) {
  return i && typeof i == "object" ? i : {};
}
function Dh(i = "") {
  return String(i ?? "").trim();
}
function Lv(i) {
  return (Array.isArray(i) ? i : Object.values(Ph(i))).map(Dh).filter(Boolean);
}
function Yu(i = []) {
  return (Array.isArray(i) ? i : Object.values(Ph(i))).map((t) => ({
    ...t ?? {},
    weaponIds: Lv(t == null ? void 0 : t.weaponIds)
  }));
}
function _v(i = [], e = []) {
  const t = Yu(i), n = new Set(Array.from(e ?? []).map(Dh).filter(Boolean));
  let a = !1;
  const r = t.map((s) => {
    const o = s.weaponIds.filter((l) => n.has(l));
    return o.length !== s.weaponIds.length && (a = !0), {
      ...s,
      weaponIds: o
    };
  });
  return {
    changed: a,
    groups: r
  };
}
const { ApplicationV2: xv, HandlebarsApplicationMixin: $v } = foundry.applications.api, Sr = class Sr extends $v(xv) {
  static get DEFAULT_OPTIONS() {
    return foundry.utils.mergeObject(super.DEFAULT_OPTIONS, {
      id: "select-actor",
      classes: ["select-actor", "anarchy-dialog"],
      position: { width: 300, height: "auto" },
      window: {
        resizable: !0
      }
    }, { inplace: !1 });
  }
  static async selectActor(e, t, n = async (r) => {
  }, a = async () => {
  }) {
    var o, l, c, u;
    const r = {
      id: `select-actor-${foundry.utils.randomID()}`,
      classes: [((u = (c = (l = (o = game.system) == null ? void 0 : o.mwd) == null ? void 0 : l.styles) == null ? void 0 : c.selectCssClass) == null ? void 0 : u.call(c)) ?? "", ...Sr.DEFAULT_OPTIONS.classes].filter(Boolean),
      window: { title: e }
    };
    return new Sr({ actors: t, onActorSelected: n, onCancel: a }, r).render({ force: !0 });
  }
  constructor(e = {}, t = {}) {
    super(e, t), this.actors = e.actors, this.onActorSelected = e.onActorSelected, this.onCancel = e.onCancel, this._actorSelected = !1;
  }
  async _prepareContext() {
    return { actors: this.actors };
  }
  async activateListeners(e) {
    const t = e instanceof HTMLElement ? e : e[0];
    await super.activateListeners(t);
    const n = $(t);
    n.find(".click-select-actor").click((a) => this.onSelectActor(a)), n.find('[data-action="cancel"]').on("click", async () => await this.close());
  }
  async onSelectActor(e) {
    const t = $(e.currentTarget).attr("data-actor-id"), n = this.actors.find((a) => a.id == t);
    n && (this._actorSelected = !0, await this.onActorSelected(n), await this.close());
  }
  async close(e) {
    return !this._actorSelected && this.onCancel && await this.onCancel(), super.close(e);
  }
};
z(Sr, "PARTS", {
  body: {
    template: `${se}/dialog/select-actor.hbs`
  }
});
let pc = Sr;
function He(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? t : e;
}
function Oh(i = []) {
  return i.map((e) => String(e ?? "").trim()).filter(Boolean);
}
function Bv(i) {
  return String(i ?? "").replace(/<[^>]*>/g, " ").replace(/\s+/g, " ").trim();
}
function Im(i, e = 180) {
  const t = Bv(i);
  return t ? t.length <= e ? t : `${t.slice(0, Math.max(0, e - 3)).trim()}...` : "";
}
const Nm = Object.freeze({
  energy: "ENG",
  ballistic: "BAL",
  missile: "MSL",
  omni: "OMNI"
});
function zn(i = []) {
  return i.map((e) => {
    const t = e ?? {};
    return {
      ...t,
      label: String(t.label ?? "").trim(),
      value: String(t.value ?? "").trim(),
      emphasis: t.emphasis ?? "",
      title: String(t.title ?? "").trim(),
      tone: String(t.tone ?? "").trim(),
      parts: Array.isArray(t.parts) ? t.parts.filter((n) => n && n.value !== void 0 && n.value !== null && String(n.value).trim() !== "").map((n) => ({
        label: String(n.label ?? "").trim(),
        value: String(n.value ?? "").trim(),
        tone: String(n.tone ?? "").trim(),
        title: String(n.title ?? "").trim()
      })) : []
    };
  }).filter((e) => e.value !== "" || e.parts.length).map((e) => ({
    ...e,
    hasParts: e.parts.length > 0
  }));
}
function ol(i = []) {
  return Oh(i).map((e) => ({ label: e }));
}
function ir(i = []) {
  return i.filter((e) => e && e.value !== void 0 && e.value !== null && String(e.value).trim() !== "").map((e) => ({
    label: String(e.label ?? "").trim(),
    value: String(e.value ?? "").trim()
  }));
}
function zv(i = "") {
  const e = String(i ?? "").trim().toLowerCase();
  return Nm[e] ? Nm[e] : oe(e).slice(0, 3).toUpperCase() || "SLOT";
}
function Fv(i = {}) {
  return ["close", "near", "far", "extreme", "max"].filter((n) => (i == null ? void 0 : i[n]) !== void 0 && (i == null ? void 0 : i[n]) !== null && String(i[n]).trim() !== "").map((n) => {
    const a = i[n];
    return n === "max" ? `Max ${oe(a)}` : `${oe(n)} ${He(a, 0)}`;
  }).join(" | ");
}
const Hv = Object.freeze({
  handling: "Handling",
  system: "System",
  chassis: "Chassis",
  reliability: "Reliability"
}), ll = Object.freeze({
  mechWeapon: "Machine Weapon",
  vehicleWeapon: "Vehicle Weapon",
  personalWeapon: "Personal Weapon",
  assetModule: "Asset Module",
  vehicleUpgrade: "Vehicle Upgrade",
  mechEquipment: "Mech Equipment",
  gear: "Gear",
  quality: "Trait",
  skill: "Skill"
}), Rm = Object.freeze({
  battlemech: Object.freeze({
    artPath: "systems/mwd/img/mek/misc/repair/location_mek.png",
    mode: "silhouette",
    positions: Object.freeze({
      head: Object.freeze({ top: "9%", left: "50%" }),
      torso: Object.freeze({ top: "40%", left: "50%" }),
      arms: Object.freeze({ top: "34%", left: "18%" }),
      legs: Object.freeze({ top: "75%", left: "50%" })
    })
  }),
  vehicle: Object.freeze({
    artPath: "",
    mode: "schematic",
    positions: Object.freeze({
      front: Object.freeze({ top: "13%", left: "50%" }),
      side: Object.freeze({ top: "40%", left: "18%" }),
      turret: Object.freeze({ top: "33%", left: "50%" }),
      core: Object.freeze({ top: "57%", left: "50%" }),
      rear: Object.freeze({ top: "83%", left: "50%" }),
      rotor: Object.freeze({ top: "17%", left: "79%" })
    })
  })
});
function Uv({ condition: i = 0, destroyed: e = !1, stress: t = 0 } = {}) {
  return e ? "dark-red" : Number(i ?? 0) >= 4 ? "red" : Number(i ?? 0) >= 3 ? "orange" : Number(i ?? 0) >= 2 ? "yellow" : Number(t ?? 0) > 0 ? "green" : "";
}
var nn, Pa, Da, Vn, me, Lh, _h, xh, gc, hc, $h, Bh, Qi, yc, zh, Ns, Fh, fr, Hh;
const Be = class Be extends _r {
  constructor() {
    super(...arguments);
    Ce(this, me);
    Ce(this, nn, /* @__PURE__ */ new Set());
    Ce(this, Pa, null);
    Ce(this, Da, null);
    Ce(this, Vn, []);
  }
  async close(t = {}) {
    var n, a;
    return v(this, me, Bh).call(this), (n = Y(this, Da)) == null || n.abort(), (a = Y(this, Pa)) == null || a.abort(), super.close(t);
  }
  async _prepareContext(t) {
    var a, r, s, o;
    const n = await super._prepareContext(t);
    return n._mwdThemeClass = ((o = (s = (r = (a = game.system) == null ? void 0 : a.mwd) == null ? void 0 : r.styles) == null ? void 0 : s.selectCssClass) == null ? void 0 : o.call(s)) ?? "", n.layout = await oa.get(this.constructor.LAYOUT_ID ?? Be.LAYOUT_ID), n.vehicleSheet = {
      summaryStats: this._buildSummaryStats(),
      summaryActions: this._buildSummaryActions(),
      alerts: this._buildAlerts(),
      statusAction: {
        label: "Statuses",
        disabled: !this._resolveStatusToken(this.getPersistentActor() ?? this.actor),
        reason: "Statuses require a token for this actor on the current scene."
      },
      ewPanel: this._buildEwPanel(),
      activeCrits: this._buildActiveCrits(),
      attributes: this._buildAttributeCards(),
      movement: this._buildMovementCards(),
      degradation: this._buildDegradationPanel(),
      hardpoints: this._buildMachineHardpoints(),
      hardpointOptions: this._buildHardpointOptions(),
      sections: this._buildVehicleSections(),
      pilotPanel: await this._buildPilotPanel()
    }, n.conditionMonitors = this._buildConditionMonitors(), n;
  }
  async _buildPilotPanel() {
    var r, s, o;
    const t = ((r = this.getPersistentActor) == null ? void 0 : r.call(this)) ?? this.actor, n = String(((o = (s = t.system) == null ? void 0 : s.pilot) == null ? void 0 : o.uuid) ?? "").trim();
    let a = null;
    if (n)
      try {
        a = await fromUuid(n);
      } catch {
      }
    return {
      uuid: n,
      linked: !!a,
      name: (a == null ? void 0 : a.name) ?? null,
      id: (a == null ? void 0 : a.id) ?? null,
      canEdit: !!this.isEditable
    };
  }
  async _onAssignPilot(t, n) {
    var s, o;
    if (!this.isEditable) return;
    const a = (((s = game.actors) == null ? void 0 : s.contents) ?? []).filter((l) => l.type === "character");
    if (!a.length) {
      (o = ui.notifications) == null || o.warn("No character actors found in this world.");
      return;
    }
    const r = this.getPersistentActor() ?? this.actor;
    await pc.selectActor(
      "Assign Pilot",
      a,
      async (l) => r.update({ "system.pilot.uuid": l.uuid })
    );
  }
  async _onRemovePilot(t, n) {
    if (!this.isEditable) return;
    await (this.getPersistentActor() ?? this.actor).update({ "system.pilot.uuid": "" });
  }
  async _onOpenPilot(t, n) {
    var o, l, c;
    const a = ((o = this.getPersistentActor) == null ? void 0 : o.call(this)) ?? this.actor, r = String(((c = (l = a.system) == null ? void 0 : l.pilot) == null ? void 0 : c.uuid) ?? "").trim();
    if (!r) return;
    const s = await fromUuid(r).catch(() => null);
    s && s.sheet.render(!0, { focus: !0 });
  }
  async _onDrop(t) {
    var a, r, s;
    if (!this.isEditable) return (a = super._onDrop) == null ? void 0 : a.call(this, t);
    let n;
    try {
      n = (((r = foundry.applications.ux.TextEditor) == null ? void 0 : r.implementation) ?? TextEditor).getDragEventData(t);
    } catch {
    }
    if (!await this._handleHardpointItemDrop(t, n)) {
      if ((n == null ? void 0 : n.type) === "Actor") {
        const o = await fromUuid(n.uuid).catch(() => null);
        if ((o == null ? void 0 : o.type) === "character") {
          await (this.getPersistentActor() ?? this.actor).update({ "system.pilot.uuid": o.uuid });
          return;
        }
      }
      return (s = super._onDrop) == null ? void 0 : s.call(this, t);
    }
  }
  _onRender(t, n) {
    var a;
    (a = super._onRender) == null || a.call(this, t, n), v(this, me, Lh).call(this), v(this, me, _h).call(this), v(this, me, $h).call(this);
  }
  _buildSummaryStats() {
    var r, s, o, l, c, u, d, m, f, p, g, h, y;
    const t = ((r = this.actor.system) == null ? void 0 : r.attributes) ?? {}, n = ((o = (s = this.actor.system) == null ? void 0 : s.monitors) == null ? void 0 : o.structure) ?? {}, a = Pg({
      actorType: this.actor.type,
      movement: (l = this.actor.system) == null ? void 0 : l.movement,
      legacyMoves: (c = this.actor.system) == null ? void 0 : c.moves,
      jumpProfile: ((m = (d = (u = this.actor.system) == null ? void 0 : u.mwd) == null ? void 0 : d.mobility) == null ? void 0 : m.jumping) ?? null
    });
    return zn([
      { label: "Handling", value: He((f = t.handling) == null ? void 0 : f.value, 0), emphasis: "strong" },
      { label: "Move", parts: a },
      { label: "System", value: He((p = t.system) == null ? void 0 : p.value, 0) },
      { label: "Chassis", value: He((g = t.chassis) == null ? void 0 : g.value, 0) },
      { label: "Reliability", value: He(((h = t.reliability) == null ? void 0 : h.value) ?? ((y = t.condition) == null ? void 0 : y.value), 0) },
      { label: "Structure", value: `${He(n.value, 0)} / ${He(n.max, 0)}` }
    ]);
  }
  _buildAlerts() {
    return [];
  }
  _buildSummaryActions() {
    return [];
  }
  _buildEwPanel() {
    var a;
    const t = ((a = this.getPersistentActor) == null ? void 0 : a.call(this)) ?? this.actor, n = this._resolveStatusToken(t);
    return tc({ actor: t, token: n });
  }
  _buildAttributeCards() {
    var n;
    const t = ((n = this.actor.system) == null ? void 0 : n.attributes) ?? {};
    return Object.entries(Hv).map(([a, r]) => {
      var s;
      return {
        key: a,
        label: r,
        value: He((s = t == null ? void 0 : t[a]) == null ? void 0 : s.value, 0),
        path: `system.attributes.${a}.value`
      };
    });
  }
  _buildMovementCards() {
    var t, n, a, r, s;
    return Rg({
      actorType: this.actor.type,
      movement: (t = this.actor.system) == null ? void 0 : t.movement,
      legacyMoves: (n = this.actor.system) == null ? void 0 : n.moves,
      editing: this.editing,
      jumpProfile: ((s = (r = (a = this.actor.system) == null ? void 0 : a.mwd) == null ? void 0 : r.mobility) == null ? void 0 : s.jumping) ?? null
    });
  }
  _buildDegradationPanel() {
    var g, h, y, b;
    const t = da(
      foundry.utils.deepClone(this.actor.system ?? {}),
      this.actor.type
    ), n = t.attributes ?? {}, a = t.mwd ?? {}, r = He(((g = n.reliability) == null ? void 0 : g.value) ?? ((h = n.condition) == null ? void 0 : h.value), 0), s = Ip(r), o = He((y = a.shock) == null ? void 0 : y.value, 0), l = He((b = a.reliabilitySpendable) == null ? void 0 : b.value, r), c = zt(this.actor), u = Rw(this.actor), d = Rm[this.actor.type] ?? Rm.vehicle, m = EA(this.actor.type), p = Object.entries(a.locations ?? {}).sort(([S], [w]) => {
      const T = m.indexOf(S), k = m.indexOf(w), C = T >= 0 ? T : Number.MAX_SAFE_INTEGER, I = k >= 0 ? k : Number.MAX_SAFE_INTEGER;
      return C !== I ? C - I : String(S).localeCompare(String(w));
    }).map(([S, w]) => {
      const T = He(w == null ? void 0 : w.condition, 0), k = He(w == null ? void 0 : w.stress, 0), C = !!(w != null && w.destroyed), I = Uv({ condition: T, destroyed: C, stress: k }), O = d.positions[S] ?? { top: "50%", left: "50%" };
      return {
        key: S,
        label: Mr(S),
        stress: k,
        conditionValue: T,
        conditionLabel: gu(T),
        conditionModifier: hu(T),
        destroyed: C,
        effectSummary: Pw(this.actor.type, S, w),
        enabled: (w == null ? void 0 : w.enabled) !== !1,
        tone: I,
        style: `--pin-top:${O.top}; --pin-left:${O.left};`,
        stressLabel: k > 0 ? `Stress ${k}` : "Stress 0"
      };
    });
    return {
      mode: d.mode,
      artPath: d.artPath,
      reliability: r,
      spendable: l,
      shock: o,
      threshold: s,
      summaryEffects: c.effectTexts ?? [],
      movementEffects: u,
      locations: p
    };
  }
  _buildConditionMonitors() {
    var n, a, r, s, o;
    const t = ((a = (n = this.actor.system) == null ? void 0 : n.monitors) == null ? void 0 : a.structure) ?? ((o = (s = (r = this.actor.system) == null ? void 0 : r.mwd) == null ? void 0 : s.monitors) == null ? void 0 : o.structure) ?? {};
    return [
      cc({ id: "structure", label: "Structure", kind: "structure", monitor: t, editable: this.isEditable })
    ];
  }
  _buildVehicleSections() {
    var n, a;
    const t = ((a = (n = this.actor.system) == null ? void 0 : n.mwd) == null ? void 0 : a.items) ?? {};
    return {
      slots: this._buildHardpointSlotSection(),
      upgrades: this._buildRecordSection({
        sectionId: "upgrades",
        itemType: "vehicleUpgrade",
        addLabel: "Add Upgrade",
        emptyLabel: "No vehicle upgrades installed.",
        items: t.vehicleUpgrades ?? []
      }),
      modules: this._buildRecordSection({
        sectionId: "modules",
        itemType: "assetModule",
        addLabel: "Add Module",
        emptyLabel: "No asset modules assigned.",
        items: t.assetModules ?? []
      }),
      gear: this._buildRecordSection({
        sectionId: "gear",
        itemType: "gear",
        addLabel: "Add Gear",
        emptyLabel: "No stored gear.",
        items: t.gear ?? []
      })
    };
  }
  _buildHardpointSlotSection() {
    var r;
    const t = ((r = this.getPersistentActor) == null ? void 0 : r.call(this)) ?? this.actor;
    return {
      sectionId: "slots",
      itemType: "",
      addLabel: "",
      emptyLabel: "No hardpoint slots configured.",
      records: zi(t).map(
        (s, o) => this._buildHardpointSlotRecord(s, o)
      )
    };
  }
  _buildMachineHardpoints() {
    var d, m, f, p;
    const t = ((d = this.getPersistentActor) == null ? void 0 : d.call(this)) ?? this.actor, n = zi(t), a = Array.from(((p = (f = (m = t.system) == null ? void 0 : m.mwd) == null ? void 0 : f.loadout) == null ? void 0 : p.hardpoints) ?? []), r = new Map(this._getHardpointSlottableItems().map((g) => [String((g == null ? void 0 : g.id) ?? "").trim(), g]).filter(([g]) => g)), s = new Map(a.map((g, h) => [
      String((g == null ? void 0 : g.id) ?? `hardpoint-${h + 1}`),
      g
    ])), o = this._getHardpointTypeLabels(), l = this._getHardpointSizeLabels(), c = this._getHardpointLocationLabels(), u = this._buildHardpointOptions();
    return n.map((g, h) => {
      var C;
      const y = String((g == null ? void 0 : g.id) ?? `hardpoint-${h + 1}`), b = s.get(y) ?? {}, S = String((g == null ? void 0 : g.type) ?? (b == null ? void 0 : b.type) ?? "energy").trim() || "energy", w = String((g == null ? void 0 : g.size) ?? (b == null ? void 0 : b.size) ?? "small").trim() || "small", T = String((g == null ? void 0 : g.location) ?? (b == null ? void 0 : b.location) ?? "").trim() || this._getDefaultHardpointLocation(), k = String(
        ((C = r.get(String((g == null ? void 0 : g.itemId) ?? "").trim())) == null ? void 0 : C.name) ?? (b == null ? void 0 : b.occupiedByName) ?? ""
      ).trim();
      return {
        id: y,
        index: h,
        itemId: String((g == null ? void 0 : g.itemId) ?? "").trim(),
        type: S,
        size: w,
        location: T,
        typeLabel: o[S] ?? oe(S),
        sizeLabel: l[w] ?? oe(w),
        locationLabel: c[T] ?? oe(T),
        occupiedByName: k,
        occupancyLabel: k || "Open",
        typeOptions: u.types.map((I) => ({ ...I, selected: I.value === S })),
        sizeOptions: u.sizes.map((I) => ({ ...I, selected: I.value === w })),
        locationOptions: u.locations.map((I) => ({ ...I, selected: I.value === T }))
      };
    });
  }
  _buildHardpointOptions() {
    return {
      types: Object.entries(this._getHardpointTypeLabels()).map(([t, n]) => ({ value: t, label: n })),
      sizes: Object.entries(this._getHardpointSizeLabels()).map(([t, n]) => ({ value: t, label: n })),
      locations: this._getAvailableHardpointLocations().map((t) => ({
        value: t,
        label: this._getHardpointLocationLabels()[t] ?? oe(t)
      }))
    };
  }
  _getHardpointTypeLabels() {
    var t, n, a;
    return ((t = q == null ? void 0 : q.mwd) == null ? void 0 : t.hardpointType) ?? ((a = (n = q == null ? void 0 : q.mwd) == null ? void 0 : n.hardpoint) == null ? void 0 : a.type) ?? {};
  }
  _getHardpointSizeLabels() {
    var t, n, a;
    return ((t = q == null ? void 0 : q.mwd) == null ? void 0 : t.hardpointSize) ?? ((a = (n = q == null ? void 0 : q.mwd) == null ? void 0 : n.hardpoint) == null ? void 0 : a.size) ?? {};
  }
  _getHardpointLocationLabels() {
    var t, n, a;
    return ((t = q == null ? void 0 : q.mwd) == null ? void 0 : t.hardpointLocation) ?? ((a = (n = q == null ? void 0 : q.mwd) == null ? void 0 : n.hardpoint) == null ? void 0 : a.location) ?? {};
  }
  _getAvailableHardpointLocations() {
    var t;
    return ((t = this.actor) == null ? void 0 : t.type) === "battlemech" ? ["arms", "head", "torso"] : ["turret"];
  }
  async _commitEditsToActor() {
    await super._commitEditsToActor(), await this._persistStagedHardpointFields();
  }
  _getCompatibleHardpointItems(t, { includeAssignedItem: n = !1 } = {}) {
    var o;
    const a = ((o = this.getPersistentActor) == null ? void 0 : o.call(this)) ?? this.actor, r = BA(a), s = String((t == null ? void 0 : t.itemId) ?? "").trim();
    return this._getHardpointSlottableItems().filter((l) => {
      const c = String((l == null ? void 0 : l.id) ?? "").trim();
      return !c || !n && r.has(c) && c !== s ? !1 : yu(t, l);
    });
  }
  async _handleHardpointItemDrop(t, n = null) {
    var f, p, g, h, y, b, S, w, T;
    const a = (p = (f = t == null ? void 0 : t.target) == null ? void 0 : f.closest) == null ? void 0 : p.call(f, ".mwd-record[data-hardpoint-id]");
    if (!a) return !1;
    const r = String(((g = a.dataset) == null ? void 0 : g.hardpointId) ?? "").trim();
    if (!r) return !1;
    const s = await this._persistStagedHardpointFields(), o = this.getPersistentActor() ?? this.actor, l = s.find((k) => k.id === r);
    if (!l) return !1;
    if (String((l == null ? void 0 : l.itemId) ?? "").trim())
      return (h = ui.notifications) == null || h.warn("That hardpoint is already filled."), !0;
    const c = await this._resolveDroppedHardpointItem(n);
    if (!c)
      return String((n == null ? void 0 : n.type) ?? "").trim() === "Item" ? ((y = ui.notifications) == null || y.warn("That item could not be mounted into this hardpoint."), !0) : !1;
    const u = zA(l, c);
    if (u)
      return (b = ui.notifications) == null || b.warn(u), !0;
    let d = String((c == null ? void 0 : c.id) ?? "").trim();
    if (!((c == null ? void 0 : c.parent) === o || (c == null ? void 0 : c.actor) === o)) {
      const k = foundry.utils.deepClone(((S = c.toObject) == null ? void 0 : S.call(c)) ?? c ?? {});
      delete k._id;
      const C = await o.createEmbeddedDocuments("Item", [k]);
      d = String(((w = C == null ? void 0 : C[0]) == null ? void 0 : w.id) ?? "").trim();
    }
    return d ? (await this._setHardpointOccupant(r, d, { hardpoints: s }), this.render({ force: !0 }), !0) : ((T = ui.notifications) == null || T.warn("That item could not be mounted."), !0);
  }
  async _resolveDroppedHardpointItem(t = null) {
    var l, c, u, d;
    if (!t || (t == null ? void 0 : t.type) !== "Item") return null;
    const n = ((l = CONFIG == null ? void 0 : CONFIG.Item) == null ? void 0 : l.documentClass) ?? globalThis.Item ?? null;
    if (typeof (n == null ? void 0 : n.fromDropData) == "function") {
      const m = await n.fromDropData(t).catch(() => null);
      if (m) return m;
    }
    const a = ((c = this.getPersistentActor) == null ? void 0 : c.call(this)) ?? this.actor, r = String((t == null ? void 0 : t.itemId) ?? (t == null ? void 0 : t._id) ?? (t == null ? void 0 : t.id) ?? "").trim(), s = String((t == null ? void 0 : t.actorId) ?? (t == null ? void 0 : t.parentId) ?? "").trim();
    if (r && s && s === String((a == null ? void 0 : a.id) ?? "").trim()) {
      const m = ((d = (u = a == null ? void 0 : a.items) == null ? void 0 : u.get) == null ? void 0 : d.call(u, r)) ?? null;
      if (m) return m;
    }
    const o = t != null && t.uuid ? await fromUuid(t.uuid).catch(() => null) : null;
    return !o && (t != null && t.data) && typeof n == "function" ? new n(t.data, { parent: a }) : o || null;
  }
  async _promptHardpointAssignment(t) {
    const n = this._getCompatibleHardpointItems(t);
    if (!n.length) return "";
    if (n.length === 1) return n[0].id ?? "";
    const a = `<form class="mwd-quick-select">${n.map((r, s) => `
      <label class="quick-select-option">
        <input type="radio" name="slot-item" value="${r.id}" ${s === 0 ? "checked" : ""}>
        <span>${r.name}</span>
      </label>`).join("")}</form>`;
    return await Dialog.prompt({
      title: "Assign Hardpoint Item",
      content: a,
      label: "Assign",
      callback: (r) => r.find('input[name="slot-item"]:checked').val() ?? n[0].id
    });
  }
  async _setHardpointOccupant(t = "", n = "", { hardpoints: a = null } = {}) {
    const r = String(t ?? "").trim();
    if (!r) return;
    const s = this.getPersistentActor() ?? this.actor, o = xA(
      a ?? gs(s),
      r,
      n,
      { defaultLocation: this._getDefaultHardpointLocation() }
    );
    o.changed && await s.update({ "system.mwd.hardpoints": o.hardpoints }), await this._sanitizeMountedWeaponGroups(s);
  }
  async _deleteMountedHardpointItem(t = "") {
    var l, c;
    const n = String(t ?? "").trim();
    if (!n) return;
    const a = this.getPersistentActor() ?? this.actor, r = zi(a).find((u) => u.id === n), s = String((r == null ? void 0 : r.itemId) ?? "").trim();
    !s || !(((c = (l = a.items) == null ? void 0 : l.get) == null ? void 0 : c.call(l, s)) ?? null) || await a.deleteEmbeddedDocuments("Item", [s]);
  }
  _getHardpointSlottableItems() {
    var n;
    const t = ((n = this.getPersistentActor) == null ? void 0 : n.call(this)) ?? this.actor;
    return Array.from((t == null ? void 0 : t.items) ?? []).filter((a) => String((a == null ? void 0 : a.canonicalType) ?? (a == null ? void 0 : a.type) ?? "").trim() === "mechWeapon");
  }
  _buildHardpointSlotRecord(t, n = 0) {
    var k, C, I, O, x, B, P;
    const a = ((k = this.getPersistentActor) == null ? void 0 : k.call(this)) ?? this.actor, r = String((t == null ? void 0 : t.id) ?? `hardpoint-${n + 1}`).trim(), s = String((t == null ? void 0 : t.type) ?? "").trim(), o = String((t == null ? void 0 : t.size) ?? "").trim(), l = String((t == null ? void 0 : t.location) ?? "").trim() || this._getDefaultHardpointLocation(), c = this._getHardpointTypeLabels(), u = this._getHardpointSizeLabels(), d = this._getHardpointLocationLabels(), m = String((t == null ? void 0 : t.itemId) ?? "").trim(), f = m ? ((I = (C = a == null ? void 0 : a.items) == null ? void 0 : C.get) == null ? void 0 : I.call(C, m)) ?? null : null, p = typeof (f == null ? void 0 : f.getCombatProfile) == "function" ? f.getCombatProfile() : null, g = `slot:${r}`, h = c[s] ?? oe(s), y = u[o] ?? oe(o), b = d[l] ?? oe(l);
    if (!f)
      return {
        id: "",
        hardpointId: r,
        accordionId: g,
        className: "is-hardpoint-slot is-empty-hardpoint",
        mediaClassName: "is-slot-placeholder",
        isExpanded: Y(this, nn).has(g),
        isEmptyHardpoint: !0,
        slotTypeCode: zv(s),
        slotSizeCode: y.slice(0, 1).toUpperCase() || "S",
        slotOpenLabel: "Open",
        img: "systems/mwd/img/default/Default_Weapon.svg",
        name: `Empty ${h} ${y} Slot`,
        subtitle: `${b} mount available`,
        summaryStats: zn([
          { label: "Mount", value: `${h} ${y}`, emphasis: "strong" },
          { label: "Status", value: "Open", tone: "green" }
        ]),
        detailTags: ol(["Open Hardpoint", h, y]),
        detailRows: ir([
          { label: "Location", value: b },
          { label: "Accepts", value: `${y} ${h} weapons` },
          { label: "Assigned Weapon", value: "Empty" }
        ]),
        detailText: "Drag a matching mech weapon item onto this slot, or create one directly for this slot.",
        createHardpointItem: {
          hardpointId: r,
          itemType: "mechWeapon",
          label: "Create Weapon for Slot"
        }
      };
    const S = (f == null ? void 0 : f.canonicalType) ?? (f == null ? void 0 : f.type) ?? "", w = ll[S] ?? oe(S || "item"), T = ((O = f == null ? void 0 : f.system) == null ? void 0 : O.notes) ?? ((x = f == null ? void 0 : f.system) == null ? void 0 : x.description) ?? ((P = (B = f == null ? void 0 : f.system) == null ? void 0 : B.references) == null ? void 0 : P.description) ?? "";
    return {
      id: f.id ?? "",
      hardpointId: r,
      accordionId: g,
      className: "is-hardpoint-slot is-occupied-hardpoint",
      isExpanded: Y(this, nn).has(g),
      name: f.name ?? w,
      img: f.img ?? "icons/svg/item-bag.svg",
      subtitle: `${w} | ${b}`,
      summaryStats: zn(p ? [
        { label: "DV", value: He(p.damage, 0), emphasis: "strong" },
        { label: "AP", value: He(p.ap, 0) },
        { label: "Type", value: p.damageTypeLabel ?? p.damageType ?? "" }
      ] : [
        { label: "Type", value: w, emphasis: "strong" },
        { label: "Slot", value: u[o] ?? oe(o) }
      ]),
      detailTags: ol([
        "Mounted",
        h
      ]),
      detailRows: ir([
        { label: "Slot Type", value: h },
        { label: "Slot Size", value: y },
        { label: "Location", value: b },
        { label: "Assigned Item", value: f.name ?? w }
      ]),
      detailText: Im(T),
      suppressDeleteButton: !0,
      clearHardpointItem: {
        hardpointId: r,
        label: "Empty Slot",
        hint: "Delete the attached weapon and leave this slot open.",
        icon: "fa-trash-can",
        buttonClassName: "is-danger",
        inlineLabel: "Empty Slot"
      },
      machineAttack: S === "mechWeapon" ? { label: "Attack", itemId: f.id ?? "" } : null
    };
  }
  _buildRecordSection({ sectionId: t = "", itemType: n = "", addLabel: a = "", emptyLabel: r = "", items: s = [] } = {}) {
    return {
      sectionId: t,
      itemType: n,
      addLabel: String(a ?? "").trim(),
      emptyLabel: String(r ?? "Nothing here yet.").trim(),
      records: Array.from(s ?? []).map((o) => this._buildItemRecord(o, { sectionId: t }))
    };
  }
  _buildItemRecord(t, { sectionId: n = "" } = {}) {
    var f, p, g, h, y, b, S, w, T;
    const a = (t == null ? void 0 : t.system) ?? {}, r = (t == null ? void 0 : t.canonicalType) ?? (t == null ? void 0 : t.type) ?? "", s = typeof (t == null ? void 0 : t.getCombatProfile) == "function" ? t.getCombatProfile() : null, o = `${String(n ?? "").trim()}:${String((t == null ? void 0 : t.id) ?? "").trim()}`, l = ll[r] ?? oe(r || "item"), c = a.notes ?? a.description ?? ((f = a.references) == null ? void 0 : f.description) ?? "", u = a.quantity, d = s ? zn([
      { label: "DV", value: He(s.damage, 0), emphasis: "strong" },
      { label: "AP", value: He(s.ap, 0) },
      { label: "Type", value: s.damageTypeLabel ?? s.damageType ?? "" }
    ]) : r === "assetModule" && ((g = (p = a == null ? void 0 : a.mobility) == null ? void 0 : p.jumping) != null && g.enabled) ? zn([
      { label: "Type", value: l },
      { label: "Jump", value: He(a.mobility.jumping.movement, 0), emphasis: "strong" },
      { label: "Heat", value: He(a.mobility.jumping.heat, 0) }
    ]) : zn([
      { label: "Type", value: l },
      ...u !== void 0 ? [{ label: "Qty", value: He(u, 0) }] : []
    ]), m = s ? ir([
      { label: "Skill", value: ((h = s.skillDef) == null ? void 0 : h.label) ?? s.skill ?? "" },
      { label: "Category", value: s.category ?? a.weaponCategory ?? a.category ?? "" },
      { label: "Range", value: Fv(s.range) }
    ]) : r === "assetModule" && ((b = (y = a == null ? void 0 : a.mobility) == null ? void 0 : y.jumping) != null && b.enabled) ? ir([
      { label: "Category", value: a.category ?? l },
      { label: "Heat", value: He(a.mobility.jumping.heat, 0) },
      { label: "AR Bonus", value: He(a.mobility.jumping.attackRatingBonus, 0) },
      { label: "DR Bonus", value: He(a.mobility.jumping.defenseRatingBonus, 0) },
      { label: "DFA", value: a.mobility.jumping.dfaEnabled ? "Enabled" : "Disabled" }
    ]) : ir([
      { label: "Category", value: a.category ?? l },
      { label: "Quantity", value: u !== void 0 ? He(u, 0) : "" }
    ]);
    return {
      id: (t == null ? void 0 : t.id) ?? "",
      accordionId: o,
      isExpanded: Y(this, nn).has(o),
      name: (t == null ? void 0 : t.name) ?? l,
      img: (t == null ? void 0 : t.img) ?? "icons/svg/item-bag.svg",
      subtitle: ((S = s == null ? void 0 : s.skillDef) == null ? void 0 : S.label) ?? a.category ?? l,
      summaryStats: d,
      detailTags: ol([
        a.equipped ? "Equipped" : "",
        a.isPrimary ? "Primary" : "",
        (T = (w = a.mobility) == null ? void 0 : w.jumping) != null && T.enabled ? "Jumping" : "",
        a.weaponCategory ?? a.category ?? ""
      ]),
      detailRows: m,
      detailText: Im(c),
      equipped: !!a.equipped,
      isPrimary: !!a.isPrimary,
      canAdjustQuantity: !1,
      machineAttack: ["mechWeapon", "vehicleWeapon"].includes(r) ? {
        label: "Attack",
        itemId: (t == null ? void 0 : t.id) ?? ""
      } : null
    };
  }
  async _onCreateOwnedItem(t, n) {
    var l, c, u;
    if ((l = t == null ? void 0 : t.preventDefault) == null || l.call(t), (c = t == null ? void 0 : t.stopPropagation) == null || c.call(t), !this.isEditable) return;
    const a = String(((u = n == null ? void 0 : n.dataset) == null ? void 0 : u.itemType) ?? "").trim();
    if (!a) return;
    const r = this.getPersistentActor() ?? this.actor, s = ll[a] ?? oe(a), o = r.items.filter((d) => d.type === a).length;
    await r.createEmbeddedDocuments("Item", [{
      name: `${s} ${o + 1}`,
      type: a
    }]), this.render({ force: !0 });
  }
  async _onEditOwnedItem(t, n) {
    var r, s, o;
    (r = t == null ? void 0 : t.preventDefault) == null || r.call(t), (s = t == null ? void 0 : t.stopPropagation) == null || s.call(t);
    const a = v(this, me, gc).call(this, n, t);
    (o = a == null ? void 0 : a.sheet) == null || o.render(!0);
  }
  async _onDeleteOwnedItem(t, n) {
    var o, l;
    if ((o = t == null ? void 0 : t.preventDefault) == null || o.call(t), (l = t == null ? void 0 : t.stopPropagation) == null || l.call(t), !this.isEditable) return;
    const a = v(this, me, gc).call(this, n, t);
    if (!a) return;
    const r = this.getPersistentActor() ?? this.actor, s = zi(r).find(
      (c) => String((c == null ? void 0 : c.itemId) ?? "").trim() === String((a == null ? void 0 : a.id) ?? "").trim()
    );
    await r.deleteEmbeddedDocuments("Item", [a.id]), s != null && s.id && await this._setHardpointOccupant(s.id, ""), this.render({ force: !0 });
  }
  async _onToggleInventoryAccordion(t, n) {
    var r, s, o, l, c, u, d, m, f, p;
    (r = t == null ? void 0 : t.preventDefault) == null || r.call(t), (s = t == null ? void 0 : t.stopPropagation) == null || s.call(t);
    const a = String(
      ((o = n == null ? void 0 : n.dataset) == null ? void 0 : o.accordionId) ?? ((u = (c = (l = n == null ? void 0 : n.closest) == null ? void 0 : l.call(n, "[data-accordion-id]")) == null ? void 0 : c.dataset) == null ? void 0 : u.accordionId) ?? ((p = (f = (m = (d = t == null ? void 0 : t.target) == null ? void 0 : d.closest) == null ? void 0 : m.call(d, "[data-accordion-id]")) == null ? void 0 : f.dataset) == null ? void 0 : p.accordionId) ?? ""
    ).trim();
    a && (Y(this, nn).has(a) ? Y(this, nn).delete(a) : Y(this, nn).add(a), this.render({ force: !1 }));
  }
  async _onAddHardpoint(t, n) {
    var l, c, u, d, m, f;
    if ((l = t == null ? void 0 : t.preventDefault) == null || l.call(t), (c = t == null ? void 0 : t.stopPropagation) == null || c.call(t), !this.isEditable) return;
    const a = await this._persistStagedHardpointFields(), r = this.getPersistentActor() ?? this.actor, s = this._buildHardpointOptions(), o = LA(a, {
      id: (d = (u = foundry.utils).randomID) == null ? void 0 : d.call(u),
      type: ((m = s.types[0]) == null ? void 0 : m.value) ?? "energy",
      size: ((f = s.sizes[0]) == null ? void 0 : f.value) ?? "small",
      location: this._getDefaultHardpointLocation()
    }, {
      defaultLocation: this._getDefaultHardpointLocation(),
      idFactory: (p) => {
        var g, h;
        return ((h = (g = foundry.utils).randomID) == null ? void 0 : h.call(g)) ?? `hardpoint-${p + 1}`;
      }
    });
    await r.update({ "system.mwd.hardpoints": o }), this.render({ force: !0 });
  }
  async _onDeleteHardpoint(t, n) {
    var u, d, m, f, p, g, h, y, b, S;
    if ((u = t == null ? void 0 : t.preventDefault) == null || u.call(t), (d = t == null ? void 0 : t.stopPropagation) == null || d.call(t), !this.isEditable) return;
    const a = String(
      ((m = n == null ? void 0 : n.dataset) == null ? void 0 : m.hardpointId) ?? ((g = (p = (f = n == null ? void 0 : n.closest) == null ? void 0 : f.call(n, "[data-hardpoint-id]")) == null ? void 0 : p.dataset) == null ? void 0 : g.hardpointId) ?? ((S = (b = (y = (h = t == null ? void 0 : t.target) == null ? void 0 : h.closest) == null ? void 0 : y.call(h, "[data-hardpoint-id]")) == null ? void 0 : b.dataset) == null ? void 0 : S.hardpointId) ?? ""
    ).trim();
    if (!a) return;
    const r = await this._persistStagedHardpointFields(), s = this.getPersistentActor() ?? this.actor, { hardpoints: o, removed: l } = _A(r, a, {
      defaultLocation: this._getDefaultHardpointLocation()
    });
    if (!l) return;
    const c = String((l == null ? void 0 : l.itemId) ?? "").trim();
    c && await s.deleteEmbeddedDocuments("Item", [c]), await s.update({ "system.mwd.hardpoints": o }), await this._sanitizeMountedWeaponGroups(s), this.render({ force: !0 });
  }
  async _onCreateHardpointItem(t, n) {
    var d, m, f, p, g, h, y, b, S, w;
    if ((d = t == null ? void 0 : t.preventDefault) == null || d.call(t), (m = t == null ? void 0 : t.stopPropagation) == null || m.call(t), !this.isEditable) return;
    const a = String(
      ((f = n == null ? void 0 : n.dataset) == null ? void 0 : f.hardpointId) ?? ((h = (g = (p = n == null ? void 0 : n.closest) == null ? void 0 : p.call(n, "[data-hardpoint-id]")) == null ? void 0 : g.dataset) == null ? void 0 : h.hardpointId) ?? ""
    ).trim();
    if (!a) return;
    const r = await this._persistStagedHardpointFields(), s = this.getPersistentActor() ?? this.actor, o = r.find(
      (T) => String((T == null ? void 0 : T.id) ?? "").trim() === a
    );
    if (!o) return;
    const l = s.items.filter((T) => (T.canonicalType ?? T.type) === "mechWeapon").length, c = ["penetrating", "concussive", "energy", "thermal", "electrical"].includes(String((o == null ? void 0 : o.type) ?? "").trim()) ? String(o.type).trim() : "penetrating", u = await s.createEmbeddedDocuments("Item", [{
      name: `Mech Weapon ${l + 1}`,
      type: "mechWeapon",
      system: {
        size: String((o == null ? void 0 : o.size) ?? "").trim() || "small",
        damageType: c
      }
    }]);
    await this._setHardpointOccupant(a, ((y = u == null ? void 0 : u[0]) == null ? void 0 : y.id) ?? "", { hardpoints: r }), (w = (S = (b = u == null ? void 0 : u[0]) == null ? void 0 : b.sheet) == null ? void 0 : S.render) == null || w.call(S, !0), this.render({ force: !0 });
  }
  async _onAssignHardpointItem(t, n) {
    var l, c, u, d, m, f;
    if ((l = t == null ? void 0 : t.preventDefault) == null || l.call(t), (c = t == null ? void 0 : t.stopPropagation) == null || c.call(t), !this.isEditable) return;
    const a = String(
      ((u = n == null ? void 0 : n.dataset) == null ? void 0 : u.hardpointId) ?? ((f = (m = (d = n == null ? void 0 : n.closest) == null ? void 0 : d.call(n, "[data-hardpoint-id]")) == null ? void 0 : m.dataset) == null ? void 0 : f.hardpointId) ?? ""
    ).trim();
    if (!a) return;
    const r = await this._persistStagedHardpointFields();
    this.getPersistentActor() ?? this.actor;
    const s = r.find((p) => p.id === a);
    if (!s) return;
    const o = await this._promptHardpointAssignment(s);
    o && (await this._setHardpointOccupant(a, o, { hardpoints: r }), this.render({ force: !0 }));
  }
  async _onClearHardpointItem(t, n) {
    var s, o, l, c, u, d;
    if ((s = t == null ? void 0 : t.preventDefault) == null || s.call(t), (o = t == null ? void 0 : t.stopPropagation) == null || o.call(t), !this.isEditable) return;
    const a = String(
      ((l = n == null ? void 0 : n.dataset) == null ? void 0 : l.hardpointId) ?? ((d = (u = (c = n == null ? void 0 : n.closest) == null ? void 0 : c.call(n, "[data-hardpoint-id]")) == null ? void 0 : u.dataset) == null ? void 0 : d.hardpointId) ?? ""
    ).trim();
    if (!a) return;
    const r = await this._persistStagedHardpointFields();
    await this._deleteMountedHardpointItem(a), await this._setHardpointOccupant(a, "", { hardpoints: r }), this.render({ force: !0 });
  }
  _collectStagedHardpointFields() {
    var n;
    const t = (n = this._getRootElement) == null ? void 0 : n.call(this);
    return t instanceof HTMLElement ? Array.from(t.querySelectorAll("[data-hardpoint-config][data-hardpoint-id]")).map((a) => {
      var o;
      const r = String(((o = a.dataset) == null ? void 0 : o.hardpointId) ?? "").trim();
      if (!r) return null;
      const s = this._collectHardpointRowSettings(a);
      return s ? { id: r, ...s } : null;
    }).filter(Boolean) : [];
  }
  _collectHardpointRowSettings(t = null) {
    var a;
    if (!(t instanceof HTMLElement)) return null;
    const n = {};
    for (const r of t.querySelectorAll("[data-hardpoint-field]")) {
      if (!(r instanceof HTMLInputElement || r instanceof HTMLSelectElement || r instanceof HTMLTextAreaElement) || r.disabled) continue;
      const s = String(((a = r.dataset) == null ? void 0 : a.hardpointField) ?? "").trim();
      ["type", "size", "location"].includes(s) && (n[s] = r.value);
    }
    return Object.keys(n).length ? n : null;
  }
  async _persistStagedHardpointFields() {
    const t = this.getPersistentActor() ?? this.actor, n = gs(t), a = this._getDefaultHardpointLocation(), r = un(n, { defaultLocation: a });
    if (!this.isEditable) return r;
    const s = this._collectStagedHardpointFields();
    if (!s.length) return r;
    const o = OA(n, s, { defaultLocation: a });
    return JSON.stringify(o) === JSON.stringify(r) ? r : (await t.update({ "system.mwd.hardpoints": o }), o);
  }
  async _sanitizeMountedWeaponGroups(t = this.getPersistentActor() ?? this.actor) {
    var o, l;
    const n = t ?? this.actor, a = zi(n).map((c) => String((c == null ? void 0 : c.itemId) ?? "").trim()).filter(Boolean), r = Yu(foundry.utils.deepClone((l = (o = n == null ? void 0 : n.system) == null ? void 0 : o.mwd) == null ? void 0 : l.weaponGroups)), s = _v(r, a);
    s.changed && await n.update({ "system.mwd.weaponGroups": s.groups });
  }
  _buildActiveCrits() {
    var r;
    const t = ((r = this.getPersistentActor) == null ? void 0 : r.call(this)) ?? this.actor, n = this._buildDegradationPanel(), a = new Map((n.locations ?? []).map((s) => [s.key, s]));
    return _n(t).map((s) => {
      var f;
      const o = Pn(s.remedyKey), l = Mu(s), c = a.get(String(s.locationKey ?? "").trim()) ?? null, u = String(s.remedySkillKey ?? o.skillKey ?? "").trim(), d = ((f = Lt(u)) == null ? void 0 : f.label) ?? oe(u), m = He(s.remedyBaseDn ?? o.baseDn, 0) + He((c == null ? void 0 : c.conditionModifier) ?? 0, 0);
      return {
        id: s.id,
        label: s.label ?? oe(s.key),
        locationLabel: s.locationLabel ?? oe(s.locationKey),
        detail: Oh([
          l.statusLabel ? `Status: ${l.statusLabel}` : "",
          l.scopeSummary,
          l.automationMode === "engine" ? "Automated" : "Reminder Only",
          s.escalationKey ? `Escalates: ${s.escalationKey}` : ""
        ]).join(" | "),
        effectSummary: l.effectText,
        remedyLabel: o.label,
        remedySummary: d ? `Reliability + ${d} vs DN ${m}${c ? ` (${c.conditionLabel})` : ""}` : "",
        remedyKey: o.key,
        remediable: o.remediable !== !1,
        machineActorUuid: (t == null ? void 0 : t.uuid) ?? ""
      };
    });
  }
  async _onToggleStatuses(t, n) {
    var s, o, l, c, u, d;
    if ((s = t == null ? void 0 : t.preventDefault) == null || s.call(t), (o = t == null ? void 0 : t.stopPropagation) == null || o.call(t), ((l = n == null ? void 0 : n.dataset) == null ? void 0 : l.actionDisabled) === "true")
      return (u = ui.notifications) == null || u.warn(((c = n == null ? void 0 : n.dataset) == null ? void 0 : c.actionReason) || "Statuses are not available right now."), !1;
    const a = this.getPersistentActor() ?? this.actor, r = this._resolveStatusToken(a);
    return r ? Xf({
      actor: a,
      token: r
    }) : ((d = ui.notifications) == null || d.warn("Statuses require a token for this actor on the current scene."), !1);
  }
  async _onMachineWeaponAttack(t, n) {
    var u, d, m, f, p, g, h, y, b, S;
    (u = t == null ? void 0 : t.preventDefault) == null || u.call(t), (d = t == null ? void 0 : t.stopPropagation) == null || d.call(t);
    const a = this.getPersistentActor() ?? this.actor, r = String(((m = n == null ? void 0 : n.dataset) == null ? void 0 : m.itemId) ?? "").trim(), s = r ? (p = (f = a.items) == null ? void 0 : f.get) == null ? void 0 : p.call(f, r) : null;
    if (!s)
      return (g = ui.notifications) == null || g.warn("That weapon is no longer available."), !1;
    const o = ((h = game.mwd) == null ? void 0 : h.roll) ?? ((b = (y = game.system) == null ? void 0 : y.mwd) == null ? void 0 : b.roll);
    if (!(o != null && o.execute))
      return (S = ui.notifications) == null || S.error("MWD roll system not initialized."), !1;
    const l = this._resolveStatusToken(a), c = await o.execute({
      actor: a,
      payload: {
        intent: "attack",
        sourceType: "mechWeapon",
        sourceId: s.id,
        weaponId: s.id,
        edge: { pool: "physical.grit", allowed: ["pre", "post"] },
        tags: ["combat", "attack", "machine"],
        sourceTokenId: (l == null ? void 0 : l.id) ?? null
      },
      event: t
    });
    return c ? !0 : !!c;
  }
  async _onMachineCritRemedy(t, n) {
    var o, l, c, u, d, m, f, p, g, h, y;
    (o = t == null ? void 0 : t.preventDefault) == null || o.call(t), (l = t == null ? void 0 : t.stopPropagation) == null || l.call(t);
    const a = this.getPersistentActor() ?? this.actor, r = await No({
      machineActorUuid: ((c = n == null ? void 0 : n.dataset) == null ? void 0 : c.machineActorUuid) ?? a.uuid,
      critId: ((u = n == null ? void 0 : n.dataset) == null ? void 0 : u.critId) ?? "",
      remedyKey: ((d = n == null ? void 0 : n.dataset) == null ? void 0 : d.remedyKey) ?? ""
    }, {
      gmOverride: !!((m = game.user) != null && m.isGM)
    });
    if (!r.ok)
      return (f = ui.notifications) == null || f.warn(r.reason ?? "Unable to launch that machine remedy."), !1;
    const s = ((p = game.mwd) == null ? void 0 : p.roll) ?? ((h = (g = game.system) == null ? void 0 : g.mwd) == null ? void 0 : h.roll);
    return s != null && s.execute ? (await s.execute({
      actor: r.actor,
      payload: r.payload,
      event: t
    }), !0) : ((y = ui.notifications) == null || y.error("MWD roll system not initialized."), !1);
  }
  async _onEwAcquire(t, n) {
    var a, r;
    return (a = t == null ? void 0 : t.preventDefault) == null || a.call(t), (r = t == null ? void 0 : t.stopPropagation) == null || r.call(t), v(this, me, hc).call(this, "acquire", t, n);
  }
  async _onEwTarget(t, n) {
    var a, r;
    return (a = t == null ? void 0 : t.preventDefault) == null || a.call(t), (r = t == null ? void 0 : t.stopPropagation) == null || r.call(t), v(this, me, hc).call(this, "targeting", t, n);
  }
  _resolveStatusToken(t = this.actor) {
    var n;
    return Gg(t, {
      sheetToken: ((n = this.getSheetTokenDocument) == null ? void 0 : n.call(this)) ?? null
    });
  }
  _getDefaultHardpointLocation() {
    return this._getAvailableHardpointLocations()[0] ?? "torso";
  }
};
nn = new WeakMap(), Pa = new WeakMap(), Da = new WeakMap(), Vn = new WeakMap(), me = new WeakSet(), Lh = function() {
  var a, r;
  const t = (a = this._getRootElement) == null ? void 0 : a.call(this);
  if (!t) return;
  (r = Y(this, Pa)) == null || r.abort();
  const n = new AbortController();
  Ie(this, Pa, n), t.addEventListener("dragstart", (s) => {
    var u, d, m, f;
    const o = (d = (u = s.target) == null ? void 0 : u.closest) == null ? void 0 : d.call(u, ".mwd-record[data-item-id][draggable='true']");
    if (!o || !t.contains(o)) return;
    const l = this._getOwnedItemFromTarget(o, s), c = ((m = l == null ? void 0 : l.toDragData) == null ? void 0 : m.call(l)) ?? (l != null && l.uuid ? {
      type: "Item",
      uuid: l.uuid
    } : null);
    if (!c) {
      s.preventDefault();
      return;
    }
    s.stopPropagation(), (f = s.dataTransfer) == null || f.setData("text/plain", JSON.stringify(c)), s.dataTransfer && (s.dataTransfer.effectAllowed = "copyMove");
  }, { signal: n.signal }), t.addEventListener("dragover", (s) => {
    var l, c;
    const o = (c = (l = s.target) == null ? void 0 : l.closest) == null ? void 0 : c.call(l, ".mwd-record[data-hardpoint-id]");
    !o || !t.contains(o) || (s.preventDefault(), s.dataTransfer && (s.dataTransfer.dropEffect = "move"));
  }, { signal: n.signal });
}, _h = function() {
  var a, r;
  const t = (a = this._getRootElement) == null ? void 0 : a.call(this);
  if (!t) return;
  (r = Y(this, Da)) == null || r.abort();
  const n = new AbortController();
  Ie(this, Da, n), t.addEventListener("change", (s) => {
    var l, c;
    const o = (c = (l = s.target) == null ? void 0 : l.closest) == null ? void 0 : c.call(l, "[data-hardpoint-field]");
    !o || !t.contains(o) || v(this, me, xh).call(this, o);
  }, { signal: n.signal });
}, xh = async function(t) {
  var l, c;
  if (!this.isEditable) return;
  const n = (l = t == null ? void 0 : t.closest) == null ? void 0 : l.call(t, "[data-hardpoint-config][data-hardpoint-id]"), a = String(((c = n == null ? void 0 : n.dataset) == null ? void 0 : c.hardpointId) ?? "").trim();
  if (!n || !a) return;
  const r = this._collectHardpointRowSettings(n);
  if (!r) return;
  const s = this.getPersistentActor() ?? this.actor, o = $A(
    gs(s),
    a,
    r,
    { defaultLocation: this._getDefaultHardpointLocation() }
  );
  o.changed && await s.update({ "system.mwd.hardpoints": o.hardpoints });
}, gc = function(t, n) {
  var r, s, o, l, c, u, d, m;
  const a = String(
    ((r = t == null ? void 0 : t.dataset) == null ? void 0 : r.itemId) ?? ((l = (o = (s = t == null ? void 0 : t.closest) == null ? void 0 : s.call(t, "[data-item-id]")) == null ? void 0 : o.dataset) == null ? void 0 : l.itemId) ?? ((m = (d = (u = (c = n == null ? void 0 : n.target) == null ? void 0 : c.closest) == null ? void 0 : u.call(c, "[data-item-id]")) == null ? void 0 : d.dataset) == null ? void 0 : m.itemId) ?? ""
  ).trim();
  return a ? this.actor.items.get(a) ?? null : null;
}, hc = async function(t, n, a) {
  var f, p, g, h, y, b, S, w;
  const r = ((f = this.getPersistentActor) == null ? void 0 : f.call(this)) ?? this.actor, s = this._resolveStatusToken(r), o = ((p = game.mwd) == null ? void 0 : p.roll) ?? ((h = (g = game.system) == null ? void 0 : g.mwd) == null ? void 0 : h.roll);
  if (!(o != null && o.execute)) return !1;
  const l = tc({ actor: r, token: s }), c = String(((y = a == null ? void 0 : a.dataset) == null ? void 0 : y.targetTokenUuid) ?? "").trim(), u = String(((b = a == null ? void 0 : a.dataset) == null ? void 0 : b.targetTokenId) ?? "").trim(), d = c || u ? (l.rows ?? []).find(
    (T) => c && (T == null ? void 0 : T.targetTokenUuid) === c || u && (T == null ? void 0 : T.targetTokenId) === u
  ) ?? null : Hg(l, t);
  if (!d) {
    const T = t === "targeting" ? "generate targeting data" : "acquire";
    return (S = ui.notifications) == null || S.warn(`No targeted token is ready to ${T}.`), !1;
  }
  if (!(t === "targeting" ? d.canTarget : d.canAcquire))
    return (w = ui.notifications) == null || w.warn(t === "targeting" ? "That target is not ready for targeting data yet." : "That target cannot advance its detection state right now."), !1;
  try {
    return await o.execute({
      actor: r,
      payload: {
        intent: t,
        sourceTokenId: (s == null ? void 0 : s.id) ?? null,
        targetTokenId: d.targetTokenId,
        targetTokenUuid: d.targetTokenUuid
      },
      event: n
    }), v(this, me, Qi).call(this), !0;
  } catch (T) {
    return console.error(`MWD | Failed to launch EW ${t}`, T), sn(T, `Unable to launch ${t === "targeting" ? "targeting" : "acquire"} roll.`), !1;
  }
}, $h = function() {
  Y(this, Vn).length || Ie(this, Vn, [
    ["targetToken", Hooks.on("targetToken", (t) => {
      var n;
      (t == null ? void 0 : t.id) === ((n = game.user) == null ? void 0 : n.id) && v(this, me, Qi).call(this);
    })],
    ["updateToken", Hooks.on("updateToken", (t, n) => {
      v(this, me, Hh).call(this, n) && v(this, me, zh).call(this, t) && (ik(t, n), v(this, me, Qi).call(this));
    })],
    ["updateCombatant", Hooks.on("updateCombatant", (t, n) => {
      v(this, me, Ns).call(this, t) && v(this, me, Fh).call(this, n) && v(this, me, Qi).call(this);
    })],
    ["createCombatant", Hooks.on("createCombatant", (t) => {
      v(this, me, Ns).call(this, t) && v(this, me, Qi).call(this);
    })],
    ["deleteCombatant", Hooks.on("deleteCombatant", (t) => {
      v(this, me, Ns).call(this, t) && v(this, me, Qi).call(this);
    })],
    ["updateCombat", Hooks.on("updateCombat", (t) => {
      v(this, me, yc).call(this, t) && v(this, me, Qi).call(this);
    })],
    ["deleteCombat", Hooks.on("deleteCombat", (t) => {
      v(this, me, yc).call(this, t) && v(this, me, Qi).call(this);
    })]
  ]);
}, Bh = function() {
  for (const [t, n] of Y(this, Vn))
    Hooks.off(t, n);
  Ie(this, Vn, []);
}, Qi = function() {
  this.rendered && (this._captureScrollPosition(), this.render({ force: !1 }));
}, yc = function(t) {
  var n;
  return !!(t != null && t.id && t.id === ((n = game.combat) == null ? void 0 : n.id));
}, zh = function(t) {
  var o, l, c, u;
  const n = String((t == null ? void 0 : t.id) ?? ((o = t == null ? void 0 : t.document) == null ? void 0 : o.id) ?? "").trim();
  if (!n) return !1;
  const a = this._resolveStatusToken(((l = this.getPersistentActor) == null ? void 0 : l.call(this)) ?? this.actor), r = String((a == null ? void 0 : a.id) ?? ((c = a == null ? void 0 : a.document) == null ? void 0 : c.id) ?? "").trim();
  return r && n === r ? !0 : new Set(
    Array.from(((u = game.user) == null ? void 0 : u.targets) ?? []).map((d) => {
      var m;
      return String((d == null ? void 0 : d.id) ?? ((m = d == null ? void 0 : d.document) == null ? void 0 : m.id) ?? "").trim();
    }).filter(Boolean)
  ).has(n);
}, Ns = function(t) {
  var o, l, c, u, d, m;
  if (!t) return !1;
  const n = this._resolveStatusToken(((o = this.getPersistentActor) == null ? void 0 : o.call(this)) ?? this.actor), a = String((n == null ? void 0 : n.id) ?? ((l = n == null ? void 0 : n.document) == null ? void 0 : l.id) ?? "").trim(), r = String((t == null ? void 0 : t.tokenId) ?? ((c = t == null ? void 0 : t.token) == null ? void 0 : c.id) ?? ((d = (u = t == null ? void 0 : t.token) == null ? void 0 : u.document) == null ? void 0 : d.id) ?? "").trim();
  return r ? a && r === a ? !0 : new Set(
    Array.from(((m = game.user) == null ? void 0 : m.targets) ?? []).map((f) => {
      var p;
      return String((f == null ? void 0 : f.id) ?? ((p = f == null ? void 0 : f.document) == null ? void 0 : p.id) ?? "").trim();
    }).filter(Boolean)
  ).has(r) : !1;
}, Fh = function(t) {
  return v(this, me, fr).call(this, t, "flags.mwd.targeting") || v(this, me, fr).call(this, t, "flags.mwd.ewState") || v(this, me, fr).call(this, t, "flags.mwd.personalCombat") || v(this, me, fr).call(this, t, "tokenId");
}, fr = function(t, n) {
  if (foundry.utils.hasProperty(t, n)) return !0;
  const a = `${n}.`;
  return Object.keys(t ?? {}).some((r) => r === n || r.startsWith(a));
}, Hh = function(t) {
  return foundry.utils.hasProperty(t, "x") || foundry.utils.hasProperty(t, "y") || foundry.utils.hasProperty(t, "elevation");
}, z(Be, "LAYOUT_ID", "vehicle"), z(Be, "PARTS", {
  sheet: {
    get template() {
      return `${se}/v2/actor/vehicle-sheet.hbs`;
    }
  }
}), z(Be, "DEFAULT_OPTIONS", foundry.utils.mergeObject(Oi(Be, Be, "DEFAULT_OPTIONS"), {
  classes: ["vehicle-sheet", M, "actor-sheet-v2", "mwd-vehicle-sheet", "mwd-sheet"],
  window: { minWidth: 520, minHeight: 720, resizable: !0 },
  position: { width: 940, height: 900 },
  actions: {
    ...Oi(Be, Be, "DEFAULT_OPTIONS").actions,
    createOwnedItem: Be.prototype._onCreateOwnedItem,
    editOwnedItem: Be.prototype._onEditOwnedItem,
    deleteOwnedItem: Be.prototype._onDeleteOwnedItem,
    toggleInventoryAccordion: Be.prototype._onToggleInventoryAccordion,
    addHardpoint: Be.prototype._onAddHardpoint,
    deleteHardpoint: Be.prototype._onDeleteHardpoint,
    createHardpointItem: Be.prototype._onCreateHardpointItem,
    assignHardpointItem: Be.prototype._onAssignHardpointItem,
    clearHardpointItem: Be.prototype._onClearHardpointItem,
    machineWeaponAttack: Be.prototype._onMachineWeaponAttack,
    ewAcquire: Be.prototype._onEwAcquire,
    ewTarget: Be.prototype._onEwTarget,
    toggleStatuses: Be.prototype._onToggleStatuses,
    machineCritRemedy: Be.prototype._onMachineCritRemedy,
    assignPilot: Be.prototype._onAssignPilot,
    removePilot: Be.prototype._onRemovePilot,
    openPilot: Be.prototype._onOpenPilot
  }
}, { inplace: !1 }));
let Js = Be;
function Ue(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? t : e;
}
function nr(i) {
  var t, n;
  const e = foundry.utils.deepClone((n = (t = i == null ? void 0 : i.system) == null ? void 0 : t.mwd) == null ? void 0 : n.weaponGroups);
  return Yu(e);
}
function jv(i = []) {
  return i.map((e) => String(e ?? "").trim()).filter(Boolean);
}
function cl(i = []) {
  return i.map((e) => {
    const t = e ?? {};
    return {
      ...t,
      label: String(t.label ?? "").trim(),
      value: String(t.value ?? "").trim(),
      emphasis: t.emphasis ?? "",
      title: String(t.title ?? "").trim(),
      tone: String(t.tone ?? "").trim(),
      parts: Array.isArray(t.parts) ? t.parts.filter((n) => n && n.value !== void 0 && n.value !== null && String(n.value).trim() !== "").map((n) => ({
        label: String(n.label ?? "").trim(),
        value: String(n.value ?? "").trim(),
        tone: String(n.tone ?? "").trim(),
        title: String(n.title ?? "").trim()
      })) : []
    };
  }).filter((e) => e.value !== "" || e.parts.length).map((e) => ({
    ...e,
    hasParts: e.parts.length > 0
  }));
}
function Pm(i = []) {
  return jv(i).map((e) => ({ label: e }));
}
function Dm(i = []) {
  return i.filter((e) => e && e.value !== void 0 && e.value !== null && String(e.value).trim() !== "").map((e) => ({
    label: String(e.label ?? "").trim(),
    value: String(e.value ?? "").trim()
  }));
}
function Uh(i = "") {
  const e = String(i ?? "").trim().toLowerCase();
  return e ? e.charAt(0).toUpperCase() + e.slice(1) : "";
}
function Kv(i = {}) {
  return ["close", "near", "far", "extreme"].map((e) => `${Uh(e)} ${Ue(i == null ? void 0 : i[e], 0)}`).join(" | ");
}
function ar(i = "") {
  var t, n;
  const e = ((n = (t = q == null ? void 0 : q.actor) == null ? void 0 : t.vehicle) == null ? void 0 : n.quickActions) ?? {};
  return String((e == null ? void 0 : e[i]) ?? oe(i)).trim() || oe(i);
}
var dt, jh, Rs, Kh, Gh, Wh, qh, Sc, Vh;
const Bt = class Bt extends Js {
  constructor() {
    super(...arguments);
    Ce(this, dt);
  }
  async _prepareContext(t) {
    const n = await super._prepareContext(t);
    return n.battlemechSheet = {
      heat: this._buildHeatModel(),
      quickActions: this._buildQuickActions(),
      weaponGroupSummary: this._buildWeaponGroupSummary(),
      weaponGroups: this._buildWeaponGroups(),
      hardpoints: this._buildHardpoints(),
      chassisFields: this._buildChassisFields()
    }, n;
  }
  _onRender(t, n) {
    super._onRender(t, n);
    const a = this._getRootElement();
    a && a.querySelectorAll(".mwd-battlemech-groups__name-input").forEach((r) => {
      r.addEventListener("change", (s) => v(this, dt, jh).call(this, s));
    });
  }
  _buildChassisFields() {
    var r, s, o, l;
    const t = Ue((s = (r = this.actor.system) == null ? void 0 : r.mwd) == null ? void 0 : s.tonnage, 0), n = ((l = (o = this.actor.system) == null ? void 0 : o.mwd) == null ? void 0 : l.weightClass) ?? "medium", a = q.mwd.weightClass;
    return [
      {
        label: "Tonnage",
        path: "system.mwd.tonnage",
        isNumber: !0,
        value: t,
        displayValue: String(t)
      },
      {
        label: a.label,
        path: "system.mwd.weightClass",
        isSelect: !0,
        value: n,
        displayValue: a[n] ?? oe(n),
        options: Object.entries(a).filter(([c]) => c !== "label").map(([c, u]) => ({ value: c, label: u, selected: n === c }))
      }
    ];
  }
  _buildConditionMonitors() {
    var a, r, s, o;
    const t = ((r = (a = this.actor.system) == null ? void 0 : a.monitors) == null ? void 0 : r.structure) ?? {}, n = ((o = (s = this.actor.system) == null ? void 0 : s.monitors) == null ? void 0 : o.armor) ?? {};
    return [
      cc({ id: "structure", label: "Structure", kind: "structure", monitor: t, editable: this.isEditable }),
      cc({ id: "armor", label: "Armor", kind: "armor", monitor: n, editable: this.isEditable })
    ];
  }
  _buildSummaryStats() {
    var c, u, d, m, f, p, g, h, y, b, S, w, T, k, C;
    const t = ((u = (c = this.actor.system) == null ? void 0 : c.monitors) == null ? void 0 : u.armor) ?? {}, n = ((m = (d = this.actor.system) == null ? void 0 : d.monitors) == null ? void 0 : m.structure) ?? {}, a = an(this.actor), r = a.status.toUpperCase(), s = lC({ armor: t, structure: n }), o = rh(((p = (f = this.actor.system) == null ? void 0 : f.mwd) == null ? void 0 : p.crits) ?? []), l = Pg({
      actorType: this.actor.type,
      movement: (g = this.actor.system) == null ? void 0 : g.movement,
      legacyMoves: (h = this.actor.system) == null ? void 0 : h.moves,
      jumpProfile: ((S = (b = (y = this.actor.system) == null ? void 0 : y.mwd) == null ? void 0 : b.mobility) == null ? void 0 : S.jumping) ?? null
    });
    return cl([
      { label: "Weight", value: oe(((T = (w = this.actor.system) == null ? void 0 : w.mwd) == null ? void 0 : T.weightClass) ?? "medium"), emphasis: "strong" },
      { label: "Move", parts: l },
      { label: "Tonnage", value: Ue((C = (k = this.actor.system) == null ? void 0 : k.mwd) == null ? void 0 : C.tonnage, 0) },
      { label: "Integrity", parts: s.parts, title: s.title },
      { label: "Heat", value: `${a.current} ${r}`, title: a.status },
      { label: "Pending Heat", value: String(a.pendingGenerated), tone: a.pendingGenerated > 0 ? "orange" : "" },
      { label: "Status", value: o.value, title: o.title, tone: o.count > 0 ? "red" : "" }
    ]);
  }
  _buildSummaryActions() {
    return [];
  }
  _buildAlerts() {
    var n, a;
    const t = ((a = (n = this.actor.system) == null ? void 0 : n.mwd) == null ? void 0 : a.loadout) ?? {};
    return [
      ...Array.isArray(t.errors) ? t.errors.map((r) => ({ tone: "danger", text: r })) : [],
      ...Array.isArray(t.warnings) ? t.warnings.map((r) => ({ tone: "warning", text: r })) : []
    ];
  }
  _buildVehicleSections() {
    var n, a;
    const t = ((a = (n = this.actor.system) == null ? void 0 : n.mwd) == null ? void 0 : a.items) ?? {};
    return {
      slots: this._buildHardpointSlotSection(),
      equipment: this._buildRecordSection({
        sectionId: "equipment",
        itemType: "mechEquipment",
        addLabel: "Add Equipment",
        emptyLabel: "No BattleMech equipment installed.",
        items: t.mechEquipment ?? []
      }),
      modules: this._buildRecordSection({
        sectionId: "modules",
        itemType: "assetModule",
        addLabel: "Add Module",
        emptyLabel: "No asset modules installed.",
        items: t.assetModules ?? []
      }),
      gear: this._buildRecordSection({
        sectionId: "gear",
        itemType: "gear",
        addLabel: "Add Gear",
        emptyLabel: "No stored gear.",
        items: t.gear ?? []
      })
    };
  }
  _buildHeatModel() {
    const t = an(this.actor), n = t.thresholds ?? {};
    return {
      label: "Heat",
      current: t.current,
      trackLength: t.trackLength,
      displayMax: t.displayMax,
      dissipation: t.dissipation,
      effectiveDissipation: t.effectiveDissipation,
      coolingImpaired: t.coolingImpaired,
      pendingGenerated: t.pendingGenerated,
      editable: !!this.isEditable,
      status: t.status,
      thresholds: {
        runningHot: Ue(n.runningHot, 0),
        overheated: Ue(n.overheated, 0),
        shutdown: Ue(n.shutdown, 0),
        hot: Ue(n.hot, 0),
        overheat: Ue(n.overheat, 0),
        danger: Ue(n.danger, 0)
      },
      penalties: {
        movementPenalty: t.penalties.movementPenalty,
        rangedDicePenalty: t.penalties.rangedDicePenalty,
        dangerLevel: t.penalties.dangerLevel
      },
      dangerChecks: t.dangerChecks,
      dangerActions: t.inDanger && t.dangerChecks ? [
        {
          label: "Shutdown Check",
          hint: `Roll ${Ue(t.dangerChecks.shutdownPool, 0)}d6 vs DN ${Ue(t.dangerChecks.shutdownDN, 0)}`,
          dataset: {
            checkKind: "shutdown",
            dn: Ue(t.dangerChecks.shutdownDN, 0)
          }
        },
        {
          label: "Explosion Check",
          hint: `Roll ${Ue(t.dangerChecks.explosionPool, 0)}d6 vs DN ${Ue(t.dangerChecks.explosionDN, 0)}`,
          dataset: {
            checkKind: "explosion",
            dn: Ue(t.dangerChecks.explosionDN, 0)
          }
        }
      ] : [],
      volatile: t.volatile,
      inDanger: t.inDanger,
      segments: Array.from({ length: t.displayMax }, (a, r) => {
        const s = r + 1, o = Ue(n.runningHot, 0), l = Ue(n.overheated, 0), c = Ue(n.shutdown, 0), u = s >= c ? "danger" : s >= l ? "overheat" : s >= o ? "hot" : "safe";
        return {
          value: s,
          filled: s <= t.current,
          current: s === t.current,
          band: u,
          bandLabel: oe(u)
        };
      })
    };
  }
  async _onOpenHeatDialog(t, n) {
    var r;
    if ((r = t == null ? void 0 : t.preventDefault) == null || r.call(t), !this.isEditable) return;
    const a = this.getPersistentActor() ?? this.actor;
    await v(this, dt, qh).call(this, a);
  }
  _buildQuickActions() {
    var c, u;
    const t = this.getPersistentActor() ?? this.actor, n = this._getPreparedRangedWeaponGroups(t), a = ((c = t.system) == null ? void 0 : c.quickActions) ?? {}, r = n.find((d) => d.isPrimary) ?? null, s = n.filter((d) => d.isAttackLegal && d.isAvailableThisActivation), o = Array.isArray((u = this.actor.system) == null ? void 0 : u.meleeProfiles) && this.actor.system.meleeProfiles.length > 0, l = r != null && r.isAttackLegal && (r != null && r.isAvailableThisActivation) ? r.name : (r == null ? void 0 : r.disableReason) || "No ready primary ranged group";
    return [
      {
        label: ar("primaryWeapons"),
        hint: l,
        handler: "mechAttack",
        disabled: !(r != null && r.isAttackLegal && (r != null && r.isAvailableThisActivation)),
        dataset: { attackKind: "primary" }
      },
      {
        label: ar("rangedAttack"),
        hint: s.length > 0 ? "Prompt for a ready weapon group" : "No ready ranged weapon groups",
        handler: "mechAttack",
        disabled: s.length === 0,
        dataset: { attackKind: "ranged" }
      },
      {
        label: ar("meleeAttack"),
        hint: "Prompt for a melee profile",
        handler: "mechAttack",
        disabled: !o,
        dataset: { attackKind: "melee" }
      },
      {
        label: ar("pilotingCheck"),
        hint: "Vehicle handling test",
        handler: "mechRoll",
        disabled: !1,
        dataset: { rollKind: "piloting" }
      },
      {
        label: "EW",
        hint: "Acquire or generate fire solution",
        handler: "mechRoll",
        disabled: !a.hasSensorSweep,
        dataset: { rollKind: "sensor" }
      },
      {
        label: ar("emergencyRepair"),
        hint: "Choose a crit or repairable status",
        handler: "mechRoll",
        disabled: !1,
        dataset: { rollKind: "repair" }
      }
    ];
  }
  _buildWeaponGroupSummary() {
    var o, l, c;
    const t = this.getPersistentActor() ?? this.actor, n = new Og(t).compute(), a = (n == null ? void 0 : n.mountPoints) ?? ((c = (l = (o = t.system) == null ? void 0 : o.mwd) == null ? void 0 : l.loadout) == null ? void 0 : c.mountPoints) ?? {}, r = Ue(a.total, 0), s = Ue(a.used, 0);
    return {
      total: r,
      used: s,
      remaining: Math.max(0, Ue(a.remaining, r - s))
    };
  }
  _buildLoadedHardpointChoices(t) {
    var s, o, l;
    const n = ((s = q == null ? void 0 : q.mwd) == null ? void 0 : s.hardpointType) ?? {}, a = ((o = q == null ? void 0 : q.mwd) == null ? void 0 : o.hardpointSize) ?? {}, r = ((l = q == null ? void 0 : q.mwd) == null ? void 0 : l.hardpointLocation) ?? {};
    return zi(t).map((c, u) => {
      var y, b;
      const d = String((c == null ? void 0 : c.itemId) ?? "").trim();
      if (!d) return null;
      const m = ((b = (y = t.items) == null ? void 0 : y.get) == null ? void 0 : b.call(y, d)) ?? null;
      if (!m) return null;
      const f = r[c.location] ?? oe(c.location), p = n[c.type] ?? oe(c.type), g = a[c.size] ?? oe(c.size), h = `${f} ${p} ${g}`.trim();
      return {
        hardpointId: String((c == null ? void 0 : c.id) ?? `hardpoint-${u + 1}`).trim(),
        itemId: d,
        itemName: String((m == null ? void 0 : m.name) ?? "Mounted Weapon").trim() || "Mounted Weapon",
        hardpointName: h,
        chipLabel: `${(m == null ? void 0 : m.name) ?? "Mounted Weapon"} | ${h}`,
        detailLabel: `${h}: ${(m == null ? void 0 : m.name) ?? "Mounted Weapon"}`
      };
    }).filter(Boolean);
  }
  _buildWeaponGroups() {
    const t = this.getPersistentActor() ?? this.actor, n = this._getPreparedRangedWeaponGroups(t), a = this._buildLoadedHardpointChoices(t), r = new Map(n.map((o) => [o.id, o.name])), s = /* @__PURE__ */ new Map();
    for (const o of n)
      for (const l of Array.from(o.weaponIds ?? [])) {
        const c = String(l ?? "").trim();
        !c || s.has(c) || s.set(c, o.id);
      }
    return n.map((o) => {
      var d, m, f, p, g, h;
      const l = Array.from(o.weaponIds ?? []).map((y) => String(y ?? "").trim()).filter(Boolean), c = a.filter((y) => l.includes(y.itemId)), u = a.map((y) => {
        const b = l.includes(y.itemId), S = s.get(y.itemId) ?? "", w = !!(S && S !== o.id);
        return {
          ...y,
          selected: b,
          assignedElsewhere: w,
          disabled: w,
          title: b ? `Remove ${y.itemName} from ${o.name}` : w ? `${y.itemName} is already bundled in ${r.get(S) ?? "another group"}` : `Add ${y.itemName} to ${o.name}`
        };
      });
      return {
        id: o.id,
        index: o.index,
        isPrimary: !!o.isPrimary,
        name: o.name,
        subtitle: c.length ? c.map((y) => y.itemName).join(", ") : (o.memberWeapons ?? []).map((y) => y.name).join(", "),
        summaryStats: cl([
          { label: "Weapons", value: Array.isArray(o.memberWeapons) ? o.memberWeapons.length : 0, emphasis: "strong" },
          { label: "Damage", value: Ue((d = o.attackSummary) == null ? void 0 : d.damage, 0) },
          { label: "AP", value: Ue((m = o.attackSummary) == null ? void 0 : m.ap, 0) },
          { label: "Heat", value: Ue((f = o.attackSummary) == null ? void 0 : f.heat, 0) }
        ]),
        detailTags: Pm([
          o.isPrimary ? "Primary" : "",
          ((p = o.attackSummary) == null ? void 0 : p.damageTypeLabel) ?? "",
          o.isAttackLegal ? o.isAvailableThisActivation ? "Ready" : "Used" : "Blocked"
        ]),
        detailRows: Dm([
          { label: "Weapon Names", value: (o.memberWeapons ?? []).map((y) => y.name).join(", ") },
          { label: "Bundled Hardpoints", value: c.map((y) => y.detailLabel).join(" | ") },
          { label: "Range Cap", value: Uh(((g = o.attackSummary) == null ? void 0 : g.rangeCap) ?? "") },
          { label: "Attack Ratings", value: Kv(((h = o.attackSummary) == null ? void 0 : h.attackRatings) ?? {}) },
          { label: "Missing IDs", value: (o.missingWeaponIds ?? []).join(", ") },
          { label: "Warnings", value: (o.compatibilityWarnings ?? []).join(" | ") },
          { label: "Status", value: o.disableReason || (o.isAvailableThisActivation ? "Ready to fire" : "Already fired this activation") }
        ]),
        bundleChoices: u,
        bundleHelp: a.length ? "Bundle mounted slot weapons here. Only attached slot weapons appear." : "Mount weapons into loadout slots first, then bundle the mounted slot weapons here.",
        action: {
          label: o.isAttackLegal && o.isAvailableThisActivation ? "Attack Group" : "Unavailable",
          disabled: !(o.isAttackLegal && o.isAvailableThisActivation),
          title: o.disableReason || "Attack Group",
          dataset: {
            attackKind: "group",
            groupId: o.id
          }
        }
      };
    });
  }
  _buildHardpoints() {
    var s, o, l, c, u;
    const t = ((o = (s = this.actor.system) == null ? void 0 : s.mwd) == null ? void 0 : o.loadout) ?? {}, n = ((l = q == null ? void 0 : q.mwd) == null ? void 0 : l.hardpointType) ?? {}, a = ((c = q == null ? void 0 : q.mwd) == null ? void 0 : c.hardpointSize) ?? {}, r = ((u = q == null ? void 0 : q.mwd) == null ? void 0 : u.hardpointLocation) ?? {};
    return Array.from(t.hardpoints ?? []).map((d) => ({
      id: d.id,
      name: `${n[d.type] ?? oe(d.type)} ${a[d.size] ?? oe(d.size)}`,
      subtitle: r[d.location] ?? oe(d.location),
      summaryStats: cl([
        { label: "Type", value: n[d.type] ?? oe(d.type), emphasis: "strong" },
        { label: "Size", value: a[d.size] ?? oe(d.size) }
      ]),
      detailTags: Pm([
        d.occupiedByName ? `Occupied by ${d.occupiedByName}` : "Open"
      ]),
      detailRows: Dm([
        { label: "Location", value: r[d.location] ?? oe(d.location) },
        { label: "Assigned Group", value: d.occupiedByName ?? "Unassigned" }
      ])
    }));
  }
  async _onMechAttack(t, n) {
    var l, c, u, d, m, f, p;
    (l = t == null ? void 0 : t.preventDefault) == null || l.call(t), (c = t == null ? void 0 : t.stopPropagation) == null || c.call(t);
    const a = this.getPersistentActor() ?? this.actor, r = String(((u = n == null ? void 0 : n.dataset) == null ? void 0 : u.attackKind) ?? "").trim(), s = String(((d = n == null ? void 0 : n.dataset) == null ? void 0 : d.groupId) ?? "").trim(), o = this._getPreparedRangedWeaponGroups(a);
    try {
      if (r === "group" && s)
        await v(this, dt, Rs).call(this, a, s);
      else if (r === "primary") {
        const g = o.find((h) => h == null ? void 0 : h.isPrimary) ?? null;
        g != null && g.id ? await v(this, dt, Rs).call(this, a, g.id) : await ((m = a.rollRangedAttack) == null ? void 0 : m.call(a));
      } else if (r === "ranged") {
        const g = await v(this, dt, Gh).call(this, a, o);
        g != null && g.id && await v(this, dt, Rs).call(this, a, g.id);
      } else if (r === "melee") {
        const g = await v(this, dt, Wh).call(this, a);
        g != null && g.weaponId ? await v(this, dt, Kh).call(this, a, g.weaponId) : await ((f = a.rollMeleeAttack) == null ? void 0 : f.call(a));
      } else
        await ((p = a.rollRangedAttack) == null ? void 0 : p.call(a));
    } catch (g) {
      console.error("MWD | Failed to launch BattleMech attack", g), sn(g, "Unable to launch that BattleMech attack.");
    }
  }
  async _onMechRoll(t, n) {
    var s, o, l, c, u, d, m;
    (s = t == null ? void 0 : t.preventDefault) == null || s.call(t), (o = t == null ? void 0 : t.stopPropagation) == null || o.call(t);
    const a = this.getPersistentActor() ?? this.actor, r = String(((l = n == null ? void 0 : n.dataset) == null ? void 0 : l.rollKind) ?? "").trim();
    try {
      r === "piloting" ? await ((c = a.rollPilotingCheck) == null ? void 0 : c.call(a)) : r === "sensor" ? await (((u = a.rollElectronicWarfare) == null ? void 0 : u.call(a)) ?? ((d = a.rollSensorSweep) == null ? void 0 : d.call(a))) : r === "repair" && await ((m = a.rollEmergencyRepair) == null ? void 0 : m.call(a));
    } catch (f) {
      console.error("MWD | Failed to launch BattleMech check", f), sn(f, "Unable to launch that BattleMech check.");
    }
  }
  async _onHeatDangerCheck(t, n) {
    var c, u, d, m, f, p, g, h, y, b, S;
    (c = t == null ? void 0 : t.preventDefault) == null || c.call(t), (u = t == null ? void 0 : t.stopPropagation) == null || u.call(t);
    const a = this.getPersistentActor() ?? this.actor, r = String(((d = n == null ? void 0 : n.dataset) == null ? void 0 : d.checkKind) ?? "").trim();
    if (!["shutdown", "explosion"].includes(r)) {
      (m = ui.notifications) == null || m.warn("Unknown heat danger check.");
      return;
    }
    const s = an(a);
    if (!s.inDanger || !s.dangerChecks) {
      (f = ui.notifications) == null || f.warn("Heat danger checks are only available while the BattleMech is in Danger heat.");
      return;
    }
    const o = Ue(r === "shutdown" ? s.dangerChecks.shutdownDN : s.dangerChecks.explosionDN, 1), l = ((p = game.mwd) == null ? void 0 : p.roll) ?? ((h = (g = game.system) == null ? void 0 : g.mwd) == null ? void 0 : h.roll);
    if (!(l != null && l.execute)) {
      (y = ui.notifications) == null || y.error("MWD roll system not initialized.");
      return;
    }
    try {
      await l.execute({
        actor: a,
        event: t,
        payload: {
          intent: "heatDangerCheck",
          checkKind: r,
          dn: o,
          tags: ["machine", "heat", "danger", r],
          edge: { allowed: [] },
          sourceTokenId: ((S = ((b = this.getSheetTokenDocument) == null ? void 0 : b.call(this)) ?? this._resolveStatusToken(a)) == null ? void 0 : S.id) ?? null
        }
      });
    } catch (w) {
      console.error("MWD | Failed to launch BattleMech heat danger check", w), sn(w, "Unable to launch that heat danger check.");
    }
  }
  async _onAddWeaponGroup(t, n) {
    var s, o, l, c, u, d;
    if ((s = t == null ? void 0 : t.preventDefault) == null || s.call(t), (o = t == null ? void 0 : t.stopPropagation) == null || o.call(t), !this.isEditable) return;
    const a = this.getPersistentActor() ?? this.actor, r = nr(a);
    r.push({
      id: ((c = (l = foundry.utils).randomID) == null ? void 0 : c.call(l)) ?? `group-${r.length + 1}`,
      name: ((d = (u = q == null ? void 0 : q.mwd) == null ? void 0 : u.loadout) == null ? void 0 : d.newGroup) ?? `Weapon Group ${r.length + 1}`,
      weaponIds: [],
      isPrimary: r.length === 0
    }), await a.update({ "system.mwd.weaponGroups": r }), this.render({ force: !0 });
  }
  async _onDeleteWeaponGroup(t, n) {
    var l, c, u, d, m, f, p, g, h, y;
    if ((l = t == null ? void 0 : t.preventDefault) == null || l.call(t), (c = t == null ? void 0 : t.stopPropagation) == null || c.call(t), !this.isEditable) return;
    const a = String(
      ((u = n == null ? void 0 : n.dataset) == null ? void 0 : u.groupId) ?? ((f = (m = (d = n == null ? void 0 : n.closest) == null ? void 0 : d.call(n, "[data-group-id]")) == null ? void 0 : m.dataset) == null ? void 0 : f.groupId) ?? ((y = (h = (g = (p = t == null ? void 0 : t.target) == null ? void 0 : p.closest) == null ? void 0 : g.call(p, "[data-group-id]")) == null ? void 0 : h.dataset) == null ? void 0 : y.groupId) ?? ""
    ).trim();
    if (!a) return;
    const r = this.getPersistentActor() ?? this.actor, s = nr(r), o = s.findIndex((b) => String((b == null ? void 0 : b.id) ?? "").trim() === a);
    o < 0 || (s.splice(o, 1), await r.update({ "system.mwd.weaponGroups": s }), this.render({ force: !0 }));
  }
  async _onTogglePrimaryWeaponGroup(t, n) {
    var l, c, u, d, m, f, p, g, h, y;
    if ((l = t == null ? void 0 : t.preventDefault) == null || l.call(t), (c = t == null ? void 0 : t.stopPropagation) == null || c.call(t), !this.isEditable) return;
    const a = String(
      ((u = n == null ? void 0 : n.dataset) == null ? void 0 : u.groupId) ?? ((f = (m = (d = n == null ? void 0 : n.closest) == null ? void 0 : d.call(n, "[data-group-id]")) == null ? void 0 : m.dataset) == null ? void 0 : f.groupId) ?? ((y = (h = (g = (p = t == null ? void 0 : t.target) == null ? void 0 : p.closest) == null ? void 0 : g.call(p, "[data-group-id]")) == null ? void 0 : h.dataset) == null ? void 0 : y.groupId) ?? ""
    ).trim();
    if (!a) return;
    const r = this.getPersistentActor() ?? this.actor, s = nr(r);
    let o = !1;
    for (const b of s) {
      const S = String((b == null ? void 0 : b.id) ?? "").trim() === a;
      !!(b != null && b.isPrimary) !== S && (b.isPrimary = S, o = !0);
    }
    o && (await r.update({ "system.mwd.weaponGroups": s }), this.render({ force: !0 }));
  }
  async _onToggleWeaponGroupHardpoint(t, n) {
    var f, p, g, h, y, b, S, w, T, k, C, I, O, x, B, P, L, D, G, V, Z, E, X, H, he, ie;
    if ((f = t == null ? void 0 : t.preventDefault) == null || f.call(t), (p = t == null ? void 0 : t.stopPropagation) == null || p.call(t), !this.isEditable) return;
    const a = String(
      ((g = n == null ? void 0 : n.dataset) == null ? void 0 : g.groupId) ?? ((b = (y = (h = n == null ? void 0 : n.closest) == null ? void 0 : h.call(n, "[data-group-id]")) == null ? void 0 : y.dataset) == null ? void 0 : b.groupId) ?? ((k = (T = (w = (S = t == null ? void 0 : t.target) == null ? void 0 : S.closest) == null ? void 0 : w.call(S, "[data-group-id]")) == null ? void 0 : T.dataset) == null ? void 0 : k.groupId) ?? ""
    ).trim(), r = String(
      ((C = n == null ? void 0 : n.dataset) == null ? void 0 : C.hardpointId) ?? ((x = (O = (I = n == null ? void 0 : n.closest) == null ? void 0 : I.call(n, "[data-hardpoint-id]")) == null ? void 0 : O.dataset) == null ? void 0 : x.hardpointId) ?? ((D = (L = (P = (B = t == null ? void 0 : t.target) == null ? void 0 : B.closest) == null ? void 0 : P.call(B, "[data-hardpoint-id]")) == null ? void 0 : L.dataset) == null ? void 0 : D.hardpointId) ?? ""
    ).trim();
    if (!a || !r) return;
    const s = this.getPersistentActor() ?? this.actor, o = zi(s).find((le) => String((le == null ? void 0 : le.id) ?? "").trim() === r) ?? null, l = String((o == null ? void 0 : o.itemId) ?? "").trim();
    if (!l) {
      (G = ui.notifications) == null || G.warn("Only loaded hardpoints can be bundled into a weapon group.");
      return;
    }
    const c = nr(s), u = c.find((le) => String((le == null ? void 0 : le.id) ?? "").trim() === a) ?? null;
    if (!u) return;
    u.weaponIds = Array.isArray(u.weaponIds) ? u.weaponIds.map((le) => String(le ?? "").trim()).filter(Boolean) : [];
    const d = u.weaponIds.includes(l), m = c.find(
      (le) => String((le == null ? void 0 : le.id) ?? "").trim() !== a && Array.isArray(le == null ? void 0 : le.weaponIds) && le.weaponIds.map((ze) => String(ze ?? "").trim()).includes(l)
    ) ?? null;
    if (!d && m) {
      (X = ui.notifications) == null || X.warn(`${((E = (Z = (V = s.items) == null ? void 0 : V.get) == null ? void 0 : Z.call(V, l)) == null ? void 0 : E.name) ?? "That weapon"} is already bundled in ${m.name ?? "another group"}.`);
      return;
    }
    u.weaponIds = d ? u.weaponIds.filter((le) => le !== l) : [...u.weaponIds, l], this._captureScrollPosition(), await s.update({ "system.mwd.weaponGroups": c }), (ie = (he = (H = n == null ? void 0 : n.closest) == null ? void 0 : H.call(n, ".mwd-weapon-group-editor__choice")) == null ? void 0 : he.classList) == null || ie.toggle("is-selected", !d), this.render({ force: !1 });
  }
  _getPreparedRangedWeaponGroups(t) {
    var s, o, l;
    const n = ((s = this.getSheetTokenDocument) == null ? void 0 : s.call(this)) ?? this._resolveStatusToken(t), a = ((o = F.getSnapshot) == null ? void 0 : o.call(F, t, { token: n })) ?? null, r = a != null && a.isCurrentTurn ? ((l = F.getUsedWeaponGroupIds) == null ? void 0 : l.call(F, t, { token: n, snapshot: a })) ?? [] : [];
    return js(t, { usedWeaponGroupIds: r });
  }
};
dt = new WeakSet(), jh = async function(t) {
  var c, u, d;
  if (!this.isEditable) return;
  const n = t.target, a = String((n == null ? void 0 : n.value) ?? "").trim();
  if (!a) return;
  const r = String(
    ((d = (u = (c = n.closest) == null ? void 0 : c.call(n, "[data-group-id]")) == null ? void 0 : u.dataset) == null ? void 0 : d.groupId) ?? ""
  ).trim();
  if (!r) return;
  const s = this.getPersistentActor() ?? this.actor, o = nr(s), l = o.find((m) => String((m == null ? void 0 : m.id) ?? "").trim() === r);
  !l || l.name === a || (l.name = a, await s.update({ "system.mwd.weaponGroups": o }));
}, Rs = async function(t, n) {
  var l, c, u, d, m, f;
  const a = this._getPreparedRangedWeaponGroups(t).find((p) => String((p == null ? void 0 : p.id) ?? "").trim() === String(n ?? "").trim()) ?? null;
  if (!a) {
    (l = ui.notifications) == null || l.warn("That weapon group is no longer available.");
    return;
  }
  if (!a.isAttackLegal || !a.isAvailableThisActivation) {
    (c = ui.notifications) == null || c.warn(a.disableReason || "That weapon group cannot attack right now.");
    return;
  }
  const r = ((u = game.mwd) == null ? void 0 : u.roll) ?? ((m = (d = game.system) == null ? void 0 : d.mwd) == null ? void 0 : m.roll);
  if (!(r != null && r.execute)) {
    await ((f = t.rollRangedAttack) == null ? void 0 : f.call(t));
    return;
  }
  const s = this._resolveStatusToken(t);
  if (await r.execute({
    actor: t,
    payload: {
      intent: "attack",
      sourceType: "weaponGroup",
      sourceId: a.id,
      weaponGroupId: a.id,
      edge: { pool: "physical.grit", allowed: ["pre", "post"] },
      tags: ["combat", "attack", "machine", "groupFire"],
      sourceTokenId: (s == null ? void 0 : s.id) ?? null
    }
  }))
    return !0;
}, Kh = async function(t, n) {
  var l, c, u, d, m, f, p;
  const a = n ? (c = (l = t.items) == null ? void 0 : l.get) == null ? void 0 : c.call(l, n) : null;
  if (!a) {
    (u = ui.notifications) == null || u.warn("That weapon is no longer available.");
    return;
  }
  const r = ((d = game.mwd) == null ? void 0 : d.roll) ?? ((f = (m = game.system) == null ? void 0 : m.mwd) == null ? void 0 : f.roll);
  if (!(r != null && r.execute)) {
    await ((p = t.rollRangedAttack) == null ? void 0 : p.call(t));
    return;
  }
  const s = this._resolveStatusToken(t);
  if (await r.execute({
    actor: t,
    payload: {
      intent: "attack",
      sourceType: "mechWeapon",
      sourceId: a.id,
      weaponId: a.id,
      edge: { pool: "physical.grit", allowed: ["pre", "post"] },
      tags: ["combat", "attack", "machine"],
      sourceTokenId: (s == null ? void 0 : s.id) ?? null
    }
  }))
    return !0;
}, Gh = async function(t, n = null) {
  const r = (Array.isArray(n) && n.length ? n : this._getPreparedRangedWeaponGroups(t)).filter(
    (c) => Array.isArray(c == null ? void 0 : c.weaponIds) && c.weaponIds.length > 0 && c.isAttackLegal && c.isAvailableThisActivation
  );
  if (!r.length) return null;
  if (r.length === 1) return r[0];
  const s = r.find((c) => c == null ? void 0 : c.isPrimary) ?? r[0], o = `<form class="mwd-quick-select">${r.map((c) => `
      <label class="quick-select-option">
        <input type="radio" name="weapon-group" value="${c.id}" ${c.id === s.id ? "checked" : ""}>
        <span>${c.name}${c.isPrimary ? ` (${q.actor.vehicle.quickActions.primaryLabel})` : ""}</span>
      </label>`).join("")}</form>`, l = await foundry.applications.api.DialogV2.wait({
    window: { title: q.actor.vehicle.quickActions.selectWeaponGroup },
    content: o,
    rejectClose: !1,
    buttons: [
      {
        action: "select",
        label: q.common.roll.button,
        icon: "fa-solid fa-dice",
        default: !0,
        callback: (c, u) => {
          var d, m;
          return ((m = (d = u.form) == null ? void 0 : d.elements["weapon-group"]) == null ? void 0 : m.value) ?? s.id;
        }
      }
    ]
  });
  return r.find((c) => c.id === l) ?? s;
}, Wh = async function(t) {
  var o;
  const n = Array.from(((o = t.system) == null ? void 0 : o.meleeProfiles) ?? []);
  if (!n.length) return null;
  if (n.length === 1) return n[0];
  const a = n[0], r = `<form class="mwd-quick-select">${n.map((l) => `
      <label class="quick-select-option">
        <input type="radio" name="melee-profile" value="${l.id}" ${l.id === a.id ? "checked" : ""}>
        <span>${l.name}</span>
      </label>`).join("")}</form>`, s = await foundry.applications.api.DialogV2.wait({
    window: { title: q.actor.vehicle.quickActions.selectMeleeProfile },
    content: r,
    rejectClose: !1,
    buttons: [
      {
        action: "select",
        label: q.common.roll.button,
        icon: "fa-solid fa-dice",
        default: !0,
        callback: (l, c) => {
          var u, d;
          return ((d = (u = c.form) == null ? void 0 : u.elements["melee-profile"]) == null ? void 0 : d.value) ?? a.id;
        }
      }
    ]
  });
  return n.find((l) => l.id === s) ?? a;
}, qh = async function(t) {
  const n = an(t), a = `
      <form class="mwd-heat-dialog" style="display:grid; gap:0.75rem;">
        <label style="display:grid; gap:0.25rem;">
          <span style="font-weight:700; text-transform:uppercase; letter-spacing:0.08em;">Current Heat</span>
          <input type="number" name="currentHeat" value="${n.current}" min="0" step="1" />
        </label>
        <label style="display:grid; gap:0.25rem;">
          <span style="font-weight:700; text-transform:uppercase; letter-spacing:0.08em;">Pending Heat</span>
          <input type="number" name="pendingHeat" value="${n.pendingGenerated}" min="0" step="1" />
        </label>
        <p style="margin:0; opacity:0.8;">Use pending heat for movement surcharges or GM adjustments. Resolve it here when you need an out-of-band heat cycle.</p>
      </form>
    `;
  await foundry.applications.api.DialogV2.wait({
    window: {
      title: `${t.name ?? "BattleMech"} Heat`
    },
    position: {
      width: 420
    },
    content: a,
    buttons: [
      {
        action: "apply",
        label: "Apply",
        icon: "fa-solid fa-check",
        default: !0,
        callback: async (r, s) => {
          var o;
          try {
            return await v(this, dt, Sc).call(this, t, s == null ? void 0 : s.form), !0;
          } catch (l) {
            return console.error("MWD | Failed to apply heat dialog changes", l), (o = ui.notifications) == null || o.error("Unable to apply heat changes."), !1;
          }
        }
      },
      {
        action: "resolve",
        label: "Resolve Pending Heat",
        icon: "fa-solid fa-fire",
        callback: async (r, s) => {
          var o;
          try {
            return await v(this, dt, Sc).call(this, t, s == null ? void 0 : s.form), await v(this, dt, Vh).call(this, t, "heat dialog"), !0;
          } catch (l) {
            return console.error("MWD | Failed to resolve heat from dialog", l), (o = ui.notifications) == null || o.error("Unable to resolve pending heat."), !1;
          }
        }
      },
      {
        action: "cancel",
        label: "Cancel",
        icon: "fa-solid fa-xmark",
        callback: () => !1
      }
    ],
    close: () => !1
  });
}, Sc = async function(t, n) {
  var l, c, u, d, m, f, p, g, h, y, b;
  const a = (c = (l = n == null ? void 0 : n.elements) == null ? void 0 : l.namedItem) == null ? void 0 : c.call(l, "currentHeat"), r = (d = (u = n == null ? void 0 : n.elements) == null ? void 0 : u.namedItem) == null ? void 0 : d.call(u, "pendingHeat"), s = Math.max(0, Number((a == null ? void 0 : a.value) ?? ((p = (f = (m = t.system) == null ? void 0 : m.monitors) == null ? void 0 : f.heat) == null ? void 0 : p.value) ?? 0) || 0), o = Math.max(0, Number((r == null ? void 0 : r.value) ?? ((y = (h = (g = t.system) == null ? void 0 : g.mwd) == null ? void 0 : h.heat) == null ? void 0 : y.pendingGenerated) ?? 0) || 0);
  await ((b = t.setMonitorValue) == null ? void 0 : b.call(t, "heat", s)), await Tu(t, o, { reason: "heat dialog" });
}, Vh = async function(t, n = "sheet control") {
  var o, l;
  const a = ((o = this.getSheetTokenDocument) == null ? void 0 : o.call(this)) ?? this._resolveStatusToken(t), r = ((l = F.getSnapshot) == null ? void 0 : l.call(F, t, { token: a })) ?? null, s = r != null && r.hasCombatant && (r != null && r.isCurrentTurn) ? r.activation : null;
  await ku(t, {
    source: n,
    activation: s,
    postDangerCard: !0
  });
}, z(Bt, "LAYOUT_ID", "battlemech"), z(Bt, "PARTS", {
  sheet: {
    get template() {
      return `${se}/v2/actor/battlemech-sheet.hbs`;
    }
  }
}), z(Bt, "DEFAULT_OPTIONS", foundry.utils.mergeObject(Oi(Bt, Bt, "DEFAULT_OPTIONS"), {
  classes: ["battlemech-sheet", M, "actor-sheet-v2", "mwd-battlemech-sheet", "mwd-sheet"],
  position: { width: 980, height: 940 },
  actions: {
    ...Oi(Bt, Bt, "DEFAULT_OPTIONS").actions,
    mechAttack: Bt.prototype._onMechAttack,
    mechRoll: Bt.prototype._onMechRoll,
    heatDangerCheck: Bt.prototype._onHeatDangerCheck,
    openHeatDialog: Bt.prototype._onOpenHeatDialog,
    addWeaponGroup: Bt.prototype._onAddWeaponGroup,
    deleteWeaponGroup: Bt.prototype._onDeleteWeaponGroup,
    togglePrimaryWeaponGroup: Bt.prototype._onTogglePrimaryWeaponGroup,
    toggleWeaponGroupHardpoint: Bt.prototype._onToggleWeaponGroupHardpoint
  }
}, { inplace: !1 }));
let bc = Bt;
function Gv() {
  console.log(`${Le}Registering Actor sheets (V2)`);
  const { Actors: i } = foundry.documents.collections;
  i.registerSheet(M, dc, {
    types: ["character"],
    makeDefault: !0,
    label: "Character (V2)"
  }), i.registerSheet(M, Rh, {
    types: ["npc"],
    makeDefault: !0,
    label: "NPC (V2)"
  }), i.registerSheet(M, Js, {
    types: ["vehicle"],
    makeDefault: !0,
    label: "Vehicle (V2)"
  }), i.registerSheet(M, bc, {
    types: ["battlemech"],
    makeDefault: !0,
    label: "BattleMech (V2)"
  });
}
const { HandlebarsApplicationMixin: Wv } = foundry.applications.api, { HTMLField: Om, StringField: qv } = foundry.data.fields, ul = /* @__PURE__ */ new Set(["system.notes", "system.description"]), Vv = /* @__PURE__ */ new Set(["name"]), Yv = Object.freeze({
  [A.itemType.personalWeapon]: `${se}/v2/item/personal-weapon-root.hbs`,
  [A.itemType.mechWeapon]: `${se}/v2/item/mech-weapon-root.hbs`,
  [A.itemType.armor]: `${se}/v2/item/armor-root.hbs`
});
function dl(i, e) {
  const t = new i({ required: !1, blank: !0, initial: "" });
  return t.name = e, t;
}
function Jv(i = {}) {
  return {
    ...i,
    sourceReference: i.sourceReference ?? dl(qv, "system.sourceReference"),
    notes: i.notes ?? dl(Om, "system.notes"),
    description: i.description ?? dl(Om, "system.description")
  };
}
function Qv(i = {}) {
  return Object.fromEntries(
    Object.entries(i ?? {}).filter(([, e]) => e !== void 0)
  );
}
var Yn, Mn, Jn, Oa, Ui, pr, Ac;
const kt = class kt extends Wv(foundry.applications.sheets.ItemSheetV2) {
  constructor() {
    super(...arguments);
    Ce(this, Ui);
    Ce(this, Yn, /* @__PURE__ */ new Map());
    Ce(this, Mn, /* @__PURE__ */ new Map());
    Ce(this, Jn, null);
    Ce(this, Oa, /* @__PURE__ */ new Map());
    /** @override */
    z(this, "tabGroups", {
      primary: "main"
      // Default tab
    });
  }
  _getCanonicalItemType() {
    var t, n;
    return ((t = this.item) == null ? void 0 : t.canonicalType) ?? ((n = this.item) == null ? void 0 : n.type);
  }
  _getCanonicalItemTypeFromOptions(t) {
    const n = t == null ? void 0 : t.document;
    return (n == null ? void 0 : n.canonicalType) ?? (n == null ? void 0 : n.type);
  }
  /** @override */
  static get DEFAULT_OPTIONS() {
    return foundry.utils.mergeObject(super.DEFAULT_OPTIONS, {
      classes: ["sheet", "item", M, "appv2", "mwd-sheet", "item-sheet"],
      position: {
        width: 760,
        height: 860
      },
      window: {
        resizable: !0
      },
      actions: {
        editImage: kt._onEditImage,
        tab: kt.prototype._onClickTab,
        accordion: kt.prototype._onClickAccordion,
        checkbarElement: kt._onClickCheckbar,
        modifierAdd: kt._onModifierAdd,
        modifierDelete: kt._onModifierDelete,
        modifierValueChange: kt._onModifierValueChange,
        modifierConditionChange: kt._onModifierConditionChange,
        modifierSelectionChange: kt._onModifierSelectionChange,
        effectCreate: kt._onEffectCreate,
        effectEdit: kt._onEffectEdit,
        effectDelete: kt._onEffectDelete,
        effectToggleDisabled: kt._onEffectToggleDisabled
      },
      form: {
        submitOnChange: !1,
        closeOnSubmit: !1,
        handler: kt.prototype._onSubmitForm
      }
    }, { inplace: !1 });
  }
  static get defaultOptions() {
    return this.DEFAULT_OPTIONS;
  }
  _initializeApplicationOptions(t) {
    var s, o, l, c;
    t = super._initializeApplicationOptions(t), t.classes = Array.from(t.classes ?? []);
    const n = this._getCanonicalItemTypeFromOptions(t);
    n && t.classes.push(String(n));
    const a = ((c = (l = (o = (s = game.system) == null ? void 0 : s.mwd) == null ? void 0 : o.styles) == null ? void 0 : l.selectCssClass) == null ? void 0 : c.call(l)) ?? "mwd-theme-default", r = ["mwd-theme-default", "mwd-theme-sra"];
    return t.classes = t.classes.filter((u) => !r.includes(u)), t.classes.push(a), t;
  }
  /* -------------------------------------------- */
  /*  Rendering                                   */
  /* -------------------------------------------- */
  /**
   * Dynamically determine the template based on item type.
   * @param {string} partId - The part identifier
   * @returns {string} The template path
   * @override
   */
  _getPartTemplate(t) {
    var n;
    if (t === "sheet") {
      const a = this._getCanonicalItemType();
      return Yv[a] ?? `${se}/v2/item/${a}.hbs`;
    }
    return ((n = super._getPartTemplate) == null ? void 0 : n.call(this, t)) ?? "";
  }
  /**
   * Override title to show localized item type and name.
   * @override
   */
  get title() {
    const t = this._getCanonicalItemType();
    return `${q.itemType.singular[t] ?? t}: ${this.item.name}`;
  }
  /**
   * Prepare context data for rendering.
   * @param {object} options - Rendering options
   * @returns {Promise<object>} The context object
   * @override
   */
  async _prepareContext(t) {
    var C, I, O, x, B, P, L, D, G;
    const n = await super._prepareContext(t), a = ((I = (C = game.system.mwd.modifiers) == null ? void 0 : C.getEnums) == null ? void 0 : I.call(C)) ?? {}, r = foundry.utils.deepClone((n == null ? void 0 : n.options) ?? {}), s = Jv((n == null ? void 0 : n.fields) ?? ((x = (O = this.item.system) == null ? void 0 : O.schema) == null ? void 0 : x.fields) ?? {}), o = ((P = (B = this.item.actor) == null ? void 0 : B.getAttributes) == null ? void 0 : P.call(B, this.item)) ?? [], l = this._getCanonicalItemType(), c = !this.item.actor, u = !!this.item.actor, d = q.itemType.singular[l] ?? l, m = this._getEffectEntries(), f = m.filter((V) => V.syncedCount > 0).length, p = this.constructor.LAYOUT_ID, g = this.item.actor ? (V) => o.includes(V) : (V) => !0, h = l === A.itemType.skill, b = ["mwd", "item-sheet", this.isEditable ? "editable" : "locked"], S = b.join(" ");
    r.classes = b, r.cssClass = S;
    const w = async (V, { secrets: Z = this.item.isOwner } = {}) => foundry.applications.ux.TextEditor.implementation.enrichHTML(V ?? "", {
      async: !0,
      secrets: Z,
      relativeTo: this.item
    }), T = foundry.utils.expandObject({
      "system.notes": await w(this.item.system.notes ?? ""),
      "system.description": await w(this.item.system.description ?? "")
    }), k = {
      ...n,
      item: this.item,
      data: this.item,
      system: this.item.system,
      // AppV2 prose editors need both raw field definitions and pre-enriched
      // HTML. Keeping both here avoids template-specific enrichment branches.
      fields: s,
      enriched: T,
      enrichedDescription: ((L = T == null ? void 0 : T.system) == null ? void 0 : L.description) ?? "",
      options: {
        ...r,
        isGM: game.user.isGM,
        limited: !this.document.isOwner,
        owner: this.document.isOwner,
        isOwned: !!this.item.actor,
        editable: this.isEditable,
        cssClass: S,
        viewMode: !1
        // Items don't have view mode like actors do
      },
      ENUMS: {
        ...Oe.getEnums(g, h),
        ...a
      },
      MWD: q,
      itemSheet: {
        canonicalType: l,
        typeLabel: d,
        isArmorSheet: l === A.itemType.armor,
        isStandalone: c,
        canUseActorControls: u,
        supportsEffectSync: !!((G = (D = this.item).supportsEquippedEffectSync) != null && G.call(D)),
        effectEntries: m,
        effectCount: m.length,
        syncedEffectCount: f,
        summaryChips: this._getSummaryChips(),
        stateChips: this._getStateChips(m)
      },
      cssClass: S,
      tabs: this._getTabs()
    };
    return p && (k.layout = await oa.get(p)), k;
  }
  /**
   * Get tab configuration for this item type.
   * Override in subclasses if needed.
   * @returns {object} Tab configuration
   * @protected
   */
  _getTabs() {
    return {
      main: { id: "main", group: "primary", label: "Details" },
      modifiers: { id: "modifiers", group: "primary", label: "Modifiers" },
      effects: { id: "effects", group: "primary", label: "Effects" }
    };
  }
  _getSummaryChips() {
    return [];
  }
  _getStateChips(t = []) {
    var a, r, s;
    const n = [];
    return n.push({ kind: "ownership", label: this.item.actor ? "Owned Item" : "World Item" }), Object.prototype.hasOwnProperty.call(this.item.system ?? {}, "equipped") && n.push({
      kind: "equipment",
      label: (a = this.item.system) != null && a.equipped ? "Equipped" : "Unequipped",
      tone: (r = this.item.system) != null && r.equipped ? "active" : "muted"
    }), Object.prototype.hasOwnProperty.call(this.item.system ?? {}, "isPrimary") && ((s = this.item.system) != null && s.isPrimary) && n.push({ kind: "role", label: "Primary", tone: "accent" }), t.length && n.push({
      kind: "effects",
      label: `${t.length} Effect${t.length === 1 ? "" : "s"}`,
      tone: t.some((o) => o.syncedCount > 0) ? "active" : "muted"
    }), n;
  }
  _getEffectEntries() {
    var a, r, s, o, l, c;
    const t = /* @__PURE__ */ new Map(), n = ((r = (a = this.item).getSyncedActorEffects) == null ? void 0 : r.call(a)) ?? [];
    for (const u of n) {
      const d = (l = (o = (s = u.flags) == null ? void 0 : s[M]) == null ? void 0 : o.equippedItemSync) == null ? void 0 : l.sourceEffectId;
      if (!d) continue;
      const m = t.get(d) ?? [];
      m.push(u), t.set(d, m);
    }
    return Array.from(((c = this.item.effects) == null ? void 0 : c.contents) ?? []).map((u) => {
      var m, f, p, g, h, y, b;
      const d = t.get(u.id) ?? [];
      return {
        id: u.id,
        name: u.name || "New Effect",
        img: u.img || "icons/svg/aura.svg",
        disabled: !!u.disabled,
        transfer: !!u.transfer,
        changesCount: Array.isArray(u.changes) ? u.changes.length : 0,
        statusesCount: Number(((m = u.statuses) == null ? void 0 : m.size) ?? ((f = u.statuses) == null ? void 0 : f.length) ?? 0),
        durationLabel: (p = u.duration) != null && p.seconds ? `${u.duration.seconds}s` : (g = u.duration) != null && g.rounds ? `${u.duration.rounds} rounds` : "Passive",
        syncedCount: d.length,
        syncLabel: this.item.actor ? (y = (h = this.item).supportsEquippedEffectSync) != null && y.call(h) ? (b = this.item.system) != null && b.equipped ? d.length ? `Synced to actor (${d.length})` : "Pending sync" : "Applies when equipped" : "No equip sync" : "World item"
      };
    });
  }
  _getRootElement() {
    var t;
    return this.element instanceof HTMLElement ? this.element : (t = this.element) == null ? void 0 : t[0];
  }
  _onClickTab(t, n) {
    var l, c, u;
    const a = ((l = n == null ? void 0 : n.closest) == null ? void 0 : l.call(n, ".csb-tab-link[data-tab]")) ?? ((u = (c = t == null ? void 0 : t.target) == null ? void 0 : c.closest) == null ? void 0 : u.call(c, ".csb-tab-link[data-tab]"));
    if (!a) return;
    const r = a.closest(".csb-tabs");
    if (!r) return;
    const s = r.dataset.group || "default", o = a.dataset.tab;
    o && (Y(this, Yn).set(s, o), v(this, Ui, pr).call(this, this._getRootElement(), s, o));
  }
  _onClickAccordion(t, n) {
    var u, d, m;
    const a = ((u = n == null ? void 0 : n.closest) == null ? void 0 : u.call(n, ".csb-accordion__trigger[data-section]")) ?? ((m = (d = t == null ? void 0 : t.target) == null ? void 0 : d.closest) == null ? void 0 : m.call(d, ".csb-accordion__trigger[data-section]"));
    if (!a) return;
    const r = a.dataset.section, s = a.closest(".csb-accordion");
    if (!s || !r) return;
    const o = s.dataset.group || "default", c = (Y(this, Mn).has(o) ? Y(this, Mn).get(o) : s.dataset.default || null) === r ? null : r;
    Y(this, Mn).set(o, c), v(this, Ui, Ac).call(this, s, c);
  }
  _onRender(t, n) {
    var s, o, l, c;
    (s = super._onRender) == null || s.call(this, t, n), (o = this.window) != null && o.title && (this.window.title.textContent = this.title);
    const a = this._getRootElement();
    if (!a) return;
    const r = a.querySelector('.item-name input[name="name"]');
    r instanceof HTMLInputElement && (r.setAttribute("dir", "ltr"), r.style.direction = "ltr", r.style.unicodeBidi = "isolate", r.style.textAlign = "left", r.style.writingMode = "horizontal-tb");
    for (const u of a.querySelectorAll(".sheet-tabs")) {
      const d = u.dataset.group || "default", m = Array.from(u.querySelectorAll("[data-tab]"));
      if (!m.length) continue;
      for (const h of m)
        h.addEventListener("click", (y) => {
          y.preventDefault(), y.stopPropagation();
          const b = h.dataset.tab;
          b && (Y(this, Yn).set(d, b), v(this, Ui, pr).call(this, a, d, b));
        });
      const f = Y(this, Yn).get(d), p = u.dataset.default || ((l = m[0]) == null ? void 0 : l.dataset.tab), g = f || p;
      g && v(this, Ui, pr).call(this, a, d, g);
    }
    for (const u of a.querySelectorAll(".csb-tabs")) {
      const d = u.dataset.group || "default", m = Array.from(u.querySelectorAll(".csb-tab-link[data-tab]"));
      if (!m.length) continue;
      const f = Y(this, Yn).get(d), p = u.dataset.default || ((c = m[0]) == null ? void 0 : c.dataset.tab), g = f || p;
      g && v(this, Ui, pr).call(this, a, d, g);
    }
    for (const u of a.querySelectorAll(".csb-accordion")) {
      const d = u.dataset.group || "default", m = Y(this, Mn).has(d) ? Y(this, Mn).get(d) : u.dataset.default || null;
      v(this, Ui, Ac).call(this, u, m);
    }
    for (const u of a.querySelectorAll("prose-mirror[name]")) {
      const d = u.getAttribute("name") ?? "";
      ul.has(d) && u.addEventListener("change", (m) => {
        m.preventDefault(), m.stopPropagation(), this._updateRichTextField(u);
      });
    }
    if (this.isEditable)
      for (const u of a.querySelectorAll("input[name], select[name], textarea[name]")) {
        if (u.closest("prose-mirror") || u.hasAttribute("data-action") || !(u instanceof HTMLElement)) continue;
        const d = String(u.getAttribute("name") ?? "").trim();
        u instanceof HTMLInputElement && !Vv.has(d) && !["checkbox", "radio"].includes(u.type) ? u.addEventListener("input", (m) => {
          m.preventDefault(), this._queueNamedFieldSync(m.currentTarget ?? u);
        }) : u instanceof HTMLTextAreaElement && u.addEventListener("input", (m) => {
          m.preventDefault(), this._queueNamedFieldSync(m.currentTarget ?? u);
        }), u.addEventListener("change", (m) => {
          m.preventDefault(), this._syncNamedField(m.currentTarget ?? u);
        });
      }
    this._restoreScrollPositions();
  }
  async _updateRichTextField(t) {
    var s;
    const n = String(((s = t == null ? void 0 : t.getAttribute) == null ? void 0 : s.call(t, "name")) ?? (t == null ? void 0 : t.name) ?? "");
    if (!this.isEditable || !ul.has(n)) return;
    const a = String(t.value ?? ""), r = String(foundry.utils.getProperty(this.item, n) ?? "");
    if (a !== r)
      try {
        await this.item.update({ [n]: a });
      } catch (o) {
        console.warn("MWD | Rich text item update failed:", o);
      }
  }
  _queueNamedFieldSync(t, n = {}) {
    var o;
    if (!this.isEditable) return;
    const a = String(((o = t == null ? void 0 : t.getAttribute) == null ? void 0 : o.call(t, "name")) ?? "").trim() || foundry.utils.randomID(), r = Y(this, Oa).get(a);
    r && clearTimeout(r);
    const s = setTimeout(() => {
      Y(this, Oa).delete(a), this._syncNamedField(t, n);
    }, 180);
    Y(this, Oa).set(a, s);
  }
  _getNamedFieldUpdate(t) {
    var r, s;
    if (!(t instanceof HTMLElement)) return null;
    const n = String(((r = t.getAttribute) == null ? void 0 : r.call(t, "name")) ?? "").trim();
    if (!n || ul.has(n)) return null;
    if (t instanceof HTMLInputElement) {
      if (t.type === "radio" && !t.checked) return null;
      if (t.type === "checkbox") return { [n]: t.checked };
      if (t.type === "number") {
        const o = Number(t.value);
        return Number.isFinite(o) ? { [n]: o } : null;
      }
    }
    const a = String(((s = t.dataset) == null ? void 0 : s.dtype) ?? "").trim().toLowerCase();
    if (a === "number") {
      const o = Number(t.value);
      return Number.isFinite(o) ? { [n]: o } : null;
    }
    return a === "boolean" ? { [n]: t.value === "true" } : { [n]: String(t.value ?? "") };
  }
  async _syncNamedField(t, n = {}) {
    if (!this.isEditable) return;
    const a = this._getNamedFieldUpdate(t), r = Qv({
      ...a ?? {},
      ...n && typeof n == "object" ? n : {}
    });
    if (Object.keys(r).length) {
      this._captureScrollPositions();
      try {
        await this.item.update(r);
      } catch (s) {
        console.warn("MWD | Item field sync failed:", { updates: r, err: s });
      }
    }
  }
  async _onSubmitForm(t, n, a, { updateData: r = null } = {}) {
    if (!this.isEditable || !(n instanceof HTMLFormElement)) return;
    this._captureScrollPositions();
    const s = this._prepareSubmitData(t, n, a, r ?? {});
    await this._processSubmitData(t, n, s);
  }
  _getScrollRestoreSelectors() {
    return [".sheet-body", ".csb-tab-panels"];
  }
  _captureScrollPositions() {
    const t = this._getRootElement();
    if (!t) {
      Ie(this, Jn, null);
      return;
    }
    const n = [];
    for (const a of this._getScrollRestoreSelectors())
      t.querySelectorAll(a).forEach((r, s) => {
        r instanceof HTMLElement && n.push({
          selector: a,
          index: s,
          top: r.scrollTop,
          left: r.scrollLeft
        });
      });
    Ie(this, Jn, n.length ? n : null);
  }
  _restoreScrollPositions() {
    const t = Y(this, Jn);
    if (!(t != null && t.length)) return;
    const n = () => {
      const a = this._getRootElement();
      if (a)
        for (const r of t) {
          const s = a.querySelectorAll(r.selector).item(r.index);
          s instanceof HTMLElement && (s.scrollTop = r.top, s.scrollLeft = r.left);
        }
    };
    n(), requestAnimationFrame(n), Ie(this, Jn, null);
  }
  /**
   * Override header buttons to add custom controls.
   * @returns {object[]} Array of header button configurations
   * @override
   */
  _getHeaderControls() {
    return super._getHeaderControls();
  }
  /* -------------------------------------------- */
  /*  Action Handlers                             */
  /* -------------------------------------------- */
  /**
   * Handle clicking a checkbar element (monitor).
   * @param {Event} event - The triggering event
   * @param {HTMLElement} target - The clicked element
   * @returns {Promise<void>}
   * @static
   */
  static async _onClickCheckbar(t, n) {
    const a = this.item;
    if (!a.parent) return;
    const r = n.closest(".checkbar-root");
    if (!r) return;
    const s = r.dataset.monitorCode, o = Number.parseInt(n.dataset.index), l = n.dataset.checked === "true";
    await a.parent.switchMonitorCheck(s, o, l);
  }
  static async _onEditImage(t) {
    var r, s, o;
    if ((r = t == null ? void 0 : t.preventDefault) == null || r.call(t), (s = t == null ? void 0 : t.stopPropagation) == null || s.call(t), !this.isEditable) return;
    const n = foundry.applications.apps.FilePicker.implementation;
    new n({
      type: "image",
      current: ((o = this.item) == null ? void 0 : o.img) ?? "",
      callback: async (l) => {
        l && await this.item.update({ img: l });
      }
    }).render(!0);
  }
  /**
   * Handle adding a new modifier.
   * @param {Event} event - The triggering event
   * @param {HTMLElement} target - The clicked element
   * @returns {Promise<void>}
   * @static
   */
  static async _onModifierAdd(t, n) {
    await this.item.createModifier();
  }
  /**
   * Handle deleting a modifier.
   * @param {Event} event - The triggering event
   * @param {HTMLElement} target - The clicked element
   * @returns {Promise<void>}
   * @static
   */
  static async _onModifierDelete(t, n) {
    const a = n.closest(".define-modifier");
    if (!a) return;
    const r = a.dataset.modifierId;
    r && await this.item.deleteModifier(r);
  }
  /**
   * Handle changing a modifier's value.
   * @param {Event} event - The triggering event
   * @param {HTMLElement} target - The input element
   * @returns {Promise<void>}
   * @static
   */
  static async _onModifierValueChange(t, n) {
    const a = n.closest(".define-modifier");
    if (!a) return;
    const r = a.dataset.modifierId;
    r && await this.item.changeModifierValue(r, n.value);
  }
  /**
   * Handle changing a modifier's condition.
   * @param {Event} event - The triggering event
   * @param {HTMLElement} target - The input element
   * @returns {Promise<void>}
   * @static
   */
  static async _onModifierConditionChange(t, n) {
    const a = n.closest(".define-modifier");
    if (!a) return;
    const r = a.dataset.modifierId;
    r && await this.item.changeModifierCondition(r, n.value);
  }
  /**
   * Handle changing a modifier's selection (dropdown).
   * @param {Event} event - The triggering event
   * @param {HTMLElement} target - The select element
   * @returns {Promise<void>}
   * @static
   */
  static async _onModifierSelectionChange(t, n) {
    const a = n.closest(".define-modifier");
    if (!a) return;
    const r = a.dataset.modifierId, s = n.dataset.modifierSelect;
    r && s && await this.item.changeModifierSelection(r, s, n.value);
  }
  static async _onEffectCreate(t, n) {
    var r, s, o;
    (r = t == null ? void 0 : t.preventDefault) == null || r.call(t), (s = t == null ? void 0 : t.stopPropagation) == null || s.call(t);
    const [a] = await this.item.createEmbeddedDocuments("ActiveEffect", [{
      name: `${this.item.name} Effect`,
      img: this.item.img || "icons/svg/aura.svg",
      disabled: !1,
      transfer: !1,
      changes: []
    }]);
    (o = a == null ? void 0 : a.sheet) == null || o.render(!0);
  }
  static async _onEffectEdit(t, n) {
    var s, o, l, c, u, d, m;
    (s = t == null ? void 0 : t.preventDefault) == null || s.call(t), (o = t == null ? void 0 : t.stopPropagation) == null || o.call(t);
    const a = ((l = n == null ? void 0 : n.dataset) == null ? void 0 : l.effectId) ?? ((d = (u = (c = n == null ? void 0 : n.closest) == null ? void 0 : c.call(n, "[data-effect-id]")) == null ? void 0 : u.dataset) == null ? void 0 : d.effectId);
    if (!a) return;
    const r = this.item.effects.get(a);
    (m = r == null ? void 0 : r.sheet) == null || m.render(!0);
  }
  static async _onEffectDelete(t, n) {
    var r, s, o, l, c, u;
    (r = t == null ? void 0 : t.preventDefault) == null || r.call(t), (s = t == null ? void 0 : t.stopPropagation) == null || s.call(t);
    const a = ((o = n == null ? void 0 : n.dataset) == null ? void 0 : o.effectId) ?? ((u = (c = (l = n == null ? void 0 : n.closest) == null ? void 0 : l.call(n, "[data-effect-id]")) == null ? void 0 : c.dataset) == null ? void 0 : u.effectId);
    a && await this.item.deleteEmbeddedDocuments("ActiveEffect", [a]);
  }
  static async _onEffectToggleDisabled(t, n) {
    var s, o, l, c, u, d;
    (s = t == null ? void 0 : t.preventDefault) == null || s.call(t), (o = t == null ? void 0 : t.stopPropagation) == null || o.call(t);
    const a = ((l = n == null ? void 0 : n.dataset) == null ? void 0 : l.effectId) ?? ((d = (u = (c = n == null ? void 0 : n.closest) == null ? void 0 : c.call(n, "[data-effect-id]")) == null ? void 0 : u.dataset) == null ? void 0 : d.effectId);
    if (!a) return;
    const r = this.item.effects.get(a);
    r && await r.update({ disabled: !r.disabled });
  }
};
Yn = new WeakMap(), Mn = new WeakMap(), Jn = new WeakMap(), Oa = new WeakMap(), Ui = new WeakSet(), pr = function(t, n, a) {
  t && (t.querySelectorAll(`.csb-tabs[data-group="${n}"] .csb-tab-link[data-tab]`).forEach((r) => {
    r.classList.toggle("is-active", r.dataset.tab === a);
  }), t.querySelectorAll(`.csb-tabs[data-group="${n}"] .csb-tab-panel[data-tab]`).forEach((r) => {
    r.classList.toggle("is-active", r.dataset.tab === a);
  }), t.querySelectorAll(".sheet-tabs [data-tab]").forEach((r) => {
    var o;
    (((o = r.closest(".sheet-tabs")) == null ? void 0 : o.dataset.group) || "default") === n && r.classList.toggle("active", r.dataset.tab === a);
  }), t.querySelectorAll(`.tab[data-group="${n}"]`).forEach((r) => {
    r.classList.toggle("active", r.dataset.tab === a);
  }));
}, Ac = function(t, n) {
  t.dataset.activeSection = n ?? "", t.querySelectorAll(".csb-accordion__section").forEach((a) => {
    const r = a.dataset.section === n;
    a.classList.toggle("is-active", r);
  }), t.querySelectorAll(".csb-accordion__trigger").forEach((a) => {
    const r = a.dataset.section === n;
    a.classList.toggle("is-active", r), a.setAttribute("aria-expanded", r ? "true" : "false");
  }), t.querySelectorAll(".csb-accordion__panel").forEach((a) => {
    const r = a.closest(".csb-accordion__section"), s = (r == null ? void 0 : r.dataset.section) === n;
    a.classList.toggle("is-active", s);
  });
}, z(kt, "LAYOUT_ID", null), /** @override */
z(kt, "PARTS", {
  sheet: {
    template: "",
    // Set dynamically in _getPartTemplate
    scrollable: [".sheet-body"]
  }
}), z(kt, "TABS", {
  primary: {
    id: "primary",
    group: "primary",
    navSelector: ".sheet-tabs",
    contentSelector: ".sheet-body",
    initial: "main",
    // This must match your tab name
    tabs: [
      { id: "main" },
      { id: "modifiers" }
    ]
  }
});
let qi = kt;
class wc extends qi {
}
z(wc, "LAYOUT_ID", "contact"), z(wc, "PARTS", {
  sheet: {
    template: `${se}/v2/item/contact.hbs`,
    scrollable: [".sheet-body"]
  }
});
const Xv = Object.freeze([
  { value: "audiovisual", label: "Audiovisual Gear" },
  { value: "communication", label: "Communication Gear" },
  { value: "computing", label: "Computing Gear" },
  { value: "espionage", label: "Espionage Gear" },
  { value: "hostileEnvironment", label: "Hostile Environment Gear" },
  { value: "medical", label: "Medical Gear" },
  { value: "optical", label: "Optical Gear" },
  { value: "power", label: "Power Gear" },
  { value: "repairSalvage", label: "Repair/Salvage Gear" },
  { value: "survival", label: "Survival Gear" },
  { value: "surveillance", label: "Surveillance Gear" }
]), Zv = Object.freeze([
  { value: "ammo", label: "Ammunition" },
  { value: "explosive", label: "Explosive" },
  { value: "medical", label: "Medical" },
  { value: "repair", label: "Repair" },
  { value: "fuel", label: "Fuel / Power Cell" },
  { value: "utility", label: "Utility" }
]);
function eE(i) {
  return i === "consumable" ? Zv : Xv;
}
class Tc extends qi {
  async _prepareContext(e) {
    var s;
    const t = await super._prepareContext(e), n = this._getCanonicalItemType(), a = this.item.system ?? {}, r = eE(n);
    return t.system = {
      ...a,
      quantity: Math.max(0, Math.trunc(Number(a.quantity ?? 1) || 0)),
      rating: Math.max(0, Math.trunc(Number(a.rating ?? 0) || 0)),
      category: String(a.category ?? "").trim(),
      tags: Array.isArray(a.tags) ? a.tags.map((o) => String(o ?? "").trim()).filter(Boolean) : []
    }, t.gearEditor = {
      categories: r.map((o) => ({ ...o }))
    }, t.tagsText = t.system.tags.join(", "), t.itemSheet = {
      ...t.itemSheet ?? {},
      summaryChips: [
        { label: "Qty", value: String(t.system.quantity) },
        { label: "Rating", value: String(t.system.rating) },
        {
          label: "Category",
          value: ((s = r.find((o) => o.value === t.system.category)) == null ? void 0 : s.label) ?? "Uncategorized"
        }
      ]
    }, t.layout = await oa.get(n === "consumable" ? "consumable" : "gear"), t;
  }
}
// One sheet class intentionally backs both gear and consumables so quantity,
// rating, and reference editing never drift into parallel implementations.
z(Tc, "LAYOUT_ID", null), z(Tc, "PARTS", {
  sheet: {
    template: `${se}/v2/item/gear.hbs`,
    scrollable: [".sheet-body"]
  }
});
class kc extends qi {
  async _prepareContext(e) {
    var s, o;
    const t = await super._prepareContext(e), n = Ii(this.item.system ?? {}), a = sp(), r = Array.isArray((s = t.ENUMS) == null ? void 0 : s.skills) ? t.ENUMS.skills.map((l) => ({
      value: String((l == null ? void 0 : l.value) ?? "").trim(),
      label: String((l == null ? void 0 : l.label) ?? (l == null ? void 0 : l.value) ?? "").trim()
    })).filter((l) => l.value) : [];
    return t.system = {
      ...n,
      effects: (Array.isArray(n.effects) ? n.effects : []).map((l) => ({
        ...l,
        showSkillPicker: rp(l) || Array.isArray(l.skillKeys) && l.skillKeys.length > 0,
        isEdgeEvent: l.type === "edgeEvent"
      }))
    }, t.traitEditor = {
      ...a,
      skills: r
    }, t.itemSheet = {
      ...t.itemSheet ?? {},
      sheetClass: "mwd-item-sheet--quality",
      summaryChips: [
        { label: "Category", value: ds(n.category) },
        { label: "Tier", value: ms(n.tier) },
        { label: "Activation", value: String(n.activation ?? "passive").trim() || "Passive" },
        { label: "Effects", value: String(((o = n.effects) == null ? void 0 : o.length) ?? 0) }
      ]
    }, t.tagsText = Array.isArray(n.tags) ? n.tags.join(", ") : "", t;
  }
  _onRender(e, t) {
    var r, s;
    (r = super._onRender) == null || r.call(this, e, t);
    const n = (s = this._getRootElement) == null ? void 0 : s.call(this);
    if (!n) return;
    const a = (o) => {
      var l;
      return (l = this._captureScrollPositions) == null || l.call(this), o();
    };
    n.querySelectorAll(".mwd-quality-prereq-add").forEach((o) => {
      o.addEventListener("click", (l) => {
        l.preventDefault(), l.stopPropagation(), a(() => {
          var c, u;
          return (u = (c = this.item).createQualityPrerequisite) == null ? void 0 : u.call(c);
        });
      });
    }), n.querySelectorAll(".mwd-quality-prereq-delete").forEach((o) => {
      o.addEventListener("click", (l) => {
        l.preventDefault(), l.stopPropagation(), a(() => {
          var c, u;
          return (u = (c = this.item).deleteQualityPrerequisite) == null ? void 0 : u.call(c, o.dataset.prereqId);
        });
      });
    }), n.querySelectorAll(".mwd-quality-prereq-field").forEach((o) => {
      o.addEventListener("change", (l) => {
        l.preventDefault(), l.stopPropagation(), a(() => {
          var c, u;
          return (u = (c = this.item).updateQualityPrerequisite) == null ? void 0 : u.call(
            c,
            o.dataset.prereqId,
            o.dataset.field,
            o.value
          );
        });
      });
    }), n.querySelectorAll(".mwd-quality-effect-add").forEach((o) => {
      o.addEventListener("click", (l) => {
        l.preventDefault(), l.stopPropagation(), a(() => {
          var c, u;
          return (u = (c = this.item).createQualityEffect) == null ? void 0 : u.call(c);
        });
      });
    }), n.querySelectorAll(".mwd-quality-effect-delete").forEach((o) => {
      o.addEventListener("click", (l) => {
        l.preventDefault(), l.stopPropagation(), a(() => {
          var c, u;
          return (u = (c = this.item).deleteQualityEffect) == null ? void 0 : u.call(c, o.dataset.effectId);
        });
      });
    }), n.querySelectorAll(".mwd-quality-effect-field").forEach((o) => {
      o.addEventListener("change", (l) => {
        l.preventDefault(), l.stopPropagation(), a(() => {
          var c, u;
          return (u = (c = this.item).updateQualityEffect) == null ? void 0 : u.call(
            c,
            o.dataset.effectId,
            o.dataset.field,
            o instanceof HTMLSelectElement && o.multiple ? Array.from(o.selectedOptions).map((d) => d.value) : o.value
          );
        });
      });
    }), n.querySelectorAll(".mwd-quality-effect-skill-toggle").forEach((o) => {
      o.addEventListener("change", (l) => {
        l.preventDefault(), l.stopPropagation();
        const c = o.dataset.effectId, u = Array.from(n.querySelectorAll(`.mwd-quality-effect-skill-toggle[data-effect-id="${c}"]`)).filter((d) => d instanceof HTMLInputElement && d.checked).map((d) => d.value);
        a(() => {
          var d, m;
          return (m = (d = this.item).updateQualityEffect) == null ? void 0 : m.call(
            d,
            c,
            o.dataset.field,
            u
          );
        });
      });
    }), n.querySelectorAll(".mwd-quality-effect-condition-add").forEach((o) => {
      o.addEventListener("click", (l) => {
        l.preventDefault(), l.stopPropagation(), a(() => {
          var c, u;
          return (u = (c = this.item).createQualityEffectCondition) == null ? void 0 : u.call(c, o.dataset.effectId);
        });
      });
    }), n.querySelectorAll(".mwd-quality-effect-condition-delete").forEach((o) => {
      o.addEventListener("click", (l) => {
        l.preventDefault(), l.stopPropagation(), a(() => {
          var c, u;
          return (u = (c = this.item).deleteQualityEffectCondition) == null ? void 0 : u.call(c, o.dataset.effectId, o.dataset.conditionId);
        });
      });
    }), n.querySelectorAll(".mwd-quality-effect-condition-field").forEach((o) => {
      o.addEventListener("change", (l) => {
        l.preventDefault(), l.stopPropagation(), a(() => {
          var c, u;
          return (u = (c = this.item).updateQualityEffectCondition) == null ? void 0 : u.call(
            c,
            o.dataset.effectId,
            o.dataset.conditionId,
            o.dataset.field,
            o.value
          );
        });
      });
    });
  }
}
z(kc, "LAYOUT_ID", "quality"), z(kc, "PARTS", {
  sheet: {
    template: `${se}/v2/item/quality.hbs`,
    scrollable: [".sheet-body"]
  }
});
class Mc extends qi {
}
z(Mc, "LAYOUT_ID", "asset-module"), z(Mc, "PARTS", {
  sheet: {
    template: `${se}/v2/item/assetModule.hbs`,
    scrollable: [".sheet-body"]
  }
});
class Cc extends qi {
  static get DEFAULT_OPTIONS() {
    return foundry.utils.mergeObject(super.DEFAULT_OPTIONS, {
      position: {
        width: 640,
        height: 620
      },
      window: {
        ...super.DEFAULT_OPTIONS.window,
        minWidth: 560,
        minHeight: 480
      }
    }, { inplace: !1 });
  }
  _getTabs() {
    return {
      main: { id: "main", group: "primary", label: "Details" }
    };
  }
  _getSummaryChips() {
    const e = Dr(this.item.system ?? {}), t = En(e.catalogId), a = Mo(t, e.selectedGrants, { legacySelectedSkill: e.selectedSkill }).map((s) => s.choice).filter(Boolean).map((s) => Pr(s, { includeBonusText: !0 })).join(", "), r = this.item.actor ? Nn(this.item.actor).stateByItemId.get(this.item.id) ?? null : null;
    return [
      { label: "Slot", value: za(e.moduleType) },
      { label: "Module", value: (t == null ? void 0 : t.label) ?? "Unlinked" },
      { label: "Bonuses", value: a || "Pending choice" },
      r ? { label: "Status", value: r.isActive ? "Active" : "Inactive" } : null
    ].filter(Boolean);
  }
  async _prepareContext(e) {
    const t = await super._prepareContext(e), n = Dr(this.item.system ?? {}), a = n.moduleType, r = En(n.catalogId), s = a ? Nu(a) : [], o = wg(r, n.selectedGrants, { legacySelectedSkill: n.selectedSkill }), l = this.item.actor ? Nn(this.item.actor).stateByItemId.get(this.item.id) ?? null : null;
    return t.lifeModuleEditor = {
      moduleType: a,
      moduleTypeLabel: za(a),
      moduleTypes: hg().map((c) => ({
        ...c,
        selected: c.value === a
      })),
      availableEntries: s.map((c) => ({
        id: c.id,
        label: c.label,
        selected: c.id === n.catalogId
      })),
      hasAvailableEntries: s.length > 0,
      selectedEntry: r,
      selectedGrants: n.selectedGrants,
      grantFields: o,
      requiresAnyLabels: ((r == null ? void 0 : r.requiresAny) ?? []).map((c) => {
        var u;
        return ((u = En(c)) == null ? void 0 : u.label) ?? c;
      }),
      excludesAnyLabels: ((r == null ? void 0 : r.excludesAny) ?? []).map((c) => {
        var u;
        return ((u = En(c)) == null ? void 0 : u.label) ?? c;
      }),
      actorState: l,
      warningLabels: [...(l == null ? void 0 : l.warningLabels) ?? []],
      isOwned: !!this.item.actor,
      statusLabel: l ? l.isActive ? "Active" : "Inactive" : r ? "Configured" : "Unlinked",
      statusReason: (l == null ? void 0 : l.inactiveReason) ?? ""
    }, t;
  }
}
z(Cc, "LAYOUT_ID", "life-module"), z(Cc, "PARTS", {
  sheet: {
    template: `${se}/v2/item/lifeModule.hbs`,
    scrollable: [".sheet-body"]
  }
});
class vc extends qi {
}
z(vc, "LAYOUT_ID", "skill"), z(vc, "PARTS", {
  sheet: {
    template: `${se}/v2/item/skill.hbs`,
    scrollable: [".sheet-body"]
  }
});
const tE = Object.freeze([
  "firearms",
  "projectileWeapons",
  "heavyWeapons",
  "meleeCombat"
]), iE = Object.freeze([
  { value: "melee", label: "Melee" },
  { value: "ranged", label: "Ranged" }
]), Lm = Object.freeze([
  { value: "quantity", label: "Quantity" },
  { value: "durability.current", label: "Durability" },
  { value: "", label: "Custom Path" }
]), nE = "consumable";
function aE(i) {
  return String((i == null ? void 0 : i.canonicalType) ?? (i == null ? void 0 : i.type) ?? "item").trim().replace(/([a-z])([A-Z])/g, "$1 $2").replace(/^./, (t) => t.toUpperCase());
}
function Yh(i) {
  return String((i == null ? void 0 : i.canonicalType) ?? (i == null ? void 0 : i.type) ?? "").trim() === nE;
}
function rE(i, e = "") {
  var n;
  const t = String(e ?? "").trim();
  return Array.from(((n = i == null ? void 0 : i.actor) == null ? void 0 : n.items) ?? []).filter((a) => {
    const r = String((a == null ? void 0 : a.id) ?? "").trim();
    return !r || r === (i == null ? void 0 : i.id) ? !1 : r === t || Yh(a);
  }).sort((a, r) => String((a == null ? void 0 : a.name) ?? "").localeCompare(String((r == null ? void 0 : r.name) ?? ""))).map((a) => ({
    value: a.id,
    label: `${a.name || "Unnamed Item"} (${aE(a)})`
  }));
}
function Ec(i, e, t) {
  const n = String(e ?? "").trim();
  return !n || i.some((a) => a.value === n) ? i : i.concat({ value: n, label: t(n) });
}
function sE(i, e) {
  var d, m, f, p, g, h, y;
  const t = $i(e), n = rE(i, (d = t.link) == null ? void 0 : d.itemId), a = bb({
    source: t,
    actor: (i == null ? void 0 : i.actor) ?? null
  }), r = ((g = (f = (m = i == null ? void 0 : i.actor) == null ? void 0 : m.items) == null ? void 0 : f.get) == null ? void 0 : g.call(f, ((p = t.link) == null ? void 0 : p.itemId) ?? "")) ?? null, s = Ec(
    [...Lm],
    (h = t.link) == null ? void 0 : h.itemPath,
    (b) => `Custom (${b})`
  ), o = new Set(Lm.map((b) => String(b.value ?? "").trim())), l = String(((y = t.link) == null ? void 0 : y.itemPath) ?? "").trim(), c = !!(i != null && i.actor);
  let u = "";
  return t.kind === "itemRef" && (c ? n.length ? r ? Yh(r) ? l ? u = a.isTracked ? `Linked to ${r.name} | Available ${Number(a.current ?? 0)}` : `Linked to ${r.name} | Path not resolving to a tracked value yet.` : u = `Linked to ${r.name}. Pick which field should be consumed.` : u = `Linked to ${r.name} | Legacy non-consumable source. Repoint this to a Consumable item when convenient.` : u = "Pick an owned Consumable item to consume from." : u = "Add an owned Consumable item to the actor, then link this weapon to it." : u = "Embed this weapon in an actor to link it to owned inventory."), {
    ...t,
    resolvedState: a,
    ui: {
      ownedItemOptions: n,
      itemPathOptions: s,
      hasOwnedActor: c,
      linkedItemName: (r == null ? void 0 : r.name) ?? "",
      showCustomItemPath: t.kind === "itemRef" && !o.has(l),
      preview: u
    }
  };
}
class $r extends qi {
  /** @override */
  static get DEFAULT_OPTIONS() {
    return foundry.utils.mergeObject(super.DEFAULT_OPTIONS, {
      actions: {
        ...super.DEFAULT_OPTIONS.actions,
        weaponSkillChange: $r._onWeaponSkillChange,
        weaponCategoryChange: $r._onWeaponCategoryChange
      }
    }, { inplace: !1 });
  }
  _getTabs() {
    return {
      ...super._getTabs(),
      modifiers: { id: "modifiers", group: "primary", label: "Roll Modifiers" },
      effects: { id: "effects", group: "primary", label: "Active Effects" }
    };
  }
  /**
   * Prepare context data, adding weapon-specific enums.
   * @param {object} options - Rendering options
   * @returns {Promise<object>} The prepared context
   * @override
   */
  async _prepareContext(e) {
    var u, d, m, f, p, g, h, y, b, S, w, T;
    const t = await super._prepareContext(e), n = this._getCanonicalItemType();
    t.ENUMS = {
      ...t.ENUMS ?? {},
      defenses: it.getDefenses()
    };
    const a = Array.isArray((u = t.ENUMS) == null ? void 0 : u.skills) ? t.ENUMS.skills : [], r = (d = this.item.system) == null ? void 0 : d.skill, s = (m = this.item.system) == null ? void 0 : m.damageType, o = n === "personalWeapon" ? Ec(
      a.filter((k) => tE.includes(k.value)),
      r,
      (k) => {
        var C;
        return ((C = a.find((I) => I.value === k)) == null ? void 0 : C.label) ?? k;
      }
    ) : a, l = String(((f = this.item.system) == null ? void 0 : f.weaponCategory) ?? ((p = this.item.system) == null ? void 0 : p.category) ?? "ranged").trim().toLowerCase() === "melee" ? "melee" : "ranged", c = ((g = q == null ? void 0 : q.mwd) == null ? void 0 : g.hardpointSize) ?? ((y = (h = q == null ? void 0 : q.mwd) == null ? void 0 : h.hardpoint) == null ? void 0 : y.size) ?? {};
    return t.weaponProfile = ((S = (b = this.item).getCombatProfile) == null ? void 0 : S.call(b)) ?? null, t.weaponEditor = {
      skills: o,
      categories: n === "mechWeapon" ? [...iE] : [
        { value: "melee", label: "Melee" },
        { value: "ranged", label: "Ranged" },
        { value: "thrown", label: "Thrown" },
        { value: "other", label: "Other" }
      ],
      damageTypes: Ec(
        [...Os],
        s,
        (k) => Pi(k)
      ),
      ranges: yi.RANGE_ORDER.map((k) => ({
        value: k,
        label: n === "personalWeapon" ? Gs(k) : k.charAt(0).toUpperCase() + k.slice(1)
      })),
      rangeBandLabels: Object.fromEntries(yi.RANGE_ORDER.map((k) => [
        k,
        n === "personalWeapon" ? Gs(k) : k.charAt(0).toUpperCase() + k.slice(1)
      ])),
      weaponCapabilityOptions: Ey,
      payloadCapabilityOptions: Iy,
      ammoDamageTypes: [{ value: "", label: "Use Weapon Default" }, ...Os],
      payloadTemplateShapes: of,
      payloadTemplatePlacements: lf,
      areaEffectKinds: [
        { value: ai.discrete, label: "Discrete" },
        { value: ai.persistent, label: "Persistent Hazard" }
      ],
      exposureTiers: [
        { value: pe.minor, label: "Minor" },
        { value: pe.major, label: "Major" },
        { value: pe.full, label: "Full" }
      ],
      resolverKeys: [
        { value: "standard", label: "Standard" },
        { value: "template", label: "Template" }
      ],
      payloadSourceKinds: [
        { value: "untracked", label: "Untracked" },
        { value: "internal", label: "Internal" },
        { value: "actorResource", label: "Actor Resource" },
        { value: "itemRef", label: "Linked Item" }
      ],
      hardpointSizes: Object.entries(c).map(([k, C]) => ({ value: k, label: C })),
      consumptionSources: Array.isArray((w = this.item.system) == null ? void 0 : w.consumptionSources) ? this.item.system.consumptionSources.map((k) => sE(this.item, k)) : []
    }, t.itemSheet = {
      ...t.itemSheet ?? {},
      isCompactWeaponSheet: !0,
      weaponSheetVariant: n === "mechWeapon" ? "mech" : "personal",
      mechDerivedSkillLabel: l === "melee" ? "Melee Combat" : "Gunnery",
      mechIsMelee: n === "mechWeapon" && l === "melee"
    }, t.itemSheet.stateChips = (t.itemSheet.stateChips ?? []).filter(
      (k) => !["ownership", "equipment", "role"].includes(k.kind)
    ), t.itemSheet.currentPayloadLabel = ((T = t.weaponProfile) == null ? void 0 : T.payloadLabel) ?? "", t;
  }
  /**
   * Handle changing the weapon skill, which updates the defense attribute.
   * @param {Event} event - The triggering event
   * @param {HTMLElement} target - The changed select element
   * @static
   * @async
   */
  _onRender(e, t) {
    var n;
    (n = super._onRender) == null || n.call(this, e, t), this._bindPayloadEditorControls();
  }
  _bindPayloadEditorControls() {
    var n;
    const e = (n = this._getRootElement) == null ? void 0 : n.call(this);
    if (!e) return;
    const t = (a) => {
      var r;
      return (r = this._captureScrollPositions) == null || r.call(this), a();
    };
    e.querySelectorAll(".mwd-payload-add").forEach((a) => {
      a.addEventListener("click", (r) => {
        r.preventDefault(), t(() => {
          var s, o;
          return (o = (s = this.item).createPayload) == null ? void 0 : o.call(s);
        });
      });
    }), e.querySelectorAll(".mwd-payload-delete").forEach((a) => {
      a.addEventListener("click", (r) => {
        r.preventDefault(), t(() => {
          var s, o;
          return (o = (s = this.item).deletePayload) == null ? void 0 : o.call(s, a.dataset.payloadId);
        });
      });
    }), e.querySelectorAll(".mwd-payload-field").forEach((a) => {
      a.addEventListener("change", (r) => {
        r.preventDefault(), t(() => {
          var s, o;
          return (o = (s = this.item).updatePayloadField) == null ? void 0 : o.call(
            s,
            a.dataset.payloadId,
            a.dataset.field,
            a.value
          );
        });
      });
    }), e.querySelectorAll(".mwd-source-add").forEach((a) => {
      a.addEventListener("click", (r) => {
        r.preventDefault(), t(() => {
          var s, o;
          return (o = (s = this.item).createConsumptionSource) == null ? void 0 : o.call(s);
        });
      });
    }), e.querySelectorAll(".mwd-source-delete").forEach((a) => {
      a.addEventListener("click", (r) => {
        r.preventDefault(), t(() => {
          var s, o;
          return (o = (s = this.item).deleteConsumptionSource) == null ? void 0 : o.call(s, a.dataset.sourceId);
        });
      });
    }), e.querySelectorAll(".mwd-source-field").forEach((a) => {
      a.addEventListener("change", (r) => {
        r.preventDefault(), t(() => {
          var s, o;
          return (o = (s = this.item).updateConsumptionSourceField) == null ? void 0 : o.call(
            s,
            a.dataset.sourceId,
            a.dataset.field,
            a.value
          );
        });
      });
    }), e.querySelectorAll(".mwd-capability-picker").forEach((a) => {
      a.addEventListener("change", (r) => {
        r.preventDefault();
        const s = String(a.value ?? "").trim();
        if (!s) return;
        const o = String(a.dataset.values ?? "").split(",").map((d) => d.trim()).filter(Boolean), l = Array.from(/* @__PURE__ */ new Set([...o, s]));
        a.value = "";
        const c = String(a.dataset.payloadId ?? "").trim(), u = String(a.dataset.field ?? "").trim();
        if (u) {
          if (c) {
            t(() => {
              var d, m;
              return (m = (d = this.item).updatePayloadField) == null ? void 0 : m.call(d, c, u, l.join(", "));
            });
            return;
          }
          t(() => this.item.update({ [u]: l }));
        }
      });
    });
  }
  static async _onWeaponSkillChange(e, t) {
    var r, s;
    const n = t.value, a = (s = (r = game.system.mwd.skills) == null ? void 0 : r.get) == null ? void 0 : s.call(r, n);
    await this._syncNamedField(t, {
      ...a != null && a.defense ? { "system.defense": a.defense } : {}
    });
  }
  static async _onWeaponCategoryChange(e, t) {
    var r, s, o;
    const n = String((t == null ? void 0 : t.value) ?? "").trim().toLowerCase() === "melee" ? "melee" : "ranged";
    if ((((r = this._getCanonicalItemType) == null ? void 0 : r.call(this)) ?? ((s = this.item) == null ? void 0 : s.canonicalType) ?? ((o = this.item) == null ? void 0 : o.type)) === "mechWeapon") {
      const l = {
        "system.category": n,
        "system.weaponCategory": n,
        "system.skill": n === "melee" ? "meleeCombat" : "gunnery"
      };
      n === "melee" && (l["system.range.max"] = "close", l["system.range.close"] = 0, l["system.range.near"] = 0, l["system.range.far"] = 0, l["system.range.extreme"] = 0), await this._syncNamedField(t, l);
      return;
    }
    await this._syncNamedField(t);
  }
}
const Ea = class Ea extends $r {
  static get DEFAULT_OPTIONS() {
    return foundry.utils.mergeObject(super.DEFAULT_OPTIONS, {
      position: {
        width: 680,
        height: 720
      },
      window: {
        ...super.DEFAULT_OPTIONS.window,
        minWidth: 680,
        minHeight: 480,
        maxWidth: 960
      },
      actions: {
        ...super.DEFAULT_OPTIONS.actions,
        attackWeapon: Ea._onAttackWeapon,
        reloadWeaponPayload: Ea._onReloadWeaponPayload
      }
    }, { inplace: !1 });
  }
  async _prepareContext(e) {
    var s, o, l;
    const t = await super._prepareContext(e), n = this.item.actor ?? null, a = t.weaponProfile ?? null, r = !!(n && typeof n.isCharacterLike == "function" && n.isCharacterLike() && ((o = (s = this.item).isPersonalWeapon) != null && o.call(s)));
    return t.itemSheet = {
      ...t.itemSheet ?? {},
      canAttack: r,
      attackDisabled: !r || !((l = this.item.system) != null && l.equipped)
    }, t.itemSheet.summaryChips = this._getSummaryChips(a), t.itemSheet.reloadState = this._getReloadDisplayState(a), t;
  }
  _getReloadDisplayState(e = ((t) => ((n) => (n = (t = this.item).getCombatProfile) == null ? void 0 : n.call(t))())() ?? null) {
    var d, m, f, p, g;
    const a = ((m = (d = this.item).canReloadActivePayload) == null ? void 0 : m.call(d, { detailed: !0 })) ?? { canReload: !1, reason: "" }, r = !!((f = e == null ? void 0 : e.sourceState) != null && f.isTracked), s = String((e == null ? void 0 : e.payloadLabel) ?? (a == null ? void 0 : a.payloadLabel) ?? "").trim() || "Unloaded", o = Number(((p = e == null ? void 0 : e.sourceState) == null ? void 0 : p.current) ?? (a == null ? void 0 : a.current) ?? 0) || 0, l = Number(((g = e == null ? void 0 : e.sourceState) == null ? void 0 : g.max) ?? (a == null ? void 0 : a.max) ?? 0) || 0, c = r ? `${s} ${o}/${l}` : s, u = a.canReload ? "Click to reload" : String(a.reason ?? "").trim() || "Payload read-only";
    return {
      canReload: !!a.canReload,
      disabled: !a.canReload,
      value: c,
      hint: u,
      title: a.canReload ? `Reload ${s}` : u
    };
  }
  _getSummaryChips(e = ((t) => ((n) => (n = (t = this.item).getCombatProfile) == null ? void 0 : n.call(t))())() ?? null) {
    var r, s, o;
    if (!e) return [];
    const a = [
      { label: "Category", value: String(e.category ?? "").trim() || "Ranged" },
      { label: "Skill", value: ((r = e.skillDef) == null ? void 0 : r.label) ?? e.skill ?? "Firearms" },
      { label: "DV", value: String(Number(e.damage ?? 0)) },
      { label: "AP", value: String(Number(e.ap ?? 0)) },
      { label: "Type", value: Pi(e.damageType) || "Penetrating" },
      { label: "Range", value: String(((s = e.range) == null ? void 0 : s.max) ?? "near").trim() || "Near" }
    ];
    return String(e.category ?? "").trim().toLowerCase() !== "melee" && a.push((o = e == null ? void 0 : e.sourceState) != null && o.isTracked ? { label: "Payload", value: `${(e == null ? void 0 : e.payloadLabel) || "Unloaded"} (${Number(e.sourceState.current ?? 0)}/${Number(e.sourceState.max ?? 0)})` } : { label: "Payload", value: (e == null ? void 0 : e.payloadLabel) || "Unloaded" }), a;
  }
  static async _onAttackWeapon(e) {
    var n, a, r, s;
    (n = e == null ? void 0 : e.preventDefault) == null || n.call(e), (a = e == null ? void 0 : e.stopPropagation) == null || a.call(e), !(!(this.item.actor ?? null) || !((s = (r = this.item).isPersonalWeapon) != null && s.call(r))) && await Do({ weapon: this.item, event: e });
  }
  static async _onReloadWeaponPayload(e) {
    var n, a, r, s, o, l, c, u;
    if ((n = e == null ? void 0 : e.preventDefault) == null || n.call(e), (a = e == null ? void 0 : e.stopPropagation) == null || a.call(e), !((s = (r = this.item) == null ? void 0 : r.isPersonalWeapon) != null && s.call(r))) return;
    (o = this._captureScrollPositions) == null || o.call(this);
    const t = await ((c = (l = this.item).reloadActivePayload) == null ? void 0 : c.call(l));
    t != null && t.ok || (u = ui.notifications) == null || u.warn((t == null ? void 0 : t.reason) ?? "Unable to reload that weapon.");
  }
  _onRender(e, t) {
    var a, r;
    (a = super._onRender) == null || a.call(this, e, t), (r = this._getRootElement) == null || r.call(this);
  }
};
z(Ea, "LAYOUT_ID", "personal-weapon"), z(Ea, "PARTS", {
  sheet: {
    template: `${se}/v2/item/personal-weapon-root.hbs`,
    scrollable: [".sheet-body"]
  }
});
let Ic = Ea;
class Nc extends $r {
}
z(Nc, "LAYOUT_ID", "mech-weapon"), z(Nc, "PARTS", {
  sheet: {
    template: `${se}/v2/item/mech-weapon-root.hbs`,
    scrollable: [".sheet-body"]
  }
});
const oE = {
  penetrating: "Penetrating",
  concussive: "Concussive",
  energy: "Energy",
  thermal: "Thermal",
  electrical: "Electrical"
};
function _m(i) {
  const e = Number(i ?? 0) || 0;
  return e > 0 ? `+${e}` : `${e}`;
}
function lE({ defenseBonus: i = 0, mitigationByType: e = {} } = {}) {
  const t = [], n = Number(i ?? 0) || 0;
  n !== 0 && t.push(`Defense ${_m(n)}`);
  const a = Ki(e);
  for (const [r, s] of Object.entries(oE)) {
    const o = Number((a == null ? void 0 : a[r]) ?? 0) || 0;
    o !== 0 && t.push(`${s} ${_m(o)}`);
  }
  return t.join(" | ");
}
class Rc extends qi {
  static get DEFAULT_OPTIONS() {
    return foundry.utils.mergeObject(super.DEFAULT_OPTIONS, {
      position: {
        width: 960,
        height: 860
      },
      window: {
        ...super.DEFAULT_OPTIONS.window,
        minWidth: 920,
        minHeight: 760
      }
    }, { inplace: !1 });
  }
  async _prepareContext(e) {
    var l, c, u, d, m, f, p, g, h, y, b, S, w, T, k, C;
    const t = await super._prepareContext(e), n = this.item, a = n.actor ?? null, r = ((l = a == null ? void 0 : a.getPersonalCombatLoadout) == null ? void 0 : l.call(a)) ?? null, s = ((c = r == null ? void 0 : r.activeArmor) == null ? void 0 : c.id) ?? null, o = ((u = r == null ? void 0 : r.activeArmor) == null ? void 0 : u.id) === n.id ? r.activeArmor : (d = n.getArmorProfile) == null ? void 0 : d.call(n, { actor: a });
    return t.armorState = o, t.isActiveArmor = s === n.id, t.effectiveDurabilityCurrent = Number(
      ((m = o == null ? void 0 : o.durability) == null ? void 0 : m.current) ?? ((p = (f = n.system) == null ? void 0 : f.durability) == null ? void 0 : p.current) ?? ((h = (g = n.system) == null ? void 0 : g.durability) == null ? void 0 : h.max) ?? ((y = n.system) == null ? void 0 : y.rating) ?? 0
    ), t.effectiveArmorRating = Number(
      (o == null ? void 0 : o.currentArmorRating) ?? (o == null ? void 0 : o.ratingCurrent) ?? Math.min(
        Number(((b = n.system) == null ? void 0 : b.rating) ?? 0),
        Number(((w = (S = n.system) == null ? void 0 : S.durability) == null ? void 0 : w.current) ?? ((k = (T = n.system) == null ? void 0 : T.durability) == null ? void 0 : k.max) ?? ((C = n.system) == null ? void 0 : C.rating) ?? 0)
      )
    ), t.effectiveArmorResist = Number(
      (o == null ? void 0 : o.baseMitigation) ?? (o == null ? void 0 : o.baseResistance) ?? 0
    ), t.armorModifierSummary = this._getArmorModifierSummary(o), t.itemSheet = { ...t.itemSheet ?? {} }, t.itemSheet.summaryChips = this._getSummaryChips(o), t.armorEditor = {
      standardTraits: [...rb]
    }, t;
  }
  _getSummaryChips(e = null) {
    var r, s, o, l, c, u, d, m, f, p, g, h, y, b, S;
    const t = this.item.system ?? {}, n = [
      {
        label: "Rating",
        value: String(Number(
          (e == null ? void 0 : e.ratingCurrent) ?? (e == null ? void 0 : e.currentArmorRating) ?? Math.min(
            Number(t.rating ?? 0),
            Number(((r = t.durability) == null ? void 0 : r.current) ?? ((s = t.durability) == null ? void 0 : s.max) ?? t.rating ?? 0)
          )
        ))
      },
      { label: "Defense", value: String(Number(t.defenseBonus ?? 0)) },
      {
        label: "Durability",
        value: `${Number(((o = e == null ? void 0 : e.durability) == null ? void 0 : o.current) ?? ((l = t.durability) == null ? void 0 : l.current) ?? ((c = t.durability) == null ? void 0 : c.max) ?? 0)}/${Number(((u = e == null ? void 0 : e.durability) == null ? void 0 : u.max) ?? ((d = t.durability) == null ? void 0 : d.max) ?? t.rating ?? 0)}`
      },
      {
        label: "Resist",
        value: String(Number((e == null ? void 0 : e.baseMitigation) ?? (e == null ? void 0 : e.baseResistance) ?? 0))
      }
    ], a = Number(((f = (m = e == null ? void 0 : e.traitState) == null ? void 0 : m.reinforced) == null ? void 0 : f.max) ?? ((g = (p = t == null ? void 0 : t.traitState) == null ? void 0 : p.reinforced) == null ? void 0 : g.max) ?? 0);
    return a > 0 && n.push({
      label: "Reinforced",
      value: `${Number(((y = (h = e == null ? void 0 : e.traitState) == null ? void 0 : h.reinforced) == null ? void 0 : y.current) ?? ((S = (b = t == null ? void 0 : t.traitState) == null ? void 0 : b.reinforced) == null ? void 0 : S.current) ?? 0)}/${a}`
    }), n;
  }
  _getArmorModifierSummary(e = null) {
    const t = this.item.system ?? {};
    return lE({
      defenseBonus: (e == null ? void 0 : e.defenseBonus) ?? t.defenseBonus ?? 0,
      mitigationByType: (e == null ? void 0 : e.mitigationByType) ?? (e == null ? void 0 : e.typedMitigation) ?? t.mitigationByType ?? {}
    });
  }
  _onRender(e, t) {
    var a, r;
    (a = super._onRender) == null || a.call(this, e, t);
    const n = (r = this._getRootElement) == null ? void 0 : r.call(this);
    n && (n.querySelectorAll(".mwd-armor-standard-trait-add").forEach((s) => {
      s.addEventListener("click", (o) => {
        var l, c;
        o.preventDefault(), (c = (l = this.item).createArmorStandardTrait) == null || c.call(l);
      });
    }), n.querySelectorAll(".mwd-armor-standard-trait-delete").forEach((s) => {
      s.addEventListener("click", (o) => {
        var l, c;
        o.preventDefault(), (c = (l = this.item).deleteArmorStandardTrait) == null || c.call(l, s.dataset.traitId);
      });
    }), n.querySelectorAll(".mwd-armor-standard-trait-field").forEach((s) => {
      s.addEventListener("change", (o) => {
        var l, c;
        o.preventDefault(), (c = (l = this.item).updateArmorStandardTrait) == null || c.call(
          l,
          s.dataset.traitId,
          s.dataset.field,
          s.value
        );
      });
    }));
  }
}
z(Rc, "LAYOUT_ID", "armor"), z(Rc, "PARTS", {
  sheet: {
    template: `${se}/v2/item/armor-root.hbs`,
    scrollable: [".sheet-body"]
  }
});
function cE() {
  console.log(`${Le}Registering Item sheets (V2)`);
  const { Items: i } = foundry.documents.collections;
  i.registerSheet(M, wc, { types: ["contact"], makeDefault: !0, label: "Contact (V2)" }), i.registerSheet(M, Tc, { types: ["gear", "consumable"], makeDefault: !0, label: "Gear / Consumable (V2)" }), i.registerSheet(M, kc, { types: ["quality"], makeDefault: !0, label: "Quality (V2)" }), i.registerSheet(M, Mc, { types: ["assetModule"], makeDefault: !0, label: "Asset Module (V2)" }), i.registerSheet(M, Cc, { types: ["lifeModule"], makeDefault: !0, label: "Life Module (V2)" }), i.registerSheet(M, vc, { types: ["skill"], makeDefault: !0, label: "Skill (V2)" }), i.registerSheet(M, Ic, { types: ["personalWeapon", "weapon"], makeDefault: !0, label: "Personal Weapon (V2)" }), i.registerSheet(M, Nc, { types: ["mechWeapon"], makeDefault: !0, label: "Mech Weapon (V2)" }), i.registerSheet(M, Rc, { types: ["armor"], makeDefault: !0, label: "Armor (V2)" });
}
const xm = [
  // UI (CSB render entry point + node types)
  `systems/${M}/templates/v2/ui/layout-root.hbs`,
  `systems/${M}/templates/v2/ui/nodes/hexabox.hbs`,
  `systems/${M}/templates/v2/ui/nodes/stack.hbs`,
  `systems/${M}/templates/v2/ui/nodes/panel.hbs`,
  `systems/${M}/templates/v2/ui/nodes/include.hbs`,
  `systems/${M}/templates/v2/ui/nodes/tabs.hbs`,
  `systems/${M}/templates/v2/ui/nodes/accordion.hbs`,
  `systems/${M}/templates/v2/ui/nodes/unknown.hbs`,
  `systems/${M}/templates/common/view-mode.hbs`,
  `systems/${M}/templates/common/label.hbs`,
  `systems/${M}/templates/common/enum-value-label.hbs`,
  `systems/${M}/templates/common/damage-code.hbs`,
  `systems/${M}/templates/common/damage-armor.hbs`,
  `systems/${M}/templates/v2/ui/mod-stepper.hbs`,
  `systems/${M}/templates/v2/ui/condition-monitors.hbs`,
  `systems/${M}/templates/v2/roll/_mwd-roll-card.hbs`,
  `systems/${M}/templates/v2/roll/_mwd-damage-application-card.hbs`,
  `systems/${M}/templates/v2/roll/_mwd-hazard-card.hbs`,
  `systems/${M}/templates/v2/components/checkbox.hbs`,
  `systems/${M}/templates/v2/components/radio.hbs`,
  // Character UI
  `systems/${M}/templates/v2/ui/character/attributes.hbs`,
  `systems/${M}/templates/v2/ui/character/combat-actions.hbs`,
  `systems/${M}/templates/v2/ui/character/skills-column.hbs`,
  `systems/${M}/templates/v2/ui/character/skill-row.hbs`,
  `systems/${M}/templates/v2/ui/character/edge-console.hbs`,
  `systems/${M}/templates/v2/ui/character/burn-monitor.hbs`,
  `systems/${M}/templates/v2/ui/character/status-dashboard.hbs`,
  `systems/${M}/templates/v2/ui/character/inventory-section.hbs`,
  `systems/${M}/templates/v2/ui/character/inventory-record.hbs`,
  `systems/${M}/templates/v2/ui/character/bio-identity.hbs`,
  `systems/${M}/templates/v2/ui/character/bio-history.hbs`,
  `systems/${M}/templates/v2/ui/vehicle/summary-bar.hbs`,
  `systems/${M}/templates/v2/ui/vehicle/attributes-grid.hbs`,
  `systems/${M}/templates/v2/ui/vehicle/movement-grid.hbs`,
  `systems/${M}/templates/v2/ui/vehicle/degradation-grid.hbs`,
  `systems/${M}/templates/v2/ui/vehicle/active-crits.hbs`,
  `systems/${M}/templates/v2/ui/vehicle/record-section.hbs`,
  `systems/${M}/templates/v2/ui/vehicle/ew-panel.hbs`,
  `systems/${M}/templates/v2/ui/battlemech/chassis-fields.hbs`,
  `systems/${M}/templates/v2/ui/battlemech/heat-track.hbs`,
  `systems/${M}/templates/v2/ui/battlemech/quick-actions.hbs`,
  `systems/${M}/templates/v2/ui/battlemech/weapon-groups.hbs`,
  `systems/${M}/templates/v2/ui/battlemech/hardpoints.hbs`,
  `systems/${M}/templates/v2/ui/battlemech/loadout-hardpoints.hbs`,
  `systems/${M}/templates/v2/ui/battlemech/ew-status.hbs`,
  `systems/${M}/templates/v2/ui/vehicle/pilot-panel.hbs`,
  `systems/${M}/templates/v2/ui/character/assigned-mech.hbs`,
  // Sheet wrapper
  `systems/${M}/templates/v2/actor/_sheet-root.hbs`,
  `systems/${M}/templates/v2/actor/npc-sheet.hbs`,
  `systems/${M}/templates/v2/actor/vehicle-sheet.hbs`,
  `systems/${M}/templates/v2/actor/battlemech-sheet.hbs`,
  // Placeholders
  `systems/${M}/templates/v2/ui/placeholders/assigned-systems.hbs`,
  `systems/${M}/templates/v2/ui/placeholders/inventory-gear.hbs`,
  `systems/${M}/templates/v2/ui/placeholders/inventory-consumables.hbs`,
  `systems/${M}/templates/v2/ui/placeholders/inventory-armor.hbs`,
  `systems/${M}/templates/v2/ui/placeholders/inventory-weapons.hbs`,
  `systems/${M}/templates/v2/ui/placeholders/bio-description.hbs`,
  `systems/${M}/templates/v2/ui/placeholders/bio-traits.hbs`,
  `systems/${M}/templates/v2/ui/placeholders/bio-life-modules.hbs`,
  `systems/${M}/templates/v2/ui/actor/field-grid.hbs`,
  `systems/${M}/templates/v2/ui/actor/owned-item-list.hbs`,
  `systems/${M}/templates/v2/ui/actor/action-buttons.hbs`,
  `systems/${M}/templates/v2/ui/actor/notes-editor.hbs`,
  `systems/${M}/templates/v2/ui/actor/hardpoint-list.hbs`,
  `systems/${M}/templates/v2/ui/actor/weapon-group-list.hbs`,
  // V2 item partials
  `systems/${M}/templates/v2/item/_item-sheet-root.hbs`,
  `systems/${M}/templates/v2/item/contact.hbs`,
  `systems/${M}/templates/v2/item/gear.hbs`,
  `systems/${M}/templates/v2/item/consumable.hbs`,
  `systems/${M}/templates/v2/item/assetModule.hbs`,
  `systems/${M}/templates/v2/item/skill.hbs`,
  `systems/${M}/templates/v2/item/lifeModule.hbs`,
  `systems/${M}/templates/v2/item/quality.hbs`,
  `systems/${M}/templates/v2/item/personal-weapon-root.hbs`,
  `systems/${M}/templates/v2/item/mech-weapon-root.hbs`,
  `systems/${M}/templates/v2/item/armor-root.hbs`,
  `systems/${M}/templates/v2/item/parts/itemname.hbs`,
  `systems/${M}/templates/v2/item/parts/inactive.hbs`,
  `systems/${M}/templates/v2/item/parts/references.hbs`,
  `systems/${M}/templates/v2/item/parts/gear-main.hbs`,
  `systems/${M}/templates/v2/item/parts/consumable-main.hbs`,
  `systems/${M}/templates/v2/item/parts/asset-module-main.hbs`,
  `systems/${M}/templates/v2/item/parts/skill-main.hbs`,
  `systems/${M}/templates/v2/item/parts/life-module-main.hbs`,
  `systems/${M}/templates/v2/item/parts/quality-main.hbs`,
  `systems/${M}/templates/v2/item/parts/quality-limits.hbs`,
  `systems/${M}/templates/v2/item/parts/quality-prerequisites.hbs`,
  `systems/${M}/templates/v2/item/parts/quality-effects.hbs`,
  `systems/${M}/templates/v2/item/parts/modifier.hbs`,
  `systems/${M}/templates/v2/item/parts/modifiers.hbs`,
  `systems/${M}/templates/v2/item/parts/weapon-main.hbs`,
  `systems/${M}/templates/v2/item/parts/weapon-compact-main.hbs`,
  `systems/${M}/templates/v2/item/parts/weapon-summary-hero.hbs`,
  `systems/${M}/templates/v2/item/parts/weapon-combat-essentials.hbs`,
  `systems/${M}/templates/v2/item/parts/weapon-usage-bands.hbs`,
  `systems/${M}/templates/v2/item/parts/weapon-ammo-profiles.hbs`,
  `systems/${M}/templates/v2/item/parts/weapon-consumption-sources.hbs`,
  `systems/${M}/templates/v2/item/parts/weapon-notes-reference.hbs`,
  `systems/${M}/templates/v2/item/parts/armor-main.hbs`,
  `systems/${M}/templates/v2/item/parts/armor-summary-hero.hbs`,
  `systems/${M}/templates/v2/item/parts/armor-combat-essentials.hbs`,
  `systems/${M}/templates/v2/item/parts/armor-usage-protection.hbs`,
  `systems/${M}/templates/v2/item/parts/armor-resistance-modifiers.hbs`,
  `systems/${M}/templates/v2/item/parts/armor-identity-fields.hbs`,
  `systems/${M}/templates/v2/item/parts/armor-standard-traits.hbs`,
  `systems/${M}/templates/v2/item/parts/armor-notes-reference.hbs`,
  `systems/${M}/templates/v2/item/parts/item-effects.hbs`,
  // Actors
  `systems/${M}/templates/v2/actor/character-sheet.hbs`,
  `systems/${M}/templates/v2/actor/vehicle-sheet.hbs`,
  `systems/${M}/templates/v2/actor/battlemech-sheet.hbs`
];
function uE(i) {
  const e = String(i).replaceAll("\\", "/"), t = `systems/${M}/templates/`, n = e.indexOf(t);
  return `mwd.${(n >= 0 ? e.slice(n + t.length) : e).replace(/\.hbs$/i, "").split("/").filter(Boolean).map((o) => o.replace(/^_+/, "")).join(".")}`;
}
function dE() {
  var i, e;
  return ((e = (i = foundry == null ? void 0 : foundry.applications) == null ? void 0 : i.handlebars) == null ? void 0 : e.Handlebars) ?? Handlebars;
}
async function mE() {
  var e, t;
  const i = dE();
  try {
    const n = {};
    for (const r of xm)
      n[uE(r)] = r, n[r] = r;
    await foundry.applications.handlebars.loadTemplates(n);
    const a = "mwd.v2.ui.layout-root";
    if (!((e = Handlebars.partials) != null && e[a])) {
      const r = Object.keys(Handlebars.partials ?? {});
      throw console.error("Missing required partial:", a), console.error("Closest matches:", r.filter((s) => s.includes("layout-root"))), new Error(`Template preload failed: ${a} not registered`);
    }
    if (i !== Handlebars) {
      for (const [r, s] of Object.entries(i.partials ?? {}))
        if (!((t = Handlebars.partials) != null && t[r]))
          try {
            Handlebars.registerPartial(r, s);
          } catch {
          }
    }
    console.log(`${Le}preloadTemplatesV2 OK`, { loaded: xm.length });
  } catch (n) {
    throw console.error(`${Le}preloadTemplatesV2 FAILED`, n), n;
  }
}
function $m(i) {
  const e = Math.max(0, Number(i) || 0);
  return -Math.floor(e / 3);
}
function fE(i) {
  const e = Math.max(0, Number(i) || 0);
  return e === 0 ? 0 : Math.ceil(e / 4);
}
function pE(i = {}) {
  const e = i.physical ?? {}, t = i.fatigue ?? {}, n = i.armor ?? {}, a = Number(e.value) || 0, r = Number(t.value) || 0, s = Math.max(Number(n.value) || 0, Number(n.max) || 0);
  return {
    physical: { penalty: $m(a) },
    fatigue: { penalty: $m(r) },
    armor: { resistance: fE(s) }
  };
}
const ml = {
  penaltyPer3Damage: (i) => {
    const e = Math.max(0, Number(i) || 0);
    return -Math.floor(e / 3);
  },
  resistancePerQuarter: (i) => {
    const e = Math.max(0, Number(i) || 0);
    return e === 0 ? 0 : Math.ceil(e / 4);
  }
  // heatPenaltyCurve: ...
};
function gE(i, e, t, n) {
  const a = i.system ?? {}, r = `monitors.${e}`, s = Number(foundry.utils.getProperty(a, `${r}.max`)) || 0, o = Number(foundry.utils.getProperty(a, `${r}.value`)) || 0;
  switch (t) {
    case "value":
      return n;
    case "armorPersonalBase":
      return n;
    case "mechArmorBase":
      return Math.max(n, s, o);
    case "vehicleArmorBase":
      return Math.max(n, s, o);
    default:
      return n;
  }
}
function hE(i = {}) {
  return Object.entries(Ki(i)).filter(([, e]) => Number(e) > 0).map(([e, t]) => `${e} +${t}`).join(", ");
}
class yE extends Actor {
  /** @override */
  async _preCreate(e, t, n) {
    super._preCreate && await super._preCreate(e, t, n);
    const a = await Mg("Actor", (e == null ? void 0 : e.type) ?? this.type), r = {};
    a.system && Object.keys(a.system).length && (r.system = foundry.utils.mergeObject(
      foundry.utils.deepClone(a.system),
      foundry.utils.deepClone((e == null ? void 0 : e.system) ?? this.system ?? {}),
      { inplace: !1, recursive: !0, overwrite: !0 }
    )), a.prototypeToken && (r.prototypeToken = foundry.utils.mergeObject(
      foundry.utils.deepClone(a.prototypeToken),
      foundry.utils.deepClone((e == null ? void 0 : e.prototypeToken) ?? this.prototypeToken ?? {}),
      { inplace: !1, recursive: !0, overwrite: !0 }
    )), Object.keys(r).length && this.updateSource(r);
  }
  /* -------------------------------------------- */
  /* Base & Derived Data                           */
  /* -------------------------------------------- */
  /** @override */
  prepareBaseData() {
    var e, t;
    if (super.prepareBaseData(), this.isCharacterLike()) {
      const n = this.system ?? {};
      if (Fb(n), n.speed = Math.max(0, Math.trunc(Number(n.speed ?? 12) || 12)), (e = n.skills) != null && e.skills && typeof n.skills.skills == "object") {
        for (const [a, r] of Object.entries(n.skills.skills))
          (t = n.skills)[a] ?? (t[a] = r);
        delete n.skills.skills;
      }
    }
    this._prepareEdgePoolsBase();
  }
  /** @override */
  prepareDerivedData() {
    super.prepareDerivedData(), this._prepareEdgePoolsDerived(), this._prepareMonitors(), this._preparePersonalCombatDerived();
  }
  /**
   * Base-data prep for Edge pools:
   * - Ensure numeric rating/value where present
   * - Initialize value ONLY if missing/invalid (NOT if 0)
   * - Remove legacy keys (max)
   * - No clamping, no "start full" behavior
   */
  _prepareEdgePoolsBase() {
    var t, n;
    if (this.type !== "character") return;
    const e = (n = (t = this.system) == null ? void 0 : t.counters) == null ? void 0 : n.edgePools;
    if (!(!e || typeof e != "object"))
      for (const a of Object.values(e)) {
        if (!a || typeof a != "object") continue;
        a.rating = Math.max(0, Number(a.rating ?? 0));
        const r = Object.prototype.hasOwnProperty.call(a, "value"), s = Number(a.value);
        (!r || !Number.isFinite(s)) && (a.value = a.rating), "max" in a && delete a.max;
      }
  }
  /**
   * Derived-data prep for Edge pools:
   * - Computes cap/effectiveMax/effectiveValue
   * - Stores in a non-persisted cache on the actor instance
   * - No writes to system data (prevents UI snap-back / loops)
   */
  _prepareEdgePoolsDerived() {
    var n, a;
    this._mwdDerived ?? (this._mwdDerived = {}), this._mwdDerived.edgePools = null;
    const e = this.getEdgeCap(), t = this.type === "character" ? Nn(this).bonusByEdgePool ?? {} : {};
    if (this.type === "character" && this.hasEdgePools()) {
      const r = ((a = (n = this.system) == null ? void 0 : n.counters) == null ? void 0 : a.edgePools) ?? {}, s = {};
      for (const [o, l] of Object.entries(r)) {
        const c = Math.max(0, Number((l == null ? void 0 : l.rating) ?? 0)), u = Math.max(0, Number((l == null ? void 0 : l.value) ?? 0)), d = Math.max(0, Number((t == null ? void 0 : t[o]) ?? 0)), m = c + d, f = Math.min(m, e), p = Math.min(u, f);
        s[o] = {
          key: o,
          rating: c,
          ratingBonus: d,
          effectiveRating: m,
          value: u,
          cap: e,
          effectiveMax: f,
          effectiveValue: p,
          hasPools: !0,
          isEmpty: p <= 0,
          isCapped: m > e
        };
      }
      this._mwdDerived.edgePools = { cap: e, pools: s };
      return;
    }
    this._mwdDerived.edgePools = { cap: e, pools: {} };
  }
  /* -------------------------------------------- */
  /* Capabilities                                  */
  /* -------------------------------------------- */
  isCharacterLike() {
    return this.type === "character" || this.type === "npc";
  }
  hasSkills() {
    return this.type === "character" || this.type === "npc";
  }
  hasEdgePools() {
    var e, t;
    return this.type === "character" && !!((t = (e = this.system) == null ? void 0 : e.counters) != null && t.edgePools);
  }
  getAttributeValue(e) {
    var t, n, a;
    return Math.max(0, Number(((a = (n = (t = this.system) == null ? void 0 : t.attributes) == null ? void 0 : n[e]) == null ? void 0 : a.value) ?? 0));
  }
  getSkillRating(e) {
    var t, n, a;
    return Math.max(0, Number(((a = (n = (t = this.system) == null ? void 0 : t.skills) == null ? void 0 : n[e]) == null ? void 0 : a.rating) ?? 0));
  }
  getOwnedItem(e) {
    var t, n;
    return ((n = (t = this.items) == null ? void 0 : t.get) == null ? void 0 : n.call(t, e)) ?? null;
  }
  async setCheckbarValue(e, t) {
    return await this.update({ [e]: t });
  }
  getPersonalCombatLoadout({ refresh: e = !1 } = {}) {
    var n;
    if (!e) {
      const a = (n = this._mwdDerived) == null ? void 0 : n.personalCombat;
      if (a) return a;
    }
    const t = this._computePersonalCombatLoadout();
    return this._mwdDerived ?? (this._mwdDerived = {}), this._mwdDerived.personalCombat = t, t;
  }
  _computePersonalCombatLoadout() {
    const e = [], t = this.items.filter((f) => {
      var p;
      return ((p = f.isPersonalWeapon) == null ? void 0 : p.call(f)) ?? f.type === A.itemType.personalWeapon;
    }).map((f) => {
      var p;
      return ((p = f.getCombatProfile) == null ? void 0 : p.call(f)) ?? null;
    }).filter(Boolean), n = this.items.filter((f) => {
      var p;
      return ((p = f.isArmor) == null ? void 0 : p.call(f)) ?? f.type === A.itemType.armor;
    }).map((f) => {
      var p;
      return ((p = f.getArmorProfile) == null ? void 0 : p.call(f, { actor: this })) ?? null;
    }).filter(Boolean), a = t.filter((f) => f.equipped), r = n.filter((f) => f.equipped), s = a.filter((f) => f.isPrimary), o = r.filter((f) => f.isPrimary);
    let l = null, c = null, u = !1;
    s.length === 1 ? (c = s[0], l = c) : s.length > 1 ? (e.push("Multiple equipped primary weapons found; attack selection requires a chooser."), u = !0) : a.length === 1 ? l = a[0] : a.length > 1 ? u = !0 : l = yi.buildDefaultUnarmedProfile(this);
    let d = null, m = null;
    return o.length === 1 ? (d = o[0], m = this._buildActiveArmorState(d)) : o.length > 1 ? (e.push("Multiple equipped primary armor items found; using the first equipped armor."), m = r[0] ? this._buildActiveArmorState(r[0]) : null) : r.length === 1 ? m = this._buildActiveArmorState(r[0]) : r.length > 1 && (e.push("Multiple equipped armor items found without a single primary; using the first equipped armor."), m = this._buildActiveArmorState(r[0])), {
      weapons: t,
      equippedWeapons: a,
      primaryWeapon: c,
      defaultWeapon: l,
      weaponChoiceRequired: u,
      armor: n,
      equippedArmor: r,
      primaryArmor: d,
      activeArmor: m,
      warnings: e
    };
  }
  _buildActiveArmorState(e) {
    var l, c;
    if (!e) return null;
    const t = Math.max(0, Number(((l = e == null ? void 0 : e.durability) == null ? void 0 : l.max) ?? (e == null ? void 0 : e.rating) ?? 0)), n = Math.min(
      t,
      Math.max(0, Number(((c = e == null ? void 0 : e.durability) == null ? void 0 : c.current) ?? (e == null ? void 0 : e.remainingDurability) ?? (e == null ? void 0 : e.currentArmorRating) ?? t))
    ), a = Math.max(0, Number((e == null ? void 0 : e.rating) ?? 0)), r = Math.min(a, n), s = Ki(e == null ? void 0 : e.mitigationByType), o = Vc(r);
    return {
      ...e,
      armorId: e.id,
      remainingDurability: n,
      currentArmorRating: r,
      baseMitigation: o,
      baseResistance: o,
      mitigationByType: s,
      typedMitigation: s,
      ratingCurrent: r,
      isDestroyed: n <= 0,
      durability: {
        current: n,
        max: t
      }
    };
  }
  async setOwnedItemEquipped(e, t) {
    var a, r, s;
    const n = this.getOwnedItem(e);
    return !n || !((a = n.isPersonalWeapon) != null && a.call(n) || (r = n.isArmor) != null && r.call(n)) ? null : this.updateEmbeddedDocuments("Item", [{
      _id: n.id,
      "system.equipped": !!t,
      "system.isPrimary": t ? !!((s = n.system) != null && s.isPrimary) : !1
    }]);
  }
  async setOwnedItemPrimary(e, t) {
    var s, o, l, c;
    const n = this.getOwnedItem(e);
    if (!n || !((s = n.isPersonalWeapon) != null && s.call(n) || (o = n.isArmor) != null && o.call(n))) return null;
    const a = [], r = !!t;
    if (r)
      for (const u of this.items.filter((d) => d.type === n.type && d.id !== n.id))
        (l = u.system) != null && l.isPrimary && a.push({ _id: u.id, "system.isPrimary": !1 });
    return a.push({
      _id: n.id,
      "system.isPrimary": r,
      "system.equipped": r ? !0 : !!((c = n.system) != null && c.equipped)
    }), this.updateEmbeddedDocuments("Item", a);
  }
  /* -------------------------------------------- */
  /* Edge API (cap + pools w/ rating + current)    */
  /* -------------------------------------------- */
  getEdgeCap() {
    var e, t, n;
    return Math.max(0, Number(((n = (t = (e = this.system) == null ? void 0 : e.attributes) == null ? void 0 : t.edge) == null ? void 0 : n.value) ?? 0));
  }
  getEdgePoolRaw(e) {
    var t, n, a;
    return ((a = (n = (t = this.system) == null ? void 0 : t.counters) == null ? void 0 : n.edgePools) == null ? void 0 : a[e]) ?? null;
  }
  /**
   * Canonical pool accessor.
   * - Character: returns raw + effective values (effective is clamped by cap)
   * - NPC: no pools; Edge attribute acts as a single “pool” (effective max/value = cap)
   * - Vehicle/Mech: safe zeros
   */
  getEdgePool(e) {
    var d, m, f, p;
    const t = this.getEdgeCap();
    if (this.type === "npc" && !this.hasEdgePools()) {
      const g = t, h = t;
      return {
        key: e,
        value: h,
        rating: g,
        effectiveValue: h,
        effectiveMax: g,
        cap: t,
        hasPools: !1
      };
    }
    if (!this.hasEdgePools())
      return {
        key: e,
        value: 0,
        rating: 0,
        effectiveValue: 0,
        effectiveMax: 0,
        cap: t,
        hasPools: !1
      };
    const n = (f = (m = (d = this._mwdDerived) == null ? void 0 : d.edgePools) == null ? void 0 : m.pools) == null ? void 0 : f[e];
    if (n)
      return {
        key: n.key,
        value: n.value,
        rating: n.rating,
        ratingBonus: n.ratingBonus,
        effectiveRating: n.effectiveRating,
        effectiveValue: n.effectiveValue,
        effectiveMax: n.effectiveMax,
        cap: n.cap,
        hasPools: !0
      };
    const a = this.getEdgePoolRaw(e), r = Math.max(0, Number((a == null ? void 0 : a.rating) ?? 0)), s = Math.max(0, Number((a == null ? void 0 : a.value) ?? 0)), o = Math.max(0, Number(((p = Nn(this).bonusByEdgePool) == null ? void 0 : p[e]) ?? 0)), l = r + o, c = Math.min(l, t), u = Math.min(s, c);
    return {
      key: e,
      value: s,
      rating: r,
      ratingBonus: o,
      effectiveRating: l,
      effectiveValue: u,
      effectiveMax: c,
      cap: t,
      hasPools: !0
    };
  }
  getEdgePoolValue(e) {
    return this.getEdgePool(e).effectiveValue;
  }
  getEdgePoolMax(e) {
    return this.getEdgePool(e).effectiveMax;
  }
  /**
   * Set the CURRENT value for a pool (admin/adjustment or spend).
   * - Characters only (six pools).
   * - Clamps to [0, effectiveMax] where effectiveMax = min(rating, edgeCap).
   * - Does not modify rating.
   */
  async setEdgePoolValue(e, t) {
    var s;
    if (!this.hasEdgePools()) return;
    const n = Math.max(0, Number(((s = this.getEdgePool(e)) == null ? void 0 : s.effectiveMax) ?? 0)), a = Number(t ?? 0), r = Math.max(0, Math.min(a, n));
    return this.update({
      [`system.counters.edgePools.${e}.value`]: r
    });
  }
  /**
   * Convenience: adjust CURRENT value by delta (e.g., spend -1, award +1).
   */
  async adjustEdgePoolValue(e, t) {
    var r;
    if (!this.hasEdgePools()) return;
    const n = Math.max(0, Number(((r = this.getEdgePoolRaw(e)) == null ? void 0 : r.value) ?? 0)), a = Number(t ?? 0);
    return this.setEdgePoolValue(e, n + a);
  }
  /**
   * Set the RATING (advancement ceiling) for a pool.
   * Stores true rating (not cap-clamped), then clamps current value to new effective max.
   */
  async setEdgePoolRating(e, t) {
    var c, u;
    if (!this.hasEdgePools()) return;
    const n = this.getEdgeCap(), a = Math.max(0, Number(t ?? 0)), r = Math.max(0, Number(((c = Nn(this).bonusByEdgePool) == null ? void 0 : c[e]) ?? 0)), s = Math.min(a + r, n), o = Math.max(0, Number(((u = this.getEdgePoolRaw(e)) == null ? void 0 : u.value) ?? 0)), l = Math.min(o, s);
    return this.update({
      [`system.counters.edgePools.${e}.rating`]: a,
      [`system.counters.edgePools.${e}.value`]: l
    });
  }
  /**
   * Sheet-facing summary for rendering.
   * If `groups` is provided, returns grouped pool arrays.
   */
  getEdgePoolSummary({ groups: e } = {}) {
    var n, a, r, s;
    const t = this.getEdgeCap();
    if (this.hasEdgePools()) {
      const o = ((a = (n = this._mwdDerived) == null ? void 0 : n.edgePools) == null ? void 0 : a.pools) ?? {};
      if (e && typeof e == "object") {
        const c = Object.entries(e).map(([u, d]) => {
          const m = (d ?? []).map((f) => {
            const p = o[f] ?? this.getEdgePool(f);
            return {
              ...p,
              isEmpty: (p.effectiveValue ?? 0) <= 0,
              isCapped: (p.effectiveRating ?? p.rating ?? 0) > (p.cap ?? t)
            };
          });
          return { id: u, pools: m };
        });
        return { cap: t, hasPools: !0, groups: c, pools: [] };
      }
      const l = Object.keys(((s = (r = this.system) == null ? void 0 : r.counters) == null ? void 0 : s.edgePools) ?? {}).map((c) => {
        const u = o[c] ?? this.getEdgePool(c);
        return {
          ...u,
          isEmpty: (u.effectiveValue ?? 0) <= 0,
          isCapped: (u.effectiveRating ?? u.rating ?? 0) > (u.cap ?? t)
        };
      });
      return { cap: t, hasPools: !0, groups: [], pools: l };
    }
    return { cap: t, hasPools: !1, groups: [], pools: [] };
  }
  /**
   * Spend Edge from a pool (decrement current value).
   * - Characters only (six pools)
   * - Amount defaults to 1
   * - Safe no-op if pool missing
   */
  async spendEdge(e, t = 1, n = {}) {
    if (!this.hasEdgePools()) return;
    const a = Math.max(0, Number(t ?? 1));
    if (!a) return;
    let r = a;
    if (!n.skipTraitHooks) {
      const o = n.runtime ?? {}, l = {
        poolKey: e,
        amount: a,
        source: String(n.source ?? "").trim(),
        eventKey: String(n.eventKey ?? "").trim()
      }, c = hi({
        actor: this,
        phase: "onEdgeSpend",
        facts: Ll({ actor: this, packet: l, phase: "onEdgeSpend", runtime: o }),
        packet: l,
        options: { runtime: o, consumeUsage: !0 }
      });
      await rn({ actor: this, mutations: c.mutations, runtime: o }), r = Math.max(0, Number(c.packet.amount ?? a) || 0);
    }
    const s = r;
    if (s)
      return this.adjustEdgePoolValue(e, -s);
  }
  async gainEdge(e, t = 1, n = {}) {
    if (!this.hasEdgePools()) return;
    const a = Number(t ?? 0);
    if (!a) return;
    let r = a;
    if (!n.skipTraitHooks) {
      const s = n.runtime ?? {}, o = {
        poolKey: e,
        amount: a,
        source: String(n.source ?? "").trim(),
        eventKey: String(n.eventKey ?? "").trim()
      }, l = hi({
        actor: this,
        phase: "onEdgeGain",
        facts: Ll({ actor: this, packet: o, phase: "onEdgeGain", runtime: s }),
        packet: o,
        options: { runtime: s, consumeUsage: !0 }
      });
      await rn({ actor: this, mutations: l.mutations, runtime: s }), r = Number(l.packet.amount ?? a) || 0;
    }
    return this.adjustEdgePoolValue(e, r);
  }
  /* -------------------------------------------- */
  /* Document Lifecycle                            */
  /* -------------------------------------------- */
  /** @override */
  async _onUpdate(e, t, n) {
    await super._onUpdate(e, t, n), game.userId === n && (t != null && t.mwdSyncOverloadedFromEffect || foundry.utils.hasProperty(e, "system.burn.overloaded") && await this._syncOverloadedEffect(!!e.system.burn.overloaded));
  }
  _onCreateDescendantDocuments(e, t, n, a, r, s) {
    super._onCreateDescendantDocuments(e, t, n, a, r, s), t === "effects" && this._syncOverloadedFieldFromEffects();
  }
  _onUpdateDescendantDocuments(e, t, n, a, r, s) {
    super._onUpdateDescendantDocuments(e, t, n, a, r, s), t === "effects" && this._syncOverloadedFieldFromEffects();
  }
  _onDeleteDescendantDocuments(e, t, n, a, r, s) {
    super._onDeleteDescendantDocuments(e, t, n, a, r, s), t === "effects" && this._syncOverloadedFieldFromEffects();
  }
  async _syncOverloadedEffect(e) {
    await this.toggleStatusEffect("overloaded", { active: e, overlay: !1 });
  }
  async _syncOverloadedFieldFromEffects() {
    var n, a, r, s;
    const e = ((a = (n = this.statuses) == null ? void 0 : n.has) == null ? void 0 : a.call(n, "overloaded")) ?? !1, t = !!((s = (r = this.system) == null ? void 0 : r.burn) != null && s.overloaded);
    e !== t && await this.update(
      { "system.burn.overloaded": e },
      { mwdSyncOverloadedFromEffect: !0 }
    );
  }
  /* -------------------------------------------- */
  /* Condition Monitors                            */
  /* -------------------------------------------- */
  async setMonitorValue(e, t, { source: n = "unknown" } = {}) {
    var d, m, f, p, g, h;
    if (e === "burn") {
      const y = Math.max(0, Number(t) || 0);
      return this.update({ "system.burn.value": y });
    }
    if (e === "heat" && this.type === "battlemech") {
      const y = Number(foundry.utils.getProperty(this, "system.monitors.heat.max")), b = Number(foundry.utils.getProperty(this, "system.mwd.heat.max")), S = Number(foundry.utils.getProperty(this, "system.mwd.heat.hardMax")), w = Math.max(
        0,
        Number.isFinite(y) && y > 0 ? y : Number.isFinite(b) && b > 0 ? b : Number.isFinite(S) ? S : 0
      ), T = Math.max(0, Number(t) || 0);
      return this.update({
        "system.monitors.heat.value": T,
        "system.monitors.heat.max": w,
        "system.mwd.heat.current": T,
        "system.mwd.heat.max": w
      });
    }
    if (e === "armor" && this.isCharacterLike()) {
      const y = this.getPersonalCombatLoadout({ refresh: !0 }), b = ((d = y == null ? void 0 : y.activeArmor) == null ? void 0 : d.armorId) ?? ((m = y == null ? void 0 : y.activeArmor) == null ? void 0 : m.id) ?? null, S = b ? this.items.get(b) : null;
      if (!(S != null && S.id)) return null;
      const w = Math.max(0, Number(((f = S.system) == null ? void 0 : f.rating) ?? 0) || 0), T = Math.max(0, Number(((g = (p = S.system) == null ? void 0 : p.durability) == null ? void 0 : g.max) ?? 0) || 0), k = T > 0 ? T : w, C = Math.min(Math.max(0, Number(t) || 0), k);
      return this.updateEmbeddedDocuments("Item", [{
        _id: S.id,
        "system.durability.max": k,
        "system.durability.current": C
      }]);
    }
    const a = `system.monitors.${e}`, r = Number(foundry.utils.getProperty(this, `${a}.max`)) || 0, s = Math.max(0, r), o = Math.min(Math.max(0, Number(t) || 0), s), l = { [`${a}.value`]: o }, c = this.type, u = (h = xo == null ? void 0 : xo[c]) == null ? void 0 : h[e];
    if (u != null && u.derived)
      for (const [y, b] of Object.entries(u.derived)) {
        const S = ml == null ? void 0 : ml[b.fn];
        if (typeof S != "function") continue;
        const w = gE(this, e, b.source, o);
        l[`${a}.derived.${y}`] = S(w);
      }
    return this.update(l);
  }
  _prepareMonitors() {
    var o, l, c, u, d, m, f, p;
    const e = this.system.monitors ?? {}, t = pE(e);
    (o = this.system).derived ?? (o.derived = {}), this.system.derived.monitors = t;
    const n = Number(((l = t == null ? void 0 : t.physical) == null ? void 0 : l.penalty) ?? 0), a = Number(((c = t == null ? void 0 : t.fatigue) == null ? void 0 : c.penalty) ?? 0), r = Number(((u = t == null ? void 0 : t.armor) == null ? void 0 : u.resistance) ?? 0), s = n + a;
    e.physical ?? (e.physical = {}), (d = e.physical).derived ?? (d.derived = {}), e.physical.derived.penalty = n, e.fatigue ?? (e.fatigue = {}), (m = e.fatigue).derived ?? (m.derived = {}), e.fatigue.derived.penalty = a, e.armor ?? (e.armor = {}), (f = e.armor).derived ?? (f.derived = {}), e.armor.derived.resistance = r, (p = this.system.derived).condition ?? (p.condition = {}), this.system.derived.condition.physicalPenalty = n, this.system.derived.condition.fatiguePenalty = a, this.system.derived.condition.totalPenalty = s, this.system.derived.conditionPenalty = s;
  }
  _preparePersonalCombatDerived() {
    var s, o, l, c, u, d;
    if (!this.isCharacterLike()) return;
    const e = this.getPersonalCombatLoadout({ refresh: !0 }), t = (o = (s = this.system) == null ? void 0 : s.monitors) == null ? void 0 : o.armor;
    if (!t) return;
    const n = e.activeArmor, a = Math.max(0, Number(((l = n == null ? void 0 : n.durability) == null ? void 0 : l.max) ?? 0)), r = Math.max(0, Number((n == null ? void 0 : n.remainingDurability) ?? ((c = n == null ? void 0 : n.durability) == null ? void 0 : c.current) ?? 0));
    t.max = a, t.value = Math.min(a, r), t.resistance = {
      default: Number((n == null ? void 0 : n.baseMitigation) ?? (n == null ? void 0 : n.baseResistance) ?? 0),
      byType: {}
    }, t.resistanceBonusByType = n != null && n.isDestroyed ? {} : (n == null ? void 0 : n.mitigationByType) ?? (n == null ? void 0 : n.typedMitigation) ?? {}, t.derived ?? (t.derived = {}), t.derived.resistance = Number((n == null ? void 0 : n.baseMitigation) ?? (n == null ? void 0 : n.baseResistance) ?? 0), t.effect = n != null && n.isDestroyed ? "Destroyed" : n ? hE(n.mitigationByType ?? n.typedMitigation) : "", (u = this.system).derived ?? (u.derived = {}), this.system.derived.personalCombat = {
      defaultWeaponId: ((d = e.defaultWeapon) == null ? void 0 : d.id) ?? null,
      activeArmorId: (n == null ? void 0 : n.id) ?? null,
      warnings: [...e.warnings ?? []]
    };
  }
}
function bE(i = null) {
  return (i == null ? void 0 : i.type) === A.actorTypes.vehicle || (i == null ? void 0 : i.type) === A.actorTypes.battlemech;
}
function SE(i = "") {
  return String(i ?? "").replace(/([a-z0-9])([A-Z])/g, "$1 $2").replace(/[_-]+/g, " ").replace(/\s+/g, " ").trim().replace(/\b\w/g, (e) => e.toUpperCase());
}
async function AE({ actor: i, payload: e } = {}) {
  var B, P, L, D, G, V, Z, E, X;
  if (!i) throw new Error("resolveSkill requires actor");
  const t = String((e == null ? void 0 : e.key) ?? "").trim(), n = Lt(t);
  if (!n) throw new Error(`Unknown skill: ${t}`);
  const a = bE(i), r = a ? await xn({
    machineActor: i,
    operatorActorUuid: String((e == null ? void 0 : e.operatorActorUuid) ?? "").trim()
  }) : null, s = (r == null ? void 0 : r.actor) ?? i, o = s.system ?? {}, l = String((e == null ? void 0 : e.machineAttributeKey) ?? "").trim(), c = String(
    l || (e == null ? void 0 : e.attrKey) || (a && t === "piloting" ? A.actorAttributes.handling : n.attribute) || ""
  ).trim();
  if (!c) throw new Error(`Skill ${t} missing attribute key`);
  const u = l || a && t === "piloting" ? i : s, d = u.system ?? {}, m = Number(((P = (B = d == null ? void 0 : d.attributes) == null ? void 0 : B[c]) == null ? void 0 : P.value) ?? 0), f = Number(((D = (L = o == null ? void 0 : o.skills) == null ? void 0 : L[t]) == null ? void 0 : D.rating) ?? 0), p = Number(((V = (G = o == null ? void 0 : o.skills) == null ? void 0 : G[t]) == null ? void 0 : V.bonus) ?? 0), g = new Set(co(o, t)), h = Xc(t, e == null ? void 0 : e.specializationKey), y = h && g.has(h.key) ? h : null, b = y ? Yc : 0, S = Array.isArray(e == null ? void 0 : e.domains) ? e.domains : n.domains ?? [], w = Number.isFinite(Number(e == null ? void 0 : e.diceTarget)) ? Number(e.diceTarget) : Number.isFinite(Number(e == null ? void 0 : e.target)) ? Number(e.target) : 5, T = Number.isFinite(Number(e == null ? void 0 : e.dn)) ? Number(e.dn) : 1, k = a && t === "piloting" ? yw(i) : 0, C = T + k, I = SE(c), O = u !== s && (u != null && u.name) ? ` (${u.name})` : "", x = a && (s != null && s.name) ? ` (${s.name})` : "";
  return {
    intent: "skill",
    rollType: "simple",
    title: `${n.label} (${I})`,
    subtitle: i.name ?? "Actor",
    domains: S,
    // Per-die threshold (cs>=X). Kept separate from dnHits.
    diceTarget: w,
    // DN = hits needed for success
    difficulty: { dn: C },
    dn: {
      parts: [
        { id: "difficulty.base", label: "Base DN", value: T, tags: ["base"] },
        ...k ? [{ id: "machineCrit.unstable", label: "Unstable", value: k, tags: ["machineCrit"] }] : []
      ],
      total: C
    },
    edge: {
      earn: { enabled: !0, rate: 4, maxPerRoll: 1 }
    },
    pool: { attribute: m, skill: f, bonus: p, specialization: b },
    breakdown: [
      { id: "attribute", label: `${I}${O}`, value: m },
      { id: "skill", label: `${n.label}${x}`, value: f },
      { id: "bonus", label: "Bonus", value: p },
      ...y ? [{
        id: "specialization",
        label: `Specialization (${y.label})`,
        value: b
      }] : []
    ],
    specialization: y ? {
      key: y.key,
      label: y.label,
      value: b,
      skillKey: t
    } : null,
    // optional extra metadata (safe to stash)
    data: {
      skillKey: t,
      attrKey: c,
      machineActorUuid: a ? i.uuid ?? "" : "",
      operatorActorUuid: ((Z = r == null ? void 0 : r.actor) == null ? void 0 : Z.uuid) ?? "",
      label: `${c}+${n.label}`,
      specializationKey: (y == null ? void 0 : y.key) ?? "",
      specializationLabel: (y == null ? void 0 : y.label) ?? ""
    },
    rollActor: s,
    machineActor: a ? i : null,
    operator: r ? {
      actorUuid: ((E = r.actor) == null ? void 0 : E.uuid) ?? "",
      name: ((X = r.actor) == null ? void 0 : X.name) ?? "",
      source: r.source ?? "",
      reason: r.reason ?? ""
    } : null
  };
}
const wE = /* @__PURE__ */ new Set([
  "grit",
  "chaos",
  "insight",
  "rumor",
  "legend",
  "credibility"
]), TE = {
  grit: "physical",
  chaos: "physical",
  insight: "mental",
  rumor: "mental",
  legend: "social",
  credibility: "social"
};
async function kE({ actor: i, payload: e } = {}) {
  if (!i) throw new Error("resolveEdge requires actor");
  const t = String((e == null ? void 0 : e.pool) ?? "").trim();
  if (!wE.has(t)) throw new Error(`Invalid edge pool: ${t}`);
  const n = i.getEdgePool(t), a = Math.max(0, Number((n == null ? void 0 : n.effectiveValue) ?? 0));
  return {
    intent: "edge",
    title: `Edge — ${t}`,
    subtitle: i.name ?? "Actor",
    domains: [TE[t] ?? "unknown"],
    // drop "edge" tag unless you truly want it
    // Make it directly rollable by the core roll pipeline
    target: 5,
    poolTotal: a,
    breakdown: [
      { id: "current", label: "Current", value: Number((n == null ? void 0 : n.value) ?? 0) },
      { id: "rating", label: "Rating", value: Number((n == null ? void 0 : n.rating) ?? 0) },
      { id: "cap", label: "Edge Cap", value: Number((n == null ? void 0 : n.cap) ?? 0) },
      { id: "usable", label: "Usable", value: a }
    ],
    data: { poolKey: t }
  };
}
async function ME({ actor: i, payload: e, event: t } = {}) {
  return {
    title: "Attribute",
    intent: "attribute",
    domainTags: ["general"],
    pool: { attribute: 0, skill: 0, bonus: 0 },
    breakdown: [],
    mods: []
  };
}
async function CE({ actor: i, payload: e } = {}) {
  if (!i) throw new Error("resolveCommon requires actor");
  const t = String((e == null ? void 0 : e.id) ?? "").trim();
  if (!t) throw new Error("Common rolls require payload.id");
  const n = nu(t);
  if (!n) throw new Error(`Unknown common check: ${t}`);
  const a = Array.isArray(n.formula) ? n.formula : [];
  if (a.length !== 2)
    throw new Error(`Common check ${t} must define exactly two attributes.`);
  const r = a.map((c) => {
    var d, m, f;
    const u = mS(c);
    if (!u) throw new Error(`Common check ${t} uses unsupported attribute code: ${c}`);
    return {
      code: String(c).trim().toUpperCase(),
      key: u,
      label: fS(c),
      value: Number(((f = (m = (d = i.system) == null ? void 0 : d.attributes) == null ? void 0 : m[u]) == null ? void 0 : f.value) ?? 0)
    };
  }), s = r.reduce((c, u) => c + Number(u.value ?? 0), 0), o = Array.isArray(n.tags) ? [...n.tags] : [], l = Array.isArray(n.domains) ? [...n.domains] : [];
  return {
    intent: "common",
    rollType: "simple",
    title: String(n.label ?? t).trim() || t,
    subtitle: i.name ?? "Actor",
    domains: l,
    tags: o,
    formula: pS(a),
    difficulty: {
      dn: Number.isFinite(Number(e == null ? void 0 : e.dn)) ? Number(e.dn) : 1
    },
    edge: {
      earn: { enabled: !0, rate: 4, maxPerRoll: 1 }
    },
    pool: {
      attribute: s,
      skill: 0,
      bonus: 0,
      specialization: 0
    },
    breakdown: r.map((c) => ({
      id: `attribute.${c.code.toLowerCase()}`,
      label: c.label,
      value: c.value
    })),
    data: {
      commonCheckId: t,
      label: String(n.label ?? t).trim() || t,
      formulaCodes: a,
      tags: o,
      attributes: r
    }
  };
}
const vE = 90;
var nf;
const EE = Number(((nf = CONST == null ? void 0 : CONST.REGION_VISIBILITY) == null ? void 0 : nf.ALWAYS) ?? 2) || 2;
function Qs() {
  var i, e;
  return Number(((i = canvas.grid) == null ? void 0 : i.size) ?? ((e = canvas.dimensions) == null ? void 0 : e.size) ?? 100) || 100;
}
function IE(i = {}) {
  return Math.max(0, Number((i == null ? void 0 : i.size) ?? 0) || 0);
}
function Wa(i) {
  var t, n, a, r;
  return ((n = (t = canvas.tokens) == null ? void 0 : t.controlled) == null ? void 0 : n.find((s) => {
    var o;
    return ((o = s.actor) == null ? void 0 : o.id) === (i == null ? void 0 : i.id);
  })) ?? null ?? ((r = (a = i == null ? void 0 : i.getActiveTokens) == null ? void 0 : a.call(i, !0, !0)) == null ? void 0 : r[0]) ?? null;
}
function Bm(i) {
  var e, t;
  return Number(
    ((e = i == null ? void 0 : i.document) == null ? void 0 : e.disposition) ?? (i == null ? void 0 : i.disposition) ?? ((t = CONST == null ? void 0 : CONST.TOKEN_DISPOSITIONS) == null ? void 0 : t.NEUTRAL) ?? 0
  );
}
function Xs(i) {
  var s, o, l, c, u;
  const e = (i == null ? void 0 : i.center) ?? ((s = i == null ? void 0 : i.object) == null ? void 0 : s.center);
  if (e) return { x: Number(e.x ?? 0), y: Number(e.y ?? 0) };
  const t = Number((i == null ? void 0 : i.x) ?? ((o = i == null ? void 0 : i.document) == null ? void 0 : o.x) ?? 0), n = Number((i == null ? void 0 : i.y) ?? ((l = i == null ? void 0 : i.document) == null ? void 0 : l.y) ?? 0), a = Number((i == null ? void 0 : i.w) ?? (i == null ? void 0 : i.width) ?? ((c = i == null ? void 0 : i.document) == null ? void 0 : c.width) ?? 1) * Qs(), r = Number((i == null ? void 0 : i.h) ?? (i == null ? void 0 : i.height) ?? ((u = i == null ? void 0 : i.document) == null ? void 0 : u.height) ?? 1) * Qs();
  return { x: t + a / 2, y: n + r / 2 };
}
function NE(i) {
  var n, a, r, s;
  const e = Number((i == null ? void 0 : i.w) ?? ((n = i == null ? void 0 : i.object) == null ? void 0 : n.w) ?? 0) || Number(((a = i == null ? void 0 : i.document) == null ? void 0 : a.width) ?? 1) * Qs(), t = Number((i == null ? void 0 : i.h) ?? ((r = i == null ? void 0 : i.object) == null ? void 0 : r.h) ?? 0) || Number(((s = i == null ? void 0 : i.document) == null ? void 0 : s.height) ?? 1) * Qs();
  return Math.max(e, t) / 2;
}
function RE() {
  var t;
  const i = ((t = canvas == null ? void 0 : canvas.stage) == null ? void 0 : t.pivot) ?? null, e = (canvas == null ? void 0 : canvas.dimensions) ?? {};
  return {
    x: Number((i == null ? void 0 : i.x) ?? e.width / 2 ?? 0) || 0,
    y: Number((i == null ? void 0 : i.y) ?? e.height / 2 ?? 0) || 0
  };
}
function PE() {
  var i;
  return Array.from(((i = game.user) == null ? void 0 : i.targets) ?? []).find((e) => e == null ? void 0 : e.actor) ?? null;
}
function DE(i, e) {
  return {
    x: (Number((i == null ? void 0 : i.x) ?? 0) + Number((e == null ? void 0 : e.x) ?? 0)) / 2,
    y: (Number((i == null ? void 0 : i.y) ?? 0) + Number((e == null ? void 0 : e.y) ?? 0)) / 2
  };
}
function OE({ template: i = {}, actor: e = null } = {}) {
  const t = String((i == null ? void 0 : i.placement) ?? "").trim().toLowerCase(), n = Wa(e), a = PE(), r = n ? Xs(n) : null, s = a ? Xs(a) : null;
  return t === "origin" && r ? r : t === "targeted" && s ? s : t === "placed" && r && s ? DE(r, s) : RE();
}
function LE({ attack: i = {}, actor: e = null } = {}) {
  const t = (i == null ? void 0 : i.template) ?? null, n = String((t == null ? void 0 : t.shape) ?? "").trim().toLowerCase();
  if (!n) return null;
  const a = OE({ template: t, actor: e });
  return ct({
    shape: n,
    x: a.x,
    y: a.y,
    direction: 0,
    distance: IE(t),
    angle: n === "cone" ? vE : null,
    width: n === "line" ? 1 : null,
    placementMode: (t == null ? void 0 : t.placement) ?? null
  });
}
function _E() {
  var e, t;
  const i = new PIXI.Container();
  return i.eventMode = "none", i.sortableChildren = !0, (t = (e = canvas.stage) == null ? void 0 : e.addChild) == null || t.call(e, i), i;
}
function xE(i) {
  var e;
  i != null && i.parent && i.parent.removeChild(i), (e = i == null ? void 0 : i.destroy) == null || e.call(i, { children: !0 });
}
function $E() {
  const i = _E(), e = new PIXI.Container();
  e.eventMode = "none", e.zIndex = 5;
  const t = new PIXI.Container();
  return t.eventMode = "none", t.zIndex = 10, i.addChild(e), i.addChild(t), { root: i, templateLayer: e, markerLayer: t };
}
function BE(i) {
  xE((i == null ? void 0 : i.root) ?? i);
}
function zE() {
  var t;
  const i = String(((t = game.user) == null ? void 0 : t.color) ?? "#ff6400").replace("#", "").trim(), e = Number.parseInt(i, 16);
  return Number.isFinite(e) ? e : 16737280;
}
function Jh(i) {
  var e;
  (e = i == null ? void 0 : i.removeChildren) == null || e.call(i).forEach((t) => {
    var n;
    return (n = t.destroy) == null ? void 0 : n.call(t, { children: !0 });
  });
}
function Pc(i) {
  var c, u, d;
  const e = ((c = canvas == null ? void 0 : canvas.app) == null ? void 0 : c.view) ?? null, t = ((u = canvas == null ? void 0 : canvas.app) == null ? void 0 : u.renderer) ?? null, n = (canvas == null ? void 0 : canvas.stage) ?? null;
  if (!e || !n) return null;
  const a = Number((i == null ? void 0 : i.clientX) ?? NaN), r = Number((i == null ? void 0 : i.clientY) ?? NaN);
  if (!Number.isFinite(a) || !Number.isFinite(r)) return null;
  const s = e.getBoundingClientRect();
  if (a < s.left || a > s.right || r < s.top || r > s.bottom) return null;
  const o = new PIXI.Point();
  if (typeof ((d = t == null ? void 0 : t.events) == null ? void 0 : d.mapPositionToPoint) == "function")
    t.events.mapPositionToPoint(o, a, r);
  else {
    const m = Number((t == null ? void 0 : t.resolution) ?? window.devicePixelRatio ?? 1) || 1;
    o.x = (a - s.left) * m, o.y = (r - s.top) * m;
  }
  const l = n.toLocal(o);
  return {
    x: Number((l == null ? void 0 : l.x) ?? 0) || 0,
    y: Number((l == null ? void 0 : l.y) ?? 0) || 0
  };
}
function FE(i, e) {
  const t = Number((e == null ? void 0 : e.x) ?? 0) - Number((i == null ? void 0 : i.x) ?? 0), n = Number((e == null ? void 0 : e.y) ?? 0) - Number((i == null ? void 0 : i.y) ?? 0);
  return t === 0 && n === 0 ? 0 : Math.atan2(n, t) * 180 / Math.PI;
}
function fl(i = 0) {
  var n, a, r, s, o;
  const e = Number(((n = canvas.grid) == null ? void 0 : n.size) ?? ((a = canvas.dimensions) == null ? void 0 : a.size) ?? 100) || 100, t = Number(((s = (r = canvas.scene) == null ? void 0 : r.grid) == null ? void 0 : s.distance) ?? ((o = canvas.dimensions) == null ? void 0 : o.distance) ?? 1) || 1;
  return Number(i ?? 0) * (e / t);
}
function HE({ geometry: i = null, pointer: e = null, attack: t = {}, actor: n = null } = {}) {
  var l;
  const a = ct(i);
  if (!a) return null;
  const r = ji(a) ?? null;
  if (!r || !e) return r;
  const o = String(((l = t == null ? void 0 : t.template) == null ? void 0 : l.placement) ?? r.placementMode ?? "").trim().toLowerCase() !== "origin";
  if (o && (r.x = e.x, r.y = e.y), ["line", "cone", "rect"].includes(String(r.shape ?? "").trim().toLowerCase())) {
    const c = Wa(n), u = c ? Xs(c) : null, d = o ? u ?? { x: Number(a.x ?? 0), y: Number(a.y ?? 0) } : { x: Number(r.x ?? 0), y: Number(r.y ?? 0) };
    r.direction = FE(d, e);
  }
  return ct(r);
}
function UE(i, e = null) {
  if (!i) return;
  Jh(i);
  const t = ct(e);
  if (!t) return;
  const n = zE(), a = new PIXI.Graphics();
  switch (a.lineStyle(3, n, 0.95), a.beginFill(n, 0.18), String(t.shape ?? "").trim().toLowerCase()) {
    case "blast": {
      a.drawCircle(
        Number(t.x ?? 0),
        Number(t.y ?? 0),
        fl(t.distance ?? 0)
      );
      break;
    }
    case "rect": {
      const r = fl(t.width ?? 0), s = fl(t.height ?? 0);
      a.position.set(Number(t.x ?? 0), Number(t.y ?? 0)), a.rotation = Number(t.direction ?? 0) * Math.PI / 180, a.drawRect(
        -(Number(t.anchorX ?? 0) || 0) * r,
        -(Number(t.anchorY ?? 0) || 0) * s,
        r,
        s
      );
      break;
    }
    default: {
      const [r] = ao(t);
      (r == null ? void 0 : r.type) === "polygon" && Array.isArray(r.points) && r.points.length >= 3 && a.drawPolygon(r.points.flatMap((s) => [Number((s == null ? void 0 : s.x) ?? 0), Number((s == null ? void 0 : s.y) ?? 0)]));
      break;
    }
  }
  a.endFill(), i.addChild(a);
}
function jE(i = pe.none) {
  return i === pe.full ? 14042437 : i === pe.major ? 15174447 : i === pe.minor ? 15782993 : 10134706;
}
function KE(i, e = []) {
  if (i) {
    Jh(i);
    for (const t of e) {
      const n = Xs(t.token), a = Math.max(20, NE(t.token) + 12), r = jE(t.exposureTier), s = new PIXI.Graphics();
      s.lineStyle(4, r, 0.95), s.beginFill(r, 0.14), s.drawCircle(n.x, n.y, a), s.endFill(), s.zIndex = 10;
      const o = new PIXI.Text(bi(t.exposureTier), {
        fontFamily: "MWD UI",
        fontSize: 18,
        fontWeight: "700",
        fill: r,
        stroke: 1118481,
        strokeThickness: 4,
        align: "center"
      });
      o.anchor.set(0.5, 1), o.position.set(n.x, n.y - a - 6), o.zIndex = 11, i.addChild(s), i.addChild(o);
    }
  }
}
function Qh(i, e = {}) {
  var r, s, o, l, c, u, d, m, f, p, g, h, y, b, S, w;
  const t = (i == null ? void 0 : i.actor) ?? null;
  if (!t) return null;
  const n = ((r = t == null ? void 0 : t.getPersonalCombatLoadout) == null ? void 0 : r.call(t)) ?? null, a = (n == null ? void 0 : n.activeArmor) ?? null;
  return {
    tokenId: (i == null ? void 0 : i.id) ?? null,
    tokenUuid: ((s = i == null ? void 0 : i.document) == null ? void 0 : s.uuid) ?? null,
    actorId: t.id,
    actorUuid: t.uuid,
    name: t.name ?? (i == null ? void 0 : i.name) ?? "Target",
    attributes: {
      reflexes: Number(((c = (l = (o = t == null ? void 0 : t.system) == null ? void 0 : o.attributes) == null ? void 0 : l.reflexes) == null ? void 0 : c.value) ?? 0) || 0
    },
    skills: {
      tactics: {
        rating: Number(((m = (d = (u = t == null ? void 0 : t.system) == null ? void 0 : u.skills) == null ? void 0 : d.tactics) == null ? void 0 : m.rating) ?? 0) || 0
      }
    },
    activeArmor: a ? {
      armorId: a.id,
      rating: Number(a.ratingCurrent ?? a.rating ?? 0),
      currentArmorRating: Number(a.currentArmorRating ?? ((f = a.durability) == null ? void 0 : f.current) ?? 0),
      remainingDurability: Number(a.remainingDurability ?? ((p = a.durability) == null ? void 0 : p.current) ?? 0),
      baseMitigation: Number(a.baseMitigation ?? a.baseResistance ?? 0),
      baseResistance: Number(a.baseMitigation ?? a.baseResistance ?? 0),
      mitigationByType: { ...a.mitigationByType ?? a.typedMitigation ?? {} },
      tags: [...a.tags ?? []],
      isDestroyed: !!a.isDestroyed,
      defenseBonus: Number(a.defenseBonus ?? 0)
    } : null,
    exposure: Rn({
      tier: ((g = e == null ? void 0 : e.exposure) == null ? void 0 : g.initialTier) ?? ((h = e == null ? void 0 : e.exposure) == null ? void 0 : h.tier) ?? (e == null ? void 0 : e.exposureTier) ?? pe.none,
      appliedTier: ((y = e == null ? void 0 : e.exposure) == null ? void 0 : y.finalTier) ?? ((b = e == null ? void 0 : e.exposure) == null ? void 0 : b.appliedTier) ?? (e == null ? void 0 : e.exposureTier) ?? pe.none,
      evadeUsed: !!((S = e == null ? void 0 : e.exposure) != null && S.evadeUsed),
      evadeLocked: !!((w = e == null ? void 0 : e.exposure) != null && w.evadeLocked)
    }),
    areaEffect: e != null && e.areaEffect ? foundry.utils.deepClone(e.areaEffect) : null
  };
}
function GE({ attack: i = {}, geometry: e = null, attacker: t = null } = {}) {
  var o;
  const n = (i == null ? void 0 : i.template) ?? null, a = ct(e);
  if (!n || !a) return [];
  const r = Wa(t), s = (r == null ? void 0 : r.id) ?? null;
  return (((o = canvas.tokens) == null ? void 0 : o.placeables) ?? []).filter((l) => l == null ? void 0 : l.actor).filter((l) => l.id !== s || (n == null ? void 0 : n.placement) === "origin").filter((l) => no(a, l)).map((l) => {
    const c = bf({ geometry: a, token: l });
    return Qh(l, {
      exposureTier: c,
      areaEffect: {
        templateShape: (n == null ? void 0 : n.shape) ?? "",
        templatePlacement: (n == null ? void 0 : n.placement) ?? "",
        templateGeometry: ji(a)
      }
    });
  }).filter(Boolean);
}
function WE({ attack: i = {}, geometry: e = null, attacker: t = null } = {}) {
  var o;
  const n = (i == null ? void 0 : i.template) ?? null, a = Wa(t), r = (a == null ? void 0 : a.id) ?? null, s = ct(e);
  return !n || !s ? [] : (((o = canvas.tokens) == null ? void 0 : o.placeables) ?? []).filter((l) => l == null ? void 0 : l.actor).filter((l) => l.id !== r || (n == null ? void 0 : n.placement) === "origin").filter((l) => no(s, l)).map((l) => ({
    token: l,
    exposureTier: bf({ geometry: s, token: l })
  }));
}
function qE({ geometry: i = null, attack: e = {}, attacker: t = null } = {}) {
  var m, f, p, g;
  const n = (e == null ? void 0 : e.template) ?? null, a = ct(i);
  if (!n || !a) return [];
  const r = Wa(t), s = (r == null ? void 0 : r.id) ?? null, o = Number(((m = CONST == null ? void 0 : CONST.TOKEN_DISPOSITIONS) == null ? void 0 : m.HOSTILE) ?? -1), l = Number(((f = CONST == null ? void 0 : CONST.TOKEN_DISPOSITIONS) == null ? void 0 : f.FRIENDLY) ?? 1), c = Number(((p = CONST == null ? void 0 : CONST.TOKEN_DISPOSITIONS) == null ? void 0 : p.NEUTRAL) ?? 0), u = Bm(r), d = (h) => {
    const y = Bm(h);
    return r ? u === l ? y === o : u === o ? y === l : u === c ? y === o : y !== u : !0;
  };
  return (((g = canvas.tokens) == null ? void 0 : g.placeables) ?? []).filter((h) => h == null ? void 0 : h.actor).filter((h) => h.id !== s || (n == null ? void 0 : n.placement) === "origin").filter((h) => no(a, h)).filter(d).map((h) => String(h.id ?? "").trim()).filter(Boolean);
}
function VE(i = {}) {
  var n;
  const e = String(((n = i == null ? void 0 : i.template) == null ? void 0 : n.shape) ?? "template").trim().toLowerCase();
  return `${e ? `${e.slice(0, 1).toUpperCase()}${e.slice(1)}` : "Template"} placement: left-click to place, right-click or Esc to cancel, Enter or Space to confirm.`;
}
async function YE({ attack: i = {} } = {}) {
  var t, n;
  const e = VE(i);
  return e && ((n = (t = ui.notifications) == null ? void 0 : t.info) == null || n.call(t, e)), new Promise((a) => {
    let r = !1;
    const s = () => {
      window.removeEventListener("pointerdown", u, !0), window.removeEventListener("keydown", d, !0), window.removeEventListener("contextmenu", m, !0);
    }, o = (f = !1) => {
      r || (r = !0, s(), a(!!f));
    }, l = (f) => {
      var p, g, h;
      (p = f == null ? void 0 : f.preventDefault) == null || p.call(f), (g = f == null ? void 0 : f.stopPropagation) == null || g.call(f), (h = f == null ? void 0 : f.stopImmediatePropagation) == null || h.call(f);
    }, c = (f) => {
      if (!(f instanceof HTMLElement)) return !1;
      const p = String(f.tagName ?? "").trim().toUpperCase();
      return f.isContentEditable || ["INPUT", "TEXTAREA", "SELECT"].includes(p);
    }, u = (f) => {
      const p = Number((f == null ? void 0 : f.button) ?? 0), g = Pc(f);
      if (p === 2 && g) {
        l(f), o(!1);
        return;
      }
      p !== 0 || !g || (l(f), o(!0));
    }, d = (f) => {
      const p = String((f == null ? void 0 : f.key) ?? ""), g = String((f == null ? void 0 : f.code) ?? "");
      if (p === "Escape") {
        l(f), o(!1);
        return;
      }
      c((f == null ? void 0 : f.target) ?? document.activeElement) || (p === "Enter" || p === "NumpadEnter" || p === " " || p === "Spacebar" || g === "Space") && (l(f), o(!0));
    }, m = (f) => {
      Pc(f) && l(f);
    };
    window.addEventListener("pointerdown", u, !0), window.addEventListener("keydown", d, !0), window.addEventListener("contextmenu", m, !0);
  });
}
async function JE({ actor: i = null, attack: e = {}, templateGeometry: t = null } = {}) {
  var o, l, c, u, d, m;
  if (!(canvas != null && canvas.scene) || pf((e == null ? void 0 : e.areaEffect) ?? ((o = e == null ? void 0 : e.payload) == null ? void 0 : o.areaEffect) ?? {})) return null;
  const n = ct(t, {
    template: e == null ? void 0 : e.template,
    placement: e == null ? void 0 : e.templatePlacement
  });
  if (!n) return null;
  const a = ao(n);
  if (!a.length) return null;
  const r = `${String(((l = e == null ? void 0 : e.weapon) == null ? void 0 : l.name) ?? (e == null ? void 0 : e.name) ?? "Template").trim() || "Template"} Template`, [s] = await canvas.scene.createEmbeddedDocuments("Region", [{
    name: r,
    color: String(((c = game.user) == null ? void 0 : c.color) ?? "#ff6400").trim() || "#ff6400",
    visibility: EE,
    locked: !1,
    shapes: a,
    flags: {
      mwd: {
        templateIndicator: {
          sourceActorUuid: (i == null ? void 0 : i.uuid) ?? null,
          sourceItemUuid: ((u = e == null ? void 0 : e.weapon) == null ? void 0 : u.uuid) ?? null,
          payloadId: ((d = e == null ? void 0 : e.payloadState) == null ? void 0 : d.activePayloadId) ?? ((m = e == null ? void 0 : e.payload) == null ? void 0 : m.id) ?? "",
          label: r,
          templateGeometry: ji(n),
          templatePlacement: foundry.utils.deepClone((e == null ? void 0 : e.templatePlacement) ?? null),
          template: foundry.utils.deepClone((e == null ? void 0 : e.template) ?? null)
        }
      }
    }
  }]);
  return s ?? null;
}
async function QE({ actor: i, attack: e } = {}) {
  if (!(canvas != null && canvas.scene))
    throw rt("Templated attacks require an active scene canvas.", { severity: "warn" });
  const t = (e == null ? void 0 : e.template) ?? null;
  if (!(t != null && t.shape) || !(Number(t == null ? void 0 : t.size) > 0))
    throw rt("Templated attack is missing valid template configuration.", { severity: "warn" });
  if (!Ny.includes(t.shape))
    throw rt(`Template shape "${t.shape}" is normalized but not supported in v1.`, { severity: "warn" });
  const n = Wa(i);
  if (t.placement === "origin" && !n)
    throw rt("Origin-placed templated attacks require the attacker to have a token on the current scene.", { severity: "warn" });
  const a = LE({ attack: e, actor: i });
  if (!a)
    throw rt("Unable to initialize template placement for this attack.", { severity: "warn" });
  const r = $E();
  let s = ji(a), o = "";
  const l = (d = null) => JSON.stringify({
    shape: (d == null ? void 0 : d.shape) ?? "",
    x: Number((d == null ? void 0 : d.x) ?? 0),
    y: Number((d == null ? void 0 : d.y) ?? 0),
    direction: Number((d == null ? void 0 : d.direction) ?? 0),
    distance: Number((d == null ? void 0 : d.distance) ?? 0),
    angle: Number((d == null ? void 0 : d.angle) ?? 0),
    width: Number((d == null ? void 0 : d.width) ?? 0),
    height: Number((d == null ? void 0 : d.height) ?? 0),
    anchorX: Number((d == null ? void 0 : d.anchorX) ?? 0),
    anchorY: Number((d == null ? void 0 : d.anchorY) ?? 0),
    placementMode: (d == null ? void 0 : d.placementMode) ?? ""
  }), c = () => {
    UE(r.templateLayer, s), KE(r.markerLayer, WE({ attack: e, geometry: s, attacker: i }));
  }, u = (d) => {
    const m = Pc(d);
    if (!m) return;
    const f = HE({
      geometry: s,
      pointer: m,
      attack: e,
      actor: i
    });
    if (!f) return;
    const p = l(f);
    p !== o && (s = f, o = p, c());
  };
  try {
    if (o = l(s), c(), window.addEventListener("pointermove", u), !await YE({
      attack: {
        ...e,
        actor: i
      }
    })) return null;
    const m = ji(s);
    if (!m) return null;
    const f = Vy(m, t), p = GE({
      attack: e,
      geometry: m,
      attacker: i
    });
    return {
      templateGeometry: ji(m),
      placement: (f == null ? void 0 : f.placement) ?? null,
      autoTargetTokenIds: qE({
        geometry: m,
        attack: e,
        attacker: i
      }),
      targetSnapshots: p
    };
  } finally {
    window.removeEventListener("pointermove", u), BE(r);
  }
}
function XE(i) {
  return Array.isArray(i) ? i : Array.isArray(i == null ? void 0 : i.contents) ? i.contents : i && typeof i[Symbol.iterator] == "function" ? Array.from(i) : [];
}
function ZE(i = {}) {
  var e;
  return String((i == null ? void 0 : i.type) ?? ((e = i == null ? void 0 : i.actor) == null ? void 0 : e.type) ?? "").trim();
}
function eI(i = {}) {
  var t;
  const e = (i == null ? void 0 : i.assetModules) ?? (i == null ? void 0 : i.items) ?? ((t = i == null ? void 0 : i.actor) == null ? void 0 : t.items) ?? [];
  return XE(e).filter((n) => ((n == null ? void 0 : n.canonicalType) ?? (n == null ? void 0 : n.type)) === A.itemType.assetModule);
}
function pl() {
  return {
    active: !1,
    diceModifier: 0,
    targetNumberModifier: 0,
    sourceIds: [],
    sourceNames: [],
    sourceLabel: ""
  };
}
function tI(i = {}) {
  const e = ZE(i);
  if (e && e !== A.actorTypes.battlemech && e !== A.actorTypes.vehicle)
    return { clustering: pl() };
  const t = eI(i).map((a) => ({
    item: a,
    profile: ZT(a)
  })).filter(
    (a) => {
      var r, s;
      return Number(((r = a.profile) == null ? void 0 : r.diceModifier) ?? 0) !== 0 || Number(((s = a.profile) == null ? void 0 : s.targetNumberModifier) ?? 0) !== 0;
    }
  );
  if (!t.length)
    return { clustering: pl() };
  const n = t.reduce((a, r) => {
    var s, o, l, c;
    return {
      active: !0,
      diceModifier: a.diceModifier + (Number(((s = r.profile) == null ? void 0 : s.diceModifier) ?? 0) || 0),
      targetNumberModifier: a.targetNumberModifier + (Number(((o = r.profile) == null ? void 0 : o.targetNumberModifier) ?? 0) || 0),
      sourceIds: (l = r.item) != null && l.id ? a.sourceIds.concat([r.item.id]) : a.sourceIds,
      sourceNames: (c = r.item) != null && c.name ? a.sourceNames.concat([r.item.name]) : a.sourceNames
    };
  }, pl());
  return {
    clustering: {
      ...n,
      sourceLabel: n.sourceNames.join(", ")
    }
  };
}
function iI(i = {}) {
  return tI(i).clustering;
}
const nI = 30, Xh = Object.freeze({
  stationary: 0,
  moved1: 1,
  moved2: 2,
  moved3Plus: 3
}), aI = Object.freeze([
  Object.freeze({ min: 0, max: 0, penalty: 0 }),
  Object.freeze({ min: 1, max: 2, penalty: -1 }),
  Object.freeze({ min: 3, max: 4, penalty: -2 }),
  Object.freeze({ min: 5, max: 6, penalty: -3 }),
  Object.freeze({ min: 7, max: 8, penalty: -4 }),
  Object.freeze({ min: 9, max: 1 / 0, penalty: -5 })
]), Dc = Object.freeze({
  stationary: "Stationary",
  moved1: "Moved 1",
  moved2: "Moved 2",
  moved3Plus: "Moved 3+"
}), rI = /* @__PURE__ */ new Set([A.actorTypes.vehicle, A.actorTypes.battlemech]);
function Zh(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? t : e;
}
function Oc(i, e = 0) {
  return Math.max(0, Math.trunc(Zh(i, e)));
}
function Ci(i = null) {
  return rI.has(i == null ? void 0 : i.type);
}
function sI(i = 0) {
  return Math.max(0, Math.round(Zh(i, 0) / nI));
}
function oI(i = 0) {
  var t;
  const e = Oc(i, 0);
  return ((t = aI.find((n) => e >= n.min && e <= n.max)) == null ? void 0 : t.penalty) ?? 0;
}
function Oo(i = "stationary") {
  const e = String(i ?? "").trim();
  return Object.prototype.hasOwnProperty.call(Xh, e) ? e : "stationary";
}
function lI(i = "stationary") {
  return Xh[Oo(i)] ?? 0;
}
function cI(i = null) {
  var t, n;
  if (!Ci(i)) return 0;
  const e = vo(((t = i.system) == null ? void 0 : t.movement) ?? {}, {
    actorType: i.type,
    legacyMoves: (n = i.system) == null ? void 0 : n.moves
  });
  return Math.max(
    Oc(e.ground, 0),
    Oc(e.flight, 0)
  );
}
function uI(i = {}) {
  const e = i != null && i.machineMotion && typeof i.machineMotion == "object" ? i.machineMotion : {};
  return {
    targetMotion: Oo(e.targetMotion ?? (i == null ? void 0 : i.targetMotion)),
    jumped: !!(e.jumped ?? (i == null ? void 0 : i.targetJumped))
  };
}
function ey({
  targetActor: i = null,
  payload: e = {}
} = {}) {
  const t = uI(e), n = t.targetMotion, a = n !== "stationary", r = cI(i), s = a ? sI(r) : 0, o = a ? oI(s) : 0, l = t.jumped ? 1 : 0, c = t.jumped ? -1 : 0, u = lI(n);
  return {
    targetMotion: n,
    targetMotionLabel: Dc[n] ?? Dc.stationary,
    jumped: t.jumped,
    speedMeters: r,
    trackingHexes: s,
    motionDn: u,
    jumpDn: l,
    dnModifier: u + l,
    movementTrackingPenalty: o,
    jumpTrackingPenalty: c,
    trackingPenalty: o + c
  };
}
function dI(i = "close") {
  const e = String(i ?? "").trim().toLowerCase();
  return e === "near" ? -2 : e === "far" ? -3 : e === "extreme" ? -4 : -1;
}
function mI(i = {}) {
  var e;
  return Array.isArray(i == null ? void 0 : i.targetSnapshots) ? i.targetSnapshots : Array.from(((e = game.user) == null ? void 0 : e.targets) ?? []).map(Qh).filter(Boolean);
}
function Ju(i, e = {}) {
  var a, r, s, o, l, c, u, d, m;
  const t = String((e == null ? void 0 : e.sourceTokenId) ?? "").trim();
  if (t) {
    const f = ((r = (a = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : a.get) == null ? void 0 : r.call(a, t)) ?? ((l = (o = (s = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : s.placeables) == null ? void 0 : o.find) == null ? void 0 : l.call(o, (p) => (p == null ? void 0 : p.id) === t)) ?? null;
    if (f) return f;
  }
  return ((u = (c = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : c.controlled) == null ? void 0 : u.find((f) => {
    var p;
    return ((p = f.actor) == null ? void 0 : p.id) === (i == null ? void 0 : i.id);
  })) ?? null ?? ((m = (d = i == null ? void 0 : i.getActiveTokens) == null ? void 0 : d.call(i, !0, !0)) == null ? void 0 : m[0]) ?? null;
}
function ty(i = {}) {
  var t, n, a, r, s;
  const e = String((i == null ? void 0 : i.tokenId) ?? "").trim();
  return e ? ((n = (t = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : t.get) == null ? void 0 : n.call(t, e)) ?? ((s = (r = (a = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : a.placeables) == null ? void 0 : r.find) == null ? void 0 : s.call(r, (o) => (o == null ? void 0 : o.id) === e)) ?? null : null;
}
function fI(i = {}) {
  var e;
  return ((e = ty(i)) == null ? void 0 : e.actor) ?? null;
}
function pI(i, e) {
  var s, o, l, c;
  const t = canvas == null ? void 0 : canvas.grid, n = (i == null ? void 0 : i.center) ?? ((s = i == null ? void 0 : i.object) == null ? void 0 : s.center) ?? null, a = (e == null ? void 0 : e.center) ?? ((o = e == null ? void 0 : e.object) == null ? void 0 : o.center) ?? null;
  if (!t || !n || !a) return null;
  if (typeof t.measurePath == "function")
    try {
      const u = t.measurePath([n, a], { gridSpaces: !0 }), d = Number(
        (u == null ? void 0 : u.distance) ?? (u == null ? void 0 : u.cost) ?? (u == null ? void 0 : u.totalDistance) ?? (u == null ? void 0 : u.totalCost) ?? NaN
      );
      if (Number.isFinite(d)) return d;
    } catch {
    }
  const r = ((c = (l = foundry == null ? void 0 : foundry.canvas) == null ? void 0 : l.geometry) == null ? void 0 : c.Ray) ?? globalThis.Ray;
  if (typeof t.measureDistances == "function" && typeof r == "function")
    try {
      const u = t.measureDistances([{ ray: new r(n, a) }], { gridSpaces: !0 }), d = Number(Array.isArray(u) ? u[0] : NaN);
      if (Number.isFinite(d)) return d;
    } catch {
      return null;
    }
  return null;
}
function gI({ actor: i, payload: e, weapon: t, targets: n = [] } = {}) {
  const a = String((e == null ? void 0 : e.rangeBand) ?? "").trim().toLowerCase(), r = ((t == null ? void 0 : t.type) === "personalWeapon" || (t == null ? void 0 : t.isSynthetic)) && n.length === 1, s = Ci(i) && n.length === 1;
  if (!r && !s)
    return a || String((t == null ? void 0 : t.defaultRangeBand) ?? "close").trim() || "close";
  const o = Ju(i, e), l = ty(n[0]), c = pI(o, l);
  if (s) {
    const d = ug(c, (t == null ? void 0 : t.defaultRangeBand) ?? "close");
    return d === "outOfRange" ? d : a || d;
  }
  const u = yT(c, (t == null ? void 0 : t.range) ?? {}, (t == null ? void 0 : t.defaultRangeBand) ?? "close");
  return u === "outOfRange" ? u : a || ((t == null ? void 0 : t.type) !== "personalWeapon" && !(t != null && t.isSynthetic) || n.length !== 1 ? String((t == null ? void 0 : t.defaultRangeBand) ?? "close").trim() || "close" : u);
}
function iy(i) {
  return ["mechWeapon", "vehicleWeapon"].includes((i == null ? void 0 : i.canonicalType) ?? (i == null ? void 0 : i.type));
}
function hI(i = "") {
  return String(i ?? "").replace(/([a-z0-9])([A-Z])/g, "$1 $2").replace(/[_-]+/g, " ").replace(/\s+/g, " ").trim().replace(/\b\w/g, (e) => e.toUpperCase());
}
function yI(i = {}, e = {}) {
  return {
    close: Number(i.close ?? 0) + Number(e.close ?? 0),
    near: Number(i.near ?? 0) + Number(e.near ?? 0),
    far: Number(i.far ?? 0) + Number(e.far ?? 0),
    extreme: Number(i.extreme ?? 0) + Number(e.extreme ?? 0)
  };
}
function bI(i, e) {
  var f, p, g, h;
  const t = String((e == null ? void 0 : e.sourceId) ?? (e == null ? void 0 : e.weaponGroupId) ?? ((f = e == null ? void 0 : e.machineWeaponGroup) == null ? void 0 : f.id) ?? "").trim();
  if (!t) return null;
  const n = Array.from(((p = i.system) == null ? void 0 : p.weaponGroups) ?? ((h = (g = i.system) == null ? void 0 : g.mwd) == null ? void 0 : h.weaponGroupDetails) ?? []).find((y) => String((y == null ? void 0 : y.id) ?? "").trim() === t) ?? null, a = Array.isArray(n == null ? void 0 : n.weaponIds) ? n.weaponIds : Array.isArray(n == null ? void 0 : n.weapons) ? n.weapons.map((y) => y == null ? void 0 : y.id).filter(Boolean) : [], r = a.map((y) => {
    var b, S;
    return (S = (b = i.items) == null ? void 0 : b.get) == null ? void 0 : S.call(b, y);
  }).filter((y) => y && iy(y));
  if (!n || !r.length) return null;
  const s = r.map((y) => {
    var b;
    return ((b = y.getCombatProfile) == null ? void 0 : b.call(y)) ?? null;
  }).filter(Boolean), o = s[0] ?? {}, l = s.reduce((y, b) => yI(y, b.attackRatingBand), {}), c = s.reduce((y, b) => y + (Number(b.damage ?? 0) || 0), 0), u = s.reduce((y, b) => y + Math.max(0, Number(b.clusteringDice ?? 0) || 0), 0), d = Math.max(0, ...s.map((y) => Number(y.ap ?? 0) || 0)), m = String(o.skill ?? "gunnery").trim() || "gunnery";
  return {
    id: n.id,
    uuid: i.uuid ?? null,
    name: n.name || "Weapon Group",
    img: o.img,
    type: "mechWeaponGroup",
    machineWeaponGroup: {
      id: n.id,
      weaponIds: a,
      weaponNames: r.map((y) => y.name)
    },
    category: o.category ?? "ranged",
    skill: m,
    skillDef: Lt(m),
    damage: c,
    clusteringDice: u,
    ap: d,
    damageType: o.damageType ?? "kinetic",
    attackRatingBand: l,
    range: o.range ?? {},
    defaultRangeBand: o.defaultRangeBand ?? "near",
    effects: {},
    notes: s.map((y) => y.notes).filter(Boolean).join(`
`)
  };
}
function SI(i, e) {
  var s, o, l, c, u, d, m, f, p, g, h, y;
  const t = String((e == null ? void 0 : e.sourceType) ?? "").trim(), n = String((e == null ? void 0 : e.sourceId) ?? "").trim();
  if (((s = e == null ? void 0 : e.syntheticWeapon) == null ? void 0 : s.id) === "unarmed") {
    const b = yi.buildDefaultUnarmedProfile(i);
    return {
      ...b,
      ...e.syntheticWeapon,
      damage: b.damage,
      attackRatingBand: {
        ...((o = e.syntheticWeapon) == null ? void 0 : o.attackRatingBand) ?? b.attackRatingBand,
        close: b.attackRatingBand.close
      },
      isSynthetic: !0,
      defaultRangeBand: "close"
    };
  }
  if (Ci(i)) {
    if ((i == null ? void 0 : i.type) === A.actorTypes.battlemech && (t === "weaponGroup" || e != null && e.weaponGroupId)) {
      const T = Ju(i, e), k = ((l = F.getSnapshot) == null ? void 0 : l.call(F, i, { token: T })) ?? null, C = k != null && k.isCurrentTurn ? ((c = F.getUsedWeaponGroupIds) == null ? void 0 : c.call(F, i, { token: T, snapshot: k })) ?? [] : [], I = eT(i, n || e.weaponGroupId, {
        usedWeaponGroupIds: C
      });
      if (!(I != null && I.ok))
        throw rt((I == null ? void 0 : I.reason) || "That weapon group cannot attack right now.", {
          severity: "warn"
        });
      return I.profile;
    }
    const b = bI(i, e);
    if (b) return b;
    const S = t === "mechWeapon" ? n : String((e == null ? void 0 : e.weaponId) ?? "").trim(), w = ((d = (u = i.items) == null ? void 0 : u.get) == null ? void 0 : d.call(u, S ?? "")) ?? null;
    if (!w || !iy(w))
      throw new Error("Machine attack requires an owned vehicle or BattleMech weapon.");
    return ((m = w.getCombatProfile) == null ? void 0 : m.call(w)) ?? null;
  }
  const a = t === "personalWeapon" ? n : String((e == null ? void 0 : e.weaponId) ?? "").trim(), r = ((p = (f = i.items) == null ? void 0 : f.get) == null ? void 0 : p.call(f, a ?? "")) ?? null;
  if (!r || !(((g = r.isPersonalWeapon) == null ? void 0 : g.call(r)) ?? r.type === "personalWeapon") || !((h = r.system) != null && h.equipped))
    throw new Error("Attack requires an equipped personal weapon.");
  return ((y = r.getCombatProfile) == null ? void 0 : y.call(r, { payloadId: e == null ? void 0 : e.payloadId })) ?? null;
}
async function AI({ actor: i, payload: e } = {}) {
  var X, H, he, ie, le, ze, Xe, mt, ft, pt, gt, nt, ht, Ye, at, Ct, _t, xt, St, Kt, Ft, R, U, ne, ke, Me, xe, Ze;
  if (!i) throw new Error("resolveAttack requires actor");
  const t = SI(i, e);
  if (!t) throw new Error("Unable to resolve weapon profile.");
  const n = Ci(i) ? await xn({
    machineActor: i,
    operatorActorUuid: String((e == null ? void 0 : e.operatorActorUuid) ?? "").trim()
  }) : null, a = (n == null ? void 0 : n.actor) ?? i, r = Ci(i) ? iI(i) : null, s = uC({
    clusteringDice: Number((t == null ? void 0 : t.clusteringDice) ?? 0) || 0,
    clusteringTargetNumber: Number((t == null ? void 0 : t.clusteringTargetNumber) ?? 5) || 5,
    diceModifier: Number((r == null ? void 0 : r.diceModifier) ?? 0) || 0,
    targetNumberModifier: Number((r == null ? void 0 : r.targetNumberModifier) ?? 0) || 0
  }), o = {
    ...t,
    clusteringDice: s.dice,
    clusteringTargetNumber: s.targetNumber,
    clusteringBaseDice: s.baseDice,
    clusteringBaseTargetNumber: s.baseTargetNumber,
    clusteringModifiers: {
      diceModifier: s.diceModifier,
      targetNumberModifier: s.targetNumberModifier,
      sourceIds: Array.isArray(r == null ? void 0 : r.sourceIds) ? [...r.sourceIds] : [],
      sourceNames: Array.isArray(r == null ? void 0 : r.sourceNames) ? [...r.sourceNames] : [],
      sourceLabel: String((r == null ? void 0 : r.sourceLabel) ?? "").trim()
    }
  };
  if (Ci(i)) {
    const $e = Uw(i, {
      weaponGroupId: e == null ? void 0 : e.weaponGroupId,
      weaponId: e == null ? void 0 : e.weaponId,
      weapon: o
    });
    if ($e.blocked)
      throw rt($e.reason || "That weapon group cannot attack right now.", {
        severity: "warn"
      });
    const Fe = Tw(i, {
      weaponGroupId: e == null ? void 0 : e.weaponGroupId,
      weaponId: e == null ? void 0 : e.weaponId,
      weapon: o
    });
    if (Fe.blocked)
      throw rt(Fe.reason || "The machine's current state prevents this attack.", {
        severity: "warn"
      });
  }
  if (Array.isArray((X = o == null ? void 0 : o.capabilityReport) == null ? void 0 : X.errors) && o.capabilityReport.errors.length > 0)
    throw rt(
      ((H = o.capabilityReport.errors[0]) == null ? void 0 : H.message) ?? "Weapon capability data is invalid for this attack.",
      { severity: "warn" }
    );
  const l = Lt(o.skill) ?? {
    code: o.skill,
    label: o.skill || "Attack",
    attribute: "reflexes",
    domains: ["physical"]
  }, c = String(l.attribute ?? "reflexes").trim() || "reflexes", u = hI(c), d = ((he = a.getAttributeValue) == null ? void 0 : he.call(a, c)) ?? Number(((ze = (le = (ie = a.system) == null ? void 0 : ie.attributes) == null ? void 0 : le[c]) == null ? void 0 : ze.value) ?? 0), m = ((Xe = a.getSkillRating) == null ? void 0 : Xe.call(a, o.skill)) ?? Number(((pt = (ft = (mt = a.system) == null ? void 0 : mt.skills) == null ? void 0 : ft[o.skill]) == null ? void 0 : pt.rating) ?? 0), f = Number(((ht = (nt = (gt = a.system) == null ? void 0 : gt.skills) == null ? void 0 : nt[o.skill]) == null ? void 0 : ht.bonus) ?? 0), p = new Set(co(a.system ?? {}, o.skill)), g = Xc(o.skill, e == null ? void 0 : e.specializationKey), h = g && p.has(g.key) ? g : null, y = h ? Yc : 0, b = Number(((Ye = o == null ? void 0 : o.effects) == null ? void 0 : Ye.accuracyMod) ?? 0) || 0, S = f + b, w = mI(e), T = gI({ actor: i, payload: e, weapon: o, targets: w }), k = (o == null ? void 0 : o.type) === "personalWeapon" || o != null && o.isSynthetic ? So(T) : cg(T), C = Number(((at = o == null ? void 0 : o.attackRatingBand) == null ? void 0 : at[T]) ?? 0) || 0, I = !!((Ct = o == null ? void 0 : o.capabilityReport) != null && Ct.isTemplated), O = (_t = e == null ? void 0 : e.aim) != null && _t.active ? {
    active: !0,
    eligible: !I && w.length === 1,
    ineligibleReason: I ? "Aim cannot apply to template attacks." : w.length !== 1 ? "Aim cannot apply to multi-target attacks." : "",
    skillCode: o.skill,
    skillLabel: l.label ?? o.skill ?? "Attack Skill"
  } : null;
  if (!I && w.length === 0)
    throw rt("Target at least one token to attack.", { severity: "warn" });
  const x = Number(o.ap ?? 0) + Number(((xt = o == null ? void 0 : o.effects) == null ? void 0 : xt.ap) ?? 0), B = e != null && e.attackOptions && typeof e.attackOptions == "object" ? e.attackOptions : {};
  if (Ci(i) && B.losBlocked && !B.indirectAttack)
    throw rt("Line of sight is fully blocked. Use Indirect Attack or sensor-enabled fire.", { severity: "warn" });
  const P = Ci(i) && w.length === 1 ? ey({
    targetActor: fI(w[0]),
    payload: e
  }) : null, L = (o == null ? void 0 : o.type) === "personalWeapon" || o != null && o.isSynthetic ? gT(T, 1) : hT(T, 1), D = Number.isFinite(Number(e == null ? void 0 : e.dn)), G = [{
    id: "difficulty.current",
    label: (o == null ? void 0 : o.type) === "personalWeapon" || o != null && o.isSynthetic ? `Base DN (${k})` : `Base DN (${k})`,
    value: L,
    tags: ["base", "range"]
  }];
  P != null && P.motionDn && G.push({
    id: "machineMotion.actions",
    label: `Target Motion (${P.targetMotionLabel})`,
    value: P.motionDn,
    tags: ["motion"]
  }), P != null && P.jumpDn && G.push({
    id: "machineMotion.jump",
    label: "Target Jumped",
    value: P.jumpDn,
    tags: ["motion", "jump"]
  });
  const V = G.reduce(($e, Fe) => $e + Number(Fe.value ?? 0), 0), Z = D ? Number(e.dn) : V;
  if (Ci(i) && vw(i) && T !== "close")
    throw rt("Sensor Blind limits attacks to Close range.", { severity: "warn" });
  let E = null;
  if (Ci(i) && w.length > 0) {
    const $e = w[0], Fe = String(($e == null ? void 0 : $e.tokenUuid) ?? "").trim(), $t = Ju(i, e), yt = sa($t), vt = (Kt = (St = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : St.get) == null ? void 0 : Kt.call(St, Fe), At = (vt == null ? void 0 : vt.visible) ?? !0 ? Eo(yt, Fe) : "blind";
    if (yt && At === "blind")
      throw rt("No targeting solution. Acquire contact first.", { severity: "warn" });
    const Ht = Number(((U = (R = (Ft = i == null ? void 0 : i.system) == null ? void 0 : Ft.attributes) == null ? void 0 : R.system) == null ? void 0 : U.value) ?? 0) || 0, st = Fg(yt, Fe, Ht, At, (ne = game.combat) == null ? void 0 : ne.round);
    E = {
      detectionState: At,
      detectionStateLabel: vi(At),
      targetTokenUuid: Fe,
      attackerCombatantId: (yt == null ? void 0 : yt.id) ?? null,
      activePacketId: (st == null ? void 0 : st.id) ?? null,
      targetingDataValue: (st == null ? void 0 : st.value) ?? 0
    };
  }
  return {
    intent: "attack",
    rollType: "simple",
    title: `${o.name} Attack`,
    subtitle: i.name ?? "Actor",
    domains: Array.isArray(l.domains) && l.domains.length ? l.domains : ["physical"],
    domainTags: ["combat", "attack"],
    diceTarget: Number.isFinite(Number(e == null ? void 0 : e.diceTarget)) ? Number(e.diceTarget) : 5,
    difficulty: { dn: Z },
    dn: {
      parts: D ? [{
        id: "difficulty.override",
        label: "DN Override",
        value: Z,
        tags: ["override"]
      }] : G,
      total: Z
    },
    edge: {
      earn: { enabled: !0, rate: 4, maxPerRoll: 1 }
    },
    pool: { attribute: d, skill: m, bonus: S, specialization: y },
    breakdown: [
      {
        id: "attribute",
        label: (ke = n == null ? void 0 : n.actor) != null && ke.name ? `${u} (${n.actor.name})` : u,
        value: d
      },
      { id: "skill", label: (Me = n == null ? void 0 : n.actor) != null && Me.name ? `${l.label} (${n.actor.name})` : l.label, value: m },
      { id: "bonus", label: "Skill Bonus", value: f },
      ...h ? [{
        id: "specialization",
        label: `Specialization (${h.label})`,
        value: y
      }] : [],
      { id: "weaponAccuracy", label: "Weapon Accuracy", value: b },
      { id: "damage", label: "Damage", value: Number(o.damage ?? 0) || 0 },
      ...s.active ? [
        { id: "clusterDice", label: "Cluster Dice", value: s.dice },
        { id: "clusterTarget", label: "Cluster TN", value: s.targetNumber }
      ] : [],
      { id: "ap", label: "AP", value: x },
      { id: "attackRating", label: `Attack Rating (${k})`, value: C }
    ],
    attack: {
      rangeBand: T,
      weapon: o,
      payload: (o == null ? void 0 : o.payload) ?? null,
      payloadState: (o == null ? void 0 : o.payloadState) ?? null,
      source: (o == null ? void 0 : o.source) ?? null,
      sourceState: (o == null ? void 0 : o.sourceState) ?? null,
      template: (o == null ? void 0 : o.template) ?? null,
      areaEffect: (o == null ? void 0 : o.areaEffect) ?? null,
      templateGeometry: (e == null ? void 0 : e.templateGeometry) ?? null,
      templatePlacement: (e == null ? void 0 : e.templatePlacement) ?? null,
      resolution: (o == null ? void 0 : o.resolution) ?? null,
      resolverKey: (o == null ? void 0 : o.resolverKey) ?? "standard",
      fireModes: (o == null ? void 0 : o.fireModes) ?? null,
      keywords: (o == null ? void 0 : o.keywords) ?? [],
      capabilityReport: (o == null ? void 0 : o.capabilityReport) ?? null,
      skill: {
        code: l.code ?? o.skill,
        label: l.label ?? o.skill,
        attribute: c,
        specialization: h ? {
          key: h.key,
          label: h.label,
          value: y
        } : null
      },
      targets: w,
      aim: O,
      totalAp: x,
      ewContext: E,
      machineMotion: P,
      operator: n ? {
        actorUuid: ((xe = n.actor) == null ? void 0 : xe.uuid) ?? "",
        name: ((Ze = n.actor) == null ? void 0 : Ze.name) ?? "",
        source: n.source ?? "",
        reason: n.reason ?? ""
      } : null,
      attackOptions: {
        indirectAttack: !!B.indirectAttack,
        losBlocked: !!B.losBlocked
      }
    },
    rollActor: a,
    specialization: h ? {
      key: h.key,
      label: h.label,
      value: y,
      skillKey: l.code ?? o.skill
    } : null
  };
}
async function wI({ actor: i, payload: e, event: t } = {}) {
  return {
    title: "Defense",
    intent: "defense",
    domainTags: ["combat"],
    pool: { attribute: 0, skill: 0, bonus: 0 },
    breakdown: [],
    mods: []
  };
}
async function TI({ actor: i, payload: e, event: t } = {}) {
  return {
    title: "Resistance",
    intent: "resistance",
    domainTags: ["combat"],
    pool: { attribute: 0, skill: 0, bonus: 0 },
    breakdown: [],
    mods: []
  };
}
function kI(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? t : e;
}
function gl(i, e) {
  var t, n, a;
  return Math.max(0, kI((a = (n = (t = i == null ? void 0 : i.system) == null ? void 0 : t.attributes) == null ? void 0 : n[e]) == null ? void 0 : a.value, 0));
}
function MI(i) {
  return (i == null ? void 0 : i.type) === A.actorTypes.vehicle || (i == null ? void 0 : i.type) === A.actorTypes.battlemech;
}
function CI({ machineActor: i = null, pilotActor: e = null } = {}) {
  const t = gl(i, A.actorAttributes.handling), n = gl(i, A.actorAttributes.system), a = gl(e, A.actorAttributes.reflexes), r = n > t ? A.actorAttributes.system : A.actorAttributes.handling, s = n > t ? "System" : "Handling", o = Math.max(t, n);
  return {
    handling: t,
    system: n,
    machineAttributeKey: r,
    machineAttributeLabel: s,
    machineAttributeValue: o,
    pilotReflexes: a,
    totalBonus: o + a
  };
}
async function vI({ actor: i } = {}) {
  var n, a, r, s, o, l, c;
  if (MI(i)) {
    const u = await xn({ machineActor: i }), d = CI({
      machineActor: i,
      pilotActor: u.actor
    }), m = (n = u.actor) != null && n.name ? `Pilot REF (${u.actor.name})` : "Pilot REF";
    return {
      intent: "initiative",
      title: "Initiative",
      rollType: "sum",
      domains: ["combat", "machine"],
      sum: {
        formula: "2d6 + @machine + @pilotReflexes",
        data: {
          machine: d.machineAttributeValue,
          pilotReflexes: d.pilotReflexes
        }
      },
      breakdown: [
        { id: "base", label: "2d6", value: 0 },
        {
          id: `machine.${d.machineAttributeKey}`,
          label: `Best Machine (${d.machineAttributeLabel})`,
          value: d.machineAttributeValue
        },
        {
          id: "pilot.reflexes",
          label: m,
          value: d.pilotReflexes,
          title: u.reason
        }
      ],
      pool: { attribute: 0, skill: 0, bonus: 0 }
    };
  }
  const e = Number(((s = (r = (a = i.system) == null ? void 0 : a.attributes) == null ? void 0 : r.reflexes) == null ? void 0 : s.value) ?? 0), t = Number(((c = (l = (o = i.system) == null ? void 0 : o.attributes) == null ? void 0 : l.edge) == null ? void 0 : c.value) ?? 0);
  return {
    intent: "initiative",
    title: "Initiative",
    rollType: "sum",
    // <- tells executor how to roll
    domains: ["combat"],
    // <- for organizational/filtering purposes
    sum: {
      formula: "2d6 + @ref + @edge",
      data: { ref: e, edge: t }
    },
    breakdown: [
      { id: "base", label: "2d6", value: 0 },
      { id: "ref", label: "REF", value: e },
      { id: "edge", label: "EDGE", value: t }
    ],
    // keep pool numeric to satisfy normalizeResolvedContext (if still used)
    pool: { attribute: 0, skill: 0, bonus: 0 }
  };
}
async function EI({ actor: i }) {
  var n, a, r, s, o;
  const e = Number(((a = (n = i.system) == null ? void 0 : n.burn) == null ? void 0 : a.value) ?? 0);
  e < 6 && ui.notifications.warn("Overload check is only required at Burn 6+.");
  const t = Number(((o = (s = (r = i.system) == null ? void 0 : r.attributes) == null ? void 0 : s.willpower) == null ? void 0 : o.value) ?? 0);
  return {
    intent: "overload",
    title: "Overload Check",
    domains: ["mental"],
    pool: {
      attribute: t,
      skill: t,
      bonus: 0
    },
    difficulty: {
      dn: Math.max(0, e - 5)
    },
    breakdown: [
      { id: "will1", label: "Will", value: t },
      { id: "will2", label: "Will", value: t }
    ]
  };
}
function Lc(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? t : e;
}
function II(i = null, e = "") {
  var t, n, a, r, s, o;
  return {
    rating: Lc((a = (n = (t = i == null ? void 0 : i.system) == null ? void 0 : t.skills) == null ? void 0 : n[e]) == null ? void 0 : a.rating, 0),
    bonus: Lc((o = (s = (r = i == null ? void 0 : i.system) == null ? void 0 : r.skills) == null ? void 0 : s[e]) == null ? void 0 : o.bonus, 0)
  };
}
function zm(i = "", e = null) {
  const t = String(i ?? "").trim(), n = String((e == null ? void 0 : e.name) ?? "").trim();
  return n ? `${t} (${n})` : t;
}
async function NI({ actor: i, payload: e } = {}) {
  var l, c, u, d, m, f, p, g, h;
  const t = await No(e, {
    gmOverride: !!(e != null && e.gmOverride)
  });
  if (!t.ok)
    throw new Error(t.reason ?? "Machine remedy could not be prepared.");
  const n = await Io(e, {
    gmOverride: !!(e != null && e.gmOverride)
  });
  if (!n.ok)
    throw new Error(n.reason ?? "Machine remedy could not be resolved.");
  const a = Lt(n.skillKey);
  if (!a)
    throw new Error(`Machine remedy is missing a valid skill: ${n.skillKey}`);
  const r = t.actor ?? i ?? n.operatorActor ?? n.machineActor, s = Lc((d = (u = (c = (l = n.machineActor) == null ? void 0 : l.system) == null ? void 0 : c.attributes) == null ? void 0 : u.reliability) == null ? void 0 : d.value, 0), o = II(r, n.skillKey);
  return {
    intent: "machineRemedy",
    rollType: "simple",
    title: `${n.remedy.label}: ${n.crit.label ?? "Critical Remedy"}`,
    subtitle: `${((m = n.machineActor) == null ? void 0 : m.name) ?? "Machine"} | ${n.crit.locationLabel ?? n.locationKey}`,
    domains: ["mental"],
    diceTarget: 5,
    difficulty: { dn: n.totalDn },
    pool: {
      attribute: s,
      skill: o.rating,
      bonus: o.bonus,
      specialization: 0
    },
    breakdown: [
      { id: "attribute", label: zm("Reliability", n.machineActor), value: s },
      { id: "skill", label: zm(a.label, n.operatorActor), value: o.rating },
      ...o.bonus ? [{ id: "bonus", label: "Skill Bonus", value: o.bonus }] : []
    ],
    specialization: null,
    machineRemedy: {
      machineActorUuid: ((f = n.machineActor) == null ? void 0 : f.uuid) ?? "",
      operatorActorUuid: ((p = n.operatorActor) == null ? void 0 : p.uuid) ?? "",
      critId: n.crit.id,
      critLabel: n.crit.label ?? "",
      locationKey: n.locationKey,
      locationLabel: n.crit.locationLabel ?? n.locationKey,
      remedyKey: n.remedy.key,
      remedyLabel: n.remedy.label,
      skillKey: n.skillKey,
      skillLabel: a.label,
      baseDn: n.baseDn,
      conditionModifier: n.locationConditionModifier,
      conditionLabel: n.locationConditionLabel,
      conditionValue: n.locationCondition,
      totalDn: n.totalDn,
      gmOverride: n.gmOverride,
      remedyEffect: n.remedyEffect,
      cost: n.remedy.cost
    },
    data: {
      skillKey: n.skillKey,
      attrKey: "reliability",
      machineActorUuid: ((g = n.machineActor) == null ? void 0 : g.uuid) ?? "",
      operatorActorUuid: ((h = n.operatorActor) == null ? void 0 : h.uuid) ?? "",
      label: `reliability+${a.label}`
    },
    rollActor: r,
    machineActor: n.machineActor
  };
}
function RI(i) {
  return (i == null ? void 0 : i.type) === A.actorTypes.vehicle || (i == null ? void 0 : i.type) === A.actorTypes.battlemech;
}
function PI(i) {
  var n, a, r, s, o;
  const e = String((i == null ? void 0 : i.targetTokenUuid) ?? "").trim();
  if (e)
    return ((a = (n = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : n.placeables) == null ? void 0 : a.find((l) => {
      var c;
      return (((c = l.document) == null ? void 0 : c.uuid) ?? l.uuid) === e;
    })) ?? null;
  const t = String((i == null ? void 0 : i.targetTokenId) ?? "").trim();
  return t ? ((s = (r = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : r.get) == null ? void 0 : s.call(r, t)) ?? null : Array.from(((o = game.user) == null ? void 0 : o.targets) ?? []).find((l) => l.actor) ?? null;
}
function DI(i, e) {
  var n, a, r, s, o, l, c, u;
  const t = String((e == null ? void 0 : e.sourceTokenId) ?? "").trim();
  return t ? ((a = (n = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : n.get) == null ? void 0 : a.call(n, t)) ?? ((s = (r = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : r.placeables) == null ? void 0 : s.find((d) => d.id === t)) ?? null : ((l = (o = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : o.controlled) == null ? void 0 : l.find((d) => {
    var m;
    return ((m = d.actor) == null ? void 0 : m.id) === (i == null ? void 0 : i.id);
  })) ?? ((u = (c = i == null ? void 0 : i.getActiveTokens) == null ? void 0 : c.call(i, !0, !0)) == null ? void 0 : u[0]) ?? null;
}
function OI(i, e = "Target") {
  var t;
  return String((i == null ? void 0 : i.name) ?? ((t = i == null ? void 0 : i.actor) == null ? void 0 : t.name) ?? e).trim() || e;
}
function Fm(i = "", e = null) {
  const t = String(i ?? "").trim(), n = String((e == null ? void 0 : e.name) ?? "").trim();
  return n ? `${t} (${n})` : t;
}
async function Hm({ actor: i, payload: e } = {}) {
  var T, k, C, I, O, x, B, P, L, D, G, V, Z, E, X;
  if (!i) throw new Error("resolveAcquire requires actor");
  if (!RI(i))
    throw rt("Acquire is a machine action.", { severity: "warn" });
  if (kw(i))
    throw rt("Sensor actions are blocked by the machine's current state.", { severity: "warn" });
  const t = PI(e);
  if (!(t != null && t.actor))
    throw rt("Target a token to acquire.", { severity: "warn" });
  const n = t.actor, a = ((T = t.document) == null ? void 0 : T.uuid) ?? t.uuid ?? "", r = OI(t), s = DI(i, e), o = sa(s), l = Eo(o, a);
  if (l === "lock")
    throw rt("Target is already at lock state.", { severity: "info" });
  const c = zg(n);
  if (l === "track" && c === "track")
    throw rt("ECM prevents advancing beyond Track on this target.", { severity: "warn" });
  const u = await xn({
    machineActor: i,
    operatorActorUuid: String((e == null ? void 0 : e.operatorActorUuid) ?? "").trim()
  }), d = u.actor ?? i, m = "system", f = "perception", p = Math.max(0, Number(((I = (C = (k = i == null ? void 0 : i.system) == null ? void 0 : k.attributes) == null ? void 0 : C[m]) == null ? void 0 : I.value) ?? 0) || 0), g = Lt("perception"), h = Number(((B = (x = (O = d == null ? void 0 : d.system) == null ? void 0 : O.skills) == null ? void 0 : x[f]) == null ? void 0 : B.rating) ?? 0) || 0, y = Number(((D = (L = (P = d == null ? void 0 : d.system) == null ? void 0 : P.skills) == null ? void 0 : L[f]) == null ? void 0 : D.bonus) ?? 0) || 0, b = tk(l), S = mk(n), w = b + S;
  return {
    intent: "acquire",
    rollType: "simple",
    title: "Acquire Target",
    subtitle: r,
    domains: ["mental"],
    diceTarget: 5,
    difficulty: { dn: w },
    dn: {
      parts: [
        { id: "difficulty.base", label: "Base DN", value: b, tags: ["base"] },
        ...S ? [{ id: "ew.ecmDn", label: "ECM Modifier", value: S, tags: ["ew"] }] : []
      ],
      total: w
    },
    edge: { earn: { enabled: !0, rate: 4, maxPerRoll: 1 } },
    pool: {
      attribute: p,
      skill: h,
      bonus: y,
      specialization: 0
    },
    breakdown: [
      { id: "attribute", label: Fm("System", i), value: p },
      { id: "skill", label: Fm((g == null ? void 0 : g.label) ?? "Perception", u.actor), value: h },
      ...y ? [{ id: "bonus", label: "Perception Bonus", value: y }] : []
    ],
    specialization: null,
    data: {
      skillKey: f,
      attrKey: m,
      machineActorUuid: i.uuid ?? "",
      operatorActorUuid: ((G = u.actor) == null ? void 0 : G.uuid) ?? "",
      label: `${m}+${(g == null ? void 0 : g.label) ?? "Perception"}`
    },
    acquire: {
      machineActorUuid: i.uuid ?? "",
      operatorActorUuid: ((V = u.actor) == null ? void 0 : V.uuid) ?? "",
      operatorName: ((Z = u.actor) == null ? void 0 : Z.name) ?? "",
      attackerTokenId: (s == null ? void 0 : s.id) ?? ((E = s == null ? void 0 : s.document) == null ? void 0 : E.id) ?? "",
      attackerTokenUuid: ((X = s == null ? void 0 : s.document) == null ? void 0 : X.uuid) ?? (s == null ? void 0 : s.uuid) ?? "",
      attackerCombatantId: (o == null ? void 0 : o.id) ?? "",
      targetTokenUuid: a,
      targetTokenId: (t == null ? void 0 : t.id) ?? "",
      targetName: r,
      currentState: l,
      currentStateLabel: vi(l),
      ceiling: c
    },
    rollActor: d,
    machineActor: i
  };
}
function LI(i) {
  return (i == null ? void 0 : i.type) === A.actorTypes.vehicle || (i == null ? void 0 : i.type) === A.actorTypes.battlemech;
}
function _I(i) {
  var n, a, r, s, o;
  const e = String((i == null ? void 0 : i.targetTokenUuid) ?? "").trim();
  if (e)
    return ((a = (n = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : n.placeables) == null ? void 0 : a.find((l) => {
      var c;
      return (((c = l.document) == null ? void 0 : c.uuid) ?? l.uuid) === e;
    })) ?? null;
  const t = String((i == null ? void 0 : i.targetTokenId) ?? "").trim();
  return t ? ((s = (r = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : r.get) == null ? void 0 : s.call(r, t)) ?? null : Array.from(((o = game.user) == null ? void 0 : o.targets) ?? []).find((l) => l.actor) ?? null;
}
function xI(i, e) {
  var n, a, r, s, o, l, c, u;
  const t = String((e == null ? void 0 : e.sourceTokenId) ?? "").trim();
  return t ? ((a = (n = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : n.get) == null ? void 0 : a.call(n, t)) ?? ((s = (r = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : r.placeables) == null ? void 0 : s.find((d) => d.id === t)) ?? null : ((l = (o = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : o.controlled) == null ? void 0 : l.find((d) => {
    var m;
    return ((m = d.actor) == null ? void 0 : m.id) === (i == null ? void 0 : i.id);
  })) ?? ((u = (c = i == null ? void 0 : i.getActiveTokens) == null ? void 0 : c.call(i, !0, !0)) == null ? void 0 : u[0]) ?? null;
}
function $I(i, e = "Target") {
  var t;
  return String((i == null ? void 0 : i.name) ?? ((t = i == null ? void 0 : i.actor) == null ? void 0 : t.name) ?? e).trim() || e;
}
function Um(i = "", e = null) {
  const t = String(i ?? "").trim(), n = String((e == null ? void 0 : e.name) ?? "").trim();
  return n ? `${t} (${n})` : t;
}
const hl = 2;
async function jm({ actor: i, payload: e } = {}) {
  var y, b, S, w, T, k, C, I, O, x, B, P, L, D, G;
  if (!i) throw new Error("resolveTargeting requires actor");
  if (!LI(i))
    throw rt("Targeting is a machine action.", { severity: "warn" });
  if (Mw(i))
    throw rt("Targeting Data cannot be generated in the machine's current state.", { severity: "warn" });
  const t = _I(e);
  if (!(t != null && t.actor))
    throw rt("Target a token to generate targeting data.", { severity: "warn" });
  const n = ((y = t.document) == null ? void 0 : y.uuid) ?? t.uuid ?? "", a = $I(t), r = xI(i, e), s = sa(r), o = Eo(s, n);
  if (o !== "track" && o !== "lock")
    throw rt("Track or Lock is required to generate targeting data.", { severity: "warn" });
  const l = await xn({
    machineActor: i,
    operatorActorUuid: String((e == null ? void 0 : e.operatorActorUuid) ?? "").trim()
  }), c = l.actor ?? i, u = "system", d = "gunnery", m = Math.max(0, Number(((w = (S = (b = i == null ? void 0 : i.system) == null ? void 0 : b.attributes) == null ? void 0 : S[u]) == null ? void 0 : w.value) ?? 0) || 0), f = Lt(d), p = Number(((C = (k = (T = c == null ? void 0 : c.system) == null ? void 0 : T.skills) == null ? void 0 : k[d]) == null ? void 0 : C.rating) ?? 0) || 0, g = Number(((x = (O = (I = c == null ? void 0 : c.system) == null ? void 0 : I.skills) == null ? void 0 : O[d]) == null ? void 0 : x.bonus) ?? 0) || 0, h = Pu(m, o);
  return {
    intent: "targeting",
    rollType: "simple",
    title: "Generate Targeting Data",
    subtitle: a,
    domains: ["mental"],
    diceTarget: 5,
    difficulty: { dn: hl },
    dn: {
      parts: [{ id: "difficulty.base", label: "DN (Targeting)", value: hl, tags: ["base"] }],
      total: hl
    },
    edge: { earn: { enabled: !0, rate: 4, maxPerRoll: 1 } },
    pool: {
      attribute: m,
      skill: p,
      bonus: g,
      specialization: 0
    },
    breakdown: [
      { id: "attribute", label: Um("System", i), value: m },
      { id: "skill", label: Um((f == null ? void 0 : f.label) ?? "Gunnery", l.actor), value: p },
      ...g ? [{ id: "bonus", label: "Gunnery Bonus", value: g }] : []
    ],
    specialization: null,
    data: {
      skillKey: d,
      attrKey: u,
      machineActorUuid: i.uuid ?? "",
      operatorActorUuid: ((B = l.actor) == null ? void 0 : B.uuid) ?? "",
      label: `${u}+${(f == null ? void 0 : f.label) ?? "Gunnery"}`
    },
    targeting: {
      machineActorUuid: i.uuid ?? "",
      operatorActorUuid: ((P = l.actor) == null ? void 0 : P.uuid) ?? "",
      operatorName: ((L = l.actor) == null ? void 0 : L.name) ?? "",
      attackerTokenId: (r == null ? void 0 : r.id) ?? ((D = r == null ? void 0 : r.document) == null ? void 0 : D.id) ?? "",
      attackerTokenUuid: ((G = r == null ? void 0 : r.document) == null ? void 0 : G.uuid) ?? (r == null ? void 0 : r.uuid) ?? "",
      attackerCombatantId: (s == null ? void 0 : s.id) ?? "",
      targetTokenUuid: n,
      targetTokenId: (t == null ? void 0 : t.id) ?? "",
      targetName: a,
      detectionState: o,
      detectionStateLabel: vi(o),
      cap: h
    },
    rollActor: c,
    machineActor: i
  };
}
function BI(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? t : e;
}
function Fi(i, e = 0) {
  return Math.max(e, BI(i, e));
}
function zI(i = "") {
  const e = String(i ?? "").trim().toLowerCase();
  return e === "shutdown" || e === "explosion" ? e : "";
}
function FI(i, e) {
  const t = (e == null ? void 0 : e.dangerChecks) ?? {};
  return i === "shutdown" ? {
    title: "Shutdown Check",
    pool: Fi(t.shutdownPool, 0),
    dn: Fi(t.shutdownDN, 1),
    formula: "Chassis + Reliability",
    successText: "The BattleMech stays online.",
    failureText: "On failure, compare the margin to the pilot's System Operations for an override; otherwise the BattleMech shuts down."
  } : {
    title: "Explosion Check",
    pool: Fi(t.explosionPool, 1),
    dn: Fi(t.explosionDN, 1),
    formula: "Chassis + Reliability - Danger",
    successText: "Volatile systems remain contained.",
    failureText: "Failure detonates volatile components."
  };
}
function HI(i = null, e = "") {
  var r, s, o, l;
  const t = String(e ?? "").trim();
  if (!i || !t) return null;
  const n = (r = i.getSkillRating) == null ? void 0 : r.call(i, t);
  if (Number.isFinite(Number(n))) return Fi(n, 0);
  const a = (l = (o = (s = i.system) == null ? void 0 : s.skills) == null ? void 0 : o[t]) == null ? void 0 : l.rating;
  return Number.isFinite(Number(a)) ? Fi(a, 0) : null;
}
async function UI({ actor: i, payload: e } = {}) {
  var p, g, h, y, b, S, w;
  if (!i) throw new Error("resolveHeatDangerCheck requires actor");
  if (i.type !== "battlemech") throw new Error("Heat danger checks require a BattleMech actor");
  const t = zI((e == null ? void 0 : e.checkKind) ?? (e == null ? void 0 : e.kind));
  if (!t) throw new Error("Heat danger checks require payload.checkKind of shutdown or explosion");
  const n = an(i);
  if (!n.inDanger || !n.dangerChecks)
    throw new Error("Heat danger checks are only available while the BattleMech is in Danger heat");
  const a = await xn({
    machineActor: i,
    operatorActorUuid: e == null ? void 0 : e.operatorActorUuid
  }), r = (a == null ? void 0 : a.actor) ?? null, s = HI(r, "systemOps"), o = FI(t, n), l = Number.isFinite(Number(e == null ? void 0 : e.dn)) ? Fi(e.dn, 1) : o.dn, c = Fi((h = (g = (p = i.system) == null ? void 0 : p.attributes) == null ? void 0 : g.chassis) == null ? void 0 : h.value, 0), u = Fi((S = (b = (y = i.system) == null ? void 0 : y.attributes) == null ? void 0 : b.reliability) == null ? void 0 : S.value, 0), d = Fi((w = n.penalties) == null ? void 0 : w.dangerLevel, 0), m = t === "explosion" ? -d : 0, f = t === "explosion" ? o.pool - (c + u + m) : 0;
  return {
    intent: "heatDangerCheck",
    rollType: "simple",
    title: o.title,
    subtitle: i.name ?? "BattleMech",
    domains: ["machine", "heat"],
    tags: ["machine", "heat", "danger", t],
    formula: o.formula,
    machineActor: i,
    difficulty: { dn: l },
    dn: {
      parts: [
        {
          id: `heat.${t}.dn`,
          label: t === "shutdown" ? "Danger Level" : "Explosion DN",
          value: l,
          tags: ["heat", "danger"]
        }
      ],
      total: l
    },
    edge: { earn: { enabled: !1 } },
    pool: {
      attribute: c,
      skill: u,
      bonus: m,
      specialization: f
    },
    breakdown: [
      { id: "attribute", label: "Chassis", value: c },
      { id: "skill", label: "Reliability", value: u },
      ...m ? [{ id: "bonus", label: `Danger Level ${d}`, value: m }] : [],
      ...f ? [{ id: "specialization", label: "Minimum Pool", value: f }] : []
    ],
    heatDangerCheck: {
      kind: t,
      label: o.title,
      dangerLevel: d,
      volatile: !!n.volatile,
      pool: o.pool,
      dn: l,
      operatorName: (r == null ? void 0 : r.name) ?? "",
      operatorActorUuid: (a == null ? void 0 : a.uuid) ?? (r == null ? void 0 : r.uuid) ?? "",
      operatorSource: (a == null ? void 0 : a.source) ?? "",
      operatorReason: (a == null ? void 0 : a.reason) ?? "",
      systemOpsRating: s,
      successText: o.successText,
      failureText: o.failureText
    },
    data: {
      checkKind: t,
      dangerLevel: d,
      volatile: !!n.volatile
    }
  };
}
const jI = {
  skill: AE,
  edge: kE,
  attribute: ME,
  common: CE,
  attack: AI,
  defense: wI,
  resistance: TI,
  initiative: vI,
  overload: EI,
  machineRemedy: NI,
  heatDangerCheck: UI,
  acquire: Hm,
  acquireTarget: Hm,
  targeting: jm,
  generateFireSolution: jm
};
async function yl({ actor: i, payload: e, event: t } = {}) {
  if (!i) throw new Error("resolveIntent requires actor");
  const n = String((e == null ? void 0 : e.intent) ?? "").trim();
  if (!n) throw new Error("resolveIntent requires payload.intent");
  const a = jI[n];
  if (!a) throw new Error(`Unsupported roll intent: ${n}`);
  const r = await a({ actor: i, payload: e, event: t });
  return KI(r, { intent: n });
}
function KI(i, { intent: e } = {}) {
  (!i || typeof i != "object") && (i = {}), i.intent = i.intent ?? e ?? "unknown", i.title = String(i.title ?? "Roll"), i.domains = Array.isArray(i.domains) ? i.domains : [], i.breakdown = Array.isArray(i.breakdown) ? i.breakdown : [], i.mods = Array.isArray(i.mods) ? i.mods : [];
  const t = i.pool && typeof i.pool == "object" ? i.pool : {}, n = Number(t.attribute ?? t.base ?? 0), a = Number(t.skill ?? t.rating ?? 0), r = Number(t.bonus ?? 0), s = Number(t.specialization ?? 0);
  if (![n, a, r, s].every(Number.isFinite))
    throw console.error("MWD | Invalid pool parts after intent resolution", { intent: e, ctx: i }), new Error("MWD.roll: pool parts must be numeric (attribute/skill/bonus/specialization).");
  return i.pool = {
    attribute: n,
    skill: a,
    bonus: r,
    specialization: s,
    totalBase: n + a + r + s
  }, i.rollType = i.rollType ?? "simple", i.diceTarget = Number.isFinite(i.diceTarget) ? i.diceTarget : Number(i.target ?? 5), i.difficulty && typeof i.difficulty == "object" ? i.difficulty.dn = Number(i.difficulty.dn ?? 0) : Number.isFinite(i.dn) && (i.difficulty = { dn: Number(i.dn) }), i.breakdown.length || (i.breakdown = [
    { id: "attribute", label: "Attribute", value: n },
    { id: "skill", label: "Skill", value: a },
    { id: "bonus", label: "Bonus", value: r },
    ...s ? [{ id: "specialization", label: "Specialization", value: s }] : []
  ]), i;
}
var La;
class GI {
  constructor() {
    Ce(this, La, /* @__PURE__ */ new Map());
  }
  register(e) {
    if (!(e != null && e.id) || typeof e.collect != "function")
      throw new Error("Invalid ModifierProvider: missing id or collect()");
    Y(this, La).has(e.id) || Y(this, La).set(e.id, e);
  }
  async collectAll(e) {
    const t = [];
    for (const n of Y(this, La).values()) {
      const a = await n.collect(e);
      if (a != null && a.length)
        for (const r of a)
          r && typeof r.label == "string" && typeof r.value == "number" && typeof r.source == "string" ? t.push(r) : console.warn("MWD | DROPPED MOD (bad shape)", n.id, r);
    }
    return t;
  }
}
La = new WeakMap();
const ri = new GI();
function WI(i) {
  if (i == null || i === "" || i === "—" || i === "–") return 0;
  const e = Number(i);
  return Number.isFinite(e) ? e : null;
}
function qI(i) {
  const e = WI(i == null ? void 0 : i.value);
  return e === null ? null : { ...i, value: e };
}
async function Km({
  actor: i,
  rollActor: e = null,
  machineActor: t = null,
  rollType: n,
  skillId: a,
  domains: r,
  // NEW (optional)
  payload: s,
  resolved: o,
  context: l
} = {}) {
  const c = { actor: i, rollActor: e, machineActor: t, rollType: n, skillId: a, domains: r, payload: s, resolved: o, context: l }, u = await ri.collectAll(c);
  let d = [];
  for (const f of u ?? []) {
    const p = qI(f);
    if (!p) {
      console.warn("MWD | Dropping invalid modifier value", f);
      continue;
    }
    d.push(p);
  }
  Array.isArray(r) && r.length && (d = d.filter((f) => !f.domain || r.includes(f.domain)));
  const m = d.reduce((f, p) => f + p.value, 0);
  return { mods: d, total: m };
}
function VI({
  actor: i,
  payload: e,
  ctx: t,
  roll: n,
  target: a,
  pool: r,
  mods: s = [],
  modTotal: o = 0,
  hits: l = null,
  ones: c = null,
  edge: u = null,
  outcomeModel: d = null
} = {}) {
  var O, x, B, P;
  if (!i) throw new Error("buildResolved requires actor");
  if (!(e != null && e.intent)) throw new Error("buildResolved requires payload.intent");
  if (!n) throw new Error("buildResolved requires roll");
  const m = foundry.utils.randomID(), f = (O = n.dice) == null ? void 0 : O[0], g = (Array.isArray(f == null ? void 0 : f.results) ? f.results : []).map((L, D) => {
    const G = `pool:${D}`, V = Number(L.result), Z = !!L.success;
    return {
      ref: G,
      face: V,
      isSuccess: Z,
      isFailure: !Z,
      tooltip: Z ? `Die ${D + 1}: ${V} (Success vs TN ${Number(a ?? 5)})` : `Die ${D + 1}: ${V} (Failure vs TN ${Number(a ?? 5)})`
    };
  }), h = g.filter((L) => L.isFailure).map((L) => L.ref), y = [{
    id: "pool",
    label: "Pool",
    faces: 6,
    termIndex: 0,
    // informational only
    dice: g
  }], b = (Array.isArray(s) ? s : []).map((L, D) => {
    const G = Number(L.value ?? 0), V = `mod:${JI(L.label ?? "mod")}:${D}`;
    return {
      id: L.id ?? V,
      label: L.label ?? "Modifier",
      value: G,
      domain: L.domain ?? null,
      source: L.source ?? null,
      tooltip: L.tooltip ?? `${L.label ?? "Modifier"} ${Gm(G)}`
    };
  }), S = b.map((L) => L.id), T = (Array.isArray(t == null ? void 0 : t.breakdown) ? t.breakdown : []).map((L) => ({
    id: `pool.${L.id ?? foundry.utils.randomID()}`,
    label: L.label ?? L.id ?? "Row",
    value: Number(L.value ?? 0),
    tooltip: `Contribution from ${L.label ?? L.id}: ${Number(L.value ?? 0)}`
  }));
  T.push({
    id: "mods.total",
    label: "Mods",
    value: Number(o ?? 0),
    modIds: S,
    tooltip: b.length ? b.map((L) => `${L.label}: ${Gm(L.value)}`).join(`
`) : "No roll-time modifiers."
  }), T.push({
    id: "pool.final",
    label: "Final Pool",
    value: Number(r ?? 0),
    tooltip: `Final dice pool rolled: ${Number(r ?? 0)}d6`
  });
  const k = Number.isFinite(Number(l)) ? Number(l) : g.filter((L) => L.isSuccess).length, C = Number.isFinite(Number(c)) ? Number(c) : g.filter((L) => L.face === 1).length, I = YI(u, { payload: e });
  return {
    version: 2,
    id: m,
    actorUuid: i.uuid,
    // Re-entry
    originPayload: e,
    // Render header
    title: (t == null ? void 0 : t.title) ?? "Roll",
    subtitle: (t == null ? void 0 : t.subtitle) ?? i.name ?? "Actor",
    formula: String((t == null ? void 0 : t.formula) ?? "").trim(),
    intent: (t == null ? void 0 : t.intent) ?? e.intent,
    domains: Array.isArray(t == null ? void 0 : t.domains) ? t.domains : [],
    attack: (t == null ? void 0 : t.attack) ?? null,
    machineRemedy: (t == null ? void 0 : t.machineRemedy) ?? null,
    heatDangerCheck: (t == null ? void 0 : t.heatDangerCheck) ?? null,
    specialization: (t == null ? void 0 : t.specialization) ?? null,
    dn: (t == null ? void 0 : t.dn) ?? (((x = t == null ? void 0 : t.difficulty) == null ? void 0 : x.dn) !== void 0 ? {
      parts: [{
        id: "difficulty.current",
        label: "DN",
        value: Number(t.difficulty.dn ?? 0),
        tags: ["manual"]
      }],
      total: Number(t.difficulty.dn ?? 0)
    } : null),
    // Minimal context snapshot so chat-actions can recompute interpretation
    // after post-spend rerolls mutate hits.
    ctxSnapshot: {
      rollType: (t == null ? void 0 : t.rollType) ?? "simple",
      difficulty: (t == null ? void 0 : t.difficulty) ?? null,
      dn: (t == null ? void 0 : t.dn) ?? null,
      opposed: (t == null ? void 0 : t.opposed) ?? null,
      net: (t == null ? void 0 : t.net) ?? null,
      edge: {
        pool: ((B = t == null ? void 0 : t.edge) == null ? void 0 : B.pool) ?? null,
        earn: ((P = t == null ? void 0 : t.edge) == null ? void 0 : P.earn) ?? null
      }
    },
    // Roll + dice
    roll: {
      json: n.toJSON(),
      formula: n.formula,
      target: Number(a ?? 5),
      pool: Number(r ?? 0),
      diceGroups: y,
      failureDiceRefs: h
    },
    // Outcome numbers
    outcome: {
      hits: k,
      ones: C
    },
    // New: engine-interpreted outcome (margin/net/converted/edgeEarned/etc
    outcomeModel: d,
    // Breakdown + modifiers
    breakdownRows: T,
    modifiers: {
      applied: b,
      total: Number(o ?? 0)
    },
    areaEffectPreviewState: foundry.utils.deepClone((e == null ? void 0 : e.areaEffectPreviewState) ?? {}),
    // Edge snapshot / affordances
    edge: I
  };
}
function YI(i, { payload: e } = {}) {
  var p, g, h, y, b, S, w, T, k, C, I, O, x, B;
  const t = !!((p = e == null ? void 0 : e.edge) != null && p.enabled), n = (i == null ? void 0 : i.domain) ?? null, a = (i == null ? void 0 : i.pools) ?? null, r = ((g = i == null ? void 0 : i.pre) == null ? void 0 : g.poolKey) ?? ((y = (h = e == null ? void 0 : e.edge) == null ? void 0 : h.pre) == null ? void 0 : y.poolKey) ?? (t ? ((b = e == null ? void 0 : e.edge) == null ? void 0 : b.poolKey) ?? null : null), s = Number(((S = i == null ? void 0 : i.pre) == null ? void 0 : S.spent) ?? ((T = (w = e == null ? void 0 : e.edge) == null ? void 0 : w.pre) == null ? void 0 : T.spent) ?? (t ? 1 : 0)) ? 1 : 0, o = ((k = i == null ? void 0 : i.post) == null ? void 0 : k.poolKey) ?? ((I = (C = e == null ? void 0 : e.edge) == null ? void 0 : C.post) == null ? void 0 : I.poolKey) ?? null, l = Number(((O = i == null ? void 0 : i.post) == null ? void 0 : O.spent) ?? ((B = (x = e == null ? void 0 : e.edge) == null ? void 0 : x.post) == null ? void 0 : B.spent) ?? 0) ? 1 : 0, c = (a == null ? void 0 : a.a) ?? null, u = (a == null ? void 0 : a.b) ?? null, d = [c, u].filter(Boolean);
  let m = [c, u].filter(Boolean);
  s && r && (m = m.filter((P) => P !== r));
  const f = {
    canSpendPre: d.length > 0 && !s,
    // spending pre after roll is not a thing
    canSpendPost: m.length > 0 && !l,
    canPostRerollFailures: m.length > 0 && !l
  };
  return {
    domain: n,
    pools: a ? { a: c, b: u } : null,
    pre: { poolKey: r, spent: s },
    post: { poolKey: o, spent: l },
    allowed: {
      prePools: d,
      postPools: m
    },
    availableActions: f
  };
}
function Gm(i) {
  const e = Number(i ?? 0);
  return e >= 0 ? `+${e}` : `${e}`;
}
function JI(i) {
  return String(i).toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, "");
}
const { ApplicationV2: QI, HandlebarsApplicationMixin: XI } = foundry.applications.api, ZI = Object.freeze([
  Object.freeze({ id: "attackOption.lightObscuring", label: "Light obscuring terrain", value: -1 }),
  Object.freeze({ id: "attackOption.heavyObscuring", label: "Heavy obscuring terrain", value: -2 }),
  Object.freeze({ id: "attackOption.multipleObscurants", label: "Multiple/intervening obscurants", value: -3 }),
  Object.freeze({ id: "attackOption.sensorHaze", label: "Smoke / ECM / sensor haze", value: -1 }),
  Object.freeze({ id: "attackOption.advantageousPosition", label: "Advantageous Position", value: 1 })
]);
function eN(i, e = -3, t = 3) {
  const n = [], a = "../img/dice";
  for (let r = e; r <= t; r++) {
    const s = Math.abs(r), o = s === 0 ? `${a}/BlankDice.webp` : `${a}/D6_${s}.svg`;
    n.push({
      value: r,
      abs: s,
      icon: o,
      active: r === i,
      neg: r < 0,
      pos: r > 0,
      zero: r === 0,
      title: r === 0 ? "0 (neutral)" : r < 0 ? `${r} penalty` : `+${r} bonus`
    });
  }
  return n;
}
function Wm(i) {
  return (Array.isArray(i) ? i : []).map((t) => ({
    id: (t == null ? void 0 : t.id) ?? foundry.utils.randomID(),
    label: typeof (t == null ? void 0 : t.label) == "string" ? t.label : "Manual",
    value: Number((t == null ? void 0 : t.value) ?? 0)
  }));
}
function ny(i = {}) {
  return i.attackOptions && typeof i.attackOptions == "object" ? i.attackOptions : {};
}
function _c(i = {}, e = {}) {
  i.attackOptions = {
    ...ny(i),
    ...e
  };
}
function qm(i = {}, e = {}) {
  i.machineMotion = {
    ...i.machineMotion && typeof i.machineMotion == "object" ? i.machineMotion : {},
    ...e
  };
}
function xc(i = [], e = "") {
  return i.some((t) => (t == null ? void 0 : t.id) === e);
}
function ay(i = [], e = {}) {
  const t = String((e == null ? void 0 : e.id) ?? "").trim();
  if (!t) return i;
  const n = i.findIndex((r) => (r == null ? void 0 : r.id) === t), a = {
    id: t,
    label: String((e == null ? void 0 : e.label) ?? "Manual").trim() || "Manual",
    value: Number((e == null ? void 0 : e.value) ?? 0)
  };
  return n >= 0 ? i[n] = a : i.push(a), i;
}
function tN(i = [], e = "") {
  return i.filter((t) => (t == null ? void 0 : t.id) !== e);
}
function iN({ manual: i = [], attack: e = null, payload: t = {} } = {}) {
  if (!e) return null;
  const n = ny(t), a = dI(e.rangeBand), r = !!n.indirectAttack || xc(i, "attackOption.indirectAttack");
  r && (ay(i, {
    id: "attackOption.indirectAttack",
    label: "Indirect Attack",
    value: a
  }), _c(t, { indirectAttack: !0 }));
  const s = [
    ...ZI,
    { id: "attackOption.indirectAttack", label: "Indirect Attack", value: a, flag: "indirectAttack" }
  ].map((c) => ({
    ...c,
    valueLabel: c.value >= 0 ? `+${c.value}` : String(c.value),
    active: c.id === "attackOption.indirectAttack" ? r : xc(i, c.id)
  })), o = t.machineMotion && typeof t.machineMotion == "object" ? t.machineMotion : {}, l = Oo(o.targetMotion);
  return {
    motionChoices: Object.entries(Dc).map(([c, u]) => ({
      key: c,
      label: u,
      selected: c === l
    })),
    jumped: !!o.jumped,
    options: s,
    losBlocked: !!n.losBlocked
  };
}
function bl(i, e) {
  const t = i == null ? void 0 : i.toggles;
  return t && typeof t == "object" && e in t ? !!t[e] : !!(i != null && i[e]);
}
function nN(i, e) {
  i.useEdge = !!e.useEdge, i.takeRisks = !!e.takeRisks, i.opponentRoll = !!e.opponentRoll, i.toggles = i.toggles && typeof i.toggles == "object" ? i.toggles : {}, i.toggles.useEdge = !!e.useEdge, i.toggles.takeRisks = !!e.takeRisks, i.toggles.opponentRoll = !!e.opponentRoll;
}
function Vm(i, e, t) {
  const n = String(t ?? "").trim(), a = n ? $b(e, n) : "";
  if (n && a) {
    i.specializationKey = n, i.specializationLabel = a;
    return;
  }
  delete i.specializationKey, delete i.specializationLabel;
}
function aN(i) {
  const e = Array.isArray(i == null ? void 0 : i.breakdown) ? i.breakdown : [], t = (n) => {
    var a;
    return Number(((a = e.find((r) => (r == null ? void 0 : r.id) === n)) == null ? void 0 : a.value) ?? 0);
  };
  return {
    attribute: t("attribute"),
    skill: t("skill"),
    bonus: t("bonus"),
    specialization: t("specialization")
  };
}
var si;
const Qe = class Qe extends XI(QI) {
  constructor({ actor: t, rollActor: n = null, baseContext: a, initialState: r = null, options: s = {} }) {
    var u, d;
    super(s);
    Ce(this, si, null);
    /** @type {{ baseContext: any, state: any }} */
    z(this, "_mwd", { baseContext: null, state: null });
    this.actor = t, this.rollActor = n ?? t, this._mwd.baseContext = a ?? {};
    const o = foundry.utils.deepClone(this._mwd.baseContext.payload ?? {}), l = Wm(o.manualModifiers);
    this._mwd.state = foundry.utils.mergeObject(
      {
        payload: o,
        manual: l,
        toggles: {
          useEdge: bl(o, "useEdge"),
          takeRisks: bl(o, "takeRisks"),
          opponentRoll: bl(o, "opponentRoll")
        }
      },
      r ?? {},
      { inplace: !1, insertKeys: !0, insertValues: !0, overwrite: !0 }
    );
    const c = String(((d = (u = o == null ? void 0 : o.edge) == null ? void 0 : u.pre) == null ? void 0 : d.poolKey) ?? "").trim() || null;
    this._mwd.state.edge = {
      prePoolKey: c
    };
  }
  async wait() {
    return new Promise((t) => {
      Ie(this, si, t), this.render(!0);
    });
  }
  async close(t = {}) {
    if (Y(this, si)) {
      const n = Y(this, si);
      Ie(this, si, null), n(null);
    }
    return super.close(t);
  }
  /* --------------------------- */
  /* Prepare Context             */
  /* --------------------------- */
  async _prepareContext(t) {
    var B, P, L, D, G, V, Z, E, X, H, he, ie, le, ze, Xe, mt, ft, pt, gt, nt, ht, Ye, at, Ct, _t, xt, St, Kt, Ft, R, U, ne, ke, Me, xe, Ze, $e, Fe, $t, yt, vt, Gt, At, Ht, st;
    const n = this._mwd.baseContext ?? {}, a = this._mwd.state ?? {}, r = Number.isFinite(Number((B = a == null ? void 0 : a.payload) == null ? void 0 : B.dn)) ? Number(a.payload.dn) : Number.isFinite(Number((L = (P = n == null ? void 0 : n.resolved) == null ? void 0 : P.dn) == null ? void 0 : L.total)) ? Number(n.resolved.dn.total) : Number.isFinite(Number(n == null ? void 0 : n.dn)) ? Number(n.dn) : Number.isFinite(Number((G = (D = n == null ? void 0 : n.resolved) == null ? void 0 : D.difficulty) == null ? void 0 : G.dn)) ? Number(n.resolved.difficulty.dn) : 1, s = (n == null ? void 0 : n.intent) ?? "skill";
    let o, l;
    const c = Array.isArray(a.manual) ? a.manual.reduce((ce, Re) => ce + Number((Re == null ? void 0 : Re.value) || 0), 0) : 0;
    if (s === "edge") {
      const ce = (n == null ? void 0 : n.resolved) ?? {}, Re = Array.isArray(ce.breakdown) ? ce.breakdown : [], _ = (de) => {
        var fe;
        return Number(((fe = Re.find((et) => et.id === de)) == null ? void 0 : fe.value) ?? 0);
      }, Q = Number(((V = ce == null ? void 0 : ce.pool) == null ? void 0 : V.attribute) ?? 0);
      o = {
        pool: Q,
        rating: _("rating"),
        cap: _("cap"),
        modifiers: Number(((Z = n == null ? void 0 : n.dice) == null ? void 0 : Z.modifiers) ?? 0)
      }, l = Math.max(0, Q + o.modifiers + c);
    } else {
      o = {
        attribute: Number(((E = n == null ? void 0 : n.dice) == null ? void 0 : E.attribute) ?? 0),
        attributeLabel: String(((X = n == null ? void 0 : n.dice) == null ? void 0 : X.attributeLabel) ?? "Attribute").trim() || "Attribute",
        skill: Number(((H = n == null ? void 0 : n.dice) == null ? void 0 : H.skill) ?? 0),
        skillLabel: String(((he = n == null ? void 0 : n.dice) == null ? void 0 : he.skillLabel) ?? "Skill").trim() || "Skill",
        bonus: Number(((ie = n == null ? void 0 : n.dice) == null ? void 0 : ie.bonus) ?? 0),
        bonusLabel: String(((le = n == null ? void 0 : n.dice) == null ? void 0 : le.bonusLabel) ?? "Bonus").trim() || "Bonus",
        specialization: Number(((ze = n == null ? void 0 : n.dice) == null ? void 0 : ze.specialization) ?? 0),
        specializationLabel: String(((Xe = n == null ? void 0 : n.dice) == null ? void 0 : Xe.specializationLabel) ?? "Specialization").trim() || "Specialization",
        modifiers: Number(((mt = n == null ? void 0 : n.dice) == null ? void 0 : mt.modifiers) ?? 0)
      };
      const ce = o.modifiers + c, Re = o.attribute + o.skill + o.bonus + o.specialization;
      l = Math.max(0, Re + ce);
    }
    const u = Array.isArray((ft = n == null ? void 0 : n.resolved) == null ? void 0 : ft.domains) ? n.resolved.domains : [], d = u.includes("physical") ? "physical" : u.includes("mental") ? "mental" : u.includes("social") ? "social" : null, f = (d === "physical" ? ["grit", "chaos"] : d === "mental" ? ["insight", "rumor"] : d === "social" ? ["legend", "credibility"] : []).map((ce) => {
      var Re, _, Q, de;
      return {
        key: ce,
        label: ce.charAt(0).toUpperCase() + ce.slice(1),
        available: Number(((Q = (_ = (Re = this.rollActor) == null ? void 0 : Re.getEdgePool) == null ? void 0 : _.call(Re, ce)) == null ? void 0 : Q.effectiveValue) ?? 0),
        selected: ce === (((de = a.edge) == null ? void 0 : de.prePoolKey) ?? null)
      };
    }), p = f.find((ce) => ce.selected), g = (p == null ? void 0 : p.label) ?? null, h = ((pt = n == null ? void 0 : n.resolved) == null ? void 0 : pt.attack) ?? null, y = String(
      ((gt = h == null ? void 0 : h.skill) == null ? void 0 : gt.code) ?? ((ht = (nt = n == null ? void 0 : n.resolved) == null ? void 0 : nt.specialization) == null ? void 0 : ht.skillKey) ?? ((at = (Ye = n == null ? void 0 : n.resolved) == null ? void 0 : Ye.data) == null ? void 0 : at.skillKey) ?? ((Ct = n == null ? void 0 : n.payload) == null ? void 0 : Ct.key) ?? ""
    ).trim(), b = y ? Ff(((_t = this.rollActor) == null ? void 0 : _t.system) ?? {}, y) : [], S = String(((xt = a == null ? void 0 : a.payload) == null ? void 0 : xt.specializationKey) ?? "").trim(), w = b.find((ce) => ce.key === S) ?? null;
    if (s !== "edge") {
      o.specialization = w ? Number(((Kt = (St = n == null ? void 0 : n.resolved) == null ? void 0 : St.specialization) == null ? void 0 : Kt.value) ?? 2) : 0;
      const ce = o.modifiers + c, Re = o.attribute + o.skill + o.bonus + o.specialization;
      l = Math.max(0, Re + ce);
    }
    const T = Array.isArray((Ft = h == null ? void 0 : h.payloadState) == null ? void 0 : Ft.payloads) ? h.payloadState.payloads : [], k = String(((R = h == null ? void 0 : h.weapon) == null ? void 0 : R.category) ?? "").trim().toLowerCase() !== "melee" && T.length > 0, C = String(((U = a == null ? void 0 : a.payload) == null ? void 0 : U.payloadId) ?? ((ne = h == null ? void 0 : h.payloadState) == null ? void 0 : ne.activePayloadId) ?? "").trim(), I = T.find((ce) => ce.id === C) ?? null, O = (n == null ? void 0 : n.machineActor) ?? this.actor, x = s === "attack" && h && Ci(O) ? iN({ manual: a.manual ?? [], attack: h, payload: a.payload ?? {} }) : null;
    return {
      header: {
        left: ((ke = n == null ? void 0 : n.header) == null ? void 0 : ke.left) ?? "Roll",
        right: ((Me = n == null ? void 0 : n.header) == null ? void 0 : Me.right) ?? ((xe = this.actor) == null ? void 0 : xe.name) ?? ""
      },
      formula: String((n == null ? void 0 : n.formula) ?? ((Ze = n == null ? void 0 : n.resolved) == null ? void 0 : Ze.formula) ?? "").trim(),
      dice: o,
      modifiers: Array.isArray(n.modifiers) ? n.modifiers : [],
      manual: (a.manual ?? []).map((ce) => ({
        ...ce,
        steps: eN(Number(ce.value ?? 0), Math.min(-4, Number(ce.value ?? 0), -3), 3)
      })),
      edge: {
        domain: d,
        choices: f,
        selectedLabel: g
      },
      toggles: s === "edge" ? { useEdge: !1, takeRisks: !1, opponentRoll: !1 } : a.toggles,
      totalPool: l,
      intent: s,
      dn: r,
      specialization: b.length ? {
        skillCode: y,
        options: b.map((ce) => ({
          key: ce.key,
          label: ce.label,
          selected: ce.key === S
        })),
        selectedKey: S,
        selectedLabel: (w == null ? void 0 : w.label) ?? ""
      } : null,
      attack: h ? {
        weaponName: (($e = h == null ? void 0 : h.weapon) == null ? void 0 : $e.name) ?? "Weapon",
        rangeBand: ((Fe = h == null ? void 0 : h.weapon) == null ? void 0 : Fe.type) === "personalWeapon" || ($t = h == null ? void 0 : h.weapon) != null && $t.isSynthetic ? So((h == null ? void 0 : h.rangeBand) ?? "") : (h == null ? void 0 : h.rangeBand) ?? "",
        damageType: ((yt = I == null ? void 0 : I.modifies) == null ? void 0 : yt.damageType) || ((vt = h == null ? void 0 : h.weapon) == null ? void 0 : vt.damageTypeLabel) || ((Gt = h == null ? void 0 : h.weapon) == null ? void 0 : Gt.damageType) || "",
        usesPayloads: k,
        source: (h == null ? void 0 : h.sourceState) ?? null,
        payloads: T.map((ce) => {
          var Re;
          return {
            id: ce.id,
            name: ce.label,
            damageType: (Re = ce.modifies) == null ? void 0 : Re.damageType,
            selected: ce.id === C
          };
        }),
        selectedPayloadId: C,
        selectedPayloadLabel: (I == null ? void 0 : I.label) ?? ((At = h == null ? void 0 : h.payload) == null ? void 0 : At.label) ?? ((Ht = h == null ? void 0 : h.weapon) == null ? void 0 : Ht.payloadLabel) ?? "",
        selectedSourceLabel: ((st = h == null ? void 0 : h.sourceState) == null ? void 0 : st.label) ?? ""
      } : null,
      machineAttackOptions: x
    };
  }
  /* --------------------------- */
  /* Actions                     */
  /* --------------------------- */
  async _onCancel(t) {
    if (t == null || t.preventDefault(), Y(this, si)) {
      const n = Y(this, si);
      Ie(this, si, null), n(null);
    }
    return this.close();
  }
  async _onSubmit(t) {
    var a, r, s, o, l, c, u, d, m, f, p, g, h;
    t == null || t.preventDefault();
    const n = this._mwd.state;
    if (n.payload.manualModifiers = (n.manual ?? []).filter((y) => {
      var b;
      return y && (((b = y.label) == null ? void 0 : b.trim()) || Number(y.value));
    }).map((y) => {
      var b;
      return {
        id: y.id,
        label: ((b = y.label) == null ? void 0 : b.trim()) || "Manual",
        value: Number(y.value ?? 0)
      };
    }), nN(n.payload, n.toggles ?? {}), Vm(
      n.payload,
      ((a = n.payload) == null ? void 0 : a.intent) === "attack" ? ((r = n.payload) == null ? void 0 : r.skillKey) ?? ((c = (l = (o = (s = this._mwd.baseContext) == null ? void 0 : s.resolved) == null ? void 0 : o.attack) == null ? void 0 : l.skill) == null ? void 0 : c.code) : ((u = n.payload) == null ? void 0 : u.key) ?? ((f = (m = (d = this._mwd.baseContext) == null ? void 0 : d.resolved) == null ? void 0 : m.data) == null ? void 0 : f.skillKey),
      (p = n.payload) == null ? void 0 : p.specializationKey
    ), Y(this, si)) {
      const y = Y(this, si);
      Ie(this, si, null), y({ payload: n.payload });
    }
    if (n.payload.edge = n.payload.edge && typeof n.payload.edge == "object" ? n.payload.edge : {}, n.payload.edge.pre = n.payload.edge.pre && typeof n.payload.edge.pre == "object" ? n.payload.edge.pre : {}, (g = n.toggles) != null && g.useEdge) {
      const y = String(((h = n.edge) == null ? void 0 : h.prePoolKey) ?? "").trim() || null;
      n.payload.edge.pre.poolKey = y, n.payload.edge.pre.spent = y ? 1 : 0;
    } else
      n.payload.edge.pre.poolKey = null, n.payload.edge.pre.spent = 0;
    return this.close();
  }
  async _onAddManual(t) {
    return t == null || t.preventDefault(), this._mwd.state.manual.push({
      id: foundry.utils.randomID(),
      label: "Manual",
      value: 0
    }), this.render(!1);
  }
  async _onRemoveManual(t, n) {
    var r;
    t == null || t.preventDefault();
    const a = (r = n == null ? void 0 : n.dataset) == null ? void 0 : r.id;
    if (a)
      return this._mwd.state.manual = this._mwd.state.manual.filter((s) => s.id !== a), this.render(!1);
  }
  async _onSetManualValue(t, n) {
    var o, l;
    t == null || t.preventDefault();
    const a = (o = n == null ? void 0 : n.dataset) == null ? void 0 : o.id, r = (l = n == null ? void 0 : n.dataset) == null ? void 0 : l.field;
    if (!a || !r) return;
    const s = this._mwd.state.manual.find((c) => c.id === a);
    if (s)
      return r === "label" && (s.label = String(n.value ?? "")), r === "value" && (s.value = Number(n.value ?? 0)), this.render(!1);
  }
  async _onSetManualStepper(t, n) {
    var o, l;
    t == null || t.preventDefault();
    const a = (o = n == null ? void 0 : n.dataset) == null ? void 0 : o.id, r = Number((l = n == null ? void 0 : n.dataset) == null ? void 0 : l.value);
    if (!a || Number.isNaN(r)) return;
    const s = this._mwd.state.manual.find((c) => c.id === a);
    if (s)
      return s.value = r, this.render(!1);
  }
  async _onSetEdgePrePool(t, n) {
    var r;
    t == null || t.preventDefault();
    const a = String(((r = n == null ? void 0 : n.dataset) == null ? void 0 : r.poolKey) ?? "").trim();
    if (a)
      return this._mwd.state.edge = this._mwd.state.edge ?? {}, this._mwd.state.edge.prePoolKey = a, this._mwd.state.toggles.useEdge = !0, this.render(!1);
  }
  async _onToggleCheckbox(t, n) {
    var r;
    t == null || t.preventDefault();
    const a = (r = n == null ? void 0 : n.dataset) == null ? void 0 : r.key;
    if (a)
      return this._mwd.state.toggles[a] = !!n.checked, this.render(!1);
  }
  async _onSetMachineTargetMotion(t, n) {
    var r;
    t == null || t.preventDefault();
    const a = Oo((r = n == null ? void 0 : n.dataset) == null ? void 0 : r.motion);
    return qm(this._mwd.state.payload, { targetMotion: a }), this.render(!1);
  }
  async _onToggleMachineTargetJumped(t, n) {
    return t == null || t.preventDefault(), qm(this._mwd.state.payload, { jumped: !!(n != null && n.checked) }), this.render(!1);
  }
  async _onToggleAttackOption(t, n) {
    var l, c, u, d;
    t == null || t.preventDefault();
    const a = String(((l = n == null ? void 0 : n.dataset) == null ? void 0 : l.optionId) ?? "").trim();
    if (!a) return;
    const r = this._mwd.state.manual ?? [], s = xc(r, a);
    this._mwd.state.manual = s ? tN(r, a) : ay(r, {
      id: a,
      label: ((c = n == null ? void 0 : n.dataset) == null ? void 0 : c.label) ?? "Attack Option",
      value: Number(((u = n == null ? void 0 : n.dataset) == null ? void 0 : u.value) ?? 0)
    });
    const o = String(((d = n == null ? void 0 : n.dataset) == null ? void 0 : d.flag) ?? "").trim();
    return o && _c(this._mwd.state.payload, { [o]: !s }), this.render(!1);
  }
  async _onToggleAttackFlag(t, n) {
    var r;
    t == null || t.preventDefault();
    const a = String(((r = n == null ? void 0 : n.dataset) == null ? void 0 : r.flag) ?? "").trim();
    if (a)
      return _c(this._mwd.state.payload, { [a]: !!(n != null && n.checked) }), this.render(!1);
  }
  async _onSetDn(t, n) {
    t == null || t.preventDefault();
    const a = String((n == null ? void 0 : n.value) ?? "").trim(), r = a === "" ? null : Number(a);
    return this._mwd.state.payload.dn = Number.isFinite(r) ? Math.max(0, Math.trunc(r)) : null, this.render(!1);
  }
  async _onSetPayload(t, n) {
    return t == null || t.preventDefault(), this._mwd.state.payload.payloadId = String((n == null ? void 0 : n.value) ?? "").trim(), this.render(!1);
  }
  async _onSetSpecialization(t, n) {
    var s;
    t == null || t.preventDefault();
    const a = String(((s = n == null ? void 0 : n.dataset) == null ? void 0 : s.skillCode) ?? "").trim(), r = String((n == null ? void 0 : n.value) ?? "").trim();
    if (a)
      return Vm(this._mwd.state.payload, a, r), this.render(!1);
  }
  _onRender(t, n) {
    var r, s;
    (r = super._onRender) == null || r.call(this, t, n);
    const a = this.element instanceof HTMLElement ? this.element : (s = this.element) == null ? void 0 : s[0];
    a && (a.querySelectorAll("[data-action='setPayload']").forEach((o) => {
      o.addEventListener("change", (l) => {
        this._onSetPayload(l, l.currentTarget);
      });
    }), a.querySelectorAll("[data-action='setSpecialization']").forEach((o) => {
      o.addEventListener("change", (l) => {
        this._onSetSpecialization(l, l.currentTarget);
      });
    }), a.querySelectorAll("[data-action='setDn']").forEach((o) => {
      o.addEventListener("change", (l) => {
        this._onSetDn(l, l.currentTarget);
      });
    }), a.querySelectorAll("[data-action='toggleMachineTargetJumped']").forEach((o) => {
      o.addEventListener("change", (l) => {
        this._onToggleMachineTargetJumped(l, l.currentTarget);
      });
    }), a.querySelectorAll("[data-action='toggleAttackFlag']").forEach((o) => {
      o.addEventListener("change", (l) => {
        this._onToggleAttackFlag(l, l.currentTarget);
      });
    }));
  }
  /**
   * Open the roll dialog as a payload editor and return an updated payload.
   * Cancel returns null.
   *
   * IMPORTANT:
   *  - Prefer passing explicit dice parts via args.diceParts (attribute/skill/bonus).
   *  - This avoids scraping resolved.breakdown.
   */
  static async prompt({ actor: t, rollActor: n = null, basePayload: a, resolved: r, diceParts: s = null, mods: o = [], modTotal: l = 0 } = {}) {
    var h, y, b, S, w, T, k, C, I, O, x, B, P, L;
    const c = foundry.utils.deepClone(a ?? {});
    try {
      const D = (r == null ? void 0 : r.rollType) ?? "simple", G = String((c == null ? void 0 : c.intent) ?? (r == null ? void 0 : r.intent) ?? "").trim().toLowerCase();
      if (D === "simple" && G !== "attack" && (c == null ? void 0 : c.dn) == null) {
        const V = Number(game.settings.get(game.system.id, "gmNextDn"));
        Number.isFinite(V) && (c.dn = Math.max(0, Math.trunc(V)));
      }
    } catch (D) {
      console.warn("MWD: failed to default DN from GM Gadget", D);
    }
    const u = {
      left: (r == null ? void 0 : r.title) ?? "Roll",
      right: (t == null ? void 0 : t.name) ?? ""
    }, d = s ?? aN(r), m = {
      attribute: Number((d == null ? void 0 : d.attribute) ?? 0),
      attributeLabel: ((b = (y = (h = r == null ? void 0 : r.breakdown) == null ? void 0 : h.find) == null ? void 0 : y.call(h, (D) => (D == null ? void 0 : D.id) === "attribute")) == null ? void 0 : b.label) ?? "Attribute",
      skill: Number((d == null ? void 0 : d.skill) ?? 0),
      skillLabel: ((T = (w = (S = r == null ? void 0 : r.breakdown) == null ? void 0 : S.find) == null ? void 0 : w.call(S, (D) => (D == null ? void 0 : D.id) === "skill")) == null ? void 0 : T.label) ?? "Skill",
      bonus: Number((d == null ? void 0 : d.bonus) ?? 0),
      bonusLabel: ((I = (C = (k = r == null ? void 0 : r.breakdown) == null ? void 0 : k.find) == null ? void 0 : C.call(k, (D) => (D == null ? void 0 : D.id) === "bonus")) == null ? void 0 : I.label) ?? "Bonus",
      specialization: Number((d == null ? void 0 : d.specialization) ?? 0),
      specializationLabel: ((B = (x = (O = r == null ? void 0 : r.breakdown) == null ? void 0 : O.find) == null ? void 0 : x.call(O, (D) => (D == null ? void 0 : D.id) === "specialization")) == null ? void 0 : B.label) ?? "Specialization",
      modifiers: Number(l ?? 0)
    }, f = (Array.isArray(o) ? o : []).map((D) => ({
      label: D.label ?? "Modifier",
      source: D.source ?? "",
      value: Number(D.value ?? 0)
    }));
    c.manualModifiers = Wm(c.manualModifiers);
    const g = await new Qe({
      actor: t,
      rollActor: n,
      baseContext: {
        intent: (r == null ? void 0 : r.intent) ?? "skill",
        header: u,
        formula: String((r == null ? void 0 : r.formula) ?? "").trim(),
        dice: m,
        modifiers: f,
        payload: c,
        resolved: r,
        // keep full resolved for edge display
        rollActor: n,
        machineActor: t,
        dn: Number((c == null ? void 0 : c.dn) ?? ((P = r == null ? void 0 : r.dn) == null ? void 0 : P.total) ?? ((L = r == null ? void 0 : r.difficulty) == null ? void 0 : L.dn) ?? 1)
      }
    }).wait();
    return (g == null ? void 0 : g.payload) ?? null;
  }
};
si = new WeakMap(), z(Qe, "DEFAULT_OPTIONS", foundry.utils.mergeObject(
  Oi(Qe, Qe, "DEFAULT_OPTIONS"),
  {
    id: "mwd-roll-dialog",
    classes: [
      ...Oi(Qe, Qe, "DEFAULT_OPTIONS").classes ?? [],
      "mwd",
      "mwd-roll-dialog"
    ],
    window: { title: "Roll", resizable: !1 },
    position: { width: 520, height: "auto" },
    actions: {
      submit: Qe.prototype._onSubmit,
      cancel: Qe.prototype._onCancel,
      addManual: Qe.prototype._onAddManual,
      removeManual: Qe.prototype._onRemoveManual,
      setManualValue: Qe.prototype._onSetManualValue,
      setManualStepper: Qe.prototype._onSetManualStepper,
      setEdgePrePool: Qe.prototype._onSetEdgePrePool,
      toggleCheckbox: Qe.prototype._onToggleCheckbox,
      setMachineTargetMotion: Qe.prototype._onSetMachineTargetMotion,
      toggleMachineTargetJumped: Qe.prototype._onToggleMachineTargetJumped,
      toggleAttackOption: Qe.prototype._onToggleAttackOption,
      toggleAttackFlag: Qe.prototype._onToggleAttackFlag,
      setDn: Qe.prototype._onSetDn,
      setPayload: Qe.prototype._onSetPayload,
      setSpecialization: Qe.prototype._onSetSpecialization
    }
  },
  { inplace: !1 }
)), z(Qe, "PARTS", {
  body: { template: "systems/mwd/templates/v2/roll/mwd-roll-dialog.hbs" }
});
let $c = Qe;
const { ApplicationV2: rN, HandlebarsApplicationMixin: sN } = foundry.applications.api, Ar = class Ar extends sN(rN) {
  static get DEFAULT_OPTIONS() {
    return foundry.utils.mergeObject(super.DEFAULT_OPTIONS, {
      id: "select-item",
      classes: ["select-item", "anarchy-dialog"],
      position: { width: 360, height: "auto" },
      window: {
        resizable: !0
      }
    }, { inplace: !1 });
  }
  static async selectItem(e, t) {
    const n = {
      id: `select-item-${foundry.utils.randomID()}`,
      classes: [game.system.anarchy.styles.selectCssClass(), ...Ar.DEFAULT_OPTIONS.classes],
      window: { title: e }
    };
    return new Ar({ items: t }, n).wait();
  }
  constructor(e = {}, t = {}) {
    super(e, t), this.items = Array.isArray(e.items) ? e.items : [], this._selected = !1, this._resolve = null;
  }
  async _prepareContext() {
    return { items: this.items };
  }
  async activateListeners(e) {
    const t = e instanceof HTMLElement ? e : e == null ? void 0 : e[0];
    await super.activateListeners(t);
    const n = $(t);
    n.find(".click-select-item").click((a) => this.onSelectItem(a)), n.find('[data-action="cancel"]').on("click", async () => {
      if (this._resolve) {
        const a = this._resolve;
        this._resolve = null, a(null);
      }
      await this.close();
    });
  }
  wait() {
    return new Promise((e) => {
      this._resolve = e, this.render({ force: !0 });
    });
  }
  async onSelectItem(e) {
    const t = $(e.currentTarget).attr("data-item-id"), n = this.items.find((a) => a.id === t) ?? null;
    if (this._selected = !0, this._resolve) {
      const a = this._resolve;
      this._resolve = null, a(n);
    }
    await this.close();
  }
  async close(e) {
    if (!this._selected && this._resolve) {
      const t = this._resolve;
      this._resolve = null, t(null);
    }
    return super.close(e);
  }
};
z(Ar, "PARTS", {
  body: {
    template: `${se}/dialog/select-item.hbs`
  }
});
let Bc = Ar;
function Lo(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? t : e;
}
function Hi(i, e = 0) {
  return Math.max(e, Lo(i, e));
}
function oN(i = "") {
  const e = String(i ?? "").trim().toLowerCase();
  return e === "shutdown" || e === "explosion" ? e : "";
}
function lN({ check: i, result: e }) {
  return {
    scope: "Heat Danger",
    severity: `DN ${Number((i == null ? void 0 : i.dn) ?? 0)}`,
    notes: `Failed shutdown check by ${Number((e == null ? void 0 : e.failureMargin) ?? 0)}.`,
    tags: ["heat", "danger", "shutdown"]
  };
}
async function cN({ actor: i, check: e, outcomeModel: t }) {
  var f;
  const n = Hi((e == null ? void 0 : e.dn) ?? ((f = t == null ? void 0 : t.difficulty) == null ? void 0 : f.dn), 1), a = Hi(t == null ? void 0 : t.successes, 0), r = Lo(t == null ? void 0 : t.margin, a - n), s = Hi(n - a, 0), o = Number.isFinite(Number(e == null ? void 0 : e.systemOpsRating)) ? Hi(e.systemOpsRating, 0) : null, l = String((e == null ? void 0 : e.operatorName) ?? "").trim(), c = o !== null && s < o, u = {
    kind: "shutdown",
    passed: !1,
    hits: a,
    dn: n,
    margin: r,
    failureMargin: s,
    operatorName: l,
    systemOpsRating: o,
    overrideAvailable: c,
    statusId: "shutdown",
    statusApplied: !1,
    statusAlreadyActive: Gi(i, "shutdown")
  };
  if (c)
    return {
      ...u,
      outcome: "overrideAvailable",
      effectText: `Shutdown check failed by ${s}, below ${l ? `${l}'s ` : "the pilot's "}System Operations ${o}.`,
      nextStep: "Pilot may override the shutdown. Apply Shutdown only if the override is declined."
    };
  const d = await Wi({
    actor: i,
    statusId: "shutdown",
    active: !0,
    metadata: lN({ check: e, result: u })
  }), m = o === null ? "No linked pilot System Operations rating was found, so no override is available." : `Failure margin ${s} is not below System Operations ${o}.`;
  return {
    ...u,
    statusApplied: !!d,
    statusAlreadyActive: u.statusAlreadyActive || Gi(i, "shutdown"),
    outcome: "shutdownApplied",
    effectText: u.statusAlreadyActive ? "The BattleMech was already Shutdown." : "The BattleMech shuts down.",
    nextStep: `${m} Shutdown status is active until cleared through the appropriate restart or recovery procedure.`
  };
}
function uN({ check: i, outcomeModel: e }) {
  var o;
  const t = Hi((i == null ? void 0 : i.dn) ?? ((o = e == null ? void 0 : e.difficulty) == null ? void 0 : o.dn), 1), n = Hi(e == null ? void 0 : e.successes, 0), a = Lo(e == null ? void 0 : e.margin, n - t), r = Hi(t - n, 0), s = !!(i != null && i.volatile);
  return e != null && e.passed ? {
    kind: "explosion",
    passed: !0,
    hits: n,
    dn: t,
    margin: a,
    failureMargin: 0,
    volatile: s,
    explosionTriggered: !1,
    outcome: "contained",
    effectText: "Volatile systems remain contained.",
    nextStep: s ? "No detonation is triggered by this check." : "No volatile components were detected, so no explosion is applied."
  } : s ? {
    kind: "explosion",
    passed: !1,
    hits: n,
    dn: t,
    margin: a,
    failureMargin: r,
    volatile: s,
    explosionTriggered: !0,
    outcome: "explosionTriggered",
    effectText: "Volatile components detonate.",
    nextStep: "Resolve the volatile component detonation immediately, then apply any resulting damage or destruction."
  } : {
    kind: "explosion",
    passed: !1,
    hits: n,
    dn: t,
    margin: a,
    failureMargin: r,
    volatile: s,
    explosionTriggered: !1,
    outcome: "noVolatileComponents",
    effectText: "Explosion check failed, but no volatile components are present.",
    nextStep: "No detonation is applied. Continue resolving heat effects."
  };
}
async function dN({ actor: i, ctx: e, outcomeModel: t } = {}) {
  var l;
  const n = (e == null ? void 0 : e.heatDangerCheck) ?? null, a = oN(n == null ? void 0 : n.kind);
  if (!i || !n || !a || !t) return null;
  if (a === "explosion")
    return uN({ check: n, outcomeModel: t });
  const r = Hi((n == null ? void 0 : n.dn) ?? ((l = t == null ? void 0 : t.difficulty) == null ? void 0 : l.dn), 1), s = Hi(t == null ? void 0 : t.successes, 0), o = Lo(t == null ? void 0 : t.margin, s - r);
  return t.passed ? {
    kind: "shutdown",
    passed: !0,
    hits: s,
    dn: r,
    margin: o,
    failureMargin: 0,
    operatorName: String((n == null ? void 0 : n.operatorName) ?? "").trim(),
    systemOpsRating: Number.isFinite(Number(n == null ? void 0 : n.systemOpsRating)) ? Hi(n.systemOpsRating, 0) : null,
    overrideAvailable: !1,
    statusId: "shutdown",
    statusApplied: !1,
    statusAlreadyActive: Gi(i, "shutdown"),
    outcome: "staysOnline",
    effectText: "The BattleMech stays online.",
    nextStep: "No shutdown is applied."
  } : cN({ actor: i, check: n, outcomeModel: t });
}
function ry(i) {
  var t, n;
  const e = String(i ?? "").trim();
  return e ? ((n = (t = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : t.placeables) == null ? void 0 : n.find((a) => {
    var r;
    return (((r = a.document) == null ? void 0 : r.uuid) ?? a.uuid) === e;
  })) ?? null : null;
}
function sy(i) {
  var t, n, a, r;
  const e = String(i ?? "").trim();
  return e ? ((n = (t = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : t.get) == null ? void 0 : n.call(t, e)) ?? ((r = (a = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : a.placeables) == null ? void 0 : r.find((s) => {
    var o;
    return (s.id ?? ((o = s.document) == null ? void 0 : o.id)) === e;
  })) ?? null : null;
}
function mN(i) {
  var n, a, r;
  const e = String(i ?? "").trim();
  if (!e) return null;
  const t = (n = game.combat) == null ? void 0 : n.combatants;
  return ((a = t == null ? void 0 : t.get) == null ? void 0 : a.call(t, e)) ?? ((r = t == null ? void 0 : t.find) == null ? void 0 : r.call(t, (s) => (s == null ? void 0 : s.id) === e)) ?? null;
}
function oy(i = {}) {
  return mN(i == null ? void 0 : i.attackerCombatantId) ?? sa(ry(i == null ? void 0 : i.attackerTokenUuid)) ?? sa(sy(i == null ? void 0 : i.attackerTokenId));
}
async function fN({ attacker: i, ctx: e, outcomeModel: t } = {}) {
  var f;
  const n = e == null ? void 0 : e.acquire;
  if (!n) return { ok: !1, reason: "Missing acquire context." };
  const a = Number((t == null ? void 0 : t.successes) ?? (t == null ? void 0 : t.hits) ?? 0), r = Number(((f = e == null ? void 0 : e.difficulty) == null ? void 0 : f.dn) ?? 1);
  if (!(a >= r))
    return {
      ok: !1,
      reason: "Acquire roll failed.",
      previousState: n.currentState,
      newState: n.currentState,
      hits: a,
      dn: r
    };
  const o = n.ceiling ?? "lock", l = ek(n.currentState), c = Ni.indexOf(o), d = Ni.indexOf(l) <= c ? l : o;
  Hooks.callAll("mwd.beforeAcquireStateChange", {
    attacker: i,
    targetTokenUuid: n.targetTokenUuid,
    currentState: n.currentState,
    newState: d,
    ctx: e
  });
  const m = oy(n);
  return m ? await Zl(m, n.targetTokenUuid, d) : console.warn("MWD | EW acquire: no combatant found for attacker token — state change not persisted."), {
    ok: !0,
    previousState: n.currentState,
    newState: d,
    ceiling: o,
    hits: a,
    dn: r,
    hitCeiling: d !== l
  };
}
async function pN({ attacker: i, ctx: e, outcomeModel: t } = {}) {
  var m, f;
  const n = e == null ? void 0 : e.targeting;
  if (!n) return { ok: !1, reason: "Missing targeting context." };
  const a = Number((t == null ? void 0 : t.successes) ?? (t == null ? void 0 : t.hits) ?? 0), r = Number(((m = e == null ? void 0 : e.difficulty) == null ? void 0 : m.dn) ?? 2);
  if (!(a >= r))
    return {
      ok: !1,
      reason: "Targeting roll failed.",
      rawHits: a,
      dn: r
    };
  const o = a, l = Math.min(o, n.cap), c = ry(n.attackerTokenUuid) ?? sy(n.attackerTokenId), u = gk({
    value: l,
    sourceToken: c,
    round: ((f = game.combat) == null ? void 0 : f.round) ?? null
  }), d = oy(n);
  return d ? await fk(d, n.targetTokenUuid, u) : console.warn("MWD | EW targeting: no combatant found for attacker token — packet not persisted."), {
    ok: !0,
    packetValue: l,
    rawHits: o,
    dn: r,
    packetId: u.id,
    hitCap: l < o
  };
}
const Ym = { execute: kN }, gN = {
  physical: ["grit", "chaos"],
  mental: ["insight", "rumor"],
  social: ["legend", "credibility"]
};
function hN(i, e) {
  var r;
  const t = gN[e] ?? [];
  let n = null, a = -1;
  for (const s of t) {
    const o = (r = i.getEdgePool) == null ? void 0 : r.call(i, s), l = Number((o == null ? void 0 : o.rating) ?? 0), c = Number((o == null ? void 0 : o.value) ?? 0), u = Math.max(0, l - c);
    u > a && (a = u, n = s);
  }
  return n ?? t[0] ?? null;
}
function yN(i) {
  const t = (Array.isArray(i == null ? void 0 : i.manualModifiers) ? i.manualModifiers : []).map((a) => ({
    id: a.id ?? foundry.utils.randomID(),
    label: (a.label ?? "Manual").trim() || "Manual",
    value: Number(a.value ?? 0),
    source: "Manual"
  })).filter((a) => Number.isFinite(a.value) && a.value !== 0), n = t.reduce((a, r) => a + r.value, 0);
  return { mods: t, total: n };
}
function bN(...i) {
  const e = /* @__PURE__ */ new Set();
  return i.filter((t) => {
    if (!t) return !1;
    const n = t.uuid ?? t.id ?? t;
    return e.has(n) ? !1 : (e.add(n), !0);
  });
}
function Jm(i = {}) {
  const e = i.toggles ?? {}, t = String((i == null ? void 0 : i.payloadId) ?? (i == null ? void 0 : i.ammoTypeId) ?? "").trim();
  return {
    ...i,
    ...t ? { payloadId: t } : {},
    toggles: {
      useEdge: !!e.useEdge,
      takeRisks: !!e.takeRisks,
      opponentRoll: !!e.opponentRoll
    },
    manualModifiers: AN(i.manualModifiers)
  };
}
async function SN({ actor: i, payload: e } = {}) {
  var l, c, u, d, m, f, p, g, h, y, b, S, w;
  if ((e == null ? void 0 : e.intent) !== "attack") return e;
  const t = foundry.utils.deepClone(e ?? {}), n = ((l = i.getPersonalCombatLoadout) == null ? void 0 : l.call(i, { refresh: !0 })) ?? null, a = aa(i), r = String((t == null ? void 0 : t.sourceType) ?? "").trim(), s = String((t == null ? void 0 : t.sourceId) ?? "").trim(), o = (T) => {
    var C, I, O, x, B;
    const k = ((I = (C = i.items) == null ? void 0 : C.get) == null ? void 0 : I.call(C, T)) ?? null;
    return !k || !(((O = k.isPersonalWeapon) == null ? void 0 : O.call(k)) ?? k.type === A.itemType.personalWeapon) || !((x = k.system) != null && x.equipped) ? null : ((B = k.getCombatProfile) == null ? void 0 : B.call(k, { payloadId: t == null ? void 0 : t.payloadId })) ?? null;
  };
  if (!r || !s) {
    const T = String((t == null ? void 0 : t.weaponGroupId) ?? ((c = t == null ? void 0 : t.machineWeaponGroup) == null ? void 0 : c.id) ?? "").trim(), k = String((t == null ? void 0 : t.weaponId) ?? "").trim();
    T ? (t.sourceType = "weaponGroup", t.sourceId = T) : k && (t.sourceType = a ? "mechWeapon" : "personalWeapon", t.sourceId = k);
  }
  if (String((t == null ? void 0 : t.sourceType) ?? "").trim() === "weaponGroup") {
    if (t.weaponGroupId = String((t == null ? void 0 : t.sourceId) ?? (t == null ? void 0 : t.weaponGroupId) ?? "").trim(), t.sourceId = t.weaponGroupId, !t.weaponGroupId)
      throw new Error("Attack requires a valid weapon group source.");
    return t;
  }
  if (String((t == null ? void 0 : t.sourceType) ?? "").trim() === "mechWeapon") {
    if (t.weaponId = String((t == null ? void 0 : t.sourceId) ?? (t == null ? void 0 : t.weaponId) ?? "").trim(), t.sourceId = t.weaponId, !t.weaponId)
      throw new Error("Attack requires a valid machine weapon source.");
    return t;
  }
  if (String((t == null ? void 0 : t.sourceType) ?? "").trim() === "personalWeapon" && s && (t.weaponId = s), t.weaponId) {
    const T = o(t.weaponId);
    if (!T)
      throw new Error("Attack requires an owned equipped personal weapon.");
    return t.sourceType = "personalWeapon", t.sourceId = t.weaponId, t.payloadId = t.payloadId ?? ((u = T == null ? void 0 : T.payloadState) == null ? void 0 : u.activePayloadId) ?? "", t;
  }
  if (t.mode === "auto") {
    if (n != null && n.weaponChoiceRequired) {
      const T = await Bc.selectItem(
        "Choose Weapon",
        n.equippedWeapons ?? []
      );
      return T ? (t.weaponId = T.id, t.sourceType = "personalWeapon", t.sourceId = T.id, t.payloadId = t.payloadId ?? ((d = T == null ? void 0 : T.payloadState) == null ? void 0 : d.activePayloadId) ?? "", delete t.mode, t) : null;
    }
    if ((m = n == null ? void 0 : n.defaultWeapon) != null && m.isSynthetic || ((f = n == null ? void 0 : n.defaultWeapon) == null ? void 0 : f.id) === "unarmed")
      return t.syntheticWeapon = foundry.utils.deepClone(n.defaultWeapon ?? yi.buildDefaultUnarmedProfile(i)), t.weaponId = t.syntheticWeapon.id, t.sourceType = "personalWeapon", t.sourceId = t.syntheticWeapon.id, t.payloadId = t.payloadId ?? ((g = (p = t.syntheticWeapon) == null ? void 0 : p.payloadState) == null ? void 0 : g.activePayloadId) ?? "", delete t.mode, t;
    if ((h = n == null ? void 0 : n.defaultWeapon) != null && h.id)
      return t.weaponId = n.defaultWeapon.id, t.sourceType = "personalWeapon", t.sourceId = n.defaultWeapon.id, t.payloadId = t.payloadId ?? ((b = (y = n.defaultWeapon) == null ? void 0 : y.payloadState) == null ? void 0 : b.activePayloadId) ?? "", delete t.mode, t;
  }
  if (t.fallback === "unarmed")
    return t.syntheticWeapon = foundry.utils.deepClone(yi.buildDefaultUnarmedProfile(i)), t.weaponId = t.syntheticWeapon.id, t.sourceType = "personalWeapon", t.sourceId = t.syntheticWeapon.id, t.payloadId = t.payloadId ?? ((w = (S = t.syntheticWeapon) == null ? void 0 : S.payloadState) == null ? void 0 : w.activePayloadId) ?? "", delete t.mode, t;
  throw new Error("Attack could not resolve a usable weapon.");
}
function AN(i) {
  return Array.isArray(i) ? i.map((e) => ({
    id: (e == null ? void 0 : e.id) ?? foundry.utils.randomID(),
    label: typeof (e == null ? void 0 : e.label) == "string" ? e.label : "Manual",
    value: Number((e == null ? void 0 : e.value) ?? 0)
  })) : [];
}
async function wN(i = []) {
  var t, n, a, r, s, o, l, c, u;
  const e = Array.from(new Set(
    (Array.isArray(i) ? i : []).map((d) => String(d ?? "").trim()).filter(Boolean)
  ));
  if (typeof ((t = game.user) == null ? void 0 : t.updateTokenTargets) == "function") {
    await game.user.updateTokenTargets(e);
    return;
  }
  for (const d of Array.from(((n = game.user) == null ? void 0 : n.targets) ?? []))
    (a = d == null ? void 0 : d.setTarget) == null || a.call(d, !1, { releaseOthers: !1, user: game.user });
  for (const d of e) {
    const m = ((s = (r = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : r.get) == null ? void 0 : s.call(r, d)) ?? ((c = (l = (o = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : o.placeables) == null ? void 0 : l.find) == null ? void 0 : c.call(l, (f) => (f == null ? void 0 : f.id) === d)) ?? null;
    (u = m == null ? void 0 : m.setTarget) == null || u.call(m, !0, { releaseOthers: !1, user: game.user });
  }
}
function Qu(i, e = {}) {
  var n, a, r, s, o, l, c, u, d, m, f;
  const t = String((e == null ? void 0 : e.sourceTokenId) ?? "").trim();
  if (t) {
    const p = ((a = (n = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : n.get) == null ? void 0 : a.call(n, t)) ?? ((o = (s = (r = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : r.placeables) == null ? void 0 : s.find) == null ? void 0 : o.call(s, (g) => (g == null ? void 0 : g.id) === t)) ?? null;
    if (p) return p.document ?? p;
  }
  return ((l = i == null ? void 0 : i.token) == null ? void 0 : l.document) ?? (i == null ? void 0 : i.token) ?? ((d = (u = (c = i == null ? void 0 : i.getActiveTokens) == null ? void 0 : c.call(i, !0, !0)) == null ? void 0 : u[0]) == null ? void 0 : d.document) ?? ((f = (m = i == null ? void 0 : i.getActiveTokens) == null ? void 0 : m.call(i, !0, !0)) == null ? void 0 : f[0]) ?? null;
}
async function TN(i, e = {}, { rollActor: t = null } = {}) {
  var u, d, m, f;
  if (!aa(i)) return;
  const n = Qu(i, e), a = t ?? i, r = ((u = F.getSnapshot) == null ? void 0 : u.call(F, a, { token: n })) ?? null;
  if (!(r != null && r.hasCombatant)) return;
  const s = Hw(i), o = (i == null ? void 0 : i.type) === A.actorTypes.battlemech && String((e == null ? void 0 : e.sourceType) ?? "").trim() === "weaponGroup" && String((e == null ? void 0 : e.sourceId) ?? (e == null ? void 0 : e.weaponGroupId) ?? "").trim(), l = o ? 1 + Number((s == null ? void 0 : s.extraCost) ?? 0) : Number((s == null ? void 0 : s.totalCost) ?? 0), c = await F.spendResource(a, {
    token: n,
    resource: "sa",
    cost: l,
    actionId: "attack",
    actionLabel: "Attack",
    actionCostLabel: `${l} SA`,
    actionCategory: "complex"
  });
  if (!(c != null && c.ok)) {
    (d = ui.notifications) == null || d.warn((c == null ? void 0 : c.reason) ?? "Unable to record attack action.");
    return;
  }
  if (o) {
    const p = await ((m = F.markWeaponGroupUsed) == null ? void 0 : m.call(F, i, {
      token: n,
      groupId: String((e == null ? void 0 : e.sourceId) ?? (e == null ? void 0 : e.weaponGroupId) ?? "").trim()
    }));
    p != null && p.ok || (f = ui.notifications) == null || f.warn((p == null ? void 0 : p.reason) ?? "Unable to record BattleMech weapon-group usage.");
  }
}
async function Qm(i, e = "", t = {}, { rollActor: n = null } = {}) {
  var c, u;
  if (!aa(i)) return;
  const a = Mp(e);
  if (!(a != null && a.cost) || (a == null ? void 0 : a.resource) !== "sa") return;
  const r = Qu(i, t), s = n ?? i, o = ((c = F.getSnapshot) == null ? void 0 : c.call(F, s, { token: r })) ?? null;
  if (!(o != null && o.hasCombatant)) return;
  const l = await F.spendResource(s, {
    token: r,
    resource: a.resource,
    cost: a.cost,
    actionId: a.key,
    actionLabel: a.label,
    actionCostLabel: `${a.cost} SA`,
    actionCategory: a.category
  });
  l != null && l.ok || (u = ui.notifications) == null || u.warn((l == null ? void 0 : l.reason) ?? `Unable to record ${a.label}.`);
}
async function kN({ actor: i, payload: e, event: t } = {}) {
  var Z, E, X, H, he, ie, le, ze, Xe, mt, ft, pt, gt, nt, ht, Ye, at, Ct, _t, xt, St, Kt, Ft, R, U, ne, ke, Me, xe, Ze, $e, Fe, $t, yt, vt, Gt, At, Ht, st, ce, Re, _, Q, de, fe, et, Wt, Si, Di, ot, Ge, mi, $n, qa, Va;
  if (i != null && i.actor && (i = i.actor), (Z = i == null ? void 0 : i.document) != null && Z.actor && (i = i.document.actor), !i) throw new Error("MWD.roll.execute requires actor");
  if (!(e != null && e.intent)) throw new Error("MWD.roll.execute requires payload.intent");
  if (e = Jm(e), e = await SN({ actor: i, payload: e }), !e) return null;
  let n = await yl({ actor: i, payload: e, event: t }), a = (n == null ? void 0 : n.rollActor) ?? i;
  if (e.intent === "attack" && ((X = (E = n == null ? void 0 : n.attack) == null ? void 0 : E.capabilityReport) != null && X.isTemplated)) {
    const ee = await QE({
      actor: i,
      attack: n.attack
    });
    if (!ee) return null;
    try {
      await JE({
        actor: i,
        attack: n.attack,
        templateGeometry: ee.templateGeometry ?? null
      });
    } catch (ge) {
      console.warn("MWD | Unable to create visual template indicator", ge);
    }
    if (await wN(ee.autoTargetTokenIds ?? []), !pf(((H = n == null ? void 0 : n.attack) == null ? void 0 : H.areaEffect) ?? ((ie = (he = n == null ? void 0 : n.attack) == null ? void 0 : he.payload) == null ? void 0 : ie.areaEffect) ?? {}) && (!Array.isArray(ee.targetSnapshots) || ee.targetSnapshots.length === 0))
      return (le = ui.notifications) == null || le.warn("Template placement did not affect any targets."), null;
    e.targetSnapshots = Array.isArray(ee.targetSnapshots) ? ee.targetSnapshots : [], e.templateGeometry = ee.templateGeometry ?? null, e.templatePlacement = ee.placement, n = await yl({ actor: i, payload: e, event: t }), a = (n == null ? void 0 : n.rollActor) ?? i;
  } else e.intent === "attack" && (delete e.targetSnapshots, delete e.templatePlacement, delete e.templateGeometry);
  let r = await Km({
    actor: i,
    rollActor: a,
    machineActor: (n == null ? void 0 : n.machineActor) ?? null,
    rollType: e.intent,
    skillId: e.key,
    domains: n.domains,
    payload: e,
    resolved: n,
    context: { event: t }
  });
  const s = await $c.prompt({
    actor: i,
    rollActor: a,
    basePayload: e,
    resolved: n,
    diceParts: {
      attribute: ((ze = n == null ? void 0 : n.pool) == null ? void 0 : ze.attribute) ?? 0,
      skill: ((Xe = n == null ? void 0 : n.pool) == null ? void 0 : Xe.skill) ?? 0,
      bonus: ((mt = n == null ? void 0 : n.pool) == null ? void 0 : mt.bonus) ?? 0,
      specialization: ((ft = n == null ? void 0 : n.pool) == null ? void 0 : ft.specialization) ?? 0
    },
    mods: r.mods,
    modTotal: r.total,
    options: {
      allowEdge: e.intent !== "initiative"
    }
  });
  if (!s) return null;
  if (e = Jm(s), n = await yl({ actor: i, payload: e, event: t }), a = (n == null ? void 0 : n.rollActor) ?? i, e.intent === "attack" && !((gt = (pt = n == null ? void 0 : n.attack) == null ? void 0 : pt.capabilityReport) != null && gt.isTemplated) && (delete e.targetSnapshots, delete e.templatePlacement, delete e.templateGeometry), e.intent === "attack" && e.weaponId) {
    const ee = ((ht = (nt = i.items) == null ? void 0 : nt.get) == null ? void 0 : ht.call(nt, e.weaponId)) ?? null;
    if ((Ye = ee == null ? void 0 : ee.isPersonalWeapon) != null && Ye.call(ee)) {
      const ge = String(e.payloadId ?? "").trim(), dn = String(((at = ee.system) == null ? void 0 : at.selectedPayloadId) ?? "").trim();
      if (ge && ge !== dn && await ((Ct = ee.setActivePayload) == null ? void 0 : Ct.call(ee, ge)), !((_t = ee.canConsumePayload) != null && _t.call(ee, { payloadId: ge }))) {
        const fi = (xt = ee.getPayloadState) == null ? void 0 : xt.call(ee, { payloadId: ge }), mn = fi != null && fi.payloadLabel ? ` (${fi.payloadLabel})` : "";
        return (St = ui.notifications) == null || St.warn(`Not enough payload${mn} for ${ee.name}.`), null;
      }
    }
  }
  r = await Km({
    actor: i,
    rollActor: a,
    machineActor: (n == null ? void 0 : n.machineActor) ?? null,
    rollType: e.intent,
    skillId: e.key,
    domains: n.domains,
    payload: e,
    resolved: n,
    context: { event: t }
  });
  const { mods: o, total: l } = r, { mods: c, total: u } = yN(e);
  let d = [...o, ...c], m = Number(l ?? 0) + Number(u ?? 0);
  const f = Number(((Kt = n == null ? void 0 : n.pool) == null ? void 0 : Kt.attribute) ?? 0) + Number(((Ft = n == null ? void 0 : n.pool) == null ? void 0 : Ft.skill) ?? 0) + Number(((R = n == null ? void 0 : n.pool) == null ? void 0 : R.bonus) ?? 0) + Number(((U = n == null ? void 0 : n.pool) == null ? void 0 : U.specialization) ?? 0), p = Math.max(0, f + Number(m ?? 0)), g = e.intent !== "initiative", h = g ? MN({ actor: a, ctx: n, payload: e }) : null, y = (ne = h == null ? void 0 : h.pre) != null && ne.spent ? 4 : Number(n.diceTarget ?? n.target ?? 5), b = {
    snapshot: ((xe = (Me = (ke = game.mwd) == null ? void 0 : ke.personalCombat) == null ? void 0 : Me.getSnapshot) == null ? void 0 : xe.call(Me, i)) ?? null
  };
  let S = null, w = null;
  if (n.intent === "machineRemedy") {
    if (w = await Io(e, {
      gmOverride: !!(e != null && e.gmOverride)
    }), !w.ok)
      return (Ze = ui.notifications) == null || Ze.warn(w.reason ?? "Unable to resolve the machine remedy."), null;
    if (S = await Dk(w), !(S != null && S.ok))
      return ($e = ui.notifications) == null || $e.warn((S == null ? void 0 : S.reason) ?? "Unable to spend the remedy action."), null;
  }
  for (const ee of bN(a, i)) {
    const ge = {
      snapshot: ((yt = ($t = (Fe = game.mwd) == null ? void 0 : Fe.personalCombat) == null ? void 0 : $t.getSnapshot) == null ? void 0 : yt.call($t, ee)) ?? null
    }, dn = hi({
      actor: ee,
      phase: "onBuildRoll",
      facts: ou({ actor: ee, resolved: n, payload: e, runtime: ge }),
      packet: {},
      options: { runtime: ge, consumeUsage: !0 }
    });
    await rn({ actor: ee, mutations: dn.mutations, runtime: ge });
  }
  g && ((vt = h == null ? void 0 : h.pre) != null && vt.spent) && ((Gt = h == null ? void 0 : h.pre) != null && Gt.poolKey) && await ((At = a.spendEdge) == null ? void 0 : At.call(a, h.pre.poolKey, 1));
  let T, k = 0, C = 0;
  if (n.rollType === "sum" && ((Ht = n.sum) != null && Ht.formula))
    T = await new Roll(n.sum.formula, n.sum.data ?? {}).evaluate(), k = Number(T.total ?? 0) + Number(m ?? 0);
  else {
    T = await new Roll(`${p}d6cs>=${y}`).evaluate();
    const ee = (st = T.dice) == null ? void 0 : st[0];
    k = Array.isArray(ee == null ? void 0 : ee.results) ? ee.results.filter((ge) => ge.success).length : 0, C = Array.isArray(ee == null ? void 0 : ee.results) ? ee.results.filter((ge) => ge.result === 1).length : 0;
  }
  if (n.intent === "initiative" && (T == null ? void 0 : T.total) != null) {
    const ee = { total: Number(T.total ?? 0) + Number(m ?? 0) }, ge = hi({
      actor: i,
      phase: "onInitiativeResolved",
      facts: dp({ actor: i, packet: ee, runtime: b }),
      packet: ee,
      options: { runtime: b, consumeUsage: !0 }
    });
    if (await rn({ actor: i, mutations: ge.mutations, runtime: b }), ge.modifiers.length) {
      const dn = ge.modifiers.reduce((fi, mn) => fi + Number(mn.value ?? 0), 0);
      d = d.concat(ge.modifiers), m += dn, k = Number(ge.packet.total ?? 0), await Xm({ actor: i, total: ge.packet.total ?? T.total }), n.breakdown = (n.breakdown ?? []).concat(ge.modifiers.map((fi, mn) => ({
        id: `traitInitiative${mn + 1}`,
        label: fi.label,
        value: Number(fi.value ?? 0)
      })));
    } else
      k = Number(ee.total ?? 0), await Xm({ actor: i, total: ee.total });
  }
  const I = dh(
    n,
    { successes: k, raw: (ce = T == null ? void 0 : T.toJSON) == null ? void 0 : ce.call(T) },
    null
    // opposed rolls can pass defender result later
  ), O = I == null ? void 0 : I.edgeEarned;
  if ((O == null ? void 0 : O.amount) > 0) {
    const ee = (Re = n == null ? void 0 : n.domains) != null && Re.includes("physical") ? "physical" : (_ = n == null ? void 0 : n.domains) != null && _.includes("mental") ? "mental" : (Q = n == null ? void 0 : n.domains) != null && Q.includes("social") ? "social" : null, ge = hN(a, ee);
    await ((de = a.gainEdge) == null ? void 0 : de.call(a, ge, O.amount)), I.edgeEarned.pool = ge;
  }
  n.intent === "overload" && await EN({ actor: i, passed: I.passed });
  let x = null;
  n.intent === "heatDangerCheck" && (x = await dN({ actor: i, ctx: n, outcomeModel: I }));
  let B = null, P = null;
  n.intent === "attack" ? B = await lh({
    attacker: i,
    ctx: n,
    outcomeModel: I
  }) : n.intent === "machineRemedy" && (P = await Ok(e, {
    gmOverride: !!(e != null && e.gmOverride),
    passed: !!(I != null && I.passed)
  }));
  let L = null, D = null;
  if (n.intent === "acquire" && (L = await fN({ attacker: i, ctx: n, outcomeModel: I })), n.intent === "targeting" && (D = await pN({ attacker: i, ctx: n, outcomeModel: I })), n.intent === "attack" && ((et = (fe = n.attack) == null ? void 0 : fe.ewContext) != null && et.activePacketId)) {
    const ee = Qu(i, e), ge = sa(ee);
    ge && (Hooks.callAll("mwd.beforeTargetingPacketConsume", {
      attacker: i,
      targetTokenUuid: n.attack.ewContext.targetTokenUuid,
      packetId: n.attack.ewContext.activePacketId,
      ctx: n
    }), await pk(ge, n.attack.ewContext.targetTokenUuid, n.attack.ewContext.activePacketId));
  }
  const G = VI({
    actor: i,
    payload: e,
    ctx: n,
    roll: T,
    target: y,
    pool: p,
    mods: d,
    modTotal: m,
    hits: k,
    ones: C,
    edge: h,
    outcomeModel: I
  });
  B && (G.attackResult = B), x && (G.heatDangerResult = x), n.intent === "machineRemedy" && (G.machineRemedy = n.machineRemedy ?? null, G.machineRemedyResult = {
    ...P ?? { ok: !1, reason: "Machine remedy result missing." },
    spend: S,
    context: w
  }), L && (G.ewAcquireResult = L), D && (G.ewTargetingResult = D), n.acquire && (G.acquire = n.acquire), n.targeting && (G.targeting = n.targeting);
  const V = await ga({ resolved: G });
  if (e.intent === "attack" && e.weaponId) {
    const ee = ((Si = (Wt = i.items) == null ? void 0 : Wt.get) == null ? void 0 : Si.call(Wt, e.weaponId)) ?? null;
    (Di = ee == null ? void 0 : ee.isPersonalWeapon) != null && Di.call(ee) && (await ((ot = ee.consumePayload) == null ? void 0 : ot.call(ee, { payloadId: e.payloadId })) || (Ge = ui.notifications) == null || Ge.warn(`Payload could not be consumed for ${ee.name}.`));
  }
  if (n.intent === "attack" && i.type === "battlemech") {
    const ee = Array.from(/* @__PURE__ */ new Set([
      ...(((qa = ($n = (mi = n == null ? void 0 : n.attack) == null ? void 0 : mi.weapon) == null ? void 0 : $n.machineWeaponGroup) == null ? void 0 : qa.weaponIds) ?? []).map((ge) => String(ge ?? "").trim()).filter(Boolean),
      ...e != null && e.weaponId ? [String(e.weaponId).trim()] : []
    ]));
    try {
      await tg(i, {
        weaponIds: ee,
        attackProfile: ((Va = n == null ? void 0 : n.attack) == null ? void 0 : Va.weapon) ?? null,
        reason: "attack resolution"
      });
    } catch (ge) {
      console.warn("MWD | Unable to record BattleMech attack heat", ge);
    }
  }
  return n.intent === "attack" ? await TN(i, e, { rollActor: a }) : n.intent === "acquire" ? await Qm(i, "acquireTarget", e, { rollActor: a }) : n.intent === "targeting" && await Qm(i, "generateFireSolution", e, { rollActor: a }), ChatMessage.create({
    speaker: ChatMessage.getSpeaker({ actor: i }),
    content: V,
    flags: {
      mwd: {
        payload: e,
        resolved: G
      }
    }
  });
}
function MN({ actor: i, ctx: e, payload: t }) {
  var p, g, h, y, b, S, w;
  const n = CN(e == null ? void 0 : e.domains), a = vN[n] ?? null, r = (a == null ? void 0 : a.a) ?? null, s = (a == null ? void 0 : a.b) ?? null, o = [r, s].filter(Boolean), l = !!((p = t == null ? void 0 : t.toggles) != null && p.useEdge) || !!(t != null && t.useEdge);
  let c = String(((h = (g = t == null ? void 0 : t.edge) == null ? void 0 : g.pre) == null ? void 0 : h.poolKey) ?? "").trim() || null;
  c && !o.includes(c) && (c = null);
  const u = l && c ? 1 : 0;
  let d = [...o];
  u && c && (d = d.filter((T) => T !== c));
  let m = String(((b = (y = t == null ? void 0 : t.edge) == null ? void 0 : y.post) == null ? void 0 : b.poolKey) ?? "").trim() || null;
  m && !d.includes(m) && (m = null);
  const f = Number(((w = (S = t == null ? void 0 : t.edge) == null ? void 0 : S.post) == null ? void 0 : w.spent) ?? 0) ? 1 : 0;
  return {
    domain: n,
    pools: a ? { a: r, b: s } : null,
    pre: { poolKey: c, spent: u },
    post: { poolKey: m, spent: f },
    allowed: { prePools: o, postPools: d }
  };
}
function CN(i) {
  return Array.isArray(i) ? i.includes("physical") ? "physical" : i.includes("mental") ? "mental" : i.includes("social") ? "social" : null : null;
}
const vN = {
  physical: { a: "grit", b: "chaos" },
  mental: { a: "insight", b: "rumor" },
  social: { a: "legend", b: "credibility" }
};
async function Xm({ actor: i, total: e }) {
  var o, l, c, u, d;
  const t = (l = (o = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : o.controlled) == null ? void 0 : l.find((m) => {
    var f;
    return ((f = m.actor) == null ? void 0 : f.id) === i.id;
  }), n = ((u = (c = i.getActiveTokens) == null ? void 0 : c.call(i, !0, !0)) == null ? void 0 : u[0]) ?? null, a = t ?? n;
  if (!a) {
    (d = ui.notifications) == null || d.warn("Initiative requires a token on the current scene.");
    return;
  }
  let r = game.combat;
  r || (r = await Combat.create({
    scene: canvas.scene.id,
    active: !0
  }));
  let s = r.combatants.find((m) => m.tokenId === a.id);
  if (!s) {
    const m = await r.createEmbeddedDocuments("Combatant", [{
      tokenId: a.id,
      actorId: i.id,
      sceneId: canvas.scene.id
    }]);
    s = m == null ? void 0 : m[0];
  }
  s && await s.update({ initiative: Number(e) });
}
async function EN({ actor: i, passed: e }) {
  e || await i.update({ "system.burn.overloaded": !0 });
}
const IN = /* @__PURE__ */ new Set(["physical", "mental", "social"]);
function NN(i) {
  if (i == null || i === "" || i === "—" || i === "–") return 0;
  const e = Number(i);
  return Number.isFinite(e) ? e : null;
}
function RN(i) {
  if (!i) return;
  const e = String(i).trim().toLowerCase();
  return IN.has(e) ? e : void 0;
}
function PN(...i) {
  const e = /* @__PURE__ */ new Set();
  return i.filter((t) => {
    if (!t) return !1;
    const n = t.uuid ?? t.id ?? t;
    return e.has(n) ? !1 : (e.add(n), !0);
  });
}
class DN {
  constructor() {
    z(this, "id", "mwd.itemModifiers");
    z(this, "label", "Item Modifiers");
  }
  collect(e) {
    var n, a;
    const t = [];
    for (const r of PN(e == null ? void 0 : e.actor, e == null ? void 0 : e.rollActor))
      for (const s of r.items ?? []) {
        const o = (a = (n = s.flags) == null ? void 0 : n.mwd) == null ? void 0 : a.modifiers;
        if (!(!Array.isArray(o) || o.length === 0))
          for (const l of o) {
            if (!l) continue;
            const c = NN(l.value);
            if (c === null) {
              console.warn("MWD | Dropping item modifier with invalid value", {
                actor: r.name,
                item: s.name,
                modifier: l
              });
              continue;
            }
            t.push({
              label: l.label ?? s.name,
              value: c,
              source: s.name,
              domain: RN(l.domain)
            });
          }
      }
    return t;
  }
}
function ON(...i) {
  const e = /* @__PURE__ */ new Set();
  return i.filter((t) => {
    if (!t) return !1;
    const n = t.uuid ?? t.id ?? t;
    return e.has(n) ? !1 : (e.add(n), !0);
  });
}
class LN {
  constructor() {
    z(this, "id", "mwd.statusEffects");
    z(this, "label", "Status Effects");
  }
  collect({ actor: e, rollActor: t } = {}) {
    var a;
    const n = [];
    for (const r of ON(e, t)) {
      const s = r == null ? void 0 : r.statuses;
      if (s)
        for (const o of s) {
          const l = Ur(o), c = l ? mo(l, r) ? l.modifierKey : "" : o, u = Ia == null ? void 0 : Ia[c];
          if ((a = u == null ? void 0 : u.mods) != null && a.length)
            for (const d of u.mods) {
              const m = Array.isArray(d.domains) ? d.domains : [], f = d.value;
              if (m.length)
                for (const p of m)
                  n.push({
                    label: u.label ?? o,
                    value: f,
                    source: "Status",
                    domain: p
                  });
              else
                n.push({
                  label: u.label ?? o,
                  value: f,
                  source: "Status"
                });
            }
        }
    }
    return n;
  }
}
class _N {
  constructor() {
    z(this, "id", "mwd.baseRollModifiers");
    z(this, "label", "Roll (Base)");
  }
  collect({ payload: e } = {}) {
    var s, o, l;
    const t = [], n = (s = e == null ? void 0 : e.modifiers) == null ? void 0 : s.manual;
    if (Array.isArray(n) && n.length) {
      for (const c of n) {
        if (!c) continue;
        const u = Number(c.value);
        !Number.isFinite(u) || u === 0 || t.push({
          id: c.id ?? void 0,
          label: c.label ?? "Manual modifier",
          value: u,
          source: "Manual",
          domain: c.domain ?? void 0
        });
      }
      return t;
    }
    const a = ((o = e == null ? void 0 : e.dialog) == null ? void 0 : o.otherMods) ?? ((l = e == null ? void 0 : e.modifiers) == null ? void 0 : l.otherMods) ?? (e == null ? void 0 : e.otherMods) ?? 0, r = Number(a);
    return Number.isFinite(r) && r !== 0 && t.push({
      id: "otherMods",
      label: "Other modifiers",
      value: r,
      source: "Roll"
    }), t;
  }
}
class xN {
  constructor() {
    z(this, "id", "mwd.condition");
    z(this, "label", "Condition");
  }
  collect({ actor: e, rollActor: t, rollType: n } = {}) {
    var c, u, d, m, f, p, g;
    const a = t ?? e;
    if (!a) return [];
    if (n === "edge") return [];
    const r = ((c = a.system) == null ? void 0 : c.derived) ?? {}, s = Number(
      ((u = r == null ? void 0 : r.condition) == null ? void 0 : u.physicalPenalty) ?? ((m = (d = r == null ? void 0 : r.monitors) == null ? void 0 : d.physical) == null ? void 0 : m.penalty) ?? 0
    ), o = Number(
      ((f = r == null ? void 0 : r.condition) == null ? void 0 : f.fatiguePenalty) ?? ((g = (p = r == null ? void 0 : r.monitors) == null ? void 0 : p.fatigue) == null ? void 0 : g.penalty) ?? 0
    ), l = [];
    return Number.isFinite(s) && s !== 0 && l.push({
      id: "conditionPhysical",
      label: "Physical Penalty",
      value: s,
      source: "Physical Track"
      // domain: "physical" // optional; leave unset to apply to all domains
    }), Number.isFinite(o) && o !== 0 && l.push({
      id: "conditionFatigue",
      label: "Fatigue Penalty",
      value: o,
      source: "Fatigue Track"
      // domain: "physical" // optional
    }), l;
  }
}
const $N = {
  id: "burn",
  async collect(i) {
    var a, r;
    const e = i.rollActor ?? i.actor;
    if (!e) return [];
    const t = Number(((r = (a = e.system) == null ? void 0 : a.burn) == null ? void 0 : r.value) ?? 0), n = Math.floor(t / 2);
    return n <= 0 ? [] : [{
      id: "burn",
      label: "Burn",
      value: -n,
      source: "Burn Track",
      domain: null
    }];
  }
};
function BN(...i) {
  const e = /* @__PURE__ */ new Set();
  return i.filter((t) => {
    if (!t) return !1;
    const n = t.uuid ?? t.id ?? t;
    return e.has(n) ? !1 : (e.add(n), !0);
  });
}
class zN {
  constructor() {
    z(this, "id", "mwd.lifeModules");
    z(this, "label", "Life Modules");
  }
  collect({ actor: e, rollActor: t, resolved: n } = {}) {
    return BN(t, e).flatMap((a) => VT({ actor: a, resolved: n }));
  }
}
function FN(...i) {
  const e = /* @__PURE__ */ new Set();
  return i.filter((t) => {
    if (!t) return !1;
    const n = t.uuid ?? t.id ?? t;
    return e.has(n) ? !1 : (e.add(n), !0);
  });
}
class HN {
  constructor() {
    z(this, "id", "mwd.traits");
    z(this, "label", "Traits");
  }
  collect({ actor: e, rollActor: t, resolved: n, payload: a } = {}) {
    return FN(t, e).flatMap((r) => {
      var o, l, c;
      const s = {
        snapshot: ((c = (l = (o = game.mwd) == null ? void 0 : o.personalCombat) == null ? void 0 : l.getSnapshot) == null ? void 0 : c.call(l, r)) ?? null
      };
      return hi({
        actor: r,
        phase: "onBuildRoll",
        facts: ou({ actor: r, resolved: n, payload: a, runtime: s }),
        packet: {},
        options: { runtime: s, consumeUsage: !1 }
      }).modifiers;
    });
  }
}
const UN = Object.freeze({
  attackCQPenalty: { value: -1, intents: ["attack"], label: "Attack CQ Penalty" },
  sensorPenalty: { value: -1, skills: ["perception", "technician"], label: "Sensor Penalty" },
  pilotingPenalty: { value: -1, skills: ["piloting"], label: "Piloting Penalty" }
});
class jN {
  constructor() {
    z(this, "id", "mwd.machineCriticals");
    z(this, "label", "Machine Criticals");
  }
  collect({ actor: e, resolved: t, payload: n } = {}) {
    var l, c, u;
    const a = _n(e);
    if (!a.length) return [];
    const r = String((t == null ? void 0 : t.intent) ?? (n == null ? void 0 : n.intent) ?? "").trim(), s = String(((c = (l = t == null ? void 0 : t.attack) == null ? void 0 : l.skill) == null ? void 0 : c.code) ?? ((u = t == null ? void 0 : t.skill) == null ? void 0 : u.code) ?? (n == null ? void 0 : n.key) ?? "").trim(), o = [];
    for (const d of a)
      for (const m of d.mods ?? []) {
        const f = UN[m];
        f && (f.intents && !f.intents.includes(r) || f.skills && !f.skills.includes(s) || o.push({
          id: `machineCrit.${d.id}.${m}`,
          label: `${d.label ?? "Machine Critical"}: ${f.label}`,
          value: f.value,
          source: "Machine Critical"
        }));
      }
    return o;
  }
}
function KN(i = null) {
  return (i == null ? void 0 : i.type) === A.actorTypes.vehicle || (i == null ? void 0 : i.type) === A.actorTypes.battlemech;
}
function GN(i = {}, e = {}) {
  var r, s, o;
  const t = ((r = i == null ? void 0 : i.attack) == null ? void 0 : r.weapon) ?? {}, n = String((t == null ? void 0 : t.category) ?? (t == null ? void 0 : t.attackKind) ?? (e == null ? void 0 : e.attackKind) ?? "").trim().toLowerCase(), a = String(((o = (s = i == null ? void 0 : i.attack) == null ? void 0 : s.skill) == null ? void 0 : o.code) ?? (t == null ? void 0 : t.skill) ?? (e == null ? void 0 : e.key) ?? "").trim().toLowerCase();
  return n !== "melee" && a !== "meleecombat" && a !== "melee";
}
function Sl(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? t : e;
}
function WN(i = null) {
  var s, o, l, c;
  const e = ((o = (s = i == null ? void 0 : i.system) == null ? void 0 : s.monitors) == null ? void 0 : o.heat) ?? {}, t = ((c = (l = i == null ? void 0 : i.system) == null ? void 0 : l.mwd) == null ? void 0 : c.heat) ?? {}, n = Math.max(0, Sl(e.value ?? t.current, 0)), a = Math.max(0, Sl(e.max ?? t.max ?? t.hardMax, 0)), r = du(t.thresholds ?? {}, a);
  return Math.max(0, Sl(uu(n, r).rangedDicePenalty, 0));
}
class qN {
  constructor() {
    z(this, "id", "mwd.machineStateEffects");
    z(this, "label", "Machine State Effects");
  }
  collect({ actor: e, resolved: t, payload: n } = {}) {
    var s, o, l, c, u;
    if (!KN(e)) return [];
    const a = String((t == null ? void 0 : t.intent) ?? (n == null ? void 0 : n.intent) ?? "").trim(), r = [];
    if (a === "attack") {
      const d = ww(e, {
        weaponGroupId: ((l = (o = (s = t == null ? void 0 : t.attack) == null ? void 0 : s.weapon) == null ? void 0 : o.machineWeaponGroup) == null ? void 0 : l.id) ?? (n == null ? void 0 : n.weaponGroupId),
        weaponId: n == null ? void 0 : n.weaponId,
        weapon: (c = t == null ? void 0 : t.attack) == null ? void 0 : c.weapon,
        rangeBand: ((u = t == null ? void 0 : t.attack) == null ? void 0 : u.rangeBand) ?? (n == null ? void 0 : n.rangeBand)
      });
      if (d && r.push({
        id: "machineState.attackDice",
        label: "Machine State",
        value: d,
        source: "Machine State"
      }), e.type === A.actorTypes.battlemech && GN(t, n)) {
        const m = WN(e);
        m && r.push({
          id: "battlemechHeat.rangedDice",
          label: "Overheat",
          value: -m,
          source: "Heat"
        });
      }
    }
    if (a === "acquire") {
      const d = Sw(e);
      d && r.push({
        id: "machineState.acquireDice",
        label: "Sensors",
        value: d,
        source: "Machine State"
      });
    }
    if (a === "targeting") {
      const d = Aw(e);
      d && r.push({
        id: "machineState.targetingDice",
        label: "Fire Solution",
        value: d,
        source: "Machine State"
      });
    }
    if (a === "skill" && String((n == null ? void 0 : n.key) ?? "").trim() === "piloting") {
      const d = bw(e);
      d && r.push({
        id: "machineState.pilotingDice",
        label: "Piloting State",
        value: d,
        source: "Machine State"
      });
    }
    return r;
  }
}
function VN(i) {
  return (i == null ? void 0 : i.type) === A.actorTypes.vehicle || (i == null ? void 0 : i.type) === A.actorTypes.battlemech;
}
class YN {
  constructor() {
    z(this, "id", "mwd.ewTrackingPenalty");
    z(this, "label", "EW Tracking Penalty");
  }
  collect({ actor: e, resolved: t, payload: n } = {}) {
    var f, p, g, h;
    if (!VN(e)) return [];
    if (String((t == null ? void 0 : t.intent) ?? (n == null ? void 0 : n.intent) ?? "").trim() !== "attack") return [];
    const r = ((f = t == null ? void 0 : t.attack) == null ? void 0 : f.targets) ?? [];
    if (!r.length) return [];
    const s = r[0], o = String((s == null ? void 0 : s.tokenId) ?? "").trim();
    if (!o) return [];
    const l = ((g = (p = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : p.get) == null ? void 0 : g.call(p, o)) ?? null, c = (l == null ? void 0 : l.actor) ?? null, u = [], d = Bg(c, null);
    d && u.push({
      id: "ew.trackingPenalty",
      label: "Tracking Penalty",
      value: -d,
      source: "EW"
    });
    const m = ((h = t == null ? void 0 : t.attack) == null ? void 0 : h.machineMotion) ?? ey({ targetActor: c, payload: n });
    return m != null && m.movementTrackingPenalty && u.push({
      id: "machineMotion.tracking",
      label: `Target Movement (${Number(m.trackingHexes ?? 0)} hex)`,
      value: m.movementTrackingPenalty,
      source: "Motion"
    }), m != null && m.jumpTrackingPenalty && u.push({
      id: "machineMotion.jumpTracking",
      label: "Target Jumped",
      value: m.jumpTrackingPenalty,
      source: "Motion"
    }), u;
  }
}
function JN(i) {
  return (i == null ? void 0 : i.type) === A.actorTypes.vehicle || (i == null ? void 0 : i.type) === A.actorTypes.battlemech;
}
class QN {
  constructor() {
    z(this, "id", "mwd.ewTargetingData");
    z(this, "label", "EW Targeting Data");
  }
  collect({ actor: e, resolved: t, payload: n } = {}) {
    var s, o;
    if (!JN(e)) return [];
    if (String((t == null ? void 0 : t.intent) ?? (n == null ? void 0 : n.intent) ?? "").trim() !== "attack") return [];
    const r = Number(((o = (s = t == null ? void 0 : t.attack) == null ? void 0 : s.ewContext) == null ? void 0 : o.targetingDataValue) ?? 0);
    return r ? [{
      id: "ew.targetingData",
      label: "Targeting Data",
      value: r,
      source: "EW"
    }] : [];
  }
}
function zc(i, e = 0, t = 1) {
  const n = Number(i);
  return Number.isFinite(n) ? Math.min(t, Math.max(e, n)) : e;
}
function XN(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? t : e;
}
function rr(i, e = 0) {
  return Math.max(e, XN(i, e));
}
function Zm(i, e, t) {
  return t <= e ? i >= t ? 1 : 0 : zc((i - e) / (t - e), 0, 1);
}
function ZN(i = {}) {
  return !!(i && typeof i == "object" && "current" in i && "thresholds" in i);
}
function eR(i = null) {
  var e;
  return !i || i != null && i.type && i.type !== "battlemech" ? null : ZN(i) ? i : (e = i == null ? void 0 : i.actor) != null && e.type && i.actor.type !== "battlemech" ? null : an(i);
}
function tR(i = null) {
  var u;
  const e = eR(i);
  if (!e) return null;
  const t = rr(e.current, 0), n = e.thresholds ?? {}, a = rr(n.runningHot ?? n.hot, 0), r = Math.max(a, rr(n.overheated ?? n.overheat, a)), s = Math.max(r, rr(n.shutdown ?? n.danger, r || 1)), o = rr((u = e == null ? void 0 : e.penalties) == null ? void 0 : u.dangerLevel, t >= s ? t - s + 1 : 0), l = zc(s > 0 ? t / s : 0, 0, 1);
  if (t < a || a <= 0)
    return {
      active: !1,
      heat: t,
      normalized: l,
      band: "safe",
      warmth: 0,
      glow: 0,
      shimmer: 0,
      blur: 0,
      pulseAmplitude: 0,
      pulseSpeed: 0,
      animated: !1
    };
  if (t < r) {
    const d = Zm(t, a, Math.max(a + 1, r - 1));
    return {
      active: !0,
      heat: t,
      normalized: l,
      band: "hot",
      warmth: 0.12 + d * 0.16,
      glow: 0.04 + d * 0.16,
      shimmer: 0.02 + d * 0.1,
      blur: 0,
      pulseAmplitude: 0,
      pulseSpeed: 0,
      animated: !1
    };
  }
  if (t < s) {
    const d = Zm(t, r, Math.max(r + 1, s - 1));
    return {
      active: !0,
      heat: t,
      normalized: l,
      band: "overheat",
      warmth: 0.3 + d * 0.24,
      glow: 0.18 + d * 0.24,
      shimmer: 0.16 + d * 0.25,
      blur: d * 0.18,
      pulseAmplitude: 0.2 + d * 0.15,
      pulseSpeed: 0,
      animated: !0
    };
  }
  const c = zc((o - 1) / 4, 0, 1);
  return {
    active: !0,
    heat: t,
    normalized: l,
    band: "danger",
    warmth: 0.58 + c * 0.1,
    glow: 0.42 + c * 0.25,
    shimmer: 0.42 + c * 0.5,
    blur: 0.18 + c * 0.1,
    pulseAmplitude: 0.3 + c * 0.15,
    pulseSpeed: 0.95 + c * 0.55,
    animated: !0
  };
}
const iR = Object.freeze({
  uTime: 0,
  uHeat: 0,
  uShimmer: 0,
  uGlow: 0,
  uPulse: 0,
  uTopBias: 1.75
}), nR = `
attribute vec2 aVertexPosition;
attribute vec2 aTextureCoord;

uniform mat3 projectionMatrix;

varying vec2 vTextureCoord;

void main(void) {
  vTextureCoord = aTextureCoord;
  gl_Position = vec4((projectionMatrix * vec3(aVertexPosition, 1.0)).xy, 0.0, 1.0);
}
`, aR = `
varying vec2 vTextureCoord;

uniform sampler2D uSampler;
uniform float uTime;
uniform float uHeat;
uniform float uShimmer;
uniform float uGlow;
uniform float uPulse;
uniform float uTopBias;

float hash(vec2 p) {
  return fract(sin(dot(p, vec2(127.1, 311.7))) * 43758.5453123);
}

void main(void) {
  vec2 uv = vTextureCoord;
  float topBias = pow(clamp(1.0 - uv.y, 0.0, 1.0), max(0.001, uTopBias));
  float wave = sin((uv.y * 46.0) + (uTime * 3.1)) * 0.0025 * uShimmer * (0.3 + topBias);
  float jitter = (hash(vec2(floor(uv.y * 64.0), floor((uTime * 8.0) + (uv.x * 24.0)))) - 0.5)
    * 0.0020
    * uShimmer
    * (0.2 + topBias);
  vec2 sampleUv = uv + vec2(wave + jitter, 0.0);

  vec4 base = texture2D(uSampler, sampleUv);
  float pulse = 1.0 + (sin(uTime * 4.0) * uPulse);
  float glowFactor = uGlow * pulse * (0.35 + (topBias * 0.65));

  vec3 warmed = base.rgb;
  warmed.r += (uHeat * 0.18) + (glowFactor * 0.18);
  warmed.g += (uHeat * 0.04) + (glowFactor * 0.04);
  warmed.b = max(0.0, warmed.b - (uHeat * 0.08));

  vec3 finalColor = mix(base.rgb, warmed, clamp((uHeat * 0.85) + (glowFactor * 0.55), 0.0, 1.0));
  finalColor += vec3(1.0, 0.38, 0.08) * glowFactor * 0.10 * base.a;

  gl_FragColor = vec4(finalColor, base.a);
}
`;
class rR {
  constructor(e = "", t = "", n = {}) {
    this.uniforms = { ...n }, this.enabled = !0, this.padding = 0, this.autoFit = !0;
  }
}
function sR() {
  var i, e, t, n, a;
  return ((n = (t = (e = (i = globalThis.foundry) == null ? void 0 : i.canvas) == null ? void 0 : e.rendering) == null ? void 0 : t.filters) == null ? void 0 : n.AbstractBaseFilter) ?? ((a = globalThis.PIXI) == null ? void 0 : a.Filter) ?? rR;
}
const oR = sR();
class lR extends oR {
  constructor(e = {}) {
    super(nR, aR, {
      ...iR,
      ...e
    }), this.padding = 6, this.autoFit = !0;
  }
  static create(e = {}) {
    return new this(e);
  }
  setVisualState(e = {}, { photosensitive: t = !1 } = {}) {
    const n = this.uniforms ?? {};
    n.uHeat = Number((e == null ? void 0 : e.normalized) ?? 0) || 0, n.uGlow = Number((e == null ? void 0 : e.glow) ?? 0) || 0, n.uTopBias = 1.75, n.uShimmer = t ? 0 : Number((e == null ? void 0 : e.shimmer) ?? 0) || 0, n.uPulse = t ? 0 : Number((e == null ? void 0 : e.pulseAmplitude) ?? 0) || 0;
  }
  setTime(e = 0) {
    const t = this.uniforms ?? {};
    t.uTime = Number(e) || 0;
  }
}
function cR(i = {}) {
  return lR.create(i);
}
function Sa(i, e) {
  var t, n;
  return (n = (t = globalThis.foundry) == null ? void 0 : t.utils) != null && n.hasProperty ? foundry.utils.hasProperty(i, e) : String(e ?? "").split(".").filter(Boolean).reduce((a, r) => a == null ? void 0 : a[r], i) !== void 0;
}
function uR() {
  var i, e, t;
  return ((i = globalThis.PIXI) == null ? void 0 : i.ColorMatrixFilter) ?? ((t = (e = globalThis.PIXI) == null ? void 0 : e.filters) == null ? void 0 : t.ColorMatrixFilter) ?? null;
}
function dR() {
  var i, e, t;
  return ((i = globalThis.PIXI) == null ? void 0 : i.BlurFilter) ?? ((t = (e = globalThis.PIXI) == null ? void 0 : e.filters) == null ? void 0 : t.BlurFilter) ?? null;
}
function ef(i) {
  var e;
  return String(((e = i == null ? void 0 : i.document) == null ? void 0 : e.uuid) ?? (i == null ? void 0 : i.id) ?? "").trim();
}
function ly(i) {
  return Array.isArray(i) ? i : [];
}
function Fc(i, e = 0) {
  const t = Number(i);
  return Number.isFinite(t) ? Math.max(e, t) : e;
}
function mR(i, e) {
  var t, n, a;
  return !i || !e ? !1 : ((t = i.actor) == null ? void 0 : t.id) === e.id ? !0 : String(((a = (n = i == null ? void 0 : i.document) == null ? void 0 : n.baseActor) == null ? void 0 : a.id) ?? "").trim() === String(e.id ?? "").trim();
}
function tf(i = [], e = []) {
  return ly(i).filter((t) => t && !e.includes(t));
}
function fR(i = 0) {
  const e = Fc(i, 0);
  return [
    1 + e * 0.34,
    e * 0.05,
    0,
    0,
    0,
    e * 0.01,
    1 + e * 0.07,
    0,
    0,
    0,
    0,
    e * 0.03,
    Math.max(0.62, 1 - e * 0.24),
    0,
    0,
    0,
    0,
    0,
    1,
    0
  ];
}
function pR(i, e) {
  var t;
  (t = i == null ? void 0 : i.add) == null || t.call(i, e);
}
function gR(i, e) {
  var t;
  (t = i == null ? void 0 : i.remove) == null || t.call(i, e);
}
function hR(i = {}) {
  return Sa(i, "system.monitors.heat.value") || Sa(i, "system.mwd.heat.current") || Sa(i, "system.mwd.heat.thresholds") || Sa(i, "system.mwd.heat.thresholds.runningHot") || Sa(i, "system.mwd.heat.thresholds.overheated") || Sa(i, "system.mwd.heat.thresholds.shutdown");
}
class yR {
  constructor({
    settingKey: e = rf,
    tickerAdd: t = pR,
    tickerRemove: n = gR
  } = {}) {
    this.settingKey = e, this._records = /* @__PURE__ */ new Map(), this._animatedKeys = /* @__PURE__ */ new Set(), this._tickerAdd = t, this._tickerRemove = n, this._tickerFn = null;
  }
  init() {
    Hooks.on("canvasReady", () => this.refreshAll()), Hooks.on("refreshToken", (e) => this._onRefreshToken(e)), Hooks.on("updateActor", (e, t) => this._onUpdateActor(e, t)), Hooks.on("canvasTearDown", () => this.onCanvasTearDown()), Hooks.on("updateSetting", (e) => this._onUpdateSetting(e));
  }
  isEnabled() {
    try {
      return game.settings.get(M, this.settingKey) !== !1;
    } catch {
      return !0;
    }
  }
  syncActor(e) {
    var t;
    if (!(!e || e.type !== "battlemech" || !(canvas != null && canvas.ready)))
      for (const n of ((t = canvas.tokens) == null ? void 0 : t.placeables) ?? [])
        mR(n, e) && this.syncToken(n);
  }
  refreshAll() {
    var e, t;
    if (canvas != null && canvas.ready)
      for (const n of ((e = canvas.tokens) == null ? void 0 : e.placeables) ?? [])
        ((t = n == null ? void 0 : n.actor) == null ? void 0 : t.type) === "battlemech" ? this.syncToken(n) : this.clearToken(n);
  }
  syncToken(e) {
    if (!e) return null;
    if (!this.isEnabled())
      return this.clearToken(e), null;
    const t = e.actor ?? null, n = tR(t);
    if (!(n != null && n.active))
      return this.clearToken(e), n;
    const a = this.ensure(e);
    return a && this._applyState(a, n, {
      photosensitive: !!(canvas != null && canvas.photosensitiveMode)
    }), n;
  }
  ensure(e) {
    const t = this._getDisplayObject(e);
    if (!t) return null;
    const n = ef(e);
    if (!n) return null;
    let a = this._records.get(n) ?? null;
    if (a && a.display !== t && (this._detachRecord(a), this._records.delete(n), this._animatedKeys.delete(n), a = null), a)
      a.token = e, a.display = t;
    else {
      const r = this._createColorFilter(), s = this._createBlurFilter(), o = cR();
      a = {
        key: n,
        token: e,
        display: t,
        color: r,
        blur: s,
        shimmer: o,
        time: 0,
        animated: !1
      }, this._records.set(n, a);
    }
    return this._attachRecord(a), a;
  }
  clearToken(e) {
    const t = ef(e);
    if (!t) return;
    const n = this._records.get(t) ?? null;
    n && (this._detachRecord(n), this._records.delete(t), this._animatedKeys.delete(t), this._teardownTickerIfIdle());
  }
  onCanvasTearDown() {
    for (const e of Array.from(this._records.keys())) {
      const t = this._records.get(e);
      t && this._detachRecord(t);
    }
    this._records.clear(), this._animatedKeys.clear(), this._teardownTicker(!0);
  }
  _onRefreshToken(e) {
    var t;
    if (((t = e == null ? void 0 : e.actor) == null ? void 0 : t.type) !== "battlemech") {
      this.clearToken(e);
      return;
    }
    this.syncToken(e);
  }
  _onUpdateActor(e, t) {
    (e == null ? void 0 : e.type) === "battlemech" && hR(t) && this.syncActor(e);
  }
  _onUpdateSetting(e) {
    if (String((e == null ? void 0 : e.key) ?? "") === `${M}.${this.settingKey}`) {
      if (!this.isEnabled()) {
        this.onCanvasTearDown();
        return;
      }
      this.refreshAll();
    }
  }
  _getDisplayObject(e) {
    return (e == null ? void 0 : e.mesh) ?? null;
  }
  _createColorFilter() {
    const e = uR();
    return e ? new e() : null;
  }
  _createBlurFilter() {
    if (typeof (canvas == null ? void 0 : canvas.createBlurFilter) == "function")
      return canvas.createBlurFilter(1e-4, 2);
    const e = dR();
    return e ? new e() : null;
  }
  _ownedFilters(e) {
    return [e.shimmer, e.color, e.blur].filter(Boolean);
  }
  _attachRecord(e) {
    var r;
    const t = ly((r = e.display) == null ? void 0 : r.filters), n = this._ownedFilters(e), a = tf(t, n).concat(n);
    e.display.filters = a.length ? a : null;
  }
  _detachRecord(e) {
    var n, a;
    const t = e.display;
    if (t) {
      const r = tf(t.filters, this._ownedFilters(e));
      t.filters = r.length ? r : null;
    }
    e.blur && ((a = (n = canvas == null ? void 0 : canvas.blurFilters) == null ? void 0 : n.delete) == null || a.call(n, e.blur));
  }
  _applyState(e, t, { photosensitive: n = !1 } = {}) {
    if (e.color && (typeof e.color.reset == "function" && e.color.reset(), e.color.matrix = fR(t.warmth), e.color.alpha = 1, e.color.enabled = !0), e.blur) {
      const r = Fc(t.blur, 0);
      e.blur.enabled = r > 1e-3, "blur" in e.blur && (e.blur.blur = r), "strength" in e.blur && (e.blur.strength = r), "blurX" in e.blur && (e.blur.blurX = r), "blurY" in e.blur && (e.blur.blurY = r);
    }
    e.shimmer && (e.shimmer.enabled = !0, e.shimmer.setVisualState(t, { photosensitive: n }), n ? e.shimmer.setTime(0) : e.shimmer.setTime(e.time)), !!t.animated && !n ? (this._animatedKeys.add(e.key), e.animated = !0, this._ensureTicker()) : (e.animated = !1, e.time = 0, this._animatedKeys.delete(e.key), this._teardownTickerIfIdle());
  }
  _ensureTicker() {
    var e;
    this._tickerFn || !((e = canvas == null ? void 0 : canvas.app) != null && e.ticker) || (this._tickerFn = (t) => this._onTick(t), this._tickerAdd(canvas.app.ticker, this._tickerFn));
  }
  _teardownTicker(e = !1) {
    var t;
    !this._tickerFn || !((t = canvas == null ? void 0 : canvas.app) != null && t.ticker) || !e && this._animatedKeys.size > 0 || (this._tickerRemove(canvas.app.ticker, this._tickerFn), this._tickerFn = null);
  }
  _teardownTickerIfIdle() {
    this._teardownTicker(!1);
  }
  _onTick(e) {
    var n, a;
    if (canvas != null && canvas.photosensitiveMode) return;
    const t = Fc(((e == null ? void 0 : e.deltaMS) ?? 16.6667) / 1e3, 0);
    for (const r of Array.from(this._animatedKeys)) {
      const s = this._records.get(r);
      if (!(s != null && s.display) || s.display.destroyed) {
        this._animatedKeys.delete(r), this._records.delete(r);
        continue;
      }
      s.time += t, (a = (n = s.shimmer) == null ? void 0 : n.setTime) == null || a.call(n, s.time);
    }
    this._teardownTickerIfIdle();
  }
}
function bR() {
  Object.assign(CONFIG.fontDefinitions, {
    "MWD UI": {
      editor: !0,
      fonts: [
        { urls: ["systems/mwd/fonts/Exo2/Exo2-Regular.woff2"], weight: 400, style: "normal" },
        { urls: ["systems/mwd/fonts/Exo2/Exo2-Italic.woff2"], weight: 400, style: "italic" },
        { urls: ["systems/mwd/fonts/Exo2/Exo2-Bold.woff2"], weight: 700, style: "normal" }
      ]
    },
    "MWD Display": {
      editor: !1,
      fonts: [
        { urls: ["systems/mwd/fonts/btclassic/BattletechOldStyle.woff2"], weight: 400, style: "normal" }
      ]
    },
    "MWD Body": {
      editor: !0,
      fonts: [
        { urls: ["systems/mwd/fonts/bitter/Bitter-Regular.woff2"], weight: 400, style: "normal" },
        { urls: ["systems/mwd/fonts/bitter/Bitter-Bold.woff2"], weight: 700, style: "normal" }
      ]
    },
    "MWD Numeric": {
      editor: !1,
      fonts: [
        { urls: ["systems/mwd/fonts/anta/Anta-Regular.woff2"], weight: 400, style: "normal" }
      ]
    },
    "Material Symbols Rounded": {
      editor: !1,
      fonts: [
        { urls: ["systems/mwd/fonts/Icons/MaterialSymbolsRounded.woff2"], weight: 400, style: "normal" }
      ]
    },
    "MWD Logo": {
      editor: !1,
      fonts: [
        { urls: ["systems/mwd/fonts/btclassic/BTLogo_old.woff2"], weight: 400, style: "normal" }
      ]
    }
  });
}
function SR() {
  return {
    get(i) {
      return Lt(i);
    },
    getSkills({ withKnowledge: i = !1 } = {}) {
      return _s();
    },
    list() {
      return _s();
    }
  };
}
function AR() {
  return {
    get(i) {
      return En(i);
    },
    list() {
      return ko();
    },
    listByType(i) {
      return Nu(i);
    },
    getTypeLabel(i) {
      return za(i);
    },
    evaluate(i) {
      return Nn(i);
    }
  };
}
function wR() {
  return {
    normalizeQualitySystem(i) {
      return Ii(i);
    },
    getEditorConfig() {
      return sp();
    },
    evaluatePhase(i) {
      return hi(i);
    },
    applyMutations(i) {
      return rn(i);
    },
    buildRollFacts(i) {
      return ou(i);
    },
    buildActionCostFacts(i) {
      return up(i);
    },
    buildBurnFacts(i) {
      return fs(i);
    },
    buildInitiativeFacts(i) {
      return dp(i);
    },
    buildDamageFacts(i) {
      return mp(i);
    },
    buildEdgeFacts(i) {
      return Ll(i);
    },
    buildEndOfActivationFacts(i) {
      return fp(i);
    }
  };
}
class Xu {
  static start() {
    const e = new Xu();
    Hooks.once("init", () => e.onInit()), Hooks.once("ready", () => e.onReady());
  }
  async onInit() {
    console.log(Le + "AnarchySystem.onInit"), game.system.mwd = this, game.system.anarchy = this, game.mwd ?? (game.mwd = {}), bR(), WC(), fM("mwd"), game.mwd.roll = Ym, game.mwd.attacks = km, game.mwd.personalCombat = F, game.mwd.harm = Jt, game.mwd.machineHeat = {
      adjustPendingHeat: $w,
      buildModel: an,
      recordAttackHeat: tg,
      resolvePendingHeat: ku,
      setPendingHeat: Tu
    }, game.mwd.tokenHeatFx = new yR(), game.mwd.tokenHeatFx.init(), this.roll = Ym, this.attacks = km, this.personalCombat = F, this.harm = Jt, this.machineHeat = game.mwd.machineHeat, this.tokenHeatFx = game.mwd.tokenHeatFx, this.skills = SR(), this.lifeModules = AR(), this.traits = wR(), this.remoteCall = new Rl(), game.system.mwd.skills = this.skills, game.system.mwd.lifeModules = this.lifeModules, game.system.mwd.traits = this.traits, game.mwd.skills = this.skills, game.mwd.lifeModules = this.lifeModules, game.mwd.traits = this.traits, Oe.init(), this.modifiers = new Te(), ri.register(new DN()), ri.register(new LN()), ri.register(new _N()), ri.register(new xN()), ri.register($N), ri.register(new zN()), ri.register(new HN()), ri.register(new jN()), ri.register(new qN()), ri.register(new YN()), ri.register(new QN()), ri.register(new $k()), it.init(), Handlebars.registerHelper("mwdClassList", (e) => Array.isArray(e) ? e.join(" ") : typeof e == "string" ? e : ""), this.actorClasses = {
      character: em,
      npc: em,
      vehicle: Dg,
      battlemech: Lk
    }, this.hooks = new ea(), this.styles = new k0(), this.handlebarsManager = new Ru(), F.init(), JM.register(), Hooks.on("updateSetting", (e) => {
      (e == null ? void 0 : e.key) === `${M}.statusConditionCatalog` && dd();
    }), console.log(Le + "AnarchySystem.onInit | loading system"), CONFIG.ANARCHY = q, CONFIG.Combat.initiative = { formula: "2d6" }, dd(), CONFIG.Actor.documentClass = yE, CONFIG.Item.documentClass = Ua, Ua.init(), yh(), oS(), Gv(), cE(), await mE(), console.log(Le + "AnarchySystem.onInit | done");
  }
  async onReady() {
    var t, n, a;
    if (console.log(Le + "AnarchySystem.onReady"), await F.onReady(), (a = (n = (t = game.mwd) == null ? void 0 : t.tokenHeatFx) == null ? void 0 : n.refreshAll) == null || a.call(n), !game.user.isGM) return;
    await UT();
    const e = game.settings.get(M, "enableGMGadget");
    if (!e) {
      console.log(`${Le}GMManager render skipped (enableGMGadget=false)`);
      return;
    }
    game.mwd = game.mwd ?? {}, game.mwd.gmGadget = () => pM({ systemId: M }).render({ force: !0 }), e && game.mwd.gmGadget();
  }
}
Xu.start();
//# sourceMappingURL=index.mjs.map
