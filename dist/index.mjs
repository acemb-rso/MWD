var oI = Object.defineProperty;
var lI = Object.getPrototypeOf;
var cI = Reflect.get;
var lb = (t) => {
  throw TypeError(t);
};
var uI = (t, e, n) => e in t ? oI(t, e, { enumerable: !0, configurable: !0, writable: !0, value: n }) : t[e] = n;
var F = (t, e, n) => uI(t, typeof e != "symbol" ? e + "" : e, n), Fd = (t, e, n) => e.has(t) || lb("Cannot " + n);
var X = (t, e, n) => (Fd(t, e, "read from private field"), n ? n.call(t) : e.get(t)), Fe = (t, e, n) => e.has(t) ? lb("Cannot add the same private member more than once") : e instanceof WeakSet ? e.add(t) : e.set(t, n), De = (t, e, n, i) => (Fd(t, e, "write to private field"), i ? i.call(t, n) : e.set(t, n), n), E = (t, e, n) => (Fd(t, e, "access private method"), n);
var vi = (t, e, n) => cI(lI(t), n, e);
const z = {
  TYPES: {
    Actor: {
      character: "Character",
      vehicle: "Vehicle/drone",
      battlemech: "Battlemech"
    },
    Item: {
      contact: "Contact",
      gear: "Gear",
      consumable: "Consumable",
      quality: "Trait",
      assetModule: "Asset Module",
      skill: "Skill",
      mechWeapon: "Mech-Scale Weapon",
      personalWeapon: "Personal Weapon",
      weaponPayload: "Weapon Payload",
      armor: "Armor"
    }
  },
  settings: {
    defaultCssClass: {
      name: "Style of Destiny UI",
      hint: "Select the style used for actors, items and GM Manager"
    },
    skillSet: {
      name: "Skill set",
      hint: "Select the set of skills to use"
    },
    gmDifficulty: {
      name: "Default difficulty pools",
      hint: "The default difficulty pools, represented as 'Trivial:4,Easy:6' ...",
      default: "Trivial:4,Easy:6,Average:8,Hard:10,Very hard:12",
      chatMessage: "The difficulty {difficulty} ({pool}d6) is {success}"
    },
    damageMode: {
      name: "Damage application",
      hint: "Determine how Armor / Damage Resistance (RD) applies",
      values: {
        resistanceArmorMonitor: "MechWarrior: Destiny: Resistance, Armor, then Monitor",
        armorResistanceMonitor: "Armor, Resistance, Monitor",
        armorGivesResistance: "RD = Armor/3 + Str/4, damaged per blow, AA ignores armor",
        armorGiveResistanceHitsAvoid: "RD = Armor/3, damaged per blow, AA reduces resistance"
      }
    },
    useDestinyMechanics: {
      name: "Use Destiny opposed roll mechanics",
      hint: "Roll 2D6 and add the computed pool as a modifier instead of counting successes."
    }
  },
  chat: {
    blindMessageToGM: "Blind message from {user}:<br>{message}",
    sufferedDrain: "{actor} suffered a drain of {drain}",
    noDrain: "{actor} did not suffer any drain",
    defendAttack: "Defense against {success} success",
    partiallyDefended: "Attack exceeds defense of {success} success",
    fullyDefended: "The attack is fully defended",
    applyDamage: "Apply {damage}"
  },
  chat_actions: {
    rollDice: {
      title: "Roll Destiny dice",
      instruction: "Number of dice to roll: ",
      error: "Veuillez entrer un nombre valide de dés.",
      result: "Roll of {count}d6, {success} success! {ones} dice with a value of 1"
    }
  },
  user: {
    selectedTokenActors: "Selected actor tokens"
  },
  common: {
    newEntry: "New entry...",
    newName: "New {type}",
    cancel: "Cancel",
    add: "Add",
    resize: "Resize",
    say: "Say:",
    edit: "Edit",
    activate: "Activate",
    del: "Remove",
    favorite: "Favorites",
    addFavorite: "Add to favorites",
    delFavorite: "Remove from favorites",
    viewMode: "View mode",
    editMode: "Edit mode",
    attach: "Change owner",
    attachCopy: "Give a copy",
    roll: {
      button: "Roll",
      title: "Roll {name} {specialization}",
      attribute: "Attribute",
      attribute2: "Second attribute",
      modifiers: {
        edge: "Use edge",
        edgePool: "Spend from",
        specialization: "Specialization",
        poolModifiers: "Asset Modules",
        social: {
          credibility: "Credibility",
          rumor: "Rumor"
        },
        anarchyDisposition: "Anarchy - Dispositions",
        anarchyRisk: "Anarchy - Take risks",
        glitch: "Glitch dice",
        convergence: "GOD Convergence",
        drain: "Drain",
        wounds: "Wounds modifiers",
        weaponRange: "Range",
        weaponArea: "Multiple targets",
        other: "Other modifiers",
        virtualReality: "Virtual Reality",
        reduced: "Pool reduced",
        reroll: "Rerolls",
        rerollForced: "Rerolls successes",
        opponentReroll: "Force opponent rerolls",
        opponentPool: "Reduce opponent pool"
      },
      rollTheme: {
        dicePool: "Dice pool",
        reroll: "Failure rerolls",
        removed: "Success to reroll",
        rerollRemoved: "Reroll removed success",
        glitch: "Glitch dice",
        drain: "Drain",
        convergence: "Convergence",
        anarchyRisk: "Anarchy risk dice"
      },
      opponentRoll: "Opponent roll",
      totalSuccess: "Total successes",
      success: "Successes",
      risk: {
        prowess: "Prowess",
        mixed: "Prowess and Glitch",
        nothing: "no effect",
        glitch: "Glitch"
      },
      rerollSuccess: "Successes after reroll",
      rerollForcedLoss: "Forced reroll",
      rerollForcedSuccess: "Forced reroll successes"
    },
    monitorValue: "Monitor value",
    confirmation: {
      del: "Confirm removal",
      delItem: "Confirm removal of {name} ({type})",
      delowner: "Confirm detach from owner {name}",
      attach: "Attach actor to owner",
      attachOrCopy: "Attach {ownedType} {ownedName} to {ownerType} {ownerName}, or create a copy first"
    },
    selection: {
      actorSettingMarks: "Select actor setting Marks on {name}"
    },
    errors: {
      insufficient: "Not enough {resource}: required {required}, available {available}",
      outOfRange: "Cannot set {resource} to {value}, out of range [{min} , {max}]",
      onlyGM: "Only allowed for GM",
      noEdgeForActor: "{actor} is a {actorType}, so it cannot use Edge",
      expectedType: "Item is of type {type} instead of expected type {expectedType}",
      ignoredTargets: "This action cannot target {targets} due to its damage type",
      noTargetSelected: "No valid target is selected, consider selecting a valid target before using this {weapon}",
      maxTargetsExceedeed: "{weapon} has a {area} area of effect, with a maximum of {max} targets. You are currently targetting {count} targets",
      noDefenseOnWeapon: "No defense configured for {actor} weapon: {weapon}.<br>Configure defense against this weapon to be able to attack with it.",
      weaponNotFound: "No weapon could be found for this roll.",
      noTokenActor: "Token is not attached to an Actor!",
      cannotUseEdgeAnymore: "Too late to use edge. A defender already rolled his defense!",
      actorCannotApplyDamage: "Actor {actor} cannot apply {damageType} : maybe check the decker has connected his cyberdeck",
      actorCannotReceiveDamage: "Cannot apply {damageType} to actor {actor} : it does not have a condition monitor for this type of damage",
      actorDoesNotHaveDefense: "Actor {actor} does not have a {defense}, the attack should not to target {actorType}.<br>Or it may be a bug, please report and manage manually"
    },
    sourceReference: "Source reference",
    sourceReferenceHelp: "rulebook, page, ...",
    description: "Description"
  },
  actor: {
    characterSheet: "Character sheet",
    characterTabbedSheet: "Character sheet (tabs)",
    characterEnhancedSheet: "Character enhanced sheet (tabs)",
    vehicleSheet: "Vehicle sheet",
    battlemechSheet: "Battlemech sheet",
    characterNPCSheet: "NPC sheet",
    actorName: "Name",
    celebrity: "Legend",
    famous: "Fame",
    edgePools: {
      title: "Edge Pools",
      physical: "Physical",
      mental: "Mental",
      social: "Social",
      grit: "Grit",
      insight: "Insight",
      rumor: "Rumor",
      legend: "Legend",
      credibility: "Credibility",
      chaos: "Chaos",
      rating: "Rating",
      current: "Current"
    },
    tabs: {
      main: "Character",
      equipment: "Equipment",
      biography: "Biography"
    },
    words: {
      keywords: "Keywords",
      cues: "Cues",
      dispositions: "Dispositions"
    },
    counters: {
      xp: "XP",
      xpUnused: "Unspent XP",
      xpTotal: "Lifetime XP",
      current: "Current",
      lifetime: "Lifetime",
      edge: "Edge",
      edgePools: {
        physical: "Physical",
        mental: "Mental",
        social: "Social",
        grit: "Grit",
        insight: "Insight",
        rumor: "Rumor",
        legend: "Legend",
        credibility: "Credibility",
        chaos: "Chaos",
        rating: "Rating",
        current: "Current"
      },
      mental: {
        insight: "Insight",
        rumor: "Rumor"
      },
      social: {
        legend: "Legend",
        credibility: "Credibility"
      }
    },
    monitors: {
      conditionMonitors: "Condition monitors",
      overflow: "{actor}: Overflow of {monitor} condition monitor, transfering {overflow} to {overflowMonitor} condition monitor",
      physical: "Physical",
      fatigue: "Fatigue",
      armor: "Armor",
      structure: "Structure",
      heat: "Heat",
      effect: "Effect",
      grit: "Grit",
      insight: "Insight",
      rumor: "Rumor",
      legend: "Legend",
      credibility: "Credibility",
      chaos: "Chaos",
      resistance: "Resistance",
      resistanceBase: "Base resistance",
      resistanceByType: "By damage type",
      resistanceByTypeButton: "Resist by type",
      resistanceByTypeTitle: "Type-specific resistance",
      resistanceBonusLabel: "Bonuses",
      resistanceTotal: "Total",
      resistanceFallback: "Falls back to base",
      damageType: "Damage type",
      resistancePresets: {
        label: "Presets",
        biological: "Biological",
        armoredVehicle: "Armored vehicle",
        energyShielded: "Energy shielded"
      },
      resistanceApplied: "{actor} resisted {value} vs {damageType} on {monitor} ({source})",
      resistanceSources: {
        default: "base",
        type: "type-specific"
      }
    },
    vehicle: {
      moves: "Moves",
      attacks: "Attacks",
      stealth: "Stealth",
      category: "Category",
      skill: "Skill",
      weapons: "Vehicle Weapons",
      heatDissipation: "Heat dissipation",
      criticalTrack: "Criticals",
      locationFront: "Front effects",
      locationSide: "Side effects",
      locationRear: "Rear effects",
      locationCore: "Core effects",
      crew: {
        label: "Crew",
        placeholder: "Crew names or notes"
      },
      quickActions: {
        title: "Quick Actions",
        rangedAttack: "Ranged Attack",
        meleeAttack: "Melee Attack",
        pilotingCheck: "Piloting Check",
        sensorSweep: "Electronic Warfare",
        emergencyRepair: "Critical Repair",
        allWeapons: "All Weapons",
        unarmed: "Unarmed (Punch/Kick)",
        unarmedNotes: "Basic unarmed strike.",
        selectWeaponGroup: "Select Weapon Group",
        selectMeleeProfile: "Select Melee Profile",
        selectSensorSkill: "Select Sensor Sweep Skill",
        weaponGroup: "Weapon Group",
        weaponsUsed: "Weapons",
        meleeProfile: "Melee Profile",
        meleeDamage: "Damage",
        skillUsed: "Skill",
        tooltips: {
          ranged: "Roll an attack using a Weapon Group",
          melee: "Roll a melee attack using fists, kicks, or installed melee weapons",
          piloting: "Piloting roll for movement, jumping, stability, or hazard checks",
          sensorSweep: "Acquire contact or generate targeting data using the machine sensor suite",
          emergencyRepair: "Choose a critical or repairable status and attempt the matching remedy"
        },
        errors: {
          noRanged: "No weapon groups available for ranged attack.",
          noMelee: "No melee attacks available.",
          noSensorSweep: "EW actions require an eligible targeted token and an available sensor action."
        }
      }
    },
    battlemech: {
      chassis: "Chassis",
      tonnage: "Tonnage",
      heat: {
        thresholds: "Heat thresholds",
        runningHot: "Hot",
        overheated: "Overheat",
        shutdown: "Danger",
        statusLabel: "Current heat state",
        status: {
          safe: "Safe",
          hot: "Hot",
          overheat: "Overheat",
          danger: "Danger",
          runningHot: "Hot",
          overheated: "Overheat",
          shutdown: "Danger"
        }
      },
      hardpoints: {
        title: "Hardpoint summary",
        type: "Type",
        size: "Size",
        location: "Location",
        assigned: "Assigned to",
        none: "No hardpoints configured."
      },
      weaponGroups: {
        title: "Weapon groups",
        mountPoints: "Mount points used: {used} / {total}",
        group: "Group",
        weapons: "Weapons",
        empty: "No weapons assigned to this group.",
        none: "No weapon groups configured.",
        missingWeapon: "Weapon {missingId} is missing from the actor."
      },
      weapons: {
        title: "Battlemech weapons",
        weapon: "Weapon",
        category: "Category",
        mount: "Mount",
        hardpoint: "Hardpoint",
        heat: "Heat",
        none: "No mech weapons equipped.",
        mountUnknown: "Unspecified"
      }
    },
    ownership: {
      owner: "Owner",
      unknown: "Unknown",
      owned: "Owned"
    }
  },
  actorType: {
    character: "Character",
    npc: "NPC",
    vehicle: "Vehicle",
    battlemech: "Battlemech"
  },
  item: {
    sheet: "Sheet for ",
    tabs: {
      main: "Details",
      modifiers: "Modifiers"
    },
    skill: {
      code: "Internal code",
      copyDefault: "Configure skill",
      isKnowledge: "Knowledge",
      attribute: "Attribute",
      value: "Level",
      specialization: "Specialisation",
      specializationHelp: "Type the name to choose a specialization",
      isSocial: "Social skill",
      hasDrain: "Drain",
      hasConvergence: "Convergence"
    },
    quality: {
      positive: "Legacy positive flag",
      category: "Category",
      tier: "Tier",
      activation: "Activation",
      tags: "Tags",
      effects: "Effects",
      prerequisites: "Prerequisites",
      limits: "Limits",
      categoryOptions: {
        positive: "Positive",
        negative: "Negative",
        narrative: "Narrative"
      },
      tierOptions: {
        minor: "Minor",
        major: "Major"
      },
      activationOptions: {
        passive: "Passive",
        triggered: "Triggered"
      }
    },
    assetModule: {
      category: "Category",
      level: "Level",
      levelShort: "Lvl",
      jumping: {
        enabled: "Grants Jumping",
        movement: "Jump Move",
        heat: "Jump Heat",
        attackRatingBonus: "Jump AR Bonus",
        defenseRatingBonus: "Jump DR Bonus",
        dfaEnabled: "Enable Death From Above"
      }
    },
    gear: {
      quantity: "Quantity",
      quantityShort: "Qty",
      rating: "Rating",
      category: "Category",
      relatedSkill: "Related Skill",
      availability: "Availability",
      rulesHook: "Rules Hook / Notes",
      tags: "Tags",
      categoryLabels: {
        audiovisual: "Audiovisual Gear",
        communication: "Communication Gear",
        computing: "Computing Gear",
        espionage: "Espionage Gear",
        hostileEnvironment: "Hostile Environment Gear",
        medical: "Medical Gear",
        optical: "Optical Gear",
        power: "Power Gear",
        repairSalvage: "Repair/Salvage Gear",
        survival: "Survival Gear",
        surveillance: "Surveillance Gear"
      }
    },
    consumable: {
      quantity: "Quantity",
      quantityShort: "Qty",
      rating: "Potency",
      category: "Consumable Type",
      relatedSkill: "Related Skill",
      availability: "Availability",
      rulesHook: "Rules Hook / Notes",
      tags: "Tags",
      categoryLabels: {
        ammo: "Ammunition",
        explosive: "Explosive",
        medical: "Medical",
        repair: "Repair",
        fuel: "Fuel / Power Cell",
        utility: "Utility"
      }
    },
    lifeModule: {
      moduleType: "Module Type",
      type: {
        faction: "Faction",
        childhood: "Childhood",
        higherEducation: "Higher Education",
        realLife: "Real Life"
      }
    },
    mechWeapon: {
      category: "Weapon Category",
      size: "Size",
      hardpoint: "Hardpoint",
      damage: "Damage Value",
      damageType: "Damage Type",
      heat: "Heat",
      area: "Area of effect",
      range: {
        max: "Maximum range"
      }
    },
    personalWeapon: {
      skill: "Skill",
      category: "Weapon Category",
      damage: "Damage Value",
      ap: "Armor Piercing",
      damageType: "Damage Type",
      damageShort: "DV",
      apShort: "AP",
      weaponWithoutActor: "No Actor",
      equipped: "Equipped",
      primary: "Primary",
      attack: "Attack",
      traits: "Traits",
      notes: "Notes",
      attackRatingBand: {
        label: "Attack Rating Modifiers",
        close: "Close",
        near: "Near",
        far: "Far",
        extreme: "Extreme"
      },
      range: {
        max: "Maximum range"
      },
      withArmor: "Armor protects"
    },
    armor: {
      equipped: "Equipped",
      primary: "Primary",
      rating: "Armor Rating",
      defenseBonus: "Defense Bonus",
      mitigation: "Type Modifiers",
      durability: "Durability",
      tags: "Armor Tags",
      availability: "Availability",
      traits: "Traits",
      notes: "Notes"
    }
  },
  itemType: {
    singular: {
      skill: "Skill",
      quality: "Trait",
      assetModule: "Asset Module",
      gear: "Gear",
      consumable: "Consumable",
      contact: "Contact",
      lifeModule: "Life Module",
      mechWeapon: "Mech-Scale Weapon",
      personalWeapon: "Personal Weapon",
      weaponPayload: "Weapon Payload",
      armor: "Armor"
    },
    plural: {
      skill: "Skills",
      quality: "Qualities",
      assetModule: "Asset Modules",
      gear: "Gears",
      consumable: "Consumables",
      contact: "Contacts",
      lifeModule: "Life Modules",
      action: "Actions",
      monitor: "Monitors",
      mechWeapon: "Mech-Scale Weapons",
      personalWeapon: "Personal Weapons",
      weaponPayload: "Weapon Payloads",
      armor: "Armor"
    }
  },
  monitor: {
    physical: "Physical",
    fatigue: "Fatigue"
  },
  monitorLetter: {
    physical: "P",
    fatigue: "F"
  },
  assetModuleCategory: {
    faction: "Faction",
    logistics: "Logistics",
    mobility: "Mobility",
    training: "Training",
    influence: "Influence",
    personal: "Personal",
    operations: "Operations",
    special: "Special"
  },
  attributes: {
    strength: "Strength",
    reflexes: "Reflexes",
    guts: "Guts",
    intelligence: "Intelligence",
    charisma: "Charisma",
    edge: "Edge",
    // Legacy synonyms retained for migration safety:
    willpower: "Guts",
    agility: "Reflexes",
    logic: "Intelligence",
    knowledge: "Knowledge",
    noAttribute: "No attribute chosen",
    // Legacy vehicle attributes retained (if still referenced somewhere):
    autopilot: "Autopilot",
    handling: "Handling",
    firewall: "Firewall",
    system: "System",
    // MWD vehicle attributes:
    chassis: "Chassis",
    reliability: "Reliability",
    condition: "Condition"
  },
  attributeAction: {
    defense: "Defense",
    judgeIntentions: "Judge intentions",
    perception: "Perception / Mental resistance",
    resistTorture: "Resist torture / Physical resistance",
    composure: "Composure / Social resistance",
    memory: "Memory",
    catch: "Catch object",
    lift: "Lift/carry"
  },
  defense: {
    physicalDefense: "Physical defense",
    physicalResistance: "Physical resistance",
    socialDefense: "Social defense",
    mentalResistance: "Mental resistance"
  },
  skill: {
    athletics: "Athletics",
    heavyWeapons: "Heavy Weapons",
    escapeArtist: "Escape Artist",
    gunnery: "Gunnery",
    meleeCombat: "Melee Combat",
    piloting: "Piloting",
    projectileWeapons: "Projectile Weapons",
    firearms: "Firearms",
    stealth: "Stealth",
    zeroGOps: "Zero-G Operations",
    art: "Art",
    artillery: "Artillery",
    systemOps: "System Operations",
    computers: "Computers",
    demolitions: "Demolitions",
    knowledge: "Knowledge",
    medTech: "MedTech",
    science: "Science",
    perception: "Perception",
    tactics: "Tactics",
    technician: "Technician",
    tracking: "Tracking",
    navigation: "Navigation",
    animalHandling: "Animal Handling",
    survival: "Survival",
    acting: "Acting",
    deception: "Deception",
    leadership: "Leadership",
    negotiation: "Negotiation",
    etiquette: "Etiquette",
    streetwise: "Streetwise",
    intimidation: "Intimidation",
    Administration: "Administration"
  },
  area: {
    none: "None",
    shotgun: "Shotgun",
    circle: "Circle",
    cone: "Cone",
    rect: "Rectangle",
    ray: "Ray"
  },
  range: {
    contact: "Contact",
    short: "Short",
    medium: "Medium",
    far: "Far",
    extreme: "Extreme"
  },
  vehicleCategory: {
    drone: "Drone",
    personal: "Personal",
    combat: "Combat",
    aerospace: "Aerospace",
    mech: "Mech"
  },
  mwd: {
    armorMitigationType: {
      penetrating: "Penetrating",
      concussive: "Concussive",
      energy: "Energy",
      thermal: "Thermal",
      electrical: "Electrical"
    },
    weightClass: {
      label: "Weight class",
      light: "Light",
      medium: "Medium",
      heavy: "Heavy",
      assault: "Assault"
    },
    hardpoint: {
      type: {
        penetrating: "Penetrating",
        concussive: "Concussive",
        energy: "Energy",
        support: "Support",
        omni: "Omni"
      },
      size: {
        small: "Small",
        medium: "Medium",
        large: "Large"
      },
      location: {
        head: "Head",
        torso: "Torso",
        arms: "Arms",
        front: "Front",
        side: "Side",
        rear: "Rear",
        turret: "Turret"
      }
    },
    hardpointType: {
      penetrating: "Penetrating",
      concussive: "Concussive",
      energy: "Energy",
      support: "Support",
      omni: "Omni"
    },
    hardpointSize: {
      small: "Small",
      medium: "Medium",
      large: "Large"
    },
    hardpointLocation: {
      head: "Head",
      torso: "Torso",
      arms: "Arms",
      front: "Front",
      side: "Side",
      rear: "Rear",
      turret: "Turret"
    },
    weaponCategory: {
      ranged: "Ranged",
      melee: "Melee"
    },
    melee: {
      title: "Melee options",
      baseProfile: "Unarmed",
      baseProfileLabel: "Base melee profile",
      damagePlaceholder: "Damage code",
      notesPlaceholder: "Notes",
      maxWeapons: "Maximum equipped melee weapons",
      allowedLocations: "Allowed locations",
      availableProfiles: "Available melee profiles",
      location: {
        head: "Head",
        torso: "Torso",
        arm: "Arm",
        leg: "Leg"
      },
      locationAny: "Any location"
    },
    loadout: {
      title: "Weapon loadout",
      mountPoints: "Mount points used",
      hardpoints: "Hardpoints",
      weaponGroups: "Weapon groups",
      occupied: "{{weaponGroup}} assigned",
      emptyHardpoint: "Empty",
      errors: {
        label: "Errors",
        mountPointsExceeded: "Loadout uses {used} mount points but only {total} are available.",
        hardpointUnavailable: "No matching hardpoint for {weapon} ({type}, {size}).",
        weaponAlreadyGrouped: "{weapon} is already assigned to another group.",
        meleeLimitExceeded: "Equipped melee weapons {equipped} exceed limit {limit}.",
        meleeLocationRestricted: "{weapon} cannot be mounted at that location."
      },
      warnings: {
        label: "Warnings",
        weaponMissing: "Weapon with id {weapon} is missing."
      },
      newGroup: "New weapon group"
    },
    weapon: {
      damageType: {
        energy: "Energy",
        kinetic: "Kinetic",
        ballistic: "Ballistic",
        explosive: "Explosive",
        plasma: "Plasma",
        electrical: "Electrical",
        melee: "Melee",
        none: "None"
      }
    },
    personalWeapon: {
      damageType: {
        penetrating: "Penetrating",
        concussive: "Concussive",
        energy: "Energy",
        thermal: "Thermal",
        electrical: "Electrical"
      }
    }
  },
  modifier: {
    column: {
      group: "Group",
      effect: "Effect",
      value: "Value",
      category: "",
      subCategory: "",
      condition: "When"
    },
    group: {
      roll: "Roll",
      attribute: "Attribute",
      monitor: "Monitor",
      other: "Other"
    },
    roll: {
      effect: {
        pool: "Pool bonus",
        reroll: "Rerolls",
        rerollMax: "Reroll allowance cap",
        glitch: "Glitch dice",
        successReroll: "Reroll own successes",
        opponentPool: "Opponent pool malus",
        opponentReroll: "Opponent rerolls"
      },
      category: {
        attribute: "Attribute",
        skill: "Skill",
        attributeAction: "Attribute action"
      }
    },
    monitor: {
      effect: {
        armor: "Armor",
        structure: "Structure",
        fatigue: "Fatigue",
        physical: "Physical"
      },
      category: {
        max: "Increased max",
        resistance: "Resistance",
        resistanceByType: "Damage-type resistance"
      }
    },
    other: {
      effect: {
        ignoreWounds: "Ignore wounds",
        damageArmor: "Damage to armor",
        initiative: "Initiative bonus",
        celebrity: "Adjust legend"
      },
      category: {}
    },
    condition: {
      always: "Always"
    }
  }
}, ne = z, I = "mwd", dI = "MechWarrior: Destiny", Go = `system.${I}`, mI = I, Dl = `systems/${I}`, PA = `${Dl}/style`, To = `${Dl}/third-party/style`, ht = `systems/${I}/templates`, RA = `${Dl}/img/icons`, Ke = `${RA}/skills`, tt = "MWD | ", DA = "enableBattlemechTokenHeatFx", fI = 2, gI = 5, pI = 4, Ym = 8, Ra = {
  reflexes: "reflexes",
  strength: "strength",
  guts: "guts",
  intelligence: "intelligence",
  charisma: "charisma",
  edge: "edge",
  handling: "handling",
  system: "system",
  chassis: "chassis",
  reliability: "reliability",
  condition: "condition"
}, Qm = {
  autopilot: "autopilot",
  firewall: "firewall",
  knowledge: "knowledge"
}, Kn = {
  grit: "grit",
  chaos: "chaos",
  insight: "insight",
  rumor: "rumor",
  legend: "legend",
  credibility: "credibility"
}, Hg = {
  physical: [Kn.grit, Kn.chaos],
  mental: [Kn.insight, Kn.rumor],
  social: [Kn.legend, Kn.credibility]
}, M = {
  actorTypes: {
    character: "character",
    npc: "npc",
    vehicle: "vehicle",
    battlemech: "battlemech"
  },
  itemType: {
    skill: "skill",
    quality: "quality",
    assetModule: "assetModule",
    mechWeapon: "mechWeapon",
    personalWeapon: "personalWeapon",
    weaponPayload: "weaponPayload",
    armor: "armor",
    gear: "gear",
    consumable: "consumable",
    contact: "contact",
    lifeModule: "lifeModule",
    mechEquipment: "mechEquipment",
    vehicleUpgrade: "vehicleUpgrade"
  },
  actorAttributes: Ra,
  itemAttributes: Qm,
  attributes: { ...Ra, ...Qm },
  monitors: {
    fatigue: "fatigue",
    armor: "armor",
    physical: "physical",
    structure: "structure",
    heat: "heat"
  },
  counters: {
    xp: "xp",
    xpTotal: "xpTotal",
    xpUnused: "xpUnused",
    edge: "edge",
    edgePools: Kn,
    edgePoolGroups: Hg,
    physical: {
      grit: Kn.grit,
      chaos: Kn.chaos
    },
    mental: {
      insight: Kn.insight,
      rumor: Kn.rumor
    },
    social: {
      legend: Kn.legend,
      credibility: Kn.credibility
    },
    chaos: Kn.chaos
  },
  area: {
    none: "none",
    shotgun: "shotgun",
    circle: "circle",
    cone: "cone",
    rect: "rect",
    ray: "ray"
  }
}, hI = Object.freeze({
  physical: "physical",
  mental: "mental",
  social: "social"
});
Object.freeze(Object.values(hI));
function ce(t = "") {
  return String(t ?? "").replace(/([a-z0-9])([A-Z])/g, "$1 $2").replace(/[-_]+/g, " ").replace(/\s+/g, " ").trim().replace(/\b\w/g, (e) => e.toUpperCase());
}
const bI = {
  [M.actorTypes.character]: [
    M.actorAttributes.strength,
    M.actorAttributes.reflexes,
    M.actorAttributes.guts,
    M.actorAttributes.intelligence,
    M.actorAttributes.charisma,
    M.actorAttributes.edge
  ],
  [M.actorTypes.npc]: [
    M.actorAttributes.strength,
    M.actorAttributes.reflexes,
    M.actorAttributes.guts,
    M.actorAttributes.intelligence,
    M.actorAttributes.charisma,
    M.actorAttributes.edge
  ],
  [M.actorTypes.vehicle]: [
    M.actorAttributes.handling,
    M.actorAttributes.system,
    M.actorAttributes.chassis,
    M.actorAttributes.reliability
  ],
  [M.actorTypes.battlemech]: [
    M.actorAttributes.handling,
    M.actorAttributes.system,
    M.actorAttributes.chassis,
    M.actorAttributes.reliability
  ]
}, Ud = {
  character: {
    physical: {
      status: { label: "Penalty", path: "derived.penalty" },
      derived: {
        penalty: { fn: "penaltyPer3Damage", source: "value" }
      }
    },
    fatigue: {
      status: { label: "Penalty", path: "derived.penalty" },
      derived: {
        penalty: { fn: "penaltyPer3Damage", source: "value" }
      }
    },
    armor: {
      status: { label: "Resist", path: "derived.resistance" },
      derived: {
        resistance: { fn: "resistancePerQuarter", source: "armorPersonalBase" }
      }
    }
  },
  battlemech: {
    armor: {
      status: { label: "Resist", path: "derived.resistance" },
      derived: {
        resistance: { fn: "resistancePerQuarter", source: "mechArmorBase" }
      }
    },
    structure: {
      status: null
    },
    heat: {
      status: { label: "Penalty", path: "derived.penalty" },
      derived: {
        penalty: { fn: "penaltyPer3Damage", source: "value" }
      }
    }
  },
  vehicle: {
    armor: {
      status: { label: "Resist", path: "derived.resistance" },
      derived: {
        resistance: { fn: "resistancePerQuarter", source: "vehicleArmorBase" }
      }
    },
    durability: {
      status: { label: "Penalty", path: "derived.penalty" },
      derived: {
        penalty: { fn: "penaltyPer3Damage", source: "value" }
      }
    }
  }
}, jg = {
  title: "title",
  pool: "pool",
  reroll: "reroll",
  rerollMax: "rerollMax",
  rerollForced: "rerollForced",
  successReroll: "successReroll",
  glitch: "glitch",
  edge: "edge",
  risk: "risk",
  opponentPool: "opponentPool",
  opponentReroll: "opponentReroll"
}, zu = {
  rollType: {
    attributeAction: "attributeAction",
    defense: "defense",
    attribute: "attribute",
    skill: "skill",
    weapon: "weapon"
  },
  actions: {
    defense: "defense",
    resistTorture: "resistTorture",
    judgeIntentions: "judgeIntentions",
    perception: "perception",
    composure: "composure",
    memory: "memory",
    catch: "catch",
    lift: "lift"
  },
  defenses: {
    physicalDefense: "physicalDefense",
    physicalResistance: "physicalResistance",
    socialDefense: "socialDefense",
    mentalResistance: "mentalResistance"
  },
  fixedDefenseCode: {
    // fix for old incorrect defense codes
    mentalDefense: "physicalResistance"
  }
};
globalThis.ANARCHY_CONSTANTS = {
  SYSTEM_NAME: I,
  SYSTEM_DESCRIPTION: dI,
  SYSTEM_SOCKET: Go,
  SYSTEM_SCOPE: mI,
  SYSTEM_PATH: Dl,
  STYLE_PATH: PA,
  THIRD_PARTY_STYLE_PATH: To,
  TEMPLATES_PATH: ht,
  ICONS_PATH: RA,
  ICONS_SKILLS_PATH: Ke,
  LOG_HEAD: tt,
  SPECIALIZATION_BONUS: fI,
  TARGET_SUCCESS: gI,
  TARGET_SUCCESS_EDGE: pI,
  BASE_MONITOR: Ym,
  ACTOR_ATTRIBUTES: Ra,
  ITEM_ATTRIBUTES: Qm,
  EDGE_POOL_GROUPS: Hg,
  TEMPLATE: M,
  ANARCHY_SYSTEM: zu,
  ROLL_PARAMETER_CATEGORY: jg
};
const Oi = class Oi {
  static ascending(e = (n) => n) {
    return (n, i) => Oi.sortingBy(e(n), e(i));
  }
  static descending(e = (n) => n) {
    return (n, i) => Oi.sortingBy(e(i), e(n));
  }
  static sortingBy(e, n) {
    return e > n ? 1 : e < n ? -1 : 0;
  }
  static bySortedArray(e) {
    return (n) => e.indexOf(n);
  }
  static ascendingBySortedArray(e) {
    return Oi.ascending(Oi.bySortedArray(e));
  }
  static sortedMap(e, n = (i, a) => 0) {
    return Object.keys(e).sort(n).reduce(
      (i, a) => (i[a] = e[a], i),
      {}
    );
  }
  static reindexIds(e) {
    let n = 1;
    return e.forEach((i) => i.id = n++), e;
  }
  static distinct(e) {
    return [...new Set(e)];
  }
  static sum() {
    return (e, n) => e + n;
  }
  static sumValues(e, n = (i) => i) {
    return e.map(n).filter((i) => i != null).reduce(Oi.sum(), 0);
  }
  static divint(e, n) {
    return Math.floor(e / n);
  }
  static divup(e, n) {
    return Math.ceil(e / n);
  }
  static join(e, n = "") {
    return e.reduce(Oi.joiner(n));
  }
  static joiner(e = "") {
    return (n, i) => n + e + i;
  }
  static classify(e, n = (i) => i.type) {
    let i = {};
    return Oi.classifyInto(i, e, n), i;
  }
  static classifyFirst(e, n) {
    let i = {};
    for (const a of e) {
      const r = n(a);
      i[r] || (i[r] = a);
    }
    return i;
  }
  static classifyInto(e, n, i = (a) => a.type) {
    for (const a of n) {
      const r = i(a);
      let s = e[r];
      s || (s = [], e[r] = s), s.push(a);
    }
  }
  static showControlWhen(e, n) {
    n ? e.show() : e.hide();
  }
  static minmax(e, n, i) {
    return Math.max(n, Math.min(e, i));
  }
};
F(Oi, "isString", (e) => typeof e == "string" || e instanceof String);
let ot = Oi;
const yI = {
  keyword: "keywords",
  disposition: "dispositions",
  cue: "cues"
}, j = class j {
  /**
   * Initialize enum caches.
   * Call once during system init/setup after config/constants are available.
   */
  static init() {
    var n, i, a, r, s, o, l, c, u, d, f;
    j.hbsAttributes = j.mapObjectToKeyValue(ne.attributes).filter((m) => m.value !== "knowledge" && m.value !== "noAttribute"), j.hbsItemTypes = j.mapObjectToKeyValue(ne.itemType), j.hbsMonitors = j.mapObjectToKeyValue(ne.monitor), j.hbsMonitorLetters = j.mapObjectToKeyValue(ne.monitorLetter), j.hbsAssetModuleCategories = j.mapObjectToKeyValue(ne.assetModuleCategory), (i = (n = ne.item) == null ? void 0 : n.lifeModule) != null && i.type ? j.hbsLifeModuleTypes = j.mapObjectToKeyValue(ne.item.lifeModule.type) : (console.warn("MWD | MWD.item.lifeModule.type is missing; life module enums disabled."), j.hbsLifeModuleTypes = []), j.hbsAreas = j.mapObjectToKeyValue(ne.area), j.hbsRanges = j.mapObjectToKeyValue(ne.range), j.hbsVehicleCategories = j.mapObjectToKeyValue(ne.vehicleCategory), j.hbsMwdWeightClasses = j.mapObjectToKeyValue((a = ne.mwd) == null ? void 0 : a.weightClass), j.hbsMwdHardpointTypes = j.mapObjectToKeyValue((r = ne.mwd) == null ? void 0 : r.hardpointType), j.hbsMwdHardpointSizes = j.mapObjectToKeyValue((s = ne.mwd) == null ? void 0 : s.hardpointSize), j.hbsMwdHardpointLocations = j.mapObjectToKeyValue((o = ne.mwd) == null ? void 0 : o.hardpointLocation), j.hbsMwdWeaponCategories = j.mapObjectToKeyValue((l = ne.mwd) == null ? void 0 : l.weaponCategory), j.hbsMwdWeaponDamageTypes = j.mapObjectToKeyValue((c = ne.mwd) == null ? void 0 : c.weaponDamageType), j.hbsPersonalWeaponDamageTypes = j.mapObjectToKeyValue((u = ne.mwd) == null ? void 0 : u.personalDamageType), j.hbsPersonalWeaponDamageCategories = j.mapObjectToKeyValue((d = ne.mwd) == null ? void 0 : d.personalDamageCategory), j.hbsMwdMeleeLocations = j.mapObjectToKeyValue((f = ne.mwd) == null ? void 0 : f.meleeLocation), j.hbsDamageTypes = ot.distinct(
      (j.hbsMwdWeaponDamageTypes ?? []).concat(j.hbsPersonalWeaponDamageTypes ?? []),
      (m) => m.value
    );
    const e = Object.values(bI).flat();
    j.sortedAttributeKeys = ot.distinct(
      e.concat(Object.keys(ne.attributes ?? {}))
    ), j.registerHandleBarHelpers(), j.ENUMS = j.getEnums();
  }
  static registerHandleBarHelpers() {
    typeof Handlebars > "u" || Handlebars.registerHelper("sortedAttributes", (e) => {
      if (!e || typeof e != "object") return [];
      const n = Object.keys(e), i = j.sortedAttributeKeys ?? [], a = new Map(i.map((r, s) => [r, s]));
      return n.sort((r, s) => {
        const o = a.has(r) ? a.get(r) : 9999, l = a.has(s) ? a.get(s) : 9999;
        return o !== l ? o - l : String(r).localeCompare(String(s));
      }), n.map((r) => {
        const s = e[r];
        return s && typeof s == "object" ? { key: r, ...s } : { key: r, value: s };
      });
    });
  }
  static getDamageTypes() {
    return j.hbsDamageTypes ?? [];
  }
  static getAttributes(e = () => !0) {
    return (j.hbsAttributes ?? []).filter((n) => e(n.value));
  }
  static getActorWordTypes() {
    return yI;
  }
  static getMonitors() {
    return j.hbsMonitors ?? [];
  }
  /**
   * Returns a big blob of enums suitable for sheet contexts.
   * `withKnowledge` is passed through to the skills service.
   */
  static getEnums(e = () => !0, n = !1) {
    return {
      attributes: j.getAttributes(e),
      itemTypes: j.hbsItemTypes ?? [],
      monitors: j.hbsMonitors ?? [],
      monitorLetters: j.hbsMonitorLetters ?? [],
      assetModuleCategories: j.hbsAssetModuleCategories ?? [],
      lifeModuleTypes: j.hbsLifeModuleTypes ?? [],
      areas: j.hbsAreas ?? [],
      ranges: j.hbsRanges ?? [],
      vehicleCategories: j.hbsVehicleCategories ?? [],
      // Skills: now sourced from MWD namespace (with safe fallback)
      skills: j.getSkillsEnum({ withKnowledge: n }),
      // MWD enums
      mwdWeightClasses: j.hbsMwdWeightClasses ?? [],
      mwdHardpointTypes: j.hbsMwdHardpointTypes ?? [],
      mwdHardpointSizes: j.hbsMwdHardpointSizes ?? [],
      mwdHardpointLocations: j.hbsMwdHardpointLocations ?? [],
      mwdWeaponCategories: j.hbsMwdWeaponCategories ?? [],
      mwdWeaponDamageTypes: j.hbsMwdWeaponDamageTypes ?? [],
      personalWeaponDamageTypes: j.hbsPersonalWeaponDamageTypes ?? [],
      personalWeaponDamageCategories: j.hbsPersonalWeaponDamageCategories ?? [],
      damageTypes: j.hbsDamageTypes ?? [],
      mwdMeleeLocations: j.hbsMwdMeleeLocations ?? []
    };
  }
  static getSkillsEnum({ withKnowledge: e = !1 } = {}) {
    var a, r, s, o, l;
    const n = ((r = (a = game == null ? void 0 : game.system) == null ? void 0 : a.mwd) == null ? void 0 : r.skills) ?? ((o = (s = game == null ? void 0 : game.system) == null ? void 0 : s.anarchy) == null ? void 0 : o.skills);
    return (((l = n == null ? void 0 : n.getSkills) == null ? void 0 : l.call(n, { withKnowledge: e })) ?? []).map((c) => ({
      value: c.code,
      label: c.label ?? c.code
    }));
  }
  /**
   * Convert an object map into an array like [{ value, label }, ...]
   * Accepts:
   * - { key: "Label" }
   * - { key: { label: "Label" } }
   */
  static mapObjectToKeyValue(e, n = "value", i = "label") {
    return !e || typeof e != "object" ? [] : Object.keys(e).map((a) => {
      const r = e[a];
      let s;
      return r && typeof r == "object" ? s = r.label ?? r.name ?? r.value ?? String(a) : r != null ? s = String(r) : s = String(a), {
        [n]: a,
        [i]: s
      };
    });
  }
  // Backwards-compat alias (the legacy file misspelled this)
  static mapObjetToKeyValue(e, n = "value", i = "label") {
    return j.mapObjectToKeyValue(e, n, i);
  }
};
F(j, "ENUMS"), // HBS-friendly arrays of { value, label } (or key/value depending on caller)
F(j, "hbsAttributes"), F(j, "hbsItemTypes"), F(j, "hbsMonitors"), F(j, "hbsMonitorLetters"), F(j, "hbsAssetModuleCategories"), F(j, "hbsLifeModuleTypes"), F(j, "hbsAreas"), F(j, "hbsRanges"), F(j, "hbsVehicleCategories"), // MWD-specific enum groups
F(j, "hbsMwdWeightClasses"), F(j, "hbsMwdHardpointTypes"), F(j, "hbsMwdHardpointSizes"), F(j, "hbsMwdHardpointLocations"), F(j, "hbsMwdWeaponCategories"), F(j, "hbsMwdWeaponDamageTypes"), F(j, "hbsPersonalWeaponDamageTypes"), F(j, "hbsPersonalWeaponDamageCategories"), F(j, "hbsDamageTypes"), F(j, "hbsMwdMeleeLocations"), F(j, "sortedAttributeKeys");
let pt = j;
class SI {
  static monitor(e) {
    return pt.getFromList(pt.getMonitors(), e) ?? "";
  }
  static letter(e) {
    return pt.getFromList(pt.getMonitorLetters(), e) ?? "";
  }
}
class AI {
  static toLowerCaseNoAccent(e) {
    return e == null ? void 0 : e.normalize("NFD").toLowerCase().replace(/[\u0300-\u036f]/g, "");
  }
  static toUpperCaseNoAccent(e) {
    return e == null ? void 0 : e.normalize("NFD").toUpperCase().replace(/[\u0300-\u036f]/g, "");
  }
}
const kI = [
  "fas fa-dice",
  "fas fa-dice-one",
  "fas fa-dice-two",
  "fas fa-dice-three",
  "fas fa-dice-four",
  "fas fa-dice-five",
  "fas fa-dice-six"
];
class he {
  static fontAwesome(e) {
    return `<i class="${e}"></i>`;
  }
  static iconSystemPath(e, n) {
    return he.iconPath(`${PA}/${e}`, n);
  }
  static iconPath(e, n) {
    return `<img class="${n}" src="${e}" />`;
  }
  static iconD6(e) {
    if (e < 0 || e > 6)
      throw `Dice ${e} is out of dice range [1..6] or 0 for multidice`;
    return he.fontAwesome(kI[e]);
  }
}
globalThis.ANARCHY_ICONS = he;
const At = (t, e = {}) => t.replace(/\{(.*?)\}/g, (n, i) => e[i] ?? "");
function Gg(t) {
  return t && typeof t == "object" && !Array.isArray(t) ? Object.values(t).flatMap((e) => Gg(e)) : Array.isArray(t) ? t.map((e) => String(e ?? "").trim()).filter(Boolean) : String(t ?? "").split(",").map((e) => e.trim()).filter(Boolean);
}
function qo(t = []) {
  return Array.from(new Set(t.map((e) => String(e ?? "").trim()).filter(Boolean)));
}
function Tc(t, e = "standard") {
  return String(t ?? "").trim() || e;
}
function TI(t, e = {}) {
  t && (t.movedToKeywords ?? (t.movedToKeywords = []), t.movedToKeywords.push(e));
}
function wI(t, e, n = {}) {
  t && (t.errors ?? (t.errors = []), t.errors.push({ message: e, ...n }));
}
const qg = Object.freeze(["templated"]), MI = Object.freeze([
  "burstCapable",
  "fullAutoCapable",
  "salvoCapable"
]), vI = Object.freeze([
  "clustered",
  "mineLayer",
  "smoke",
  "incendiary",
  "emp",
  "armorBypass"
]), OA = Object.freeze([
  { value: "templated", label: "Templated" },
  { value: "clustered", label: "Clustered" },
  { value: "mineLayer", label: "Mine Layer" },
  { value: "smoke", label: "Smoke" },
  { value: "incendiary", label: "Incendiary" },
  { value: "emp", label: "EMP" },
  { value: "armorBypass", label: "Armor Bypass" }
]), Wg = Object.freeze([
  { value: "blast", label: "Blast" },
  { value: "cone", label: "Cone" },
  { value: "line", label: "Line" },
  { value: "cloud", label: "Cloud" },
  { value: "minefield", label: "Minefield" }
]), Vg = Object.freeze([
  { value: "targeted", label: "Targeted (start on target)" },
  { value: "origin", label: "Origin (start on attacker)" },
  { value: "placed", label: "Placed (start free)" }
]), CI = Object.freeze(["blast", "cone", "line"]);
new Set(qg);
const II = /* @__PURE__ */ new Set([
  ...qg,
  ...MI
]), EI = /* @__PURE__ */ new Set([
  ...qg,
  ...vI
]);
function Jg() {
  return {
    movedToKeywords: [],
    errors: []
  };
}
function eu(t) {
  return qo(Gg(t));
}
function LA({
  traits: t = [],
  keywords: e = [],
  recognized: n = /* @__PURE__ */ new Set(),
  report: i = null,
  owner: a = "weapon",
  path: r = ""
} = {}) {
  const s = Gg(t), o = eu(e), l = [], c = [...o];
  for (const u of s) {
    if (n.has(u)) {
      l.push(u);
      continue;
    }
    c.push(u), TI(i, {
      owner: a,
      from: r || "traits",
      to: r ? r.replace(/traits$/u, "keywords") : "keywords",
      value: u
    });
  }
  return {
    traits: qo(l),
    keywords: qo(c)
  };
}
function $A({
  traits: t = [],
  keywords: e = [],
  report: n = null,
  path: i = "system.traits"
} = {}) {
  return LA({
    traits: t,
    keywords: e,
    recognized: II,
    report: n,
    owner: "weapon",
    path: i
  });
}
function _A({
  traits: t = [],
  keywords: e = [],
  report: n = null,
  path: i = "system.payloads[].traits"
} = {}) {
  return LA({
    traits: t,
    keywords: e,
    recognized: EI,
    report: n,
    owner: "payload",
    path: i
  });
}
function BA(t = {}, e = "standard") {
  const n = t ?? {}, i = Tc(
    n.resolverKey ?? n.damageModel ?? n.resolver,
    e
  ), a = String(n.damageModel ?? "").trim(), r = n.onHitEffect;
  return {
    resolverKey: i,
    damageModel: a,
    onHitEffect: r === null ? null : String(r ?? "").trim() || null
  };
}
function Kd(t = {}) {
  const e = t ?? {}, n = !!e.enabled, i = e.shots, a = e.accuracyMod, r = e.addHeat, s = e.consumption;
  return {
    enabled: n,
    ...i !== void 0 ? { shots: Math.max(0, Number(i ?? 0) || 0) } : {},
    ...a !== void 0 ? { accuracyMod: Number(a ?? 0) || 0 } : {},
    ...r !== void 0 ? { addHeat: Number(r ?? 0) || 0 } : {},
    ...s !== void 0 ? { consumption: Math.max(0, Number(s ?? 0) || 0) } : {}
  };
}
function NI(t = {}) {
  const e = t ?? {};
  return {
    single: Kd(e.single),
    burst: Kd(e.burst),
    fullAuto: Kd(e.fullAuto)
  };
}
function PI(t) {
  const e = String(t ?? "").trim().toLowerCase();
  return e === "circle" ? "blast" : e === "ray" ? "line" : Wg.some((n) => n.value === e) ? e : "";
}
function RI(t) {
  const e = String(t ?? "").trim().toLowerCase();
  return ["target", "targeted"].includes(e) ? "targeted" : Vg.some((n) => n.value === e) ? e : "";
}
function DI(t = null) {
  const e = t ?? {}, n = PI(e.shape ?? e.t ?? e.type), i = e.size ?? e.distance ?? e.radius ?? e.length, a = RI(e.placement ?? e.origin ?? e.mode);
  return !n && i === void 0 && !a ? null : {
    shape: n,
    size: Math.max(0, Number(i ?? 0) || 0),
    placement: a || "targeted"
  };
}
function OI({
  weapon: t = {},
  payload: e = {},
  effectiveTraits: n = [],
  effectiveResolution: i = null,
  report: a = null
} = {}) {
  var h, b;
  const r = qo((t == null ? void 0 : t.traits) ?? []), s = qo((e == null ? void 0 : e.traits) ?? []), o = n.includes("templated"), l = r.includes("templated"), c = s.includes("templated"), u = (e == null ? void 0 : e.template) ?? null, d = (t == null ? void 0 : t.template) ?? null, f = Tc((h = e == null ? void 0 : e.resolution) == null ? void 0 : h.resolverKey, ""), m = Tc((b = t == null ? void 0 : t.resolution) == null ? void 0 : b.resolverKey, ""), g = Tc(i == null ? void 0 : i.resolverKey, "standard"), p = [];
  if (!o)
    return {
      errors: p,
      liveCapabilities: [],
      template: null,
      resolverKey: g,
      isTemplated: !1
    };
  l && p.push("Weapon-authored templated attacks are not supported in personal weapon capability v1."), l && c && p.push("Templated capability cannot be authored on both weapon and payload."), d && p.push("Template configuration must be authored on the payload for templated attacks."), c || p.push("Templated attacks require the active payload to author the templated capability."), (!(u != null && u.shape) || !(Number(u == null ? void 0 : u.size) > 0)) && p.push("Templated payloads require a valid template shape and size."), u != null && u.placement || p.push("Templated payloads require a template placement mode."), g !== "template" && p.push("Templated attacks require resolution.resolverKey to be template."), f && f !== "template" && p.push("Payload templated attacks must author resolution.resolverKey as template."), m === "template" && p.push("Weapon-level template resolver routing is not supported for personal weapon capability v1.");
  for (const y of p)
    wI(a, y, { capability: "templated" });
  return {
    errors: p,
    liveCapabilities: ["templated"],
    template: u,
    resolverKey: g,
    isTemplated: !0
  };
}
const Wo = Object.freeze(["none", "minor", "major", "full"]), LI = Object.freeze(["blast", "cone", "line", "rect"]), $I = Object.freeze({
  blast: "circle",
  cone: "cone",
  line: "ray",
  rect: "rect"
}), _I = Object.freeze({
  circle: "blast",
  cone: "cone",
  ray: "line",
  rect: "rect",
  rectangle: "rect"
}), Te = Object.freeze({
  none: "none",
  minor: "minor",
  major: "major",
  full: "full"
}), BI = Object.freeze({
  none: 0,
  minor: 0.25,
  major: 0.5,
  full: 1
}), kt = Object.freeze({
  none: "none",
  discrete: "discrete",
  persistent: "persistent"
});
function W(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function xI(t, e = !1) {
  if (typeof t == "boolean") return t;
  const n = String(t ?? "").trim().toLowerCase();
  return ["true", "1", "yes", "y", "on"].includes(n) ? !0 : ["false", "0", "no", "n", "off"].includes(n) ? !1 : e;
}
function xA(t) {
  return foundry.utils.deepClone(t);
}
function Mt(t, e = Te.none) {
  const n = String(t ?? "").trim().toLowerCase();
  return Wo.includes(n) ? n : e;
}
function Xm(t) {
  return Number(BI[Mt(t)] ?? 0) || 0;
}
function xa(t) {
  return Wo.indexOf(Mt(t));
}
function Zm(t, e = 1) {
  const n = Math.max(0, xa(t)), i = Math.max(0, n - Math.max(0, Math.trunc(W(e, 1))));
  return Wo[i] ?? Te.none;
}
function zI(t, e = 1) {
  const n = Math.max(0, xa(t)), i = Math.min(Wo.length - 1, n + Math.max(0, Math.trunc(W(e, 1))));
  return Wo[i] ?? Te.full;
}
function ci(t) {
  return Mt(t).toUpperCase();
}
function Yg(t = {}) {
  var a, r, s, o, l;
  const e = t ?? {}, n = Math.max(1, Math.trunc(W(
    ((a = e == null ? void 0 : e.escalation) == null ? void 0 : a.intervalTurns) ?? ((r = e == null ? void 0 : e.escalation) == null ? void 0 : r.interval) ?? 1,
    1
  ))), i = Math.max(0, Math.trunc(W(((s = e == null ? void 0 : e.escalation) == null ? void 0 : s.rate) ?? 1, 1)));
  return {
    startExposure: Mt(e.startExposure, Te.minor),
    escalation: {
      rate: i,
      intervalTurns: n,
      max: Mt((o = e == null ? void 0 : e.escalation) == null ? void 0 : o.max, Te.full)
    },
    onFull: {
      burnDelta: Math.max(0, Math.trunc(W(((l = e == null ? void 0 : e.onFull) == null ? void 0 : l.burnDelta) ?? 0, 0)))
    },
    clearOnExit: xI(e.clearOnExit, !0)
  };
}
function Gi(t = {}) {
  const e = t ?? {}, n = String(e.kind ?? kt.none).trim().toLowerCase(), i = n === kt.persistent ? kt.persistent : n === kt.discrete ? kt.discrete : kt.none;
  return {
    kind: i,
    hazard: i === kt.persistent ? Yg(e.hazard ?? e) : null
  };
}
function zA(t = {}) {
  return Gi(t).kind === kt.persistent;
}
function $r(t, e) {
  return Math.max(0, Math.ceil(W(t, 0) * Xm(e)));
}
function Fu(t, e = "") {
  const n = String(t ?? "").trim().toLowerCase();
  return LI.includes(n) ? n : e;
}
function FA(t, e = "circle") {
  return $I[Fu(t)] ?? e;
}
function FI(t, e = "") {
  const n = String(t ?? "").trim().toLowerCase();
  return _I[n] ?? e;
}
function Qg(t) {
  let e = W(t, 0);
  for (; e < 0; ) e += 360;
  for (; e >= 360; ) e -= 360;
  return e;
}
function ha() {
  var t, e, n;
  return W(((e = (t = canvas == null ? void 0 : canvas.scene) == null ? void 0 : t.grid) == null ? void 0 : e.distance) ?? ((n = canvas == null ? void 0 : canvas.dimensions) == null ? void 0 : n.distance), 1) || 1;
}
function tu() {
  var t, e;
  return W(((t = canvas == null ? void 0 : canvas.grid) == null ? void 0 : t.size) ?? ((e = canvas == null ? void 0 : canvas.dimensions) == null ? void 0 : e.size), 100) || 100;
}
function Cn(t = 0) {
  return W(t, 0) * (tu() / ha());
}
function co(t = 0) {
  return W(t, 0) * (ha() / tu());
}
function ef(t = {}, e = {}) {
  return Math.hypot(W(t.x, 0) - W(e.x, 0), W(t.y, 0) - W(e.y, 0));
}
function Ol(t) {
  return W(t, 0) * Math.PI / 180;
}
function UI({ geometry: t = {}, tokenCenter: e = {} } = {}) {
  const n = W(e.x, 0) - W(t.x, 0), i = W(e.y, 0) - W(t.y, 0), a = Ol(t.direction ?? 0), r = Math.cos(a), s = Math.sin(a);
  return Math.max(0, n * r + i * s);
}
function Hd(t = 0, e = 0) {
  if (!(e > 0)) return Te.none;
  const n = Math.max(0, Math.min(1, t / e));
  return n <= 1 / 3 ? Te.full : n <= 2 / 3 ? Te.major : n <= 1 ? Te.minor : Te.none;
}
function cb({ template: t = {}, placement: e = {} } = {}) {
  var l, c;
  const n = Fu((e == null ? void 0 : e.shape) ?? (t == null ? void 0 : t.shape), "");
  if (!n) return null;
  const i = W(
    (e == null ? void 0 : e.distance) ?? (t == null ? void 0 : t.distance) ?? (t == null ? void 0 : t.size),
    0
  );
  if (!(i > 0)) return null;
  const a = n === "cone" ? W((e == null ? void 0 : e.angle) ?? 90, 90) : null, r = n === "line" ? W((e == null ? void 0 : e.width) ?? ha(), ha()) : null, s = n === "rect" ? W((e == null ? void 0 : e.width) ?? (t == null ? void 0 : t.width) ?? (e == null ? void 0 : e.distance) ?? (t == null ? void 0 : t.distance) ?? (t == null ? void 0 : t.size), 0) : null, o = n === "rect" ? W((e == null ? void 0 : e.height) ?? (t == null ? void 0 : t.height) ?? (e == null ? void 0 : e.distance) ?? (t == null ? void 0 : t.distance) ?? (t == null ? void 0 : t.size), 0) : null;
  return n === "rect" && (!(s > 0) || !(o > 0)) ? null : {
    shape: n,
    measuredTemplateType: FA(n),
    x: W((l = e == null ? void 0 : e.anchor) == null ? void 0 : l.x, 0),
    y: W((c = e == null ? void 0 : e.anchor) == null ? void 0 : c.y, 0),
    direction: Qg((e == null ? void 0 : e.direction) ?? 0),
    distance: n === "rect" ? Math.max(s, o) : i,
    angle: a,
    width: r,
    height: n === "rect" ? o : null,
    anchorX: n === "rect" ? W((e == null ? void 0 : e.anchorX) ?? (t == null ? void 0 : t.anchorX) ?? 0, 0) : null,
    anchorY: n === "rect" ? W((e == null ? void 0 : e.anchorY) ?? (t == null ? void 0 : t.anchorY) ?? 0, 0) : null,
    placementMode: String((t == null ? void 0 : t.placement) ?? (e == null ? void 0 : e.placementMode) ?? "").trim() || null
  };
}
function vt(t = null, { template: e = null, placement: n = null } = {}) {
  var u, d, f, m;
  const i = t && typeof t == "object" ? t : {};
  if ((!i || !Object.keys(i).length) && (e || n))
    return cb({ template: e, placement: n });
  const a = String(
    i.measuredTemplateType ?? i.t ?? i.type ?? ""
  ).trim().toLowerCase(), r = Fu(
    i.shape ?? FI(a) ?? "",
    ""
  );
  if (!r)
    return e || n ? cb({ template: e, placement: n }) : null;
  const s = r === "rect" ? W(i.width ?? (n == null ? void 0 : n.width) ?? (e == null ? void 0 : e.width) ?? i.distance ?? i.size, 0) : 0, o = r === "rect" ? W(i.height ?? (n == null ? void 0 : n.height) ?? (e == null ? void 0 : e.height) ?? i.distance ?? i.size, 0) : 0, l = W(
    i.distance ?? i.size ?? i.templateDistance ?? (n == null ? void 0 : n.distance) ?? (e == null ? void 0 : e.distance) ?? (e == null ? void 0 : e.size),
    0
  );
  if (r === "rect") {
    if (!(s > 0) || !(o > 0)) return null;
  } else if (!(l > 0)) return null;
  return {
    shape: r,
    measuredTemplateType: a || FA(r),
    x: W(i.x ?? ((u = i.anchor) == null ? void 0 : u.x) ?? ((d = n == null ? void 0 : n.anchor) == null ? void 0 : d.x), 0),
    y: W(i.y ?? ((f = i.anchor) == null ? void 0 : f.y) ?? ((m = n == null ? void 0 : n.anchor) == null ? void 0 : m.y), 0),
    direction: Qg(i.direction ?? i.rotation ?? (n == null ? void 0 : n.direction) ?? 0),
    distance: r === "rect" ? Math.max(s, o) : l,
    angle: r === "cone" ? W(i.angle ?? (n == null ? void 0 : n.angle) ?? 90, 90) : null,
    width: r === "line" ? W(i.width ?? (n == null ? void 0 : n.width) ?? ha(), ha()) : r === "rect" ? s : null,
    height: r === "rect" ? o : null,
    anchorX: r === "rect" ? W(i.anchorX ?? (n == null ? void 0 : n.anchorX) ?? (e == null ? void 0 : e.anchorX) ?? 0, 0) : null,
    anchorY: r === "rect" ? W(i.anchorY ?? (n == null ? void 0 : n.anchorY) ?? (e == null ? void 0 : e.anchorY) ?? 0, 0) : null,
    placementMode: String(i.placementMode ?? (e == null ? void 0 : e.placement) ?? "").trim() || null
  };
}
function KI(t = null) {
  return t ? typeof (t == null ? void 0 : t.toObject) == "function" ? t.toObject() : typeof (t == null ? void 0 : t.toJSON) == "function" ? t.toJSON() : t && typeof t == "object" ? xA(t) : null : null;
}
function HI(t = []) {
  const e = [];
  for (const n of t) {
    const i = Number(n);
    i > 0 && (e.some((a) => Math.abs(a - i) < 1e-3) || e.push(i));
  }
  return e;
}
function jI(t = null, { placementMode: e = "region", shapeHint: n = "" } = {}) {
  const i = KI(t);
  if (!i || typeof i != "object") return null;
  const a = String(i.type ?? "").trim().toLowerCase(), r = Fu(n, "");
  if (a === "circle")
    return vt({
      shape: r || "blast",
      measuredTemplateType: "circle",
      x: i.x,
      y: i.y,
      distance: co(i.radius),
      placementMode: e
    });
  if (a === "ellipse") {
    const s = W(i.radiusX, 0), o = W(i.radiusY, 0);
    return !(s > 0) || Math.abs(s - o) > 1e-3 ? null : vt({
      shape: r || "blast",
      measuredTemplateType: "circle",
      x: W(i.x, 0) + s,
      y: W(i.y, 0) + o,
      distance: co(s),
      placementMode: e
    });
  }
  if (a === "cone")
    return vt({
      shape: r || "cone",
      measuredTemplateType: "cone",
      x: i.x,
      y: i.y,
      direction: i.rotation ?? i.direction,
      distance: co(i.radius),
      angle: i.angle,
      placementMode: e
    });
  if (a === "line") {
    const s = Array.from((t == null ? void 0 : t.measuredSegments) ?? []), o = HI(s.map((m) => m == null ? void 0 : m.distance)), l = Math.max(
      W(i.distance, 0),
      W(i.length, 0),
      W(i.radius, 0),
      ...o,
      0
    ), c = o.filter((m) => Math.abs(m - l) >= 1e-3), u = Math.max(
      0,
      W(i.width, 0),
      W(i.thickness, 0),
      c.length ? Math.min(...c) : 0
    ) || ha(), d = (t == null ? void 0 : t.origin) ?? i.origin ?? {}, f = s.reduce((m, g) => {
      const p = Number((g == null ? void 0 : g.distance) ?? 0), h = Number((m == null ? void 0 : m.distance) ?? 0);
      return p > h ? g : m;
    }, null);
    return vt({
      shape: r || "line",
      measuredTemplateType: "ray",
      x: d.x ?? i.x,
      y: d.y ?? i.y,
      direction: (f == null ? void 0 : f.angle) ?? i.rotation ?? i.direction,
      distance: l,
      width: u,
      placementMode: e
    });
  }
  return a === "rectangle" || a === "rect" ? vt({
    shape: r || "rect",
    measuredTemplateType: "rect",
    x: i.x,
    y: i.y,
    direction: i.rotation ?? i.direction,
    width: co(i.width),
    height: co(i.height),
    anchorX: i.anchorX,
    anchorY: i.anchorY,
    placementMode: e
  }) : null;
}
function UA(t = null, { placementMode: e = "region", shapeHint: n = "" } = {}) {
  const i = (t == null ? void 0 : t.document) ?? t ?? null, a = Array.from((i == null ? void 0 : i.shapes) ?? []);
  return a.length !== 1 ? null : jI(a[0], { placementMode: e, shapeHint: n });
}
function GI(t = null, e = null) {
  const n = vt(t);
  return n ? {
    template: {
      shape: n.shape,
      placement: n.placementMode ?? (e == null ? void 0 : e.placement) ?? null,
      distance: n.distance,
      size: n.distance
    },
    placement: {
      shape: n.shape,
      anchor: {
        x: n.x,
        y: n.y
      },
      distance: n.distance,
      direction: n.direction,
      angle: n.angle ?? void 0,
      width: n.width ?? void 0,
      placementMode: n.placementMode ?? null
    }
  } : null;
}
function KA(t) {
  var o, l, c, u, d, f, m, g, p;
  const e = (t == null ? void 0 : t.center) ?? ((o = t == null ? void 0 : t.object) == null ? void 0 : o.center) ?? null;
  if (e)
    return {
      x: W(e.x, 0),
      y: W(e.y, 0)
    };
  const n = W((t == null ? void 0 : t.x) ?? ((l = t == null ? void 0 : t.document) == null ? void 0 : l.x), 0), i = W((t == null ? void 0 : t.y) ?? ((c = t == null ? void 0 : t.document) == null ? void 0 : c.y), 0), a = W((t == null ? void 0 : t.w) ?? ((u = t == null ? void 0 : t.object) == null ? void 0 : u.w) ?? ((d = t == null ? void 0 : t.document) == null ? void 0 : d.width), 1), r = W((t == null ? void 0 : t.h) ?? ((f = t == null ? void 0 : t.object) == null ? void 0 : f.h) ?? ((m = t == null ? void 0 : t.document) == null ? void 0 : m.height), 1), s = W(((g = canvas == null ? void 0 : canvas.grid) == null ? void 0 : g.size) ?? ((p = canvas == null ? void 0 : canvas.dimensions) == null ? void 0 : p.size), 100);
  return {
    x: n + a * s / 2,
    y: i + r * s / 2
  };
}
function qI(t) {
  var i, a, r, s;
  const e = W((t == null ? void 0 : t.w) ?? ((i = t == null ? void 0 : t.object) == null ? void 0 : i.w) ?? ((a = t == null ? void 0 : t.document) == null ? void 0 : a.width), 1) * tu(), n = W((t == null ? void 0 : t.h) ?? ((r = t == null ? void 0 : t.object) == null ? void 0 : r.h) ?? ((s = t == null ? void 0 : t.document) == null ? void 0 : s.height), 1) * tu();
  return Math.max(e, n) / 2;
}
function WI({ geometry: t, tokenCenter: e, tokenRadius: n }) {
  const i = Cn(t.distance);
  return ef({ x: t.x, y: t.y }, e) <= i + n;
}
function VI({ geometry: t, tokenCenter: e, tokenRadius: n }) {
  const i = Cn(t.distance), a = Cn(t.width ?? ha()), r = Ol(t.direction), s = e.x - t.x, o = e.y - t.y, l = Math.cos(r), c = Math.sin(r), u = s * l + o * c;
  if (u < -n || u > i + n) return !1;
  const d = Math.max(0, Math.min(i, u)), f = t.x + d * l, m = t.y + d * c;
  return Math.hypot(e.x - f, e.y - m) <= n + a / 2;
}
function JI({ geometry: t, tokenCenter: e, tokenRadius: n }) {
  const i = Cn(t.distance), a = e.x - t.x, r = e.y - t.y, s = Math.hypot(a, r);
  if (s > i + n) return !1;
  if (s === 0) return !0;
  let l = Math.atan2(r, a) * 180 / Math.PI - t.direction;
  for (; l <= -180; ) l += 360;
  for (; l > 180; ) l -= 360;
  const c = W(t.angle, 90) / 2, u = Math.asin(Math.min(1, n / Math.max(s, 1))) * 180 / Math.PI;
  return Math.abs(l) <= c + u;
}
function YI({ geometry: t, tokenCenter: e, tokenRadius: n }) {
  const i = Cn(W(t.width, 0)), a = Cn(W(t.height, 0));
  if (!(i > 0) || !(a > 0)) return !1;
  const r = W(t.anchorX, 0), s = W(t.anchorY, 0), o = W(t.x, 0), l = W(t.y, 0), c = o + i * (0.5 - r), u = l + a * (0.5 - s), d = -Ol(t.direction ?? 0), f = Math.cos(d), m = Math.sin(d), g = e.x - c, p = e.y - u, h = g * f - p * m, b = g * m + p * f;
  return Math.abs(h) <= i / 2 + n && Math.abs(b) <= a / 2 + n;
}
function Uu(t = null, e = null) {
  const n = vt(t);
  if (!n || !e) return !1;
  const i = KA(e), a = qI(e);
  return n.shape === "blast" ? WI({ geometry: n, tokenCenter: i, tokenRadius: a }) : n.shape === "line" ? VI({ geometry: n, tokenCenter: i, tokenRadius: a }) : n.shape === "cone" ? JI({ geometry: n, tokenCenter: i, tokenRadius: a }) : n.shape === "rect" ? YI({ geometry: n, tokenCenter: i, tokenRadius: a }) : !1;
}
function HA({ template: t = {}, placement: e = {}, geometry: n = null, token: i = null } = {}) {
  if (!i) return Te.none;
  const a = vt(n, { template: t, placement: e });
  if (!a || !Uu(a, i))
    return Te.none;
  const r = KA(i), s = Cn(a.distance);
  if (!(s > 0)) return Te.none;
  if (a.shape === "line" || a.shape === "cone") {
    const l = UI({ geometry: a, tokenCenter: r });
    return Hd(l, s);
  }
  if (a.shape === "rect") {
    const l = {
      x: W(a.x, 0) + Cn(W(a.width, 0)) * (0.5 - W(a.anchorX, 0)),
      y: W(a.y, 0) + Cn(W(a.height, 0)) * (0.5 - W(a.anchorY, 0))
    }, c = ef(l, r);
    return Hd(c, s);
  }
  const o = ef({ x: a.x, y: a.y }, r);
  return Hd(o, s);
}
function Va({ tier: t = Te.none, appliedTier: e = null, evadeUsed: n = !1, evadeLocked: i = !1 } = {}) {
  const a = Mt(t, Te.none), r = Mt(e ?? a, a);
  return {
    initialTier: a,
    initialLabel: ci(a),
    initialMultiplier: Xm(a),
    finalTier: r,
    finalLabel: ci(r),
    finalMultiplier: Xm(r),
    evadeUsed: !!n,
    evadeLocked: !!i
  };
}
function Xg(t = {}, { locked: e = !1, active: n = !1 } = {}) {
  const i = Mt((t == null ? void 0 : t.initialTier) ?? (t == null ? void 0 : t.tier), Te.none);
  if (!n || e || i === Te.none)
    return Va({
      tier: i,
      appliedTier: i,
      evadeUsed: !1,
      evadeLocked: !!e
    });
  const a = Zm(i, 1);
  return Va({
    tier: i,
    appliedTier: a,
    evadeUsed: i !== a,
    evadeLocked: !!e
  });
}
function jA(t = []) {
  return t.map((e) => ({
    x: Math.round(W(e.x, 0)),
    y: Math.round(W(e.y, 0))
  }));
}
function QI(t = {}) {
  const e = Cn(W(t.distance, 0)), n = Cn(W(t.width, ha())) / 2, i = Ol(t.direction ?? 0), a = Math.cos(i), r = Math.sin(i), s = -r, o = a, l = {
    x: W(t.x, 0) + e * a,
    y: W(t.y, 0) + e * r
  };
  return {
    type: "polygon",
    points: jA([
      { x: t.x + s * n, y: t.y + o * n },
      { x: l.x + s * n, y: l.y + o * n },
      { x: l.x - s * n, y: l.y - o * n },
      { x: t.x - s * n, y: t.y - o * n }
    ])
  };
}
function XI(t = {}) {
  const e = W(t.angle, 90), n = Cn(W(t.distance, 0)), i = W(t.direction, 0), a = e / 2, r = [{ x: t.x, y: t.y }];
  for (let s = 0; s <= 8; s += 1) {
    const o = -a + e / 8 * s, l = Ol(i + o);
    r.push({
      x: W(t.x, 0) + Math.cos(l) * n,
      y: W(t.y, 0) + Math.sin(l) * n
    });
  }
  return {
    type: "polygon",
    points: jA(r)
  };
}
function ZI(t = {}) {
  return {
    type: "rectangle",
    x: Math.round(W(t.x, 0)),
    y: Math.round(W(t.y, 0)),
    width: Math.round(Cn(W(t.width, 0))),
    height: Math.round(Cn(W(t.height, 0))),
    rotation: Qg(t.direction ?? 0),
    anchorX: W(t.anchorX, 0),
    anchorY: W(t.anchorY, 0)
  };
}
function Ku(t = null) {
  const e = vt(t);
  if (!e) return [];
  if (e.shape === "blast") {
    const n = Cn(W(e.distance, 0));
    return [{
      type: "ellipse",
      x: Math.round(W(e.x, 0) - n),
      y: Math.round(W(e.y, 0) - n),
      radiusX: Math.round(n),
      radiusY: Math.round(n),
      rotation: 0
    }];
  }
  return e.shape === "line" ? [QI(e)] : e.shape === "cone" ? [XI(e)] : e.shape === "rect" ? [ZI(e)] : [];
}
function Ui(t = null) {
  const e = vt(t);
  return e ? xA(e) : null;
}
const GA = Object.freeze({
  penetrating: "Penetrating",
  concussive: "Concussive",
  energy: "Energy",
  thermal: "Thermal",
  electrical: "Electrical"
}), Vo = Object.freeze(
  Object.entries(GA).map(([t, e]) => ({ value: t, label: e }))
), eE = Object.freeze({
  ballistic: "penetrating",
  kinetic: "concussive",
  explosive: "concussive",
  laser: "energy",
  plasma: "thermal",
  electrical: "electrical",
  melee: "penetrating",
  corrosive: "thermal",
  poison: "concussive",
  none: "concussive",
  penetrating: "penetrating",
  concussive: "concussive",
  energy: "energy",
  thermal: "thermal"
}), tE = Object.freeze(
  Vo.map((t) => t.value)
), nu = Object.freeze({
  fatigue: Object.freeze({
    key: "fatigue",
    label: "Fatigue",
    rated: !1,
    aliases: ["fatigue", "stun", "nonlethal", "non-lethal"],
    resolve: () => ({ damageTrack: "fatigue", flags: ["fatigue"] })
  }),
  concealable: Object.freeze({
    key: "concealable",
    label: "Concealable",
    rated: !1,
    aliases: ["concealable", "conceal", "concealed"],
    resolve: () => ({ flags: ["concealable"] })
  }),
  singleShot: Object.freeze({
    key: "singleShot",
    label: "Single Shot",
    rated: !1,
    aliases: ["singleShot", "single-shot", "single shot", "normal"],
    resolve: () => ({
      fireModes: {
        single: { enabled: !0 },
        burst: { enabled: !1 },
        fullAuto: { enabled: !1 }
      },
      flags: ["singleShot"]
    })
  }),
  automatic: Object.freeze({
    key: "automatic",
    label: "Automatic",
    rated: !1,
    aliases: ["automatic", "auto", "burst", "fullAuto", "full-auto", "full auto"],
    resolve: () => ({
      fireModes: {
        single: { enabled: !0 },
        burst: { enabled: !0 },
        fullAuto: { enabled: !0 }
      },
      flags: ["automatic"]
    })
  }),
  spread: Object.freeze({
    key: "spread",
    label: "Spread",
    rated: !1,
    aliases: ["spread", "scatter", "shotgun"],
    resolve: () => ({ flags: ["spread"] })
  }),
  spaceCapable: Object.freeze({
    key: "spaceCapable",
    label: "Space Capable",
    rated: !1,
    aliases: ["spaceCapable", "space-capable", "space capable", "vacuum", "vacuumCapable"],
    resolve: () => ({ flags: ["spaceCapable"] })
  }),
  armorBypass: Object.freeze({
    key: "armorBypass",
    label: "Armor Bypass",
    rated: !1,
    aliases: ["armorBypass", "armor-bypass", "armor bypass", "bypass"],
    resolve: () => ({ flags: ["armorBypass"] })
  })
}), Hu = Object.freeze({
  bulky: Object.freeze({
    key: "bulky",
    label: "Bulky",
    rated: !1,
    aliases: ["bulky"],
    resolve: () => ({
      attributeModifiers: { reflexes: -1 },
      flags: ["bulky"]
    })
  }),
  stealth: Object.freeze({
    key: "stealth",
    label: "Stealth",
    rated: !1,
    aliases: ["stealth", "sneak"],
    resolve: () => ({
      noticeDn: 1,
      sensorTrackingPenalty: 1,
      flags: ["stealth"]
    })
  }),
  sealed: Object.freeze({
    key: "sealed",
    label: "Sealed",
    rated: !1,
    aliases: ["sealed", "environmental"],
    resolve: () => ({
      resistanceDice: { gas: 1, chemical: 1 },
      flags: ["sealed"]
    })
  }),
  concealable: Object.freeze({
    key: "concealable",
    label: "Concealable",
    rated: !1,
    aliases: ["concealable", "conceal", "concealed"],
    resolve: () => ({
      concealArmorDn: 1,
      flags: ["concealable"]
    })
  })
}), nE = Object.freeze(
  Object.values(nu).map((t) => ({
    value: t.key,
    label: t.label,
    rated: t.rated
  }))
), iE = Object.freeze(/* @__PURE__ */ new Set(["armorBypass"])), aE = Object.freeze(
  Object.values(Hu).map((t) => ({
    value: t.key,
    label: t.label,
    rated: t.rated
  }))
), qA = VA(nu), rE = VA(Hu);
function Ir(t) {
  return t && typeof t == "object" && !Array.isArray(t) ? Object.values(t).flatMap((e) => Ir(e)) : Array.isArray(t) ? t.map((e) => String(e ?? "").trim()).filter(Boolean) : String(t ?? "").split(",").map((e) => e.trim()).filter(Boolean);
}
function Xn(t, e = "penetrating") {
  const n = String(t ?? "").trim().toLowerCase();
  return eE[n] ?? e;
}
function WA(t) {
  const e = String(t ?? "").trim();
  return e ? Xn(e, "") : "";
}
function sE(t) {
  const e = String(t ?? "").trim().toLowerCase();
  return tE.includes(e);
}
function In(t) {
  const e = Xn(t, "");
  return GA[e] ?? String(t ?? "").trim();
}
function Ki(t) {
  const e = t ?? {}, n = Number(e.ballistic ?? 0) || 0, i = Number(e.melee ?? 0) || 0;
  return {
    penetrating: e.penetrating !== void 0 ? Number(e.penetrating ?? 0) || 0 : Math.max(n, i),
    concussive: e.concussive !== void 0 ? Number(e.concussive ?? 0) || 0 : Number(e.explosive ?? 0) || 0,
    energy: Number(e.energy ?? 0) || 0,
    thermal: Number(e.thermal ?? 0) || 0,
    electrical: Number(e.electrical ?? 0) || 0
  };
}
function wc(t) {
  return Ir(t);
}
function Jo(t) {
  return Ir(t);
}
function jd(t) {
  return eu(t);
}
function Mc(t = {}, e = "standard") {
  return BA(t, e);
}
function As(t = {}) {
  return NI(t);
}
function oE(t = null) {
  return DI(t);
}
function Yo(t = "id") {
  var n, i;
  const e = (i = (n = globalThis.foundry) == null ? void 0 : n.utils) == null ? void 0 : i.randomID;
  return typeof e == "function" ? e() : `${t}-${Math.random().toString(36).slice(2, 10)}`;
}
function VA(t) {
  const e = {};
  return Object.values(t).forEach((n) => {
    [n.key, ...n.aliases ?? []].forEach((i) => {
      e[iu(i)] = n.key;
    });
  }), Object.freeze(e);
}
function iu(t) {
  return String(t ?? "").trim().toLowerCase().replace(/[^a-z0-9]+/g, "");
}
function Zs(t) {
  return Array.isArray(t) ? t : t && typeof t == "object" ? Object.values(t) : t == null || t === "" ? [] : [t];
}
function JA(t, e) {
  return Zs(t).map((n) => lE(n, e)).filter(Boolean);
}
function lE(t, e) {
  if (typeof t == "string" || typeof t == "number") {
    const i = e[iu(t)];
    return i ? { id: Yo("trait"), key: i, rating: 1 } : null;
  }
  if (!t || typeof t != "object") return null;
  const n = e[iu(t.key ?? t.value ?? t.name)];
  return n ? {
    id: String(t.id ?? "").trim() || Yo("trait"),
    key: n,
    rating: Math.max(0, Number(t.rating ?? 0) || 0)
  } : null;
}
function Di(t) {
  return JA(t, qA);
}
function na(t) {
  return JA(t, rE);
}
function au(t) {
  return {
    close: Number((t == null ? void 0 : t.close) ?? (t == null ? void 0 : t.short) ?? 0) || 0,
    near: Number((t == null ? void 0 : t.near) ?? (t == null ? void 0 : t.medium) ?? 0) || 0,
    far: Number((t == null ? void 0 : t.far) ?? (t == null ? void 0 : t.long) ?? 0) || 0,
    extreme: Number((t == null ? void 0 : t.extreme) ?? 0) || 0
  };
}
function cE(t = {}, e = {}) {
  const n = au(t), i = au(e);
  return {
    close: n.close + i.close,
    near: n.near + i.near,
    far: n.far + i.far,
    extreme: n.extreme + i.extreme
  };
}
function uE(t, e) {
  var n;
  return ((n = e[t]) == null ? void 0 : n.label) ?? t;
}
function YA(t, e) {
  var a;
  const n = uE(t == null ? void 0 : t.key, e), i = Math.max(0, Number((t == null ? void 0 : t.rating) ?? 0) || 0);
  return (a = e[t == null ? void 0 : t.key]) != null && a.rated && i > 0 ? `${n} ${i}` : n;
}
function QA(t, e) {
  return Zs(t).map((n) => {
    const i = n == null ? void 0 : n.key, a = e[i];
    return a != null && a.resolve ? {
      entry: n,
      effect: a.resolve(n),
      label: YA(n, e)
    } : null;
  }).filter(Boolean);
}
function dE(t, e) {
  const n = { ...t ?? {} };
  return Object.entries(e ?? {}).forEach(([i, a]) => {
    n[i] = (Number(n[i] ?? 0) || 0) + (Number(a ?? 0) || 0);
  }), n;
}
function mE(t = []) {
  const e = {}, n = /* @__PURE__ */ new Set();
  for (const i of t.filter(Boolean)) {
    if (i.damageTrack !== void 0) {
      const a = String(i.damageTrack ?? "").trim();
      a && (e.damageTrack = a);
    }
    i.accuracyMod !== void 0 && (e.accuracyMod = (Number(e.accuracyMod ?? 0) || 0) + (Number(i.accuracyMod ?? 0) || 0)), i.ap !== void 0 && (e.ap = (Number(e.ap ?? 0) || 0) + (Number(i.ap ?? 0) || 0)), i.addHeat !== void 0 && (e.addHeat = (Number(e.addHeat ?? 0) || 0) + (Number(i.addHeat ?? 0) || 0)), i.bonusVsArmorTag && (e.bonusVsArmorTag = dE(e.bonusVsArmorTag, i.bonusVsArmorTag)), i.fireModes && (e.fireModes = XA(e.fireModes, i.fireModes));
    for (const a of i.flags ?? []) {
      const r = String(a ?? "").trim();
      r && n.add(r);
    }
  }
  return n.size > 0 && (e.flags = Array.from(n)), e;
}
function XA(t = {}, e = {}) {
  const n = As(t), i = As(e);
  return {
    single: { ...n.single, ...i.single },
    burst: { ...n.burst, ...i.burst },
    fullAuto: { ...n.fullAuto, ...i.fullAuto }
  };
}
function fE(t = [], e = []) {
  const n = Array.isArray(t) || typeof t == "string" ? { traits: t, standardTraits: e } : t ?? {}, i = Jo(n.traits), a = Di(n.standardTraits), r = QA(a, nu), s = i.map((o) => {
    var u;
    const l = qA[iu(o)];
    if (!l) return null;
    const c = (u = nu[l]) == null ? void 0 : u.resolve;
    return typeof c == "function" ? c({ key: l, rating: 1 }) : null;
  });
  return mE([
    ...r.map((o) => o.effect),
    ...s
  ]);
}
function gE(t) {
  const e = t ?? {}, n = Jg(), i = _A({
    traits: e.traits,
    keywords: e.keywords,
    report: n,
    path: "ammo.types[].traits"
  });
  return {
    id: String(e.id ?? "").trim() || Yo("ammo"),
    name: String(e.name ?? "").trim() || "Ammo",
    damageType: WA(e.damageType),
    apMod: Number(e.apMod ?? e.ap ?? 0) || 0,
    attackRatingBandMod: au(e.attackRatingBandMod ?? e.attackRatingBand),
    traits: i.traits,
    keywords: i.keywords,
    migration: n
  };
}
function pE(t) {
  var l;
  const e = t ?? {}, n = Math.max(0, Number(e.max ?? 0) || 0), i = Number(e.current), a = Number.isFinite(i) ? Math.max(0, Math.min(i, n > 0 ? n : i)) : Math.max(0, n), r = Zs(e.types).map(gE), s = String(e.activeTypeId ?? "").trim(), o = r.some((c) => c.id === s) ? s : ((l = r[0]) == null ? void 0 : l.id) ?? "";
  return {
    current: a,
    max: n,
    consumePerAttack: Math.max(1, Number(e.consumePerAttack ?? 1) || 1),
    activeTypeId: o,
    types: r
  };
}
function hE(t, e = "untracked") {
  const n = String(t ?? "").trim();
  return n ? n === "linked" ? "internal" : n === "perAttack" ? e : ["untracked", "internal", "actorResource", "itemRef"].includes(n) ? n : e : e;
}
function tf(t = {}) {
  const e = t ?? {};
  return {
    amount: Math.max(1, Number(e.amount ?? e.consumePerUse ?? e.consumePerAttack ?? 1) || 1),
    sourceId: String(e.sourceId ?? "").trim()
  };
}
function wt(t) {
  return String(t ?? "").trim().toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "");
}
function ub(t = {}) {
  const e = t ?? {};
  return {
    damage: Number(e.damage ?? e.damageMod ?? e.damageModifier ?? 0) || 0,
    damageType: WA(e.damageType),
    ap: Number(e.ap ?? e.apMod ?? 0) || 0,
    clusteringDice: Math.max(0, Number(e.clusteringDice ?? e.clusterDice ?? 0) || 0),
    attackRatingBand: au(e.attackRatingBand ?? e.attackRatingBandMod)
  };
}
function nf(t = {}) {
  return BA(t, "standard");
}
function bE(t) {
  return String(t ?? "").trim().toLowerCase() === "unloaded";
}
function on(t, { report: e = null, path: n = "system.payloads[]" } = {}) {
  var l;
  const i = t ?? {}, a = String(i.id ?? "").trim() || Yo("payload"), r = _A({
    traits: i.traits ?? ((l = i.modifies) == null ? void 0 : l.traits),
    keywords: i.keywords,
    report: e,
    path: `${n}.traits`
  }), s = Ir(i.compatibleWith ?? i.compatible), o = oE(i.template);
  return bE(a) ? {
    id: "unloaded",
    label: "Unloaded",
    compatibleWith: [],
    modifies: ub({}),
    traits: [],
    keywords: [],
    template: null,
    areaEffect: Gi({ kind: kt.none }),
    resolution: nf({ resolverKey: "standard" }),
    consumption: tf({ amount: 1, sourceId: "" })
  } : {
    id: a,
    label: String(i.label ?? i.name ?? "").trim() || "Payload",
    sourceType: String(i.sourceType ?? "").trim(),
    payloadKey: wt(i.payloadKey),
    itemId: String(i.itemId ?? "").trim(),
    itemUuid: String(i.itemUuid ?? "").trim(),
    reserveQuantity: Math.max(0, Math.trunc(Number(i.reserveQuantity ?? i.quantity ?? 0) || 0)),
    families: Ir(i.families),
    tags: Ir(i.tags),
    compatibleWith: s,
    modifies: ub(i.modifies ?? i),
    traits: r.traits,
    keywords: r.keywords,
    template: o,
    areaEffect: Gi(i.areaEffect ?? {}),
    resolution: nf(i.resolution ?? i),
    consumption: tf(i.consumption ?? i)
  };
}
function si(t) {
  var o, l, c, u, d, f;
  const e = t ?? {}, n = hE(
    e.kind || e.type || ((o = e.link) != null && o.actorPath || e.actorPath ? "actorResource" : "") || ((l = e.link) != null && l.itemId || e.itemId || (c = e.link) != null && c.itemPath || e.itemPath ? "itemRef" : "") || (e.tracking || e.current !== void 0 || e.max !== void 0 ? "internal" : "") || "untracked",
    "untracked"
  ), i = e.tracking ?? e, a = Math.max(0, Number(i.max ?? 0) || 0), r = Number(i.current), s = Number.isFinite(r) ? Math.max(0, Math.min(r, a > 0 ? a : r)) : Math.max(0, a);
  return {
    id: String(e.id ?? "").trim() || Yo("source"),
    label: String(e.label ?? e.name ?? "").trim() || "Source",
    kind: n,
    reloadable: e.reloadable !== void 0 ? !!e.reloadable : n === "internal",
    loadedPayloadKey: n === "untracked" ? "" : wt(e.loadedPayloadKey),
    tracking: {
      current: s,
      max: a
    },
    link: {
      actorPath: String(((u = e.link) == null ? void 0 : u.actorPath) ?? e.actorPath ?? "").trim(),
      itemId: String(((d = e.link) == null ? void 0 : d.itemId) ?? e.itemId ?? "").trim(),
      itemPath: String(((f = e.link) == null ? void 0 : f.itemPath) ?? e.itemPath ?? "").trim()
    }
  };
}
function ZA({ report: t = null, path: e = "system.payloads" } = {}) {
  return {
    payloads: [on({
      id: "unloaded",
      label: "Unloaded",
      resolution: { resolverKey: "standard" },
      consumption: { amount: 1, sourceId: "" }
    }, { report: t, path: `${e}[0]` })],
    selectedPayloadId: "unloaded",
    consumptionSources: [si({
      id: "untracked",
      label: "Untracked",
      kind: "untracked"
    })]
  };
}
function ek(t) {
  return String(t ?? "").trim().toLowerCase() === "melee";
}
function af(t = [], { report: e = null, path: n = "system.payloads" } = {}) {
  const i = Zs(t).map((a, r) => on(a, { report: e, path: `${n}[${r}]` })).filter(Boolean);
  return i.some((a) => a.id === "unloaded") ? i : [
    on({
      id: "unloaded",
      label: "Unloaded",
      resolution: { resolverKey: "standard" },
      consumption: { amount: 1, sourceId: "" }
    }, { report: e, path: `${n}[0]` }),
    ...i
  ];
}
function ju(t = {}, { report: e = null, path: n = "system.payloads" } = {}) {
  var d;
  const i = pE(t), a = Math.max(1, Number(i.consumePerAttack ?? 1) || 1), r = i.max > 0, s = r ? "internal-magazine" : "untracked", o = [si(r ? {
    id: s,
    label: "Internal Source",
    kind: "internal",
    tracking: {
      current: i.current,
      max: i.max
    }
  } : {
    id: s,
    label: "Untracked",
    kind: "untracked",
    tracking: {}
  })], l = i.types.length ? i.types.map((f, m) => on({
    id: f.id,
    label: f.name,
    modifies: {
      damageType: f.damageType,
      ap: f.apMod,
      attackRatingBand: f.attackRatingBandMod,
      traits: f.traits
    },
    keywords: f.keywords,
    resolution: { resolverKey: "standard" },
    consumption: {
      amount: a,
      sourceId: r ? s : ""
    }
  }, { report: e, path: `${n}[${m}]` })) : [on({
    id: "unloaded",
    label: "Unloaded",
    resolution: { resolverKey: "standard" },
    consumption: {
      amount: a,
      sourceId: r ? s : ""
    }
  }, { report: e, path: `${n}[0]` })], c = af(l, { report: e, path: n }), u = c.some((f) => f.id === i.activeTypeId) ? i.activeTypeId : ((d = c[0]) == null ? void 0 : d.id) ?? "unloaded";
  return {
    payloads: c,
    selectedPayloadId: u,
    consumptionSources: o
  };
}
function sa(t, { legacyAmmo: e = null, category: n = "", report: i = null, path: a = "system.payloads" } = {}) {
  if (ek(n)) return [];
  const r = Zs(t).map((s, o) => on(s, { report: i, path: `${a}[${o}]` })).filter(Boolean);
  return r.length > 0 ? af(r, { report: i, path: a }) : e ? af(ju(e, { report: i, path: a }).payloads, { report: i, path: a }) : ZA({ report: i, path: a }).payloads;
}
function Ia(t, { legacyAmmo: e = null } = {}) {
  const n = Zs(t).map(si).filter(Boolean);
  return n.length > 0 ? n : e ? ju(e).consumptionSources : ZA().consumptionSources;
}
function Oo(t, e = [], { legacyAmmo: n = null, category: i = "" } = {}) {
  var s;
  if (ek(i)) return "";
  const a = sa(e, { legacyAmmo: n, category: i }), r = String(t ?? "").trim();
  if (a.some((o) => o.id === r)) return r;
  if (n) {
    const o = ju(n).selectedPayloadId;
    if (a.some((l) => l.id === o)) return o;
  }
  return ((s = a[0]) == null ? void 0 : s.id) ?? "unloaded";
}
function db({ root: t = null, path: e = "", fallback: n = {} } = {}) {
  const i = String(e ?? "").trim();
  if (!t || !i)
    return {
      current: Math.max(0, Number(n.current ?? 0) || 0),
      max: Math.max(0, Number(n.max ?? 0) || 0),
      currentPath: i
    };
  const a = foundry.utils.getProperty(t, i);
  if (a && typeof a == "object") {
    const o = Math.max(0, Number(a.max ?? n.max ?? 0) || 0), l = Number(a.current);
    return {
      current: Number.isFinite(l) ? Math.max(0, Math.min(l, o > 0 ? o : l)) : Math.max(0, o),
      max: o,
      currentPath: `${i}.current`
    };
  }
  const r = Math.max(0, Number(a ?? n.current ?? 0) || 0), s = Math.max(r, Math.max(0, Number(n.max ?? 0) || 0));
  return {
    current: s > 0 ? Math.min(r, s) : r,
    max: s,
    currentPath: i
  };
}
function tk({ source: t = null, actor: e = null } = {}) {
  var i, a, r, s, o, l, c;
  if (!t)
    return {
      id: "",
      label: "",
      kind: "untracked",
      isTracked: !1,
      current: 0,
      max: 0,
      consumePerUse: 1,
      actorPath: "",
      itemId: "",
      itemPath: "",
      loadedPayloadKey: "",
      reloadable: !1
    };
  const n = {
    id: t.id,
    label: t.label,
    kind: t.kind,
    actorPath: String(((i = t.link) == null ? void 0 : i.actorPath) ?? "").trim(),
    itemId: String(((a = t.link) == null ? void 0 : a.itemId) ?? "").trim(),
    itemPath: String(((r = t.link) == null ? void 0 : r.itemPath) ?? "").trim(),
    loadedPayloadKey: wt(t.loadedPayloadKey),
    reloadable: !!t.reloadable
  };
  if (t.kind === "internal") {
    const u = Math.max(0, Number(((s = t.tracking) == null ? void 0 : s.current) ?? 0) || 0), d = Math.max(0, Number(((o = t.tracking) == null ? void 0 : o.max) ?? 0) || 0);
    return {
      ...n,
      isTracked: d > 0 || u > 0,
      current: u,
      max: d,
      currentPath: "",
      loadedPayloadKey: n.loadedPayloadKey,
      reloadable: n.reloadable
    };
  }
  if (t.kind === "actorResource") {
    const u = db({
      root: (e == null ? void 0 : e.system) ?? null,
      path: n.actorPath,
      fallback: t.tracking
    });
    return {
      ...n,
      isTracked: !0,
      current: u.current,
      max: u.max,
      currentPath: u.currentPath,
      loadedPayloadKey: n.loadedPayloadKey,
      reloadable: n.reloadable
    };
  }
  if (t.kind === "itemRef") {
    const u = ((c = (l = e == null ? void 0 : e.items) == null ? void 0 : l.get) == null ? void 0 : c.call(l, n.itemId)) ?? null, d = db({
      root: (u == null ? void 0 : u.system) ?? null,
      path: n.itemPath,
      fallback: t.tracking
    });
    return {
      ...n,
      isTracked: !0,
      current: d.current,
      max: d.max,
      currentPath: d.currentPath,
      sourceItem: u,
      loadedPayloadKey: n.loadedPayloadKey,
      reloadable: n.reloadable
    };
  }
  return {
    ...n,
    isTracked: !1,
    current: 0,
    max: 0,
    currentPath: "",
    loadedPayloadKey: "",
    reloadable: !1
  };
}
function yE({ source: t = null, actor: e = null } = {}) {
  return tk({ source: t, actor: e });
}
function rf({
  payloads: t = [],
  selectedPayloadId: e = "",
  consumptionSources: n = [],
  actor: i = null,
  payloadId: a = "",
  category: r = ""
} = {}) {
  const s = sa(t, { category: r }), o = Ia(n), l = Oo(a || e, s, { category: r }), c = s.find((m) => m.id === l) ?? s[0] ?? null, u = (c == null ? void 0 : c.consumption) ?? tf(), d = u.sourceId ? o.find((m) => m.id === u.sourceId) ?? null : o.find((m) => m.kind === "untracked") ?? si({
    id: "untracked",
    label: "Untracked",
    kind: "untracked"
  }), f = tk({ source: d, actor: i });
  return {
    payloads: s,
    activePayload: c,
    activePayloadId: (c == null ? void 0 : c.id) ?? "",
    payloadLabel: (c == null ? void 0 : c.label) ?? "",
    source: d,
    sourceState: {
      ...f,
      consumePerUse: Math.max(1, Number(u.amount ?? 1) || 1),
      sourceId: (d == null ? void 0 : d.id) ?? "",
      loadedPayloadKey: f.loadedPayloadKey ?? ""
    }
  };
}
function SE({
  damage: t = 0,
  damageType: e = "penetrating",
  ap: n = 0,
  attackRatingBand: i = {},
  traits: a = [],
  keywords: r = [],
  standardTraits: s = [],
  resolution: o = {},
  fireModes: l = {},
  payloads: c = [],
  selectedPayloadId: u = "",
  consumptionSources: d = [],
  payloadId: f = "",
  actor: m = null,
  ammo: g = null,
  ammoTypeId: p = "",
  category: h = ""
} = {}) {
  var G, q, ie, Z, Se, se, te;
  const b = rf({
    payloads: c != null && c.length ? c : void 0,
    selectedPayloadId: u || p,
    consumptionSources: d,
    actor: m,
    payloadId: f || p,
    category: h
  }), S = ((!c || c.length === 0) && g ? rf({
    ...ju(g),
    actor: m,
    payloadId: f || p,
    category: h
  }) : null) ?? b, A = S.activePayload, T = $A({
    traits: a,
    keywords: r
  }), C = Array.from(/* @__PURE__ */ new Set([
    ...T.traits,
    ...Jo(A == null ? void 0 : A.traits)
  ])), k = eu([
    ...T.keywords,
    ...eu(A == null ? void 0 : A.keywords)
  ]), w = Mc(o, "standard"), v = (G = A == null ? void 0 : A.resolution) != null && G.resolverKey ? nf(A.resolution) : w, N = As(l), P = Jg(), L = OI({
    weapon: {
      traits: T.traits,
      resolution: w
    },
    payload: A,
    effectiveTraits: C,
    effectiveResolution: v,
    report: P
  }), _ = Di(s), O = fE({
    traits: [],
    standardTraits: _
  }), x = Jo((A == null ? void 0 : A.traits) ?? []).filter((pe) => iE.has(pe));
  x.length > 0 && (O.flags = Array.from(/* @__PURE__ */ new Set([...O.flags ?? [], ...x])));
  const H = {
    ...S.sourceState
  };
  return delete H.sourceItem, {
    damage: Math.max(0, (Number(t ?? 0) || 0) + (Number(((q = A == null ? void 0 : A.modifies) == null ? void 0 : q.damage) ?? 0) || 0)),
    damageType: ((ie = A == null ? void 0 : A.modifies) == null ? void 0 : ie.damageType) || Xn(e),
    damageTrack: O.damageTrack || "physical",
    ap: (Number(n ?? 0) || 0) + (Number(((Z = A == null ? void 0 : A.modifies) == null ? void 0 : Z.ap) ?? 0) || 0),
    clusteringDice: Number(((Se = A == null ? void 0 : A.modifies) == null ? void 0 : Se.clusteringDice) ?? 0) || 0,
    attackRatingBand: cE(
      i,
      ((se = A == null ? void 0 : A.modifies) == null ? void 0 : se.attackRatingBand) ?? {}
    ),
    effects: O,
    traits: C,
    keywords: k,
    standardTraits: _,
    payloadLabel: S.payloadLabel,
    payload: A ? foundry.utils.deepClone(A) : null,
    payloadState: {
      payloads: S.payloads.map((pe) => foundry.utils.deepClone(pe)),
      activePayloadId: S.activePayloadId,
      payloadLabel: S.payloadLabel,
      sourceId: ((te = S.source) == null ? void 0 : te.id) ?? "",
      sourceLabel: S.sourceState.label ?? "",
      sourceKind: S.sourceState.kind ?? "untracked",
      isTracked: S.sourceState.isTracked,
      current: S.sourceState.current,
      max: S.sourceState.max,
      consumePerUse: S.sourceState.consumePerUse
    },
    source: S.source ? foundry.utils.deepClone(S.source) : null,
    sourceState: foundry.utils.deepClone(H),
    template: L.template ? foundry.utils.deepClone(L.template) : null,
    areaEffect: Gi((A == null ? void 0 : A.areaEffect) ?? {}),
    resolution: foundry.utils.deepClone(v),
    resolverKey: String((v == null ? void 0 : v.resolverKey) ?? "standard").trim() || "standard",
    fireModes: foundry.utils.deepClone(XA(N, O.fireModes)),
    capabilityReport: {
      ...P,
      liveCapabilities: L.liveCapabilities,
      isTemplated: L.isTemplated,
      template: L.template ? foundry.utils.deepClone(L.template) : null,
      resolverKey: String((v == null ? void 0 : v.resolverKey) ?? "standard").trim() || "standard"
    },
    ammoLabel: S.payloadLabel,
    ammoType: A ? foundry.utils.deepClone(A) : null,
    ammoState: {
      current: H.current,
      max: H.max,
      consumePerAttack: H.consumePerUse,
      activeTypeId: S.activePayloadId,
      types: S.payloads.map((pe) => {
        var K, be;
        return {
          id: pe.id,
          name: pe.label,
          damageType: ((K = pe.modifies) == null ? void 0 : K.damageType) ?? "",
          clusteringDice: Number(((be = pe.modifies) == null ? void 0 : be.clusteringDice) ?? 0) || 0,
          traits: pe.traits ?? [],
          keywords: pe.keywords ?? []
        };
      }),
      isTracked: H.isTracked,
      ammoLabel: S.payloadLabel
    }
  };
}
function ks(t = {}) {
  var e, n, i;
  return {
    attributeModifiers: {
      reflexes: Number(((e = t == null ? void 0 : t.attributeModifiers) == null ? void 0 : e.reflexes) ?? 0) || 0
    },
    noticeDn: Number((t == null ? void 0 : t.noticeDn) ?? 0) || 0,
    sensorTrackingPenalty: Number((t == null ? void 0 : t.sensorTrackingPenalty) ?? 0) || 0,
    resistanceDice: {
      gas: Number(((n = t == null ? void 0 : t.resistanceDice) == null ? void 0 : n.gas) ?? 0) || 0,
      chemical: Number(((i = t == null ? void 0 : t.resistanceDice) == null ? void 0 : i.chemical) ?? 0) || 0
    },
    concealArmorDn: Number((t == null ? void 0 : t.concealArmorDn) ?? 0) || 0,
    flags: Array.from(new Set(Ir(t == null ? void 0 : t.flags)))
  };
}
function AE(t = {}, e = {}) {
  const n = ks(t), i = ks(e);
  return ks({
    attributeModifiers: {
      reflexes: n.attributeModifiers.reflexes + i.attributeModifiers.reflexes
    },
    noticeDn: n.noticeDn + i.noticeDn,
    sensorTrackingPenalty: n.sensorTrackingPenalty + i.sensorTrackingPenalty,
    resistanceDice: {
      gas: n.resistanceDice.gas + i.resistanceDice.gas,
      chemical: n.resistanceDice.chemical + i.resistanceDice.chemical
    },
    concealArmorDn: n.concealArmorDn + i.concealArmorDn,
    flags: [...n.flags, ...i.flags]
  });
}
function Gd({ standardTraits: t = [] } = {}) {
  const e = na(t), n = QA(
    e,
    Hu
  ), i = n.reduce((a, r) => AE(a, r.effect ?? {}), ks({}));
  return {
    mitigationByType: Ki({}),
    reinforcedMax: 0,
    traitState: {
      reinforced: {
        current: 0,
        max: 0
      }
    },
    effects: i,
    traitEffects: i,
    labels: n.map((a) => a.label),
    standardTraits: e
  };
}
function kE({ standardTraits: t = [] } = {}) {
  return [
    ...na(t).map((n) => YA(n, Hu))
  ].filter(Boolean);
}
function Zg(t = null) {
  var o, l, c;
  const e = ((l = (o = t == null ? void 0 : t._mwdDerived) == null ? void 0 : o.personalCombat) == null ? void 0 : l.activeArmor) ?? null;
  if (e) return ks(e.traitEffects ?? e.effects);
  const n = t == null ? void 0 : t.items, a = (typeof (n == null ? void 0 : n.filter) == "function" ? n.filter(() => !0) : typeof (n == null ? void 0 : n.values) == "function" ? Array.from(n.values()) : Array.from(n ?? [])).filter((u) => {
    var f, m;
    const d = (u == null ? void 0 : u.canonicalType) ?? (u == null ? void 0 : u.type);
    return (((f = u == null ? void 0 : u.isArmor) == null ? void 0 : f.call(u)) ?? d === "armor") && ((m = u == null ? void 0 : u.system) == null ? void 0 : m.equipped);
  }), r = a.find((u) => {
    var d;
    return (d = u == null ? void 0 : u.system) == null ? void 0 : d.isPrimary;
  }) ?? a[0] ?? null, s = ((c = r == null ? void 0 : r.getArmorProfile) == null ? void 0 : c.call(r, { actor: t })) ?? null;
  return ks((s == null ? void 0 : s.traitEffects) ?? (s == null ? void 0 : s.effects));
}
function ep(t) {
  const e = Math.max(0, Number(t ?? 0) || 0);
  return e <= 0 ? 0 : Math.ceil(e / 4);
}
function qd(t = 0, e = {}, {
  previousRating: n = null,
  previousDurability: i = null,
  ratingChanged: a = !1,
  currentChanged: r = !1
} = {}) {
  const s = Math.max(0, Number(t ?? 0) || 0), o = Number(e == null ? void 0 : e.current), l = Number(e == null ? void 0 : e.max), c = Math.max(0, Number((i == null ? void 0 : i.max) ?? n ?? 0) || 0), u = Number(i == null ? void 0 : i.current), d = Number.isFinite(o), f = Number.isFinite(u), m = !Number.isFinite(l) || l <= 0, g = !d || o <= 0, p = s > 0 && m && g, h = !f || u >= c || c <= 0;
  return {
    current: Math.min(s, Math.max(0, Number((p || a && !r && h ? s : d ? o : f ? u : s) ?? 0) || 0)),
    max: s
  };
}
function TE({
  currentArmorRating: t = 0,
  mitigationByType: e = {},
  damageType: n
} = {}) {
  const i = Math.max(0, Number(t ?? 0) || 0);
  if (i <= 0)
    return {
      currentArmorRating: 0,
      baseMitigation: 0,
      typeMitigationMod: 0,
      totalMitigation: 0,
      isDestroyed: !0
    };
  const a = Xn(n, "penetrating"), r = Ki(e), s = ep(i), o = Number(r[a] ?? 0) || 0;
  return {
    currentArmorRating: i,
    baseMitigation: s,
    typeMitigationMod: o,
    totalMitigation: s + o,
    isDestroyed: !1
  };
}
function wE({ damageIncoming: t = 0, armorTags: e = [], effects: n = {} } = {}) {
  const i = new Set(wc(e));
  let a = Number(t ?? 0) || 0;
  const r = [];
  return Object.entries((n == null ? void 0 : n.bonusVsArmorTag) ?? {}).forEach(([s, o]) => {
    if (!i.has(s)) return;
    const l = Number(o ?? 0) || 0;
    l && (a *= 1 + l, r.push({ tag: s, bonus: l }));
  }), {
    damageIncoming: a,
    applied: r
  };
}
class tp {
  static checkSufficient(e, n, i) {
    if (n > i) {
      const a = At(ne.common.errors.insufficient, {
        resource: e,
        required: n,
        available: i
      });
      throw ui.notifications.error(a), a;
    }
  }
  static checkOutOfRange(e, n, i, a) {
    if (n < i || n > a) {
      const r = At(ne.common.errors.outOfRange, {
        resource: e,
        value: n,
        min: i,
        max: a
      });
      throw ui.notifications.error(r), r;
    }
  }
  static checkUserGM() {
    if (!game.user.isGM) {
      const e = ne.common.errors.onlyGM;
      throw ui.notifications.error(e), e;
    }
  }
  static checkItemType(e, n) {
    if (e.type != n) {
      const i = At(ne.common.errors.expectedType, {
        type: e.type ? ne.itemType.singular[e.type] : e.type,
        expectedType: n
      });
      throw ui.notifications.error(i), i;
    }
  }
  static checkActorCanReceiveDamage(e, n, i) {
    if (!n) {
      const a = At(ne.common.errors.actorCannotReceiveDamage, {
        actor: i.name,
        damageType: sE(e) ? In(e) : ne.actor.monitors[e] ?? ne.mwd.weaponDamageType[e] ?? ne.mwd.personalDamageType[e] ?? e
      });
      throw ui.notifications.error(a), a;
    }
  }
  static checkWeaponDefense(e, n) {
    var a;
    const i = e.getDefense();
    if ((((a = e.isPersonalWeapon) == null ? void 0 : a.call(e)) ?? e.type === M.itemType.personalWeapon) && !i) {
      const r = At(ne.common.errors.noDefenseOnWeapon, { actor: n.name, weapon: e.name });
      throw ui.notifications.error(r), r;
    }
  }
  static checkTargetsCount(e, n, i) {
    if (e > 0 && n.length > e) {
      const a = At(ne.common.errors.maxTargetsExceedeed, {
        weapon: this.name,
        area: ne.area[i],
        count: n.length,
        max: e
      });
      throw ui.notifications.error(a), a;
    }
  }
  static checkActorDefenseAction(e, n, i) {
    if (!e) {
      const a = At(ne.common.errors.actorDoesNotHaveDefense, {
        actor: n.name,
        defense: i.labelkey,
        actorType: ne.actorType[n.type]
      });
      throw ui.notifications.error(a), a;
    }
  }
}
function Xi(t, e, n, i, a, r = (s) => !0) {
  return {
    code: t,
    labelkey: ne.attributeAction[t],
    label: ne.attributeAction[t],
    attributeFunction1: e ?? ((s) => {
    }),
    attributeFunction2: n ?? ((s) => {
    }),
    icon: i,
    actorTypes: a,
    condition: r
  };
}
function tc(t, e) {
  return {
    code: t,
    labelkey: ne.defense[t],
    label: ne.defense[t],
    actionCode: e
  };
}
const Kt = M.actorAttributes, Ht = M.actorTypes, Wn = zu.actions, nc = zu.defenses, Wd = [
  Xi(Wn.defense, (t) => Kt.reflexes, (t) => Kt.intelligence, he.fontAwesome("fas fa-shield-alt"), [Ht.character, Ht.npc]),
  Xi(Wn.resistTorture, (t) => Kt.strength, (t) => Kt.guts, he.fontAwesome("fas fa-angry"), [Ht.character, Ht.npc]),
  Xi(Wn.perception, (t) => Kt.logic, (t) => Kt.guts, he.fontAwesome("fas fa-eye"), [Ht.character, Ht.npc]),
  Xi(Wn.perception, (t) => Kt.system, (t) => Kt.handling, he.fontAwesome("fas fa-video"), [Ht.vehicle, Ht.battlemech]),
  Xi(Wn.composure, (t) => Kt.charisma, (t) => Kt.guts, he.fontAwesome("fas fa-meh"), [Ht.character, Ht.npc]),
  Xi(Wn.judgeIntentions, (t) => Kt.charisma, (t) => Kt.charisma, he.fontAwesome("fas fa-theater-masks"), [Ht.character, Ht.npc]),
  Xi(Wn.memory, (t) => Kt.logic, (t) => Kt.logic, he.fontAwesome("fas fa-brain"), [Ht.character, Ht.npc]),
  Xi(Wn.catch, (t) => Kt.reflexes, (t) => Kt.reflexes, he.fontAwesome("fas fa-baseball-ball"), [Ht.character, Ht.npc]),
  Xi(Wn.lift, (t) => Kt.strength, (t) => Kt.strength, he.fontAwesome("fas fa-dumbbell"), [Ht.character, Ht.npc])
], ic = [
  tc(nc.physicalDefense, Wn.defense),
  tc(nc.physicalResistance, Wn.resistTorture),
  tc(nc.socialDefense, Wn.composure),
  tc(nc.mentalResistance, Wn.perception)
];
class _t {
  static init() {
    Handlebars.registerHelper("fixedDefenseCode", (e) => _t.fixedDefenseCode(e));
  }
  static all(e = void 0) {
    return e ? Wd.filter(e) : Wd;
  }
  static getActorActions(e) {
    return Wd.filter((n) => n.actorTypes.includes(e.type) && n.condition(e));
  }
  static fixedDefenseCode(e) {
    return zu.fixedDefenseCode[e] ?? e;
  }
  static getActorDefenses(e) {
    return ic.map((n) => {
      const i = _t.getActorAction(e, n.actionCode);
      return _t._convertToDefense(i, n);
    }).filter((n) => n == null ? void 0 : n.code);
  }
  static getDefenseAttributeAction(e) {
    var n;
    return (n = ic.find((i) => i.code == e)) == null ? void 0 : n.actionCode;
  }
  static getActorAction(e, n) {
    return _t.getActorActions(e).find((i) => i.code == n);
  }
  static getActorDefense(e, n) {
    n = _t.fixedDefenseCode(n);
    const i = ic.find((r) => r.code == n), a = _t.getActorAction(e, i.actionCode);
    return tp.checkActorDefenseAction(a, e, i), _t._convertToDefense(a, i);
  }
  static _convertToDefense(e, n) {
    return e ? foundry.utils.mergeObject(
      n,
      e ?? {},
      { overwrite: !1, inplace: !1 }
    ) : void 0;
  }
  static getDefenses() {
    return ic;
  }
  static prepareShortcut(e, n) {
    const i = _t.getActorActions(e).find((a) => a.code == n);
    if (i)
      return {
        icon: i.icon,
        label: i.labelkey,
        callback: (a) => a.actor.rollAttributeAction(n)
      };
  }
}
class ba {
  constructor() {
    this.remoteCalls = {}, game.socket.on(Go, async (e) => this.onSocketMessage(e));
  }
  static async register(e, n) {
    game.system.anarchy.remoteCall._register(e, n);
  }
  async _register(e, n) {
    if (this.remoteCalls[e])
      throw `RemoteCall msg ${e} is already registered`;
    foundry.utils.mergeObject(n, {
      callback: (i) => {
        console.log(tt + "RemoteCall [", e, "] (", i, ")");
      },
      condition: (i) => !0,
      multiple: !1
      /* true if multiple users should handle the message */
    }, { overwrite: !1 }), this.remoteCalls[e] = n, console.log(tt + "RemoteCall registered", e);
  }
  static call(e, n) {
    return game.system.anarchy.remoteCall._remoteCall(e, n);
  }
  _remoteCall(e, n) {
    const i = this.remoteCalls[e];
    return !i || i.condition(game.user) || !i.multiple && _i.isUniqueConnectedGM() ? !1 : (game.socket.emit(Go, { msg: e, data: n }), !0);
  }
  async onSocketMessage(e) {
    const n = this.remoteCalls[e.msg];
    if (n) {
      const i = n.condition(game.user), a = n.multiple, r = _i.isUniqueConnectedGM();
      i && (a || r) ? n.callback(e.data) : console.log(tt + "RemoteCall.onSocketMessage(", e, ") ignored :", i, a, r);
    } else
      console.log(tt + "RemoteCall: No callback registered for", e);
  }
}
const mb = "Users.blindMessageToGM";
class _i {
  static init() {
    ba.register(mb, {
      callback: (e) => _i.blindMessageToGM(e),
      condition: (e) => e.isGM
    });
  }
  static blindMessageToGM(e) {
    ba.call(mb, e) || ChatMessage.create({
      user: e.user,
      whisper: ChatMessage.getWhisperRecipients("GM"),
      blind: !0,
      content: At(ne.chat.blindMessageToGM, {
        user: game.user.name,
        message: e.content
      })
    });
  }
  static getUsers(e = (n) => !0) {
    return (game.version ? game.users : game.users.entities).filter(e);
  }
  static firstConnectedGM() {
    return _i.getUsers((e) => e.isGM && e.active).sort(ot.ascending((e) => e.id)).at(0) ?? {};
  }
  /**
   * @returns true pour un seul utilisateur: le premier GM connecté par ordre d'id
   */
  static isUniqueConnectedGM(e = game.user) {
    return e.id == _i.firstConnectedGM().id;
  }
  static firstResponsible(e) {
    if (!(e != null && e.testUserPermission))
      return;
    const n = _i.getUsers(
      (i) => i.active && e.testUserPermission(i, CONST.DOCUMENT_OWNERSHIP_LEVELS.OWNER)
    ).sort(ot.ascending((i) => i.id)).at(0);
    return (n == null ? void 0 : n.id) === game.user.id ? e : void 0;
  }
  static getTargetTokens(e) {
    return Array.from(e.targets);
  }
  static getSelectedTokens(e) {
    return Array.from(canvas.tokens.controlled);
  }
  static getSelectedActors() {
    return Array.from(canvas.tokens.controlled).map((e) => e.actor);
  }
  static getPlayerActor() {
    return game.user.character;
  }
}
const Xr = ne.actor.monitors, ea = ne.actor.counters, nk = {
  armor: {
    path: "system.monitors.armor.value",
    monitor: (t) => t.system.monitors.armor,
    iconChecked: he.fontAwesome("fas fa-shield-slash"),
    iconUnchecked: he.fontAwesome("fas fa-shield-alt"),
    iconHit: he.fontAwesome("fas fa-bahai"),
    resource: Xr.armor
  },
  fatigue: {
    path: "system.monitors.fatigue.value",
    monitor: (t) => t.system.monitors.fatigue,
    iconChecked: he.fontAwesome("fas fa-grimace"),
    iconUnchecked: he.fontAwesome("far fa-smile"),
    iconHit: he.fontAwesome("fas fa-bahai"),
    resource: Xr.fatigue,
    overflow: (t) => M.monitors.physical,
    useArmor: !0
  },
  physical: {
    path: "system.monitors.physical.value",
    monitor: (t) => t.system.monitors.physical,
    iconChecked: he.fontAwesome("fas fa-heartbeat"),
    iconUnchecked: he.fontAwesome("far fa-heart"),
    iconHit: he.fontAwesome("fas fa-bahai"),
    resource: Xr.physical,
    useArmor: !0
  },
  structure: {
    path: "system.monitors.structure.value",
    monitor: (t) => t.system.monitors.structure,
    iconChecked: he.fontAwesome("fas fa-car-crash"),
    iconUnchecked: he.fontAwesome("fas fa-car-alt"),
    iconHit: he.fontAwesome("fas fa-bahai"),
    resource: Xr.structure
  },
  heat: {
    path: "system.monitors.heat.value",
    monitor: (t) => t.system.monitors.heat,
    iconChecked: he.fontAwesome("fas fa-fire"),
    iconUnchecked: he.fontAwesome("far fa-sun"),
    iconHit: he.fontAwesome("fas fa-temperature-high"),
    resource: Xr.heat
  },
  criticals: {
    path: "system.hybrid.criticals.value",
    monitor: (t) => {
      var e;
      return ((e = t.system.hybrid) == null ? void 0 : e.criticals) ?? { value: 0, max: 0 };
    },
    iconChecked: he.fontAwesome("fas fa-bolt"),
    iconUnchecked: he.fontAwesome("far fa-dot-circle"),
    iconHit: he.fontAwesome("fas fa-exclamation-triangle"),
    resource: Xr.structure
  },
  anarchy: {
    path: "system.counters.anarchy.value",
    monitor: (t) => ({
      value: t.system.counters.anarchy.value,
      max: 6
    }),
    iconChecked: he.iconPath(`${To}/anarchy-point.webp`, "checkbar-img"),
    iconUnchecked: he.iconPath(`${To}/anarchy-point-off.webp`, "checkbar-img"),
    resource: ea.anarchy
  },
  plot: {
    path: "system.counters.anarchy.value",
    monitor: (t) => {
      const e = t.system.counters.anarchy.value;
      return { value: e, max: e + 1 };
    },
    iconChecked: he.iconPath(`${To}/danger-point.webp`, "checkbar-img"),
    iconUnchecked: he.iconPath(`${To}/danger-point-off.webp`, "checkbar-img"),
    resource: ea.anarchy
  },
  sceneAnarchy: {
    path: "system.counters.edgePools.chaos.value",
    monitor: (t) => {
      const e = t.getEdgePoolValue(M.counters.edgePools.chaos), n = t.getAttributeValue(M.actorAttributes.edge);
      return { value: e, max: n };
    },
    iconChecked: he.iconPath("systems/mwd/icons/default/explosion.svg", "checkbar-img"),
    iconUnchecked: he.iconPath("systems/mwd/icons/default/explosion.svg", "checkbar-img"),
    resource: ea.edgePools.chaos
  },
  grit: {
    path: "system.counters.edgePools.grit.value",
    monitor: (t) => ({ value: t.getEdgePoolValue(M.counters.edgePools.grit), max: t.getAttributeValue(M.actorAttributes.edge) }),
    iconChecked: he.iconPath("systems/mwd/icons/default/shield.svg", "checkbar-img"),
    iconUnchecked: he.iconPath("systems/mwd/icons/default/shield.svg", "checkbar-img"),
    resource: ea.edgePools.grit
  },
  insight: {
    path: "system.counters.edgePools.insight.value",
    monitor: (t) => ({ value: t.getEdgePoolValue(M.counters.edgePools.insight), max: t.getAttributeValue(M.actorAttributes.edge) }),
    iconChecked: he.iconPath("systems/mwd/icons/default/eye.svg", "checkbar-img"),
    iconUnchecked: he.iconPath("systems/mwd/icons/default/eye.svg", "checkbar-img"),
    resource: ea.edgePools.insight
  },
  legend: {
    path: "system.counters.edgePools.legend.value",
    monitor: (t) => ({ value: t.getEdgePoolValue(M.counters.edgePools.legend), max: t.getAttributeValue(M.actorAttributes.edge) }),
    iconChecked: he.iconPath("systems/mwd/icons/default/tower-flag.svg", "checkbar-img"),
    iconUnchecked: he.iconPath("systems/mwd/icons/default/tower-flag.svg", "checkbar-img"),
    resource: ea.edgePools.legend
  },
  credibility: {
    path: "system.counters.edgePools.credibility.value",
    monitor: (t) => ({ value: t.getEdgePoolValue(M.counters.edgePools.credibility), max: t.getAttributeValue(M.actorAttributes.edge) }),
    iconChecked: he.iconPath("systems/mwd/icons/misc/hand.svg", "checkbar-img"),
    iconUnchecked: he.iconPath("systems/mwd/icons/misc/hand.svg", "checkbar-img"),
    resource: ea.edgePools.credibility
  },
  rumor: {
    path: "system.counters.edgePools.rumor.value",
    monitor: (t) => ({ value: t.getEdgePoolValue(M.counters.edgePools.rumor), max: t.getAttributeValue(M.actorAttributes.edge) }),
    iconChecked: he.iconPath("systems/mwd/icons/default/mystery-man.svg", "checkbar-img"),
    iconUnchecked: he.iconPath("systems/mwd/icons/default/mystery-man.svg", "checkbar-img"),
    resource: ea.edgePools.rumor
  }
}, Ci = foundry.utils.mergeObject(nk, {});
class _e {
  static init() {
    Handlebars.registerHelper("iconCheckbar", _e.iconCheckbar), Handlebars.registerHelper("iconCheckbarHit", _e.iconHit);
  }
  static hackCheckbars(e) {
    if (e) {
      const n = foundry.utils.mergeObject(nk, {});
      foundry.utils.mergeObject(n, e, { recursive: !0 }), foundry.utils.mergeObject(Ci, n, { overwrite: !0 });
    }
  }
  static iconCheckbar(e, n) {
    return n ? _e.iconChecked(e) : _e.iconUnchecked(e);
  }
  static iconChecked(e) {
    var n;
    return (n = Ci[e]) == null ? void 0 : n.iconChecked;
  }
  static iconUnchecked(e) {
    var n;
    return (n = Ci[e]) == null ? void 0 : n.iconUnchecked;
  }
  static iconHit(e) {
    var n, i;
    return ((n = Ci[e]) == null ? void 0 : n.iconHit) ?? ((i = Ci[e]) == null ? void 0 : i.iconChecked);
  }
  static useArmor(e) {
    var n;
    return (n = Ci[e]) == null ? void 0 : n.useArmor;
  }
  static max(e, n) {
    var a;
    const i = (a = Ci[n]) == null ? void 0 : a.monitor(e);
    return ((i == null ? void 0 : i.max) ?? 0) + ((i == null ? void 0 : i.maxBonus) ?? 0);
  }
  static value(e, n) {
    var a;
    const i = (a = Ci[n]) == null ? void 0 : a.monitor(e);
    return (i == null ? void 0 : i.value) ?? 0;
  }
  static resistance(e, n, i = void 0) {
    return _e.resistanceDetail(e, n, i).value;
  }
  static resistanceDetail(e, n, i = void 0) {
    var l, c;
    const a = (l = Ci[n]) == null ? void 0 : l.monitor(e), r = _e._resolveResistance(a == null ? void 0 : a.resistance, i), s = _e._resolveResistance(a == null ? void 0 : a.resistanceBonus, i), o = i === void 0 ? 0 : Number(((c = a == null ? void 0 : a.resistanceBonusByType) == null ? void 0 : c[i]) ?? 0);
    return {
      value: r.value + s.value + o,
      damageType: i,
      source: r.source,
      bonusSource: s.source,
      bonusByType: o,
      usedType: r.source === "type" || s.source === "type" || o !== 0
    };
  }
  static _resolveResistance(e, n = void 0) {
    var r;
    if (typeof e == "number")
      return { value: e ?? 0, source: "legacy" };
    const i = n !== void 0 ? (r = e == null ? void 0 : e.byType) == null ? void 0 : r[n] : void 0;
    return i !== void 0 ? { value: Number(i) || 0, source: "type" } : { value: Number((e == null ? void 0 : e.default) ?? 0) || 0, source: "default" };
  }
  static newValue(e, n) {
    return e + (n ? 0 : 1);
  }
  static async switchMonitorCheck(e, n, i, a, r = void 0, s = void 0) {
    await _e.setCounter(e, n, _e.newValue(i, a), r, s);
  }
  static async addCounter(e, n, i, a = void 0) {
    if (i != 0) {
      const r = _e.getCounterValue(e, n, a) ?? 0;
      await _e.setCounter(e, n, r + i, a);
    }
  }
  static async setCounter(e, n, i, a = void 0, r = void 0) {
    switch (n) {
      case M.monitors.anarchy:
        return await _e.setAnarchy(e, i);
      case M.monitors.sceneAnarchy:
        return await _e.setSceneAnarchy(e, i);
    }
    return await _e.setCheckbar(e, n, i);
  }
  static getCounterValue(e, n, i) {
    switch (n) {
      case M.monitors.anarchy:
        return _e.getAnarchy(e, n);
    }
    return _e.value(e, n);
  }
  static async setCheckbar(e, n, i) {
    if (i == _e.getCounterValue(e, n))
      return;
    const a = Ci[n];
    if (a.path) {
      const r = _e.max(e, n);
      if (r <= 0)
        return;
      await _e._manageOverflow(a, e, n, i, r), i = Math.min(i, r), tp.checkOutOfRange(a.resource, i, 0, r), await e.setCheckbarValue(a.path, i);
    }
  }
  static async _manageOverflow(e, n, i, a, r) {
    if (a > r) {
      const s = e.overflow ? e.overflow(n) : void 0, o = e.recomputeOverflow ? e.recomputeOverflow(a - r) : a - r;
      s && o > 0 && (_e._notifyOverflow(n, i, o, s), await _e.addCounter(n, s, o));
    }
  }
  static _notifyOverflow(e, n, i, a) {
    const r = At(ne.actor.monitors.overflow, {
      actor: e.name,
      monitor: ne.actor.monitors[n],
      overflow: i,
      overflowMonitor: ne.actor.monitors[a]
    });
    ui.notifications.warn(r);
  }
  static async _manageFatigueOverflow(e, n, i) {
    await _e.addCounter(e, M.monitors.physical, n - i);
  }
  static async setAnarchy(e, n) {
    if (e.hasOwnAnarchy()) {
      if (e.hasGMAnarchy()) {
        await game.system.anarchy.gmAnarchy.setAnarchy(n), e.render();
        return;
      }
      await _e._setAnarchyMonitor(e, M.monitors.anarchy, n);
    }
  }
  static async setSceneAnarchy(e, n) {
    await _e._setAnarchyMonitor(e, M.monitors.sceneAnarchy, n);
  }
  static async _setAnarchyMonitor(e, n, i) {
    const a = _e.value(e, n);
    await _e.setCheckbar(e, n, i), game.user.isGM || _e.notifyAnarchyChange(e, n, a, i);
  }
  static getAnarchy(e, n) {
    return !game.user.isGM && (!e.hasOwnAnarchy() || e.hasGMAnarchy()) || n == ea.anarchy && (!e.hasOwnAnarchy() || e.hasGMAnarchy()) ? 0 : _e.value(e, n);
  }
  static notifyAnarchyChange(e, n, i, a) {
    _i.blindMessageToGM({
      from: game.user.id,
      content: At(
        ne.gmManager.playerChangedAnarchy,
        {
          user: game.user.name,
          actor: e.name,
          monitor: ne.actor.counters[n],
          from: i,
          to: a
        }
      )
    });
  }
}
const np = 2, sf = "skillSpecializationCatalog", ME = [
  { key: "running", label: "Running" },
  { key: "jumping", label: "Jumping" },
  { key: "swimming", label: "Swimming" },
  { key: "climbing", label: "Climbing" },
  { key: "acrobatics", label: "Acrobatics" }
], ik = /* @__PURE__ */ new Set(), tn = [
  // Strength
  { code: "athletics", label: "Athletics", attribute: "strength", icon: `${Ke}/athletics.svg`, domains: ["physical"], specializations: ME },
  { code: "heavyWeapons", label: "Heavy Weapons", attribute: "strength", icon: `${Ke}/heavy-weapons.svg`, defense: "physicalDefense", domains: ["physical"] },
  // Reflexes
  { code: "escapeArtist", label: "Escape Artist", attribute: "reflexes", icon: `${Ke}/escape-artist.svg`, domains: ["physical"] },
  { code: "gunnery", label: "Gunnery", attribute: "reflexes", icon: `${Ke}/vehicle-weapons.svg`, defense: "physicalDefense", domains: ["physical"] },
  { code: "meleeCombat", label: "Melee Combat", attribute: "reflexes", icon: `${Ke}/close-combat.svg`, defense: "physicalDefense", domains: ["physical"] },
  { code: "piloting", label: "Piloting", attribute: "reflexes", icon: `${Ke}/piloting-ground-steering-wheel.svg`, domains: ["physical"] },
  { code: "projectileWeapons", label: "Projectile Weapons", attribute: "reflexes", icon: `${Ke}/projectile-weapons.svg`, defense: "physicalDefense", domains: ["physical"] },
  { code: "firearms", label: "Firearms", attribute: "reflexes", icon: `${Ke}/firearms.svg`, defense: "physicalDefense", domains: ["physical"] },
  { code: "stealth", label: "Stealth", attribute: "reflexes", icon: `${Ke}/stealth.svg`, domains: ["physical"] },
  { code: "zeroGOps", label: "Zero-G Operations", attribute: "reflexes", icon: `${Ke}/fly.svg`, domains: ["physical"] },
  // Intelligence
  { code: "art", label: "Art", attribute: "intelligence", icon: `${Ke}/art.svg`, domains: ["mental"] },
  { code: "artillery", label: "Artillery", attribute: "intelligence", icon: `${Ke}/artillery.svg`, domains: ["mental"] },
  { code: "systemOps", label: "System Operations", attribute: "intelligence", icon: `${Ke}/electronics.svg`, domains: ["mental"] },
  { code: "computers", label: "Computers", attribute: "intelligence", icon: `${Ke}/hacking.svg`, domains: ["mental"] },
  { code: "demolitions", label: "Demolitions", attribute: "intelligence", icon: `${Ke}/demolition.svg`, domains: ["mental"] },
  { code: "medTech", label: "Medtech", attribute: "intelligence", icon: `${Ke}/biotech.svg`, domains: ["mental"] },
  { code: "science", label: "Science", attribute: "intelligence", icon: `${Ke}/skills.svg`, domains: ["mental"] },
  { code: "perception", label: "Perception", attribute: "intelligence", icon: `${Ke}/skills.svg`, domains: ["mental"] },
  { code: "tactics", label: "Tactics", attribute: "intelligence", icon: `${Ke}/skills.svg`, domains: ["mental"] },
  { code: "technician", label: "Technician", attribute: "intelligence", icon: `${Ke}/engineering.svg`, domains: ["mental"] },
  { code: "tracking", label: "Tracking", attribute: "intelligence", icon: `${Ke}/tracking.svg`, domains: ["physical", "mental"] },
  { code: "navigation", label: "Navigation", attribute: "intelligence", icon: `${Ke}/piloting-other.svg`, domains: ["mental"] },
  // Guts
  { code: "administration", label: "Administration", attribute: "guts", icon: `${Ke}/knowledge.svg`, domains: ["social", "mental"] },
  { code: "animalHandling", label: "Animal Handling", attribute: "guts", icon: `${Ke}/animals.svg`, domains: ["physical", "mental"] },
  { code: "survival", label: "Survival", attribute: "guts", icon: `${Ke}/survival.svg`, domains: ["physical", "mental"] },
  // Charisma
  { code: "acting", label: "Acting", attribute: "charisma", icon: `${Ke}/con-art.svg`, domains: ["social"] },
  { code: "deception", label: "Deception", attribute: "charisma", icon: `${Ke}/disguise.svg`, domains: ["social", "mental"] },
  { code: "leadership", label: "Leadership", attribute: "charisma", icon: `${Ke}/psychology.svg`, domains: ["social"] },
  { code: "negotiation", label: "Negotiation", attribute: "charisma", icon: `${Ke}/negotiation.svg`, domains: ["social"] },
  { code: "etiquette", label: "Etiquette", attribute: "charisma", icon: `${Ke}/etiquette.svg`, domains: ["social"] },
  { code: "streetwise", label: "Streetwise", attribute: "charisma", icon: `${Ke}/etiquette2.svg`, domains: ["social"] },
  { code: "intimidation", label: "Intimidation", attribute: "charisma", icon: `${Ke}/intimidation.svg`, domains: ["social", "mental"] }
].map(vE);
for (const t of tn)
  ik.add(t.code);
function vE(t) {
  return {
    ...t,
    label: t.label ?? t.code,
    icon: t.icon ?? `${Dl}/icons/skills/skills.svg`,
    specializations: ap(t.specializations)
  };
}
function ip(t) {
  return String(t ?? "").trim().toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, "");
}
function ap(t = []) {
  const e = /* @__PURE__ */ new Set();
  return (Array.isArray(t) ? t : []).map((n) => {
    const i = ip((n == null ? void 0 : n.key) ?? (n == null ? void 0 : n.label) ?? n);
    return !i || e.has(i) ? null : (e.add(i), {
      key: i,
      label: String((n == null ? void 0 : n.label) ?? (n == null ? void 0 : n.key) ?? n ?? i).trim() || i
    });
  }).filter(Boolean);
}
function CE(t = []) {
  const e = new Error(t[0] ?? "Invalid skill specialization data.");
  return e.validationErrors = t, e;
}
function IE() {
  const t = {};
  for (const e of tn) {
    const n = (Array.isArray(e.specializations) ? e.specializations : []).map((i) => String((i == null ? void 0 : i.label) ?? "").trim()).filter(Boolean);
    n.length && (t[e.code] = n);
  }
  return t;
}
const EE = Object.freeze(IE());
function NE(t, e = [], { strict: n = !1, errors: i = [] } = {}) {
  var r, s;
  if (!Array.isArray(e)) {
    if (n) {
      const o = ((r = of(t)) == null ? void 0 : r.label) ?? t;
      i.push(`${o}: expected an array of specialization labels.`);
    }
    return [];
  }
  const a = [];
  for (const o of e) {
    const l = String(o ?? "").trim();
    if (!l) {
      if (n) {
        const c = ((s = of(t)) == null ? void 0 : s.label) ?? t;
        i.push(`${c}: specialization labels cannot be blank.`);
      }
      continue;
    }
    a.push(l);
  }
  return ap(a).map((o) => o.label);
}
function of(t) {
  return tn.find((e) => e.code === t);
}
function ak(t, { strict: e = !1 } = {}) {
  const n = t && typeof t == "object" && !Array.isArray(t) ? t : {}, i = [], a = {};
  for (const [r, s] of Object.entries(n)) {
    if (!ik.has(r)) {
      e && i.push(`Unknown skill code "${r}".`);
      continue;
    }
    const o = NE(r, s, { strict: e, errors: i });
    o.length && (a[r] = o);
  }
  if (e && i.length) throw CE(i);
  return Object.fromEntries(
    tn.map((r) => [r.code, a[r.code]]).filter(([, r]) => Array.isArray(r) && r.length)
  );
}
function PE() {
  var t, e, n;
  try {
    const i = globalThis.game;
    if ((n = (e = (t = i == null ? void 0 : i.settings) == null ? void 0 : t.settings) == null ? void 0 : e.has) != null && n.call(e, `${I}.${sf}`))
      return i.settings.get(I, sf);
  } catch {
  }
  return ok();
}
function rk() {
  const t = ak(PE(), { strict: !1 });
  return Object.fromEntries(
    Object.entries(t).map(([e, n]) => [
      e,
      ap(n)
    ])
  );
}
function sk(t = [], { allowedKeys: e = null } = {}) {
  const n = /* @__PURE__ */ new Set();
  return (Array.isArray(t) ? t : []).map((i) => ip(i)).filter((i) => !i || n.has(i) || e && !e.has(i) ? !1 : (n.add(i), !0));
}
function Ft(t) {
  const e = of(t);
  if (e)
    return {
      ...e,
      specializations: Hi(e.code)
    };
}
function Qo() {
  const t = rk();
  return [...tn].map((e) => ({
    ...e,
    specializations: [...t[e.code] ?? []]
  })).sort((e, n) => e.label.localeCompare(n.label));
}
function Hi(t) {
  return [...rk()[t] ?? []];
}
function rp(t, e) {
  const n = ip(e);
  if (n)
    return Hi(t).find((i) => i.key === n);
}
function RE(t, e) {
  var n;
  return ((n = rp(t, e)) == null ? void 0 : n.label) ?? "";
}
function ok() {
  var e, n;
  return (((n = (e = globalThis.foundry) == null ? void 0 : e.utils) == null ? void 0 : n.deepClone) ?? ((i) => JSON.parse(JSON.stringify(i ?? null))))(EE);
}
function Gu(t, { strict: e = !1 } = {}) {
  return ak(t, { strict: e });
}
function Xo(t = []) {
  return sk(t);
}
function DE(t, e = []) {
  const n = new Set(Hi(t).map((a) => a.key)), i = new Set(sk(e, { allowedKeys: n }));
  return Hi(t).filter((a) => i.has(a.key)).map((a) => a.key);
}
function lf(t, e) {
  var n, i;
  return Xo(
    ((i = (n = t == null ? void 0 : t.skills) == null ? void 0 : n[e]) == null ? void 0 : i.specializations) ?? []
  );
}
function Ts(t, e) {
  return DE(
    e,
    lf(t, e)
  );
}
function lk(t, e) {
  const n = new Set(Ts(t, e));
  return Hi(e).filter((i) => n.has(i.key));
}
function OE(t) {
  const e = Math.ceil(t.length / 2);
  return { left: t.slice(0, e), right: t.slice(e) };
}
function LE(t) {
  var e, n;
  t.skills ?? (t.skills = {}), t.skills.disguise && !t.skills.deception && (t.skills.deception = { ...t.skills.disguise });
  for (const i of tn) {
    const a = (e = t.skills)[n = i.code] ?? (e[n] = {});
    a.rating == null && (a.rating = 0), a.bonus == null && (a.bonus = 0), a.specializations = Xo(a.specializations);
  }
}
function ck(t, { bonusBySkill: e = null } = {}) {
  const n = Qo(), { left: i, right: a } = OE(n), r = (s) => {
    var b, y, S, A, T, C;
    const o = s.code, l = s.attribute, c = Number(((y = (b = t == null ? void 0 : t.skills) == null ? void 0 : b[o]) == null ? void 0 : y.rating) ?? 0), u = Number(((A = (S = t == null ? void 0 : t.attributes) == null ? void 0 : S[l]) == null ? void 0 : A.value) ?? 0), d = Number(((C = (T = t == null ? void 0 : t.skills) == null ? void 0 : T[o]) == null ? void 0 : C.bonus) ?? 0), f = Number((e == null ? void 0 : e[o]) ?? 0), m = d + f, g = lk(t, o), p = g.length > 0 ? [] : Hi(o), h = u + c + m;
    return {
      code: o,
      label: s.label,
      icon: s.icon,
      attribute: l,
      attributeLabel: pt != null && pt.localizeAttribute ? pt.localizeAttribute(l) : l,
      rating: c,
      base: u,
      bonus: m,
      total: h,
      rollPayload: JSON.stringify({ intent: "skill", key: o }),
      canAddSpecialization: c >= 2 && p.length > 0,
      specializations: g.map((k) => ({
        ...k,
        bonus: np,
        rollPayload: JSON.stringify({
          intent: "skill",
          key: o,
          specializationKey: k.key,
          specializationLabel: k.label
        })
      })),
      // Input wiring paths (so templates don’t concat strings themselves)
      pathRating: `system.skills.${o}.rating`,
      pathBonus: `system.skills.${o}.bonus`
    };
  };
  return {
    left: i.map(r),
    right: a.map(r)
  };
}
const ws = {
  prone: {
    label: "Prone",
    mods: [
      { domains: ["physical", "combat", "movement", "movement.ground", "attack", "attack.melee", "defense"], value: -2 }
    ]
  },
  blinded: {
    label: "Blinded",
    mods: [
      { domains: ["physical", "combat", "attack", "attack.ranged", "attack.melee", "sensor", "sensor.acquire", "sensor.targeting", "skill.perception"], value: -3 },
      { domains: ["social"], value: -1 }
    ]
  },
  frightened: {
    label: "Frightened",
    mods: [
      { domains: ["mental", "social", "combat", "morale"], value: -1 }
    ]
  },
  deafened: {
    label: "Deafened",
    mods: [
      { domains: ["social", "skill.perception", "sensor"], value: -2 }
    ]
  },
  hidden: {
    label: "Hidden",
    mods: [
      { domains: ["skill.stealth", "stealth"], value: 2 }
    ]
  },
  suppressed: {
    label: "Suppressed",
    mods: [
      { domains: ["combat", "attack", "attack.ranged", "attack.melee"], value: -2 }
    ]
  },
  grappled: {
    label: "Grappled",
    mods: [
      { domains: ["physical", "movement", "movement.ground", "attack.melee"], value: -2 },
      { domains: ["combat", "attack"], value: -1 }
    ]
  },
  stunned: {
    label: "Stunned",
    mods: [
      { domains: ["physical", "combat", "mental", "attack", "movement"], value: -3 }
    ]
  },
  onFire: {
    label: "On Fire",
    mods: [
      { domains: ["physical", "combat", "attack", "movement"], value: -2 },
      { domains: ["mental"], value: -1 }
    ]
  },
  drugged: {
    label: "Drugged",
    mods: [
      { domains: ["physical", "mental", "combat", "attack", "movement"], value: -2 },
      { domains: ["social"], value: -1 }
    ]
  },
  radiation: {
    label: "Radiation",
    mods: [
      { domains: ["physical", "mental"], value: -1 }
    ]
  },
  overloaded: {
    label: "Overloaded",
    mods: [
      { domains: ["mental", "heat", "reactor"], value: -2 }
    ]
  },
  concussionMinor: {
    label: "Concussion I",
    mods: [
      { domains: ["physical", "mental", "combat", "initiative", "action"], value: -1 }
    ]
  },
  concussionModerate: {
    label: "Concussion II",
    mods: [
      { domains: ["physical", "mental", "combat", "initiative", "action"], value: -2 }
    ]
  },
  concussionSevere: {
    label: "Concussion III",
    mods: [
      { domains: ["physical", "mental", "combat", "initiative", "action"], value: -3 }
    ]
  },
  crippledMinor: {
    label: "Crippled I",
    mods: [
      { domains: ["movement", "movement.ground"], value: -2 }
    ]
  },
  crippledModerate: {
    label: "Crippled II",
    mods: [
      { domains: ["movement", "movement.ground"], value: -4 }
    ]
  },
  crippledSevere: {
    label: "Crippled III",
    mods: [
      { domains: ["movement", "movement.ground"], value: -6 }
    ]
  },
  hamperedModerate: {
    label: "Hampered II",
    mods: [
      { domains: ["physical"], value: -1 }
    ]
  },
  hamperedSevere: {
    label: "Hampered III",
    mods: [
      { domains: ["physical"], value: -1 }
    ]
  },
  offbalanceMinor: {
    label: "Off Balance I",
    mods: [
      { domains: ["physical", "combat", "attack", "defense"], value: -2 }
    ]
  },
  offbalanceModerate: {
    label: "Off Balance II",
    mods: [
      { domains: ["physical", "combat", "attack", "defense"], value: -4 }
    ]
  },
  offbalanceSevere: {
    label: "Off Balance III",
    mods: [
      { domains: ["physical", "combat", "attack", "defense"], value: -6 }
    ]
  }
}, Ms = "statusConditionCatalog", $E = Object.freeze([
  { value: "person", label: "Person" },
  { value: "machine", label: "Machine" },
  { value: "all", label: "All Actors" },
  { value: "character", label: "Character" },
  { value: "npc", label: "NPC" },
  { value: "vehicle", label: "Vehicle" },
  { value: "battlemech", label: "BattleMech" }
]), fb = Object.freeze([
  { value: "false", label: "No" },
  { value: "true", label: "Yes" }
]), uk = Object.freeze([
  M.actorTypes.character,
  M.actorTypes.npc
]), dk = Object.freeze([
  M.actorTypes.vehicle,
  M.actorTypes.battlemech
]), _E = Object.freeze([...uk, ...dk]), ru = Object.freeze({
  person: uk,
  machine: dk,
  all: _E,
  character: [M.actorTypes.character],
  npc: [M.actorTypes.npc],
  vehicle: [M.actorTypes.vehicle],
  battlemech: [M.actorTypes.battlemech]
}), BE = "systems/mwd/img/icons/status", xE = "systems/mwd/img/icons/systems", uo = `${xE}/Targeting.webp`, zE = Object.freeze([
  // Person conditions: lightly filtered from the existing status/icon pool.
  Q("prone", "Prone", "person", "physical", ["movement", "posture"], "prone.svg", { modifierKey: "prone", order: 10 }),
  Q("blinded", "Blinded", "person", "sensory", ["vision"], "blinded.svg", { modifierKey: "blinded", order: 20 }),
  Q("frightened", "Frightened", "person", "mental", ["morale"], "brain_injury.svg", { modifierKey: "frightened", order: 30 }),
  Q("deafened", "Deafened", "person", "sensory", ["hearing"], "deafened.svg", { modifierKey: "deafened", order: 40 }),
  Q("hidden", "Hidden", "person", "tactical", ["stealth"], "hidden.svg", { modifierKey: "hidden", order: 50 }),
  Q("suppressed", "Suppressed", "person", "tactical", ["offense"], "suppressed.svg", { modifierKey: "suppressed", order: 60 }),
  Q("grappled", "Grappled", "person", "physical", ["movement"], "grappled.svg", { modifierKey: "grappled", order: 70 }),
  Q("stunned", "Stunned", "person", "physical", ["action"], "concussion.svg", { modifierKey: "stunned", order: 80 }),
  Q("knockedOut", "Knocked Out", "person", "physical", ["unconscious"], "knockout.svg", { order: 90 }),
  Q("onFire", "On Fire", "all", "hazard", ["fire", "heat", "escalating"], "on_fire.svg", { modifierKey: "onFire", order: 100 }),
  Q("drugged", "Drugged", "person", "chemical", ["impairment"], "drugged.svg", { modifierKey: "drugged", order: 110 }),
  Q("radiation", "Radiation", "person", "hazard", ["radiation"], "radiation_low.svg", { modifierKey: "radiation", order: 120 }),
  Q("overloaded", "Overloaded", "all", "reactor", ["heat", "actionRestriction"], "surge.svg", { managed: !0, modifierKey: "overloaded", order: 130 }),
  Q("preparedInterrupt", "Prepared", "person", "tactical", ["reaction", "prepared"], "readied_action.svg", { manual: !1, managed: !0, order: 140 }),
  Q("personalCritical", "Personal Critical", "person", "damage", ["critical", "personal"], "surge.svg", { manual: !1, managed: !0, order: 145 }),
  Q("battleArmorWorn", "Battle Armor Worn", "person", "battleArmor", ["battleArmor", "worn"], "cover.svg", { manual: !1, managed: !0, order: 146 }),
  Q("battleArmorBreached", "Battle Armor Breached", "person", "battleArmor", ["battleArmor", "breached"], "surge.svg", { manual: !1, managed: !0, order: 147 }),
  Q("battleArmorWrecked", "Battle Armor Wrecked", "person", "battleArmor", ["battleArmor", "wrecked"], "broken_weapon.svg", { manual: !1, managed: !0, order: 148 }),
  Q("battleArmorRevealed", "Battle Armor Revealed", "person", "battleArmor", ["battleArmor", "revealed", "targeting"], "Targeting.webp", { icon: uo, order: 149 }),
  Q("machineCritical", "Machine Critical", "machine", "damage", ["critical", "system"], "surge.svg", { manual: !1, managed: !0, order: 150 }),
  Q("destroyed", "Destroyed", "machine", "damage", ["catastrophic", "destroyed", "ruined"], "on_fire.svg", { order: 160 }),
  // Personal critical bands.
  Q("windedMinor", "Winded I", "person", "critical", ["personalCritical", "burn"], "surge.svg", { manual: !1, managed: !0, order: 200 }),
  Q("windedModerate", "Winded II", "person", "critical", ["personalCritical", "burn"], "surge.svg", { manual: !1, managed: !0, order: 201 }),
  Q("windedSevere", "Winded III", "person", "critical", ["personalCritical", "burn"], "surge.svg", { manual: !1, managed: !0, order: 202 }),
  Q("concussionMinor", "Concussion I", "person", "critical", ["personalCritical", "action"], "concussion.svg", { manual: !1, managed: !0, modifierKey: "concussionMinor", order: 210 }),
  Q("concussionModerate", "Concussion II", "person", "critical", ["personalCritical", "action"], "concussion.svg", { manual: !1, managed: !0, modifierKey: "concussionModerate", order: 211 }),
  Q("concussionSevere", "Concussion III", "person", "critical", ["personalCritical", "action"], "concussion.svg", { manual: !1, managed: !0, modifierKey: "concussionSevere", order: 212 }),
  Q("crippledMinor", "Crippled I", "person", "critical", ["personalCritical", "movement"], "broken_leg.svg", { manual: !1, managed: !0, modifierKey: "crippledMinor", order: 220 }),
  Q("crippledModerate", "Crippled II", "person", "critical", ["personalCritical", "movement"], "broken_leg.svg", { manual: !1, managed: !0, modifierKey: "crippledModerate", order: 221 }),
  Q("crippledSevere", "Crippled III", "person", "critical", ["personalCritical", "movement"], "broken_leg.svg", { manual: !1, managed: !0, modifierKey: "crippledSevere", order: 222 }),
  Q("hamperedMinor", "Hampered I", "person", "critical", ["personalCritical", "weapon"], "broken_weapon.svg", { manual: !1, managed: !0, order: 230 }),
  Q("hamperedModerate", "Hampered II", "person", "critical", ["personalCritical", "weapon"], "broken_weapon.svg", { manual: !1, managed: !0, modifierKey: "hamperedModerate", order: 231 }),
  Q("hamperedSevere", "Hampered III", "person", "critical", ["personalCritical", "weapon", "posture"], "broken_weapon.svg", { manual: !1, managed: !0, modifierKey: "hamperedSevere", order: 232 }),
  Q("offbalanceMinor", "Off Balance I", "person", "critical", ["personalCritical", "attack", "defense"], "falling.svg", { manual: !1, managed: !0, modifierKey: "offbalanceMinor", order: 240 }),
  Q("offbalanceModerate", "Off Balance II", "person", "critical", ["personalCritical", "attack", "defense"], "falling.svg", { manual: !1, managed: !0, modifierKey: "offbalanceModerate", order: 241 }),
  Q("offbalanceSevere", "Off Balance III", "person", "critical", ["personalCritical", "attack", "defense"], "falling.svg", { manual: !1, managed: !0, modifierKey: "offbalanceSevere", order: 242 }),
  Q("shakenMinor", "Shaken I", "person", "critical", ["personalCritical", "actionRestriction"], "brain_injury.svg", { manual: !1, managed: !0, order: 250 }),
  Q("shakenModerate", "Shaken II", "person", "critical", ["personalCritical", "actionRestriction", "reaction"], "brain_injury.svg", { manual: !1, managed: !0, order: 251 }),
  Q("shakenSevere", "Shaken III", "person", "critical", ["personalCritical", "actionRestriction", "reaction"], "brain_injury.svg", { manual: !1, managed: !0, order: 252 }),
  // Machine stability and movement.
  Q("unstable", "Unstable", "machine", "stability", ["movement", "piloting", "knockdown"], "falling.svg", { order: 1e3 }),
  Q("staggeredMechanical", "Staggered (Mechanical)", "machine", "stability", ["movement", "actionRestriction"], "falling.svg", { order: 1010 }),
  Q("proneMechFall", "Prone", "battlemech", "stability", ["movement", "posture", "standUp"], "prone.svg", { order: 1020 }),
  Q("skidding", "Skidding", "machine", "movement", ["forcedMovement", "tracking"], "falling.svg", { order: 1030 }),
  Q("stalled", "Stalled", "machine", "movement", ["movement", "actionRestriction"], "emp.svg", { order: 1040 }),
  Q("limping", "Limping", "machine", "movement", ["movement", "location"], "broken_leg.svg", { order: 1050 }),
  Q("jumpJetFailure", "Jump Jet Failure", "battlemech", "movement", ["jump", "equipment"], "surge.svg", { order: 1060 }),
  Q("actuatorFailure", "Actuator Failure", "machine", "movement", ["movement", "handling"], "broken_leg.svg", { order: 1070 }),
  Q("gyroDamage", "Gyro Damage", "battlemech", "movement", ["movement", "piloting"], "falling.svg", { order: 1080 }),
  // Machine weapons.
  Q("weaponFailure", "Weapon Failure", "machine", "weapon", ["weapon", "mountScoped"], "broken_weapon.svg", { order: 1100 }),
  Q("jammedBallistic", "Jammed (Ballistic)", "machine", "weapon", ["weapon", "ballistic", "clearAction"], "broken_weapon.svg", { order: 1110 }),
  Q("armDestroyed", "Arm Destroyed", "battlemech", "damage", ["location", "weapon", "arm"], "dismembered_arm.svg", { order: 1120 }),
  // Sensors and electronics.
  Q("sensorDegraded", "Sensor Degraded", "machine", "sensor", ["sensor", "perception"], "all-seeing-eye.webp", { order: 1200 }),
  Q("sensorBlind", "Sensor Blind", "machine", "sensor", ["sensor", "targeting", "rangeLimit"], "damaged_eye.svg", { order: 1210 }),
  Q("ecmJamming", "ECM Jamming", "machine", "electronicWarfare", ["ecm", "tracking"], "emp.svg", { order: 1220 }),
  Q("ecmShrouded", "ECM Shrouded", "machine", "electronicWarfare", ["ecm", "defense"], "hidden.svg", { order: 1230 }),
  Q("epmBoosted", "EPM Boosted", "machine", "electronicWarfare", ["ecm", "sensor"], "all-seeing-eye.webp", { order: 1240 }),
  Q("eccmBoosted", "EPM Boosted (Legacy)", "machine", "electronicWarfare", ["ecm", "sensor", "legacy"], "all-seeing-eye.webp", { order: 1241 }),
  Q("sensorLocked", "Sensor Locked", "machine", "sensor", ["sensor", "targeted"], "all-seeing-eye.webp", { order: 1250 }),
  Q("trackingLost", "Tracking Lost", "machine", "sensor", ["sensor", "targetingData"], "damaged_eye.svg", { order: 1260 }),
  Q("signatureRevealed", "Signature Revealed", "machine", "sensor", ["stealth", "signature", "revealed"], "Targeting.webp", { icon: uo, order: 1270 }),
  Q("stealthActive", "Stealth Active", "machine", "sensor", ["stealth", "signature", "active"], "hidden.svg", { order: 1271 }),
  Q("highEmission", "High Emission", "machine", "sensor", ["stealth", "signature", "emission"], "surge.svg", { order: 1272 }),
  // Reactor and heat.
  Q("reactorInstability", "Reactor Instability", "machine", "reactor", ["heat", "reactor", "escalating"], "surge.svg", { order: 1300 }),
  Q("shutdown", "Shutdown", "machine", "reactor", ["heat", "actionRestriction"], "emp.svg", { order: 1310 }),
  Q("overheating", "Thermal Surge", "machine", "reactor", ["heat", "escalating"], "on_fire_mild.svg", { order: 1320 }),
  Q("coolingFailure", "Cooling Failure", "machine", "reactor", ["heat", "cooling"], "surge.svg", { order: 1325 }),
  Q("reactorBreach", "Reactor Breach", "machine", "reactor", ["reactor", "catastrophic", "countdown"], "radiation_high.svg", { order: 1330 }),
  // Machine damage and battlefield exposure.
  Q("legDestroyed", "Leg Destroyed", "battlemech", "damage", ["location", "movement", "leg"], "dismembered_leg.svg", { order: 1400 }),
  Q("exposed", "Exposed", "machine", "tactical", ["defense", "vulnerable"], "Targeting.webp", { icon: uo, order: 1410 }),
  Q("entrenchedHullDown", "Entrenched / Hull Down", "machine", "tactical", ["defense", "cover"], "cover.svg", { order: 1420 }),
  Q("obscured", "Obscured (Smoke/Dust)", "machine", "visibility", ["visibility", "cover"], "hidden.svg", { order: 1430 }),
  Q("obscuredLight", "Obscured (Light)", "machine", "visibility", ["visibility", "cover", "light"], "hidden.svg", { order: 1431 }),
  Q("obscuredHeavy", "Obscured (Heavy)", "machine", "visibility", ["visibility", "cover", "heavy"], "hidden.svg", { order: 1432 }),
  // Tactical markers.
  Q("evasiveWeave", "Evasive", "machine", "tactical", ["defense", "attackPenalty", "selfInduced"], "falling.svg", { order: 1500 }),
  Q("braced", "Braced", "machine", "tactical", ["defense", "mobilityPenalty"], "cover.svg", { order: 1510 }),
  Q("overextended", "Overextended", "machine", "tactical", ["attack", "defensePenalty"], "surge.svg", { order: 1520 }),
  Q("targetFocused", "Target Focused", "machine", "tactical", ["targeted", "attack"], "all-seeing-eye.webp", { order: 1530 }),
  Q("suppressedMechanical", "Suppressed", "machine", "tactical", ["offense", "suppressed"], "suppressed.svg", { order: 1540 }),
  Q("attachedToMachine", "Attached to Machine", "person", "battleArmor", ["battleArmor", "attached"], "grappled.svg", { order: 1600 }),
  Q("tagged", "TAGed", "all", "targeting", ["tag", "designated", "targeting"], "Targeting.webp", { icon: uo, order: 1610 }),
  Q("narced", "NARCed", "all", "targeting", ["narc", "beacon", "targeting"], "Targeting.webp", { icon: uo, order: 1620 })
]);
function Q(t, e, n, i, a, r, s = {}) {
  return {
    id: t,
    label: e,
    actorGroup: n,
    category: i,
    tags: a,
    icon: s.icon ?? `${BE}/${r}`,
    manual: s.manual ?? !0,
    managed: s.managed ?? !1,
    modifierKey: s.modifierKey ?? "",
    order: s.order ?? 0
  };
}
function FE() {
  return typeof foundry < "u" && (foundry == null ? void 0 : foundry.utils);
}
function mk(t) {
  return FE() && typeof foundry.utils.deepClone == "function" ? foundry.utils.deepClone(t) : JSON.parse(JSON.stringify(t));
}
function gb(t, e = !1) {
  if (typeof t == "boolean") return t;
  const n = String(t ?? "").trim().toLowerCase();
  return n ? ["true", "1", "yes", "y", "on"].includes(n) : e;
}
function sp() {
  return mk(zE);
}
function op(t) {
  const e = String(t ?? "").trim();
  if (!e) return "";
  const n = e.replace(/([a-z0-9])([A-Z])/g, "$1 $2").split(/[^A-Za-z0-9]+/).map((i) => i.trim()).filter(Boolean);
  return n.length ? n.map((i, a) => {
    const r = i.toLowerCase();
    return a === 0 ? r : `${r.charAt(0).toUpperCase()}${r.slice(1)}`;
  }).join("") : "";
}
function lp(t = []) {
  const e = Array.isArray(t) ? t : String(t ?? "").split(","), n = /* @__PURE__ */ new Set(), i = [];
  for (const a of e) {
    const r = op(a);
    !r || n.has(r) || (n.add(r), i.push(r));
  }
  return i;
}
function UE(t = []) {
  return lp(t).join(", ");
}
function fk(t, e = "person") {
  const i = String(t ?? "").trim().toLowerCase();
  return i === "battlemech" ? "battlemech" : Object.prototype.hasOwnProperty.call(ru, i) ? i : e;
}
function KE(t) {
  return [...ru[fk(t)] ?? []];
}
function HE(t = {}, { strict: e = !1, index: n = 0 } = {}) {
  const i = [], a = `Row ${n + 1}`, r = String((t == null ? void 0 : t.id) ?? "").trim(), s = String((t == null ? void 0 : t.label) ?? "").trim(), o = String((t == null ? void 0 : t.actorGroup) ?? "person").trim(), l = fk(o, ""), c = op((t == null ? void 0 : t.category) ?? "general") || "general", u = lp(t == null ? void 0 : t.tags), d = String((t == null ? void 0 : t.icon) ?? "").trim(), f = String((t == null ? void 0 : t.modifierKey) ?? "").trim(), m = Number((t == null ? void 0 : t.order) ?? 0);
  if (r || i.push(`${a}: id cannot be blank.`), s || i.push(`${a}: label cannot be blank.`), (!l || o && !Object.prototype.hasOwnProperty.call(ru, l)) && i.push(`${a}: actorGroup must be one of ${Object.keys(ru).join(", ")}.`), f && !(ws != null && ws[f]) && i.push(`${a}: modifierKey "${f}" is not a known mechanics-backed status.`), Number.isFinite(m) || i.push(`${a}: order must be numeric.`), e && i.length) {
    const g = new Error(i[0]);
    throw g.validationErrors = i, g;
  }
  return {
    id: r,
    label: s || r || "Status",
    actorGroup: l || "person",
    category: c,
    tags: u,
    icon: d,
    manual: gb(t == null ? void 0 : t.manual, !0),
    managed: gb(t == null ? void 0 : t.managed, !1),
    modifierKey: f,
    order: Number.isFinite(m) ? Math.trunc(m) : 0
  };
}
function Ja(t = [], { strict: e = !1 } = {}) {
  const n = Array.isArray(t) ? t : [], i = [], a = /* @__PURE__ */ new Set(), r = [];
  if (n.forEach((s, o) => {
    try {
      const l = HE(s, { strict: e, index: o });
      if (!l.id) return;
      const c = l.id.toLowerCase();
      if (a.has(c)) {
        e && i.push(`Row ${o + 1}: duplicate id "${l.id}".`);
        return;
      }
      a.add(c), r.push(l);
    } catch (l) {
      e && i.push(...Array.isArray(l.validationErrors) ? l.validationErrors : [l.message]);
    }
  }), e && i.length) {
    const s = new Error(i[0]);
    throw s.validationErrors = i, s;
  }
  return r.sort((s, o) => s.order !== o.order ? s.order - o.order : s.label.localeCompare(o.label));
}
function qu(t = void 0) {
  return t !== void 0 ? Ja(t, { strict: !1 }) : GE();
}
function gk(t = []) {
  const e = new Map(sp().map((r) => [r.id, r])), n = Ja(t, { strict: !1 }), i = [...n], a = new Set(n.map((r) => r.id));
  for (const [r, s] of e.entries())
    a.has(r) || i.push(mk(s));
  return Ja(i, { strict: !1 });
}
async function jE() {
  var t, e, n, i;
  try {
    if (!((i = (n = (e = (t = globalThis.game) == null ? void 0 : t.settings) == null ? void 0 : e.settings) == null ? void 0 : n.has) != null && i.call(n, `${I}.${Ms}`))) return;
    const a = game.settings.get(I, Ms), r = gk(a);
    JSON.stringify(a) !== JSON.stringify(r) && await game.settings.set(I, Ms, r);
  } catch {
  }
}
function GE() {
  var e, n, i;
  const t = (e = globalThis.game) == null ? void 0 : e.settings;
  try {
    if ((i = (n = t == null ? void 0 : t.settings) == null ? void 0 : n.has) != null && i.call(n, `${I}.${Ms}`))
      return gk(t.get(I, Ms));
  } catch {
  }
  return sp();
}
function Ll(t, e = qu()) {
  const n = String(t ?? "").trim();
  return n ? e.find((i) => String(i.id ?? "").trim() === n) ?? null : null;
}
function pk(t = null) {
  return String(
    typeof t == "string" ? t : (t == null ? void 0 : t.type) ?? ""
  ).trim();
}
function Wu(t, e = null) {
  const n = pk(e);
  return !n || !t ? !1 : KE(t.actorGroup).includes(n);
}
function qE({ statusId: t = "", actor: e = null, metadata: n = {}, catalogEntry: i = null } = {}) {
  const a = i ?? Ll(t), r = lp((n == null ? void 0 : n.tags) ?? (a == null ? void 0 : a.tags) ?? []);
  return {
    id: String(t || (a == null ? void 0 : a.id) || "").trim(),
    category: op((n == null ? void 0 : n.category) ?? (a == null ? void 0 : a.category) ?? "general") || "general",
    tags: r,
    actorGroup: String((n == null ? void 0 : n.actorGroup) ?? (a == null ? void 0 : a.actorGroup) ?? "").trim(),
    actorType: pk(e),
    scope: String((n == null ? void 0 : n.scope) ?? "").trim(),
    location: String((n == null ? void 0 : n.location) ?? "").trim(),
    itemUuid: String((n == null ? void 0 : n.itemUuid) ?? "").trim(),
    targetUuid: String((n == null ? void 0 : n.targetUuid) ?? "").trim(),
    severity: String((n == null ? void 0 : n.severity) ?? "").trim(),
    notes: String((n == null ? void 0 : n.notes) ?? "").trim()
  };
}
function WE(t = qu()) {
  return Ja(t, { strict: !1 }).map((e) => ({
    id: e.id,
    name: e.label,
    label: e.label,
    img: e.icon,
    icon: e.icon
  }));
}
function pb() {
  if (typeof CONFIG > "u") return [];
  const t = WE();
  return CONFIG.statusEffects = t, t;
}
const VE = /* @__PURE__ */ new Set(["overloaded", "preparedInterrupt"]);
function hb(t) {
  return t ? (t == null ? void 0 : t.document) ?? t : null;
}
function JE(t, e) {
  var i, a, r;
  if (!t) return null;
  const n = hb(e) ?? hb(t == null ? void 0 : t.token);
  return n ? n.isLinked ? n.baseActor ?? ((r = (i = game.actors) == null ? void 0 : i.get) == null ? void 0 : r.call(i, ((a = n == null ? void 0 : n.baseActor) == null ? void 0 : a.id) ?? "")) ?? n.actor ?? t : n.actor ?? t : t;
}
function Vu(t) {
  const e = String(t ?? "").trim();
  if (!e) return "Status";
  const a = (e.includes(".") ? e.split(".").at(-1) : e).replace(/^status/i, "").replace(/([a-z0-9])([A-Z])/g, "$1 $2").replace(/[_-]+/g, " ").trim();
  return a ? a.replace(/\b\w/g, (r) => r.toUpperCase()) : e;
}
function YE(t) {
  const e = String((t == null ? void 0 : t.name) ?? (t == null ? void 0 : t.label) ?? (t == null ? void 0 : t.id) ?? "Status").trim();
  return e ? Vu(e) : "Status";
}
function QE(t) {
  const e = typeof (t == null ? void 0 : t.img) == "string" ? t.img.trim() : "";
  if (e) return e;
  const n = t ? Object.getOwnPropertyDescriptor(t, "icon") : null;
  return "value" in (n ?? {}) ? String(n.value ?? "").trim() : "";
}
function XE(t) {
  var e, n;
  return ((n = (e = globalThis.CSS) == null ? void 0 : e.escape) == null ? void 0 : n.call(e, String(t ?? ""))) ?? String(t ?? "").replace(/["\\]/g, "\\$&");
}
function hk(t) {
  var e;
  return Object.prototype.hasOwnProperty.call(((e = t == null ? void 0 : t.system) == null ? void 0 : e.burn) ?? {}, "overloaded");
}
function bk(t, e) {
  var a;
  const n = String(e ?? "").trim();
  return !t || !n ? null : Array.from(((a = t.effects) == null ? void 0 : a.contents) ?? t.effects ?? []).find((r) => {
    var s, o, l, c, u, d, f;
    return (o = (s = r == null ? void 0 : r.statuses) == null ? void 0 : s.has) != null && o.call(s, n) || Array.isArray(r == null ? void 0 : r.statuses) && r.statuses.includes(n) || ((c = (l = r == null ? void 0 : r.getFlag) == null ? void 0 : l.call(r, I, "status")) == null ? void 0 : c.id) === n || ((f = (d = (u = r == null ? void 0 : r.flags) == null ? void 0 : u[I]) == null ? void 0 : d.status) == null ? void 0 : f.id) === n ? !0 : String((r == null ? void 0 : r.statusId) ?? (r == null ? void 0 : r.id) ?? "").trim() === n;
  }) ?? null;
}
function yk(t, e) {
  var i, a, r;
  const n = bk(t, e);
  return ((i = n == null ? void 0 : n.getFlag) == null ? void 0 : i.call(n, I, "status")) ?? ((r = (a = n == null ? void 0 : n.flags) == null ? void 0 : a[I]) == null ? void 0 : r.status) ?? null;
}
function qi(t, e) {
  var n, i, a, r, s, o;
  return e === "overloaded" && hk(t) ? !!((i = (n = t == null ? void 0 : t.system) == null ? void 0 : n.burn) != null && i.overloaded) || !!((r = (a = t == null ? void 0 : t.statuses) == null ? void 0 : a.has) != null && r.call(a, e)) : ((o = (s = t == null ? void 0 : t.statuses) == null ? void 0 : s.has) == null ? void 0 : o.call(s, e)) ?? !1;
}
function ZE(t, e) {
  const n = qi(e, t.id), i = yk(e, t.id) ?? {};
  return {
    id: t.id,
    label: t.label,
    icon: t.icon,
    active: n,
    managed: !!t.managed || VE.has(t.id),
    manual: !!t.manual,
    legacy: !1,
    category: t.category,
    tags: [...t.tags ?? []],
    scope: String(i.scope ?? "").trim(),
    notes: String(i.notes ?? "").trim()
  };
}
function eN(t, e) {
  const n = (CONFIG.statusEffects ?? []).find((a) => String((a == null ? void 0 : a.id) ?? "").trim() === t) ?? null, i = yk(e, t) ?? {};
  return {
    id: t,
    label: n ? YE(n) : Vu(t),
    icon: n ? QE(n) : "",
    active: qi(e, t),
    managed: !1,
    manual: !1,
    legacy: !0,
    category: "",
    tags: [],
    scope: String(i.scope ?? "").trim(),
    notes: String(i.notes ?? "").trim()
  };
}
function Ju(t) {
  const e = /* @__PURE__ */ new Set(), n = qu(), i = [];
  for (const a of n) {
    const r = String((a == null ? void 0 : a.id) ?? "").trim();
    if (!r || e.has(r)) continue;
    const s = qi(t, r), o = Wu(a, t);
    !s && (!o || !a.manual) || (e.add(r), i.push(ZE(a, t)));
  }
  for (const a of Array.from((t == null ? void 0 : t.statuses) ?? [])) {
    const r = String(a ?? "").trim();
    !r || e.has(r) || (e.add(r), i.push(eN(r, t)));
  }
  return i.sort((a, r) => a.active !== r.active ? a.active ? -1 : 1 : a.legacy !== r.legacy ? a.legacy ? 1 : -1 : a.label.localeCompare(r.label));
}
function tN(t) {
  const e = /* @__PURE__ */ new Set();
  return Ju(t).filter((n) => n.active).map((n) => ({
    id: String(n.id ?? "").trim(),
    label: String(n.label ?? "").trim() || Vu(n.id)
  })).filter((n) => !n.id || e.has(n.id) ? !1 : (e.add(n.id), !0));
}
function nN(t) {
  if (!t.length)
    return "<p>No token statuses are configured.</p>";
  const e = foundry.utils.escapeHTML;
  return `
    <div class="mwd-token-status-dialog">
      <p style="margin-top: 0;">Toggle the statuses shown on this token.</p>
      <div style="display: grid; gap: 0.2rem; max-height: 20rem; overflow-y: auto; padding-right: 0.25rem;">
        ${t.map((i) => {
    const a = i.active ? "checked" : "", r = i.icon ? `<img src="${e(i.icon)}" alt="" width="20" height="20" style="flex: 0 0 20px; border: 0;" />` : "", s = i.managed ? '<small style="opacity: 0.7;">Managed by system state</small>' : "", o = i.legacy ? '<small style="opacity: 0.7;">Legacy / uncataloged</small>' : "";
    return `
      <div class="mwd-token-status-dialog__row" data-status-id="${e(i.id)}" style="display: grid; gap: 0.2rem; padding: 0.35rem 0; border-bottom: 1px solid rgba(255,255,255,0.08);">
        <label style="display: flex; align-items: center; gap: 0.5rem;">
          <input type="checkbox" name="status.${e(i.id)}.active" value="1" ${a} />
          ${r}
          <span style="flex: 1 1 auto;">${e(i.label)}</span>
          ${s}
          ${o}
        </label>
        ${i.legacy ? "" : `
          <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 0.35rem; padding-left: 1.85rem;">
            <input type="text" name="status.${e(i.id)}.scope" value="${e(i.scope ?? "")}" placeholder="Scope" />
            <input type="text" name="status.${e(i.id)}.notes" value="${e(i.notes ?? "")}" placeholder="Notes" />
          </div>
        `}
      </div>
    `;
  }).join("")}
      </div>
    </div>
  `;
}
async function iN({ actor: t, effects: e, selectedStatusIds: n }) {
  const i = new Map(n.map((a) => [a.id, a]));
  for (const a of e) {
    const r = i.get(a.id), s = !!(r != null && r.active);
    await mt({
      actor: t,
      statusId: a.id,
      active: s,
      metadata: (r == null ? void 0 : r.metadata) ?? {}
    });
  }
}
async function bb(t, e, n = {}) {
  const i = Ll(e);
  if (!i) return !1;
  const a = bk(t, e);
  if (!a) return !1;
  const r = qE({
    actor: t,
    statusId: e,
    metadata: n,
    catalogEntry: i
  }), s = { [`flags.${I}.status`]: r };
  return typeof a.update == "function" ? (await a.update(s), !0) : a.id && typeof t.updateEmbeddedDocuments == "function" ? (await t.updateEmbeddedDocuments("ActiveEffect", [{ _id: a.id, ...s }]), !0) : !1;
}
async function mt({ actor: t, statusId: e, active: n, metadata: i = {} }) {
  if (!t || !e) return !1;
  const a = qi(t, e);
  if (!!n === a)
    return n ? bb(t, e, i) : !1;
  const r = Ll(e), s = r ? Wu(r, t) : !1;
  return n && r && !s ? !1 : e === "overloaded" && hk(t) ? (await t.update({ "system.burn.overloaded": !!n }), !0) : (await t.toggleStatusEffect(e, { active: !!n, overlay: !1 }), n && await bb(t, e, i), !0);
}
function aN(t) {
  var i, a, r, s, o;
  const e = /* @__PURE__ */ new Map(), n = Array.from(((i = t == null ? void 0 : t.querySelectorAll) == null ? void 0 : i.call(t, "[data-status-id]")) ?? []);
  for (const l of n) {
    const c = String(((a = l == null ? void 0 : l.dataset) == null ? void 0 : a.statusId) ?? "").trim();
    if (!c) continue;
    const u = XE(c), d = !!((r = l.querySelector(`input[name="status.${u}.active"]`)) != null && r.checked), f = String(((s = l.querySelector(`input[name="status.${u}.scope"]`)) == null ? void 0 : s.value) ?? "").trim(), m = String(((o = l.querySelector(`input[name="status.${u}.notes"]`)) == null ? void 0 : o.value) ?? "").trim();
    e.set(c, {
      id: c,
      active: d,
      metadata: { scope: f, notes: m }
    });
  }
  return Array.from(e.values());
}
async function Yu({ actor: t, token: e } = {}) {
  var a;
  if (!t || !e) return !1;
  const n = JE(t, e), i = Ju(n);
  return i.length ? foundry.applications.api.DialogV2.wait({
    window: {
      title: `Token Statuses: ${e.name ?? t.name ?? "Token"}`
    },
    position: {
      width: 420
    },
    content: nN(i),
    buttons: [
      {
        action: "apply",
        label: "Apply",
        icon: "fa-solid fa-check",
        default: !0,
        callback: async (r, s) => {
          var o;
          try {
            const l = aN(s.form);
            return await iN({ actor: n, effects: i, selectedStatusIds: l }), !0;
          } catch (l) {
            return console.error("MWD | Failed to update token statuses", l), (o = ui.notifications) == null || o.error("Unable to update token statuses."), !1;
          }
        }
      },
      {
        action: "cancel",
        label: "Cancel",
        icon: "fa-solid fa-xmark",
        callback: () => !1
      }
    ],
    close: () => !1
  }) : ((a = ui.notifications) == null || a.warn("No token statuses are configured."), !1);
}
function rN() {
  typeof Hooks > "u" || Hooks.on("renderTokenHUD", (t, e, n = {}) => {
    var d, f, m, g, p;
    const i = (n == null ? void 0 : n._id) ?? (n == null ? void 0 : n.id) ?? "", a = ((f = (d = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : d.get) == null ? void 0 : f.call(d, i)) ?? null, r = (a == null ? void 0 : a.actor) ?? null;
    if (!r) return;
    const s = qu(), o = new Map(s.map((h) => [h.id, h])), c = typeof jQuery < "u" && e instanceof jQuery ? e[0] : e;
    if (!(c instanceof HTMLElement)) return;
    const u = c.querySelectorAll("[data-status-id], [data-statusId], [data-effect-id]");
    for (const h of u) {
      const b = String(
        ((m = h.dataset) == null ? void 0 : m.statusId) ?? ((g = h.dataset) == null ? void 0 : g.statusid) ?? ((p = h.dataset) == null ? void 0 : p.effectId) ?? ""
      ).trim();
      if (!b) continue;
      const y = o.get(b);
      if (!y) continue;
      !qi(r, b) && !Wu(y, r) && (h.hidden = !0, h.style.display = "none");
    }
  });
}
const sN = Object.freeze({
  STR: Ra.strength,
  REF: Ra.reflexes,
  GUTS: Ra.guts,
  INT: Ra.intelligence,
  CHA: Ra.charisma
}), oN = Object.freeze({
  STR: "Strength",
  REF: "Reflexes",
  GUTS: "Guts",
  INT: "Intelligence",
  CHA: "Charisma"
}), lN = Object.freeze({
  composure: {
    id: "composure",
    label: "Composure",
    formula: ["GUTS", "CHA"],
    tags: ["combat", "utility", "mental"],
    domains: ["mental"]
  },
  judgeIntent: {
    id: "judgeIntent",
    label: "Judge Intent",
    formula: ["INT", "CHA"],
    tags: ["combat", "utility", "social", "mental"],
    domains: ["social", "mental"]
  },
  memory: {
    id: "memory",
    label: "Memory",
    formula: ["INT", "INT"],
    tags: ["combat", "utility", "mental"],
    domains: ["mental"]
  },
  lift: {
    id: "lift",
    label: "Lift",
    formula: ["STR", "STR"],
    tags: ["combat", "utility", "physical"],
    domains: ["physical"]
  },
  endure: {
    id: "endure",
    label: "Endure",
    formula: ["STR", "GUTS"],
    tags: ["combat", "utility", "physical", "mental"],
    domains: ["physical", "mental"]
  },
  steady: {
    id: "steady",
    label: "Steady",
    formula: ["REF", "GUTS"],
    tags: ["combat", "utility", "physical", "mental", "recovery"],
    domains: ["physical", "mental"]
  }
});
function cp(t) {
  const e = String(t ?? "").trim();
  return e ? lN[e] ?? null : null;
}
function Sk(t) {
  const e = cp(t);
  return e ? {
    intent: "common",
    id: e.id
  } : null;
}
function cN(t) {
  return sN[String(t ?? "").trim().toUpperCase()] ?? null;
}
function uN(t) {
  return oN[String(t ?? "").trim().toUpperCase()] ?? String(t ?? "").trim().toUpperCase();
}
function dN(t = []) {
  return (Array.isArray(t) ? t : []).map((e) => String(e ?? "").trim().toUpperCase()).filter(Boolean).join(" + ");
}
const mN = Object.freeze(["automatic", "optional", "action", "triggered", "narrative"]), fN = Object.freeze([
  "dicePart",
  "dnPart",
  "cqPart",
  "damageAdjustment",
  "heatAdjustment",
  "targetingConstraint",
  "targetingDataModifier",
  "actionAvailability",
  "actionCostAdjustment",
  "derivedStatus",
  "resourceSpendPreview",
  "activationBudgetAdjustment",
  "burnRuleAdjustment",
  "conditionPenaltyAdjustment",
  "edgeEventHook",
  "creationBudgetAdjustment",
  "personalSpeedAdjustment",
  "initiativeAdjustment",
  "aimBonusAdjustment",
  "actionEffectAdjustment",
  "queuedDomainRequest",
  "summary"
]), Vd = Object.freeze([
  "eq",
  "neq",
  "gt",
  "gte",
  "lt",
  "lte",
  "includes",
  "notIncludes",
  "truthy",
  "falsy"
]), up = Object.freeze(["perActivation", "perRound", "perScene"]);
function gN() {
  var t, e, n;
  return ((n = (e = (t = globalThis.foundry) == null ? void 0 : t.utils) == null ? void 0 : e.randomID) == null ? void 0 : n.call(e)) ?? Math.random().toString(36).slice(2, 18);
}
function Er(t) {
  var e, n;
  return typeof ((n = (e = globalThis.foundry) == null ? void 0 : e.utils) == null ? void 0 : n.deepClone) == "function" ? globalThis.foundry.utils.deepClone(t) : JSON.parse(JSON.stringify(t ?? {}));
}
function Ak(t, e = "") {
  var i, a;
  const n = re(e);
  if (!(!t || !n))
    return typeof ((a = (i = globalThis.foundry) == null ? void 0 : i.utils) == null ? void 0 : a.getProperty) == "function" ? globalThis.foundry.utils.getProperty(t, n) : n.split(".").reduce((r, s) => r == null ? void 0 : r[s], t);
}
function Wr(t) {
  return t && typeof t == "object" && !Array.isArray(t) ? t : {};
}
function re(t, e = "") {
  return String(t ?? "").trim() || e;
}
function st(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function Ka(t, e = 0) {
  return Math.trunc(st(t, e));
}
function Gn(t = []) {
  return (Array.isArray(t) ? t : typeof t == "string" ? t.split(",") : t instanceof Set ? Array.from(t) : []).map((n) => String(n ?? "").trim()).filter(Boolean);
}
function dp(t) {
  if (typeof t != "string") return t;
  const e = t.trim();
  if (!e) return "";
  if (e === "true") return !0;
  if (e === "false") return !1;
  if (/^-?\d+(\.\d+)?$/.test(e)) return Number(e);
  if (e.startsWith("[") && e.endsWith("]") || e.startsWith("{") && e.endsWith("}"))
    try {
      return JSON.parse(e);
    } catch {
      return e;
    }
  return e;
}
function kk(t) {
  if (t == null) return "";
  if (typeof t == "string") return t;
  if (typeof t == "number" || typeof t == "boolean") return String(t);
  try {
    return JSON.stringify(t);
  } catch {
    return String(t);
  }
}
function pN(t = "") {
  const e = re(t, "automatic");
  return mN.includes(e) ? e : "automatic";
}
function hN(t = {}) {
  const e = Wr(t), a = (Vd.find((s) => e[s] !== void 0 && e[s] !== null) ?? (Vd.includes(String(e.op ?? "").trim()) ? String(e.op).trim() : "") ?? (Vd.includes(String(e.comparator ?? "").trim()) ? String(e.comparator).trim() : "")) || "eq", r = {
    id: re(e.id, gN()),
    fact: re(e.fact),
    op: a
  };
  return a !== "truthy" && a !== "falsy" && (r.value = dp(e[a] ?? e.value ?? "")), r;
}
function cf(t = []) {
  return (Array.isArray(t) ? t : []).map(hN);
}
function bN(t) {
  return t == null || t === "" ? null : Math.max(0, Ka(t, 0));
}
function Tk(t = {}) {
  const e = Wr(t);
  return up.reduce((n, i) => (Object.prototype.hasOwnProperty.call(e, i) && (n[i] = bN(e[i])), n), {});
}
function yN(t = null) {
  if (!t || typeof t != "object" || Array.isArray(t)) return null;
  const e = t, n = {};
  return e.charges !== void 0 && e.charges !== null && e.charges !== "" && (n.charges = Math.max(0, Ka(e.charges, 0))), e.cooldownTurns !== void 0 && e.cooldownTurns !== null && e.cooldownTurns !== "" && (n.cooldownTurns = Math.max(0, Ka(e.cooldownTurns, 0))), e.consume !== void 0 && (n.consume = !!e.consume), Object.keys(n).length ? n : null;
}
function SN(t = {}) {
  if (typeof t == "string") return re(t);
  const e = Wr(t);
  return {
    actorTypes: Gn(e.actorTypes),
    tags: Gn(e.tags),
    forbidsTags: Gn(e.forbidsTags),
    weaponTags: Gn(e.weaponTags),
    forbidsWeaponTags: Gn(e.forbidsWeaponTags),
    statuses: Gn(e.statuses),
    forbidsStatuses: Gn(e.forbidsStatuses),
    actionIds: Gn(e.actionIds ?? e.actions ?? e.action),
    skillIds: Gn(e.skillIds ?? e.skills ?? e.skill),
    modes: Gn(e.modes ?? e.mode),
    detectionState: re(e.detectionState),
    targetState: re(e.targetState),
    heatBand: re(e.heatBand)
  };
}
function AN(t = {}, e = {}, n = 0) {
  return {
    id: re(t.id, `${e.id}.output-${n + 1}`),
    label: re(t.label, e.label),
    phase: re(t.phase, e.phase),
    tags: Gn(t.tags),
    exclusiveGroup: re(t.exclusiveGroup ?? e.exclusiveGroup),
    priority: st(t.priority ?? e.priority, 0)
  };
}
function kN(t = {}) {
  const e = Wr(t.presentation);
  return Object.keys(e).length ? {
    label: re(e.label),
    defaultEnabled: !!e.defaultEnabled,
    showOnSheet: e.showOnSheet !== !1,
    showInRollDialog: e.showInRollDialog !== !1
  } : null;
}
function TN(t = {}, e = {}, n = 0) {
  const i = Wr(t), a = re(i.type), r = fN.includes(a) ? a : "summary", s = AN(i, e, n);
  switch (r) {
    case "dicePart":
    case "dnPart":
      return { ...s, type: r, value: st(i.value, 0) };
    case "cqPart":
      return { ...s, type: r, ar: st(i.ar, 0), dr: st(i.dr, 0) };
    case "damageAdjustment":
      return {
        ...s,
        type: r,
        value: st(i.value, 0),
        damageType: re(i.damageType),
        appliesTo: Gn(i.appliesTo),
        track: re(i.track)
      };
    case "heatAdjustment":
      return {
        ...s,
        type: r,
        value: st(i.value, 0),
        timing: re(i.timing)
      };
    case "targetingConstraint":
      return {
        ...s,
        type: r,
        constraint: re(i.constraint),
        value: i.value
      };
    case "targetingDataModifier":
      return {
        ...s,
        type: r,
        value: st(i.value, 0),
        packetMode: re(i.packetMode, "best")
      };
    case "actionAvailability":
      return {
        ...s,
        type: r,
        actionId: re(i.actionId),
        enabled: dp(i.enabled ?? i.available ?? !0) !== !1,
        reason: re(i.reason)
      };
    case "actionCostAdjustment":
      return {
        ...s,
        type: r,
        actionId: re(i.actionId),
        resource: re(i.resource),
        value: st(i.value, 0),
        timing: re(i.timing)
      };
    case "derivedStatus":
      return {
        ...s,
        type: r,
        key: re(i.key ?? i.status),
        value: i.value === void 0 ? !0 : i.value
      };
    case "activationBudgetAdjustment":
      return {
        ...s,
        type: r,
        resource: re(i.resource),
        value: st(i.value, 0)
      };
    case "burnRuleAdjustment":
      return {
        ...s,
        type: r,
        trigger: re(i.trigger),
        value: st(i.value, 0),
        min: i.min === void 0 ? null : st(i.min, 0),
        max: i.max === void 0 ? null : st(i.max, 0)
      };
    case "conditionPenaltyAdjustment":
      return {
        ...s,
        type: r,
        track: re(i.track),
        value: st(i.value, 0),
        minPenalty: i.minPenalty === void 0 ? null : st(i.minPenalty, 0),
        maxPenalty: i.maxPenalty === void 0 ? null : st(i.maxPenalty, 0)
      };
    case "edgeEventHook":
      return {
        ...s,
        type: r,
        trigger: re(i.trigger),
        effect: Er(i.effect ?? {})
      };
    case "creationBudgetAdjustment":
      return {
        ...s,
        type: r,
        budget: re(i.budget),
        value: st(i.value, 0)
      };
    case "personalSpeedAdjustment":
      return {
        ...s,
        type: r,
        value: st(i.value, 0),
        unit: re(i.unit, "meters")
      };
    case "initiativeAdjustment":
      return {
        ...s,
        type: r,
        value: st(i.value, 0),
        appliesTo: re(i.appliesTo, "total")
      };
    case "aimBonusAdjustment":
      return {
        ...s,
        type: r,
        value: st(i.value, 0),
        appliesTo: re(i.appliesTo)
      };
    case "actionEffectAdjustment":
      return {
        ...s,
        type: r,
        actionId: re(i.actionId),
        key: re(i.key),
        value: i.value
      };
    case "resourceSpendPreview":
      return {
        ...s,
        type: r,
        resource: re(i.resource),
        value: st(i.value, 0)
      };
    case "queuedDomainRequest":
      return {
        ...s,
        type: r,
        domain: re(i.domain),
        request: Er(i.request ?? {})
      };
    case "summary":
    default:
      return {
        ...s,
        type: "summary",
        text: re(i.text ?? e.summary)
      };
  }
}
function mp(t = {}, e = 0) {
  const n = Wr(t), i = {
    ...n,
    id: re(n.id, `rule-${e + 1}`),
    label: re(n.label, n.id ? String(n.id) : `Rule ${e + 1}`),
    sourceType: re(n.sourceType),
    enabled: dp(n.enabled ?? !0) !== !1,
    phase: re(n.phase, "passive"),
    mode: pN(n.mode),
    selector: SN(n.selector),
    trigger: Er(n.trigger ?? {}),
    requires: cf(n.requires ?? n.prerequisites),
    conditions: cf(n.conditions),
    outputs: [],
    limits: Tk(n.limits),
    usage: yN(n.usage),
    stackingKey: re(n.stackingKey),
    exclusiveGroup: re(n.exclusiveGroup),
    priority: st(n.priority, 0),
    summary: re(n.summary)
  };
  return i.presentation = kN(n), i.outputs = (Array.isArray(n.outputs) ? n.outputs : []).map((a, r) => TN(a, i, r)), i;
}
function Qu(t = {}, e = {}) {
  const n = (t == null ? void 0 : t.system) ?? t ?? {}, i = Wr(n);
  return {
    ...i,
    rules: (Array.isArray(i.rules) ? i.rules : []).map((a, r) => mp(a, r)).filter((a) => a.id && a.phase)
  };
}
function wk(t = []) {
  return new Set(Gn(t));
}
function or(t = {}, e = "") {
  const n = t == null ? void 0 : t[e];
  return n instanceof Set ? Array.from(n) : Array.isArray(n) ? n : [];
}
function lr(t = {}, ...e) {
  for (const n of e) {
    const i = Ak(t, n);
    if (i != null && i !== "") return i;
  }
  return "";
}
function Jd(t, e) {
  const n = wk(t);
  return e.every((i) => n.has(i));
}
function Yd(t, e) {
  const n = wk(t);
  return e.some((i) => n.has(i));
}
function wN(t = {}, e = {}) {
  var s, o, l, c, u, d, f, m, g, p;
  if (typeof t == "string") {
    const h = re(t);
    return h ? or(e, "selectors").some((y) => y === h || String(y ?? "").startsWith(`${h}.`)) : !0;
  }
  const n = re(lr(e, "actorType", "actor.type"));
  if ((s = t.actorTypes) != null && s.length && !t.actorTypes.includes(n) || (o = t.tags) != null && o.length && !Jd(or(e, "tags"), t.tags) || (l = t.forbidsTags) != null && l.length && Yd(or(e, "tags"), t.forbidsTags) || (c = t.weaponTags) != null && c.length && !Jd(or(e, "weaponTags"), t.weaponTags) || (u = t.forbidsWeaponTags) != null && u.length && Yd(or(e, "weaponTags"), t.forbidsWeaponTags) || (d = t.statuses) != null && d.length && !Jd(or(e, "statuses"), t.statuses) || (f = t.forbidsStatuses) != null && f.length && Yd(or(e, "statuses"), t.forbidsStatuses)) return !1;
  const i = re(lr(e, "action.id", "actionId"));
  if ((m = t.actionIds) != null && m.length && !t.actionIds.includes(i)) return !1;
  const a = re(lr(e, "skill.key", "skillId"));
  if ((g = t.skillIds) != null && g.length && !t.skillIds.includes(a)) return !1;
  const r = re(lr(e, "module.selectedMode", "module.activation.selectedMode"));
  return !((p = t.modes) != null && p.length && !t.modes.includes(r) || t.detectionState && t.detectionState !== re(lr(e, "detectionState")) || t.targetState && t.targetState !== re(lr(e, "targetState")) || t.heatBand && t.heatBand !== re(lr(e, "heatBand")));
}
function MN(t, e, n) {
  switch (e) {
    case "truthy":
      return !!t;
    case "falsy":
      return !t;
    case "neq":
      return t !== n;
    case "gt":
      return Number(t) > Number(n);
    case "gte":
      return Number(t) >= Number(n);
    case "lt":
      return Number(t) < Number(n);
    case "lte":
      return Number(t) <= Number(n);
    case "includes":
      return Array.isArray(t) ? t.includes(n) : String(t ?? "").includes(String(n ?? ""));
    case "notIncludes":
      return Array.isArray(t) ? !t.includes(n) : !String(t ?? "").includes(String(n ?? ""));
    case "eq":
    default:
      return t === n;
  }
}
function yb(t, e) {
  if (!re(t == null ? void 0 : t.fact)) return !0;
  const n = Ak(e, t.fact);
  return MN(n, t.op ?? t.comparator, t.value);
}
function Sb(t = []) {
  return t.map((e) => e.fact).filter(Boolean).join(", ");
}
function Mk(t = {}) {
  var r, s, o, l, c;
  const e = re(t.combatId ?? ((r = t.combat) == null ? void 0 : r.id)), n = Math.max(0, Ka(t.round ?? ((s = t.combat) == null ? void 0 : s.round), 0)), i = re(t.sceneId ?? ((o = t.scene) == null ? void 0 : o.id) ?? ((c = (l = globalThis.canvas) == null ? void 0 : l.scene) == null ? void 0 : c.id));
  return {
    activationKey: re(t.activationId ?? t.activationKey),
    roundKey: e ? `${e}:${n}` : "",
    sceneKey: i
  };
}
function vN(t = null, e = {}) {
  var a, r;
  const n = ((a = t == null ? void 0 : t.flags) == null ? void 0 : a[I]) ?? {}, i = e.ruleUsage ?? {};
  return {
    activation: i.activation ?? {},
    round: i.round ?? {},
    scene: ((r = n.ruleUsage) == null ? void 0 : r.scene) ?? {}
  };
}
function CN(t, e, n, i) {
  var a, r, s, o, l;
  switch (n) {
    case "perActivation":
      return Math.max(0, Ka((a = t.activation) == null ? void 0 : a[i], 0));
    case "perRound":
      return Math.max(0, Ka((s = (r = t.round) == null ? void 0 : r[e.roundKey]) == null ? void 0 : s[i], 0));
    case "perScene":
      return Math.max(0, Ka((l = (o = t.scene) == null ? void 0 : o[e.sceneKey]) == null ? void 0 : l[i], 0));
    default:
      return 0;
  }
}
function vk(t = {}) {
  return `${t.sourceId}:${t.ruleId}`;
}
function IN(t, e, n = {}, i = "") {
  const a = [];
  for (const r of up) {
    if (!Object.prototype.hasOwnProperty.call(n, r)) continue;
    const s = n[r];
    if (s == null) continue;
    if (s === 0) {
      a.push(`${r} unavailable`);
      continue;
    }
    CN(t, e, r, i) >= s && a.push(`${r} limit reached`);
  }
  return a;
}
function EN(t = {}) {
  var i, a;
  const e = vk(t), n = [];
  for (const r of up) {
    const s = (a = (i = t.rule) == null ? void 0 : i.limits) == null ? void 0 : a[r];
    s == null || s <= 0 || n.push({
      kind: "ruleUsage",
      scope: r,
      key: e,
      delta: 1,
      sourceId: t.sourceId,
      ruleId: t.ruleId
    });
  }
  return n;
}
function NN(t = {}, e = {}) {
  return {
    ...t,
    sourceId: e.sourceId,
    sourceName: e.sourceName,
    sourceUuid: e.sourceUuid,
    ruleId: e.ruleId,
    ruleLabel: e.label
  };
}
function PN(t = {}) {
  return (t == null ? void 0 : t.item) ?? t;
}
function RN(t = {}) {
  var e;
  return Array.isArray(t == null ? void 0 : t.rules) ? t.rules.map((n, i) => mp(n, i)) : Qu((t == null ? void 0 : t.system) ?? ((e = t == null ? void 0 : t.item) == null ? void 0 : e.system) ?? t).rules;
}
function uf({
  actor: t = null,
  carrierItems: e = [],
  phase: n = "",
  facts: i = {},
  runtime: a = {},
  packet: r = {},
  mode: s = ""
} = {}) {
  const o = re(n), l = vN(t, a), c = Mk(a), u = re(s), d = {
    packet: Er(r),
    entries: [],
    outputs: [],
    disabled: [],
    summaries: [],
    pendingUsageMutations: [],
    mutations: [],
    applied: [],
    skipped: []
  };
  if (!o) return d;
  for (const f of Array.isArray(e) ? e : []) {
    const m = PN(f), g = RN(f);
    for (const p of g.filter((h) => h.phase === o)) {
      if (u && u !== "all" && p.mode !== u) continue;
      const h = re((m == null ? void 0 : m.id) ?? (f == null ? void 0 : f.id)), b = re((m == null ? void 0 : m.name) ?? (f == null ? void 0 : f.name), "Rule Source"), y = re((m == null ? void 0 : m.uuid) ?? (f == null ? void 0 : f.uuid)), S = {
        id: `${h || "source"}.${p.id}`,
        sourceId: h,
        sourceName: b,
        sourceUuid: y,
        ruleId: p.id,
        label: p.label || b,
        phase: p.phase,
        mode: p.mode,
        rule: p
      };
      if (!p.enabled) {
        const v = { ...S, reason: "Rule is disabled." };
        d.disabled.push(v), d.skipped.push(v);
        continue;
      }
      if (!wN(p.selector, i)) {
        const v = { ...S, reason: "Selector did not match." };
        d.disabled.push(v), d.skipped.push(v);
        continue;
      }
      const A = p.requires.filter((v) => re(v == null ? void 0 : v.fact)).filter((v) => !yb(v, i));
      if (A.length) {
        const v = { ...S, reason: `Prerequisites not met: ${Sb(A)}` };
        d.disabled.push(v), d.skipped.push(v);
        continue;
      }
      const T = p.conditions.filter((v) => re(v == null ? void 0 : v.fact)).filter((v) => !yb(v, i));
      if (T.length) {
        const v = { ...S, reason: `Conditions not met: ${Sb(T)}` };
        d.disabled.push(v), d.skipped.push(v);
        continue;
      }
      const C = IN(l, c, p.limits, vk(S));
      if (C.length) {
        const v = { ...S, reason: C.join(", ") };
        d.disabled.push(v), d.skipped.push(v);
        continue;
      }
      const k = p.outputs.map((v) => NN(v, S)), w = {
        ...S,
        outputs: k,
        summary: p.summary,
        usage: p.usage
      };
      w.usageMutations = EN(w), d.entries.push(w), d.outputs.push(...k), w.summary && d.summaries.push({ label: w.label, text: w.summary, source: w.sourceName }), d.pendingUsageMutations.push(...w.usageMutations), d.applied.push({
        label: w.label,
        value: k.map((v) => kk(v.value ?? v.ar ?? v.dr ?? v.key ?? v.text)).filter(Boolean).join(", "),
        source: w.sourceName,
        phase: w.phase
      });
    }
  }
  return d.mutations = d.pendingUsageMutations, d;
}
function DN({ entries: t = [] } = {}) {
  const e = [];
  for (const n of Array.isArray(t) ? t : [])
    Array.isArray(n == null ? void 0 : n.usageMutations) && e.push(...n.usageMutations);
  return { usageMutations: e };
}
async function ON({ actor: t = null, usageMutations: e = [], runtime: n = {} } = {}) {
  var o, l, c, u, d, f, m;
  if (!(t != null && t.setFlag) || !Array.isArray(e) || !e.length) return !1;
  const i = Er(((l = (o = t.flags) == null ? void 0 : o[I]) == null ? void 0 : l.ruleUsage) ?? {}), a = Er(n.ruleUsage ?? {}), r = Mk(n);
  let s = !1;
  for (const g of e) {
    if ((g == null ? void 0 : g.kind) !== "ruleUsage") continue;
    const p = re(g.key), h = Math.max(0, Ka(g.delta, 0));
    if (!(!p || !h))
      switch (s = !0, g.scope) {
        case "perActivation":
          a.activation ?? (a.activation = {}), a.activation[p] = Math.max(0, st(a.activation[p], 0) + h);
          break;
        case "perRound":
          if (!r.roundKey) break;
          a.round ?? (a.round = {}), (c = a.round)[u = r.roundKey] ?? (c[u] = {}), a.round[r.roundKey][p] = Math.max(0, st(a.round[r.roundKey][p], 0) + h);
          break;
        case "perScene":
          if (!r.sceneKey) break;
          i.scene ?? (i.scene = {}), (d = i.scene)[f = r.sceneKey] ?? (d[f] = {}), i.scene[r.sceneKey][p] = Math.max(0, st(i.scene[r.sceneKey][p], 0) + h);
          break;
      }
  }
  if (!s) return !1;
  if (n.state && ((m = n.combatant) != null && m.setFlag)) {
    const g = Er(n.state);
    g.ruleUsage = a, await n.combatant.setFlag(I, "personalCombat", g);
  }
  return await t.setFlag(I, "ruleUsage", i), !0;
}
function LN(t = []) {
  return (Array.isArray(t) ? t : []).map((e) => ({
    label: (e == null ? void 0 : e.label) ?? "Rule",
    value: kk((e == null ? void 0 : e.value) ?? (e == null ? void 0 : e.summary) ?? ""),
    source: (e == null ? void 0 : e.source) ?? (e == null ? void 0 : e.sourceName) ?? ""
  }));
}
const fp = Object.freeze([
  { value: "positive", label: "Positive" },
  { value: "negative", label: "Negative" },
  { value: "narrative", label: "Narrative" }
]), gp = Object.freeze([
  { value: "minor", label: "Minor" },
  { value: "major", label: "Major" }
]), Ck = Object.freeze([
  { value: "passive", label: "Passive" },
  { value: "triggered", label: "Triggered" }
]), Ik = Object.freeze([
  { value: "rollMod", label: "Roll Modifier" },
  { value: "burnAdjust", label: "Burn Adjust" },
  { value: "actionCostMod", label: "Action Cost Mod" },
  { value: "initiativeMod", label: "Initiative Mod" },
  { value: "damageMod", label: "Damage Intake Mod" },
  { value: "speedMod", label: "Speed Modifier" },
  { value: "attackRatingMod", label: "Attack Rating Mod" },
  { value: "suppressAttackerMotionDN", label: "Suppress Attacker Motion DN" },
  { value: "defenseRatingMod", label: "Defense Rating Mod" },
  { value: "saCapMod", label: "SA Cap Modifier" },
  { value: "faCapMod", label: "FA Cap Modifier" },
  { value: "raCapMod", label: "RA Cap Modifier" },
  { value: "conditionPenaltyMod", label: "Condition Penalty Mod" },
  { value: "overloadDNMod", label: "Overload DN Mod" },
  { value: "overloadThresholdMod", label: "Overload Threshold Mod" },
  { value: "edgeEvent", label: "Edge Event" }
]), Ek = Object.freeze([
  { value: "onBuildRoll", label: "Build Roll" },
  { value: "onBeforeBurnApplied", label: "Before Burn Applied" },
  { value: "onBeforeActionCostFinalized", label: "Before Action Cost Finalized" },
  { value: "onInitiativeResolved", label: "Initiative Resolved" },
  { value: "onDamageResolved", label: "Damage Resolved" },
  { value: "onDerivedPersonalCombat", label: "Derived Personal Combat" },
  { value: "onAttackRatingResolved", label: "Attack Rating Resolved" },
  { value: "onDefenseRatingResolved", label: "Defense Rating Resolved" },
  { value: "onActivationBudgetResolved", label: "Activation Budget Resolved" },
  { value: "onConditionPenaltyResolved", label: "Condition Penalty Resolved" },
  { value: "onEndOfActivation", label: "End of Activation" },
  { value: "onEdgeSpend", label: "Edge Spend" },
  { value: "onEdgeGain", label: "Edge Gain" }
]), Nk = Object.freeze([
  { value: "automatic", label: "Automatic" },
  { value: "optional", label: "Prompted Optional" }
]), Pk = Object.freeze([
  { value: "eq", label: "=" },
  { value: "neq", label: "!=" },
  { value: "gt", label: ">" },
  { value: "gte", label: ">=" },
  { value: "lt", label: "<" },
  { value: "lte", label: "<=" },
  { value: "includes", label: "Includes" },
  { value: "notIncludes", label: "Excludes" },
  { value: "truthy", label: "Is True" },
  { value: "falsy", label: "Is False" }
]), Rk = Object.freeze([
  { value: "adjustAmount", label: "Adjust Amount" },
  { value: "grantPool", label: "Grant Pool" }
]), $N = new Set(fp.map((t) => t.value)), _N = new Set(gp.map((t) => t.value)), BN = new Set(Ck.map((t) => t.value)), xN = new Set(Ik.map((t) => t.value)), Dk = new Set(Ek.map((t) => t.value));
new Set(Pk.map((t) => t.value));
const zN = new Set(Rk.map((t) => t.value)), FN = new Set(Nk.map((t) => t.value)), UN = Object.freeze({
  speedMod: "system.traitMods.speedMod",
  attackRatingMod: "system.traitMods.attackRatingMod",
  suppressAttackerMotionDN: "system.traitMods.suppressAttackerMotionDN",
  defenseRatingMod: "system.traitMods.defenseRatingMod",
  saCapMod: "system.traitMods.saCapMod",
  faCapMod: "system.traitMods.faCapMod",
  raCapMod: "system.traitMods.raCapMod",
  conditionPhysicalValueMod: "system.traitMods.conditionPhysicalValueMod",
  conditionFatigueValueMod: "system.traitMods.conditionFatigueValueMod",
  conditionPhysicalPenaltyMod: "system.traitMods.conditionPhysicalPenaltyMod",
  conditionFatiguePenaltyMod: "system.traitMods.conditionFatiguePenaltyMod",
  overloadDNMod: "system.traitMods.overloadDNMod",
  overloadThresholdMod: "system.traitMods.overloadThresholdMod"
});
function ve(t, e = "") {
  return String(t ?? "").trim() || e;
}
function Ce(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function KN(t, e = "") {
  var i;
  const n = ve(e);
  if (!(!t || !n))
    return typeof ((i = foundry == null ? void 0 : foundry.utils) == null ? void 0 : i.getProperty) == "function" ? foundry.utils.getProperty(t, n) : n.split(".").reduce((a, r) => a == null ? void 0 : a[r], t);
}
function oi(t, e = "") {
  const n = UN[e] ?? "";
  if (!t || !n) return 0;
  const i = n.startsWith("system.") ? n.slice(7) : n;
  return Ce(KN(t.system ?? {}, i), 0);
}
function su(t) {
  return foundry.utils.deepClone(t);
}
function Ok(t = []) {
  return (Array.isArray(t) ? t : typeof t == "string" ? t.split(",") : []).map((n) => String(n ?? "").trim()).filter(Boolean);
}
function Qd(t) {
  const e = Math.max(0, Math.trunc(Ce(t, 0)));
  return e > 0 ? e : 0;
}
function za(t = {}) {
  const e = Tk(t);
  return {
    perActivation: Qd(e.perActivation),
    perRound: Qd(e.perRound),
    perScene: Qd(e.perScene)
  };
}
function ia(t = []) {
  return cf(t).map((e) => ({
    ...e,
    comparator: e.op
  }));
}
function HN(t = {}) {
  const e = t && typeof t == "object" ? t : {}, n = xN.has(String(e.type ?? "").trim()) ? String(e.type).trim() : "rollMod", i = jN(n), a = Dk.has(String(e.phase ?? "").trim()) ? String(e.phase).trim() : i, r = zN.has(String(e.operation ?? "").trim()) ? String(e.operation).trim() : "adjustAmount";
  return {
    id: ve(e.id, foundry.utils.randomID()),
    type: n,
    phase: a,
    selector: ve(e.selector),
    skillKeys: Ok(e.skillKeys),
    label: ve(e.label),
    value: Ce(e.value, 0),
    min: e.min === void 0 || e.min === null || e.min === "" ? null : Ce(e.min, 0),
    max: e.max === void 0 || e.max === null || e.max === "" ? null : Ce(e.max, 0),
    pool: ve(e.pool),
    operation: r,
    application: FN.has(String(e.application ?? "").trim()) ? String(e.application).trim() : "automatic",
    defaultEnabled: e.defaultEnabled === !0,
    conditions: ia(e.conditions),
    limit: za(e.limit)
  };
}
function Lk(t = {}) {
  const e = ve(t == null ? void 0 : t.selector);
  return e === "intent.skill" || e.startsWith("intent.skill.");
}
function mr(t = []) {
  return (Array.isArray(t) ? t : []).map(HN).filter((n) => n.phase && n.type);
}
function Yn(t = {}) {
  const e = t && typeof t == "object" ? su(t) : {}, n = e.positive === !1 ? "negative" : "positive", i = $N.has(String(e.category ?? "").trim()) ? String(e.category).trim() : n, a = _N.has(String(e.tier ?? "").trim()) ? String(e.tier).trim() : "minor", r = BN.has(String(e.activation ?? "").trim()) ? String(e.activation).trim() : "passive";
  return {
    ...e,
    positive: i === "positive",
    category: i,
    tier: a,
    activation: r,
    tags: Ok(e.tags),
    effects: mr(e.effects),
    prerequisites: ia(e.prerequisites),
    limits: za(e.limits)
  };
}
function $k() {
  return {
    categories: [...fp],
    tiers: [...gp],
    activations: [...Ck],
    effectTypes: [...Ik],
    phases: [...Ek],
    comparators: [...Pk],
    edgeOperations: [...Rk],
    applications: [...Nk]
  };
}
function vc(t = "") {
  var e;
  return ((e = fp.find((n) => n.value === t)) == null ? void 0 : e.label) ?? "Positive";
}
function Cc(t = "") {
  var e;
  return ((e = gp.find((n) => n.value === t)) == null ? void 0 : e.label) ?? "Minor";
}
function jN(t = "") {
  switch (t) {
    case "burnAdjust":
      return "onBeforeBurnApplied";
    case "actionCostMod":
      return "onBeforeActionCostFinalized";
    case "initiativeMod":
      return "onInitiativeResolved";
    case "damageMod":
      return "onDamageResolved";
    case "speedMod":
      return "onDerivedPersonalCombat";
    case "attackRatingMod":
      return "onAttackRatingResolved";
    case "defenseRatingMod":
      return "onDefenseRatingResolved";
    case "saCapMod":
      return "onActivationBudgetResolved";
    case "faCapMod":
      return "onActivationBudgetResolved";
    case "raCapMod":
      return "onActivationBudgetResolved";
    case "conditionPenaltyMod":
      return "onConditionPenaltyResolved";
    case "edgeEvent":
      return "onEdgeGain";
    default:
      return "onBuildRoll";
  }
}
function GN(t) {
  return Array.from((t == null ? void 0 : t.items) ?? []).filter((e) => ((e == null ? void 0 : e.canonicalType) ?? (e == null ? void 0 : e.type)) === "quality").map((e) => ({
    item: e,
    system: Yn(e.system ?? {})
  }));
}
function qN(t = {}, e = {}) {
  const n = za(t), i = za(e);
  return {
    perActivation: i.perActivation || n.perActivation,
    perRound: i.perRound || n.perRound,
    perScene: i.perScene || n.perScene
  };
}
function _k(t = {}) {
  var a, r, s;
  const e = ve(t.combatId ?? ((a = t.combat) == null ? void 0 : a.id)), n = Math.max(0, Math.trunc(Ce(t.round ?? ((r = t.combat) == null ? void 0 : r.round), 0))), i = ve(t.sceneId ?? ((s = canvas == null ? void 0 : canvas.scene) == null ? void 0 : s.id));
  return {
    activationKey: ve(t.activationKey),
    roundKey: e ? `${e}:${n}` : "",
    sceneKey: i
  };
}
function WN(t, e = {}) {
  var r, s, o, l;
  const n = ((r = t == null ? void 0 : t.flags) == null ? void 0 : r[I]) ?? {}, i = ((s = n == null ? void 0 : n.traitUsage) == null ? void 0 : s.scene) ?? {}, a = e.state ?? {};
  return {
    activation: ((o = a == null ? void 0 : a.traitUsage) == null ? void 0 : o.activation) ?? {},
    round: ((l = a == null ? void 0 : a.traitUsage) == null ? void 0 : l.round) ?? {},
    scene: i
  };
}
function VN(t, e, n, i) {
  var a, r, s, o, l;
  switch (n) {
    case "perActivation":
      return Math.max(0, Math.trunc(Ce((a = t.activation) == null ? void 0 : a[i], 0)));
    case "perRound":
      return Math.max(0, Math.trunc(Ce((s = (r = t.round) == null ? void 0 : r[e.roundKey]) == null ? void 0 : s[i], 0)));
    case "perScene":
      return Math.max(0, Math.trunc(Ce((l = (o = t.scene) == null ? void 0 : o[e.sceneKey]) == null ? void 0 : l[i], 0)));
    default:
      return 0;
  }
}
function JN(t, e, n, i) {
  const a = [];
  for (const r of ["perActivation", "perRound", "perScene"]) {
    const s = Math.max(0, Math.trunc(Ce(n == null ? void 0 : n[r], 0)));
    if (!s) continue;
    VN(t, e, r, i) >= s && a.push(`${r} limit reached`);
  }
  return a;
}
function YN(t, e, n) {
  switch (e) {
    case "truthy":
      return !!t;
    case "falsy":
      return !t;
    case "neq":
      return t !== n;
    case "gt":
      return Number(t) > Number(n);
    case "gte":
      return Number(t) >= Number(n);
    case "lt":
      return Number(t) < Number(n);
    case "lte":
      return Number(t) <= Number(n);
    case "includes":
      return Array.isArray(t) ? t.includes(n) : String(t ?? "").includes(String(n ?? ""));
    case "notIncludes":
      return Array.isArray(t) ? !t.includes(n) : !String(t ?? "").includes(String(n ?? ""));
    case "eq":
    default:
      return t === n;
  }
}
function Ab(t, e) {
  if (!ve(t == null ? void 0 : t.fact)) return !0;
  const n = foundry.utils.getProperty(e, t.fact);
  return YN(n, t.comparator, t.value);
}
function QN(t = "", e = {}) {
  const n = ve(t);
  return n ? (Array.isArray(e == null ? void 0 : e.selectors) ? e.selectors : []).some(
    (a) => a === n || a.startsWith(`${n}.`)
  ) : !0;
}
function Bk(t, e) {
  return `${t.id}:${e.id}`;
}
function XN(t, e) {
  var n;
  return !!((n = t.system) != null && n.inactive) || !!(e != null && e.inactive);
}
function kb(t = []) {
  return t.map((e) => e.fact).filter(Boolean).join(", ");
}
function jn(t, e, n) {
  const i = Ce(t[e], 0);
  let a = i;
  return typeof n.value == "number" && (a += n.value), typeof n.min == "number" && (a = Math.max(n.min, a)), typeof n.max == "number" && (a = Math.min(n.max, a)), t[e] = a, a - i;
}
function hn(t, e, n, i, a) {
  i && t.push({
    id: `trait:${a}:${e.id}:${n.id}`,
    label: n.label || e.name,
    value: i,
    source: e.name,
    traitItemId: e.id,
    traitEffectId: n.id,
    application: n.application,
    defaultEnabled: n.defaultEnabled === !0
  });
}
function ZN({ item: t, effect: e, phase: n, packet: i, result: a }) {
  switch (e.type) {
    case "rollMod": {
      const r = Ce(e.value, 0);
      return hn(a.modifiers, t, e, r, n), r;
    }
    case "burnAdjust": {
      if (n === "onEndOfActivation") {
        const s = jn(i, "burnDelta", e);
        return hn(a.modifiers, t, e, s, n), s;
      }
      const r = jn(i, "amount", e);
      return hn(a.modifiers, t, e, r, n), r;
    }
    case "actionCostMod": {
      const r = jn(i, "cost", e);
      return hn(a.modifiers, t, e, r, n), r;
    }
    case "initiativeMod": {
      const r = jn(i, "total", e);
      return hn(a.modifiers, t, e, r, n), r;
    }
    case "damageMod": {
      const r = jn(i, "amount", e);
      return hn(a.modifiers, t, e, r, n), r;
    }
    case "speedMod": {
      const r = jn(i, "modifier", e);
      return hn(a.modifiers, t, e, r, n), r;
    }
    case "attackRatingMod": {
      const r = jn(i, "total", e);
      return hn(a.modifiers, t, e, r, n), r;
    }
    case "defenseRatingMod": {
      const r = jn(i, "total", e);
      return hn(a.modifiers, t, e, r, n), r;
    }
    case "saCapMod": {
      if (ve(i.resource) !== "sa") return 0;
      const r = jn(i, "max", e);
      return hn(a.modifiers, t, e, r, n), r;
    }
    case "faCapMod": {
      if (ve(i.resource) !== "fa") return 0;
      const r = jn(i, "max", e);
      return hn(a.modifiers, t, e, r, n), r;
    }
    case "raCapMod": {
      if (ve(i.resource) !== "ra") return 0;
      const r = jn(i, "max", e);
      return hn(a.modifiers, t, e, r, n), r;
    }
    case "conditionPenaltyMod": {
      const r = ve(e.selector).startsWith("condition.fatigue") ? Object.prototype.hasOwnProperty.call(i, "fatigueValue") ? "fatigueValue" : "fatiguePenalty" : ve(e.selector).startsWith("condition.physical") ? Object.prototype.hasOwnProperty.call(i, "physicalValue") ? "physicalValue" : "physicalPenalty" : "totalPenalty", o = jn(i, r === "totalPenalty" ? "totalPenalty" : r, e);
      return hn(a.modifiers, t, e, o, n), o;
    }
    case "edgeEvent": {
      if (n === "onEndOfActivation" && e.operation === "grantPool" && e.pool)
        return i.edgeAdjustments ?? (i.edgeAdjustments = []), i.edgeAdjustments.push({
          poolKey: e.pool,
          amount: Ce(e.value, 0),
          label: e.label || t.name,
          source: t.name
        }), hn(a.modifiers, t, e, Ce(e.value, 0), n), Ce(e.value, 0);
      const r = jn(i, "amount", e);
      return hn(a.modifiers, t, e, r, n), r;
    }
    default:
      return 0;
  }
}
function eP(t, e, n) {
  const i = Bk(t, e), a = [];
  return n.perActivation > 0 && a.push({ kind: "usage", scope: "perActivation", key: i, delta: 1 }), n.perRound > 0 && a.push({ kind: "usage", scope: "perRound", key: i, delta: 1 }), n.perScene > 0 && a.push({ kind: "usage", scope: "perScene", key: i, delta: 1 }), a;
}
function xk(t = "") {
  const e = ve(t);
  return e ? [`action.${e}`] : [];
}
function fi(t, e = {}) {
  var o, l, c, u, d;
  const n = e.snapshot ?? null, i = e.state ?? (n == null ? void 0 : n.state) ?? {}, a = Array.isArray(i == null ? void 0 : i.actionLog) ? i.actionLog.map((f) => ve(f == null ? void 0 : f.id)).filter(Boolean) : [], r = (i == null ? void 0 : i.actionState) ?? {}, s = [];
  return r != null && r.aim && s.push("state.aim"), r != null && r.preparedInterrupt && s.push("state.preparedInterrupt"), {
    activation: {
      moved: a.includes("move") || !!((o = r == null ? void 0 : r.move) != null && o.moved),
      saSpent: Math.max(0, Math.trunc(Ce(i == null ? void 0 : i.saSpentThisActivation, 0))),
      attacksThisActivation: Math.max(0, Math.trunc(Ce(i == null ? void 0 : i.attacksThisActivation, 0))),
      burnThisActivation: Math.max(0, Math.trunc(Ce(i == null ? void 0 : i.burnThisActivation, 0))),
      actions: a
    },
    actionState: {
      aim: (r == null ? void 0 : r.aim) ?? null,
      move: (r == null ? void 0 : r.move) ?? null,
      preparedInterrupt: (r == null ? void 0 : r.preparedInterrupt) ?? null
    },
    burn: {
      current: Math.max(0, Math.trunc(Ce((c = (l = t == null ? void 0 : t.system) == null ? void 0 : l.burn) == null ? void 0 : c.value, 0))),
      overloaded: !!((d = (u = t == null ? void 0 : t.system) == null ? void 0 : u.burn) != null && d.overloaded)
    },
    timing: {
      firstAttackThisActivation: Math.max(0, Math.trunc(Ce(i == null ? void 0 : i.attacksThisActivation, 0))) === 0,
      firstExtraSAThisActivation: Math.max(0, Math.trunc(Ce(i == null ? void 0 : i.saSpentThisActivation, 0))) <= 3
    },
    selectors: s
  };
}
function Xu({ actor: t, resolved: e, payload: n, runtime: i = {} } = {}) {
  var m, g, p, h, b, y, S, A, T, C, k, w, v;
  const a = fi(t, i), r = ve((e == null ? void 0 : e.intent) ?? (n == null ? void 0 : n.intent), "skill"), s = Array.isArray(e == null ? void 0 : e.domains) ? e.domains : [], o = ve(((m = e == null ? void 0 : e.attack) == null ? void 0 : m.rangeBand) ?? (n == null ? void 0 : n.rangeBand)), l = ve(((p = (g = n == null ? void 0 : n.edge) == null ? void 0 : g.pre) == null ? void 0 : p.poolKey) ?? ((h = n == null ? void 0 : n.edge) == null ? void 0 : h.poolKey) ?? ""), c = ve(((b = e == null ? void 0 : e.data) == null ? void 0 : b.commonCheckId) ?? (n == null ? void 0 : n.id)), u = [
    ...Array.isArray(e == null ? void 0 : e.tags) ? e.tags : [],
    ...Array.isArray(e == null ? void 0 : e.domainTags) ? e.domainTags : []
  ].map((N) => ve(N)).filter(Boolean), d = ve(
    ((y = e == null ? void 0 : e.data) == null ? void 0 : y.skillKey) ?? ((S = e == null ? void 0 : e.specialization) == null ? void 0 : S.skillKey) ?? (r === "skill" ? n == null ? void 0 : n.key : "")
  ), f = ve(
    ((C = (T = (A = e == null ? void 0 : e.breakdown) == null ? void 0 : A.find) == null ? void 0 : T.call(A, (N) => (N == null ? void 0 : N.id) === "skill")) == null ? void 0 : C.label) ?? (e == null ? void 0 : e.title)
  );
  return a.intent = r, a.domains = s, a.rangeBand = o, a.skill = {
    key: d,
    label: f
  }, a.common = {
    id: r === "common" ? c : ""
  }, a.tags = u, a.edge = {
    stage: (k = n == null ? void 0 : n.toggles) != null && k.useEdge ? "pre" : "",
    pool: l,
    spent: !!((w = n == null ? void 0 : n.toggles) != null && w.useEdge)
  }, a.selectors.push(`intent.${r}`), s.forEach((N) => a.selectors.push(`domain.${N}`)), o && a.selectors.push(`range.${o}`), r === "skill" && d && a.selectors.push(`skill.${d}`), r === "common" && c && a.selectors.push(`common.${c}`), u.forEach((N) => a.selectors.push(`tag.${N}`)), (v = n == null ? void 0 : n.toggles) != null && v.useEdge && a.selectors.push("edge.pre"), a;
}
function df({ actor: t, packet: e = {}, runtime: n = {} } = {}) {
  const i = fi(t, n);
  return i.action = {
    id: ve(e.actionId),
    category: ve(e.category),
    resource: ve(e.resource),
    cost: Ce(e.cost, 0),
    effectiveCost: Ce(e.effectiveCost ?? e.cost, 0)
  }, i.selectors.push(...xk(e.actionId)), i.action.category && i.selectors.push(`actionCategory.${i.action.category}`), i.action.resource && i.selectors.push(`actionResource.${i.action.resource}`), i;
}
function Ic({ actor: t, packet: e = {}, runtime: n = {} } = {}) {
  const i = fi(t, n);
  return i.action = {
    id: ve(e.actionId),
    category: ve(e.category),
    resource: ve(e.resource)
  }, i.burn = {
    ...i.burn,
    amount: Ce(e.amount, 0),
    source: ve(e.source)
  }, e.source === "extraSA" && e.extraSaIndex === 1 && i.selectors.push("activation.extraSA:first"), e.source && i.selectors.push(`burn.${e.source}`), i.action.id && i.selectors.push(...xk(i.action.id)), i.action.category && i.selectors.push(`actionCategory.${i.action.category}`), i.action.category === "reaction" && i.selectors.push(`reaction.${i.action.id}`), i;
}
function zk({ actor: t, packet: e = {}, runtime: n = {} } = {}) {
  const i = fi(t, n);
  return i.intent = "initiative", i.domains = ["combat"], i.initiative = {
    total: Ce(e.total, 0)
  }, i.selectors.push("intent.initiative"), i;
}
function Fk({ actor: t, packet: e = {}, runtime: n = {} } = {}) {
  const i = fi(t, n);
  return i.damage = {
    amount: Ce(e.amount, 0),
    track: ve(e.track),
    damageType: ve(e.damageType)
  }, i.selectors.push("incoming"), i.damage.track && i.selectors.push(`incoming.${i.damage.track}`), i.damage.damageType && i.selectors.push(`damageType.${i.damage.damageType}`), i;
}
function Uk({ actor: t, packet: e = {}, runtime: n = {} } = {}) {
  const i = fi(t, n);
  return i.personalCombat = {
    speed: {
      base: Ce(e.base, 0),
      modifier: Ce(e.modifier, 0)
    }
  }, i.selectors.push("derived.personalCombat", "derived.personalCombat.speed"), i;
}
function tP({ actor: t, packet: e = {}, runtime: n = {} } = {}) {
  const i = fi(t, n);
  return i.attackRating = {
    total: Ce(e.total, 0)
  }, i.intent = "attack", i.domains = ["combat", "attack"], i.selectors.push("attackRating", "intent.attack", "domain.attack"), i;
}
function Kk({ actor: t, packet: e = {}, runtime: n = {} } = {}) {
  const i = fi(t, n);
  return i.defenseRating = {
    total: Ce(e.total, 0)
  }, i.intent = "attack", i.domains = ["combat", "attack", "defense"], i.selectors.push("defenseRating", "intent.attack", "domain.defense"), i;
}
function Hk({ actor: t, packet: e = {}, runtime: n = {} } = {}) {
  const i = fi(t, n);
  return i.activationBudget = {
    max: Ce(e.max, 0),
    resource: ve(e.resource)
  }, i.selectors.push("activationBudget"), i.activationBudget.resource && i.selectors.push(`activationBudget.${i.activationBudget.resource}`), i;
}
function jk({ actor: t, packet: e = {}, runtime: n = {} } = {}) {
  const i = fi(t, n);
  return i.condition = {
    physicalValue: Ce(e.physicalValue, 0),
    fatigueValue: Ce(e.fatigueValue, 0),
    physicalPenalty: Ce(e.physicalPenalty, 0),
    fatiguePenalty: Ce(e.fatiguePenalty, 0),
    totalPenalty: Ce(e.totalPenalty, 0)
  }, i.selectors.push("condition"), (i.condition.physicalPenalty !== 0 || i.condition.physicalValue !== 0) && i.selectors.push("condition.physical"), (i.condition.fatiguePenalty !== 0 || i.condition.fatigueValue !== 0) && i.selectors.push("condition.fatigue"), i.condition.totalPenalty !== 0 && i.selectors.push("condition.total"), i;
}
function nP(t, e) {
  const n = String(e ?? "automatic").trim() || "automatic";
  return n === "all" ? !0 : String((t == null ? void 0 : t.application) ?? "automatic").trim() === n;
}
function mf({ actor: t, packet: e = {}, phase: n = "onEdgeGain", runtime: i = {} } = {}) {
  const a = fi(t, i);
  return a.edge = {
    pool: ve(e.poolKey),
    amount: Ce(e.amount, 0),
    eventKey: ve(e.eventKey),
    source: ve(e.source)
  }, a.selectors.push(n === "onEdgeSpend" ? "edge.spend" : "edge.gain"), a.edge.eventKey && a.selectors.push(`event.${a.edge.eventKey}`), a;
}
function Gk({ actor: t, packet: e = {}, runtime: n = {} } = {}) {
  const i = fi(t, n);
  return i.event = {
    phase: "endOfActivation"
  }, i.selectors.push("endOfActivation"), Ce(e.burnDelta, 0) !== 0 && i.selectors.push("burn.adjust"), i;
}
function zt({ actor: t, phase: e, facts: n = {}, packet: i = {}, options: a = {} } = {}) {
  var u;
  const r = {
    packet: su(i),
    modifiers: [],
    mutations: [],
    applied: [],
    skipped: []
  };
  if (!t || !Dk.has(String(e ?? "").trim()))
    return r;
  const s = a.runtime ?? {}, o = WN(t, s), l = _k(s), c = GN(t);
  for (const { item: d, system: f } of c) {
    if (XN(d, f)) {
      r.skipped.push({
        traitItemId: d.id,
        traitEffectId: "",
        label: d.name,
        reason: "Trait is inactive"
      });
      continue;
    }
    const m = f.prerequisites.filter((g) => ve(g == null ? void 0 : g.fact)).filter((g) => !Ab(g, n));
    if (m.length) {
      r.skipped.push({
        traitItemId: d.id,
        traitEffectId: "",
        label: d.name,
        reason: `Prerequisites not met: ${kb(m)}`
      });
      continue;
    }
    for (const g of f.effects.filter((p) => p.phase === e)) {
      if (!nP(g, a.application))
        continue;
      if (!QN(g.selector, n)) {
        r.skipped.push({
          traitItemId: d.id,
          traitEffectId: g.id,
          label: g.label || d.name,
          reason: `Selector did not match (${g.selector || "any"})`
        });
        continue;
      }
      if (Lk(g) && g.skillKeys.length) {
        const A = ve((u = n == null ? void 0 : n.skill) == null ? void 0 : u.key);
        if (!A || !g.skillKeys.includes(A)) {
          r.skipped.push({
            traitItemId: d.id,
            traitEffectId: g.id,
            label: g.label || d.name,
            reason: `Skill did not match (${g.skillKeys.join(", ")})`
          });
          continue;
        }
      }
      const p = g.conditions.filter((A) => ve(A == null ? void 0 : A.fact)).filter((A) => !Ab(A, n));
      if (p.length) {
        r.skipped.push({
          traitItemId: d.id,
          traitEffectId: g.id,
          label: g.label || d.name,
          reason: `Conditions not met: ${kb(p)}`
        });
        continue;
      }
      const h = qN(f.limits, g.limit), b = Bk(d, g), y = JN(o, l, h, b);
      if (y.length) {
        r.skipped.push({
          traitItemId: d.id,
          traitEffectId: g.id,
          label: g.label || d.name,
          reason: y.join(", ")
        });
        continue;
      }
      const S = ZN({
        item: d,
        effect: g,
        phase: e,
        packet: r.packet,
        result: r
      });
      r.applied.push({
        traitItemId: d.id,
        traitEffectId: g.id,
        label: g.label || d.name,
        value: S,
        phase: e,
        source: d.name
      }), a.consumeUsage && r.mutations.push(...eP(d, g, h));
    }
  }
  return r;
}
function qk({ actor: t, rollActor: e = null, resolved: n = {}, payload: i = {} } = {}) {
  return iP(e, t).flatMap((a) => {
    var o, l, c;
    const r = {
      snapshot: ((c = (l = (o = game.mwd) == null ? void 0 : o.personalCombat) == null ? void 0 : l.getSnapshot) == null ? void 0 : c.call(l, a)) ?? null
    };
    return zt({
      actor: a,
      phase: "onBuildRoll",
      facts: Xu({ actor: a, resolved: n, payload: i, runtime: r }),
      packet: {},
      options: { runtime: r, consumeUsage: !1, application: "optional" }
    }).modifiers.map((u) => ({
      id: u.id,
      label: u.label,
      value: Number(u.value ?? 0),
      enabled: u.defaultEnabled === !0,
      source: "Trait",
      traitItemId: u.traitItemId,
      traitEffectId: u.traitEffectId,
      optional: !0
    }));
  });
}
function iP(...t) {
  const e = /* @__PURE__ */ new Set();
  return t.filter((n) => {
    if (!n) return !1;
    const i = n.uuid ?? n.id ?? n;
    return e.has(i) ? !1 : (e.add(i), !0);
  });
}
async function ga({ actor: t, mutations: e = [], runtime: n = {} } = {}) {
  var o, l, c, u, d, f, m, g, p;
  if (!t || !Array.isArray(e) || !e.length) return;
  const i = e.filter((h) => (h == null ? void 0 : h.kind) === "usage");
  if (!i.length) return;
  const a = su(((c = (l = (o = t.flags) == null ? void 0 : o[I]) == null ? void 0 : l.traitUsage) == null ? void 0 : c.scene) ?? {}), r = n.state ? su(n.state) : null, s = _k(n);
  for (const h of i) {
    const b = ve(h.key), y = Math.max(0, Math.trunc(Ce(h.delta, 0)));
    if (!(!b || !y))
      switch (h.scope) {
        case "perActivation": {
          if (!r) break;
          r.traitUsage ?? (r.traitUsage = {}), (u = r.traitUsage).activation ?? (u.activation = {}), r.traitUsage.activation[b] = Math.max(0, Ce(r.traitUsage.activation[b], 0) + y);
          break;
        }
        case "perRound": {
          if (!r || !s.roundKey) break;
          r.traitUsage ?? (r.traitUsage = {}), (d = r.traitUsage).round ?? (d.round = {}), (f = r.traitUsage.round)[m = s.roundKey] ?? (f[m] = {}), r.traitUsage.round[s.roundKey][b] = Math.max(
            0,
            Ce(r.traitUsage.round[s.roundKey][b], 0) + y
          );
          break;
        }
        case "perScene": {
          if (!s.sceneKey) break;
          a[g = s.sceneKey] ?? (a[g] = {}), a[s.sceneKey][b] = Math.max(0, Ce(a[s.sceneKey][b], 0) + y);
          break;
        }
      }
  }
  r && ((p = n.combatant) != null && p.id) && await n.combatant.setFlag(I, "personalCombat", r), await t.setFlag(I, "traitUsage", { scene: a });
}
const ou = "personalActionCatalog", He = Object.freeze({
  standard: "standard",
  complex: "complex",
  free: "free",
  reaction: "reaction",
  recovery: "recovery"
}), ff = Object.freeze([
  { value: He.standard, label: "Standard" },
  { value: He.complex, label: "Complex" },
  { value: He.free, label: "Free" },
  { value: He.reaction, label: "Reaction" },
  { value: He.recovery, label: "Burn & Recovery" }
]), gt = Object.freeze({
  action: "action",
  attack: "attack",
  movement: "movement",
  targeting: "targeting",
  remediation: "remediation",
  recovery: "recovery",
  interaction: "interaction"
}), Wk = Object.freeze([
  { value: gt.action, label: "Action" },
  { value: gt.attack, label: "Attack" },
  { value: gt.movement, label: "Movement" },
  { value: gt.targeting, label: "Targeting" },
  { value: gt.remediation, label: "Remediation" },
  { value: gt.recovery, label: "Recovery" },
  { value: gt.interaction, label: "Interaction" }
]), Bi = Object.freeze({
  fa: "fa",
  sa: "sa",
  ra: "ra",
  none: "none"
}), Vk = Object.freeze([
  { value: Bi.fa, label: "FA" },
  { value: Bi.sa, label: "SA" },
  { value: Bi.ra, label: "RA" },
  { value: Bi.none, label: "None" }
]), ta = Object.freeze({
  none: "none",
  target: "target",
  item: "item",
  weapon: "weapon",
  payload: "payload",
  skill: "skill",
  status: "status",
  confirm: "confirm"
}), Jk = Object.freeze([
  { value: ta.none, label: "None" },
  { value: ta.target, label: "Target" },
  { value: ta.item, label: "Item" },
  { value: ta.weapon, label: "Weapon" },
  { value: ta.payload, label: "Payload" },
  { value: ta.skill, label: "Skill" },
  { value: ta.status, label: "Status" },
  { value: ta.confirm, label: "Confirm" }
]), da = Object.freeze({
  ready: "ready",
  stub: "stub",
  disabled: "disabled",
  legacy: "legacy"
}), Yk = Object.freeze([
  { value: da.ready, label: "Ready" },
  { value: da.stub, label: "Stub" },
  { value: da.disabled, label: "Disabled" },
  { value: da.legacy, label: "Legacy" }
]), Tb = new Set(ff.map((t) => t.value)), wb = new Set(Wk.map((t) => t.value)), Mb = new Set(Vk.map((t) => t.value)), vb = new Set(Jk.map((t) => t.value)), Cb = new Set(Yk.map((t) => t.value)), aP = /* @__PURE__ */ new Set(["recoverBurn", "gesture"]), rP = Object.freeze({
  combatAction: gt.action,
  combatAttack: gt.attack,
  combatEvade: gt.recovery,
  combatAssist: gt.action,
  combatInterrupt: gt.action,
  combatFirstAid: gt.recovery,
  combatReduceBurn: gt.recovery,
  combatOverloadCheck: gt.recovery
});
function Xt(t) {
  return JSON.parse(JSON.stringify(t ?? null));
}
function gf(t, e = !1) {
  if (typeof t == "boolean") return t;
  const n = String(t ?? "").trim().toLowerCase();
  return ["true", "1", "yes", "y", "on"].includes(n) ? !0 : ["false", "0", "no", "n", "off", ""].includes(n) ? !1 : e;
}
function sP(t) {
  return Array.isArray(t) ? t.map((e) => String(e ?? "").trim()).filter(Boolean) : String(t ?? "").split(",").map((e) => e.trim()).filter(Boolean);
}
function Xd(t) {
  return Array.from(new Set(sP(t)));
}
function ri(t, e) {
  return Object.freeze({
    resource: t,
    value: Math.max(0, Math.trunc(Number(e ?? 0) || 0))
  });
}
function fs(t = da.ready, e = "") {
  return Object.freeze({
    state: t,
    reason: String(e ?? "").trim()
  });
}
function mn(t = ta.none, e = !1) {
  return Object.freeze({
    type: t,
    required: !!e
  });
}
function oP(t = null) {
  return t ? Object.freeze(Xt(t)) : null;
}
function Ue(t) {
  const e = t.category ?? He.standard, n = e === He.free ? ri(Bi.fa, 1) : e === He.reaction ? ri(Bi.ra, 1) : ri(Bi.sa, e === He.complex ? 2 : 1);
  return Object.freeze({
    id: t.id,
    label: t.label,
    category: e,
    cost: t.cost ?? n,
    scale: Object.freeze(t.scale ?? ["personal"]),
    resolver: t.resolver ?? gt.action,
    roll: oP(t.roll ?? null),
    prompt: t.prompt ?? mn(),
    tags: Object.freeze(t.tags ?? ["combat"]),
    resolves: Object.freeze(t.resolves ?? []),
    payload: Object.freeze(t.payload ?? { intent: "combatAction", actionId: t.id }),
    implementation: t.implementation ?? fs(),
    description: String(t.description ?? "").trim(),
    prominent: !!t.prominent,
    prominentWhenBurning: !!t.prominentWhenBurning,
    hideFromMenus: !!t.hideFromMenus
  });
}
const Zu = Object.freeze([
  Ue({ id: "move", label: "Move", category: "standard", resolver: "movement", tags: ["combat", "movement"], description: "Reposition within the scene and establish your location." }),
  Ue({ id: "carefulMove", label: "Careful Move / Crawl", category: "standard", resolver: "movement", tags: ["combat", "movement", "careful"], description: "Move cautiously, crawl, or reposition without rushing." }),
  Ue({ id: "aim", label: "Aim", category: "standard", resolver: "action", tags: ["combat", "aim"], description: "Line up your next attack for a stronger single-target shot." }),
  Ue({ id: "interact", label: "Interact / Use Object", category: "standard", resolver: "interaction", tags: ["combat", "interaction"], description: "Manipulate an object or the environment with intent." }),
  Ue({ id: "readyWeapon", label: "Ready Weapon", category: "standard", resolver: "interaction", prompt: mn("weapon", !1), tags: ["combat", "interaction", "weapon", "ready"], description: "Ready a weapon or substantial piece of equipment." }),
  Ue({ id: "reload", label: "Reload / Load Weapon", category: "standard", resolver: "interaction", prompt: mn("weapon", !0), tags: ["combat", "interaction", "weapon", "reload"], resolves: ["jammed", "empty"], description: "Spend the action to reload or reset a weapon feed." }),
  Ue({ id: "assess", label: "Observe in Detail", category: "standard", resolver: "action", tags: ["combat", "assessment"], description: "Read the situation and gather useful tactical information." }),
  Ue({ id: "useSkill", label: "Simple Skill Use", category: "standard", resolver: "action", prompt: mn("skill", !0), roll: { intent: "skill" }, tags: ["combat", "skill"], description: "Make a trained, quick, normal-risk skill check." }),
  Ue({ id: "recover", label: "Recover from Stun", category: "standard", resolver: "recovery", prompt: mn("status", !1), tags: ["combat", "recovery", "status"], resolves: ["stunned"], description: "Recover from simple stun/status effects when eligible." }),
  Ue({ id: "stand", label: "Stand Up from Prone", category: "standard", resolver: "movement", tags: ["combat", "movement", "recovery", "posture"], resolves: ["prone"], description: "Stand up from Prone." }),
  Ue({ id: "leadTeam", label: "Assist / Lead Team", category: "standard", resolver: "action", tags: ["combat", "assist", "support"], description: "Provide active support during your activation." }),
  Ue({ id: "attack", label: "Attack", category: "complex", cost: ri("sa", 2), resolver: "attack", prompt: mn("weapon", !1), tags: ["combat", "attack"], prominent: !0, description: "Make an offensive action and resolve it through the attack pipeline." }),
  Ue({ id: "suppressionFire", label: "Suppression Fire", category: "complex", cost: ri("sa", 2), resolver: "attack", tags: ["combat", "attack", "suppression"], implementation: fs("stub", "Suppression resolver not implemented yet."), description: "Lay down suppressive fire once the suppression resolver exists." }),
  Ue({ id: "firstAid", label: "First Aid", category: "complex", cost: ri("sa", 2), resolver: "recovery", prompt: mn("target", !0), roll: { intent: "skill", key: "medicine" }, tags: ["combat", "recovery", "medical"], description: "Stabilize or recover harm through focused treatment." }),
  Ue({ id: "useComplexSkill", label: "Use Complex Skill", category: "complex", cost: ri("sa", 2), resolver: "action", prompt: mn("skill", !0), roll: { intent: "skill" }, tags: ["combat", "skill", "complex"], description: "Make an extended or higher-risk skill check." }),
  Ue({ id: "readyHeavyItem", label: "Ready Heavy Weapon", category: "complex", cost: ri("sa", 2), resolver: "interaction", prompt: mn("weapon", !1), tags: ["combat", "interaction", "weapon", "ready", "heavy"], description: "Ready a large or crew-served weapon." }),
  Ue({ id: "extinguish", label: "Extinguish Fire", category: "complex", cost: ri("sa", 2), resolver: "recovery", tags: ["combat", "recovery", "hazard", "fire"], resolves: ["onFire"], description: "Extinguish yourself or an eligible nearby fire status." }),
  Ue({ id: "communicate", label: "Communicate", category: "free", resolver: "action", tags: ["combat", "communication"], description: "Speak, signal, gesture, or coordinate without changing the mechanics." }),
  Ue({ id: "drop", label: "Drop Object", category: "free", resolver: "interaction", tags: ["combat", "interaction", "drop"], description: "Release or discard something you are holding." }),
  Ue({ id: "observeQuickly", label: "Observe Quickly", category: "free", resolver: "action", tags: ["combat", "assessment"], description: "Make a surface-level observation." }),
  Ue({ id: "changeFireMode", label: "Select Fire Mode", category: "free", resolver: "interaction", prompt: mn("weapon", !1), tags: ["combat", "weapon", "fireMode"], description: "Select a supported fire mode." }),
  Ue({ id: "selectPayload", label: "Select Ammunition / Payload", category: "free", resolver: "interaction", prompt: mn("payload", !0), tags: ["combat", "weapon", "payload"], description: "Select an owned compatible payload for a weapon." }),
  Ue({ id: "readyItem", label: "Ready Item", category: "free", resolver: "interaction", prompt: mn("item", !1), tags: ["combat", "interaction", "ready"], hideFromMenus: !0, description: "Ready an item as a personal critical remedy." }),
  Ue({ id: "prepare", label: "Prepare", category: "free", resolver: "action", prompt: mn("confirm", !0), tags: ["combat", "prepare", "interrupt"], description: "Declare a trigger now so you can interrupt later." }),
  Ue({ id: "activateItem", label: "Activate Item", category: "free", resolver: "interaction", prompt: mn("item", !1), tags: ["combat", "interaction", "activate"], description: "Switch on or initialize an item without resolving its full effect." }),
  Ue({ id: "defend", label: "Dodge", category: "free", resolver: "recovery", tags: ["combat", "defense", "dodge"], implementation: fs("stub", "Dodge mechanics are not implemented yet."), description: "Dodge when supported by the direct-defense resolver." }),
  Ue({ id: "react", label: "React", category: "reaction", resolver: "action", tags: ["combat", "reaction"], implementation: fs("stub", "Use a specific reaction action when available."), description: "Take a generic response to an outside trigger." }),
  Ue({ id: "evade", label: "Evade", category: "reaction", resolver: "recovery", tags: ["combat", "reaction", "evade"], description: "Avoid or soften incoming non-direct danger." }),
  Ue({ id: "opportunity", label: "Opportunity Attack", category: "reaction", resolver: "attack", tags: ["combat", "reaction", "attack", "opportunity"], description: "Exploit an opening and make a reactive attack." }),
  Ue({ id: "assist", label: "Assist Ally", category: "reaction", resolver: "action", prompt: mn("target", !0), tags: ["combat", "reaction", "assist"], description: "Support another combatant when their moment comes." }),
  Ue({ id: "interrupt", label: "Interrupt from Prepare", category: "reaction", resolver: "action", tags: ["combat", "reaction", "interrupt"], description: "Resolve a prepared response when its trigger is met." }),
  Ue({ id: "breakGrappleDefense", label: "Break Grapple / Melee Defense", category: "reaction", resolver: "recovery", tags: ["combat", "reaction", "defense", "grapple"], implementation: fs("stub", "Close-combat defensive reactions are not implemented yet."), description: "Break a grapple or defend in close combat once supported." }),
  Ue({ id: "reduceBurn", label: "Reduce Burn", category: "standard", cost: ri("sa", 1), resolver: "recovery", tags: ["combat", "recovery", "burn"], prominentWhenBurning: !0, description: "Take a breather and bring your Burn down by one." }),
  Ue(
    { id: "overloadCheck", label: "Overload Check", category: "recovery", cost: ri("none", 0), resolver: "recovery", roll: { intent: "overload" }, tags: ["combat", "recovery", "burn", "overload"], prominentWhenBurning: !0, description: "Roll to see whether mounting Burn pushes you into overload." }
  )
].map((t) => Object.freeze(Xt(t)))), lP = new Map(Zu.map((t) => [t.id, t]));
function cP(t, e, { strict: n = !1, prefix: i = "Action" } = {}) {
  const a = (e == null ? void 0 : e.cost) ?? ri("sa", (e == null ? void 0 : e.category) === "complex" ? 2 : 1), r = t == null ? void 0 : t.cost, s = String(
    (t == null ? void 0 : t.costResource) ?? (r == null ? void 0 : r.resource) ?? (typeof r == "string" || typeof r == "number" ? a.resource : void 0) ?? a.resource ?? ""
  ).trim().toLowerCase(), o = Number(
    (t == null ? void 0 : t.costValue) ?? (r == null ? void 0 : r.value) ?? (typeof r == "string" || typeof r == "number" ? r : void 0) ?? a.value ?? 0
  ), l = [];
  if (Mb.has(s) || l.push(`${i}: cost resource must be one of ${Array.from(Mb).join(", ")}.`), (!Number.isFinite(o) || o < 0) && l.push(`${i}: cost value must be a non-negative number.`), l.length) {
    if (n) {
      const c = new Error(l[0]);
      throw c.validationErrors = l, c;
    }
    return Xt(a);
  }
  return { resource: s, value: Math.trunc(o) };
}
function uP(t, e, { strict: n = !1, prefix: i = "Action" } = {}) {
  var o, l;
  const a = t == null ? void 0 : t.prompt, r = String((t == null ? void 0 : t.promptType) ?? (a == null ? void 0 : a.type) ?? ((o = e == null ? void 0 : e.prompt) == null ? void 0 : o.type) ?? "none").trim() || "none", s = gf((t == null ? void 0 : t.promptRequired) ?? (a == null ? void 0 : a.required), !!((l = e == null ? void 0 : e.prompt) != null && l.required));
  if (!vb.has(r)) {
    if (n) {
      const c = new Error(`${i}: prompt type must be one of ${Array.from(vb).join(", ")}.`);
      throw c.validationErrors = [c.message], c;
    }
    return Xt((e == null ? void 0 : e.prompt) ?? mn());
  }
  return { type: r, required: s };
}
function dP(t, e, { strict: n = !1, prefix: i = "Action" } = {}) {
  var o, l;
  const a = t == null ? void 0 : t.implementation, r = String((t == null ? void 0 : t.implementationState) ?? (a == null ? void 0 : a.state) ?? ((o = e == null ? void 0 : e.implementation) == null ? void 0 : o.state) ?? "ready").trim() || "ready", s = String((t == null ? void 0 : t.reason) ?? (t == null ? void 0 : t.implementationReason) ?? (a == null ? void 0 : a.reason) ?? ((l = e == null ? void 0 : e.implementation) == null ? void 0 : l.reason) ?? "").trim();
  if (!Cb.has(r)) {
    if (n) {
      const c = new Error(`${i}: implementation state must be one of ${Array.from(Cb).join(", ")}.`);
      throw c.validationErrors = [c.message], c;
    }
    return Xt((e == null ? void 0 : e.implementation) ?? fs("disabled", s));
  }
  return { state: r, reason: s };
}
function mP(t, e = null) {
  const n = t == null ? void 0 : t.roll;
  if (n === null) return null;
  if (typeof n == "object" && n) return Xt(n);
  const i = String((t == null ? void 0 : t.rollIntent) ?? (e == null ? void 0 : e.intent) ?? "").trim();
  return i ? { intent: i } : Xt(e ?? null);
}
function fP(t, e, n) {
  const i = t == null ? void 0 : t.payload;
  return Xt(i && typeof i == "object" ? i : (e == null ? void 0 : e.payload) ?? { intent: "combatAction", actionId: n });
}
function gP(t, e) {
  const n = String((t == null ? void 0 : t.resolver) ?? "").trim();
  if (n) return n;
  const i = String((t == null ? void 0 : t.handler) ?? (e == null ? void 0 : e.handler) ?? "").trim();
  return rP[i] ?? (e == null ? void 0 : e.resolver) ?? gt.action;
}
function Qk(t, { strict: e = !1, index: n = 0 } = {}) {
  var p, h;
  let i = t;
  const a = String((i == null ? void 0 : i.id) ?? "").trim(), r = lP.get(a) ?? {}, s = `Row ${n + 1}`, o = [];
  a || o.push(`${s}: id cannot be blank.`), aP.has(a) && o.push(`${s}: action id "${a}" has been retired.`), a === "defend" && String((i == null ? void 0 : i.label) ?? "").trim() === "Dodge / Defensive Response" && (i = {
    ...i,
    label: r.label,
    category: r.category,
    cost: Xt(r.cost),
    tags: Xt(r.tags),
    description: r.description,
    implementation: Xt(r.implementation)
  }), a === "communicate" && String((i == null ? void 0 : i.label) ?? "").trim() === "Speak / Signal" && (i = {
    ...i,
    label: r.label,
    tags: Xt(r.tags),
    description: r.description
  });
  const l = String((i == null ? void 0 : i.category) ?? r.category ?? "").trim();
  Tb.has(l) || o.push(`${s}: category must be one of ${Array.from(Tb).join(", ")}.`);
  const c = String((i == null ? void 0 : i.label) ?? r.label ?? "").trim();
  c || o.push(`${s}: label cannot be blank.`);
  const u = gP(i, r);
  if (wb.has(u) || o.push(`${s}: resolver must be one of ${Array.from(wb).join(", ")}.`), o.length) {
    if (e) {
      const b = new Error(o[0]);
      throw b.validationErrors = o, b;
    }
    return null;
  }
  let d, f, m;
  try {
    d = cP(i, r, { strict: e, prefix: s }), f = uP(i, r, { strict: e, prefix: s }), m = dP(i, r, { strict: e, prefix: s });
  } catch (b) {
    if (e) throw b;
    return null;
  }
  const g = {
    ...Xt(r),
    id: a,
    label: c,
    category: l,
    cost: d,
    scale: Xd((i == null ? void 0 : i.scale) ?? r.scale ?? ["personal"]),
    resolver: u,
    roll: mP(i, r.roll ?? null),
    prompt: f,
    tags: Xd((i == null ? void 0 : i.tags) ?? r.tags ?? ["combat"]),
    resolves: Xd((i == null ? void 0 : i.resolves) ?? r.resolves ?? []),
    payload: fP(i, r, a),
    implementation: m,
    description: String((i == null ? void 0 : i.description) ?? r.description ?? "").trim(),
    prominent: gf(i == null ? void 0 : i.prominent, !!r.prominent),
    prominentWhenBurning: gf(i == null ? void 0 : i.prominentWhenBurning, !!r.prominentWhenBurning)
  };
  return g.handler = "combatIntent", (p = g.payload) != null && p.actionId || (g.payload.actionId = a), (h = g.payload) != null && h.intent || (g.payload.intent = "combatAction"), g.roll || delete g.roll, g.description || delete g.description, g.prominent || delete g.prominent, g.prominentWhenBurning || delete g.prominentWhenBurning, g;
}
function pp() {
  return Xt(Zu);
}
function pP(t) {
  const e = Vr(t, { strict: !1, includeDefaults: !1 }), n = new Set(e.map((a) => a.id.toLowerCase())), i = [...e];
  for (const a of Zu)
    n.has(a.id.toLowerCase()) || i.push(Xt(a));
  return i;
}
function Vr(t, { strict: e = !1, includeDefaults: n = !0 } = {}) {
  if (!Array.isArray(t)) {
    if (e) {
      const o = new Error("Action catalog must be an array.");
      throw o.validationErrors = [o.message], o;
    }
    return pp();
  }
  const i = [], a = /* @__PURE__ */ new Set(), r = [];
  if (t.forEach((o, l) => {
    try {
      const c = Qk(o, { strict: e, index: l });
      if (!c) return;
      const u = c.id.toLowerCase();
      if (a.has(u)) {
        const d = `Row ${l + 1}: duplicate action id "${c.id}".`;
        e && r.push(d);
        return;
      }
      a.add(u), i.push(c);
    } catch (c) {
      e && r.push(...Array.isArray(c.validationErrors) ? c.validationErrors : [c.message]);
    }
  }), r.length) {
    const o = new Error(r[0]);
    throw o.validationErrors = r, o;
  }
  if (!n) return i;
  const s = [...i];
  for (const o of Zu)
    a.has(o.id.toLowerCase()) || s.push(Xt(o));
  return s;
}
function Xk() {
  var t, e;
  try {
    const n = (e = (t = game == null ? void 0 : game.settings) == null ? void 0 : t.get) == null ? void 0 : e.call(t, I, ou);
    return Vr(n, { strict: !1, includeDefaults: !0 });
  } catch {
    return pp();
  }
}
async function hP() {
  const t = game == null ? void 0 : game.settings;
  if (!(t != null && t.get) || !(t != null && t.set)) return { ok: !1, reason: "Settings are not available." };
  const e = t.get(I, ou), n = pP(e), i = Vr(e, { strict: !1, includeDefaults: !1 }).map((r) => r.id), a = n.map((r) => r.id);
  return i.length === a.length && i.every((r, s) => r === a[s]) ? { ok: !0, changed: !1 } : (await t.set(I, ou, n), { ok: !0, changed: !0 });
}
function vs(t) {
  const e = String(t ?? "").trim();
  return Xk().find((n) => n.id === e) ?? null;
}
function bP(t) {
  return Xk().filter((e) => e.category === t).map((e) => Object.freeze(Xt(e)));
}
const Nr = "hazard";
function yP(t) {
  return t && typeof t == "object" ? t : {};
}
function Ha(t) {
  var a, r, s;
  const e = ((a = t == null ? void 0 : t.getFlag) == null ? void 0 : a.call(t, "mwd", Nr)) ?? ((s = (r = t == null ? void 0 : t.flags) == null ? void 0 : r.mwd) == null ? void 0 : s[Nr]) ?? null;
  if (!e || typeof e != "object") return null;
  const n = Gi(e.areaEffect ?? { kind: kt.persistent, hazard: e.hazardDef }), i = vt(
    e.templateGeometry,
    {
      template: e.template,
      placement: e.templatePlacement
    }
  ) ?? null;
  return {
    ...foundry.utils.deepClone(yP(e)),
    areaEffect: n,
    hazardDef: Yg(e.hazardDef ?? n.hazard ?? {}),
    templateGeometry: i
  };
}
function SP(t) {
  return !!Ha(t);
}
async function Zd(t) {
  var i, a, r;
  const e = ((i = t == null ? void 0 : t.getFlag) == null ? void 0 : i.call(t, "mwd", Nr)) ?? ((r = (a = t == null ? void 0 : t.flags) == null ? void 0 : a.mwd) == null ? void 0 : r[Nr]) ?? null;
  if (!e || typeof e != "object") return null;
  if (e != null && e.templateGeometry) return Ha(t);
  const n = Ha(t);
  return !(n != null && n.templateGeometry) || !(t != null && t.setFlag) ? n : (await t.setFlag("mwd", Nr, {
    ...foundry.utils.deepClone(e),
    templateGeometry: Ui(n.templateGeometry)
  }), Ha(t));
}
async function AP({ attacker: t = null, attack: e = {}, targetResult: n = null } = {}) {
  var c, u, d, f, m, g, p, h, b, y, S, A;
  const i = (canvas == null ? void 0 : canvas.scene) ?? null;
  if (!i) return null;
  const a = vt(
    e == null ? void 0 : e.templateGeometry,
    {
      template: e == null ? void 0 : e.template,
      placement: e == null ? void 0 : e.templatePlacement
    }
  ), r = Gi((e == null ? void 0 : e.areaEffect) ?? ((c = e == null ? void 0 : e.payload) == null ? void 0 : c.areaEffect) ?? {});
  if (r.kind !== kt.persistent || !a) return null;
  const s = Ku(a);
  if (!s.length) return null;
  const o = {
    sourceActorUuid: (t == null ? void 0 : t.uuid) ?? null,
    sourceItemUuid: ((u = e == null ? void 0 : e.weapon) == null ? void 0 : u.uuid) ?? null,
    payloadId: ((d = e == null ? void 0 : e.payloadState) == null ? void 0 : d.activePayloadId) ?? ((f = e == null ? void 0 : e.payload) == null ? void 0 : f.id) ?? "",
    templateGeometry: Ui(a),
    templatePlacement: foundry.utils.deepClone((e == null ? void 0 : e.templatePlacement) ?? null),
    template: foundry.utils.deepClone((e == null ? void 0 : e.template) ?? null),
    damage: Number(((m = n == null ? void 0 : n.damage) == null ? void 0 : m.effectiveWeaponDamage) ?? ((g = e == null ? void 0 : e.weapon) == null ? void 0 : g.damage) ?? 0) || 0,
    ap: Number((e == null ? void 0 : e.totalAp) ?? ((p = e == null ? void 0 : e.weapon) == null ? void 0 : p.ap) ?? 0) || 0,
    damageType: String(((h = n == null ? void 0 : n.damage) == null ? void 0 : h.damageType) ?? ((b = e == null ? void 0 : e.weapon) == null ? void 0 : b.damageType) ?? "concussive").trim() || "concussive",
    label: `${String(((y = e == null ? void 0 : e.weapon) == null ? void 0 : y.name) ?? "Hazard").trim() || "Hazard"} (${ci(((S = r.hazard) == null ? void 0 : S.startExposure) ?? "minor")})`,
    areaEffect: r,
    hazardDef: r.hazard
  }, [l] = await i.createEmbeddedDocuments("Region", [{
    name: o.label,
    color: ((A = game.user) == null ? void 0 : A.color) ?? "#d86a2c",
    shapes: s,
    flags: {
      mwd: {
        [Nr]: o
      }
    }
  }]);
  return l ?? null;
}
function Ib(t = null) {
  var r, s, o, l;
  const e = (t == null ? void 0 : t.document) ?? t ?? null;
  if (!e) return [];
  const n = (e == null ? void 0 : e.parent) ?? (canvas == null ? void 0 : canvas.scene) ?? null;
  if (!n) return [];
  const i = ((r = e == null ? void 0 : e.object) == null ? void 0 : r.center) ?? (e == null ? void 0 : e.center) ?? {
    x: Number((e == null ? void 0 : e.x) ?? 0) + (Number((e == null ? void 0 : e.width) ?? 1) || 1) * (Number(((s = canvas == null ? void 0 : canvas.grid) == null ? void 0 : s.size) ?? 100) || 100) / 2,
    y: Number((e == null ? void 0 : e.y) ?? 0) + (Number((e == null ? void 0 : e.height) ?? 1) || 1) * (Number(((o = canvas == null ? void 0 : canvas.grid) == null ? void 0 : o.size) ?? 100) || 100) / 2
  }, a = {
    x: Number((i == null ? void 0 : i.x) ?? 0) || 0,
    y: Number((i == null ? void 0 : i.y) ?? 0) || 0,
    elevation: Number((e == null ? void 0 : e.elevation) ?? ((l = e == null ? void 0 : e.object) == null ? void 0 : l.elevation) ?? 0) || 0
  };
  return Array.from(n.regions ?? []).filter(SP).filter((c) => {
    var u, d, f;
    if ((d = (u = c == null ? void 0 : c.tokens) == null ? void 0 : u.has) != null && d.call(u, e)) return !0;
    try {
      return ((f = c == null ? void 0 : c.testPoint) == null ? void 0 : f.call(c, a)) ?? !1;
    } catch {
      return !1;
    }
  });
}
function Ea(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function kP({ actor: t = null, token: e = null } = {}) {
  var r;
  const n = (e == null ? void 0 : e.document) ?? e ?? null, i = String(((r = n == null ? void 0 : n.texture) == null ? void 0 : r.src) ?? "").trim(), a = String((t == null ? void 0 : t.img) ?? "").trim();
  return i || a || "icons/svg/mystery-man.svg";
}
function TP(t = []) {
  return Array.isArray(t) ? t.map((e) => ({
    key: String((e == null ? void 0 : e.key) ?? "").trim(),
    label: String((e == null ? void 0 : e.label) ?? (e == null ? void 0 : e.key) ?? "").trim(),
    value: Ea(e == null ? void 0 : e.value, 0)
  })).filter((e) => e.key) : [];
}
function hp(t = {}) {
  var c, u, d;
  const e = (t == null ? void 0 : t.preview) ?? {}, n = Mt(((c = t == null ? void 0 : t.exposure) == null ? void 0 : c.initialTier) ?? (t == null ? void 0 : t.exposureTier) ?? (t == null ? void 0 : t.tier), "none"), i = Mt(
    ((u = t == null ? void 0 : t.exposure) == null ? void 0 : u.finalTier) ?? (e.evadeActive ? e.finalTier : n),
    n
  ), a = Va({
    tier: n,
    appliedTier: i,
    evadeUsed: !!(e.evadeActive && n !== i),
    evadeLocked: !!((t == null ? void 0 : t.evadeLocked) ?? ((d = t == null ? void 0 : t.exposure) == null ? void 0 : d.evadeLocked))
  }), r = Math.max(0, Ea(t == null ? void 0 : t.baseDamage, 0)), s = Math.max(0, Ea(t == null ? void 0 : t.damageBefore, $r(r, a.initialTier))), o = Math.max(0, Ea(t == null ? void 0 : t.damageAfter, $r(r, a.finalTier))), l = (e == null ? void 0 : e.reactionPreview) ?? {};
  return {
    kind: String((t == null ? void 0 : t.kind) ?? "hazard").trim() || "hazard",
    eventType: String((t == null ? void 0 : t.eventType) ?? "entry").trim() || "entry",
    regionId: String((t == null ? void 0 : t.regionId) ?? "").trim(),
    regionName: String((t == null ? void 0 : t.regionName) ?? (t == null ? void 0 : t.label) ?? "Hazard").trim() || "Hazard",
    actorUuid: String((t == null ? void 0 : t.actorUuid) ?? "").trim() || null,
    tokenUuid: String((t == null ? void 0 : t.tokenUuid) ?? "").trim() || null,
    actorName: String((t == null ? void 0 : t.actorName) ?? "Target").trim() || "Target",
    turnsExposed: Math.max(0, Ea(t == null ? void 0 : t.turnsExposed, 0)),
    baseDamage: r,
    ap: Math.max(0, Ea(t == null ? void 0 : t.ap, 0)),
    damageType: Xn(t == null ? void 0 : t.damageType, "concussive"),
    damageTypeLabel: In((t == null ? void 0 : t.damageType) ?? "concussive") || "Damage",
    source: String((t == null ? void 0 : t.source) ?? (t == null ? void 0 : t.regionName) ?? "Hazard").trim() || "Hazard",
    applied: !!(t != null && t.applied),
    applyReason: String((t == null ? void 0 : t.applyReason) ?? "").trim(),
    onFullBurnDelta: Math.max(0, Ea(t == null ? void 0 : t.onFullBurnDelta, 0)),
    exposure: a,
    damageBefore: s,
    damageAfter: o,
    nextTier: Mt(t == null ? void 0 : t.nextTier, a.finalTier),
    nextLabel: ci((t == null ? void 0 : t.nextTier) ?? a.finalTier),
    preview: {
      evadeActive: !!e.evadeActive,
      edgePoolKey: String(e.edgePoolKey ?? "").trim() || null,
      finalTier: a.finalTier,
      burnDelta: Ea(l == null ? void 0 : l.burnDelta, 0),
      canSpendEdge: !!(l != null && l.canSpendEdge),
      disabled: !!(l != null && l.disabled),
      reason: String((l == null ? void 0 : l.reason) ?? "").trim(),
      reactionPreview: {
        disabled: !!(l != null && l.disabled),
        reason: String((l == null ? void 0 : l.reason) ?? "").trim()
      },
      edgePools: TP(l == null ? void 0 : l.edgePools)
    }
  };
}
function wP(t = {}, { actor: e = null, token: n = null } = {}) {
  const i = hp(t), a = i.eventType === "tick" ? "Hazard Tick" : i.eventType === "exit" ? "Hazard Exit" : "Hazard Entry", r = i.exposure.initialLabel === i.exposure.finalLabel ? i.exposure.initialLabel : `${i.exposure.initialLabel} -> ${i.exposure.finalLabel}`, s = i.damageBefore === i.damageAfter ? String(i.damageAfter) : `${i.damageBefore} -> ${i.damageAfter}`;
  return {
    classes: [
      "mwd-chat-card",
      "mwd-hazard-card",
      `is-${i.exposure.finalTier}`,
      i.applied ? "is-applied" : ""
    ].filter(Boolean).join(" "),
    header: {
      left: i.regionName,
      right: a
    },
    target: {
      name: i.actorName,
      image: kP({ actor: e, token: n })
    },
    rows: [
      { label: "Exposure", value: r },
      { label: "Damage", value: s },
      { label: "Next Tier", value: i.nextLabel },
      { label: "Turns Exposed", value: String(i.turnsExposed) }
    ],
    reactionHint: i.exposure.evadeLocked ? "Evade is locked for this hazard." : i.preview.evadeActive ? i.preview.edgePoolKey ? "Evade active. Reaction Burn canceled by Edge." : i.preview.burnDelta > 0 ? `Evade active. This reaction adds +${i.preview.burnDelta} Burn.` : "Evade active." : "",
    actions: i.applied ? [] : MP(i)
  };
}
function MP(t = {}) {
  var n, i, a, r, s, o;
  const e = [];
  if (!t.exposure.evadeLocked && t.exposure.initialTier !== "none") {
    const l = !!(((i = (n = t.preview) == null ? void 0 : n.reactionPreview) == null ? void 0 : i.disabled) ?? ((a = t.preview) == null ? void 0 : a.disabled)), c = String(((s = (r = t.preview) == null ? void 0 : r.reactionPreview) == null ? void 0 : s.reason) ?? ((o = t.preview) == null ? void 0 : o.reason) ?? "").trim();
    e.push({
      action: "toggleHazardEvade",
      label: l ? c || "Reaction Disabled" : t.preview.evadeActive ? "Clear Evade" : "Use Reaction",
      cssClass: `mwd-target-row__action ${t.preview.evadeActive ? "is-active" : ""}`,
      disabled: l
    });
  }
  if (t.preview.evadeActive && t.preview.canSpendEdge)
    for (const l of t.preview.edgePools)
      e.push({
        action: "toggleHazardEvadeEdge",
        label: t.preview.edgePoolKey === l.key ? `Edge: ${l.label}` : `Use ${l.label}`,
        cssClass: `mwd-target-row__action ${t.preview.edgePoolKey === l.key ? "is-active" : ""}`,
        dataset: { "pool-key": l.key }
      });
  return e.push({
    action: "applyHazardTick",
    label: "Apply",
    cssClass: "mwd-target-row__action mwd-apply-attack-damage"
  }), e;
}
async function Zk(t = {}, { actor: e = null, token: n = null } = {}) {
  return foundry.applications.handlebars.renderTemplate(
    "mwd.v2.roll.mwd-hazard-card",
    wP(t, { actor: e, token: n })
  );
}
const eT = Object.freeze({
  ground: "Ground",
  flight: "Flight",
  jump: "Jump"
}), vP = 30, Eb = 10;
function Qn(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? Math.max(0, Math.trunc(n)) : e;
}
function bp(t = 0) {
  return Qn(t, 0) * vP;
}
function _r(t = 0, e = 0, { immobile: n = !1, minimum: i = Eb } = {}) {
  const a = Qn(t, 0);
  if (n) return 0;
  const r = Qn(e, 0);
  if (a <= 0 || r <= 0) return a;
  const s = Math.min(a, Qn(i, Eb));
  return Math.max(s, a - r);
}
function CP(t = "") {
  return t === M.actorTypes.vehicle || t === M.actorTypes.battlemech;
}
function IP(t = "") {
  return t === M.actorTypes.battlemech ? ["ground", "flight"] : t === M.actorTypes.vehicle ? ["ground", "flight"] : [];
}
function $l(t = {}, { actorType: e = "", legacyMoves: n = 0 } = {}) {
  if (!CP(e)) return {};
  const i = t && typeof t == "object" ? t : {}, a = Qn(n, 0), r = {
    ground: Qn(i.ground ?? i.move ?? i.run, a),
    flight: Qn(i.flight ?? i.fly, 0)
  };
  return e === M.actorTypes.battlemech && i.jump !== void 0 && (r.jump = Qn(i.jump, 0)), r;
}
function EP(t = null, e = {}) {
  if (!(t != null && t.enabled)) return null;
  const n = Qn(t.movement, 0);
  if (n <= 0) return null;
  const i = !!(e != null && e.immobile || e != null && e.noJump), a = [
    t.heat > 0 ? `Heat +${Qn(t.heat, 0)}` : "",
    Number(t.attackRatingBonus ?? 0) ? `AR ${Number(t.attackRatingBonus ?? 0) >= 0 ? "+" : ""}${Number(t.attackRatingBonus ?? 0)}` : "",
    Number(t.defenseRatingBonus ?? 0) ? `DR ${Number(t.defenseRatingBonus ?? 0) >= 0 ? "+" : ""}${Number(t.defenseRatingBonus ?? 0)}` : "",
    t.dfaEnabled ? "DFA" : ""
  ].filter(Boolean);
  return {
    key: "jump",
    label: eT.jump ?? "Jump",
    value: n,
    displayValue: String(n),
    path: "",
    visible: !0,
    editable: !1,
    derived: !0,
    title: String(t.sourceLabel ?? "").trim(),
    detail: a.join(" | "),
    blocked: !!(t.blocked || i),
    blockedReason: i ? e != null && e.immobile ? "Machine is immobilized." : "Jump movement is blocked by current damage or status effects." : String(t.blockedReason ?? "").trim()
  };
}
function tT({
  actorType: t = "",
  movement: e = {},
  legacyMoves: n = 0,
  editing: i = !1,
  basePath: a = "system.movement",
  jumpProfile: r = null,
  movementEffects: s = {}
} = {}) {
  const o = $l(e, { actorType: t, legacyMoves: n }), l = Qn(s == null ? void 0 : s.movementPenalty, 0), c = Qn(s == null ? void 0 : s.movementBonus, 0), u = !!(s != null && s.immobile), d = IP(t).map((m) => {
    const g = Qn(o[m], 0), p = g + c, h = _r(p, l, { immobile: u }), b = m === "flight", y = [];
    return !i && c > 0 && y.push(`+${c} m`), !i && u && p > 0 ? y.push("Immobilized") : !i && h !== p && y.push(`-${l} m`), {
      key: m,
      label: eT[m] ?? m,
      value: g,
      adjustedValue: h,
      displayValue: String(i ? g : h),
      path: `${a}.${m}`,
      visible: i || !b || p > 0,
      editable: !0,
      derived: !1,
      title: "",
      detail: y.join(" | ")
    };
  }).filter((m) => m.visible), f = t === M.actorTypes.battlemech ? EP(r, s) : null;
  return f && d.push(f), d;
}
function nT({ actorType: t = "", movement: e = {}, legacyMoves: n = 0, jumpProfile: i = null, movementEffects: a = {} } = {}) {
  const r = $l(e, { actorType: t, legacyMoves: n });
  return tT({
    actorType: t,
    movement: r,
    legacyMoves: n,
    editing: !1,
    jumpProfile: i,
    movementEffects: a
  }).map((s) => ({
    label: s.label,
    value: String(s.adjustedValue ?? s.value)
  }));
}
function NP(t, e) {
  const n = Math.max(0, e.runningHot ?? e.hot ?? 0), i = Math.max(0, e.overheated ?? e.overheat ?? 0), a = Math.max(0, e.shutdown ?? e.danger ?? 0), r = Math.max(0, t);
  if (n === 0 || i === 0 || a === 0)
    return { levelsInHot: 0, levelsInOverheat: 0, levelsInDanger: 0 };
  const s = Math.max(0, Math.min(r, i - 1) - (n - 1)), o = Math.max(0, Math.min(r, a - 1) - (i - 1)), l = Math.max(0, r - (a - 1));
  return { levelsInHot: s, levelsInOverheat: o, levelsInDanger: l };
}
function ed(t, e) {
  const { levelsInHot: n, levelsInOverheat: i, levelsInDanger: a } = NP(t, e);
  return {
    movementPenalty: bp(n),
    rangedDicePenalty: i,
    dangerLevel: a
  };
}
function PP(t, e, n, i = 1 / 0) {
  return Math.max(0, t + e - n);
}
function iT(t, e, n) {
  return {
    shutdownDN: t,
    shutdownPool: Math.max(0, e + n),
    explosionDN: 1,
    explosionPool: Math.max(1, e + n - t)
  };
}
function RP(t = {}) {
  return Object.values(t).some(
    (e) => (e == null ? void 0 : e.enabled) && !(e != null && e.destroyed) && Array.isArray(e == null ? void 0 : e.tags) && e.tags.includes("ammoStore")
  );
}
const Nb = Object.freeze({
  safe: "Safe",
  hot: "Hot",
  overheat: "Overheat",
  danger: "Danger",
  runningHot: "Hot",
  overheated: "Overheat",
  shutdown: "Danger"
});
function wo(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function td(t = {}, e = 0) {
  const n = Math.max(0, wo(e, 0)), i = Math.max(0, wo(t.hot ?? t.runningHot, 2)), a = Math.max(0, wo(t.overheat ?? t.overheated, 3)), r = Math.max(0, wo(t.danger ?? t.shutdown, n || 4));
  return {
    hot: i,
    overheat: a,
    danger: r,
    runningHot: i,
    overheated: a,
    shutdown: r
  };
}
function aT(t = 0, e = {}, n = 0) {
  const i = Math.max(0, wo(t, 0)), a = td(e, n);
  return i >= a.danger ? "danger" : i >= a.overheat ? "overheat" : i >= a.hot ? "hot" : "safe";
}
function rT(t = "safe") {
  return Nb[String(t ?? "").trim()] ?? Nb.safe;
}
const Rt = Object.freeze({
  ew: Object.freeze({
    beforeAcquireSolution: "mwd.beforeAcquireSolution",
    afterAcquireSolution: "mwd.afterAcquireSolution",
    beforeGenerateTargetingData: "mwd.beforeGenerateTargetingData",
    afterGenerateTargetingData: "mwd.afterGenerateTargetingData",
    beforeAttackTargeting: "mwd.beforeAttackTargeting",
    consumeTargetingData: "mwd.consumeTargetingData",
    suppressTargetingNetwork: "mwd.suppressTargetingNetwork"
  }),
  harm: Object.freeze({
    beforeMachineDamagePreview: "mwd.beforeMachineDamagePreview",
    beforeMachineDamageApply: "mwd.beforeMachineDamageApply",
    afterMachineDamageApply: "mwd.afterMachineDamageApply",
    beforePilotDamageApply: "mwd.beforePilotDamageApply",
    beforeCatastrophicEventApply: "mwd.beforeCatastrophicEventApply"
  }),
  heat: Object.freeze({
    collectHeatProfile: "mwd.collectHeatProfile",
    beforeHeatGenerated: "mwd.beforeHeatGenerated",
    afterHeatGenerated: "mwd.afterHeatGenerated",
    beforeHeatDissipation: "mwd.beforeHeatDissipation",
    afterHeatDissipation: "mwd.afterHeatDissipation",
    beforeDangerCheck: "mwd.beforeDangerCheck"
  })
});
function Nn(t = "", e = {}) {
  const n = String(t ?? "").trim();
  return !n || !globalThis.Hooks ? !0 : typeof globalThis.Hooks.call == "function" ? globalThis.Hooks.call(n, e) : (typeof globalThis.Hooks.callAll == "function" && globalThis.Hooks.callAll(n, e), !0);
}
const DP = /* @__PURE__ */ new Set([M.actorTypes.vehicle, M.actorTypes.battlemech]), OP = Object.freeze({
  head: "Head",
  torso: "Torso",
  arms: "Arms",
  legs: "Legs",
  body: "Body",
  mobility: "Mobility",
  core: "Core",
  front: "Front",
  side: "Side",
  rear: "Rear",
  turret: "Turret",
  rotor: "Rotor"
});
function LP(t) {
  const e = Math.trunc(Number(t ?? 0));
  return Number.isFinite(e) ? Math.min(18, Math.max(3, e)) : 10;
}
function sT(t = null) {
  return String((t == null ? void 0 : t.type) ?? t ?? "").trim();
}
function $P(t = null) {
  var n, i;
  const e = ((i = (n = t == null ? void 0 : t.system) == null ? void 0 : n.mwd) == null ? void 0 : i.locations) ?? {};
  return Object.entries(e).filter(([, a]) => (a == null ? void 0 : a.enabled) !== !1).map(([a]) => a);
}
function nt(t, e = [], n = "core") {
  const i = new Set($P(t));
  return e.find((a) => i.has(a)) ?? e[0] ?? n;
}
function Pb(t = "") {
  return t === "head" ? "head" : t === "arms" ? "arms" : t === "legs" ? "legs" : ["front", "side", "rear", "rotor"].includes(t) ? "motive" : t === "turret" ? "weapon" : t === "torso" ? "torso" : "core";
}
function Rb(t = "", e = "", n = "") {
  const i = String(n ?? "").trim(), a = String(e ?? "").trim();
  return t === M.actorTypes.battlemech ? ["head", "torso", "arms", "legs"].includes(a) ? a : ["head", "torso", "arms", "legs"].includes(i) ? i : "torso" : i === "weapon" || a === "turret" ? "turret" : a === "front" ? "body" : i === "motive" || ["side", "rear", "rotor"].includes(a) ? "mobility" : "body";
}
function _P(t, e) {
  return e <= 4 ? { locationKey: nt(t, ["torso", "head"]), family: "critical" } : e === 5 ? { locationKey: nt(t, ["legs", "torso"]), family: "legs" } : e === 6 ? { locationKey: nt(t, ["legs", "torso"]), family: "legs" } : e === 7 ? { locationKey: nt(t, ["arms", "torso"]), family: "arms" } : e === 8 ? { locationKey: nt(t, ["arms", "torso"]), family: "arms" } : e <= 13 ? { locationKey: nt(t, ["torso", "head"]), family: "torso" } : e === 14 ? { locationKey: nt(t, ["arms", "torso"]), family: "arms" } : e === 15 ? { locationKey: nt(t, ["arms", "torso"]), family: "arms" } : e === 16 ? { locationKey: nt(t, ["arms", "torso"]), family: "arms" } : e === 17 ? { locationKey: nt(t, ["legs", "torso"]), family: "legs" } : { locationKey: nt(t, ["head", "torso"]), family: "head" };
}
function BP(t, e) {
  return e <= 4 ? { locationKey: nt(t, ["core", "front"]), family: "critical" } : e === 5 ? { locationKey: nt(t, ["front", "core"]), family: "motive" } : e <= 7 ? { locationKey: nt(t, ["side", "front"]), family: "motive" } : e === 8 ? { locationKey: nt(t, ["rear", "side"]), family: "motive" } : e === 9 ? { locationKey: nt(t, ["front", "core"]), family: "motive" } : e === 10 ? { locationKey: nt(t, ["core", "front"]), family: "core" } : e === 11 ? { locationKey: nt(t, ["turret", "core"]), family: "weapon" } : e === 12 ? { locationKey: nt(t, ["side", "front"]), family: "motive" } : e === 13 ? { locationKey: nt(t, ["rear", "side"]), family: "motive" } : e === 14 ? { locationKey: nt(t, ["front", "side"]), family: "motive" } : e === 15 ? { locationKey: nt(t, ["core", "rear"]), family: "core" } : e === 16 ? { locationKey: nt(t, ["turret", "core"]), family: "weapon" } : e === 17 ? { locationKey: nt(t, ["side", "front", "rotor"]), family: "motive" } : { locationKey: nt(t, ["core", "front"]), family: "core" };
}
function $i(t = "") {
  return OP[t] ?? (String(t ?? "").trim() || "Location");
}
function Fs(t = null) {
  return DP.has(sT(t));
}
function oT() {
  if (typeof Roll == "function")
    try {
      const t = new Roll("3d6"), e = t.evaluateSync();
      return Number((e == null ? void 0 : e.total) ?? t.total ?? 10) || 10;
    } catch {
    }
  return Array.from({ length: 3 }, () => 1 + Math.floor(Math.random() * 6)).reduce((t, e) => t + e, 0);
}
function lT({
  actor: t = null,
  rollTotal: e = oT(),
  armorBefore: n = 0,
  structureBefore: i = 0
} = {}) {
  const a = sT(t), r = LP(e), s = Math.max(0, Number(n ?? 0) || 0) <= 0, o = a === M.actorTypes.battlemech ? _P(t, r) : BP(t, r), l = r <= 4, c = s && r >= 16, u = l || c, d = !u && r >= 16, f = r === 18 && a === M.actorTypes.battlemech ? nt(t, ["torso", "head"]) : o.locationKey, m = o.family || Pb(o.locationKey), g = Rb(a, o.locationKey, m), p = Rb(a, f, Pb(f));
  return {
    rollTotal: r,
    actorType: a,
    locationKey: o.locationKey,
    locationLabel: $i(o.locationKey),
    impactLabel: $i(o.locationKey),
    rulesLocation: g,
    rulesLocationLabel: $i(g),
    locationFamily: m,
    isForcedCritical: l,
    isStructureCritical: c,
    isAutomaticCritical: u,
    chaosCriticalOption: d,
    chaosTargetLocationKey: f,
    chaosTargetLocationLabel: $i(f),
    chaosRulesLocation: p,
    chaosRulesLocationLabel: $i(p),
    descriptiveOnly: !u,
    pureStructureHit: s,
    armorBefore: Math.max(0, Number(n ?? 0) || 0),
    structureBefore: Math.max(0, Number(i ?? 0) || 0)
  };
}
const xP = Object.freeze([
  "action",
  "attack",
  "movement",
  "targeting",
  "remediation",
  "recovery",
  "interaction"
]), zP = Object.freeze([
  "ready",
  "stub",
  "disabled",
  "legacy"
]), FP = Object.freeze(["fa", "sa", "ra", "none"]), UP = new Set(xP), KP = new Set(zP), HP = new Set(FP);
function ac(t) {
  if (Array.isArray(t)) return t.map((n) => String(n ?? "").trim()).filter(Boolean);
  const e = String(t ?? "").trim();
  return e ? [e] : [];
}
function jP(t = null) {
  return Object.freeze(!t || typeof t != "object" ? { type: "none", required: !1 } : {
    type: String(t.type ?? "none").trim() || "none",
    required: !!t.required
  });
}
function GP(t = null) {
  const e = String((t == null ? void 0 : t.state) ?? "ready").trim() || "ready", n = KP.has(e) ? e : "stub";
  return Object.freeze({
    state: n,
    reason: String((t == null ? void 0 : t.reason) ?? "").trim()
  });
}
function ue(t, e = {}) {
  var d, f;
  const n = String(e.resource ?? "sa").trim() || "sa", i = HP.has(n) ? n : "sa", a = Math.max(0, Math.trunc(Number(e.cost ?? 0) || 0)), r = String(e.resolver ?? "action").trim() || "action", s = UP.has(r) ? r : "action", o = String(
    e.category ?? (i === "fa" ? "free" : a >= 2 ? "complex" : a === 0 ? "none" : "simple")
  ).trim() || "simple", l = GP(e.implementation), c = ac(e.tags), u = ac(e.resolves);
  return Object.freeze({
    key: t,
    id: t,
    label: e.label ?? t,
    attributeKey: String(e.attributeKey ?? "").trim(),
    skillKey: String(e.skillKey ?? "").trim(),
    // Compatibility fields used by current machine remedy/EW/action-cost code.
    resource: i,
    cost: a,
    category: o,
    remediable: e.remediable !== !1,
    intent: String(e.intent ?? "").trim(),
    use: String(e.use ?? "").trim(),
    notes: String(e.notes ?? "").trim(),
    // Declarative action fields used by the central machine action executor.
    actionCost: Object.freeze({ resource: i, value: a }),
    scale: Object.freeze(ac(e.scale).length ? ac(e.scale) : ["machine"]),
    resolver: s,
    roll: e.roll === void 0 ? null : e.roll,
    prompt: jP(e.prompt),
    tags: Object.freeze(c),
    resolves: Object.freeze(u),
    payload: Object.freeze({
      intent: String(((d = e.payload) == null ? void 0 : d.intent) ?? "machineAction").trim() || "machineAction",
      actionId: String(((f = e.payload) == null ? void 0 : f.actionId) ?? t).trim() || t,
      ...e.payload && typeof e.payload == "object" ? e.payload : {}
    }),
    implementation: l
  });
}
const Db = (t) => ({ state: "ready", reason: "" }), Ob = (t) => ({ state: "stub", reason: t }), Lb = Object.freeze({
  none: ue("none", {
    label: "No Field Remedy",
    remediable: !1,
    resource: "none",
    cost: 0,
    category: "none",
    implementation: Db()
  }),
  // Free actions.
  communicate: ue("communicate", {
    label: "Communicate",
    resource: "fa",
    cost: 1,
    category: "free",
    resolver: "action",
    tags: ["communication", "free"],
    notes: "Brief communication, gesture, or signal."
  }),
  activateElectronics: ue("activateElectronics", {
    label: "Activate / Deactivate Electronics",
    resource: "fa",
    cost: 1,
    category: "free",
    resolver: "interaction",
    tags: ["systems", "toggle", "electronics"],
    notes: "Simple electronics toggle; tactically significant toggles may be raised to 1 SA by a future resolver."
  }),
  toggleHeatSinks: ue("toggleHeatSinks", {
    label: "Activate / Deactivate Heat Sinks",
    resource: "fa",
    cost: 1,
    category: "free",
    resolver: "interaction",
    tags: ["heat", "toggle"],
    notes: "Heat-sink state toggle."
  }),
  activateMasc: ue("activateMasc", {
    label: "Activate / Deactivate MASC",
    resource: "fa",
    cost: 1,
    category: "free",
    resolver: "movement",
    tags: ["movement", "toggle", "masc"],
    notes: "MASC state toggle; risk, heat, or strain hooks belong in the movement resolver."
  }),
  selectFireMode: ue("selectFireMode", {
    label: "Select Fire Mode",
    resource: "fa",
    cost: 1,
    category: "free",
    resolver: "interaction",
    prompt: { type: "fireMode", required: !0 },
    tags: ["weapon", "fireMode"],
    notes: "Change active fire mode when the machine supports modes."
  }),
  selectAmmoType: ue("selectAmmoType", {
    label: "Select Ammunition Type",
    resource: "fa",
    cost: 1,
    category: "free",
    resolver: "interaction",
    prompt: { type: "payload", required: !0 },
    tags: ["weapon", "payload", "ammunition"],
    notes: "Select a preloaded ammunition or payload type; physical reloads remain separate."
  }),
  torsoTwist: ue("torsoTwist", {
    label: "Torso Twist / Rotate Turret",
    resource: "fa",
    cost: 1,
    category: "free",
    resolver: "movement",
    tags: ["facing", "arc", "posture"],
    notes: "Arc or facing adjustment; CQ-granting variants can be raised to 1 SA later."
  }),
  avoidShutdown: ue("avoidShutdown", {
    label: "Avoid Shutdown",
    resource: "ra",
    cost: 1,
    category: "reaction",
    resolver: "recovery",
    roll: { intent: "heatDangerCheck", checkKind: "shutdown" },
    tags: ["heat", "shutdown", "reaction"],
    resolves: ["shutdown"],
    notes: "Danger heat shutdown check."
  }),
  // Standard actions / 1 SA.
  walk: ue("walk", {
    label: "Walk / Safe Thrust",
    attributeKey: "handling",
    skillKey: "piloting",
    cost: 1,
    category: "simple",
    resolver: "movement",
    tags: ["movement", "safe"]
  }),
  safeThrust: ue("safeThrust", {
    label: "Safe Thrust",
    attributeKey: "handling",
    skillKey: "piloting",
    cost: 1,
    category: "simple",
    resolver: "movement",
    tags: ["movement", "safe", "thrust"]
  }),
  run: ue("run", {
    label: "Run / Flank / Max Thrust",
    attributeKey: "handling",
    skillKey: "piloting",
    cost: 2,
    category: "complex",
    resolver: "movement",
    tags: ["movement", "fast"],
    notes: "Fast movement up to 2x ground speed, or 2x flight speed while airborne; generates 1 Heat."
  }),
  jumpMove: ue("jumpMove", {
    label: "Jump Movement",
    attributeKey: "handling",
    skillKey: "piloting",
    cost: 1,
    category: "simple",
    resolver: "movement",
    tags: ["movement", "jump"]
  }),
  aim: ue("aim", {
    label: "Careful Aim",
    cost: 1,
    category: "simple",
    resolver: "action",
    tags: ["combat", "aim"],
    notes: "Posture/action-economy anchor for careful aim bonuses."
  }),
  rangedAttack: ue("rangedAttack", {
    label: "Ranged Attack",
    attributeKey: "handling",
    skillKey: "gunnery",
    cost: 1,
    category: "simple",
    resolver: "attack",
    prompt: { type: "weapon", required: !0 },
    tags: ["combat", "attack", "ranged"]
  }),
  physicalAttack: ue("physicalAttack", {
    label: "Physical Attack",
    attributeKey: "handling",
    skillKey: "piloting",
    cost: 1,
    category: "simple",
    resolver: "attack",
    prompt: { type: "weapon", required: !1 },
    tags: ["combat", "attack", "melee", "physical"]
  }),
  sensorSweep: ue("sensorSweep", {
    label: "Sensor Sweep / Observe Battlefield",
    attributeKey: "system",
    skillKey: "perception",
    cost: 1,
    category: "simple",
    resolver: "targeting",
    intent: "sensorSweep",
    tags: ["sensors", "assessment"],
    notes: "General battlefield scan, hidden-unit detection, and contact identification."
  }),
  assess: ue("assess", {
    label: "Observe in Detail",
    attributeKey: "system",
    skillKey: "perception",
    cost: 1,
    category: "simple",
    resolver: "targeting",
    intent: "sensorSweep",
    tags: ["sensors", "assessment"]
  }),
  acquireTarget: ue("acquireTarget", {
    label: "Acquire Target",
    attributeKey: "system",
    skillKey: "perception",
    cost: 1,
    category: "simple",
    resolver: "targeting",
    intent: "acquireTarget",
    prompt: { type: "target", required: !0 },
    tags: ["sensors", "targeting", "detection"]
  }),
  generateFireSolution: ue("generateFireSolution", {
    label: "Generate Fire Solution",
    attributeKey: "system",
    skillKey: "gunnery",
    cost: 1,
    category: "simple",
    resolver: "targeting",
    intent: "generateFireSolution",
    prompt: { type: "target", required: !0 },
    tags: ["sensors", "targeting", "fireSolution"]
  }),
  sensorLock: ue("sensorLock", {
    label: "Sensor Lock",
    attributeKey: "system",
    skillKey: "perception",
    cost: 1,
    category: "simple",
    resolver: "targeting",
    intent: "acquireTarget",
    prompt: { type: "target", required: !0 },
    tags: ["sensors", "targeting", "lock"],
    notes: "State upgrade through the acquire-target resolver."
  }),
  goDark: ue("goDark", {
    label: "Go Dark",
    cost: 1,
    category: "simple",
    resolver: "targeting",
    prompt: null,
    tags: ["stealth", "targeting", "emissions"],
    notes: "Clear system-owned reveal and transient emission, then return to passive signature posture.",
    implementation: Db(),
    payload: { intent: "machineAction", actionId: "goDark" }
  }),
  brace: ue("brace", {
    label: "Brace",
    cost: 1,
    category: "simple",
    resolver: "movement",
    tags: ["posture", "defense"],
    notes: "CQ posture package or defensive setup."
  }),
  hullDown: ue("hullDown", {
    label: "Hull Down",
    cost: 1,
    category: "simple",
    resolver: "movement",
    tags: ["posture", "cover", "defense"],
    notes: "CQ/DR posture package for vehicles and prepared firing positions."
  }),
  eject: ue("eject", {
    label: "Eject",
    cost: 1,
    category: "simple",
    resolver: "interaction",
    tags: ["crew", "emergency"],
    implementation: Ob("Manual ejection state changes are not automated yet.")
  }),
  dropProne: ue("dropProne", {
    label: "Drop Prone",
    cost: 1,
    category: "simple",
    resolver: "movement",
    tags: ["posture", "prone"],
    resolves: ["standing"]
  }),
  // Complex actions / 2+ SA.
  sprint: ue("sprint", {
    label: "Sprint",
    attributeKey: "handling",
    skillKey: "piloting",
    cost: 3,
    category: "complex",
    resolver: "movement",
    tags: ["movement", "sprint", "stability"],
    notes: "High-commitment movement up to 3x ground speed, or 3x flight speed while airborne; generates 2 Heat and requires a stability roll."
  }),
  chargeAttack: ue("chargeAttack", {
    label: "Charge / Ram / DFA",
    attributeKey: "handling",
    skillKey: "piloting",
    cost: 2,
    category: "complex",
    resolver: "attack",
    tags: ["movement", "attack", "collision"]
  }),
  evasiveManeuver: ue("evasiveManeuver", {
    label: "Evasive Maneuver",
    attributeKey: "handling",
    skillKey: "piloting",
    cost: 2,
    category: "complex",
    resolver: "movement",
    tags: ["movement", "defense", "evasion"],
    notes: "Withdraw safely. Move up to normal distance without provoking parting attacks. Gain Evasive (+3 DR / −2 AR) until start of next activation."
  }),
  shield: ue("shield", {
    label: "Shield",
    cost: 2,
    category: "complex",
    resolver: "movement",
    tags: ["defense", "shield", "posture"],
    notes: "Guarded stance. Gain Shielded (+4 DR / −1 AR) until start of next activation. First hit reduces incoming damage by 2, then Shield ends."
  }),
  powerCycle: ue("powerCycle", {
    label: "Power Cycle",
    attributeKey: "system",
    skillKey: "computers",
    cost: 2,
    category: "complex",
    resolver: "remediation",
    tags: ["systems", "shutdown", "remediation"],
    resolves: ["shutdown"]
  }),
  spotIndirect: ue("spotIndirect", {
    label: "Spot for Indirect Fire",
    attributeKey: "system",
    skillKey: "perception",
    cost: 2,
    category: "complex",
    resolver: "targeting",
    prompt: { type: "target", required: !0 },
    tags: ["sensors", "targeting", "indirect"],
    implementation: Ob("Indirect-fire network effects are not automated yet.")
  }),
  useComplexSkill: ue("useComplexSkill", {
    label: "Use Complex Skill",
    cost: 2,
    category: "complex",
    resolver: "action",
    prompt: { type: "skill", required: !0 },
    tags: ["skill", "complex"],
    notes: "Generic complex machine-scale skill action."
  }),
  pilotingCheck: ue("pilotingCheck", {
    label: "Climb / Evade Terrain / Difficult Maneuver",
    attributeKey: "handling",
    skillKey: "piloting",
    cost: 2,
    category: "complex",
    resolver: "movement",
    tags: ["movement", "piloting", "terrain"]
  }),
  emergencyRepair: ue("emergencyRepair", {
    label: "Critical Repair",
    attributeKey: "reliability",
    skillKey: "technician",
    cost: 2,
    category: "complex",
    resolver: "remediation",
    tags: ["repair", "critical", "remediation"]
  }),
  coolantDump: ue("coolantDump", {
    label: "Coolant Dump",
    attributeKey: "reliability",
    skillKey: "systemOps",
    cost: 2,
    category: "complex",
    resolver: "remediation",
    tags: ["heat", "remediation"],
    notes: "Restore the heat-management system."
  }),
  epmFilter: ue("epmFilter", {
    label: "EPM Filter",
    attributeKey: "system",
    skillKey: "perception",
    cost: 2,
    category: "complex",
    resolver: "targeting",
    intent: "epmFilter",
    tags: ["sensors", "ew", "ecm"],
    resolves: ["ecmJamming"],
    notes: "Remove or reduce ECM Jamming."
  }),
  swat: ue("swat", {
    label: "Swat Battle Armor / Remove NARC",
    attributeKey: "handling",
    skillKey: "piloting",
    cost: 2,
    category: "complex",
    resolver: "remediation",
    intent: "swat",
    tags: ["physical", "remediation", "narc"],
    notes: "Remove BattleArmor or NARC."
  }),
  // Existing remediation and EW anchors.
  systemReset: ue("systemReset", {
    label: "System Reset",
    attributeKey: "reliability",
    skillKey: "systemOps",
    cost: 1,
    category: "simple",
    resolver: "remediation",
    tags: ["systems", "remediation"]
  }),
  reboot: ue("reboot", {
    label: "Reboot",
    attributeKey: "reliability",
    skillKey: "computers",
    cost: 1,
    category: "simple",
    resolver: "remediation",
    tags: ["systems", "remediation"]
  }),
  feedReset: ue("feedReset", {
    label: "Feed Reset",
    attributeKey: "reliability",
    skillKey: "gunnery",
    cost: 1,
    category: "simple",
    resolver: "remediation",
    tags: ["weapon", "feed", "remediation"]
  }),
  pilotRecovery: ue("pilotRecovery", {
    label: "Pilot Recovery",
    attributeKey: "reliability",
    skillKey: "piloting",
    cost: 1,
    category: "simple",
    resolver: "recovery",
    tags: ["pilot", "recovery"]
  }),
  stand: ue("stand", {
    label: "Stand",
    attributeKey: "handling",
    skillKey: "piloting",
    cost: 1,
    category: "simple",
    resolver: "movement",
    tags: ["posture", "recovery"],
    resolves: ["prone"]
  }),
  stabalize: ue("stabalize", {
    label: "Stabilize",
    attributeKey: "handling",
    skillKey: "piloting",
    cost: 2,
    category: "complex",
    resolver: "recovery",
    tags: ["stabilize", "recovery"]
  }),
  stabilize: ue("stabilize", {
    label: "Stabilize",
    attributeKey: "handling",
    skillKey: "piloting",
    cost: 2,
    category: "complex",
    resolver: "recovery",
    tags: ["stabilize", "recovery"]
  }),
  powerReroute: ue("powerReroute", {
    label: "Power Reroute",
    attributeKey: "system",
    skillKey: "technician",
    cost: 1,
    category: "simple",
    resolver: "remediation",
    tags: ["systems", "remediation"]
  }),
  breakLock: ue("breakLock", {
    label: "Break Lock",
    attributeKey: "handling",
    skillKey: "stealth",
    resource: "sa",
    cost: 1,
    category: "simple",
    resolver: "targeting",
    intent: "breakLock",
    tags: ["sensors", "defense", "stealth"],
    notes: "Degrade detection state from an attacker."
  }),
  defensiveJink: ue("defensiveJink", {
    label: "Defensive Jink",
    attributeKey: "handling",
    skillKey: "piloting",
    resource: "ra",
    cost: 1,
    category: "reaction",
    resolver: "targeting",
    intent: "defensiveJink",
    tags: ["defense", "reaction", "targetingData", "piloting"],
    notes: "Reduce an enemy targetingData packet against this machine by 1."
  }),
  ecmSpike: ue("ecmSpike", {
    label: "ECM Spike",
    attributeKey: "system",
    skillKey: "systemOps",
    cost: 1,
    category: "simple",
    resolver: "targeting",
    intent: "ecmSpike",
    tags: ["ew", "ecm", "targeting"],
    notes: "Apply ECM Jamming."
  }),
  suppressBeacon: ue("suppressBeacon", {
    label: "Suppress Beacon / Suppress NARC/TAG",
    attributeKey: "system",
    skillKey: "systemOps",
    cost: 1,
    category: "simple",
    resolver: "targeting",
    intent: "suppressBeacon",
    tags: ["ew", "beacon", "narc", "tag"],
    notes: "Temporarily suppress beacon-based targeting support."
  }),
  extinguish: ue("extinguish", {
    label: "Extinguish Fire",
    attributeKey: "system",
    skillKey: "piloting",
    cost: 2,
    category: "complex",
    resolver: "recovery",
    tags: ["hazard", "fire", "recovery"],
    resolves: ["burning"]
  }),
  tagTarget: ue("tagTarget", {
    label: "TAG Target",
    attributeKey: "handling",
    skillKey: "gunnery",
    cost: 1,
    category: "simple",
    resolver: "targeting",
    intent: "tagTarget",
    tags: ["targeting", "tag"],
    notes: "Apply a TAG enabler flag for guided systems."
  }),
  narcTarget: ue("narcTarget", {
    label: "NARC Target",
    attributeKey: "handling",
    skillKey: "gunnery",
    cost: 1,
    category: "simple",
    resolver: "targeting",
    intent: "narcTarget",
    tags: ["targeting", "narc"],
    notes: "Attach a NARC beacon to target for persistent guided-system support."
  }),
  shareTargetingData: ue("shareTargetingData", {
    label: "Share Sensor Feed / C3 Network Support",
    attributeKey: "system",
    skillKey: "",
    resource: "fa",
    cost: 1,
    category: "free",
    resolver: "targeting",
    intent: "shareTargetingData",
    tags: ["targeting", "network", "c3"],
    notes: "Provider-driven network support; share best detection state and best eligible packet."
  }),
  emergencyJettison: ue("emergencyJettison", {
    label: "Emergency Jettison",
    attributeKey: "system",
    skillKey: "gunnery",
    resource: "ra",
    cost: 1,
    category: "reaction",
    resolver: "interaction",
    tags: ["emergency", "jettison", "reaction"]
  }),
  jettisonCore: ue("jettisonCore", {
    label: "Jettison Core",
    attributeKey: "system",
    skillKey: "technician",
    cost: 2,
    category: "complex",
    resolver: "interaction",
    tags: ["emergency", "jettison"]
  }),
  reposition: ue("reposition", {
    label: "Reposition",
    attributeKey: "handling",
    skillKey: "piloting",
    cost: 1,
    category: "simple",
    resolver: "movement",
    tags: ["movement", "posture"]
  }),
  toggle: ue("toggle", {
    label: "Toggle",
    attributeKey: "",
    skillKey: "",
    resource: "fa",
    cost: 1,
    category: "free",
    resolver: "interaction",
    tags: ["toggle"]
  }),
  majorRepair: ue("majorRepair", {
    label: "Major Repair",
    attributeKey: "reliability",
    skillKey: "technician",
    resource: "none",
    cost: 0,
    category: "narrative",
    resolver: "remediation",
    remediable: !1,
    tags: ["repair", "downtime"]
  })
}), qP = Object.freeze({
  stabilize: "stabalize",
  changeFireMode: "selectFireMode",
  selectAmmunition: "selectAmmoType",
  selectAmmunitionType: "selectAmmoType",
  physicalDefense: "shield",
  dodge: "evasiveManeuver",
  evade: "evasiveManeuver",
  move: "walk",
  flank: "run",
  maxThrust: "run",
  prone: "dropProne"
});
function _l(t = "") {
  const e = String(t ?? "").trim(), n = qP[e] ?? e;
  return Lb[n] ?? Lb.none;
}
function Dt(t, e = {}) {
  const n = _l(t);
  return Object.freeze({
    key: n.key,
    label: e.label ?? n.label,
    actionId: e.actionId ?? `machineCrit${n.key.charAt(0).toUpperCase()}${n.key.slice(1)}`,
    actionLabel: e.actionLabel ?? n.label,
    resource: n.resource,
    cost: n.cost,
    category: n.category,
    remediable: e.remediable ?? n.remediable,
    skillKey: e.skillKey ?? n.skillKey,
    baseDn: Number(e.baseDn ?? n.cost ?? 0)
  });
}
const pf = Object.freeze(Object.fromEntries(
  [
    ["none", Dt("none", { baseDn: 0 })],
    ["emergencyRepair", Dt("emergencyRepair", { baseDn: 2 })],
    ["systemReset", Dt("systemReset", { baseDn: 2 })],
    ["coolantDump", Dt("coolantDump", { baseDn: 1 })],
    ["reboot", Dt("reboot", { baseDn: 2 })],
    ["feedReset", Dt("feedReset", { label: "Feed Reset", baseDn: 1 })],
    ["pilotRecovery", Dt("pilotRecovery", { baseDn: 2 })],
    ["stand", Dt("stand", { baseDn: 1 })],
    ["stabalize", Dt("stabalize", { baseDn: 2 })],
    ["powerReroute", Dt("powerReroute", { baseDn: 2 })],
    ["epmFilter", Dt("epmFilter", { baseDn: 2 })],
    ["powerCycle", Dt("powerCycle", { baseDn: 2 })],
    ["acquireTarget", Dt("acquireTarget", { baseDn: 2 })],
    ["jettisonCore", Dt("jettisonCore", { baseDn: 3 })],
    ["breakLock", Dt("breakLock", { baseDn: 2 })],
    ["extinguish", Dt("extinguish", { baseDn: 2 })],
    ["reposition", Dt("reposition", { baseDn: 0 })],
    ["toggle", Dt("toggle", { baseDn: 0, remediable: !0 })],
    ["majorRepair", Dt("majorRepair", { baseDn: 0, remediable: !1 })]
  ]
));
function Ya(t = "") {
  const e = String(t ?? "").trim();
  return pf[e] ?? pf.emergencyRepair;
}
function WP(t = "") {
  return Object.prototype.hasOwnProperty.call(pf, String(t ?? "").trim());
}
function VP(t = {}) {
  return [
    t == null ? void 0 : t.key,
    t == null ? void 0 : t.label,
    t == null ? void 0 : t.locationLabel,
    ...Array.isArray(t == null ? void 0 : t.gates) ? t.gates : [],
    ...Array.isArray(t == null ? void 0 : t.mods) ? t.mods : []
  ].map((e) => String(e ?? "").trim()).filter(Boolean).join(" ").toLowerCase();
}
function Zo(t = {}, e = null) {
  const n = e ?? Ya((t == null ? void 0 : t.remedyKey) ?? ""), i = String((t == null ? void 0 : t.remedySkillKey) ?? "").trim();
  if (i) return i;
  if (n.key === "systemReset") {
    const a = VP(t);
    if (/(sensor|optic|target|tracking|communications|comms|processor|computer|fire-control|fire control)/i.test(a))
      return "computers";
  }
  return String(n.skillKey ?? "").trim();
}
function el(t = {}, e = null) {
  const n = e ?? Ya((t == null ? void 0 : t.remedyKey) ?? ""), i = Number(t == null ? void 0 : t.remedyBaseDn);
  if (Number.isFinite(i) && i >= 0) return i;
  const a = Number(n.baseDn ?? n.cost ?? 1);
  return Number.isFinite(a) ? Math.max(0, a) : 1;
}
function yp(t = {}) {
  const e = (t == null ? void 0 : t.remedyEffect) ?? {}, n = String((e == null ? void 0 : e.onSuccess) ?? "clear").trim() || "clear", i = String((e == null ? void 0 : e.onFailure) ?? "noChange").trim() || "noChange";
  return { onSuccess: n, onFailure: i };
}
const Zt = Object.freeze({
  intact: 0,
  impaired: 1,
  damaged: 2,
  crippled: 3,
  disabled: 4
}), hf = Object.freeze({
  [Zt.intact]: "Intact",
  [Zt.impaired]: "Impaired",
  [Zt.damaged]: "Damaged",
  [Zt.crippled]: "Crippled",
  [Zt.disabled]: "Disabled"
}), JP = Object.freeze({
  [Zt.intact]: 0,
  [Zt.impaired]: 1,
  [Zt.damaged]: 2,
  [Zt.crippled]: 3,
  [Zt.disabled]: 4
}), YP = Object.freeze({
  0: 1,
  1: 2,
  2: 3,
  3: 4,
  4: 6,
  5: 8
}), QP = Object.freeze([
  "head",
  "torso",
  "arms",
  "legs"
]), XP = Object.freeze([
  "body",
  "turret",
  "mobility"
]), ZP = Object.freeze({
  head: Object.freeze({ enabled: !0, stress: 0, condition: 0, tags: ["cockpit", "sensor"], destroyed: !1 }),
  torso: Object.freeze({ enabled: !0, stress: 0, condition: 0, tags: ["weaponGroup", "engine", "gyro", "ammoStore"], destroyed: !1 }),
  arms: Object.freeze({ enabled: !0, stress: 0, condition: 0, tags: ["weaponGroup"], destroyed: !1 }),
  legs: Object.freeze({ enabled: !0, stress: 0, condition: 0, tags: ["motiveSystem"], destroyed: !1 })
}), eR = Object.freeze({
  body: Object.freeze({ enabled: !0, stress: 0, condition: 0, tags: ["crewCompartment", "engine", "ammoStore"], destroyed: !1 }),
  turret: Object.freeze({ enabled: !0, stress: 0, condition: 0, tags: ["turret", "weaponGroup"], destroyed: !1 }),
  mobility: Object.freeze({ enabled: !0, stress: 0, condition: 0, tags: ["motiveSystem", "rotor"], destroyed: !1 })
}), tR = Object.freeze({
  [M.actorTypes.battlemech]: Object.freeze({
    head: Object.freeze({ type: "cockpitCatastrophe", destroyed: !0, statusState: "destroyed" }),
    torso: Object.freeze({ type: "torsoCollapse", destroyed: !0, statusState: "destroyed" }),
    arms: Object.freeze({ type: "armSystemCollapse", destroyed: !0, statusState: "" }),
    legs: Object.freeze({ type: "legCollapse", destroyed: !0, statusState: "immobilized" })
  }),
  [M.actorTypes.vehicle]: Object.freeze({
    body: Object.freeze({ type: "hullCollapse", destroyed: !0, statusState: "destroyed" }),
    turret: Object.freeze({ type: "turretDestroyed", destroyed: !0, statusState: "" }),
    mobility: Object.freeze({ type: "mobilityCollapse", destroyed: !0, statusState: "immobilized" })
  })
});
function Pr(t) {
  var e;
  if (typeof foundry < "u" && typeof ((e = foundry == null ? void 0 : foundry.utils) == null ? void 0 : e.deepClone) == "function")
    try {
      return foundry.utils.deepClone(t);
    } catch {
    }
  return JSON.parse(JSON.stringify(t ?? null));
}
function Xe(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function Sp(t, e, n) {
  return Math.min(n, Math.max(e, t));
}
function Jr(t = null) {
  const e = String((t == null ? void 0 : t.type) ?? t ?? "").trim();
  return e === M.actorTypes.battlemech || e === "mech" ? M.actorTypes.battlemech : M.actorTypes.vehicle;
}
function nR(t = M.actorTypes.vehicle) {
  return t === M.actorTypes.battlemech ? ZP : eR;
}
function cT(t = M.actorTypes.vehicle) {
  return t === M.actorTypes.battlemech ? QP : XP;
}
function ya(t = 0) {
  return Sp(Math.trunc(Xe(t, 0)), 0, Zt.disabled);
}
function $b(t = {}, e = {}) {
  return {
    enabled: (t == null ? void 0 : t.enabled) !== void 0 ? !!t.enabled : !!e.enabled,
    stress: Math.max(0, Xe((t == null ? void 0 : t.stress) ?? (e == null ? void 0 : e.stress), 0)),
    condition: ya((t == null ? void 0 : t.condition) ?? (e == null ? void 0 : e.condition) ?? 0),
    tags: Array.isArray(t == null ? void 0 : t.tags) ? t.tags.slice() : Array.isArray(e == null ? void 0 : e.tags) ? e.tags.slice() : [],
    destroyed: !!((t == null ? void 0 : t.destroyed) ?? (e == null ? void 0 : e.destroyed))
  };
}
function iR(t = {}) {
  const e = {
    head: ["head"],
    torso: ["torso", "torsoFront", "torsoRear", "core"],
    arms: ["arms", "leftArm", "rightArm"],
    legs: ["legs", "leftLeg", "rightLeg"]
  }, n = {};
  for (const [i, a] of Object.entries(e)) {
    const r = a.map((d) => [d, t == null ? void 0 : t[d]]).filter(([, d]) => d && typeof d == "object");
    if (!r.length) continue;
    const s = r.reduce((d, [, f]) => d + Math.max(0, Xe(f == null ? void 0 : f.stress, 0)), 0), o = r.reduce((d, [, f]) => Math.max(d, ya(f == null ? void 0 : f.condition)), 0), l = r.some(([, d]) => (d == null ? void 0 : d.enabled) !== !1), c = r.some(([, d]) => (d == null ? void 0 : d.destroyed) === !0), u = Array.from(new Set(r.flatMap(([, d]) => Array.isArray(d == null ? void 0 : d.tags) ? d.tags : [])));
    n[i] = {
      enabled: l,
      stress: s,
      condition: o,
      destroyed: c,
      tags: u
    };
  }
  for (const [i, a] of Object.entries(t ?? {}))
    Object.hasOwn(n, i) || ["torsoFront", "torsoRear", "core", "leftArm", "rightArm", "leftLeg", "rightLeg"].includes(i) || (n[i] = a);
  return n;
}
function aR(t = {}) {
  const e = {
    body: ["body", "core", "front"],
    turret: ["turret"],
    mobility: ["mobility", "side", "rear", "rotor"]
  }, n = {};
  for (const [i, a] of Object.entries(e)) {
    const r = a.map((d) => [d, t == null ? void 0 : t[d]]).filter(([, d]) => d && typeof d == "object");
    if (!r.length) continue;
    const s = r.reduce((d, [, f]) => d + Math.max(0, Xe(f == null ? void 0 : f.stress, 0)), 0), o = r.reduce((d, [, f]) => Math.max(d, ya(f == null ? void 0 : f.condition)), 0), l = r.some(([, d]) => (d == null ? void 0 : d.enabled) !== !1), c = r.some(([, d]) => (d == null ? void 0 : d.destroyed) === !0), u = Array.from(new Set(r.flatMap(([, d]) => Array.isArray(d == null ? void 0 : d.tags) ? d.tags : [])));
    n[i] = {
      enabled: l,
      stress: s,
      condition: o,
      destroyed: c,
      tags: u
    };
  }
  for (const [i, a] of Object.entries(t ?? {}))
    Object.hasOwn(n, i) || ["core", "front", "side", "rear", "rotor"].includes(i) || (n[i] = a);
  return n;
}
function rR(t = {}, e = M.actorTypes.vehicle) {
  const n = nR(e), i = e === M.actorTypes.battlemech ? iR(t ?? {}) : aR(t ?? {}), a = {};
  for (const [r, s] of Object.entries(n))
    a[r] = $b((i == null ? void 0 : i[r]) ?? {}, s);
  for (const [r, s] of Object.entries(i ?? {}))
    a[r] || (a[r] = $b(s, {}));
  return a;
}
function sR(t = {}, e = M.actorTypes.vehicle) {
  const n = t.mwd = t.mwd ?? {};
  return n.unitType = String(n.unitType ?? (e === M.actorTypes.battlemech ? "mech" : "vehicle")).trim() || (e === M.actorTypes.battlemech ? "mech" : "vehicle"), n.status = n.status ?? { state: "operational", reasons: [] }, n.crits = Array.isArray(n.crits) ? n.crits : [], n;
}
function uT(t = {}) {
  var i;
  const e = ((i = t == null ? void 0 : t.monitors) == null ? void 0 : i.structure) ?? {}, n = Math.max(0, Xe(e == null ? void 0 : e.max, 0));
  return n <= 0 ? null : Sp(Xe(e == null ? void 0 : e.value, 0), 0, n);
}
function oR(t = {}, e = M.actorTypes.vehicle) {
  var i;
  if (Jr(e) !== M.actorTypes.vehicle || (uT(t) ?? 1) > 0) return t;
  const n = ((i = t == null ? void 0 : t.mwd) == null ? void 0 : i.locations) ?? {};
  for (const a of Object.values(n))
    !a || a.enabled !== !0 || (a.condition = Zt.disabled);
  return t;
}
function lR(t = {}) {
  var a, r;
  const e = t.attributes = t.attributes ?? {}, n = (a = e.reliability) == null ? void 0 : a.value, i = (r = e.condition) == null ? void 0 : r.value;
  return Math.max(0, Xe(n ?? i, 0));
}
function cR(t = [], e = -1) {
  return !Array.isArray(t) || e < 0 ? !1 : t.some((n) => Math.trunc(Xe(n, -1)) === e);
}
function dT(t, e, n) {
  !e || !Number.isFinite(n) || n === 0 || (t.stressDelta[e] = Number(t.stressDelta[e] ?? 0) + n);
}
function mT(t = "") {
  const e = String(t ?? "").trim();
  return e === "highMargin" ? 3 : e === "hit" ? 2 : e === "graze" ? 1 : 0;
}
function uR(t, e, n, i = "") {
  if (e.stress !== t.stress) return e.stress - t.stress;
  if (e.condition !== t.condition) return e.condition - t.condition;
  if (t.condition >= Zt.disabled && e.condition < Zt.disabled) return 1;
  if (e.condition >= Zt.disabled && t.condition < Zt.disabled) return -1;
  const a = String(i ?? "").trim();
  if (a) {
    const l = t.key === a, c = e.key === a;
    if (l !== c) return l ? -1 : 1;
  }
  const r = cT(n), s = r.indexOf(t.key), o = r.indexOf(e.key);
  if (s !== o) {
    const l = s >= 0 ? s : Number.MAX_SAFE_INTEGER, c = o >= 0 ? o : Number.MAX_SAFE_INTEGER;
    return l - c;
  }
  return String(t.key).localeCompare(String(e.key));
}
function dR(t = {}, e = M.actorTypes.vehicle, n = "") {
  return Object.entries(t).filter(([, i]) => i && i.enabled === !0 && i.destroyed !== !0).map(([i, a]) => ({
    key: i,
    stress: Math.max(0, Xe(a == null ? void 0 : a.stress, 0)),
    condition: ya(a == null ? void 0 : a.condition)
  })).sort((i, a) => uR(i, a, e, n));
}
function mR(t = {}, e = M.actorTypes.vehicle, n = "", { forcePreferred: i = !0 } = {}) {
  var r;
  const a = String(n ?? "").trim();
  if (a && i) {
    const s = t == null ? void 0 : t[a];
    if (s && s.enabled === !0 && s.destroyed !== !0) return a;
  }
  return ((r = dR(t, e, a)[0]) == null ? void 0 : r.key) ?? "";
}
function fR(t, e, n, i) {
  !n || !e || (t.fallbackEvents.push({
    location: n,
    type: e.type,
    destroyed: !!e.destroyed,
    statusState: String(e.statusState ?? "").trim()
  }), e.destroyed && (i[n].destroyed = !0), e.statusState && (t.statusState = e.statusState));
}
function gR({ result: t, locations: e, locationKey: n, reliability: i, threshold: a, currentShock: r }) {
  var c;
  const s = Math.max(0, i), o = Math.max(0, Xe((c = e == null ? void 0 : e[n]) == null ? void 0 : c.stress, 0)), l = Math.max(0, o - s);
  return e[n].stress = l, dT(t, n, l - o), Math.max(0, r - Math.max(1, a));
}
function pR({ locations: t = {}, shockBefore: e = 0, shockGain: n = 0, threshold: i = 1, reliability: a = 0, spendableBefore: r = 0 } = {}) {
  return {
    stressDelta: {},
    shockDelta: 0,
    conditionAdvancements: [],
    reliabilitySpends: [],
    fallbackEvents: [],
    spendOpportunities: [],
    statusState: "",
    summary: {
      shockBefore: e,
      shockGain: n,
      shockAfter: e + n,
      threshold: i,
      reliability: a,
      reliabilitySpendableBefore: r,
      reliabilitySpendableAfter: r,
      selectedLocations: [],
      locationsBefore: Pr(t),
      locationsAfter: null
    }
  };
}
function hR(t, e = "critical") {
  if (typeof t == "string") {
    const i = String(t ?? "").trim();
    return i ? { locationKey: i, source: e, applyReductions: !0, allowSpend: !0 } : null;
  }
  if (!t || typeof t != "object") return null;
  const n = String(t.locationKey ?? t.location ?? "").trim();
  return n ? {
    locationKey: n,
    source: String(t.source ?? e).trim() || e,
    applyReductions: t.applyReductions !== !1,
    allowSpend: t.allowSpend !== !1
  } : null;
}
function fT(t = 0) {
  const e = Sp(Math.trunc(Xe(t, 0)), 0, 5);
  return Math.max(1, Number(YP[e] ?? 1));
}
function Ap(t = 0) {
  return hf[ya(t)] ?? hf[0];
}
function kp(t = 0) {
  return JP[ya(t)] ?? 0;
}
function bR(t = M.actorTypes.vehicle) {
  return cT(Jr(t)).slice();
}
function eo(t = {}, e = M.actorTypes.vehicle) {
  var o, l, c;
  const n = Jr(e), i = t.attributes = t.attributes ?? {}, a = lR(t);
  i.reliability = i.reliability ?? {}, i.reliability.value = a, i.condition = i.condition ?? {}, i.condition.value = Math.max(0, Xe(((o = i.condition) == null ? void 0 : o.value) ?? a, a));
  const r = sR(t, n);
  r.shock = r.shock ?? {}, r.shock.value = Math.max(0, Xe((l = r.shock) == null ? void 0 : l.value, 0)), r.reliabilitySpendable = r.reliabilitySpendable ?? {};
  const s = (c = r.reliabilitySpendable) == null ? void 0 : c.value;
  return r.reliabilitySpendable.value = Math.max(0, Xe(s ?? a, a)), r.locations = rR(r.locations ?? {}, n), oR(t, n), t;
}
function gT(t = null, e = null) {
  var s;
  const n = Jr(t);
  if (n !== M.actorTypes.vehicle) return {};
  const i = eo(Pr((t == null ? void 0 : t.system) ?? {}), n), a = Number.isFinite(Number(e)) ? Math.max(0, Number(e)) : uT(i);
  if (a === null || a > 0) return {};
  const r = {};
  for (const [o, l] of Object.entries(((s = i.mwd) == null ? void 0 : s.locations) ?? {}))
    !l || l.enabled !== !0 || (r[`system.mwd.locations.${o}.condition`] = Zt.disabled);
  return r;
}
function yR({ actorSnapshot: t = null, unitType: e = "", locationKey: n = "" } = {}) {
  var s;
  const i = Jr(e || (t == null ? void 0 : t.type) || (t == null ? void 0 : t.actorType)), a = String(n ?? "").trim(), r = ((s = tR[i]) == null ? void 0 : s[a]) ?? {
    type: "catastrophicFailure",
    destroyed: !0,
    statusState: ""
  };
  return {
    location: a,
    type: r.type,
    destroyed: !!r.destroyed,
    statusState: String(r.statusState ?? "").trim()
  };
}
function SR({
  actorSnapshot: t = null,
  locationKey: e = "",
  machineDamageDealt: n = 0,
  attackQuality: i = "",
  shockGainOverride: a = null,
  extraShockGain: r = 0,
  allowReliabilitySpend: s = !1,
  reliabilitySpendSelections: o = [],
  directConditionLocations: l = [],
  maxIterations: c = 10
} = {}) {
  var P, L, _, O, x, H, G, q;
  const u = Pr(t ?? {}), d = Jr((u == null ? void 0 : u.type) ?? (u == null ? void 0 : u.actorType)), f = eo(u.system ?? {}, d), m = Pr(((P = f.mwd) == null ? void 0 : P.locations) ?? {}), g = Math.max(0, Xe((_ = (L = f.attributes) == null ? void 0 : L.reliability) == null ? void 0 : _.value, 0));
  let p = Math.max(0, Xe((x = (O = f.mwd) == null ? void 0 : O.reliabilitySpendable) == null ? void 0 : x.value, g));
  const h = fT(g), b = Math.max(0, Xe(n, 0)), y = Math.max(0, Xe((G = (H = f.mwd) == null ? void 0 : H.shock) == null ? void 0 : G.value, 0)), S = a != null && Number.isFinite(Number(a)) ? Math.max(0, Xe(a, 0)) : b > 0 ? mT(i) : 0, A = Math.max(0, S + Math.max(0, Xe(r, 0)));
  let T = Math.max(0, y + A);
  const C = pR({
    locations: m,
    shockBefore: y,
    shockGain: A,
    threshold: h,
    reliability: g,
    spendableBefore: p
  }), k = String(e ?? "").trim();
  if (k && m[k] && b > 0) {
    const Z = Math.max(0, Xe((q = m[k]) == null ? void 0 : q.stress, 0)) + b;
    m[k].stress = Z, dT(C, k, b);
  }
  let w = 0, v = 0;
  const N = ({
    forcedLocationKey: ie = "",
    source: Z = "shock",
    applyReductions: Se = !0,
    allowSpendForThisAdvancement: se = !0
  } = {}) => {
    const te = mR(m, d, ie || k, {
      forcePreferred: !!ie
    });
    if (!te) return !1;
    const pe = m[te];
    C.summary.selectedLocations.push({ source: Z, location: te });
    const K = !!s && !!se && p > 0, be = K && cR(o, w);
    if (C.spendOpportunities.push({
      index: w,
      location: te,
      source: Z,
      canSpend: K,
      selected: be
    }), w += 1, be)
      p = Math.max(0, p - 1), C.reliabilitySpends.push({ location: te, prevented: !0, source: Z });
    else {
      const Ie = ya(pe.condition);
      if (Ie >= Zt.disabled)
        fR(
          C,
          yR({ actorSnapshot: u, unitType: d, locationKey: te }),
          te,
          m
        );
      else {
        const Oe = ya(Ie + 1);
        m[te].condition = Oe, C.conditionAdvancements.push({ location: te, from: Ie, to: Oe, source: Z });
      }
    }
    return Se && (T = gR({
      result: C,
      locations: m,
      locationKey: te,
      reliability: g,
      threshold: h,
      currentShock: T
    }), T = Math.max(0, T)), !0;
  };
  for (; T >= h && v < Math.max(1, Math.trunc(Xe(c, 10))); ) {
    const ie = N({ source: "shock" });
    if (v += 1, !ie) break;
  }
  v >= Math.max(1, Math.trunc(Xe(c, 10))) && T >= h && (C.loopGuardTriggered = !0);
  for (const ie of Array.isArray(l) ? l : []) {
    const Z = hR(ie, "critical");
    Z && N({
      forcedLocationKey: Z.locationKey,
      source: Z.source,
      applyReductions: Z.applyReductions,
      allowSpendForThisAdvancement: Z.allowSpend
    });
  }
  return C.shockDelta = Math.max(0, T) - y, C.summary.shockAfter = Math.max(0, T), C.summary.reliabilitySpendableAfter = p, C.summary.locationsAfter = Pr(m), C;
}
function AR(t = null, e = null) {
  var l, c, u, d, f;
  if (!t || !e) return {};
  const n = Jr(t), i = eo(Pr(t.system ?? {}), n), a = Pr(((l = i.mwd) == null ? void 0 : l.locations) ?? {}), r = {};
  for (const [m, g] of Object.entries(e.stressDelta ?? {})) {
    const p = a[m];
    p && (p.stress = Math.max(0, Math.max(0, Xe(p.stress, 0)) + Xe(g, 0)), r[`system.mwd.locations.${m}.stress`] = p.stress);
  }
  for (const m of Array.from(e.conditionAdvancements ?? [])) {
    const g = String((m == null ? void 0 : m.location) ?? "").trim(), p = a[g];
    p && (p.condition = ya((m == null ? void 0 : m.to) ?? p.condition), r[`system.mwd.locations.${g}.condition`] = p.condition);
  }
  for (const m of Array.from(e.fallbackEvents ?? [])) {
    const g = String((m == null ? void 0 : m.location) ?? "").trim(), p = a[g];
    p && m.destroyed && (p.destroyed = !0, r[`system.mwd.locations.${g}.destroyed`] = !0);
  }
  const s = Array.isArray(e.reliabilitySpends) ? e.reliabilitySpends.length : 0, o = Math.max(0, Xe((u = (c = i.mwd) == null ? void 0 : c.reliabilitySpendable) == null ? void 0 : u.value, 0));
  return r["system.mwd.reliabilitySpendable.value"] = Math.max(0, o - s), r["system.mwd.shock.value"] = Math.max(0, Xe((f = (d = i.mwd) == null ? void 0 : d.shock) == null ? void 0 : f.value, 0) + Xe(e.shockDelta, 0)), Object.assign(r, gT(t)), e.statusState && (r["system.mwd.status.state"] = e.statusState), r;
}
const pT = Object.freeze({
  penetrating: "Penetrating",
  concussive: "Concussive",
  energy: "Energy"
}), kR = Object.freeze(
  Object.entries(pT).map(([t, e]) => ({ value: t, label: e }))
), TR = Object.freeze({
  penetrating: "penetrating",
  p: "penetrating",
  ballistic: "penetrating",
  melee: "penetrating",
  kinetic: "concussive",
  concussive: "concussive",
  c: "concussive",
  explosive: "concussive",
  missile: "concussive",
  energy: "energy",
  thermal: "energy",
  electrical: "energy",
  electric: "energy",
  plasma: "energy",
  laser: "energy"
}), wR = Object.freeze(/* @__PURE__ */ new Set([
  "energy",
  "thermal",
  "electrical",
  "electric",
  "plasma",
  "laser"
])), MR = Object.freeze(/* @__PURE__ */ new Set([
  "penetrating",
  "ballistic",
  "melee"
]));
function Bl(t = "") {
  return String(t ?? "").trim().toLowerCase();
}
function vR(t = "") {
  const e = Bl(t);
  return e === "thermal" ? "thermal" : e === "electrical" || e === "electric" ? "electrical" : "";
}
function CR(t = [], e = "payload", n = null) {
  const i = new Set(t.map((s) => String((s == null ? void 0 : s.id) ?? "").trim()).filter(Boolean)), a = String(e ?? "payload").trim() || "payload";
  if (!i.has(a)) return a;
  if (typeof n == "function") {
    const s = String(n(a) ?? "").trim();
    if (s && !i.has(s)) return s;
  }
  let r = 2;
  for (; i.has(`${a}-${r}`); ) r += 1;
  return `${a}-${r}`;
}
function ei(t, e = "energy") {
  const n = Bl(t);
  return TR[n] ?? e;
}
function En(t) {
  const e = ei(t, "");
  return pT[e] ?? String(t ?? "").trim();
}
function tl(t) {
  const e = Bl(t);
  return wR.has(e) ? !0 : ei(e, "") === "energy";
}
function _b(t) {
  const e = Bl(t);
  return MR.has(e) ? !0 : ei(e, "") === "penetrating";
}
function to(t, e = "energy") {
  const n = Bl(t);
  return n === "support" || n === "omni" ? n : ei(n, e);
}
function Bb(t = {}, { idFactory: e = null } = {}) {
  const n = String((t == null ? void 0 : t.weaponCategory) ?? (t == null ? void 0 : t.category) ?? "ranged").trim() || "ranged", i = t == null ? void 0 : t.ammo, a = sa(t == null ? void 0 : t.payloads, { legacyAmmo: i, category: n }), r = vR(t == null ? void 0 : t.damageType), s = ei(t == null ? void 0 : t.damageType, "energy");
  let o = a, l = Oo(t == null ? void 0 : t.selectedPayloadId, o, { legacyAmmo: i, category: n });
  if (r) {
    let c = o.find(
      (u) => {
        var d;
        return (u == null ? void 0 : u.id) !== "unloaded" && ((d = u == null ? void 0 : u.modifies) == null ? void 0 : d.damageType) === r;
      }
    );
    c || (c = on({
      id: CR(o, r, e),
      label: In(r),
      modifies: { damageType: r },
      resolution: { resolverKey: "standard" },
      consumption: { amount: 1, sourceId: "" }
    }), o = [...o, c]), l = c.id;
  }
  return {
    damageType: s,
    payloads: o,
    selectedPayloadId: l,
    payloadDamageType: r
  };
}
const IR = Object.freeze(["small", "medium", "large"]), ls = Object.freeze({
  type: "energy",
  size: "small",
  location: "",
  itemId: ""
});
function Sa(t, e = "small") {
  const n = String(t ?? "").trim().toLowerCase();
  return IR.includes(n) ? n : e;
}
function Ec(t = null) {
  var n, i;
  const e = (i = (n = t == null ? void 0 : t.system) == null ? void 0 : n.mwd) == null ? void 0 : i.hardpoints;
  return e ? Array.isArray(e) ? [...e] : Object.values(e) : [];
}
function Aa(t = [], { defaultLocation: e = "", idFactory: n = null } = {}) {
  return (Array.isArray(t) ? t : Object.values(t && typeof t == "object" ? t : {})).map((a, r) => ({
    id: String((a == null ? void 0 : a.id) ?? `hardpoint-${r + 1}`).trim(),
    type: to((a == null ? void 0 : a.type) ?? ls.type, ls.type),
    size: Sa((a == null ? void 0 : a.size) ?? ls.size),
    location: String((a == null ? void 0 : a.location) ?? e ?? ls.location).trim(),
    itemId: String((a == null ? void 0 : a.itemId) ?? ls.itemId).trim()
  })).map((a, r) => ({
    ...a,
    id: a.id || (typeof n == "function" ? n(r) : `hardpoint-${r + 1}`)
  }));
}
function bi(t = null) {
  return Aa(Ec(t));
}
function ER(t = [], e = [], { defaultLocation: n = "" } = {}) {
  const i = Aa(t, { defaultLocation: n }), a = new Map(
    Aa(e, { defaultLocation: n }).map((r) => [r.id, r]).filter(([r]) => r)
  );
  return i.map((r) => {
    const s = a.get(r.id);
    return s ? {
      ...r,
      type: to(s.type, r.type),
      size: s.size,
      location: s.location || n,
      itemId: r.itemId
    } : r;
  });
}
function NR(t = [], e = {}, { defaultLocation: n = "", idFactory: i = null } = {}) {
  const a = Aa(t, { defaultLocation: n }), r = String((e == null ? void 0 : e.id) ?? "").trim() || (typeof i == "function" ? i(a.length) : `hardpoint-${a.length + 1}`);
  return [
    ...a,
    ...Aa([{
      ...ls,
      location: n,
      ...e,
      id: r
    }], { defaultLocation: n })
  ];
}
function PR(t = [], e = "", { defaultLocation: n = "" } = {}) {
  const i = String(e ?? "").trim(), a = Aa(t, { defaultLocation: n });
  let r = null;
  return { hardpoints: a.filter((o) => o.id !== i ? !0 : (r = o, !1)), removed: r };
}
function RR(t = [], e = "", n = "", { defaultLocation: i = "" } = {}) {
  const a = String(e ?? "").trim(), r = String(n ?? "").trim(), s = Aa(t, { defaultLocation: i });
  let o = !1;
  return { hardpoints: s.map((c) => {
    let u = c.itemId;
    return r && c.id !== a && c.itemId === r && (u = ""), c.id === a && (u = r), u !== c.itemId && (o = !0), {
      ...c,
      itemId: u
    };
  }), changed: o };
}
function DR(t = [], e = "", n = {}, { defaultLocation: i = "" } = {}) {
  const a = String(e ?? "").trim(), r = Aa(t, { defaultLocation: i });
  let s = !1;
  return { hardpoints: r.map((l) => {
    if (l.id !== a) return l;
    const c = {
      ...l,
      type: to((n == null ? void 0 : n.type) ?? l.type, l.type),
      size: Sa((n == null ? void 0 : n.size) ?? l.size, l.size),
      location: String((n == null ? void 0 : n.location) ?? l.location ?? i).trim() || i,
      itemId: l.itemId
    };
    return s || (s = JSON.stringify(c) !== JSON.stringify(l)), c;
  }), changed: s };
}
function nl(t = null, e = "") {
  const n = String(e ?? "").trim();
  return n ? bi(t).find((i) => i.itemId === n) ?? null : null;
}
function OR(t = null) {
  return new Set(
    bi(t).map((e) => e.itemId).filter(Boolean)
  );
}
function nd(t = null, { canonicalType: e = "" } = {}) {
  const n = String(e ?? "").trim(), i = new Map(
    Array.from((t == null ? void 0 : t.items) ?? []).map((r) => [String((r == null ? void 0 : r.id) ?? "").trim(), r]).filter(([r, s]) => r && s)
  ), a = /* @__PURE__ */ new Set();
  return bi(t).map((r) => {
    const s = String((r == null ? void 0 : r.itemId) ?? "").trim();
    return !s || a.has(s) ? null : (a.add(s), i.get(s) ?? null);
  }).filter((r) => r ? n ? String((r == null ? void 0 : r.canonicalType) ?? (r == null ? void 0 : r.type) ?? "").trim() === n : !0 : !1);
}
function hT(t = null) {
  var e;
  return ei(((e = t == null ? void 0 : t.system) == null ? void 0 : e.damageType) ?? "energy", "energy");
}
function bT(t = null) {
  var e;
  return Sa(((e = t == null ? void 0 : t.system) == null ? void 0 : e.size) ?? "small");
}
function xb(t = "") {
  return String(t ?? "").trim().toLowerCase().replace(/[^a-z0-9]+/g, "");
}
function Tp(t = null, e = "") {
  var r;
  const n = (r = t == null ? void 0 : t.system) == null ? void 0 : r.keywords, i = Array.isArray(n) ? n : String(n ?? "").split(","), a = xb(e);
  return a ? i.some((s) => xb(s) === a) : !1;
}
function yT(t = "energy", e = null) {
  return t === "support" ? Tp(e, "support") : t === "omni" ? !0 : t === hT(e);
}
function wp(t = {}, e = null) {
  if (String((e == null ? void 0 : e.canonicalType) ?? (e == null ? void 0 : e.type) ?? "").trim() !== M.itemType.mechWeapon) return !1;
  const i = to((t == null ? void 0 : t.type) ?? "energy", "energy"), a = Sa((t == null ? void 0 : t.size) ?? "small"), r = bT(e);
  return yT(i, e) && a === r;
}
function LR(t = {}, e = null) {
  if (String((e == null ? void 0 : e.canonicalType) ?? (e == null ? void 0 : e.type) ?? "").trim() !== M.itemType.mechWeapon)
    return "Only mech weapons can be mounted in hardpoint slots.";
  const i = to((t == null ? void 0 : t.type) ?? "energy", "energy"), a = Sa((t == null ? void 0 : t.size) ?? "small"), r = hT(e), s = bT(e);
  return yT(i, e) ? a !== s ? `${(e == null ? void 0 : e.name) ?? "That weapon"} is ${ce(s)} and cannot fit a ${ce(a)} slot.` : "" : i === "support" ? `${(e == null ? void 0 : e.name) ?? "That weapon"} needs the Support keyword to fit a Support slot.` : `${(e == null ? void 0 : e.name) ?? "That weapon"} is ${ce(r)} and cannot fit a ${ce(i)} slot.`;
}
function il(t) {
  return Array.isArray(t) ? t : [];
}
function Ze(t = "") {
  return String(t ?? "").trim();
}
function lu(t = "") {
  const e = String(t ?? "").trim().toLowerCase();
  return e ? ["head", "cockpit"].includes(e) ? "head" : ["arm", "arms", "leftarm", "rightarm", "left-arm", "right-arm"].includes(e) ? "arms" : ["leg", "legs", "leftleg", "rightleg", "left-leg", "right-leg"].includes(e) ? "legs" : ["torso", "centertorso", "lefttorso", "righttorso", "center-torso", "left-torso", "right-torso", "core"].includes(e) ? "torso" : ["turret", "weapon", "weapons"].includes(e) ? "turret" : ["mobility", "motive", "drive", "wheel", "track", "tracks", "rotor"].includes(e) ? "mobility" : ["body", "front", "side", "rear"].includes(e) ? "body" : e : "";
}
function id(t = null) {
  var n, i, a;
  const e = Array.isArray((n = t == null ? void 0 : t.system) == null ? void 0 : n.weaponGroups) ? t.system.weaponGroups : [];
  return e.length ? e : Array.isArray((a = (i = t == null ? void 0 : t.system) == null ? void 0 : i.mwd) == null ? void 0 : a.weaponGroupDetails) ? t.system.mwd.weaponGroupDetails : [];
}
function al(t = null, e = null) {
  const n = il(e == null ? void 0 : e.weaponIds).map(Ze).filter(Boolean);
  return n.length ? n.map((i) => {
    var a, r;
    return (r = (a = t == null ? void 0 : t.items) == null ? void 0 : a.get) == null ? void 0 : r.call(a, i);
  }).filter(Boolean) : il(e == null ? void 0 : e.weapons).filter(Boolean);
}
function ST(t = {}) {
  const e = (t == null ? void 0 : t.system) ?? t ?? {}, n = [
    e.baseDamageType,
    e.damageType,
    e.category,
    e.weaponCategory,
    e.attackType
  ].map((a) => String(a ?? "").trim().toLowerCase()).filter(Boolean), i = il(e.traits).map((a) => String(a ?? "").trim().toLowerCase());
  return n.some(tl) || i.some(tl);
}
function AT(t = {}) {
  const e = (t == null ? void 0 : t.system) ?? t ?? {}, n = [
    e.baseDamageType,
    e.damageType,
    e.category,
    e.weaponCategory,
    e.attackType
  ].map((a) => String(a ?? "").trim().toLowerCase()).filter(Boolean), i = il(e.traits).map((a) => String(a ?? "").trim().toLowerCase());
  return n.some(_b) || i.some(_b);
}
function $R(t = {}, e = null, n = "") {
  const i = lu(n);
  return i ? al(e, t).some(
    (a) => {
      var r;
      return lu((r = nl(e, a == null ? void 0 : a.id)) == null ? void 0 : r.location) === i;
    }
  ) : !1;
}
function kT(t = null, { statusId: e = "", locationFamily: n = "" } = {}) {
  const i = id(t);
  if (!i.length)
    return {
      weaponGroupId: "",
      weaponGroupName: "",
      weaponIds: [],
      automationMode: "callout"
    };
  const a = e === "jammedBallistic" ? i.filter((s) => al(t, s).some(AT)) : i;
  if (!a.length)
    return {
      weaponGroupId: "",
      weaponGroupName: "",
      weaponIds: [],
      automationMode: "callout"
    };
  const r = a.find((s) => $R(s, t, n)) ?? a[0] ?? null;
  return {
    weaponGroupId: Ze(r == null ? void 0 : r.id),
    weaponGroupName: String((r == null ? void 0 : r.name) ?? "").trim(),
    weaponIds: al(t, r).map((s) => Ze(s == null ? void 0 : s.id)).filter(Boolean),
    automationMode: r ? "engine" : "callout"
  };
}
function Mp(t = {}) {
  const e = String((t == null ? void 0 : t.statusLabel) ?? "").trim();
  if (e) return e;
  const n = Ze(t == null ? void 0 : t.statusId);
  return n === "staggeredMechanical" ? "Staggered" : n === "proneMechFall" ? "Prone" : ce(n || (t == null ? void 0 : t.key) || "Critical");
}
function ad(t = {}) {
  const e = String((t == null ? void 0 : t.effectText) ?? "").trim();
  if (e) return e;
  const n = Ze(t == null ? void 0 : t.key), i = Ze(t == null ? void 0 : t.statusId), a = String((t == null ? void 0 : t.weaponGroupName) ?? "").trim(), r = a ? ` (${a})` : "";
  return n === "targetingProcessorLock" ? "All fire modes require +1 SA to attack." : n === "opticsCoolantFog" ? "No attacks beyond Close until repaired." : i === "staggeredMechanical" ? "At the start of activation, immediately spend 1 Simple Action fighting the controls." : i === "stalled" ? "-1 Handling and -1 System." : i === "unstable" ? "-2 dice to Piloting tests. Each move action is followed by a piloting roll to avoid falling prone." : i === "overheating" ? "At the start of the turn, gain 2 Heat." : i === "reactorInstability" ? "Energy weapon groups produce +1 Heat and -1 damage." : i === "weaponFailure" ? `Weapon group unusable for attacks${r}.` : i === "jammedBallistic" ? `Ballistic attacks from this weapon group cannot be used${r}.` : i === "skidding" ? "At the start of the turn, make an immediate full move in the last direction of travel; collision ends the skid and leaves the machine prone." : i === "limping" ? "-30 m movement and +1 Piloting DN until repaired." : i === "proneMechFall" ? "The machine immediately falls prone." : n === "sensorOverload" ? "Contacts are capped at Track until repaired." : n === "opticsFracture" ? "All targets are treated as Obscured until repaired." : n === "commsSensorSuiteOut" ? "No sensor or ECM actions may be taken until repaired." : n === "balanceTimingFault" ? "Movement actions require manual piloting-fall follow-up." : n === "mobilityOverstress" ? "Call out the mobility overstress rider after movement actions." : n === "legStabilizerDamage" ? "Advanced maneuver penalties remain reminder-only for now." : "";
}
function vp(t = {}) {
  const e = String((t == null ? void 0 : t.automationMode) ?? "").trim();
  if (e === "engine" || e === "callout") return e;
  const n = Ze(t == null ? void 0 : t.key), i = Ze(t == null ? void 0 : t.statusId);
  return n === "targetingProcessorLock" || n === "opticsCoolantFog" || ["staggeredMechanical", "stalled", "unstable", "overheating", "reactorInstability", "weaponFailure", "jammedBallistic"].includes(i) ? "engine" : "callout";
}
function _R(t = {}) {
  const e = String((t == null ? void 0 : t.weaponGroupName) ?? "").trim();
  if (e) return `Weapon Group: ${e}`;
  const n = lu(t == null ? void 0 : t.locationFamily);
  return n ? `Location: ${ce(n)}` : "";
}
function BR(t = null, e = {}, n = {}) {
  const i = String((e == null ? void 0 : e.key) ?? "").trim(), a = String((e == null ? void 0 : e.statusId) ?? "").trim(), r = ["weaponFailure", "jammedBallistic"].includes(a) ? kT(t, { statusId: a, locationFamily: (n == null ? void 0 : n.locationFamily) ?? "" }) : { weaponGroupId: "", weaponGroupName: "", weaponIds: [], automationMode: "" };
  return {
    statusLabel: String((e == null ? void 0 : e.statusLabel) ?? "").trim() || Mp({ key: i, statusId: a }),
    effectText: String((e == null ? void 0 : e.effectText) ?? "").trim() || ad({
      key: i,
      statusId: a,
      weaponGroupName: r.weaponGroupName
    }),
    automationMode: String((e == null ? void 0 : e.automationMode) ?? "").trim() || r.automationMode || vp({ key: i, statusId: a }),
    weaponGroupId: r.weaponGroupId,
    weaponGroupName: r.weaponGroupName,
    weaponIds: r.weaponIds
  };
}
function TT(t = {}, e = null) {
  const n = ["weaponFailure", "jammedBallistic"].includes(String((t == null ? void 0 : t.statusId) ?? "").trim()) && !String((t == null ? void 0 : t.weaponGroupId) ?? "").trim() ? kT(e, {
    statusId: t == null ? void 0 : t.statusId,
    locationFamily: t == null ? void 0 : t.locationFamily
  }) : {
    weaponGroupId: String((t == null ? void 0 : t.weaponGroupId) ?? "").trim(),
    weaponGroupName: String((t == null ? void 0 : t.weaponGroupName) ?? "").trim(),
    weaponIds: il(t == null ? void 0 : t.weaponIds).map((i) => String(i ?? "").trim()).filter(Boolean),
    automationMode: ""
  };
  return {
    ...t,
    statusLabel: String((t == null ? void 0 : t.statusLabel) ?? "").trim() || Mp(t),
    effectText: String((t == null ? void 0 : t.effectText) ?? "").trim() || ad({
      ...t,
      weaponGroupName: n.weaponGroupName
    }),
    automationMode: String((t == null ? void 0 : t.automationMode) ?? "").trim() || n.automationMode || vp(t),
    weaponGroupId: n.weaponGroupId,
    weaponGroupName: n.weaponGroupName,
    weaponIds: n.weaponIds
  };
}
function rl(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
const zb = "machineMonitorStorage", xR = "remaining-v1";
function zR(t, e = 0) {
  const n = Math.max(0, rl(e, 0));
  return Math.min(n, Math.max(0, rl(t, 0)));
}
function Fb(t = null, e = "") {
  var r, s;
  const n = ((s = (r = t == null ? void 0 : t.system) == null ? void 0 : r.monitors) == null ? void 0 : s[e]) ?? {}, i = Math.max(0, rl(n.max, 0)), a = zR(n.value, i);
  return {
    max: i,
    value: a,
    remaining: a,
    damageTaken: Math.max(0, i - a)
  };
}
function FR(t = 0, e = 0) {
  const n = Math.max(0, rl(t, 0)), i = Math.max(0, rl(e, 0));
  return n === i ? Math.max(0, i - 1) : i;
}
function Ta(t = null) {
  const e = typeof t == "string" ? t : t == null ? void 0 : t.type;
  return e === "vehicle" || e === "battlemech";
}
const Ub = 10, UR = Object.freeze(["personal", "machine"]), KR = new Set(UR);
function Us(t = "", e = "personal") {
  const n = String(t ?? "").trim().toLowerCase();
  return KR.has(n) ? n : e;
}
function HR(t = 0) {
  const e = Number(t);
  return Number.isFinite(e) ? Math.max(0, e) : 0;
}
function Cp({
  damage: t = 0,
  sourceScale: e = "personal",
  targetScale: n = "personal"
} = {}) {
  const i = HR(t), a = Us(e, "personal"), r = Us(n, "personal");
  let s = i, o = 1;
  return a === "machine" && r === "personal" ? (o = Ub, s = i * o) : a === "personal" && r === "machine" && (o = 1 / Ub, s = Math.floor(i * o)), {
    sourceScale: a,
    targetScale: r,
    original: i,
    converted: Math.max(0, s),
    factor: o,
    changed: a !== r
  };
}
const wT = "machineCritical", MT = "machineCriticalTableGeneralUuid", jR = "machineCriticalTableBattlemechUuid", GR = "machineCriticalTableVehicleUuid", vT = "machineCriticalTableBattlemechHeadUuid", CT = "machineCriticalTableBattlemechTorsoUuid", IT = "machineCriticalTableBattlemechArmsUuid", ET = "machineCriticalTableBattlemechLegsUuid", NT = "machineCriticalTableVehicleBodyUuid", PT = "machineCriticalTableVehicleTurretUuid", RT = "machineCriticalTableVehicleMobilityUuid", Vt = Object.freeze({
  general: "Compendium.mwd.critical-hit-tables.RollTable.MWDGeneralCrit01",
  battlemech: "Compendium.mwd.critical-hit-tables.RollTable.MWDMechTorsoCrit01",
  vehicle: "Compendium.mwd.critical-hit-tables.RollTable.MWDVehicleBodyCrit1",
  mechHead: "Compendium.mwd.critical-hit-tables.RollTable.MWDMechHeadCrit01",
  mechTorso: "Compendium.mwd.critical-hit-tables.RollTable.MWDMechTorsoCrit01",
  mechArms: "Compendium.mwd.critical-hit-tables.RollTable.MWDMechArmsCrit01",
  mechLegs: "Compendium.mwd.critical-hit-tables.RollTable.MWDMechLegsCrit01",
  vehicleBody: "Compendium.mwd.critical-hit-tables.RollTable.MWDVehicleBodyCrit1",
  vehicleTurret: "Compendium.mwd.critical-hit-tables.RollTable.MWDVehicleTurretCrit",
  vehicleMobility: "Compendium.mwd.critical-hit-tables.RollTable.MWDVehicleMobility"
}), qR = /* @__PURE__ */ new Set(["physical", "fatigue", ""]), bf = Object.freeze({
  2: Object.freeze({ key: "catastrophicCascade", remedyKey: "none", gates: [], mods: [], resourceEffects: {}, pilotDamage: {}, escalationKey: "cascade" }),
  3: Object.freeze({ key: "hardLock", remedyKey: "emergencyRepair", gates: [], mods: [], resourceEffects: {}, pilotDamage: {}, escalationKey: "lockout" }),
  4: Object.freeze({ key: "powerSurge", remedyKey: "coolantDump", gates: [], mods: [], resourceEffects: {}, pilotDamage: {}, escalationKey: "surge" }),
  5: Object.freeze({ key: "feedFlowDisruption", remedyKey: "feedReset", gates: [], mods: [], resourceEffects: {}, pilotDamage: {}, escalationKey: "feed" }),
  6: Object.freeze({ key: "controlFault", remedyKey: "systemReset", gates: [], mods: [], resourceEffects: {}, pilotDamage: {}, escalationKey: "control" }),
  7: Object.freeze({ key: "systemDesync", remedyKey: "systemReset", gates: [], mods: [], resourceEffects: {}, pilotDamage: {}, escalationKey: "desync" }),
  8: Object.freeze({ key: "structuralShock", remedyKey: "emergencyRepair", gates: [], mods: [], resourceEffects: {}, pilotDamage: {}, escalationKey: "shock" }),
  9: Object.freeze({ key: "overload", remedyKey: "coolantDump", gates: [], mods: [], resourceEffects: {}, pilotDamage: {}, escalationKey: "overload" }),
  10: Object.freeze({ key: "degradationSpike", remedyKey: "emergencyRepair", gates: [], mods: [], resourceEffects: {}, pilotDamage: {}, escalationKey: "degradation" }),
  11: Object.freeze({ key: "partialOutage", remedyKey: "emergencyRepair", gates: [], mods: [], resourceEffects: {}, pilotDamage: {}, escalationKey: "outage" }),
  12: Object.freeze({ key: "criticalBreach", remedyKey: "none", gates: [], mods: [], resourceEffects: {}, pilotDamage: {}, escalationKey: "conditionAdvance" })
}), Kb = Object.freeze({
  battlemech: Object.freeze({
    head: Object.freeze({
      2: ee("cockpitShock", "Cockpit Shock", "none", ["sensor"], ["sensorBlind"], {}, { track: "physical", amount: 3 }, "cascade", "sensorBlind"),
      3: ee("targetingProcessorLock", "Targeting Processor Lock", "reboot", ["attack"], [], { extraAttackCost: 1 }, { track: "physical", amount: 2 }, "lockout", "", "All fire modes require +1 SA to attack.", "engine"),
      4: ee("neuralFeedback", "Neural Feedback", "systemReset", [], [], {}, { track: "fatigue", amount: 1 }, "surge", "staggeredMechanical"),
      5: ee("opticsCoolantFog", "Optics Coolant Fog / View Obstruction", "coolantDump", ["attack"], ["rangeLimitClose"], {}, {}, "feed", "", "No attacks beyond Close until repaired.", "engine"),
      6: ee("commandInputDelay", "Command Input Delay", "reboot", [], [], {}, { track: "fatigue", amount: 2 }, "control", "stalled"),
      7: ee("fireControlDesyncHead", "Fire-Control Desync", "systemReset", ["attack"], ["noCqBonus"], {}, {}, "desync", "sensorDegraded"),
      8: ee("cockpitImpact", "Cockpit Impact", "pilotRecovery", ["piloting"], ["stabilityCheck"], {}, { track: "physical", amount: 2 }, "shock", "unstable"),
      9: ee("sensorOverload", "Sensor Overload", "systemReset", ["sensor"], [], {}, { track: "fatigue", amount: 2 }, "overload", "", "Contacts are capped at Track until repaired.", "callout"),
      10: ee("opticsFracture", "Optics Fracture", "emergencyRepair", ["attack"], [], {}, {}, "degradation", "", "All targets are treated as Obscured until repaired.", "callout"),
      11: ee("commsSensorSuiteOut", "Communications / Sensor Suite Out", "systemReset", ["sensor"], [], {}, {}, "outage", "", "No sensor or ECM actions may be taken until repaired.", "callout"),
      12: ee("headCriticalBreach", "Head condition +1", "none", [], [], {}, { track: "physical", amount: 2 }, "conditionAdvance", "")
    }),
    torso: Object.freeze({
      2: ee("reactorGyroCascade", "Reactor / Gyro Cascade", "none", ["piloting"], ["stabilityCheck"], {}, { track: "fatigue", amount: 3 }, "cascade", "unstable"),
      3: ee("gyroLock", "Gyro Lock", "emergencyRepair", ["move", "jump"], ["pilotingPenalty"], {}, {}, "lockout", "stalled"),
      4: ee("reactorUnstable", "Reactor Unstable", "coolantDump", ["energyWeapon"], [], { heatPerEnergyAttack: 1 }, { track: "fatigue", amount: 2 }, "heat", "reactorInstability"),
      5: ee("powerRoutingFault", "Power Routing Fault", "emergencyRepair", ["weaponGroup"], [], {}, {}, "feed", "weaponFailure"),
      6: ee("coreResponseDelay", "Core Response Delay", "reboot", [], [], {}, { track: "fatigue", amount: 2 }, "control", "staggeredMechanical"),
      7: ee("targetingMovementSyncFault", "Targeting / Movement Sync Fault", "systemReset", ["attack", "move"], ["noMovementFireAdvantage"], {}, {}, "desync", ""),
      8: ee("internalShock", "Internal Shock", "emergencyRepair", ["piloting"], ["stabilityCheck"], {}, { track: "physical", amount: 2, condition: "failedFallImpact" }, "shock", "unstable"),
      9: ee("heatSinkSaturation", "Heat Sink Saturation", "coolantDump", ["attack"], [], {}, {}, "heat", "overheating"),
      10: ee("gyroDrift", "Gyro Drift", "emergencyRepair", ["move"], ["highMobilityBlocked"], {}, {}, "degradation", "limping"),
      11: ee("powerBusOutage", "Power Bus Outage", "emergencyRepair", ["subsystem"], [], {}, {}, "outage", ""),
      12: ee("torsoCriticalBreach", "Torso condition +1", "none", [], [], {}, {}, "conditionAdvance", "")
    }),
    arms: Object.freeze({
      2: ee("weaponMountCascade", "Weapon Mount Cascade", "emergencyRepair", ["attack"], [], {}, {}, "cascade", "weaponFailure"),
      3: ee("actuatorLockArm", "Actuator Lock", "emergencyRepair", ["attack"], [], {}, {}, "lockout", "weaponFailure"),
      4: ee("weaponFeedback", "Weapon Feedback", "emergencyRepair", ["attack"], [], {}, {}, "surge", "", "Arm-specific recoil and feedback effects remain reminder-only.", "callout"),
      5: ee("ammoFeedFaultArm", "Ammo / Feed Fault", "feedReset", ["attack"], [], {}, {}, "feed", "jammedBallistic"),
      6: ee("fineActuationError", "Fine Actuation Error", "emergencyRepair", ["attack"], [], {}, {}, "control", "", "Fine-control arm effects remain reminder-only.", "callout"),
      7: ee("targetingMisalignmentArm", "Targeting Misalignment", "systemReset", ["attack"], [], {}, {}, "desync", "", "Arm-mounted targeting issues remain reminder-only.", "callout"),
      8: ee("recoilShock", "Recoil Shock", "emergencyRepair", ["attack"], [], {}, {}, "shock", "", "Recoil shock remains reminder-only for the affected arm.", "callout"),
      9: ee("servoStrainArm", "Servo Strain", "emergencyRepair", ["attack"], [], {}, {}, "overload", "", "Servo strain remains reminder-only for the affected arm.", "callout"),
      10: ee("stabilizerDamageArm", "Stabilizer Damage", "emergencyRepair", ["attack"], [], {}, {}, "degradation", "", "Stabilizer damage remains reminder-only for the affected arm.", "callout"),
      11: ee("localPowerLossArm", "Local Power Loss", "emergencyRepair", ["subsystem"], [], {}, {}, "outage", "", "Local arm power-loss effects remain reminder-only.", "callout"),
      12: ee("armsCriticalBreach", "Arms condition +1", "none", [], [], {}, {}, "conditionAdvance", "")
    }),
    legs: Object.freeze({
      2: ee("mobilityCascadeLegs", "Mobility Cascade", "emergencyRepair", ["move"], [], {}, { track: "physical", amount: 2, condition: "resultingFallImpact" }, "cascade", "proneMechFall"),
      3: ee("legActuatorLock", "Leg Actuator Lock", "emergencyRepair", ["move", "jump"], [], {}, {}, "lockout", "limping"),
      4: ee("myomerSurge", "Myomer Surge", "coolantDump", ["move"], [], { heatOrStrainOnMove: 1 }, {}, "surge", ""),
      5: ee("jumpJetMobilityFeedFault", "Jump Jet / Mobility Feed Fault", "emergencyRepair", ["move", "jump"], [], {}, {}, "feed", "jumpJetFailure"),
      6: ee("gaitFault", "Gait Fault", "emergencyRepair", ["move"], [], {}, {}, "control", "unstable"),
      7: ee("balanceTimingFault", "Balance Timing Fault", "systemReset", ["move"], [], {}, {}, "desync", "", "Movement actions require manual piloting-fall follow-up.", "callout"),
      8: ee("forcedStabilityTest", "Forced Stability Test", "emergencyRepair", ["move", "piloting"], ["stabilityCheck"], {}, { track: "physical", amount: 2, condition: "resultingFallImpact" }, "shock", "unstable"),
      9: ee("mobilityOverstress", "Mobility Overstress", "emergencyRepair", ["move"], [], {}, {}, "overload", "", "Call out the mobility overstress rider after movement actions.", "callout"),
      10: ee("legStabilizerDamage", "Leg Stabilizer Damage", "emergencyRepair", ["move"], [], {}, {}, "degradation", "", "Advanced maneuver penalties remain reminder-only for now.", "callout"),
      11: ee("partialMobilityOutageLegs", "Partial Mobility Outage", "emergencyRepair", ["move"], [], {}, {}, "outage", "limping"),
      12: ee("legsCriticalBreach", "Legs condition +1", "none", [], [], {}, {}, "conditionAdvance", "")
    })
  }),
  vehicle: Object.freeze({
    body: Object.freeze({
      2: ee("internalSystemsCascade", "Internal Systems Cascade", "none", [], [], {}, { track: "physical", amount: 2, condition: "openToppedOrCatastrophic" }, "cascade", ""),
      3: ee("coreSystemsLock", "Core Systems Lock", "systemReset", ["subsystem"], [], {}, {}, "lockout", ""),
      4: ee("enginePowerSurge", "Engine / Power Surge", "coolantDump", [], [], {}, { track: "fatigue", amount: 2, condition: "crewApplicable" }, "surge", ""),
      5: ee("fuelFeedDisruption", "Fuel / Feed Disruption", "emergencyRepair", ["subsystem"], [], {}, {}, "feed", ""),
      6: ee("controlFaultBody", "Control Fault", "systemReset", ["move"], [], {}, {}, "control", "stalled"),
      7: ee("systemsDesyncBody", "Systems Desync", "systemReset", ["attack", "move"], [], {}, {}, "desync", "stalled"),
      8: ee("structuralShockBody", "Structural Shock", "emergencyRepair", ["piloting"], ["controlTest"], {}, { track: "physical", amount: 2, condition: "crashImpact" }, "shock", "unstable"),
      9: ee("overloadBody", "Overload", "coolantDump", [], [], {}, {}, "overload", ""),
      10: ee("hullStressSpike", "Hull Stress Spike", "emergencyRepair", [], [], {}, {}, "degradation", ""),
      11: ee("partialOutageBody", "Partial Outage", "emergencyRepair", ["subsystem"], [], {}, {}, "outage", ""),
      12: ee("bodyCriticalBreach", "Body condition +1", "none", [], [], {}, {}, "conditionAdvance", "")
    }),
    turret: Object.freeze({
      2: ee("turretWeaponCascade", "Turret Weapon Cascade", "emergencyRepair", ["attack"], [], {}, {}, "cascade", "weaponFailure"),
      3: ee("traverseLock", "Traverse Lock", "emergencyRepair", ["attack"], [], {}, {}, "lockout", "weaponFailure"),
      4: ee("fireControlSurgeTurret", "Fire-Control Surge", "systemReset", ["attack"], [], { heatOrStrainOnTurretAttack: 1 }, {}, "surge", "weaponFailure"),
      5: ee("ammoFeedFaultTurret", "Ammo / Feed Fault", "feedReset", ["attack"], [], {}, {}, "feed", "jammedBallistic"),
      6: ee("controlFaultTurret", "Control Fault", "systemReset", ["attack"], [], { extraAttackCost: 1 }, {}, "control", "weaponFailure"),
      7: ee("trackingDesyncTurret", "Tracking Desync", "systemReset", ["attack"], ["noCqBonus"], {}, {}, "desync", "sensorDegraded"),
      8: ee("mountShockTurret", "Mount Shock", "emergencyRepair", ["attack"], ["nextTurretAttackBlocked"], {}, {}, "shock", "weaponFailure"),
      9: ee("overloadTurret", "Overload", "emergencyRepair", ["attack"], [], { turretAttackStress: 1 }, {}, "overload", "weaponFailure"),
      10: ee("stabilizerDamageTurret", "Stabilizer Damage", "emergencyRepair", ["attack"], ["limitedArcFire"], {}, {}, "degradation", "weaponFailure"),
      11: ee("turretSubsystemOutage", "Turret Subsystem Outage", "emergencyRepair", ["subsystem"], [], {}, {}, "outage", ""),
      12: ee("turretCriticalBreach", "Turret condition +1", "none", [], [], {}, {}, "conditionAdvance", "")
    }),
    mobility: Object.freeze({
      2: ee("mobilityCascadeVehicle", "Mobility Cascade", "emergencyRepair", ["move"], ["skidStallCrashRisk"], {}, {}, "cascade", "stalled"),
      3: ee("driveLock", "Drive / Track / Wheel Lock", "emergencyRepair", ["move"], [], {}, {}, "lockout", "stalled"),
      4: ee("powertrainSurge", "Powertrain Surge", "coolantDump", ["move"], [], { heatOrStrainOnMove: 1 }, {}, "surge", ""),
      5: ee("transmissionRotorFeedFault", "Transmission / Rotor Feed Fault", "emergencyRepair", ["move"], [], {}, {}, "feed", "stalled"),
      6: ee("steeringFault", "Steering Fault", "emergencyRepair", ["move"], [], {}, {}, "control", "stalled"),
      7: ee("handlingDesync", "Handling Desync", "systemReset", ["move"], ["noMobilityCqBonus"], {}, {}, "desync", "unstable"),
      8: ee("chassisShock", "Chassis Shock", "emergencyRepair", ["move", "piloting"], ["controlTest"], {}, {}, "shock", "unstable"),
      9: ee("overloadMobility", "Overload", "emergencyRepair", ["move"], [], { repeatedMoveCost: 1 }, {}, "overload", "limping"),
      10: ee("suspensionLiftDamage", "Suspension / Lift Damage", "emergencyRepair", ["move"], ["majorHandlingImpairment"], {}, {}, "degradation", "stalled"),
      11: ee("partialMobilityOutageVehicle", "Partial Mobility Outage", "emergencyRepair", ["move"], [], {}, {}, "outage", "stalled"),
      12: ee("mobilityCriticalBreach", "Mobility condition +1", "none", [], [], {}, {}, "conditionAdvance", "")
    })
  })
});
function DT() {
  return typeof foundry < "u" && (foundry == null ? void 0 : foundry.utils);
}
function Qa(t) {
  return DT() && typeof foundry.utils.deepClone == "function" ? foundry.utils.deepClone(t) : JSON.parse(JSON.stringify(t ?? null));
}
function ee(t, e, n, i = [], a = [], r = {}, s = {}, o = "", l = "", c = "", u = "", d = "") {
  return Object.freeze({
    label: e,
    signal: Object.freeze({
      key: t,
      remedyKey: n,
      gates: i,
      mods: a,
      resourceEffects: r,
      pilotDamage: s,
      escalationKey: o,
      statusId: l,
      effectText: c,
      automationMode: u,
      statusLabel: d
    })
  });
}
function WR() {
  return DT() && typeof foundry.utils.randomID == "function" ? foundry.utils.randomID() : Math.random().toString(36).slice(2, 18).padEnd(16, "0").slice(0, 16);
}
function VR() {
  try {
    return (/* @__PURE__ */ new Date()).toISOString();
  } catch {
    return "";
  }
}
function Hb(t) {
  return Array.isArray(t) ? t : typeof t == "string" ? t.split(",").map((e) => e.trim()).filter(Boolean) : [];
}
function jb(t) {
  return t && typeof t == "object" && !Array.isArray(t) ? t : {};
}
function OT(t = {}) {
  var e, n, i, a, r, s, o, l;
  return ((n = (e = t == null ? void 0 : t.flags) == null ? void 0 : e.mwd) == null ? void 0 : n.crit) ?? ((r = (a = (i = t == null ? void 0 : t.document) == null ? void 0 : i.flags) == null ? void 0 : a.mwd) == null ? void 0 : r.crit) ?? ((l = (o = (s = t == null ? void 0 : t.data) == null ? void 0 : s.flags) == null ? void 0 : o.mwd) == null ? void 0 : l.crit) ?? t;
}
function JR(t, e, n) {
  if (!n) return null;
  const i = new Error(t);
  throw i.validationErrors = e.length ? e : [t], i;
}
function Gb(t = 7) {
  const e = Math.min(12, Math.max(2, Math.trunc(Number(t ?? 7)) || 7));
  return Qa(bf[e] ?? bf[7]);
}
function pa(t = {}, { strict: e = !1 } = {}) {
  const n = OT(t), i = [], a = String((n == null ? void 0 : n.key) ?? "").trim(), r = String((n == null ? void 0 : n.remedyKey) ?? "emergencyRepair").trim() || "emergencyRepair", s = Hb(n == null ? void 0 : n.gates).map((y) => String(y ?? "").trim()).filter(Boolean), o = Hb(n == null ? void 0 : n.mods).map((y) => String(y ?? "").trim()).filter(Boolean), l = jb(n == null ? void 0 : n.resourceEffects), c = jb(n == null ? void 0 : n.pilotDamage), u = String((n == null ? void 0 : n.escalationKey) ?? "").trim(), d = String((n == null ? void 0 : n.statusId) ?? "").trim(), f = String((n == null ? void 0 : n.statusLabel) ?? "").trim(), m = String((n == null ? void 0 : n.effectText) ?? "").trim(), g = String((n == null ? void 0 : n.automationMode) ?? "").trim();
  a || i.push("Critical signal key cannot be blank."), WP(r) || i.push(`Unknown machine critical remedy "${r}".`);
  for (const [y, S] of Object.entries(l))
    Number.isFinite(Number(S)) || i.push(`Resource effect "${y}" must be numeric.`);
  const p = String((c == null ? void 0 : c.track) ?? "").trim(), h = Number((c == null ? void 0 : c.amount) ?? 0), b = String((c == null ? void 0 : c.condition) ?? "").trim();
  return qR.has(p) || i.push(`Pilot damage track "${p}" is invalid.`), (!Number.isFinite(h) || h < 0) && i.push("Pilot damage amount must be non-negative."), i.length ? (JR(i[0], i, e), null) : {
    key: a,
    remedyKey: r,
    gates: s,
    mods: o,
    resourceEffects: Object.fromEntries(
      Object.entries(l).map(([y, S]) => [String(y), Number(S)])
    ),
    pilotDamage: p || h || b ? {
      track: p || "fatigue",
      amount: Math.trunc(h),
      ...b ? { condition: b } : {}
    } : {},
    escalationKey: u,
    statusId: d,
    statusLabel: f,
    effectText: m,
    automationMode: g
  };
}
function tr(t, e = {}) {
  var i, a;
  return (Array.isArray((a = (i = t == null ? void 0 : t.system) == null ? void 0 : i.mwd) == null ? void 0 : a.crits) ? t.system.mwd.crits : []).map((r) => TT(r, t)).filter((r) => r && r.active !== !1).filter((r) => !e.key || r.key === e.key).filter((r) => !e.locationKey || r.locationKey === e.locationKey).filter((r) => !e.locationFamily || r.locationFamily === e.locationFamily).filter((r) => !e.gate || Array.isArray(r.gates) && r.gates.includes(e.gate)).filter((r) => !e.mod || Array.isArray(r.mods) && r.mods.includes(e.mod));
}
function YR(t) {
  return (t == null ? void 0 : t.type) === M.actorTypes.vehicle || (t == null ? void 0 : t.type) === M.actorTypes.battlemech;
}
function QR(t, e, n, i) {
  const a = e != null && e.hitLocation && typeof e.hitLocation == "object" ? e.hitLocation : lT({
    actor: t,
    rollTotal: e == null ? void 0 : e.hitLocationRollTotal,
    armorBefore: n,
    structureBefore: i
  });
  return {
    ...a,
    impactLabel: String((a == null ? void 0 : a.impactLabel) ?? (a == null ? void 0 : a.locationLabel) ?? $i(a == null ? void 0 : a.locationKey)).trim(),
    rulesLocation: String((a == null ? void 0 : a.rulesLocation) ?? (a == null ? void 0 : a.locationFamily) ?? (a == null ? void 0 : a.locationKey) ?? "").trim(),
    rulesLocationLabel: String((a == null ? void 0 : a.rulesLocationLabel) ?? $i((a == null ? void 0 : a.rulesLocation) ?? (a == null ? void 0 : a.locationFamily) ?? (a == null ? void 0 : a.locationKey))).trim(),
    armorBefore: n,
    structureBefore: i,
    pureStructureHit: n <= 0
  };
}
function Ip(t = {}) {
  return Math.max(0, Math.trunc(Number((t == null ? void 0 : t.previewRevision) ?? 0) || 0));
}
function LT(t = {}, e = !1) {
  return e && t.chaosTargetLocationKey ? {
    locationKey: t.chaosTargetLocationKey,
    rulesLocation: t.chaosRulesLocation ?? t.chaosTargetLocationKey,
    locationFamily: t.chaosRulesLocation ?? (t.locationFamily === "head" ? "torso" : t.locationFamily),
    locationLabel: t.chaosTargetLocationLabel ?? $i(t.chaosTargetLocationKey)
  } : {
    locationKey: t.locationKey,
    rulesLocation: t.rulesLocation ?? t.locationFamily ?? t.locationKey,
    locationFamily: t.rulesLocation ?? t.locationFamily,
    locationLabel: t.locationLabel ?? $i(t.locationKey)
  };
}
function XR(t = {}, e = !1) {
  return !!(t.isAutomaticCritical || t.chaosCriticalOption && e);
}
function ZR(t = {}, e = {}, n = !1) {
  const i = !!t.isAutomaticCritical, a = !!t.chaosCriticalOption, r = XR(t, n), s = i ? "automatic" : a && n ? "chaosSelected" : a ? "chaosOptional" : "none";
  return {
    eligible: i || a,
    mode: s,
    source: r ? "hitLocation" : null,
    selected: r,
    reason: i ? "Hit location generated an automatic critical." : s === "chaosSelected" ? "Chaos Edge selected to convert this hit location into a critical." : s === "chaosOptional" ? "Chaos Edge can convert this hit location into a critical." : "Location hit is descriptive only.",
    automatic: i,
    optional: a,
    chaosCriticalSelected: !!n,
    locationKey: e.locationKey,
    rulesLocation: e.rulesLocation ?? e.locationFamily ?? e.locationKey,
    locationFamily: e.locationFamily,
    locationLabel: e.locationLabel
  };
}
function eD(t = {}) {
  return {
    damageIncoming: Number((t == null ? void 0 : t.damageIncoming) ?? 0),
    adjustedIncoming: Number((t == null ? void 0 : t.adjustedIncoming) ?? 0),
    finalDamage: Number((t == null ? void 0 : t.finalDamage) ?? 0),
    appliedDelta: Number((t == null ? void 0 : t.appliedDelta) ?? 0),
    usedArmor: !!(t != null && t.usedArmor),
    damageType: String((t == null ? void 0 : t.damageType) ?? "").trim(),
    effectiveAp: Number((t == null ? void 0 : t.effectiveAp) ?? 0),
    beforeLabel: String((t == null ? void 0 : t.beforeLabel) ?? "").trim(),
    afterLabel: String((t == null ? void 0 : t.afterLabel) ?? "").trim(),
    machine: Qa((t == null ? void 0 : t.machine) ?? {}),
    sourceScale: String((t == null ? void 0 : t.sourceScale) ?? "").trim(),
    targetScale: String((t == null ? void 0 : t.targetScale) ?? "").trim(),
    scaleConversion: Qa((t == null ? void 0 : t.scaleConversion) ?? null)
  };
}
function $T(t = null) {
  var i, a;
  const e = Array.isArray(t == null ? void 0 : t.spendOpportunities) ? t.spendOpportunities : [], n = e.find((r) => r == null ? void 0 : r.canSpend) ?? e[0] ?? null;
  return {
    canSpend: e.some((r) => r == null ? void 0 : r.canSpend),
    selected: e.some((r) => r == null ? void 0 : r.selected),
    cost: 1,
    prevents: ["conditionAdvance"],
    pressureDeltaPreview: Number(((i = t == null ? void 0 : t.summary) == null ? void 0 : i.shockAfter) ?? 0) - Number(((a = t == null ? void 0 : t.summary) == null ? void 0 : a.shockBefore) ?? 0),
    stressDeltaPreview: Object.values((t == null ? void 0 : t.stressDelta) ?? {}).reduce((r, s) => r + Number(s ?? 0), 0),
    index: Number.isInteger(Number(n == null ? void 0 : n.index)) ? Number(n.index) : null,
    location: String((n == null ? void 0 : n.location) ?? "").trim(),
    options: e.map((r) => ({
      index: Number((r == null ? void 0 : r.index) ?? 0),
      location: String((r == null ? void 0 : r.location) ?? "").trim(),
      source: String((r == null ? void 0 : r.source) ?? "").trim(),
      canSpend: !!(r != null && r.canSpend),
      selected: !!(r != null && r.selected),
      cost: 1,
      prevents: ["conditionAdvance"]
    }))
  };
}
function yf(t = {}) {
  const e = String((t == null ? void 0 : t.attackQuality) ?? "").trim();
  if (["graze", "hit", "highMargin"].includes(e)) return e;
  const n = String((t == null ? void 0 : t.outcome) ?? "").trim(), i = Math.max(0, Number((t == null ? void 0 : t.netHits) ?? 0) || 0);
  return n === "graze" ? "graze" : n === "hit" && i >= 4 ? "highMargin" : n === "hit" ? "hit" : "";
}
function tD({
  actor: t = null,
  payload: e = {},
  hitLocation: n = null,
  chaosCriticalSelected: i = !1
} = {}) {
  var N;
  if (!YR(t)) return { ok: !1, reason: "Machine damage requires a vehicle or BattleMech actor." };
  const a = Us((e == null ? void 0 : e.sourceScale) ?? (e == null ? void 0 : e.scale), "machine"), r = "machine", s = Cp({
    damage: (e == null ? void 0 : e.damage) ?? (e == null ? void 0 : e.amount) ?? 0,
    sourceScale: a,
    targetScale: r
  }), o = Math.max(0, Math.ceil(Number(s.converted ?? 0) || 0)), l = {
    actor: t,
    payload: e,
    damageIncoming: o,
    adjustedIncoming: o,
    effectiveAp: Math.max(0, Number((e == null ? void 0 : e.ap) ?? 0) || 0),
    damageType: String((e == null ? void 0 : e.damageType) ?? "kinetic").trim() || "kinetic",
    contributions: []
  };
  Nn(Rt.harm.beforeMachineDamagePreview, l);
  const c = Math.max(0, Math.ceil(Number(l.adjustedIncoming ?? l.damageIncoming ?? o) || 0)), u = Fb(t, M.monitors.armor), d = Fb(t, M.monitors.structure), f = n ? { ...n, armorBefore: u.remaining, structureBefore: d.remaining, pureStructureHit: u.remaining <= 0 } : QR(t, e, u.remaining, d.remaining), g = Array.isArray((N = e == null ? void 0 : e.effects) == null ? void 0 : N.flags) && e.effects.flags.includes("armorBypass") ? 0 : Math.min(c, t.type === M.actorTypes.vehicle && u.max <= 0 ? 0 : u.remaining), p = Math.min(d.remaining, Math.max(0, c - g)), h = Math.max(0, u.remaining - g), b = Math.max(0, d.remaining - p), y = Math.max(0, u.max - h), S = Math.max(0, d.max - b), A = LT(f, i), T = ZR(f, A, i), C = yf(e), k = p, w = Ip(e), v = {
    damageIncoming: o,
    adjustedIncoming: c,
    finalDamage: p,
    appliedDelta: p,
    usedArmor: g > 0,
    damageType: l.damageType,
    effectiveAp: l.effectiveAp,
    assetModuleContributions: l.contributions,
    beforeLabel: `Armor ${u.remaining}/${u.max}, Structure ${d.remaining}/${d.max}`,
    afterLabel: `Armor ${h}/${u.max}, Structure ${b}/${d.max}`,
    sourceScale: a,
    targetScale: r,
    scaleConversion: s
  };
  return {
    ok: !0,
    mode: "machineAttackDamage",
    actorName: t.name ?? "Machine",
    previewRevision: w,
    damageIncoming: v.damageIncoming,
    adjustedIncoming: v.adjustedIncoming,
    finalDamage: v.finalDamage,
    sourceScale: a,
    targetScale: r,
    scaleConversion: s,
    attackDamage: Qa((e == null ? void 0 : e.attackDamage) ?? null),
    requestedDelta: o,
    appliedDelta: v.appliedDelta,
    usedArmor: v.usedArmor,
    damageType: v.damageType,
    effectiveAp: v.effectiveAp,
    assetModuleContributions: v.assetModuleContributions,
    hitLocation: f,
    critical: T,
    machine: {
      armorRemainingBefore: u.remaining,
      armorRemainingAfter: h,
      armorBefore: u.remaining,
      armorAfter: h,
      armorDamageTakenBefore: u.damageTaken,
      armorDamageTakenAfter: y,
      armorDamageBefore: u.damageTaken,
      armorDamageAfter: y,
      armorMax: u.max,
      armorAbsorbed: g,
      structureRemainingBefore: d.remaining,
      structureRemainingAfter: b,
      structureBefore: d.remaining,
      structureAfter: b,
      structureDamageTakenBefore: d.damageTaken,
      structureDamageTakenAfter: S,
      structureDamageBefore: d.damageTaken,
      structureDamageAfter: S,
      structureMax: d.max,
      structureDamage: p,
      pureStructureHit: u.remaining <= 0,
      locationStressGain: k,
      locationTakesStress: k > 0
    },
    degradation: {
      attackQuality: C,
      locationStressGain: k
    },
    damagePreview: {
      ...v,
      machine: {
        armorRemainingBefore: u.remaining,
        armorRemainingAfter: h,
        armorBefore: u.remaining,
        armorAfter: h,
        armorDamageTakenBefore: u.damageTaken,
        armorDamageTakenAfter: y,
        armorDamageBefore: u.damageTaken,
        armorDamageAfter: y,
        armorMax: u.max,
        armorAbsorbed: g,
        structureRemainingBefore: d.remaining,
        structureRemainingAfter: b,
        structureBefore: d.remaining,
        structureAfter: b,
        structureDamageTakenBefore: d.damageTaken,
        structureDamageTakenAfter: S,
        structureDamageBefore: d.damageTaken,
        structureDamageAfter: S,
        structureMax: d.max,
        structureDamage: p,
        pureStructureHit: u.remaining <= 0,
        locationStressGain: k,
        locationTakesStress: k > 0
      },
      assetModuleContributions: v.assetModuleContributions
    },
    reliabilityOptions: $T(null),
    beforeLabel: v.beforeLabel,
    afterLabel: v.afterLabel,
    source: String((e == null ? void 0 : e.source) ?? "").trim(),
    notes: String((e == null ? void 0 : e.notes) ?? "").trim()
  };
}
function Pi(t, e = "") {
  var n, i;
  try {
    return ((i = (n = game == null ? void 0 : game.settings) == null ? void 0 : n.get) == null ? void 0 : i.call(n, I, t)) || e;
  } catch {
    return e;
  }
}
function nD(t = null) {
  return Pi(MT, Vt.general);
}
async function _T(t = null, e = "") {
  const n = String(e || nD(t)).trim();
  if (!n || typeof fromUuid != "function") return null;
  try {
    return await fromUuid(n);
  } catch (i) {
    return console.warn("MWD | Unable to resolve machine critical table", n, i), null;
  }
}
function BT(t = null, e = {}) {
  const n = String((e == null ? void 0 : e.rulesLocation) ?? (e == null ? void 0 : e.locationFamily) ?? (e == null ? void 0 : e.locationKey) ?? "").trim();
  return (t == null ? void 0 : t.type) === M.actorTypes.battlemech ? n === "head" ? "head" : n === "arms" || n === "arm" || /arm/i.test(String((e == null ? void 0 : e.locationKey) ?? "")) ? "arms" : n === "legs" || n === "leg" || /leg/i.test(String((e == null ? void 0 : e.locationKey) ?? "")) ? "legs" : "torso" : n === "turret" || n === "weapon" || /turret|weapon/i.test(String((e == null ? void 0 : e.locationKey) ?? "")) ? "turret" : n === "front" || /front/i.test(String((e == null ? void 0 : e.locationKey) ?? "")) ? "body" : n === "mobility" || n === "motive" || /mobility|motive|drive|wheel|track/i.test(String((e == null ? void 0 : e.locationKey) ?? "")) ? "mobility" : "body";
}
function iD(t = null, e = {}) {
  const n = BT(t, e);
  return (t == null ? void 0 : t.type) === M.actorTypes.battlemech ? n === "head" ? Pi(vT, Vt.mechHead) : n === "arms" ? Pi(IT, Vt.mechArms) : n === "legs" ? Pi(ET, Vt.mechLegs) : Pi(CT, Vt.mechTorso) || Pi(jR, Vt.battlemech) : n === "turret" ? Pi(PT, Vt.vehicleTurret) : n === "mobility" ? Pi(RT, Vt.vehicleMobility) : Pi(NT, Vt.vehicleBody) || Pi(GR, Vt.vehicle);
}
function aD(t = null, e = {}, n = 7) {
  var s, o, l, c;
  const i = (t == null ? void 0 : t.type) === M.actorTypes.vehicle ? "vehicle" : "battlemech", a = BT(t, e), r = Math.min(12, Math.max(2, Math.trunc(Number(n ?? 7)) || 7));
  return Qa(((o = (s = Kb[i]) == null ? void 0 : s[a]) == null ? void 0 : o[r]) ?? ((c = (l = Kb[i]) == null ? void 0 : l[a]) == null ? void 0 : c[7]));
}
function rc(t = {}, e = {}) {
  const n = Number((t == null ? void 0 : t.rollTotal) ?? 0);
  if (Number.isFinite(n) && n >= 2 && n <= 12) return Math.trunc(n);
  const i = String((e == null ? void 0 : e.key) ?? "").trim();
  for (const [a, r] of Object.entries(bf))
    if (r.key === i) return Number(a);
  return i === "cascade" || (e == null ? void 0 : e.escalationKey) === "cascade" ? 2 : (e == null ? void 0 : e.escalationKey) === "conditionAdvance" ? 12 : 7;
}
function em(t = {}, e = 0) {
  return e === 2 || (t == null ? void 0 : t.key) === "catastrophicCascade" || (t == null ? void 0 : t.key) === "cascade" || (t == null ? void 0 : t.escalationKey) === "cascade";
}
function rD(t, e) {
  var i;
  return Array.from((t == null ? void 0 : t.results) ?? ((i = t == null ? void 0 : t.results) == null ? void 0 : i.contents) ?? []).find((a) => {
    const r = Array.isArray(a == null ? void 0 : a.range) ? a.range : [], s = Number(r[0] ?? 0), o = Number(r[1] ?? r[0] ?? 0);
    return e >= s && e <= o;
  }) ?? null;
}
function Zr(t = {}, e = "") {
  return String((t == null ? void 0 : t[e]) ?? "").trim() !== "";
}
function qb(t = {}, e = "") {
  return Array.isArray(t == null ? void 0 : t[e]) ? t[e].length > 0 : String((t == null ? void 0 : t[e]) ?? "").trim() !== "";
}
function Wb(t = {}, e = "") {
  const n = t == null ? void 0 : t[e];
  return n && typeof n == "object" && !Array.isArray(n) && Object.keys(n).length > 0;
}
function sD(t = {}, e = {}) {
  const n = pa(t, { strict: !0 }), i = pa(e, { strict: !0 });
  if (n.key !== i.key) return n;
  const a = OT(t);
  return pa({
    ...n,
    remedyKey: Zr(a, "remedyKey") ? n.remedyKey : i.remedyKey,
    gates: qb(a, "gates") ? n.gates : i.gates,
    mods: qb(a, "mods") ? n.mods : i.mods,
    resourceEffects: Wb(a, "resourceEffects") ? n.resourceEffects : i.resourceEffects,
    pilotDamage: Wb(a, "pilotDamage") ? n.pilotDamage : i.pilotDamage,
    escalationKey: Zr(a, "escalationKey") ? n.escalationKey : i.escalationKey,
    statusId: Zr(a, "statusId") ? n.statusId : i.statusId,
    effectText: Zr(a, "effectText") ? n.effectText : i.effectText,
    automationMode: Zr(a, "automationMode") ? n.automationMode : i.automationMode,
    statusLabel: Zr(a, "statusLabel") ? n.statusLabel : i.statusLabel
  }, { strict: !0 });
}
async function Vb({ actor: t = null, hitLocation: e = {}, rollTotal: n = 7, tableUuid: i = "" } = {}) {
  const a = aD(t, e, n), r = pa(a.signal, { strict: !0 }), s = String(i || iD(t, e)).trim();
  if (!a) return { error: "No location critical table is defined for this hit location." };
  if (!s || typeof fromUuid != "function")
    return {
      signal: r,
      label: a.label,
      tableUuid: s,
      resultId: "",
      rollTotal: n
    };
  const o = await _T(t, s);
  if (!o) return { error: `Machine location critical table could not be resolved: ${s}` };
  const l = rD(o, n);
  if (!l) return { error: `Machine location critical table has no result for ${n}: ${s}` };
  const c = sD(l, a.signal);
  return {
    signal: c,
    label: String((l == null ? void 0 : l.name) ?? c.key).trim() || c.key,
    tableUuid: o.uuid ?? s,
    resultId: l.id ?? l._id ?? "",
    rollTotal: n
  };
}
async function Jb({ actor: t = null, drawFn: e = null, tableUuid: n = "", recursiveCascade: i = !1 } = {}) {
  var l;
  if (typeof e == "function") {
    const c = await e({ actor: t, recursiveCascade: i }), u = pa((c == null ? void 0 : c.signal) ?? c, { strict: !0 });
    return {
      signal: u,
      label: String((c == null ? void 0 : c.label) ?? u.key).trim() || u.key,
      tableUuid: String((c == null ? void 0 : c.tableUuid) ?? n ?? "").trim(),
      resultId: String((c == null ? void 0 : c.resultId) ?? "").trim(),
      rollTotal: Number((c == null ? void 0 : c.rollTotal) ?? 0) || null
    };
  }
  const a = await _T(t, n);
  if (!(a != null && a.draw)) return { error: "Machine critical table is not configured." };
  const r = await a.draw({ displayChat: !1 }), s = Array.from((r == null ? void 0 : r.results) ?? [])[0] ?? null;
  if (!s) return { error: "Machine critical table returned no result." };
  const o = pa(s, { strict: !0 });
  return {
    signal: o,
    label: String((s == null ? void 0 : s.name) ?? o.key).trim() || o.key,
    tableUuid: a.uuid ?? n,
    resultId: s.id ?? s._id ?? "",
    rollTotal: Number(((l = r == null ? void 0 : r.roll) == null ? void 0 : l.total) ?? 0) || null
  };
}
function tm({ actor: t, drawn: e, hitLocation: n, source: i = {}, cascade: a = !1, previewRevision: r = 0 } = {}) {
  var f, m, g, p, h, b, y;
  const s = pa((e == null ? void 0 : e.signal) ?? e, { strict: !0 }), o = Ya(s.remedyKey), l = LT(n, !1), c = String((e == null ? void 0 : e.label) ?? s.key).trim() || s.key, u = BR(t, s, l), d = Zo({
    key: s.key,
    label: c,
    locationLabel: l.locationLabel,
    gates: s.gates,
    mods: s.mods,
    remedyKey: s.remedyKey
  }, o);
  return {
    id: WR(),
    previewRevision: Ip({ previewRevision: r }),
    key: s.key,
    resultKey: s.key,
    label: c,
    table: String((e == null ? void 0 : e.tableUuid) ?? "").trim(),
    rollTotal: Number((e == null ? void 0 : e.rollTotal) ?? 0) || null,
    rulesLocation: l.rulesLocation ?? l.locationFamily ?? l.locationKey,
    tableUuid: String((e == null ? void 0 : e.tableUuid) ?? "").trim(),
    resultId: String((e == null ? void 0 : e.resultId) ?? "").trim(),
    generalKey: String(((f = e == null ? void 0 : e.general) == null ? void 0 : f.key) ?? "").trim(),
    generalLabel: String(((m = e == null ? void 0 : e.general) == null ? void 0 : m.label) ?? "").trim(),
    generalRollTotal: Number(((g = e == null ? void 0 : e.general) == null ? void 0 : g.rollTotal) ?? (e == null ? void 0 : e.rollTotal) ?? 0) || null,
    generalTableUuid: String(((p = e == null ? void 0 : e.general) == null ? void 0 : p.tableUuid) ?? "").trim(),
    generalResultId: String(((h = e == null ? void 0 : e.general) == null ? void 0 : h.resultId) ?? "").trim(),
    locationKey: l.locationKey,
    locationFamily: l.locationFamily,
    locationLabel: l.locationLabel,
    gates: s.gates,
    mods: s.mods,
    resourceEffects: s.resourceEffects,
    pilotDamage: s.pilotDamage,
    remedy: {
      key: s.remedyKey,
      label: o.label,
      skillKey: Zo({
        key: s.key,
        label: c,
        locationLabel: l.locationLabel,
        gates: s.gates,
        mods: s.mods,
        remedyKey: s.remedyKey
      }, o),
      baseDn: el({ remedyKey: s.remedyKey }, o)
    },
    effects: {
      gates: s.gates,
      mods: s.mods,
      resourceEffects: s.resourceEffects,
      pilotDamage: s.pilotDamage,
      statusId: s.statusId,
      effectText: u.effectText,
      automationMode: u.automationMode
    },
    remedyKey: s.remedyKey,
    remedyLabel: o.label,
    remedySkillKey: d,
    remedyBaseDn: el({ remedyKey: s.remedyKey }, o),
    remedyEffect: yp({}),
    escalationKey: s.escalationKey,
    statusId: s.statusId,
    statusLabel: u.statusLabel,
    effectText: u.effectText,
    automationMode: u.automationMode,
    weaponGroupId: u.weaponGroupId,
    weaponGroupName: u.weaponGroupName,
    weaponIds: u.weaponIds,
    active: !0,
    cascade: !!a,
    createdRound: Number(((y = (b = globalThis.game) == null ? void 0 : b.combat) == null ? void 0 : y.round) ?? 0) || 0,
    createdAt: VR(),
    source: Qa(i ?? {}),
    actorType: (t == null ? void 0 : t.type) ?? ""
  };
}
async function oD({
  actor: t = null,
  hitLocation: e = {},
  source: n = {},
  drawFn: i = null,
  tableUuid: a = "",
  previewRevision: r = 0
} = {}) {
  try {
    const s = await Jb({ actor: t, drawFn: i, tableUuid: a, recursiveCascade: !1 });
    if (s != null && s.error) return { ok: !1, reason: s.error, crits: [] };
    const o = pa(s.signal, { strict: !0 }), l = rc(s, o), c = await Vb({ actor: t, hitLocation: e, rollTotal: l });
    if (c != null && c.error) return { ok: !1, reason: c.error, crits: [] };
    const u = {
      ...c,
      general: {
        key: o.key,
        label: String((s == null ? void 0 : s.label) ?? o.key).trim() || o.key,
        rollTotal: l,
        tableUuid: String((s == null ? void 0 : s.tableUuid) ?? "").trim(),
        resultId: String((s == null ? void 0 : s.resultId) ?? "").trim()
      }
    };
    if (!em(o, l))
      return { ok: !0, crits: [tm({ actor: t, drawn: u, hitLocation: e, source: n, previewRevision: r })], cascade: !1 };
    const d = await Jb({ actor: t, drawFn: i, tableUuid: a, recursiveCascade: !0 }), f = d != null && d.error ? Gb(12) : pa(d.signal, { strict: !0 }), m = em(f, rc(d, f)) ? 12 : rc(d, f), g = m === 12 && em(f, rc(d, f)) ? Gb(12) : f, p = await Vb({ actor: t, hitLocation: e, rollTotal: m });
    if (p != null && p.error) return { ok: !1, reason: p.error, crits: [] };
    const h = {
      ...p,
      general: {
        key: g.key,
        label: String((d == null ? void 0 : d.label) ?? g.key).trim() || g.key,
        rollTotal: m,
        tableUuid: String((d == null ? void 0 : d.tableUuid) ?? "").trim(),
        resultId: String((d == null ? void 0 : d.resultId) ?? "").trim()
      }
    };
    return {
      ok: !0,
      cascade: !0,
      crits: [
        tm({ actor: t, drawn: u, hitLocation: e, source: n, cascade: !0, previewRevision: r }),
        tm({ actor: t, drawn: h, hitLocation: e, source: n, previewRevision: r })
      ]
    };
  } catch (s) {
    return { ok: !1, reason: (s == null ? void 0 : s.message) ?? "Unable to draw machine critical.", crits: [] };
  }
}
function lD(t = {}, e = null) {
  return Array.isArray(t == null ? void 0 : t.preparedCriticalRecords) ? t.preparedCriticalRecords.map((n) => TT(Qa(n), e)) : [];
}
function cD(t = {}, e = []) {
  if (!e.length) return { ok: !0 };
  const n = Ip(t);
  return e.find((a) => Number((a == null ? void 0 : a.previewRevision) ?? n) !== n) ? {
    ok: !1,
    reason: "Critical preview is stale. Rebuild the attack damage preview before applying."
  } : { ok: !0 };
}
function uD(t = []) {
  return Array.from(t ?? []).filter((e) => String((e == null ? void 0 : e.escalationKey) ?? "").trim() === "conditionAdvance").map((e) => String((e == null ? void 0 : e.locationKey) ?? "").trim()).filter(Boolean);
}
function dD(t = [], e = "") {
  return Array.from(t ?? []).some((i) => String((i == null ? void 0 : i.escalationKey) ?? "").trim() === "shock") ? Math.max(1, mT(e)) : 0;
}
function mD(t = {}) {
  var n, i, a;
  if (!((n = t == null ? void 0 : t.machine) != null && n.pureStructureHit)) return [];
  if (Math.max(0, Number(((i = t == null ? void 0 : t.machine) == null ? void 0 : i.structureDamage) ?? 0) || 0) <= 0) return [];
  const e = String(((a = t == null ? void 0 : t.hitLocation) == null ? void 0 : a.locationKey) ?? "").trim();
  return e ? [{ locationKey: e, source: "pureStructure", applyReductions: !1, allowSpend: !1 }] : [];
}
async function fD(t, e) {
  if (!(!(t != null && t.toggleStatusEffect) || !e))
    try {
      await mt({
        actor: t,
        statusId: wT,
        active: !0,
        metadata: {
          scope: "Machine critical effects",
          notes: "Visual marker for active system.mwd.crits entries."
        }
      });
    } catch (n) {
      console.warn("MWD | Unable to sync machine critical status", n);
    }
}
async function gD(t, e) {
  var n, i;
  for (const a of e) {
    const r = String((a == null ? void 0 : a.statusId) ?? "").trim();
    if (!(!r || (((i = (n = t.statuses) == null ? void 0 : n.has) == null ? void 0 : i.call(n, r)) ?? !1)))
      try {
        await mt({ actor: t, statusId: r, active: !0 });
      } catch (s) {
        console.warn(`MWD | Unable to apply crit status "${r}"`, s);
      }
  }
}
async function pD(t, e) {
  var i, a, r, s, o, l;
  if (String((e == null ? void 0 : e.statusState) ?? "").trim() === "destroyed" && !(((a = (i = t.statuses) == null ? void 0 : i.has) == null ? void 0 : a.call(i, "destroyed")) ?? !1))
    try {
      await mt({
        actor: t,
        statusId: "destroyed",
        active: !0,
        metadata: {
          scope: "Machine destroyed",
          notes: "Applied by catastrophic machine degradation."
        }
      });
    } catch (c) {
      console.warn("MWD | Unable to apply destroyed status", c);
    }
  const n = new Set(TO(t));
  for (const c of n)
    if (!(!c || (((s = (r = t.statuses) == null ? void 0 : r.has) == null ? void 0 : s.call(r, c)) ?? !1)))
      try {
        await mt({ actor: t, statusId: c, active: !0 });
      } catch (u) {
        console.warn(`MWD | Unable to apply degradation-derived status "${c}"`, u);
      }
  for (const c of Array.from((e == null ? void 0 : e.fallbackEvents) ?? [])) {
    if (!(c != null && c.destroyed)) continue;
    const u = String(c.location ?? "").trim(), d = u === "legs" ? "legDestroyed" : u === "arms" ? "armDestroyed" : "";
    if (!(!d || (((l = (o = t.statuses) == null ? void 0 : o.has) == null ? void 0 : l.call(o, d)) ?? !1)))
      try {
        await mt({ actor: t, statusId: d, active: !0 });
      } catch (f) {
        console.warn(`MWD | Unable to apply degradation status "${d}"`, f);
      }
  }
}
async function hD({
  actor: t = null,
  token: e = null,
  payload: n = {},
  options: i = {}
} = {}) {
  var g, p, h, b, y;
  if ((n == null ? void 0 : n.applied) === !0)
    return (g = n == null ? void 0 : n.appliedResult) != null && g.ok ? { ...n.appliedResult, skipped: !0, reason: "That machine attack damage has already been applied." } : { ok: !1, skipped: !0, reason: "That machine attack damage has already been applied." };
  const a = tD({
    actor: t,
    payload: n,
    chaosCriticalSelected: !!(n != null && n.chaosCriticalSelected)
  });
  if (!a.ok) return a;
  const r = { actor: t, token: e, payload: n, options: i, preview: a };
  if (Nn(Rt.harm.beforeMachineDamageApply, r) === !1)
    return { ok: !1, reason: "Machine damage application was cancelled by a module hook." };
  const s = !!i.dryRun, o = lD(n, t), l = cD(n, o);
  if (!l.ok) return l;
  if (!s && (n == null ? void 0 : n.requirePreparedCriticalRecords) === !0 && a.critical.selected && !o.length)
    return { ok: !1, reason: "Critical preview is missing. Rebuild the attack damage preview before applying." };
  let c = o.length ? { ok: !0, crits: o, cascade: o.length > 1 } : { ok: !0, crits: [] };
  !o.length && a.critical.selected && (c = await oD({
    actor: t,
    hitLocation: {
      ...a.hitLocation,
      locationKey: a.critical.locationKey,
      rulesLocation: a.critical.rulesLocation,
      locationFamily: a.critical.locationFamily,
      locationLabel: a.critical.locationLabel
    },
    source: {
      ...(n == null ? void 0 : n.sourceData) ?? {},
      source: (n == null ? void 0 : n.source) ?? "",
      tokenUuid: (e == null ? void 0 : e.uuid) ?? (n == null ? void 0 : n.targetTokenUuid) ?? ""
    },
    drawFn: i.drawCritical,
    tableUuid: (n == null ? void 0 : n.criticalTableUuid) ?? "",
    previewRevision: a.previewRevision
  }));
  const u = SR({
    actorSnapshot: t,
    locationKey: a.critical.locationKey || a.hitLocation.locationKey,
    machineDamageDealt: a.machine.structureDamage,
    attackQuality: a.machine.structureDamage > 0 ? ((p = a.degradation) == null ? void 0 : p.attackQuality) ?? yf(n) : "",
    extraShockGain: dD(c.ok ? c.crits : [], ((h = a.degradation) == null ? void 0 : h.attackQuality) ?? yf(n)),
    allowReliabilitySpend: !0,
    reliabilitySpendSelections: Array.isArray(n == null ? void 0 : n.reliabilitySpendSelections) ? n.reliabilitySpendSelections : [],
    directConditionLocations: [
      ...mD(a),
      ...c.ok ? uD(c.crits) : []
    ]
  });
  u.loopGuardTriggered && console.warn("MWD | Machine degradation loop guard triggered", {
    actor: (t == null ? void 0 : t.name) ?? (t == null ? void 0 : t.id) ?? "Machine",
    payload: n,
    degradation: u
  });
  const d = Array.isArray((y = (b = t == null ? void 0 : t.system) == null ? void 0 : b.mwd) == null ? void 0 : y.crits) ? Qa(t.system.mwd.crits) : [], f = c.ok && c.crits.length ? d.concat(c.crits) : d;
  if (!s) {
    const S = {
      "system.monitors.armor.value": a.machine.armorRemainingAfter,
      "system.monitors.structure.value": a.machine.structureRemainingAfter,
      ...AR(t, u),
      ...gT(t, a.machine.structureAfter)
    };
    c.ok && c.crits.length && (S["system.mwd.crits"] = f), await t.update(S);
    const A = f.some((T) => (T == null ? void 0 : T.active) !== !1);
    await fD(t, A), c.ok && c.crits.length && await gD(t, c.crits), await pD(t, u);
  }
  const m = {
    ...a,
    dryRun: s,
    appliedDelta: a.machine.structureDamage,
    damagePreview: eD(a),
    reliabilityOptions: $T(u),
    critical: {
      ...a.critical,
      drawOk: !!c.ok,
      reason: c.ok ? "" : c.reason,
      records: c.ok ? c.crits : [],
      cascade: !!c.cascade
    },
    degradation: u
  };
  return Nn(Rt.harm.afterMachineDamageApply, {
    actor: t,
    token: e,
    payload: n,
    options: i,
    preview: a,
    result: m
  }), m;
}
function ln(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? Math.trunc(n) : e;
}
function cu(t, e = 0) {
  return Math.max(0, ln(t, e));
}
function ja(t, e = "") {
  return String(t ?? "").trim() || e;
}
function Hn(t) {
  return (Array.isArray(t) ? t : typeof t == "string" ? t.split(",") : []).map((n) => String(n ?? "").trim()).filter(Boolean);
}
function sl(t, e, n) {
  const i = String(t ?? "").trim();
  return e.includes(i) ? i : n;
}
const bD = Object.freeze(["module", "equipment"]), yD = Object.freeze(["passive", "toggle", "mode", "triggered"]), SD = Object.freeze(["", "suppressed", "offline", "destroyed", "disabled", "cooldown"]), xT = Object.freeze(["ready", "active", "triggered"]), zT = Object.freeze(["self", "target", "alliesInRange", "enemiesInRange", "sourceTargetPair"]), Sf = Object.freeze(["ecmShrouded"]);
class Ep extends Error {
  constructor(e, { itemName: n = "Asset Module", itemId: i = "" } = {}) {
    super(`${n}: ${e}`), this.name = "AssetModuleValidationError", this.itemName = n, this.itemId = i, this.userMessage = this.message;
  }
}
function Vn(t, e = {}) {
  throw new Ep(t, e);
}
function Ge(t, e = "") {
  return `effects[${t}]${e ? `.${e}` : ""}`;
}
function es(t, e, n) {
  t != null && (typeof t != "object" || Array.isArray(t)) && Vn(`${e} must be an object.`, n);
}
function An(t, e, n) {
  t != null && typeof t != "string" && (Array.isArray(t) && t.every((i) => typeof i == "string") || Vn(`${e} must be a string or an array of strings.`, n));
}
function cs(t, e, n) {
  t != null && (Number.isFinite(Number(t)) || Vn(`${e} must be a finite number.`, n));
}
function Yb(t, e, n, i) {
  t == null || t === "" || e.includes(String(t).trim()) || Vn(`${n} must be one of: ${e.join(", ")}.`, i);
}
function AD(t, e, n) {
  t != null && (Array.isArray(t) || Vn(`${e} must be an array.`, n), t.forEach((i, a) => {
    (!i || typeof i != "object" || Array.isArray(i)) && Vn(`${e}[${a}] must be an object.`, n), An(i.actionIds ?? i.actions ?? i.actionId, `${e}[${a}].actionIds`, n), cs(i.cost, `${e}[${a}].cost`, n), i.category !== void 0 && An(String(i.category), `${e}[${a}].category`, n), i.resource !== void 0 && An(String(i.resource), `${e}[${a}].resource`, n);
  }));
}
function Np(t = {}, e = {}) {
  const n = t == null ? void 0 : t.effects;
  return n == null ? [] : (Array.isArray(n) || Vn("system.effects must be an array.", e), n.forEach((i, a) => {
    var c;
    (!i || typeof i != "object" || Array.isArray(i)) && Vn(`${Ge(a)} must be an object.`, e), (typeof i.id != "string" || !i.id.trim()) && Vn(`${Ge(a, "id")} is required and must be a non-empty string.`, e), i.label !== void 0 && typeof i.label != "string" && Vn(`${Ge(a, "label")} must be a string.`, e), Yb(i.timing, xT, Ge(a, "timing"), e), Yb(i.scope, zT, Ge(a, "scope"), e), es(i.requires, Ge(a, "requires"), e);
    const r = i.requires ?? {};
    An(r.tags, Ge(a, "requires.tags"), e), An(r.actionIds ?? r.actions ?? r.action, Ge(a, "requires.actionIds"), e), An(r.skillIds ?? r.skills ?? r.skill, Ge(a, "requires.skillIds"), e), An(r.weaponTags, Ge(a, "requires.weaponTags"), e), An(r.statuses, Ge(a, "requires.statuses"), e), An(r.forbidsStatuses, Ge(a, "requires.forbidsStatuses"), e), An(r.forbidsTags, Ge(a, "requires.forbidsTags"), e), An(r.modes ?? r.mode, Ge(a, "requires.modes"), e), es(i.grants, Ge(a, "grants"), e);
    const s = i.grants ?? {};
    An(s.statuses, Ge(a, "grants.statuses"), e), Hn(s.statuses).includes("ecmBoosted") && Vn(`${Ge(a, "grants.statuses")} must use epmBoosted, not ecmBoosted.`, e), AD(s.actionOverrides, Ge(a, "grants.actionOverrides"), e), An(s.actions, Ge(a, "grants.actions"), e), An(s.reactions, Ge(a, "grants.reactions"), e), es(i.modifies, Ge(a, "modifies"), e);
    const o = i.modifies ?? {};
    ["dice", "ar", "dr", "trackingPenalty", "targetingData", "clusteringTarget", "clusteringDice", "movementMeters"].forEach((u) => cs(o[u], Ge(a, `modifies.${u}`), e)), An(o.bypassStatuses, Ge(a, "modifies.bypassStatuses"), e);
    for (const u of Hn(o.bypassStatuses))
      Sf.includes(u) || Vn(`${Ge(a, "modifies.bypassStatuses")} may only include: ${Sf.join(", ")}.`, e);
    es(i.costs, Ge(a, "costs"), e);
    const l = i.costs ?? {};
    cs(l.heat, Ge(a, "costs.heat"), e), cs(l.charges, Ge(a, "costs.charges"), e), l.stress !== void 0 && l.stress !== null && (es(l.stress, Ge(a, "costs.stress"), e), (typeof l.stress.location != "string" || !l.stress.location.trim()) && Vn(`${Ge(a, "costs.stress.location")} is required when stress cost is present.`, e), cs(l.stress.value, Ge(a, "costs.stress.value"), e)), es(i.limits, Ge(a, "limits"), e), cs((c = i.limits) == null ? void 0 : c.cooldownTurns, Ge(a, "limits.cooldownTurns"), e);
  }), n);
}
function kD(t = {}) {
  const e = t && typeof t == "object" ? t : {};
  return {
    mode: sl(e.mode, yD, "passive"),
    active: !!e.active,
    selectedMode: ja(e.selectedMode, ""),
    cooldownUntilRound: cu(e.cooldownUntilRound, 0)
  };
}
function TD(t = {}) {
  const e = t && typeof t == "object" ? t : {};
  return {
    suppressed: !!e.suppressed,
    offline: !!e.offline,
    destroyed: !!e.destroyed,
    reason: sl(e.reason, SD, "")
  };
}
function wD(t = {}) {
  const e = t && typeof t == "object" ? t : {};
  return {
    tags: Hn(e.tags),
    actionIds: Hn(e.actionIds ?? e.actions ?? e.action),
    skillIds: Hn(e.skillIds ?? e.skills ?? e.skill),
    weaponTags: Hn(e.weaponTags),
    statuses: Hn(e.statuses),
    forbidsStatuses: Hn(e.forbidsStatuses),
    forbidsTags: Hn(e.forbidsTags),
    detectionState: ja(e.detectionState, ""),
    targetState: ja(e.targetState, ""),
    heatBand: ja(e.heatBand, ""),
    modes: Hn(e.modes ?? e.mode)
  };
}
function MD(t = {}) {
  const e = t && typeof t == "object" ? t : {};
  return {
    statuses: Hn(e.statuses),
    actionOverrides: Array.isArray(e.actionOverrides) ? e.actionOverrides.filter(Boolean) : [],
    actions: Hn(e.actions),
    reactions: Hn(e.reactions)
  };
}
function vD(t = {}) {
  const e = t && typeof t == "object" ? t : {}, n = Hn(e.bypassStatuses).filter((i) => Sf.includes(i));
  return {
    dice: ln(e.dice, 0),
    ar: ln(e.ar, 0),
    dr: ln(e.dr, 0),
    trackingPenalty: ln(e.trackingPenalty, 0),
    targetingData: ln(e.targetingData, 0),
    clusteringTarget: ln(e.clusteringTarget, 0),
    clusteringDice: ln(e.clusteringDice, 0),
    movementMeters: ln(e.movementMeters, 0),
    bypassStatuses: n
  };
}
function CD(t = {}) {
  const e = t && typeof t == "object" ? t : {}, n = e.stress && typeof e.stress == "object" ? {
    location: ja(e.stress.location, ""),
    value: ln(e.stress.value, 0)
  } : null;
  return {
    heat: ln(e.heat, 0),
    stress: n,
    charges: ln(e.charges, 0)
  };
}
function ID(t = {}) {
  const e = t && typeof t == "object" ? t : {};
  return {
    oncePerActivation: !!e.oncePerActivation,
    cooldownTurns: cu(e.cooldownTurns, 0)
  };
}
function ED(t = {}, e = 0) {
  const n = t && typeof t == "object" && !Array.isArray(t) ? t : {}, i = ja(n.id, `effect-${e + 1}`), a = ja(n.label, i);
  return {
    id: i,
    label: a,
    timing: sl(n.timing, xT, "ready"),
    scope: sl(n.scope, zT, "self"),
    requires: wD(n.requires),
    grants: MD(n.grants),
    modifies: vD(n.modifies),
    costs: CD(n.costs),
    limits: ID(n.limits)
  };
}
function ND(t = []) {
  return Np({ effects: t }), (Array.isArray(t) ? t : []).map((e, n) => ED(e, n));
}
function FT(t = {}) {
  const e = t && typeof t == "object" ? t : {}, n = !!e.enabled;
  return {
    enabled: n,
    movement: cu(e.movement, 0),
    heat: cu(e.heat, 0),
    attackRatingBonus: ln(e.attackRatingBonus, 0),
    defenseRatingBonus: ln(e.defenseRatingBonus, 0),
    dfaEnabled: !!(e.dfaEnabled ?? n)
  };
}
function PD(t = {}) {
  const e = t && typeof t == "object" ? t : {};
  return {
    diceModifier: ln(e.diceModifier, 0),
    targetNumberModifier: ln(e.targetNumberModifier, 0)
  };
}
function Br(t = {}) {
  var s, o, l, c;
  const e = t && typeof t == "object" ? t : {}, n = e.mobility && typeof e.mobility == "object" ? e.mobility : {}, i = e.targeting && typeof e.targeting == "object" ? e.targeting : {}, a = TD({
    ...e.state && typeof e.state == "object" ? e.state : {},
    suppressed: ((s = e.state) == null ? void 0 : s.suppressed) ?? e.suppressed,
    offline: ((o = e.state) == null ? void 0 : o.offline) ?? e.offline,
    destroyed: ((l = e.state) == null ? void 0 : l.destroyed) ?? e.destroyed,
    reason: ((c = e.state) == null ? void 0 : c.reason) ?? e.stateReason ?? ""
  }), r = (e.enabled === void 0 || !!e.enabled) && !e.inactive;
  return {
    ...e,
    enabled: r,
    state: a,
    installClass: sl(e.installClass, bD, "module"),
    category: ja(e.category, "special"),
    level: Math.max(1, ln(e.level, 1)),
    activation: kD(e.activation),
    rules: Qu(e).rules,
    effects: ND(e.effects),
    mobility: {
      ...n,
      jumping: FT(n.jumping)
    },
    targeting: {
      ...i,
      clustering: PD(i.clustering)
    }
  };
}
function no(t = null, { installed: e = null, currentRound: n = null } = {}) {
  var h, b, y, S, A, T, C, k;
  const i = Br((t == null ? void 0 : t.system) ?? {}), a = n == null ? Number(((b = (h = globalThis.game) == null ? void 0 : h.combat) == null ? void 0 : b.round) ?? 0) || 0 : Number(n) || 0, r = Number(((y = i.activation) == null ? void 0 : y.cooldownUntilRound) ?? 0) || 0, s = r > 0 && a > 0 && r >= a, o = e == null ? !!((t == null ? void 0 : t.actor) ?? (t == null ? void 0 : t.parent) ?? (t == null ? void 0 : t.isOwned) ?? (t == null ? void 0 : t.id)) : !!e, l = ((S = i.activation) == null ? void 0 : S.mode) ?? "passive", c = l === "passive", u = !!i.enabled && !i.inactive, d = !!((A = i.state) != null && A.suppressed), f = !!((T = i.state) != null && T.offline), m = !!((C = i.state) != null && C.destroyed), g = o && u && !d && !f && !m && !s, p = g && (c || !!((k = i.activation) != null && k.active));
  return {
    installed: o,
    enabled: u,
    passive: c,
    ready: g,
    active: p,
    contributes: p,
    coolingDown: s,
    suppressed: d,
    offline: f,
    destroyed: m,
    mode: l,
    activation: i.activation,
    state: i.state,
    system: i
  };
}
function RD(t = {}) {
  const e = (t == null ? void 0 : t.system) ?? t;
  return Br(e).mobility.jumping;
}
function DD(t = {}) {
  const e = (t == null ? void 0 : t.system) ?? t;
  return Br(e).targeting.clustering;
}
const OD = /* @__PURE__ */ new Set([M.actorTypes.battlemech, M.actorTypes.vehicle]);
function LD(t) {
  return Array.isArray(t) ? t : Array.isArray(t == null ? void 0 : t.contents) ? t.contents : t && typeof t[Symbol.iterator] == "function" ? Array.from(t) : [];
}
function ge(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function Pt(t, e = "") {
  return String(t ?? "").trim() || e;
}
function ka(t) {
  return (Array.isArray(t) ? t : typeof t == "string" ? t.split(",") : []).map((n) => String(n ?? "").trim()).filter(Boolean);
}
function $D(t = null) {
  return ((t == null ? void 0 : t.canonicalType) ?? (t == null ? void 0 : t.type)) === M.itemType.assetModule;
}
function UT(t = {}) {
  var n;
  const e = (t == null ? void 0 : t.assetModules) ?? (t == null ? void 0 : t.items) ?? ((n = t == null ? void 0 : t.actor) == null ? void 0 : n.items) ?? [];
  return LD(e).filter($D);
}
function KT(t = {}) {
  var e;
  return String((t == null ? void 0 : t.type) ?? ((e = t == null ? void 0 : t.actor) == null ? void 0 : e.type) ?? "").trim();
}
function _D(t = {}) {
  return OD.has(KT(t));
}
function ts(t, e) {
  for (const n of ka(e)) t.add(n);
}
function nm(t) {
  return t instanceof Set ? Array.from(t) : ka(t);
}
function BD({ payload: t = {}, resolved: e = {} } = {}) {
  var a, r, s, o;
  const n = Pt(
    t.machineActionKey ?? t.actionId ?? ((r = (a = e == null ? void 0 : e.quickAction) == null ? void 0 : a.ewAction) == null ? void 0 : r.id) ?? ((o = (s = t == null ? void 0 : t.quickAction) == null ? void 0 : s.ewAction) == null ? void 0 : o.id),
    ""
  );
  if (n) return n;
  const i = Pt((e == null ? void 0 : e.intent) ?? (t == null ? void 0 : t.intent), "");
  return i === "acquire" ? "acquireTarget" : i === "targeting" ? "generateFireSolution" : i;
}
function xD(t, { payload: e = {}, resolved: n = {} } = {}) {
  var c, u, d, f, m, g, p;
  if (String((n == null ? void 0 : n.intent) ?? (e == null ? void 0 : e.intent) ?? "").trim() !== "attack") return;
  const i = ((c = n == null ? void 0 : n.attack) == null ? void 0 : c.weapon) ?? {}, a = Pt(i.category ?? i.attackKind ?? e.attackKind, "").toLowerCase(), r = Pt(((d = (u = n == null ? void 0 : n.attack) == null ? void 0 : u.skill) == null ? void 0 : d.code) ?? i.skill ?? e.key, "").toLowerCase(), s = a === "melee" || r === "meleecombat" || r === "melee";
  t.tags.add(s ? "attack.melee" : "attack.ranged"), t.tags.add("intent.attack"), t.weaponTags.add(s ? "weapon.melee" : "weapon.ranged");
  const o = [
    i.weaponType,
    i.weaponCategory,
    i.category,
    i.damageType,
    i.baseDamageType,
    i.baseWeaponType,
    (f = i.source) == null ? void 0 : f.type,
    (m = i.sourceState) == null ? void 0 : m.type,
    (p = (g = i.payloadState) == null ? void 0 : g.payload) == null ? void 0 : p.type
  ];
  for (const h of o) {
    const b = Pt(h, "").toLowerCase();
    b && (t.weaponTags.add(`weapon.${b}`), t.tags.add(`weapon.${b}`));
  }
  for (const h of ka(i.keywords))
    t.weaponTags.add(h), t.tags.add(h);
  const l = Pt(i.damageType ?? i.baseDamageType, "").toLowerCase();
  l && t.tags.add(`damage.${l}`), Number(i.clusteringDice ?? 0) > 0 && (t.tags.add("attack.cluster"), t.weaponTags.add("weapon.cluster"));
}
function zD({ actor: t = null, payload: e = {}, resolved: n = {}, context: i = {} } = {}) {
  var m, g, p, h, b, y;
  const a = Pt((n == null ? void 0 : n.intent) ?? (e == null ? void 0 : e.intent), ""), r = BD({ payload: e, resolved: n }), s = Pt((e == null ? void 0 : e.key) ?? ((m = n == null ? void 0 : n.data) == null ? void 0 : m.skillKey), ""), o = /* @__PURE__ */ new Set(), l = /* @__PURE__ */ new Set(), c = /* @__PURE__ */ new Set();
  a && o.add(`intent.${a}`), r && (o.add(`action.${r}`), o.add(r)), s && (o.add(`skill.${s}`), o.add(s)), ts(o, e == null ? void 0 : e.tags), ts(o, n == null ? void 0 : n.domainTags), ts(o, n == null ? void 0 : n.domains), ts(o, i == null ? void 0 : i.tags), ts(c, Array.from((t == null ? void 0 : t.statuses) ?? [])), ts(c, i == null ? void 0 : i.statuses), xD({ tags: o, weaponTags: l }, { payload: e, resolved: n });
  const u = Pt(
    (i == null ? void 0 : i.detectionState) ?? ((p = (g = n == null ? void 0 : n.attack) == null ? void 0 : g.ewContext) == null ? void 0 : p.detectionState) ?? ((h = n == null ? void 0 : n.targeting) == null ? void 0 : h.detectionState) ?? ((b = n == null ? void 0 : n.acquire) == null ? void 0 : b.currentState),
    ""
  ), d = Pt((i == null ? void 0 : i.targetState) ?? ((y = n == null ? void 0 : n.acquire) == null ? void 0 : y.currentState), ""), f = Pt((i == null ? void 0 : i.heatBand) ?? (e == null ? void 0 : e.heatBand), "");
  return {
    actor: t,
    actorType: KT(t),
    intent: a,
    actionId: r,
    action: { id: r },
    skillId: s,
    skill: { key: s },
    tags: o,
    weaponTags: l,
    statuses: c,
    selectors: Array.from(o),
    detectionState: u,
    targetState: d,
    heatBand: f,
    event: { trigger: Pt(i == null ? void 0 : i.trigger, "") },
    trigger: Pt(i == null ? void 0 : i.trigger, "")
  };
}
function HT(t) {
  return Np((t == null ? void 0 : t.system) ?? {}, {
    itemName: (t == null ? void 0 : t.name) ?? "Asset Module",
    itemId: (t == null ? void 0 : t.id) ?? ""
  }), Br((t == null ? void 0 : t.system) ?? {}).effects;
}
function FD(t = {}) {
  const e = t.requires ?? {};
  return {
    tags: e.tags ?? [],
    forbidsTags: e.forbidsTags ?? [],
    actionIds: e.actionIds ?? [],
    skillIds: e.skillIds ?? [],
    weaponTags: e.weaponTags ?? [],
    statuses: e.statuses ?? [],
    forbidsStatuses: e.forbidsStatuses ?? [],
    detectionState: e.detectionState ?? "",
    targetState: e.targetState ?? "",
    heatBand: e.heatBand ?? "",
    modes: e.modes ?? []
  };
}
function UD(t = {}) {
  return t.timing === "active" ? [{ fact: "module.active", op: "eq", value: !0 }] : t.timing === "triggered" ? [
    { fact: "module.ready", op: "eq", value: !0 },
    { fact: "event.trigger", op: "truthy" }
  ] : [{ fact: "module.ready", op: "eq", value: !0 }];
}
function KD(t = {}) {
  var r;
  const e = [], n = t.modifies ?? {}, i = t.grants ?? {};
  ge(n.dice, 0) && e.push({ type: "dicePart", id: `${t.id}.dice`, label: t.label, value: ge(n.dice, 0) }), (ge(n.ar, 0) || ge(n.dr, 0)) && e.push({ type: "cqPart", id: `${t.id}.cq`, label: t.label, ar: ge(n.ar, 0), dr: ge(n.dr, 0) }), ge(n.trackingPenalty, 0) && e.push({ type: "targetingConstraint", id: `${t.id}.trackingPenalty`, label: t.label, constraint: "trackingPenalty", value: ge(n.trackingPenalty, 0) }), ge(n.targetingData, 0) && e.push({ type: "targetingDataModifier", id: `${t.id}.targetingData`, label: t.label, value: ge(n.targetingData, 0) });
  for (const s of n.bypassStatuses ?? [])
    e.push({
      type: "targetingConstraint",
      id: `${t.id}.bypass.${s}`,
      label: t.label,
      constraint: "bypassStatus",
      value: s
    });
  ge(n.movementMeters, 0) && e.push({ type: "queuedDomainRequest", id: `${t.id}.movement`, label: t.label, domain: "movement", request: { movementMeters: ge(n.movementMeters, 0) } }), (ge(n.clusteringDice, 0) || ge(n.clusteringTarget, 0)) && e.push({
    type: "queuedDomainRequest",
    id: `${t.id}.clustering`,
    label: t.label,
    domain: "clustering",
    request: {
      diceModifier: ge(n.clusteringDice, 0),
      targetNumberModifier: ge(n.clusteringTarget, 0)
    }
  });
  for (const s of i.statuses ?? [])
    e.push({ type: "derivedStatus", id: `${t.id}.status.${s}`, label: t.label, key: s, value: !0 });
  for (const s of i.actionOverrides ?? []) {
    for (const o of ka(s.actionIds ?? s.actions ?? s.actionId))
      e.push({
        type: "actionAvailability",
        id: `${t.id}.action.${o}`,
        label: t.label,
        actionId: o,
        enabled: !0,
        reason: ""
      });
    e.push({
      type: "queuedDomainRequest",
      id: `${t.id}.actionOverride`,
      label: t.label,
      domain: "actionOverride",
      request: {
        actionIds: ka(s.actionIds ?? s.actions ?? s.actionId),
        resource: Pt(s.resource, "fa"),
        cost: ge(s.cost, 0),
        category: Pt(s.category, "free")
      }
    });
  }
  for (const s of i.reactions ?? [])
    e.push({
      type: "queuedDomainRequest",
      id: `${t.id}.reaction.${s}`,
      label: t.label,
      domain: "reactionGrant",
      request: { reactionId: s }
    });
  const a = t.costs ?? {};
  return ge(a.heat, 0) && e.push({ type: "resourceSpendPreview", id: `${t.id}.heat`, label: t.label, resource: "heat", value: ge(a.heat, 0) }), ge(a.charges, 0) && e.push({ type: "resourceSpendPreview", id: `${t.id}.charges`, label: t.label, resource: "charges", value: ge(a.charges, 0) }), (r = a.stress) != null && r.location && ge(a.stress.value, 0) && e.push({
    type: "queuedDomainRequest",
    id: `${t.id}.stress`,
    label: t.label,
    domain: "stressCost",
    request: {
      location: a.stress.location,
      value: ge(a.stress.value, 0)
    }
  }), e;
}
function HD(t = {}) {
  var e, n;
  return mp({
    id: t.id,
    label: t.label,
    scope: t.scope,
    phase: "assetModuleEffect",
    mode: t.timing === "triggered" ? "triggered" : "automatic",
    selector: FD(t),
    requires: UD(t),
    outputs: KD(t),
    limits: (e = t.limits) != null && e.oncePerActivation ? { perActivation: 1 } : {},
    usage: (n = t.costs) != null && n.charges ? { charges: t.costs.charges } : null,
    summary: t.label
  });
}
function jD(t = {}) {
  var r, s, o, l, c, u, d, f, m, g, p, h, b, y, S;
  const e = {}, n = { statuses: [], actionOverrides: [], actions: [], reactions: [] }, i = {}, a = {
    oncePerActivation: ((s = (r = t.rule) == null ? void 0 : r.limits) == null ? void 0 : s.perActivation) === 1,
    cooldownTurns: ge((l = (o = t.rule) == null ? void 0 : o.usage) == null ? void 0 : l.cooldownTurns, 0)
  };
  for (const A of t.outputs ?? [])
    A.type === "dicePart" && (e.dice = ge(e.dice, 0) + ge(A.value, 0)), A.type === "cqPart" && (e.ar = ge(e.ar, 0) + ge(A.ar, 0), e.dr = ge(e.dr, 0) + ge(A.dr, 0)), A.type === "targetingConstraint" && A.constraint === "trackingPenalty" && (e.trackingPenalty = ge(e.trackingPenalty, 0) + ge(A.value, 0)), A.type === "targetingConstraint" && A.constraint === "bypassStatus" && A.value && (e.bypassStatuses ?? (e.bypassStatuses = []), e.bypassStatuses.push(A.value)), A.type === "targetingDataModifier" && (e.targetingData = ge(e.targetingData, 0) + ge(A.value, 0)), A.type === "derivedStatus" && A.key && n.statuses.push(A.key), A.type === "queuedDomainRequest" && A.domain === "movement" && (e.movementMeters = ge(e.movementMeters, 0) + ge((c = A.request) == null ? void 0 : c.movementMeters, 0)), A.type === "queuedDomainRequest" && A.domain === "clustering" && (e.clusteringDice = ge(e.clusteringDice, 0) + ge((u = A.request) == null ? void 0 : u.diceModifier, 0), e.clusteringTarget = ge(e.clusteringTarget, 0) + ge((d = A.request) == null ? void 0 : d.targetNumberModifier, 0)), A.type === "queuedDomainRequest" && A.domain === "actionOverride" && n.actionOverrides.push({
      actionIds: ka(((f = A.request) == null ? void 0 : f.actionIds) ?? ((m = A.request) == null ? void 0 : m.actionId)),
      resource: Pt((g = A.request) == null ? void 0 : g.resource, "fa"),
      cost: ge((p = A.request) == null ? void 0 : p.cost, 0),
      category: Pt((h = A.request) == null ? void 0 : h.category, "free")
    }), A.type === "queuedDomainRequest" && A.domain === "reactionGrant" && ((b = A.request) != null && b.reactionId) && n.reactions.push(A.request.reactionId), A.type === "queuedDomainRequest" && A.domain === "stressCost" && ((y = A.request) != null && y.location) && (i.stress = {
      location: A.request.location,
      value: ge(A.request.value, 0)
    }), A.type === "resourceSpendPreview" && A.resource === "heat" && (i.heat = ge(i.heat, 0) + ge(A.value, 0)), A.type === "resourceSpendPreview" && A.resource === "charges" && (i.charges = ge(i.charges, 0) + ge(A.value, 0)), A.type === "actionAvailability" && A.actionId && (n.actionOverrides.some((C) => ka(C.actionIds).includes(A.actionId)) || n.actionOverrides.push({
      actionIds: [A.actionId],
      cost: 0,
      resource: "fa",
      category: "free"
    }));
  return {
    id: t.ruleId,
    label: t.label,
    timing: t.mode === "triggered" ? "triggered" : "ready",
    scope: Pt((S = t.rule) == null ? void 0 : S.scope, "self"),
    requires: {},
    grants: n,
    modifies: e,
    costs: i,
    limits: a,
    sourceId: t.sourceId,
    sourceName: t.sourceName,
    moduleActive: !0,
    moduleReady: !0,
    ruleOutputs: t.outputs ?? []
  };
}
function Ji(t = null, e = {}) {
  var a;
  if (!_D(t)) return { effects: [] };
  const n = zD({ actor: t, ...e }), i = [];
  for (const r of UT(t)) {
    const s = no(r, { installed: !0 }), o = {
      ...n,
      tags: nm(n.tags),
      weaponTags: nm(n.weaponTags),
      statuses: nm(n.statuses),
      module: {
        ready: s.ready,
        active: s.active,
        enabled: s.enabled,
        destroyed: s.destroyed,
        suppressed: s.suppressed,
        offline: s.offline,
        selectedMode: ((a = s.activation) == null ? void 0 : a.selectedMode) ?? "",
        activation: s.activation ?? {}
      }
    }, l = Qu(r.system ?? {}).rules;
    if (l.length) {
      const c = uf({
        actor: t,
        carrierItems: [{ item: r, rules: l }],
        phase: "assetModuleEffect",
        facts: o,
        runtime: e.runtime ?? {}
      });
      i.push(...c.entries.map(jD));
      continue;
    }
    for (const c of HT(r)) {
      const u = uf({
        actor: t,
        carrierItems: [{ item: r, rules: [HD(c)] }],
        phase: "assetModuleEffect",
        facts: o,
        runtime: e.runtime ?? {}
      });
      if (!u.entries.length) continue;
      const d = u.entries[0];
      i.push({
        ...c,
        sourceId: r.id ?? "",
        sourceName: r.name ?? "Asset Module",
        moduleActive: s.active,
        moduleReady: s.ready,
        ruleOutputs: d.outputs ?? []
      });
    }
  }
  return { effects: i, facts: n };
}
function uu(t = null, e = {}) {
  return Ji(t, e).effects.filter((n) => {
    var i, a;
    return ge((i = n.modifies) == null ? void 0 : i.ar, 0) || ge((a = n.modifies) == null ? void 0 : a.dr, 0);
  });
}
function GD(t = null, e = {}) {
  var i;
  const n = /* @__PURE__ */ new Set();
  for (const a of Ji(t, e).effects)
    for (const r of ((i = a.modifies) == null ? void 0 : i.bypassStatuses) ?? []) n.add(r);
  return n;
}
function Pp(t = null, e = {}) {
  var i;
  const n = /* @__PURE__ */ new Set();
  for (const a of Ji(t, e).effects)
    if (a.scope === "self")
      for (const r of ((i = a.grants) == null ? void 0 : i.statuses) ?? []) n.add(r);
  return n;
}
function qD(t = null, e = {}) {
  return Ji(t, e).effects.reduce((n, i) => {
    var a;
    return n + ge((a = i.modifies) == null ? void 0 : a.movementMeters, 0);
  }, 0);
}
function WD(t = null, e = {}) {
  return Ji(t, {
    ...e,
    payload: { ...e.payload ?? {}, intent: "attack" },
    resolved: {
      ...e.resolved ?? {},
      intent: "attack"
    }
  }).effects.reduce((i, a) => {
    var r, s;
    return {
      diceModifier: i.diceModifier + ge((r = a.modifies) == null ? void 0 : r.clusteringDice, 0),
      targetNumberModifier: i.targetNumberModifier + ge((s = a.modifies) == null ? void 0 : s.clusteringTarget, 0),
      sourceIds: a.sourceId ? i.sourceIds.concat([a.sourceId]) : i.sourceIds,
      sourceNames: a.sourceName ? i.sourceNames.concat([a.sourceName]) : i.sourceNames
    };
  }, {
    diceModifier: 0,
    targetNumberModifier: 0,
    sourceIds: [],
    sourceNames: []
  });
}
function jT(t = null, e = "", n = {}) {
  var r;
  const i = Pt(e, "");
  if (!i) return null;
  const { effects: a } = Ji(t, {
    ...n,
    payload: {
      ...n.payload ?? {},
      actionId: i
    }
  });
  for (const s of a)
    for (const o of ((r = s.grants) == null ? void 0 : r.actionOverrides) ?? []) {
      const l = ka((o == null ? void 0 : o.actionIds) ?? (o == null ? void 0 : o.actions) ?? (o == null ? void 0 : o.actionId));
      if (!(l.length && !l.includes(i)))
        return {
          ...o,
          sourceId: s.sourceId,
          sourceName: s.sourceName
        };
    }
  return null;
}
function GT(t = "") {
  return String(t ?? "").trim().toLowerCase().replace(/[^a-z0-9]+/g, "");
}
function VD(t = null, e = {}) {
  var r, s, o;
  const n = /* @__PURE__ */ new Set(), i = (l) => {
    const c = GT(l);
    c && n.add(c);
  }, a = (l) => ka(l).forEach(i);
  i(t == null ? void 0 : t.name), i(e == null ? void 0 : e.name), i(e == null ? void 0 : e.label), i(e == null ? void 0 : e.category), a(e == null ? void 0 : e.tags), a(e == null ? void 0 : e.keywords);
  for (const l of Br(e).effects) {
    i(l.id), i(l.label), a((r = l.requires) == null ? void 0 : r.tags), a((s = l.grants) == null ? void 0 : s.actions);
    for (const c of ((o = l.grants) == null ? void 0 : o.actionOverrides) ?? [])
      a((c == null ? void 0 : c.actionIds) ?? (c == null ? void 0 : c.actions) ?? (c == null ? void 0 : c.actionId));
  }
  return n;
}
function JD(t = null, e = "", n = []) {
  const i = [e, ...n].map(GT).filter(Boolean);
  if (!i.length) return !1;
  for (const a of UT(t)) {
    const r = no(a, { installed: !0 });
    if (!r.ready) continue;
    const s = Array.from(VD(a, r.system ?? (a == null ? void 0 : a.system) ?? {}));
    if (i.some((o) => s.some((l) => l === o || l.includes(o)))) return !0;
  }
  return !1;
}
function YD(t = null, e = "", n = {}) {
  const i = Pt(e, "");
  if (!i) return { heat: 0, stress: [], charges: 0, effects: [] };
  const { effects: a } = Ji(t, {
    ...n,
    payload: {
      ...n.payload ?? {},
      actionId: i
    },
    context: {
      ...n.context ?? {},
      trigger: "onActionUse"
    }
  });
  return a.reduce((r, s) => {
    var u, d, f;
    const o = ge((u = s.costs) == null ? void 0 : u.heat, 0), l = ge((d = s.costs) == null ? void 0 : d.charges, 0), c = (f = s.costs) == null ? void 0 : f.stress;
    return {
      heat: r.heat + o,
      charges: r.charges + l,
      stress: c != null && c.location && c.value ? r.stress.concat([{ ...c, sourceName: s.sourceName, effectId: s.id }]) : r.stress,
      effects: o || l || c != null && c.location && c.value ? r.effects.concat([s]) : r.effects
    };
  }, { heat: 0, stress: [], charges: 0, effects: [] });
}
function Ii(t = 0) {
  const e = ge(t, 0);
  return `${e >= 0 ? "+" : ""}${e}`;
}
function QD(t = {}) {
  var s, o, l, c, u, d, f, m, g, p, h, b, y, S, A;
  const e = [], n = t.timing === "active" ? "Active" : t.timing === "triggered" ? "Triggered" : "Ready", i = ((s = t.grants) == null ? void 0 : s.statuses) ?? [];
  if (i.length && e.push(`applies ${i.join(", ")}`), (o = t.modifies) != null && o.dice && e.push(`${Ii(t.modifies.dice)} dice`), (l = t.modifies) != null && l.ar && e.push(`${Ii(t.modifies.ar)} AR`), (c = t.modifies) != null && c.dr && e.push(`${Ii(t.modifies.dr)} DR`), (u = t.modifies) != null && u.trackingPenalty && e.push(`${Ii(t.modifies.trackingPenalty)} trackingPenalty`), (d = t.modifies) != null && d.targetingData && e.push(`${Ii(t.modifies.targetingData)} targetingData`), (f = t.modifies) != null && f.clusteringDice && e.push(`${Ii(t.modifies.clusteringDice)} cluster dice`), (m = t.modifies) != null && m.clusteringTarget && e.push(`${Ii(t.modifies.clusteringTarget)} cluster target`), (g = t.modifies) != null && g.movementMeters && e.push(`${Ii(t.modifies.movementMeters)} m movement`), (h = (p = t.modifies) == null ? void 0 : p.bypassStatuses) != null && h.length && e.push(`bypasses ${t.modifies.bypassStatuses.join(", ")}`), (b = t.costs) != null && b.heat && e.push(`Heat ${Ii(t.costs.heat)}`), (S = (y = t.costs) == null ? void 0 : y.stress) != null && S.location && t.costs.stress.value && e.push(`${t.costs.stress.location} stress ${Ii(t.costs.stress.value)}`), !e.length) return `${n}: ${t.label}`;
  const a = ((A = t.requires) == null ? void 0 : A.actionIds) ?? [], r = a.length ? ` to ${a.join(" / ")}` : "";
  return `${n}: ${e.join("; ")}${r}.`;
}
function qT(t = null) {
  try {
    return {
      summary: HT(t).map(QD).filter(Boolean).join(" "),
      errors: []
    };
  } catch (e) {
    if (!(e instanceof Ep)) throw e;
    return {
      summary: "",
      errors: [e.userMessage ?? e.message]
    };
  }
}
const im = Object.freeze({
  strained: 2,
  overstressed: 4,
  critical: 6
}), WT = 6;
function XD(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function ji(t, e = 0) {
  return Math.max(0, Math.trunc(XD(t, e)));
}
function VT(t = {}) {
  return {
    strained: Math.max(1, ji(t.strained, im.strained)),
    overstressed: Math.max(1, ji(t.overstressed, im.overstressed)),
    critical: Math.max(1, ji(t.critical, im.critical))
  };
}
function ZD(t = {}, e = M.actorTypes.vehicle) {
  if (e !== M.actorTypes.vehicle) return t;
  const n = t.mwd = t.mwd ?? {}, i = n.strain ?? {}, a = VT(i.thresholds ?? {}), r = Math.max(a.critical, ji(i.max, WT));
  return n.strain = {
    value: Math.min(r, ji(i.value, 0)),
    pendingGenerated: Math.max(0, ji(i.pendingGenerated, 0)),
    max: r,
    thresholds: a
  }, t;
}
function ol(t = {}) {
  var c, u;
  const e = (t == null ? void 0 : t.system) ?? t ?? {}, i = ((u = ZD(
    typeof foundry < "u" && ((c = foundry.utils) != null && c.deepClone) ? foundry.utils.deepClone(e) : JSON.parse(JSON.stringify(e ?? {})),
    M.actorTypes.vehicle
  ).mwd) == null ? void 0 : u.strain) ?? {}, a = ji(i.value, 0), r = ji(i.pendingGenerated, 0), s = VT(i.thresholds ?? {}), o = a >= s.critical ? "critical" : a >= s.overstressed ? "overstressed" : a >= s.strained ? "strained" : "normal", l = {
    normal: "Normal",
    strained: "Strained",
    overstressed: "Overstressed",
    critical: "Critical"
  };
  return {
    value: a,
    pendingGenerated: r,
    max: Math.max(s.critical, ji(i.max, WT)),
    thresholds: s,
    status: o,
    label: l[o] ?? "Normal",
    redlineBlocked: o === "critical",
    redlineBlockReason: o === "critical" ? "Vehicle strain is critical; Redline is blocked until reset or recovery." : "",
    doctrine: "Temporary operational stress from Redline, not BattleMech heat accumulation."
  };
}
function JT(t = null) {
  if ((t == null ? void 0 : t.type) !== M.actorTypes.vehicle)
    return {
      handling: 0,
      system: 0,
      pilotingDice: 0,
      pilotingDn: 0,
      movementPenalty: 0,
      redlineBlocked: !1,
      effectTexts: []
    };
  const e = ol(t), n = {
    handling: 0,
    system: 0,
    pilotingDice: 0,
    pilotingDn: 0,
    movementPenalty: 0,
    redlineBlocked: e.redlineBlocked,
    effectTexts: []
  };
  return e.status === "strained" ? (n.pilotingDn += 1, n.effectTexts.push("Strained: Redline stress is making handling checks riskier.")) : e.status === "overstressed" ? (n.handling += -1, n.system += -1, n.pilotingDn += 1, n.effectTexts.push("Overstressed: -1 Handling, -1 System, and harder vehicle control checks.")) : e.status === "critical" && (n.handling += -2, n.system += -1, n.pilotingDn += 2, n.movementPenalty += bp(1), n.effectTexts.push("Critical strain: Redline blocked; mobility and control are under severe stress.")), n;
}
async function eO(t, e = 0, { reason: n = "" } = {}) {
  if (!t || t.type !== M.actorTypes.vehicle) return { ok: !1, reason: "Vehicle actor required." };
  const i = ol(t), a = Math.max(0, i.pendingGenerated + ji(e, 0));
  return await t.update({ "system.mwd.strain.pendingGenerated": a }), { ok: !0, pendingGenerated: a, reason: n };
}
async function tO(t, { reason: e = "" } = {}) {
  if (!t || t.type !== M.actorTypes.vehicle) return { ok: !1, reason: "Vehicle actor required." };
  const n = ol(t), i = Math.min(n.max, n.value + n.pendingGenerated);
  return await t.update({
    "system.mwd.strain.value": i,
    "system.mwd.strain.pendingGenerated": 0
  }), { ok: !0, value: i, applied: n.pendingGenerated, reason: e };
}
const sc = Object.freeze({
  contact: 1,
  track: 2,
  lock: 3
});
function du(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function rd(t = null) {
  return (t == null ? void 0 : t.type) === M.actorTypes.battlemech ? M.actorTypes.battlemech : M.actorTypes.vehicle;
}
function Ot(t = null, e = "") {
  var n, i;
  return (((i = (n = t == null ? void 0 : t.statuses) == null ? void 0 : n.has) == null ? void 0 : i.call(n, e)) ?? !1) || Pp(t).has(e);
}
function Qb(t = "lock", e = "lock") {
  const n = sc[t] ?? sc.lock;
  return (sc[e] ?? sc.lock) < n ? e : t;
}
function $t(t = null, e = "") {
  var n, i, a;
  return ((a = (i = (n = t == null ? void 0 : t.system) == null ? void 0 : n.mwd) == null ? void 0 : i.locations) == null ? void 0 : a[e]) ?? {};
}
function Wt(t = {}) {
  return Math.max(0, Math.trunc(du(t == null ? void 0 : t.condition, 0)));
}
function YT(t = null, { weaponGroupId: e = "", weaponId: n = "", weapon: i = null, rangeBand: a = "" } = {}) {
  var m, g;
  const r = Ze(e), s = Ze(n), o = id(t), l = o.find((p) => Ze(p == null ? void 0 : p.id) === r) ?? null, c = i ?? (s ? ((g = (m = t == null ? void 0 : t.items) == null ? void 0 : m.get) == null ? void 0 : g.call(m, s)) ?? null : null), u = l ?? o.find(
    (p) => (Array.isArray(p == null ? void 0 : p.weaponIds) ? p.weaponIds : []).some((h) => Ze(h) === s)
  ) ?? null, d = u ? al(t, u) : c ? [c] : [], f = new Set(d.map(
    (p) => {
      var h;
      return lu((h = nl(t, p == null ? void 0 : p.id)) == null ? void 0 : h.location);
    }
  ).filter(Boolean));
  return {
    weaponGroupId: Ze(u == null ? void 0 : u.id),
    weapons: d,
    families: f,
    isArmMounted: f.has("arms"),
    isTurretMounted: f.has("turret"),
    hasEnergy: d.some(ST),
    rangeBand: String(a ?? "").trim().toLowerCase()
  };
}
function Ne(t, e = "") {
  const n = String(e ?? "").trim();
  n && (t.effectTexts.includes(n) || t.effectTexts.push(n));
}
function mu(t, e = 1) {
  t.movementPenalty += bp(e);
}
function nO(t, e = null) {
  for (const n of tr(e)) {
    const i = String((n == null ? void 0 : n.statusId) ?? "").trim();
    i === "unstable" ? (t.pilotingDice += -2, Ne(t, "Unstable: -2 dice to Piloting; movement risks a fall.")) : i === "staggeredMechanical" ? t.startTurnSaLoss += 1 : i === "overheating" ? t.startTurnHeat += 2 : i === "reactorInstability" ? (t.energyAttackHeat += 1, t.energyAttackDamage += -1) : i === "stalled" ? (t.handling += -1, t.system += -1, t.noSprint = !0, t.noJump = !0) : i === "limping" ? (mu(t, 1), t.pilotingDn += 1) : i === "jumpJetFailure" && (t.noJump = !0), String((n == null ? void 0 : n.key) ?? "").trim() === "opticsCoolantFog" && (t.rangeCapClose = !0, Ne(t, "Optics Coolant Fog: attacks beyond Close are blocked."));
  }
}
function iO(t, e = null) {
  Ot(e, "sensorDegraded") && (t.acquireDice += -2, Ne(t, "Sensor Degraded: -2 dice to Acquire Target.")), Ot(e, "sensorBlind") && (t.rangeCapClose = !0, t.noTargetingDataGeneration = !0, t.noTargetingDataUse = !0, Ne(t, "Sensor Blind: no Targeting Data and attacks beyond Close are blocked.")), Ot(e, "trackingLost") && (t.noTargetingDataUse = !0, Ne(t, "Tracking Lost: stored Targeting Data is unusable.")), (Ot(e, "epmBoosted") || Ot(e, "eccmBoosted")) && (t.ecmSpikeDefenseDice += 2, Ne(t, "EPM Boosted: -2 dice against incoming ECM Spike attempts.")), Ot(e, "coolingFailure") && (t.coolingImpaired = !0, Ne(t, "Cooling Failure: heat dissipation reduced by 2.")), Ot(e, "shutdown") && (t.cannotAct = !0, t.rangeCapClose = !0, t.noTargetingDataGeneration = !0, t.noTargetingDataUse = !0, Ne(t, "Shutdown: the machine cannot act until it is power-cycled.")), Ot(e, "proneMechFall") && (t.prone = !0, t.noSprint = !0, t.noJump = !0, Ne(t, "Prone: crawl only; Close attacks are impaired and Close defense is compromised.")), Ot(e, "onFire") && (t.startTurnHeat += 1, Ne(t, "On Fire: gain 1 Heat at the start of the turn.")), Ot(e, "actuatorFailure") && (t.handling += -1), Ot(e, "gyroDamage") && (t.pilotingDn += 1), Ot(e, "suppressedMechanical") && (t.attackAr += -2, t.noTargetingDataGeneration = !0), Ot(e, "evasiveWeave") && (t.attackAr += -2, t.defenseDr += 3), Ot(e, "shielded") && (t.attackAr += -1, t.defenseDr += 4), Ot(e, "braced") && (t.attackAr += 1), Ot(e, "targetFocused") && (t.defenseDr += -2, t.targetFocused = !0), Ot(e, "entrenchedHullDown") && (t.defenseDr += (e == null ? void 0 : e.type) === M.actorTypes.vehicle ? 7 : 5, (e == null ? void 0 : e.type) === M.actorTypes.vehicle && Ne(t, "Hull Down: vehicle gains a strong prepared-position defense bonus.")), Ot(e, "exposed") && (t.defenseDr += -2);
}
function aO(t, e = null) {
  if (rd(e) !== M.actorTypes.vehicle) return;
  const n = JT(e);
  t.handling += n.handling, t.system += n.system, t.pilotingDice += n.pilotingDice, t.pilotingDn += n.pilotingDn, t.movementPenalty += n.movementPenalty, t.redlineBlocked = !!n.redlineBlocked;
  for (const i of n.effectTexts ?? []) Ne(t, i);
}
function rO(t, e = null) {
  var c, u;
  if (rd(e) !== M.actorTypes.battlemech) return;
  const n = (e == null ? void 0 : e.system) ?? {}, i = ((c = n == null ? void 0 : n.monitors) == null ? void 0 : c.heat) ?? {}, a = ((u = n == null ? void 0 : n.mwd) == null ? void 0 : u.heat) ?? {}, r = Math.max(0, du(i.value ?? a.current, 0)), s = Math.max(0, du(i.max ?? a.max ?? a.hardMax, 0)), o = td(a.thresholds ?? {}, s), l = ed(r, o).movementPenalty;
  l <= 0 || (t.movementPenalty += l, Ne(t, `Heat: -${l} m movement speed.`));
}
function sO(t, e = null) {
  const n = $t(e, "head"), i = $t(e, "torso"), a = $t(e, "arms"), r = $t(e, "legs"), s = Wt(n);
  s >= 1 && (t.trackingPenaltyAsTarget += 1, Ne(t, "Head impaired: attackers gain +1 tracking penalty bonus against this machine.")), s >= 2 && (t.detectionStateCap = Qb(t.detectionStateCap, "track"), Ne(t, "Head damaged: detection state against this machine is capped at Track.")), s >= 3 && (t.noSensorActions = !0, Ne(t, "Head crippled: no sensor or ECM actions.")), s >= 4 && (t.detectionStateCap = Qb(t.detectionStateCap, "contact"), Ne(t, "Head disabled: detection state against this machine is capped at Contact."));
  const o = Wt(i);
  o >= 1 && (t.generalAttackDice += -1, Ne(t, "Torso impaired: -1 die on attacks.")), o >= 2 && (t.attackHeat += 1, Ne(t, "Torso damaged: attacks generate +1 Heat.")), o >= 3 && (t.pilotingDn += 1, Ne(t, "Torso crippled: Gyro Damage (+1 Piloting DN).")), o >= 4 && (t.reactorBreach = !0, Ne(t, "Torso disabled: Reactor Breach risk is active."));
  const l = Wt(a);
  l >= 1 && (t.armAttackDice += -1), l >= 2 && (t.armAttackDice += -1), l >= 3 && (t.armAttackDice += -1), l >= 4 && (t.armMountedOffline = !0, Ne(t, "Arms disabled: all arm-mounted weapon groups are offline.")), l > 0 && Ne(t, `Arms ${ce(["", "impaired", "damaged", "crippled", "disabled"][Math.min(l, 4)])}: arm-mounted attacks are degraded.`);
  const c = Wt(r);
  c >= 1 && (mu(t, 1), Ne(t, "Legs impaired: -30 m movement.")), c >= 2 && (t.noSprint = !0, Ne(t, "Legs damaged: cannot Sprint.")), c >= 3 && (t.handling += -1, t.noSprint = !0, Ne(t, "Legs crippled: Actuator Failure (-1 Handling).")), c >= 4 && (t.immobile = !0, Ne(t, "Legs disabled: immobile.")), r != null && r.destroyed && (t.immobile = !0, t.forcedProne = !0, Ne(t, "Legs destroyed: prone and immobilized.")), a != null && a.destroyed && Ne(t, "Arms destroyed: arm-mounted weapon groups are destroyed."), n != null && n.destroyed && Ne(t, "Head destroyed: pilot survival is unlikely."), i != null && i.destroyed && Ne(t, "Torso destroyed: reactor detonation is imminent.");
}
function oO(t, e = null) {
  const n = $t(e, "body"), i = $t(e, "turret"), a = $t(e, "mobility"), r = Wt(n);
  r >= 1 && (t.system += -1, t.reliability += -1, Ne(t, "Body impaired: -1 System and -1 Reliability.")), r >= 2 && (t.attackHeat += 1, Ne(t, "Body damaged: attacks generate +1 Heat.")), r >= 3 && (t.system += -2, t.handling += -2, Ne(t, "Body crippled: -2 System and -2 Handling.")), r >= 4 && (t.cannotAct = !0, Ne(t, "Body disabled: the vehicle is disabled."));
  const s = Wt(i);
  s >= 1 && (t.turretAttackDice += -1), s >= 2 && (t.turretAttackDice += -1), s >= 3 && (t.trackingPenaltyAsTarget += 2, Ne(t, "Turret crippled: +2 tracking penalty against the vehicle.")), s >= 4 && (t.turretOffline = !0, Ne(t, "Turret disabled: turret weapons are offline."));
  const o = Wt(a);
  o >= 1 && mu(t, 1), o >= 2 && mu(t, 1), o >= 3 && (t.noSprint = !0, Ne(t, "Mobility crippled: cannot Sprint.")), o >= 4 && (t.immobile = !0, Ne(t, "Mobility disabled: immobile."));
}
function pn(t = null) {
  const e = {
    handling: 0,
    system: 0,
    reliability: 0,
    pilotingDn: 0,
    pilotingDice: 0,
    acquireDice: 0,
    targetingDice: 0,
    generalAttackDice: 0,
    armAttackDice: 0,
    turretAttackDice: 0,
    attackAr: 0,
    defenseDr: 0,
    movementBonus: 0,
    movementPenalty: 0,
    trackingPenaltyAsTarget: 0,
    ecmSpikeDefenseDice: 0,
    startTurnHeat: 0,
    startTurnSaLoss: 0,
    attackHeat: 0,
    energyAttackHeat: 0,
    energyAttackDamage: 0,
    noSprint: !1,
    noJump: !1,
    noSensorActions: !1,
    noTargetingDataGeneration: !1,
    noTargetingDataUse: !1,
    cannotAct: !1,
    immobile: !1,
    rangeCapClose: !1,
    coolingImpaired: !1,
    detectionStateCap: "lock",
    armMountedOffline: !1,
    turretOffline: !1,
    targetFocused: !1,
    reactorBreach: !1,
    forcedProne: !1,
    prone: !1,
    redlineBlocked: !1,
    effectTexts: []
  };
  return aO(e, t), rO(e, t), e.movementBonus += qD(t), nO(e, t), iO(e, t), rd(t) === M.actorTypes.battlemech ? sO(e, t) : oO(e, t), e;
}
function lO(t = null) {
  return pn(t).pilotingDn;
}
function cO(t = null) {
  return pn(t).pilotingDice;
}
function uO(t = null) {
  return pn(t).acquireDice;
}
function dO(t = null) {
  return pn(t).targetingDice;
}
function mO(t = null, e = {}) {
  const n = pn(t), i = YT(t, e);
  let a = n.generalAttackDice;
  return i.isArmMounted && (a += n.armAttackDice), i.isTurretMounted && (a += n.turretAttackDice), n.prone && i.rangeBand === "close" && (a += -3), a;
}
function ll(t = null, e = {}) {
  const n = pn(t), i = String((e == null ? void 0 : e.rangeBand) ?? "").trim().toLowerCase();
  return {
    ar: n.attackAr,
    dr: n.defenseDr + (n.prone && i === "close" ? -5 : 0)
  };
}
function fO(t = null, e = {}) {
  const n = pn(t), i = YT(t, e);
  return n.cannotAct ? { blocked: !0, reason: "Shutdown prevents the machine from acting." } : n.armMountedOffline && i.isArmMounted ? { blocked: !0, reason: "Arm-mounted weapon groups are offline from degradation." } : n.turretOffline && i.isTurretMounted ? { blocked: !0, reason: "Turret weapons are offline from degradation." } : { blocked: !1, reason: "" };
}
function gO(t = null) {
  const e = pn(t);
  return e.cannotAct || e.noSensorActions;
}
function pO(t = null) {
  const e = pn(t);
  return e.cannotAct || e.noSensorActions || e.noTargetingDataGeneration;
}
function hO(t = null) {
  const e = pn(t);
  return e.noTargetingDataUse || e.cannotAct;
}
function bO(t = null) {
  return pn(t).rangeCapClose;
}
function yO(t = null) {
  return pn(t).trackingPenaltyAsTarget;
}
function SO(t = null) {
  return pn(t).detectionStateCap;
}
function AO({ attacker: t = null, targetActor: e = null, value: n = 0 } = {}) {
  let i = Math.max(0, du(n, 0));
  return hO(t) ? 0 : (Ot(e, "ecmJamming") && (i = Math.max(0, i - 2)), i);
}
function Rp(t = null) {
  const e = pn(t);
  return {
    startTurnHeat: e.startTurnHeat,
    coolingImpaired: e.coolingImpaired,
    attackHeat: e.attackHeat,
    energyAttackHeat: e.energyAttackHeat,
    energyAttackDamage: e.energyAttackDamage
  };
}
function Rr(t = null) {
  const e = pn(t);
  return {
    movementPenalty: e.movementPenalty,
    movementBonus: e.movementBonus,
    noSprint: e.noSprint,
    noJump: e.noJump,
    immobile: e.immobile,
    forcedProne: e.forcedProne
  };
}
function kO(t = "", e = "", n = {}) {
  const i = Wt(n), a = !!(n != null && n.destroyed);
  if (t === M.actorTypes.battlemech) {
    if (e === "head") {
      if (a) return "Pilot dead.";
      if (i >= 4) return "Contact state capped at Contact; pilot incapacitation risk.";
      if (i >= 3) return "No sensor or ECM actions.";
      if (i >= 2) return "Contact state capped at Track.";
      if (i >= 1) return "+1 tracking penalty.";
    }
    if (e === "torso") {
      if (a) return "Reactor detonation; pilot dead.";
      if (i >= 4) return "Reactor Breach.";
      if (i >= 3) return "Gyro Damage.";
      if (i >= 2) return "+1 Heat to all actions.";
      if (i >= 1) return "-1 die on attacks.";
    }
    if (e === "arms") {
      if (a) return "All arm-mounted weapon groups destroyed.";
      if (i >= 4) return "All arm-mounted weapon groups offline.";
      if (i >= 3) return "-3 dice on attacks with arm-mounted weapon groups.";
      if (i >= 2) return "-2 dice on attacks with arm-mounted weapon groups.";
      if (i >= 1) return "-1 die on attacks with arm-mounted weapon groups.";
    }
    if (e === "legs") {
      if (a) return "Prone and immobilized.";
      if (i >= 4) return "Immobile.";
      if (i >= 3) return "Actuator Failure; cannot run.";
      if (i >= 2) return "Cannot Sprint.";
      if (i >= 1) return "-30 m movement.";
    }
  } else {
    if (e === "body") {
      if (a) return "Vehicle destroyed; crew dead.";
      if (i >= 4) return "Vehicle disabled; crew incapacitation risk.";
      if (i >= 3) return "-2 System and -2 Handling.";
      if (i >= 2) return "+1 Heat for all actions.";
      if (i >= 1) return "-1 System and -1 Reliability.";
    }
    if (e === "turret") {
      if (a) return "Weapons destroyed.";
      if (i >= 4) return "Weapons offline.";
      if (i >= 3) return "+2 tracking penalty.";
      if (i >= 2) return "-2 dice on attacks.";
      if (i >= 1) return "-1 die on attacks.";
    }
    if (e === "mobility") {
      if (a || i >= 4) return "Immobile.";
      if (i >= 3) return "Cannot Sprint.";
      if (i >= 2) return "-60 m movement.";
      if (i >= 1) return "-30 m movement.";
    }
  }
  return "";
}
function TO(t = null) {
  var i, a;
  const e = rd(t), n = /* @__PURE__ */ new Set();
  return e === M.actorTypes.battlemech && (Wt($t(t, "head")) >= 1 && n.add("sensorDegraded"), Wt($t(t, "head")) >= 3 && n.add("sensorBlind"), Wt($t(t, "torso")) >= 3 && n.add("gyroDamage"), Wt($t(t, "torso")) >= 4 && n.add("reactorBreach"), Wt($t(t, "legs")) >= 3 && n.add("actuatorFailure"), (i = $t(t, "arms")) != null && i.destroyed && n.add("armDestroyed"), (Wt($t(t, "legs")) >= 4 || (a = $t(t, "legs")) != null && a.destroyed) && n.add("legDestroyed")), e === M.actorTypes.vehicle && (Wt($t(t, "turret")) >= 4 && n.add("weaponFailure"), Wt($t(t, "mobility")) >= 4 && n.add("stalled"), Wt($t(t, "body")) >= 2 && n.add("sensorDegraded"), Wt($t(t, "body")) >= 4 && n.add("shutdown")), Array.from(n);
}
function fu(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function Qt(t, e = 0) {
  return Math.max(e, fu(t, e));
}
function cl(t) {
  return Array.isArray(t) ? t : [];
}
function Dp(t = {}) {
  return (t == null ? void 0 : t.system) ?? t ?? {};
}
function wO(t) {
  return String((t == null ? void 0 : t.name) ?? "BattleMech").trim() || "BattleMech";
}
function QT(t = {}) {
  var e;
  return cl((e = t == null ? void 0 : t.mwd) == null ? void 0 : e.crits).filter((n) => (n == null ? void 0 : n.active) !== !1);
}
function gu(t = {}) {
  var e, n, i;
  return Qt((i = (n = (e = Dp(t)) == null ? void 0 : e.mwd) == null ? void 0 : n.heat) == null ? void 0 : i.pendingGenerated, 0);
}
function MO(t = null) {
  if (!t) return "";
  const e = String((t == null ? void 0 : t.combatId) ?? "").trim(), n = String((t == null ? void 0 : t.combatantId) ?? "").trim(), i = Number((t == null ? void 0 : t.round) ?? NaN), a = Number((t == null ? void 0 : t.turn) ?? NaN);
  return !e || !n || !Number.isFinite(i) || !Number.isFinite(a) ? "" : `${e}:${n}:${i}:${a}`;
}
function Af(t = {}) {
  const e = (t == null ? void 0 : t.system) ?? t ?? {}, n = [
    e.baseDamageType,
    e.damageType,
    e.category,
    e.weaponCategory,
    e.attackType
  ].map((a) => String(a ?? "").trim().toLowerCase()).filter(Boolean), i = cl(e.traits).map((a) => String(a ?? "").trim().toLowerCase());
  return n.some(tl) || i.some(tl);
}
function vO({ weapons: t = [], crits: e = [] } = {}) {
  const n = cl(t).filter(Boolean), i = cl(e).filter((c) => (c == null ? void 0 : c.active) !== !1), a = n.reduce((c, u) => {
    var d;
    return c + Qt(((d = u == null ? void 0 : u.system) == null ? void 0 : d.heat) ?? (u == null ? void 0 : u.heat), 0);
  }, 0), r = n.some(Af), s = i.map((c) => (c == null ? void 0 : c.resourceEffects) ?? {}).filter((c) => c && typeof c == "object"), o = s.reduce((c, u) => c + Qt(u.heatPerAttack, 0), 0), l = r ? s.reduce((c, u) => c + Qt(u.heatPerEnergyAttack, 0), 0) : 0;
  return {
    weaponCount: n.length,
    baseHeat: a,
    extraAttackHeat: o,
    extraEnergyHeat: l,
    total: a + o + l
  };
}
function ma(t = {}) {
  var v, N, P, L, _, O, x, H;
  const e = Dp(t), n = ((v = e == null ? void 0 : e.monitors) == null ? void 0 : v.heat) ?? {}, i = ((N = e == null ? void 0 : e.mwd) == null ? void 0 : N.heat) ?? {}, a = ((P = e == null ? void 0 : e.hybrid) == null ? void 0 : P.heat) ?? {}, r = QT(e), s = Rp(t), o = Qt(n.value ?? i.current, 0), l = Qt(n.max ?? i.max ?? i.hardMax, 0), c = td(i.thresholds ?? {}, l), u = {
    actor: t != null && t.system ? t : null,
    source: t,
    systemData: e,
    dissipationModifier: 0,
    effectiveDissipationModifier: 0,
    thresholds: c
  };
  Nn(Rt.heat.collectHeatProfile, u);
  const d = Math.max(
    l,
    o,
    Qt(c.shutdown ?? c.danger, 0)
  ), f = Qt((a.dissipation ?? i.ventPerTurn) + fu(u.dissipationModifier, 0), 1), m = r.some((G) => (G == null ? void 0 : G.escalationKey) === "heat"), g = !!(i.coolingImpaired || m || s.coolingImpaired), p = Math.max(
    1,
    (g ? Math.max(1, f - 2) : f) + fu(u.effectiveDissipationModifier, 0)
  ), h = gu(e), b = String(i.lastResolvedActivationKey ?? "").trim(), y = aT(o, c, l), S = ed(o, c), A = S.dangerLevel > 0, T = Qt((_ = (L = e == null ? void 0 : e.attributes) == null ? void 0 : L.chassis) == null ? void 0 : _.value, 0), C = Qt((x = (O = e == null ? void 0 : e.attributes) == null ? void 0 : O.reliability) == null ? void 0 : x.value, 0), k = A ? iT(S.dangerLevel, T, C) : null, w = RP(((H = e == null ? void 0 : e.mwd) == null ? void 0 : H.locations) ?? {});
  return {
    current: o,
    max: l,
    trackLength: l,
    displayMax: d,
    dissipation: f,
    effectiveDissipation: p,
    coolingImpaired: g,
    pendingGenerated: h,
    lastResolvedActivationKey: b,
    thresholds: c,
    statusCode: y,
    status: rT(y),
    penalties: S,
    inDanger: A,
    dangerChecks: k,
    volatile: w
  };
}
function CO(t = {}, { pendingGenerated: e = null } = {}) {
  var g, p, h, b;
  const n = Dp(t), i = ma(n), a = e === null ? i.pendingGenerated : Qt(e, 0), r = {
    source: t,
    systemData: n,
    heat: i,
    generated: a,
    effectiveDissipation: i.effectiveDissipation
  };
  Nn(Rt.heat.beforeHeatDissipation, r);
  const s = Qt(r.generated, 0), o = Qt(r.effectiveDissipation, 1), l = PP(i.current, s, o, i.trackLength), c = aT(l, i.thresholds, i.trackLength), u = ed(l, i.thresholds), d = u.dangerLevel > 0, f = Qt((p = (g = n == null ? void 0 : n.attributes) == null ? void 0 : g.chassis) == null ? void 0 : p.value, 0), m = Qt((b = (h = n == null ? void 0 : n.attributes) == null ? void 0 : h.reliability) == null ? void 0 : b.value, 0);
  return {
    ...i,
    previousHeat: i.current,
    generated: s,
    effectiveDissipation: o,
    newHeat: l,
    statusCode: c,
    status: rT(c),
    penalties: u,
    inDanger: d,
    dangerChecks: d ? iT(u.dangerLevel, f, m) : null,
    pendingGeneratedAfter: 0
  };
}
async function Op(t, e, { reason: n = "" } = {}) {
  if (!t || t.type !== "battlemech") return { ok: !1, reason: "BattleMech actor required." };
  const i = Qt(e, 0);
  return await t.update({
    "system.mwd.heat.pendingGenerated": i,
    "system.mwd.heat.lastResolvedActivationKey": ""
  }), {
    ok: !0,
    actor: t,
    pendingGenerated: i,
    reason: n
  };
}
async function sd(t, e, { reason: n = "" } = {}) {
  if (!t || t.type !== "battlemech") return { ok: !1, reason: "BattleMech actor required." };
  const i = gu(t);
  return Op(t, i + fu(e, 0), { reason: n });
}
async function XT(t, { weaponIds: e = [], attackProfile: n = null, reason: i = "" } = {}) {
  var m, g;
  if (!t || t.type !== "battlemech") return { ok: !1, reason: "BattleMech actor required." };
  const a = Array.from(new Set(cl(e).map((p) => String(p ?? "").trim()).filter(Boolean))), r = a.map((p) => {
    var h, b;
    return (b = (h = t.items) == null ? void 0 : h.get) == null ? void 0 : b.call(h, p);
  }).filter(Boolean), s = Qt((n == null ? void 0 : n.heat) ?? ((m = n == null ? void 0 : n.attackSummary) == null ? void 0 : m.heat), 0), o = vO({
    weapons: r,
    crits: QT(t.system)
  });
  o.weaponCount = Math.max(o.weaponCount, a.length, s > 0 ? 1 : 0), o.baseHeat = Math.max(o.baseHeat, s);
  const l = Rp(t), c = r.some(Af) || Af(n);
  o.extraAttackHeat += Math.max(0, Number(l.attackHeat ?? 0)), o.extraEnergyHeat += c ? Math.max(0, Number(l.energyAttackHeat ?? 0)) : 0, o.total = o.baseHeat + o.extraAttackHeat + o.extraEnergyHeat;
  const u = {
    actor: t,
    reason: i,
    weaponIds: a,
    attackProfile: n,
    contribution: o
  };
  if (Nn(Rt.heat.beforeHeatGenerated, u), o.total = Qt(((g = u.contribution) == null ? void 0 : g.total) ?? o.total, 0), o.total <= 0)
    return { ok: !0, actor: t, pendingGenerated: gu(t), contribution: o };
  const f = gu(t) + o.total;
  return await t.update({
    "system.mwd.heat.pendingGenerated": f,
    "system.mwd.heat.lastResolvedActivationKey": ""
  }), Nn(Rt.heat.afterHeatGenerated, {
    ...u,
    pendingGenerated: f
  }), {
    ok: !0,
    actor: t,
    reason: i,
    pendingGenerated: f,
    contribution: o
  };
}
async function IO(t, e, { source: n = "" } = {}) {
  if (!t || !(e != null && e.inDanger) || !(e != null && e.dangerChecks)) return;
  const i = wO(t), a = String(n ?? "").trim(), r = [
    `<h3>${i} - Danger Zone (${e.penalties.dangerLevel} level${e.penalties.dangerLevel > 1 ? "s" : ""})</h3>`,
    a ? `<p><em>Heat resolved via ${a}.</em></p>` : "",
    `<p><strong>Shutdown Check:</strong> Roll <strong>${e.dangerChecks.shutdownPool}d6</strong> vs DN <strong>${e.dangerChecks.shutdownDN}</strong> (Chassis + Reliability). On failure, compare margin to pilot's System Operations: if margin &lt; System Operations the pilot may override; otherwise the mech shuts down.</p>`,
    e.volatile ? `<p><strong>Explosion Check:</strong> Roll <strong>${e.dangerChecks.explosionPool}d6</strong> vs DN <strong>${e.dangerChecks.explosionDN}</strong> (Chassis + Reliability - ${e.penalties.dangerLevel}). Failure detonates volatile components.</p>` : "<p><em>No volatile components detected - Explosion check skipped.</em></p>"
  ].filter(Boolean);
  await ChatMessage.create({
    speaker: ChatMessage.getSpeaker({ actor: t }),
    content: r.join("")
  });
}
async function Lp(t, { source: e = "", postDangerCard: n = !0, activation: i = null, force: a = !1 } = {}) {
  if (!t || t.type !== "battlemech") return { ok: !1, reason: "BattleMech actor required." };
  const r = CO(t), s = MO(i);
  return !a && s && r.lastResolvedActivationKey === s ? {
    ok: !0,
    actor: t,
    source: e,
    skipped: !0,
    resolution: r
  } : (await t.update({
    "system.monitors.heat.value": r.newHeat,
    "system.mwd.heat.current": r.newHeat,
    "system.mwd.heat.pendingGenerated": r.pendingGeneratedAfter,
    "system.mwd.heat.lastResolvedActivationKey": s
  }), Nn(Rt.heat.afterHeatDissipation, {
    actor: t,
    source: e,
    activation: i,
    resolution: r
  }), n && r.inDanger && (Nn(Rt.heat.beforeDangerCheck, {
    actor: t,
    source: e,
    activation: i,
    resolution: r
  }), await IO(t, r, { source: e })), {
    ok: !0,
    actor: t,
    source: e,
    resolution: r
  });
}
function EO(t = null, e = "") {
  const n = Ze(e);
  return n ? id(t).find((i) => Ze(i == null ? void 0 : i.id) === n) ?? null : null;
}
function NO(t = {}, e = {}) {
  const n = Ze(t == null ? void 0 : t.weaponGroupId);
  if (n && n === Ze(e.weaponGroupId)) return !0;
  const i = new Set((Array.isArray(e.weaponIds) ? e.weaponIds : []).map(Ze).filter(Boolean));
  return i.size > 0 && (Array.isArray(t == null ? void 0 : t.weaponIds) ? t.weaponIds : []).some((a) => i.has(Ze(a)));
}
function ZT(t = null, { weaponGroupId: e = "", weaponId: n = "", weapon: i = null } = {}) {
  var d, f;
  const a = Ze(e), r = Ze(n), s = EO(t, a), o = i ?? (r ? ((f = (d = t == null ? void 0 : t.items) == null ? void 0 : d.get) == null ? void 0 : f.call(d, r)) ?? null : null), l = id(t);
  let c = s;
  !c && r && (c = l.find((m) => (Array.isArray(m == null ? void 0 : m.weaponIds) ? m.weaponIds : []).map(Ze).includes(r)) ?? null);
  const u = c ? al(t, c) : o ? [o] : [];
  return {
    weaponGroupId: Ze(c == null ? void 0 : c.id),
    weaponGroupName: String((c == null ? void 0 : c.name) ?? "").trim(),
    weaponIds: u.map((m) => Ze(m == null ? void 0 : m.id)).filter(Boolean),
    weapons: u,
    hasEnergy: u.some(ST),
    hasBallistic: u.some(AT)
  };
}
function od(t = null) {
  const n = tr(t).reduce((i, a) => Ze(a == null ? void 0 : a.key) === "targetingProcessorLock" ? i + 1 : i, 0);
  return {
    baseCost: 2,
    extraCost: n,
    totalCost: 2 + n
  };
}
function PO(t = null, { weaponGroupId: e = "", weaponId: n = "", weapon: i = null } = {}) {
  const a = ZT(t, { weaponGroupId: e, weaponId: n, weapon: i }), r = tr(t);
  for (const s of r) {
    if (!NO(s, a)) continue;
    const o = Ze(s == null ? void 0 : s.statusId);
    if (o === "weaponFailure")
      return {
        blocked: !0,
        crit: s,
        reason: s != null && s.weaponGroupName ? `${s.weaponGroupName} is unusable because of ${s.label}.` : `${s.label} prevents this weapon group from attacking.`
      };
    if (o === "jammedBallistic" && a.hasBallistic)
      return {
        blocked: !0,
        crit: s,
        reason: s != null && s.weaponGroupName ? `${s.weaponGroupName} is jammed and cannot make ballistic attacks.` : `${s.label} prevents this ballistic attack.`
      };
  }
  return { blocked: !1, crit: null, reason: "" };
}
function RO(t = null, { weaponGroupId: e = "", weaponId: n = "", weapon: i = null } = {}) {
  return ZT(t, { weaponGroupId: e, weaponId: n, weapon: i }).hasEnergy ? tr(t).reduce((r, s) => Ze(s == null ? void 0 : s.statusId) === "reactorInstability" ? r - 1 : r, 0) : 0;
}
function DO(t = null) {
  const e = tr(t), n = {
    saCost: 0,
    heatDelta: 0,
    automated: [],
    reminders: []
  };
  for (const i of e) {
    const a = Ze(i == null ? void 0 : i.statusId);
    if (a === "staggeredMechanical") {
      n.saCost += 1, n.automated.push({
        crit: i,
        label: "Staggered",
        text: "Spent 1 SA at the start of activation."
      });
      continue;
    }
    if (a === "overheating") {
      n.heatDelta += 2, n.automated.push({
        crit: i,
        label: "Overheating",
        text: "Gained 2 Heat at the start of activation."
      });
      continue;
    }
    a === "skidding" && n.reminders.push({
      crit: i,
      label: "Skidding",
      text: ad(i)
    });
  }
  return n;
}
function $p(t = {}) {
  return {
    statusLabel: Mp(t),
    effectText: ad(t),
    automationMode: vp(t),
    scopeSummary: _R(t)
  };
}
function ew(t = {}) {
  const e = $p(t), n = e.automationMode === "engine" ? "Automated" : "Reminder";
  return [
    e.statusLabel ? `${e.statusLabel}: ${e.effectText}` : e.effectText,
    e.scopeSummary,
    n
  ].filter(Boolean).join(" | ");
}
function Nt(t = null) {
  return (t == null ? void 0 : t.type) === M.actorTypes.vehicle || (t == null ? void 0 : t.type) === M.actorTypes.battlemech;
}
const ul = Object.freeze({
  chainFire: Object.freeze({
    id: "chainFire",
    label: "Chain Fire",
    saCost: 1,
    implemented: !0,
    coverageHint: "1 group",
    hint: "Controlled fire with one selected weapon group."
  }),
  alphaStrike: Object.freeze({
    id: "alphaStrike",
    label: "Alpha Strike",
    saCost: 2,
    implemented: !0,
    coverageHint: "all groups",
    hint: "Baseline BattleMech attack: fire every available weapon group."
  }),
  rapidFire: Object.freeze({
    id: "rapidFire",
    label: "Rapid Fire",
    saCost: 2,
    implemented: !0,
    coverageHint: "1 group xN",
    hint: "Sustained fire from one rapid-capable weapon group."
  })
}), Jt = "alphaStrike", OO = Object.freeze(Object.keys(ul));
function tw(t = "") {
  return ul[String(t ?? "").trim()] ?? ul[Jt];
}
const vn = Object.freeze(["blind", "contact", "track", "lock"]), LO = Object.freeze({
  blind: "Blind",
  contact: "Contact",
  track: "Track",
  lock: "Lock"
});
function $O(t) {
  const e = vn.indexOf(t);
  return e < 0 || e >= vn.length - 1 ? vn.includes(t) ? t : "blind" : vn[e + 1];
}
function _p(t, e) {
  const n = Math.max(0, Number(t) || 0);
  return e === "lock" ? n + 1 : e === "track" ? n : 0;
}
function wn(t) {
  return LO[t] ?? String(t ?? "Unknown");
}
function nw(t) {
  return t === "blind" ? 1 : t === "contact" ? 2 : t === "track" ? 3 : 1;
}
const Cs = Object.freeze({
  intact: "intact",
  breached: "breached",
  wrecked: "wrecked"
}), Nc = Object.freeze({
  worn: "battleArmorWorn",
  breached: "battleArmorBreached",
  wrecked: "battleArmorWrecked",
  revealed: "battleArmorRevealed",
  attached: "attachedToMachine",
  tagged: "tagged",
  narced: "narced"
}), iw = 1;
function Xb(t) {
  var e;
  return typeof foundry < "u" && ((e = foundry == null ? void 0 : foundry.utils) != null && e.deepClone) ? foundry.utils.deepClone(t) : JSON.parse(JSON.stringify(t ?? null));
}
function aw(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function ti(t, e = 0) {
  return Math.max(0, Math.trunc(aw(t, e)));
}
function Pa(t, e = !1) {
  if (typeof t == "boolean") return t;
  const n = String(t ?? "").trim().toLowerCase();
  return n ? ["true", "1", "yes", "y", "on"].includes(n) : e;
}
function Bp(t = []) {
  return (Array.isArray(t) ? t : String(t ?? "").split(",")).map((n) => String(n ?? "").trim()).filter(Boolean);
}
function xp(t = "", e = "lock") {
  const n = String(t ?? "").trim().toLowerCase();
  return n === "detect" ? "contact" : n === "acquire" ? "lock" : ["contact", "track", "lock"].includes(n) ? n : e;
}
function _O(t = {}, e = 0) {
  const n = ti(t == null ? void 0 : t.max, e);
  return {
    value: Math.min(n, ti(t == null ? void 0 : t.value, n)),
    max: n
  };
}
function rw(t = 0, e = {}, {
  previousStructure: n = null,
  maxChanged: i = !1,
  valueChanged: a = !1
} = {}) {
  const r = ti(t, 0), s = Number(e == null ? void 0 : e.value), o = Number(e == null ? void 0 : e.max), l = ti((n == null ? void 0 : n.max) ?? 0, 0), c = Number(n == null ? void 0 : n.value), u = Number.isFinite(s), d = Number.isFinite(c), f = !Number.isFinite(o) || o <= 0, m = !u || s <= 0, g = r > 0 && f && m, p = !d || c >= l || l <= 0;
  return {
    value: Math.min(r, Math.max(0, (g || i && !a && p ? r : u ? s : d ? c : r) || 0)),
    max: r
  };
}
function xr(t = {}) {
  var i, a;
  const e = ti((i = t == null ? void 0 : t.armorPool) == null ? void 0 : i.value, 0), n = ti((a = t == null ? void 0 : t.structure) == null ? void 0 : a.value, 0);
  return e > 0 ? Cs.intact : n > 0 ? Cs.breached : Cs.wrecked;
}
function zp(t = {}) {
  var n;
  const e = typeof t == "boolean" ? { enabled: t } : t && typeof t == "object" ? t : {};
  return {
    enabled: Pa(e.enabled, !1),
    trackingPenalty: ti(e.trackingPenalty ?? e.bonusTrackingPenalty, 2),
    detectionStateCap: xp(e.detectionStateCap ?? e.passiveAcquireCeiling, "track"),
    revealedOnAttack: e.revealedOnAttack !== !1,
    revealedOnJump: e.revealedOnJump !== !1,
    revealedOnHit: e.revealedOnHit !== !1,
    counteredBy: Bp((n = e.counteredBy) != null && n.length ? e.counteredBy : ["activeProbe", "tag", "narc", "pointblank", "revealed"])
  };
}
function sw(t = {}) {
  const e = t && typeof t == "object" ? t : {};
  return {
    stealth: zp(e.stealth),
    jump: Pa(e.jump, !1),
    enhancedStrength: Pa(e.enhancedStrength, !1),
    sealed: Pa(e.sealed, !1),
    basicSensors: Pa(e.basicSensors, !1),
    medicalSuppression: Pa(e.medicalSuppression, !1),
    attachedEligible: Pa(e.attachedEligible, !1)
  };
}
function BO(t = {}, e = sw({})) {
  var i;
  const n = e.stealth ?? zp(!1);
  return {
    machineTargetable: t.machineTargetable !== !1,
    targetClass: String(t.targetClass ?? "battleArmor").trim() || "battleArmor",
    sizePenalty: iw,
    signature: String(t.signature ?? "low").trim() || "low",
    stealthTrackingPenalty: ti(t.stealthTrackingPenalty, n.enabled ? n.trackingPenalty : 0),
    detectionStateCap: t.detectionStateCap === null || t.acquireCeiling === null ? null : xp(t.detectionStateCap ?? t.acquireCeiling ?? (n.enabled ? n.detectionStateCap : ""), null),
    counteredBy: Bp((i = t.counteredBy) != null && i.length ? t.counteredBy : n.counteredBy)
  };
}
function Ai(t = {}) {
  var a;
  const e = t && typeof t == "object" ? t : {}, n = sw(e.systems), i = {
    enabled: Pa(e.enabled, !1),
    armorPool: _O(e.armorPool, 0),
    structure: rw((a = e.structure) == null ? void 0 : a.max, e.structure),
    state: "",
    scale: "personal",
    systems: n,
    machineTargetProfile: BO(e.machineTargetProfile, n),
    attachedToTokenUuid: String(e.attachedToTokenUuid ?? "").trim() || null,
    attachedLocationHint: String(e.attachedLocationHint ?? "").trim(),
    revealedUntil: e.revealedUntil ?? null
  };
  return i.state = xr(i), i;
}
function dl(t = {}) {
  const e = t && typeof t == "object" ? t : {};
  return {
    mountedOnItemId: String(e.mountedOnItemId ?? "").trim(),
    mountType: String(e.mountType ?? "").trim()
  };
}
function Is(t = "", e = "personal") {
  return Us(t, e);
}
function ki(t = null) {
  return !!(t != null && t.enabled);
}
function ow(t = null) {
  return ki(t) && xr(t) !== Cs.wrecked;
}
function ml(t = {}) {
  var n;
  const e = ti((n = t == null ? void 0 : t.structure) == null ? void 0 : n.value, 0);
  return e > 0 ? Math.ceil(e / 4) : 0;
}
function xO(t = null) {
  var n, i;
  if (!ow(t) || !((n = t == null ? void 0 : t.systems) != null && n.enhancedStrength)) return 0;
  const e = ti((i = t == null ? void 0 : t.structure) == null ? void 0 : i.max, 0);
  return e > 0 ? Math.ceil(e / 4) : 0;
}
function lw(t = null) {
  const e = t == null ? void 0 : t.items;
  return e ? Array.isArray(e) ? e : Array.isArray(e.contents) ? e.contents : typeof e[Symbol.iterator] == "function" ? Array.from(e) : [] : [];
}
function mo(t = null) {
  return t ? Array.isArray(t) ? t : Array.isArray(t.contents) ? t.contents : Array.isArray(t.placeables) ? t.placeables : typeof t[Symbol.iterator] == "function" ? Array.from(t) : [] : [];
}
function cw(t = null) {
  var e;
  return (t == null ? void 0 : t.actor) ?? ((e = t == null ? void 0 : t.document) == null ? void 0 : e.actor) ?? null;
}
function kf(t = null) {
  var e, n, i;
  return String(((e = t == null ? void 0 : t.document) == null ? void 0 : e.uuid) ?? (t == null ? void 0 : t.uuid) ?? ((i = (n = t == null ? void 0 : t.object) == null ? void 0 : n.document) == null ? void 0 : i.uuid) ?? "").trim();
}
function uw(t = null) {
  var e, n;
  return String(((e = t == null ? void 0 : t.document) == null ? void 0 : e.id) ?? (t == null ? void 0 : t.id) ?? ((n = t == null ? void 0 : t.object) == null ? void 0 : n.id) ?? "").trim();
}
function zO(t = null, e = null) {
  var a, r, s;
  const n = /* @__PURE__ */ new Set(), i = (o) => {
    const l = String(o ?? "").trim();
    l && n.add(l);
  };
  return i(kf(e)), i((a = e == null ? void 0 : e.document) == null ? void 0 : a.uuid), i(e == null ? void 0 : e.uuid), i((s = (r = e == null ? void 0 : e.object) == null ? void 0 : r.document) == null ? void 0 : s.uuid), i(uw(e)), i(t == null ? void 0 : t.uuid), i(t == null ? void 0 : t.id), n;
}
function Tf(t = null) {
  const e = io(t), n = Ai(e == null ? void 0 : e.battleArmor);
  if (!ki(n)) return null;
  const i = String(n.attachedToTokenUuid ?? "").trim();
  return i ? { activeArmor: e, profile: n, attachedToTokenUuid: i } : null;
}
function Zb(t, e, {
  actor: n = null,
  token: i = null,
  attachment: a = null,
  source: r = ""
} = {}) {
  var c;
  const s = n ?? cw(i), o = a ?? Tf(s);
  if (!s || !o) return;
  const l = kf(i) || s.uuid || s.id || ((c = o.activeArmor) == null ? void 0 : c.id);
  !l || e.has(l) || (e.add(l), t.push({
    actor: s,
    token: i,
    tokenId: uw(i),
    tokenUuid: kf(i),
    source: r,
    armor: o.activeArmor,
    battleArmor: o.profile,
    attachedToTokenUuid: o.attachedToTokenUuid
  }));
}
function io(t = null, { loadout: e = null } = {}) {
  var i, a;
  if (!t) return null;
  const n = (e == null ? void 0 : e.activeArmor) ?? ((a = (i = t.getPersonalCombatLoadout) == null ? void 0 : i.call(t, { refresh: !0 })) == null ? void 0 : a.activeArmor) ?? null;
  return ki(n == null ? void 0 : n.battleArmor) ? n : lw(t).filter((r) => ((r == null ? void 0 : r.canonicalType) ?? (r == null ? void 0 : r.type)) === "armor").map((r) => {
    var s;
    return ((s = r.getArmorProfile) == null ? void 0 : s.call(r, { actor: t })) ?? null;
  }).filter((r) => (r == null ? void 0 : r.equipped) && ki(r == null ? void 0 : r.battleArmor)).sort((r, s) => +!!s.isPrimary - +!!r.isPrimary)[0] ?? null;
}
function FO(t = null, {
  machineToken: e = null,
  targetTokens: n = null,
  selectedTokens: i = null
} = {}) {
  var l, c, u, d;
  const a = zO(t, e);
  if (!a.size) return [];
  const r = [], s = /* @__PURE__ */ new Set(), o = (f, m) => {
    const g = cw(f), p = Tf(g);
    !p || !a.has(p.attachedToTokenUuid) || Zb(r, s, { actor: g, token: f, attachment: p, source: m });
  };
  for (const f of mo(n)) o(f, "targeted");
  for (const f of mo(i)) o(f, "selected");
  for (const f of mo((c = (l = globalThis.game) == null ? void 0 : l.user) == null ? void 0 : c.targets)) o(f, "targeted");
  for (const f of mo((u = globalThis.canvas) == null ? void 0 : u.tokens)) o(f, "scene");
  for (const f of mo((d = globalThis.game) == null ? void 0 : d.actors)) {
    const m = Tf(f);
    !m || !a.has(m.attachedToTokenUuid) || Zb(r, s, { actor: f, attachment: m, source: "actor" });
  }
  return r;
}
function UO(t = null, e = "") {
  const n = String(e ?? "").trim();
  return !t || !n ? [] : lw(t).filter((i) => {
    var r;
    return dl((r = i == null ? void 0 : i.system) == null ? void 0 : r.mount).mountedOnItemId === n;
  }).map((i) => {
    var a, r;
    return {
      id: i.id ?? "",
      uuid: i.uuid ?? "",
      name: i.name ?? "Mounted Item",
      img: i.img ?? "",
      type: i.canonicalType ?? i.type,
      item: i,
      system: i.system ?? {},
      mount: dl((a = i.system) == null ? void 0 : a.mount),
      combatProfile: ((r = i.getCombatProfile) == null ? void 0 : r.call(i)) ?? null
    };
  });
}
function KO(t = null, e = "", { rangeBand: n = "" } = {}) {
  var a, r, s, o, l, c, u, d;
  const i = String(e ?? "").trim();
  return i ? i === "revealed" ? ((r = (a = t == null ? void 0 : t.statuses) == null ? void 0 : a.has) == null ? void 0 : r.call(a, Nc.revealed)) ?? !1 : i === "tag" || i === "tagged" ? ((o = (s = t == null ? void 0 : t.statuses) == null ? void 0 : s.has) == null ? void 0 : o.call(s, Nc.tagged)) ?? !1 : i === "narc" || i === "narced" ? ((c = (l = t == null ? void 0 : t.statuses) == null ? void 0 : l.has) == null ? void 0 : c.call(l, Nc.narced)) ?? !1 : i === "pointblank" ? String(n ?? "").trim().toLowerCase() === "close" : i === "activeProbe" ? !1 : ((d = (u = t == null ? void 0 : t.statuses) == null ? void 0 : u.has) == null ? void 0 : d.call(u, i)) ?? !1 : !1;
}
function HO(t = null, e = null, n = {}) {
  var a, r, s;
  return Bp(((a = e == null ? void 0 : e.machineTargetProfile) == null ? void 0 : a.counteredBy) ?? ((s = (r = e == null ? void 0 : e.systems) == null ? void 0 : r.stealth) == null ? void 0 : s.counteredBy) ?? []).some((o) => KO(t, o, n));
}
function ey(t = "lock") {
  return { contact: 1, track: 2, lock: 3 }[xp(t, "lock")] ?? 3;
}
function jO(t = "lock", e = "lock") {
  return ey(e) < ey(t) ? e : t;
}
function ld(t = null, e = {}) {
  var g, p, h, b, y, S, A, T, C;
  const n = io(t, e), i = Ai(n == null ? void 0 : n.battleArmor);
  if (!ki(i)) return null;
  const a = xr(i), r = !!((p = (g = i.systems) == null ? void 0 : g.stealth) != null && p.enabled), s = r && HO(t, i, e), o = !!i.attachedToTokenUuid, l = String(e.friendlyMachineTokenUuid ?? "").trim(), c = o && (!!e.attachedToFriendlyMachine || l && i.attachedToTokenUuid === l), u = iw, d = r && !s ? ti((h = i.machineTargetProfile) == null ? void 0 : h.stealthTrackingPenalty, i.systems.stealth.trackingPenalty) : 0, f = c ? 2 : o ? 1 : 0, m = r && !s ? ((b = i.machineTargetProfile) == null ? void 0 : b.detectionStateCap) ?? ((S = (y = i.systems) == null ? void 0 : y.stealth) == null ? void 0 : S.detectionStateCap) ?? "track" : null;
  return {
    armorId: n.id ?? n.armorId ?? "",
    armorName: n.name ?? "Battle Armor",
    state: a,
    machineTargetable: ((A = i.machineTargetProfile) == null ? void 0 : A.machineTargetable) !== !1 && a !== Cs.wrecked,
    targetClass: ((T = i.machineTargetProfile) == null ? void 0 : T.targetClass) ?? "battleArmor",
    signature: ((C = i.machineTargetProfile) == null ? void 0 : C.signature) ?? "low",
    trackingPenalty: u + d + f,
    normalPenalty: u,
    stealthPenalty: d,
    attachedPenalty: f,
    detectionStateCap: m,
    stealthEnabled: r,
    stealthCountered: s,
    attached: o,
    attachedToTokenUuid: i.attachedToTokenUuid,
    attachedToFriendlyMachine: c,
    friendlyFireRisk: c
  };
}
function GO(t = null, e = {}) {
  if (!io(t)) return !1;
  const n = String((e == null ? void 0 : e.mode) ?? "").trim();
  if (n === "attackDamage") return !0;
  if (n !== "trackDelta" || String((e == null ? void 0 : e.track) ?? "physical").trim() !== "physical" || !(e != null && e.useArmor) || !String((e == null ? void 0 : e.damageType) ?? "").trim()) return !1;
  const i = String((e == null ? void 0 : e.sourceKind) ?? (e == null ? void 0 : e.sourceType) ?? (e == null ? void 0 : e.source) ?? "").trim().toLowerCase();
  return i !== "internal" && i !== "direct";
}
function qO(t = {}, { damage: e = 0, sourceScale: n = "personal" } = {}) {
  const i = Ai(t), a = Is(n), r = Cp({
    damage: aw(e, 0),
    sourceScale: a,
    targetScale: "personal"
  }), s = r.factor;
  let o = Math.max(0, Math.ceil(r.converted));
  const l = i.armorPool.value, c = i.armorPool.max, u = i.structure.value, d = i.structure.max, f = l > 0;
  let m = 0, g = 0, p = !1;
  o > 0 && i.armorPool.value > 0 && (m = Math.min(o, i.armorPool.value), i.armorPool.value -= m, o -= m);
  const h = ml(i);
  o > 0 && i.structure.value > 0 && (g = Math.min(o, h), o = Math.max(0, o - h), g > 0 && (p = !0, i.structure.value = Math.max(0, i.structure.value - 1))), i.state = xr(i);
  const b = Math.max(0, u - i.structure.value);
  return {
    sourceScale: a,
    targetScale: "personal",
    multiplier: s,
    scaleConversion: r,
    incomingOriginal: Math.max(0, Math.ceil(r.original)),
    incomingScaled: Math.max(0, Math.ceil(r.converted)),
    hadArmorShellAtStart: f,
    armorBefore: l,
    armorAfter: i.armorPool.value,
    armorPoolMax: c,
    armorAbsorbed: m,
    structureBefore: u,
    structureAfter: i.structure.value,
    structureMax: d,
    structureDamage: b,
    structureResistance: h,
    structureReduced: g,
    structureDegraded: p,
    wearerDamage: o,
    stateBefore: xr({ armorPool: { value: l }, structure: { value: u } }),
    stateAfter: i.state,
    profile: i
  };
}
function WO(t = {}) {
  return {
    "system.battleArmor.armorPool.value": ti(t.armorAfter, 0),
    "system.battleArmor.structure.value": ti(t.structureAfter, 0),
    "system.battleArmor.state": String(t.stateAfter ?? "").trim() || Cs.wrecked
  };
}
function Fp(t = null) {
  var r, s, o, l, c, u, d, f, m, g, p, h, b, y, S;
  const e = io(t), n = Ai(e == null ? void 0 : e.battleArmor);
  if (!ki(n)) return { enabled: !1, hasSuit: !1 };
  const i = UO(t, e.id ?? e.armorId), a = ld(t) ?? {};
  return {
    enabled: !0,
    hasSuit: !0,
    armorId: e.id ?? "",
    name: e.name ?? "Battle Armor",
    img: e.img ?? "",
    suit: {
      id: e.id ?? "",
      uuid: e.uuid ?? "",
      name: e.name ?? "Battle Armor",
      img: e.img ?? ""
    },
    state: xr(n),
    stateLabel: xr(n).replace(/^./, (A) => A.toUpperCase()),
    armorPool: Xb(n.armorPool),
    structure: Xb(n.structure),
    structureResistance: ml(n),
    systems: {
      stealth: zp((r = n.systems) == null ? void 0 : r.stealth),
      jump: !!((s = n.systems) != null && s.jump),
      enhancedStrength: !!((o = n.systems) != null && o.enhancedStrength),
      sealed: !!((l = n.systems) != null && l.sealed),
      basicSensors: !!((c = n.systems) != null && c.basicSensors),
      medicalSuppression: !!((u = n.systems) != null && u.medicalSuppression),
      attachedEligible: !!((d = n.systems) != null && d.attachedEligible)
    },
    systemTags: [
      (m = (f = n.systems) == null ? void 0 : f.stealth) != null && m.enabled ? "Stealth Armor" : "",
      (g = n.systems) != null && g.jump ? "Jump Capable" : "",
      (p = n.systems) != null && p.sealed ? "Sealed" : "",
      (h = n.systems) != null && h.enhancedStrength ? "Enhanced Strength" : "",
      (b = n.systems) != null && b.basicSensors ? "Basic Sensors" : ""
    ].filter(Boolean).map((A) => ({ label: A })),
    machineTargetProfile: a,
    mountedItems: i.map((A) => {
      var k, w, v, N;
      const T = A.type === "personalWeapon", C = String(((w = (k = A.combatProfile) == null ? void 0 : k.resolution) == null ? void 0 : w.onHitEffect) ?? "").trim();
      return {
        id: A.id,
        uuid: A.uuid,
        name: A.name,
        img: A.img,
        type: A.type,
        scale: ((v = A.combatProfile) == null ? void 0 : v.scale) ?? ((N = A.system) == null ? void 0 : N.scale) ?? "personal",
        effect: C,
        isWeapon: T,
        actionLabel: C === "tagged" || C === "narced" ? "Designate" : "Attack",
        attackRoll: T ? JSON.stringify({
          intent: "attack",
          weaponId: A.id,
          edge: { pool: "physical.grit", allowed: ["pre", "post"] },
          tags: ["combat", "attack", "battleArmor"]
        }) : ""
      };
    }),
    attachedToTokenUuid: n.attachedToTokenUuid ?? "",
    attachedLocationHint: n.attachedLocationHint ?? "",
    revealed: ((S = (y = t == null ? void 0 : t.statuses) == null ? void 0 : y.has) == null ? void 0 : S.call(y, Nc.revealed)) ?? !1
  };
}
const VO = Object.freeze([
  "targetingPacket",
  "networkShare",
  "targetingSuppression",
  "aura",
  "useLimit",
  "charges",
  "incomingDamageReduction",
  "interceptAttack",
  "pilotDamageMitigation",
  "catastrophicInterception",
  "currentHeatAdjustment",
  "startOfActivationRepair"
]), JO = new Set(VO);
function wf(t) {
  return Array.isArray(t) ? t : Array.isArray(t == null ? void 0 : t.contents) ? t.contents : t && typeof t[Symbol.iterator] == "function" ? Array.from(t) : [];
}
function YO(t = null) {
  return ((t == null ? void 0 : t.canonicalType) ?? (t == null ? void 0 : t.type)) === M.itemType.assetModule;
}
function QO(t = {}, { item: e = null, index: n = 0 } = {}) {
  if (!t || typeof t != "object" || Array.isArray(t)) return null;
  const i = String(t.kind ?? "").trim();
  if (!JO.has(i))
    throw new Error(`${(e == null ? void 0 : e.name) ?? "Asset Module"}: runtime packet ${n + 1} has unsupported kind "${i}".`);
  return {
    ...t,
    id: String(t.id ?? `${i}-${n + 1}`).trim() || `${i}-${n + 1}`,
    kind: i,
    hook: String(t.hook ?? "").trim(),
    sourceId: (e == null ? void 0 : e.id) ?? "",
    sourceUuid: (e == null ? void 0 : e.uuid) ?? "",
    sourceName: (e == null ? void 0 : e.name) ?? "Asset Module"
  };
}
function cd(t = null) {
  return wf((t == null ? void 0 : t.assetModules) ?? (t == null ? void 0 : t.items)).filter(YO).map((e) => ({ item: e, state: no(e, { installed: !0 }) })).filter((e) => e.state.active);
}
function Up(t = null, { kind: e = "", hook: n = "" } = {}) {
  var s, o, l;
  const i = String(e ?? "").trim(), a = String(n ?? "").trim(), r = [];
  for (const { item: c, state: u } of cd(t))
    [
      ...wf((o = (s = c == null ? void 0 : c.system) == null ? void 0 : s.runtime) == null ? void 0 : o.packets),
      ...wf((l = c == null ? void 0 : c.system) == null ? void 0 : l.runtimePackets)
    ].forEach((f, m) => {
      const g = QO(f, { item: c, index: m });
      g && (i && g.kind !== i || a && g.hook && g.hook !== a || r.push({
        ...g,
        moduleActive: u.active,
        moduleReady: u.ready
      }));
    });
  return r;
}
const dw = /* @__PURE__ */ new Map();
function ty(t = null) {
  var o;
  const e = (t == null ? void 0 : t.document) ?? t ?? null, n = (t == null ? void 0 : t.object) ?? (e == null ? void 0 : e.object) ?? t ?? null, i = String((e == null ? void 0 : e.id) ?? (t == null ? void 0 : t.id) ?? "").trim(), a = dw.get(i) ?? null, r = Number((a == null ? void 0 : a.x) ?? (e == null ? void 0 : e.x) ?? (t == null ? void 0 : t.x)), s = Number((a == null ? void 0 : a.y) ?? (e == null ? void 0 : e.y) ?? (t == null ? void 0 : t.y));
  if (n && Number.isFinite(r) && Number.isFinite(s)) {
    if (typeof n.getCenterPoint == "function")
      return n.getCenterPoint({ x: r, y: s });
    if (typeof n.getCenter == "function")
      return n.getCenter(r, s);
  }
  return (n == null ? void 0 : n.center) ?? ((o = e == null ? void 0 : e.object) == null ? void 0 : o.center) ?? null;
}
function XO(t = null, e = {}) {
  var r, s, o;
  const n = String((t == null ? void 0 : t.id) ?? ((r = t == null ? void 0 : t.document) == null ? void 0 : r.id) ?? "").trim();
  if (!n) return !1;
  const i = Object.prototype.hasOwnProperty.call(e ?? {}, "x") ? Number(e.x) : Number((t == null ? void 0 : t.x) ?? ((s = t == null ? void 0 : t.document) == null ? void 0 : s.x)), a = Object.prototype.hasOwnProperty.call(e ?? {}, "y") ? Number(e.y) : Number((t == null ? void 0 : t.y) ?? ((o = t == null ? void 0 : t.document) == null ? void 0 : o.y));
  return !Number.isFinite(i) || !Number.isFinite(a) ? !1 : (dw.set(n, { x: i, y: a }), !0);
}
function xl(t = null, e = null) {
  var s, o, l, c;
  const n = (s = globalThis.canvas) == null ? void 0 : s.grid, i = ty(t), a = ty(e);
  if (!n || !i || !a) return null;
  if (typeof n.measurePath == "function")
    try {
      const u = n.measurePath([i, a], { gridSpaces: !0 }), d = Number(
        (u == null ? void 0 : u.distance) ?? (u == null ? void 0 : u.cost) ?? (u == null ? void 0 : u.totalDistance) ?? (u == null ? void 0 : u.totalCost) ?? NaN
      );
      if (Number.isFinite(d)) return d;
    } catch {
    }
  const r = ((c = (l = (o = globalThis.foundry) == null ? void 0 : o.canvas) == null ? void 0 : l.geometry) == null ? void 0 : c.Ray) ?? globalThis.Ray;
  if (typeof n.measureDistances == "function" && typeof r == "function")
    try {
      const u = n.measureDistances([{ ray: new r(i, a) }], { gridSpaces: !0 }), d = Number(Array.isArray(u) ? u[0] : NaN);
      if (Number.isFinite(d)) return d;
    } catch {
      return null;
    }
  return null;
}
function Mf(t, e = "") {
  if (!Number.isFinite(t)) return "";
  const n = Math.round(Number(t) * 10) / 10, i = Number.isInteger(n) ? String(n) : n.toFixed(1);
  return e ? `${i} ${e}` : i;
}
const vf = Object.freeze([
  Object.freeze({ key: "close", label: "Close", min: 0, max: 5, baseDn: 2 }),
  Object.freeze({ key: "near", label: "Near", min: 6, max: 26, baseDn: 3 }),
  Object.freeze({ key: "far", label: "Far", min: 27, max: 62, baseDn: 4 }),
  Object.freeze({ key: "extreme", label: "Extreme", min: 63, max: 120, baseDn: 5 })
]), Pc = Object.freeze([
  Object.freeze({ key: "close", label: "Close", min: 0, max: 59, baseDn: 2 }),
  Object.freeze({ key: "near", label: "Near", min: 60, max: 269, baseDn: 3 }),
  Object.freeze({ key: "far", label: "Far", min: 270, max: 629, baseDn: 4 }),
  Object.freeze({ key: "extreme", label: "Extreme", min: 630, max: 1200, baseDn: 5 })
]), ZO = new Map(vf.map((t) => [t.key, t])), eL = new Map(Pc.map((t) => [t.key, t]));
function oc(t, e) {
  const n = Number(t);
  return !Number.isFinite(n) || n <= 0 ? e : Math.max(0, Math.trunc(n));
}
function ud(t = "") {
  return ZO.get(String(t ?? "").trim().toLowerCase()) ?? null;
}
function Kp(t = "") {
  return eL.get(String(t ?? "").trim().toLowerCase()) ?? null;
}
function pu(t = "") {
  if (String(t ?? "").trim().toLowerCase() === "outofrange") return "Out of Range";
  const e = ud(t);
  return e ? `${e.label} ${e.min}-${e.max} m` : String(t ?? "").trim() || "Range";
}
function dd(t = "") {
  if (String(t ?? "").trim().toLowerCase() === "outofrange") return "Out of Range";
  const e = ud(t);
  return e ? e.label : String(t ?? "").trim() || "Range";
}
function Hp(t = "") {
  if (String(t ?? "").trim().toLowerCase() === "outofrange") return "Out of Range";
  const e = Kp(t);
  return e ? e.label : String(t ?? "").trim() || "Range";
}
function tL(t = "", e = 1) {
  if (String(t ?? "").trim().toLowerCase() === "outofrange") return 6;
  const n = ud(t);
  return Number.isFinite(Number(n == null ? void 0 : n.baseDn)) ? Number(n.baseDn) : e;
}
function mw(t = "", e = 1) {
  if (String(t ?? "").trim().toLowerCase() === "outofrange") return 6;
  const n = Kp(t);
  return Number.isFinite(Number(n == null ? void 0 : n.baseDn)) ? Number(n.baseDn) : e;
}
function jp(t = {}) {
  return {
    max: String((t == null ? void 0 : t.max) ?? "").trim().toLowerCase() || "extreme",
    close: oc((t == null ? void 0 : t.close) ?? (t == null ? void 0 : t.short), 5),
    near: oc((t == null ? void 0 : t.near) ?? (t == null ? void 0 : t.medium), 26),
    far: oc((t == null ? void 0 : t.far) ?? (t == null ? void 0 : t.long), 62),
    extreme: oc(t == null ? void 0 : t.extreme, 120)
  };
}
function nL(t, e = {}, n = "close") {
  var u;
  const i = Number(t);
  if (!Number.isFinite(i) || i < 0)
    return String(n ?? "close").trim().toLowerCase() || "close";
  const a = jp(e), r = ((u = ud(a.max)) == null ? void 0 : u.key) ?? "extreme", s = vf.findIndex((d) => d.key === r), o = Number((a == null ? void 0 : a[r]) ?? NaN);
  if (Number.isFinite(o) && i > o)
    return "outOfRange";
  let l = "extreme";
  i <= a.close ? l = "close" : i <= a.near ? l = "near" : i <= a.far && (l = "far");
  const c = vf.findIndex((d) => d.key === l);
  return s >= 0 && c > s ? r : l;
}
function md(t, e = "close") {
  const n = Number(t);
  if (!Number.isFinite(n) || n < 0)
    return String(e ?? "close").trim().toLowerCase() || "close";
  const i = String(e ?? "close").trim().toLowerCase() || "close", a = Pc[Pc.length - 1] ?? null;
  if (a && n > Number(a.max ?? NaN))
    return "outOfRange";
  for (const r of Pc)
    if (n <= r.max) return r.key;
  return i;
}
const iL = /* @__PURE__ */ new Set([M.actorTypes.battlemech, M.actorTypes.vehicle]), aL = /* @__PURE__ */ new Set(["passive", "active", "suppressed"]), rL = /* @__PURE__ */ new Set(["untilNextActivation", "endOfRound", "manual"]), sL = Object.freeze(["tagged", "narced"]), fw = "signatureRevealed", oL = "stealthActive", fa = "highEmission";
function Ti(t = null) {
  return iL.has(t == null ? void 0 : t.type);
}
function di(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function am(t, e = 0, n = 3) {
  return Math.min(n, Math.max(e, Number(t) || 0));
}
function gn(t, e = 0) {
  return Math.max(0, Math.trunc(di(t, e)));
}
function zl(t = "") {
  const e = String(t ?? "").trim().toLowerCase();
  return aL.has(e) ? e : "passive";
}
function gw(t = "", e = "untilNextActivation") {
  const n = String(t ?? "").trim();
  return rL.has(n) ? n : e;
}
function Gp(t = "") {
  const e = String(t ?? "").trim().toLowerCase().replace(/[^a-z0-9]+/g, "");
  return e === "low" || e === "medium" || e === "high" ? e : e === "verylow" || e === "minimal" ? "low" : "";
}
function Es(t) {
  return (Array.isArray(t) ? t : typeof t == "string" ? t.split(",") : []).map((n) => String(n ?? "").trim()).filter(Boolean);
}
function Ga(t = "") {
  return String(t ?? "").trim().toLowerCase().replace(/[^a-z0-9]+/g, "");
}
function lL(t = {}, e = "") {
  var n, i;
  return typeof ((i = (n = globalThis.foundry) == null ? void 0 : n.utils) == null ? void 0 : i.getProperty) == "function" ? globalThis.foundry.utils.getProperty(t, e) : String(e ?? "").split(".").filter(Boolean).reduce((a, r) => {
    if (!(!a || typeof a != "object"))
      return a[r];
  }, t);
}
function cL(t) {
  var e, n;
  return typeof ((n = (e = globalThis.foundry) == null ? void 0 : e.utils) == null ? void 0 : n.deepClone) == "function" ? globalThis.foundry.utils.deepClone(t) : JSON.parse(JSON.stringify(t ?? {}));
}
function uL(t = {}) {
  var n;
  const e = (t == null ? void 0 : t.combat) ?? ((n = globalThis.game) == null ? void 0 : n.combat) ?? null;
  return {
    round: gn((t == null ? void 0 : t.round) ?? (e == null ? void 0 : e.round), 0),
    turn: gn((t == null ? void 0 : t.turn) ?? (e == null ? void 0 : e.turn), 0)
  };
}
function nr(t = null) {
  var n, i, a, r, s;
  const e = ((n = t == null ? void 0 : t.getFlag) == null ? void 0 : n.call(t, I, "stealth")) ?? ((a = (i = t == null ? void 0 : t.flags) == null ? void 0 : i[I]) == null ? void 0 : a.stealth) ?? ((s = (r = t == null ? void 0 : t.flags) == null ? void 0 : r.mwd) == null ? void 0 : s.stealth) ?? {};
  return e && typeof e == "object" && !Array.isArray(e) ? cL(e) : {};
}
async function Ks(t = null, e = {}) {
  var i;
  if (!t) return !1;
  const n = e && typeof e == "object" && !Array.isArray(e) ? e : {};
  return typeof t.setFlag == "function" ? (await t.setFlag(I, "stealth", n), !0) : typeof t.update == "function" ? (await t.update({ [`flags.${I}.stealth`]: n }), !0) : (t.flags ?? (t.flags = {}), (i = t.flags)[I] ?? (i[I] = {}), t.flags[I].stealth = n, !0);
}
function qp(t = null, e = "") {
  const n = String(e ?? "").trim();
  return n ? Array.from((t == null ? void 0 : t.effects) ?? []).filter((i) => {
    var r, s, o, l, c, u, d;
    const a = i == null ? void 0 : i.statuses;
    return (r = a == null ? void 0 : a.has) != null && r.call(a, n) || Array.isArray(a) && a.includes(n) ? !0 : String(((l = (o = (s = i == null ? void 0 : i.flags) == null ? void 0 : s[I]) == null ? void 0 : o.status) == null ? void 0 : l.id) ?? ((d = (u = (c = i == null ? void 0 : i.flags) == null ? void 0 : c.mwd) == null ? void 0 : u.status) == null ? void 0 : d.id) ?? "").trim() === n;
  }) : [];
}
function pw(t = null) {
  var e, n, i, a;
  return !!(((n = (e = t == null ? void 0 : t.flags) == null ? void 0 : e[I]) == null ? void 0 : n.stealthLifecycleOwned) ?? ((a = (i = t == null ? void 0 : t.flags) == null ? void 0 : i.mwd) == null ? void 0 : a.stealthLifecycleOwned));
}
async function dL(t = null, e = "", { reason: n = "", source: i = "" } = {}) {
  var r;
  const a = {
    [`flags.${I}.stealthLifecycleOwned`]: !0,
    [`flags.${I}.stealthReason`]: String(n ?? "").trim(),
    [`flags.${I}.stealthSource`]: String(i ?? "").trim()
  };
  for (const s of qp(t, e))
    typeof s.update == "function" ? await s.update(a) : s.id && typeof (t == null ? void 0 : t.updateEmbeddedDocuments) == "function" ? await t.updateEmbeddedDocuments("ActiveEffect", [{ _id: s.id, ...a }]) : (s.flags ?? (s.flags = {}), (r = s.flags)[I] ?? (r[I] = {}), s.flags[I].stealthLifecycleOwned = !0, s.flags[I].stealthReason = String(n ?? "").trim(), s.flags[I].stealthSource = String(i ?? "").trim());
}
async function Fl(t = null, e = "", n = !0, { reason: i = "", source: a = "" } = {}) {
  var l, c;
  if (!t || !e) return !1;
  const r = nr(t);
  if (r.ownedStatuses ?? (r.ownedStatuses = {}), n)
    return await mt({
      actor: t,
      statusId: e,
      active: !0,
      metadata: {
        scope: "stealthLifecycle",
        notes: String(i ?? "").trim()
      }
    }), r.ownedStatuses[e] = {
      active: !0,
      reason: String(i ?? "").trim(),
      source: String(a ?? "system").trim() || "system"
    }, await Ks(t, r), await dL(t, e, { reason: i, source: a }), !0;
  const s = !!((c = (l = r.ownedStatuses) == null ? void 0 : l[e]) != null && c.active), o = qp(t, e).some(pw);
  return !s && !o ? !1 : (await mt({ actor: t, statusId: e, active: !1 }), r.ownedStatuses && delete r.ownedStatuses[e], await Ks(t, r), !0);
}
function hw({ active: t = !0, reason: e = "", penalty: n = 1, rating: i = 1, source: a = "system", duration: r = "untilNextActivation", ctx: s = {} } = {}) {
  const { round: o, turn: l } = uL(s);
  return {
    active: !!t,
    reason: String(e ?? "").trim(),
    penalty: gn(n, 1),
    rating: gn(i, 1),
    source: String(a ?? "system").trim() || "system",
    round: o,
    turn: l,
    duration: gw(r)
  };
}
function mL(t = null) {
  const e = /* @__PURE__ */ new Set(), n = (i) => {
    const a = Ga(i);
    a && e.add(a);
  };
  for (const { item: i } of cd(t)) {
    n(i == null ? void 0 : i.id), n(i == null ? void 0 : i.name);
    const a = (i == null ? void 0 : i.system) ?? {};
    n(a.category), Es(a.tags).forEach(n), Es(a.capabilities).forEach(n), Es(a.keywords).forEach(n);
  }
  return e;
}
function fL(t = null) {
  var n, i, a;
  const e = ((i = (n = t == null ? void 0 : t.system) == null ? void 0 : n.targeting) == null ? void 0 : i.stealthProfile) ?? ((a = t == null ? void 0 : t.system) == null ? void 0 : a.stealthProfile) ?? null;
  return !e || typeof e != "object" || Array.isArray(e) ? null : {
    ratingBonus: Math.max(0, di(e.ratingBonus ?? e.rating, 0)),
    tags: Es(e.tags),
    requiresActiveMode: !!e.requiresActiveMode
  };
}
function bw(t = null, e = null, { mode: n = null } = {}) {
  var r, s, o;
  const i = fL(e);
  if (!i || i.ratingBonus <= 0) return null;
  const a = zl(n ?? ((o = (s = (r = t == null ? void 0 : t.system) == null ? void 0 : r.mwd) == null ? void 0 : s.stealth) == null ? void 0 : o.mode));
  return i.requiresActiveMode && a !== "active" ? null : {
    id: `stealth.module.${(e == null ? void 0 : e.id) ?? Ga(e == null ? void 0 : e.name)}`,
    label: (e == null ? void 0 : e.name) ?? "Stealth Module",
    value: i.ratingBonus,
    tags: Array.from(/* @__PURE__ */ new Set(["stealth", "module", ...i.tags])),
    sourceId: (e == null ? void 0 : e.id) ?? "",
    sourceName: (e == null ? void 0 : e.name) ?? ""
  };
}
function gL(t = null, e = {}) {
  var a, r, s;
  const n = /* @__PURE__ */ new Set();
  if (!Ti(t)) return n;
  const i = zl((e == null ? void 0 : e.mode) ?? ((s = (r = (a = t == null ? void 0 : t.system) == null ? void 0 : a.mwd) == null ? void 0 : r.stealth) == null ? void 0 : s.mode));
  for (const { item: o } of cd(t)) {
    const l = bw(t, o, { mode: i });
    l && (l.sourceId && n.add(l.sourceId), l.sourceName && n.add(l.sourceName));
  }
  return n;
}
function pL(t = null, { mode: e = null } = {}) {
  const n = [];
  for (const { item: i } of cd(t)) {
    const a = bw(t, i, { mode: e });
    a && n.push(a);
  }
  return {
    rating: n.reduce((i, a) => i + di(a.value, 0), 0),
    parts: n
  };
}
function yw(t = null) {
  return Array.from((t == null ? void 0 : t.items) ?? []).filter((e) => ["quality", "trait"].includes(String((e == null ? void 0 : e.canonicalType) ?? (e == null ? void 0 : e.type) ?? "").trim()));
}
function Sw(t = null) {
  var n, i, a, r, s;
  const e = ((i = (n = t == null ? void 0 : t.system) == null ? void 0 : n.mwd) == null ? void 0 : i.stealth) ?? {};
  return Gp(e.signature ?? ((r = (a = t == null ? void 0 : t.system) == null ? void 0 : a.mwd) == null ? void 0 : r.signature) ?? ((s = t == null ? void 0 : t.system) == null ? void 0 : s.signature));
}
function hL(t = null) {
  var i, a, r, s, o;
  const e = [];
  Sw(t) === "low" && e.push({
    id: "stealth.signature.low",
    label: "Low Signature",
    value: 1,
    tags: ["stealth", "signature"],
    sourceId: "system.mwd.stealth.signature",
    sourceName: "Signature"
  });
  for (const l of yw(t))
    Gp(((a = (i = l == null ? void 0 : l.system) == null ? void 0 : i.targeting) == null ? void 0 : a.signature) ?? ((r = l == null ? void 0 : l.system) == null ? void 0 : r.signature) ?? ((o = (s = l == null ? void 0 : l.system) == null ? void 0 : s.mwd) == null ? void 0 : o.signature)) === "low" && e.push({
      id: `stealth.signature.${(l == null ? void 0 : l.id) ?? Ga(l == null ? void 0 : l.name)}`,
      label: (l == null ? void 0 : l.name) ?? "Low Signature",
      value: 1,
      tags: ["stealth", "signature", "trait"],
      sourceId: (l == null ? void 0 : l.id) ?? "",
      sourceName: (l == null ? void 0 : l.name) ?? ""
    });
  return {
    rating: e.reduce((l, c) => l + di(c.value, 0), 0),
    parts: e
  };
}
function fo(t, e = {}) {
  const n = String(e.sourceId ?? e.id ?? e.label ?? "").trim(), i = String(e.sourceName ?? e.label ?? "").trim(), a = Ga(n || i || e.id);
  if (!a) return;
  const r = gn(e.value, 1);
  if (r <= 0) return;
  const s = t.get(a);
  (!s || r > di(s.value, 0)) && t.set(a, {
    id: e.id,
    label: e.label,
    value: r,
    tags: Array.from(/* @__PURE__ */ new Set(["stealth", "emission", ...e.tags ?? []])),
    sourceId: n,
    sourceName: i,
    kind: e.kind ?? "authored"
  });
}
function bL(t = null, e = {}) {
  var r, s, o, l, c, u, d, f, m, g;
  const n = /* @__PURE__ */ new Map();
  Sw(t) === "high" && fo(n, {
    id: "emission.signature.high",
    label: "High Signature",
    value: 1,
    sourceId: "system.mwd.stealth.signature",
    sourceName: "Signature",
    tags: ["signature"]
  });
  for (const p of yw(t)) {
    Gp(((s = (r = p == null ? void 0 : p.system) == null ? void 0 : r.targeting) == null ? void 0 : s.signature) ?? ((o = p == null ? void 0 : p.system) == null ? void 0 : o.signature) ?? ((c = (l = p == null ? void 0 : p.system) == null ? void 0 : l.mwd) == null ? void 0 : c.signature)) === "high" && fo(n, {
      id: `emission.signature.${(p == null ? void 0 : p.id) ?? Ga(p == null ? void 0 : p.name)}`,
      label: (p == null ? void 0 : p.name) ?? "High Signature",
      value: 1,
      sourceId: (p == null ? void 0 : p.id) ?? "",
      sourceName: (p == null ? void 0 : p.name) ?? "",
      tags: ["signature", "trait"]
    });
    for (const b of Array.isArray((u = p == null ? void 0 : p.system) == null ? void 0 : u.rules) ? p.system.rules : [])
      for (const y of Array.isArray(b == null ? void 0 : b.outputs) ? b.outputs : [])
        String((y == null ? void 0 : y.type) ?? "").trim() === "derivedStatus" && String((y == null ? void 0 : y.key) ?? (y == null ? void 0 : y.status) ?? "").trim() === fa && fo(n, {
          id: y.id ?? `${b.id ?? p.id}.highEmission`,
          label: y.label ?? b.label ?? p.name ?? "High Emission",
          value: y.value === !0 || y.value === void 0 ? 1 : y.value,
          sourceId: (p == null ? void 0 : p.id) ?? "",
          sourceName: (p == null ? void 0 : p.name) ?? "",
          tags: ["trait"]
        });
  }
  const a = Ji(t, {
    ...e,
    payload: {
      ...(e == null ? void 0 : e.payload) ?? {},
      intent: (e == null ? void 0 : e.intent) ?? ((d = e == null ? void 0 : e.payload) == null ? void 0 : d.intent) ?? "attack"
    },
    resolved: {
      ...(e == null ? void 0 : e.resolved) ?? {},
      intent: (e == null ? void 0 : e.intent) ?? ((f = e == null ? void 0 : e.resolved) == null ? void 0 : f.intent) ?? "attack"
    }
  }).effects;
  for (const p of a) {
    let h = !1;
    for (const S of p.ruleOutputs ?? [])
      String((S == null ? void 0 : S.type) ?? "").trim() === "derivedStatus" && String((S == null ? void 0 : S.key) ?? (S == null ? void 0 : S.status) ?? "").trim() === fa && (fo(n, {
        id: S.id ?? `${p.id}.highEmission`,
        label: S.label ?? p.label ?? "High Emission",
        value: S.value === !0 || S.value === void 0 ? 1 : S.value,
        sourceId: p.sourceId,
        sourceName: p.sourceName,
        tags: ["module"]
      }), h = !0);
    const b = (((m = p.grants) == null ? void 0 : m.statuses) ?? []).includes(fa), y = Es(p.tags ?? ((g = p.system) == null ? void 0 : g.tags)).some((S) => Ga(S) === Ga(fa));
    !h && (b || y) && fo(n, {
      id: `${p.id ?? p.sourceId}.highEmission`,
      label: p.label ?? p.sourceName ?? "High Emission",
      value: 1,
      sourceId: p.sourceId,
      sourceName: p.sourceName,
      tags: ["module", "legacy"]
    });
  }
  return Array.from(n.values());
}
function yL(t = null) {
  const n = nr(t).transientEmission ?? {};
  if (!(n != null && n.active)) return null;
  const i = gn(n.rating, 1);
  return i <= 0 ? null : {
    id: "emission.transient",
    label: "Transient High Emission",
    value: i,
    tags: ["stealth", "emission", "transient"],
    sourceId: "flags.mwd.stealth.transientEmission",
    sourceName: n.reason || "Transient High Emission",
    kind: "transient"
  };
}
function SL(t = null, e = [], n = null) {
  var r, s, o, l;
  return !(((s = (r = t == null ? void 0 : t.statuses) == null ? void 0 : r.has) == null ? void 0 : s.call(r, fa)) ?? !1) || n || e.some((c) => c.value > 0) || !!((l = (o = nr(t).ownedStatuses) == null ? void 0 : o[fa]) != null && l.active) || qp(t, fa).some(pw) ? null : {
    id: "emission.status.highEmission",
    label: "High Emission Status",
    value: 1,
    tags: ["stealth", "emission", "status", "fallback"],
    sourceId: "status.highEmission",
    sourceName: "High Emission",
    kind: "status"
  };
}
function AL(t = null) {
  var s, o, l, c, u, d, f;
  const n = nr(t).revealed ?? {};
  if (n != null && n.active)
    return {
      active: !0,
      reason: String(n.reason ?? "").trim(),
      penalty: gn(n.penalty, 1),
      source: String(n.source ?? "").trim(),
      duration: gw(n.duration),
      round: gn(n.round, 0),
      turn: gn(n.turn, 0)
    };
  const i = ((o = (s = t == null ? void 0 : t.system) == null ? void 0 : s.mwd) == null ? void 0 : o.stealth) ?? {}, a = ((l = t == null ? void 0 : t.getFlag) == null ? void 0 : l.call(t, I, "stealthReveal")) ?? ((u = (c = t == null ? void 0 : t.flags) == null ? void 0 : c[I]) == null ? void 0 : u.stealthReveal) ?? ((f = (d = t == null ? void 0 : t.flags) == null ? void 0 : d.mwd) == null ? void 0 : f.stealthReveal) ?? null, r = !!(i.revealed || i.revealedUntil || a != null && a.active);
  return {
    active: r,
    reason: r ? "manual" : "",
    penalty: r ? gn(a == null ? void 0 : a.penalty, 1) : 0,
    source: "manual",
    duration: "manual",
    round: 0,
    turn: 0
  };
}
function kL(t = {}) {
  return !t.active || !t.penalty ? null : {
    id: "stealth.lifecycle.revealed",
    label: "Signature Revealed",
    value: -t.penalty,
    tags: ["stealth", "lifecycle", "reveal"]
  };
}
function fd(t = null, e = {}) {
  if (!Ti(t))
    return {
      authoredEmissionRating: 0,
      transientEmissionRating: 0,
      statusEmissionRating: 0,
      effectiveEmissionRating: 0,
      parts: []
    };
  const n = bL(t, e), i = yL(t), a = SL(t, n, i), r = i ? [i] : [], s = a ? [a] : [], o = n.reduce((u, d) => u + di(d.value, 0), 0), l = r.reduce((u, d) => u + di(d.value, 0), 0), c = s.reduce((u, d) => u + di(d.value, 0), 0);
  return {
    authoredEmissionRating: o,
    transientEmissionRating: l,
    statusEmissionRating: c,
    effectiveEmissionRating: o + l + c,
    parts: [...n, ...r, ...s]
  };
}
function gd(t = null, e = {}) {
  if (!Ti(t))
    return {
      enabled: !1,
      baseRating: 0,
      contributionRating: 0,
      rawRating: 0,
      counterableRating: 0,
      effectiveRating: 0,
      mode: "passive",
      revealed: !1,
      revealPenalty: 0,
      emissionPenalty: 0,
      emission: {
        authoredEmissionRating: 0,
        transientEmissionRating: 0,
        statusEmissionRating: 0,
        effectiveEmissionRating: 0,
        parts: []
      },
      detectionCap: null,
      counteredBy: [],
      parts: []
    };
  const n = lL(t, "system.mwd.stealth") ?? {}, i = zl(n.mode), a = Math.max(0, di(n.enabled ? n.rating : 0, 0)), r = pL(t, { mode: i }), s = hL(t), o = a + r.rating + s.rating, l = am(o, 0, 3), c = AL(t), u = kL(c), d = fd(t, e), f = c.active ? gn(c.penalty, 1) : 0, m = gn(d.effectiveEmissionRating, 0), p = am((i === "suppressed" ? 0 : l) - f, 0, 3), h = am(p - m, 0, 3), b = [
    ...a ? [{
      id: "stealth.base",
      label: "Base Stealth Rating",
      value: a,
      tags: ["stealth", "base"]
    }] : [],
    ...r.parts,
    ...s.parts,
    ...u ? [u] : [],
    ...d.parts.map((y) => ({
      id: `stealth.penalty.${y.id}`,
      label: y.label,
      value: -di(y.value, 0),
      tags: Array.from(/* @__PURE__ */ new Set(["stealth", "emission", "penalty", ...y.tags ?? []])),
      sourceId: y.sourceId,
      sourceName: y.sourceName
    }))
  ];
  return {
    enabled: l > 0,
    baseRating: a,
    contributionRating: r.rating + s.rating,
    rawRating: o,
    clampedRawRating: l,
    counterableRating: p,
    effectiveRating: h,
    mode: i,
    revealed: c.active,
    revealPenalty: f,
    reveal: c,
    emissionPenalty: m,
    emission: d,
    detectionCap: n.detectionCap || null,
    counteredBy: Es(n.counteredBy),
    parts: b
  };
}
async function pd(t = null, e = {}) {
  if (!Ti(t)) return !1;
  const n = nr(t);
  return n.revealed = {
    ...hw({
      ...e,
      reason: (e == null ? void 0 : e.reason) ?? "weaponAttack",
      penalty: (e == null ? void 0 : e.penalty) ?? 1,
      source: (e == null ? void 0 : e.source) ?? "system",
      duration: (e == null ? void 0 : e.duration) ?? "untilNextActivation",
      ctx: e
    })
  }, delete n.revealed.rating, await Ks(t, n), await Fl(t, fw, !0, {
    reason: n.revealed.reason,
    source: n.revealed.source
  }), !0;
}
async function TL(t = null, e = {}) {
  var i;
  if (!Ti(t)) return !1;
  const n = nr(t);
  return (i = n.revealed) != null && i.active && (n.revealed = { ...n.revealed, active: !1 }, await Ks(t, n)), await Fl(t, fw, !1, {
    reason: (e == null ? void 0 : e.reason) ?? "clearReveal",
    source: (e == null ? void 0 : e.source) ?? "system"
  }), !0;
}
async function Aw(t = null, e = {}) {
  if (!Ti(t)) return !1;
  const n = gn(e == null ? void 0 : e.rating, 1);
  if (n <= 0) return !1;
  const i = nr(t);
  return i.transientEmission = {
    ...hw({
      ...e,
      reason: (e == null ? void 0 : e.reason) ?? "highEmission",
      rating: n,
      source: (e == null ? void 0 : e.source) ?? "system",
      duration: (e == null ? void 0 : e.duration) ?? "untilNextActivation",
      ctx: e
    })
  }, delete i.transientEmission.penalty, await Ks(t, i), await Fl(t, fa, !0, {
    reason: i.transientEmission.reason,
    source: i.transientEmission.source
  }), !0;
}
async function wL(t = null, e = {}) {
  var i;
  if (!Ti(t)) return !1;
  const n = nr(t);
  return (i = n.transientEmission) != null && i.active && (n.transientEmission = { ...n.transientEmission, active: !1 }, await Ks(t, n)), await Fl(t, fa, !1, {
    reason: (e == null ? void 0 : e.reason) ?? "clearEmission",
    source: (e == null ? void 0 : e.source) ?? "system"
  }), !0;
}
async function ML(t = null, e = "passive", n = {}) {
  var a, r;
  if (!Ti(t)) return !1;
  const i = zl(e);
  return typeof t.update == "function" ? await t.update({ "system.mwd.stealth.mode": i }) : (t.system ?? (t.system = {}), (a = t.system).mwd ?? (a.mwd = {}), (r = t.system.mwd).stealth ?? (r.stealth = {}), t.system.mwd.stealth.mode = i), await Fl(t, oL, i === "active", {
    reason: (n == null ? void 0 : n.reason) ?? "stealthMode",
    source: (n == null ? void 0 : n.source) ?? "system"
  }), !0;
}
async function vL(t = null, e = {}) {
  var a, r, s;
  if (!Ti(t)) return { ok: !1, reason: "Go Dark is a machine action." };
  const n = zl((s = (r = (a = t == null ? void 0 : t.system) == null ? void 0 : a.mwd) == null ? void 0 : r.stealth) == null ? void 0 : s.mode);
  await TL(t, { ...e, reason: (e == null ? void 0 : e.reason) ?? "goDark" }), await wL(t, { ...e, reason: (e == null ? void 0 : e.reason) ?? "goDark" }), n !== "suppressed" && await ML(t, "passive", { ...e, reason: (e == null ? void 0 : e.reason) ?? "goDark" });
  const i = gd(t, e);
  return {
    ok: !0,
    actionId: "goDark",
    mode: i.mode,
    effectiveRating: i.effectiveRating,
    suppressed: i.mode === "suppressed"
  };
}
function ny(t, e = []) {
  return e.some((n) => t.has(Ga(n)));
}
function CL(t = {}) {
  var r, s;
  const e = String((t == null ? void 0 : t.rangeBand) ?? ((r = t == null ? void 0 : t.attack) == null ? void 0 : r.rangeBand) ?? ((s = t == null ? void 0 : t.payload) == null ? void 0 : s.rangeBand) ?? "").trim().toLowerCase();
  if (e) return e;
  const n = (t == null ? void 0 : t.attackerToken) ?? (t == null ? void 0 : t.sourceToken) ?? null, i = (t == null ? void 0 : t.targetToken) ?? null, a = xl(n, i);
  return Number.isFinite(a) ? md(a, "close") : "";
}
function IL(t = {}) {
  const e = (t == null ? void 0 : t.targetToken) ?? null;
  return e ? e.visible !== !1 : !!(t != null && t.visualContact);
}
function kw(t = null, e = null, n = {}) {
  const i = [], a = (e == null ? void 0 : e.statuses) ?? /* @__PURE__ */ new Set(), r = sL.find((l) => {
    var c;
    return (c = a.has) == null ? void 0 : c.call(a, l);
  });
  if (r)
    return {
      value: 0,
      bypass: !0,
      parts: [{
        id: `stealth.counter.${r}`,
        label: r === "narced" ? "NARC Beacon" : "TAG Designation",
        value: "bypass",
        tags: ["stealth", "counter", "beacon"]
      }]
    };
  const s = mL(t), o = fd(e, n);
  return o.effectiveEmissionRating > 0 && i.push({
    id: "stealth.counter.highEmission",
    label: "Target High Emission",
    value: -o.effectiveEmissionRating,
    tags: ["stealth", "counter", "emission"],
    emission: o
  }), ny(s, ["activeProbe", "probe", "probeking", "advancedSensors"]) && i.push({
    id: "stealth.counter.activeProbe",
    label: "Active Probe",
    value: -1,
    tags: ["stealth", "counter", "activeProbe"]
  }), (n != null && n.c3SharedLock || ny(s, ["c3", "c3network", "network", "sharedLock"])) && i.push({
    id: "stealth.counter.c3",
    label: "C3 / Shared Lock",
    value: -1,
    tags: ["stealth", "counter", "c3"]
  }), CL(n) === "close" && IL(n) && i.push({
    id: "stealth.counter.visualClose",
    label: "Close Visual Contact",
    value: -1,
    tags: ["stealth", "counter", "visual"]
  }), {
    value: i.reduce((l, c) => l + Math.abs(di(c.value, 0)), 0),
    bypass: !1,
    parts: i
  };
}
function EL(t = null, e = null, n = {}) {
  const i = gd(e, n);
  if (!i.enabled) return [];
  const a = kw(t, e, n);
  if (a.bypass)
    return a.parts.map((u) => ({
      ...u,
      value: 0,
      displayValue: "bypass"
    }));
  const r = gn(i.counterableRating ?? i.effectiveRating, 0);
  if (r <= 0) return [];
  const s = Math.max(0, r - a.value), o = r - s, l = [{
    id: "target.stealth",
    label: "Target Signature Suppression",
    value: r,
    tags: ["stealth", "sensors", "targeting"]
  }];
  let c = o;
  for (const u of a.parts) {
    if (c <= 0) break;
    const d = Math.min(c, Math.abs(di(u.value, 0)));
    d <= 0 || (l.push({
      ...u,
      value: -d
    }), c -= d);
  }
  return l;
}
function NL(t = null, e = null, n = {}) {
  const i = gd(e, n);
  if (!i.enabled) return null;
  const a = kw(t, e, n);
  if (a.bypass) return null;
  const r = gn(i.counterableRating ?? i.effectiveRating, 0), s = Math.max(0, r - a.value);
  return s <= 0 ? null : {
    id: "tracking.stealth",
    label: "Target Stealth Signature",
    value: -s,
    source: "Stealth",
    tags: ["tracking", "stealth", "sensors"]
  };
}
function PL(t = null, e = null, n = {}) {
  if (!Ti(t) || !Ti(e)) return null;
  const i = fd(e, n), a = gn(i.effectiveEmissionRating, 0);
  return a <= 0 ? null : {
    id: "acquire.highEmission",
    label: "Target High Emission",
    value: a,
    source: "Signature",
    tags: ["acquire", "stealth", "emission", "sensors"],
    emission: i
  };
}
const hu = "mwd", Tw = "targeting", RL = "ewState";
function DL(t = "") {
  var n, i, a, r;
  const e = String(t ?? "").trim();
  return e ? ((r = (a = (i = (n = globalThis.canvas) == null ? void 0 : n.tokens) == null ? void 0 : i.placeables) == null ? void 0 : a.find((s) => {
    var o;
    return (((o = s.document) == null ? void 0 : o.uuid) ?? s.uuid) === e;
  })) == null ? void 0 : r.actor) ?? null : null;
}
function Yi(t = "") {
  return String(t ?? "").trim();
}
function Wp(t) {
  return t && typeof t == "object" ? t : {};
}
function OL(t = {}, e = "") {
  var i, a;
  const n = Yi(e);
  if (n) {
    if (Object.prototype.hasOwnProperty.call(t, n)) return t[n];
    if (typeof ((a = (i = globalThis.foundry) == null ? void 0 : i.utils) == null ? void 0 : a.getProperty) == "function") {
      const r = globalThis.foundry.utils.getProperty(t, n);
      if (r !== void 0) return r;
    }
    return n.split(".").reduce((r, s) => {
      if (!(!r || typeof r != "object"))
        return r[s];
    }, t);
  }
}
function LL(t = {}, e = "") {
  const n = Yi(e).split(".").filter(Boolean);
  if (!n.length) return;
  const i = [];
  let a = t;
  for (const r of n.slice(0, -1)) {
    if (!a || typeof a != "object" || !Object.prototype.hasOwnProperty.call(a, r)) return;
    i.push([a, r]), a = a[r];
  }
  if (!(!a || typeof a != "object")) {
    delete a[n.at(-1)];
    for (let r = i.length - 1; r >= 0; r -= 1) {
      const [s, o] = i[r], l = s[o];
      if (l && typeof l == "object" && !Array.isArray(l) && Object.keys(l).length === 0)
        delete s[o];
      else
        break;
    }
  }
}
function hd(t = {}, e = "", n = null) {
  const i = Yi(e);
  return i && (LL(t, i), t[i] = n), t;
}
function $L(t) {
  return t ? Array.isArray(t) ? t : Array.isArray(t.contents) ? t.contents : typeof t[Symbol.iterator] == "function" ? Array.from(t) : [] : [];
}
function ww(t) {
  var r, s, o, l, c, u;
  const e = String(t ?? "").trim(), n = (s = (r = globalThis.game) == null ? void 0 : r.combat) == null ? void 0 : s.combatants;
  if (!e || !n) return null;
  const i = (o = n.get) == null ? void 0 : o.call(n, e), a = (i == null ? void 0 : i.tokenId) ?? ((l = i == null ? void 0 : i.token) == null ? void 0 : l.id) ?? ((u = (c = i == null ? void 0 : i.token) == null ? void 0 : c.document) == null ? void 0 : u.id);
  return i && a === e ? i : $L(n).find((d) => {
    var g, p, h;
    const f = Array.isArray(d) ? d[1] : d;
    return ((f == null ? void 0 : f.tokenId) ?? ((g = f == null ? void 0 : f.token) == null ? void 0 : g.id) ?? ((h = (p = f == null ? void 0 : f.token) == null ? void 0 : p.document) == null ? void 0 : h.id)) === e;
  }) ?? null;
}
function Pn(t) {
  var n;
  if (!t) return null;
  const e = (t == null ? void 0 : t.id) ?? ((n = t == null ? void 0 : t.document) == null ? void 0 : n.id) ?? (t == null ? void 0 : t.tokenId) ?? String(t ?? "").trim();
  return ww(e);
}
function ao(t) {
  return ww(t);
}
function Vp(t) {
  if (!t || typeof t != "object") return null;
  const e = String(t.id ?? "").trim();
  if (!e) return null;
  const n = Number.isFinite(Number(t.round)) ? Number(t.round) : null, i = Number.isFinite(Number(t.expiresAfterRound)) ? Number(t.expiresAfterRound) : n;
  return {
    id: e,
    value: Math.max(0, Number(t.value ?? 0) || 0),
    sourceTokenUuid: String(t.sourceTokenUuid ?? "").trim(),
    type: String(t.type ?? "self").trim() || "self",
    shareable: !!t.shareable,
    persistent: !!t.persistent,
    suppressedBy: t.suppressedBy ?? null,
    round: n,
    expiresAfterRound: i
  };
}
function _L(t = null) {
  return t && typeof t == "object" && !Array.isArray(t) ? Vp(t) : null;
}
function BL(t = []) {
  const e = (Array.isArray(t) ? t : []).map(Vp).filter(Boolean);
  return e.length ? e.sort((n, i) => {
    const a = Number(n.round ?? -1), r = Number(i.round ?? -1);
    return a !== r ? r - a : Number(i.value ?? 0) - Number(n.value ?? 0);
  })[0] ?? null : null;
}
function Jp(t) {
  const e = vn.includes(t == null ? void 0 : t.detectionState) ? t.detectionState : "blind", n = _L(t == null ? void 0 : t.packet) ?? BL(t == null ? void 0 : t.packets);
  return {
    detectionState: e,
    packet: n,
    packets: n ? [n] : []
  };
}
function Yp(t) {
  if (!t) return {};
  const e = t.getFlag(hu, Tw);
  return e && typeof e == "object" && Object.keys(e).length ? e : t.getFlag(hu, RL) ?? {};
}
function Ul(t, e = {}) {
  return t ? t.setFlag(hu, Tw, e) : Promise.resolve();
}
function xL(t) {
  const e = Wp(Yp(t));
  return Object.entries(e).map(([n, i]) => {
    const a = Yi(n);
    return a ? {
      targetTokenUuid: a,
      state: Jp(i)
    } : null;
  }).filter(Boolean);
}
function ir(t, e) {
  const n = Yi(e), i = Wp(Yp(t));
  return Jp(OL(i, n));
}
function ro(t, e) {
  return ir(t, e).detectionState;
}
function bd(t, e, n = null) {
  const i = (n == null ? void 0 : n.statuses) ?? /* @__PURE__ */ new Set();
  return i.has("tagged") || i.has("narced") ? "lock" : ro(t, e);
}
function Qp(t, e, n = {}) {
  var c, u;
  let i = 0;
  const a = (t == null ? void 0 : t.statuses) ?? /* @__PURE__ */ new Set(), r = Pp(t);
  a.has("ecmJamming") && (i += 2), (a.has("ecmShrouded") || r.has("ecmShrouded")) && (i += 1), a.has("obscuredLight") && (i += 1), a.has("obscuredHeavy") && (i += 3), a.has("obscured") && (i += 1);
  const s = a.has("narced"), o = gL(t), l = Ji(t, {
    payload: { intent: "attack" },
    resolved: { intent: "attack" }
  }).effects.reduce((d, f) => {
    var m;
    return o.has(f.sourceId) || o.has(f.sourceName) ? d : d + (Number(((m = f.modifies) == null ? void 0 : m.trackingPenalty) ?? 0) || 0);
  }, 0);
  if (i += s ? 0 : l, i += s ? 0 : Number(Zg(t).sensorTrackingPenalty ?? 0) || 0, e) {
    const d = ((c = e.getFlag(hu, "personalCombat")) == null ? void 0 : c.actionState) ?? {};
    d.move !== null && d.move !== void 0 && (i += 1);
  }
  return i += yO(t), i += ((u = ld(t, n)) == null ? void 0 : u.trackingPenalty) ?? 0, Math.max(0, i);
}
function zL(t, { attacker: e = null, payload: n = {}, resolved: i = {} } = {}) {
  const a = (t == null ? void 0 : t.statuses) ?? /* @__PURE__ */ new Set(), r = Pp(t);
  return a.has("ecmShrouded") || r.has("ecmShrouded") ? GD(e, {
    payload: { ...n, actionId: "acquireTarget" },
    resolved: { ...i, intent: "acquire" }
  }).has("ecmShrouded") ? 0 : 1 : 0;
}
function Mw(t, e = {}) {
  var c;
  const i = ((t == null ? void 0 : t.statuses) ?? /* @__PURE__ */ new Set()).has("ecmJamming") ? "track" : "lock", a = SO(t), r = ((c = ld(t, e)) == null ? void 0 : c.detectionStateCap) ?? "lock", s = vn.indexOf(i), o = vn.indexOf(a), l = o >= 0 && o < s ? a : i;
  return jO(l, r);
}
function Xp(t, e, n, i, a) {
  const r = { combatant: t, targetTokenUuid: e, systemAttr: n, detectionState: i, currentRound: a };
  if (Nn(Rt.ew.beforeAttackTargeting, r), i !== "track" && i !== "lock") return null;
  const { packet: s } = ir(t, e);
  if (!s || s.suppressedBy) return null;
  const o = Number.isFinite(Number(a)) ? Number(a) : null;
  if (!s.persistent && s.expiresAfterRound !== null && o !== null && o > s.expiresAfterRound)
    return null;
  const l = _p(n, i), c = DL(e), u = AO({
    attacker: (t == null ? void 0 : t.actor) ?? null,
    targetActor: c,
    value: Math.min(s.value, l)
  });
  return {
    id: s.id,
    value: u,
    sourceTokenUuid: s.sourceTokenUuid,
    round: s.round,
    expiresAfterRound: s.expiresAfterRound
  };
}
function Kl(t) {
  return foundry.utils.deepClone(Wp(Yp(t)));
}
async function bu(t, e, n) {
  const i = Yi(e);
  if (!i || !t) return;
  const a = vn.includes(n) ? n : "blind", r = a === "track" || a === "lock" ? Rt.ew.beforeAcquireSolution : Rt.ew.suppressTargetingNetwork;
  Nn(r, { combatant: t, targetTokenUuid: i, detectionState: a });
  const s = Kl(t), o = ir(t, i);
  hd(s, i, {
    detectionState: a,
    packet: o.packet ? foundry.utils.deepClone(o.packet) : null
  }), await Ul(t, s), Nn(Rt.ew.afterAcquireSolution, { combatant: t, targetTokenUuid: i, detectionState: a });
}
async function FL(t, e, n) {
  const i = Yi(e), a = Vp(n);
  if (!i || !t || !a) return;
  Nn(Rt.ew.beforeGenerateTargetingData, {
    combatant: t,
    targetTokenUuid: i,
    packet: a
  });
  const r = Kl(t), s = ir(t, i);
  hd(r, i, {
    detectionState: s.detectionState,
    packet: a
  }), await Ul(t, r), Nn(Rt.ew.afterGenerateTargetingData, {
    combatant: t,
    targetTokenUuid: i,
    packet: a
  });
}
async function Cf(t, e) {
  const n = Yi(e);
  if (!n || !t) return;
  const i = Kl(t), a = ir(t, n);
  hd(i, n, {
    detectionState: a.detectionState,
    packet: null
  }), await Ul(t, i);
}
async function UL(t, e, n = 1, { packetId: i = "", source: a = "defensiveJink" } = {}) {
  const r = Yi(e);
  if (!r || !t) return { ok: !1, reason: "Missing targeting state target." };
  const s = Math.max(0, Math.trunc(Number(n) || 0));
  if (s <= 0) return { ok: !1, reason: "No targetingData reduction requested." };
  const o = ir(t, r), l = o.packet ? foundry.utils.deepClone(o.packet) : null;
  if (!l) return { ok: !1, reason: "No targetingData packet is available." };
  const c = String(i ?? "").trim();
  if (c && l.id !== c)
    return { ok: !1, reason: "TargetingData packet no longer matches." };
  const u = Math.max(0, Number(l.value ?? 0) || 0), d = Math.max(0, u - s), f = Kl(t);
  return hd(f, r, {
    detectionState: o.detectionState,
    packet: d > 0 ? {
      ...l,
      value: d,
      reducedBy: a
    } : null
  }), await Ul(t, f), {
    ok: !0,
    previousValue: u,
    newValue: d,
    reduction: u - d,
    removed: d <= 0,
    packetId: l.id,
    targetTokenUuid: r
  };
}
async function KL(t, e, n = "") {
  const i = Yi(e), a = String(n ?? "").trim();
  if (!i || !t) return;
  const r = ir(t, i);
  r.packet && (a && r.packet.id !== a || (Nn(Rt.ew.consumeTargetingData, {
    combatant: t,
    targetTokenUuid: i,
    packet: r.packet
  }), await Cf(t, i)));
}
async function HL(t) {
  if (!t) return;
  const e = Kl(t);
  if (!Object.keys(e).length) return;
  const n = {};
  for (const [i, a] of Object.entries(e)) {
    const r = Jp(a);
    n[i] = { detectionState: "blind", packet: r.packet ? foundry.utils.deepClone(r.packet) : null };
  }
  await Ul(t, n);
}
function jL({ value: t, sourceToken: e, round: n } = {}) {
  var a;
  const i = Number.isFinite(Number(n)) ? Number(n) : null;
  return {
    id: foundry.utils.randomID(),
    value: Math.max(0, Number(t) || 0),
    sourceTokenUuid: ((a = e == null ? void 0 : e.document) == null ? void 0 : a.uuid) ?? (e == null ? void 0 : e.uuid) ?? "",
    type: "self",
    shareable: !1,
    persistent: !1,
    suppressedBy: null,
    round: i,
    expiresAfterRound: i
  };
}
const fl = ["close", "near", "far", "extreme"], GL = ["near", "close", "far", "extreme"];
function Hs(t) {
  return Array.isArray(t) ? t : [];
}
function Jn(t = "") {
  return String(t ?? "").trim();
}
function Bt(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function vw(t = {}) {
  const e = String((t == null ? void 0 : t.max) ?? (t == null ? void 0 : t.bandCap) ?? "near").trim().toLowerCase();
  return fl.includes(e) ? e : "near";
}
function yu(t = "near") {
  const e = fl.indexOf(String(t ?? "").trim().toLowerCase());
  return e >= 0 ? e : fl.indexOf("near");
}
function qL(t = "near", e = "near") {
  return yu(t) <= yu(e) ? t : e;
}
function If(t = {}) {
  return {
    max: vw(t),
    close: Bt((t == null ? void 0 : t.close) ?? (t == null ? void 0 : t.short), 0),
    near: Bt((t == null ? void 0 : t.near) ?? (t == null ? void 0 : t.medium), 0),
    far: Bt((t == null ? void 0 : t.far) ?? (t == null ? void 0 : t.long), 0),
    extreme: Bt(t == null ? void 0 : t.extreme, 0)
  };
}
function WL(t = null, e = null) {
  var n, i;
  return String(
    (e == null ? void 0 : e.category) ?? ((n = t == null ? void 0 : t.system) == null ? void 0 : n.category) ?? ((i = t == null ? void 0 : t.system) == null ? void 0 : i.weaponCategory) ?? "ranged"
  ).trim().toLowerCase() || "ranged";
}
function Cw(t = null, e = null) {
  var n, i, a, r;
  return String(
    (e == null ? void 0 : e.resolverKey) ?? ((n = e == null ? void 0 : e.resolution) == null ? void 0 : n.resolverKey) ?? ((a = (i = t == null ? void 0 : t.system) == null ? void 0 : i.resolution) == null ? void 0 : a.resolverKey) ?? ((r = t == null ? void 0 : t.system) == null ? void 0 : r.resolverKey) ?? "standard"
  ).trim().toLowerCase() || "standard";
}
function VL(t = null, e = null) {
  var r, s, o, l;
  if (Cw(t, e) !== "standard" || String(
    ((r = e == null ? void 0 : e.areaEffect) == null ? void 0 : r.kind) ?? ((o = (s = t == null ? void 0 : t.system) == null ? void 0 : s.areaEffect) == null ? void 0 : o.kind) ?? ""
  ).trim().toLowerCase() === "persistent") return !0;
  const a = String(
    (e == null ? void 0 : e.area) ?? ((l = t == null ? void 0 : t.system) == null ? void 0 : l.area) ?? "none"
  ).trim().toLowerCase();
  return !!(a && a !== "none");
}
function Ef(t = {}) {
  return fl.reduce((e, n) => (e[n] = Bt(t == null ? void 0 : t[n], 0), e), {});
}
function JL(t = null) {
  var i, a, r;
  const e = (a = (i = t == null ? void 0 : t.system) == null ? void 0 : i.mwd) == null ? void 0 : a.weaponGroups;
  if (e) {
    const s = Array.isArray(e) ? e : Object.values(e);
    if (s.length) return s;
  }
  const n = (r = t == null ? void 0 : t.system) == null ? void 0 : r.weaponGroups;
  return Array.isArray(n) ? n : [];
}
function YL(t = null) {
  return JL(t).map((e, n) => ({
    id: Jn(e == null ? void 0 : e.id) || `group-${n + 1}`,
    index: Number.isInteger(Number(e == null ? void 0 : e.index)) ? Number(e.index) : n,
    name: String((e == null ? void 0 : e.name) ?? `Weapon Group ${n + 1}`).trim() || `Weapon Group ${n + 1}`,
    weaponIds: Hs(e == null ? void 0 : e.weaponIds).map(Jn).filter(Boolean)
  }));
}
function Iw(t = null) {
  var n;
  const e = (t == null ? void 0 : t.state) ?? t ?? {};
  return Hs((n = e == null ? void 0 : e.actionState) == null ? void 0 : n.usedWeaponGroupIds).map(Jn).filter(Boolean);
}
function iy(t = {}, e = "") {
  const n = Jn(e);
  if (!n) return t;
  t.actionState ?? (t.actionState = {});
  const i = new Set(Iw(t));
  return i.add(n), t.actionState.usedWeaponGroupIds = Array.from(i), t;
}
function Ew(t, e) {
  const n = Array.isArray(t) ? t : String(t ?? "").split(","), i = ay(e);
  return i ? n.some((a) => ay(a) === i) : !1;
}
function ay(t = "") {
  return String(t ?? "").trim().toLowerCase().replace(/[^a-z0-9]+/g, "");
}
function QL(t = null, e = null, { token: n = null } = {}) {
  var P, L, _, O, x, H, G, q, ie, Z, Se, se, te, pe, K, be, Ie, Oe, Be;
  const i = Hs(e == null ? void 0 : e.weaponIds).map(Jn).filter(Boolean), a = i.map((R) => {
    var V, B;
    return ((B = (V = t == null ? void 0 : t.items) == null ? void 0 : V.get) == null ? void 0 : B.call(V, R)) ?? null;
  }).filter(Boolean), r = i.filter((R) => !a.some((V) => Jn(V == null ? void 0 : V.id) === R)), s = [], o = [], l = [], c = [];
  for (const R of a) {
    if (((R == null ? void 0 : R.canonicalType) ?? (R == null ? void 0 : R.type)) !== M.itemType.mechWeapon) {
      c.push(`${(R == null ? void 0 : R.name) ?? "Grouped item"} is not a BattleMech weapon.`);
      continue;
    }
    if (((P = R == null ? void 0 : R.isActive) == null ? void 0 : P.call(R)) === !1) {
      l.push(`${R.name ?? "Weapon"} is inactive and does not contribute to ranged fire.`);
      continue;
    }
    const B = ((L = R.getCombatProfile) == null ? void 0 : L.call(R)) ?? null;
    if (WL(R, B) === "melee") {
      l.push(`${R.name ?? "Weapon"} is melee-only and is ignored for ranged fire.`);
      continue;
    }
    if (VL(R, B)) {
      c.push(`${R.name ?? "Weapon"} uses a special attack mode and cannot be grouped in v1.`);
      continue;
    }
    const oe = nl(t, R == null ? void 0 : R.id);
    if (!oe) {
      c.push(`${R.name ?? "Weapon"} is not mounted in a loaded hardpoint.`);
      continue;
    }
    if (!wp(oe, R)) {
      c.push(`${R.name ?? "Weapon"} no longer matches its assigned hardpoint.`);
      continue;
    }
    const Ee = ei(
      (B == null ? void 0 : B.baseDamageType) ?? ((_ = R == null ? void 0 : R.system) == null ? void 0 : _.damageType) ?? "energy",
      "energy"
    ), Ye = String((B == null ? void 0 : B.damageType) ?? Ee).trim() || Ee, bt = String(
      (B == null ? void 0 : B.damageTypeLabel) ?? (Ye === Ee ? En(Ee) : ce(Ye))
    ).trim() || ce(Ye);
    s.push({
      id: Jn(R == null ? void 0 : R.id),
      name: String((R == null ? void 0 : R.name) ?? "Weapon").trim() || "Weapon",
      img: (R == null ? void 0 : R.img) ?? "",
      skill: String((B == null ? void 0 : B.skill) ?? ((O = R == null ? void 0 : R.system) == null ? void 0 : O.skill) ?? "gunnery").trim() || "gunnery",
      baseDamageType: Ee,
      baseDamageTypeLabel: En(Ee),
      damageType: Ye,
      damageTypeLabel: bt,
      resolverKey: Cw(R, B),
      range: If((B == null ? void 0 : B.range) ?? ((x = R == null ? void 0 : R.system) == null ? void 0 : x.range) ?? {}),
      rangeCap: vw((B == null ? void 0 : B.range) ?? ((H = R == null ? void 0 : R.system) == null ? void 0 : H.range) ?? {}),
      attackRatingBand: Ef((B == null ? void 0 : B.attackRatingBand) ?? ((G = R == null ? void 0 : R.system) == null ? void 0 : G.attackRatingBand) ?? {}),
      damage: Bt((B == null ? void 0 : B.damage) ?? ((q = R == null ? void 0 : R.system) == null ? void 0 : q.damage), 0),
      clusteringDice: Math.max(0, Bt(B == null ? void 0 : B.clusteringDice, 0)),
      ap: Bt((B == null ? void 0 : B.ap) ?? ((ie = R == null ? void 0 : R.system) == null ? void 0 : ie.ap) ?? ((Z = R == null ? void 0 : R.system) == null ? void 0 : Z.armorPiercing), 0),
      heat: Bt(((Se = R == null ? void 0 : R.system) == null ? void 0 : Se.heat) ?? (B == null ? void 0 : B.heat), 0),
      usesPerActivation: Math.max(1, Bt(((se = B == null ? void 0 : B.fireControl) == null ? void 0 : se.usesPerActivation) ?? ((pe = (te = R == null ? void 0 : R.system) == null ? void 0 : te.fireControl) == null ? void 0 : pe.usesPerActivation), 1)),
      notes: String((B == null ? void 0 : B.notes) ?? ((K = R == null ? void 0 : R.system) == null ? void 0 : K.notes) ?? ((be = R == null ? void 0 : R.system) == null ? void 0 : be.description) ?? "").trim(),
      keywords: (B == null ? void 0 : B.keywords) ?? ((Ie = R == null ? void 0 : R.system) == null ? void 0 : Ie.keywords) ?? [],
      skillDef: (B == null ? void 0 : B.skillDef) ?? null,
      hardpointId: Jn(oe == null ? void 0 : oe.id),
      hardpointType: String((oe == null ? void 0 : oe.type) ?? "").trim(),
      hardpointSize: String((oe == null ? void 0 : oe.size) ?? "").trim(),
      hardpointLocation: String((oe == null ? void 0 : oe.location) ?? "").trim(),
      isLockOnly: Ew((B == null ? void 0 : B.keywords) ?? ((Oe = R == null ? void 0 : R.system) == null ? void 0 : Oe.keywords), "lock-only"),
      sourceWeapon: R
    }), o.push({
      id: Jn(oe == null ? void 0 : oe.id),
      type: String((oe == null ? void 0 : oe.type) ?? "").trim(),
      size: String((oe == null ? void 0 : oe.size) ?? "").trim(),
      location: String((oe == null ? void 0 : oe.location) ?? "").trim(),
      itemId: Jn(R == null ? void 0 : R.id),
      weaponName: String((R == null ? void 0 : R.name) ?? "Weapon").trim() || "Weapon"
    });
  }
  if (s.some((R) => R.isLockOnly)) {
    const R = (n == null ? void 0 : n.combatant) ?? null;
    if (R) {
      const V = Array.from(((Be = game == null ? void 0 : game.user) == null ? void 0 : Be.targets) ?? []);
      V.length > 0 ? V.some((me) => {
        var Ee;
        const oe = ((Ee = me.document) == null ? void 0 : Ee.uuid) ?? "";
        return oe && bd(R, oe, me.actor) === "lock";
      }) || c.push("Requires sensor lock on target.") : xL(R).some(
        (me) => {
          var oe;
          return ((oe = me.state) == null ? void 0 : oe.detectionState) === "lock";
        }
      ) || c.push("Requires sensor lock: no locked targets.");
    }
  }
  r.length && c.push(`Missing weapon ids: ${r.join(", ")}.`), s.length === 0 && c.length === 0 && c.push("No active ranged weapons in this group."), new Set(s.map((R) => R.baseDamageType)).size > 1 && c.push("All grouped ranged weapons must share the same damage type."), new Set(s.map((R) => R.skill)).size > 1 && c.push("All grouped ranged weapons must use the same attack skill."), new Set(s.map((R) => R.resolverKey)).size > 1 && c.push("All grouped ranged weapons must share the same resolver family.");
  const m = s[0] ?? null, g = new Set(s.map((R) => R.damageType)), p = g.size === 1 ? (m == null ? void 0 : m.damageType) ?? (m == null ? void 0 : m.baseDamageType) ?? "energy" : (m == null ? void 0 : m.baseDamageType) ?? "energy", h = g.size === 1 ? (m == null ? void 0 : m.damageTypeLabel) ?? ce(p) : (m == null ? void 0 : m.baseDamageTypeLabel) ?? En(p), b = s.reduce(
    (R, V) => qL(R, V.rangeCap),
    (m == null ? void 0 : m.rangeCap) ?? "near"
  ), y = yu(b), S = fl.reduce((R, V, B) => (R[V] = B <= y ? s.reduce((me, oe) => {
    var Ee;
    return Math.min(me, Bt((Ee = oe.attackRatingBand) == null ? void 0 : Ee[V], 0));
  }, Number.POSITIVE_INFINITY) : 0, Number.isFinite(R[V]) || (R[V] = 0), R), {}), A = {
    ...(m == null ? void 0 : m.range) ?? If({ max: b }),
    max: b
  }, T = GL.find((R) => yu(R) <= y) ?? "close", C = s.length > 0 ? {
    damage: s.reduce((R, V) => R + V.damage, 0),
    clusteringDice: s.reduce((R, V) => R + V.clusteringDice, 0),
    ap: Math.max(0, ...s.map((R) => R.ap)),
    heat: s.reduce((R, V) => R + V.heat, 0),
    baseDamageType: (m == null ? void 0 : m.baseDamageType) ?? "energy",
    baseDamageTypeLabel: (m == null ? void 0 : m.baseDamageTypeLabel) ?? "Energy",
    damageType: p,
    damageTypeLabel: h,
    rangeCap: b,
    range: A,
    defaultRangeBand: T,
    attackRatings: S
  } : null, k = s.length > 0 ? s.reduce((R, V) => Math.min(R, V.usesPerActivation), Number.POSITIVE_INFINITY) : 1, w = s.find((R) => R.usesPerActivation === k) ?? null, v = s.length > 0 && k >= 2 && c.length === 0, N = {
    eligible: v,
    repeatCount: v ? k : 1,
    reason: v ? `${k} uses/activation` : w ? `Not rapid-fire capable. Limited by ${w.name}: ${k} use/activation` : "Not rapid-fire capable. No weapons in group"
  };
  return {
    id: Jn(e == null ? void 0 : e.id),
    index: Number.isInteger(Number(e == null ? void 0 : e.index)) ? Number(e.index) : 0,
    name: String((e == null ? void 0 : e.name) ?? "Weapon Group").trim() || "Weapon Group",
    weaponIds: i,
    missingWeaponIds: r,
    memberWeapons: s.map((R) => ({
      id: R.id,
      name: R.name,
      img: R.img,
      skill: R.skill,
      baseDamageType: R.baseDamageType,
      baseDamageTypeLabel: R.baseDamageTypeLabel,
      damageType: R.damageType,
      damageTypeLabel: R.damageTypeLabel,
      clusteringDice: R.clusteringDice,
      heat: R.heat,
      usesPerActivation: R.usesPerActivation
    })),
    memberHardpoints: o,
    compatibilityWarnings: l,
    baseDisableReason: c[0] ?? "",
    isAttackLegal: c.length === 0 && s.length > 0,
    attackSummary: C,
    rapidFire: N,
    _memberProfiles: s
  };
}
function Nw(t = null, { usedWeaponGroupIds: e = [], fireMode: n = Jt, token: i = null } = {}) {
  const a = new Set(Hs(e).map(Jn).filter(Boolean)), r = String(n ?? Jt).trim() || Jt;
  return YL(t).map((s) => {
    const o = QL(t, s, { token: i }), c = a.has(o.id), u = !c, d = c ? "Already fired this activation." : o.baseDisableReason;
    return {
      id: o.id,
      index: o.index,
      name: o.name,
      weaponIds: o.weaponIds,
      missingWeaponIds: o.missingWeaponIds,
      memberWeapons: o.memberWeapons,
      memberHardpoints: o.memberHardpoints,
      compatibilityWarnings: o.compatibilityWarnings,
      isAttackLegal: o.isAttackLegal,
      isAvailableThisActivation: u,
      disableReason: d,
      attackSummary: o.attackSummary,
      rapidFire: o.rapidFire,
      fireMode: r,
      _memberProfiles: o._memberProfiles
    };
  });
}
function XL(t = null, e = "", { usedWeaponGroupIds: n = [] } = {}) {
  const i = Jn(e);
  return i ? Nw(t, { usedWeaponGroupIds: n }).find((a) => a.id === i) ?? null : null;
}
function ZL(t = null, e = "", { usedWeaponGroupIds: n = [] } = {}) {
  var u;
  const i = XL(t, e, { usedWeaponGroupIds: n });
  if (!i)
    return { ok: !1, reason: "That weapon group no longer exists.", group: null, profile: null };
  if (!i.isAttackLegal)
    return { ok: !1, reason: i.disableReason || "That weapon group cannot be fired.", group: i, profile: null };
  if (!i.isAvailableThisActivation)
    return { ok: !1, reason: i.disableReason || "That weapon group has already fired this activation.", group: i, profile: null };
  const a = ((u = i._memberProfiles) == null ? void 0 : u[0]) ?? null, r = i.attackSummary ?? {}, s = Array.isArray(i._memberProfiles) ? i._memberProfiles : [], o = [];
  s.some((d) => {
    var f, m;
    return Ew(
      d.keywords ?? ((m = (f = d.sourceWeapon) == null ? void 0 : f.system) == null ? void 0 : m.keywords) ?? [],
      "armorBypass"
    );
  }) && o.push("armorBypass");
  const l = o.length > 0 ? { flags: o } : {}, c = [
    ...Hs(i.compatibilityWarnings),
    ...Hs(i._memberProfiles).map((d) => d.notes).filter(Boolean)
  ].join(`
`);
  return {
    ok: !0,
    reason: "",
    group: i,
    profile: {
      id: i.id,
      uuid: (t == null ? void 0 : t.uuid) ?? null,
      name: i.name || "Weapon Group",
      img: (a == null ? void 0 : a.img) ?? "",
      item: null,
      type: "mechWeaponGroup",
      equipped: !0,
      category: "ranged",
      skill: (a == null ? void 0 : a.skill) ?? "gunnery",
      skillDef: (a == null ? void 0 : a.skillDef) ?? null,
      damage: Bt(r.damage, 0),
      clusteringDice: Math.max(0, Bt(r.clusteringDice, 0)),
      ap: Bt(r.ap, 0),
      heat: Bt(r.heat, 0),
      baseDamageType: String(r.baseDamageType ?? "energy").trim() || "energy",
      baseDamageTypeLabel: String(r.baseDamageTypeLabel ?? r.baseDamageType ?? "energy").trim() || "Energy",
      damageType: String(r.damageType ?? r.baseDamageType ?? "energy").trim() || "energy",
      damageTypeLabel: String(r.damageTypeLabel ?? r.damageType ?? r.baseDamageTypeLabel ?? "Energy").trim() || "Energy",
      attackRatingBand: Ef(r.attackRatings ?? {}),
      range: If(r.range ?? { max: r.rangeCap ?? "near" }),
      defaultRangeBand: String(r.defaultRangeBand ?? "near").trim() || "near",
      traits: [],
      effects: l,
      notes: c,
      resolverKey: "standard",
      capabilityReport: {
        isTemplated: !1,
        errors: []
      },
      attackSummary: {
        damage: Bt(r.damage, 0),
        clusteringDice: Math.max(0, Bt(r.clusteringDice, 0)),
        ap: Bt(r.ap, 0),
        heat: Bt(r.heat, 0),
        baseDamageType: String(r.baseDamageType ?? "energy").trim() || "energy",
        baseDamageTypeLabel: String(r.baseDamageTypeLabel ?? r.baseDamageType ?? "energy").trim() || "Energy",
        damageType: String(r.damageType ?? r.baseDamageType ?? "energy").trim() || "energy",
        damageTypeLabel: String(r.damageTypeLabel ?? r.damageType ?? r.baseDamageTypeLabel ?? "Energy").trim() || "Energy",
        rangeCap: String(r.rangeCap ?? "near").trim() || "near",
        attackRatings: Ef(r.attackRatings ?? {})
      },
      machineWeaponGroup: {
        id: i.id,
        weaponIds: i.memberWeapons.map((d) => d.id),
        weaponNames: i.memberWeapons.map((d) => d.name),
        memberWeapons: i.memberWeapons.map((d) => ({ id: d.id, name: d.name })),
        hardpointIds: i.memberHardpoints.map((d) => d.id),
        memberHardpoints: i.memberHardpoints.map((d) => ({
          id: d.id,
          type: d.type,
          size: d.size,
          location: d.location,
          itemId: d.itemId,
          weaponName: d.weaponName
        }))
      },
      compatibilityWarnings: [...i.compatibilityWarnings]
    }
  };
}
const Pw = Object.freeze({
  none: Object.freeze({ id: "none", label: "No Critical", roman: "" }),
  minor: Object.freeze({ id: "minor", label: "Minor", roman: "I" }),
  moderate: Object.freeze({ id: "moderate", label: "Moderate", roman: "II" }),
  severe: Object.freeze({ id: "severe", label: "Severe", roman: "III" })
});
function jt({
  statusId: t,
  statusLabel: e,
  effectText: n,
  effectKind: i = "",
  effectPayload: a = {},
  remedyKey: r,
  remedyLabel: s,
  remedySkillKey: o = "",
  remedyBaseDn: l = 0
} = {}) {
  return Object.freeze({
    statusId: t,
    statusLabel: e,
    effectText: n,
    effectKind: i,
    effectPayload: Object.freeze({ ...a ?? {} }),
    remedyKey: r,
    remedyLabel: s,
    remedySkillKey: o,
    remedyBaseDn: l
  });
}
function ns(t, e, n, i) {
  return Object.freeze({
    id: t,
    roll: e,
    label: n,
    bands: Object.freeze(i)
  });
}
const Rw = Object.freeze({
  winded: ns("winded", 1, "Winded", {
    minor: jt({
      statusId: "windedMinor",
      statusLabel: "Winded I",
      effectText: "Add 1 Burn. Clear with Reduce Burn.",
      effectKind: "resource",
      effectPayload: { burn: 1 },
      remedyKey: "reduceBurn",
      remedyLabel: "Reduce Burn"
    }),
    moderate: jt({
      statusId: "windedModerate",
      statusLabel: "Winded II",
      effectText: "Add 2 Burn. Clear with Reduce Burn.",
      effectKind: "resource",
      effectPayload: { burn: 2 },
      remedyKey: "reduceBurn",
      remedyLabel: "Reduce Burn"
    }),
    severe: jt({
      statusId: "windedSevere",
      statusLabel: "Winded III",
      effectText: "Add 3 Burn. Clear with Reduce Burn.",
      effectKind: "resource",
      effectPayload: { burn: 3 },
      remedyKey: "reduceBurn",
      remedyLabel: "Reduce Burn"
    })
  }),
  concussion: ns("concussion", 2, "Concussion", {
    minor: jt({
      statusId: "concussionMinor",
      statusLabel: "Concussion I",
      effectText: "-1 die to actions and initiative.",
      effectKind: "rollMod",
      remedyKey: "endure",
      remedyLabel: "Endure"
    }),
    moderate: jt({
      statusId: "concussionModerate",
      statusLabel: "Concussion II",
      effectText: "-2 dice to actions and initiative.",
      effectKind: "rollMod",
      remedyKey: "endure",
      remedyLabel: "Endure"
    }),
    severe: jt({
      statusId: "concussionSevere",
      statusLabel: "Concussion III",
      effectText: "-3 dice to actions and initiative.",
      effectKind: "rollMod",
      remedyKey: "endure",
      remedyLabel: "Endure"
    })
  }),
  crippled: ns("crippled", 3, "Crippled", {
    minor: jt({
      statusId: "crippledMinor",
      statusLabel: "Crippled I",
      effectText: "-2 m movement.",
      effectKind: "speed",
      effectPayload: { speed: -2 },
      remedyKey: "firstAid",
      remedyLabel: "First Aid"
    }),
    moderate: jt({
      statusId: "crippledModerate",
      statusLabel: "Crippled II",
      effectText: "-4 m movement.",
      effectKind: "speed",
      effectPayload: { speed: -4 },
      remedyKey: "firstAid",
      remedyLabel: "First Aid"
    }),
    severe: jt({
      statusId: "crippledSevere",
      statusLabel: "Crippled III",
      effectText: "-6 m movement.",
      effectKind: "speed",
      effectPayload: { speed: -6 },
      remedyKey: "firstAid",
      remedyLabel: "First Aid"
    })
  }),
  hampered: ns("hampered", 4, "Hampered", {
    minor: jt({
      statusId: "hamperedMinor",
      statusLabel: "Hampered I",
      effectText: "Weapon unequipped; Ready Item clears it.",
      effectKind: "gate",
      effectPayload: { weaponUnequipped: !0 },
      remedyKey: "readyItem",
      remedyLabel: "Ready Item"
    }),
    moderate: jt({
      statusId: "hamperedModerate",
      statusLabel: "Hampered II",
      effectText: "Weapon unequipped and -1 die to physical actions.",
      effectKind: "gate",
      effectPayload: { weaponUnequipped: !0, physicalPenalty: -1 },
      remedyKey: "readyItem",
      remedyLabel: "Ready Item"
    }),
    severe: jt({
      statusId: "hamperedSevere",
      statusLabel: "Hampered III",
      effectText: "Weapon unequipped, -1 die to physical actions, and fall prone.",
      effectKind: "gate",
      effectPayload: { weaponUnequipped: !0, physicalPenalty: -1, prone: !0 },
      remedyKey: "readyItem",
      remedyLabel: "Ready Item"
    })
  }),
  offbalance: ns("offbalance", 5, "Off Balance", {
    minor: jt({
      statusId: "offbalanceMinor",
      statusLabel: "Off Balance I",
      effectText: "-2 attack rating and defense rating. Steady DN 2 clears it.",
      effectKind: "rollMod",
      remedyKey: "steady",
      remedyLabel: "Steady",
      remedyBaseDn: 2
    }),
    moderate: jt({
      statusId: "offbalanceModerate",
      statusLabel: "Off Balance II",
      effectText: "-4 attack rating and defense rating. Steady DN 2 clears it.",
      effectKind: "rollMod",
      remedyKey: "steady",
      remedyLabel: "Steady",
      remedyBaseDn: 2
    }),
    severe: jt({
      statusId: "offbalanceSevere",
      statusLabel: "Off Balance III",
      effectText: "-6 attack rating and defense rating. Steady DN 2 clears it.",
      effectKind: "rollMod",
      remedyKey: "steady",
      remedyLabel: "Steady",
      remedyBaseDn: 2
    })
  }),
  shaken: ns("shaken", 6, "Shaken", {
    minor: jt({
      statusId: "shakenMinor",
      statusLabel: "Shaken I",
      effectText: "Cannot Aim. Composure DN 2 clears it.",
      effectKind: "gate",
      effectPayload: { cannotAim: !0 },
      remedyKey: "composure",
      remedyLabel: "Composure",
      remedyBaseDn: 2
    }),
    moderate: jt({
      statusId: "shakenModerate",
      statusLabel: "Shaken II",
      effectText: "Cannot Aim or take reactions. Composure DN 2 clears it.",
      effectKind: "gate",
      effectPayload: { cannotAim: !0, cannotReact: !0 },
      remedyKey: "composure",
      remedyLabel: "Composure",
      remedyBaseDn: 2
    }),
    severe: jt({
      statusId: "shakenSevere",
      statusLabel: "Shaken III",
      effectText: "Cannot Aim, take reactions, or perform complex actions. Composure DN 2 clears it.",
      effectKind: "gate",
      effectPayload: { cannotAim: !0, cannotReact: !0, cannotComplex: !0 },
      remedyKey: "composure",
      remedyLabel: "Composure",
      remedyBaseDn: 2
    })
  })
}), e$ = Object.freeze(
  Object.fromEntries(Object.values(Rw).map((t) => [t.roll, t]))
);
function Dw(t) {
  return Rw[String(t ?? "").trim()] ?? null;
}
function t$(t) {
  return e$[Math.max(1, Math.min(6, Math.trunc(Number(t) || 1)))] ?? null;
}
function n$(t, e) {
  var i;
  const n = Dw(t);
  return ((i = n == null ? void 0 : n.bands) == null ? void 0 : i[String(e ?? "").trim()]) ?? null;
}
const ry = Object.freeze({
  reduceBurn: Object.freeze({
    key: "reduceBurn",
    label: "Reduce Burn",
    actionId: "reduceBurn",
    actionKind: "personalAction",
    skillKey: "",
    baseDn: 0,
    remediable: !0
  }),
  endure: Object.freeze({
    key: "endure",
    label: "Endure",
    actionId: "endure",
    actionKind: "commonCheck",
    skillKey: "endure",
    baseDn: 1,
    remediable: !0
  }),
  composure: Object.freeze({
    key: "composure",
    label: "Composure",
    actionId: "composure",
    actionKind: "commonCheck",
    skillKey: "composure",
    baseDn: 2,
    remediable: !0
  }),
  firstAid: Object.freeze({
    key: "firstAid",
    label: "First Aid",
    actionId: "firstAid",
    actionKind: "personalAction",
    skillKey: "medtech",
    baseDn: 1,
    remediable: !0
  }),
  readyItem: Object.freeze({
    key: "readyItem",
    label: "Ready Item",
    actionId: "readyItem",
    actionKind: "personalAction",
    skillKey: "",
    baseDn: 0,
    remediable: !0
  }),
  steady: Object.freeze({
    key: "steady",
    label: "Steady",
    actionId: "steady",
    actionKind: "commonCheck",
    skillKey: "steady",
    baseDn: 2,
    remediable: !0
  }),
  none: Object.freeze({
    key: "none",
    label: "No Remedy",
    actionId: "",
    actionKind: "none",
    skillKey: "",
    baseDn: 0,
    remediable: !1
  })
});
function Ow(t) {
  const e = String(t ?? "").trim();
  return ry[e] ?? ry.none;
}
const i$ = "personalCritical", rm = Object.freeze(["minor", "moderate", "severe"]);
function js(t) {
  var e, n;
  if (t !== void 0)
    return (n = (e = globalThis.foundry) == null ? void 0 : e.utils) != null && n.deepClone ? foundry.utils.deepClone(t) : JSON.parse(JSON.stringify(t ?? null));
}
function Lw() {
  var t, e, n;
  return ((n = (e = (t = globalThis.foundry) == null ? void 0 : t.utils) == null ? void 0 : e.randomID) == null ? void 0 : n.call(e)) ?? Math.random().toString(36).slice(2, 18);
}
function $w() {
  try {
    return (/* @__PURE__ */ new Date()).toISOString();
  } catch {
    return "";
  }
}
function Zp(t = {}) {
  return Math.max(0, Math.trunc(Number((t == null ? void 0 : t.previewRevision) ?? 0) || 0));
}
function _w(t) {
  var e;
  return ((e = Pw[t]) == null ? void 0 : e.label) ?? String(t ?? "").trim();
}
function sy(t) {
  var e;
  return ((e = Pw[t]) == null ? void 0 : e.roman) ?? "";
}
function a$(t) {
  const e = Number(t ?? 0);
  return e <= 9 ? "none" : e === 10 ? "minor" : e === 11 ? "moderate" : "severe";
}
function Bw(t) {
  if (t == null || t === "") return null;
  const e = Number(t);
  return Number.isFinite(e) ? e : null;
}
async function xw(t) {
  if (typeof Roll != "function") {
    const s = String(t).trim() === "1d6" ? 1 : 2;
    return { total: Array.from({ length: s }).reduce((l) => l + 1, 0), formula: t, results: Array.from({ length: s }, () => 1) };
  }
  const e = new Roll(t), n = typeof e.evaluate == "function" ? await e.evaluate({ async: !0 }) : e, i = Number((n == null ? void 0 : n.total) ?? e.total ?? 0) || 0, r = (Array.isArray(n == null ? void 0 : n.dice) ? n.dice : Array.isArray(e.dice) ? e.dice : []).flatMap((s) => Array.from((s == null ? void 0 : s.results) ?? []).map((o) => Number((o == null ? void 0 : o.result) ?? o) || 0));
  return { total: i, formula: t, results: r, roll: n ?? e };
}
function zw(t) {
  const e = Math.max(0, Math.trunc(Number(t ?? 0) || 0));
  return e >= 7 ? 3 : e >= 5 ? 2 : e >= 3 ? 1 : 0;
}
async function r$({ severity: t = 0, rollTotal: e = null } = {}) {
  const n = Math.max(0, Math.trunc(Number(t ?? 0) || 0)), i = Bw(e), a = i !== null ? { total: i, formula: "2d6", results: [] } : await xw("2d6"), r = Number(a.total ?? 0) + n;
  return {
    band: a$(r),
    total: r,
    severity: n,
    roll: a
  };
}
async function s$({ rollTotal: t = null } = {}) {
  const e = Bw(t), n = e !== null ? { total: e, formula: "1d6", results: [] } : await xw("1d6"), i = t$(n.total);
  return {
    familyId: (i == null ? void 0 : i.id) ?? "winded",
    familyLabel: (i == null ? void 0 : i.label) ?? "Winded",
    roll: n
  };
}
function Dr(t = {}, e = null) {
  var d, f, m, g, p, h, b;
  const n = String((t == null ? void 0 : t.familyId) ?? "").trim(), i = String((t == null ? void 0 : t.band) ?? "").trim(), a = Dw(n), r = n$(n, i);
  if (!a || !r) return null;
  const s = Ow((t == null ? void 0 : t.remedyKey) ?? r.remedyKey), o = String((t == null ? void 0 : t.statusLabel) ?? r.statusLabel ?? "").trim(), l = String((t == null ? void 0 : t.weaponUuid) ?? ((d = t == null ? void 0 : t.source) == null ? void 0 : d.weaponUuid) ?? "").trim(), c = String((t == null ? void 0 : t.weaponId) ?? "").trim(), u = l && (e != null && e.items) ? Array.from(((m = (f = e.items).values) == null ? void 0 : m.call(f)) ?? e.items ?? []).find((y) => (y == null ? void 0 : y.uuid) === l) ?? null : c && ((g = e == null ? void 0 : e.items) != null && g.get) ? e.items.get(c) : null;
  return {
    id: String((t == null ? void 0 : t.id) ?? "").trim() || Lw(),
    previewRevision: Zp(t),
    familyId: n,
    familyLabel: a.label,
    band: i,
    bandLabel: _w(i),
    bandRoman: sy(i),
    severity: Math.max(0, Math.trunc(Number((t == null ? void 0 : t.severity) ?? 0) || 0)),
    label: String((t == null ? void 0 : t.label) ?? `${a.label} ${sy(i)}`).trim(),
    statusId: String((t == null ? void 0 : t.statusId) ?? r.statusId ?? "").trim(),
    statusLabel: o,
    effectText: String((t == null ? void 0 : t.effectText) ?? r.effectText ?? "").trim(),
    effectKind: String((t == null ? void 0 : t.effectKind) ?? r.effectKind ?? "").trim(),
    effectPayload: js((t == null ? void 0 : t.effectPayload) ?? r.effectPayload ?? {}),
    remedyKey: s.key,
    remedyLabel: String((t == null ? void 0 : t.remedyLabel) ?? r.remedyLabel ?? s.label).trim(),
    remedySkillKey: String((t == null ? void 0 : t.remedySkillKey) ?? r.remedySkillKey ?? s.skillKey ?? "").trim(),
    remedyBaseDn: Math.max(0, Number((t == null ? void 0 : t.remedyBaseDn) ?? r.remedyBaseDn ?? s.baseDn ?? 0) || 0),
    active: (t == null ? void 0 : t.active) !== !1,
    createdRound: Math.max(0, Math.trunc(Number((t == null ? void 0 : t.createdRound) ?? ((h = (p = globalThis.game) == null ? void 0 : p.combat) == null ? void 0 : h.round) ?? 0) || 0)),
    createdAt: String((t == null ? void 0 : t.createdAt) ?? $w()).trim(),
    source: js((t == null ? void 0 : t.source) ?? {}),
    weaponUuid: l,
    weaponId: c,
    weaponName: String((t == null ? void 0 : t.weaponName) ?? (u == null ? void 0 : u.name) ?? ((b = t == null ? void 0 : t.source) == null ? void 0 : b.weaponName) ?? "").trim()
  };
}
function o$({
  actor: t = null,
  familyId: e = "",
  band: n = "",
  severity: i = 0,
  source: a = {},
  previewRevision: r = 0,
  weaponUuid: s = "",
  weaponId: o = "",
  weaponName: l = ""
} = {}) {
  var c, u;
  return Dr({
    id: Lw(),
    previewRevision: r,
    familyId: e,
    band: n,
    severity: i,
    active: !0,
    createdRound: Number(((u = (c = globalThis.game) == null ? void 0 : c.combat) == null ? void 0 : u.round) ?? 0) || 0,
    createdAt: $w(),
    source: js(a ?? {}),
    weaponUuid: s,
    weaponId: o,
    weaponName: l
  }, t);
}
function zr(t, e = {}) {
  var i;
  return (Array.isArray((i = t == null ? void 0 : t.system) == null ? void 0 : i.criticals) ? t.system.criticals : []).map((a) => Dr(a, t)).filter((a) => a && a.active !== !1).filter((a) => !e.familyId || a.familyId === e.familyId).filter((a) => !e.band || a.band === e.band).filter((a) => !e.statusId || a.statusId === e.statusId);
}
function l$(t) {
  return zr(t).reduce((e, n) => {
    var a;
    const i = Number(((a = n == null ? void 0 : n.effectPayload) == null ? void 0 : a.speed) ?? 0);
    return e + (Number.isFinite(i) ? i : 0);
  }, 0);
}
function Fw(t) {
  var c;
  const e = Math.max(0, Math.trunc(Number(((c = t == null ? void 0 : t.system) == null ? void 0 : c.speed) ?? 0) || 0)), n = Math.trunc(l$(t)), i = Math.trunc(oi(t, "speedMod")), a = { base: e, modifier: n + i }, r = zt({
    actor: t,
    phase: "onDerivedPersonalCombat",
    facts: Uk({ actor: t, packet: a, runtime: {} }),
    packet: a,
    options: { consumeUsage: !1 }
  }), s = Math.trunc(Number(r.packet.modifier ?? n) || 0), o = Math.max(0, e + s), l = {
    base: e,
    modifier: s,
    effective: o,
    adjusted: s !== 0
  };
  return r.modifiers.length && (l.modifiers = r.modifiers), l;
}
function c$(t = {}, e = null) {
  return Array.isArray(t == null ? void 0 : t.preparedCriticalRecords) ? t.preparedCriticalRecords.map((n) => Dr(js(n), e)).filter(Boolean) : [];
}
function u$(t = {}, e = []) {
  if (!e.length) return { ok: !0 };
  const n = Zp(t);
  return e.find((a) => Number((a == null ? void 0 : a.previewRevision) ?? n) !== n) ? { ok: !1, reason: "Critical preview is stale. Rebuild the attack damage preview before applying." } : { ok: !0 };
}
async function oy({
  actor: t = null,
  payload: e = {},
  outcome: n = "",
  netHits: i = 0,
  source: a = {},
  previewRevision: r = 0,
  weaponUuid: s = "",
  weaponId: o = "",
  weaponName: l = "",
  bandRollTotal: c = null,
  familyRollTotal: u = null
} = {}) {
  var A, T;
  const d = String(n || (e == null ? void 0 : e.outcome) || "").trim(), f = Math.max(0, Math.trunc(Number(i ?? (e == null ? void 0 : e.critNetHits) ?? (e == null ? void 0 : e.netHits) ?? 0) || 0)), m = Math.max(0, Math.trunc(Number((e == null ? void 0 : e.critSeverity) ?? zw(f)) || 0)), g = Zp({ previewRevision: (e == null ? void 0 : e.previewRevision) ?? r }), p = c$(e, t), h = u$({ previewRevision: g }, p);
  if (!h.ok) return h;
  if (p.length)
    return {
      ok: !0,
      selected: !0,
      severity: m,
      netHits: f,
      band: ((A = p[0]) == null ? void 0 : A.band) ?? "",
      familyId: ((T = p[0]) == null ? void 0 : T.familyId) ?? "",
      records: p,
      previewRevision: g,
      prepared: !0
    };
  if (e != null && e.criticalPreview && e.criticalPreview.selected === !1)
    return {
      ok: !0,
      selected: !1,
      severity: m,
      netHits: f,
      band: String(e.criticalPreview.band ?? "none"),
      bandTotal: Number(e.criticalPreview.bandTotal ?? 0) || 0,
      records: [],
      previewRevision: g,
      prepared: !0
    };
  if (d !== "hit")
    return { ok: !0, selected: !1, severity: m, netHits: f, band: "none", records: [], previewRevision: g };
  const b = await r$({ severity: m, rollTotal: c });
  if (b.band === "none")
    return {
      ok: !0,
      selected: !1,
      severity: m,
      netHits: f,
      band: "none",
      bandTotal: b.total,
      bandRoll: b,
      records: [],
      previewRevision: g
    };
  const y = await s$({ rollTotal: u }), S = o$({
    actor: t,
    familyId: y.familyId,
    band: b.band,
    severity: m,
    source: a,
    previewRevision: g,
    weaponUuid: s,
    weaponId: o,
    weaponName: l
  });
  return {
    ok: !0,
    selected: !0,
    severity: m,
    netHits: f,
    band: b.band,
    bandLabel: _w(b.band),
    bandTotal: b.total,
    bandRoll: b,
    familyId: y.familyId,
    familyLabel: y.familyLabel,
    familyRoll: y,
    records: S ? [S] : [],
    previewRevision: g
  };
}
async function Uw(t, e) {
  if (t != null && t.toggleStatusEffect)
    try {
      await mt({
        actor: t,
        statusId: i$,
        active: !!e,
        metadata: {
          scope: "Personal critical effects",
          notes: "Visual marker for active system.criticals entries."
        }
      });
    } catch (n) {
      console.warn("MWD | Unable to sync personal critical status", n);
    }
}
async function d$(t, e, n = []) {
  if (n.some((i) => {
    var a;
    return (a = i == null ? void 0 : i.effectPayload) == null ? void 0 : a.cannotAim;
  }))
    try {
      await D.clearAim(t, { token: e });
    } catch {
    }
}
async function ly(t, e, n = []) {
  var i, a, r, s, o;
  for (const l of n) {
    const c = String((l == null ? void 0 : l.statusId) ?? "").trim();
    if (c)
      try {
        await mt({ actor: t, statusId: c, active: !0 });
      } catch (f) {
        console.warn(`MWD | Unable to apply personal crit status "${c}"`, f);
      }
    const u = (l == null ? void 0 : l.effectPayload) ?? {}, d = Math.max(0, Number(u.burn ?? 0) || 0);
    if (d > 0 && await t.update({ "system.burn.value": Math.max(0, Number(((a = (i = t.system) == null ? void 0 : i.burn) == null ? void 0 : a.value) ?? 0) + d) }), u.weaponUnequipped) {
      const f = l.weaponId && ((r = t.items) != null && r.get) ? t.items.get(l.weaponId) : Array.from(((o = (s = t.items) == null ? void 0 : s.values) == null ? void 0 : o.call(s)) ?? []).find((m) => (m == null ? void 0 : m.uuid) === l.weaponUuid);
      f != null && f.update && await f.update({ "system.equipped": !1, "system.isPrimary": !1 });
    }
    if (u.prone)
      try {
        await mt({ actor: t, statusId: "prone", active: !0 });
      } catch (f) {
        console.warn("MWD | Unable to apply prone from personal critical", f);
      }
  }
  await d$(t, e, n);
}
async function cy({ actor: t = null, token: e = null, records: n = [], dryRun: i = !1 } = {}) {
  var u, d, f;
  if (!t) return { ok: !1, reason: "Personal criticals require an actor." };
  const a = Array.from(n ?? []).map((m) => Dr(m, t)).filter(Boolean);
  if (!a.length) return { ok: !0, records: [], activeCrits: zr(t) };
  const r = Array.isArray((u = t == null ? void 0 : t.system) == null ? void 0 : u.criticals) ? js(t.system.criticals) : [], s = [], o = [];
  for (const m of a) {
    const g = r.findIndex(
      (p) => String((p == null ? void 0 : p.familyId) ?? "").trim() === m.familyId && (p == null ? void 0 : p.active) !== !1
    );
    if (g >= 0) {
      const p = String(((d = r[g]) == null ? void 0 : d.band) ?? "").trim(), h = rm.indexOf(p), b = rm.indexOf(m.band), y = rm[Math.max(0, Math.max(h, b))], S = y !== p, A = String(((f = r[g]) == null ? void 0 : f.statusId) ?? "").trim(), T = Dr({
        ...r[g],
        band: y,
        statusId: void 0,
        statusLabel: void 0,
        effectText: void 0,
        effectKind: void 0,
        effectPayload: void 0,
        remedyKey: void 0,
        remedyLabel: void 0,
        remedySkillKey: void 0,
        remedyBaseDn: void 0,
        label: void 0
      }, t);
      r[g] = T, o.push({ oldStatusId: A, updatedRecord: T, bandChanged: S });
    } else
      s.push(m), r.push(m);
  }
  const l = r;
  if (!i) {
    await t.update({ "system.criticals": l }), await Uw(t, l.some((m) => (m == null ? void 0 : m.active) !== !1));
    for (const { oldStatusId: m, updatedRecord: g, bandChanged: p } of o)
      if (p) {
        if (m && m !== g.statusId)
          try {
            await mt({ actor: t, statusId: m, active: !1 });
          } catch (h) {
            console.warn(`MWD | Unable to deactivate old personal crit status "${m}"`, h);
          }
        await ly(t, e, [g]);
      }
    await ly(t, e, s);
  }
  return {
    ok: !0,
    records: [...s, ...o.map((m) => m.updatedRecord)],
    activeCrits: i ? l.map((m) => Dr(m, t)).filter(Boolean) : zr(t)
  };
}
async function m$({ actor: t = null, critId: e = "" } = {}) {
  var s;
  const n = String(e ?? "").trim();
  if (!t || !n) return { ok: !1, reason: "Personal critical not specified." };
  const i = Array.isArray((s = t.system) == null ? void 0 : s.criticals) ? js(t.system.criticals) : [], a = i.findIndex((o) => String((o == null ? void 0 : o.id) ?? "").trim() === n);
  if (a < 0) return { ok: !1, reason: "That critical effect is no longer active." };
  const r = Dr(i[a], t);
  if (i.splice(a, 1), await t.update({ "system.criticals": i }), r != null && r.statusId)
    try {
      await mt({ actor: t, statusId: r.statusId, active: !1 });
    } catch (o) {
      console.warn(`MWD | Unable to clear personal crit status "${r.statusId}"`, o);
    }
  return await Uw(t, i.some((o) => (o == null ? void 0 : o.active) !== !1)), { ok: !0, crit: r, activeCrits: zr(t) };
}
function f$(t = {}) {
  const e = [];
  return t.effectText && e.push(t.effectText), t.remedyLabel && t.remedyKey !== "none" && e.push(`Remedy: ${t.remedyLabel}${t.remedyBaseDn ? ` DN ${t.remedyBaseDn}` : ""}`), e.join(" | ");
}
function Kw(t = []) {
  return Array.from(t).map((e) => String(e ?? "").trim()).filter(Boolean);
}
function lc(t, e, n, i) {
  return {
    id: t,
    label: e,
    reason: n,
    critId: (i == null ? void 0 : i.id) ?? "",
    sourceLabel: (i == null ? void 0 : i.statusLabel) ?? (i == null ? void 0 : i.label) ?? ""
  };
}
function Hl(t) {
  var r, s, o;
  const e = zr(t), n = [], i = /* @__PURE__ */ new Map();
  for (const l of e) {
    const c = (l == null ? void 0 : l.effectPayload) ?? {}, u = (l == null ? void 0 : l.statusLabel) ?? (l == null ? void 0 : l.label) ?? "Personal Critical";
    if (c.cannotAim && n.push(lc("cannotAim", "Cannot Aim", u, l)), c.cannotReact && n.push(lc("cannotReact", "Cannot React", u, l)), c.cannotComplex && n.push(lc("cannotComplex", "Cannot Complex Action", u, l)), c.weaponUnequipped) {
      const d = Kw([l.weaponUuid, l.weaponId]);
      for (const f of d)
        i.set(f, {
          reason: `Weapon Unequipped (${u})`,
          critId: l.id,
          sourceLabel: u,
          weaponName: l.weaponName ?? ""
        });
      n.push(lc("weaponUnequipped", "Weapon Unequipped", u, l));
    }
  }
  const a = (l) => n.find((c) => c.id === l) ?? null;
  return {
    crits: e,
    restrictions: n,
    cannotAim: !!a("cannotAim"),
    cannotReact: !!a("cannotReact"),
    cannotComplex: !!a("cannotComplex"),
    aimReason: ((r = a("cannotAim")) == null ? void 0 : r.reason) ?? "",
    reactionReason: ((s = a("cannotReact")) == null ? void 0 : s.reason) ?? "",
    complexReason: ((o = a("cannotComplex")) == null ? void 0 : o.reason) ?? "",
    weaponRestrictions: i,
    restrictionLabels: n.map((l) => l.label)
  };
}
function cc(t, e = {}) {
  const n = Hl(t), i = String((e == null ? void 0 : e.id) ?? "").trim(), a = String((e == null ? void 0 : e.category) ?? "").trim();
  return i === "aim" && n.cannotAim ? `Disabled (${n.aimReason})` : a === He.reaction && n.cannotReact ? `Disabled (${n.reactionReason})` : a === He.complex && n.cannotComplex ? `Disabled (${n.complexReason})` : "";
}
function eh(t, e = null) {
  const n = Hl(t);
  if (n.cannotComplex) return `Disabled (${n.complexReason})`;
  const i = Kw([e == null ? void 0 : e.uuid, e == null ? void 0 : e.id]);
  for (const a of i) {
    const r = n.weaponRestrictions.get(a);
    if (r != null && r.reason) return r.reason;
  }
  return "";
}
function g$(t) {
  return Hl(t).restrictions.map((e) => ({
    id: e.id,
    label: e.label,
    reason: e.reason
  }));
}
const St = "mwd", Gt = "personalCombat", hr = "preparedInterrupt", p$ = "systems/mwd/img/icons/status/readied_action.svg", Nf = "destroyed", aa = 3, Hw = 1, jw = 1;
function uc(t, e) {
  return !(t != null && t.activation) || !e ? !1 : t.activation.combatId === e.combatId && Number(t.activation.round ?? -1) === Number(e.round ?? -1) && Number(t.activation.turn ?? -1) === Number(e.turn ?? -1) && t.activation.combatantId === e.combatantId;
}
function th(t = null, e = {}) {
  const n = Math.max(0, Number(e.fa ?? Hw) || 0), i = Math.max(0, Number(e.ra ?? jw) || 0);
  return {
    saRemaining: aa,
    faRemaining: n,
    raRemaining: i,
    saSpentThisActivation: 0,
    burnThisActivation: 0,
    attacksThisActivation: 0,
    reactionBurnSinceLastActivation: 0,
    traitUsage: {
      activation: {},
      round: {}
    },
    actionState: {
      aim: null,
      move: null,
      preparedInterrupt: null,
      usedWeaponGroupIds: [],
      fireModeChangedThisActivation: !1
    },
    hazards: {},
    pendingReaction: null,
    machineCritsProcessed: !1,
    actionLog: [],
    activation: t
  };
}
function Mo(t, e = null, n = {}) {
  return foundry.utils.mergeObject(
    th(e, n),
    foundry.utils.deepClone(t ?? {}),
    { inplace: !1, overwrite: !0 }
  );
}
function Pf(t, e = null, n = {}) {
  const i = Mo(t ?? {}, (t == null ? void 0 : t.activation) ?? e, n);
  return i.actionLog = br(i.actionLog), i.hazards = Su(i.hazards), i.pendingReaction = Rc(i.pendingReaction), i;
}
function Su(t) {
  return !t || typeof t != "object" ? {} : Object.fromEntries(
    Object.entries(t).map(([e, n]) => {
      const i = String(e ?? "").trim();
      return !i || !n || typeof n != "object" ? null : [i, {
        tier: Mt(n.tier, Te.none),
        turnsExposed: Math.max(0, Number(n.turnsExposed ?? 0) || 0),
        evadeLocked: !!n.evadeLocked,
        lastProcessedRound: Number(n.lastProcessedRound ?? 0) || 0
      }];
    }).filter(Boolean)
  );
}
function Rc(t) {
  if (!t || typeof t != "object") return null;
  const e = String(t.type ?? "").trim();
  return e ? {
    type: e,
    sourceKind: String(t.sourceKind ?? "").trim() || null,
    sourceId: String(t.sourceId ?? "").trim() || null,
    messageId: String(t.messageId ?? "").trim() || null,
    resultIndex: Number.isInteger(Number(t.resultIndex)) ? Number(t.resultIndex) : null,
    exposureBefore: Mt(t.exposureBefore, Te.none),
    exposureAfterPreview: Mt(t.exposureAfterPreview, Te.none),
    edgePoolKey: String(t.edgePoolKey ?? "").trim() || null,
    allowCurrentTurn: !!t.allowCurrentTurn
  } : null;
}
function br(t) {
  return Array.isArray(t) ? t.map((e) => {
    const n = String((e == null ? void 0 : e.label) ?? "").trim();
    if (!n) return null;
    const i = {
      id: String((e == null ? void 0 : e.id) ?? "").trim(),
      label: n,
      costLabel: String((e == null ? void 0 : e.costLabel) ?? "").trim()
    }, a = nh(e == null ? void 0 : e.resource);
    a && (i.resource = a);
    for (const [s, o] of Object.entries({
      cost: e == null ? void 0 : e.cost,
      saSpentDelta: e == null ? void 0 : e.saSpentDelta,
      attackDelta: e == null ? void 0 : e.attackDelta,
      burnThisActivationDelta: e == null ? void 0 : e.burnThisActivationDelta,
      reactionBurnDelta: e == null ? void 0 : e.reactionBurnDelta,
      actorBurnDelta: e == null ? void 0 : e.actorBurnDelta,
      actorEdgeDelta: e == null ? void 0 : e.actorEdgeDelta
    })) {
      const l = Number(o);
      Number.isFinite(l) && l !== 0 && (i[s] = l);
    }
    const r = String((e == null ? void 0 : e.edgePoolKey) ?? "").trim();
    return r && (i.edgePoolKey = r), i;
  }).filter(Boolean) : [];
}
function nh(t) {
  const e = String(t ?? "").trim().toLowerCase();
  return ["sa", "fa", "ra", "burn", "edge"].includes(e) ? e : "";
}
function h$(t = "") {
  const e = String(t ?? "").trim();
  if (!e) return {};
  if (/^free$/i.test(e)) return { resource: "fa", cost: 1 };
  const n = e.match(/^\+?\s*(\d+)\s*burn\b/i);
  if (n) return { resource: "burn", cost: Math.max(0, Number(n[1]) || 0) };
  const i = e.match(/^(\d+)\s*edge(?:\s*\(([^)]+)\))?/i);
  if (i)
    return {
      resource: "edge",
      cost: Math.max(0, Number(i[1]) || 0),
      edgePoolKey: String(i[2] ?? "").trim()
    };
  const a = e.match(/^(\d+)\s*(sa|fa|ra)\b/i);
  return a ? {
    resource: a[2].toLowerCase(),
    cost: Math.max(0, Number(a[1]) || 0)
  } : {};
}
function b$(t = {}) {
  const e = h$(t.costLabel), n = nh(t.resource) || e.resource || "", i = Math.max(0, Number(t.cost ?? e.cost ?? 0) || 0), a = vs(t.id), r = t.id === "attack" ? 1 : 0, s = n === "sa" ? i : 0, o = n === "burn" && (a == null ? void 0 : a.category) === He.reaction ? i : 0, l = Math.max(0, Number(
    t.burnThisActivationDelta ?? t.burnDelta ?? 0
  ) || 0), c = Number.isFinite(Number(t.actorBurnDelta)) ? Number(t.actorBurnDelta) : t.id === "reduceBurn" ? -1 : Math.max(0, l + (n === "burn" ? i : 0));
  return {
    resource: n,
    cost: i,
    saSpentDelta: Math.max(0, Number(t.saSpentDelta ?? s) || 0),
    attackDelta: Math.max(0, Number(t.attackDelta ?? r) || 0),
    burnThisActivationDelta: l,
    reactionBurnDelta: Math.max(0, Number(t.reactionBurnDelta ?? o) || 0),
    actorBurnDelta: c,
    actorEdgeDelta: Number(t.actorEdgeDelta ?? (n === "edge" ? -i : 0)) || 0,
    edgePoolKey: String(t.edgePoolKey ?? e.edgePoolKey ?? "").trim()
  };
}
function y$(t = []) {
  return br(t).filter((e) => {
    const n = vs(e == null ? void 0 : e.id);
    return (n == null ? void 0 : n.category) === He.reaction;
  });
}
function uy(t = null, e = null, n = null, i = {}) {
  const a = gs(n, i), r = th(e, a);
  return r.reactionBurnSinceLastActivation = Math.max(0, Number((t == null ? void 0 : t.reactionBurnSinceLastActivation) ?? 0) || 0), r.actionLog = y$(t == null ? void 0 : t.actionLog), r.hazards = Su(t == null ? void 0 : t.hazards), r;
}
function go(t = null) {
  var n;
  const e = (t == null ? void 0 : t.actionEconomyActor) ?? (t == null ? void 0 : t.actor) ?? null;
  return Pf(
    t == null ? void 0 : t.state,
    ((n = t == null ? void 0 : t.state) == null ? void 0 : n.activation) ?? null,
    gs(e, t ?? {})
  );
}
function S$(t, e) {
  return t === "none" ? "No Cost" : t === "free" ? "Free" : t === "burn" ? `+${e} Burn` : `${e} ${String(t).toUpperCase()}`;
}
function sm(t = {}, e = 0) {
  const n = t == null ? void 0 : t.cost;
  return n && typeof n == "object" ? Math.max(0, Number(n.value ?? e) || 0) : Math.max(0, Number(n ?? e) || 0);
}
function A$(t = {}, e = "sa") {
  const n = t == null ? void 0 : t.cost;
  return n && typeof n == "object" && String(n.resource ?? e).trim() || e;
}
function dy(t = {}) {
  var n, i;
  const e = String(((n = t == null ? void 0 : t.implementation) == null ? void 0 : n.state) ?? "ready").trim() || "ready";
  return e === "ready" || e === "legacy" ? "" : String(((i = t == null ? void 0 : t.implementation) == null ? void 0 : i.reason) ?? (t == null ? void 0 : t.reason) ?? "That action is not implemented yet.").trim() || "That action is not implemented yet.";
}
function k$(t = {}, e = "", { snapshot: n = null, metadata: i = {} } = {}) {
  var s, o, l, c;
  const a = foundry.utils.deepClone(t ?? {});
  a.actionState ?? (a.actionState = {});
  const r = {
    actionId: e,
    round: Number(((s = n == null ? void 0 : n.combat) == null ? void 0 : s.round) ?? 0),
    turn: Number(((o = n == null ? void 0 : n.combat) == null ? void 0 : o.turn) ?? 0),
    combatantId: ((l = n == null ? void 0 : n.combatant) == null ? void 0 : l.id) ?? null
  };
  return e === "aim" && (a.actionState.aim = {
    ...r,
    target: ((c = n == null ? void 0 : n.targeting) == null ? void 0 : c.target) ?? null
  }), e === "move" && (a.actionState.move = {
    ...r,
    moved: !0
  }), e === "prepare" && (a.actionState.preparedInterrupt = {
    ...r,
    condition: String((i == null ? void 0 : i.condition) ?? "").trim(),
    scope: String((i == null ? void 0 : i.scope) ?? "").trim()
  }), e === "changeFireMode" && (a.actionState.fireModeChangedThisActivation = !0), a;
}
function po(t = {}) {
  var a;
  const e = ((a = t == null ? void 0 : t.actionState) == null ? void 0 : a.preparedInterrupt) ?? null;
  if (!e) return null;
  const n = String((e == null ? void 0 : e.condition) ?? "").trim(), i = String((e == null ? void 0 : e.scope) ?? "").trim();
  return !n && !i ? null : {
    ...e,
    condition: n,
    scope: i
  };
}
function T$(t = null) {
  if (!t) return "";
  const e = [];
  return t.condition && e.push(`Trigger: ${t.condition}`), t.scope && e.push(`Scope: ${t.scope}`), e.join(" | ");
}
function my() {
  return (CONFIG.statusEffects ?? []).find((t) => String((t == null ? void 0 : t.id) ?? "").trim() === hr) ?? {
    id: hr,
    name: "Prepared",
    icon: p$
  };
}
function w$(t) {
  const e = (CONFIG.statusEffects ?? []).find((i) => String((i == null ? void 0 : i.id) ?? "").trim() === t), n = String((e == null ? void 0 : e.name) ?? (e == null ? void 0 : e.label) ?? t ?? "").trim();
  return Vu(n);
}
function is(t) {
  const e = Number(t);
  return !Number.isFinite(e) || e === 0 ? "0" : e > 0 ? `+${e}` : String(e);
}
function M$(t) {
  if (typeof t == "number") return Number.isFinite(t) ? t : 0;
  const e = String(t ?? "").trim().match(/[-+]?\d+(\.\d+)?/);
  return e ? Number(e[0]) : 0;
}
function fy(t) {
  var o;
  const e = (t == null ? void 0 : t.document) ?? t ?? null, n = (t == null ? void 0 : t.object) ?? (e == null ? void 0 : e.object) ?? t ?? null, i = String((e == null ? void 0 : e.id) ?? "").trim(), a = D._pendingTokenPositions.get(i) ?? null, r = Number((a == null ? void 0 : a.x) ?? (e == null ? void 0 : e.x)), s = Number((a == null ? void 0 : a.y) ?? (e == null ? void 0 : e.y));
  if (n && Number.isFinite(r) && Number.isFinite(s)) {
    if (typeof n.getCenterPoint == "function")
      return n.getCenterPoint({ x: r, y: s });
    if (typeof n.getCenter == "function")
      return n.getCenter(r, s);
  }
  return (n == null ? void 0 : n.center) ?? ((o = e == null ? void 0 : e.object) == null ? void 0 : o.center) ?? null;
}
function v$(t, e = "") {
  if (!Number.isFinite(t)) return "";
  const n = Math.round(t * 10) / 10, i = Number.isInteger(n) ? String(n) : n.toFixed(1);
  return e ? `${i} ${e}` : i;
}
function om(t) {
  var n, i;
  const e = (i = (n = game.settings) == null ? void 0 : n.get) == null ? void 0 : i.call(n, "core", "messageMode");
  return typeof ChatMessage.applyMode == "function" && ChatMessage.applyMode(t, e), t;
}
function lm(t) {
  return !!Ha(t);
}
function gy(t = null) {
  return t ? Array.isArray(t) ? t : t.contents && Array.isArray(t.contents) ? t.contents : typeof t.values == "function" ? Array.from(t.values()) : typeof t[Symbol.iterator] == "function" ? Array.from(t) : [] : [];
}
function Gw(t = null) {
  var a, r, s, o, l;
  const e = /* @__PURE__ */ new Set(), n = (c) => {
    const u = String(c ?? "").trim();
    u && e.add(u);
  }, i = t == null ? void 0 : t.statuses;
  return (i != null && i.forEach || Array.isArray(i)) && i.forEach(n), n((r = (a = t == null ? void 0 : t.getFlag) == null ? void 0 : a.call(t, St, "status")) == null ? void 0 : r.id), n((l = (o = (s = t == null ? void 0 : t.flags) == null ? void 0 : s[St]) == null ? void 0 : o.status) == null ? void 0 : l.id), n(t == null ? void 0 : t.statusId), e;
}
function C$(t = null, e = "") {
  var r;
  const n = String(e ?? "").trim();
  if (!t || !n) return !1;
  const i = t.statuses;
  return (r = i == null ? void 0 : i.has) != null && r.call(i, n) || Array.isArray(i) && i.includes(n) ? !0 : [
    ...gy(t.effects),
    ...gy(t.appliedEffects)
  ].some((s) => Gw(s).has(n));
}
function I$(t = null) {
  var e, n, i;
  return Nt(t) ? C$(t, Nf) ? !0 : String(((i = (n = (e = t == null ? void 0 : t.system) == null ? void 0 : e.mwd) == null ? void 0 : n.status) == null ? void 0 : i.state) ?? "").trim().toLowerCase() === Nf : !1;
}
function E$(t = []) {
  const e = Array.isArray(t) ? t.filter(Boolean) : [];
  if (!e.length) return "";
  const n = e.slice().sort((a, r) => xa(r == null ? void 0 : r.tier) - xa(a == null ? void 0 : a.tier))[0] ?? null;
  if (!n) return "";
  const i = `HAZARD ${ci(n.tier)} (${Math.max(0, Number(n.turnsExposed ?? 0) || 0)})`;
  return n.evadeLocked ? `${i} LOCK` : i;
}
function gl(t = null) {
  var i, a, r, s;
  const e = /* @__PURE__ */ new Set(), n = (o) => {
    const l = String(o ?? "").trim();
    l && e.add(l);
  };
  return n(t == null ? void 0 : t.id), n(t == null ? void 0 : t._id), n(t == null ? void 0 : t.uuid), n((i = t == null ? void 0 : t.actor) == null ? void 0 : i.id), n((a = t == null ? void 0 : t.actor) == null ? void 0 : a.uuid), n((r = t == null ? void 0 : t.baseActor) == null ? void 0 : r.id), n((s = t == null ? void 0 : t.baseActor) == null ? void 0 : s.uuid), e;
}
function N$(t = "") {
  var i, a, r;
  const e = String(t ?? "").trim();
  if (!e) return null;
  if (typeof fromUuidSync == "function")
    try {
      const s = fromUuidSync(e);
      if (s) return (s == null ? void 0 : s.actor) ?? (s == null ? void 0 : s.baseActor) ?? s;
    } catch {
    }
  const n = e.startsWith("Actor.") ? e.slice(6).split(".")[0] : e;
  return ((a = (i = game == null ? void 0 : game.actors) == null ? void 0 : i.get) == null ? void 0 : a.call(i, n)) ?? (((r = game == null ? void 0 : game.actors) == null ? void 0 : r.contents) ?? []).find((s) => gl(s).has(e)) ?? null;
}
function Dc(t = null) {
  var n, i, a, r, s, o, l, c, u, d, f;
  if (!Nt(t)) return null;
  const e = String(
    ((i = (n = t == null ? void 0 : t.system) == null ? void 0 : n.pilot) == null ? void 0 : i.uuid) ?? ((s = (r = (a = t == null ? void 0 : t.system) == null ? void 0 : a.mwd) == null ? void 0 : r.pilot) == null ? void 0 : s.uuid) ?? ((c = (l = (o = t == null ? void 0 : t.system) == null ? void 0 : o.mwd) == null ? void 0 : l.crew) == null ? void 0 : c.operatorActorUuid) ?? ((f = (d = (u = t == null ? void 0 : t.system) == null ? void 0 : u.mwd) == null ? void 0 : d.crew) == null ? void 0 : f.pilotActorUuid) ?? ""
  ).trim();
  return N$(e);
}
function cm(t = null, e = null) {
  var r, s, o, l, c, u, d, f, m, g, p;
  if (!Nt(t) || !e) return !1;
  const n = String(
    ((s = (r = t == null ? void 0 : t.system) == null ? void 0 : r.pilot) == null ? void 0 : s.uuid) ?? ((c = (l = (o = t == null ? void 0 : t.system) == null ? void 0 : o.mwd) == null ? void 0 : l.pilot) == null ? void 0 : c.uuid) ?? ((f = (d = (u = t == null ? void 0 : t.system) == null ? void 0 : u.mwd) == null ? void 0 : d.crew) == null ? void 0 : f.operatorActorUuid) ?? ((p = (g = (m = t == null ? void 0 : t.system) == null ? void 0 : m.mwd) == null ? void 0 : g.crew) == null ? void 0 : p.pilotActorUuid) ?? ""
  ).trim(), i = gl(e);
  if (i.has(n)) return !0;
  const a = Dc(t);
  if (!a) return !1;
  for (const h of gl(a))
    if (i.has(h)) return !0;
  return !1;
}
const ys = class ys {
  static init() {
    var e;
    Hooks.on("updateCombat", (n, i) => this._onUpdateCombat(n, i)), Hooks.on("updateCombatant", (n, i) => this._onUpdateCombatant(n, i)), Hooks.on("updateActor", (n, i) => this._onUpdateActor(n, i)), Hooks.on("updateToken", (n, i) => this._onUpdateToken(n, i)), Hooks.on("refreshToken", (n) => this._onRefreshToken(n)), Hooks.on("createCombatant", (n) => this._onCreateCombatant(n)), Hooks.on("deleteCombatant", (n) => this._onDeleteCombatant(n)), Hooks.on("deleteCombat", (n) => this._onDeleteCombat(n)), Hooks.on("createActiveEffect", (n) => this._onActiveEffectChange(n)), Hooks.on("updateActiveEffect", (n) => this._onActiveEffectChange(n)), Hooks.on("deleteActiveEffect", (n) => this._onActiveEffectChange(n)), Hooks.on("createRegion", (n) => this._onCreateRegion(n)), Hooks.on("updateRegion", (n) => this._onUpdateRegion(n)), Hooks.on("deleteRegion", (n) => this._onDeleteRegion(n)), Hooks.on("targetToken", (n, i, a) => this._onTargetToken(n, i, a));
    for (const n of ["TOKEN_ENTER", "TOKEN_EXIT", "TOKEN_MOVE_IN", "TOKEN_MOVE_OUT"]) {
      const i = (e = CONST == null ? void 0 : CONST.REGION_EVENTS) == null ? void 0 : e[n];
      i && Hooks.on(i, (...a) => this._onRegionTokenEvent(...a));
    }
  }
  static async onReady() {
    var e;
    await this.reconcileOperatedCombatants(game.combat), await this.syncDestroyedMachineDefeatedCombatants(game.combat), await this.ensureCurrentCombatantState(), await this.syncPreparedIndicators(), await this._syncAllSceneHazards(), await this._processCurrentCombatantMachineCrits(), (e = game.combat) != null && e.id && this._lastActivationByCombat.set(
      game.combat.id,
      this.getActivationIdentity(game.combat, game.combat.combatant)
    ), this.renderOpenActorSheets();
  }
  static _asTokenDocument(e) {
    return e ? (e == null ? void 0 : e.document) ?? e : null;
  }
  static _getTokenSceneId(e) {
    var i, a, r, s;
    const n = this._asTokenDocument(e);
    return ((i = n == null ? void 0 : n.parent) == null ? void 0 : i.id) ?? ((a = n == null ? void 0 : n.scene) == null ? void 0 : a.id) ?? ((s = (r = n == null ? void 0 : n.object) == null ? void 0 : r.scene) == null ? void 0 : s.id) ?? null;
  }
  static _getSceneTokenDocumentById(e, n = ((i) => (i = canvas == null ? void 0 : canvas.scene) == null ? void 0 : i.id)()) {
    var s, o, l, c, u;
    const a = String(e ?? "").trim();
    if (!a || !n) return null;
    const r = ((s = canvas == null ? void 0 : canvas.scene) == null ? void 0 : s.id) === n ? canvas.scene : (l = (o = game.scenes) == null ? void 0 : o.get) == null ? void 0 : l.call(o, n);
    return ((u = (c = r == null ? void 0 : r.tokens) == null ? void 0 : c.get) == null ? void 0 : u.call(c, a)) ?? null;
  }
  static _getCombatantTokenDocument(e, n = ((i) => (i = canvas == null ? void 0 : canvas.scene) == null ? void 0 : i.id)()) {
    const a = this._asTokenDocument(e == null ? void 0 : e.token);
    return a && typeof a == "object" ? a : this._getSceneTokenDocumentById(this._getCombatantTokenId(e), n);
  }
  static _getCombatantTokenId(e) {
    var n, i, a, r, s;
    return String(
      (e == null ? void 0 : e.tokenId) ?? ((n = e == null ? void 0 : e.token) == null ? void 0 : n.id) ?? ((i = e == null ? void 0 : e.token) == null ? void 0 : i._id) ?? ((r = (a = e == null ? void 0 : e.token) == null ? void 0 : a.document) == null ? void 0 : r.id) ?? ((s = e == null ? void 0 : e._source) == null ? void 0 : s.tokenId) ?? ""
    ).trim();
  }
  static _getCombatantActorId(e) {
    var i, a, r, s;
    const n = this._asTokenDocument(e == null ? void 0 : e.token);
    return String(
      (e == null ? void 0 : e.actorId) ?? ((i = e == null ? void 0 : e.actor) == null ? void 0 : i.id) ?? ((a = e == null ? void 0 : e._source) == null ? void 0 : a.actorId) ?? (n == null ? void 0 : n.actorId) ?? ((r = n == null ? void 0 : n.actor) == null ? void 0 : r.id) ?? ((s = n == null ? void 0 : n.baseActor) == null ? void 0 : s.id) ?? ""
    ).trim();
  }
  static _getCombatants(e) {
    return e != null && e.combatants ? typeof e.combatants.values == "function" ? Array.from(e.combatants.values()) : Array.from(e.combatants ?? []) : [];
  }
  static _getCombatantActor(e, n = ((i) => (i = canvas == null ? void 0 : canvas.scene) == null ? void 0 : i.id)()) {
    const a = this._getCombatantTokenDocument(e, n);
    return (a == null ? void 0 : a.actor) ?? (e == null ? void 0 : e.actor) ?? null;
  }
  static _actorsMatch(e = null, n = null) {
    if (!e || !n) return !1;
    const i = gl(n);
    return Array.from(gl(e)).some((a) => i.has(a));
  }
  static _findMachineCombatantForPilot(e, n = game == null ? void 0 : game.combat) {
    var a;
    if (!e || !n) return null;
    const i = this._getCombatSceneId(n) || ((a = canvas == null ? void 0 : canvas.scene) == null ? void 0 : a.id);
    return this._getCombatants(n).find((r) => {
      const s = this._getCombatantActor(r, i);
      return cm(s, e);
    }) ?? null;
  }
  static _isDuplicatePilotCombatant(e, n = (e == null ? void 0 : e.combat) ?? (game == null ? void 0 : game.combat)) {
    var s;
    const i = this._getCombatSceneId(n) || this._getCombatantSceneId(e) || ((s = canvas == null ? void 0 : canvas.scene) == null ? void 0 : s.id), a = this._getCombatantActor(e, i);
    if (!a || Nt(a)) return !1;
    const r = this._findMachineCombatantForPilot(a, n);
    return !!(r && r.id !== (e == null ? void 0 : e.id));
  }
  static getPlatformActorForCombatant(e, n = ((i) => (i = canvas == null ? void 0 : canvas.scene) == null ? void 0 : i.id)()) {
    const a = this._getCombatantActor(e, n);
    return Nt(a) ? a : null;
  }
  static getActionEconomyActorForCombatant(e, n = ((i) => (i = canvas == null ? void 0 : canvas.scene) == null ? void 0 : i.id)()) {
    const a = this._getCombatantActor(e, n);
    return Nt(a) ? Dc(a) ?? a : a;
  }
  static resolveCombatantForActor(e, { combat: n = game == null ? void 0 : game.combat, token: i = null } = {}) {
    var a;
    return ((a = this.resolveActivationUnit({ actor: e, combat: n, token: i })) == null ? void 0 : a.combatant) ?? null;
  }
  static resolveActivationUnit({ actor: e = null, combat: n = game == null ? void 0 : game.combat, token: i = null, combatant: a = null } = {}) {
    var f, m;
    const r = this._getCombatSceneId(n) || ((f = canvas == null ? void 0 : canvas.scene) == null ? void 0 : f.id), s = (e == null ? void 0 : e.actor) ?? ((m = e == null ? void 0 : e.document) == null ? void 0 : m.actor) ?? e ?? null;
    let o = a ?? null;
    if (!o && s && !Nt(s) && (o = this._findMachineCombatantForPilot(s, n)), !o && s) {
      const g = this.getCombat(s, i);
      o = (g == null ? void 0 : g.combatant) ?? null, n = (g == null ? void 0 : g.combat) ?? n;
    }
    const l = this._getCombatantActor(o, r), c = Nt(l) ? l : Nt(s) ? s : null, u = c ? Dc(c) : null;
    return {
      kind: c && u ? "operatedMachine" : "actor",
      combat: n,
      combatant: o,
      actor: s,
      actingActor: u ?? s ?? l ?? c ?? null,
      operatorActor: u ?? null,
      platformActor: c ?? null,
      combatStateCombatant: o
    };
  }
  static rememberActivation(e, n) {
    !(e != null && e.id) || !(n != null && n.id) || this._lastActivationByCombat.set(e.id, this.getActivationIdentity(e, n));
  }
  static _getCombatSceneId(e) {
    var n, i, a;
    return String(
      ((n = e == null ? void 0 : e.scene) == null ? void 0 : n.id) ?? (e == null ? void 0 : e.sceneId) ?? ((i = e == null ? void 0 : e._source) == null ? void 0 : i.scene) ?? ((a = e == null ? void 0 : e._source) == null ? void 0 : a.sceneId) ?? ""
    ).trim();
  }
  static _getCombatantSceneId(e) {
    var i, a, r, s, o;
    const n = this._asTokenDocument(e == null ? void 0 : e.token);
    return String(
      (e == null ? void 0 : e.sceneId) ?? ((i = e == null ? void 0 : e._source) == null ? void 0 : i.sceneId) ?? ((a = n == null ? void 0 : n.parent) == null ? void 0 : a.id) ?? ((r = n == null ? void 0 : n.scene) == null ? void 0 : r.id) ?? ((o = (s = n == null ? void 0 : n.object) == null ? void 0 : s.scene) == null ? void 0 : o.id) ?? ""
    ).trim();
  }
  static _findCombatantForToken(e, n = null, i = ((a) => (a = canvas == null ? void 0 : canvas.scene) == null ? void 0 : a.id)()) {
    var l, c, u;
    const r = this._asTokenDocument(n), s = String((r == null ? void 0 : r.id) ?? "").trim();
    if (!e || !s) return null;
    if (((c = (l = r == null ? void 0 : r.combatant) == null ? void 0 : l.combat) == null ? void 0 : c.id) === e.id) return r.combatant;
    let o = null;
    if (typeof e.getCombatantsByToken == "function")
      try {
        o = ((u = e.getCombatantsByToken(s)) == null ? void 0 : u[0]) ?? null;
      } catch {
        o = null;
      }
    else if (typeof e.getCombatantByToken == "function")
      try {
        o = e.getCombatantByToken(s) ?? null;
      } catch {
        o = null;
      }
    return o || (this._getCombatants(e).find((d) => {
      const f = this._getCombatantTokenDocument(d, i), m = this._getCombatantTokenId(d) || String((f == null ? void 0 : f.id) ?? "").trim(), g = this._getCombatantSceneId(d) || i;
      return m === s && (!i || !g || g === i);
    }) ?? null);
  }
  static _collectActorIds(e, n = null) {
    var s, o;
    const i = /* @__PURE__ */ new Set(), a = (l) => {
      const c = String(l ?? "").trim();
      c && i.add(c);
    };
    a(e == null ? void 0 : e.id), a(e == null ? void 0 : e._id);
    const r = this._asTokenDocument(n) ?? this._asTokenDocument(e == null ? void 0 : e.token);
    return a((s = r == null ? void 0 : r.actor) == null ? void 0 : s.id), a((o = r == null ? void 0 : r.baseActor) == null ? void 0 : o.id), a(r == null ? void 0 : r.actorId), i;
  }
  static _tokenDocumentMatchesActor(e, n, i = null) {
    var s, o;
    const a = this._asTokenDocument(e);
    if (!a || !n) return !1;
    const r = i ?? this._collectActorIds(n, a);
    return [
      (s = a == null ? void 0 : a.actor) == null ? void 0 : s.id,
      (o = a == null ? void 0 : a.baseActor) == null ? void 0 : o.id,
      a == null ? void 0 : a.actorId
    ].some((l) => r.has(String(l ?? "").trim()));
  }
  static getPreferredTokenDocument(e) {
    var a, r;
    if (!e) return null;
    const n = this._asTokenDocument(e == null ? void 0 : e.token);
    return n || (((r = (((a = e.getActiveTokens) == null ? void 0 : a.call(e, !0, !0)) ?? [])[0]) == null ? void 0 : r.document) ?? null);
  }
  static getPreferredToken(e) {
    const n = this.getPreferredTokenDocument(e);
    return n ? n.object ?? this._getSceneTokenById(n.id) : null;
  }
  static getCurrentSceneTokenDocument(e, n = null) {
    var g, p, h, b;
    const i = (g = canvas == null ? void 0 : canvas.scene) == null ? void 0 : g.id, a = this._asTokenDocument(n);
    if (this._getTokenSceneId(a) === i) return a;
    const r = String((a == null ? void 0 : a.id) ?? (n == null ? void 0 : n.id) ?? "").trim();
    if (r) {
      const y = this._getSceneTokenDocumentById(r, i);
      if (y) return y;
    }
    const s = this.getPreferredTokenDocument(e);
    if (this._getTokenSceneId(s) === i) return s;
    const o = String((s == null ? void 0 : s.id) ?? "").trim();
    if (o) {
      const y = this._getSceneTokenDocumentById(o, i);
      if (y) return y;
    }
    const c = ((h = (((p = e == null ? void 0 : e.getActiveTokens) == null ? void 0 : p.call(e, !0, !0)) ?? []).find((y) => {
      var S, A;
      return ((A = (S = y == null ? void 0 : y.document) == null ? void 0 : S.parent) == null ? void 0 : A.id) === i;
    })) == null ? void 0 : h.document) ?? null;
    if (c) return c;
    const u = Array.from(((b = canvas == null ? void 0 : canvas.scene) == null ? void 0 : b.tokens) ?? []), d = this._collectActorIds(e, s), f = u.filter((y) => this._tokenDocumentMatchesActor(y, e, d));
    return f.find((y) => {
      var S, A, T;
      return ((S = y == null ? void 0 : y.combatant) == null ? void 0 : S.id) === ((T = (A = game.combat) == null ? void 0 : A.combatant) == null ? void 0 : T.id);
    }) ?? null ?? f[0] ?? null;
  }
  static getCurrentSceneToken(e, n = null) {
    const i = this.getCurrentSceneTokenDocument(e, n);
    return i ? i.object ?? this._getSceneTokenById(i.id) : null;
  }
  static _getSceneTokenById(e) {
    var n, i, a, r;
    return e ? ((i = (n = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : n.get) == null ? void 0 : i.call(n, e)) ?? ((r = (a = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : a.placeables) == null ? void 0 : r.find((s) => s.id === e)) ?? null : null;
  }
  static _measureTokenDistance(e, n) {
    var o, l;
    const i = canvas == null ? void 0 : canvas.grid, a = fy(e), r = fy(n);
    if (!i || !a || !r) return null;
    if (typeof i.measurePath == "function")
      try {
        const c = i.measurePath([a, r], { gridSpaces: !0 }), u = Number(
          (c == null ? void 0 : c.distance) ?? (c == null ? void 0 : c.cost) ?? (c == null ? void 0 : c.totalDistance) ?? (c == null ? void 0 : c.totalCost) ?? NaN
        );
        if (Number.isFinite(u)) return u;
      } catch {
      }
    const s = ((l = (o = foundry == null ? void 0 : foundry.canvas) == null ? void 0 : o.geometry) == null ? void 0 : l.Ray) ?? globalThis.Ray;
    if (typeof i.measureDistances == "function" && typeof s == "function")
      try {
        const c = i.measureDistances([{ ray: new s(a, r) }], { gridSpaces: !0 }), u = Number(Array.isArray(c) ? c[0] : NaN);
        if (Number.isFinite(u)) return u;
      } catch {
        return null;
      }
    return null;
  }
  static getUserTargetTokens(e = game.user) {
    var a;
    const i = (Array.isArray((a = e == null ? void 0 : e.targets) == null ? void 0 : a.ids) ? e.targets.ids : []).map((r) => this._getSceneTokenById(r)).filter(Boolean);
    return i.length ? i : Array.from((e == null ? void 0 : e.targets) ?? []).map((r) => (r == null ? void 0 : r.object) ?? r).filter(Boolean);
  }
  static getTargetingSnapshot(e = null, n = game.user) {
    var u, d, f, m, g, p, h, b;
    const i = this.getUserTargetTokens(n), a = i.length;
    if (a === 0)
      return {
        count: 0,
        none: !0,
        single: !1,
        multiple: !1,
        heading: "Target",
        primaryLabel: "No target selected",
        detailRows: [],
        target: null
      };
    if (a > 1)
      return {
        count: a,
        none: !1,
        single: !1,
        multiple: !0,
        heading: "Targets",
        primaryLabel: `${a} selected`,
        detailRows: [],
        target: null
      };
    const r = i[0], s = this._measureTokenDistance(e, r), o = String(((d = (u = canvas == null ? void 0 : canvas.scene) == null ? void 0 : u.grid) == null ? void 0 : d.units) ?? ((m = (f = game.system) == null ? void 0 : f.grid) == null ? void 0 : m.units) ?? "").trim(), l = v$(s, o), c = String((r == null ? void 0 : r.name) ?? ((g = r == null ? void 0 : r.actor) == null ? void 0 : g.name) ?? "Target").trim() || "Target";
    return {
      count: a,
      none: !1,
      single: !0,
      multiple: !1,
      heading: "Target",
      primaryLabel: c,
      detailRows: l ? [{ label: "Distance", value: l }] : [],
      target: {
        id: (r == null ? void 0 : r.id) ?? null,
        name: c,
        img: ((h = (p = r == null ? void 0 : r.document) == null ? void 0 : p.texture) == null ? void 0 : h.src) ?? ((b = r == null ? void 0 : r.texture) == null ? void 0 : b.src) ?? "",
        distance: Number.isFinite(s) ? s : null,
        distanceLabel: l
      }
    };
  }
  static getRollImpact(e = []) {
    const n = (Array.isArray(e) ? e : []).map((a) => {
      const r = M$((a == null ? void 0 : a.numericValue) ?? (a == null ? void 0 : a.value) ?? 0);
      return {
        label: String((a == null ? void 0 : a.label) ?? "").trim() || "Modifier",
        numericValue: r,
        value: String((a == null ? void 0 : a.value) ?? is(r)).trim() || is(r)
      };
    }), i = n.reduce((a, r) => a + r.numericValue, 0);
    return {
      total: i,
      totalLabel: is(i),
      entries: n
    };
  }
  static getCombat(e, n = null) {
    var h;
    const i = (h = canvas == null ? void 0 : canvas.scene) == null ? void 0 : h.id, a = game.combat, r = this._getCombatSceneId(a), o = !!this._asTokenDocument(n), l = this.getCurrentSceneTokenDocument(e, n), c = (l == null ? void 0 : l.object) ?? this._getSceneTokenById((l == null ? void 0 : l.id) ?? null);
    if (!a || r && i && r !== i)
      return {
        combat: null,
        combatant: null,
        token: c,
        tokenDocument: l
      };
    let d = (Nt(e) ? null : this._findMachineCombatantForPilot(e, a)) ?? this._findCombatantForToken(a, l, i);
    if (!o) {
      const b = (a == null ? void 0 : a.combatant) ?? null, y = this._getCombatantActor(b, i);
      cm(y, e) && (d = b);
    }
    const f = this._getCombatants(a);
    if (!d) {
      const b = this._collectActorIds(e, l), y = String((l == null ? void 0 : l.id) ?? "").trim(), S = f.filter((C) => {
        const k = this._getCombatantTokenId(C), w = this._getCombatantTokenDocument(C, i), v = k || String((w == null ? void 0 : w.id) ?? "").trim();
        if (o && y) return v === y;
        if (b.has(this._getCombatantActorId(C))) return !0;
        const N = (w == null ? void 0 : w.actor) ?? (C == null ? void 0 : C.actor) ?? null;
        return this._tokenDocumentMatchesActor(w, e, b) || !o && cm(N, e);
      }), A = S.find((C) => {
        var k;
        return C.id === ((k = a == null ? void 0 : a.combatant) == null ? void 0 : k.id);
      }) ?? null;
      d = S.find(
        (C) => {
          var k;
          return y && (this._getCombatantTokenId(C) || String(((k = this._getCombatantTokenDocument(C, i)) == null ? void 0 : k.id) ?? "").trim()) === y;
        }
      ) ?? null ?? A ?? S[0] ?? null;
    }
    const m = this._getCombatantTokenDocument(d, i), g = l ?? m ?? null, p = c ?? (m == null ? void 0 : m.object) ?? this._getSceneTokenById(this._getCombatantTokenId(d)) ?? null;
    return {
      combat: a,
      combatant: d,
      token: p,
      tokenDocument: g
    };
  }
  static getSnapshot(e, { token: n = null } = {}) {
    var P, L, _, O, x, H, G, q, ie, Z, Se;
    const {
      combat: i,
      combatant: a,
      token: r,
      tokenDocument: s
    } = this.getCombat(e, n), o = this.resolveActivationUnit({ actor: e, combat: i, token: s ?? r ?? n, combatant: a }), l = (o == null ? void 0 : o.actingActor) ?? e, c = (o == null ? void 0 : o.platformActor) ?? null, u = !!a && ((P = i == null ? void 0 : i.combatant) == null ? void 0 : P.id) === a.id, d = a ? this.getActivationIdentity(i, a) : null;
    u && (i != null && i.id) && !this._lastActivationByCombat.has(i.id) && this.rememberActivation(i, a);
    const f = a ? a.getFlag(St, Gt) : null, m = gs(l, { combat: i, combatant: a, sceneId: ((L = i == null ? void 0 : i.scene) == null ? void 0 : L.id) ?? ((_ = canvas == null ? void 0 : canvas.scene) == null ? void 0 : _.id) ?? "" }), g = a ? u ? uc(f, d) ? Pf(f, d, m) : uy(f, d, l, { combat: i, combatant: a }) : Pf(f, d, m) : th(d, m);
    g.actionLog = br(g.actionLog);
    const p = Math.max(0, Number(((x = (O = l == null ? void 0 : l.system) == null ? void 0 : O.burn) == null ? void 0 : x.value) ?? ((G = (H = e == null ? void 0 : e.system) == null ? void 0 : H.burn) == null ? void 0 : G.value) ?? 0)), h = Math.floor(p / 2), b = !!(((ie = (q = l == null ? void 0 : l.system) == null ? void 0 : q.burn) == null ? void 0 : ie.overloaded) ?? ((Se = (Z = e == null ? void 0 : e.system) == null ? void 0 : Z.burn) == null ? void 0 : Se.overloaded)), y = po(g), S = this.getActiveStatuses(l), A = S.filter(
      (se) => !(b && se.id === "overloaded") && se.id !== hr
    ), T = this.getModifierSummary(l, h), C = this.getRollImpact(T), k = Math.max(0, Number(g.burnThisActivation ?? 0)), w = a ? u ? "" : "Waiting for this combatant's activation." : "No combatant on the current scene.", v = [];
    b && v.push({ id: "overloaded", label: "Overloaded" }), y && v.push({
      id: "preparedInterrupt",
      label: "Prepared",
      hint: T$(y)
    });
    const N = Object.entries(g.hazards ?? {});
    if (N.length) {
      const se = N.map(([, te]) => te).sort((te, pe) => xa(pe == null ? void 0 : pe.tier) - xa(te == null ? void 0 : te.tier))[0] ?? null;
      se && v.push({
        id: "hazard",
        label: `Hazard ${ci(se.tier)}`,
        hint: `${N.length} active hazard${N.length === 1 ? "" : "s"}`
      });
    }
    return {
      token: r,
      tokenDocument: s,
      combat: i,
      combatant: a,
      actionEconomyActor: l,
      actingActor: l,
      operatorActor: (o == null ? void 0 : o.operatorActor) ?? null,
      platformActor: c,
      activationUnit: o,
      hasCombatant: !!a,
      isCurrentTurn: u,
      overloaded: b,
      burn: {
        value: p,
        penalty: h,
        threshold: l.overloadThreshold,
        canOverloadCheck: p >= l.overloadThreshold && !b
      },
      state: g,
      hazards: g.hazards ?? {},
      pendingReaction: g.pendingReaction ?? null,
      preparedInterrupt: y,
      targeting: this.getTargetingSnapshot(r),
      states: v,
      effects: A,
      statuses: S,
      rollImpact: C,
      summaryText: `SA: ${g.saRemaining} / ${aa}   FA: ${g.faRemaining}   RA: ${g.raRemaining}`,
      activation: {
        burnThisActivation: k,
        burnThisActivationLabel: `+${k}`,
        items: [
          { label: "SA", value: `${g.saRemaining}/${aa}` },
          { label: "FA", value: String(g.faRemaining) },
          { label: "RA", value: String(g.raRemaining) },
          { label: "Burn", value: `+${k}`, detail: "this activation" }
        ]
      },
      inactiveReason: w,
      modifierSummary: T
    };
  }
  static getAvailableReactionEdgePools(e) {
    var n, i;
    return (n = e == null ? void 0 : e.hasEdgePools) != null && n.call(e) ? (((i = e.getEdgePoolSummary) == null ? void 0 : i.call(e).pools) ?? []).filter((a) => Number((a == null ? void 0 : a.effectiveValue) ?? 0) > 0).map((a) => ({
      key: String(a.key ?? "").trim(),
      label: String(a.key ?? "").trim(),
      value: Number(a.effectiveValue ?? 0)
    })).filter((a) => a.key) : [];
  }
  static getReactionSpendPreview(e, { token: n = null, edgePoolKey: i = "" } = {}) {
    var d;
    const a = this.getSnapshot(e, { token: n }), r = Hl(e), s = Number(((d = a.state) == null ? void 0 : d.raRemaining) ?? 0) > 0, o = this.getAvailableReactionEdgePools(e), l = String(i ?? "").trim(), c = !s && o.some((f) => f.key === l);
    return {
      snapshot: a,
      usesReaction: s,
      burnDelta: s || c ? 0 : 2,
      canSpendEdge: !s && o.length > 0,
      edgePools: o,
      edgePoolKey: c ? l : null,
      disabled: r.cannotReact,
      reason: r.cannotReact ? `Disabled (${r.reactionReason})` : "",
      costLabel: s ? "1 RA" : c ? `1 Edge (${l})` : "+2 Burn"
    };
  }
  static async commitReactionSpend(e, {
    token: n = null,
    actionId: i = "",
    actionLabel: a = "",
    actionCategory: r = He.reaction,
    logLabel: s = "",
    edgePoolKey: o = "",
    allowCurrentTurn: l = !1
  } = {}) {
    var p, h, b, y;
    const c = this.getReactionSpendPreview(e, { token: n, edgePoolKey: o }), u = c.snapshot;
    if (!u.hasCombatant) return { ok: !1, reason: "No combatant on the current scene." };
    if (c.disabled) return { ok: !1, reason: c.reason || "Reactions are disabled." };
    if (!l && u.isCurrentTurn) return { ok: !1, reason: "Only outside your activation." };
    const d = go(u), f = {
      combat: u.combat,
      combatant: u.combatant,
      state: d,
      sceneId: ((p = canvas == null ? void 0 : canvas.scene) == null ? void 0 : p.id) ?? "",
      snapshot: { ...u, state: d }
    };
    let m = 0, g = null;
    if (c.usesReaction)
      d.raRemaining = Math.max(0, Number(d.raRemaining ?? 0) - 1);
    else {
      const S = c.edgePoolKey ? 0 : 2, A = zt({
        actor: e,
        phase: "onBeforeBurnApplied",
        facts: Ic({
          actor: e,
          packet: {
            actionId: i,
            category: r,
            resource: "reaction",
            amount: S,
            source: "reaction"
          },
          runtime: f
        }),
        packet: {
          actionId: i,
          category: r,
          resource: "reaction",
          amount: S,
          source: "reaction"
        },
        options: { runtime: f, consumeUsage: !0 }
      });
      f.pendingMutations = (f.pendingMutations ?? []).concat(A.mutations), m = Math.max(0, Number(A.packet.amount ?? S) || 0), c.edgePoolKey ? (await e.spendEdge(c.edgePoolKey, 1, { source: "reactionBurnCancel" }), g = c.edgePoolKey) : m > 0 && (d.reactionBurnSinceLastActivation = Math.max(
        0,
        Number(d.reactionBurnSinceLastActivation ?? 0) + m
      ));
    }
    return this._appendActionLog(d, {
      id: i,
      label: s || a,
      costLabel: c.costLabel,
      resource: c.usesReaction ? "ra" : c.edgePoolKey ? "edge" : "burn",
      cost: c.usesReaction || c.edgePoolKey ? 1 : m,
      reactionBurnDelta: m,
      actorBurnDelta: m,
      actorEdgeDelta: c.edgePoolKey ? -1 : 0,
      edgePoolKey: c.edgePoolKey ?? ""
    }), (h = f.pendingMutations) != null && h.length ? await ga({ actor: e, mutations: f.pendingMutations, runtime: f }) : await u.combatant.setFlag(St, Gt, d), m > 0 && await e.update({ "system.burn.value": Math.max(0, Number(((y = (b = e.system) == null ? void 0 : b.burn) == null ? void 0 : y.value) ?? 0) + m) }), {
      ok: !0,
      snapshot: this.getSnapshot(e, { token: n }),
      costLabel: c.costLabel,
      burnDelta: m,
      spentEdgePoolKey: g,
      usedReaction: c.usesReaction
    };
  }
  static async updateCombatantState(e, { token: n = null, mutate: i = null } = {}) {
    const a = this.getSnapshot(e, { token: n });
    if (!(a != null && a.combatant)) return { ok: !1, reason: "No combatant on the current scene." };
    const r = go(a), s = typeof i == "function" ? i(r, a) ?? r : r;
    return await a.combatant.setFlag(St, Gt, s), { ok: !0, snapshot: this.getSnapshot(e, { token: n }) };
  }
  // Move actions feed both the "Aim after moving" gate and the personal attack
  // DN (attacker/target motion). Each Move action logs one entry, so the log
  // count is the authoritative tally for this activation.
  static getMoveActionCountFromState(e = {}) {
    var a, r;
    const i = (Array.isArray(e == null ? void 0 : e.actionLog) ? e.actionLog : []).filter((s) => String((s == null ? void 0 : s.id) ?? "").trim() === "move").length;
    return i > 0 ? i : (r = (a = e == null ? void 0 : e.actionState) == null ? void 0 : a.move) != null && r.moved ? 1 : 0;
  }
  // Reading a combatant's stored flag lets us tally a target's movement without
  // running their full activation snapshot. We only count moves from the current
  // round so stale data from a prior activation does not affect the attack DN.
  static getMoveActionCountForCombatant(e, { combat: n = game == null ? void 0 : game.combat } = {}) {
    var o, l, c, u;
    if (!e) return 0;
    const i = ((o = e.getFlag) == null ? void 0 : o.call(e, St, Gt)) ?? ((c = (l = e.flags) == null ? void 0 : l[St]) == null ? void 0 : c[Gt]) ?? null;
    if (!i) return 0;
    const a = ((u = i == null ? void 0 : i.actionState) == null ? void 0 : u.move) ?? null, r = Number((n == null ? void 0 : n.round) ?? 0), s = Number((a == null ? void 0 : a.round) ?? 0);
    return a && r > 0 && s > 0 && r !== s ? 0 : this.getMoveActionCountFromState(i);
  }
  static getUsedWeaponGroupIds(e, { token: n = null, snapshot: i = null, currentTurnOnly: a = !0 } = {}) {
    const r = i ?? this.getSnapshot(e, { token: n });
    return r != null && r.hasCombatant ? a && !r.isCurrentTurn ? [] : Iw(r) : [];
  }
  static async markWeaponGroupUsed(e, { token: n = null, groupId: i = "" } = {}) {
    const a = String(i ?? "").trim();
    return a ? this.updateCombatantState(e, {
      token: n,
      mutate: (r) => iy(r, a)
    }) : { ok: !1, reason: "Weapon group id is required." };
  }
  static async markWeaponGroupsUsed(e, { token: n = null, groupIds: i = [] } = {}) {
    const a = Array.from(i ?? []).map((r) => String(r ?? "").trim()).filter(Boolean);
    return a.length ? this.updateCombatantState(e, {
      token: n,
      mutate: (r) => {
        for (const s of a)
          iy(r, s);
        return r;
      }
    }) : { ok: !0, skipped: !0 };
  }
  static async setPendingReaction(e, { token: n = null, pendingReaction: i = null } = {}) {
    return this.updateCombatantState(e, {
      token: n,
      mutate: (a) => (a.pendingReaction = Rc(i), a)
    });
  }
  static async clearPendingReaction(e, { token: n = null } = {}) {
    return this.setPendingReaction(e, { token: n, pendingReaction: null });
  }
  static async setHazardState(e, { token: n = null, regionId: i = "", hazardState: a = null } = {}) {
    const r = String(i ?? "").trim();
    return r ? this.updateCombatantState(e, {
      token: n,
      mutate: (s) => (s.hazards ?? (s.hazards = {}), a ? s.hazards[r] = Su({ [r]: a })[r] : delete s.hazards[r], s)
    }) : { ok: !1, reason: "Hazard region id is required." };
  }
  static getModifierSummary(e, n = Math.floor(Number(((a) => (a = ((i) => (i = e == null ? void 0 : e.system) == null ? void 0 : i.burn)()) == null ? void 0 : a.value)() ?? 0) / 2)) {
    var c, u;
    const r = ((u = (c = e == null ? void 0 : e.system) == null ? void 0 : c.derived) == null ? void 0 : u.condition) ?? {}, s = [];
    n > 0 && s.push({
      label: "Burn Penalty",
      numericValue: -n,
      value: is(-n)
    });
    const o = Number(r.fatiguePenalty ?? 0);
    o && s.push({
      label: "Fatigue",
      numericValue: o,
      value: is(o)
    });
    const l = Number(r.physicalPenalty ?? 0);
    return l && s.push({
      label: "Physical",
      numericValue: l,
      value: is(l)
    }), s.length || s.push({
      label: "Current Modifiers",
      numericValue: 0,
      value: "0"
    }), s;
  }
  static getActiveStatuses(e) {
    return Array.from((e == null ? void 0 : e.statuses) ?? []).map((i) => ({
      id: i,
      label: w$(i)
    })).sort((i, a) => i.label.localeCompare(a.label));
  }
  static buildActionModel(e, n) {
    var o, l, c, u;
    const i = (d) => {
      const f = cp(d), m = Sk(d);
      return !m || !f ? null : {
        id: d,
        label: f.label,
        handler: "roll",
        roll: JSON.stringify(m),
        disabled: !1,
        reason: ""
      };
    }, a = (d) => {
      const f = bP(d).filter((m) => m.id !== "overloadCheck").filter((m) => !m.hideFromMenus).filter((m) => !(d === He.recovery && m.id === "reduceBurn"));
      if (d === He.standard) {
        const m = vs("reduceBurn");
        m && !f.some((g) => g.id === "reduceBurn") && f.push(m);
      }
      return f.map((m) => this._buildCatalogAction(e, n, m));
    }, r = (d) => {
      const f = vs(d);
      if (!f) return null;
      const m = this._buildCatalogAction(e, n, f);
      return m.disabled ? null : m;
    }, s = (o = n.burn) != null && o.canOverloadCheck ? r("overloadCheck") : null;
    return {
      utilityButtons: [
        {
          id: "initiative",
          label: "Initiative",
          handler: "roll",
          roll: JSON.stringify({ intent: "initiative" }),
          disabled: !1,
          reason: ""
        },
        {
          id: "statuses",
          label: "Statuses",
          handler: "toggleStatuses",
          disabled: !1,
          reason: n.token ? "" : "Requires a token on the current scene."
        },
        i("composure"),
        i("judgeIntent"),
        i("memory"),
        i("lift"),
        i("endure"),
        i("steady")
      ].filter(Boolean),
      summaryPills: [
        { label: "SA", value: `${n.state.saRemaining}/${aa}` },
        { label: "Cap", value: `${Math.max(0, Number(((l = n.state) == null ? void 0 : l.saSpentThisActivation) ?? 0))}/${Oc(e, { snapshot: n })}` },
        { label: "FA", value: `${n.state.faRemaining}` },
        { label: "RA", value: `${n.state.raRemaining}` },
        {
          label: "Burn/Turn",
          value: `+${Math.max(0, Number(((c = n.state) == null ? void 0 : c.burnThisActivation) ?? 0))}`,
          action: s
        }
      ],
      activationLog: br((u = n.state) == null ? void 0 : u.actionLog).map((d, f) => ({
        ...d,
        index: f + 1,
        logIndex: f,
        canRemove: !!(n.hasCombatant && n.isCurrentTurn)
      })),
      menus: [
        {
          id: "standard",
          label: "Standard Actions",
          actions: a(He.standard)
        },
        {
          id: "complex",
          label: "Complex Actions",
          actions: a(He.complex)
        },
        {
          id: "free",
          label: "Free Actions",
          actions: a(He.free)
        },
        {
          id: "reaction",
          label: "Reactions",
          actions: a(He.reaction)
        },
        {
          id: "burn",
          label: "Burn & Recovery",
          actions: a(He.recovery)
        }
      ].filter((d) => d.actions.length)
    };
  }
  static _buildCatalogAction(e, n, i) {
    const a = n.hasCombatant ? "" : "No current-scene combatant.", r = n.isCurrentTurn ? "" : "Only during your activation.", s = n.overloaded ? "Overloaded actors can only recover Burn." : "", o = dc(e, n), l = n.state ?? {}, c = i.category;
    let u = A$(i, "sa"), d = sm(i, 0), f = S$(u, d), m = "";
    if (i.id === "reduceBurn")
      u = "sa", d = 1, f = "1 SA", m = a || r || (o <= 0 ? "Activation SA cap reached." : "") || (n.burn.value <= 0 ? "Burn is already at 0." : "");
    else if (i.id === "overloadCheck")
      u = "check", d = 0, f = "Check", m = a || r || (n.burn.canOverloadCheck ? "" : n.overloaded ? "Already Overloaded." : "Burn below 6.");
    else if (i.id === "interrupt") {
      const g = po(l);
      u = Number(l.raRemaining ?? 0) > 0 ? "ra" : "burn", d = u === "ra" ? 1 : 2, f = u === "ra" ? "1 RA" : "+2 Burn", m = a || (n.isCurrentTurn ? "Only outside your activation." : "") || (g ? "" : "Prepare an interrupt first.");
    } else if (i.id === "evade") {
      const g = Rc(l.pendingReaction), p = n.isCurrentTurn && !(g != null && g.allowCurrentTurn) ? "Only outside your activation." : "";
      u = Number(l.raRemaining ?? 0) > 0 ? "ra" : "burn", d = u === "ra" ? 1 : 2, f = u === "ra" ? "1 RA" : "+2 Burn", m = a || p || (g ? "" : "Use an area effect or hazard card to trigger Evade.");
    } else if (c === He.standard)
      m = a || r || s || (o < d ? "Activation SA cap reached." : "");
    else if (c === He.complex)
      m = a || r || s || (o < d ? "Activation SA cap reached." : "");
    else if (c === He.free) {
      const g = Number(l.faRemaining ?? 0) > 0;
      u = g ? "fa" : "sa", d = 1, f = g ? "Free" : "1 SA", m = a || r || !g && s || (!g && o < 1 ? "Activation SA cap reached." : "");
    } else if (c === He.reaction) {
      const g = Number(l.raRemaining ?? 0) > 0;
      u = g ? "ra" : "burn", d = g ? 1 : 2, f = g ? "1 RA" : "+2 Burn", m = a || (n.isCurrentTurn ? "Only outside your activation." : "");
    } else c === He.recovery && (m = a || r);
    return m = dy(i) || m, m = m || cc(e, i), {
      id: i.id,
      label: i.label,
      category: c,
      handler: i.handler || "combatIntent",
      description: String(i.description ?? "").trim(),
      resource: u,
      cost: d,
      costLabel: f,
      disabled: !!m,
      reason: m,
      roll: i.roll ? JSON.stringify(i.roll) : "",
      actionPayload: JSON.stringify({ intent: "combatAction", actionId: i.id }),
      prominent: !!(i.prominent || i.prominentWhenBurning && n.burn.value >= n.burn.threshold)
    };
  }
  static async executeAction(e, { token: n = null, actionId: i = "", metadata: a = {} } = {}) {
    const r = vs(i);
    if (!r) return { ok: !1, reason: "Unknown combat action." };
    const s = dy(r);
    if (s) return { ok: !1, reason: s };
    const o = cc(e, r);
    return o ? { ok: !1, reason: o } : r.category === He.standard ? this._executeStandardAction(e, { token: n, action: r, metadata: a }) : r.category === He.free ? this._executeFreeAction(e, { token: n, action: r, metadata: a }) : r.category === He.reaction ? this._executeReactionAction(e, { token: n, action: r, metadata: a }) : { ok: !1, reason: r.reason || "That action is not implemented yet." };
  }
  static async _executeStandardAction(e, { token: n = null, action: i, metadata: a = {} } = {}) {
    const r = this.getSnapshot(e, { token: n }), s = cc(e, i);
    if (s) return { ok: !1, reason: s };
    if (!r.hasCombatant) return { ok: !1, reason: "No combatant on the current scene." };
    if (!r.isCurrentTurn) return { ok: !1, reason: "Only available during your activation." };
    if (r.overloaded) return { ok: !1, reason: "Overloaded actors can only recover Burn." };
    if (i.id === "aim" && this.getMoveActionCountFromState(r.state) > 0)
      return { ok: !1, reason: "Cannot aim after taking a move action this activation." };
    if (dc(e, r) < sm(i, 1))
      return { ok: !1, reason: "Activation SA cap reached." };
    const o = sm(i, 1) || 1, l = await this.spendResource(e, {
      token: n,
      resource: "sa",
      cost: o,
      actionId: i.id,
      actionLabel: i.label,
      actionCostLabel: `${o} SA`,
      actionCategory: i.category
    });
    return l != null && l.ok ? (await this._applyActionState(e, {
      token: n,
      actionId: i.id,
      metadata: a,
      snapshot: l.snapshot
    }), { ok: !0, snapshot: this.getSnapshot(e, { token: n }) }) : l;
  }
  static async _executeFreeAction(e, { token: n = null, action: i, metadata: a = {} } = {}) {
    var c;
    const r = this.getSnapshot(e, { token: n }), s = cc(e, i);
    if (s) return { ok: !1, reason: s };
    if (!r.hasCombatant) return { ok: !1, reason: "No combatant on the current scene." };
    if (!r.isCurrentTurn) return { ok: !1, reason: "Only available during your activation." };
    const o = Number(((c = r.state) == null ? void 0 : c.faRemaining) ?? 0) > 0;
    if (!o && r.overloaded)
      return { ok: !1, reason: "Overloaded actors can only recover Burn." };
    if (!o && dc(e, r) < 1)
      return { ok: !1, reason: "Activation SA cap reached." };
    const l = await this.spendResource(e, {
      token: n,
      resource: o ? "fa" : "sa",
      cost: 1,
      actionId: i.id,
      actionLabel: i.label,
      actionCostLabel: o ? "Free" : "1 SA",
      actionCategory: i.category
    });
    return l != null && l.ok ? (await this._applyActionState(e, {
      token: n,
      actionId: i.id,
      metadata: a,
      snapshot: l.snapshot
    }), { ok: !0, snapshot: this.getSnapshot(e, { token: n }) }) : l;
  }
  static async _promptSpendEdgeForReaction(e) {
    var o, l, c;
    if (!((o = e.hasEdgePools) != null && o.call(e))) return null;
    const i = Object.keys(((c = (l = e.system) == null ? void 0 : l.counters) == null ? void 0 : c.edgePools) ?? {}).map((u) => e.getEdgePool(u)).filter((u) => u.hasPools && u.effectiveValue > 0);
    if (!i.length) return null;
    const a = (u) => String(u).charAt(0).toUpperCase() + String(u).slice(1);
    let r;
    i.length === 1 ? r = `<input type="hidden" name="poolKey" value="${i[0].key}">
        <p>from <strong>${a(i[0].key)}</strong> (${i[0].effectiveValue} available)</p>` : r = i.map((u, d) => `
        <label style="display:block">
          <input type="radio" name="poolKey" value="${u.key}" ${d === 0 ? "checked" : ""}>
          ${a(u.key)} &mdash; ${u.effectiveValue} available
        </label>
      `).join("");
    const s = `<p>This reaction costs <strong>+2 Burn</strong>. Spend 1 Edge to ignore it?</p><form>${r}</form>`;
    return Dialog.confirm({
      title: "Reaction: Spend Edge?",
      content: s,
      yes: (u) => {
        const d = u.find("[name='poolKey']:checked, [name='poolKey'][type='hidden']").first().val();
        return String(d ?? i[0].key).trim() || i[0].key;
      },
      no: () => null,
      defaultYes: !1
    });
  }
  static async _executeReactionAction(e, { token: n = null, action: i, metadata: a = {} } = {}) {
    var d, f;
    const r = this.getSnapshot(e, { token: n });
    if (!r.hasCombatant) return { ok: !1, reason: "No combatant on the current scene." };
    const s = Rc((d = r.state) == null ? void 0 : d.pendingReaction), o = i.id === "evade" && (s == null ? void 0 : s.allowCurrentTurn);
    if (r.isCurrentTurn && !o) return { ok: !1, reason: "Only outside your activation." };
    if (i.id === "interrupt" && !po(r.state))
      return { ok: !1, reason: "Prepare an interrupt first." };
    const l = i.id === "assist" && (a != null && a.targetName) ? `${i.label}: ${a.targetName}` : i.id === "interrupt" && (a != null && a.scope) ? `${i.label}: ${String(a.scope).trim()}` : i.label;
    let c = String((a == null ? void 0 : a.edgePoolKey) ?? "").trim();
    !c && Number(((f = r.state) == null ? void 0 : f.raRemaining) ?? 0) <= 0 && (c = await ys._promptSpendEdgeForReaction(e) ?? "");
    const u = await this.commitReactionSpend(e, {
      token: n,
      actionId: i.id,
      actionLabel: i.label,
      actionCategory: i.category,
      logLabel: l,
      edgePoolKey: c,
      allowCurrentTurn: o
    });
    return u != null && u.ok ? { ...u, actionLabel: l } : u;
  }
  static async _applyActionState(e, { token: n = null, actionId: i = "", metadata: a = {}, snapshot: r = null } = {}) {
    const s = r ?? this.getSnapshot(e, { token: n });
    if (!(s != null && s.combatant)) return { ok: !1, reason: "No combatant on the current scene." };
    const o = k$(s.state, i, {
      snapshot: s,
      metadata: a
    });
    return await s.combatant.setFlag(St, Gt, o), { ok: !0, snapshot: this.getSnapshot(e, { token: n }) };
  }
  static async clearAim(e, { token: n = null } = {}) {
    const i = this.getSnapshot(e, { token: n });
    if (!(i != null && i.combatant)) return { ok: !1, reason: "No combatant on the current scene." };
    const a = go(i);
    return a.actionState ?? (a.actionState = {}), a.actionState.aim ? (a.actionState.aim = null, await i.combatant.setFlag(St, Gt, a), { ok: !0, snapshot: this.getSnapshot(e, { token: n }) }) : { ok: !0, snapshot: i };
  }
  static getPreparedInterrupt(e = null) {
    const n = (e == null ? void 0 : e.state) ?? e ?? {};
    return po(n);
  }
  static async clearPreparedInterrupt(e, { token: n = null } = {}) {
    const i = this.getSnapshot(e, { token: n });
    if (!(i != null && i.combatant)) return { ok: !1, reason: "No combatant on the current scene." };
    const a = go(i);
    return a.actionState ?? (a.actionState = {}), a.actionState.preparedInterrupt ? (a.actionState.preparedInterrupt = null, await i.combatant.setFlag(St, Gt, a), { ok: !0, snapshot: this.getSnapshot(e, { token: n }) }) : { ok: !0, snapshot: i };
  }
  static async _syncPreparedIndicatorForCombatant(e) {
    var u, d, f;
    if (!game.user.isGM || !e) return;
    const n = this._getCombatantSceneId(e) || ((u = canvas == null ? void 0 : canvas.scene) == null ? void 0 : u.id), i = this._getCombatantTokenDocument(e, n), a = (i == null ? void 0 : i.actor) ?? (e == null ? void 0 : e.actor) ?? null;
    if (!i || !a) return;
    const r = e.getFlag(St, Gt), s = !!po(r), o = my(), l = String((o == null ? void 0 : o.id) ?? hr).trim() || hr;
    (((f = (d = a == null ? void 0 : a.statuses) == null ? void 0 : d.has) == null ? void 0 : f.call(d, l)) ?? !1) !== s && await a.toggleStatusEffect(l, { active: s, overlay: !1 });
  }
  static async syncPreparedIndicators(e = game.combat) {
    if (!(!game.user.isGM || !e))
      for (const n of this._getCombatants(e))
        await this._syncPreparedIndicatorForCombatant(n);
  }
  static async clearPreparedIndicatorForCombatant(e) {
    var o, l, c;
    if (!game.user.isGM || !e) return;
    const n = this._getCombatantSceneId(e) || ((o = canvas == null ? void 0 : canvas.scene) == null ? void 0 : o.id), i = this._getCombatantTokenDocument(e, n), a = (i == null ? void 0 : i.actor) ?? (e == null ? void 0 : e.actor) ?? null;
    if (!i || !a) return;
    const r = my(), s = String((r == null ? void 0 : r.id) ?? hr).trim() || hr;
    (((c = (l = a == null ? void 0 : a.statuses) == null ? void 0 : l.has) == null ? void 0 : c.call(l, s)) ?? !1) && await a.toggleStatusEffect(s, { active: !1, overlay: !1 });
  }
  static _buildSpendAction(e, n, i = "") {
    var l;
    const a = Number(((l = e.state) == null ? void 0 : l[`${n.resource}Remaining`]) ?? 0), r = n.resource === "sa" ? "" : a < n.cost ? `No ${String(n.resource).toUpperCase()} remaining.` : "", s = i || r, o = this._formatCostLabel(n.resource, n.cost);
    return {
      id: n.id,
      label: n.label,
      costLabel: o,
      handler: "combatSpend",
      resource: n.resource,
      cost: n.cost,
      disabled: !!s,
      reason: s,
      prominent: !1
    };
  }
  static _buildStubAction(e) {
    return {
      ...e,
      handler: "",
      disabled: !0,
      reason: "Not yet implemented."
    };
  }
  static _formatCostLabel(e, n) {
    return `${n} ${String(e).toUpperCase()}`;
  }
  static _appendActionLog(e, {
    id: n = "",
    label: i = "",
    costLabel: a = "",
    resource: r = "",
    cost: s = 0,
    saSpentDelta: o = 0,
    attackDelta: l = 0,
    burnThisActivationDelta: c = 0,
    reactionBurnDelta: u = 0,
    actorBurnDelta: d = 0,
    actorEdgeDelta: f = 0,
    edgePoolKey: m = ""
  } = {}) {
    const g = String(i ?? "").trim();
    if (!g) return;
    const p = br(e == null ? void 0 : e.actionLog), h = {
      id: String(n ?? "").trim(),
      label: g,
      costLabel: String(a ?? "").trim()
    }, b = nh(r);
    b && (h.resource = b);
    for (const [S, A] of Object.entries({
      cost: s,
      saSpentDelta: o,
      attackDelta: l,
      burnThisActivationDelta: c,
      reactionBurnDelta: u,
      actorBurnDelta: d,
      actorEdgeDelta: f
    })) {
      const T = Number(A);
      Number.isFinite(T) && T !== 0 && (h[S] = T);
    }
    const y = String(m ?? "").trim();
    y && (h.edgePoolKey = y), p.push(h), e.actionLog = p;
  }
  static async removeActivationLogEntry(e, { token: n = null, index: i = null } = {}) {
    var u, d, f;
    const a = this.getSnapshot(e, { token: n });
    if (!a.hasCombatant) return { ok: !1, reason: "No combatant on the current scene." };
    if (!a.isCurrentTurn) return { ok: !1, reason: "Only available during your activation." };
    const r = br((u = a.state) == null ? void 0 : u.actionLog), s = Number(i);
    if (!Number.isInteger(s) || s < 0 || s >= r.length)
      return { ok: !1, reason: "That action is no longer in this activation log." };
    const [o] = r.splice(s, 1), l = b$(o), c = go(a);
    return c.actionLog = r, l.resource === "sa" ? (c.saSpentThisActivation = Math.max(
      0,
      Number(c.saSpentThisActivation ?? 0) - l.saSpentDelta
    ), c.saRemaining = Math.max(0, aa - Number(c.saSpentThisActivation ?? 0))) : l.resource === "fa" ? c.faRemaining = Math.min(
      Ns(e, "fa", { snapshot: a }),
      Math.max(0, Number(c.faRemaining ?? 0) + l.cost)
    ) : l.resource === "ra" && (c.raRemaining = Math.min(
      Ns(e, "ra", { snapshot: a }),
      Math.max(0, Number(c.raRemaining ?? 0) + l.cost)
    )), c.attacksThisActivation = Math.max(
      0,
      Number(c.attacksThisActivation ?? 0) - l.attackDelta
    ), c.burnThisActivation = Math.max(
      0,
      Number(c.burnThisActivation ?? 0) - l.burnThisActivationDelta
    ), c.reactionBurnSinceLastActivation = Math.max(
      0,
      Number(c.reactionBurnSinceLastActivation ?? 0) - l.reactionBurnDelta
    ), c.actionState ?? (c.actionState = {}), o.id === "aim" && !r.some((m) => m.id === "aim") && (c.actionState.aim = null), o.id === "move" && !r.some((m) => m.id === "move") && (c.actionState.move = null), o.id === "prepare" && !r.some((m) => m.id === "prepare") && (c.actionState.preparedInterrupt = null), await a.combatant.setFlag(St, Gt, c), l.actorBurnDelta !== 0 && await e.update({
      "system.burn.value": Math.max(
        0,
        Number(((f = (d = e.system) == null ? void 0 : d.burn) == null ? void 0 : f.value) ?? 0) - l.actorBurnDelta
      )
    }), l.actorEdgeDelta < 0 && l.edgePoolKey && e.gainEdge ? await e.gainEdge(l.edgePoolKey, Math.abs(l.actorEdgeDelta), { source: "removeActivationLogEntry" }) : l.actorEdgeDelta > 0 && l.edgePoolKey && e.spendEdge && await e.spendEdge(l.edgePoolKey, l.actorEdgeDelta, { source: "removeActivationLogEntry" }), { ok: !0, snapshot: this.getSnapshot(e, { token: a.token }), removed: o };
  }
  static getActivationIdentity(e, n) {
    return {
      combatId: (e == null ? void 0 : e.id) ?? null,
      combatantId: (n == null ? void 0 : n.id) ?? null,
      round: Number((e == null ? void 0 : e.round) ?? 0),
      turn: Number((e == null ? void 0 : e.turn) ?? 0)
    };
  }
  static async ensureCurrentCombatantState() {
    var c, u, d, f;
    if (!game.user.isGM) return;
    const e = game.combat, n = e == null ? void 0 : e.combatant;
    if (!e || !n || ((c = e.scene) == null ? void 0 : c.id) !== ((u = canvas == null ? void 0 : canvas.scene) == null ? void 0 : u.id)) return;
    this.rememberActivation(e, n);
    const i = this.getActivationIdentity(e, n), a = n.getFlag(St, Gt);
    if (uc(a, i)) return;
    const r = ((d = e.scene) == null ? void 0 : d.id) ?? ((f = canvas == null ? void 0 : canvas.scene) == null ? void 0 : f.id) ?? "", s = this._getCombatantActor(n, r), o = this.resolveActivationUnit({ actor: s, combat: e, combatant: n }), l = (o == null ? void 0 : o.actingActor) ?? s;
    await n.setFlag(St, Gt, uy(a, i, l, { combat: e, combatant: n, sceneId: r }));
  }
  static async reconcileOperatedCombatants(e = game == null ? void 0 : game.combat) {
    var s, o, l, c, u, d, f;
    if (!((s = game.user) != null && s.isGM) || !e) return { ok: !0, removed: [] };
    const n = this._getCombatSceneId(e) || ((o = canvas == null ? void 0 : canvas.scene) == null ? void 0 : o.id), i = this._getCombatants(e), a = [];
    for (const m of i) {
      const g = this._getCombatantActor(m, n);
      if (!Nt(g)) continue;
      const p = Dc(g);
      if (p)
        for (const h of i) {
          if ((h == null ? void 0 : h.id) === m.id) continue;
          const b = this._getCombatantActor(h, n);
          if (!this._actorsMatch(b, p)) continue;
          const y = (l = m.getFlag) == null ? void 0 : l.call(m, St, Gt), S = (c = h.getFlag) == null ? void 0 : c.call(h, St, Gt);
          !y && S && typeof m.setFlag == "function" && await m.setFlag(St, Gt, S), a.push(h.id);
        }
    }
    const r = Array.from(new Set(a.filter(Boolean)));
    if (!r.length) return { ok: !0, removed: [] };
    if (typeof e.deleteEmbeddedDocuments == "function")
      await e.deleteEmbeddedDocuments("Combatant", r);
    else
      for (const m of r) {
        const g = (d = (u = e.combatants) == null ? void 0 : u.get) == null ? void 0 : d.call(u, m);
        await ((f = g == null ? void 0 : g.delete) == null ? void 0 : f.call(g));
      }
    return { ok: !0, removed: r };
  }
  static async syncDestroyedMachineDefeatedCombatants(e = game == null ? void 0 : game.combat, n = null) {
    var r, s, o, l;
    if (!((r = game.user) != null && r.isGM) || !e) return { ok: !0, updated: [] };
    const i = this._getCombatSceneId(e) || ((s = canvas == null ? void 0 : canvas.scene) == null ? void 0 : s.id), a = [];
    for (const c of this._getCombatants(e)) {
      const u = this._getCombatantActor(c, i);
      if (!Nt(u) || n && !this._actorsMatch(u, n)) continue;
      const d = I$(u);
      !!(c != null && c.defeated) !== d && a.push({ _id: c.id, defeated: d });
    }
    if (!a.length) return { ok: !0, updated: [] };
    if (typeof e.updateEmbeddedDocuments == "function")
      await e.updateEmbeddedDocuments("Combatant", a);
    else
      for (const c of a) {
        const u = (l = (o = e.combatants) == null ? void 0 : o.get) == null ? void 0 : l.call(o, c._id);
        typeof (u == null ? void 0 : u.update) == "function" ? await u.update({ defeated: c.defeated }) : u && (u.defeated = c.defeated);
      }
    return { ok: !0, updated: a };
  }
  static async _skipDuplicatePilotTurn(e) {
    var n;
    return !((n = game.user) != null && n.isGM) || !(e != null && e.combatant) || !this._isDuplicatePilotCombatant(e.combatant, e) ? !1 : (e != null && e.id && this._lastActivationByCombat.set(e.id, this.getActivationIdentity(e, e.combatant)), typeof e.nextTurn == "function" && await e.nextTurn(), await this.reconcileOperatedCombatants(e), !0);
  }
  static async spendResource(e, {
    token: n = null,
    resource: i = "sa",
    cost: a = 1,
    actionId: r = "",
    actionLabel: s = "",
    actionCostLabel: o = "",
    actionCategory: l = ""
  } = {}) {
    var A, T, C, k, w, v, N;
    const c = this.getSnapshot(e, { token: n }), u = c.actionEconomyActor ?? e;
    if (!c.hasCombatant)
      return { ok: !1, reason: "No combatant on the current scene." };
    if (!c.isCurrentTurn)
      return { ok: !1, reason: "Only available during your activation." };
    this.rememberActivation(c.combat, c.combatant);
    const d = {
      combat: c.combat,
      combatant: c.combatant,
      state: Mo(
        c.state,
        this.getActivationIdentity(c.combat, c.combatant),
        gs(u, { snapshot: c })
      ),
      sceneId: ((A = canvas == null ? void 0 : canvas.scene) == null ? void 0 : A.id) ?? "",
      snapshot: c
    };
    let f = Math.max(0, Number(a ?? 0) || 0);
    const m = zt({
      actor: u,
      phase: "onBeforeActionCostFinalized",
      facts: df({
        actor: u,
        packet: { actionId: r, category: l, resource: i, cost: f, effectiveCost: f },
        runtime: d
      }),
      packet: { actionId: r, category: l, resource: i, cost: f, effectiveCost: f },
      options: { runtime: d, consumeUsage: !0 }
    });
    f = Math.max(0, Number(m.packet.cost ?? f) || 0), d.pendingMutations = (d.pendingMutations ?? []).concat(m.mutations);
    const g = `${i}Remaining`, p = Number(((T = c.state) == null ? void 0 : T[g]) ?? 0);
    if (i !== "sa" && p < f)
      return { ok: !1, reason: `No ${String(i).toUpperCase()} remaining.` };
    const h = d.state, b = i === "sa" ? Oc(u, { snapshot: c }) : 0, y = Math.max(0, Number(((C = c.state) == null ? void 0 : C.saSpentThisActivation) ?? 0) || 0);
    if (i === "sa" && y + f > b)
      return { ok: !1, reason: "Activation SA cap reached." };
    h[g] = Math.max(0, p - f), i === "sa" && (h.saSpentThisActivation = y + f, r === "attack" && (h.attacksThisActivation = Number(h.attacksThisActivation ?? 0) + 1));
    let S = 0;
    if (i === "sa") {
      const P = Math.max(0, y - aa), L = Math.max(0, h.saSpentThisActivation - aa), _ = Math.max(0, Number(((k = c.state) == null ? void 0 : k.attacksThisActivation) ?? 0) || 0), O = Math.max(0, Number(h.attacksThisActivation ?? 0) || 0);
      for (let x = P + 1; x <= L; x += 1) {
        const H = zt({
          actor: u,
          phase: "onBeforeBurnApplied",
          facts: Ic({
            actor: u,
            packet: {
              actionId: r,
              category: l,
              resource: i,
              amount: 1,
              source: "extraSA",
              extraSaIndex: x
            },
            runtime: d
          }),
          packet: {
            actionId: r,
            category: l,
            resource: i,
            amount: 1,
            source: "extraSA",
            extraSaIndex: x
          },
          options: { runtime: d, consumeUsage: !0 }
        });
        d.pendingMutations = (d.pendingMutations ?? []).concat(H.mutations), S += Math.max(0, Number(H.packet.amount ?? 0) || 0);
      }
      for (let x = _ + 1; x <= O; x += 1) {
        if (x <= 1) continue;
        const H = zt({
          actor: u,
          phase: "onBeforeBurnApplied",
          facts: Ic({
            actor: u,
            packet: {
              actionId: r,
              category: l,
              resource: i,
              amount: 1,
              source: "attack"
            },
            runtime: d
          }),
          packet: {
            actionId: r,
            category: l,
            resource: i,
            amount: 1,
            source: "attack",
            attackIndex: x
          },
          options: { runtime: d, consumeUsage: !0 }
        });
        d.pendingMutations = (d.pendingMutations ?? []).concat(H.mutations), S += Math.max(0, Number(H.packet.amount ?? 0) || 0);
      }
      h.burnThisActivation = Math.max(0, Number(h.burnThisActivation ?? 0) + S);
    }
    return this._appendActionLog(h, {
      id: r,
      label: s,
      costLabel: o || this._formatCostLabel(i, f),
      resource: i,
      cost: f,
      saSpentDelta: i === "sa" ? f : 0,
      attackDelta: i === "sa" && r === "attack" ? 1 : 0,
      burnThisActivationDelta: S,
      actorBurnDelta: S
    }), (w = d.pendingMutations) != null && w.length ? await ga({
      actor: u,
      mutations: d.pendingMutations,
      runtime: {
        ...d,
        state: h
      }
    }) : await c.combatant.setFlag(St, Gt, h), S > 0 && await u.update({ "system.burn.value": Math.max(0, Number(((N = (v = u.system) == null ? void 0 : v.burn) == null ? void 0 : N.value) ?? 0) + S) }), {
      ok: !0,
      snapshot: this.getSnapshot(e, { token: c.token }),
      burnDelta: S,
      finalCost: f,
      costLabel: o || this._formatCostLabel(i, f)
    };
  }
  static previewResourceSpend(e, {
    token: n = null,
    resource: i = "sa",
    cost: a = 1,
    actionId: r = "",
    actionLabel: s = "",
    actionCostLabel: o = "",
    actionCategory: l = ""
  } = {}) {
    var y, S, A;
    const c = this.getSnapshot(e, { token: n }), u = c.actionEconomyActor ?? e;
    if (!c.hasCombatant)
      return { ok: !1, reason: "No combatant on the current scene." };
    if (!c.isCurrentTurn)
      return { ok: !1, reason: "Only available during your activation." };
    const d = {
      combat: c.combat,
      combatant: c.combatant,
      state: Mo(
        c.state,
        this.getActivationIdentity(c.combat, c.combatant),
        gs(u, { snapshot: c })
      ),
      sceneId: ((y = canvas == null ? void 0 : canvas.scene) == null ? void 0 : y.id) ?? "",
      snapshot: c
    };
    let f = Math.max(0, Number(a ?? 0) || 0);
    const m = zt({
      actor: u,
      phase: "onBeforeActionCostFinalized",
      facts: df({
        actor: u,
        packet: { actionId: r, category: l, resource: i, cost: f, effectiveCost: f },
        runtime: d
      }),
      packet: { actionId: r, category: l, resource: i, cost: f, effectiveCost: f },
      options: { runtime: d, consumeUsage: !1 }
    });
    f = Math.max(0, Number(m.packet.cost ?? f) || 0);
    const g = `${i}Remaining`, p = Number(((S = c.state) == null ? void 0 : S[g]) ?? 0);
    if (i !== "sa" && p < f)
      return { ok: !1, reason: `No ${String(i).toUpperCase()} remaining.`, finalCost: f };
    const h = i === "sa" ? Oc(u, { snapshot: c }) : 0, b = Math.max(0, Number(((A = c.state) == null ? void 0 : A.saSpentThisActivation) ?? 0) || 0);
    return i === "sa" && b + f > h ? { ok: !1, reason: "Activation SA cap reached.", finalCost: f } : {
      ok: !0,
      snapshot: c,
      finalCost: f,
      costLabel: o || this._formatCostLabel(i, f),
      actionLabel: s
    };
  }
  static async reduceBurn(e, { token: n = null } = {}) {
    var l, c, u, d;
    const i = this.getSnapshot(e, { token: n }), a = i.actionEconomyActor ?? e;
    if (!i.hasCombatant) return { ok: !1, reason: "No combatant on the current scene." };
    if (!i.isCurrentTurn) return { ok: !1, reason: "Only available during your activation." };
    if (dc(a, i) <= 0) return { ok: !1, reason: "Activation SA cap reached." };
    if (i.burn.value <= 0) return { ok: !1, reason: "Burn is already at 0." };
    const r = await this.spendResource(a, {
      token: i.token,
      resource: "sa",
      cost: 1,
      actionId: "reduceBurn",
      actionLabel: "Reduce Burn",
      actionCostLabel: "1 SA",
      actionCategory: He.standard
    });
    if (!r.ok) return r;
    const s = Math.max(0, Number(((c = (l = a.system) == null ? void 0 : l.burn) == null ? void 0 : c.value) ?? 0) - 1), o = { "system.burn.value": s };
    return s === 0 && ((d = (u = a.system) == null ? void 0 : u.burn) != null && d.overloaded) && (o["system.burn.overloaded"] = !1), await a.update(o), await this.updateCombatantState(a, {
      token: i.token,
      mutate: (f) => {
        var p;
        const m = br(f.actionLog), g = ((p = m.findLastIndex) == null ? void 0 : p.call(m, (h) => h.id === "reduceBurn")) ?? (() => {
          var h;
          for (let b = m.length - 1; b >= 0; b -= 1)
            if (((h = m[b]) == null ? void 0 : h.id) === "reduceBurn") return b;
          return -1;
        })();
        return g >= 0 && (m[g] = {
          ...m[g],
          actorBurnDelta: Math.max(0, Number(r.burnDelta ?? 0) || 0) - 1
        }, f.actionLog = m), f;
      }
    }), { ok: !0, snapshot: this.getSnapshot(a, { token: i.token }) };
  }
  static async finalizeActivation(e, n) {
    var h, b, y, S, A, T, C, k, w, v, N, P, L, _;
    if (!game.user.isGM || !n || !e) return;
    const i = ((b = (h = e.combatants) == null ? void 0 : h.get) == null ? void 0 : b.call(h, n)) ?? null, a = this._getCombatantActor(i, ((y = e == null ? void 0 : e.scene) == null ? void 0 : y.id) ?? ((S = canvas == null ? void 0 : canvas.scene) == null ? void 0 : S.id));
    if (!i || !a) return;
    if (!Nt(a)) {
      const O = this._findMachineCombatantForPilot(a, e);
      if (O && O.id !== i.id) return;
    }
    const r = this.resolveActivationUnit({ actor: a, combat: e, combatant: i }), s = (r == null ? void 0 : r.actingActor) ?? this.getActionEconomyActorForCombatant(i, ((A = e == null ? void 0 : e.scene) == null ? void 0 : A.id) ?? ((T = canvas == null ? void 0 : canvas.scene) == null ? void 0 : T.id)), o = (r == null ? void 0 : r.platformActor) ?? (Nt(a) ? a : null);
    if (!s) return;
    const l = i.getFlag(St, Gt), c = gs(s, { combat: e, combatant: i, sceneId: ((C = e.scene) == null ? void 0 : C.id) ?? ((k = canvas == null ? void 0 : canvas.scene) == null ? void 0 : k.id) ?? "" }), u = uc(l, this.getActivationIdentity(e, i)) ? Mo(l, this.getActivationIdentity(e, i), c) : Mo(l, null, c), f = {
      burnDelta: Number(u.saSpentThisActivation ?? 0) <= aa && Number(u.burnThisActivation ?? 0) <= 0 && Number(u.reactionBurnSinceLastActivation ?? 0) <= 0 ? -2 : 0,
      edgeAdjustments: []
    }, m = {
      combat: e,
      combatant: i,
      state: u,
      sceneId: ((w = e.scene) == null ? void 0 : w.id) ?? ((v = canvas == null ? void 0 : canvas.scene) == null ? void 0 : v.id) ?? ""
    }, g = zt({
      actor: s,
      phase: "onEndOfActivation",
      facts: Gk({ actor: s, packet: f, runtime: m }),
      packet: f,
      options: { runtime: m, consumeUsage: !0 }
    });
    await ga({ actor: s, mutations: g.mutations, runtime: m });
    const p = Number(g.packet.burnDelta ?? f.burnDelta) || 0;
    if (p) {
      const O = Math.max(0, Number(((P = (N = s.system) == null ? void 0 : N.burn) == null ? void 0 : P.value) ?? 0) + p), x = { "system.burn.value": O };
      O === 0 && ((_ = (L = s.system) == null ? void 0 : L.burn) != null && _.overloaded) && (x["system.burn.overloaded"] = !1), await s.update(x);
    }
    for (const O of g.packet.edgeAdjustments ?? []) {
      const x = Number((O == null ? void 0 : O.amount) ?? 0) || 0;
      !x || !(O != null && O.poolKey) || (x > 0 ? await s.gainEdge(O.poolKey, x, { skipTraitHooks: !0, source: "endOfActivationTrait" }) : await s.spendEdge(O.poolKey, Math.abs(x), { skipTraitHooks: !0, source: "endOfActivationTrait" }));
    }
    (o == null ? void 0 : o.type) === "battlemech" && await Lp(o, {
      source: "combat turn advance",
      postDangerCard: !0,
      activation: this.getActivationIdentity(e, i)
    });
  }
  static async _onUpdateCombat(e, n) {
    if (Object.prototype.hasOwnProperty.call(n ?? {}, "turn") || Object.prototype.hasOwnProperty.call(n ?? {}, "round")) {
      const a = this._lastActivationByCombat.get(e == null ? void 0 : e.id) ?? null, r = typeof a == "string" ? a : (a == null ? void 0 : a.combatantId) ?? null, s = this.getActivationIdentity(e, e == null ? void 0 : e.combatant), o = a && typeof a == "object" ? !uc(a, s) : r && r !== s.combatantId;
      if (r && o && await this.finalizeActivation(e, r), await this._skipDuplicatePilotTurn(e)) {
        this.renderOpenActorSheets();
        return;
      }
      await this.reconcileOperatedCombatants(e), await this.syncDestroyedMachineDefeatedCombatants(e), await this.ensureCurrentCombatantState(), await this._processCurrentCombatantHazards(e), await this._processCurrentCombatantMachineCrits(e), e != null && e.id && this._lastActivationByCombat.set(e.id, s);
    }
    this.renderOpenActorSheets();
  }
  static async _onCreateCombatant(e) {
    var a, r, s, o, l;
    const n = e == null ? void 0 : e.combat;
    this._isDuplicatePilotCombatant(e, n) && ((r = (a = ui.notifications) == null ? void 0 : a.warn) == null || r.call(a, "Linked pilots act through their machine combatant and are not added as separate turns.")), await this.reconcileOperatedCombatants(n), await this.syncDestroyedMachineDefeatedCombatants(n), ((s = n == null ? void 0 : n.combatant) == null ? void 0 : s.id) === (e == null ? void 0 : e.id) && await this.ensureCurrentCombatantState(), await this._syncPreparedIndicatorForCombatant(e);
    const i = this._getCombatantTokenDocument(e, ((o = n == null ? void 0 : n.scene) == null ? void 0 : o.id) ?? ((l = canvas == null ? void 0 : canvas.scene) == null ? void 0 : l.id));
    i && await this._syncHazardPresenceForToken(i), this.renderOpenActorSheets();
  }
  static async _onDeleteCombatant(e) {
    await this.clearPreparedIndicatorForCombatant(e), await this.reconcileOperatedCombatants(e == null ? void 0 : e.combat), this.renderOpenActorSheets();
  }
  static async _onDeleteCombat(e) {
    e != null && e.id && this._lastActivationByCombat.delete(e.id);
    for (const n of this._getCombatants(e))
      await this.clearPreparedIndicatorForCombatant(n);
    this.renderOpenActorSheets();
  }
  static _onUpdateActor(e, n) {
    const i = foundry.utils.hasProperty(n, "system.pilot.uuid") || foundry.utils.hasProperty(n, "system.mwd.pilot.uuid") || foundry.utils.hasProperty(n, "system.mwd.crew.operatorActorUuid") || foundry.utils.hasProperty(n, "system.mwd.crew.pilotActorUuid"), a = foundry.utils.hasProperty(n, "system.mwd.status.state");
    !i && !a || (i && this.reconcileOperatedCombatants(game.combat), a && this.syncDestroyedMachineDefeatedCombatants(game.combat, e), this.renderOpenActorSheets());
  }
  static _onActiveEffectChange(e) {
    const n = (e == null ? void 0 : e.parent) ?? null;
    Nt(n) && Gw(e).has(Nf) && this._queueDestroyedMachineDefeatedSync(n);
  }
  static _queueDestroyedMachineDefeatedSync(e) {
    const n = () => {
      this.syncDestroyedMachineDefeatedCombatants(game.combat, e), this.renderOpenActorSheets(e == null ? void 0 : e.id);
    };
    typeof queueMicrotask == "function" ? queueMicrotask(n) : Promise.resolve().then(n);
  }
  static _onUpdateCombatant(e, n) {
    var r, s, o, l;
    const i = foundry.utils.hasProperty(n, `flags.${St}.${Gt}`), a = foundry.utils.hasProperty(n, `flags.${St}.targeting`) || foundry.utils.hasProperty(n, `flags.${St}.ewState`);
    if (i) {
      this._syncPreparedIndicatorForCombatant(e);
      const c = this._getCombatantTokenDocument(e, this._getCombatantSceneId(e) || ((r = canvas == null ? void 0 : canvas.scene) == null ? void 0 : r.id));
      c && this._queueHazardOverlayRefresh(c);
    }
    if (i || a) {
      this.renderOpenActorSheets((s = e == null ? void 0 : e.actor) == null ? void 0 : s.id);
      const c = this.getActionEconomyActorForCombatant(
        e,
        this._getCombatantSceneId(e) || ((o = canvas == null ? void 0 : canvas.scene) == null ? void 0 : o.id)
      );
      c != null && c.id && c.id !== ((l = e == null ? void 0 : e.actor) == null ? void 0 : l.id) && this.renderOpenActorSheets(c.id);
    }
  }
  static _onTargetToken(e, n, i) {
    var a;
    (e == null ? void 0 : e.id) === ((a = game.user) == null ? void 0 : a.id) && this.queueCharacterSheetRefresh();
  }
  static _onUpdateToken(e, n) {
    var r, s;
    if (!["x", "y", "elevation"].some(
      (o) => Object.prototype.hasOwnProperty.call(n ?? {}, o)
    ) || ((r = e == null ? void 0 : e.parent) == null ? void 0 : r.id) !== ((s = canvas == null ? void 0 : canvas.scene) == null ? void 0 : s.id)) return;
    const a = String((e == null ? void 0 : e.id) ?? "").trim();
    if (a) {
      const o = Object.prototype.hasOwnProperty.call(n ?? {}, "x") ? Number(n.x) : Number(e == null ? void 0 : e.x), l = Object.prototype.hasOwnProperty.call(n ?? {}, "y") ? Number(n.y) : Number(e == null ? void 0 : e.y);
      Number.isFinite(o) && Number.isFinite(l) && this._pendingTokenPositions.set(a, { x: o, y: l });
    }
    this._syncHazardPresenceForToken(e), this.queueCharacterSheetRefresh();
  }
  static _onRefreshToken(e) {
    this._refreshHazardOverlay(e);
  }
  static _getTokenDocumentFromRegionEvent(e = []) {
    var n, i, a, r, s, o;
    for (const l of e) {
      if (!l) continue;
      const c = [
        l == null ? void 0 : l.document,
        l == null ? void 0 : l.token,
        l == null ? void 0 : l.tokenDocument,
        (n = l == null ? void 0 : l.object) == null ? void 0 : n.document,
        (i = l == null ? void 0 : l.data) == null ? void 0 : i.token,
        (a = l == null ? void 0 : l.data) == null ? void 0 : a.tokenDocument,
        (r = l == null ? void 0 : l.eventData) == null ? void 0 : r.token,
        (s = l == null ? void 0 : l.eventData) == null ? void 0 : s.tokenDocument
      ];
      for (const u of c) {
        const d = (u == null ? void 0 : u.document) ?? u ?? null;
        if ((d == null ? void 0 : d.documentName) === "Token" || ((o = d == null ? void 0 : d.constructor) == null ? void 0 : o.documentName) === "Token")
          return d;
      }
    }
    return null;
  }
  static _onRegionTokenEvent(...e) {
    const n = this._getTokenDocumentFromRegionEvent(e);
    n && this._syncHazardPresenceForToken(n);
  }
  static async _onCreateRegion(e) {
    lm(e) && (await Zd(e), await this._syncAllSceneHazards((e == null ? void 0 : e.parent) ?? (canvas == null ? void 0 : canvas.scene) ?? null));
  }
  static async _onUpdateRegion(e) {
    lm(e) && (await Zd(e), await this._syncAllSceneHazards((e == null ? void 0 : e.parent) ?? (canvas == null ? void 0 : canvas.scene) ?? null));
  }
  static async _onDeleteRegion(e) {
    var r, s, o;
    const n = String((e == null ? void 0 : e.id) ?? "").trim();
    if (!n) return;
    const i = (e == null ? void 0 : e.parent) ?? (canvas == null ? void 0 : canvas.scene) ?? null, a = game.combat;
    for (const l of this._getCombatants(a)) {
      const c = this._getCombatantTokenDocument(l, (i == null ? void 0 : i.id) ?? ((r = canvas == null ? void 0 : canvas.scene) == null ? void 0 : r.id)), u = (c == null ? void 0 : c.actor) ?? (l == null ? void 0 : l.actor) ?? null;
      if (!u || !c) continue;
      const d = this.getSnapshot(u, { token: c });
      (s = d == null ? void 0 : d.hazards) != null && s[n] && (await this.setHazardState(u, { token: c, regionId: n, hazardState: null }), ((o = d == null ? void 0 : d.pendingReaction) == null ? void 0 : o.sourceKind) === "hazard" && d.pendingReaction.sourceId === n && await this.clearPendingReaction(u, { token: c }), this._queueHazardOverlayRefresh(c));
    }
  }
  static async _syncAllSceneHazards(e = (canvas == null ? void 0 : canvas.scene) ?? null) {
    if (e) {
      for (const n of Array.from(e.regions ?? []))
        lm(n) && await Zd(n);
      for (const n of Array.from(e.tokens ?? []))
        await this._syncHazardPresenceForToken(n), this._queueHazardOverlayRefresh(n);
    }
  }
  static async _syncHazardPresenceForToken(e) {
    var c, u, d, f, m, g;
    const n = this._asTokenDocument(e), i = (n == null ? void 0 : n.actor) ?? null;
    if (!this._supportsHazardActor(i) || !n) {
      this._queueHazardOverlayRefresh(n);
      return;
    }
    const a = this.getSnapshot(i, { token: n });
    if (!(a != null && a.hasCombatant)) {
      this._queueHazardOverlayRefresh(n);
      return;
    }
    Su(a.hazards);
    const r = Ib(n), s = new Map(
      r.map((p) => {
        const h = Ha(p);
        return h ? [String(p.id ?? "").trim(), { region: p, flag: h }] : null;
      }).filter(Boolean)
    ), o = [], l = [];
    await this.updateCombatantState(i, {
      token: n,
      mutate: (p) => {
        var h, b, y, S, A;
        p.hazards ?? (p.hazards = {});
        for (const [T, { flag: C }] of s.entries()) {
          if (p.hazards[T]) continue;
          const k = {
            tier: Mt((h = C == null ? void 0 : C.hazardDef) == null ? void 0 : h.startExposure, Te.minor),
            turnsExposed: 0,
            evadeLocked: !1,
            lastProcessedRound: 0
          };
          p.hazards[T] = k, o.push({ regionId: T, flag: C, hazardState: k });
        }
        for (const [T, C] of Object.entries(p.hazards ?? {})) {
          if (s.has(T)) continue;
          const k = Ha((S = (y = (b = canvas == null ? void 0 : canvas.scene) == null ? void 0 : b.regions) == null ? void 0 : y.get) == null ? void 0 : S.call(y, T)) ?? null;
          ((A = k == null ? void 0 : k.hazardDef) == null ? void 0 : A.clearOnExit) !== !1 && (delete p.hazards[T], l.push({ regionId: T, hazardState: C, flag: k }));
        }
        return p;
      }
    });
    for (const p of o) {
      const h = ((c = s.get(p.regionId)) == null ? void 0 : c.region) ?? ((f = (d = (u = canvas == null ? void 0 : canvas.scene) == null ? void 0 : u.regions) == null ? void 0 : d.get) == null ? void 0 : f.call(d, p.regionId)) ?? null;
      await this._createHazardEventChatCard({
        actor: i,
        token: n,
        region: h,
        hazardFlag: p.flag,
        hazardState: p.hazardState,
        eventType: "entry",
        nextTier: p.hazardState.tier,
        allowEvade: !p.hazardState.evadeLocked
      });
    }
    for (const p of l) {
      ((m = a == null ? void 0 : a.pendingReaction) == null ? void 0 : m.sourceKind) === "hazard" && a.pendingReaction.sourceId === p.regionId && await this.clearPendingReaction(i, { token: n });
      const h = String(((g = p == null ? void 0 : p.flag) == null ? void 0 : g.label) ?? "Hazard").trim() || "Hazard", b = `<div class="mwd-gm-notice"><b>${foundry.utils.escapeHTML(h)}:</b> ${foundry.utils.escapeHTML(i.name ?? "Target")} leaves the zone.</div>`;
      await ChatMessage.create(om({
        speaker: ChatMessage.getSpeaker({ actor: i, token: n }),
        content: b
      }));
    }
    this._queueHazardOverlayRefresh(n);
  }
  static async _processCurrentCombatantHazards(e = game.combat) {
    var l, c, u;
    const n = (e == null ? void 0 : e.combatant) ?? null, i = this._getCombatantTokenDocument(n, ((l = e == null ? void 0 : e.scene) == null ? void 0 : l.id) ?? ((c = canvas == null ? void 0 : canvas.scene) == null ? void 0 : c.id)), a = (i == null ? void 0 : i.actor) ?? (n == null ? void 0 : n.actor) ?? null;
    if (!n || !i || !this._supportsHazardActor(a)) return;
    const r = this.getSnapshot(a, { token: i }), s = Number((e == null ? void 0 : e.round) ?? 0) || 0, o = new Map(
      Ib(i).map((d) => {
        const f = Ha(d);
        return f ? [String(d.id ?? "").trim(), { region: d, flag: f }] : null;
      }).filter(Boolean)
    );
    for (const [d, f] of Object.entries(r.hazards ?? {})) {
      if ((Number((f == null ? void 0 : f.lastProcessedRound) ?? 0) || 0) >= s) continue;
      const m = o.get(d);
      if (!m) continue;
      const g = this._getHazardNextTier(f, ((u = m.flag) == null ? void 0 : u.hazardDef) ?? {});
      await this._createHazardEventChatCard({
        actor: a,
        token: i,
        region: m.region,
        hazardFlag: m.flag,
        hazardState: f,
        eventType: "tick",
        nextTier: g,
        allowEvade: !f.evadeLocked
      }), await this.setHazardState(a, {
        token: i,
        regionId: d,
        hazardState: {
          ...f,
          lastProcessedRound: s
        }
      });
    }
    this._queueHazardOverlayRefresh(i);
  }
  static async _processCurrentCombatantMachineCrits(e = game.combat) {
    var l, c, u, d, f, m, g, p, h;
    const n = (e == null ? void 0 : e.combatant) ?? null, i = this._getCombatantTokenDocument(n, ((l = e == null ? void 0 : e.scene) == null ? void 0 : l.id) ?? ((c = canvas == null ? void 0 : canvas.scene) == null ? void 0 : c.id)), a = (i == null ? void 0 : i.actor) ?? (n == null ? void 0 : n.actor) ?? null;
    if (!n || !i || !a || !Nt(a)) return;
    const r = this.getSnapshot(a, { token: i });
    if (!(r != null && r.hasCombatant) || !(r != null && r.isCurrentTurn) || (u = r == null ? void 0 : r.state) != null && u.machineCritsProcessed) return;
    const s = DO(a), o = [];
    if (s.saCost > 0) {
      const b = await this.spendResource(a, {
        token: i,
        resource: "sa",
        cost: s.saCost,
        actionId: "machineCritStaggered",
        actionLabel: "Staggered",
        actionCostLabel: `${s.saCost} SA`,
        actionCategory: "simple"
      });
      o.push(b != null && b.ok ? `Staggered: spent ${s.saCost} SA at the start of activation.` : `Staggered: unable to auto-spend ${s.saCost} SA (${(b == null ? void 0 : b.reason) ?? "manual resolution required"}).`);
    }
    if (s.heatDelta > 0 && a.type === "battlemech") {
      const y = Math.max(0, Number(((m = (f = (d = a.system) == null ? void 0 : d.monitors) == null ? void 0 : f.heat) == null ? void 0 : m.value) ?? ((h = (p = (g = a.system) == null ? void 0 : g.mwd) == null ? void 0 : p.heat) == null ? void 0 : h.current) ?? 0) || 0) + s.heatDelta;
      await a.update({
        "system.monitors.heat.value": y,
        "system.mwd.heat.current": y
      }), o.push(`Overheating: gained ${s.heatDelta} Heat at the start of activation.`);
    }
    if (o.push(...s.reminders.map((b) => `${b.label}: ${b.text}`)), o.length) {
      const b = [
        '<div class="mwd-gm-notice">',
        `<strong>${foundry.utils.escapeHTML(a.name ?? "Machine")} - Critical Effects</strong>`,
        `<ul>${o.map((y) => `<li>${foundry.utils.escapeHTML(y)}</li>`).join("")}</ul>`,
        "</div>"
      ].join("");
      await ChatMessage.create(om({
        speaker: ChatMessage.getSpeaker({ actor: a, token: i }),
        content: b
      }));
    }
    await this.updateCombatantState(a, {
      token: i,
      mutate: (b) => (b.machineCritsProcessed = !0, b)
    });
  }
  static _getHazardNextTier(e = {}, n = {}) {
    var l, c, u, d;
    const i = Math.max(0, Number((e == null ? void 0 : e.turnsExposed) ?? 0) || 0), a = Math.max(1, Number(((l = n == null ? void 0 : n.escalation) == null ? void 0 : l.intervalTurns) ?? 1) || 1), r = Math.max(0, Number(((c = n == null ? void 0 : n.escalation) == null ? void 0 : c.rate) ?? 1) || 0);
    if (!(r > 0 && (i + 1) % a === 0)) return Mt(e == null ? void 0 : e.tier, Te.none);
    let o = Mt(e == null ? void 0 : e.tier, Te.none);
    for (let f = 0; f < r; f += 1)
      if (o = zI(o, 1), xa(o) >= xa(((u = n == null ? void 0 : n.escalation) == null ? void 0 : u.max) ?? Te.full)) {
        o = Mt((d = n == null ? void 0 : n.escalation) == null ? void 0 : d.max, Te.full);
        break;
      }
    return o;
  }
  static async _createHazardEventChatCard({
    actor: e = null,
    token: n = null,
    region: i = null,
    hazardFlag: a = {},
    hazardState: r = {},
    eventType: s = "entry",
    nextTier: o = null,
    allowEvade: l = !1
  } = {}) {
    var p, h, b;
    if (!e) return null;
    const c = Mt(r == null ? void 0 : r.tier, Te.none), u = Mt(o, c), d = l && c !== Te.none && !(r != null && r.evadeLocked) ? this.getReactionSpendPreview(e, { token: n }) : null, f = {
      kind: "hazard",
      eventType: s,
      regionId: String((i == null ? void 0 : i.id) ?? "").trim(),
      regionName: String((a == null ? void 0 : a.label) ?? (i == null ? void 0 : i.name) ?? "Hazard").trim() || "Hazard",
      actorUuid: e.uuid,
      tokenUuid: (n == null ? void 0 : n.uuid) ?? ((p = n == null ? void 0 : n.document) == null ? void 0 : p.uuid) ?? null,
      actorName: e.name ?? "Target",
      turnsExposed: Math.max(0, Number((r == null ? void 0 : r.turnsExposed) ?? 0) || 0),
      baseDamage: Math.max(0, Number((a == null ? void 0 : a.damage) ?? 0) || 0),
      damageBefore: $r(Number((a == null ? void 0 : a.damage) ?? 0) || 0, c),
      damageAfter: $r(
        Number((a == null ? void 0 : a.damage) ?? 0) || 0,
        l && !(r != null && r.evadeLocked) ? Zm(c, 1) : c
      ),
      damageType: String((a == null ? void 0 : a.damageType) ?? "concussive").trim() || "concussive",
      ap: Math.max(0, Number((a == null ? void 0 : a.ap) ?? 0) || 0),
      onFullBurnDelta: Math.max(0, Number(((b = (h = a == null ? void 0 : a.hazardDef) == null ? void 0 : h.onFull) == null ? void 0 : b.burnDelta) ?? 0) || 0),
      source: String((a == null ? void 0 : a.label) ?? (i == null ? void 0 : i.name) ?? "Hazard").trim() || "Hazard",
      nextTier: u,
      exposure: {
        initialTier: c,
        finalTier: c,
        initialLabel: ci(c),
        finalLabel: ci(c),
        evadeLocked: !!(r != null && r.evadeLocked)
      },
      preview: {
        evadeActive: !1,
        edgePoolKey: null,
        finalTier: c,
        reactionPreview: d ? {
          burnDelta: Number(d.burnDelta ?? 0),
          canSpendEdge: !!d.canSpendEdge,
          edgePools: Array.isArray(d.edgePools) ? d.edgePools : []
        } : {}
      }
    }, m = await Zk(f, { actor: e, token: n }), g = await ChatMessage.create(om({
      speaker: ChatMessage.getSpeaker({ actor: e, token: n }),
      content: m,
      flags: {
        mwd: {
          hazardCard: f
        }
      }
    }));
    return g && d && c !== Te.none && !(r != null && r.evadeLocked) && await this.setPendingReaction(e, {
      token: n,
      pendingReaction: {
        type: "evade",
        sourceKind: "hazard",
        sourceId: String((i == null ? void 0 : i.id) ?? "").trim() || null,
        messageId: g.id,
        exposureBefore: c,
        exposureAfterPreview: Zm(c, 1),
        edgePoolKey: null,
        allowCurrentTurn: !0
      }
    }), g;
  }
  static _supportsHazardActor(e) {
    return (e == null ? void 0 : e.type) === "character" || (e == null ? void 0 : e.type) === "npc";
  }
  static _queueHazardOverlayRefresh(e) {
    var i;
    const n = (e == null ? void 0 : e.object) ?? e ?? null;
    (i = n == null ? void 0 : n.refresh) == null || i.call(n);
  }
  static _refreshHazardOverlay(e) {
    var c, u, d;
    const n = (e == null ? void 0 : e.object) ?? e ?? null, i = (n == null ? void 0 : n.document) ?? e ?? null;
    if (!n || !i) return;
    const a = (i == null ? void 0 : i.actor) ?? null, r = a ? this.getSnapshot(a, { token: i }) : null, s = Object.values((r == null ? void 0 : r.hazards) ?? {}), o = E$(s);
    let l = n.mwdHazardOverlay ?? null;
    if (!o) {
      l != null && l.parent && l.parent.removeChild(l), (c = l == null ? void 0 : l.destroy) == null || c.call(l), n.mwdHazardOverlay = null;
      return;
    }
    l || (l = new PIXI.Text(o, {
      fontFamily: "MWD UI",
      fontSize: 14,
      fontWeight: "700",
      fill: "#fff2d5",
      stroke: "#23150d",
      strokeThickness: 4,
      align: "center"
    }), (d = (u = l.anchor) == null ? void 0 : u.set) == null || d.call(u, 0, 1), n.addChild(l), n.mwdHazardOverlay = l), l.text = o, l.x = 6, l.y = Math.max(18, Number(n.h ?? 0) - 4);
  }
  static queueCharacterSheetRefresh(e = null) {
    this._targetRefreshTimeout && clearTimeout(this._targetRefreshTimeout), this._targetRefreshTimeout = setTimeout(() => {
      this._targetRefreshTimeout = null, this.renderOpenActorSheets(e);
    }, 0);
  }
  static _collectOpenActorSheetApps(e = null) {
    var r, s;
    const n = Array.isArray(e) && e.length ? new Set(e.map((o) => String(o ?? "").trim()).filter(Boolean)) : null, i = /* @__PURE__ */ new Set(), a = (o) => {
      var l;
      for (const c of Object.values((o == null ? void 0 : o.apps) ?? {})) {
        const u = String(((l = c == null ? void 0 : c.actor) == null ? void 0 : l.type) ?? "").trim();
        u && (n && !n.has(u) || i.add(c));
      }
    };
    for (const o of Array.from(game.actors ?? []))
      a(o);
    for (const o of Array.from(((r = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : r.placeables) ?? []))
      a(o == null ? void 0 : o.actor);
    for (const o of Object.values(ui.windows ?? {})) {
      const l = String(((s = o == null ? void 0 : o.actor) == null ? void 0 : s.type) ?? "").trim();
      l && (n && !n.has(l) || i.add(o));
    }
    return Array.from(i);
  }
  static renderOpenActorSheets(e = null, n = null) {
    var a;
    const i = this._collectOpenActorSheetApps(n);
    for (const r of i)
      if (!(e && ((a = r.actor) == null ? void 0 : a.id) !== e)) {
        if (typeof r.requestCombatDashboardRefresh == "function") {
          r.requestCombatDashboardRefresh();
          continue;
        }
        r.render({ force: !0 });
      }
  }
  static renderOpenCharacterSheets(e = null) {
    this.renderOpenActorSheets(e, ["character"]);
  }
};
F(ys, "_targetRefreshTimeout", null), F(ys, "_pendingTokenPositions", /* @__PURE__ */ new Map()), F(ys, "_lastActivationByCombat", /* @__PURE__ */ new Map());
let D = ys;
function P$(t, e = "sa") {
  var a, r, s, o, l, c, u;
  if (e === "fa") return Hw;
  if (e === "ra") return jw;
  const n = Math.max(0, Number(((a = t == null ? void 0 : t.getAttributeValue) == null ? void 0 : a.call(t, "reflexes")) ?? ((o = (s = (r = t == null ? void 0 : t.system) == null ? void 0 : r.attributes) == null ? void 0 : s.reflexes) == null ? void 0 : o.value) ?? 0) || 0), i = Math.max(0, Number(((u = (c = (l = t == null ? void 0 : t.system) == null ? void 0 : l.attributes) == null ? void 0 : c.guts) == null ? void 0 : u.value) ?? 0) || 0);
  return aa + Math.floor((n + i) / 2);
}
function Ns(t, e = "sa", n = {}) {
  const i = String(e ?? "sa").trim().toLowerCase() || "sa", a = `${i}CapMod`, r = P$(t, i) + oi(t, a), s = { resource: i, max: r }, o = zt({
    actor: t,
    phase: "onActivationBudgetResolved",
    facts: Hk({ actor: t, packet: s, runtime: n }),
    packet: s,
    options: { runtime: n, consumeUsage: !1 }
  });
  return Math.max(0, Math.trunc(Number(o.packet.max ?? r) || 0));
}
function gs(t, e = {}) {
  return {
    sa: Ns(t, "sa", e),
    fa: Ns(t, "fa", e),
    ra: Ns(t, "ra", e)
  };
}
function Oc(t, e = {}) {
  return Ns(t, "sa", e);
}
function dc(t, e) {
  var n;
  return Math.max(
    0,
    Oc(t, { snapshot: e }) - Math.max(0, Number(((n = e == null ? void 0 : e.state) == null ? void 0 : n.saSpentThisActivation) ?? 0) || 0)
  );
}
const Ps = "lifeModuleCatalog", yd = Object.freeze([
  { moduleType: "faction", label: "Faction" },
  { moduleType: "childhood", label: "Childhood" },
  { moduleType: "higherEducation", label: "Higher Education" },
  { moduleType: "realLife", label: "Real Life" }
]), R$ = Object.freeze(
  Object.fromEntries(yd.map((t) => [t.moduleType, t.label]))
), D$ = new Set(yd.map((t) => t.moduleType)), O$ = /* @__PURE__ */ new Set(["skill", "edgePool"]), ih = Object.freeze({
  grit: "Grit",
  chaos: "Chaos",
  insight: "Insight",
  rumor: "Rumor",
  legend: "Legend",
  credibility: "Credibility"
}), qw = Object.freeze(Object.keys(ih)), L$ = Object.freeze({
  skill: "Skill",
  edgePool: "Edge Pool"
}), $$ = Object.freeze({
  disguise: "deception"
}), _$ = Object.freeze(K$()), B$ = Object.freeze(H$()), x$ = /* @__PURE__ */ new Set(["artillery", "gunnery", "piloting", "heavyWeapons"]), z$ = /* @__PURE__ */ new Set([
  "artillery",
  "gunnery",
  "heavyWeapons",
  "meleeCombat",
  "piloting",
  "projectileWeapons",
  "firearms"
]), F$ = Object.freeze(
  tn.map((t) => t.code).filter((t) => !z$.has(t))
), U$ = Object.freeze(Yr([
  {
    id: "faction-capellan-confederation",
    label: "Capellan Confederation",
    moduleType: "faction",
    grants: [
      { id: "skill", choices: [{ type: "skill", value: "deception" }] },
      { id: "edge-pool", choices: [{ type: "edgePool", value: "rumor" }] }
    ]
  },
  {
    id: "faction-draconis-combine",
    label: "Draconis Combine",
    moduleType: "faction",
    grants: [
      { id: "edge-pool", choices: [{ type: "edgePool", value: "legend" }] },
      { id: "skill", choices: [{ type: "skill", value: "meleeCombat" }] }
    ]
  },
  {
    id: "faction-federated-suns",
    label: "Federated Suns",
    moduleType: "faction",
    grants: [
      { id: "edge-pool", choices: [{ type: "edgePool", value: "credibility" }] },
      { id: "skill", choices: [{ type: "skill", value: "firearms" }] }
    ]
  },
  {
    id: "faction-free-worlds-league",
    label: "Free Worlds League",
    moduleType: "faction",
    grants: [
      { id: "skill", choices: [{ type: "skill", value: "negotiation" }] },
      { id: "edge-pool", choices: [{ type: "edgePool", value: "rumor" }] }
    ]
  },
  {
    id: "faction-lyran-commonwealth",
    label: "Lyran Commonwealth",
    moduleType: "faction",
    grants: [
      { id: "skill", choices: [{ type: "skill", value: "administration" }] },
      { id: "edge-pool", choices: [{ type: "edgePool", value: "legend" }] }
    ]
  },
  {
    id: "faction-taurian-concordat",
    label: "Taurian Concordat",
    moduleType: "faction",
    grants: [
      { id: "skill", choices: [{ type: "skill", value: "survival" }] },
      { id: "edge-pool", choices: [{ type: "edgePool", value: "grit" }] }
    ]
  },
  {
    id: "faction-magistracy-of-canopus",
    label: "Magistracy of Canopus",
    moduleType: "faction",
    grants: [
      { id: "edge-pool", choices: [{ type: "edgePool", value: "chaos" }] },
      { id: "skill", choices: [{ type: "skill", value: "medTech" }] }
    ]
  },
  {
    id: "faction-outworlds-alliance",
    label: "Outworlds Alliance",
    moduleType: "faction",
    grants: [
      { id: "edge-pool", choices: [{ type: "edgePool", value: "insight" }] },
      { id: "skill", choices: [{ type: "skill", value: "navigation" }] }
    ]
  },
  {
    id: "faction-pirate",
    label: "Pirate",
    moduleType: "faction",
    grants: [
      { id: "skill", choices: [{ type: "skill", value: "streetwise" }] },
      { id: "edge-pool", choices: [{ type: "edgePool", value: "chaos" }] }
    ]
  },
  {
    id: "faction-comstar",
    label: "ComStar",
    moduleType: "faction",
    grants: [
      { id: "skill", choices: [{ type: "skill", value: "systemOps" }] },
      { id: "edge-pool", choices: [{ type: "edgePool", value: "rumor" }] }
    ]
  },
  {
    id: "faction-mercenary",
    label: "Mercenary",
    moduleType: "faction",
    grants: [
      { id: "skill", choices: [{ type: "skill", value: "tactics" }] },
      { id: "edge-pool", choices: [{ type: "edgePool", value: "grit" }] }
    ]
  },
  {
    id: "faction-unaffiliated",
    label: "Unaffiliated",
    moduleType: "faction",
    grants: [{
      id: "choice",
      choices: [
        ...F$.map((t) => ({ type: "skill", value: t })),
        ...qw.map((t) => ({ type: "edgePool", value: t }))
      ]
    }]
  },
  { id: "childhood-backwoods", label: "Backwoods", moduleType: "childhood", skillChoices: ["tracking", "projectileWeapons"] },
  { id: "childhood-blue-collar", label: "Blue Collar", moduleType: "childhood", skillChoices: ["art", "technician", "zeroGOps"] },
  { id: "childhood-mercenary-brat", label: "Mercenary Brat", moduleType: "childhood", skillChoices: ["firearms"] },
  { id: "childhood-farm", label: "Farm", moduleType: "childhood", skillChoices: ["animalHandling"] },
  { id: "childhood-fugitives", label: "Fugitives", moduleType: "childhood", skillChoices: ["escapeArtist", "deception"] },
  { id: "childhood-nobility", label: "Nobility", moduleType: "childhood", skillChoices: ["etiquette"] },
  { id: "childhood-slave", label: "Slave", moduleType: "childhood", skillChoices: ["athletics"] },
  { id: "childhood-spacer-family", label: "Spacer Family", moduleType: "childhood", skillChoices: ["zeroGOps"] },
  { id: "childhood-street", label: "Street", moduleType: "childhood", skillChoices: ["streetwise"] },
  { id: "childhood-war-orphan", label: "War Orphan", moduleType: "childhood", skillChoices: ["survival"] },
  { id: "childhood-white-collar", label: "White Collar", moduleType: "childhood", skillChoices: ["etiquette"] },
  { id: "higher-education-technical-college", label: "Technical College", moduleType: "higherEducation", skillChoices: ["technician", "systemOps"] },
  { id: "higher-education-trade-school", label: "Trade School", moduleType: "higherEducation", skillChoices: ["computers", "administration"] },
  { id: "higher-education-university", label: "University", moduleType: "higherEducation", skillChoices: ["science", "medTech"] },
  { id: "higher-education-solaris-vii-internship", label: "Solaris VII Internship", moduleType: "higherEducation", skillChoices: ["streetwise"] },
  { id: "higher-education-police-academy", label: "Police Academy", moduleType: "higherEducation", skillChoices: ["negotiation", "perception"] },
  { id: "higher-education-intelligence-operative-training", label: "Intelligence Operative Training", moduleType: "higherEducation", skillChoices: ["intimidation", "tracking"] },
  { id: "higher-education-military-academy", label: "Military Academy", moduleType: "higherEducation", skillChoices: ["perception", "administration"] },
  { id: "higher-education-military-enlistment", label: "Military Enlistment", moduleType: "higherEducation", skillChoices: ["heavyWeapons", "artillery"] },
  { id: "higher-education-family-training", label: "Family Training", moduleType: "higherEducation", skillChoices: ["leadership", "etiquette"], requiresAny: ["childhood-nobility"] },
  { id: "higher-education-officer-candidate-school", label: "Officer Candidate School", moduleType: "higherEducation", skillChoices: ["tactics", "leadership"] },
  { id: "real-life-agitator", label: "Agitator", moduleType: "realLife", skillChoices: ["meleeCombat"] },
  { id: "real-life-civilian-job", label: "Civilian Job", moduleType: "realLife", skillChoices: ["art", "administration"] },
  { id: "real-life-combat-correspondent", label: "Combat Correspondent", moduleType: "realLife", skillChoices: ["perception"] },
  { id: "real-life-comstar-service", label: "ComStar Service", moduleType: "realLife", skillChoices: ["systemOps", "computers"] },
  { id: "real-life-covert-operations", label: "Covert Operations", moduleType: "realLife", skillChoices: ["stealth", "demolitions"] },
  { id: "real-life-explorer", label: "Explorer", moduleType: "realLife", skillChoices: ["navigation"] },
  { id: "real-life-guerrilla-insurgent", label: "Guerrilla Insurgent", moduleType: "realLife", skillChoices: ["stealth", "survival"] },
  { id: "real-life-merchant", label: "Merchant", moduleType: "realLife", skillChoices: ["negotiation"] },
  { id: "real-life-neer-do-well", label: "Ne'er-Do-Well", moduleType: "realLife", skillChoices: ["acting"] },
  { id: "real-life-organized-crime", label: "Organized Crime", moduleType: "realLife", skillChoices: ["intimidation"] },
  { id: "real-life-postgraduate-studies", label: "Postgraduate Studies", moduleType: "realLife", skillChoices: ["science", "technician"] },
  { id: "real-life-solaris-insider", label: "Solaris Insider", moduleType: "realLife", skillChoices: ["negotiation"] },
  { id: "real-life-solaris-vii-games", label: "Solaris VII Games", moduleType: "realLife", skillChoices: ["piloting", "gunnery"] },
  { id: "real-life-think-tank", label: "Think Tank", moduleType: "realLife", skillChoices: ["science", "tactics"] },
  {
    id: "real-life-tour-of-duty",
    label: "Tour of Duty",
    moduleType: "realLife",
    skillChoices: ["artillery", "gunnery", "meleeCombat", "piloting", "projectileWeapons", "firearms", "heavyWeapons"],
    requiresAny: [
      "higher-education-military-enlistment",
      "higher-education-military-academy",
      "higher-education-family-training",
      "higher-education-officer-candidate-school"
    ]
  },
  {
    id: "real-life-to-serve-and-protect",
    label: "To Serve and Protect",
    moduleType: "realLife",
    skillChoices: ["firearms", "intimidation"],
    requiresAny: ["higher-education-police-academy"]
  },
  {
    id: "real-life-vagabond",
    label: "Vagabond",
    moduleType: "realLife",
    skillChoices: tn.map((t) => t.code).filter((t) => !x$.has(t)),
    excludesAny: [
      "higher-education-military-enlistment",
      "higher-education-military-academy",
      "higher-education-family-training",
      "higher-education-officer-candidate-school"
    ]
  }
], { strict: !1 }));
function K$() {
  const t = /* @__PURE__ */ new Map();
  for (const e of tn) {
    const n = String(e.code ?? "").trim(), i = String(e.label ?? "").trim();
    n && (t.set(n.toLowerCase(), n), i && t.set(i.toLowerCase(), n));
  }
  for (const [e, n] of Object.entries($$))
    tn.some((i) => i.code === n) && t.set(e.toLowerCase(), n);
  return t;
}
function H$() {
  const t = /* @__PURE__ */ new Map();
  for (const [e, n] of Object.entries(ih))
    t.set(e.toLowerCase(), e), t.set(n.toLowerCase(), e), t.set(`${n.toLowerCase()} pool`, e);
  return t;
}
function j$(t = []) {
  const e = Array.isArray(t) ? t.filter(Boolean) : [String(t ?? "").trim()].filter(Boolean), n = new Error(e[0] ?? "Invalid life module data.");
  return n.validationErrors = e, n;
}
function Ww(t) {
  return Array.isArray(t) ? t : typeof t == "string" ? t.split(",").map((e) => e.trim()).filter(Boolean) : [];
}
function jl(t) {
  return String(t ?? "").trim().toLowerCase().replace(/['\u2019]/g, "").replace(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, "");
}
function Sd(t) {
  const e = String(t ?? "").trim();
  return D$.has(e) ? e : "";
}
function Ad(t) {
  const e = String(t ?? "").trim();
  return e ? _$.get(e.toLowerCase()) ?? "" : "";
}
function G$(t) {
  const e = String(t ?? "").trim();
  return e ? B$.get(e.toLowerCase()) ?? "" : "";
}
function q$(t, { strict: e = !1, errors: n = [], prefix: i = "Entry" } = {}) {
  const a = /* @__PURE__ */ new Set(), r = [];
  for (const s of Ww(t)) {
    const o = Ad(s);
    if (!o) {
      e && n.push(`${i}: unknown skill "${s}".`);
      continue;
    }
    a.has(o) || (a.add(o), r.push(o));
  }
  return r;
}
function py(t) {
  const e = /* @__PURE__ */ new Set();
  return Ww(t).map(jl).filter((n) => !n || e.has(n) ? !1 : (e.add(n), !0));
}
function hy(t = [], e = /* @__PURE__ */ new Map()) {
  return t.map((n) => {
    var i;
    return ((i = e.get(n)) == null ? void 0 : i.label) ?? n;
  });
}
function Gs(t = {}) {
  return `${t.type}:${t.value}`;
}
function W$(t) {
  var e;
  return ((e = Ft(t)) == null ? void 0 : e.label) ?? t;
}
function Vw(t) {
  return ih[t] ?? t;
}
function V$(t) {
  return L$[t] ?? t;
}
function J$(t = {}, { includeTypePrefix: e = !1 } = {}) {
  const n = String((t == null ? void 0 : t.type) ?? "").trim(), i = String((t == null ? void 0 : t.value) ?? "").trim();
  if (!n || !i) return "";
  const a = n === "skill" ? W$(i) : `${Vw(i)} Pool`;
  return e ? `${V$(n)}: ${a}` : a;
}
function pl(t = {}, { includeBonusText: e = !1, includeTypePrefix: n = !1 } = {}) {
  const i = J$(t, { includeTypePrefix: n });
  return i ? e ? t.type === "skill" ? `+1 ${i} rolls` : `+1 ${i}` : i : "";
}
function Y$(t) {
  const e = String(t ?? "").trim(), n = e.indexOf(":");
  return n < 0 ? null : {
    type: e.slice(0, n).trim(),
    value: e.slice(n + 1).trim()
  };
}
function Q$(t, { strict: e = !1, errors: n = [], prefix: i = "Entry", grantLabel: a = "Bonus" } = {}) {
  const r = typeof t == "string" ? Y$(t) : t, s = String((r == null ? void 0 : r.type) ?? "").trim(), o = String((r == null ? void 0 : r.value) ?? "").trim();
  if (!O$.has(s))
    return e && n.push(`${i} ${a}: unknown bonus type "${s || t}".`), null;
  const l = s === "skill" ? Ad(o) : G$(o);
  return l ? {
    type: s,
    value: l
  } : (e && n.push(`${i} ${a}: unknown ${s === "skill" ? "skill" : "edge pool"} "${o}".`), null);
}
function Rf(t, { strict: e = !1, errors: n = [], prefix: i = "Entry", grantLabel: a = "Bonus" } = {}) {
  const r = /* @__PURE__ */ new Set(), s = [], o = Array.isArray(t) ? t : [];
  for (const l of o) {
    const c = Q$(l, { strict: e, errors: n, prefix: i, grantLabel: a });
    if (!c) continue;
    const u = Gs(c);
    r.has(u) || (r.add(u), s.push(c));
  }
  return s;
}
function Jw(t, { strict: e = !1, errors: n = [], prefix: i = "Entry" } = {}) {
  const a = q$(t, { strict: e, errors: n, prefix: i });
  return a.length ? [{
    id: "skill",
    label: "",
    choices: a.map((r) => ({ type: "skill", value: r }))
  }] : [];
}
function X$(t, { strict: e = !1, errors: n = [], prefix: i = "Entry" } = {}) {
  const a = String(t ?? "").trim();
  return a ? a.split(";").map((s) => s.trim()).filter(Boolean).map((s, o) => {
    const l = `Bonus ${o + 1}`, c = Rf(
      s.split("|").map((u) => u.trim()).filter(Boolean),
      { strict: e, errors: n, prefix: i, grantLabel: l }
    );
    return {
      id: `grant-${o + 1}`,
      label: "",
      choices: c
    };
  }).filter((s) => s.choices.length) : [];
}
function Yw(t, e = "grant") {
  return jl(t) || e;
}
function Z$(t, e, { strict: n = !1, errors: i = [], prefix: a = "Entry" } = {}) {
  const r = `grant-${e + 1}`, s = `Bonus ${e + 1}`;
  if (typeof t == "string") {
    const u = Rf(
      t.split("|").map((d) => d.trim()).filter(Boolean),
      { strict: n, errors: i, prefix: a, grantLabel: s }
    );
    return u.length ? { id: r, label: "", choices: u } : null;
  }
  const o = Yw(t == null ? void 0 : t.id, r), l = String((t == null ? void 0 : t.label) ?? "").trim(), c = Rf(t == null ? void 0 : t.choices, { strict: n, errors: i, prefix: a, grantLabel: s });
  return c.length ? { id: o, label: l, choices: c } : (n && i.push(`${a} ${s}: define at least one bonus choice.`), null);
}
function e_(t, { strict: e = !1, errors: n = [], prefix: i = "Entry" } = {}) {
  if (Array.isArray(t)) {
    if (t.every((r) => typeof r == "string" && !String(r).includes(":")))
      return Jw(t, { strict: e, errors: n, prefix: i });
    const a = /* @__PURE__ */ new Set();
    return t.map((r, s) => Z$(r, s, { strict: e, errors: n, prefix: i })).filter((r) => r ? a.has(r.id) ? (e && n.push(`${i}: duplicate bonus id "${r.id}".`), !1) : (a.add(r.id), !0) : !1);
  }
  return typeof t == "string" ? X$(t, { strict: e, errors: n, prefix: i }) : [];
}
function t_(t = []) {
  return (Array.isArray(t) ? t : []).map((e) => (Array.isArray(e == null ? void 0 : e.choices) ? e.choices : []).map((i) => `${i.type}:${i.value}`).join("|")).filter(Boolean).join("; ");
}
function ah() {
  return foundry.utils.deepClone(U$);
}
function qs(t) {
  return R$[t] ?? (String(t ?? "").trim() || "Life Module");
}
function Qw() {
  return yd.map((t) => ({
    value: t.moduleType,
    label: t.label
  }));
}
function Yr(t = [], { strict: e = !1 } = {}) {
  const n = Array.isArray(t) ? t : [], i = [], a = /* @__PURE__ */ new Set(), r = n.map((o, l) => {
    const c = `Entry ${l + 1}`, u = String((o == null ? void 0 : o.label) ?? "").trim(), d = jl((o == null ? void 0 : o.id) ?? u), f = Sd(o == null ? void 0 : o.moduleType), m = (o == null ? void 0 : o.grants) != null ? e_(o.grants, { strict: e, errors: i, prefix: c }) : Jw(o == null ? void 0 : o.skillChoices, { strict: e, errors: i, prefix: c }), g = py(o == null ? void 0 : o.requiresAny), p = py(o == null ? void 0 : o.excludesAny);
    return !d && e && i.push(`${c}: id cannot be blank.`), !u && e && i.push(`${c}: label cannot be blank.`), !f && e && i.push(`${c}: choose a valid module type.`), !m.length && e && i.push(`${c}: choose at least one bonus.`), d && a.has(d) && e && i.push(`${c}: duplicate id "${d}".`), d && a.add(d), {
      id: d,
      label: u,
      moduleType: f,
      grants: m,
      requiresAny: g,
      excludesAny: p
    };
  }), s = new Map(r.map((o) => [o.id, o]));
  for (const o of r) {
    for (const l of o.requiresAny)
      l === o.id && e && i.push(`${o.label || o.id}: cannot require itself.`), !s.has(l) && e && i.push(`${o.label || o.id}: unknown requirement "${l}".`);
    for (const l of o.excludesAny)
      l === o.id && e && i.push(`${o.label || o.id}: cannot exclude itself.`), !s.has(l) && e && i.push(`${o.label || o.id}: unknown exclusion "${l}".`);
  }
  if (e && i.length) throw j$(i);
  return r.filter((o) => o.id && o.label && o.moduleType && o.grants.length).map((o) => ({
    id: o.id,
    label: o.label,
    moduleType: o.moduleType,
    grants: o.grants.map((l) => ({
      id: l.id,
      label: l.label,
      choices: l.choices.map((c) => ({
        type: c.type,
        value: c.value
      }))
    })),
    requiresAny: [...o.requiresAny],
    excludesAny: [...o.excludesAny]
  }));
}
function Xw(t = []) {
  const e = new Map(ah().map((r) => [r.id, r])), n = Yr(t, { strict: !1 }), i = [...n], a = new Set(n.map((r) => r.id));
  for (const [r, s] of e.entries())
    a.has(r) || i.push(foundry.utils.deepClone(s));
  return i;
}
async function n_() {
  var t, e, n;
  try {
    if (!((n = (e = (t = game == null ? void 0 : game.settings) == null ? void 0 : t.settings) == null ? void 0 : e.has) != null && n.call(e, `${I}.${Ps}`))) return;
    const i = game.settings.get(I, Ps), a = Xw(i);
    JSON.stringify(i) !== JSON.stringify(a) && await game.settings.set(I, Ps, a);
  } catch {
  }
}
function i_() {
  var t, e, n;
  try {
    if ((n = (e = (t = game == null ? void 0 : game.settings) == null ? void 0 : t.settings) == null ? void 0 : e.has) != null && n.call(e, `${I}.${Ps}`))
      return Xw(game.settings.get(I, Ps));
  } catch {
  }
  return ah();
}
function kd() {
  return Yr(i_(), { strict: !1 });
}
function Fa(t) {
  const e = jl(t);
  return e ? kd().find((n) => n.id === e) ?? null : null;
}
function rh(t) {
  const e = Sd(t);
  return kd().filter((n) => n.moduleType === e);
}
function Zw(t) {
  return !t || typeof t != "object" || Array.isArray(t) ? {} : Object.fromEntries(
    Object.entries(t).map(([e, n]) => [Yw(e, ""), String(n ?? "").trim()]).filter(([e]) => !!e)
  );
}
function e0(t, e = "", { legacySelectedSkill: n = "" } = {}) {
  const i = new Set((Array.isArray(t == null ? void 0 : t.choices) ? t.choices : []).map(Gs)), a = String(e ?? "").trim();
  if (i.has(a)) return a;
  if (n) {
    const r = Ad(n), s = r ? `skill:${r}` : "";
    if (s && i.has(s)) return s;
  }
  return i.size === 1 ? Array.from(i)[0] : "";
}
function t0(t, e = {}, { legacySelectedSkill: n = "" } = {}) {
  const i = Array.isArray(t == null ? void 0 : t.grants) ? t.grants : [], a = Zw(e);
  return Object.fromEntries(
    i.map((r) => [
      r.id,
      e0(r, a[r.id], { legacySelectedSkill: n })
    ])
  );
}
function Td(t, e = {}, { legacySelectedSkill: n = "" } = {}) {
  const i = Array.isArray(t == null ? void 0 : t.grants) ? t.grants : [], a = t0(t, e, { legacySelectedSkill: n });
  return i.map((r, s) => {
    const o = e0(r, a[r.id], { legacySelectedSkill: n }), l = (Array.isArray(r.choices) ? r.choices : []).find((c) => Gs(c) === o) ?? null;
    return {
      id: r.id,
      index: s,
      label: String((r == null ? void 0 : r.label) ?? "").trim() || (i.length > 1 ? `Bonus ${s + 1}` : "Granted Bonus"),
      selectedKey: o,
      choice: l,
      isResolved: !!l,
      requiresSelection: (Array.isArray(r == null ? void 0 : r.choices) ? r.choices : []).length > 1
    };
  });
}
function a_(t, e = {}, { legacySelectedSkill: n = "" } = {}) {
  var i;
  return ((i = Td(t, e, { legacySelectedSkill: n }).map((a) => a.choice).find((a) => (a == null ? void 0 : a.type) === "skill")) == null ? void 0 : i.value) ?? "";
}
function hl(t = {}) {
  const e = foundry.utils.deepClone(t ?? {}), n = jl(e.catalogId), i = n ? Fa(n) : null, a = Sd(e.moduleType || (i == null ? void 0 : i.moduleType)), r = i ? t0(i, e.selectedGrants, { legacySelectedSkill: e.selectedSkill }) : Zw(e.selectedGrants);
  return e.moduleType = a, e.catalogId = n, e.selectedGrants = r, e.selectedSkill = i ? a_(i, r, { legacySelectedSkill: e.selectedSkill }) : Ad(e.selectedSkill), e;
}
function n0(t, e = {}, { legacySelectedSkill: n = "" } = {}) {
  return Td(t, e, { legacySelectedSkill: n }).map((i) => {
    var l, c;
    const a = Array.isArray((c = (l = t == null ? void 0 : t.grants) == null ? void 0 : l[i.index]) == null ? void 0 : c.choices) ? t.grants[i.index].choices : [], r = new Set(a.map((u) => u.type)).size > 1, s = a.map((u) => ({
      value: Gs(u),
      label: pl(u, { includeTypePrefix: r }),
      selected: Gs(u) === i.selectedKey
    })), o = s.length === 1 ? {
      value: s[0].value,
      label: s[0].label,
      displayLabel: pl(a[0], { includeBonusText: !0 })
    } : null;
    return {
      id: i.id,
      label: i.label,
      selectionPath: `system.selectedGrants.${i.id}`,
      selectedKey: i.selectedKey,
      options: s,
      singleOption: o,
      hasMultipleChoices: s.length > 1
    };
  });
}
function r_(t, e) {
  return t.isDuplicate ? `Duplicate ${qs(t.moduleType)} slot item.` : t.catalog ? t.unresolvedGrantCount > 0 ? "Choose valid bonus options." : t.excludedBy.length ? `Blocked by ${hy(t.excludedBy, e).join(", ")}.` : t.requiresAny.length && !t.matchedRequirementIds.length ? `Requires ${hy(t.requiresAny, e).join(" or ")}.` : "" : "Catalog entry is missing or unlinked.";
}
function s_(t, e = [], n = {}) {
  var a, r, s;
  if (!t || !Array.isArray(e) || !e.length) return [];
  const i = Math.max(0, Number(((s = (r = (a = t.system) == null ? void 0 : a.attributes) == null ? void 0 : r.edge) == null ? void 0 : s.value) ?? 0));
  return e.filter((o) => (o == null ? void 0 : o.type) === "edgePool").map((o) => {
    var g, p, h, b;
    const l = String(o.value ?? "").trim(), c = Vw(l), u = Math.max(0, Number(((b = (h = (p = (g = t.system) == null ? void 0 : g.counters) == null ? void 0 : p.edgePools) == null ? void 0 : h[l]) == null ? void 0 : b.rating) ?? 0)), d = Math.max(0, Number((n == null ? void 0 : n[l]) ?? 0)), f = Math.max(0, u + d - i);
    return f ? `${c} Pool bonus loses ${f} ${f === 1 ? "point" : "points"} to the Edge cap.` : "";
  }).filter(Boolean);
}
function qa(t) {
  var f;
  const e = kd(), n = new Map(e.map((m) => [m.id, m])), i = Array.from((t == null ? void 0 : t.items) ?? []).filter((m) => m.type === M.itemType.lifeModule), a = /* @__PURE__ */ new Map();
  for (const m of i) {
    const g = Sd((f = m.system) == null ? void 0 : f.moduleType);
    !g || a.has(g) || a.set(g, m.id);
  }
  const r = i.map((m) => {
    var T;
    const g = hl(m.system ?? {}), p = n.get(g.catalogId) ?? null, h = g.moduleType || (p == null ? void 0 : p.moduleType) || "", b = p ? Td(p, g.selectedGrants, { legacySelectedSkill: g.selectedSkill }) : [], y = b.map((C) => C.choice).filter(Boolean), S = ((T = y.find((C) => C.type === "skill")) == null ? void 0 : T.value) ?? "", A = S ? Ft(S) : null;
    return {
      item: m,
      itemId: m.id,
      moduleType: h,
      catalogId: (p == null ? void 0 : p.id) ?? g.catalogId,
      catalog: p,
      label: (p == null ? void 0 : p.label) ?? m.name,
      selectedGrants: g.selectedGrants,
      resolvedGrants: b,
      unresolvedGrantCount: b.filter((C) => !C.isResolved).length,
      selectedChoices: y,
      selectedChoiceLabels: y.map((C) => pl(C, { includeBonusText: !0 })),
      selectedSkill: S,
      selectedSkillLabel: (A == null ? void 0 : A.label) ?? S,
      requiresAny: [...(p == null ? void 0 : p.requiresAny) ?? []],
      excludesAny: [...(p == null ? void 0 : p.excludesAny) ?? []],
      matchedRequirementIds: [],
      excludedBy: [],
      isDuplicate: h ? a.get(h) !== m.id : !1,
      isActive: !1,
      inactiveReason: "",
      bonus: 0
    };
  }), s = /* @__PURE__ */ new Map();
  for (const m of r) {
    if (!m.catalogId) continue;
    const g = s.get(m.catalogId) ?? [];
    g.push(m), s.set(m.catalogId, g);
  }
  for (const m of r)
    m.excludedBy = m.excludesAny.filter((g) => (s.get(g) ?? []).length > 0);
  let o = !0;
  for (; o; ) {
    o = !1;
    for (const m of r) {
      const g = m.requiresAny.filter(
        (h) => (s.get(h) ?? []).some((b) => b.isActive)
      ), p = !m.isDuplicate && !!m.catalog && m.unresolvedGrantCount === 0 && m.excludedBy.length === 0 && (m.requiresAny.length === 0 || g.length > 0);
      m.isActive !== p && (m.isActive = p, o = !0), m.matchedRequirementIds.join("|") !== g.join("|") && (m.matchedRequirementIds = g);
    }
  }
  const l = Object.fromEntries(tn.map((m) => [m.code, 0])), c = Object.fromEntries(qw.map((m) => [m, 0])), u = /* @__PURE__ */ new Map();
  for (const m of r) {
    const g = m.isActive ? m.selectedChoices : [], p = g.filter((b) => b.type === "skill"), h = g.filter((b) => b.type === "edgePool");
    m.bonus = p.length;
    for (const b of p)
      l[b.value] = Number(l[b.value] ?? 0) + 1;
    for (const b of h)
      c[b.value] = Number(c[b.value] ?? 0) + 1;
    m.inactiveReason = m.isActive ? "" : r_(m, n), u.set(m.itemId, m);
  }
  for (const m of r)
    m.warningLabels = m.isActive ? s_(t, m.selectedChoices, c) : [];
  const d = yd.map((m) => {
    const g = r.find((p) => p.moduleType === m.moduleType && !p.isDuplicate) ?? null;
    return {
      moduleType: m.moduleType,
      label: m.label,
      availableEntries: e.filter((p) => p.moduleType === m.moduleType),
      state: g
    };
  });
  return {
    catalog: e,
    states: r,
    stateByItemId: u,
    slotStates: d,
    bonusBySkill: l,
    bonusByEdgePool: c
  };
}
function o_(t = {}) {
  var n, i, a;
  const e = String((t == null ? void 0 : t.intent) ?? "").trim();
  return e === "skill" ? String(((n = t == null ? void 0 : t.data) == null ? void 0 : n.skillKey) ?? "").trim() : e === "attack" ? String(((a = (i = t == null ? void 0 : t.attack) == null ? void 0 : i.skill) == null ? void 0 : a.code) ?? "").trim() : "";
}
function l_({ actor: t, resolved: e } = {}) {
  const n = o_(e);
  return !t || !n ? [] : qa(t).states.flatMap(
    (i) => i.isActive ? i.selectedChoices.filter((a) => a.type === "skill" && a.value === n).map((a) => ({
      id: `life-module:${i.itemId}:${Gs(a)}`,
      label: i.label,
      value: 1,
      source: "Life Module",
      tooltip: `${i.label}: +1 to ${pl(a)} rolls`
    })) : []
  );
}
const i0 = "weaponPayloadFamilyCatalog", a0 = "weaponPayloadTagCatalog", r0 = Object.freeze([
  { key: "bullet", label: "Bullet", description: "General ballistic bullets and similar small ammunition." },
  { key: "smallArmsBallistic", label: "Small Arms Ballistic", description: "Broad family for conventional small arms payloads." },
  { key: "shotgunShell", label: "Shotgun Shell", description: "Shotgun shells and specialty shot payloads." },
  { key: "40mmGrenade", label: "40mm Grenade", description: "Grenade launcher payloads using a 40mm grenade profile." },
  { key: "handGrenade", label: "Hand Grenade", description: "Thrown grenade payloads." },
  { key: "rocket", label: "Rocket", description: "Unguided rocket payloads." },
  { key: "srmMissile", label: "SRM Missile", description: "Short-range missile payloads." },
  { key: "lrmMissile", label: "LRM Missile", description: "Long-range missile payloads." },
  { key: "mortarShell", label: "Mortar Shell", description: "Mortar or indirect shell payloads." }
]), s0 = Object.freeze([
  { key: "armorPiercing", label: "Armor Piercing", description: "Improves armor penetration or mitigation interaction." },
  { key: "frag", label: "Frag", description: "Fragmentation or anti-personnel effect." },
  { key: "smoke", label: "Smoke", description: "Creates smoke, concealment, or vision interference." },
  { key: "incendiary", label: "Incendiary", description: "Thermal, burning, or fire-starting effect." },
  { key: "emp", label: "EMP", description: "Electromagnetic disruption effect." },
  { key: "nonlethal", label: "Nonlethal", description: "Designed to incapacitate without lethal harm." },
  { key: "stun", label: "Stun", description: "Applies shock, stun, or temporary impairment." },
  { key: "thermal", label: "Thermal", description: "Thermal damage or heat-oriented effect." },
  { key: "electrical", label: "Electrical", description: "Electrical damage or disruption." },
  { key: "chemical", label: "Chemical", description: "Chemical payload or exposure effect." },
  { key: "gas", label: "Gas", description: "Gas cloud or inhaled exposure effect." },
  { key: "cluster", label: "Cluster", description: "Clustered, bomblet, or multi-impact payload." },
  { key: "tracer", label: "Tracer", description: "Tracer or marking payload." }
]);
function Lc(t) {
  return String(t ?? "").trim();
}
function Wi(t = [], { defaults: e = [] } = {}) {
  const n = Array.isArray(t) ? t : e, i = [], a = /* @__PURE__ */ new Set();
  for (const r of Array.isArray(n) ? n : []) {
    const s = Lc((r == null ? void 0 : r.key) ?? (r == null ? void 0 : r.id) ?? (r == null ? void 0 : r.value));
    if (!s) continue;
    const o = s.toLowerCase();
    a.has(o) || (a.add(o), i.push({
      key: s,
      label: Lc(r == null ? void 0 : r.label) || s,
      description: Lc(r == null ? void 0 : r.description)
    }));
  }
  return i;
}
function by() {
  return Wi(r0);
}
function yy() {
  return Wi(s0);
}
function o0(t, e) {
  var a, r, s;
  const n = (a = globalThis.game) == null ? void 0 : a.settings;
  return ((s = (r = n == null ? void 0 : n.settings) == null ? void 0 : r.has) == null ? void 0 : s.call(r, `${I}.${t}`)) ? Wi(n.get(I, t), { defaults: e }) : Wi(e);
}
function sh() {
  return o0(i0, r0);
}
function oh() {
  return o0(a0, s0);
}
function Au(t = []) {
  return Wi(t).map((e) => ({
    value: e.key,
    label: e.label === e.key ? e.key : `${e.label} (${e.key})`
  }));
}
function l0(t, e = []) {
  const n = Lc(t);
  if (!n) return "";
  const i = n.toLowerCase(), a = Wi(e).find(
    (r) => r.key.toLowerCase() === i || r.label.toLowerCase() === i
  );
  return (a == null ? void 0 : a.key) ?? n;
}
function c_(t) {
  return l0(t, sh());
}
function u_(t) {
  return l0(t, oh());
}
const lh = "weaponPayload";
function c0(t, { canonicalize: e = (n) => n } = {}) {
  return t && typeof t == "object" && !Array.isArray(t) ? Object.values(t).flatMap((n) => c0(n, { canonicalize: e })) : Array.isArray(t) ? t.map((n) => e(String(n ?? "").trim())).filter(Boolean) : String(t ?? "").split(",").map((n) => e(n.trim())).filter(Boolean);
}
function u0(t, e = {}) {
  return Array.from(new Set(c0(t, e)));
}
function ch(t) {
  return u0(t, { canonicalize: c_ });
}
function uh(t) {
  return u0(t, { canonicalize: u_ });
}
function vo(t = {}) {
  const e = t ?? {};
  return {
    families: ch(e.families ?? e.family),
    tagsAll: uh(e.tagsAll ?? e.requiredTags)
  };
}
function ku(t = {}, { name: e = "Payload" } = {}) {
  var r, s, o;
  const n = t ?? {}, i = String(((r = n.profile) == null ? void 0 : r.label) ?? n.label ?? e ?? "Payload").trim() || "Payload", a = on({
    id: "profile",
    label: i,
    ...n.profile ?? {}
  });
  return a.consumption = {
    ...a.consumption,
    amount: Math.max(1, Number(((s = a.consumption) == null ? void 0 : s.amount) ?? ((o = n.consumption) == null ? void 0 : o.amount) ?? 1) || 1),
    sourceId: ""
  }, {
    payloadKey: wt(n.payloadKey ?? i ?? e),
    families: ch(n.families ?? n.family),
    tags: uh(n.tags),
    quantity: Math.max(0, Math.trunc(Number(n.quantity ?? 1) || 0)),
    profile: a
  };
}
function us(t) {
  return ((t == null ? void 0 : t.canonicalType) ?? (t == null ? void 0 : t.type)) === lh;
}
function d_(t) {
  return t ? typeof t.values == "function" ? Array.from(t.values()) : Array.isArray(t) ? t : Array.isArray(t.contents) ? t.contents : [] : [];
}
function $c(t = {}, e = {}) {
  const n = vo(t), i = new Set(ch(e.families)), a = new Set(uh(e.tags));
  return !(!n.families.some((r) => i.has(r)) || n.tagsAll.some((r) => !a.has(r)));
}
function d0(t) {
  return String(t ?? "").trim() || null;
}
function Fr(t = {}) {
  const e = t && typeof t == "object" && !Array.isArray(t) ? t : {};
  return Object.entries(e).reduce((n, [i, a]) => {
    const r = wt(i);
    if (!r) return n;
    const s = a && typeof a == "object" ? a : { sourceId: a };
    return n[r] = {
      sourceId: d0(s.sourceId)
    }, n;
  }, {});
}
function m_(t, { weaponCompatibility: e = null, sourceAssignments: n = {} } = {}) {
  var o, l, c;
  if (!us(t)) return null;
  const i = ku(t.system ?? {}, { name: t.name });
  if (e && !$c(e, i)) return null;
  const a = wt(i.payloadKey);
  if (!a) return null;
  const r = d0((o = n[a]) == null ? void 0 : o.sourceId);
  return {
    ...on({
      ...i.profile,
      id: a,
      payloadKey: a,
      label: String(((l = i.profile) == null ? void 0 : l.label) ?? t.name ?? "Payload").trim() || "Payload",
      reserveQuantity: i.quantity,
      consumption: {
        ...((c = i.profile) == null ? void 0 : c.consumption) ?? {},
        sourceId: r ?? ""
      }
    }),
    sourceType: lh,
    itemId: String(t.id ?? "").trim(),
    itemUuid: String(t.uuid ?? t.id ?? "").trim(),
    payloadKey: a,
    reserveQuantity: i.quantity,
    families: i.families,
    tags: i.tags
  };
}
function Sy({ actor: t = null, weaponCompatibility: e = {}, sourceAssignments: n = {} } = {}) {
  const i = /* @__PURE__ */ new Map(), a = Fr(n);
  for (const r of d_(t == null ? void 0 : t.items)) {
    const s = m_(r, {
      weaponCompatibility: e,
      sourceAssignments: a
    });
    if (!s) continue;
    const o = s.payloadKey, l = i.get(o);
    if (!l) {
      i.set(o, {
        ...s,
        itemIds: s.itemId ? [s.itemId] : [],
        itemUuids: s.itemUuid ? [s.itemUuid] : []
      });
      continue;
    }
    l.reserveQuantity += s.reserveQuantity, s.itemId && l.itemIds.push(s.itemId), s.itemUuid && l.itemUuids.push(s.itemUuid);
  }
  return { payloads: Array.from(i.values()), consumptionSources: [] };
}
const wd = {
  Actor: {
    types: ["character", "npc", "vehicle", "battlemech"],
    templates: {
      description: {
        ownerId: "",
        description: "",
        gmnotes: "",
        favorites: [],
        state: {
          physical: { value: 0, max: 0 },
          fatigue: { value: 0, max: 0 }
        }
      },
      "attribute-reflexes": { attributes: { reflexes: { value: 1 } } },
      "attribute-strength": { attributes: { strength: { value: 1 } } },
      "attribute-guts": { attributes: { guts: { value: 1 } } },
      "attribute-charisma": { attributes: { charisma: { value: 1 } } },
      "attribute-intelligence": { attributes: { intelligence: { value: 1 } } },
      "attribute-edge": {
        attributes: { edge: { value: 1 } },
        counters: {
          edgePools: {
            grit: { value: null },
            insight: { value: null },
            rumor: { value: null },
            legend: { value: null },
            credibility: { value: null },
            chaos: { value: null }
          }
        }
      },
      "attribute-handling": { attributes: { handling: { value: 0 } } },
      "mwd-base": {
        mwd: {
          unitType: "vehicle",
          model: "",
          cbillCost: 0,
          heat: {
            current: 0,
            safeMax: 1,
            hardMax: 10,
            coolingImpaired: !1
          },
          shock: { value: 0 },
          reliabilitySpendable: { value: 0 },
          locations: {},
          crits: [],
          crew: {
            count: 1,
            effectiveCount: 1,
            injuryLevel: 0,
            bailedOut: !1
          },
          status: { state: "operational", reasons: [] },
          stealth: {
            enabled: !1,
            rating: 0,
            mode: "passive",
            revealedUntil: null,
            detectionCap: "",
            signature: "medium",
            counteredBy: ["activeProbe", "tag", "narc", "c3", "visualClose"],
            notes: ""
          },
          config: {
            critTargetNumber: 8,
            critOnSnakeEyes: !0,
            maxLocationStress: 3,
            heatBands: {
              safe: 1,
              runningHot: 2,
              overheated: 3,
              shutdown: 4
            }
          }
        }
      },
      "mwd-vehicle": {
        templates: ["mwd-base"],
        attributes: {
          handling: { value: 3 },
          system: { value: 3 },
          chassis: { value: 3 },
          reliability: { value: 3 }
        },
        mwd: {
          hardpoints: [],
          strain: {
            value: 0,
            pendingGenerated: 0,
            max: 6,
            thresholds: { strained: 2, overstressed: 4, critical: 6 }
          },
          movementProfile: "tracked",
          flightSubtype: "",
          favoredTerrain: ["open", "rough", "urban"],
          adverseTerrain: ["water"],
          affordances: ["stabilizedFire", "hullDown"],
          locations: {
            body: { enabled: !0, stress: 0, condition: 0, tags: ["crewCompartment", "engine", "ammoStore"], destroyed: !1 },
            turret: { enabled: !0, stress: 0, condition: 0, tags: ["turret", "weaponGroup"], destroyed: !1 },
            mobility: { enabled: !0, stress: 0, condition: 0, tags: ["motiveSystem", "rotor"], destroyed: !1 }
          }
        },
        movement: { ground: 0, flight: 0 }
      },
      "mwd-battlemech": {
        templates: ["mwd-base"],
        attributes: {
          handling: { value: 4 },
          system: { value: 3 },
          chassis: { value: 4 },
          reliability: { value: 3 }
        },
        mwd: {
          unitType: "mech",
          locations: {
            head: { enabled: !0, stress: 0, condition: 0, tags: ["cockpit", "sensor"], destroyed: !1 },
            torso: { enabled: !0, stress: 0, condition: 0, tags: ["weaponGroup", "engine", "gyro", "ammoStore"], destroyed: !1 },
            arms: { enabled: !0, stress: 0, condition: 0, tags: ["weaponGroup"], destroyed: !1 },
            legs: { enabled: !0, stress: 0, condition: 0, tags: ["motiveSystem"], destroyed: !1 }
          },
          crew: {
            count: 1,
            effectiveCount: 1,
            injuryLevel: 0,
            bailedOut: !1
          },
          heat: {
            current: 0,
            max: 10,
            thresholds: { runningHot: 2, overheated: 3, shutdown: 4 }
          },
          chassis: "",
          tonnage: 0,
          weightClass: "medium",
          fireMode: "alphaStrike",
          hardpoints: [],
          weaponGroups: [],
          melee: {
            baseProfile: { name: "Unarmed", damage: "", notes: "" },
            maxWeapons: 0,
            allowedLocations: []
          }
        }
      },
      "machine-movement-battlemech": {
        movement: { ground: 0, flight: 0 }
      }
    },
    character: {
      templates: [
        "description",
        "counters",
        "ownership",
        "attribute-reflexes",
        "attribute-strength",
        "attribute-guts",
        "attribute-intelligence",
        "attribute-charisma",
        "attribute-edge"
      ],
      monitors: {
        physical: { value: 1, max: 10, resistance: { default: 0, byType: {} } },
        fatigue: { value: 1, max: 10, resistance: { default: 0, byType: {} } },
        armor: { label: "Armor", value: 9, max: 9, effect: "", resistance: "" }
      },
      counters: {
        xp: { value: 0, total: 0 },
        edgePools: {
          grit: { value: 2, rating: 2 },
          insight: { value: 0, rating: 1 },
          rumor: { value: 1, rating: 1 },
          legend: { value: 0, rating: 1 },
          credibility: { value: 1, rating: 1 },
          chaos: { value: 1, rating: 1 }
        }
      },
      prototypeToken: {
        actorLink: !0,
        disposition: 1,
        displayName: 20,
        displayBars: 40
      },
      style: "",
      speed: 12,
      traitMods: {
        speedMod: 0,
        defenseRatingMod: 0,
        saCapMod: 0,
        faCapMod: 0,
        raCapMod: 0,
        conditionPhysicalValueMod: 0,
        conditionFatigueValueMod: 0,
        conditionPhysicalPenaltyMod: 0,
        conditionFatiguePenaltyMod: 0,
        attackRatingMod: 0,
        suppressAttackerMotionDN: 0,
        overloadDNMod: 0,
        overloadThresholdMod: 0
      },
      keywords: [],
      dispositions: [],
      cues: [],
      knowledgeSkills: [],
      burn: { value: 0, overloaded: !1 },
      criticals: [],
      biography: {
        faction: "",
        age: 0,
        rank: "",
        height: 0,
        weight: 0,
        history: "",
        experienceLevel: "green"
      }
    },
    npc: {
      templates: ["description", "ownership"],
      attributes: {
        strength: { value: 1 },
        reflexes: { value: 1 },
        intelligence: { value: 1 },
        guts: { value: 1 },
        charisma: { value: 1 },
        edge: { value: 1 }
      },
      monitors: {
        physical: { value: 0, max: 10, resistance: { default: 0, byType: {} } },
        fatigue: { value: 0, max: 10, resistance: { default: 0, byType: {} } },
        armor: { label: "Armor", value: 0, max: 9, effect: "", resistance: "" }
      },
      role: "",
      biography: "",
      burn: { value: 0, overloaded: !1 },
      criticals: [],
      traitMods: {
        speedMod: 0,
        defenseRatingMod: 0,
        saCapMod: 0,
        faCapMod: 0,
        raCapMod: 0,
        conditionPhysicalValueMod: 0,
        conditionFatigueValueMod: 0,
        conditionPhysicalPenaltyMod: 0,
        conditionFatiguePenaltyMod: 0,
        attackRatingMod: 0,
        suppressAttackerMotionDN: 0,
        overloadDNMod: 0,
        overloadThresholdMod: 0
      },
      style: "sra-enhanced"
    },
    vehicle: {
      templates: ["description", "matrix-monitor", "mwd-vehicle"],
      attributes: {},
      monitors: {
        structure: { value: 15, max: 15, resistance: { default: 2, byType: {} } },
        armor: { value: 12, max: 12, resistance: { default: 1, byType: {} } }
      },
      weaponGroups: [],
      meleeProfiles: [],
      skills: {},
      moves: 0,
      attacks: 0,
      stealth: 0,
      category: "",
      skill: "piloting",
      passengers: 4,
      pilot: { uuid: "" },
      crew: ""
    },
    battlemech: {
      templates: ["description", "mwd-battlemech"],
      attributes: {},
      monitors: {
        structure: { value: 18, max: 18, resistance: { default: 1, byType: {} } },
        armor: { value: 15, max: 15, resistance: { default: 1, byType: {} } },
        heat: { value: 0, max: 10, resistance: { default: 0, byType: {} } }
      },
      hybrid: {
        heat: { dissipation: 1 },
        criticals: { value: 0, max: 4, notes: "" },
        locations: { front: "", sides: "", rear: "", core: "" }
      },
      weaponGroups: [],
      meleeProfiles: [],
      skills: {},
      moves: 0,
      attacks: 0,
      stealth: 0,
      category: "mech",
      skill: "gunnery",
      passengers: 1,
      pilot: { uuid: "" },
      crew: ""
    }
  },
  Item: {
    types: [
      "contact",
      "gear",
      "consumable",
      "quality",
      "assetModule",
      "skill",
      "lifeModule",
      "mechWeapon",
      "personalWeapon",
      "weaponPayload",
      "armor",
      "mechEquipment",
      "vehicleUpgrade"
    ],
    templates: {
      modifiers: { modifiers: [] },
      inactive: { inactive: !1 },
      references: { sourceReference: "", description: "", gmnotes: "" }
    },
    skill: {
      templates: ["inactive", "references"],
      code: "",
      attribute: "knowledge",
      value: 0,
      hasDrain: !1,
      hasConvergence: !1,
      isSocial: !1
    },
    quality: {
      templates: ["modifiers", "inactive", "references"],
      positive: !0,
      category: "positive",
      tier: "minor",
      tags: [],
      activation: "passive",
      effects: [],
      prerequisites: [],
      limits: { perActivation: 0, perRound: 0, perScene: 0 }
    },
    assetModule: {
      templates: ["modifiers", "inactive", "references"],
      installClass: "module",
      category: "special",
      level: 1,
      activation: {
        mode: "passive",
        active: !1,
        selectedMode: "",
        cooldownUntilRound: 0
      },
      effects: [],
      mobility: {
        jumping: {
          enabled: !1,
          movement: 0,
          heat: 0,
          attackRatingBonus: 0,
          defenseRatingBonus: 0,
          dfaEnabled: !1
        }
      },
      targeting: {
        stealthProfile: {
          ratingBonus: 0,
          tags: [],
          requiresActiveMode: !1
        }
      }
    },
    lifeModule: {
      templates: ["inactive", "references"],
      moduleType: "faction"
    },
    mechWeapon: {
      templates: ["modifiers", "inactive", "references"],
      category: "ranged",
      weaponCategory: "ranged",
      skill: "gunnery",
      size: "small",
      damage: 0,
      clusteringDice: 0,
      clusteringTargetNumber: 5,
      ap: 0,
      damageType: "energy",
      attackRatingBand: { close: 0, near: 0, far: 0, extreme: 0 },
      payloads: [],
      selectedPayloadId: "",
      selectedPayloadKey: "",
      payloadCompatibility: {
        families: [],
        tagsAll: []
      },
      payloadSourceAssignments: {},
      consumptionSources: [],
      fireControl: {
        usesPerActivation: 1
      },
      keywords: [],
      heat: 0,
      volatile: !1,
      area: "none",
      notes: "",
      range: { max: "close", close: 0, near: 0, far: 0, extreme: 0 }
    },
    personalWeapon: {
      templates: ["modifiers", "inactive", "references"],
      equipped: !1,
      isPrimary: !1,
      scale: "personal",
      mount: { mountedOnItemId: "", mountType: "" },
      category: "ranged",
      skill: "firearms",
      damage: 0,
      damageAttribute: "",
      damageAttributeScale: 1,
      ap: 0,
      damageType: "penetrating",
      attackRatingBand: { close: 0, near: 0, far: 0, extreme: 0 },
      range: { max: "extreme", close: 5, near: 26, far: 62, extreme: 120 },
      standardTraits: [],
      availability: "",
      payloadCompatibility: {
        families: [],
        tagsAll: []
      },
      selectedPayloadUuid: "",
      selectedPayloadKey: "",
      payloadSourceAssignments: {},
      ammo: {
        current: 0,
        max: 0,
        consumePerAttack: 1,
        activeTypeId: "",
        types: []
      },
      traits: [],
      notes: ""
    },
    weaponPayload: {
      templates: ["inactive", "references"],
      payloadKey: "",
      families: [],
      tags: [],
      quantity: 1,
      profile: {
        id: "profile",
        label: "Payload",
        compatibleWith: [],
        modifies: {
          damage: 0,
          damageType: "",
          ap: 0,
          clusteringDice: 0,
          attackRatingBand: { close: 0, near: 0, far: 0, extreme: 0 }
        },
        traits: [],
        keywords: [],
        template: null,
        areaEffect: { kind: "none" },
        resolution: { resolverKey: "standard", damageModel: "", onHitEffect: null },
        consumption: { amount: 1, sourceId: "" }
      }
    },
    armor: {
      templates: ["modifiers", "inactive", "references"],
      equipped: !1,
      isPrimary: !1,
      rating: 0,
      defenseBonus: 0,
      mitigationByType: {
        penetrating: 0,
        concussive: 0,
        energy: 0,
        thermal: 0,
        electrical: 0
      },
      durability: { current: 0, max: 0 },
      standardTraits: [],
      traitState: { reinforced: { current: 0, max: 0 } },
      battleArmor: {
        enabled: !1,
        armorPool: { value: 0, max: 0 },
        structure: { value: 0, max: 0 },
        state: "wrecked",
        scale: "personal",
        systems: {
          stealth: {
            enabled: !1,
            trackingPenalty: 2,
            detectionStateCap: "track",
            revealedOnAttack: !0,
            revealedOnJump: !0,
            revealedOnHit: !0,
            counteredBy: ["activeProbe", "tag", "narc", "pointblank", "revealed"]
          },
          jump: !1,
          enhancedStrength: !1,
          sealed: !1,
          basicSensors: !1,
          medicalSuppression: !1,
          attachedEligible: !1
        },
        machineTargetProfile: {
          machineTargetable: !0,
          targetClass: "battleArmor",
          signature: "low",
          stealthTrackingPenalty: 0,
          detectionStateCap: null,
          counteredBy: []
        },
        attachedToTokenUuid: null,
        attachedLocationHint: "",
        revealedUntil: null
      },
      availability: "",
      tags: [],
      traits: [],
      notes: ""
    },
    gear: {
      templates: ["inactive", "references"],
      quantity: 1,
      rating: 0,
      category: "",
      relatedSkill: "",
      availability: "",
      rulesHook: "",
      mount: { mountedOnItemId: "", mountType: "" },
      tags: []
    },
    consumable: {
      templates: ["inactive", "references"],
      quantity: 1,
      rating: 0,
      category: "ammo",
      relatedSkill: "",
      availability: "",
      rulesHook: "",
      tags: []
    },
    contact: {
      templates: ["inactive", "references"]
    },
    mechEquipment: {
      templates: ["modifiers", "inactive", "references"],
      category: "",
      ammo: { current: 0, max: 0 },
      state: { suppressed: !1, offline: !1, destroyed: !1, reason: "" },
      effects: [],
      notes: ""
    },
    vehicleUpgrade: {
      templates: ["modifiers", "inactive", "references"],
      category: "",
      state: { suppressed: !1, offline: !1, destroyed: !1, reason: "" },
      effects: [],
      notes: ""
    }
  }
}, Ay = Object.freeze({
  Actor: /* @__PURE__ */ new Set(["prototypeToken"]),
  Item: /* @__PURE__ */ new Set()
});
function bl(t) {
  return !!t && typeof t == "object" && !Array.isArray(t);
}
function Ws(t) {
  return typeof structuredClone == "function" ? structuredClone(t) : JSON.parse(JSON.stringify(t));
}
function yl(t = {}, e = {}) {
  const n = Ws(t);
  for (const [i, a] of Object.entries(e ?? {})) {
    if (bl(a) && bl(n[i])) {
      n[i] = yl(n[i], a);
      continue;
    }
    n[i] = Ws(a);
  }
  return n;
}
function m0(t = "", e = wd) {
  const n = e == null ? void 0 : e[t];
  return bl(n) ? n : {};
}
function f0(t = wd, e = "", n = "", i = /* @__PURE__ */ new Set()) {
  var c;
  const a = String(n ?? "").trim();
  if (!a || i.has(a)) return {};
  const r = m0(e, t), s = (c = r == null ? void 0 : r.templates) == null ? void 0 : c[a];
  if (!bl(s)) return {};
  i.add(a);
  let o = {};
  for (const u of Array.from(s.templates ?? []))
    o = yl(
      o,
      f0(t, e, u, i)
    );
  const l = Ws(s);
  return delete l.templates, yl(o, l);
}
function f_(t = wd, e = "", n = "") {
  const i = String(n ?? "").trim();
  if (!i) return {};
  const a = m0(e, t), r = a == null ? void 0 : a[i];
  if (!bl(r)) return {};
  let s = {};
  for (const l of Array.from(r.templates ?? []))
    s = yl(
      s,
      f0(t, e, l)
    );
  const o = Ws(r);
  return delete o.templates, yl(s, o);
}
function g_(t = "", e = "", n = wd) {
  const i = f_(n, t, e), a = Ay[t] ?? Ay.Item, r = { system: {} };
  for (const [s, o] of Object.entries(i))
    a.has(s) ? r[s] = Ws(o) : r.system[s] = Ws(o);
  return r;
}
async function g0(t = "", e = "") {
  return g_(t, e);
}
const p0 = Object.freeze({
  weapon: M.itemType.personalWeapon,
  vehicleWeapon: M.itemType.mechWeapon,
  shadowamp: M.itemType.assetModule
}), p_ = Object.freeze({
  contact: "systems/mwd/img/default/mystery-man.svg",
  gear: "systems/mwd/img/default/Default_Gear.svg",
  consumable: "systems/mwd/img/default/Default_Gear.svg",
  quality: "systems/mwd/img/default/card-joker.svg",
  assetModule: "systems/mwd/img/default/upgrade.svg",
  skill: "systems/mwd/img/default/Default_Skill.svg",
  lifeModule: "systems/mwd/img/default/book.svg",
  mechWeapon: "systems/mwd/img/icons/systems/upgrades/grenade_launcher_underbarrel.svg",
  personalWeapon: "systems/mwd/img/colt-m1911.svg",
  weaponPayload: "systems/mwd/img/default/Default_Gear.svg",
  armor: "systems/mwd/img/default/Default_Armor.svg",
  mechEquipment: "systems/mwd/img/default/upgrade.svg",
  vehicleUpgrade: "systems/mwd/img/default/upgrade.svg"
});
function h0(t) {
  return p0[t] ?? t;
}
function h_(t) {
  return p_[h0(t)];
}
function b_(t) {
  return Object.prototype.hasOwnProperty.call(p0, t);
}
const Ur = Object.freeze(["close", "near", "far", "extreme"]);
function bn() {
  return foundry.data.operators.ForcedDeletion;
}
function y_(t, e) {
  const n = String(e ?? "").split(".").map((a) => a.trim()).filter(Boolean);
  if (!t || typeof t != "object" || n.length < 2) return t;
  let i = t;
  for (let a = 0; a < n.length - 1; a += 1) {
    const r = n[a], s = i == null ? void 0 : i[r];
    (!s || typeof s != "object" || Array.isArray(s)) && (i[r] = {}), i = i[r];
  }
  return t;
}
function S_(t, e) {
  const n = String(e ?? "").trim();
  if (!t || typeof t != "object" || !n) return !1;
  if (n.startsWith("areaEffect.hazard.")) {
    (!t.areaEffect || typeof t.areaEffect != "object" || Array.isArray(t.areaEffect)) && (t.areaEffect = {});
    const i = String(t.areaEffect.kind ?? "").trim().toLowerCase();
    if (i && i !== "persistent")
      return !1;
    t.areaEffect.kind = "persistent";
  }
  return y_(t, n), !0;
}
function mc(t) {
  return Jo(t);
}
function ky(t = {}) {
  const e = $A({
    traits: t.traits,
    keywords: t.keywords,
    report: Jg(),
    path: "system.traits"
  });
  return {
    traits: e.traits,
    keywords: e.keywords
  };
}
function dh(t) {
  return t === "long" ? "extreme" : t === "short" ? "close" : t === "medium" ? "near" : Ur.includes(t) ? t : "near";
}
function yr(t) {
  const e = jp(t);
  return e.max = dh(e.max ?? (t == null ? void 0 : t.max) ?? "extreme"), e;
}
function Lo(t) {
  return String(t ?? "").trim().toLowerCase() === "melee" ? "melee" : "ranged";
}
function fc(t, e = "") {
  return Lo(t) === "melee" ? "meleeCombat" : "gunnery";
}
function um(t, e) {
  return Lo(e) === "melee" ? {
    max: dh((t == null ? void 0 : t.max) ?? "close") === "near" ? "near" : "close",
    close: 0,
    near: 0,
    far: 0,
    extreme: 0
  } : yr(t);
}
function ho(t) {
  return {
    close: Number((t == null ? void 0 : t.close) ?? (t == null ? void 0 : t.short) ?? 0) || 0,
    near: Number((t == null ? void 0 : t.near) ?? (t == null ? void 0 : t.medium) ?? 0) || 0,
    far: Number((t == null ? void 0 : t.far) ?? (t == null ? void 0 : t.long) ?? 0) || 0,
    extreme: Number((t == null ? void 0 : t.extreme) ?? 0) || 0
  };
}
function Ty(t, e = 1) {
  const n = Number(t);
  return Number.isFinite(n) ? Math.max(0, Math.trunc(n)) : Math.max(0, Math.trunc(Number(e) || 0));
}
function wy(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? Math.max(0, Math.trunc(n)) : Math.max(0, Math.trunc(Number(e) || 0));
}
function My(t) {
  const e = String(t ?? "").trim();
  return e === "communications" ? "communication" : e;
}
function Ma(t) {
  return String(t ?? "").trim();
}
function gc(t) {
  return wt(t);
}
function vy(t = {}) {
  return {
    usesPerActivation: Math.max(1, Math.trunc(Number((t ?? {}).usesPerActivation ?? 1) || 1))
  };
}
function pc(t) {
  return Fr(t);
}
function A_(t = {}, e = "") {
  const n = Fr(t), i = wt(e);
  return i && delete n[i], n;
}
function k_(t = {}, e = "") {
  const n = String(e ?? "").trim(), i = Fr(t);
  return n && Object.entries(i).forEach(([a, r]) => {
    (r == null ? void 0 : r.sourceId) === n && delete i[a];
  }), i;
}
function b0(t) {
  return t ? typeof t.values == "function" ? Array.from(t.values()) : Array.isArray(t) ? t : Array.isArray(t.contents) ? t.contents : [] : [];
}
function y0(t) {
  return ((t == null ? void 0 : t.canonicalType) ?? (t == null ? void 0 : t.type)) === M.itemType.weaponPayload;
}
function S0(t) {
  var e, n, i;
  return wt(((e = t == null ? void 0 : t.system) == null ? void 0 : e.payloadKey) ?? ((i = (n = t == null ? void 0 : t.system) == null ? void 0 : n.profile) == null ? void 0 : i.label) ?? (t == null ? void 0 : t.name));
}
function A0(t, e = "") {
  const n = wt(e);
  return !t || !n ? [] : b0(t.items).filter((i) => y0(i) && S0(i) === n);
}
function T_(t, e = "") {
  return A0(t, e).reduce((n, i) => {
    var a;
    return n + Math.max(0, Math.trunc(Number(((a = i.system) == null ? void 0 : a.quantity) ?? 0) || 0));
  }, 0);
}
async function w_(t, e = "") {
  var a;
  const n = A0(t, e).find((r) => {
    var s;
    return Math.max(0, Math.trunc(Number(((s = r.system) == null ? void 0 : s.quantity) ?? 0) || 0)) > 0;
  });
  if (!n) return !1;
  const i = Math.max(0, Math.trunc(Number(((a = n.system) == null ? void 0 : a.quantity) ?? 0) || 0));
  return await n.update({ "system.quantity": Math.max(0, i - 1) }), !0;
}
function Cy(t) {
  return String(t ?? "").trim() || "ranged";
}
function dm(t) {
  const e = String(t ?? "").trim();
  return Object.values(M.actorAttributes ?? {}).includes(e) ? e : "";
}
function $o(t) {
  const e = Number(t);
  return Number.isFinite(e) ? e : 1;
}
function Iy(t) {
  return String(t ?? "").trim();
}
function Ey(t) {
  return (Array.isArray(t) ? t : typeof t == "string" ? t.split(",") : []).map((n) => String(n ?? "").trim()).filter(Boolean);
}
function bo(t) {
  return dl(t);
}
function M_(t) {
  const e = Ur.indexOf(t);
  return e >= 0 ? e : Ur.indexOf("near");
}
function v_(t = yr({})) {
  const e = ["near", "close", "far", "extreme"], n = M_(t.max);
  return e.find((i) => Ur.indexOf(i) <= n) ?? "close";
}
function C_(t) {
  const e = dh(t == null ? void 0 : t.max), n = Ur.indexOf(e);
  return Ur.map((i, a) => ({
    key: i,
    allowed: n >= 0 ? a <= n : a === 0,
    value: (t == null ? void 0 : t[i]) ?? void 0,
    labelkey: pu(i)
  }));
}
function I_(t, e, n, i, a = 1) {
  let r = Number(e);
  if (n)
    if (i !== void 0)
      r += Math.ceil(Number(i) * $o(a));
    else
      return console.warn("Weapon not attached to an actor"), z.item.personalWeapon.weaponWithoutActor;
  return r;
}
function E_(t, e, n, i = 1) {
  let a = "";
  if (n && z.attributes[n]) {
    const r = z.attributes[n].substring(0, 3).toUpperCase(), s = $o(i);
    a += s === 1 ? `${r} + ` : `${r}*${s} + `;
  }
  return a += String(e), a;
}
function N_(t, e) {
  return _e.useArmor(t) ? e ? "noArmor" : "withArmor" : "";
}
function Ny(t) {
  const e = game.system.mwd.skills.get(t);
  if (!e)
    return {
      img: DEFAULT_ITEM_ICONS.skill,
      system: {
        code: t,
        attribute: ""
      }
    };
  const n = {
    img: e.icon,
    system: {
      code: e.code,
      attribute: e.attribute
    }
  };
  return e.code !== "knowledge" && (n.name = e.label), n;
}
function mm(t = {}) {
  const e = hl(t), n = Fa(e.catalogId);
  return {
    system: e,
    ...n ? { name: n.label } : {}
  };
}
function P_(t) {
  const e = String(t ?? "").trim();
  return !!(!e || e.startsWith("icons/svg/") || e === "icons/mystery-man.svg");
}
var Pl, Zn, Df, k0, _c, T0;
const rn = class rn extends Item {
  static init() {
    X(this, Pl) || (De(this, Pl, !0), Hooks.on("createItem", (e, n, i) => {
      var a, r;
      Promise.resolve((a = e.onCreateItem) == null ? void 0 : a.call(e, n, i)).catch((s) => {
        console.error(`${tt}Item create hook failed`, s);
      }), E(r = rn, Zn, Df).call(r, e);
    }), Hooks.on("updateItem", (e) => {
      var n;
      E(n = rn, Zn, Df).call(n, e);
    }), Hooks.on("deleteItem", (e) => {
      var n, i;
      E(n = rn, Zn, k0).call(n, e), E(i = rn, Zn, T0).call(i, e);
    }), Hooks.on("createActiveEffect", (e) => {
      var n;
      E(n = rn, Zn, _c).call(n, e);
    }), Hooks.on("updateActiveEffect", (e) => {
      var n;
      E(n = rn, Zn, _c).call(n, e);
    }), Hooks.on("deleteActiveEffect", (e) => {
      var n;
      E(n = rn, Zn, _c).call(n, e);
    }));
  }
  static canonicalType(e) {
    return h0(e);
  }
  static defaultIconForType(e) {
    return h_(e);
  }
  get canonicalType() {
    return this.constructor.canonicalType(this.type);
  }
  async onCreateItem(e, n) {
  }
  async _preCreate(e, n, i) {
    super._preCreate && await super._preCreate(e, n, i);
    const a = (e == null ? void 0 : e.type) ?? this.type, r = this.constructor.canonicalType(a), s = {}, o = await g0("Item", r);
    if (o.system && Object.keys(o.system).length && (s.system = foundry.utils.mergeObject(
      foundry.utils.deepClone(o.system),
      foundry.utils.deepClone((e == null ? void 0 : e.system) ?? this.system ?? {}),
      { inplace: !1, recursive: !0, overwrite: !0 }
    )), a !== r && b_(a) && (s.type = r), P_((e == null ? void 0 : e.img) ?? this.img)) {
      const l = this.constructor.defaultIconForType(r);
      l && (s.img = l);
    }
    if (r === M.itemType.lifeModule && (!(e != null && e.name) || e.name === "DOCUMENT.Item") && (s.name = "MWD.itemType.singular.lifeModule"), r === M.itemType.lifeModule) {
      const l = mm(s.system ?? (e == null ? void 0 : e.system) ?? this.system ?? {});
      s.system = l.system, l.name && (!(e != null && e.name) || e.name === "DOCUMENT.Item") && (s.name = l.name);
    }
    Object.keys(s).length && this.updateSource(s);
  }
  async _preUpdate(e, n, i) {
    var l, c, u, d, f, m, g, p, h, b, y;
    super._preUpdate && await super._preUpdate(e, n, i);
    const a = e != null && e.system ? foundry.utils.mergeObject(foundry.utils.deepClone(this.system ?? {}), foundry.utils.deepClone(e.system), { inplace: !1 }) : null;
    if (a && this.isPersonalWeapon()) {
      e.system ?? (e.system = {});
      const S = a.ammo, A = ky(a);
      e.system.category = Cy(a.category ?? a.weaponCategory), e.system.payloadCompatibility = vo(a.payloadCompatibility), e.system.selectedPayloadUuid = String(a.selectedPayloadUuid ?? "").trim(), e.system.selectedPayloadKey = gc(a.selectedPayloadKey), e.system.payloadSourceAssignments = pc(a.payloadSourceAssignments), e.system.standardTraits = Di(a.standardTraits), e.system.payloads = sa(a.payloads, { legacyAmmo: S, category: a.category }), e.system.consumptionSources = Ia(a.consumptionSources, { legacyAmmo: S }), e.system.selectedPayloadId = Oo(
        a.selectedPayloadId,
        e.system.payloads,
        { legacyAmmo: S, category: a.category }
      ), e.system.traits = A.traits, e.system.keywords = A.keywords, e.system.resolution = Mc(a.resolution, "standard"), e.system.fireModes = As(a.fireModes), e.system.attackRatingBand = ho(a.attackRatingBand), e.system.range = yr(a.range), e.system.damageType = Xn(a.damageType), e.system.damageAttribute = dm(a.damageAttribute), e.system.damageAttributeScale = $o(a.damageAttributeScale), e.system.scale = Is(a.scale, "personal"), e.system.mount = bo(a.mount), e.system.availability = Ma(a.availability), e.system.ammo = bn();
    }
    if (a && this.isWeaponPayload()) {
      e.system ?? (e.system = {});
      const S = ku(a, { name: this.name });
      e.system.payloadKey = S.payloadKey, e.system.families = S.families, e.system.tags = S.tags, e.system.quantity = S.quantity, e.system.profile = S.profile;
    }
    if (a && this.isMechWeapon()) {
      e.system ?? (e.system = {});
      const S = a.ammo, A = Lo(a.weaponCategory ?? a.category), T = Bb({ ...a, category: A, weaponCategory: A }, {
        idFactory: () => foundry.utils.randomID()
      });
      e.system.category = A, e.system.weaponCategory = A, e.system.skill = fc(A, a.skill), e.system.size = Sa(a.size ?? a.hardpointSize ?? "small"), e.system.ap = Number(a.ap ?? a.armorPiercing ?? 0) || 0, e.system.damage = Math.max(0, Number(a.damage ?? 0) || 0), e.system.damageType = T.damageType, e.system.attackRatingBand = ho(a.attackRatingBand), e.system.range = um(a.range, A), e.system.payloadCompatibility = vo(a.payloadCompatibility), e.system.selectedPayloadKey = gc(a.selectedPayloadKey), e.system.payloadSourceAssignments = pc(a.payloadSourceAssignments), e.system.payloads = T.payloads, e.system.consumptionSources = Ia(a.consumptionSources, { legacyAmmo: S }), e.system.selectedPayloadId = T.selectedPayloadId, e.system.fireControl = vy(a.fireControl), e.system.keywords = jd(a.keywords), e.system.heat = Math.max(0, Number(a.heat ?? 0) || 0), e.system.area = String(a.area ?? "none").trim() || "none", e.system.volatile = !!a.volatile, e.system.ammo = bn(), e.system.hardpointId = bn(), e.system.hardpointType = bn(), e.system.hardpointSize = bn(), e.system.mountLocation = bn(), e.system.damageAttribute = bn(), e.system.monitor = bn(), e.system.noArmor = bn(), e.system.armorAvoidance = bn(), e.system.defense = bn();
    }
    if (a && this.isArmor()) {
      e.system ?? (e.system = {});
      const S = Object.prototype.hasOwnProperty.call(e.system, "rating"), A = Object.prototype.hasOwnProperty.call(((l = e.system) == null ? void 0 : l.durability) ?? {}, "current"), T = Math.max(0, Number(a.rating ?? 0) || 0);
      e.system.rating = T, e.system.durability = qd(T, a.durability, {
        previousRating: (c = this.system) == null ? void 0 : c.rating,
        previousDurability: (u = this.system) == null ? void 0 : u.durability,
        ratingChanged: S,
        currentChanged: A
      }), e.system.mitigationByType = Ki(a.mitigationByType ?? a.mitigation), e.system.availability = Iy(a.availability), e.system.tags = wc(a.tags), e.system.traits = mc(a.traits), e.system.standardTraits = na(a.standardTraits);
      const C = ((d = e.system) == null ? void 0 : d.battleArmor) ?? {}, k = Object.prototype.hasOwnProperty.call(C.structure ?? {}, "max"), w = Object.prototype.hasOwnProperty.call(C.structure ?? {}, "value"), v = Ai(a.battleArmor);
      v.structure = rw(
        (m = (f = a.battleArmor) == null ? void 0 : f.structure) == null ? void 0 : m.max,
        (g = a.battleArmor) == null ? void 0 : g.structure,
        {
          previousStructure: (h = (p = this.system) == null ? void 0 : p.battleArmor) == null ? void 0 : h.structure,
          maxChanged: k,
          valueChanged: w
        }
      ), e.system.battleArmor = v, e.system.traitState = Gd({
        standardTraits: e.system.standardTraits,
        traitState: a.traitState
      }).traitState;
    }
    if (a && this.isLifeModule()) {
      const S = mm(a);
      e.system ?? (e.system = {}), foundry.utils.mergeObject(e.system, S.system, { inplace: !0, overwrite: !0 }), S.name && (e.name = S.name);
      return;
    }
    if (a && this.isQuality()) {
      e.system ?? (e.system = {});
      const S = Yn(a);
      foundry.utils.mergeObject(e.system, S, { inplace: !0, overwrite: !0 });
      return;
    }
    if (a && this.canonicalType === M.itemType.assetModule) {
      e.system ?? (e.system = {});
      const S = Br(a);
      foundry.utils.mergeObject(e.system, S, { inplace: !0, overwrite: !0 });
      return;
    }
    if (a && this.isQuantityTrackedInventoryItem()) {
      e.system ?? (e.system = {}), e.system.quantity = Ty(a.quantity, 1), e.system.rating = wy(a.rating, 0), e.system.category = My(a.category), e.system.relatedSkill = Ma(a.relatedSkill), e.system.availability = Ma(a.availability), e.system.rulesHook = Ma(a.rulesHook), this.isGear() && (e.system.mount = bo(a.mount)), e.system.tags = Ey(a.tags);
      return;
    }
    if (!this.isSkill()) return;
    const r = (b = e == null ? void 0 : e.system) == null ? void 0 : b.code;
    if (r === void 0) return;
    const s = this.system.code;
    if (r === s) return;
    const o = Ny(r);
    o && ((y = o == null ? void 0 : o.system) == null || delete y.code, foundry.utils.mergeObject(e, o, { inplace: !0 }));
  }
  prepareBaseData() {
    super.prepareBaseData();
    const e = this.canonicalType;
    e === M.itemType.personalWeapon ? this._preparePersonalWeaponBaseData() : e === M.itemType.weaponPayload ? this._prepareWeaponPayloadBaseData() : e === M.itemType.mechWeapon ? this._prepareMechWeaponBaseData() : e === M.itemType.armor ? this._prepareArmorBaseData() : e === M.itemType.lifeModule ? this._prepareLifeModuleBaseData() : e === M.itemType.quality ? this._prepareQualityBaseData() : e === M.itemType.assetModule ? this._prepareAssetModuleBaseData() : [M.itemType.gear, M.itemType.consumable].includes(e) && this._prepareGearBaseData();
  }
  _prepareAssetModuleBaseData() {
    const e = this.system ?? {}, n = Br(e);
    Object.assign(e, n);
  }
  _preparePersonalWeaponBaseData() {
    const e = this.system ?? {}, n = e.ammo;
    e.equipped = !!e.equipped, e.isPrimary = !!e.isPrimary, e.category = Cy(e.category ?? e.weaponCategory), e.skill = String(e.skill ?? "firearms").trim() || "firearms", e.ap = Number(e.ap ?? e.armorPiercing ?? 0) || 0, e.damage = Number(e.damage ?? 0) || 0, e.damageAttribute = dm(e.damageAttribute), e.damageAttributeScale = $o(e.damageAttributeScale), e.damageType = Xn(e.damageType), e.attackRatingBand = ho(e.attackRatingBand), e.range = yr(e.range);
    const i = ky(e);
    e.standardTraits = Di(e.standardTraits), e.traits = i.traits, e.keywords = i.keywords, e.scale = Is(e.scale, "personal"), e.mount = bo(e.mount), e.resolution = Mc(e.resolution, "standard"), e.fireModes = As(e.fireModes), e.availability = Ma(e.availability), e.payloadCompatibility = vo(e.payloadCompatibility), e.selectedPayloadUuid = String(e.selectedPayloadUuid ?? "").trim(), e.selectedPayloadKey = gc(e.selectedPayloadKey), e.payloadSourceAssignments = pc(e.payloadSourceAssignments), e.payloads = sa(e.payloads, { legacyAmmo: n, category: e.category }), e.consumptionSources = Ia(e.consumptionSources, { legacyAmmo: n }), e.selectedPayloadId = Oo(e.selectedPayloadId, e.payloads, { legacyAmmo: n, category: e.category }), delete e.ammo, e.notes = String(e.notes ?? "").trim();
  }
  _prepareWeaponPayloadBaseData() {
    const e = this.system ?? {}, n = ku(e, { name: this.name });
    e.payloadKey = n.payloadKey, e.families = n.families, e.tags = n.tags, e.quantity = n.quantity, e.profile = n.profile;
  }
  _prepareMechWeaponBaseData() {
    const e = this.system ?? {}, n = e.ammo, i = Lo(e.weaponCategory ?? e.category);
    e.category = i, e.weaponCategory = i, e.skill = fc(i, e.skill), e.size = Sa(e.size ?? e.hardpointSize ?? "small"), e.ap = Number(e.ap ?? e.armorPiercing ?? 0) || 0, e.damage = Math.max(0, Number(e.damage ?? 0) || 0);
    const a = Bb({ ...e, category: i, weaponCategory: i });
    e.damageType = a.damageType, e.payloadCompatibility = vo(e.payloadCompatibility), e.selectedPayloadKey = gc(e.selectedPayloadKey), e.payloadSourceAssignments = pc(e.payloadSourceAssignments), e.attackRatingBand = ho(e.attackRatingBand), e.range = um(e.range, i), e.payloads = a.payloads, e.consumptionSources = Ia(e.consumptionSources, { legacyAmmo: n }), e.selectedPayloadId = a.selectedPayloadId, e.fireControl = vy(e.fireControl), e.keywords = jd(e.keywords), e.heat = Math.max(0, Number(e.heat ?? 0) || 0), e.area = String(e.area ?? "none").trim() || "none", e.volatile = !!e.volatile, e.notes = String(e.notes ?? "").trim(), delete e.ammo, delete e.hardpointId, delete e.hardpointType, delete e.hardpointSize, delete e.mountLocation, delete e.damageAttribute, delete e.monitor, delete e.noArmor, delete e.armorAvoidance, delete e.defense;
  }
  _prepareArmorBaseData() {
    const e = this.system ?? {};
    e.equipped = !!e.equipped, e.isPrimary = !!e.isPrimary, e.rating = Math.max(0, Number(e.rating ?? 0)), e.defenseBonus = Number(e.defenseBonus ?? 0) || 0, e.mitigationByType = Ki(e.mitigationByType ?? e.mitigation), delete e.mitigation, e.availability = Iy(e.availability), e.durability = qd(e.rating, e.durability), e.standardTraits = na(e.standardTraits), e.battleArmor = Ai(e.battleArmor), e.tags = wc(e.tags), e.traits = mc(e.traits), e.traitState = Gd({
      standardTraits: e.standardTraits,
      traitState: e.traitState
    }).traitState, e.notes = String(e.notes ?? "").trim();
  }
  _prepareLifeModuleBaseData() {
    const e = mm(this.system ?? {});
    foundry.utils.mergeObject(this.system, e.system, { inplace: !0, overwrite: !0 });
  }
  _prepareQualityBaseData() {
    const e = Yn(this.system ?? {});
    foundry.utils.mergeObject(this.system, e, { inplace: !0, overwrite: !0 });
  }
  _prepareGearBaseData() {
    const e = this.system ?? {};
    e.quantity = Ty(e.quantity, 1), e.rating = wy(e.rating, 0), e.category = My(e.category), e.relatedSkill = Ma(e.relatedSkill), e.availability = Ma(e.availability), e.rulesHook = Ma(e.rulesHook), this.isGear() && (e.mount = bo(e.mount)), e.tags = Ey(e.tags);
  }
  getAttributes() {
    return [];
  }
  getUsableAttributes() {
    return this.getAttributes();
  }
  getAttributeValue(e) {
    var n;
    return this.system.attributes ? ((n = this.system.attributes[e]) == null ? void 0 : n.value) ?? 0 : 0;
  }
  hasOwnAnarchy() {
    return !1;
  }
  hasGMAnarchy() {
    return !1;
  }
  async nextConnectionMode() {
  }
  async setCheckbarValue(e, n) {
    return this.update({ [e]: n });
  }
  isWeapon() {
    return [M.itemType.mechWeapon, M.itemType.personalWeapon].includes(this.canonicalType);
  }
  isPersonalWeapon() {
    return this.canonicalType === M.itemType.personalWeapon;
  }
  isMechWeapon() {
    return this.canonicalType === M.itemType.mechWeapon;
  }
  isArmor() {
    return this.canonicalType === M.itemType.armor;
  }
  isLifeModule() {
    return this.canonicalType === M.itemType.lifeModule;
  }
  isQuality() {
    return this.canonicalType === M.itemType.quality;
  }
  isGear() {
    return this.canonicalType === M.itemType.gear;
  }
  isConsumable() {
    return this.canonicalType === M.itemType.consumable;
  }
  isWeaponPayload() {
    return this.canonicalType === M.itemType.weaponPayload;
  }
  isQuantityTrackedInventoryItem() {
    return this.isGear() || this.isConsumable() || this.isWeaponPayload();
  }
  supportsEquippedEffectSync() {
    return this.isPersonalWeapon() || this.isArmor() || this.canonicalType === M.itemType.assetModule;
  }
  shouldApplyEquippedEffects() {
    var e;
    return !this.supportsEquippedEffectSync() || !this.actor ? !1 : this.canonicalType === M.itemType.assetModule ? no(this, { installed: !0 }).active : !!((e = this.system) != null && e.equipped);
  }
  getSyncedActorEffects({ actor: e = this.actor } = {}) {
    return e != null && e.effects ? e.effects.contents.filter((n) => {
      var a, r;
      const i = (r = (a = n.flags) == null ? void 0 : a[I]) == null ? void 0 : r[rn.EQUIPPED_EFFECT_FLAG];
      return (i == null ? void 0 : i.sourceItemId) === this.id;
    }) : [];
  }
  async removeSyncedActorEffects({ actor: e = this.actor } = {}) {
    const n = this.getSyncedActorEffects({ actor: e });
    return !n.length || !e ? [] : e.deleteEmbeddedDocuments("ActiveEffect", n.map((i) => i.id));
  }
  async syncEquippedActorEffects({ actor: e = this.actor } = {}) {
    var f, m, g, p;
    if (!e || !this.supportsEquippedEffectSync()) return { created: [], updated: [], deleted: [] };
    const n = this.getSyncedActorEffects({ actor: e }), i = Array.from(((f = this.effects) == null ? void 0 : f.contents) ?? []);
    if (!this.shouldApplyEquippedEffects()) {
      if (!n.length) return { created: [], updated: [], deleted: [] };
      const h = await e.deleteEmbeddedDocuments("ActiveEffect", n.map((b) => b.id));
      return { created: [], updated: [], deleted: h };
    }
    const a = /* @__PURE__ */ new Map();
    for (const h of n) {
      const b = (p = (g = (m = h.flags) == null ? void 0 : m[I]) == null ? void 0 : g[rn.EQUIPPED_EFFECT_FLAG]) == null ? void 0 : p.sourceEffectId;
      if (!b) continue;
      const y = a.get(b) ?? [];
      y.push(h), a.set(b, y);
    }
    const r = [], s = [], o = [], l = new Set(i.map((h) => h.id));
    for (const [h, b] of a.entries()) {
      if (!l.has(h)) {
        o.push(...b.map((y) => y.id));
        continue;
      }
      b.length > 1 && o.push(...b.slice(1).map((y) => y.id));
    }
    for (const h of i) {
      const y = (a.get(h.id) ?? [])[0] ?? null, S = this._prepareSyncedActorEffectData(h);
      y ? s.push({ _id: y.id, ...S }) : r.push(S);
    }
    const c = o.length ? await e.deleteEmbeddedDocuments("ActiveEffect", o) : [], u = s.length ? await e.updateEmbeddedDocuments("ActiveEffect", s) : [];
    return { created: r.length ? await e.createEmbeddedDocuments("ActiveEffect", r) : [], updated: u, deleted: c };
  }
  _prepareSyncedActorEffectData(e) {
    const n = e.toObject();
    delete n._id;
    const i = String(e.name ?? "Effect").trim() || "Effect", a = String(this.name ?? "Item").trim() || "Item", r = i.startsWith(a) ? i : `${a}: ${i}`;
    return n.name = r, n.transfer = !1, n.origin = e.uuid ?? this.uuid ?? n.origin ?? null, n.flags = foundry.utils.mergeObject(n.flags ?? {}, {
      [I]: {
        [rn.EQUIPPED_EFFECT_FLAG]: {
          synced: !0,
          sourceItemId: this.id,
          sourceItemUuid: this.uuid ?? null,
          sourceItemType: this.canonicalType,
          sourceEffectId: e.id,
          sourceEffectUuid: e.uuid ?? null
        }
      }
    }), n;
  }
  isSkill() {
    return this.canonicalType === M.itemType.skill;
  }
  async switchMonitorCheck(e, n, i, a = void 0) {
    await _e.switchMonitorCheck(this.parent, e, n, i, a, this);
  }
  async setCounter(e, n) {
    await _e.setCounter(this, e, n);
  }
  async createModifier(e = {}) {
    e = foundry.utils.mergeObject(e, {
      group: "roll",
      effect: "pool",
      category: "skill",
      subCategory: "",
      value: 0,
      condition: ""
    }), this._mutateModifiers((n) => n.concat([e]));
  }
  async deleteModifier(e) {
    await this._mutateModifiers((n) => n.filter((i) => i.id !== e));
  }
  async changeModifierSelection(e, n, i) {
    const a = this._computeModifierImpact(n, i);
    this._applyModifierUpdate(e, a);
  }
  _computeModifierImpact(e, n) {
    switch (e) {
      case "group":
        return (i) => {
          i.group !== n && (i.group = n, i.effect = "", i.category = "", i.subCategory = "");
        };
      case "effect":
        return (i) => i.effect = n;
      case "category":
        return (i) => {
          i.category !== n && (i.category = n, i.subCategory = "");
        };
      case "subCategory":
        return (i) => i.subCategory = n;
    }
    return (i) => {
    };
  }
  async changeModifierValue(e, n) {
    this._applyModifierUpdate(e, (i) => i.value = Number(n));
  }
  async changeModifierCondition(e, n) {
    this._applyModifierUpdate(e, (i) => i.condition = n);
  }
  async _applyModifierUpdate(e, n = (i) => {
  }) {
    await this._mutateModifiers((i) => i.map((a) => (a.id === e && n(a), a)));
  }
  async _mutateModifiers(e = (n) => n) {
    const n = e(this.system.modifiers);
    ot.reindexIds(n), await this.update({ "system.modifiers": n });
  }
  async _mutateQualitySystem(e = (n) => n) {
    const n = e(foundry.utils.deepClone(Yn(this.system ?? {})));
    await this.update({ system: Yn(n) });
  }
  async createQualityPrerequisite(e = {}) {
    await this._mutateQualitySystem((n) => (n.prerequisites = ia(n.prerequisites).concat([{
      id: e.id ?? foundry.utils.randomID(),
      fact: e.fact ?? "",
      comparator: e.comparator ?? "eq",
      value: e.value ?? ""
    }]), n));
  }
  async deleteQualityPrerequisite(e) {
    await this._mutateQualitySystem((n) => (n.prerequisites = ia(n.prerequisites).filter((i) => i.id !== e), n));
  }
  async updateQualityPrerequisite(e, n, i) {
    await this._mutateQualitySystem((a) => (a.prerequisites = ia(a.prerequisites).map((r) => (r.id !== e || (n === "fact" && (r.fact = i), n === "comparator" && (r.comparator = i), n === "value" && (r.value = i)), r)), a));
  }
  async createQualityEffect(e = {}) {
    await this._mutateQualitySystem((n) => (n.effects = mr(n.effects).concat([{
      id: e.id ?? foundry.utils.randomID(),
      type: e.type ?? "rollMod",
      phase: e.phase ?? "onBuildRoll",
      selector: e.selector ?? "",
      skillKeys: e.skillKeys ?? [],
      label: e.label ?? "",
      value: Number(e.value ?? 0) || 0,
      min: e.min ?? null,
      max: e.max ?? null,
      pool: e.pool ?? "",
      operation: e.operation ?? "adjustAmount",
      application: e.application ?? "automatic",
      defaultEnabled: e.defaultEnabled === !0,
      conditions: ia(e.conditions ?? []),
      limit: za(e.limit ?? {})
    }]), n));
  }
  async deleteQualityEffect(e) {
    await this._mutateQualitySystem((n) => (n.effects = mr(n.effects).filter((i) => i.id !== e), n));
  }
  async updateQualityEffect(e, n, i) {
    await this._mutateQualitySystem((a) => (a.effects = mr(a.effects).map((r) => (r.id !== e || (n === "type" && (r.type = i), n === "phase" && (r.phase = i), n === "selector" && (r.selector = i), n === "skillKeys" && (r.skillKeys = Array.isArray(i) ? i : []), n === "label" && (r.label = i), n === "value" && (r.value = Number(i ?? 0) || 0), n === "min" && (r.min = i === "" ? null : Number(i ?? 0)), n === "max" && (r.max = i === "" ? null : Number(i ?? 0)), n === "pool" && (r.pool = i), n === "operation" && (r.operation = i), n === "application" && (r.application = i), n === "defaultEnabled" && (r.defaultEnabled = i === !0), n === "limit.perActivation" && (r.limit = za({ ...r.limit ?? {}, perActivation: i })), n === "limit.perRound" && (r.limit = za({ ...r.limit ?? {}, perRound: i })), n === "limit.perScene" && (r.limit = za({ ...r.limit ?? {}, perScene: i }))), r)), a));
  }
  async createQualityEffectCondition(e, n = {}) {
    await this._mutateQualitySystem((i) => (i.effects = mr(i.effects).map((a) => (a.id !== e || (a.conditions = ia(a.conditions).concat([{
      id: n.id ?? foundry.utils.randomID(),
      fact: n.fact ?? "",
      comparator: n.comparator ?? "eq",
      value: n.value ?? ""
    }])), a)), i));
  }
  async deleteQualityEffectCondition(e, n) {
    await this._mutateQualitySystem((i) => (i.effects = mr(i.effects).map((a) => (a.id !== e || (a.conditions = ia(a.conditions).filter((r) => r.id !== n)), a)), i));
  }
  async updateQualityEffectCondition(e, n, i, a) {
    await this._mutateQualitySystem((r) => (r.effects = mr(r.effects).map((s) => (s.id !== e || (s.conditions = ia(s.conditions).map((o) => (o.id !== n || (i === "fact" && (o.fact = a), i === "comparator" && (o.comparator = a), i === "value" && (o.value = a)), o))), s)), r));
  }
  async _mutateWeaponStandardTraits(e = (n) => n) {
    var i;
    const n = e(foundry.utils.deepClone(Di((i = this.system) == null ? void 0 : i.standardTraits)));
    await this.update({ "system.standardTraits": Di(n) });
  }
  async createWeaponStandardTrait(e = {}) {
    await this._mutateWeaponStandardTraits((n) => n.concat([{
      id: e.id ?? foundry.utils.randomID(),
      key: e.key ?? "fatigue",
      rating: Math.max(0, Number(e.rating ?? 0) || 0)
    }]));
  }
  async deleteWeaponStandardTrait(e) {
    await this._mutateWeaponStandardTraits((n) => n.filter((i) => i.id !== e));
  }
  async updateWeaponStandardTrait(e, n, i) {
    await this._mutateWeaponStandardTraits((a) => a.map((r) => (r.id !== e || (n === "key" && (r.key = i), n === "rating" && (r.rating = Math.max(0, Number(i ?? 0) || 0))), r)));
  }
  async _mutateArmorStandardTraits(e = (n) => n) {
    var i;
    const n = e(foundry.utils.deepClone(na((i = this.system) == null ? void 0 : i.standardTraits)));
    await this.update({ "system.standardTraits": na(n) });
  }
  async createArmorStandardTrait(e = {}) {
    await this._mutateArmorStandardTraits((n) => n.concat([{
      id: e.id ?? foundry.utils.randomID(),
      key: e.key ?? "bulky",
      rating: Math.max(0, Number(e.rating ?? 0) || 0)
    }]));
  }
  async deleteArmorStandardTrait(e) {
    await this._mutateArmorStandardTraits((n) => n.filter((i) => i.id !== e));
  }
  async updateArmorStandardTrait(e, n, i) {
    await this._mutateArmorStandardTraits((a) => a.map((r) => (r.id !== e || (n === "key" && (r.key = i), n === "rating" && (r.rating = Math.max(0, Number(i ?? 0) || 0))), r)));
  }
  async _mutatePayloads(e = (n) => n) {
    var a, r, s, o, l, c, u;
    const n = e(foundry.utils.deepClone(
      sa((a = this.system) == null ? void 0 : a.payloads, {
        legacyAmmo: (r = this.system) == null ? void 0 : r.ammo,
        category: ((s = this.system) == null ? void 0 : s.category) ?? ((o = this.system) == null ? void 0 : o.weaponCategory)
      })
    )).map(on), i = Oo((l = this.system) == null ? void 0 : l.selectedPayloadId, n, {
      category: ((c = this.system) == null ? void 0 : c.category) ?? ((u = this.system) == null ? void 0 : u.weaponCategory)
    });
    await this.update({
      "system.payloads": n,
      "system.selectedPayloadId": i,
      "system.ammo": bn()
    });
  }
  async _mutateConsumptionSources(e = (n) => n) {
    var i, a;
    const n = e(foundry.utils.deepClone(
      Ia((i = this.system) == null ? void 0 : i.consumptionSources, { legacyAmmo: (a = this.system) == null ? void 0 : a.ammo })
    )).map(si);
    await this.update({
      "system.consumptionSources": n,
      "system.ammo": bn()
    });
  }
  async updatePayloadField(e, n, i) {
    String(e ?? "").trim() !== "unloaded" && await this._mutatePayloads((a) => a.map((r) => r.id !== e ? r : (S_(r, n) && foundry.utils.setProperty(r, n, i), on(r))));
  }
  async createPayload(e = {}) {
    await this._mutatePayloads((n) => n.concat([on({
      id: e.id ?? foundry.utils.randomID(),
      label: e.label ?? e.name ?? "Payload",
      compatibleWith: e.compatibleWith ?? [],
      modifies: e.modifies ?? {},
      traits: e.traits ?? [],
      keywords: e.keywords ?? [],
      template: e.template ?? null,
      areaEffect: e.areaEffect ?? { kind: kt.none },
      resolution: e.resolution ?? { resolverKey: "standard" },
      consumption: e.consumption ?? { amount: 1, sourceId: "" }
    })]));
  }
  async deletePayload(e) {
    var r, s, o, l, c;
    if (String(e ?? "").trim() === "unloaded") return;
    const n = ((r = this.system) == null ? void 0 : r.category) ?? ((s = this.system) == null ? void 0 : s.weaponCategory), i = sa((o = this.system) == null ? void 0 : o.payloads, {
      legacyAmmo: (l = this.system) == null ? void 0 : l.ammo,
      category: n
    }).filter((u) => u.id !== e), a = ((c = i[0]) == null ? void 0 : c.id) ?? "unloaded";
    await this.update({
      "system.payloads": i.length ? i : sa([], { category: n }),
      "system.selectedPayloadId": i.some((u) => {
        var d;
        return u.id === ((d = this.system) == null ? void 0 : d.selectedPayloadId);
      }) ? this.system.selectedPayloadId : i.length ? a : "",
      "system.ammo": bn()
    });
  }
  async createPayloadStandardTrait(e, n = {}) {
    String(e ?? "").trim() !== "unloaded" && await this._mutatePayloads((i) => i.map((a) => a.id !== e ? a : (a.modifies ?? (a.modifies = {}), a.modifies.standardTraits = Di(a.modifies.standardTraits).concat([{
      id: n.id ?? foundry.utils.randomID(),
      key: n.key ?? "fatigue",
      rating: Math.max(0, Number(n.rating ?? 0) || 0)
    }]), on(a))));
  }
  async deletePayloadStandardTrait(e, n) {
    String(e ?? "").trim() !== "unloaded" && await this._mutatePayloads((i) => i.map((a) => a.id !== e ? a : (a.modifies ?? (a.modifies = {}), a.modifies.standardTraits = Di(a.modifies.standardTraits).filter((r) => r.id !== n), on(a))));
  }
  async updatePayloadStandardTrait(e, n, i, a) {
    String(e ?? "").trim() !== "unloaded" && await this._mutatePayloads((r) => r.map((s) => s.id !== e ? s : (s.modifies ?? (s.modifies = {}), s.modifies.standardTraits = Di(s.modifies.standardTraits).map((o) => (o.id !== n || (i === "key" && (o.key = a), i === "rating" && (o.rating = Math.max(0, Number(a ?? 0) || 0))), o)), on(s))));
  }
  async createConsumptionSource(e = {}) {
    await this._mutateConsumptionSources((n) => n.concat([si({
      id: e.id ?? foundry.utils.randomID(),
      label: e.label ?? "Source",
      kind: e.kind ?? "internal",
      reloadable: e.reloadable ?? !0,
      loadedPayloadKey: e.loadedPayloadKey ?? "",
      tracking: e.tracking ?? { current: 0, max: 0 },
      link: e.link ?? {}
    })]));
  }
  async deleteConsumptionSource(e) {
    var n;
    await this._mutateConsumptionSources((i) => i.filter((a) => a.id !== e)), await this._mutatePayloads((i) => i.map((a) => {
      var r;
      return ((r = a == null ? void 0 : a.consumption) == null ? void 0 : r.sourceId) !== e ? a : (a.consumption.sourceId = "", on(a));
    })), await this.update({
      "system.payloadSourceAssignments": k_((n = this.system) == null ? void 0 : n.payloadSourceAssignments, e)
    });
  }
  async updateConsumptionSourceField(e, n, i) {
    await this._mutateConsumptionSources((a) => a.map((r) => {
      var s, o, l;
      if (r.id !== e) return r;
      if (foundry.utils.setProperty(r, n, i), n === "kind" && r.kind === "itemRef" && (r.link ?? (r.link = {}), String(r.link.itemPath ?? "").trim() || (r.link.itemPath = "quantity"), (!String(r.label ?? "").trim() || String(r.label ?? "").trim() === "Source") && (r.label = "Linked Item")), n === "link.itemId" && r.kind === "itemRef") {
        r.link ?? (r.link = {}), String(r.link.itemPath ?? "").trim() || (r.link.itemPath = "quantity");
        const c = ((l = (o = (s = this.actor) == null ? void 0 : s.items) == null ? void 0 : o.get) == null ? void 0 : l.call(o, String(r.link.itemId ?? "").trim())) ?? null;
        c && (!String(r.label ?? "").trim() || ["Source", "Linked Item"].includes(String(r.label ?? "").trim())) && (r.label = c.name ?? r.label);
      }
      return si(r);
    }));
  }
  async updatePayloadSourceAssignment(e, n) {
    var s;
    const i = wt(e);
    if (!i) return;
    const a = Fr((s = this.system) == null ? void 0 : s.payloadSourceAssignments), r = String(n ?? "").trim();
    if (!r) {
      delete a[i], await this.update({ "system.payloadSourceAssignments": a });
      return;
    }
    a[i] = {
      sourceId: r
    }, await this.update({ "system.payloadSourceAssignments": a });
  }
  getPayloadState({ payloadId: e = "", ammoTypeId: n = "" } = {}) {
    var o, l, c, u, d, f, m, g, p, h;
    const i = Sy({
      actor: this.actor ?? null,
      weaponCompatibility: ((o = this.system) == null ? void 0 : o.payloadCompatibility) ?? {},
      sourceAssignments: ((l = this.system) == null ? void 0 : l.payloadSourceAssignments) ?? {}
    }), a = String(e || n || ((c = this.system) == null ? void 0 : c.selectedPayloadKey) || ((u = this.system) == null ? void 0 : u.selectedPayloadUuid) || ((d = this.system) == null ? void 0 : d.selectedPayloadId) || "").trim(), r = wt(a), s = ((f = i.payloads.find(
      (b) => b.id === r || b.payloadKey === r || b.itemUuid === a
    )) == null ? void 0 : f.id) ?? a;
    return rf({
      payloads: [
        ...Array.isArray((m = this.system) == null ? void 0 : m.payloads) ? this.system.payloads : [],
        ...i.payloads
      ],
      selectedPayloadId: s,
      consumptionSources: [
        ...Array.isArray((g = this.system) == null ? void 0 : g.consumptionSources) ? this.system.consumptionSources : [],
        ...i.consumptionSources
      ],
      actor: this.actor ?? null,
      payloadId: s,
      category: ((p = this.system) == null ? void 0 : p.category) ?? ((h = this.system) == null ? void 0 : h.weaponCategory)
    });
  }
  getActivePayloadReloadState({ payloadId: e = "", ammoTypeId: n = "", user: i = game.user } = {}) {
    var y, S, A;
    const a = String(((y = this.system) == null ? void 0 : y.category) ?? ((S = this.system) == null ? void 0 : S.weaponCategory) ?? "").trim().toLowerCase(), r = {
      canReload: !1,
      reason: "",
      payloadLabel: "",
      activePayloadId: "",
      current: 0,
      max: 0,
      inCombat: !1,
      source: null,
      sourceState: null,
      payloadState: null
    };
    if (!this.isWeapon())
      return { ...r, reason: "Only weapons can reload payloads." };
    if (!this.actor)
      return { ...r, reason: "Reload is only available for weapons owned by an actor." };
    if (a === "melee")
      return { ...r, reason: "Melee weapons do not use reloadable payloads." };
    const s = this.getPayloadState({ payloadId: e || n }), o = (s == null ? void 0 : s.sourceState) ?? null, l = (s == null ? void 0 : s.source) ?? null, c = String((s == null ? void 0 : s.activePayloadId) ?? "").trim(), u = (s == null ? void 0 : s.activePayload) ?? null, d = wt((u == null ? void 0 : u.payloadKey) ?? c), f = String((s == null ? void 0 : s.payloadLabel) ?? "").trim() || "Unloaded", m = Math.max(0, Number((o == null ? void 0 : o.current) ?? 0) || 0), g = Math.max(0, Number((o == null ? void 0 : o.max) ?? 0) || 0), p = !!((A = D.getCombat(this.actor)) != null && A.combatant), h = String((u == null ? void 0 : u.sourceType) ?? "").trim() === M.itemType.weaponPayload, b = h ? T_(this.actor, d) : 0;
    return !c || c === "unloaded" ? {
      ...r,
      reason: "Select a payload before reloading.",
      payloadLabel: f,
      activePayloadId: c,
      payloadState: s,
      source: l,
      sourceState: o,
      current: m,
      max: g,
      inCombat: p
    } : h && !(l != null && l.id) ? {
      ...r,
      reason: "Selected payload has no assigned source.",
      payloadLabel: f,
      activePayloadId: c,
      payloadState: s,
      source: l,
      sourceState: o,
      current: m,
      max: g,
      inCombat: p
    } : o != null && o.isTracked ? o.kind !== "internal" || !o.reloadable ? {
      ...r,
      reason: "That payload source is not reloadable from the weapon sheet.",
      payloadLabel: f,
      activePayloadId: c,
      payloadState: s,
      source: l,
      sourceState: o,
      current: m,
      max: g,
      inCombat: p
    } : h && b <= 0 ? {
      ...r,
      reason: "No reserve reloads available for the selected payload.",
      payloadLabel: f,
      activePayloadId: c,
      payloadState: s,
      source: l,
      sourceState: o,
      current: m,
      max: g,
      inCombat: p
    } : g <= 0 ? {
      ...r,
      reason: "This payload source has no reloadable capacity.",
      payloadLabel: f,
      activePayloadId: c,
      payloadState: s,
      source: l,
      sourceState: o,
      current: m,
      max: g,
      inCombat: p
    } : p && !(i != null && i.isGM) ? {
      ...r,
      reason: "Only a GM can reload from the weapon sheet during combat.",
      payloadLabel: f,
      activePayloadId: c,
      payloadState: s,
      source: l,
      sourceState: o,
      current: m,
      max: g,
      inCombat: p
    } : m >= g && (!h || o.loadedPayloadKey === d) ? {
      ...r,
      reason: "Magazine already full.",
      payloadLabel: f,
      activePayloadId: c,
      payloadState: s,
      source: l,
      sourceState: o,
      current: m,
      max: g,
      inCombat: p
    } : {
      canReload: !0,
      reason: "",
      payloadLabel: f,
      activePayloadId: c,
      payloadState: s,
      source: l,
      sourceState: o,
      current: m,
      max: g,
      reserveQuantity: b,
      inCombat: p
    } : {
      ...r,
      reason: "This payload is untracked and does not need to be reloaded.",
      payloadLabel: f,
      activePayloadId: c,
      payloadState: s,
      source: l,
      sourceState: o,
      current: m,
      max: g,
      inCombat: p
    };
  }
  canReloadActivePayload({ detailed: e = !1, ...n } = {}) {
    const i = this.getActivePayloadReloadState(n);
    return e ? i : i.canReload;
  }
  async reloadActivePayload({ payloadId: e = "", ammoTypeId: n = "" } = {}) {
    var a, r, s;
    const i = this.getActivePayloadReloadState({ payloadId: e, ammoTypeId: n });
    return !i.canReload || !((a = i.source) != null && a.id) ? { ok: !1, ...i } : (await this._mutateConsumptionSources((o) => o.map((l) => {
      var u, d, f, m, g;
      if (l.id !== i.source.id) return l;
      l.tracking ?? (l.tracking = {}), l.tracking.max = Math.max(0, Number(((u = l.tracking) == null ? void 0 : u.max) ?? i.max) || i.max), l.tracking.current = i.max;
      const c = wt(((f = (d = i.payloadState) == null ? void 0 : d.activePayload) == null ? void 0 : f.payloadKey) ?? i.activePayloadId);
      return String(((g = (m = i.payloadState) == null ? void 0 : m.activePayload) == null ? void 0 : g.sourceType) ?? "").trim() === M.itemType.weaponPayload && (l.loadedPayloadKey = c), si(l);
    })), String(((s = (r = i.payloadState) == null ? void 0 : r.activePayload) == null ? void 0 : s.sourceType) ?? "").trim() === M.itemType.weaponPayload && await w_(this.actor, i.payloadState.activePayload.payloadKey ?? i.activePayloadId), {
      ok: !0,
      payloadLabel: i.payloadLabel,
      activePayloadId: i.activePayloadId,
      current: i.max,
      max: i.max,
      reloadedAmount: Math.max(0, i.max - i.current),
      sourceId: i.source.id
    });
  }
  async setActivePayload(e) {
    var r, s;
    const n = this.getPayloadState({ payloadId: e }), i = String((n == null ? void 0 : n.activePayloadId) ?? "").trim(), a = String(((r = n == null ? void 0 : n.activePayload) == null ? void 0 : r.sourceType) ?? "").trim() === M.itemType.weaponPayload;
    await this.update({
      "system.selectedPayloadId": a ? "unloaded" : i,
      "system.selectedPayloadUuid": "",
      "system.selectedPayloadKey": a ? wt(((s = n.activePayload) == null ? void 0 : s.payloadKey) ?? i) : "",
      "system.ammo": bn()
    });
  }
  getPayloadConsumptionState({ payloadId: e = "", ammoTypeId: n = "" } = {}) {
    var u;
    const i = this.getPayloadState({ payloadId: e || n }), a = (i == null ? void 0 : i.activePayload) ?? null, r = (i == null ? void 0 : i.sourceState) ?? null, s = String((a == null ? void 0 : a.sourceType) ?? "").trim() === M.itemType.weaponPayload, o = wt((a == null ? void 0 : a.payloadKey) ?? (i == null ? void 0 : i.activePayloadId));
    if (!a)
      return { ok: !0, reason: "", payloadState: i };
    if (s && String(((u = a == null ? void 0 : a.consumption) == null ? void 0 : u.sourceId) ?? "").trim() && !(i != null && i.source))
      return { ok: !1, reason: "Selected payload source is missing.", payloadState: i };
    if (s && (r != null && r.isTracked) && r.loadedPayloadKey !== o)
      return { ok: !1, reason: "Selected payload is not loaded.", payloadState: i };
    if (!(r != null && r.isTracked)) return { ok: !0, reason: "", payloadState: i };
    const l = Math.max(1, Number(r.consumePerUse ?? 1) || 1);
    return Math.max(0, Number(r.current ?? 0) || 0) < l ? { ok: !1, reason: "Not enough payload loaded.", payloadState: i } : { ok: !0, reason: "", payloadState: i };
  }
  canConsumePayload({ payloadId: e = "", ammoTypeId: n = "", detailed: i = !1 } = {}) {
    const a = this.getPayloadConsumptionState({ payloadId: e, ammoTypeId: n });
    return i ? a : a.ok;
  }
  async consumePayload({ payloadId: e = "", ammoTypeId: n = "" } = {}) {
    var o;
    const i = this.getPayloadConsumptionState({ payloadId: e, ammoTypeId: n });
    if (!i.ok) return !1;
    const a = i.payloadState;
    if (!((o = a == null ? void 0 : a.sourceState) != null && o.isTracked)) return !0;
    const r = Math.max(1, Number(a.sourceState.consumePerUse ?? 1) || 1), s = Math.max(0, Number(a.sourceState.current ?? 0) || 0);
    if (s < r) return !1;
    if (a.sourceState.kind === "internal")
      return await this._mutateConsumptionSources((l) => l.map((c) => {
        var u;
        return c.id !== ((u = a.source) == null ? void 0 : u.id) ? c : (c.tracking ?? (c.tracking = {}), c.tracking.current = Math.max(0, s - r), si(c));
      })), !0;
    if (a.sourceState.kind === "actorResource" && this.actor && a.sourceState.currentPath)
      return await this.actor.update({
        [a.sourceState.currentPath]: Math.max(0, s - r)
      }), !0;
    if (a.sourceState.kind === "itemRef" && a.sourceState.sourceItem && a.sourceState.currentPath) {
      const l = String(a.sourceState.currentPath ?? "").trim(), c = l.startsWith("system.") ? l : `system.${l}`;
      return await a.sourceState.sourceItem.update({
        [c]: Math.max(0, s - r)
      }), !0;
    }
    return !1;
  }
  getAmmoState({ ammoTypeId: e = "" } = {}) {
    return this.getPayloadState({ payloadId: e });
  }
  async setActiveAmmoType(e) {
    await this.setActivePayload(e);
  }
  canConsumeAmmo({ ammoTypeId: e = "" } = {}) {
    return this.canConsumePayload({ payloadId: e });
  }
  async consumeAmmo({ ammoTypeId: e = "" } = {}) {
    return this.consumePayload({ payloadId: e });
  }
  async createAmmoType(e = {}) {
    await this.createPayload(e);
  }
  async deleteAmmoType(e) {
    await this.deletePayload(e);
  }
  async updateAmmoType(e, n, i) {
    const a = n === "name" ? "label" : n === "damageType" ? "modifies.damageType" : n === "apMod" ? "modifies.ap" : n.startsWith("attackRatingBandMod.") ? `modifies.attackRatingBand.${n.split(".")[1]}` : n === "traits" ? "traits" : n === "keywords" ? "keywords" : n;
    await this.updatePayloadField(e, a, i);
  }
  async createAmmoTypeStandardTrait(e, n = {}) {
    await this.createPayloadStandardTrait(e, n);
  }
  async deleteAmmoTypeStandardTrait(e, n) {
    await this.deletePayloadStandardTrait(e, n);
  }
  async updateAmmoTypeStandardTrait(e, n, i, a) {
    await this.updatePayloadStandardTrait(e, n, i, a);
  }
  getCombatProfile({ payloadId: e = "", ammoTypeId: n = "" } = {}) {
    var k, w, v, N, P;
    if (!this.isWeapon()) return null;
    const i = this.system ?? {}, a = this.isMechWeapon() ? Lo(i.weaponCategory ?? i.category) : String(i.category ?? i.weaponCategory ?? "ranged").trim() || "ranged", r = this.isMechWeapon() ? um(i.range, a) : yr(i.range), s = this.isMechWeapon() ? fc(a, i.skill) : String(i.skill ?? "").trim() || "firearms", o = Ft(s), l = Number(i.damage ?? 0) || 0, c = dm(i.damageAttribute), u = this.actor ?? this.parent ?? null, d = c ? Math.max(0, Number(((k = u == null ? void 0 : u.getAttributeValue) == null ? void 0 : k.call(u, c)) ?? ((N = (v = (w = u == null ? void 0 : u.system) == null ? void 0 : w.attributes) == null ? void 0 : v[c]) == null ? void 0 : N.value) ?? 0) || 0) : 0, f = $o(i.damageAttributeScale), m = c ? Math.ceil(d * f) : 0, g = Math.max(0, l + m), p = Math.max(0, Number(i.clusteringDice ?? i.clusterDice ?? 0) || 0), h = Number(i.clusteringTargetNumber ?? i.clusterTargetNumber ?? 5) || 5, b = this.isMechWeapon() ? ei(i.damageType, "energy") : Xn(i.damageType), y = this.isWeapon() ? Sy({
      actor: this.actor ?? null,
      weaponCompatibility: i.payloadCompatibility ?? {},
      sourceAssignments: i.payloadSourceAssignments ?? {}
    }) : { payloads: [], consumptionSources: [] }, S = String(e || n || i.selectedPayloadKey || i.selectedPayloadUuid || i.selectedPayloadId || "").trim(), A = wt(S), T = ((P = y.payloads.find(
      (L) => L.id === A || L.payloadKey === A || L.itemUuid === S
    )) == null ? void 0 : P.id) ?? S, C = SE({
      damage: g,
      damageType: b,
      ap: Number(i.ap ?? i.armorPiercing ?? 0) || 0,
      attackRatingBand: ho(i.attackRatingBand),
      traits: mc(i.traits),
      keywords: jd(i.keywords),
      standardTraits: Di(i.standardTraits),
      resolution: Mc(i.resolution, "standard"),
      fireModes: As(i.fireModes),
      payloads: [
        ...sa(i.payloads, { legacyAmmo: i.ammo, category: a }),
        ...y.payloads
      ],
      selectedPayloadId: T,
      consumptionSources: [
        ...Ia(i.consumptionSources, { legacyAmmo: i.ammo }),
        ...y.consumptionSources
      ],
      payloadId: T,
      actor: this.actor ?? null,
      category: a
    });
    return {
      id: this.id ?? "weapon",
      uuid: this.uuid ?? null,
      name: this.name ?? "Weapon",
      img: this.img,
      item: this,
      type: this.canonicalType,
      equipped: !!i.equipped,
      isPrimary: !!i.isPrimary,
      scale: Is(i.scale, "personal"),
      mount: bo(i.mount),
      category: a,
      skill: s,
      skillDef: o,
      damage: C.damage ?? g,
      baseDamage: l,
      damageAttribute: c,
      damageAttributeScale: f,
      damageAttributeBonus: m,
      clusteringDice: p + Math.max(0, Number(C.clusteringDice ?? 0) || 0),
      clusteringTargetNumber: h,
      ap: C.ap,
      baseDamageType: b,
      baseDamageTypeLabel: this.isMechWeapon() ? En(b) : In(b),
      damageType: C.damageType,
      damageTypeLabel: In(C.damageType),
      damageTrack: C.damageTrack,
      attackRatingBand: C.attackRatingBand,
      range: r,
      defaultRangeBand: this.getDefaultRangeBand(r),
      traits: C.traits,
      keywords: C.keywords,
      standardTraits: C.standardTraits,
      effects: C.effects,
      payloadLabel: C.payloadLabel,
      payload: C.payload,
      payloadState: C.payloadState,
      source: C.source,
      sourceState: C.sourceState,
      template: C.template,
      areaEffect: C.areaEffect,
      resolution: C.resolution,
      resolverKey: C.resolverKey,
      fireModes: C.fireModes,
      availability: String(i.availability ?? "").trim(),
      capabilityReport: C.capabilityReport,
      ammoLabel: C.payloadLabel,
      ammoType: C.payload,
      ammoState: C.ammoState,
      heat: Math.max(0, Number(i.heat ?? 0) || 0),
      area: String(i.area ?? "none").trim() || "none",
      volatile: !!i.volatile,
      notes: String(i.notes ?? i.description ?? "").trim()
    };
  }
  getArmorProfile({ actor: e = this.actor } = {}) {
    if (!this.isArmor()) return null;
    const n = this.system ?? {}, i = Math.max(0, Number(n.rating ?? 0)), a = qd(i, n == null ? void 0 : n.durability), r = a.max, s = Math.min(
      r,
      Math.max(0, Number(a.current ?? r))
    ), o = Math.min(i, s), l = Ki((n == null ? void 0 : n.mitigationByType) ?? (n == null ? void 0 : n.mitigation)), c = Gd({
      standardTraits: na(n == null ? void 0 : n.standardTraits),
      traits: mc(n == null ? void 0 : n.traits),
      traitState: n == null ? void 0 : n.traitState
    }), u = wc(n == null ? void 0 : n.tags), d = ep(o);
    return {
      id: this.id ?? "armor",
      uuid: this.uuid ?? null,
      name: this.name ?? "Armor",
      img: this.img,
      type: this.canonicalType,
      item: this,
      actor: e,
      equipped: !!n.equipped,
      isPrimary: !!n.isPrimary,
      rating: i,
      defenseBonus: Number(n.defenseBonus ?? 0) || 0,
      currentArmorRating: o,
      ratingCurrent: o,
      remainingDurability: s,
      baseMitigation: d,
      baseResistance: d,
      mitigationByType: l,
      tags: u,
      isDestroyed: s <= 0,
      durability: {
        current: s,
        max: r
      },
      traitState: c.traitState,
      traitEffects: c.traitEffects,
      effects: c.effects,
      standardTraits: na(n.standardTraits),
      battleArmor: Ai(n.battleArmor),
      traits: kE({
        standardTraits: na(n.standardTraits)
      }),
      notes: String(n.notes ?? "").trim()
    };
  }
  getDefaultRangeBand(e = yr(((n) => (n = this.system) == null ? void 0 : n.range)())) {
    return v_(e);
  }
  isWeaponSkill(e) {
    return ((e == null ? void 0 : e.canonicalType) ?? (e == null ? void 0 : e.type)) === M.itemType.skill && e.system.code === this.system.skill;
  }
  getWeaponSkill() {
    var i;
    const e = (i = this.actor) == null ? void 0 : i.items.find((a) => this.isWeaponSkill(a));
    if (e) return e;
    const n = game.items.find((a) => this.isWeaponSkill(a));
    return n || Ny(this.system.skill);
  }
  getDefense() {
    var n, i, a;
    if (this.isMechWeapon()) {
      const r = Ft(fc(((n = this.system) == null ? void 0 : n.weaponCategory) ?? ((i = this.system) == null ? void 0 : i.category), (a = this.system) == null ? void 0 : a.skill));
      return r != null && r.defense ? _t.fixedDefenseCode(r.defense) : void 0;
    }
    if (this.system.defense)
      return _t.fixedDefenseCode(this.system.defense);
    const e = Ft(String(this.system.skill ?? "").trim());
    return e != null && e.defense ? _t.fixedDefenseCode(e.defense) : void 0;
  }
  getDamage() {
    if (!this.parent) return;
    const e = this.isWeapon() ? this.getCombatProfile() : null, n = this._getMonitor();
    if (this.isMechWeapon())
      return {
        value: Math.max(0, Number((e == null ? void 0 : e.damage) ?? this.system.damage ?? 0) || 0),
        monitor: n,
        damageType: (e == null ? void 0 : e.damageType) ?? this.system.damageType,
        damageTypeLabel: (e == null ? void 0 : e.damageTypeLabel) ?? this.getDamageTypeLabel(),
        noArmor: !1,
        armorMode: "",
        clusteringDice: Number((e == null ? void 0 : e.clusteringDice) ?? 0) || 0
      };
    const i = this.system.damageAttribute ? this.parent.getAttributeValue(this.system.damageAttribute) ?? 0 : 0;
    return {
      value: I_(
        n,
        this.system.damage,
        this.system.damageAttribute,
        i,
        this.system.damageAttributeScale
      ),
      monitor: n,
      damageType: (e == null ? void 0 : e.damageType) ?? this.system.damageType,
      damageTypeLabel: (e == null ? void 0 : e.damageTypeLabel) ?? this.getDamageTypeLabel(),
      noArmor: this.system.noArmor ?? this.system.armorAvoidance,
      armorMode: N_(n, this.system.noArmor ?? this.system.armorAvoidance)
    };
  }
  getDamageCode() {
    var e, n;
    if (this.isMechWeapon()) {
      const i = Math.max(0, Number(((e = this.system) == null ? void 0 : e.damage) ?? 0) || 0), a = Math.max(0, Number(((n = this.getCombatProfile()) == null ? void 0 : n.clusteringDice) ?? 0) || 0);
      return a > 0 ? `${i} + ${a}d6 cluster` : String(i);
    }
    return E_(
      this._getMonitor(),
      this.system.damage,
      this.system.damageAttribute,
      this.system.damageAttributeScale
    );
  }
  getDamageTypeLabel() {
    var n, i;
    if (this.isWeapon()) {
      const a = this.getCombatProfile();
      return this.isMechWeapon() && (a == null ? void 0 : a.damageType) === (a == null ? void 0 : a.baseDamageType) ? En((a == null ? void 0 : a.baseDamageType) ?? this.system.damageType) : In((a == null ? void 0 : a.damageType) ?? this.system.damageType);
    }
    const e = ((n = z.mwd.weaponDamageType) == null ? void 0 : n[this.system.damageType]) ?? ((i = z.mwd.personalDamageType) == null ? void 0 : i[this.system.damageType]);
    return e || this.system.damageType;
  }
  getRanges() {
    return C_(yr(this.system.range)).filter((e) => e.allowed).map((e) => ({ value: e.value, labelkey: e.labelkey }));
  }
  validateTargets(e) {
    var s;
    const n = (s = this.getDamage()) == null ? void 0 : s.monitor, i = _i.getTargetTokens(game.user), a = i.filter((o) => {
      var l;
      return (l = o.actor) == null ? void 0 : l.canReceiveDamage(n);
    }), r = i.filter((o) => {
      var l;
      return !((l = o.actor) != null && l.canReceiveDamage(n));
    }).map((o) => o.name);
    if (r.length > 0) {
      const o = At(z.common.errors.ignoredTargets, {
        targets: r.reduce(ot.joiner(", "))
      });
      ui.notifications.info(o);
    }
    if (a.length === 0) {
      const o = At(z.common.errors.noTargetSelected, {
        weapon: this.name ?? z.itemType.singular.weapon
      });
      ui.notifications.info(o);
    } else
      this.checkWeaponTargetsCount(a);
    return a;
  }
  _getMonitor() {
    return this.isWeapon() ? M.monitors.physical : this.system.monitor || M.monitors.physical;
  }
  isKnowledgeSkill() {
    return this.isSkill() && this.system.code === "knowledge";
  }
  isGeneralSkill() {
    return this.isSkill() && this.system.code !== "knowledge";
  }
  prepareShortcut() {
    if (this.isSkill())
      return {
        img: this.img,
        label: this.name,
        callback: (e) => e.actor.rollSkill(this)
      };
    if (this.isWeapon())
      return {
        img: this.img,
        label: this.name,
        callback: (e) => e.actor.rollWeapon(this)
      };
  }
  prepateShortcut() {
    return this.prepareShortcut();
  }
};
Pl = new WeakMap(), Zn = new WeakSet(), Df = async function(e) {
  if (typeof (e == null ? void 0 : e.syncEquippedActorEffects) == "function")
    try {
      await e.syncEquippedActorEffects();
    } catch (n) {
      console.error(`${tt}Failed to sync equipped item effects`, { item: e, error: n });
    }
}, k0 = async function(e) {
  if (typeof (e == null ? void 0 : e.removeSyncedActorEffects) == "function")
    try {
      await e.removeSyncedActorEffects({ actor: e.actor ?? e.parent ?? null });
    } catch (n) {
      console.error(`${tt}Failed to remove synced item effects`, { item: e, error: n });
    }
}, _c = async function(e) {
  const n = e == null ? void 0 : e.parent;
  if (typeof (n == null ? void 0 : n.syncEquippedActorEffects) == "function")
    try {
      await n.syncEquippedActorEffects();
    } catch (i) {
      console.error(`${tt}Failed to sync parent item effects`, { effect: e, error: i });
    }
}, T0 = async function(e) {
  var a, r, s, o, l, c;
  if (!y0(e)) return;
  const n = e.actor ?? e.parent ?? null, i = S0(e);
  if (!(!n || !i))
    try {
      const u = [];
      for (const d of b0(n.items)) {
        if (!((a = d == null ? void 0 : d.isWeapon) != null && a.call(d))) continue;
        const f = {};
        wt((r = d.system) == null ? void 0 : r.selectedPayloadKey) === i && (f["system.selectedPayloadKey"] = "", f["system.selectedPayloadId"] = "unloaded", f["system.selectedPayloadUuid"] = "");
        const m = A_((s = d.system) == null ? void 0 : s.payloadSourceAssignments, i);
        JSON.stringify(m) !== JSON.stringify(Fr((o = d.system) == null ? void 0 : o.payloadSourceAssignments)) && (f["system.payloadSourceAssignments"] = m);
        const g = Ia((l = d.system) == null ? void 0 : l.consumptionSources, { legacyAmmo: (c = d.system) == null ? void 0 : c.ammo }), p = g.map((h) => h.loadedPayloadKey === i ? si({ ...h, loadedPayloadKey: "" }) : h);
        JSON.stringify(p) !== JSON.stringify(g) && (f["system.consumptionSources"] = p), Object.keys(f).length > 0 && u.push(d.update(f));
      }
      await Promise.all(u);
    } catch (u) {
      console.error(`${tt}Failed to clean deleted payload references`, { item: e, error: u });
    }
}, Fe(rn, Zn), Fe(rn, Pl, !1), F(rn, "RANGE_ORDER", Ur), F(rn, "EQUIPPED_EFFECT_FLAG", "equippedItemSync"), F(rn, "DEFAULT_UNARMED", Object.freeze({
  id: "unarmed",
  name: "Unarmed",
  category: "melee",
  skill: "meleeCombat",
  damage: 1,
  ap: 0,
  damageType: "concussive",
  attackRatingBand: { close: 0, near: 0, far: 0, extreme: 0 },
  range: { max: "close", close: 0, near: 0, far: 0, extreme: 0 },
  standardTraits: [],
  keywords: [],
  resolution: { resolverKey: "standard", damageModel: "", onHitEffect: null },
  fireModes: {
    single: { enabled: !1 },
    burst: { enabled: !1 },
    fullAuto: { enabled: !1 }
  },
  payloads: [],
  selectedPayloadId: "",
  consumptionSources: [{
    id: "untracked",
    label: "Untracked",
    kind: "untracked",
    tracking: { current: 0, max: 0 },
    link: { actorPath: "", itemId: "", itemPath: "" }
  }],
  payloadState: {
    payloads: [],
    activePayloadId: "",
    payloadLabel: "",
    sourceId: "",
    sourceLabel: "",
    sourceKind: "",
    current: 0,
    max: 0,
    consumePerUse: 1,
    isTracked: !1
  },
  payloadLabel: "",
  traits: [],
  notes: ""
}));
let Vs = rn;
function R_(t, e, n = "") {
  const i = foundry.utils.getProperty(t, e);
  return i === void 0 ? n : i;
}
function fn(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function D_(t) {
  return String(t ?? "").replace(/<[^>]*>/g, " ").replace(/\s+/g, " ").trim();
}
function Of(t, e = 180) {
  const n = D_(t);
  return n ? n.length <= e ? n : `${n.slice(0, Math.max(0, e - 3)).trim()}...` : "";
}
function Sr(t = []) {
  return t.map((e) => String(e ?? "").trim()).filter(Boolean);
}
function Lf(t = []) {
  return t.filter((e) => e && e.value !== void 0 && e.value !== null && String(e.value).trim() !== "").map((e) => ({
    label: String(e.label ?? "").trim(),
    value: String(e.value ?? "").trim(),
    emphasis: e.emphasis ?? ""
  }));
}
function $f(t = []) {
  return Sr(t).map((e) => ({ label: e }));
}
function _f(t = []) {
  return t.filter((e) => e && e.value !== void 0 && e.value !== null && String(e.value).trim() !== "").map((e) => ({
    label: String(e.label ?? "").trim(),
    value: String(e.value ?? "").trim()
  }));
}
function w0(t, e, n = {}) {
  const {
    document: i = null,
    type: a = "text",
    value: r = R_(i, t, a === "number" ? 0 : ""),
    displayValue: s = r,
    options: o = [],
    placeholder: l = "",
    readOnly: c = !1,
    rows: u = 4,
    help: d = ""
  } = n;
  return {
    path: t,
    label: e,
    value: r,
    displayValue: s,
    placeholder: l,
    readOnly: c,
    rows: u,
    help: d,
    options: o,
    isText: a === "text",
    isNumber: a === "number",
    isSelect: a === "select",
    isTextarea: a === "textarea"
  };
}
function O_(t, e, n, i = {}) {
  return w0(e, n, { ...i, document: t, type: "text" });
}
function L_(t, e, n, i = {}) {
  return w0(e, n, { ...i, document: t, type: "number" });
}
function $_(t, e = []) {
  return e.map(
    (n) => L_(
      t,
      `system.attributes.${n.key}.value`,
      n.label
    )
  );
}
function mh(t, {
  editable: e = !1,
  tracks: n = [
    { id: "physical", label: "Physical", kind: "wound", status: { label: "Penalty", path: "derived.penalty" } },
    { id: "fatigue", label: "Fatigue", kind: "fatigue", status: { label: "Penalty", path: "derived.penalty" } },
    { id: "armor", label: "Armor", kind: "armor-personal", status: { label: "Resist", path: "derived.resistance" } }
  ]
} = {}) {
  var a;
  const i = ((a = t == null ? void 0 : t.system) == null ? void 0 : a.monitors) ?? {};
  return n.map((r) => {
    const s = (i == null ? void 0 : i[r.id]) ?? {}, o = Math.max(0, fn(foundry.utils.getProperty(s, "max"), 0)), l = Math.min(Math.max(0, fn(foundry.utils.getProperty(s, "value"), 0)), o);
    return {
      id: r.id,
      label: r.label,
      kind: r.kind,
      editable: !!e,
      value: l,
      max: o,
      segments: Array.from({ length: o }, (c, u) => {
        const d = u + 1;
        return { value: d, filled: d <= l };
      }),
      status: r.status ? { label: r.status.label, value: fn(foundry.utils.getProperty(s, r.status.path), 0) } : null
    };
  });
}
function fh(t = {}, { actor: e = null } = {}) {
  const n = e ? g$(e) : [];
  return {
    targeting: t.targeting,
    rollImpact: t.rollImpact,
    states: t.states,
    effects: [
      ...t.effects ?? [],
      ...n.map((i) => ({
        id: `critical.${i.id}`,
        label: `${i.label} (${i.reason})`
      }))
    ],
    criticalRestrictions: n,
    activation: t.activation,
    inactiveReason: t.inactiveReason
  };
}
function Py(t) {
  const e = Math.trunc(Number(t ?? 0) || 0);
  return `${e > 0 ? "+" : ""}${e} m`;
}
function gh(t) {
  var r, s, o, l;
  const e = ((o = (s = (r = t == null ? void 0 : t.system) == null ? void 0 : r.derived) == null ? void 0 : s.personalCombat) == null ? void 0 : o.speed) ?? Fw(t), n = Math.max(0, Math.trunc(Number((e == null ? void 0 : e.base) ?? ((l = t == null ? void 0 : t.system) == null ? void 0 : l.speed) ?? 0) || 0)), i = Math.trunc(Number((e == null ? void 0 : e.modifier) ?? 0) || 0), a = Math.max(0, Math.trunc(Number((e == null ? void 0 : e.effective) ?? n + i) || 0));
  return {
    base: n,
    modifier: i,
    effective: a,
    adjusted: i !== 0,
    displayValue: `${a} m`,
    modifierLabel: i !== 0 ? Py(i) : "",
    title: i !== 0 ? `Base ${n} m, criticals ${Py(i)}` : `${n} m`
  };
}
function __(t = {}) {
  return !t.remedyLabel || t.remedyKey === "none" ? "" : `Remedy: ${t.remedyLabel}${t.remedyBaseDn ? ` DN ${t.remedyBaseDn}` : ""}`;
}
function ph(t) {
  return zr(t).map((e) => {
    const n = [], i = e.effectPayload ?? {};
    return i.cannotAim && n.push("Cannot Aim"), i.cannotReact && n.push("Cannot React"), i.cannotComplex && n.push("Cannot Complex Action"), i.weaponUnequipped && n.push(`Weapon Unequipped${e.weaponName ? `: ${e.weaponName}` : ""}`), {
      ...e,
      summary: __(e),
      restrictions: n.map((a) => ({ label: a })),
      remediable: e.remedyKey && e.remedyKey !== "none"
    };
  });
}
function B_({ defenseBonus: t = 0, mitigationByType: e = {}, armorModifierLabels: n = {} } = {}) {
  const i = [], a = Number(t ?? 0) || 0;
  a !== 0 && i.push(`Defense ${Ry(a)}`);
  for (const [r, s] of Object.entries(n)) {
    const o = Number((e == null ? void 0 : e[r]) ?? 0) || 0;
    o !== 0 && i.push(`${s} ${Ry(o)}`);
  }
  return i.join(" | ");
}
function Ry(t) {
  const e = Number(t ?? 0) || 0;
  return e > 0 ? `+${e}` : `${e}`;
}
function x_(t = {}, e = ["close", "near", "far", "extreme"]) {
  return e.map((n) => {
    const i = fn(t == null ? void 0 : t[n], 0);
    return `${n.charAt(0).toUpperCase() + n.slice(1)} ${i}`;
  }).join(" | ");
}
function z_(t = {}) {
  return ["close", "near", "far", "extreme"].map((e) => `${e.charAt(0).toUpperCase()}${fn(t == null ? void 0 : t[e], 0)}`).join(" ");
}
function F_(t = "") {
  const e = String(t ?? "").trim().toLowerCase();
  return e ? e.charAt(0).toUpperCase() + e.slice(1) : "";
}
function Dy({
  item: t,
  accordionId: e,
  itemType: n,
  defaultSubtitle: i,
  categoryLabels: a = {},
  ratingLabel: r = "Rating",
  typeLabel: s = "",
  isEditable: o = !1,
  isExpanded: l = !1
} = {}) {
  var b, y, S, A, T, C, k, w, v, N;
  const c = Math.max(0, Math.trunc(fn(((b = t == null ? void 0 : t.system) == null ? void 0 : b.quantity) ?? 1, 1))), u = Math.max(0, Math.trunc(fn(((y = t == null ? void 0 : t.system) == null ? void 0 : y.rating) ?? 0, 0))), d = Sr(((S = t == null ? void 0 : t.system) == null ? void 0 : S.tags) ?? []), f = String(((A = t == null ? void 0 : t.system) == null ? void 0 : A.category) ?? "").trim(), m = a[f] ?? f, g = String(((T = t == null ? void 0 : t.system) == null ? void 0 : T.relatedSkill) ?? "").trim(), p = String(((C = t == null ? void 0 : t.system) == null ? void 0 : C.availability) ?? "").trim(), h = String(((k = t == null ? void 0 : t.system) == null ? void 0 : k.rulesHook) ?? "").trim();
  return {
    id: t.id,
    itemType: n,
    isGear: n === "gear",
    isConsumable: n === "consumable",
    accordionId: e,
    isExpanded: l,
    name: t.name,
    img: t.img,
    subtitle: m || i,
    summaryStats: Lf([
      { label: "Qty", value: c, emphasis: "strong" },
      { label: r, value: u },
      { label: "Avail", value: p }
    ]),
    detailTags: $f([
      s,
      ...d,
      (w = t == null ? void 0 : t.system) != null && w.inactive ? "Inactive" : ""
    ]),
    detailRows: _f([
      { label: "Quantity", value: c },
      { label: r, value: u },
      { label: "Related Skill", value: g },
      { label: "Availability", value: p },
      { label: "Rules Hook", value: h },
      { label: "Source", value: ((v = t == null ? void 0 : t.system) == null ? void 0 : v.sourceReference) ?? "" },
      { label: "Category", value: m },
      { label: "Tags", value: d.join(", ") }
    ]),
    detailText: Of(((N = t == null ? void 0 : t.system) == null ? void 0 : N.description) || h),
    quantity: c,
    canAdjustQuantity: o
  };
}
function M0(t, {
  items: e = {},
  isEditable: n = !1,
  isExpanded: i = () => !1,
  inventoryAccordionId: a = (l, c) => `${String(l ?? "").trim()}:${String(c ?? "").trim()}`,
  armorModifierLabels: r = {},
  gearCategoryLabels: s = {},
  consumableCategoryLabels: o = {}
} = {}) {
  var c;
  const l = ((c = t == null ? void 0 : t.getPersonalCombatLoadout) == null ? void 0 : c.call(t)) ?? { warnings: [], weapons: [], armor: [] };
  return {
    warnings: [...(l == null ? void 0 : l.warnings) ?? []],
    weapons: ((l == null ? void 0 : l.weapons) ?? []).map((u) => {
      var C, k, w, v, N, P, L;
      const d = a("weapons", u.id), f = String((u == null ? void 0 : u.category) ?? "").trim().toLowerCase() !== "melee", m = !!((C = u == null ? void 0 : u.sourceState) != null && C.isTracked), g = String((u == null ? void 0 : u.payloadLabel) ?? "").trim() || "Unloaded", p = f && m ? `${fn((k = u == null ? void 0 : u.sourceState) == null ? void 0 : k.current, 0)}/${fn((w = u == null ? void 0 : u.sourceState) == null ? void 0 : w.max, 0)}` : "", h = f ? m ? `${g} ${p}` : g : "", b = f ? m ? `Payload ${p}` : `Payload ${g}` : "", y = x_(u.attackRatingBand), S = z_(u.attackRatingBand), A = eh(t, u), T = u.equipped ? A : "Equip to attack";
      return {
        id: u.id,
        accordionId: d,
        isExpanded: i(d),
        name: u.name,
        img: u.img,
        subtitle: ((v = u.skillDef) == null ? void 0 : v.label) ?? u.category ?? "",
        summaryStats: Lf([
          { label: "DV", value: fn(u.damage, 0), emphasis: "strong" },
          { label: "AP", value: fn(u.ap, 0) },
          { label: "Type", value: u.damageTypeLabel ?? u.damageType ?? "" },
          { label: "CQ", value: S }
        ]),
        detailTags: $f([
          u.equipped ? "Equipped" : "",
          u.isPrimary ? "Primary" : "",
          b,
          ...Sr(u.traits ?? [])
        ]),
        detailRows: _f([
          { label: "Skill", value: ((N = u.skillDef) == null ? void 0 : N.label) ?? u.skill ?? "" },
          { label: "Category", value: u.category ?? "" },
          { label: "Damage Type", value: u.damageTypeLabel ?? u.damageType ?? "" },
          { label: "Max Range", value: F_(((P = u.range) == null ? void 0 : P.max) ?? u.defaultRangeBand ?? "") },
          { label: "CQ Bands", value: y },
          { label: "Payload", value: h },
          { label: "Traits", value: Sr(u.traits ?? []).join(", ") }
        ]),
        detailText: Of(u.notes),
        equipped: !!u.equipped,
        attackDisabled: !!T,
        attackDisabledReason: T,
        isPrimary: !!u.isPrimary,
        attackUuid: u.uuid ?? "",
        attackRoll: JSON.stringify({
          intent: "attack",
          weaponId: u.id,
          payloadId: ((L = u == null ? void 0 : u.payloadState) == null ? void 0 : L.activePayloadId) ?? "",
          edge: { pool: "physical.grit", allowed: ["pre", "post"] },
          tags: ["combat", "attack"]
        })
      };
    }),
    armor: ((l == null ? void 0 : l.armor) ?? []).map((u) => {
      var p, h, b, y, S, A, T;
      const d = ((p = l == null ? void 0 : l.activeArmor) == null ? void 0 : p.id) === u.id ? l.activeArmor : null, f = a("armor", u.id), m = String((u == null ? void 0 : u.availability) ?? ((h = u == null ? void 0 : u.system) == null ? void 0 : h.availability) ?? "").trim(), g = B_({
        defenseBonus: u.defenseBonus,
        mitigationByType: (d == null ? void 0 : d.mitigationByType) ?? (d == null ? void 0 : d.typedMitigation) ?? u.mitigationByType ?? {},
        armorModifierLabels: r
      });
      return {
        id: u.id,
        accordionId: f,
        isExpanded: i(f),
        name: u.name,
        img: u.img,
        subtitle: (b = u.tags) != null && b.length ? u.tags.join(", ") : "Armor",
        summaryStats: Lf([
          { label: "Rating", value: fn((d == null ? void 0 : d.ratingCurrent) ?? u.rating, 0), emphasis: "strong" },
          { label: "Res", value: fn((d == null ? void 0 : d.baseMitigation) ?? (d == null ? void 0 : d.baseResistance), 0) },
          { label: "Def", value: fn(u.defenseBonus, 0) },
          { label: "Dur", value: `${fn(((y = d == null ? void 0 : d.durability) == null ? void 0 : y.current) ?? ((S = u.durability) == null ? void 0 : S.current), 0)}/${fn(((A = d == null ? void 0 : d.durability) == null ? void 0 : A.max) ?? ((T = u.durability) == null ? void 0 : T.max), 0)}` }
        ]),
        detailTags: $f([
          u.equipped ? "Equipped" : "",
          u.isPrimary ? "Primary" : "",
          m,
          ...Sr(u.traits ?? [])
        ]),
        detailRows: _f([
          { label: "Availability", value: m },
          { label: "Modifiers", value: g },
          { label: "Traits", value: Sr(u.traits ?? []).join(", ") },
          { label: "Tags", value: Sr(u.tags ?? []).join(", ") }
        ]),
        detailText: Of(u.notes),
        equipped: !!u.equipped,
        isPrimary: !!u.isPrimary
      };
    }),
    gear: ((e == null ? void 0 : e.gear) ?? []).map((u) => {
      const d = a("gear", u.id);
      return Dy({
        item: u,
        accordionId: d,
        itemType: "gear",
        defaultSubtitle: "Gear",
        categoryLabels: s,
        ratingLabel: "Rating",
        isEditable: n,
        isExpanded: i(d)
      });
    }),
    consumables: ((e == null ? void 0 : e.consumable) ?? []).map((u) => {
      const d = a("consumables", u.id);
      return Dy({
        item: u,
        accordionId: d,
        itemType: "consumable",
        defaultSubtitle: "Consumable",
        categoryLabels: o,
        ratingLabel: "Potency",
        typeLabel: "Consumable",
        isEditable: n,
        isExpanded: i(d)
      });
    })
  };
}
const hh = "playerGadgetRememberedSubject", U_ = /* @__PURE__ */ new Set([
  M.actorTypes.character,
  M.actorTypes.npc,
  "character",
  "npc"
]);
function Rs(t = null) {
  return (t == null ? void 0 : t.document) ?? t ?? null;
}
function Bc(t = null) {
  return t ? typeof t.values == "function" ? Array.from(t.values()) : Array.isArray(t) ? t : Array.from(t ?? []) : [];
}
function Md(t = (canvas == null ? void 0 : canvas.scene) ?? null) {
  var r, s;
  const e = Bc(t == null ? void 0 : t.tokens), n = Bc((r = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : r.placeables).map(Rs), i = Bc((s = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : s.controlled).map(Rs), a = /* @__PURE__ */ new Map();
  for (const o of [...i, ...n, ...e].map(Rs)) {
    const l = String((o == null ? void 0 : o.uuid) ?? (o == null ? void 0 : o.id) ?? "").trim();
    l && !a.has(l) && a.set(l, o);
  }
  return Array.from(a.values());
}
function K_(t = null, e = (game == null ? void 0 : game.user) ?? null) {
  return !t || !e ? !1 : typeof t.testUserPermission == "function" ? t.testUserPermission(e, "OWNER") : !!t.isOwner;
}
function v0(t = null, { user: e = (game == null ? void 0 : game.user) ?? null } = {}) {
  const n = Rs(t), i = (n == null ? void 0 : n.actor) ?? (t == null ? void 0 : t.actor) ?? null;
  return !n || !i || !U_.has(String(i.type ?? "").trim()) ? !1 : K_(i, e);
}
function Oy(t = null) {
  var i, a, r;
  const e = Rs(t), n = (e == null ? void 0 : e.actor) ?? (t == null ? void 0 : t.actor) ?? null;
  return !e || !n ? null : {
    actor: n,
    token: e,
    actorId: n.id ?? "",
    actorUuid: n.uuid ?? "",
    tokenId: e.id ?? "",
    tokenUuid: e.uuid ?? "",
    sceneId: ((i = e.parent) == null ? void 0 : i.id) ?? ((a = canvas == null ? void 0 : canvas.scene) == null ? void 0 : a.id) ?? "",
    name: String(e.name ?? n.name ?? "Combatant").trim() || "Combatant",
    img: ((r = e.texture) == null ? void 0 : r.src) ?? n.img ?? "icons/svg/mystery-man.svg"
  };
}
function C0(t = null, { user: e = (game == null ? void 0 : game.user) ?? null } = {}) {
  var l, c;
  const n = (t == null ? void 0 : t.actor) ?? t ?? null, i = (t == null ? void 0 : t.token) ?? null, a = String((e == null ? void 0 : e.id) ?? "").trim() || "user", r = String((t == null ? void 0 : t.sceneId) ?? ((l = i == null ? void 0 : i.parent) == null ? void 0 : l.id) ?? ((c = canvas == null ? void 0 : canvas.scene) == null ? void 0 : c.id) ?? "").trim() || "actor", s = String((t == null ? void 0 : t.tokenId) ?? (i == null ? void 0 : i.id) ?? "").trim() || "actor", o = String((t == null ? void 0 : t.actorId) ?? (n == null ? void 0 : n.id) ?? "").trim() || "unknown";
  return `${a}:${r}:${s}:${o}`;
}
function H_(t = I) {
  var e, n;
  try {
    return String(((n = (e = game.settings) == null ? void 0 : e.get) == null ? void 0 : n.call(e, t, hh)) ?? "").trim();
  } catch {
    return "";
  }
}
async function j_(t = null, { systemId: e = I } = {}) {
  var i, a;
  const n = String((t == null ? void 0 : t.tokenUuid) ?? ((i = t == null ? void 0 : t.token) == null ? void 0 : i.uuid) ?? "").trim();
  !n || !((a = game.settings) != null && a.set) || await game.settings.set(e, hh, n);
}
function Ly(t = "", e = Md()) {
  const n = String(t ?? "").trim();
  return n ? e.find((i) => String((i == null ? void 0 : i.uuid) ?? "").trim() === n) ?? null : null;
}
function G_(t = (game == null ? void 0 : game.user) ?? null) {
  var i, a, r, s;
  const e = (t == null ? void 0 : t.character) ?? null;
  if (e) return e;
  const n = String(((i = t == null ? void 0 : t.character) == null ? void 0 : i.id) ?? ((a = t == null ? void 0 : t.data) == null ? void 0 : a.character) ?? "").trim();
  return n ? ((s = (r = game.actors) == null ? void 0 : r.get) == null ? void 0 : s.call(r, n)) ?? null : null;
}
function q_(t = null, e = Md()) {
  const n = String((t == null ? void 0 : t.uuid) ?? "").trim(), i = String((t == null ? void 0 : t.id) ?? "").trim();
  return !n && !i ? null : e.find((a) => {
    const r = (a == null ? void 0 : a.actor) ?? null;
    return String((r == null ? void 0 : r.uuid) ?? "").trim() === n || String((r == null ? void 0 : r.id) ?? "").trim() === i;
  }) ?? null;
}
function W_({ user: t = (game == null ? void 0 : game.user) ?? null, scene: e = (canvas == null ? void 0 : canvas.scene) ?? null } = {}) {
  return Md(e).filter((n) => v0(n, { user: t }));
}
function fm({
  user: t = (game == null ? void 0 : game.user) ?? null,
  scene: e = (canvas == null ? void 0 : canvas.scene) ?? null,
  rememberedTokenUuid: n = H_(),
  preferredTokenUuid: i = ""
} = {}) {
  var f;
  const r = Md(e).filter((m) => v0(m, { user: t })), s = Bc((f = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : f.controlled).map(Rs).find((m) => r.includes(m) || r.some((g) => g.uuid && g.uuid === (m == null ? void 0 : m.uuid))), o = Ly(i, r), l = Ly(n, r), c = q_(G_(t), r), u = s ?? o ?? l ?? c ?? r[0] ?? null, d = Oy(u);
  return {
    subject: d,
    eligible: r.map(Oy).filter(Boolean),
    emptyReason: d ? "" : "No owned character or NPC tokens are available on the current scene."
  };
}
const bh = "playerManualModifierPresets", I0 = "playerGadget", E0 = Object.freeze([
  Object.freeze({
    id: "cover",
    label: "Cover / Visibility",
    presets: Object.freeze([
      Object.freeze({ id: "cover.light", label: "Light Cover", value: -1 }),
      Object.freeze({ id: "cover.moderate", label: "Moderate Cover", value: -2 }),
      Object.freeze({ id: "cover.heavy", label: "Heavy Cover", value: -3 }),
      Object.freeze({ id: "cover.full", label: "Full Cover", value: -4 }),
      Object.freeze({ id: "visibility.obscured", label: "Obscured", value: -2 })
    ])
  }),
  Object.freeze({
    id: "target",
    label: "Target State",
    presets: Object.freeze([
      Object.freeze({ id: "target.surprised", label: "Target Stunned / Surprised", value: 2 }),
      Object.freeze({ id: "target.immobile", label: "Target Immobile", value: 4 }),
      Object.freeze({ id: "target.proneMelee", label: "Prone Target in Melee", value: 2 }),
      Object.freeze({ id: "target.proneRanged", label: "Prone Target at Range", value: -1 }),
      Object.freeze({ id: "target.overloaded", label: "Target Overloaded", value: 1 })
    ])
  }),
  Object.freeze({
    id: "attacker",
    label: "Attacker State",
    presets: Object.freeze([
      Object.freeze({ id: "attacker.encumbered", label: "Attacker Encumbered", value: -1 })
    ])
  }),
  Object.freeze({
    id: "attack",
    label: "Attack Behavior",
    presets: Object.freeze([
      Object.freeze({ id: "attack.areaSplash", label: "Area / Splash Attack", value: 2 })
    ])
  })
]);
function V_(t) {
  var e, n, i;
  return ((i = (n = (e = globalThis.foundry) == null ? void 0 : e.utils) == null ? void 0 : n.deepClone) == null ? void 0 : i.call(n, t)) ?? JSON.parse(JSON.stringify(t ?? null));
}
function $y() {
  var t, e, n;
  return ((n = (e = (t = globalThis.foundry) == null ? void 0 : t.utils) == null ? void 0 : e.randomID) == null ? void 0 : n.call(e)) ?? Math.random().toString(36).slice(2, 10);
}
function J_(t) {
  const e = Number(t ?? 0) || 0;
  return e > 0 ? `+${e}` : String(e);
}
function so(t = {}, { fallbackSource: e = "playerPreset" } = {}) {
  const n = Number((t == null ? void 0 : t.value) ?? 0), i = String((t == null ? void 0 : t.label) ?? "").trim();
  return !i || !Number.isFinite(n) || n === 0 ? null : {
    id: String((t == null ? void 0 : t.id) ?? $y()).trim() || $y(),
    label: i,
    value: n,
    enabled: (t == null ? void 0 : t.enabled) !== !1,
    consumeOnce: (t == null ? void 0 : t.consumeOnce) !== !1,
    source: String((t == null ? void 0 : t.source) ?? e).trim() || e
  };
}
function N0(t = {}) {
  const e = String((t == null ? void 0 : t.id) ?? "").trim();
  return so({
    id: e ? `situational:${e}` : "",
    label: t == null ? void 0 : t.label,
    value: t == null ? void 0 : t.value,
    enabled: !0,
    consumeOnce: !0,
    source: "systemPreset"
  }, { fallbackSource: "systemPreset" });
}
function Y_() {
  return E0.map((t) => ({
    ...t,
    presets: t.presets.map((e) => ({
      ...e,
      row: N0(e),
      valueLabel: J_(e.value)
    }))
  }));
}
function P0(t = I) {
  var e, n;
  try {
    const i = (n = (e = game.settings) == null ? void 0 : e.get) == null ? void 0 : n.call(e, t, bh);
    return i && typeof i == "object" && !Array.isArray(i) ? V_(i) : {};
  } catch {
    return {};
  }
}
async function Q_(t = {}, e = I) {
  var n;
  (n = game.settings) != null && n.set && await game.settings.set(e, bh, t);
}
function Wa(t, { systemId: e = I } = {}) {
  const n = C0(t), i = P0(e);
  return (Array.isArray(i[n]) ? i[n] : []).map((a) => so(a)).filter(Boolean);
}
async function Sl(t, e = [], { systemId: n = I } = {}) {
  const i = C0(t), a = P0(n);
  return a[i] = e.map((r) => so(r)).filter(Boolean), await Q_(a, n), a[i];
}
async function _y(t, e = {}, { systemId: n = I } = {}) {
  const i = so(e);
  if (!i) return Wa(t, { systemId: n });
  const a = Wa(t, { systemId: n }), r = a.findIndex((s) => s.id === i.id);
  return r >= 0 ? a[r] = i : a.push(i), Sl(t, a, { systemId: n });
}
async function X_(t, e = "", { systemId: n = I } = {}) {
  const i = String(e ?? "").trim(), a = Wa(t, { systemId: n }).filter((r) => r.id !== i);
  return Sl(t, a, { systemId: n });
}
function Z_(t = {}) {
  return String((t == null ? void 0 : t.source) ?? "").trim() === I0 && (t == null ? void 0 : t.applyManualModifierPresets) === !0;
}
function eB(t = {}, { subject: e = null, rows: n = null, systemId: i = I } = {}) {
  const a = (Array.isArray(n) ? n : Wa(e, { systemId: i })).map((l) => so(l)).filter((l) => l == null ? void 0 : l.enabled);
  if (!a.length) return t;
  const r = Array.isArray(t.manualModifiers) ? t.manualModifiers : [], s = new Set(r.map((l) => String((l == null ? void 0 : l.id) ?? "").trim()).filter(Boolean)), o = a.filter((l) => !s.has(l.id)).map((l) => ({
    id: l.id,
    label: l.label,
    value: l.value
  }));
  return {
    ...t,
    manualModifiers: [...r, ...o]
  };
}
async function tB(t, { rows: e = null, systemId: n = I } = {}) {
  const i = Array.isArray(e) ? e : Wa(t, { systemId: n }), a = i.filter((r) => !r.enabled || !r.consumeOnce);
  return a.length === i.length ? i : Sl(t, a, { systemId: n });
}
const { ApplicationV2: nB, HandlebarsApplicationMixin: iB } = foundry.applications.api, vd = "enablePlayerGadget", R0 = "playerGadgetAutoOpen", aB = "mwd-player-gadget", rB = `systems/${I}/templates/v2/mwd-player-gadget.hbs`;
function By(t, e = null) {
  const n = String(t ?? "").trim();
  if (!n) return e;
  try {
    return JSON.parse(n);
  } catch (i) {
    return console.warn("MWD | Invalid player gadget payload", n, i), e;
  }
}
function sB(t) {
  const e = Number(t ?? 0) || 0;
  return e > 0 ? `+${e}` : String(e);
}
function oB() {
  var t, e, n;
  return ((t = game.mwd) == null ? void 0 : t.roll) ?? ((n = (e = game.system) == null ? void 0 : e.mwd) == null ? void 0 : n.roll) ?? null;
}
function gm() {
  var t, e, n;
  return ((t = game.mwd) == null ? void 0 : t.personalCombatActions) ?? ((n = (e = game.system) == null ? void 0 : e.mwd) == null ? void 0 : n.personalCombatActions) ?? null;
}
function lB(t = null) {
  var e, n, i;
  return ((e = t == null ? void 0 : t.isPersonalWeapon) == null ? void 0 : e.call(t)) && ((n = t.system) == null ? void 0 : n.equipped) !== !1 && ((i = t.system) == null ? void 0 : i.carried) !== !1;
}
function cB(t = null) {
  return Array.from((t == null ? void 0 : t.items) ?? []).filter(lB).map((e) => {
    var a, r, s, o, l, c;
    const n = ((a = e.getCombatProfile) == null ? void 0 : a.call(e)) ?? null, i = eh(t, e);
    return {
      id: e.id,
      name: String(e.name ?? "Weapon").trim() || "Weapon",
      img: e.img ?? "icons/svg/sword.svg",
      subtitle: ((r = n == null ? void 0 : n.skillDef) == null ? void 0 : r.label) ?? (n == null ? void 0 : n.skill) ?? "Attack",
      stats: [
        { label: "DV", value: String(Number((n == null ? void 0 : n.damage) ?? ((s = e.system) == null ? void 0 : s.damage) ?? 0) || 0) },
        { label: "AP", value: String(Number((n == null ? void 0 : n.ap) ?? ((o = e.system) == null ? void 0 : o.ap) ?? 0) || 0) },
        { label: "Range", value: String(((l = n == null ? void 0 : n.range) == null ? void 0 : l.max) ?? ((c = e.system) == null ? void 0 : c.range) ?? "").trim() || "-" }
      ],
      disabled: !!i,
      reason: i
    };
  });
}
function uB(t = I) {
  var e;
  return (e = game.user) != null && e.isGM || !game.settings.get(t, vd) || !game.settings.get(t, R0) ? !1 : W_().length > 0;
}
function dB(t = I) {
  game.settings.register(t, vd, {
    name: "Enable Player Gadget",
    hint: "If enabled, non-GM users can use the compact Player Gadget combat surface.",
    scope: "world",
    config: !0,
    type: Boolean,
    default: !0
  }), game.settings.register(t, R0, {
    name: "Auto-open Player Gadget",
    hint: "If enabled, opens the Player Gadget when you enter a scene with an owned personal token.",
    scope: "client",
    config: !0,
    type: Boolean,
    default: !0
  }), game.settings.register(t, hh, {
    scope: "client",
    config: !1,
    type: String,
    default: ""
  }), game.settings.register(t, bh, {
    scope: "client",
    config: !1,
    type: Object,
    default: {}
  });
}
const sn = class sn extends iB(nB) {
  constructor({ systemId: e = I, ...n } = {}) {
    super(n), this.systemId = e, this.activeTab = "actions", this.preferredTokenUuid = "";
  }
  async render(e = {}) {
    var n;
    return (n = game.user) != null && n.isGM ? this : game.settings.get(this.systemId, vd) ? super.render(e) : this;
  }
  async _prepareContext(e) {
    var d, f, m, g;
    const n = await super._prepareContext(e), { subject: i, eligible: a, emptyReason: r } = fm({
      preferredTokenUuid: this.preferredTokenUuid
    }), s = (i == null ? void 0 : i.actor) ?? null, o = (i == null ? void 0 : i.token) ?? null, l = s ? D.getSnapshot(s, { token: o }) : null, c = s && l ? D.buildActionModel(s, l) : { utilityButtons: [], summaryPills: [], activationLog: [], menus: [] }, u = i ? Wa(i, { systemId: this.systemId }) : [];
    return foundry.utils.mergeObject(n, {
      currentTab: this.activeTab,
      hasSubject: !!i,
      emptyReason: r,
      subject: i,
      subjects: a.map((p) => ({
        ...p,
        selected: p.tokenUuid === (i == null ? void 0 : i.tokenUuid)
      })),
      summary: s && l ? {
        combatDashboard: fh(l, { actor: s }),
        activeCriticals: ph(s),
        speed: gh(s),
        monitors: mh(s),
        burn: {
          value: Number(((f = (d = s.system) == null ? void 0 : d.burn) == null ? void 0 : f.value) ?? 0) || 0,
          overloaded: !!((g = (m = s.system) == null ? void 0 : m.burn) != null && g.overloaded)
        }
      } : null,
      combatActions: c,
      attacks: s ? cB(s) : [],
      situationalPresetGroups: Y_(),
      presets: u.map((p) => ({
        ...p,
        valueLabel: sB(p.value)
      })),
      newPreset: {
        label: "",
        value: 1
      }
    }, { inplace: !1 });
  }
  async selectSubject(e = "") {
    this.preferredTokenUuid = String(e ?? "").trim();
    const { subject: n } = fm({ preferredTokenUuid: this.preferredTokenUuid });
    return n && await j_(n, { systemId: this.systemId }), this.render({ force: !0 }), n;
  }
  toggle() {
    return this.rendered ? this.close() : this.render({ force: !0 });
  }
  getSubject() {
    return fm({ preferredTokenUuid: this.preferredTokenUuid }).subject;
  }
  async _onSwitchTab(e, n) {
    var i, a;
    (i = e == null ? void 0 : e.preventDefault) == null || i.call(e), this.activeTab = String(((a = n == null ? void 0 : n.dataset) == null ? void 0 : a.tab) ?? "actions").trim() || "actions", this.render(!1);
  }
  async _onSelectSubject(e, n) {
    var r, s, o, l, c;
    (r = e == null ? void 0 : e.preventDefault) == null || r.call(e);
    const i = ((s = n == null ? void 0 : n.closest) == null ? void 0 : s.call(n, ".mwd-player-gadget__subject-picker")) ?? this.element, a = (n == null ? void 0 : n.value) ?? ((o = n == null ? void 0 : n.dataset) == null ? void 0 : o.tokenUuid) ?? ((c = (l = i == null ? void 0 : i.querySelector) == null ? void 0 : l.call(i, "[name='subject-token']")) == null ? void 0 : c.value) ?? "";
    await this.selectSubject(a);
  }
  async _onRoll(e, n) {
    var s, o, l;
    (s = e == null ? void 0 : e.preventDefault) == null || s.call(e);
    const i = this.getSubject();
    if (!(i != null && i.actor)) return;
    const a = By((o = n == null ? void 0 : n.dataset) == null ? void 0 : o.roll, null);
    if (!a) return;
    const r = oB();
    if (!(r != null && r.execute)) {
      (l = ui.notifications) == null || l.error("MWD roll system not initialized.");
      return;
    }
    await r.execute({
      actor: i.actor,
      payload: a,
      event: e,
      uiState: {
        source: I0,
        applyManualModifierPresets: !0,
        subject: i
      }
    }), this.render({ force: !0 });
  }
  async _onCombatIntent(e, n) {
    var s, o, l, c, u, d;
    (s = e == null ? void 0 : e.preventDefault) == null || s.call(e);
    const i = this.getSubject();
    if (!(i != null && i.actor)) return;
    const a = By((o = n == null ? void 0 : n.dataset) == null ? void 0 : o.combatPayload, null) ?? { intent: "combatAction", actionId: String(((l = n == null ? void 0 : n.dataset) == null ? void 0 : l.combatAction) ?? "").trim() };
    if (!(a != null && a.intent)) return;
    const r = await ((u = (c = gm()) == null ? void 0 : c.execute) == null ? void 0 : u.call(c, {
      actor: i.actor,
      token: i.token,
      payload: a,
      event: e
    }));
    if (r != null && r.cancelled) {
      this.render(!1);
      return;
    }
    if (!(r != null && r.ok)) {
      (d = ui.notifications) == null || d.warn((r == null ? void 0 : r.reason) ?? "Unable to perform action.");
      return;
    }
    this.render({ force: !0 });
  }
  async _onRemoveActivationAction(e, n) {
    var s, o, l, c, u;
    (s = e == null ? void 0 : e.preventDefault) == null || s.call(e);
    const i = this.getSubject(), a = Number(((o = n == null ? void 0 : n.dataset) == null ? void 0 : o.logIndex) ?? -1);
    if (!(i != null && i.actor) || !Number.isInteger(a) || a < 0) return;
    const r = await ((c = (l = gm()) == null ? void 0 : l.removeActivationLogEntry) == null ? void 0 : c.call(l, {
      actor: i.actor,
      token: i.token,
      index: a
    }));
    if (!(r != null && r.ok)) {
      (u = ui.notifications) == null || u.warn((r == null ? void 0 : r.reason) ?? "Unable to remove action.");
      return;
    }
    this.render({ force: !0 });
  }
  async _onAttackWeapon(e, n) {
    var o, l, c, u, d, f, m;
    (o = e == null ? void 0 : e.preventDefault) == null || o.call(e);
    const i = this.getSubject(), a = String(((l = n == null ? void 0 : n.dataset) == null ? void 0 : l.itemId) ?? "").trim(), r = ((d = (u = (c = i == null ? void 0 : i.actor) == null ? void 0 : c.items) == null ? void 0 : u.get) == null ? void 0 : d.call(u, a)) ?? null;
    if (!r) return;
    const s = await ((m = (f = gm()) == null ? void 0 : f.executeOwnedWeaponAttack) == null ? void 0 : m.call(f, {
      weapon: r,
      event: e,
      token: i.token
    }));
    s != null && s.ok && this.render({ force: !0 });
  }
  async _onOpenStatus(e) {
    var i;
    (i = e == null ? void 0 : e.preventDefault) == null || i.call(e);
    const n = this.getSubject();
    !(n != null && n.actor) || !(n != null && n.token) || (await Yu({ actor: n.actor, token: n.token }), this.render({ force: !0 }));
  }
  async _onAddSituationalPreset(e, n) {
    var o, l;
    (o = e == null ? void 0 : e.preventDefault) == null || o.call(e);
    const i = this.getSubject(), a = String(((l = n == null ? void 0 : n.dataset) == null ? void 0 : l.presetId) ?? "").trim(), r = E0.flatMap((c) => c.presets).find((c) => c.id === a), s = N0(r);
    !i || !s || (await _y(i, s, { systemId: this.systemId }), this.render({ force: !0 }));
  }
  async _onAddPlayerPreset(e, n) {
    var l, c, u, d, f, m, g, p;
    (l = e == null ? void 0 : e.preventDefault) == null || l.call(e);
    const i = this.getSubject(), a = ((c = n == null ? void 0 : n.closest) == null ? void 0 : c.call(n, ".mwd-player-gadget__manual-add")) ?? this.element, r = String(((d = (u = a == null ? void 0 : a.querySelector) == null ? void 0 : u.call(a, "[name='preset-label']")) == null ? void 0 : d.value) ?? "").trim(), s = Number(((m = (f = a == null ? void 0 : a.querySelector) == null ? void 0 : f.call(a, "[name='preset-value']")) == null ? void 0 : m.value) ?? 0), o = so({
      label: r,
      value: s,
      enabled: !0,
      consumeOnce: !!(((p = (g = a == null ? void 0 : a.querySelector) == null ? void 0 : g.call(a, "[name='preset-once']")) == null ? void 0 : p.checked) ?? !0),
      source: "playerPreset"
    });
    !i || !o || (await _y(i, o, { systemId: this.systemId }), this.render({ force: !0 }));
  }
  async _onTogglePlayerPreset(e, n) {
    var s, o;
    (s = e == null ? void 0 : e.preventDefault) == null || s.call(e);
    const i = this.getSubject(), a = String(((o = n == null ? void 0 : n.dataset) == null ? void 0 : o.presetId) ?? "").trim();
    if (!i || !a) return;
    const r = Wa(i, { systemId: this.systemId }).map((l) => l.id === a ? { ...l, enabled: !l.enabled } : l);
    await Sl(i, r, { systemId: this.systemId }), this.render({ force: !0 });
  }
  async _onTogglePlayerPresetOnce(e, n) {
    var s, o;
    (s = e == null ? void 0 : e.preventDefault) == null || s.call(e);
    const i = this.getSubject(), a = String(((o = n == null ? void 0 : n.dataset) == null ? void 0 : o.presetId) ?? "").trim();
    if (!i || !a) return;
    const r = Wa(i, { systemId: this.systemId }).map((l) => l.id === a ? { ...l, consumeOnce: !l.consumeOnce } : l);
    await Sl(i, r, { systemId: this.systemId }), this.render({ force: !0 });
  }
  async _onRemovePlayerPreset(e, n) {
    var r, s;
    (r = e == null ? void 0 : e.preventDefault) == null || r.call(e);
    const i = this.getSubject(), a = String(((s = n == null ? void 0 : n.dataset) == null ? void 0 : s.presetId) ?? "").trim();
    !i || !a || (await X_(i, a, { systemId: this.systemId }), this.render({ force: !0 }));
  }
  async _onOpenFullSheet(e) {
    var i, a;
    (i = e == null ? void 0 : e.preventDefault) == null || i.call(e);
    const n = this.getSubject();
    (a = n == null ? void 0 : n.actor) != null && a.sheet && (n.actor.sheet.render(!0), await this.close());
  }
};
F(sn, "DEFAULT_OPTIONS", {
  id: aB,
  classes: ["mwd-player-gadget"],
  window: {
    title: "MWD Player Gadget",
    popOut: !0,
    resizable: !0,
    minimizable: !0
  },
  position: {
    width: 360,
    height: 560
  },
  actions: {
    switchTab: sn.prototype._onSwitchTab,
    selectSubject: sn.prototype._onSelectSubject,
    roll: sn.prototype._onRoll,
    combatIntent: sn.prototype._onCombatIntent,
    removeActivationAction: sn.prototype._onRemoveActivationAction,
    attackWeapon: sn.prototype._onAttackWeapon,
    openStatus: sn.prototype._onOpenStatus,
    addSituationalPreset: sn.prototype._onAddSituationalPreset,
    addPlayerPreset: sn.prototype._onAddPlayerPreset,
    togglePlayerPreset: sn.prototype._onTogglePlayerPreset,
    togglePlayerPresetOnce: sn.prototype._onTogglePlayerPresetOnce,
    removePlayerPreset: sn.prototype._onRemovePlayerPreset,
    openFullSheet: sn.prototype._onOpenFullSheet
  }
}), F(sn, "PARTS", {
  body: { template: rB }
});
let Bf = sn, pm = null;
function D0({ systemId: t = I } = {}) {
  return pm || (pm = new Bf({ systemId: t })), pm;
}
function mB({ systemId: t = I } = {}) {
  const e = () => D0({ systemId: t });
  return {
    render: (n = { force: !0 }) => e().render(n),
    toggle: () => e().toggle(),
    selectSubject: (n) => e().selectSubject(n),
    get app() {
      return e();
    }
  };
}
function fB({ systemId: t = I } = {}) {
  uB(t) && D0({ systemId: t }).render({ force: !0 });
}
const _o = {
  /**
   * Hook to declare template data migrations
   */
  DECLARE_MIGRATIONS: "anarchy-declareMigration",
  /**
   * Hook used to declare additional styles available
   */
  REGISTER_STYLES: "anarchy-registerStyles",
  /**
   * Hook allowing to register additional roll parameters
   */
  REGISTER_ROLL_PARAMETERS: "anarchy-registerRollParameters",
  /**
   * Hook allowing to modify some parameters (from Anarchy hacks modules).
   * Setting property ignore=true allows to remove the parameter.
   */
  MODIFY_ROLL_PARAMETER: "anarchy-forbidRollParameter",
  /**
   * Hook allowing to provide alternate skill sets for Anarchy hack modules
   */
  PROVIDE_SKILL_SET: "anarchy-provideSkillSet",
  /**
   * Hook allowing to provide alternate way to apply damages for Anarchy hack modules
   */
  PROVIDE_DAMAGE_MODE: "anarchy-provideDamageMode"
}, xy = "anarchy-";
globalThis.ANARCHY_HOOKS = _o;
class Cd {
  constructor() {
    this.hooks = [], Hooks.on("getSceneControlButtons", (e) => {
      var i, a, r;
      const n = Array.isArray(e) ? e.find((s) => s.name === "notes") : e == null ? void 0 : e.notes;
      if (!n) {
        const s = Array.isArray(e) ? e.map((o) => o.name) : Object.keys(e ?? {});
        console.warn("MWD: token controls not found. Available:", s);
        return;
      }
      if (n.tools = n.tools ?? {}, (i = game.user) != null && i.isGM) {
        if (n.tools["mwd-gm-gadget"]) return;
        n.tools["mwd-gm-gadget"] = {
          name: "mwd-gm-gadget",
          title: "Open GM Gadget",
          icon: "fa-solid fa-sliders",
          order: 990,
          button: !0,
          visible: !0,
          onChange: () => {
            var s, o;
            return (o = (s = game.mwd) == null ? void 0 : s.gmGadget) == null ? void 0 : o.call(s);
          }
        };
        return;
      }
      (r = (a = game.settings) == null ? void 0 : a.get) != null && r.call(a, I, vd) && (n.tools["mwd-player-gadget"] || (n.tools["mwd-player-gadget"] = {
        name: "mwd-player-gadget",
        title: "Open Player Gadget",
        icon: "fa-solid fa-gamepad",
        order: 991,
        button: !0,
        visible: !0,
        onChange: () => {
          var s, o, l;
          return (l = (o = (s = game.mwd) == null ? void 0 : s.playerGadget) == null ? void 0 : o.toggle) == null ? void 0 : l.call(o);
        }
      }));
    });
  }
  static instance() {
    return game.system.anarchy.hooks;
  }
  static register(e) {
    Cd.instance()._register(e);
  }
  _register(e) {
    if (console.log(tt + "HooksManager.register", e), !e.startsWith(xy))
      throw `For safety Anarchy Hooks names must be prefixed by '${xy}'`;
    this.hooks.push(e);
  }
}
const zy = {
  none: { targets: 1, adjust: [0] },
  shotgun: { targets: 2, adjust: [0, -2] },
  circle: { targets: void 0 },
  cone: { targets: void 0 },
  rect: { targets: void 0 },
  ray: { targets: void 0 }
}, gB = {
  code: "weapon-range",
  options: {
    flags: { editable: !0 },
    order: 20,
    category: jg.pool,
    labelkey: z.common.roll.modifiers.weaponRange,
    hbsTemplateRoll: `${ht}/roll/parts/select-option.hbs`,
    hbsTemplateChat: void 0
    //``
  },
  isUsed: (t) => !0,
  condition: (t) => t.weapon,
  factory: (t) => {
    const e = t.weapon.getRanges(), n = e.map((i) => i.value);
    return {
      value: e[0].value,
      min: Math.min(...n),
      max: Math.max(...n),
      choices: e,
      selected: e[0].labelkey
    };
  }
}, pB = {
  code: "weapon-area",
  options: {
    used: !0,
    order: 20,
    category: jg.pool,
    labelkey: z.common.roll.modifiers.weaponArea,
    hbsTemplateRoll: `${ht}/roll/parts/input-numeric.hbs`,
    hbsTemplateChat: void 0
    //``
  },
  isUsed: (t) => t.used,
  condition: (t) => t.weapon && t.weapon.getArea() != M.area.none,
  factory: (t) => {
    var i;
    const e = ((i = t.targeting.targetedTokenIds) == null ? void 0 : i.length) ?? 1, n = t.weapon.getAreaModifier(e);
    return {
      value: n,
      min: Math.min(0, n),
      max: Math.max(0, n),
      used: e > 1
    };
  }
}, Ve = class Ve extends Vs {
  static buildDefaultUnarmedProfile(e = null) {
    var a, r, s, o, l, c, u, d;
    const n = Math.max(0, Number(
      ((a = e == null ? void 0 : e.getAttributeValue) == null ? void 0 : a.call(e, M.actorAttributes.strength)) ?? ((o = (s = (r = e == null ? void 0 : e.system) == null ? void 0 : r.attributes) == null ? void 0 : s.strength) == null ? void 0 : o.value) ?? 0
    ) || 0), i = Math.max(0, Number(
      ((l = e == null ? void 0 : e.getAttributeValue) == null ? void 0 : l.call(e, M.actorAttributes.reflexes)) ?? ((d = (u = (c = e == null ? void 0 : e.system) == null ? void 0 : c.attributes) == null ? void 0 : u.reflexes) == null ? void 0 : d.value) ?? 0
    ) || 0);
    return {
      ...foundry.utils.deepClone(this.DEFAULT_UNARMED),
      damage: Math.ceil(n / 2),
      attackRatingBand: {
        ...this.DEFAULT_UNARMED.attackRatingBand,
        close: i
      },
      range: {
        ...this.DEFAULT_UNARMED.range,
        max: "close"
      },
      uuid: null,
      img: null,
      item: null,
      equipped: !0,
      isPrimary: !1,
      defaultRangeBand: "close",
      isSynthetic: !0
    };
  }
  static init() {
    Hooks.once(_o.REGISTER_ROLL_PARAMETERS, (e) => {
      e(pB), e(gB);
    });
  }
  prepareBaseData() {
    if (super.prepareBaseData(), (this.canonicalType ?? this.type) !== M.itemType.personalWeapon) return;
    const e = this.system ?? {};
    e.equipped = !!e.equipped, e.isPrimary = !!e.isPrimary, e.category = String(e.category ?? e.weaponCategory ?? "ranged").trim() || "ranged", e.skill = String(e.skill ?? "firearms").trim() || "firearms", e.ap = Number(e.ap ?? e.armorPiercing ?? 0) || 0, e.damage = Number(e.damage ?? 0) || 0, e.damageType = Xn(e.damageType), e.attackRatingBand = Ve.normalizeAttackRatingBand(e.attackRatingBand), e.range = Ve.normalizePersonalRangeData(e.range), e.scale = Is(e.scale, "personal"), e.mount = dl(e.mount), e.traits = Ve.normalizeTraits(e.traits), e.notes = String(e.notes ?? "").trim();
  }
  static maxIndex(e) {
    const n = Ve.RANGE_ORDER.indexOf(e);
    return n >= 0 ? n : Ve.RANGE_ORDER.indexOf("near");
  }
  /**
    * Compute UI-friendly range band data:
    * - cap: normalized max band
    * - bands: [{key, allowed, value}]
    * - optimalKey: highest value among allowed (tie -> closest)
    */
  static getRangeBands(e) {
    const n = e ?? {}, i = Ve.normalizeRangeKey(n.max ?? "near"), a = Ve.maxIndex(i), r = Ve.RANGE_ORDER.map((l, c) => ({
      key: l,
      allowed: c <= a,
      value: Number(n[l] ?? (l === "extreme" && n.long !== void 0 ? n.long : 0))
    }));
    let s = "close", o = -1 / 0;
    for (const l of r)
      l.allowed && l.value > o && (o = l.value, s = l.key);
    return { cap: i, bands: r, optimalKey: s };
  }
  static get defaultIcon() {
    return "systems/mwd/img/colt-m1911.svg";
  }
  static defaultIconForType(e) {
    return e === M.itemType.mechWeapon ? "systems/mwd/img/icons/systems/upgrades/grenade_launcher_underbarrel.svg" : this.defaultIcon;
  }
  static normalizeTraits(e) {
    return Jo(e);
  }
  static normalizePersonalRangeData(e) {
    const n = jp(e);
    return n.max = Ve.normalizeRangeKey(n.max ?? (e == null ? void 0 : e.max) ?? "extreme"), n;
  }
  static normalizeRangeData(e) {
    return {
      max: Ve.normalizeRangeKey((e == null ? void 0 : e.max) ?? "near"),
      close: Number((e == null ? void 0 : e.close) ?? (e == null ? void 0 : e.short) ?? 0) || 0,
      near: Number((e == null ? void 0 : e.near) ?? (e == null ? void 0 : e.medium) ?? 0) || 0,
      far: Number((e == null ? void 0 : e.far) ?? (e == null ? void 0 : e.long) ?? 0) || 0,
      extreme: Number((e == null ? void 0 : e.extreme) ?? 0) || 0
    };
  }
  static normalizeAttackRatingBand(e) {
    return {
      close: Number((e == null ? void 0 : e.close) ?? (e == null ? void 0 : e.short) ?? 0) || 0,
      near: Number((e == null ? void 0 : e.near) ?? (e == null ? void 0 : e.medium) ?? 0) || 0,
      far: Number((e == null ? void 0 : e.far) ?? (e == null ? void 0 : e.long) ?? 0) || 0,
      extreme: Number((e == null ? void 0 : e.extreme) ?? 0) || 0
    };
  }
  getCombatProfile(e = {}) {
    if ((this.canonicalType ?? this.type) === M.itemType.personalWeapon)
      return super.getCombatProfile(e);
    const n = this.system ?? {}, i = this.canonicalType ?? this.type, a = i === M.itemType.personalWeapon ? Ve.normalizePersonalRangeData(n.range) : Ve.normalizeRangeData(n.range), r = String(n.skill ?? "").trim(), s = Ft(r), o = Number(n.damage ?? 0) || 0, l = Number(n.ap ?? n.armorPiercing ?? 0) || 0, c = String(n.category ?? n.weaponCategory ?? "ranged").trim() || "ranged", u = Ve.normalizeTraits(n.traits), d = i === M.itemType.personalWeapon ? Xn(n.damageType) : ei(n.damageType, "energy");
    return {
      id: this.id ?? "weapon",
      uuid: this.uuid ?? null,
      name: this.name ?? "Weapon",
      img: this.img,
      item: this,
      type: i,
      equipped: !!n.equipped,
      isPrimary: !!n.isPrimary,
      scale: i === M.itemType.personalWeapon ? Is(n.scale, "personal") : "machine",
      mount: dl(n.mount),
      category: c,
      skill: r || "firearms",
      skillDef: s,
      damage: o,
      ap: l,
      baseDamageType: d,
      baseDamageTypeLabel: i === M.itemType.personalWeapon ? In(d) : En(d),
      damageType: d,
      damageTypeLabel: i === M.itemType.personalWeapon ? In(d) : En(d),
      attackRatingBand: Ve.normalizeAttackRatingBand(n.attackRatingBand),
      range: a,
      defaultRangeBand: this.getDefaultRangeBand(a),
      traits: u,
      effects: {},
      notes: String(n.notes ?? n.description ?? "").trim()
    };
  }
  getDefaultRangeBand(e = Ve.normalizeRangeData(((n) => (n = this.system) == null ? void 0 : n.range)())) {
    const i = ["near", "close", "far", "extreme"], a = Ve.maxIndex(e.max);
    return i.find((r) => Ve.RANGE_ORDER.indexOf(r) <= a) ?? "close";
  }
  getWeaponSkill() {
    var i;
    const e = (i = this.actor) == null ? void 0 : i.items.find(
      (a) => a.type === M.itemType.skill && a.system.code === this.system.skill
    );
    if (e) return e;
    const n = Ft(String(this.system.skill ?? "").trim());
    return n ? {
      name: n.label,
      system: {
        code: n.code,
        attribute: n.attribute,
        value: 0
      }
    } : null;
  }
  getDefense() {
    if ((this.canonicalType ?? this.type) !== M.itemType.personalWeapon)
      return this.system.defense ? _t.fixedDefenseCode(this.system.defense) : void 0;
    if (this.system.defense)
      return _t.fixedDefenseCode(this.system.defense);
    const e = Ft(String(this.system.skill ?? "").trim());
    return e != null && e.defense ? _t.fixedDefenseCode(e.defense) : void 0;
  }
  getDamage() {
    if (!this.parent)
      return;
    const e = this._getMonitor(), n = this.system.damageAttribute ? this.parent.getAttributeValue(this.system.damageAttribute) ?? 0 : 0;
    return {
      value: Ve.damageValue(
        e,
        this.system.damage,
        this.system.damageAttribute,
        n
      ),
      monitor: e,
      damageType: this.system.damageType,
      damageTypeLabel: this.getDamageTypeLabel(),
      noArmor: this.system.noArmor ?? this.system.armorAvoidance,
      armorMode: Ve.armorMode(e, this.system.noArmor ?? this.system.armorAvoidance)
    };
  }
  static damageValue(e, n, i, a) {
    if (n = Number(n), i)
      if (a !== void 0)
        n = n + Math.ceil(Number(a) / 2);
      else
        return console.warn("Weapon not attached to an actor"), z.item.personalWeapon.weaponWithoutActor;
    return n;
  }
  getDamageCode() {
    return Ve.damageCode(
      this._getMonitor(),
      this.system.damage,
      this.system.damageAttribute
    );
  }
  static damageCode(e, n, i) {
    let a = "";
    return i && z.attributes[i] && (a += z.attributes[i].substring(0, 3).toUpperCase() + "/2 + "), a += String(n), a;
  }
  static armorMode(e, n) {
    return _e.useArmor(e) ? n ? "noArmor" : "withArmor" : "";
  }
  getDamageTypeLabel() {
    if ((this.canonicalType ?? this.type) === M.itemType.personalWeapon)
      return In(this.system.damageType);
    const e = ei(this.system.damageType, "energy");
    return En(e);
  }
  getRanges() {
    const e = (this.canonicalType ?? this.type) === M.itemType.personalWeapon, n = e ? Ve.normalizePersonalRangeData(this.system.range) : Ve.normalizeRangeData(this.system.range);
    return Ve.getRangeList(n, {
      personalScale: e
    }).filter((i) => i.allowed).map((i) => ({ value: i.value, labelkey: i.labelkey }));
  }
  _getRange(e) {
    return { value: this.system.range[e], labelkey: pt.getFromList(pt.getEnums().ranges, e) };
  }
  static getRangeList(e, { personalScale: n = !1 } = {}) {
    const i = Ve.normalizeRangeKey(e == null ? void 0 : e.max), a = Ve.RANGE_ORDER.indexOf(i);
    return Ve.RANGE_ORDER.map((r, s) => ({
      key: r,
      allowed: a >= 0 ? s <= a : s === 0,
      value: (e == null ? void 0 : e[r]) ?? (r === "extreme" && (e == null ? void 0 : e.long) !== void 0 ? e.long : void 0),
      labelkey: n ? pu(r) : pt.getFromList(pt.getEnums().ranges, r)
    }));
  }
  static normalizeRangeKey(e) {
    return e === "long" ? "extreme" : e;
  }
  prepareShortcut() {
    return {
      img: this.img,
      label: this.name,
      callback: (e) => e.actor.rollWeapon(this)
    };
  }
  validateTargets(e) {
    var s;
    const n = (s = this.getDamage()) == null ? void 0 : s.monitor, i = _i.getTargetTokens(game.user), a = i.filter((o) => {
      var l;
      return (l = o.actor) == null ? void 0 : l.canReceiveDamage(n);
    }), r = i.filter((o) => {
      var l;
      return !((l = o.actor) != null && l.canReceiveDamage(n));
    }).map((o) => o.name);
    if (r.length > 0) {
      const o = At(z.common.errors.ignoredTargets, {
        targets: r.reduce(ot.joiner(", "))
      });
      ui.notifications.info(o);
    }
    if (a.length == 0) {
      const o = At(z.common.errors.noTargetSelected, {
        weapon: this.name ?? z.itemType.singular.weapon
      });
      ui.notifications.info(o);
    } else
      this.checkWeaponTargetsCount(a);
    return a;
  }
  checkWeaponTargetsCount(e) {
    const n = this.system.area, i = zy[n] ?? {};
    tp.checkTargetsCount(i.targets ?? 0, e, n);
  }
  getAreaModifier(e) {
    const n = this.getArea(), i = zy[n] ?? {};
    return i.targets && i.adjust && e <= i.targets ? i.adjust[e - 1] ?? 0 : 0;
  }
  getArea() {
    return this.system.area == "" ? M.area.none : this.system.area ?? M.area.none;
  }
  _getMonitor() {
    return (this.canonicalType ?? this.type) === M.itemType.personalWeapon ? M.monitors.physical : this.system.monitor || M.monitors.physical;
  }
};
F(Ve, "RANGE_ORDER", ["close", "near", "far", "extreme"]), F(Ve, "DEFAULT_UNARMED", Vs.DEFAULT_UNARMED);
let li = Ve;
function hB(t) {
  const e = [];
  for (let [n, i] of Object.entries(t ?? {}))
    i !== void 0 && (n = n.replace(/[A-Z]+(?![a-z])|[A-Z]/g, (a, r) => (r ? "-" : "") + a.toLowerCase()), e.push(`data-${n}="${Handlebars.escapeExpression(i)}"`));
  return new Handlebars.SafeString(e.join(" "));
}
function bB({ hash: t }) {
  return t;
}
function yB() {
  var t, e;
  return ((e = (t = foundry == null ? void 0 : foundry.applications) == null ? void 0 : t.handlebars) == null ? void 0 : e.Handlebars) ?? Handlebars;
}
class yh {
  constructor() {
    this.registerHelpers(), Hooks.once("init", () => {
      this.registerHelpers(), console.log(`${tt}Handlebars helpers registered (init)`);
    }), console.log(`${tt}Handlebars helpers registered (ctor)`);
  }
  registerHelpers() {
    const e = yB(), n = {
      // Foundry utils
      getProperty: foundry.utils.getProperty,
      // DND5E-inspired utilities
      "mwd-dataset": hB,
      "mwd-object": bB,
      // Simple comparisons
      eq: (i, a) => i === a,
      ne: (i, a) => i !== a,
      // Strings/arrays
      concat: (...i) => ot.join(i.slice(0, -1)),
      join: (i, a = " ") => Array.isArray(i) ? i.join(a) : "",
      includes: (i, a) => i == null ? void 0 : i.includes(a),
      length: (i) => (i == null ? void 0 : i.length) || 0,
      substring: (i, a, r) => i == null ? void 0 : i.substring(a, r),
      toUpperCase: AI.toUpperCaseNoAccent,
      // Math
      modulo: (i, a) => i % a,
      divint: ot.divint,
      divup: ot.divup,
      sum: (i, a) => i + a,
      diff: (i, a) => i - a,
      times: (i, a) => i * a,
      min: (i, a) => Math.min(i, a),
      max: (i, a) => Math.max(i, a),
      // Utility blocks
      for: yh.hbsForLoop,
      // fixes “Missing helper: for”
      range: (i, a) => Array.from({ length: a - i + 1 }, (r, s) => i + s),
      ifGte: (i, a, r) => i >= a ? r.fn(this) : r.inverse(this),
      // Damage / weapons (legacy-compatible)
      weaponDamageLetter: SI.letter,
      weaponDamageCode: li.damageCode,
      weaponDamageValue: li.damageValue,
      weaponArmorMode: li.armorMode,
      weaponRangeList: li.getRangeList,
      // Icons
      iconFA: he.fontAwesome,
      iconSrc: he.iconSystemPath,
      iconPath: he.iconPath,
      iconD6: he.iconD6,
      // Enums
      localizeAttribute: pt.localizeAttribute
    };
    e.registerHelper(n), e !== Handlebars && Handlebars.registerHelper(n);
  }
  static hbsForLoop(e, n, i) {
    let a = "";
    for (let r = e; r < n; ++r) a += i.fn(r);
    return a;
  }
}
const Fy = "sheetTheme", xf = "mwd-theme-default", SB = "mwd-theme-sra", AB = [
  { name: "Default (CSB)", cssClass: xf },
  { name: "SRA", cssClass: SB }
];
class kB {
  constructor() {
    this.availableStyles = {}, Cd.register(_o.REGISTER_STYLES), Hooks.once(_o.REGISTER_STYLES, (e) => AB.forEach((n) => e(n.cssClass, n.name))), Hooks.once("ready", () => this.onReady());
  }
  async onReady() {
    Hooks.callAll(_o.REGISTER_STYLES, (e, n) => this.availableStyles[e] = n), console.log(tt + "Loaded styles", this.availableStyles), game.settings.register(I, Fy, {
      scope: "world",
      name: "Sheet Theme",
      hint: "Select the visual theme used by MWD sheets.",
      config: !0,
      default: xf,
      choices: this.availableStyles,
      type: String,
      onChange: () => {
        setTimeout(() => {
          var e, n;
          for (const i of Object.values(ui.windows ?? {})) {
            if (typeof (i == null ? void 0 : i.render) != "function") continue;
            const a = i.element instanceof HTMLElement ? i.element : (e = i.element) == null ? void 0 : e[0];
            (n = a == null ? void 0 : a.classList) != null && n.contains("actor-sheet-v2") && i.render(!1);
          }
        }, 0);
      }
    });
  }
  selectCssClass() {
    const e = game.settings.get(I, Fy);
    return this.availableStyles[e] ? e : xf;
  }
}
function Uy(t) {
  const e = Number(t ?? 0);
  return Number.isFinite(e) ? Math.trunc(e) : 0;
}
function fr(t) {
  return t === M.monitors.physical ? "Physical" : t === M.monitors.fatigue ? "Fatigue" : String(t ?? "").trim() || "Track";
}
function TB({
  incomingDamage: t = 0,
  armorBefore: e = 0,
  reinforcedBefore: n = 0,
  reinforcedMax: i = 0,
  hasArmorItem: a = !1,
  forceWear: r = !1
} = {}) {
  const s = {
    armorBefore: Math.max(0, Number(e ?? 0) || 0),
    armorAfter: Math.max(0, Number(e ?? 0) || 0),
    reinforcedBefore: Math.max(0, Number(n ?? 0) || 0),
    reinforcedAfter: Math.max(0, Number(n ?? 0) || 0),
    reinforcedMax: Math.max(0, Number(i ?? 0) || 0),
    update: {}
  };
  return !a || Math.max(0, Number(t ?? 0) || 0) <= 0 && !r ? s : s.reinforcedBefore > 0 ? (s.reinforcedAfter = Math.max(0, s.reinforcedBefore - 1), s.reinforcedAfter !== s.reinforcedBefore && (s.update["system.traitState.reinforced.current"] = s.reinforcedAfter), s) : (s.armorAfter = Math.max(0, s.armorBefore - 1), s.armorAfter !== s.armorBefore && (s.update["system.durability.current"] = s.armorAfter), s);
}
function Bo(t) {
  return t ? (t == null ? void 0 : t.document) ?? t : null;
}
function hm(t, e) {
  var i, a, r;
  if (!t) return null;
  const n = Bo(e) ?? Bo(t == null ? void 0 : t.token);
  return n ? n.isLinked ? n.baseActor ?? ((r = (i = game.actors) == null ? void 0 : i.get) == null ? void 0 : r.call(i, ((a = n == null ? void 0 : n.baseActor) == null ? void 0 : a.id) ?? "")) ?? n.actor ?? t : n.actor ?? t : t;
}
function as(t, e) {
  var n, i, a;
  return Math.max(0, Number(((a = (i = (n = t == null ? void 0 : t.system) == null ? void 0 : n.monitors) == null ? void 0 : i[e]) == null ? void 0 : a.value) ?? 0) || 0);
}
function Ky(t) {
  var e, n;
  return Math.max(0, Number(((n = (e = t == null ? void 0 : t.system) == null ? void 0 : e.burn) == null ? void 0 : n.value) ?? 0) || 0);
}
function wB(t) {
  return (t == null ? void 0 : t.type) === M.actorTypes.character || (t == null ? void 0 : t.type) === M.actorTypes.npc;
}
function MB(t) {
  return (t == null ? void 0 : t.type) === M.actorTypes.vehicle || (t == null ? void 0 : t.type) === M.actorTypes.battlemech;
}
function vB(t) {
  return [
    M.actorTypes.character,
    M.actorTypes.npc,
    M.actorTypes.vehicle,
    M.actorTypes.battlemech
  ].includes(t == null ? void 0 : t.type);
}
function CB(t, e) {
  const n = String(t ?? "").trim();
  return n === "status" ? vB(e) : n === "machineAttackDamage" ? MB(e) : wB(e);
}
function IB(t, e) {
  var n;
  return ((n = Ju(e).find((i) => i.id === t)) == null ? void 0 : n.label) ?? t;
}
function EB(t) {
  var i, a, r, s;
  const e = foundry.utils.escapeHTML, n = [];
  if (t.mode === "machineAttackDamage") {
    const o = t.appliedDelta >= 0 ? "Applied" : "Recovered";
    n.push(`<div><b>${o}:</b> ${Number(t.damageIncoming ?? t.requestedDelta ?? 0)} machine damage</div>`), (i = t.hitLocation) != null && i.locationLabel && n.push(`<div><b>Location:</b> ${e(t.hitLocation.locationLabel)} (${Number(t.hitLocation.rollTotal ?? 0)})</div>`), t.machine && (n.push(`<div><b>Armor:</b> ${Number(t.machine.armorBefore ?? 0)} -> ${Number(t.machine.armorAfter ?? 0)}</div>`), n.push(`<div><b>Structure:</b> ${Number(t.machine.structureBefore ?? 0)} -> ${Number(t.machine.structureAfter ?? 0)}</div>`)), (r = (a = t.critical) == null ? void 0 : a.records) != null && r.length ? n.push(`<div><b>Critical:</b> ${e(t.critical.records.map((l) => l.label).join(", "))}</div>`) : (s = t.critical) != null && s.reason && n.push(`<div><b>Critical:</b> ${e(t.critical.reason)}</div>`);
  }
  if (t.mode === "attackDamage" || t.mode === "trackDelta") {
    const o = t.appliedDelta >= 0 ? "Applied" : "Recovered", l = Math.abs(Number(t.appliedDelta ?? 0)), c = l === 1 ? "point" : "points", u = t.usedArmor ? ` via armor-aware ${e(In(t.damageType))}` : "";
    n.push(`<div><b>${o}:</b> ${l} ${c} to ${e(fr(t.track))}${u}</div>`), t.usedArmor && t.mitigation && (n.push(
      `<div><b>Mitigation:</b> base ${Number(t.mitigation.baseMitigation ?? 0)} + type ${Number(t.mitigation.typeMitigationMod ?? 0)} - AP ${Number(t.effectiveAp ?? 0)} = ${Number(t.mitigation.netResistance ?? 0)}</div>`
    ), Number(t.mitigation.reinforcedMax ?? 0) > 0 && n.push(
      `<div><b>Reinforced:</b> ${Number(t.mitigation.reinforcedAfter ?? 0)}/${Number(t.mitigation.reinforcedMax ?? 0)}</div>`
    ));
  }
  if (t.mode === "burnDelta") {
    const o = t.appliedDelta >= 0 ? "Adjusted Burn +" : "Adjusted Burn -";
    n.push(`<div><b>${o}</b>${Math.abs(Number(t.appliedDelta ?? 0))}</div>`);
  }
  return t.mode === "status" && n.push(
    `<div><b>Status:</b> ${t.active ? "Applied" : "Removed"} ${e(t.statusLabel ?? t.statusId ?? "Status")}</div>`
  ), n.push(`<div><b>Target:</b> ${e(t.actorName ?? "Actor")}</div>`), t.beforeLabel && t.afterLabel && n.push(`<div><b>Result:</b> ${e(t.beforeLabel)} -> ${e(t.afterLabel)}</div>`), t.source && n.push(`<div><b>Source:</b> ${e(t.source)}</div>`), t.notes && n.push(`<div><b>Notes:</b> ${e(t.notes)}</div>`), `<div class="mwd-gm-notice"><b>GM Harm:</b>${n.join("")}</div>`;
}
function NB(t) {
  var n, i;
  const e = (i = (n = game.settings) == null ? void 0 : n.get) == null ? void 0 : i.call(n, "core", "messageMode");
  return typeof ChatMessage.applyMode == "function" && ChatMessage.applyMode(t, e), t;
}
class Mn {
  static supportsActor(e, { mode: n = "" } = {}) {
    return CB(n, e);
  }
  static getActorOptions({ mode: e = "" } = {}) {
    return Array.from(game.actors ?? []).filter((n) => this.supportsActor(n, { mode: e })).sort((n, i) => String(n.name ?? "").localeCompare(String(i.name ?? ""))).map((n) => ({
      id: n.id,
      name: n.name || "Character"
    }));
  }
  static getStatusOptions(e = null) {
    return Ju(e).map((n) => ({
      value: n.id,
      label: n.label,
      active: n.active
    }));
  }
  static getSceneTarget({ mode: e = "" } = {}) {
    var a, r;
    const n = Array.from(((a = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : a.controlled) ?? []);
    if (n.length > 1)
      return { actor: null, token: null, reason: "Select only one controlled token." };
    if (n.length === 1) {
      const s = Bo(n[0]), o = hm((s == null ? void 0 : s.actor) ?? null, s);
      return this._resolveSceneTargetResult(o, s, { mode: e });
    }
    const i = Array.from(((r = game.user) == null ? void 0 : r.targets) ?? []);
    if (i.length > 1)
      return { actor: null, token: null, reason: "Target only one token." };
    if (i.length === 1) {
      const s = Bo(i[0]), o = hm((s == null ? void 0 : s.actor) ?? null, s);
      return this._resolveSceneTargetResult(o, s, { mode: e });
    }
    return { actor: null, token: null, reason: "No controlled or targeted token." };
  }
  static _resolveSceneTargetResult(e, n, { mode: i = "" } = {}) {
    return !n || !e ? { actor: null, token: null, reason: "No controlled or targeted token." } : this.supportsActor(e, { mode: i }) ? {
      actor: e,
      token: n,
      reason: ""
    } : {
      actor: null,
      token: n,
      reason: `${e.name || "Token actor"} is not supported by the GM harm tool.`
    };
  }
  static resolveTarget({ actor: e = null, token: n = null, actorId: i = "", preferSceneTarget: a = !1, mode: r = "" } = {}) {
    var l, c;
    const s = Bo(n);
    if (s) {
      const u = hm((s == null ? void 0 : s.actor) ?? e, s), d = this._resolveSceneTargetResult(u, s, { mode: r });
      if (d.actor) return { ...d, source: "token" };
    }
    if (a) {
      const u = this.getSceneTarget({ mode: r });
      if (u.actor) return { ...u, source: "scene" };
    }
    if (e && this.supportsActor(e, { mode: r }))
      return { actor: e, token: s, reason: "", source: "actor" };
    const o = i ? ((c = (l = game.actors) == null ? void 0 : l.get) == null ? void 0 : c.call(l, i)) ?? null : null;
    return o && this.supportsActor(o, { mode: r }) ? { actor: o, token: null, reason: "", source: "fallback" } : {
      actor: null,
      token: s,
      source: null,
      reason: a && this.getSceneTarget({ mode: r }).reason || "Choose a supported target."
    };
  }
  static async apply({ actor: e = null, token: n = null, payload: i = {}, options: a = {} } = {}) {
    var c;
    const r = String((i == null ? void 0 : i.mode) ?? "").trim(), s = this.resolveTarget({
      actor: e,
      token: n,
      actorId: a.actorId ?? "",
      preferSceneTarget: !!a.preferSceneTarget,
      mode: r
    });
    if (!s.actor)
      return { ok: !1, reason: s.reason || "Choose a supported target." };
    let o;
    switch (String((i == null ? void 0 : i.mode) ?? "").trim()) {
      case "attackDamage":
        o = await this._applyAttackDamage(s.actor, s.token, i, a);
        break;
      case "machineAttackDamage":
        o = await this._applyMachineAttackDamage(s.actor, s.token, i, a);
        break;
      case "trackDelta":
        o = await this._applyTrackDelta(s.actor, i, a);
        break;
      case "burnDelta":
        o = await this._applyBurnDelta(s.actor, i);
        break;
      case "status":
        o = await this._applyStatus(s.actor, i);
        break;
      default:
        return { ok: !1, reason: "Unsupported harm mode." };
    }
    const l = {
      ok: !0,
      actor: s.actor,
      token: s.token,
      actorName: s.actor.name || "Character",
      sourceType: s.source,
      dryRun: !!a.dryRun,
      ...o
    };
    if (a.logToChat && !a.dryRun) {
      const u = EB(l), d = NB({
        speaker: ChatMessage.getSpeaker({ actor: s.actor, token: s.token }),
        content: u
      });
      await ChatMessage.create(d);
    }
    return a.dryRun || (c = D.renderOpenCharacterSheets) == null || c.call(D, s.actor.id), l;
  }
  static async _applyTrackDelta(e, n, i = {}) {
    const a = (n == null ? void 0 : n.track) === M.monitors.fatigue ? M.monitors.fatigue : M.monitors.physical, r = Uy((n == null ? void 0 : n.delta) ?? (n == null ? void 0 : n.amount) ?? 0);
    if (!!(n != null && n.useArmor) && r > 0)
      return this._applyPersonalArmorAwareDamage(e, {
        mode: "trackDelta",
        track: a,
        damage: r,
        sourceScale: (n == null ? void 0 : n.sourceScale) ?? (n == null ? void 0 : n.scale),
        scale: n == null ? void 0 : n.scale,
        damageType: n == null ? void 0 : n.damageType,
        ap: (n == null ? void 0 : n.ap) ?? 0,
        effects: (n == null ? void 0 : n.effects) ?? {},
        source: n == null ? void 0 : n.source,
        notes: n == null ? void 0 : n.notes
      }, i);
    const o = as(e, a);
    i.dryRun || await _e.addCounter(e, a, r);
    const l = i.dryRun ? Math.max(0, o + r) : as(e, a);
    return {
      mode: "trackDelta",
      track: a,
      requestedDelta: r,
      appliedDelta: l - o,
      usedArmor: !1,
      beforeLabel: `${fr(a)} ${o}`,
      afterLabel: `${fr(a)} ${l}`,
      source: String((n == null ? void 0 : n.source) ?? "").trim(),
      notes: String((n == null ? void 0 : n.notes) ?? "").trim()
    };
  }
  static async _applyBurnDelta(e, n) {
    var l, c;
    const i = Uy((n == null ? void 0 : n.delta) ?? (n == null ? void 0 : n.amount) ?? 0), a = Ky(e), r = Math.max(0, a + i), s = { "system.burn.value": r };
    r === 0 && ((c = (l = e.system) == null ? void 0 : l.burn) != null && c.overloaded) && (s["system.burn.overloaded"] = !1), await e.update(s);
    const o = Ky(e);
    return {
      mode: "burnDelta",
      requestedDelta: i,
      appliedDelta: o - a,
      beforeLabel: `Burn ${a}`,
      afterLabel: `Burn ${o}`,
      source: String((n == null ? void 0 : n.source) ?? "").trim(),
      notes: String((n == null ? void 0 : n.notes) ?? "").trim()
    };
  }
  static async _applyStatus(e, n) {
    const i = String((n == null ? void 0 : n.statusId) ?? (n == null ? void 0 : n.status) ?? "").trim();
    if (!i)
      return { mode: "status", statusId: "", active: !1, statusLabel: "Status", beforeLabel: "", afterLabel: "" };
    const a = qi(e, i), r = !!(n != null && n.active);
    await mt({
      actor: e,
      statusId: i,
      active: r,
      metadata: {
        scope: n == null ? void 0 : n.scope,
        notes: n == null ? void 0 : n.notes,
        location: n == null ? void 0 : n.location,
        itemUuid: n == null ? void 0 : n.itemUuid,
        targetUuid: n == null ? void 0 : n.targetUuid,
        severity: n == null ? void 0 : n.severity
      }
    });
    const s = qi(e, i);
    return {
      mode: "status",
      statusId: i,
      statusLabel: IB(i, e),
      active: s,
      beforeLabel: a ? "Active" : "Inactive",
      afterLabel: s ? "Active" : "Inactive",
      source: String((n == null ? void 0 : n.source) ?? "").trim(),
      notes: String((n == null ? void 0 : n.notes) ?? "").trim()
    };
  }
  static async _applyAttackDamage(e, n, i, a = {}) {
    var r;
    return (i == null ? void 0 : i.applied) === !0 ? (r = i == null ? void 0 : i.appliedResult) != null && r.ok ? { ...i.appliedResult, skipped: !0, reason: "That attack damage has already been applied." } : { ok: !1, skipped: !0, reason: "That attack damage has already been applied." } : this._applyPersonalArmorAwareDamage(e, {
      mode: "attackDamage",
      track: (i == null ? void 0 : i.track) ?? M.monitors.physical,
      damage: (i == null ? void 0 : i.damage) ?? 0,
      sourceScale: (i == null ? void 0 : i.sourceScale) ?? (i == null ? void 0 : i.scale),
      scale: i == null ? void 0 : i.scale,
      netHits: (i == null ? void 0 : i.netHits) ?? 0,
      critNetHits: (i == null ? void 0 : i.critNetHits) ?? (i == null ? void 0 : i.netHits) ?? 0,
      critSeverity: i == null ? void 0 : i.critSeverity,
      outcome: i == null ? void 0 : i.outcome,
      previewRevision: (i == null ? void 0 : i.previewRevision) ?? 0,
      preparedCriticalRecords: (i == null ? void 0 : i.preparedCriticalRecords) ?? [],
      requirePreparedCriticalRecords: (i == null ? void 0 : i.requirePreparedCriticalRecords) === !0,
      weaponUuid: (i == null ? void 0 : i.weaponUuid) ?? "",
      weaponId: (i == null ? void 0 : i.weaponId) ?? "",
      weaponName: (i == null ? void 0 : i.weaponName) ?? "",
      damageType: i == null ? void 0 : i.damageType,
      ap: (i == null ? void 0 : i.ap) ?? 0,
      effects: (i == null ? void 0 : i.effects) ?? {},
      source: i == null ? void 0 : i.source,
      notes: i == null ? void 0 : i.notes
    }, { ...a, token: n });
  }
  static async _applyMachineAttackDamage(e, n, i, a = {}) {
    return hD({ actor: e, token: n, payload: i, options: a });
  }
  static async _applyPersonalArmorAwareDamage(e, n, i = {}) {
    var q, ie, Z, Se, se, te, pe, K, be, Ie, Oe;
    const a = !!i.dryRun, r = (n == null ? void 0 : n.track) === M.monitors.fatigue ? M.monitors.fatigue : M.monitors.physical, s = Us((n == null ? void 0 : n.sourceScale) ?? (n == null ? void 0 : n.scale), "personal"), o = "personal", l = Cp({
      damage: (n == null ? void 0 : n.damage) ?? 0,
      sourceScale: s,
      targetScale: o
    }), c = Math.max(0, Number((n == null ? void 0 : n.damage) ?? 0) || 0), u = Math.max(0, Number(l.converted ?? 0) || 0), d = Math.max(0, Number((n == null ? void 0 : n.netHits) ?? 0) || 0), f = (n == null ? void 0 : n.effects) ?? {}, m = ((q = e.getPersonalCombatLoadout) == null ? void 0 : q.call(e, { refresh: !0 })) ?? null, g = (m == null ? void 0 : m.activeArmor) ?? null;
    if (!i.skipBattleArmor && GO(e, { ...n, mode: (n == null ? void 0 : n.mode) ?? "attackDamage" }))
      return this._applyBattleArmorDamage(e, {
        ...n,
        track: r,
        damage: c,
        effects: f
      }, { ...i, activeArmor: g });
    const p = Math.max(0, Number((g == null ? void 0 : g.currentArmorRating) ?? ((ie = g == null ? void 0 : g.durability) == null ? void 0 : ie.current) ?? 0) || 0), h = Xn(n == null ? void 0 : n.damageType, "concussive"), b = as(e, r);
    let y = u;
    const S = p > 0 ? wE({
      damageIncoming: y,
      armorTags: (g == null ? void 0 : g.tags) ?? [],
      effects: f
    }) : { damageIncoming: y, applied: [] };
    y = S.damageIncoming;
    const A = TE({
      currentArmorRating: p,
      mitigationByType: (g == null ? void 0 : g.mitigationByType) ?? {},
      damageType: h
    }), T = Array.isArray(f == null ? void 0 : f.flags) && f.flags.includes("armorBypass"), C = T ? 0 : Math.max(0, (Number((n == null ? void 0 : n.ap) ?? 0) || 0) + (Number((f == null ? void 0 : f.ap) ?? 0) || 0)), k = T || A.isDestroyed ? 0 : Math.max(0, A.baseMitigation + A.typeMitigationMod - C);
    let w = Math.max(0, Math.ceil(y - k));
    const v = ((Z = D.getSnapshot) == null ? void 0 : Z.call(D, e)) ?? null, N = {
      snapshot: v,
      combat: (v == null ? void 0 : v.combat) ?? game.combat ?? null,
      combatant: (v == null ? void 0 : v.combatant) ?? null,
      state: (v == null ? void 0 : v.state) ?? null,
      sceneId: ((Se = canvas == null ? void 0 : canvas.scene) == null ? void 0 : Se.id) ?? ""
    }, P = zt({
      actor: e,
      phase: "onDamageResolved",
      facts: Fk({
        actor: e,
        packet: {
          amount: w,
          track: r,
          damageType: h
        },
        runtime: N
      }),
      packet: {
        amount: w,
        track: r,
        damageType: h
      },
      options: { runtime: N, consumeUsage: !0 }
    });
    a || await ga({ actor: e, mutations: P.mutations, runtime: N }), w = Math.max(0, Number(P.packet.amount ?? w) || 0), !a && w > 0 && await _e.addCounter(e, r, w);
    const L = !T && (n == null ? void 0 : n.mode) === "attackDamage" && ["hit", "graze", "highMargin"].includes(String((n == null ? void 0 : n.outcome) ?? "").trim()), _ = TB({
      incomingDamage: u,
      armorBefore: ((se = g == null ? void 0 : g.durability) == null ? void 0 : se.current) ?? 0,
      reinforcedBefore: ((pe = (te = g == null ? void 0 : g.traitState) == null ? void 0 : te.reinforced) == null ? void 0 : pe.current) ?? 0,
      reinforcedMax: ((be = (K = g == null ? void 0 : g.traitState) == null ? void 0 : K.reinforced) == null ? void 0 : be.max) ?? 0,
      hasArmorItem: !!((Ie = g == null ? void 0 : g.item) != null && Ie.id),
      forceWear: L
    });
    !a && Object.keys(_.update).length > 0 && await g.item.update(_.update);
    const O = a ? Math.max(0, b + w) : as(e, r), x = (n == null ? void 0 : n.mode) === "attackDamage" ? await oy({
      actor: e,
      payload: n,
      outcome: n == null ? void 0 : n.outcome,
      netHits: (n == null ? void 0 : n.critNetHits) ?? d,
      source: (n == null ? void 0 : n.sourceData) ?? { source: (n == null ? void 0 : n.source) ?? "" },
      previewRevision: (n == null ? void 0 : n.previewRevision) ?? 0,
      weaponUuid: (n == null ? void 0 : n.weaponUuid) ?? "",
      weaponId: (n == null ? void 0 : n.weaponId) ?? "",
      weaponName: (n == null ? void 0 : n.weaponName) ?? ""
    }) : { ok: !0, selected: !1, records: [], severity: 0, band: "none" };
    if (!x.ok) return x;
    const H = Array.isArray(n == null ? void 0 : n.preparedCriticalRecords) ? n.preparedCriticalRecords : [];
    if (!a && (n == null ? void 0 : n.requirePreparedCriticalRecords) === !0 && x.selected && H.length <= 0)
      return { ok: !1, reason: "Critical preview is missing. Rebuild the attack damage preview before applying." };
    let G = { ok: !0, records: [] };
    return !a && x.selected && Array.isArray(x.records) && x.records.length && (G = await cy({
      actor: e,
      token: i.token ?? null,
      records: x.records,
      dryRun: !1
    }), !G.ok) ? G : {
      mode: (n == null ? void 0 : n.mode) ?? "attackDamage",
      track: r,
      requestedDelta: u,
      appliedDelta: O - b,
      usedArmor: !0,
      sourceScale: s,
      targetScale: o,
      scaleConversion: l,
      damageType: h,
      effectiveAp: C,
      mitigation: {
        ...A,
        netResistance: k,
        armorBefore: _.armorBefore,
        armorAfter: _.armorAfter,
        reinforcedBefore: _.reinforcedBefore,
        reinforcedAfter: _.reinforcedAfter,
        reinforcedMax: _.reinforcedMax
      },
      damageIncoming: y,
      adjustedIncoming: y,
      finalDamage: w,
      tagEffectResult: S,
      attackDamage: (n == null ? void 0 : n.attackDamage) ?? null,
      critical: {
        ...x,
        drawOk: !0,
        records: (Oe = G.records) != null && Oe.length ? G.records : x.records ?? []
      },
      beforeLabel: `${fr(r)} ${b}`,
      afterLabel: `${fr(r)} ${O}`,
      source: String((n == null ? void 0 : n.source) ?? "").trim(),
      notes: String((n == null ? void 0 : n.notes) ?? "").trim()
    };
  }
  static async _applyBattleArmorDamage(e, n, i = {}) {
    var y, S;
    const a = !!i.dryRun, r = M.monitors.physical, s = i.activeArmor ?? io(e), o = (s == null ? void 0 : s.battleArmor) ?? null;
    if (!((y = s == null ? void 0 : s.item) != null && y.id) || !(o != null && o.enabled))
      return this._applyPersonalArmorAwareDamage(e, {
        ...n,
        mode: (n == null ? void 0 : n.mode) ?? "attackDamage"
      }, { ...i, activeArmor: null, skipBattleArmor: !0 });
    const l = Math.max(0, Number((n == null ? void 0 : n.damage) ?? 0) || 0), c = as(e, r), u = Us((n == null ? void 0 : n.sourceScale) ?? (n == null ? void 0 : n.scale), "personal"), d = qO(o, {
      damage: l,
      sourceScale: u
    }), f = Math.max(0, Number(d.wearerDamage ?? 0) || 0);
    a || (await s.item.update(WO(d)), f > 0 && await _e.addCounter(e, r, f));
    const m = a ? Math.max(0, c + f) : as(e, r), p = (n == null ? void 0 : n.mode) === "attackDamage" && (u === "machine" || !d.hadArmorShellAtStart) ? await oy({
      actor: e,
      payload: n,
      outcome: n == null ? void 0 : n.outcome,
      netHits: (n == null ? void 0 : n.critNetHits) ?? (n == null ? void 0 : n.netHits) ?? 0,
      source: (n == null ? void 0 : n.sourceData) ?? { source: (n == null ? void 0 : n.source) ?? "" },
      previewRevision: (n == null ? void 0 : n.previewRevision) ?? 0,
      weaponUuid: (n == null ? void 0 : n.weaponUuid) ?? "",
      weaponId: (n == null ? void 0 : n.weaponId) ?? "",
      weaponName: (n == null ? void 0 : n.weaponName) ?? ""
    }) : {
      ok: !0,
      selected: !1,
      records: [],
      severity: 0,
      band: "none",
      reason: d.hadArmorShellAtStart ? "Battle Armor shell suppressed personal critical trauma." : ""
    };
    if (!p.ok) return p;
    const h = Array.isArray(n == null ? void 0 : n.preparedCriticalRecords) ? n.preparedCriticalRecords : [];
    if (!a && (n == null ? void 0 : n.requirePreparedCriticalRecords) === !0 && p.selected && h.length <= 0)
      return { ok: !1, reason: "Critical preview is missing. Rebuild the attack damage preview before applying." };
    let b = { ok: !0, records: [] };
    return !a && p.selected && Array.isArray(p.records) && p.records.length && (b = await cy({
      actor: e,
      token: i.token ?? null,
      records: p.records,
      dryRun: !1
    }), !b.ok) ? b : {
      mode: (n == null ? void 0 : n.mode) ?? "attackDamage",
      track: r,
      requestedDelta: d.incomingScaled,
      appliedDelta: m - c,
      usedArmor: !0,
      usedBattleArmor: !0,
      sourceScale: u,
      targetScale: "personal",
      scaleConversion: d.scaleConversion,
      damageType: Xn(n == null ? void 0 : n.damageType, "concussive"),
      effectiveAp: 0,
      mitigation: null,
      battleArmor: {
        armorId: s.id ?? s.armorId ?? "",
        armorName: s.name ?? "Battle Armor",
        ...d,
        profile: void 0
      },
      damageIncoming: d.incomingOriginal,
      adjustedIncoming: d.incomingScaled,
      finalDamage: f,
      attackDamage: (n == null ? void 0 : n.attackDamage) ?? null,
      critical: {
        ...p,
        drawOk: !0,
        records: (S = b.records) != null && S.length ? b.records : p.records ?? []
      },
      beforeLabel: `${fr(r)} ${c}`,
      afterLabel: `${fr(r)} ${m}`,
      source: String((n == null ? void 0 : n.source) ?? "").trim(),
      notes: String((n == null ? void 0 : n.notes) ?? "").trim()
    };
  }
}
F(Mn, "MODE_OPTIONS", Object.freeze([
  { value: M.monitors.physical, label: "Physical" },
  { value: M.monitors.fatigue, label: "Fatigue" },
  { value: "burn", label: "Burn" },
  { value: "status", label: "Status" }
]));
const Hy = Vo, xc = "activeModifiers", Sh = [
  { value: "", label: "All Attributes" },
  { value: "reflexes", label: "Reflexes" },
  { value: "strength", label: "Strength" },
  { value: "guts", label: "Guts" },
  { value: "charisma", label: "Charisma" },
  { value: "intelligence", label: "Intelligence" },
  { value: "edge", label: "Edge" }
], Ah = [
  { value: "", label: "All Intents" },
  { value: "skill", label: "Skill" },
  { value: "attack", label: "Attack" },
  { value: "defense", label: "Defense" },
  { value: "common", label: "Common" },
  { value: "attribute", label: "Attribute" },
  { value: "edge", label: "Edge" },
  { value: "initiative", label: "Initiative" }
];
function jy(t) {
  const e = String(t ?? "").trim();
  return e === "" ? null : e;
}
function PB(t) {
  return String((t == null ? void 0 : t.intent) ?? "").trim() || null;
}
function RB(t) {
  var e, n, i;
  return ((e = t == null ? void 0 : t.data) == null ? void 0 : e.attrKey) ?? // skill rolls
  ((i = (n = t == null ? void 0 : t.attack) == null ? void 0 : n.skill) == null ? void 0 : i.attribute) ?? // attack rolls
  null;
}
function Gy(t, e) {
  return t ? e ? t === e : !1 : !0;
}
function O0(t) {
  return {
    id: String((t == null ? void 0 : t.id) ?? ""),
    label: String((t == null ? void 0 : t.label) ?? "").trim(),
    value: Math.trunc(Number((t == null ? void 0 : t.value) ?? 0)) || 0,
    enabled: (t == null ? void 0 : t.enabled) !== !1,
    attributeFilter: jy(t == null ? void 0 : t.attributeFilter),
    intentFilter: jy(t == null ? void 0 : t.intentFilter),
    source: (t == null ? void 0 : t.source) === "preset" ? "preset" : "adhoc"
  };
}
class DB {
  constructor() {
    F(this, "id", "mwd.sceneModifiers");
  }
  collect({ resolved: e } = {}) {
    var s;
    const n = (s = canvas == null ? void 0 : canvas.scene) == null ? void 0 : s.getFlag("mwd", xc);
    if (!Array.isArray(n) || !n.length) return [];
    const i = PB(e), a = RB(e), r = [];
    for (const o of n) {
      const l = O0(o);
      l.enabled && Gy(l.intentFilter, i) && Gy(l.attributeFilter, a) && r.push({
        id: l.id || `scene:${l.label}`,
        label: l.label,
        value: l.value,
        source: "Scene"
      });
    }
    return r;
  }
}
const OB = `systems/${I}/templates/settings/collection-editor.hbs`, L0 = /* @__PURE__ */ new Map(), bm = /* @__PURE__ */ new Map();
function Rn(t = []) {
  const e = Array.isArray(t) ? t.filter(Boolean) : [String(t ?? "").trim()].filter(Boolean), n = new Error(e[0] ?? "Invalid settings data.");
  return n.validationErrors = e, n;
}
function Xa(t) {
  $B(t), L0.set(t.id, t), game.settings.register(I, t.settingKey, {
    scope: "world",
    config: !1,
    type: t.settingType ?? Object,
    default: t.defaultData()
  }), game.settings.registerMenu(I, t.menuKey, {
    name: t.menu.name,
    label: t.menu.label,
    hint: t.menu.hint,
    icon: t.menu.icon,
    type: _B(t.id),
    restricted: t.menu.restricted ?? !0
  });
}
function LB(t) {
  return L0.get(t) ?? null;
}
function $B(t) {
  var e, n;
  if (!(t != null && t.id)) throw new Error("Settings collection definition requires an id.");
  if (!(t != null && t.settingKey)) throw new Error(`Settings collection definition "${t.id}" requires a settingKey.`);
  if (!(t != null && t.menuKey)) throw new Error(`Settings collection definition "${t.id}" requires a menuKey.`);
  if (!((e = t == null ? void 0 : t.menu) != null && e.name) || !((n = t == null ? void 0 : t.menu) != null && n.label))
    throw new Error(`Settings collection definition "${t.id}" requires menu metadata.`);
  if (typeof t.defaultData != "function")
    throw new Error(`Settings collection definition "${t.id}" requires defaultData().`);
  if (typeof t.toRows != "function")
    throw new Error(`Settings collection definition "${t.id}" requires toRows(value).`);
  if (typeof t.rowsToValue != "function")
    throw new Error(`Settings collection definition "${t.id}" requires rowsToValue(rows).`);
  if (typeof t.serializeBulk != "function" || typeof t.parseBulk != "function")
    throw new Error(`Settings collection definition "${t.id}" requires bulk serialization helpers.`);
  if (!Array.isArray(t.rowSchema) || !t.rowSchema.length)
    throw new Error(`Settings collection definition "${t.id}" requires a non-empty rowSchema.`);
}
function _B(t) {
  if (bm.has(t))
    return bm.get(t);
  class e extends zc {
  }
  return F(e, "definitionId", t), bm.set(t, e), e;
}
var ye, $0, _0, zf, Fc, Uc, ds, Ff, Co, B0, x0, yn;
class zc extends foundry.applications.api.HandlebarsApplicationMixin(
  foundry.applications.api.ApplicationV2
) {
  constructor(n = {}) {
    super(n);
    Fe(this, ye);
    const i = E(this, ye, Uc).call(this);
    this.editorState = {
      tab: "rows",
      rows: this.definition.toRows(i),
      bulkText: this.definition.serializeBulk(i),
      errors: []
    };
  }
  get id() {
    return `${I}-${this.constructor.definitionId}-editor`;
  }
  get definition() {
    const n = LB(this.constructor.definitionId);
    if (!n)
      throw new Error(`Missing settings collection definition: ${this.constructor.definitionId}`);
    return n;
  }
  get title() {
    var n;
    return this.definition.title ?? ((n = this.definition.menu) == null ? void 0 : n.name) ?? "Settings Editor";
  }
  async _prepareContext(n = {}) {
    const i = E(this, ye, x0).call(this), a = this.editorState.rows.map((r, s, o) => ({
      index: s,
      fields: i.map((l) => E(this, ye, B0).call(this, l, r, s)),
      canMoveUp: s > 0,
      canMoveDown: s < o.length - 1
    }));
    return {
      definitionId: this.definition.id,
      title: this.title,
      description: this.definition.description ?? "",
      helpText: this.definition.helpText ?? "",
      bulkHelpText: this.definition.bulkHelpText ?? "",
      currentTab: this.editorState.tab,
      isRowsTab: this.editorState.tab === "rows",
      isBulkTab: this.editorState.tab === "bulk",
      errors: [...this.editorState.errors ?? []],
      columns: i.map((r) => ({ key: r.key, label: r.label })),
      rows: a,
      hasRows: a.length > 0,
      bulkText: this.editorState.bulkText ?? "",
      addRowLabel: this.definition.addRowLabel ?? "Add Row",
      saveLabel: this.definition.saveLabel ?? "Save",
      cancelLabel: this.definition.cancelLabel ?? "Cancel",
      resetLabel: this.definition.resetLabel ?? "Reset to Saved",
      defaultsLabel: this.definition.defaultsLabel ?? "Restore Defaults",
      emptyStateText: this.definition.emptyStateText ?? "No rows yet. Add one to start this collection."
    };
  }
  _onRender(n, i) {
    this.element.querySelectorAll("[data-action]").forEach((r) => {
      r.addEventListener("click", (s) => {
        var c;
        const o = s.currentTarget, l = String(((c = o == null ? void 0 : o.dataset) == null ? void 0 : c.action) ?? "").trim();
        l && E(this, ye, _0).call(this, l, s, o);
      });
    });
    const a = this.element.querySelector("form");
    a && a.addEventListener("submit", (r) => {
      r.preventDefault(), E(this, ye, $0).call(this);
    });
  }
}
ye = new WeakSet(), $0 = async function() {
  var n;
  E(this, ye, yn).call(this, []);
  try {
    const i = this.editorState.tab === "bulk" ? this.definition.parseBulk(E(this, ye, Co).call(this)) : this.definition.rowsToValue(E(this, ye, Ff).call(this));
    await game.settings.set(I, this.definition.settingKey, i);
    const a = E(this, ye, Uc).call(this);
    E(this, ye, Fc).call(this, a), await this.close();
  } catch (i) {
    E(this, ye, yn).call(this, hc(i)), this.editorState.errors.length && ((n = ui.notifications) == null || n.error(this.editorState.errors[0])), this.render();
  }
}, _0 = async function(n, i, a) {
  var r, s, o, l, c, u, d, f;
  switch (i.preventDefault(), i.stopPropagation(), n) {
    case "switchRows":
      E(this, ye, Co).call(this), this.editorState.tab = "rows", E(this, ye, yn).call(this, []), this.render();
      return;
    case "switchBulk":
      E(this, ye, ds).call(this);
      try {
        const m = this.definition.rowsToValue(this.editorState.rows);
        this.editorState.bulkText = this.definition.serializeBulk(m), this.editorState.tab = "bulk", E(this, ye, yn).call(this, []);
      } catch (m) {
        E(this, ye, yn).call(this, hc(m)), this.editorState.errors.length && ((r = ui.notifications) == null || r.warn(this.editorState.errors[0]));
      }
      this.render();
      return;
    case "addRow":
      E(this, ye, ds).call(this), this.editorState.rows.push(((o = (s = this.definition).createEmptyRow) == null ? void 0 : o.call(s)) ?? {}), E(this, ye, yn).call(this, []), this.render();
      return;
    case "removeRow":
      E(this, ye, ds).call(this), this.editorState.rows.splice(Number(((l = a == null ? void 0 : a.dataset) == null ? void 0 : l.index) ?? -1), 1), E(this, ye, yn).call(this, []), this.render();
      return;
    case "moveRowUp":
      E(this, ye, ds).call(this), E(this, ye, zf).call(this, Number(((c = a == null ? void 0 : a.dataset) == null ? void 0 : c.index) ?? -1), -1), E(this, ye, yn).call(this, []), this.render();
      return;
    case "moveRowDown":
      E(this, ye, ds).call(this), E(this, ye, zf).call(this, Number(((u = a == null ? void 0 : a.dataset) == null ? void 0 : u.index) ?? -1), 1), E(this, ye, yn).call(this, []), this.render();
      return;
    case "loadBulk":
      try {
        const m = this.definition.parseBulk(E(this, ye, Co).call(this));
        this.editorState.rows = this.definition.toRows(m), this.editorState.bulkText = this.definition.serializeBulk(m), this.editorState.tab = "rows", E(this, ye, yn).call(this, []);
      } catch (m) {
        E(this, ye, yn).call(this, hc(m)), this.editorState.errors.length && ((d = ui.notifications) == null || d.warn(this.editorState.errors[0]));
      }
      this.render();
      return;
    case "formatBulk":
      try {
        const m = this.definition.parseBulk(E(this, ye, Co).call(this));
        this.editorState.bulkText = this.definition.serializeBulk(m), E(this, ye, yn).call(this, []);
      } catch (m) {
        E(this, ye, yn).call(this, hc(m)), this.editorState.errors.length && ((f = ui.notifications) == null || f.warn(this.editorState.errors[0]));
      }
      this.render();
      return;
    case "resetSetting":
      E(this, ye, Fc).call(this, E(this, ye, Uc).call(this)), this.render();
      return;
    case "restoreDefaults":
      E(this, ye, Fc).call(this, this.definition.defaultData()), this.render();
      return;
    case "cancel":
      await this.close();
      return;
    default:
      return;
  }
}, zf = function(n, i) {
  if (!Number.isInteger(n)) return;
  const a = n + i;
  if (n < 0 || a < 0 || a >= this.editorState.rows.length) return;
  const r = [...this.editorState.rows], [s] = r.splice(n, 1);
  r.splice(a, 0, s), this.editorState.rows = r;
}, Fc = function(n) {
  this.editorState.rows = this.definition.toRows(n), this.editorState.bulkText = this.definition.serializeBulk(n), this.editorState.tab = "rows", E(this, ye, yn).call(this, []);
}, Uc = function() {
  const n = game.settings.get(I, this.definition.settingKey);
  return foundry.utils.deepClone(n ?? this.definition.defaultData());
}, ds = function() {
  this.editorState.rows = E(this, ye, Ff).call(this);
}, Ff = function() {
  var s;
  const n = (s = this.element) == null ? void 0 : s.querySelector("form"), i = n ? new FormDataExtended(n) : { object: {} }, a = foundry.utils.expandObject(i.object), r = (a == null ? void 0 : a.rows) ?? {};
  return Object.keys(r).sort((o, l) => Number(o) - Number(l)).map((o) => {
    const l = r[o] ?? {};
    return Object.fromEntries(
      this.definition.rowSchema.map((c) => [
        c.key,
        String((l == null ? void 0 : l[c.key]) ?? "")
      ])
    );
  });
}, Co = function() {
  var a;
  const n = (a = this.element) == null ? void 0 : a.querySelector("form"), i = n == null ? void 0 : n.querySelector('textarea[name="bulkText"]');
  return i instanceof HTMLTextAreaElement && (this.editorState.bulkText = i.value), this.editorState.bulkText ?? "";
}, B0 = function(n, i, a) {
  const r = n.type ?? "text", s = String((i == null ? void 0 : i[n.key]) ?? n.default ?? ""), o = r === "select" ? BB(n).map((l) => ({
    value: String(l.value ?? ""),
    label: String(l.label ?? l.value ?? ""),
    selected: String(l.value ?? "") === s
  })) : [];
  return {
    key: n.key,
    label: n.label,
    type: r,
    inputType: r === "select" ? "text" : r,
    name: `rows.${a}.${n.key}`,
    value: s,
    placeholder: n.placeholder ?? "",
    min: n.min ?? "",
    max: n.max ?? "",
    step: n.step ?? "",
    options: o
  };
}, x0 = function() {
  return this.definition.rowSchema.map((n) => ({
    ...n,
    type: n.type ?? "text"
  }));
}, yn = function(n = []) {
  this.editorState.errors = Array.isArray(n) ? n.filter(Boolean) : [];
}, F(zc, "definitionId", ""), F(zc, "DEFAULT_OPTIONS", {
  classes: ["mwd", "mwd-settings-editor"],
  window: { resizable: !0 },
  position: { width: 880, height: 760 }
}), F(zc, "PARTS", {
  main: { template: OB }
});
function BB(t) {
  const e = typeof t.options == "function" ? t.options() : t.options;
  return Array.isArray(e) ? e : [];
}
function hc(t) {
  const e = Array.isArray(t == null ? void 0 : t.validationErrors) ? t.validationErrors.filter(Boolean) : [String((t == null ? void 0 : t.message) ?? "Unable to save settings.").trim()].filter(Boolean);
  return e.length ? e : ["Unable to save settings."];
}
const Uf = "sceneModifierTemplates", xB = "sceneModifierTemplateEditor", zB = Object.freeze([]);
function Za(t) {
  const e = String(t ?? "").trim();
  return e === "" ? "" : e;
}
function z0(t = []) {
  const e = [], n = [], i = /* @__PURE__ */ new Set();
  if ((Array.isArray(t) ? t : []).forEach((a, r) => {
    const s = String((a == null ? void 0 : a.label) ?? "").trim(), o = String((a == null ? void 0 : a.value) ?? "").trim(), l = `Row ${r + 1}`;
    if (!s) {
      n.push(`${l}: label cannot be blank.`);
      return;
    }
    if (i.has(s.toLowerCase())) {
      n.push(`${l}: duplicate label "${s}".`);
      return;
    }
    i.add(s.toLowerCase());
    const c = Number(o);
    if (!Number.isFinite(c)) {
      n.push(`${l}: value must be a number.`);
      return;
    }
    e.push({
      label: s,
      value: Math.trunc(c),
      attributeFilter: Za(a == null ? void 0 : a.attributeFilter),
      intentFilter: Za(a == null ? void 0 : a.intentFilter)
    });
  }), n.length) throw Rn(n);
  return e;
}
function FB(t = []) {
  return (Array.isArray(t) ? t : []).map((e) => ({
    label: String((e == null ? void 0 : e.label) ?? ""),
    value: String((e == null ? void 0 : e.value) ?? "0"),
    attributeFilter: Za(e == null ? void 0 : e.attributeFilter),
    intentFilter: Za(e == null ? void 0 : e.intentFilter)
  }));
}
function UB(t = "") {
  const e = String(t ?? "").trim();
  if (!e) return [];
  let n;
  try {
    n = JSON.parse(e);
  } catch (i) {
    throw Rn([
      `Bulk JSON must be valid JSON: ${i.message}`
    ]);
  }
  if (!Array.isArray(n))
    throw Rn(["Bulk JSON must be an array."]);
  return z0(n.map((i) => ({
    label: String((i == null ? void 0 : i.label) ?? ""),
    value: String((i == null ? void 0 : i.value) ?? "0"),
    attributeFilter: Za(i == null ? void 0 : i.attributeFilter),
    intentFilter: Za(i == null ? void 0 : i.intentFilter)
  })));
}
function KB(t = []) {
  return JSON.stringify(
    (Array.isArray(t) ? t : []).map((e) => ({
      label: String((e == null ? void 0 : e.label) ?? ""),
      value: Number((e == null ? void 0 : e.value) ?? 0),
      attributeFilter: Za(e == null ? void 0 : e.attributeFilter),
      intentFilter: Za(e == null ? void 0 : e.intentFilter)
    })),
    null,
    2
  );
}
const HB = {
  id: "scene-modifier-templates",
  menuKey: xB,
  settingKey: Uf,
  settingType: Array,
  title: "Scene Modifier Templates",
  description: "Define reusable scene modifier presets that can be applied per scene via the GM Gadget.",
  helpText: "Labels must be unique. Value is a signed integer (+/–). Filters are optional — blank means the modifier applies to all matching rolls.",
  bulkHelpText: 'JSON shape: [{ "label": "Darkness", "value": -2, "attributeFilter": "reflexes", "intentFilter": "attack" }]',
  emptyStateText: "No templates yet. Add one to make it available in the GM Gadget Scene tab.",
  addRowLabel: "Add Template",
  rowSchema: [
    {
      key: "label",
      label: "Label",
      type: "text",
      placeholder: "Darkness"
    },
    {
      key: "value",
      label: "Value",
      type: "number",
      step: 1,
      placeholder: "-2"
    },
    {
      key: "attributeFilter",
      label: "Attribute Filter",
      type: "select",
      options: Sh
    },
    {
      key: "intentFilter",
      label: "Intent Filter",
      type: "select",
      options: Ah
    }
  ],
  menu: {
    name: "Scene Modifier Templates",
    label: "Configure",
    hint: "Edit the preset scene modifier templates available in the GM Gadget.",
    icon: "fas fa-cloud",
    restricted: !0
  },
  defaultData: () => foundry.utils.deepClone(zB),
  createEmptyRow: () => ({
    label: "",
    value: "0",
    attributeFilter: "",
    intentFilter: ""
  }),
  toRows: FB,
  rowsToValue: z0,
  parseBulk: UB,
  serializeBulk: KB
};
function jB() {
  Xa(HB);
}
const { ApplicationV2: GB, HandlebarsApplicationMixin: qB } = foundry.applications.api, WB = "mwd-gmgadget", F0 = "gmDnPresets", Kc = "gmNextDn", Io = "gmDnAnnounceToChat", VB = Object.freeze([
  { label: "Standard", dn: 1 },
  { label: "Challenging", dn: 2 },
  { label: "Hard", dn: 3 },
  { label: "Extreme", dn: 4 }
]), JB = "systems/mwd/templates/v2/mwd-gmgadget.hbs", Eo = Object.freeze({
  actorId: "",
  mode: "physical",
  delta: 1,
  useArmor: !1,
  damageType: "concussive",
  statusId: "",
  statusActive: !0,
  source: "",
  notes: ""
}), YB = Object.freeze({
  label: "Hazard Zone",
  startExposure: Te.minor,
  escalationRate: 1,
  escalationIntervalTurns: 1,
  escalationMax: Te.full,
  onFullBurnDelta: 0,
  clearOnExit: !0,
  damage: 6,
  ap: 0,
  damageType: "thermal",
  color: "#d86a2c"
}), QB = Object.freeze({
  attackerTokenId: "",
  bulkDetectionState: "blind"
});
function XB(t = "") {
  return String(t ?? "").split(",").map((e) => e.trim()).filter(Boolean).map((e) => {
    const [n, i] = e.split(":").map((s) => (s ?? "").trim()), a = n || "DN", r = Number.isFinite(Number(i)) ? Number(i) : Number(n);
    return {
      label: a,
      dn: Number.isFinite(r) ? Math.max(0, Math.trunc(r)) : null
    };
  }).filter((e) => Number.isFinite(e.dn));
}
function ZB(t = []) {
  const e = new Error(t[0] ?? "Invalid GM DN presets.");
  return e.validationErrors = Array.isArray(t) ? t.filter(Boolean) : [], e;
}
function ex() {
  return foundry.utils.deepClone(VB);
}
function Gl(t, { strict: e = !1 } = {}) {
  const n = typeof t == "string" ? XB(t) : Array.isArray(t) ? t : [], i = [], a = [], r = /* @__PURE__ */ new Set();
  if (n.forEach((s, o) => {
    const l = String((s == null ? void 0 : s.label) ?? "").trim(), c = s == null ? void 0 : s.dn, u = `Preset ${o + 1}`;
    if (!l) {
      e && a.push(`${u}: label cannot be blank.`);
      return;
    }
    const d = l.toLowerCase();
    if (r.has(d)) {
      e && a.push(`${u}: duplicate label "${l}".`);
      return;
    }
    const f = Number(c);
    if (!Number.isFinite(f)) {
      e && a.push(`${u}: DN must be numeric.`);
      return;
    }
    if (f < 0) {
      e && a.push(`${u}: DN cannot be negative.`);
      return;
    }
    r.add(d), i.push({
      label: l,
      dn: Math.trunc(f)
    });
  }), e && a.length) throw ZB(a);
  return i;
}
function ym(t = {}) {
  return foundry.utils.mergeObject(
    foundry.utils.deepClone(Eo),
    t ?? {},
    { inplace: !1, overwrite: !0 }
  );
}
function Sm(t = {}) {
  return foundry.utils.mergeObject(
    foundry.utils.deepClone(YB),
    t ?? {},
    { inplace: !1, overwrite: !0 }
  );
}
function Am(t = {}) {
  return foundry.utils.mergeObject(
    foundry.utils.deepClone(QB),
    t ?? {},
    { inplace: !1, overwrite: !0 }
  );
}
function U0(t) {
  return (t == null ? void 0 : t.type) === M.actorTypes.vehicle || (t == null ? void 0 : t.type) === M.actorTypes.battlemech;
}
function tx() {
  var e, n;
  const t = game.combat;
  return !t || (e = t.scene) != null && e.id && t.scene.id !== ((n = canvas == null ? void 0 : canvas.scene) == null ? void 0 : n.id) ? [] : Array.from(t.combatants ?? []).filter((i) => U0(i == null ? void 0 : i.actor)).map((i) => {
    var o, l, c, u;
    const a = i.token ?? ((c = (l = (o = canvas == null ? void 0 : canvas.scene) == null ? void 0 : o.tokens) == null ? void 0 : l.get) == null ? void 0 : c.call(l, i.tokenId)) ?? null, r = i.actor ?? (a == null ? void 0 : a.actor) ?? null, s = String((a == null ? void 0 : a.name) ?? (r == null ? void 0 : r.name) ?? "Machine").trim() || "Machine";
    return {
      tokenId: String(i.tokenId ?? (a == null ? void 0 : a.id) ?? "").trim(),
      tokenUuid: String((a == null ? void 0 : a.uuid) ?? ((u = a == null ? void 0 : a.document) == null ? void 0 : u.uuid) ?? "").trim(),
      actorId: String((r == null ? void 0 : r.id) ?? "").trim(),
      actorUuid: String((r == null ? void 0 : r.uuid) ?? "").trim(),
      label: s
    };
  }).filter((i) => i.tokenId);
}
function nx(t = [], e = "") {
  var r, s, o;
  const n = String(e ?? "").trim();
  if (n && t.some((l) => l.tokenId === n))
    return n;
  const i = Array.from(((r = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : r.controlled) ?? []).find((l) => U0(l == null ? void 0 : l.actor)), a = String((i == null ? void 0 : i.id) ?? ((s = i == null ? void 0 : i.document) == null ? void 0 : s.id) ?? "").trim();
  return a && t.some((l) => l.tokenId === a) ? a : ((o = t[0]) == null ? void 0 : o.tokenId) ?? "";
}
function yo(t = "") {
  var n, i;
  const e = String(t ?? "").trim();
  return e ? ((i = (n = game.combat) == null ? void 0 : n.combatants) == null ? void 0 : i.find((a) => String((a == null ? void 0 : a.tokenId) ?? "").trim() === e)) ?? null : null;
}
function K0(t = "blind") {
  return vn.map((e) => ({
    value: e,
    label: wn(e),
    selected: e === t
  }));
}
function ix(t = null, e = []) {
  return Array.from(e ?? []).filter((n) => n == null ? void 0 : n.actor).map((n) => {
    var s, o, l;
    const i = String(((s = n.document) == null ? void 0 : s.uuid) ?? n.uuid ?? "").trim();
    if (!i) return null;
    const a = ir(t, i), r = a.packet ?? null;
    return {
      targetTokenId: String(n.id ?? ((o = n.document) == null ? void 0 : o.id) ?? "").trim(),
      targetTokenUuid: i,
      tokenName: String(n.name ?? ((l = n.actor) == null ? void 0 : l.name) ?? "Target").trim() || "Target",
      detectionState: a.detectionState,
      detectionStateLabel: wn(a.detectionState),
      stateOptions: K0(a.detectionState),
      hasPacket: !!r,
      packetValue: Number((r == null ? void 0 : r.value) ?? 0) || 0,
      packetRound: (r == null ? void 0 : r.round) ?? null,
      packetExpiry: (r == null ? void 0 : r.expiresAfterRound) ?? null
    };
  }).filter(Boolean);
}
function H0(t = null) {
  const e = (t == null ? void 0 : t.document) ?? t ?? null, n = Array.from((e == null ? void 0 : e.shapes) ?? []);
  if (n.length !== 1) return n.length > 1 ? "multiple" : "";
  const i = n[0], a = typeof (i == null ? void 0 : i.toObject) == "function" ? i.toObject() : i && typeof i == "object" ? i : null;
  return String((a == null ? void 0 : a.type) ?? "").trim().toLowerCase();
}
function qy(t = null) {
  return !!H0(t);
}
function Wy() {
  var i, a;
  const t = Array.from(((i = canvas == null ? void 0 : canvas.regions) == null ? void 0 : i.controlled) ?? []).map((r) => (r == null ? void 0 : r.document) ?? r ?? null).find(qy);
  if (t) return t;
  const e = ((a = canvas == null ? void 0 : canvas.regions) == null ? void 0 : a.hover) ?? null, n = (e == null ? void 0 : e.document) ?? e ?? null;
  return qy(n) ? n : null;
}
function ax(t = null) {
  var o, l;
  const e = (t == null ? void 0 : t.document) ?? t ?? null;
  if (!e)
    return {
      label: "No region selected",
      reason: "Select a Region created in Measured Template Mode on the current scene to turn it into a hazard.",
      supported: !1
    };
  const n = H0(e), i = UA(e);
  if (!i)
    return {
      label: "Unsupported region",
      reason: n === "multiple" ? "The selected Region has multiple shapes and cannot be converted into a hazard template." : `The selected Region shape "${n || "unknown"}" is not supported for hazard conversion yet.`,
      supported: !1
    };
  const a = String(i.shape ?? "").trim().toLowerCase(), r = (l = (o = canvas == null ? void 0 : canvas.scene) == null ? void 0 : o.grid) != null && l.units ? ` ${canvas.scene.grid.units}` : "";
  return {
    label: a === "rect" ? `RECT ${Number(i.width ?? 0) || 0} x ${Number(i.height ?? 0) || 0}${r}`.trim() : `${a.toUpperCase()} ${Number(i.distance ?? 0) || 0}${r}`.trim(),
    reason: "",
    supported: !0
  };
}
function rx(t) {
  var n, i;
  return !(t != null && t.token) || !(t != null && t.actor) ? {
    label: "No scene target",
    reason: String((t == null ? void 0 : t.reason) ?? "No controlled or targeted token."),
    supported: !1
  } : {
    label: String(((n = t.token) == null ? void 0 : n.name) ?? ((i = t.actor) == null ? void 0 : i.name) ?? "Token").trim(),
    reason: "",
    supported: !0
  };
}
function sx(t) {
  var n;
  if (!(t != null && t.actor))
    return {
      label: "No target selected",
      source: "",
      reason: String((t == null ? void 0 : t.reason) ?? "Choose a supported character target.")
    };
  const e = t.source === "scene" || t.source === "token" ? "Scene target" : "Actor fallback";
  return {
    label: String(((n = t.actor) == null ? void 0 : n.name) ?? "Character").trim() || "Character",
    source: e,
    reason: ""
  };
}
function ox(t) {
  return Mn.getStatusOptions(t);
}
function lx(t = "mwd") {
  game.settings.register(t, Kc, {
    scope: "client",
    config: !1,
    type: Number,
    default: 1
  }), game.settings.register(t, Io, {
    scope: "client",
    config: !0,
    name: "Announce GM DN changes to chat",
    hint: "If enabled, posts a small chat notice when you change the current DN preset.",
    type: Boolean,
    default: !1
  });
}
const at = class at extends qB(GB) {
  constructor({ systemId: e = "mwd", ...n } = {}) {
    super(n), this.systemId = e, this.activeTab = "difficulty", this.harmState = ym(), this.targetingState = Am(), this.hazardState = Sm();
  }
  async render(e = {}) {
    var n;
    return (n = game.user) != null && n.isGM ? super.render(e) : this;
  }
  async _prepareContext(e) {
    var C, k, w, v, N, P;
    const n = await super._prepareContext(e), i = Gl(
      game.settings.get(this.systemId, F0),
      { strict: !1 }
    ), a = Number(game.settings.get(this.systemId, Kc) ?? 1), r = !!game.settings.get(this.systemId, Io), s = ym(this.harmState), o = Mn.getActorOptions({ mode: s.mode }), l = Mn.getSceneTarget({ mode: s.mode }), c = this.harmState.actorId ? ((k = (C = game.actors) == null ? void 0 : C.get) == null ? void 0 : k.call(C, this.harmState.actorId)) ?? null : null, u = Mn.resolveTarget({
      actor: c,
      actorId: this.harmState.actorId,
      preferSceneTarget: !0,
      mode: s.mode
    }), d = ox(u.actor ?? c ?? null);
    d.length && !d.some((L) => L.value === s.statusId) && (s.statusId = d[0].value, this.harmState.statusId = s.statusId);
    const f = Vy(
      game.settings.get(this.systemId, Uf)
    ), m = Jy(
      (w = canvas == null ? void 0 : canvas.scene) == null ? void 0 : w.getFlag("mwd", xc)
    ), g = Wy(), p = ax(g), h = Sm(this.hazardState), b = Am(this.targetingState), y = tx(), S = nx(y, b.attackerTokenId);
    b.attackerTokenId = S, this.targetingState.attackerTokenId = S;
    const A = yo(S), T = ix(A, ((v = game.user) == null ? void 0 : v.targets) ?? []);
    return foundry.utils.mergeObject(n, {
      presets: i,
      currentDn: a,
      currentTab: this.activeTab,
      announce: r,
      isGM: ((N = game.user) == null ? void 0 : N.isGM) ?? !1,
      scene: {
        hasScene: !!(canvas != null && canvas.scene),
        templates: f,
        activeModifiers: m,
        attributeFilterOptions: Sh,
        intentFilterOptions: Ah
      },
      harm: {
        state: s,
        actorOptions: o,
        modes: Mn.MODE_OPTIONS,
        damageTypes: Hy,
        statusOptions: d,
        sceneTarget: rx(l),
        effectiveTarget: sx(u),
        canApply: !!u.actor,
        applyReason: u.reason || "",
        useArmorAvailable: s.mode === "physical" || s.mode === "fatigue",
        showDamageType: (s.mode === "physical" || s.mode === "fatigue") && s.useArmor,
        showStatusFields: s.mode === "status",
        showDeltaFields: s.mode !== "status"
      },
      targeting: {
        state: b,
        attackerOptions: y.map((L) => ({
          ...L,
          selected: L.tokenId === S
        })),
        hasAttackerOptions: y.length > 0,
        hasAttacker: !!A,
        attackerLabel: ((P = y.find((L) => L.tokenId === S)) == null ? void 0 : P.label) ?? "",
        rows: T,
        hasTargets: T.length > 0,
        emptyState: "Target one or more tokens on the canvas to inspect directional targeting state.",
        bulkStateOptions: K0(b.bulkDetectionState)
      },
      hazard: {
        state: h,
        template: p,
        exposureTiers: [
          { value: Te.minor, label: "Minor" },
          { value: Te.major, label: "Major" },
          { value: Te.full, label: "Full" }
        ],
        damageTypes: Hy,
        canCreate: !!(canvas != null && canvas.scene && p.supported),
        createReason: p.reason || ""
      }
    });
  }
  _getRootElement() {
    var e;
    return this.element instanceof HTMLElement ? this.element : (e = this.element) == null ? void 0 : e[0];
  }
  _captureHarmStateFromDom(e = null) {
    var r;
    const n = ((r = e == null ? void 0 : e.closest) == null ? void 0 : r.call(e, ".mwd-gmgadget__root")) ?? this._getRootElement();
    if (!(n instanceof HTMLElement)) return this.harmState;
    const i = (s, o = "") => {
      const l = n.querySelector(s);
      return l instanceof HTMLInputElement || l instanceof HTMLSelectElement || l instanceof HTMLTextAreaElement ? l.value : o;
    }, a = (s, o = !1) => {
      const l = n.querySelector(s);
      return l instanceof HTMLInputElement ? l.checked : o;
    };
    return this.harmState = ym({
      actorId: i('[name="harm-actorId"]', this.harmState.actorId),
      mode: i('[name="harm-mode"]', this.harmState.mode),
      delta: Number(i('[name="harm-delta"]', this.harmState.delta)),
      useArmor: a('[name="harm-useArmor"]', this.harmState.useArmor),
      damageType: i('[name="harm-damageType"]', this.harmState.damageType),
      statusId: i('[name="harm-statusId"]', this.harmState.statusId),
      statusActive: i('[name="harm-statusActive"]', this.harmState.statusActive ? "add" : "remove") !== "remove",
      source: i('[name="harm-source"]', this.harmState.source),
      notes: i('[name="harm-notes"]', this.harmState.notes)
    }), Number.isFinite(Number(this.harmState.delta)) || (this.harmState.delta = Eo.delta), this.harmState;
  }
  _captureTargetingStateFromDom(e = null) {
    var a;
    const n = ((a = e == null ? void 0 : e.closest) == null ? void 0 : a.call(e, ".mwd-gmgadget__root")) ?? this._getRootElement();
    if (!(n instanceof HTMLElement)) return this.targetingState;
    const i = (r, s = "") => {
      const o = n.querySelector(r);
      return o instanceof HTMLInputElement || o instanceof HTMLSelectElement ? o.value : s;
    };
    return this.targetingState = Am({
      attackerTokenId: i('[name="targeting-attackerTokenId"]', this.targetingState.attackerTokenId),
      bulkDetectionState: i('[name="targeting-bulkDetectionState"]', this.targetingState.bulkDetectionState)
    }), this.targetingState;
  }
  async _onSetDn(e, n) {
    var r, s, o;
    if (e.preventDefault(), e.stopPropagation(), !((r = game.user) != null && r.isGM)) return;
    const i = Math.max(0, Math.trunc(Number(((s = n == null ? void 0 : n.dataset) == null ? void 0 : s.dn) ?? NaN)));
    if (!Number.isFinite(i)) return;
    if (await game.settings.set(this.systemId, Kc, i), !!game.settings.get(this.systemId, Io)) {
      const l = String(((o = n == null ? void 0 : n.dataset) == null ? void 0 : o.label) ?? `DN ${i}`);
      await ChatMessage.create({
        speaker: ChatMessage.getSpeaker({ user: game.user }),
        content: `<div class="mwd-gm-notice"><b>GM Difficulty:</b> ${foundry.utils.escapeHTML(l)} (DN ${i} hits)</div>`
      });
    }
    return this.render({ parts: ["body"] });
  }
  async _onSwitchTab(e, n) {
    var a, r, s;
    (a = e == null ? void 0 : e.preventDefault) == null || a.call(e), (r = e == null ? void 0 : e.stopPropagation) == null || r.call(e);
    const i = String(((s = n == null ? void 0 : n.dataset) == null ? void 0 : s.tab) ?? "").trim();
    if (!(!i || i === this.activeTab))
      return this._captureHarmStateFromDom(n), this._captureTargetingStateFromDom(n), this._captureHazardStateFromDom(n), this.activeTab = i, this.render({ parts: ["body"] });
  }
  async _onClearDn(e, n) {
    var i;
    if (e.preventDefault(), e.stopPropagation(), !!((i = game.user) != null && i.isGM))
      return await game.settings.set(this.systemId, Kc, 1), this.render({ parts: ["body"] });
  }
  async _onToggleAnnounce(e, n) {
    var a;
    if (e.preventDefault(), e.stopPropagation(), !((a = game.user) != null && a.isGM)) return;
    const i = !game.settings.get(this.systemId, Io);
    return await game.settings.set(this.systemId, Io, i), this.render({ parts: ["body"] });
  }
  async _onHarmInputChange(e, n) {
    var a, r;
    (a = e == null ? void 0 : e.preventDefault) == null || a.call(e), this._captureHarmStateFromDom(n);
    const i = String(((r = n == null ? void 0 : n.dataset) == null ? void 0 : r.harmKey) ?? "").trim();
    if (["actorId", "mode", "useArmor"].includes(i))
      return this.render({ parts: ["body"] });
  }
  async _onRefreshHarmTarget(e, n) {
    var i, a;
    return (i = e == null ? void 0 : e.preventDefault) == null || i.call(e), (a = e == null ? void 0 : e.stopPropagation) == null || a.call(e), this._captureHarmStateFromDom(n), this.render({ parts: ["body"] });
  }
  async _onApplyHarm(e, n) {
    var s, o, l, c, u;
    if ((s = e == null ? void 0 : e.preventDefault) == null || s.call(e), (o = e == null ? void 0 : e.stopPropagation) == null || o.call(e), !((l = game.user) != null && l.isGM)) return;
    const i = this._captureHarmStateFromDom(n), a = this._buildHarmPayload(i);
    if (!a) {
      (c = ui.notifications) == null || c.warn("Choose a valid harm action before applying it.");
      return;
    }
    const r = await Mn.apply({
      payload: a,
      options: {
        actorId: i.actorId,
        preferSceneTarget: !0,
        logToChat: !0
      }
    });
    return r != null && r.ok ? this.render({ parts: ["body"] }) : ((u = ui.notifications) == null || u.warn((r == null ? void 0 : r.reason) ?? "Unable to apply harm."), this.render({ parts: ["body"] }));
  }
  _getSelectedTargetingTargetUuids(e = null) {
    var i;
    const n = ((i = e == null ? void 0 : e.closest) == null ? void 0 : i.call(e, ".mwd-gmgadget__root")) ?? this._getRootElement();
    return n instanceof HTMLElement ? Array.from(n.querySelectorAll('input[name="targeting-selected"]:checked')).map((a) => a instanceof HTMLInputElement ? String(a.value ?? "").trim() : "").filter(Boolean) : [];
  }
  async _onTargetingInputChange(e, n) {
    var i;
    return (i = e == null ? void 0 : e.preventDefault) == null || i.call(e), this._captureTargetingStateFromDom(n), this.render({ parts: ["body"] });
  }
  async _onRefreshTargeting(e, n) {
    var i, a;
    return (i = e == null ? void 0 : e.preventDefault) == null || i.call(e), (a = e == null ? void 0 : e.stopPropagation) == null || a.call(e), this._captureTargetingStateFromDom(n), this.render({ parts: ["body"] });
  }
  async _onSetTargetingState(e, n) {
    var u, d, f, m, g, p, h, b;
    if ((u = e == null ? void 0 : e.preventDefault) == null || u.call(e), (d = e == null ? void 0 : e.stopPropagation) == null || d.call(e), !((f = game.user) != null && f.isGM)) return;
    const i = this._captureTargetingStateFromDom(n), a = yo(i.attackerTokenId), r = String(((m = n == null ? void 0 : n.dataset) == null ? void 0 : m.targetTokenUuid) ?? "").trim();
    if (!a || !r) {
      (g = ui.notifications) == null || g.warn("Select a machine attacker and a target row before setting targeting state.");
      return;
    }
    const s = ((p = n == null ? void 0 : n.closest) == null ? void 0 : p.call(n, ".mwd-gmgadget__root")) ?? this._getRootElement(), o = `targeting-row-state-${String(((h = n == null ? void 0 : n.dataset) == null ? void 0 : h.targetTokenId) ?? "").trim()}`, l = s instanceof HTMLElement ? s.querySelector(`[name="${o}"]`) : null, c = l instanceof HTMLSelectElement ? String(l.value ?? "").trim() : String(((b = n == null ? void 0 : n.dataset) == null ? void 0 : b.detectionState) ?? "").trim();
    return await bu(a, r, c), this.render({ parts: ["body"] });
  }
  async _onClearTargetingPacket(e, n) {
    var s, o, l, c, u;
    if ((s = e == null ? void 0 : e.preventDefault) == null || s.call(e), (o = e == null ? void 0 : e.stopPropagation) == null || o.call(e), !((l = game.user) != null && l.isGM)) return;
    const i = this._captureTargetingStateFromDom(n), a = yo(i.attackerTokenId), r = String(((c = n == null ? void 0 : n.dataset) == null ? void 0 : c.targetTokenUuid) ?? "").trim();
    if (!a || !r) {
      (u = ui.notifications) == null || u.warn("Select a machine attacker and a target row before clearing packet data.");
      return;
    }
    return await Cf(a, r), this.render({ parts: ["body"] });
  }
  async _onBulkSetTargetingState(e, n) {
    var s, o, l, c;
    if ((s = e == null ? void 0 : e.preventDefault) == null || s.call(e), (o = e == null ? void 0 : e.stopPropagation) == null || o.call(e), !((l = game.user) != null && l.isGM)) return;
    const i = this._captureTargetingStateFromDom(n), a = yo(i.attackerTokenId), r = this._getSelectedTargetingTargetUuids(n);
    if (!a || !r.length) {
      (c = ui.notifications) == null || c.warn("Select a machine attacker and at least one checked target row.");
      return;
    }
    for (const u of r)
      await bu(a, u, i.bulkDetectionState);
    return this.render({ parts: ["body"] });
  }
  async _onBulkClearTargetingPackets(e, n) {
    var s, o, l, c;
    if ((s = e == null ? void 0 : e.preventDefault) == null || s.call(e), (o = e == null ? void 0 : e.stopPropagation) == null || o.call(e), !((l = game.user) != null && l.isGM)) return;
    const i = this._captureTargetingStateFromDom(n), a = yo(i.attackerTokenId), r = this._getSelectedTargetingTargetUuids(n);
    if (!a || !r.length) {
      (c = ui.notifications) == null || c.warn("Select a machine attacker and at least one checked target row.");
      return;
    }
    for (const u of r)
      await Cf(a, u);
    return this.render({ parts: ["body"] });
  }
  _captureHazardStateFromDom(e = null) {
    var r;
    const n = ((r = e == null ? void 0 : e.closest) == null ? void 0 : r.call(e, ".mwd-gmgadget__root")) ?? this._getRootElement();
    if (!(n instanceof HTMLElement)) return this.hazardState;
    const i = (s, o = "") => {
      const l = n.querySelector(s);
      return l instanceof HTMLInputElement || l instanceof HTMLSelectElement || l instanceof HTMLTextAreaElement ? l.value : o;
    }, a = (s, o = !1) => {
      const l = n.querySelector(s);
      return l instanceof HTMLInputElement ? l.checked : o;
    };
    return this.hazardState = Sm({
      label: i('[name="hazard-label"]', this.hazardState.label),
      startExposure: i('[name="hazard-startExposure"]', this.hazardState.startExposure),
      escalationRate: Number(i('[name="hazard-escalationRate"]', this.hazardState.escalationRate)),
      escalationIntervalTurns: Number(i('[name="hazard-escalationIntervalTurns"]', this.hazardState.escalationIntervalTurns)),
      escalationMax: i('[name="hazard-escalationMax"]', this.hazardState.escalationMax),
      onFullBurnDelta: Number(i('[name="hazard-onFullBurnDelta"]', this.hazardState.onFullBurnDelta)),
      clearOnExit: a('[name="hazard-clearOnExit"]', this.hazardState.clearOnExit),
      damage: Number(i('[name="hazard-damage"]', this.hazardState.damage)),
      ap: Number(i('[name="hazard-ap"]', this.hazardState.ap)),
      damageType: i('[name="hazard-damageType"]', this.hazardState.damageType),
      color: i('[name="hazard-color"]', this.hazardState.color)
    }), this.hazardState;
  }
  async _onHazardInputChange(e, n) {
    var i;
    (i = e == null ? void 0 : e.preventDefault) == null || i.call(e), this._captureHazardStateFromDom(n);
  }
  async _onRefreshHazardTemplate(e, n) {
    var i, a;
    return (i = e == null ? void 0 : e.preventDefault) == null || i.call(e), (a = e == null ? void 0 : e.stopPropagation) == null || a.call(e), this._captureHazardStateFromDom(n), this.render({ parts: ["body"] });
  }
  async _onCreateHazard(e, n) {
    var c, u, d, f, m, g, p, h;
    if ((c = e == null ? void 0 : e.preventDefault) == null || c.call(e), (u = e == null ? void 0 : e.stopPropagation) == null || u.call(e), !((d = game.user) != null && d.isGM)) return;
    const i = this._captureHazardStateFromDom(n), a = Wy(), r = UA(a);
    if (!(canvas != null && canvas.scene) || !r) {
      (f = ui.notifications) == null || f.warn("Select a supported Region in Measured Template Mode before creating a hazard.");
      return;
    }
    const s = Yg({
      startExposure: i.startExposure,
      escalation: {
        rate: Number(i.escalationRate ?? 1) || 1,
        intervalTurns: Number(i.escalationIntervalTurns ?? 1) || 1,
        max: i.escalationMax
      },
      onFull: {
        burnDelta: Number(i.onFullBurnDelta ?? 0) || 0
      },
      clearOnExit: !!i.clearOnExit
    }), o = Ku(r);
    if (!o.length) {
      (m = ui.notifications) == null || m.warn("Unable to convert the selected region into a hazard shape.");
      return;
    }
    const [l] = await canvas.scene.createEmbeddedDocuments("Region", [{
      name: String(i.label ?? "Hazard Zone").trim() || "Hazard Zone",
      color: String(i.color ?? "#d86a2c").trim() || "#d86a2c",
      shapes: o,
      flags: {
        mwd: {
          [Nr]: {
            sourceActorUuid: null,
            sourceItemUuid: null,
            payloadId: "gm-hazard",
            templateGeometry: Ui(r),
            damage: Math.max(0, Number(i.damage ?? 0) || 0),
            ap: Math.max(0, Number(i.ap ?? 0) || 0),
            damageType: String(i.damageType ?? "thermal").trim() || "thermal",
            label: `${String(i.label ?? "Hazard Zone").trim() || "Hazard Zone"} (${ci(s.startExposure)})`,
            areaEffect: {
              kind: "persistent",
              hazard: s
            },
            hazardDef: s
          }
        }
      }
    }]);
    return (p = (g = l == null ? void 0 : l.sheet) == null ? void 0 : g.render) == null || p.call(g, !0), (h = ui.notifications) == null || h.info("Hazard region created from the selected region."), this.render({ parts: ["body"] });
  }
  _buildHarmPayload(e) {
    const n = String((e == null ? void 0 : e.source) ?? "").trim(), i = String((e == null ? void 0 : e.notes) ?? "").trim(), a = String((e == null ? void 0 : e.mode) ?? "").trim();
    if (a === "status") {
      const r = String((e == null ? void 0 : e.statusId) ?? "").trim();
      return r ? {
        mode: "status",
        statusId: r,
        active: !!(e != null && e.statusActive),
        source: n,
        notes: i
      } : null;
    }
    return a === "burn" ? {
      mode: "burnDelta",
      delta: Yy(e == null ? void 0 : e.delta, Eo.delta),
      source: n,
      notes: i
    } : a === "physical" || a === "fatigue" ? {
      mode: "trackDelta",
      track: a,
      delta: Yy(e == null ? void 0 : e.delta, Eo.delta),
      useArmor: !!(e != null && e.useArmor),
      damageType: (e == null ? void 0 : e.damageType) ?? Eo.damageType,
      source: n,
      notes: i
    } : null;
  }
  // ---- Scene modifier actions ----
  async _onAddSceneModifierFromPreset(e, n) {
    var l, c, u, d;
    if ((l = e == null ? void 0 : e.preventDefault) == null || l.call(e), (c = e == null ? void 0 : e.stopPropagation) == null || c.call(e), !((u = game.user) != null && u.isGM)) return;
    const i = ((d = n == null ? void 0 : n.closest) == null ? void 0 : d.call(n, ".mwd-gmgadget__root")) ?? this._getRootElement(), a = i instanceof HTMLElement ? i.querySelector('select[name="scene-preset-index"]') : null, r = a instanceof HTMLSelectElement ? Number(a.value) : NaN, s = Vy(
      game.settings.get(this.systemId, Uf)
    ), o = Number.isFinite(r) ? s[r] : null;
    o && await this._mutateSceneModifiers((f) => [
      ...f,
      {
        id: foundry.utils.randomID(),
        label: o.label,
        value: o.value,
        enabled: !0,
        attributeFilter: o.attributeFilter || null,
        intentFilter: o.intentFilter || null,
        source: "preset"
      }
    ]);
  }
  async _onAddSceneModifierAdhoc(e, n) {
    var a, r, s, o;
    if ((a = e == null ? void 0 : e.preventDefault) == null || a.call(e), (r = e == null ? void 0 : e.stopPropagation) == null || r.call(e), !((s = game.user) != null && s.isGM)) return;
    const i = this._captureAdhocFormFromDom(n);
    if (!i) {
      (o = ui.notifications) == null || o.warn("Label and a numeric value are required.");
      return;
    }
    await this._mutateSceneModifiers((l) => [...l, i]);
  }
  async _onToggleSceneModifier(e, n) {
    var a, r, s, o;
    if ((a = e == null ? void 0 : e.preventDefault) == null || a.call(e), (r = e == null ? void 0 : e.stopPropagation) == null || r.call(e), !((s = game.user) != null && s.isGM)) return;
    const i = String(((o = n == null ? void 0 : n.dataset) == null ? void 0 : o.modId) ?? "").trim();
    i && await this._mutateSceneModifiers(
      (l) => l.map((c) => c.id === i ? { ...c, enabled: !c.enabled } : c)
    );
  }
  async _onRemoveSceneModifier(e, n) {
    var a, r, s, o;
    if ((a = e == null ? void 0 : e.preventDefault) == null || a.call(e), (r = e == null ? void 0 : e.stopPropagation) == null || r.call(e), !((s = game.user) != null && s.isGM)) return;
    const i = String(((o = n == null ? void 0 : n.dataset) == null ? void 0 : o.modId) ?? "").trim();
    i && await this._mutateSceneModifiers((l) => l.filter((c) => c.id !== i));
  }
  async _onClearSceneModifiers(e, n) {
    var i, a, r;
    (i = e == null ? void 0 : e.preventDefault) == null || i.call(e), (a = e == null ? void 0 : e.stopPropagation) == null || a.call(e), (r = game.user) != null && r.isGM && await this._mutateSceneModifiers(() => []);
  }
  async _mutateSceneModifiers(e) {
    const n = canvas == null ? void 0 : canvas.scene;
    if (!n) return;
    const i = Jy(n.getFlag("mwd", xc)), a = await e(i);
    return await n.setFlag("mwd", xc, a), this.render({ parts: ["body"] });
  }
  _captureAdhocFormFromDom(e) {
    var c;
    const n = ((c = e == null ? void 0 : e.closest) == null ? void 0 : c.call(e, ".mwd-gmgadget__root")) ?? this._getRootElement();
    if (!(n instanceof HTMLElement)) return null;
    const i = (u) => {
      const d = n.querySelector(u);
      return d instanceof HTMLInputElement || d instanceof HTMLSelectElement ? d.value : "";
    }, a = i('[name="scene-adhoc-label"]').trim(), r = i('[name="scene-adhoc-value"]').trim(), s = i('[name="scene-adhoc-attributeFilter"]').trim() || null, o = i('[name="scene-adhoc-intentFilter"]').trim() || null;
    if (!a) return null;
    const l = Number(r);
    return Number.isFinite(l) ? {
      id: foundry.utils.randomID(),
      label: a,
      value: Math.trunc(l),
      enabled: !0,
      attributeFilter: s,
      intentFilter: o,
      source: "adhoc"
    } : null;
  }
};
F(at, "DEFAULT_OPTIONS", {
  id: WB,
  classes: ["mwd-gmgadget"],
  window: {
    title: "MWD GM Gadget",
    popOut: !0,
    resizable: !0
  },
  position: {
    width: 440,
    height: 620
  },
  actions: {
    switchTab: at.prototype._onSwitchTab,
    setDn: at.prototype._onSetDn,
    clearDn: at.prototype._onClearDn,
    toggleAnnounce: at.prototype._onToggleAnnounce,
    harmInputChange: at.prototype._onHarmInputChange,
    refreshHarmTarget: at.prototype._onRefreshHarmTarget,
    applyHarm: at.prototype._onApplyHarm,
    targetingInputChange: at.prototype._onTargetingInputChange,
    refreshTargeting: at.prototype._onRefreshTargeting,
    setTargetingState: at.prototype._onSetTargetingState,
    clearTargetingPacket: at.prototype._onClearTargetingPacket,
    bulkSetTargetingState: at.prototype._onBulkSetTargetingState,
    bulkClearTargetingPackets: at.prototype._onBulkClearTargetingPackets,
    hazardInputChange: at.prototype._onHazardInputChange,
    refreshHazardTemplate: at.prototype._onRefreshHazardTemplate,
    createHazard: at.prototype._onCreateHazard,
    addSceneModifierFromPreset: at.prototype._onAddSceneModifierFromPreset,
    addSceneModifierAdhoc: at.prototype._onAddSceneModifierAdhoc,
    toggleSceneModifier: at.prototype._onToggleSceneModifier,
    removeSceneModifier: at.prototype._onRemoveSceneModifier,
    clearSceneModifiers: at.prototype._onClearSceneModifiers
  }
}), F(at, "PARTS", {
  body: { template: JB }
});
let Kf = at;
function Vy(t) {
  return Array.isArray(t) ? t.filter((e) => (e == null ? void 0 : e.label) && Number.isFinite(Number(e == null ? void 0 : e.value))).map((e, n) => {
    const i = Math.trunc(Number(e.value));
    return {
      index: n,
      label: String(e.label).trim(),
      value: i,
      signedValue: i >= 0 ? `+${i}` : String(i),
      attributeFilter: String(e.attributeFilter ?? "").trim() || null,
      intentFilter: String(e.intentFilter ?? "").trim() || null
    };
  }) : [];
}
function Jy(t) {
  return Array.isArray(t) ? t.map((e) => {
    var r, s;
    const n = O0(e), i = ((r = Sh.find((o) => o.value === (n.attributeFilter ?? ""))) == null ? void 0 : r.label) ?? null, a = ((s = Ah.find((o) => o.value === (n.intentFilter ?? ""))) == null ? void 0 : s.label) ?? null;
    return {
      ...n,
      attributeFilterLabel: n.attributeFilter ? i : null,
      intentFilterLabel: n.intentFilter ? a : null,
      signedValue: n.value >= 0 ? `+${n.value}` : String(n.value),
      isPositive: n.value > 0
    };
  }) : [];
}
function Yy(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? Math.trunc(n) : e;
}
let km = null;
function cx({ systemId: t = "mwd" } = {}) {
  return km || (km = new Kf({ systemId: t })), km;
}
const ux = "gmDnPresetEditor";
function dx(t = []) {
  const e = [], n = [], i = /* @__PURE__ */ new Set();
  if ((Array.isArray(t) ? t : []).forEach((a, r) => {
    const s = String((a == null ? void 0 : a.label) ?? "").trim(), o = String((a == null ? void 0 : a.dn) ?? "").trim(), l = `Row ${r + 1}`;
    if (!s) {
      n.push(`${l}: label cannot be blank.`);
      return;
    }
    if (i.has(s.toLowerCase())) {
      n.push(`${l}: duplicate label "${s}".`);
      return;
    }
    i.add(s.toLowerCase());
    const c = Number(o);
    if (!Number.isFinite(c)) {
      n.push(`${l}: DN must be a number.`);
      return;
    }
    if (c < 0) {
      n.push(`${l}: DN cannot be negative.`);
      return;
    }
    e.push({
      label: s,
      dn: Math.trunc(c)
    });
  }), n.length) throw Rn(n);
  return Gl(e, { strict: !0 });
}
function mx(t = []) {
  return Gl(t, { strict: !1 }).map((e) => ({
    label: e.label,
    dn: String(e.dn)
  }));
}
function fx(t = "") {
  const e = String(t ?? "").trim();
  if (!e) return [];
  let n;
  try {
    n = JSON.parse(e);
  } catch (i) {
    throw Rn([
      `Bulk JSON must be valid JSON: ${i.message}`
    ]);
  }
  return Gl(n, { strict: !0 });
}
function gx(t = []) {
  return JSON.stringify(
    Gl(t, { strict: !1 }),
    null,
    2
  );
}
const px = {
  id: "gm-dn-presets",
  menuKey: ux,
  settingKey: F0,
  settingType: Array,
  title: "GM DN Presets",
  description: "Edit the preset DN buttons shown in the GM Gadget difficulty tab.",
  helpText: "Rows are shown in order in the GM Gadget. Labels must be unique.",
  bulkHelpText: 'JSON shape: [{ "label": "Standard", "dn": 1 }]',
  emptyStateText: "No DN preset rows yet. Add one to show buttons in the GM Gadget.",
  addRowLabel: "Add Preset",
  rowSchema: [
    {
      key: "label",
      label: "Label",
      type: "text",
      placeholder: "Standard"
    },
    {
      key: "dn",
      label: "DN",
      type: "number",
      min: 0,
      step: 1,
      placeholder: "1"
    }
  ],
  menu: {
    name: "GM DN Presets",
    label: "Configure",
    hint: "Edit the preset DN buttons used by the GM Gadget.",
    icon: "fas fa-sliders-h",
    restricted: !0
  },
  defaultData: ex,
  createEmptyRow: () => ({
    label: "",
    dn: "1"
  }),
  toRows: mx,
  rowsToValue: dx,
  parseBulk: fx,
  serializeBulk: gx
};
function hx() {
  Xa(px);
}
const bx = "lifeModuleCatalogEditor";
function yx(t = []) {
  return Yr((Array.isArray(t) ? t : []).map((e) => ({
    id: String((e == null ? void 0 : e.id) ?? ""),
    moduleType: String((e == null ? void 0 : e.moduleType) ?? ""),
    label: String((e == null ? void 0 : e.label) ?? ""),
    grants: String((e == null ? void 0 : e.grants) ?? ""),
    requiresAny: String((e == null ? void 0 : e.requiresAny) ?? ""),
    excludesAny: String((e == null ? void 0 : e.excludesAny) ?? "")
  })), { strict: !0 });
}
function Sx(t = []) {
  return Yr(t, { strict: !1 }).map((e) => ({
    id: e.id,
    moduleType: e.moduleType,
    label: e.label,
    grants: t_(e.grants),
    requiresAny: e.requiresAny.join(", "),
    excludesAny: e.excludesAny.join(", ")
  }));
}
function Ax(t = "") {
  const e = String(t ?? "").trim();
  if (!e) return [];
  let n;
  try {
    n = JSON.parse(e);
  } catch (i) {
    const a = new Error(`Bulk JSON must be valid JSON: ${i.message}`);
    throw a.validationErrors = [a.message], a;
  }
  if (!Array.isArray(n)) {
    const i = new Error("Bulk JSON must be an array.");
    throw i.validationErrors = [i.message], i;
  }
  return Yr(n, { strict: !0 });
}
function kx(t = []) {
  return JSON.stringify(
    Yr(t, { strict: !1 }),
    null,
    2
  );
}
const Tx = {
  id: "life-module-catalog",
  menuKey: bx,
  settingKey: Ps,
  settingType: Array,
  title: "Life Module Catalog",
  description: "Define the canonical life modules available for Faction, Childhood, Higher Education, and Real Life character development.",
  helpText: 'Grants use ";" to separate separate bonuses and "|" to separate choices inside one bonus. Each choice is prefixed with "skill:" or "edgePool:". Example: "skill:deception; edgePool:rumor" or "skill:tracking|skill:projectileWeapons".',
  bulkHelpText: 'JSON shape: [{ "id": "faction-capellan-confederation", "moduleType": "faction", "label": "Capellan Confederation", "grants": [{ "id": "skill", "choices": [{ "type": "skill", "value": "deception" }] }, { "id": "edge-pool", "choices": [{ "type": "edgePool", "value": "rumor" }] }], "requiresAny": [], "excludesAny": [] }]',
  emptyStateText: "No life modules yet. Add one to start the catalog.",
  addRowLabel: "Add Life Module",
  rowSchema: [
    {
      key: "id",
      label: "Id",
      type: "text",
      placeholder: "childhood-backwoods"
    },
    {
      key: "moduleType",
      label: "Slot",
      type: "select",
      options: Qw
    },
    {
      key: "label",
      label: "Label",
      type: "text",
      placeholder: "Backwoods"
    },
    {
      key: "grants",
      label: "Grants",
      type: "text",
      placeholder: "skill:tracking|skill:projectileWeapons"
    },
    {
      key: "requiresAny",
      label: "Requires Any",
      type: "text",
      placeholder: "childhood-nobility"
    },
    {
      key: "excludesAny",
      label: "Excludes Any",
      type: "text",
      placeholder: "higher-education-military-academy"
    }
  ],
  menu: {
    name: "Life Module Catalog",
    label: "Configure",
    hint: "Edit the canonical life modules and their skill roll modifiers.",
    icon: "fas fa-book-open",
    restricted: !0
  },
  defaultData: ah,
  createEmptyRow: () => ({
    id: "",
    moduleType: "childhood",
    label: "",
    grants: "",
    requiresAny: "",
    excludesAny: ""
  }),
  toRows: Sx,
  rowsToValue: yx,
  parseBulk: Ax,
  serializeBulk: kx
};
function wx() {
  Xa(Tx);
}
const Mx = "personalActionCatalogEditor", Tm = Object.freeze([
  { value: "false", label: "No" },
  { value: "true", label: "Yes" }
]);
function vx(t = []) {
  try {
    return Vr((Array.isArray(t) ? t : []).map((e) => ({
      id: String((e == null ? void 0 : e.id) ?? ""),
      label: String((e == null ? void 0 : e.label) ?? ""),
      category: String((e == null ? void 0 : e.category) ?? ""),
      cost: {
        resource: String((e == null ? void 0 : e.costResource) ?? "sa"),
        value: String((e == null ? void 0 : e.costValue) ?? "1")
      },
      resolver: String((e == null ? void 0 : e.resolver) ?? "action"),
      prompt: {
        type: String((e == null ? void 0 : e.promptType) ?? "none"),
        required: String((e == null ? void 0 : e.promptRequired) ?? "false")
      },
      implementation: {
        state: String((e == null ? void 0 : e.implementationState) ?? "ready"),
        reason: String((e == null ? void 0 : e.implementationReason) ?? (e == null ? void 0 : e.reason) ?? "")
      },
      rollIntent: String((e == null ? void 0 : e.rollIntent) ?? ""),
      tags: String((e == null ? void 0 : e.tags) ?? ""),
      resolves: String((e == null ? void 0 : e.resolves) ?? ""),
      prominent: String((e == null ? void 0 : e.prominent) ?? "false"),
      prominentWhenBurning: String((e == null ? void 0 : e.prominentWhenBurning) ?? "false")
    })), { strict: !0 });
  } catch (e) {
    throw Rn(
      Array.isArray(e.validationErrors) ? e.validationErrors : [e.message]
    );
  }
}
function Cx(t = []) {
  return Vr(t, { strict: !1 }).map((e) => {
    var n, i, a, r, s, o, l;
    return {
      id: String(e.id ?? ""),
      label: String(e.label ?? ""),
      category: String(e.category ?? ""),
      costResource: String(((n = e.cost) == null ? void 0 : n.resource) ?? "sa"),
      costValue: String(((i = e.cost) == null ? void 0 : i.value) ?? "0"),
      resolver: String(e.resolver ?? "action"),
      promptType: String(((a = e.prompt) == null ? void 0 : a.type) ?? "none"),
      promptRequired: (r = e.prompt) != null && r.required ? "true" : "false",
      implementationState: String(((s = e.implementation) == null ? void 0 : s.state) ?? "ready"),
      implementationReason: String(((o = e.implementation) == null ? void 0 : o.reason) ?? ""),
      rollIntent: String(((l = e.roll) == null ? void 0 : l.intent) ?? ""),
      tags: Array.isArray(e.tags) ? e.tags.join(", ") : String(e.tags ?? ""),
      resolves: Array.isArray(e.resolves) ? e.resolves.join(", ") : String(e.resolves ?? ""),
      prominent: e.prominent ? "true" : "false",
      prominentWhenBurning: e.prominentWhenBurning ? "true" : "false"
    };
  });
}
function Ix(t = "") {
  const e = String(t ?? "").trim();
  if (!e) return [];
  let n;
  try {
    n = JSON.parse(e);
  } catch (i) {
    throw Rn([
      `Bulk JSON must be valid JSON: ${i.message}`
    ]);
  }
  try {
    return Vr(n, { strict: !0 });
  } catch (i) {
    throw Rn(
      Array.isArray(i.validationErrors) ? i.validationErrors : [i.message]
    );
  }
}
function Ex(t = []) {
  return JSON.stringify(
    Vr(t, { strict: !1 }),
    null,
    2
  );
}
const Nx = {
  id: "personal-action-catalog",
  menuKey: Mx,
  settingKey: ou,
  settingType: Array,
  title: "Personal Action Catalog",
  description: "Edit the action buttons shown in the personal combat action menu.",
  helpText: "Rows are shown in menu order within their category. Resolver controls which central action executor owns the action.",
  bulkHelpText: 'JSON shape: [{ "id": "move", "label": "Move", "category": "standard", "cost": { "resource": "sa", "value": 1 }, "resolver": "movement" }]',
  emptyStateText: "No actions configured. Restore defaults to rebuild the standard action catalog.",
  addRowLabel: "Add Action",
  rowSchema: [
    {
      key: "id",
      label: "Id",
      type: "text",
      placeholder: "move"
    },
    {
      key: "label",
      label: "Label",
      type: "text",
      placeholder: "Move"
    },
    {
      key: "category",
      label: "Category",
      type: "select",
      options: () => ff
    },
    {
      key: "costResource",
      label: "Cost Resource",
      type: "select",
      options: () => Vk
    },
    {
      key: "costValue",
      label: "Cost",
      type: "number",
      min: 0,
      step: 1,
      placeholder: "1"
    },
    {
      key: "resolver",
      label: "Resolver",
      type: "select",
      options: () => Wk
    },
    {
      key: "promptType",
      label: "Prompt",
      type: "select",
      options: () => Jk
    },
    {
      key: "promptRequired",
      label: "Prompt Required",
      type: "select",
      options: () => Tm
    },
    {
      key: "implementationState",
      label: "State",
      type: "select",
      options: () => Yk
    },
    {
      key: "implementationReason",
      label: "State Reason",
      type: "text",
      placeholder: "Not yet implemented."
    },
    {
      key: "rollIntent",
      label: "Roll Intent",
      type: "text",
      placeholder: "overload"
    },
    {
      key: "tags",
      label: "Tags",
      type: "text",
      placeholder: "combat, movement"
    },
    {
      key: "resolves",
      label: "Resolves",
      type: "text",
      placeholder: "prone, stunned"
    },
    {
      key: "prominent",
      label: "Prominent",
      type: "select",
      options: () => Tm
    },
    {
      key: "prominentWhenBurning",
      label: "Burn Highlight",
      type: "select",
      options: () => Tm
    }
  ],
  menu: {
    name: "Personal Action Catalog",
    label: "Configure",
    hint: "Edit the personal combat action menus and declarative intent routing.",
    icon: "fas fa-list-check",
    restricted: !0
  },
  defaultData: pp,
  createEmptyRow: () => {
    var t;
    return {
      id: "",
      label: "",
      category: ((t = ff[0]) == null ? void 0 : t.value) ?? "standard",
      costResource: "sa",
      costValue: "1",
      resolver: "action",
      promptType: "none",
      promptRequired: "false",
      implementationState: "ready",
      implementationReason: "",
      rollIntent: "",
      tags: "combat",
      resolves: "",
      prominent: "false",
      prominentWhenBurning: "false"
    };
  },
  toRows: Cx,
  rowsToValue: vx,
  parseBulk: Ix,
  serializeBulk: Ex
};
function Px() {
  Xa(Nx);
}
const Rx = "skillSpecializationEditor";
function Hf() {
  return Qo().map((t) => ({
    value: t.code,
    label: t.label
  }));
}
function Dx(t = []) {
  const e = new Set(Hf().map((a) => a.value)), n = {}, i = [];
  if ((Array.isArray(t) ? t : []).forEach((a, r) => {
    const s = String((a == null ? void 0 : a.skillCode) ?? "").trim(), o = String((a == null ? void 0 : a.label) ?? "").trim(), l = `Row ${r + 1}`;
    if (!s) {
      i.push(`${l}: choose a skill.`);
      return;
    }
    if (!e.has(s)) {
      i.push(`${l}: unknown skill code "${s}".`);
      return;
    }
    if (!o) {
      i.push(`${l}: specialization label cannot be blank.`);
      return;
    }
    (n[s] ?? (n[s] = [])).push(o);
  }), i.length) throw Rn(i);
  return Gu(n, { strict: !0 });
}
function Ox(t = {}) {
  const e = Gu(t, { strict: !1 });
  return Object.entries(e).flatMap(
    ([n, i]) => i.map((a) => ({ skillCode: n, label: a }))
  );
}
function Lx(t = "") {
  const e = String(t ?? "").trim();
  if (!e) return {};
  let n;
  try {
    n = JSON.parse(e);
  } catch (i) {
    throw Rn([
      `Bulk JSON must be valid JSON: ${i.message}`
    ]);
  }
  return Gu(n, { strict: !0 });
}
function $x(t = {}) {
  return JSON.stringify(
    Gu(t, { strict: !1 }),
    null,
    2
  );
}
const _x = {
  id: "skill-specializations",
  menuKey: Rx,
  settingKey: sf,
  title: "Skill Specializations",
  description: "Edit the world specialization catalog for existing skills. Omitted skills have no available specializations.",
  helpText: "Use rows for normal editing. Use the bulk JSON tab for fast import/export.",
  bulkHelpText: 'JSON shape: { "athletics": ["Running", "Jumping"] }',
  emptyStateText: "No specialization rows yet. Add one to start the catalog.",
  addRowLabel: "Add Specialization",
  rowSchema: [
    {
      key: "skillCode",
      label: "Skill",
      type: "select",
      options: Hf
    },
    {
      key: "label",
      label: "Specialization",
      type: "text",
      placeholder: "Running"
    }
  ],
  menu: {
    name: "Skill Specializations",
    label: "Configure",
    hint: "Edit the specialization catalog for canonical skills.",
    icon: "fas fa-list",
    restricted: !0
  },
  defaultData: ok,
  createEmptyRow: () => {
    var t;
    return {
      skillCode: ((t = Hf()[0]) == null ? void 0 : t.value) ?? "",
      label: ""
    };
  },
  toRows: Ox,
  rowsToValue: Dx,
  parseBulk: Lx,
  serializeBulk: $x
};
function Bx() {
  Xa(_x);
}
const xx = "statusConditionCatalogEditor";
function zx(t = []) {
  try {
    return Ja((Array.isArray(t) ? t : []).map((e) => ({
      id: String((e == null ? void 0 : e.id) ?? ""),
      label: String((e == null ? void 0 : e.label) ?? ""),
      actorGroup: String((e == null ? void 0 : e.actorGroup) ?? ""),
      category: String((e == null ? void 0 : e.category) ?? ""),
      tags: String((e == null ? void 0 : e.tags) ?? ""),
      icon: String((e == null ? void 0 : e.icon) ?? ""),
      manual: String((e == null ? void 0 : e.manual) ?? "true"),
      managed: String((e == null ? void 0 : e.managed) ?? "false"),
      modifierKey: String((e == null ? void 0 : e.modifierKey) ?? ""),
      order: String((e == null ? void 0 : e.order) ?? "0")
    })), { strict: !0 });
  } catch (e) {
    throw Rn(
      Array.isArray(e.validationErrors) ? e.validationErrors : [e.message]
    );
  }
}
function Fx(t = []) {
  return Ja(t, { strict: !1 }).map((e) => ({
    id: String(e.id ?? ""),
    label: String(e.label ?? ""),
    actorGroup: String(e.actorGroup ?? "person"),
    category: String(e.category ?? ""),
    tags: UE(e.tags ?? []),
    icon: String(e.icon ?? ""),
    manual: e.manual ? "true" : "false",
    managed: e.managed ? "true" : "false",
    modifierKey: String(e.modifierKey ?? ""),
    order: String(e.order ?? "0")
  }));
}
function Ux(t = "") {
  const e = String(t ?? "").trim();
  if (!e) return [];
  let n;
  try {
    n = JSON.parse(e);
  } catch (i) {
    throw Rn([
      `Bulk JSON must be valid JSON: ${i.message}`
    ]);
  }
  try {
    return Ja(n, { strict: !0 });
  } catch (i) {
    throw Rn(
      Array.isArray(i.validationErrors) ? i.validationErrors : [i.message]
    );
  }
}
function Kx(t = []) {
  return JSON.stringify(
    Ja(t, { strict: !1 }),
    null,
    2
  );
}
const Hx = {
  id: "status-condition-catalog",
  menuKey: xx,
  settingKey: Ms,
  settingType: Array,
  title: "Status Condition Catalog",
  description: "Edit the actor-aware statuses available in MWD status pickers and GM Harm.",
  helpText: "Actor group controls where a condition can be applied. Modifier Key is optional and only links to existing mechanics-backed status modifiers.",
  bulkHelpText: 'JSON shape: [{ "id": "unstable", "label": "Unstable", "actorGroup": "machine", "category": "stability", "tags": ["movement"] }]',
  emptyStateText: "No condition rows configured. Restore defaults to rebuild the standard MWD status catalog.",
  addRowLabel: "Add Condition",
  rowSchema: [
    { key: "id", label: "Id", type: "text", placeholder: "unstable" },
    { key: "label", label: "Label", type: "text", placeholder: "Unstable" },
    { key: "actorGroup", label: "Actor Group", type: "select", options: () => $E },
    { key: "category", label: "Category", type: "text", placeholder: "stability" },
    { key: "tags", label: "Tags", type: "text", placeholder: "movement, piloting" },
    { key: "icon", label: "Icon", type: "text", placeholder: "systems/mwd/img/icons/status/falling.svg" },
    { key: "manual", label: "Manual", type: "select", options: () => fb },
    { key: "managed", label: "Managed", type: "select", options: () => fb },
    { key: "modifierKey", label: "Modifier Key", type: "text", placeholder: "prone" },
    { key: "order", label: "Order", type: "number", step: 1, placeholder: "1000" }
  ],
  menu: {
    name: "Status Condition Catalog",
    label: "Configure",
    hint: "Edit person, vehicle, and BattleMech status condition availability.",
    icon: "fas fa-heart-pulse",
    restricted: !0
  },
  defaultData: sp,
  createEmptyRow: () => ({
    id: "",
    label: "",
    actorGroup: "machine",
    category: "general",
    tags: "",
    icon: "",
    manual: "true",
    managed: "false",
    modifierKey: "",
    order: "0"
  }),
  toRows: Fx,
  rowsToValue: zx,
  parseBulk: Ux,
  serializeBulk: Kx
};
function jx() {
  Xa(Hx);
}
const Gx = "weaponPayloadFamilyCatalogEditor", qx = "weaponPayloadTagCatalogEditor", Wx = /^[A-Za-z0-9][A-Za-z0-9._-]*$/u;
function j0(t = [], { noun: e = "entry" } = {}) {
  const n = [], i = [], a = /* @__PURE__ */ new Set();
  if ((Array.isArray(t) ? t : []).forEach((r, s) => {
    const o = String((r == null ? void 0 : r.key) ?? "").trim(), l = String((r == null ? void 0 : r.label) ?? "").trim(), c = String((r == null ? void 0 : r.description) ?? "").trim(), u = `Row ${s + 1}`;
    if (!o) {
      i.push(`${u}: ${e} key cannot be blank.`);
      return;
    }
    if (!Wx.test(o)) {
      i.push(`${u}: "${o}" must start with a letter or number and use only letters, numbers, dot, underscore, or hyphen.`);
      return;
    }
    const d = o.toLowerCase();
    if (a.has(d)) {
      i.push(`${u}: duplicate ${e} key "${o}".`);
      return;
    }
    a.add(d), n.push({
      key: o,
      label: l || o,
      description: c
    });
  }), i.length) throw Rn(i);
  return Wi(n);
}
function G0(t = []) {
  return Wi(t).map((e) => ({
    key: e.key,
    label: e.label,
    description: e.description
  }));
}
function q0(t = "", { defaults: e = [] } = {}) {
  const n = String(t ?? "").trim();
  if (!n) return [];
  let i;
  try {
    i = JSON.parse(n);
  } catch (a) {
    throw Rn([
      `Bulk JSON must be valid JSON: ${a.message}`
    ]);
  }
  return Wi(i, { defaults: e });
}
function W0(t = []) {
  return JSON.stringify(Wi(t), null, 2);
}
const V0 = Object.freeze([
  {
    key: "key",
    label: "Key",
    type: "text",
    placeholder: "40mmGrenade"
  },
  {
    key: "label",
    label: "Label",
    type: "text",
    placeholder: "40mm Grenade"
  },
  {
    key: "description",
    label: "Description",
    type: "text",
    placeholder: "Shown only in this settings editor"
  }
]), Vx = {
  id: "weapon-payload-families",
  menuKey: Gx,
  settingKey: i0,
  settingType: Array,
  title: "Weapon Payload Families",
  description: "Edit the canonical family keys used to match reusable payload items to compatible weapons.",
  helpText: "Families are compatibility keys. A weapon accepts payloads that share at least one family.",
  bulkHelpText: 'JSON shape: [{ "key": "40mmGrenade", "label": "40mm Grenade", "description": "..." }]',
  emptyStateText: "No payload families configured. Restore defaults to rebuild the standard catalog.",
  addRowLabel: "Add Family",
  rowSchema: V0,
  menu: {
    name: "Weapon Payload Families",
    label: "Configure",
    hint: "Edit canonical payload family keys used by weapon compatibility.",
    icon: "fas fa-crosshairs",
    restricted: !0
  },
  defaultData: by,
  createEmptyRow: () => ({ key: "", label: "", description: "" }),
  toRows: G0,
  rowsToValue: (t) => j0(t, { noun: "family" }),
  parseBulk: (t) => q0(t, { defaults: by() }),
  serializeBulk: W0
}, Jx = {
  id: "weapon-payload-tags",
  menuKey: qx,
  settingKey: a0,
  settingType: Array,
  title: "Weapon Payload Tags",
  description: "Edit the canonical tag keys used to refine payload compatibility and describe payload behavior.",
  helpText: "Tags refine an accepted family match through required, optional, or excluded tag filters.",
  bulkHelpText: 'JSON shape: [{ "key": "armorPiercing", "label": "Armor Piercing", "description": "..." }]',
  emptyStateText: "No payload tags configured. Restore defaults to rebuild the standard catalog.",
  addRowLabel: "Add Tag",
  rowSchema: V0,
  menu: {
    name: "Weapon Payload Tags",
    label: "Configure",
    hint: "Edit canonical payload tag keys used by payloads and weapon filters.",
    icon: "fas fa-tags",
    restricted: !0
  },
  defaultData: yy,
  createEmptyRow: () => ({ key: "", label: "", description: "" }),
  toRows: G0,
  rowsToValue: (t) => j0(t, { noun: "tag" }),
  parseBulk: (t) => q0(t, { defaults: yy() }),
  serializeBulk: W0
};
function Yx() {
  Xa(Vx), Xa(Jx);
}
class Qx {
  static register() {
    hx(), wx(), Px(), Bx(), jB(), jx(), Yx(), game.settings.register(I, "useDestinyMechanics", {
      name: ne.settings.useDestinyMechanics.name,
      hint: ne.settings.useDestinyMechanics.hint,
      scope: "world",
      config: !0,
      type: Boolean,
      default: !0
    }), game.settings.register(I, "enableGMGadget", {
      name: "Enable GM Gadget",
      hint: "If enabled, renders the new AppV2 GM Gadget UI on startup.",
      scope: "world",
      config: !0,
      type: Boolean,
      default: !0
    }), game.settings.register(I, DA, {
      name: "Enable BattleMech Token Heat FX",
      hint: "Show BattleMech token glow, shimmer, and subtle blur based on resolved heat.",
      scope: "client",
      config: !0,
      type: Boolean,
      default: !0
    }), game.settings.register(I, MT, {
      name: "Machine Critical Table: General",
      hint: "2d6 RollTable UUID that chooses the general type of machine critical problem.",
      scope: "world",
      config: !0,
      type: String,
      default: Vt.general
    }), game.settings.register(I, vT, {
      name: "Machine Critical Table: BattleMech Head",
      hint: "Location interpretation table for BattleMech head criticals.",
      scope: "world",
      config: !0,
      type: String,
      default: Vt.mechHead
    }), game.settings.register(I, CT, {
      name: "Machine Critical Table: BattleMech Torso",
      hint: "Location interpretation table for BattleMech torso and forced critical hits.",
      scope: "world",
      config: !0,
      type: String,
      default: Vt.mechTorso
    }), game.settings.register(I, IT, {
      name: "Machine Critical Table: BattleMech Arms",
      hint: "Location interpretation table for BattleMech arm criticals.",
      scope: "world",
      config: !0,
      type: String,
      default: Vt.mechArms
    }), game.settings.register(I, ET, {
      name: "Machine Critical Table: BattleMech Legs",
      hint: "Location interpretation table for BattleMech leg criticals.",
      scope: "world",
      config: !0,
      type: String,
      default: Vt.mechLegs
    }), game.settings.register(I, NT, {
      name: "Machine Critical Table: Vehicle Body",
      hint: "Location interpretation table for vehicle body criticals.",
      scope: "world",
      config: !0,
      type: String,
      default: Vt.vehicleBody
    }), game.settings.register(I, PT, {
      name: "Machine Critical Table: Vehicle Turret",
      hint: "Location interpretation table for vehicle turret and weapon criticals.",
      scope: "world",
      config: !0,
      type: String,
      default: Vt.vehicleTurret
    }), game.settings.register(I, RT, {
      name: "Machine Critical Table: Vehicle Mobility",
      hint: "Location interpretation table for vehicle mobility criticals.",
      scope: "world",
      config: !0,
      type: String,
      default: Vt.vehicleMobility
    });
  }
  static getSystemProperty(e, n) {
    return game.settings.get(I, e) ?? n;
  }
}
function ql(t, e, { canPost: n = !0, separator: i = " | " } = {}) {
  var c, u, d, f, m, g;
  const a = t ?? {}, r = (a == null ? void 0 : a.edge) ?? null, s = Array.isArray((c = a == null ? void 0 : a.roll) == null ? void 0 : c.failureDiceRefs) ? a.roll.failureDiceRefs : [], o = Array.isArray((u = r == null ? void 0 : r.allowed) == null ? void 0 : u.postPools) ? r.allowed.postPools : [], l = !!((d = r == null ? void 0 : r.availableActions) != null && d.canPostRerollFailures) && !!n && s.length > 0 && o.length > 0;
  if (!(r != null && r.domain) || (e.edge = {
    domain: r.domain,
    earned: ((f = a == null ? void 0 : a.outcomeModel) == null ? void 0 : f.edgeEarned) ?? null,
    preSpent: Number(((m = r == null ? void 0 : r.pre) == null ? void 0 : m.spent) ?? 0),
    postSpent: Number(((g = r == null ? void 0 : r.post) == null ? void 0 : g.spent) ?? 0),
    canPost: l,
    failureCount: s.length,
    postPools: o
  }, e.metaRows.push({
    text: `Edge: ${r.domain}${i}pre ${e.edge.preSpent}${i}post ${e.edge.postSpent}`,
    title: ""
  }), !e.edge.canPost)) return !1;
  e.footerRows.push({
    text: `Post-spend: Reroll ${e.edge.failureCount} failure${e.edge.failureCount === 1 ? "" : "s"}`
  });
  for (const p of e.edge.postPools)
    e.actions.push({
      action: "edgePostReroll",
      label: `Spend ${p}`,
      dataset: { "pool-key": p },
      cssClass: "mwd-edge-post"
    });
  return !0;
}
const kh = "firstAid", Hc = Object.freeze([
  Object.freeze({ key: "excellent", label: "Excellent Conditions", dn: 1 }),
  Object.freeze({ key: "normal", label: "Normal Conditions", dn: 2 }),
  Object.freeze({ key: "poor", label: "Poor Conditions", dn: 3 })
]);
function wm(t = "") {
  return foundry.utils.escapeHTML(String(t ?? ""));
}
function jf(t = null) {
  return (t == null ? void 0 : t.type) === M.actorTypes.character || (t == null ? void 0 : t.type) === M.actorTypes.npc;
}
function J0(t = null) {
  return (t == null ? void 0 : t.document) ?? t ?? null;
}
function Xx(t = [], e = (n) => n) {
  const n = /* @__PURE__ */ new Set(), i = [];
  for (const a of t) {
    const r = String(e(a) ?? "").trim();
    !r || n.has(r) || (n.add(r), i.push(a));
  }
  return i;
}
function Zx(t = null) {
  const e = J0(t);
  return (e == null ? void 0 : e.actor) ?? (t == null ? void 0 : t.actor) ?? null;
}
function ez(t = null) {
  var a, r, s;
  const n = [
    ...Array.from(((a = game.user) == null ? void 0 : a.targets) ?? []),
    ...Array.from(((r = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : r.controlled) ?? []),
    ...Array.from(((s = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : s.placeables) ?? [])
  ].map((o) => {
    const l = J0(o), c = Zx(o);
    return !c || !jf(c) ? null : {
      key: c.uuid,
      actorUuid: c.uuid,
      tokenUuid: (l == null ? void 0 : l.uuid) ?? "",
      label: (l == null ? void 0 : l.name) || c.name || "Target",
      actorName: c.name || (l == null ? void 0 : l.name) || "Target"
    };
  }).filter(Boolean), i = t && jf(t) ? [{
    key: t.uuid,
    actorUuid: t.uuid,
    tokenUuid: "",
    label: `${t.name || "Self"} (Self)`,
    actorName: t.name || "Self"
  }] : [];
  return Xx([...i, ...n], (o) => `${o.actorUuid}|${o.tokenUuid}`).sort((o, l) => {
    const c = o.actorUuid === (t == null ? void 0 : t.uuid) && !o.tokenUuid ? -1 : 0, u = l.actorUuid === (t == null ? void 0 : t.uuid) && !l.tokenUuid ? -1 : 0;
    return c !== u ? c - u : String(o.label).localeCompare(String(l.label));
  });
}
function tz(t = null) {
  const e = Array.from((t == null ? void 0 : t.items) ?? []).filter((n) => ["gear", "consumable"].includes(String((n == null ? void 0 : n.type) ?? (n == null ? void 0 : n.canonicalType) ?? "").trim())).filter((n) => {
    var i;
    return String(((i = n == null ? void 0 : n.system) == null ? void 0 : i.category) ?? "").trim() === "medical";
  }).filter((n) => {
    var i;
    return Math.max(0, Number(((i = n == null ? void 0 : n.system) == null ? void 0 : i.quantity) ?? 1) || 0) > 0;
  }).map((n) => {
    var i;
    return {
      itemId: n.id,
      name: n.name || "Medical Equipment",
      rating: Math.max(0, Math.trunc(Number(((i = n == null ? void 0 : n.system) == null ? void 0 : i.rating) ?? 0) || 0))
    };
  }).filter((n) => n.rating > 0).sort((n, i) => i.rating - n.rating || n.name.localeCompare(i.name));
  return [
    { itemId: "", name: "No medical equipment", rating: 0 },
    ...e
  ];
}
function Y0(t = "") {
  const e = String(t ?? "").trim();
  return Hc.find((n) => n.key === e) ?? Hc.find((n) => n.key === "normal") ?? Hc[0];
}
function Q0(t = null, e = "") {
  var s, o, l, c, u;
  const n = String(e ?? "").trim();
  if (!n) return null;
  const i = ((o = (s = t == null ? void 0 : t.items) == null ? void 0 : s.get) == null ? void 0 : o.call(s, n)) ?? null;
  if (!i || !["gear", "consumable"].includes(String((i == null ? void 0 : i.type) ?? (i == null ? void 0 : i.canonicalType) ?? "").trim()) || String(((l = i == null ? void 0 : i.system) == null ? void 0 : l.category) ?? "").trim() !== "medical") return null;
  const a = Math.max(0, Number(((c = i == null ? void 0 : i.system) == null ? void 0 : c.quantity) ?? 1) || 0), r = Math.max(0, Math.trunc(Number(((u = i == null ? void 0 : i.system) == null ? void 0 : u.rating) ?? 0) || 0));
  return a <= 0 || r <= 0 ? null : {
    itemId: i.id,
    name: i.name || "Medical Equipment",
    rating: r
  };
}
function X0(t = "") {
  return t === M.monitors.fatigue ? "Fatigue" : t === M.monitors.physical ? "Physical" : "Track";
}
function Z0(t = null, e = {}) {
  var i;
  if (!t || !(e != null && e.actorUuid)) return !1;
  if (t.uuid === e.actorUuid) return !0;
  const n = (i = globalThis.fromUuidSync) == null ? void 0 : i.call(globalThis, e.actorUuid);
  return !!(n && t.id && n.id === t.id);
}
function eM({ track: t = "", netHits: e = 0 } = {}) {
  const n = Math.max(0, Math.trunc(Number(e ?? 0) || 0));
  return t === M.monitors.fatigue ? n : t === M.monitors.physical ? Math.max(0, Math.floor((n - 1) / 2)) : 0;
}
function tM(t = {}) {
  var n;
  if (!Wl(t)) return null;
  const e = Y0((n = t == null ? void 0 : t.firstAid) == null ? void 0 : n.conditionKey);
  return {
    dn: e.dn,
    dnPart: {
      id: "firstAid.conditions",
      label: e.label,
      value: e.dn,
      tags: ["firstAid", "conditions"]
    },
    title: "First Aid (MedTech)",
    condition: e
  };
}
function nz({ actor: t = null, rollActor: e = null, payload: n = {} } = {}) {
  if (!Wl(n)) return [];
  const i = n.firstAid ?? {}, a = Q0(e ?? t, i.equipmentItemId), r = [];
  return a && r.push({
    id: "firstAid.equipment",
    label: `Medical Equipment (${a.name})`,
    value: a.rating,
    source: "First Aid",
    domain: "mental"
  }), Z0(e ?? t, { actorUuid: i.targetActorUuid }) && r.push({
    id: "firstAid.selfTreatment",
    label: "Self Treatment",
    value: -2,
    source: "First Aid",
    domain: "mental"
  }), r;
}
function nM(t = {}) {
  var a, r, s, o, l, c, u;
  const e = Number((a = t == null ? void 0 : t.outcomeModel) == null ? void 0 : a.margin);
  if (Number.isFinite(e)) return Math.max(0, Math.trunc(e));
  const n = Number(((r = t == null ? void 0 : t.outcome) == null ? void 0 : r.hits) ?? 0) || 0, i = Number(((s = t == null ? void 0 : t.dn) == null ? void 0 : s.total) ?? ((l = (o = t == null ? void 0 : t.ctxSnapshot) == null ? void 0 : o.dn) == null ? void 0 : l.total) ?? ((u = (c = t == null ? void 0 : t.ctxSnapshot) == null ? void 0 : c.difficulty) == null ? void 0 : u.dn) ?? 1) || 1;
  return Math.max(0, Math.trunc(n - i));
}
function Wl(t = {}) {
  var e;
  return String(((e = t == null ? void 0 : t.firstAid) == null ? void 0 : e.actionId) ?? "").trim() === kh;
}
function iz({ actor: t = null, selection: e = {} } = {}) {
  var a, r, s, o;
  const n = Y0(e.conditionKey), i = e.track === M.monitors.physical ? M.monitors.physical : M.monitors.fatigue;
  return {
    intent: "skill",
    key: "medTech",
    tags: ["combat", "firstAid"],
    firstAid: {
      actionId: kh,
      targetActorUuid: ((a = e.target) == null ? void 0 : a.actorUuid) ?? "",
      targetTokenUuid: ((r = e.target) == null ? void 0 : r.tokenUuid) ?? "",
      targetName: ((s = e.target) == null ? void 0 : s.actorName) ?? ((o = e.target) == null ? void 0 : o.label) ?? "Target",
      track: i,
      trackLabel: X0(i),
      conditionKey: n.key,
      equipmentItemId: e.equipmentItemId ?? ""
    }
  };
}
async function az(t = null) {
  var o;
  const e = ez(t);
  if (!e.length)
    return (o = ui.notifications) == null || o.warn("First Aid needs a character or NPC target."), null;
  const n = tz(t), i = e.map((l, c) => `
    <option value="${c}">${wm(l.label)}</option>
  `).join(""), a = n.map((l, c) => `
    <option value="${c}">${wm(l.name)}${l.rating ? ` (+${l.rating})` : ""}</option>
  `).join(""), r = Hc.map((l) => `
    <option value="${l.key}" ${l.key === "normal" ? "selected" : ""}>${wm(l.label)} (DN ${l.dn})</option>
  `).join(""), s = `
    <form class="mwd-quick-select">
      <div class="mwd-field">
        <label>Target</label>
        <select name="targetIndex">${i}</select>
      </div>
      <div class="mwd-field">
        <label>Recovery</label>
        <select name="track">
          <option value="${M.monitors.fatigue}">Fatigue</option>
          <option value="${M.monitors.physical}">Physical</option>
        </select>
      </div>
      <div class="mwd-field">
        <label>Conditions</label>
        <select name="conditionKey">${r}</select>
      </div>
      <div class="mwd-field">
        <label>Medical Equipment</label>
        <select name="equipmentIndex">${a}</select>
      </div>
    </form>
  `;
  return Dialog.confirm({
    title: "First Aid",
    content: s,
    yes: (l) => {
      const c = Math.max(0, Number(l.find("[name='targetIndex']").val() ?? 0) || 0), u = Math.max(0, Number(l.find("[name='equipmentIndex']").val() ?? 0) || 0), d = n[u] ?? n[0];
      return {
        target: e[c] ?? e[0],
        track: String(l.find("[name='track']").val() ?? M.monitors.fatigue),
        conditionKey: String(l.find("[name='conditionKey']").val() ?? "normal"),
        equipmentItemId: d.itemId
      };
    },
    no: () => null,
    defaultYes: !0
  });
}
async function rz(t = null, { token: e = null, event: n = null } = {}) {
  var o, l, c;
  if (!t) return { ok: !1, reason: "First Aid requires an acting character." };
  const i = await az(t);
  if (!i) return { ok: !0, cancelled: !0 };
  const a = iz({ actor: t, selection: i }), r = await ((c = (l = (o = game.mwd) == null ? void 0 : o.roll) == null ? void 0 : l.execute) == null ? void 0 : c.call(l, { actor: t, payload: a, event: n }));
  if (!r) return { ok: !0, cancelled: !0 };
  const s = await D.spendResource(t, {
    token: e,
    resource: "sa",
    cost: 2,
    actionId: kh,
    actionLabel: "First Aid",
    actionCostLabel: "2 SA",
    actionCategory: "complex"
  });
  return {
    ok: !!(s != null && s.ok),
    message: r,
    spend: s,
    reason: (s == null ? void 0 : s.reason) ?? ""
  };
}
async function sz(t = {}) {
  var u, d, f, m, g, p, h, b;
  const e = ((u = t == null ? void 0 : t.originPayload) == null ? void 0 : u.firstAid) ?? {};
  if (!Wl((t == null ? void 0 : t.originPayload) ?? {}))
    return { ok: !1, reason: "This roll is not a First Aid test." };
  if ((d = t == null ? void 0 : t.firstAidResult) != null && d.applied)
    return { ok: !1, reason: "First Aid has already been applied." };
  const n = e.targetActorUuid ? await fromUuid(e.targetActorUuid) : null;
  if (!n || !jf(n))
    return { ok: !1, reason: "First Aid target could not be found." };
  const i = e.track === M.monitors.physical ? M.monitors.physical : M.monitors.fatigue, a = nM(t), r = eM({ track: i, netHits: a }), s = Math.max(0, Number(((g = (m = (f = n.system) == null ? void 0 : f.monitors) == null ? void 0 : m[i]) == null ? void 0 : g.value) ?? 0) || 0), o = r > 0 ? await Mn.apply({
    actor: n,
    payload: {
      mode: "trackDelta",
      track: i,
      delta: -r,
      source: "First Aid"
    },
    options: { logToChat: !1 }
  }) : null;
  if (o && !o.ok)
    return { ok: !1, reason: o.reason ?? "Unable to apply First Aid recovery." };
  const l = Math.max(0, Number(((b = (h = (p = n.system) == null ? void 0 : p.monitors) == null ? void 0 : h[i]) == null ? void 0 : b.value) ?? s) || 0), c = Math.max(0, s - l);
  return {
    ok: !0,
    applied: !0,
    targetActorUuid: n.uuid,
    targetName: n.name || e.targetName || "Target",
    track: i,
    trackLabel: X0(i),
    netHits: a,
    requestedRecovery: r,
    recovered: c,
    before: s,
    after: l,
    harmResult: o
  };
}
function oz(t = {}) {
  var o, l, c, u, d, f, m;
  const e = ((o = t == null ? void 0 : t.originPayload) == null ? void 0 : o.firstAid) ?? {};
  if (!Wl((t == null ? void 0 : t.originPayload) ?? {})) return null;
  const n = tM((t == null ? void 0 : t.originPayload) ?? {}) ?? {}, i = Q0(
    ((l = globalThis.fromUuidSync) == null ? void 0 : l.call(globalThis, (t == null ? void 0 : t.rollActorUuid) ?? (t == null ? void 0 : t.actorUuid))) ?? null,
    e.equipmentItemId
  ), a = ((c = globalThis.fromUuidSync) == null ? void 0 : c.call(globalThis, (t == null ? void 0 : t.rollActorUuid) ?? (t == null ? void 0 : t.actorUuid))) ?? null, r = nM(t), s = eM({
    track: e.track,
    netHits: r
  });
  return {
    ...e,
    conditionLabel: ((u = n.condition) == null ? void 0 : u.label) ?? "Conditions",
    conditionDn: ((d = n.condition) == null ? void 0 : d.dn) ?? Number(((f = t == null ? void 0 : t.dn) == null ? void 0 : f.total) ?? 0),
    equipmentName: (i == null ? void 0 : i.name) ?? "",
    equipmentRating: (i == null ? void 0 : i.rating) ?? 0,
    selfTreatment: Z0(a, { actorUuid: e.targetActorUuid }),
    netHits: r,
    requestedRecovery: s,
    result: (t == null ? void 0 : t.firstAidResult) ?? null,
    applied: !!((m = t == null ? void 0 : t.firstAidResult) != null && m.applied)
  };
}
function lz(t, e) {
  var s, o, l;
  let n = t ?? {};
  const i = oz(n);
  i != null && i.applied && ((s = n.edge) != null && s.availableActions) && (n = foundry.utils.deepClone(n), n.edge.availableActions.canSpendPost = !1, n.edge.availableActions.canPostRerollFailures = !1);
  const a = Array.isArray((o = n == null ? void 0 : n.modifiers) == null ? void 0 : o.applied) ? n.modifiers.applied : [], r = Number(((l = n == null ? void 0 : n.modifiers) == null ? void 0 : l.total) ?? 0);
  if (a.length) {
    const c = Array.isArray(n == null ? void 0 : n.breakdownRows) ? n.breakdownRows.find((u) => u.id === "mods.total") : null;
    e.metaRows.push({
      text: `Mods: ${a.map((u) => `${u.label} ${Qy(u.value)}`).join(", ")} (Total ${Qy(r)})`,
      title: (c == null ? void 0 : c.tooltip) ?? ""
    });
  }
  ql(n, e, { separator: " - " }), Wl((n == null ? void 0 : n.originPayload) ?? {}) && cz(n, e, i);
}
function cz(t, e, n) {
  if (!n) return;
  const i = n.targetName || "Target", a = n.trackLabel || "Track", r = n.conditionLabel || "Conditions", s = n.equipmentRating > 0 ? ` | Equipment +${Number(n.equipmentRating ?? 0)}${n.equipmentName ? ` (${n.equipmentName})` : ""}` : "", o = n.selfTreatment ? " | Self -2" : "";
  if (e.metaRows.push({
    text: `First Aid: ${i} | ${a} | ${r}${s}${o}`,
    title: ""
  }), n.applied) {
    const l = n.result ?? {};
    e.footerRows.push({
      text: `First Aid applied: recovered ${Number(l.recovered ?? 0)} ${a} (${Number(l.before ?? 0)} -> ${Number(l.after ?? 0)})`,
      title: ""
    });
    return;
  }
  e.footerRows.push({
    text: `First Aid recovery: ${Number(n.netHits ?? 0)} net hit${Number(n.netHits ?? 0) === 1 ? "" : "s"} -> ${Number(n.requestedRecovery ?? 0)} ${a}`,
    title: ""
  }), e.actions.push({
    action: "applyFirstAid",
    label: "Apply First Aid",
    cssClass: "mwd-apply-first-aid",
    disabled: Number(n.requestedRecovery ?? 0) <= 0,
    title: Number(n.requestedRecovery ?? 0) <= 0 ? "No recovery from this result." : ""
  });
}
function Qy(t) {
  const e = Number(t ?? 0);
  return e >= 0 ? `+${e}` : `${e}`;
}
function uz(t, e) {
  var b, y, S, A, T, C, k, w, v, N, P, L, _, O, x, H, G, q, ie, Z, Se, se, te, pe, K, be, Ie, Oe, Be, R, V, B, me, oe, Ee, Ye, bt, yt, nn, Dn, On, Ln, $n, _n, cn, Bn, xn;
  const n = t ?? {}, i = (n == null ? void 0 : n.attackResult) ?? null;
  if (!i) return;
  const a = ((y = (b = n == null ? void 0 : n.attack) == null ? void 0 : b.weapon) == null ? void 0 : y.attackSummary) ?? null, r = ((A = (S = n == null ? void 0 : n.attack) == null ? void 0 : S.weapon) == null ? void 0 : A.machineWeaponGroup) ?? null, s = Array.isArray(i == null ? void 0 : i.results) ? i.results : [], o = (i == null ? void 0 : i.summary) ?? mz(s), l = s.some((U) => {
    var Y;
    return !!((Y = U == null ? void 0 : U.queuedMutation) != null && Y.applied);
  }), c = s.filter(
    (U) => (U == null ? void 0 : U.queuedMutation) && !U.queuedMutation.applied
  ), u = !!((C = (T = n == null ? void 0 : n.attack) == null ? void 0 : T.capabilityReport) != null && C.isTemplated), d = ((k = n == null ? void 0 : n.attack) == null ? void 0 : k.ewContext) ?? null;
  if (d != null && d.detectionState && d.detectionState !== "contact") {
    const U = [`EW: ${d.detectionStateLabel ?? d.detectionState}`];
    d.targetingDataValue && U.push(`+${d.targetingDataValue} targeting`), e.metaRows.push({ text: U.join(" | "), title: "" });
  }
  const f = Math.max(0, Number(((v = (w = n == null ? void 0 : n.attack) == null ? void 0 : w.weapon) == null ? void 0 : v.clusteringDice) ?? 0) || 0), m = Number(((P = (N = n == null ? void 0 : n.attack) == null ? void 0 : N.weapon) == null ? void 0 : P.clusteringTargetNumber) ?? 5) || 5;
  if (f > 0) {
    const U = Array.isArray((O = (_ = (L = n == null ? void 0 : n.attack) == null ? void 0 : L.weapon) == null ? void 0 : _.clusteringModifiers) == null ? void 0 : O.sourceNames) ? n.attack.weapon.clusteringModifiers.sourceNames.filter(Boolean) : [];
    e.metaRows.push({
      text: `Clustering: ${f}d6 @ ${m}+${U.length ? ` | Control: ${U.join(", ")}` : ""}`,
      title: ""
    });
  }
  if (r != null && r.id) {
    const U = Array.isArray(r == null ? void 0 : r.weaponNames) ? r.weaponNames.filter(Boolean) : [];
    e.metaRows.push({
      text: `Group: ${((H = (x = n == null ? void 0 : n.attack) == null ? void 0 : x.weapon) == null ? void 0 : H.name) ?? "Weapon Group"}${U.length ? ` | Members: ${U.join(", ")}` : ""}`,
      title: ""
    }), a && (e.metaRows.push({
      text: `Profile: ${a.damage ?? 0} damage${Number(a.clusteringDice ?? 0) ? ` | ${Number(a.clusteringDice ?? 0)}d6 cluster @ ${m}+` : ""} | AP ${a.ap ?? 0} | Heat ${a.heat ?? 0} | ${a.damageTypeLabel ?? a.damageType ?? "Damage"} | Range Cap ${ce(a.rangeCap ?? "")}`,
      title: ""
    }), e.footerRows.push({
      text: `Attack Ratings: ${fz(a.attackRatings ?? {})}`,
      title: ""
    }));
  }
  const g = Array.isArray((G = n == null ? void 0 : n.modifiers) == null ? void 0 : G.applied) ? n.modifiers.applied : [], p = Number(((q = n == null ? void 0 : n.modifiers) == null ? void 0 : q.total) ?? 0);
  if (g.length) {
    const U = Array.isArray(n == null ? void 0 : n.breakdownRows) ? n.breakdownRows.find((Y) => Y.id === "mods.total") : null;
    e.metaRows.push({
      text: `Mods: ${g.map((Y) => `${Y.label} ${ps(Y.value)}`).join(", ")} (Total ${ps(p)})`,
      title: (U == null ? void 0 : U.tooltip) ?? ""
    });
  }
  ql(n, e, { canPost: !l });
  const h = String((o == null ? void 0 : o.overallOutcome) ?? "").trim();
  if (e.outcomeText = s.length > 1 ? `ATTACK ${o.hits} HIT / ${o.grazes} GRAZE / ${o.misses} MISS` : h === "hit" ? "HIT!" : h === "graze" ? "GRAZE!" : "MISS!", e.metaRows.push({
    text: `Targets: ${s.length || 0}`,
    title: ""
  }), u && (e.targetRows = s.map((U, Y) => {
    var We, ke, Qe, zn, ct, It, Mi, Fn;
    const Ae = ((We = n == null ? void 0 : n.areaEffectPreviewState) == null ? void 0 : We[U == null ? void 0 : U.previewKey]) ?? {}, fe = ((ke = U == null ? void 0 : U.damage) == null ? void 0 : ke.exposure) ?? (U == null ? void 0 : U.exposure) ?? null, we = String((fe == null ? void 0 : fe.initialLabel) ?? "NONE").trim() || "NONE", Pe = String((fe == null ? void 0 : fe.finalLabel) ?? we).trim() || we, Le = Number(((Qe = U == null ? void 0 : U.damage) == null ? void 0 : Qe.incoming) ?? 0), lt = Number(((zn = U == null ? void 0 : U.damage) == null ? void 0 : zn.scaledIncoming) ?? Le), qe = (U == null ? void 0 : U.queuedMutation) ?? null, un = !!(qe != null && qe.applied || (ct = U == null ? void 0 : U.damageResult) != null && ct.applied), et = (Ae == null ? void 0 : Ae.reactionPreview) ?? null, an = [];
    if (!un && we !== "NONE" && ((It = U == null ? void 0 : U.damageResult) != null && It.ok) && !((Mi = U == null ? void 0 : U.damageResult) != null && Mi.skipped)) {
      const Et = !!(et != null && et.disabled);
      an.push({
        action: "toggleEvade",
        label: Et ? (et == null ? void 0 : et.reason) || "Reaction Disabled" : U != null && U.evadeActive ? "Clear Evade" : "Use Reaction",
        dataset: { "preview-key": U.previewKey },
        cssClass: `mwd-target-row__action ${U != null && U.evadeActive ? "is-active" : ""}`,
        disabled: Et
      });
    }
    if (U != null && U.evadeActive && (et != null && et.canSpendEdge) && Array.isArray(et.edgePools))
      for (const Et of et.edgePools)
        an.push({
          action: "toggleEvadeEdge",
          label: (Ae == null ? void 0 : Ae.edgePoolKey) === Et.key ? `Edge: ${Et.key}` : `Use ${Et.key}`,
          dataset: {
            "preview-key": U.previewKey,
            "pool-key": Et.key
          },
          cssClass: `mwd-target-row__action ${(Ae == null ? void 0 : Ae.edgePoolKey) === Et.key ? "is-active" : ""}`
        });
    return qe && !un && an.push({
      action: "applyAttackDamage",
      label: "Apply Damage",
      dataset: { "result-index": String(Y) },
      cssClass: "mwd-target-row__action mwd-apply-attack-damage"
    }), {
      targetName: ((Fn = U == null ? void 0 : U.target) == null ? void 0 : Fn.name) ?? "Target",
      applied: un,
      outcomeLabel: String((U == null ? void 0 : U.outcome) ?? "miss").toUpperCase(),
      exposureLabel: we === Pe ? we : `${we} -> ${Pe}`,
      damageLabel: Le === lt ? String(lt) : `${Le} -> ${lt}`,
      reactionHint: U != null && U.evadeActive ? Ae != null && Ae.edgePoolKey ? "Evade active. Reaction Burn canceled by Edge." : (et == null ? void 0 : et.burnDelta) > 0 ? `Evade active. This reaction adds +${et.burnDelta} Burn.` : "Evade active." : "",
      rowActions: an
    };
  })), s.length > 1 && c.length > 1 && e.actions.push({
    action: "applyAllAttackDamage",
    label: `Apply All Damage (${c.length})`,
    cssClass: "mwd-apply-all-attack-damage"
  }), !u)
    for (const U of s) {
      const Y = Number(((Z = (ie = U == null ? void 0 : U.cq) == null ? void 0 : ie.ar) == null ? void 0 : Z.total) ?? 0), Ae = Number(((se = (Se = U == null ? void 0 : U.cq) == null ? void 0 : Se.dr) == null ? void 0 : se.total) ?? 0);
      e.metaRows.push({
        text: `${((te = U == null ? void 0 : U.target) == null ? void 0 : te.name) ?? "Target"}: ${String((U == null ? void 0 : U.outcome) ?? "miss").toUpperCase()} | CQ ${ps(((pe = U == null ? void 0 : U.cq) == null ? void 0 : pe.value) ?? 0)} (AR ${Y} - DR ${Ae}) | Net ${Number((U == null ? void 0 : U.netHits) ?? 0)}`,
        title: dz(U == null ? void 0 : U.cq)
      });
    }
  if (!u)
    for (const [U, Y] of s.entries()) {
      const Ae = (Y == null ? void 0 : Y.damage) ?? null;
      if (Ae && (Y == null ? void 0 : Y.outcome) !== "miss") {
        const we = Number(((K = Ae == null ? void 0 : Ae.clustering) == null ? void 0 : K.damageBonus) ?? ((be = Ae == null ? void 0 : Ae.clustering) == null ? void 0 : be.hits) ?? 0), Pe = Number((Ae == null ? void 0 : Ae.netDamageBonus) ?? 0);
        e.footerRows.push({
          text: `${((Ie = Y == null ? void 0 : Y.target) == null ? void 0 : Ie.name) ?? "Target"}: ${Ae.damageTypeLabel} ${ps(Ae.effectiveWeaponDamage)} weapon${we ? ` + ${we} cluster` : ""}${Pe ? ` + ${Pe} net` : ""}`,
          title: ""
        }), Number(((Oe = Ae == null ? void 0 : Ae.clustering) == null ? void 0 : Oe.dice) ?? 0) > 0 && e.footerRows.push({
          text: `${((Be = Y == null ? void 0 : Y.target) == null ? void 0 : Be.name) ?? "Target"}: Cluster ${Number(Ae.clustering.dice ?? 0)}d6 @ ${Number(Ae.clustering.targetNumber ?? 5) || 5}+ -> ${Number(Ae.clustering.hits ?? 0)} hit${Number(Ae.clustering.hits ?? 0) === 1 ? "" : "s"}`,
          title: ""
        });
      }
      const fe = (Y == null ? void 0 : Y.damageResult) ?? null;
      if (fe != null && fe.ok && !(fe != null && fe.skipped)) {
        const we = (Y == null ? void 0 : Y.queuedMutation) ?? (fe == null ? void 0 : fe.queuedMutation) ?? null, Pe = !!(we != null && we.applied || fe != null && fe.applied);
        if (fe.mode === "machineAttackDamage") {
          const Le = fe.machine ?? {}, lt = fe.hitLocation ?? {}, qe = fe.degradation ?? null, un = fe.critical ?? {}, et = lt.impactLabel ?? lt.locationLabel ?? "Location", an = lt.rulesLocationLabel ?? lt.rulesLocation ?? "";
          e.footerRows.push({
            text: `${((R = Y == null ? void 0 : Y.target) == null ? void 0 : R.name) ?? "Target"}: Impact ${et}${an && an !== et ? ` | Rules ${an}` : ""}${lt.rollTotal ? ` (${lt.rollTotal})` : ""} | Armor ${Number(Le.armorBefore ?? 0)} -> ${Number(Le.armorAfter ?? 0)} | Structure ${Number(Le.structureBefore ?? 0)} -> ${Number(Le.structureAfter ?? 0)}`,
            title: ""
          }), qe != null && qe.summary && e.footerRows.push({
            text: `${((V = Y == null ? void 0 : Y.target) == null ? void 0 : V.name) ?? "Target"}: Shock ${Number(qe.summary.shockBefore ?? 0)} -> ${Number(qe.summary.shockAfter ?? 0)} | Threshold ${Number(qe.summary.threshold ?? 0)} | Reliability ${Number(qe.summary.reliability ?? 0)} | Reserve ${Number(qe.summary.reliabilitySpendableBefore ?? 0)} -> ${Number(qe.summary.reliabilitySpendableAfter ?? 0)}`,
            title: ""
          }), un.mode === "automatic" || (B = fe.critical) != null && B.automatic ? e.footerRows.push({
            text: `${((me = Y == null ? void 0 : Y.target) == null ? void 0 : me.name) ?? "Target"}: Automatic critical pending`,
            title: ""
          }) : un.mode === "chaosSelected" ? e.footerRows.push({
            text: `${((oe = Y == null ? void 0 : Y.target) == null ? void 0 : oe.name) ?? "Target"}: Chaos critical selected`,
            title: ""
          }) : (un.mode === "chaosOptional" || (Ee = fe.critical) != null && Ee.optional) && e.footerRows.push({
            text: `${((Ye = Y == null ? void 0 : Y.target) == null ? void 0 : Ye.name) ?? "Target"}: Chaos Edge can convert this location hit to a critical`,
            title: ""
          });
          for (const We of ((bt = fe.critical) == null ? void 0 : bt.records) ?? [])
            e.footerRows.push({
              text: `${((yt = Y == null ? void 0 : Y.target) == null ? void 0 : yt.name) ?? "Target"}: Critical - ${We.label}${We.locationLabel ? ` (${We.locationLabel})` : ""} | ${ew(We)}`,
              title: ""
            }), Pe && We.active !== !1 && We.remedyKey !== "none" && e.actions.push({
              action: "machineCritRemedy",
              label: `Remedy: ${We.label}`,
              dataset: {
                "machine-actor-uuid": ((nn = Y == null ? void 0 : Y.target) == null ? void 0 : nn.actorUuid) ?? "",
                "crit-id": We.id,
                "remedy-key": We.remedyKey,
                "gm-override": "true"
              },
              cssClass: "mwd-machine-crit-remedy"
            });
          if (we && !Pe && Array.isArray(qe == null ? void 0 : qe.spendOpportunities))
            for (const We of qe.spendOpportunities)
              We != null && We.canSpend && e.actions.push({
                action: "toggleMachineReliabilitySpend",
                label: We.selected ? `Clear Reliability Spend: ${We.location}` : `Spend Reliability: ${We.location}`,
                dataset: {
                  "result-index": String(U),
                  "spend-index": String(We.index)
                },
                cssClass: `mwd-toggle-machine-reliability ${We.selected ? "is-active" : ""}`
              });
        } else {
          const Le = fe.critical ?? {};
          if (Le.selected || Le.band) {
            const lt = Number(Le.severity ?? 0), qe = Le.selected ? `${Le.bandLabel ?? ce(Le.band ?? "")} ${Le.familyLabel ?? ""}`.trim() : "No Critical";
            e.footerRows.push({
              text: `${((Dn = Y == null ? void 0 : Y.target) == null ? void 0 : Dn.name) ?? "Target"}: Critical Threat (severity ${lt}) -> ${qe}`,
              title: ""
            });
          }
          for (const lt of Le.records ?? [])
            e.footerRows.push({
              text: `${((On = Y == null ? void 0 : Y.target) == null ? void 0 : On.name) ?? "Target"}: Critical - ${lt.label} | ${f$(lt)}`,
              title: ""
            });
        }
        we && !Pe && ((Ln = fe == null ? void 0 : fe.critical) == null ? void 0 : Ln.mode) === "chaosOptional" ? e.actions.push({
          action: "toggleMachineChaosCrit",
          label: `Spend Chaos Edge: ${fe.actorName ?? (($n = Y == null ? void 0 : Y.target) == null ? void 0 : $n.name) ?? "Target"}`,
          dataset: { "result-index": String(U) },
          cssClass: "mwd-toggle-machine-chaos"
        }) : we && !Pe && ((_n = fe == null ? void 0 : fe.critical) == null ? void 0 : _n.mode) === "chaosSelected" && e.actions.push({
          action: "toggleMachineChaosCrit",
          label: `Clear Chaos Critical: ${fe.actorName ?? ((cn = Y == null ? void 0 : Y.target) == null ? void 0 : cn.name) ?? "Target"}`,
          dataset: { "result-index": String(U) },
          cssClass: "mwd-toggle-machine-chaos is-active"
        }), we && !Pe && e.actions.push({
          action: "applyAttackDamage",
          label: `Apply Damage: ${fe.actorName ?? ((Bn = Y == null ? void 0 : Y.target) == null ? void 0 : Bn.name) ?? "Target"}`,
          dataset: { "result-index": String(U) },
          cssClass: "mwd-apply-attack-damage"
        });
      } else fe != null && fe.reason && e.footerRows.push({
        text: `${((xn = Y == null ? void 0 : Y.target) == null ? void 0 : xn.name) ?? "Target"}: ${fe.reason}`,
        title: ""
      });
    }
}
function dz(t = {}) {
  var i, a;
  const e = Array.isArray((i = t == null ? void 0 : t.ar) == null ? void 0 : i.parts) ? t.ar.parts : [], n = Array.isArray((a = t == null ? void 0 : t.dr) == null ? void 0 : a.parts) ? t.dr.parts : [];
  return [
    ...e.map((r) => `AR - ${r.label}: ${ps(r.value)}`),
    ...n.map((r) => `DR - ${r.label}: ${ps(r.value)}`)
  ].join(`
`);
}
function mz(t = []) {
  const e = { hits: 0, grazes: 0, misses: 0 };
  for (const n of t)
    (n == null ? void 0 : n.outcome) === "hit" ? e.hits += 1 : (n == null ? void 0 : n.outcome) === "graze" ? e.grazes += 1 : e.misses += 1;
  return {
    ...e,
    overallOutcome: e.hits > 0 ? "hit" : e.grazes > 0 ? "graze" : "miss"
  };
}
function ps(t) {
  const e = Number(t ?? 0);
  return e >= 0 ? `+${e}` : `${e}`;
}
function fz(t = {}) {
  return ["close", "near", "far", "extreme"].map((e) => `${ce(e)} ${Number((t == null ? void 0 : t[e]) ?? 0) || 0}`).join(" | ");
}
function gz(t, e) {
  var c;
  const n = t ?? {}, i = (n == null ? void 0 : n.outcomeModel) ?? {}, a = (i == null ? void 0 : i.net) ?? null;
  if (!a) return;
  e.net = a;
  const r = Number((a == null ? void 0 : a.converted) ?? 0), s = Number((a == null ? void 0 : a.value) ?? 0), o = Number((a == null ? void 0 : a.rate) ?? 4);
  e.metaRows.push({
    text: `Net: ${s} • Converted: ${r} • Rate: ${o}`,
    title: ""
  });
  const l = ((c = i == null ? void 0 : i.edgeEarned) == null ? void 0 : c.amount) > 0 ? i.edgeEarned : null;
  if (l) {
    const u = l.targetLabel ?? l.pool ?? (Array.isArray(l.pools) ? l.pools.join(", ") : "");
    e.footerRows.push({
      text: `Edge Earned: +${l.amount}${u ? ` (${u})` : ""}`,
      title: l.reason ?? ""
    });
  }
}
function pz(t, e) {
  var l, c, u, d;
  const n = t ?? {}, i = (n == null ? void 0 : n.outcomeModel) ?? {}, a = Number(((l = i == null ? void 0 : i.attacker) == null ? void 0 : l.successes) ?? (i == null ? void 0 : i.attackerHits) ?? NaN), r = Number(((c = i == null ? void 0 : i.defender) == null ? void 0 : c.successes) ?? (i == null ? void 0 : i.defenderHits) ?? NaN), s = Number((i == null ? void 0 : i.netHits) ?? (i == null ? void 0 : i.net) ?? NaN);
  Number.isFinite(a) && Number.isFinite(r) && e.metaRows.push({ text: `Opposed: Att ${a} vs Def ${r} • Net ${Number.isFinite(s) ? s : a - r}` }), (u = n == null ? void 0 : n.incoming) != null && u.label && (e.incoming = { label: n.incoming.label, value: n.incoming.value ?? "" }, e.footerRows.push({ text: `Incoming: ${e.incoming.label} ${e.incoming.value}` }));
  const o = ((d = i == null ? void 0 : i.edgeEarned) == null ? void 0 : d.amount) > 0 ? i.edgeEarned : null;
  if (o) {
    const f = o.targetLabel ?? o.pool ?? (Array.isArray(o.pools) ? o.pools.join(", ") : "");
    e.footerRows.push({ text: `Edge Earned: +${o.amount}${f ? ` (${f})` : ""}` });
  }
}
function hz(t, e) {
  var c;
  const n = t ?? {}, i = (n == null ? void 0 : n.outcomeModel) ?? {}, a = (i == null ? void 0 : i.extended) ?? null;
  if (!a) return;
  e.extended = a;
  const r = Number((a == null ? void 0 : a.progress) ?? 0), s = Number((a == null ? void 0 : a.target) ?? 0), o = Number((a == null ? void 0 : a.remaining) ?? Math.max(0, s - r));
  e.metaRows.push({
    text: `Extended: ${r}/${s} (Remaining ${o})`,
    title: ""
  }), a != null && a.completed && e.footerRows.push({ text: `Completed in ${Number((a == null ? void 0 : a.rounds) ?? (a == null ? void 0 : a.attempts) ?? 0) || "?"} attempts.` });
  const l = ((c = i == null ? void 0 : i.edgeEarned) == null ? void 0 : c.amount) > 0 ? i.edgeEarned : null;
  if (l) {
    const u = l.targetLabel ?? l.pool ?? (Array.isArray(l.pools) ? l.pools.join(", ") : "");
    e.footerRows.push({ text: `Edge Earned: +${l.amount}${u ? ` (${u})` : ""}` });
  }
}
function bz(t, e) {
  const n = (t == null ? void 0 : t.machineRemedy) ?? null, i = (t == null ? void 0 : t.machineRemedyResult) ?? null;
  if (n && (e.metaRows.push({
    text: `Problem: ${n.critLabel}${n.locationLabel ? ` | ${n.locationLabel}` : ""}`,
    title: ""
  }), e.metaRows.push({
    text: `Pool: Reliability + ${n.skillLabel}`,
    title: ""
  }), e.footerRows.push({
    text: `DN ${Number(n.baseDn ?? 0)} base + ${Number(n.conditionModifier ?? 0)} condition (${n.conditionLabel})`,
    title: ""
  }), ql(t, e, {
    canPost: !(i != null && i.ok && (i != null && i.passed) && (i != null && i.applied))
  }), !!i)) {
    if (i.ok && i.passed && i.applied) {
      e.outcomeText = "REMEDY SUCCESS!", e.footerRows.push({
        text: `${n.remedyLabel} cleared ${n.critLabel}.`,
        title: ""
      });
      return;
    }
    if (i.ok && !i.passed) {
      e.outcomeText = "REMEDY FAILED", e.footerRows.push({
        text: `${n.critLabel} remains active.`,
        title: ""
      });
      return;
    }
    i.ok || e.footerRows.push({
      text: i.reason ?? "Machine remedy could not be applied.",
      title: ""
    });
  }
}
function yz(t, e) {
  const n = (t == null ? void 0 : t.heatDangerCheck) ?? null;
  if (!n) return;
  const i = (t == null ? void 0 : t.heatDangerResult) ?? null;
  if (e.metaRows.push({
    text: `Danger Level ${Number(n.dangerLevel ?? 0)} | Pool ${Number(n.pool ?? 0)}d6 vs DN ${Number(n.dn ?? 0)}`,
    title: ""
  }), n.kind === "shutdown" && (i == null ? void 0 : i.systemOpsRating) !== null && (i == null ? void 0 : i.systemOpsRating) !== void 0) {
    const a = i.operatorName ? `${i.operatorName} ` : "";
    e.metaRows.push({
      text: `${a}System Operations ${Number(i.systemOpsRating ?? 0)}`,
      title: ""
    });
  }
  if (n.kind === "explosion" && !n.volatile && e.metaRows.push({
    text: "No volatile components detected.",
    title: ""
  }), i) {
    Sz(n, i, e);
    return;
  }
  if (e.passed) {
    e.outcomeText = n.kind === "shutdown" ? "SHUTDOWN AVOIDED" : "EXPLOSION AVOIDED", e.footerRows.push({ text: n.successText, title: "" });
    return;
  }
  e.outcomeText = n.kind === "shutdown" ? "SHUTDOWN CHECK FAILED" : "EXPLOSION CHECK FAILED", e.footerRows.push({ text: n.failureText, title: "" });
}
function Sz(t, e, n) {
  if (t.kind === "shutdown") {
    Az(e, n);
    return;
  }
  kz(e, n);
}
function Az(t, e) {
  t.passed ? e.outcomeText = "SHUTDOWN AVOIDED" : t.overrideAvailable ? e.outcomeText = "SHUTDOWN OVERRIDE AVAILABLE" : e.outcomeText = "SHUTDOWN APPLIED", t.passed || e.metaRows.push({
    text: `Failure Margin ${Number(t.failureMargin ?? 0)}`,
    title: ""
  }), (t.statusApplied || t.statusAlreadyActive) && e.metaRows.push({
    text: "Shutdown status active.",
    title: ""
  }), t.effectText && e.footerRows.push({ text: t.effectText, title: "" }), t.nextStep && e.footerRows.push({ text: t.nextStep, title: "" });
}
function kz(t, e) {
  t.passed ? e.outcomeText = "EXPLOSION AVOIDED" : t.explosionTriggered ? e.outcomeText = "EXPLOSION TRIGGERED" : e.outcomeText = "EXPLOSION NOT APPLIED", t.passed || e.metaRows.push({
    text: `Failure Margin ${Number(t.failureMargin ?? 0)}`,
    title: ""
  }), t.effectText && e.footerRows.push({ text: t.effectText, title: "" }), t.nextStep && e.footerRows.push({ text: t.nextStep, title: "" });
}
function Tz(t, e) {
  var r;
  const n = (t == null ? void 0 : t.ewAcquireResult) ?? null, i = (t == null ? void 0 : t.acquire) ?? null;
  i != null && i.currentState && e.metaRows.push({
    text: `Target State: ${wn(i.currentState)}`,
    title: ""
  });
  const a = (Array.isArray((r = t == null ? void 0 : t.dn) == null ? void 0 : r.parts) ? t.dn.parts : []).filter((s) => Array.isArray(s == null ? void 0 : s.tags) && s.tags.includes("stealth"));
  a.length && e.metaRows.push({
    text: `Stealth: ${a.map((s) => `${s.label} ${s.displayValue ?? wz(s.value)}`).join(", ")}`,
    title: ""
  }), ql(t, e), n && (n.ok ? (e.outcomeText = `${wn(n.previousState).toUpperCase()} → ${wn(n.newState).toUpperCase()}`, e.footerRows.push({
    text: `Detection: ${wn(n.newState)}`,
    title: ""
  }), n.hitCeiling && e.footerRows.push({
    text: `Ceiling: ${wn(n.ceiling)} (ECM active)`,
    title: ""
  })) : (e.outcomeText = "ACQUIRE FAILED", e.footerRows.push({
    text: n.reason ?? "Acquire roll failed — state unchanged.",
    title: ""
  })));
}
function wz(t) {
  const e = Number(t ?? 0) || 0;
  return e > 0 ? `+${e}` : e < 0 ? String(e) : "0";
}
function Mz(t, e) {
  const n = (t == null ? void 0 : t.ewTargetingResult) ?? null, i = (t == null ? void 0 : t.targeting) ?? null;
  if (i != null && i.detectionStateLabel && e.metaRows.push({
    text: `State: ${i.detectionStateLabel} | Cap: ${Number(i.cap ?? 0)}`,
    title: ""
  }), ql(t, e), !!n)
    if (n.ok) {
      const a = n.rawHits !== n.packetValue;
      e.outcomeText = `+${n.packetValue} dice${a ? ` (capped from ${n.rawHits})` : ""}`, e.footerRows.push({
        text: `Targeting data stored: +${n.packetValue} dice`,
        title: ""
      }), n.hitCap && e.footerRows.push({
        text: `Packet capped at ${n.packetValue} (raw: ${n.rawHits})`,
        title: ""
      });
    } else
      e.outcomeText = "TARGETING FAILED", e.footerRows.push({
        text: n.reason ?? "Targeting roll failed — no data stored.",
        title: ""
      });
}
const vz = {
  skill: lz,
  attack: uz,
  net: gz,
  opposed: pz,
  extended: hz,
  machineRemedy: bz,
  heatDangerCheck: yz,
  acquire: Tz,
  targeting: Mz
  // defense: enhanceDefense,
  // edge: enhanceEdge,
};
async function ar({ resolved: t } = {}) {
  const e = t ?? {}, n = Cz(e), i = vz[n.intent];
  return typeof i == "function" && i(e, n), await foundry.applications.handlebars.renderTemplate(
    "mwd.v2.roll.mwd-roll-card",
    n
  );
}
function Cz(t) {
  var m, g, p, h, b, y, S, A, T, C, k, w, v, N, P, L, _, O;
  const e = t ?? {}, n = Number(((m = e == null ? void 0 : e.roll) == null ? void 0 : m.target) ?? 5), i = Number(((g = e == null ? void 0 : e.dn) == null ? void 0 : g.total) ?? ((h = (p = e == null ? void 0 : e.ctxSnapshot) == null ? void 0 : p.dn) == null ? void 0 : h.total) ?? ((y = (b = e == null ? void 0 : e.ctxSnapshot) == null ? void 0 : b.difficulty) == null ? void 0 : y.dn) ?? 0), a = Number(((S = e == null ? void 0 : e.roll) == null ? void 0 : S.pool) ?? 0), r = Number(((A = e == null ? void 0 : e.outcome) == null ? void 0 : A.hits) ?? 0), s = (e == null ? void 0 : e.outcomeModel) ?? {}, o = typeof s.passed == "boolean" ? s.passed : r >= i, l = Number.isFinite(Number(s.margin)) ? Number(s.margin) : r - i, c = s.tier ?? null, u = Array.isArray(e == null ? void 0 : e.breakdownRows) ? e.breakdownRows.map((x) => `${x.label}: ${x.value}`).join(`
`) : "", d = {
    header: { left: (e == null ? void 0 : e.title) ?? "Roll", right: (e == null ? void 0 : e.subtitle) ?? "" },
    formula: String((e == null ? void 0 : e.formula) ?? "").trim(),
    intent: (e == null ? void 0 : e.intent) ?? "unknown",
    domains: Array.isArray(e == null ? void 0 : e.domains) ? e.domains : [],
    tn: n,
    dn: i,
    pool: a,
    hits: r,
    passed: o,
    margin: l,
    tier: c,
    breakdownTooltip: u,
    diceGroups: Array.isArray((T = e == null ? void 0 : e.roll) == null ? void 0 : T.diceGroups) ? e.roll.diceGroups : [],
    metaRows: [],
    targetRows: [],
    actions: [],
    footerRows: [],
    incoming: null,
    edge: null,
    net: null,
    opposed: null,
    extended: null
  }, f = (e == null ? void 0 : e.attack) ?? null;
  if ((C = e == null ? void 0 : e.specialization) != null && C.label && d.metaRows.push({
    text: `Specialization: ${e.specialization.label} (+${Number(e.specialization.value ?? 0)})`,
    title: ""
  }), (k = f == null ? void 0 : f.weapon) != null && k.name) {
    const x = ((w = f == null ? void 0 : f.weapon) == null ? void 0 : w.type) === "personalWeapon" || (v = f == null ? void 0 : f.weapon) != null && v.isSynthetic ? dd((f == null ? void 0 : f.rangeBand) ?? "") : String((f == null ? void 0 : f.rangeBand) ?? "").trim(), H = String(((N = f == null ? void 0 : f.weapon) == null ? void 0 : N.damageTypeLabel) ?? ((P = f == null ? void 0 : f.weapon) == null ? void 0 : P.damageType) ?? "").trim(), G = String(((L = f == null ? void 0 : f.payload) == null ? void 0 : L.label) ?? ((_ = f == null ? void 0 : f.weapon) == null ? void 0 : _.payloadLabel) ?? "").trim();
    d.metaRows.push({
      text: `Weapon: ${f.weapon.name}${x ? ` • Range: ${x}` : ""}${H ? ` • Type: ${H}` : ""}${G ? ` • Payload: ${G}` : ""}`,
      title: ""
    }), (O = f == null ? void 0 : f.sourceState) != null && O.isTracked && d.footerRows.push({
      text: `Source: ${Number(f.sourceState.current ?? 0)}/${Number(f.sourceState.max ?? 0)}`,
      title: ""
    });
  }
  return d;
}
function Iz() {
  Hooks.on("renderChatMessageHTML", (t, e) => {
    e.addEventListener("click", (n) => {
      var r;
      const i = n.target.closest("[data-mwd-action]");
      if (!i) return;
      const a = String(i.dataset.mwdAction || "").trim();
      if (a) {
        if (a === "toggleDice") {
          (r = i.closest(".mwd-roll-card")) == null || r.classList.toggle("is-dice-open");
          return;
        }
        a === "edgePostReroll" && i1(n, t), a === "toggleEvade" && Wz(n, t), a === "toggleEvadeEdge" && Vz(n, t), a === "toggleHazardEvade" && Zz(n, t), a === "toggleHazardEvadeEdge" && e1(n, t), a === "applyHazardTick" && t1(n, t), a === "toggleMachineChaosCrit" && Yz(n, t), a === "toggleMachineReliabilitySpend" && Qz(n, t), a === "machineCritRemedy" && Xz(n), a === "applyAttackDamage" && Gz(n, t), a === "applyAllAttackDamage" && Jz(n, t), a === "applyFirstAid" && a1(n, t);
      }
    });
  });
}
function rr() {
  var t, e, n;
  return ((t = game.mwd) == null ? void 0 : t.harm) ?? ((n = (e = game.system) == null ? void 0 : e.mwd) == null ? void 0 : n.harm) ?? null;
}
function iM() {
  var t, e, n;
  return ((t = game.mwd) == null ? void 0 : t.roll) ?? ((n = (e = game.system) == null ? void 0 : e.mwd) == null ? void 0 : n.roll) ?? null;
}
function Ez() {
  var t, e, n;
  return ((t = game.mwd) == null ? void 0 : t.machineActions) ?? ((n = (e = game.system) == null ? void 0 : e.mwd) == null ? void 0 : n.machineActions) ?? null;
}
function aM(t = {}) {
  var e, n;
  return !!((n = (e = rr()) == null ? void 0 : e.isMachineDamageMutation) != null && n.call(e, t));
}
function Tu(t = {}) {
  var e, n;
  return ((n = (e = rr()) == null ? void 0 : e.getMutationTargetActorUuid) == null ? void 0 : n.call(e, t)) ?? null;
}
function wu(t = {}) {
  var e, n;
  return ((n = (e = rr()) == null ? void 0 : e.getMutationTargetTokenUuid) == null ? void 0 : n.call(e, t)) ?? null;
}
function Nz(t) {
  return t === M.monitors.physical ? "Physical" : t === M.monitors.fatigue ? "Fatigue" : String(t ?? "").trim() || "Track";
}
function Pz(t) {
  const e = String(t ?? "").trim().toLowerCase();
  return e === "penetrating" ? "is-penetrating" : e === "energy" ? "is-energy" : e === "thermal" ? "is-thermal" : e === "electrical" ? "is-electrical" : "is-concussive";
}
function Rz(t) {
  const e = Math.max(0, Number(t ?? 0) || 0);
  return e <= 0 ? { key: "is-none", label: "No Penetration" } : e <= 2 ? { key: "is-light", label: "Light Damage" } : e <= 4 ? { key: "is-medium", label: "Moderate Damage" } : e <= 7 ? { key: "is-heavy", label: "Heavy Damage" } : { key: "is-critical", label: "Critical Damage" };
}
function Dz(t = {}) {
  var a;
  const e = (t == null ? void 0 : t.machine) ?? ((a = t == null ? void 0 : t.damagePreview) == null ? void 0 : a.machine) ?? null;
  if (e) {
    const r = Math.max(0, Number(e.armorAbsorbed ?? 0) || 0), s = Math.max(0, Number(e.structureDamage ?? (t == null ? void 0 : t.finalDamage) ?? (t == null ? void 0 : t.appliedDelta) ?? 0) || 0);
    return {
      isMachine: !0,
      armor: r,
      structure: s,
      total: r + s
    };
  }
  const n = (t == null ? void 0 : t.battleArmor) ?? null;
  if (n) {
    const r = Math.max(0, Number(n.armorAbsorbed ?? 0) || 0), s = Math.max(0, Number(
      n.structureDamage ?? Number(n.structureBefore ?? 0) - Number(n.structureAfter ?? 0)
    ) || 0), o = Math.max(0, Number(n.wearerDamage ?? (t == null ? void 0 : t.finalDamage) ?? (t == null ? void 0 : t.appliedDelta) ?? 0) || 0);
    return {
      isMachine: !1,
      isBattleArmor: !0,
      armor: r,
      structure: s,
      wearer: o,
      total: r + s + o
    };
  }
  const i = Math.max(0, Number((t == null ? void 0 : t.finalDamage) ?? (t == null ? void 0 : t.appliedDelta) ?? 0) || 0);
  return {
    isMachine: !1,
    isBattleArmor: !1,
    armor: 0,
    structure: i,
    wearer: i,
    total: i
  };
}
function ms(t) {
  const e = Math.max(0, Number(t ?? 0) || 0);
  return e === 1 ? "1 point" : `${e} points`;
}
function Oz({ damageTypeLabel: t = "Damage", trackLabel: e = "Track", breakdown: n = {} } = {}) {
  if (Math.max(0, Number(n.total ?? 0) || 0) <= 0) return `${t} damage did not affect the target.`;
  if (n.isMachine) {
    const a = [];
    if (n.armor > 0 && a.push(`${ms(n.armor)} to armor`), n.structure > 0 && a.push(`${ms(n.structure)} to structure`), a.length) return `${t} damage applied: ${a.join(", ")}.`;
  }
  if (n.isBattleArmor) {
    const a = [];
    if (n.armor > 0 && a.push(`${ms(n.armor)} to battle armor`), n.structure > 0 && a.push(`${ms(n.structure)} to battle armor structure`), n.wearer > 0 && a.push(`${ms(n.wearer)} to ${e}`), a.length) return `${t} damage applied: ${a.join(", ")}.`;
  }
  return `${t} damage applied to ${e}.`;
}
function Ua(t, e) {
  return `${Math.max(0, Number(t ?? 0) || 0)}/${Math.max(0, Number(e ?? 0) || 0)}`;
}
function Lz(t = null) {
  return t ? [
    {
      label: "Armor",
      value: `${Ua(t.armorBefore, t.armorMax)} -> ${Ua(t.armorAfter, t.armorMax)}`,
      class: "is-monitor"
    },
    {
      label: "Structure",
      value: `${Ua(t.structureBefore, t.structureMax)} -> ${Ua(t.structureAfter, t.structureMax)}`,
      class: "is-monitor"
    }
  ] : [];
}
function $z(t = null) {
  if (!t) return [];
  const e = Math.max(
    0,
    Number(t.armorPoolMax ?? t.armorMax ?? t.armorBefore ?? t.armorAfter ?? 0) || 0
  ), n = Math.max(
    0,
    Number(t.structureMax ?? t.structureBefore ?? t.structureAfter ?? 0) || 0
  );
  return [
    {
      label: "Battle Armor",
      value: `${Ua(t.armorBefore, e)} -> ${Ua(t.armorAfter, e)}`,
      class: "is-monitor"
    },
    {
      label: "BA Structure",
      value: `${Ua(t.structureBefore, n)} -> ${Ua(t.structureAfter, n)}`,
      class: "is-monitor"
    }
  ];
}
function _z(t = null) {
  if (!t || typeof t != "object") return "";
  const e = String(t.impactLabel ?? t.locationLabel ?? "").trim(), n = String(t.rulesLocationLabel ?? "").trim();
  return e && n && e !== n ? `${e} (${n})` : e || n || String(t.locationKey ?? t.rulesLocation ?? "").trim();
}
function Xy(t) {
  const e = Number(t ?? 0) || 0;
  return e > 0 ? `+${e}` : String(e);
}
function Bz(t = {}, e = null) {
  var i;
  const n = Object.values(((i = t == null ? void 0 : t.degradation) == null ? void 0 : i.stressDelta) ?? {});
  return n.length ? n.reduce((a, r) => a + Number(r ?? 0), 0) : Number((e == null ? void 0 : e.locationStressGain) ?? 0) || 0;
}
function xz(t = {}) {
  var i, a, r, s, o;
  if (Number.isFinite(Number((i = t == null ? void 0 : t.degradation) == null ? void 0 : i.shockDelta))) return Number(t.degradation.shockDelta);
  const e = Number((r = (a = t == null ? void 0 : t.degradation) == null ? void 0 : a.summary) == null ? void 0 : r.shockBefore), n = Number((o = (s = t == null ? void 0 : t.degradation) == null ? void 0 : s.summary) == null ? void 0 : o.shockAfter);
  return Number.isFinite(e) && Number.isFinite(n) ? n - e : 0;
}
function zz(t = {}) {
  var s;
  const e = ((s = t == null ? void 0 : t.attackDamage) == null ? void 0 : s.clustering) ?? null, n = Math.max(0, Number((e == null ? void 0 : e.dice) ?? 0) || 0);
  if (!n) return "";
  const i = Number((e == null ? void 0 : e.targetNumber) ?? 5) || 5, a = Math.max(0, Number((e == null ? void 0 : e.hits) ?? (e == null ? void 0 : e.damageBonus) ?? 0) || 0), r = Array.isArray(e == null ? void 0 : e.results) && e.results.length ? ` (${e.results.map((o) => Number((o == null ? void 0 : o.result) ?? 0) || 0).join(", ")})` : "";
  return `${n}d6 @ ${i}+ -> ${a} hit${a === 1 ? "" : "s"}${r}`;
}
function Fz(t = {}) {
  var n, i;
  const e = Array.isArray((n = t == null ? void 0 : t.critical) == null ? void 0 : n.records) ? t.critical.records : [];
  return e.length ? e.map((a) => `${a.label}${a.locationLabel ? ` (${a.locationLabel})` : ""}: ${ew(a)}`).join(" ; ") : ((i = t == null ? void 0 : t.critical) == null ? void 0 : i.drawOk) === !1 ? String(t.critical.reason ?? "Critical effect was not resolved.").trim() : "None";
}
function Uz({ actor: t = null, token: e = null } = {}) {
  var r;
  const n = (e == null ? void 0 : e.document) ?? e ?? null, i = String(((r = n == null ? void 0 : n.texture) == null ? void 0 : r.src) ?? "").trim(), a = String((t == null ? void 0 : t.img) ?? "").trim();
  return i || a || "icons/svg/mystery-man.svg";
}
function Kz(t) {
  var n, i;
  const e = (i = (n = game.settings) == null ? void 0 : n.get) == null ? void 0 : i.call(n, "core", "messageMode");
  return typeof ChatMessage.applyMode == "function" && ChatMessage.applyMode(t, e), t;
}
function Hz(t = {}) {
  var n;
  return (t == null ? void 0 : t.mode) === "machineAttackDamage" || t != null && t.machine ? [] : (Array.isArray((n = t == null ? void 0 : t.critical) == null ? void 0 : n.records) ? t.critical.records : []).map((i) => {
    const a = String(
      (i == null ? void 0 : i.label) ?? `${(i == null ? void 0 : i.familyLabel) ?? ""} ${(i == null ? void 0 : i.bandRoman) ?? ""}`
    ).trim();
    if (!a) return null;
    const r = String((i == null ? void 0 : i.remedyKey) ?? "").trim(), s = String((i == null ? void 0 : i.remedyLabel) ?? "").trim(), o = Math.max(0, Number((i == null ? void 0 : i.remedyBaseDn) ?? 0) || 0);
    return {
      label: a,
      effectText: String((i == null ? void 0 : i.effectText) ?? "").trim(),
      remedy: s && r !== "none" ? `${s}${o ? ` (DN ${o})` : ""}` : ""
    };
  }).filter(Boolean);
}
function jz({ summary: t = {}, actor: e = null, token: n = null } = {}) {
  var h;
  const i = String((t == null ? void 0 : t.damageType) ?? "").trim(), a = In(i || "concussive") || "Damage", r = Nz(t == null ? void 0 : t.track), s = Dz(t), o = Rz(s.total), l = ms(s.total), c = String((t == null ? void 0 : t.actorName) ?? (e == null ? void 0 : e.name) ?? "Target").trim() || "Target", u = [], d = (t == null ? void 0 : t.machine) ?? ((h = t == null ? void 0 : t.damagePreview) == null ? void 0 : h.machine) ?? null, f = (t == null ? void 0 : t.battleArmor) ?? null, m = Lz(d), g = $z(f);
  if (m.length ? u.push(...m) : g.length ? (u.push(...g), t != null && t.beforeLabel && (t != null && t.afterLabel) && u.push({
    label: "Wearer",
    value: `${t.beforeLabel} -> ${t.afterLabel}`,
    class: "is-monitor"
  })) : t != null && t.beforeLabel && (t != null && t.afterLabel) && u.push({
    label: "Monitor",
    value: `${t.beforeLabel} -> ${t.afterLabel}`,
    class: "is-monitor"
  }), s.isMachine) {
    const b = _z(t == null ? void 0 : t.hitLocation);
    b && u.push({
      label: "Hit Location",
      value: b
    });
  }
  u.push({
    label: "Applied Damage",
    value: l
  });
  const p = zz(t);
  return p && u.push({
    label: "Cluster Roll",
    value: p
  }), s.isMachine && (u.push({
    label: "Shock",
    value: Xy(xz(t))
  }), u.push({
    label: "Location Stress",
    value: Xy(Bz(t, d))
  }), u.push({
    label: "Critical",
    value: Fz(t)
  })), Number.isFinite(Number(t == null ? void 0 : t.damageIncoming)) && u.push({
    label: "Incoming",
    value: String(Number(t.damageIncoming ?? 0))
  }), t != null && t.usedArmor && (t != null && t.mitigation) && (u.push({
    label: "Resistance",
    value: String(Number(t.mitigation.netResistance ?? 0))
  }), u.push({
    label: "AP",
    value: String(Number(t.effectiveAp ?? 0))
  }), u.push({
    label: "Armor",
    value: `${Number(t.mitigation.armorBefore ?? 0)} -> ${Number(t.mitigation.armorAfter ?? 0)}`
  }), Number(t.mitigation.reinforcedMax ?? 0) > 0 && u.push({
    label: "Reinforced",
    value: `${Number(t.mitigation.reinforcedBefore ?? 0)} -> ${Number(t.mitigation.reinforcedAfter ?? 0)}`
  })), t != null && t.source && u.push({
    label: "Source",
    value: String(t.source).trim()
  }), t != null && t.notes && u.push({
    label: "Notes",
    value: String(t.notes).trim()
  }), {
    classes: ["mwd-damage-card", Pz(i), o.key].join(" "),
    header: {
      left: "Damage Applied",
      right: s.isBattleArmor ? "Battle Armor" : r
    },
    target: {
      name: c,
      image: Uz({ actor: e, token: n })
    },
    damageTypeLabel: a,
    severityLabel: o.label,
    impactValue: s.total,
    impactText: Oz({ damageTypeLabel: a, trackLabel: r, breakdown: s }),
    criticals: Hz(t),
    rows: u
  };
}
async function Th({ summary: t = {}, actor: e = null, token: n = null } = {}) {
  const i = await foundry.applications.handlebars.renderTemplate(
    "mwd.v2.roll.mwd-damage-application-card",
    jz({ summary: t, actor: e, token: n })
  ), a = Kz({
    speaker: ChatMessage.getSpeaker({ actor: e, token: n }),
    content: i
  });
  return ChatMessage.create(a);
}
async function Gz(t, e) {
  var r, s, o, l, c, u, d;
  t.preventDefault();
  const n = t.target.closest("[data-mwd-action='applyAttackDamage']"), i = Number((r = n == null ? void 0 : n.dataset) == null ? void 0 : r.resultIndex);
  if (!Number.isInteger(i) || i < 0) return;
  const a = await ((o = (s = rr()) == null ? void 0 : s.applyQueuedAttackDamageFromMessage) == null ? void 0 : o.call(s, { message: e, resultIndex: i }));
  if (!a.ok) {
    (c = (l = ui.notifications) == null ? void 0 : l.warn) == null || c.call(l, a.userMessage ?? a.reason ?? "Unable to apply attack damage.");
    return;
  }
  if (a.skipped) {
    (d = (u = ui.notifications) == null ? void 0 : u.info) == null || d.call(u, a.userMessage ?? a.reason ?? "That attack damage has already been applied.");
    return;
  }
  await e.update(a.updateData), await Th({
    summary: a.summary,
    actor: a.targetActor,
    token: a.targetToken
  });
}
async function wh(t = {}) {
  var i, a;
  const e = (i = t == null ? void 0 : t.target) != null && i.actorUuid ? await fromUuid(t.target.actorUuid) : null, n = (a = t == null ? void 0 : t.target) != null && a.tokenUuid ? await fromUuid(t.target.tokenUuid) : null;
  return Id({
    actor: e,
    token: n,
    edgePoolKey: (t == null ? void 0 : t.evadeEdgePoolKey) ?? ""
  });
}
function qz(t = "") {
  var e, n;
  return ((n = (e = game.messages) == null ? void 0 : e.get) == null ? void 0 : n.call(e, String(t ?? "").trim())) ?? null;
}
async function Id({ actor: t = null, token: e = null, actorUuid: n = "", tokenUuid: i = "", edgePoolKey: a = "" } = {}) {
  const r = t ?? (n ? await fromUuid(n) : null), s = e ?? (i ? await fromUuid(i) : null);
  return r ? {
    ...D.getReactionSpendPreview(r, { token: s, edgePoolKey: a }) ?? {},
    actor: r,
    token: s
  } : null;
}
async function Mh(t, e) {
  var s, o;
  const n = foundry.utils.deepClone((o = (s = t == null ? void 0 : t.flags) == null ? void 0 : s.mwd) == null ? void 0 : o.resolved);
  if (!n) return;
  await e(n);
  const i = await fromUuid(n.actorUuid);
  if (!i) return;
  const a = iM();
  if (!(a != null && a.recomputeResolvedOutcomeAndAttack)) throw new Error("MWD roll recompute service not initialized.");
  await a.recomputeResolvedOutcomeAndAttack(n, i);
  const r = await ar({ resolved: n });
  return await t.update({
    content: r,
    "flags.mwd.resolved": n
  }), n;
}
async function vh(t, e, { active: n = !1, edgePoolKey: i = "" } = {}) {
  var o, l, c, u, d, f, m, g;
  const a = (o = e == null ? void 0 : e.target) != null && o.actorUuid ? await fromUuid(e.target.actorUuid) : null, r = (l = e == null ? void 0 : e.target) != null && l.tokenUuid ? await fromUuid(e.target.tokenUuid) : null;
  if (!a) return;
  const s = D.getReactionSpendPreview(a, { token: r, edgePoolKey: i });
  if (!(n && (s != null && s.disabled))) {
    if (!n) {
      const p = D.getSnapshot(a, { token: r }), h = (p == null ? void 0 : p.pendingReaction) ?? null;
      (h == null ? void 0 : h.sourceKind) === "attack" && (h == null ? void 0 : h.messageId) === t.id && (h == null ? void 0 : h.sourceId) === (e == null ? void 0 : e.previewKey) && await D.clearPendingReaction(a, { token: r });
      return;
    }
    await D.setPendingReaction(a, {
      token: r,
      pendingReaction: {
        type: "evade",
        sourceKind: "attack",
        sourceId: (e == null ? void 0 : e.previewKey) ?? null,
        messageId: t.id,
        resultIndex: (e == null ? void 0 : e.resultIndex) ?? null,
        exposureBefore: ((u = (c = e == null ? void 0 : e.damage) == null ? void 0 : c.exposure) == null ? void 0 : u.initialTier) ?? ((d = e == null ? void 0 : e.exposure) == null ? void 0 : d.initialTier) ?? "none",
        exposureAfterPreview: ((m = (f = e == null ? void 0 : e.damage) == null ? void 0 : f.exposure) == null ? void 0 : m.finalTier) ?? ((g = e == null ? void 0 : e.exposure) == null ? void 0 : g.initialTier) ?? "none",
        edgePoolKey: i,
        allowCurrentTurn: !1
      }
    });
  }
}
async function Wz(t, e) {
  var s, o;
  t.preventDefault();
  const n = t.target.closest("[data-mwd-action='toggleEvade']"), i = String(((s = n == null ? void 0 : n.dataset) == null ? void 0 : s.previewKey) ?? "").trim();
  if (!i) return;
  const a = await Mh(e, async (l) => {
    var m;
    if (l.areaEffectPreviewState ?? (l.areaEffectPreviewState = {}), !!(l.areaEffectPreviewState[i] ?? {}).evadeActive) {
      delete l.areaEffectPreviewState[i];
      return;
    }
    l.areaEffectPreviewState[i] = {
      evadeActive: !0,
      edgePoolKey: null
    };
    const d = (Array.isArray((m = l == null ? void 0 : l.attackResult) == null ? void 0 : m.results) ? l.attackResult.results : []).find((g) => (g == null ? void 0 : g.previewKey) === i) ?? null, f = d ? await wh({ ...d, evadeEdgePoolKey: null }) : null;
    f && (l.areaEffectPreviewState[i].reactionPreview = {
      burnDelta: Number(f.burnDelta ?? 0),
      canSpendEdge: !!f.canSpendEdge,
      disabled: !!f.disabled,
      reason: String(f.reason ?? ""),
      edgePools: (f.edgePools ?? []).map((g) => ({
        key: g.key,
        label: g.label,
        value: g.value
      }))
    });
  }), r = (Array.isArray((o = a == null ? void 0 : a.attackResult) == null ? void 0 : o.results) ? a.attackResult.results : []).find((l) => (l == null ? void 0 : l.previewKey) === i) ?? null;
  a && r && await vh(e, r, {
    active: !!(r != null && r.evadeActive),
    edgePoolKey: String((r == null ? void 0 : r.evadeEdgePoolKey) ?? "").trim()
  });
}
async function Vz(t, e) {
  var o, l, c;
  t.preventDefault();
  const n = t.target.closest("[data-mwd-action='toggleEvadeEdge']"), i = String(((o = n == null ? void 0 : n.dataset) == null ? void 0 : o.previewKey) ?? "").trim(), a = String(((l = n == null ? void 0 : n.dataset) == null ? void 0 : l.poolKey) ?? "").trim();
  if (!i) return;
  const r = await Mh(e, async (u) => {
    var p;
    u.areaEffectPreviewState ?? (u.areaEffectPreviewState = {});
    const d = u.areaEffectPreviewState[i] ?? {}, f = d.edgePoolKey === a ? null : a;
    u.areaEffectPreviewState[i] = {
      ...d,
      evadeActive: !0,
      edgePoolKey: f
    };
    const m = (Array.isArray((p = u == null ? void 0 : u.attackResult) == null ? void 0 : p.results) ? u.attackResult.results : []).find((h) => (h == null ? void 0 : h.previewKey) === i) ?? null, g = m ? await wh({ ...m, evadeEdgePoolKey: f }) : null;
    g && (u.areaEffectPreviewState[i].reactionPreview = {
      burnDelta: Number(g.burnDelta ?? 0),
      canSpendEdge: !!g.canSpendEdge,
      disabled: !!g.disabled,
      reason: String(g.reason ?? ""),
      edgePools: (g.edgePools ?? []).map((h) => ({
        key: h.key,
        label: h.label,
        value: h.value
      }))
    });
  }), s = (Array.isArray((c = r == null ? void 0 : r.attackResult) == null ? void 0 : c.results) ? r.attackResult.results : []).find((u) => (u == null ? void 0 : u.previewKey) === i) ?? null;
  r && s && await vh(e, s, {
    active: !!(s != null && s.evadeActive),
    edgePoolKey: String((s == null ? void 0 : s.evadeEdgePoolKey) ?? "").trim()
  });
}
async function Jz(t, e) {
  var i, a, r, s, o, l, c;
  t.preventDefault();
  const n = await ((a = (i = rr()) == null ? void 0 : i.applyAllQueuedAttackDamageFromMessage) == null ? void 0 : a.call(i, { message: e }));
  if (!(n != null && n.ok)) {
    const u = (n == null ? void 0 : n.reason) === "none-pending" ? "info" : "warn";
    (s = (r = ui.notifications) == null ? void 0 : r[u]) == null || s.call(r, (n == null ? void 0 : n.userMessage) ?? (n == null ? void 0 : n.reason) ?? "Unable to apply queued attack damage.");
    return;
  }
  await e.update(n.updateData);
  for (const u of n.appliedResults ?? [])
    await Th({
      summary: u.summary,
      actor: u.targetActor,
      token: u.targetToken
    });
  (o = n.failures) != null && o.length && ((c = (l = ui.notifications) == null ? void 0 : l.warn) == null || c.call(l, `Applied ${n.applied} queued damage result${n.applied === 1 ? "" : "s"}; ${n.failures.length} failed.`));
}
async function Yz(t, e) {
  var f, m, g, p, h;
  t.preventDefault();
  const n = t.target.closest("[data-mwd-action='toggleMachineChaosCrit']"), i = Number(((f = n == null ? void 0 : n.dataset) == null ? void 0 : f.resultIndex) ?? -1), a = foundry.utils.deepClone(e.getFlag("mwd", "resolved")), r = ((g = (m = a == null ? void 0 : a.attackResult) == null ? void 0 : m.results) == null ? void 0 : g[i]) ?? null, s = (r == null ? void 0 : r.queuedMutation) ?? null;
  if (!s || s.applied || !aM(s)) return;
  s.payload.chaosCriticalSelected = !s.payload.chaosCriticalSelected, s.payload.reliabilitySpendSelections = [];
  const o = Tu(s) ? await fromUuid(Tu(s)) : null, l = wu(s) ? await fromUuid(wu(s)) : null, c = await ((h = (p = rr()) == null ? void 0 : p.rebuildQueuedAttackDamagePreview) == null ? void 0 : h.call(p, { mutation: s, result: r, targetActor: o, targetToken: l }));
  if (!(c != null && c.ok)) return;
  const u = c.summary;
  r.queuedMutation = s, r.damageResult = u;
  const d = await ar({ resolved: a });
  await e.update({
    content: d,
    "flags.mwd.resolved": a
  });
}
async function Qz(t, e) {
  var g, p, h, b, y, S, A;
  t.preventDefault();
  const n = t.target.closest("[data-mwd-action='toggleMachineReliabilitySpend']"), i = Number(((g = n == null ? void 0 : n.dataset) == null ? void 0 : g.resultIndex) ?? -1), a = Number(((p = n == null ? void 0 : n.dataset) == null ? void 0 : p.spendIndex) ?? -1);
  if (!Number.isInteger(i) || i < 0 || !Number.isInteger(a) || a < 0) return;
  const r = foundry.utils.deepClone(e.getFlag("mwd", "resolved")), s = ((b = (h = r == null ? void 0 : r.attackResult) == null ? void 0 : h.results) == null ? void 0 : b[i]) ?? null, o = (s == null ? void 0 : s.queuedMutation) ?? null;
  if (!o || o.applied || !aM(o)) return;
  const l = new Set(
    Array.isArray((y = o.payload) == null ? void 0 : y.reliabilitySpendSelections) ? o.payload.reliabilitySpendSelections.map((T) => Number(T)).filter(Number.isInteger) : []
  );
  l.has(a) ? l.delete(a) : l.add(a), o.payload.reliabilitySpendSelections = Array.from(l).sort((T, C) => T - C);
  const c = Tu(o) ? await fromUuid(Tu(o)) : null, u = wu(o) ? await fromUuid(wu(o)) : null, d = await ((A = (S = rr()) == null ? void 0 : S.rebuildQueuedAttackDamagePreview) == null ? void 0 : A.call(S, { mutation: o, result: s, targetActor: c, targetToken: u }));
  if (!(d != null && d.ok)) return;
  const f = d.summary;
  s.queuedMutation = o, s.damageResult = f;
  const m = await ar({ resolved: r });
  await e.update({
    content: m,
    "flags.mwd.resolved": r
  });
}
async function Xz(t, e) {
  var s, o, l, c, u, d, f, m, g, p, h, b, y;
  t.preventDefault();
  const n = t.target.closest("[data-mwd-action='machineCritRemedy']"), i = String(((s = n == null ? void 0 : n.dataset) == null ? void 0 : s.machineActorUuid) ?? "").trim(), a = i ? await fromUuid(i) : null;
  if (!a) {
    (l = (o = ui.notifications) == null ? void 0 : o.warn) == null || l.call(o, "Machine actor could not be resolved.");
    return;
  }
  const r = await ((h = (c = Ez()) == null ? void 0 : c.execute) == null ? void 0 : h.call(c, a, {
    kind: "repair",
    issueKind: "crit",
    issueId: ((u = n == null ? void 0 : n.dataset) == null ? void 0 : u.critId) ?? "",
    critId: ((d = n == null ? void 0 : n.dataset) == null ? void 0 : d.critId) ?? "",
    remedyKey: ((f = n == null ? void 0 : n.dataset) == null ? void 0 : f.remedyKey) ?? "",
    operatorActorUuid: ((m = n == null ? void 0 : n.dataset) == null ? void 0 : m.operatorActorUuid) ?? "",
    gmOverride: !!((g = game.user) != null && g.isGM && ((p = n == null ? void 0 : n.dataset) == null ? void 0 : p.gmOverride) === "true"),
    event: t
  }));
  if (!(r != null && r.ok)) {
    (y = (b = ui.notifications) == null ? void 0 : b.warn) == null || y.call(b, (r == null ? void 0 : r.userMessage) ?? (r == null ? void 0 : r.reason) ?? "Unable to launch the machine remedy roll.");
    return;
  }
}
async function rM(t, e) {
  const n = e != null && e.actorUuid ? await fromUuid(e.actorUuid) : null, i = e != null && e.tokenUuid ? await fromUuid(e.tokenUuid) : null, a = await Zk(e, { actor: n, token: i });
  return await t.update({
    content: a,
    "flags.mwd.hazardCard": e
  }), e;
}
async function Ch(t, e) {
  var i, a;
  const n = hp(foundry.utils.deepClone(((a = (i = t == null ? void 0 : t.flags) == null ? void 0 : i.mwd) == null ? void 0 : a.hazardCard) ?? {}));
  return n != null && n.actorUuid ? (await e(n), await rM(t, n), n) : null;
}
async function Ih(t, e, { active: n = !1, edgePoolKey: i = "" } = {}) {
  var o, l, c;
  const a = e != null && e.actorUuid ? await fromUuid(e.actorUuid) : null, r = e != null && e.tokenUuid ? await fromUuid(e.tokenUuid) : null;
  if (!a) return;
  const s = D.getReactionSpendPreview(a, { token: r, edgePoolKey: i });
  if (!(n && (s != null && s.disabled))) {
    if (!n) {
      const u = D.getSnapshot(a, { token: r }), d = (u == null ? void 0 : u.pendingReaction) ?? null;
      (d == null ? void 0 : d.sourceKind) === "hazard" && (d == null ? void 0 : d.messageId) === t.id && (d == null ? void 0 : d.sourceId) === (e == null ? void 0 : e.regionId) && await D.clearPendingReaction(a, { token: r });
      return;
    }
    await D.setPendingReaction(a, {
      token: r,
      pendingReaction: {
        type: "evade",
        sourceKind: "hazard",
        sourceId: (e == null ? void 0 : e.regionId) ?? null,
        messageId: t.id,
        exposureBefore: ((o = e == null ? void 0 : e.exposure) == null ? void 0 : o.initialTier) ?? "none",
        exposureAfterPreview: ((l = e == null ? void 0 : e.preview) == null ? void 0 : l.finalTier) ?? ((c = e == null ? void 0 : e.exposure) == null ? void 0 : c.initialTier) ?? "none",
        edgePoolKey: i,
        allowCurrentTurn: !0
      }
    });
  }
}
async function Zz(t, e) {
  var i, a;
  t.preventDefault();
  const n = await Ch(e, async (r) => {
    var l, c, u;
    const s = !((l = r == null ? void 0 : r.preview) != null && l.evadeActive), o = Xg(Va({
      tier: ((c = r == null ? void 0 : r.exposure) == null ? void 0 : c.initialTier) ?? "none"
    }), {
      active: s,
      locked: !!((u = r == null ? void 0 : r.exposure) != null && u.evadeLocked)
    });
    if (r.preview ?? (r.preview = {}), r.preview.evadeActive = s, r.preview.edgePoolKey = null, r.preview.finalTier = o.finalTier, r.damageAfter = $r(r.baseDamage ?? 0, o.finalTier), s) {
      const d = await Id({
        actorUuid: r.actorUuid,
        tokenUuid: r.tokenUuid,
        edgePoolKey: ""
      });
      r.preview.reactionPreview = d ? {
        burnDelta: Number(d.burnDelta ?? 0),
        canSpendEdge: !!d.canSpendEdge,
        disabled: !!d.disabled,
        reason: String(d.reason ?? ""),
        edgePools: (d.edgePools ?? []).map((f) => ({
          key: f.key,
          label: f.label,
          value: f.value
        }))
      } : {};
    } else
      r.preview.reactionPreview = {};
  });
  n && await Ih(e, n, {
    active: !!((i = n == null ? void 0 : n.preview) != null && i.evadeActive),
    edgePoolKey: String(((a = n == null ? void 0 : n.preview) == null ? void 0 : a.edgePoolKey) ?? "").trim()
  });
}
async function e1(t, e) {
  var r, s, o;
  t.preventDefault();
  const n = t.target.closest("[data-mwd-action='toggleHazardEvadeEdge']"), i = String(((r = n == null ? void 0 : n.dataset) == null ? void 0 : r.poolKey) ?? "").trim(), a = await Ch(e, async (l) => {
    l.preview ?? (l.preview = {}), l.preview.evadeActive = !0, l.preview.edgePoolKey = l.preview.edgePoolKey === i ? null : i;
    const c = await Id({
      actorUuid: l.actorUuid,
      tokenUuid: l.tokenUuid,
      edgePoolKey: l.preview.edgePoolKey ?? ""
    });
    l.preview.reactionPreview = c ? {
      burnDelta: Number(c.burnDelta ?? 0),
      canSpendEdge: !!c.canSpendEdge,
      disabled: !!c.disabled,
      reason: String(c.reason ?? ""),
      edgePools: (c.edgePools ?? []).map((u) => ({
        key: u.key,
        label: u.label,
        value: u.value
      }))
    } : {};
  });
  a && await Ih(e, a, {
    active: !!((s = a == null ? void 0 : a.preview) != null && s.evadeActive),
    edgePoolKey: String(((o = a == null ? void 0 : a.preview) == null ? void 0 : o.edgePoolKey) ?? "").trim()
  });
}
async function t1(t, e) {
  var u, d, f, m, g, p, h, b, y, S, A, T, C, k, w, v, N, P, L, _, O, x, H;
  t.preventDefault();
  const n = hp(foundry.utils.deepClone(((d = (u = e == null ? void 0 : e.flags) == null ? void 0 : u.mwd) == null ? void 0 : d.hazardCard) ?? {}));
  if (!(n != null && n.actorUuid)) return;
  if (n.applied) {
    (m = (f = ui.notifications) == null ? void 0 : f.info) == null || m.call(f, "That hazard has already been applied.");
    return;
  }
  const i = await fromUuid(n.actorUuid), a = n.tokenUuid ? await fromUuid(n.tokenUuid) : null;
  if (!i) {
    (p = (g = ui.notifications) == null ? void 0 : g.warn) == null || p.call(g, "Unable to resolve the hazard target.");
    return;
  }
  if ((h = n.preview) != null && h.evadeActive) {
    const G = await D.commitReactionSpend(i, {
      token: a,
      actionId: "evade",
      actionLabel: "Evade",
      actionCategory: "reaction",
      logLabel: `Evade: ${n.regionName}`,
      edgePoolKey: String(((b = n.preview) == null ? void 0 : b.edgePoolKey) ?? "").trim(),
      allowCurrentTurn: !0
    });
    if (!(G != null && G.ok)) {
      (S = (y = ui.notifications) == null ? void 0 : y.warn) == null || S.call(y, (G == null ? void 0 : G.reason) ?? "Unable to spend the Evade reaction.");
      return;
    }
  }
  const r = {
    mode: "attackDamage",
    track: M.monitors.physical,
    damage: Number(n.damageAfter ?? n.damageBefore ?? 0) || 0,
    netHits: 0,
    damageType: n.damageType,
    ap: Number(n.ap ?? 0) || 0,
    source: n.source,
    notes: `Hazard exposure ${n.exposure.initialLabel}${(A = n.preview) != null && A.evadeActive ? ` -> ${String(n.preview.finalTier ?? n.exposure.initialTier).toUpperCase()}` : ""}`.trim()
  }, s = await ((C = (T = rr()) == null ? void 0 : T.apply) == null ? void 0 : C.call(T, {
    actor: i,
    token: a,
    payload: r,
    options: {
      actorId: i.id,
      logToChat: !1
    }
  }));
  if (!(s != null && s.ok)) {
    (w = (k = ui.notifications) == null ? void 0 : k.warn) == null || w.call(k, (s == null ? void 0 : s.reason) ?? "Unable to apply hazard damage.");
    return;
  }
  const o = D.getSnapshot(i, { token: a }), l = ((v = o == null ? void 0 : o.hazards) == null ? void 0 : v[n.regionId]) ?? {}, c = Mt(n.nextTier, n.exposure.finalTier);
  await D.setHazardState(i, {
    token: a,
    regionId: n.regionId,
    hazardState: {
      ...l,
      tier: c,
      turnsExposed: Math.max(Number((l == null ? void 0 : l.turnsExposed) ?? 0), Number(n.turnsExposed ?? 0)) + 1,
      lastProcessedRound: Number(((N = o == null ? void 0 : o.combat) == null ? void 0 : N.round) ?? 0) || 0,
      evadeLocked: !!(l != null && l.evadeLocked) || !!(((P = n.exposure) == null ? void 0 : P.initialTier) === "full" && ((L = n.preview) == null ? void 0 : L.finalTier) === "major" && ((_ = n.preview) != null && _.evadeActive))
    }
  }), c === "full" && Number((n == null ? void 0 : n.onFullBurnDelta) ?? 0) > 0 && await i.update({
    "system.burn.value": Math.max(0, Number(((x = (O = i.system) == null ? void 0 : O.burn) == null ? void 0 : x.value) ?? 0) + Number(n.onFullBurnDelta ?? 0))
  }), await D.clearPendingReaction(i, { token: a }), n.applied = !0, n.applyReason = "Applied", await rM(e, n), await Th({
    summary: {
      actorName: i.name,
      track: s.track,
      finalDamage: Number(s.finalDamage ?? s.appliedDelta ?? 0),
      damageIncoming: Number(s.damageIncoming ?? n.damageAfter ?? 0),
      damageType: s.damageType ?? n.damageType,
      usedArmor: !!s.usedArmor,
      effectiveAp: Number(s.effectiveAp ?? n.ap ?? 0),
      mitigation: s.mitigation ?? null,
      beforeLabel: String(s.beforeLabel ?? "").trim(),
      afterLabel: String(s.afterLabel ?? "").trim(),
      source: n.source,
      notes: `Hazard exposure ${n.exposure.initialLabel}${(H = n.preview) != null && H.evadeActive ? ` -> ${String(n.preview.finalTier ?? "").toUpperCase()}` : ""}`
    },
    actor: i,
    token: a
  });
}
async function n1(t, { token: e = null } = {}) {
  var r, s;
  const n = D.getSnapshot(t, { token: e }), i = (n == null ? void 0 : n.pendingReaction) ?? null;
  if (!(i != null && i.messageId))
    return { ok: !1, reason: "Use an area effect or hazard card to trigger Evade." };
  const a = qz(i.messageId);
  if (!a)
    return await D.clearPendingReaction(t, { token: e }), { ok: !1, reason: "The pending Evade card is no longer available." };
  if (i.sourceKind === "attack") {
    const o = String(i.sourceId ?? "").trim();
    if (!o) return { ok: !1, reason: "Pending Evade target is missing." };
    const l = await Mh(a, async (u) => {
      var m;
      u.areaEffectPreviewState ?? (u.areaEffectPreviewState = {}), u.areaEffectPreviewState[o] = {
        ...u.areaEffectPreviewState[o] ?? {},
        evadeActive: !0,
        edgePoolKey: i.edgePoolKey ?? null
      };
      const d = (Array.isArray((m = u == null ? void 0 : u.attackResult) == null ? void 0 : m.results) ? u.attackResult.results : []).find((g) => (g == null ? void 0 : g.previewKey) === o) ?? null, f = d ? await wh({ ...d, evadeEdgePoolKey: i.edgePoolKey ?? "" }) : null;
      f && (u.areaEffectPreviewState[o].reactionPreview = {
        burnDelta: Number(f.burnDelta ?? 0),
        canSpendEdge: !!f.canSpendEdge,
        disabled: !!f.disabled,
        reason: String(f.reason ?? ""),
        edgePools: (f.edgePools ?? []).map((g) => ({
          key: g.key,
          label: g.label,
          value: g.value
        }))
      });
    }), c = (Array.isArray((r = l == null ? void 0 : l.attackResult) == null ? void 0 : r.results) ? l.attackResult.results : []).find((u) => (u == null ? void 0 : u.previewKey) === o) ?? null;
    return c && await vh(a, c, {
      active: !0,
      edgePoolKey: String((c == null ? void 0 : c.evadeEdgePoolKey) ?? i.edgePoolKey ?? "").trim()
    }), { ok: !0 };
  }
  if (i.sourceKind === "hazard") {
    const o = await Ch(a, async (l) => {
      var d, f;
      const c = Xg(Va({
        tier: ((d = l == null ? void 0 : l.exposure) == null ? void 0 : d.initialTier) ?? "none"
      }), {
        active: !0,
        locked: !!((f = l == null ? void 0 : l.exposure) != null && f.evadeLocked)
      });
      l.preview ?? (l.preview = {}), l.preview.evadeActive = !0, l.preview.edgePoolKey = l.preview.edgePoolKey ?? i.edgePoolKey ?? null, l.preview.finalTier = c.finalTier, l.damageAfter = $r(l.baseDamage ?? 0, c.finalTier);
      const u = await Id({
        actorUuid: l.actorUuid,
        tokenUuid: l.tokenUuid,
        edgePoolKey: l.preview.edgePoolKey ?? ""
      });
      l.preview.reactionPreview = u ? {
        burnDelta: Number(u.burnDelta ?? 0),
        canSpendEdge: !!u.canSpendEdge,
        edgePools: (u.edgePools ?? []).map((m) => ({
          key: m.key,
          label: m.label,
          value: m.value
        }))
      } : {};
    });
    return o && await Ih(a, o, {
      active: !0,
      edgePoolKey: String(((s = o == null ? void 0 : o.preview) == null ? void 0 : s.edgePoolKey) ?? i.edgePoolKey ?? "").trim()
    }), { ok: !0 };
  }
  return { ok: !1, reason: "That Evade source is not supported." };
}
async function i1(t, e) {
  var r, s, o, l, c;
  t.preventDefault();
  const n = t.target.closest("[data-mwd-action='edgePostReroll']"), i = String(((r = n == null ? void 0 : n.dataset) == null ? void 0 : r.poolKey) ?? "").trim();
  if (!i) return;
  const a = await ((o = (s = iM()) == null ? void 0 : s.applyPostRerollFailures) == null ? void 0 : o.call(s, { message: e, poolKey: i }));
  if (!(a != null && a.ok)) {
    const u = (a == null ? void 0 : a.reason) === "no-failures" ? "info" : "warn";
    (c = (l = ui.notifications) == null ? void 0 : l[u]) == null || c.call(l, (a == null ? void 0 : a.userMessage) ?? (a == null ? void 0 : a.reason) ?? "Unable to spend post-roll Edge.");
    return;
  }
  await e.update(a.updateData);
}
async function a1(t, e) {
  var r, s, o, l, c, u, d, f;
  t.preventDefault();
  const n = foundry.utils.deepClone((s = (r = e == null ? void 0 : e.flags) == null ? void 0 : r.mwd) == null ? void 0 : s.resolved);
  if (!n) {
    (o = ui.notifications) == null || o.warn("Roll data is no longer available.");
    return;
  }
  const i = await sz(n);
  if (!(i != null && i.ok)) {
    const m = (i == null ? void 0 : i.reason) === "First Aid has already been applied." ? "info" : "warn";
    (c = (l = ui.notifications) == null ? void 0 : l[m]) == null || c.call(l, (i == null ? void 0 : i.reason) ?? "Unable to apply First Aid.");
    return;
  }
  n.firstAidResult = i, (u = n.edge) != null && u.availableActions && (n.edge.availableActions.canSpendPost = !1, n.edge.availableActions.canPostRerollFailures = !1);
  const a = await ar({ resolved: n });
  await e.update({
    content: a,
    "flags.mwd.resolved": n
  }), (f = (d = ui.notifications) == null ? void 0 : d.info) == null || f.call(d, `Recovered ${Number(i.recovered ?? 0)} ${i.trackLabel ?? "damage"} for ${i.targetName ?? "target"}.`);
}
class r1 extends Error {
  constructor(e, { severity: n = "error" } = {}) {
    super(e), this.name = "UserFacingRollError", this.userFacing = !0, this.severity = n === "warn" ? "warn" : "error";
  }
}
function dt(t, e = {}) {
  return new r1(t, e);
}
function Yt(t, e = "Unable to execute roll.") {
  var i, a;
  const n = t != null && t.userFacing && (t == null ? void 0 : t.severity) === "warn" ? "warn" : "error";
  (a = (i = ui.notifications) == null ? void 0 : i[n]) == null || a.call(i, (t == null ? void 0 : t.message) ?? e);
}
const Eh = `${I}.ownedWeaponAttack`;
let Zy = !1;
function s1(t, e = null) {
  var s, o, l, c, u;
  const n = (t == null ? void 0 : t.actor) ?? null, i = {
    intent: "attack",
    weaponId: (t == null ? void 0 : t.id) ?? "",
    payloadId: ((s = t == null ? void 0 : t.system) == null ? void 0 : s.selectedPayloadKey) || ((o = t == null ? void 0 : t.system) == null ? void 0 : o.selectedPayloadUuid) || ((l = t == null ? void 0 : t.system) == null ? void 0 : l.selectedPayloadId) || "",
    edge: { pool: "physical.grit", allowed: ["pre", "post"] },
    tags: ["combat", "attack"],
    sourceTokenId: (e == null ? void 0 : e.id) ?? null
  }, a = n ? D.getSnapshot(n, { token: e }) : null, r = !!((u = (c = a == null ? void 0 : a.state) == null ? void 0 : c.actionState) != null && u.aim);
  return r && (i.aim = { active: !0 }), { payload: i, hasAim: r };
}
function o1(t, e = null) {
  return e ?? (t == null ? void 0 : t.token) ?? D.getCurrentSceneTokenDocument(t) ?? null;
}
function sM(t) {
  const e = String((t == null ? void 0 : t.uuid) ?? "").trim();
  return e ? {
    type: Eh,
    uuid: e,
    name: String((t == null ? void 0 : t.name) ?? "Weapon").trim() || "Weapon",
    img: (t == null ? void 0 : t.img) ?? "icons/svg/sword.svg"
  } : null;
}
async function Vl({ weapon: t, event: e = null, token: n = null } = {}) {
  var i, a, r, s, o, l;
  try {
    if (!((i = t == null ? void 0 : t.isPersonalWeapon) != null && i.call(t)))
      throw new Error("Attack requires an owned personal weapon.");
    const c = t.actor ?? null;
    if (!c)
      throw new Error("Attack requires an owned personal weapon.");
    const u = eh(c, t);
    if (u)
      return (a = ui.notifications) == null || a.warn(u), null;
    const d = o1(c, n), { payload: f, hasAim: m } = s1(t, d), g = ((r = game.mwd) == null ? void 0 : r.roll) ?? ((o = (s = game.system) == null ? void 0 : s.mwd) == null ? void 0 : o.roll);
    if (!(g != null && g.execute))
      throw new Error("MWD roll system not initialized.");
    const p = await g.execute({ actor: c, payload: f, event: e });
    if (p) {
      m && await D.clearAim(c, { token: d });
      const h = D.getSnapshot(c, { token: d });
      if (h != null && h.hasCombatant) {
        const b = await D.spendResource(c, {
          token: d,
          resource: "sa",
          cost: 2,
          actionId: "attack",
          actionLabel: "Attack",
          actionCostLabel: "2 SA",
          actionCategory: "complex"
        });
        b != null && b.ok || (l = ui.notifications) == null || l.warn((b == null ? void 0 : b.reason) ?? "Unable to record attack action.");
      }
    }
    return p;
  } catch (c) {
    return console.error("MWD | Failed to launch weapon attack", c), Yt(c, "Unable to attack with that weapon."), null;
  }
}
async function l1(t, { event: e = null } = {}) {
  var a, r;
  const n = String(t ?? "").trim();
  if (!n)
    return (a = ui.notifications) == null || a.warn("That weapon shortcut is missing its item reference."), null;
  const i = await fromUuid(n);
  return i ? Vl({ weapon: i, event: e }) : ((r = ui.notifications) == null || r.warn("That weapon shortcut could not find its source item."), null);
}
function c1(t) {
  return `(async () => {
  const attacks = game.mwd?.attacks;
  if (!attacks?.attackWeaponByUuid) {
    return ui.notifications?.error("MWD attack shortcuts are not available right now.");
  }
  await attacks.attackWeaponByUuid(${JSON.stringify(String(t ?? "").trim())});
})();`;
}
async function u1(t, e) {
  var s, o, l, c;
  const n = String((t == null ? void 0 : t.uuid) ?? "").trim();
  if (!n) return;
  const i = String((t == null ? void 0 : t.name) ?? "Weapon Attack").trim() || "Weapon Attack", a = c1(n);
  let r = ((o = (s = game.macros) == null ? void 0 : s.find) == null ? void 0 : o.call(
    s,
    (u) => (u == null ? void 0 : u.type) === "script" && (u == null ? void 0 : u.name) === i && (u == null ? void 0 : u.command) === a
  )) ?? null;
  r || (r = await Macro.create({
    name: i,
    type: "script",
    img: (t == null ? void 0 : t.img) ?? "icons/svg/sword.svg",
    command: a
  })), await ((c = (l = game.user) == null ? void 0 : l.assignHotbarMacro) == null ? void 0 : c.call(l, r, e));
}
function oM(t, e, n) {
  return (e == null ? void 0 : e.type) !== Eh ? !0 : (u1(e, n), !1);
}
function lM() {
  Zy || (Zy = !0, Hooks.on("hotbarDrop", oM));
}
const eS = {
  HOTBAR_ATTACK_TYPE: Eh,
  getOwnedWeaponAttackDragData: sM,
  launchOwnedWeaponAttack: Vl,
  attackWeaponByUuid: l1,
  handleWeaponAttackHotbarDrop: oM,
  registerWeaponAttackHotbarHook: lM
};
function d1(t) {
  var e, n, i;
  if (t !== void 0)
    return ((i = (n = (e = globalThis.foundry) == null ? void 0 : e.utils) == null ? void 0 : n.deepClone) == null ? void 0 : i.call(n, t)) ?? JSON.parse(JSON.stringify(t ?? null));
}
function en(t) {
  var n, i;
  const e = (i = (n = globalThis.foundry) == null ? void 0 : n.utils) == null ? void 0 : i.escapeHTML;
  return e ? e(String(t ?? "")) : String(t ?? "").replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;");
}
function Nh() {
  var t, e, n;
  return ((t = game == null ? void 0 : game.mwd) == null ? void 0 : t.roll) ?? ((n = (e = game == null ? void 0 : game.system) == null ? void 0 : e.mwd) == null ? void 0 : n.roll) ?? null;
}
function m1(t) {
  var i, a, r, s, o, l, c;
  const e = Math.max(0, Number(((i = t == null ? void 0 : t.getAttributeValue) == null ? void 0 : i.call(t, "reflexes")) ?? ((s = (r = (a = t == null ? void 0 : t.system) == null ? void 0 : a.attributes) == null ? void 0 : r.reflexes) == null ? void 0 : s.value) ?? 0) || 0), n = Math.max(0, Number(((c = (l = (o = t == null ? void 0 : t.system) == null ? void 0 : o.attributes) == null ? void 0 : l.guts) == null ? void 0 : c.value) ?? 0) || 0);
  return 3 + Math.floor((e + n) / 2);
}
function f1(t, e) {
  var n;
  return Math.max(0, m1(t) - Math.max(0, Number(((n = e == null ? void 0 : e.state) == null ? void 0 : n.saSpentThisActivation) ?? 0) || 0));
}
function tS(t, e = null) {
  const n = String(t ?? "").trim();
  if (!n) return e;
  try {
    return JSON.parse(n);
  } catch (i) {
    return console.warn("MWD | Invalid personal combat payload", n, i), e;
  }
}
function g1(t = {}) {
  var e;
  return (t == null ? void 0 : t.intent) === "combatSpend" ? {
    actionId: String(t.actionId ?? t.combatAction ?? t.id ?? "").trim(),
    action: null,
    metadata: t.metadata ?? {},
    spend: {
      resource: String(t.resource ?? "").trim(),
      cost: Math.max(0, Number(t.cost ?? 0) || 0),
      actionLabel: String(t.actionLabel ?? t.label ?? "").trim(),
      actionCostLabel: String(t.actionCostLabel ?? t.costLabel ?? "").trim()
    }
  } : t != null && t.intent && t.intent !== "combatAction" ? {
    actionId: "",
    action: null,
    metadata: {},
    roll: t
  } : t != null && t.action && typeof t.action == "object" ? {
    actionId: String(t.action.id ?? t.actionId ?? "").trim(),
    action: t.action,
    metadata: t.metadata ?? {}
  } : {
    actionId: String((t == null ? void 0 : t.actionId) ?? ((e = t == null ? void 0 : t.payload) == null ? void 0 : e.actionId) ?? (t == null ? void 0 : t.id) ?? "").trim(),
    action: (t == null ? void 0 : t.action) ?? null,
    metadata: (t == null ? void 0 : t.metadata) ?? {}
  };
}
function cM({ handler: t = "combatIntent", target: e = null, event: n = null } = {}) {
  var c, u, d, f, m, g;
  const i = e ?? ((u = (c = n == null ? void 0 : n.target) == null ? void 0 : c.closest) == null ? void 0 : u.call(c, "[data-action]")) ?? null, a = (i == null ? void 0 : i.dataset) ?? {}, r = String(a.combatAction ?? "").trim(), s = String(a.combatPayload ?? "").trim();
  if (s) {
    const p = tS(s);
    if (p) return p;
  }
  if (t === "combatSpend")
    return {
      intent: "combatSpend",
      actionId: r,
      resource: String(a.resource ?? "").trim(),
      cost: Math.max(0, Number(a.cost ?? 0) || 0),
      actionLabel: String(a.combatLabel ?? "").trim(),
      actionCostLabel: String(a.combatCostLabel ?? "").trim()
    };
  if (t === "combatOverloadCheck")
    return tS(
      a.roll ?? ((g = (m = (f = (d = n == null ? void 0 : n.target) == null ? void 0 : d.closest) == null ? void 0 : f.call(d, "[data-roll]")) == null ? void 0 : m.dataset) == null ? void 0 : g.roll),
      null
    ) ?? { intent: "combatAction", actionId: r || "overloadCheck" };
  const l = t === "combatIntent" ? r : {
    combatAction: r,
    combatAttack: r || "attack",
    combatEvade: r || "evade",
    combatAssist: r || "assist",
    combatInterrupt: r || "interrupt",
    combatFirstAid: r || "firstAid",
    combatReduceBurn: r || "reduceBurn"
  }[t];
  return l ? { intent: "combatAction", actionId: l } : null;
}
function uM(t = {}, e = {}) {
  var a, r;
  const n = String(e.resource ?? ((a = t.cost) == null ? void 0 : a.resource) ?? "none").trim(), i = Math.max(0, Number(e.value ?? ((r = t.cost) == null ? void 0 : r.value) ?? 0) || 0);
  return n === Bi.none || i <= 0 ? "No Cost" : n === Bi.fa ? "Free" : `${i} ${n.toUpperCase()}`;
}
function xo(t = {}, e = {}) {
  return e != null && e.targetName && t.id === "assist" ? `${t.label}: ${e.targetName}` : e != null && e.scope && t.id === "interrupt" ? `${t.label}: ${String(e.scope).trim()}` : e != null && e.weaponName && ["reload", "selectPayload", "changeFireMode"].includes(t.id) ? `${t.label}: ${e.weaponName}` : t.label;
}
function p1(t = null, e = () => !0) {
  return Array.from((t == null ? void 0 : t.items) ?? []).filter((n) => {
    try {
      return e(n);
    } catch {
      return !1;
    }
  });
}
function dM(t = null) {
  return p1(
    t,
    (e) => {
      var n;
      return (e == null ? void 0 : e.type) === M.itemType.personalWeapon || ((n = e == null ? void 0 : e.isPersonalWeapon) == null ? void 0 : n.call(e));
    }
  );
}
function h1(t = null) {
  var n, i;
  const e = (i = (n = t == null ? void 0 : t.getPayloadState) == null ? void 0 : n.call(t)) == null ? void 0 : i.payloads;
  return (Array.isArray(e) ? e : []).filter((a) => (a == null ? void 0 : a.id) && a.id !== "unloaded").map((a) => ({
    id: String(a.id ?? "").trim(),
    label: String(a.label ?? "Payload").trim() || "Payload",
    sourceType: String(a.sourceType ?? "").trim()
  }));
}
async function Qr({ title: t = "Choose", content: e = "", submitLabel: n = "OK", callback: i = () => ({}) } = {}) {
  var r, s, o, l;
  const a = (o = (s = (r = globalThis.foundry) == null ? void 0 : r.applications) == null ? void 0 : s.api) == null ? void 0 : o.DialogV2;
  return a != null && a.wait ? a.wait({
    window: { title: t },
    content: e,
    buttons: [{
      action: "submit",
      label: n,
      default: !0,
      callback: (c, u) => i(u.form)
    }, {
      action: "cancel",
      label: "Cancel",
      callback: () => null
    }],
    close: () => null
  }) : (l = globalThis.Dialog) != null && l.prompt ? Dialog.prompt({
    title: t,
    content: e,
    label: n,
    callback: (c) => i({
      querySelector: (u) => {
        var d;
        return ((d = c.find(u)) == null ? void 0 : d[0]) ?? null;
      }
    })
  }) : null;
}
function Kr(t, e) {
  var i;
  const n = (i = t == null ? void 0 : t.querySelector) == null ? void 0 : i.call(t, `[name="${e}"]`);
  return String((n == null ? void 0 : n.value) ?? "").trim();
}
async function b1(t) {
  return Qr({
    title: t.label,
    content: `
    <form class="mwd-quick-select">
      <div class="mwd-field">
        <label>Trigger</label>
        <input type="text" name="condition" placeholder="When..." />
      </div>
      <div class="mwd-field">
        <label>Scope</label>
        <input type="text" name="scope" placeholder="What you will do" />
      </div>
    </form>`,
    submitLabel: "Prepare",
    callback: (n) => ({
      condition: Kr(n, "condition"),
      scope: Kr(n, "scope")
    })
  });
}
async function y1(t) {
  const n = `<form class="mwd-quick-select"><div class="mwd-field"><label>Skill</label><select name="skill">${Qo().map((i) => `<option value="${en(i.code)}">${en(i.label)}</option>`).join("")}</select></div></form>`;
  return Qr({
    title: t.label,
    content: n,
    submitLabel: "Roll",
    callback: (i) => ({ skillKey: Kr(i, "skill") })
  });
}
async function S1(t, e) {
  var o, l;
  const n = dM(e);
  if (!n.length)
    return (o = t.prompt) != null && o.required ? { ok: !1, reason: "No owned personal weapons are available." } : {};
  if (n.length === 1 && !((l = t.prompt) != null && l.required))
    return { weapon: n[0], weaponId: n[0].id, weaponName: n[0].name };
  const a = `<form class="mwd-quick-select"><div class="mwd-field"><label>Weapon</label><select name="weaponId">${n.map((c) => `<option value="${en(c.id)}">${en(c.name ?? "Weapon")}</option>`).join("")}</select></div></form>`, r = await Qr({
    title: t.label,
    content: a,
    submitLabel: "Select",
    callback: (c) => ({ weaponId: Kr(c, "weaponId") })
  });
  if (!r) return null;
  const s = n.find((c) => c.id === r.weaponId) ?? null;
  return s ? { weapon: s, weaponId: s.id, weaponName: s.name } : { ok: !1, reason: "Selected weapon could not be found." };
}
async function A1(t, e) {
  var c;
  const n = dM(e).map((u) => ({
    weapon: u,
    payloads: h1(u)
  })).filter((u) => u.payloads.length);
  if (!n.length) return { ok: !1, reason: "No owned compatible payloads are available." };
  const i = n.flatMap((u) => u.payloads.map((d) => ({
    weaponId: u.weapon.id,
    weaponName: u.weapon.name,
    payloadId: d.id,
    payloadName: d.label
  }))), r = `<form class="mwd-quick-select"><div class="mwd-field"><label>Payload</label><select name="choice">${i.map((u, d) => `<option value="${d}">${en(u.weaponName)} - ${en(u.payloadName)}</option>`).join("")}</select></div></form>`, s = await Qr({
    title: t.label,
    content: r,
    submitLabel: "Select",
    callback: (u) => ({ index: Number(Kr(u, "choice")) })
  });
  if (!s) return null;
  const o = i[s.index] ?? null;
  if (!o) return { ok: !1, reason: "Selected payload could not be found." };
  const l = ((c = n.find((u) => u.weapon.id === o.weaponId)) == null ? void 0 : c.weapon) ?? null;
  return { ...o, weapon: l };
}
async function k1(t, e) {
  var o, l;
  const n = Array.from(((o = game == null ? void 0 : game.user) == null ? void 0 : o.targets) ?? []).map((c) => {
    var u, d, f;
    return {
      token: c,
      tokenUuid: String(((u = c == null ? void 0 : c.document) == null ? void 0 : u.uuid) ?? (c == null ? void 0 : c.uuid) ?? "").trim(),
      name: String((c == null ? void 0 : c.name) ?? ((d = c == null ? void 0 : c.document) == null ? void 0 : d.name) ?? ((f = c == null ? void 0 : c.actor) == null ? void 0 : f.name) ?? "Target").trim()
    };
  }).filter((c) => {
    var u;
    return c.tokenUuid || ((u = c.token) == null ? void 0 : u.id);
  });
  if (!n.length && ((l = t.prompt) != null && l.required)) return { ok: !1, reason: "Target a token before using that action." };
  if (!n.length) return {};
  if (n.length === 1) return { targetTokenUuid: n[0].tokenUuid, targetName: n[0].name };
  const a = `<form class="mwd-quick-select"><div class="mwd-field"><label>Target</label><select name="target">${n.map((c, u) => `<option value="${u}">${en(c.name)}</option>`).join("")}</select></div></form>`, r = await Qr({
    title: t.label,
    content: a,
    submitLabel: "Select",
    callback: (c) => ({ index: Number(Kr(c, "target")) })
  });
  if (!r) return null;
  const s = n[r.index] ?? null;
  return s ? { targetTokenUuid: s.tokenUuid, targetName: s.name } : { ok: !1, reason: "Selected target could not be found." };
}
function T1(t = null) {
  return t ? typeof t.values == "function" ? Array.from(t.values()) : Array.from(t ?? []) : [];
}
function w1(t) {
  var n, i;
  const e = String(((n = t == null ? void 0 : t.combatant) == null ? void 0 : n.id) ?? "").trim();
  return T1((i = t == null ? void 0 : t.combat) == null ? void 0 : i.combatants).filter((a) => a && String(a.id ?? "").trim() !== e).map((a) => {
    var l;
    const r = ((l = a.token) == null ? void 0 : l.document) ?? a.token ?? null, s = a.actor ?? (r == null ? void 0 : r.actor) ?? null, o = String(a.name ?? (r == null ? void 0 : r.name) ?? (s == null ? void 0 : s.name) ?? "Combatant").trim() || "Combatant";
    return {
      combatantId: String(a.id ?? "").trim(),
      actorUuid: (s == null ? void 0 : s.uuid) ?? null,
      tokenUuid: (r == null ? void 0 : r.uuid) ?? null,
      name: o
    };
  }).filter((a) => a.combatantId && a.name).sort((a, r) => a.name.localeCompare(r.name));
}
async function M1(t, e) {
  if (!(e != null && e.hasCombatant)) return { ok: !1, reason: "No combatant on the current scene." };
  if (e != null && e.isCurrentTurn) return { ok: !1, reason: "Only outside your activation." };
  const n = w1(e);
  if (!n.length) return { ok: !1, reason: "No other combatants are available to assist." };
  const a = `<form class="mwd-quick-select"><div class="mwd-field"><label>Assist</label><select name="combatant">${n.map((o) => `<option value="${en(o.combatantId)}">${en(o.name)}</option>`).join("")}</select></div></form>`, r = await Qr({
    title: t.label,
    content: a,
    submitLabel: "Assist",
    callback: (o) => {
      var l;
      return Kr(o, "combatant") || ((l = n[0]) == null ? void 0 : l.combatantId) || "";
    }
  });
  if (!r) return null;
  const s = n.find((o) => o.combatantId === r) ?? null;
  return s ? {
    targetCombatantId: s.combatantId,
    targetActorUuid: s.actorUuid,
    targetTokenUuid: s.tokenUuid,
    targetName: s.name
  } : { ok: !1, reason: "Selected combatant could not be found." };
}
async function v1(t, e) {
  const n = D.getPreparedInterrupt(e);
  if (!(e != null && e.hasCombatant)) return { ok: !1, reason: "No combatant on the current scene." };
  if (e != null && e.isCurrentTurn) return { ok: !1, reason: "Only outside your activation." };
  if (!n) return { ok: !1, reason: "Prepare an interrupt first." };
  const i = String((n == null ? void 0 : n.condition) ?? "").trim(), a = String((n == null ? void 0 : n.scope) ?? "").trim(), r = `
    <div class="mwd-quick-select">
      <p><strong>Trigger:</strong> ${en(i || "Unspecified trigger")}</p>
      <p><strong>Scope:</strong> ${en(a || "Unspecified response")}</p>
    </div>`;
  return await Qr({
    title: t.label,
    content: r,
    submitLabel: "Interrupt",
    callback: () => !0
  }) ? n : null;
}
async function C1(t, e, n) {
  var i, a, r, s;
  return ["firstAid", "evade", "reduceBurn", "recoverBurn"].includes(t.id) ? {} : t.id === "prepare" ? b1(t) : t.id === "assist" ? M1(t, n) : t.id === "interrupt" ? v1(t, n) : ((i = t.prompt) == null ? void 0 : i.type) === "skill" ? y1(t) : ((a = t.prompt) == null ? void 0 : a.type) === "weapon" ? S1(t, e) : ((r = t.prompt) == null ? void 0 : r.type) === "payload" ? A1(t, e) : ((s = t.prompt) == null ? void 0 : s.type) === "target" ? k1(t) : {};
}
function mM(t, e) {
  var i;
  const n = t.cost ?? { resource: "none", value: 0 };
  if (n.resource === Bi.fa) {
    const a = Number(((i = e == null ? void 0 : e.state) == null ? void 0 : i.faRemaining) ?? 0) > 0;
    return {
      resource: a ? "fa" : "sa",
      value: 1,
      label: a ? "Free" : "1 SA"
    };
  }
  return {
    resource: n.resource,
    value: Math.max(0, Number(n.value ?? 0) || 0),
    label: uM(t)
  };
}
function I1(t, e, n, i) {
  var a, r, s, o, l, c, u;
  if (((a = t.implementation) == null ? void 0 : a.state) === da.disabled)
    return ((r = t.implementation) == null ? void 0 : r.reason) || "That action is disabled.";
  if (((s = t.implementation) == null ? void 0 : s.state) === da.stub)
    return ((o = t.implementation) == null ? void 0 : o.reason) || "That action is not implemented yet.";
  if (t.id === "overloadCheck")
    return n != null && n.hasCombatant ? n != null && n.isCurrentTurn ? (l = n == null ? void 0 : n.burn) != null && l.canOverloadCheck ? "" : n != null && n.overloaded ? "Already Overloaded." : `Burn below ${((c = n == null ? void 0 : n.burn) == null ? void 0 : c.threshold) ?? 6}.` : "Only during your activation." : "No combatant on the current scene.";
  if (i.resource === "none" || i.value <= 0) return "";
  if (!(n != null && n.hasCombatant)) return "No combatant on the current scene.";
  if (t.category === He.reaction) {
    if (n.isCurrentTurn && t.id !== "evade") return "Only outside your activation.";
  } else if (!n.isCurrentTurn)
    return "Only during your activation.";
  return n.overloaded && t.id !== "reduceBurn" && t.id !== "recoverBurn" && i.resource !== "fa" ? "Overloaded actors can only recover Burn." : t.id === "aim" && ((u = D.getMoveActionCountFromState) == null ? void 0 : u.call(D, n.state)) > 0 ? "Cannot aim after taking a move action this activation." : i.resource === "sa" && f1(e, n) < i.value ? "Activation SA cap reached." : "";
}
async function Mm(t, { token: e = null, action: n, metadata: i = {}, snapshot: a = null } = {}) {
  var s, o;
  const r = mM(n, a);
  if (r.resource === "none" || r.value <= 0)
    return { ok: !0, costPaid: !1, costLabel: uM(n, r), snapshot: a };
  if (n.category === He.reaction) {
    const l = ((s = a == null ? void 0 : a.state) == null ? void 0 : s.pendingReaction) ?? null, c = n.id === "evade" && (l == null ? void 0 : l.allowCurrentTurn);
    let u = String((i == null ? void 0 : i.edgePoolKey) ?? "").trim();
    return !u && Number(((o = a == null ? void 0 : a.state) == null ? void 0 : o.raRemaining) ?? 0) <= 0 && (u = await D._promptSpendEdgeForReaction(t) ?? ""), D.commitReactionSpend(t, {
      token: e,
      actionId: n.id,
      actionLabel: n.label,
      actionCategory: n.category,
      logLabel: xo(n, i),
      edgePoolKey: u,
      allowCurrentTurn: c
    });
  }
  return D.spendResource(t, {
    token: e,
    resource: r.resource,
    cost: r.value,
    actionId: n.id,
    actionLabel: xo(n, i),
    actionCostLabel: r.label,
    actionCategory: n.category
  });
}
async function E1(t, { action: e, metadata: n = {}, event: i = null } = {}) {
  var o;
  const a = Nh();
  if (!(a != null && a.execute)) return { ok: !1, reason: "MWD roll system not initialized." };
  const r = {
    ...d1(e.roll) ?? { intent: "skill" },
    key: n.skillKey ?? ((o = e.roll) == null ? void 0 : o.key) ?? void 0,
    tags: Array.from(new Set(e.tags ?? []))
  };
  if (!r.key && r.intent === "skill")
    return { ok: !1, reason: "Choose a skill before rolling." };
  const s = await a.execute({ actor: t, payload: r, event: i });
  return { ok: !!s, rolled: !!s, value: s, cancelled: !s };
}
async function N1(t, { action: e, token: n = null, metadata: i = {}, event: a = null } = {}) {
  var c, u, d, f;
  if (e.id === "opportunity") {
    const m = Hl(t);
    if (m.cannotReact) return { ok: !1, reason: `Disabled (${m.reactionReason})` };
  }
  if (i.weapon) {
    const m = await Vl({ weapon: i.weapon, token: n, event: a });
    return { ok: !!m, rolled: !!m, value: m };
  }
  const r = Nh();
  if (!(r != null && r.execute)) return { ok: !1, reason: "MWD roll system not initialized." };
  const s = D.getSnapshot(t, { token: n }), o = {
    intent: "attack",
    edge: { pool: "physical.grit", allowed: ["pre", "post"] },
    tags: e.id === "opportunity" ? ["combat", "attack", "reaction", "opportunity"] : ["combat", "attack"],
    sourceTokenId: (n == null ? void 0 : n.id) ?? null
  };
  (u = (c = s == null ? void 0 : s.state) == null ? void 0 : c.actionState) != null && u.aim && (o.aim = { active: !0 });
  const l = await r.execute({ actor: t, payload: o, event: a });
  return l && ((f = (d = s == null ? void 0 : s.state) == null ? void 0 : d.actionState) != null && f.aim) && await D.clearAim(t, { token: n }), { ok: !!l, rolled: !!l, value: l };
}
async function P1(t, { action: e, metadata: n = {} } = {}) {
  var i;
  return e.id === "selectPayload" ? (i = n.weapon) != null && i.setActivePayload ? (await n.weapon.setActivePayload(n.payloadId), {
    ok: !0,
    stateChanges: [{ type: "itemUpdate", itemId: n.weaponId, path: "system.selectedPayloadKey", value: n.payloadId }],
    log: { title: e.label, message: `${n.weaponName}: ${n.payloadName}` }
  }) : { ok: !1, reason: "Selected weapon cannot select payloads." } : e.id === "reload" ? { ok: !0, log: { title: e.label, message: "Action spent; ammo consumption is not automated." } } : e.id === "changeFireMode" ? { ok: !0, log: { title: e.label, message: "Fire mode selection is recorded for this activation." } } : { ok: !0 };
}
async function R1(t, { action: e, token: n = null, metadata: i = {}, event: a = null } = {}) {
  var r, s, o, l, c, u;
  if (e.id === "firstAid") {
    const d = await rz(t, { token: n, event: a });
    return { ...d, rolled: !!(d && !d.cancelled) };
  }
  if (e.id === "evade")
    return n1(t, { token: n });
  if (e.id === "reduceBurn")
    return D.reduceBurn(t, { token: n });
  if (e.id === "recoverBurn") {
    const d = await D.reduceBurn(t, { token: n });
    return d != null && d.ok ? (Number(((s = (r = t.system) == null ? void 0 : r.burn) == null ? void 0 : s.value) ?? 0) > 0 && await t.update({ "system.burn.value": Math.max(0, Number(((l = (o = t.system) == null ? void 0 : o.burn) == null ? void 0 : l.value) ?? 0) - 1) }), { ok: !0, costPaid: !0, log: { title: e.label, message: "Reduced Burn through a complex recovery action." } }) : d;
  }
  if (e.id === "extinguish" || e.id === "recover") {
    const d = e.id === "extinguish" ? "onFire" : "stunned";
    return (u = (c = t == null ? void 0 : t.statuses) == null ? void 0 : c.has) != null && u.call(c, d) ? (await mt({ actor: t, statusId: d, active: !1, metadata: { source: e.id } }), { ok: !0, stateChanges: [{ type: "status", statusId: d, active: !1 }] }) : { ok: !1, reason: `${d} is not active.` };
  }
  return { ok: !0 };
}
async function D1(t, { action: e } = {}) {
  var n, i;
  return e.id === "stand" ? (i = (n = t == null ? void 0 : t.statuses) == null ? void 0 : n.has) != null && i.call(n, "prone") ? (await mt({ actor: t, statusId: "prone", active: !1, metadata: { source: e.id } }), { ok: !0, stateChanges: [{ type: "status", statusId: "prone", active: !1 }] }) : { ok: !1, reason: "Prone is not active." } : { ok: !0 };
}
async function vm(t, e) {
  const { action: n } = e;
  if (n.roll && (n.resolver === gt.action || n.id === "overloadCheck"))
    return E1(t, e);
  switch (n.resolver) {
    case gt.attack:
      return N1(t, e);
    case gt.interaction:
      return P1(t, e);
    case gt.recovery:
      return R1(t, e);
    case gt.movement:
      return D1(t, e);
    default:
      return { ok: !0 };
  }
}
async function O1({ actor: t, token: e = null, targetName: n = "", costLabel: i = "" } = {}) {
  const a = String((t == null ? void 0 : t.name) ?? "Ally").trim() || "Ally", r = String(n ?? "an ally").trim() || "an ally", s = String(i ?? "").trim(), o = `
    <div class="mwd-chat-card mwd-chat-card--assist">
      <h3>Assist</h3>
      <p><strong>${en(a)}</strong> assists <strong>${en(r)}</strong>.</p>
      ${s ? `<p><small>Cost: ${en(s)}</small></p>` : ""}
    </div>`;
  await ChatMessage.create({
    speaker: ChatMessage.getSpeaker({ actor: t, token: (e == null ? void 0 : e.object) ?? e }),
    content: o
  });
}
async function L1({ actor: t, token: e = null, preparedInterrupt: n = null, costLabel: i = "" } = {}) {
  const a = String((t == null ? void 0 : t.name) ?? "Combatant").trim() || "Combatant", r = String((n == null ? void 0 : n.condition) ?? "").trim(), s = String((n == null ? void 0 : n.scope) ?? "").trim(), o = String(i ?? "").trim(), l = `
    <div class="mwd-chat-card mwd-chat-card--interrupt">
      <h3>Interrupt</h3>
      <p><strong>${en(a)}</strong> resolves a prepared interrupt.</p>
      ${r ? `<p><strong>Trigger:</strong> ${en(r)}</p>` : ""}
      ${s ? `<p><strong>Scope:</strong> ${en(s)}</p>` : ""}
      ${o ? `<p><small>Cost: ${en(o)}</small></p>` : ""}
    </div>`;
  await ChatMessage.create({
    speaker: ChatMessage.getSpeaker({ actor: t, token: (e == null ? void 0 : e.object) ?? e }),
    content: l
  });
}
async function fM({ actor: t, token: e = null, index: n = null } = {}) {
  return D.removeActivationLogEntry(t, { token: e, index: n });
}
async function gM({ weapon: t, event: e = null, token: n = null } = {}) {
  const i = await Vl({ weapon: t, event: e, token: n });
  return { ok: !!i, rolled: !!i, cancelled: !i, value: i };
}
async function pM({ actor: t, token: e = null, payload: n = {}, event: i = null } = {}) {
  var h, b, y, S, A, T, C;
  if (!t) return { ok: !1, reason: "Combat action requires an actor." };
  const a = g1(n);
  if (a.roll) {
    const k = Nh();
    if (!(k != null && k.execute)) return { ok: !1, reason: "MWD roll system not initialized." };
    const w = await k.execute({ actor: t, payload: a.roll, event: i });
    return { ok: !!w, rolled: !!w, cancelled: !w, value: w };
  }
  if (a.spend) {
    const { resource: k, cost: w, actionLabel: v, actionCostLabel: N } = a.spend;
    return !a.actionId || !k || !w ? { ok: !1, reason: "Invalid combat spend intent." } : D.spendResource(t, {
      token: e,
      resource: k,
      cost: w,
      actionId: a.actionId,
      actionLabel: v,
      actionCostLabel: N
    });
  }
  const r = a.action ?? vs(a.actionId);
  if (!r) return { ok: !1, reason: "Unknown combat action." };
  const s = Qk(r, { strict: !1 });
  if (!s) return { ok: !1, reason: "Invalid combat action." };
  const o = String(((h = s.implementation) == null ? void 0 : h.state) ?? "ready").trim() || "ready";
  if (o === da.disabled || o === da.stub)
    return {
      ok: !1,
      reason: ((b = s.implementation) == null ? void 0 : b.reason) || "That action is not implemented yet.",
      actionId: s.id,
      costPaid: !1,
      rolled: !1,
      stateChanges: []
    };
  const l = D.getSnapshot(t, { token: e }), c = await C1(s, t, l);
  if (c === null)
    return { ok: !1, cancelled: !0, actionId: s.id, costPaid: !1, rolled: !1, stateChanges: [] };
  if ((c == null ? void 0 : c.ok) === !1) return c;
  const u = { ...a.metadata ?? {}, ...c ?? {} }, d = mM(s, l), f = I1(s, t, l, d);
  if (f) return { ok: !1, reason: f, actionId: s.id, costPaid: !1, rolled: !1, stateChanges: [] };
  if (["attack", "firstAid", "evade", "reduceBurn"].includes(s.id)) {
    const k = await vm(t, { action: s, token: e, metadata: u, event: i });
    if (s.id === "attack" && (k != null && k.ok) && !u.weapon) {
      const w = s.category === He.reaction ? await Mm(t, { token: e, action: s, metadata: u, snapshot: l }) : await D.spendResource(t, {
        token: e,
        resource: "sa",
        cost: 2,
        actionId: "attack",
        actionLabel: "Attack",
        actionCostLabel: "2 SA",
        actionCategory: "complex"
      });
      if (!(w != null && w.ok)) return { ...w, actionId: s.id, costPaid: !1, rolled: !!(k != null && k.rolled), stateChanges: [] };
    }
    return {
      ok: !!(k != null && k.ok),
      reason: (k == null ? void 0 : k.reason) ?? "",
      cancelled: !!(k != null && k.cancelled),
      actionId: s.id,
      costPaid: !!(k != null && k.ok && !(k != null && k.cancelled)),
      rolled: !!(k != null && k.rolled),
      stateChanges: (k == null ? void 0 : k.stateChanges) ?? [],
      log: (k == null ? void 0 : k.log) ?? null,
      value: k == null ? void 0 : k.value
    };
  }
  if (s.roll && (s.resolver === gt.action || s.id === "overloadCheck")) {
    const k = d.resource === "none" || d.value <= 0 ? { ok: !0 } : ((y = D.previewResourceSpend) == null ? void 0 : y.call(D, t, {
      token: e,
      resource: d.resource,
      cost: d.value,
      actionId: s.id,
      actionLabel: xo(s, u),
      actionCostLabel: d.label,
      actionCategory: s.category
    })) ?? { ok: !0 };
    if (!(k != null && k.ok)) return { ...k, actionId: s.id, costPaid: !1, rolled: !1, stateChanges: [] };
    const w = await vm(t, { action: s, token: e, metadata: u, event: i });
    if (w != null && w.cancelled)
      return { ...w, ok: !1, actionId: s.id, costPaid: !1, rolled: !!(w != null && w.rolled), stateChanges: [] };
    if (!(w != null && w.ok))
      return { ...w, actionId: s.id, costPaid: !1, rolled: !!(w != null && w.rolled), stateChanges: (w == null ? void 0 : w.stateChanges) ?? [] };
    const v = await Mm(t, { token: e, action: s, metadata: u, snapshot: l });
    if (!(v != null && v.ok)) return { ...v, actionId: s.id, costPaid: !1, rolled: !!(w != null && w.rolled), stateChanges: [] };
    const N = await ((S = D._applyActionState) == null ? void 0 : S.call(D, t, {
      token: e,
      actionId: s.id,
      metadata: u,
      snapshot: v.snapshot ?? l
    }));
    return {
      ok: !0,
      actionId: s.id,
      costPaid: !!(v.costPaid ?? ((A = s.cost) == null ? void 0 : A.resource) !== "none"),
      rolled: !!w.rolled,
      stateChanges: [
        ...N != null && N.ok ? [{ type: "combatState", actionId: s.id }] : [],
        ...w.stateChanges ?? []
      ],
      log: w.log ?? {
        title: s.label,
        message: xo(s, u)
      },
      costLabel: v.costLabel,
      value: w.value
    };
  }
  const m = await Mm(t, { token: e, action: s, metadata: u, snapshot: l });
  if (!(m != null && m.ok)) return { ...m, actionId: s.id, costPaid: !1, rolled: !1, stateChanges: [] };
  const g = await ((T = D._applyActionState) == null ? void 0 : T.call(D, t, {
    token: e,
    actionId: s.id === "carefulMove" ? "move" : s.id,
    metadata: u,
    snapshot: m.snapshot ?? l
  })), p = await vm(t, { action: s, token: e, metadata: u, event: i });
  return p != null && p.ok ? (s.id === "interrupt" ? (await D.clearPreparedInterrupt(t, { token: e }), await L1({
    actor: t,
    token: e,
    preparedInterrupt: u,
    costLabel: m.costLabel
  })) : s.id === "assist" && await O1({
    actor: t,
    token: e,
    targetName: u.targetName,
    costLabel: m.costLabel
  }), {
    ok: !0,
    actionId: s.id,
    costPaid: !!(m.costPaid ?? ((C = s.cost) == null ? void 0 : C.resource) !== "none"),
    rolled: !!p.rolled,
    stateChanges: [
      ...g != null && g.ok ? [{ type: "combatState", actionId: s.id }] : [],
      ...p.stateChanges ?? []
    ],
    log: p.log ?? {
      title: s.label,
      message: xo(s, u)
    },
    costLabel: m.costLabel,
    value: p.value
  }) : { ...p, actionId: s.id, costPaid: !0, rolled: !!(p != null && p.rolled), stateChanges: (p == null ? void 0 : p.stateChanges) ?? [] };
}
const $1 = Object.freeze({
  execute: pM,
  buildPayloadFromDataset: cM,
  executeOwnedWeaponAttack: gM,
  removeActivationLogEntry: fM
});
function nS(t) {
  var e, n, i, a, r, s, o;
  return t ? typeof ((e = t == null ? void 0 : t.outcomeModel) == null ? void 0 : e.passed) == "boolean" ? t.outcomeModel.passed : typeof (t == null ? void 0 : t.passed) == "boolean" ? t.passed : typeof ((i = (n = t == null ? void 0 : t.resolved) == null ? void 0 : n.outcomeModel) == null ? void 0 : i.passed) == "boolean" ? t.resolved.outcomeModel.passed : typeof ((o = (s = (r = (a = t == null ? void 0 : t.flags) == null ? void 0 : a.mwd) == null ? void 0 : r.resolved) == null ? void 0 : s.outcomeModel) == null ? void 0 : o.passed) == "boolean" ? t.flags.mwd.resolved.outcomeModel.passed : !1 : !1;
}
async function _1(t, e, n, i) {
  var a, r, s;
  if (n.key === "reduceBurn")
    return D.reduceBurn(t, { token: e });
  if (n.key === "firstAid")
    return D.spendResource(t, {
      token: e,
      resource: "sa",
      cost: 2,
      actionId: "firstAid",
      actionLabel: "First Aid",
      actionCostLabel: "2 SA",
      actionCategory: "complex"
    });
  if (n.actionKind === "personalAction")
    return D.executeAction(t, {
      token: e,
      actionId: n.actionId,
      metadata: {
        personalCritId: i.id,
        remedyKey: n.key
      }
    });
  if (n.actionKind === "commonCheck") {
    const o = {
      ...Sk(n.actionId) ?? { intent: "common", id: n.actionId },
      dn: Math.max(1, Number(i.remedyBaseDn ?? n.baseDn ?? 1) || 1),
      personalCritRemedy: {
        critId: i.id,
        remedyKey: n.key
      }
    }, l = await ((s = (r = (a = game.mwd) == null ? void 0 : a.roll) == null ? void 0 : r.execute) == null ? void 0 : s.call(r, { actor: t, payload: o }));
    return l ? {
      ok: nS(l),
      message: l,
      reason: nS(l) ? "" : `${n.label} did not clear ${i.label}.`
    } : { ok: !0, cancelled: !0, reason: "Remedy roll cancelled." };
  }
  return { ok: !1, reason: "That critical has no remedy." };
}
async function hM({
  actor: t = null,
  token: e = null,
  critId: n = "",
  remedyKey: i = ""
} = {}) {
  if (!t) return { ok: !1, reason: "Personal critical remedy requires an actor." };
  const a = zr(t).find((l) => String(l.id) === String(n));
  if (!a) return { ok: !1, reason: "That critical effect is no longer active." };
  const r = Ow(i || a.remedyKey);
  if (!r.remediable) return { ok: !1, reason: "That critical cannot be remedied." };
  const s = await _1(t, e, r, a);
  return s != null && s.ok ? s.cancelled ? { ok: !0, cancelled: !0, spend: s } : {
    ...await m$({ actor: t, critId: a.id }),
    spend: s,
    remedy: r
  } : { ok: !1, reason: (s == null ? void 0 : s.reason) ?? "Unable to perform the remedy action.", spend: s };
}
const Mu = Symbol("SKIP_FIELD");
function B1(t) {
  return typeof t == "number" ? t : String(t ?? "").replace(/[,_\s]/g, "");
}
function bM(t) {
  return t instanceof HTMLInputElement || t instanceof HTMLSelectElement || t instanceof HTMLTextAreaElement;
}
function x1({
  elementKind: t = "input",
  inputType: e = "",
  dtype: n = "",
  value: i = "",
  checked: a = !1
} = {}) {
  const r = String(t ?? "").trim().toLowerCase(), s = String(e ?? "").trim().toLowerCase(), o = String(n ?? "").trim().toLowerCase();
  if (!["input", "select", "textarea"].includes(r))
    return Mu;
  if (r === "input") {
    if (s === "radio")
      return a ? i : Mu;
    if (s === "checkbox")
      return !!a;
  }
  if (o === "number" || r === "input" && s === "number") {
    const l = Number(B1(i));
    return Number.isFinite(l) ? l : 0;
  }
  return o === "boolean" ? i === !0 || i === "true" : i;
}
function z1(t) {
  var e;
  return bM(t) ? x1({
    elementKind: t instanceof HTMLSelectElement ? "select" : t instanceof HTMLTextAreaElement ? "textarea" : "input",
    inputType: t instanceof HTMLInputElement ? t.type : "",
    dtype: String(((e = t.dataset) == null ? void 0 : e.dtype) ?? ""),
    value: t.value,
    checked: t instanceof HTMLInputElement ? t.checked : !1
  }) : Mu;
}
function F1({
  root: t,
  document: e,
  selector: n = "input[name], select[name], textarea[name]",
  clampByPath: i = null,
  skipNames: a = []
} = {}) {
  if (!(t instanceof HTMLElement)) return {};
  const r = new Set(Array.isArray(a) ? a : [a]), s = {};
  for (const o of t.querySelectorAll(n)) {
    if (!bM(o) || o.closest("prose-mirror") || o.disabled) continue;
    const l = String(o.getAttribute("name") ?? o.name ?? "").trim();
    if (!l || r.has(l)) continue;
    let c = z1(o);
    c === Mu || (typeof i == "function" && (c = i(l, c)), (e ? foundry.utils.getProperty(e, l) : void 0) === c) || (s[l] = c);
  }
  return s;
}
const { HandlebarsApplicationMixin: U1 } = foundry.applications.api, { HTMLField: K1 } = foundry.data.fields;
function H1(t) {
  const e = new K1({ required: !1, blank: !0, initial: "" });
  return e.name = t, e;
}
var ca, Rl, Da, Ar, ai, Oa, kr, Tr, Li, ae, hi, jc, yM, Gf, SM, AM, Un, qf, Na, Wf, Gc, kM, Vf, Jf;
const Re = class Re extends U1(foundry.applications.sheets.ActorSheetV2) {
  constructor() {
    super(...arguments);
    Fe(this, ae);
    Fe(this, ca, !1);
    /** Track active CSB tab per group across rerenders */
    Fe(this, Rl, /* @__PURE__ */ new Map());
    // group -> tabId
    Fe(this, Da, /* @__PURE__ */ new Map());
    // group -> sectionId|null
    Fe(this, Ar, null);
    Fe(this, ai, null);
    Fe(this, Oa, null);
    Fe(this, kr, /* @__PURE__ */ new Set());
    Fe(this, Tr, []);
    Fe(this, Li, null);
  }
  /** @override */
  _updatePosition(n) {
    const i = super._updatePosition(n), {
      MIN_WIDTH: a,
      MAX_WIDTH: r,
      MIN_HEIGHT: s,
      MAX_HEIGHT: o
    } = this.constructor;
    return typeof i.width == "number" && (i.width = Math.min(
      r,
      Math.max(a, i.width)
    )), typeof i.height == "number" && (i.height = Math.min(
      o,
      Math.max(s, i.height)
    )), i;
  }
  // Legacy callers still probe defaultOptions directly, so keep the alias until
  // the remaining compatibility surfaces are gone.
  static get defaultOptions() {
    return this.DEFAULT_OPTIONS;
  }
  /** Editing mode flag for templates */
  get editing() {
    return X(this, ca);
  }
  async toggleEditing() {
    if (this.isEditable) {
      if (X(this, ca)) {
        await this._commitEditsToActor(), De(this, ca, !1), this.render({ force: !0 });
        return;
      }
      De(this, ca, !0), this.render({ force: !0 });
    }
  }
  /** Get the root HTMLElement for this application */
  _getRootElement() {
    var n;
    return this.element instanceof HTMLElement ? this.element : (n = this.element) == null ? void 0 : n[0];
  }
  _getPrimaryScroller() {
    const n = this._getRootElement();
    return n ? n.querySelector(".mwd-scroll-area") ?? n.querySelector(".csb-tab-panels") ?? n.querySelector(".window-content") : null;
  }
  _captureScrollPosition() {
    const n = this._getPrimaryScroller();
    if (!(n instanceof HTMLElement)) {
      De(this, Ar, null);
      return;
    }
    De(this, Ar, {
      top: n.scrollTop,
      left: n.scrollLeft
    });
  }
  _restoreScrollPosition() {
    const n = X(this, Ar);
    if (!n) return;
    const i = () => {
      const a = this._getPrimaryScroller();
      a instanceof HTMLElement && (a.scrollTop = n.top, a.scrollLeft = n.left);
    };
    i(), requestAnimationFrame(i), De(this, Ar, null);
  }
  _renderPreservingScroll(n = !1) {
    this._captureScrollPosition(), this.render(n);
  }
  _inventoryAccordionId(n, i) {
    return `${String(n ?? "").trim()}:${String(i ?? "").trim()}`;
  }
  _isInventoryRowExpanded(n) {
    return X(this, kr).has(String(n ?? "").trim());
  }
  _buildCombatActionsContext(n = {}) {
    const i = new Set((n.menus ?? []).map((a) => a.id));
    return X(this, ai) && !i.has(X(this, ai)) && De(this, ai, null), {
      ...n,
      menus: (n.menus ?? []).map((a) => ({
        ...a,
        isOpen: a.id === X(this, ai)
      }))
    };
  }
  /**
   * Resolve the TokenDocument that launched this sheet when one exists.
   * This keeps linked-token behavior aligned with Foundry's token API.
   */
  getSheetTokenDocument() {
    var r, s;
    const n = this.actor ?? this.document ?? null, i = (n == null ? void 0 : n.token) ?? null, a = (r = this.document) != null && r.isToken ? ((s = this.document) == null ? void 0 : s.token) ?? i ?? null : i;
    return a ? (a == null ? void 0 : a.document) ?? a : null;
  }
  /**
   * Resolve the document that should persist actor-backed state.
   * Linked token sheets should write to the base actor document so state survives scene changes.
   */
  getPersistentActor() {
    var a, r, s;
    const n = this.actor ?? this.document ?? null;
    if (!n) return null;
    const i = this.getSheetTokenDocument();
    return i != null && i.isLinked ? i.baseActor ?? ((s = (a = game.actors) == null ? void 0 : a.get) == null ? void 0 : s.call(a, ((r = i == null ? void 0 : i.baseActor) == null ? void 0 : r.id) ?? "")) ?? i.actor ?? n : n;
  }
  /** @override */
  _initializeApplicationOptions(n) {
    var o, l, c, u, d;
    n = super._initializeApplicationOptions(n), n.classes = Array.from(n.classes ?? []);
    const i = (n == null ? void 0 : n.document) ?? this.document, a = (i == null ? void 0 : i.type) ?? ((o = this.actor) == null ? void 0 : o.type);
    a && n.classes.push(String(a));
    const r = ((d = (u = (c = (l = game.system) == null ? void 0 : l.anarchy) == null ? void 0 : c.styles) == null ? void 0 : u.selectCssClass) == null ? void 0 : d.call(u)) ?? "mwd-theme-default", s = ["mwd-theme-default", "mwd-theme-sra"];
    for (let f = n.classes.length - 1; f >= 0; f--)
      s.includes(n.classes[f]) && n.classes.splice(f, 1);
    return n.classes.push(r), n;
  }
  /**
   * No localization: provide a concrete title so Foundry doesn't show "TYPES.Actor.<type>".
   * @override
   */
  get title() {
    var s, o;
    const n = ((s = this.actor) == null ? void 0 : s.type) ?? "actor", a = {
      character: "Character",
      npc: "NPC",
      battlemech: "BattleMech",
      vehicle: "Vehicle",
      device: "Device"
    }[n] ?? String(n).replace(/(^|[-_])([a-z])/g, (l, c, u) => (c ? " " : "") + u.toUpperCase());
    return `${((o = this.actor) == null ? void 0 : o.name) ?? "Actor"} — ${a}`;
  }
  /**
   * Window actions (header/menu). Centralize here and dedupe by action key.
   * @override
   */
  _getHeaderControls() {
    var s, o;
    let n = ((s = super._getHeaderControls) == null ? void 0 : s.call(this)) ?? [];
    const i = ((o = this.document) == null ? void 0 : o.isToken) ?? !1, a = /* @__PURE__ */ new Set();
    i ? (a.add("prototypeToken"), a.add("configurePrototypeToken")) : (a.add("token"), a.add("configureToken")), n = n.filter((l) => {
      const c = (l == null ? void 0 : l.action) ?? "", u = String((l == null ? void 0 : l.label) ?? "");
      return !(a.has(c) || i && u.includes("Prototype") || !i && u === "Token");
    });
    const r = /* @__PURE__ */ new Set();
    return n = n.filter((l) => {
      const c = l == null ? void 0 : l.action, u = c ? `a:${c}` : `il:${(l == null ? void 0 : l.icon) ?? ""}|${(l == null ? void 0 : l.label) ?? ""}`;
      return r.has(u) ? !1 : (r.add(u), !0);
    }), n;
  }
  /**
   * AppV2 action handler: Edit/View toggle.
   * Note: actions mapping already routes clicks here; we keep this lean.
   */
  async _onToggleViewMode(n) {
    var i;
    (i = n == null ? void 0 : n.preventDefault) == null || i.call(n), await this.toggleEditing();
  }
  /**
   * AppV2 action handler: CSB tab click.
   * Defensive: derive the tab link from target or event.
   */
  _onClickTab(n, i) {
    var l, c, u;
    const a = ((l = i == null ? void 0 : i.closest) == null ? void 0 : l.call(i, ".csb-tab-link[data-tab]")) ?? ((u = (c = n == null ? void 0 : n.target) == null ? void 0 : c.closest) == null ? void 0 : u.call(c, ".csb-tab-link[data-tab]"));
    if (!a) return;
    const r = a.dataset.tab, s = a.closest(".csb-tabs");
    if (!s || !r) return;
    const o = s.dataset.group || "default";
    X(this, Rl).set(o, r), E(this, ae, Vf).call(this, s, r);
  }
  _onClickAccordion(n, i) {
    var u, d, f;
    const a = ((u = i == null ? void 0 : i.closest) == null ? void 0 : u.call(i, ".csb-accordion__trigger[data-section]")) ?? ((f = (d = n == null ? void 0 : n.target) == null ? void 0 : d.closest) == null ? void 0 : f.call(d, ".csb-accordion__trigger[data-section]"));
    if (!a) return;
    const r = a.dataset.section, s = a.closest(".csb-accordion");
    if (!s || !r) return;
    const o = s.dataset.group || "default", c = (X(this, Da).has(o) ? X(this, Da).get(o) : s.dataset.default || null) === r ? null : r;
    X(this, Da).set(o, c), E(this, ae, Jf).call(this, s, c);
  }
  /**
   * Universal roll action: data-action="roll" + data-roll='{"intent":"skill","key":"gunnery"}'
   */
  async _onRollAction(n, i) {
    var c, u, d, f, m, g, p, h, b;
    (c = n == null ? void 0 : n.preventDefault) == null || c.call(n);
    const a = ((u = i == null ? void 0 : i.closest) == null ? void 0 : u.call(i, "[data-roll]")) ?? ((f = (d = n == null ? void 0 : n.target) == null ? void 0 : d.closest) == null ? void 0 : f.call(d, "[data-roll]")), r = (m = a == null ? void 0 : a.dataset) == null ? void 0 : m.roll;
    if (!r) return;
    let s;
    try {
      s = JSON.parse(r);
    } catch (y) {
      console.warn("MWD | Invalid data-roll JSON:", r, y);
      return;
    }
    const o = !!(n != null && n.shiftKey), l = ((g = game.mwd) == null ? void 0 : g.roll) ?? ((h = (p = game.system) == null ? void 0 : p.mwd) == null ? void 0 : h.roll);
    if (!(l != null && l.execute)) {
      (b = ui.notifications) == null || b.error("MWD roll system not initialized (game.mwd.roll.execute missing).");
      return;
    }
    try {
      return await l.execute({ actor: this.actor, payload: s, event: n, quick: o });
    } catch (y) {
      return console.error("MWD | Failed to execute roll action", y), Yt(y, "Unable to execute that roll."), null;
    }
  }
  async _onEditImage(n, i) {
    var s, o, l;
    if ((s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n), !this.isEditable || !this.editing) return;
    const a = foundry.applications.apps.FilePicker.implementation;
    new a({
      type: "image",
      current: ((l = this.actor) == null ? void 0 : l.img) ?? "",
      callback: async (c) => {
        if (!c) return;
        await (this.getPersistentActor() ?? this.actor).update({ img: c });
      }
    }).render(!0);
  }
  /* -------------------------------------------- */
  /* Shared Owned Item Actions                     */
  /* -------------------------------------------- */
  // The character sheet already has richer item affordances, but NPC, vehicle,
  // and battlemech sheets only need a stable baseline: create, open, delete,
  // and the two common loadout toggles.
  _getOwnedItemFromTarget(n, i) {
    var r, s, o, l, c, u, d, f, m, g, p;
    const a = String(
      ((r = n == null ? void 0 : n.dataset) == null ? void 0 : r.itemId) ?? ((l = (o = (s = n == null ? void 0 : n.closest) == null ? void 0 : s.call(n, "[data-item-id]")) == null ? void 0 : o.dataset) == null ? void 0 : l.itemId) ?? ((f = (d = (u = (c = i == null ? void 0 : i.target) == null ? void 0 : c.closest) == null ? void 0 : u.call(c, "[data-item-id]")) == null ? void 0 : d.dataset) == null ? void 0 : f.itemId) ?? ""
    ).trim();
    return a ? ((p = (g = (m = this.actor) == null ? void 0 : m.items) == null ? void 0 : g.get) == null ? void 0 : p.call(g, a)) ?? null : null;
  }
  _getItemTypeLabel(n = "") {
    const i = String(n ?? "").trim();
    return {
      personalWeapon: "Personal Weapon",
      mechWeapon: "Mech Weapon",
      assetModule: "Asset Module",
      lifeModule: "Life Module",
      consumable: "Consumable"
    }[i] ?? i.replace(/([a-z])([A-Z])/g, "$1 $2").replace(/^./, (r) => r.toUpperCase());
  }
  async _onCreateOwnedItem(n, i) {
    var o, l, c;
    if ((o = n == null ? void 0 : n.preventDefault) == null || o.call(n), (l = n == null ? void 0 : n.stopPropagation) == null || l.call(n), !this.isEditable) return;
    const a = String(((c = i == null ? void 0 : i.dataset) == null ? void 0 : c.itemType) ?? "").trim();
    if (!a) return;
    const r = this.getPersistentActor() ?? this.actor, s = r.items.filter((u) => (u.canonicalType ?? u.type) === a).length;
    await r.createEmbeddedDocuments("Item", [{
      name: `${this._getItemTypeLabel(a)} ${s + 1}`,
      type: a
    }]), this._renderPreservingScroll({ force: !0 });
  }
  async _onEditOwnedItem(n, i) {
    var r, s, o;
    (r = n == null ? void 0 : n.preventDefault) == null || r.call(n), (s = n == null ? void 0 : n.stopPropagation) == null || s.call(n);
    const a = this._getOwnedItemFromTarget(i, n);
    (o = a == null ? void 0 : a.sheet) == null || o.render(!0);
  }
  async _onDeleteOwnedItem(n, i) {
    var s, o;
    if ((s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n), !this.isEditable) return;
    const a = this._getOwnedItemFromTarget(i, n);
    if (!a) return;
    await (this.getPersistentActor() ?? this.actor).deleteEmbeddedDocuments("Item", [a.id]), this._renderPreservingScroll({ force: !0 });
  }
  async _onToggleOwnedItemEquipped(n, i) {
    var s, o, l, c;
    if ((s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n), !this.isEditable) return;
    const a = this._getOwnedItemFromTarget(i, n);
    if (!a) return;
    const r = this.getPersistentActor() ?? this.actor;
    await ((c = r.setOwnedItemEquipped) == null ? void 0 : c.call(r, a.id, !((l = a.system) != null && l.equipped))), this._renderPreservingScroll({ force: !0 });
  }
  async _onSetOwnedItemPrimary(n, i) {
    var s, o, l, c;
    if ((s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n), !this.isEditable) return;
    const a = this._getOwnedItemFromTarget(i, n);
    if (!a) return;
    const r = this.getPersistentActor() ?? this.actor;
    await ((c = r.setOwnedItemPrimary) == null ? void 0 : c.call(r, a.id, !((l = a.system) != null && l.isPrimary))), this._renderPreservingScroll({ force: !0 });
  }
  async _onToggleInventoryAccordion(n, i) {
    var r, s, o, l, c, u, d, f, m, g;
    (r = n == null ? void 0 : n.preventDefault) == null || r.call(n), (s = n == null ? void 0 : n.stopPropagation) == null || s.call(n);
    const a = String(
      ((o = i == null ? void 0 : i.dataset) == null ? void 0 : o.accordionId) ?? ((u = (c = (l = i == null ? void 0 : i.closest) == null ? void 0 : l.call(i, "[data-accordion-id]")) == null ? void 0 : c.dataset) == null ? void 0 : u.accordionId) ?? ((g = (m = (f = (d = n == null ? void 0 : n.target) == null ? void 0 : d.closest) == null ? void 0 : f.call(d, "[data-accordion-id]")) == null ? void 0 : m.dataset) == null ? void 0 : g.accordionId) ?? ""
    ).trim();
    a && (X(this, kr).has(a) ? X(this, kr).delete(a) : X(this, kr).add(a), this._renderPreservingScroll(!1));
  }
  async _onAdjustGearQuantity(n, i) {
    var c, u, d, f, m, g, p, h, b, y, S;
    if ((c = n == null ? void 0 : n.preventDefault) == null || c.call(n), (u = n == null ? void 0 : n.stopPropagation) == null || u.call(n), !this.isEditable) return;
    const a = this._getOwnedItemFromTarget(i, n);
    if (!a || !["gear", "consumable"].includes(String(a.canonicalType ?? a.type ?? "").trim())) return;
    const r = Math.trunc(Number(
      ((d = i == null ? void 0 : i.dataset) == null ? void 0 : d.delta) ?? ((g = (m = (f = i == null ? void 0 : i.closest) == null ? void 0 : f.call(i, "[data-delta]")) == null ? void 0 : m.dataset) == null ? void 0 : g.delta) ?? ((y = (b = (h = (p = n == null ? void 0 : n.target) == null ? void 0 : p.closest) == null ? void 0 : h.call(p, "[data-delta]")) == null ? void 0 : b.dataset) == null ? void 0 : y.delta) ?? 0
    ) || 0);
    if (!r) return;
    const o = (this.getPersistentActor() ?? this.actor).items.get(a.id) ?? a, l = Math.max(0, Math.trunc(Number(((S = o.system) == null ? void 0 : S.quantity) ?? 1) || 0) + r);
    await o.update({ "system.quantity": l }), this._renderPreservingScroll({ force: !0 });
  }
  async _onAttackWeapon(n, i) {
    var l, c, u, d;
    if ((l = n == null ? void 0 : n.preventDefault) == null || l.call(n), (c = n == null ? void 0 : n.stopPropagation) == null || c.call(n), this._notifyUnavailableAction(i, n, "Equip that weapon before attacking.")) return;
    const a = this._getOwnedItemFromTarget(i, n);
    if (!((u = a == null ? void 0 : a.isPersonalWeapon) != null && u.call(a))) return;
    const r = this.getPersistentActor() ?? this.actor, s = ((d = this.getSheetTokenDocument) == null ? void 0 : d.call(this)) ?? D.getCurrentSceneTokenDocument(r) ?? D.getCurrentSceneTokenDocument(this.actor), o = await gM({ weapon: a, event: n, token: s });
    o != null && o.ok && this._renderPreservingScroll({ force: !0 });
  }
  _notifyUnavailableAction(n, i, a = "That action is not available right now.") {
    var o, l, c, u, d;
    const r = ((o = n == null ? void 0 : n.closest) == null ? void 0 : o.call(n, "[data-action-disabled='true']")) ?? ((c = (l = i == null ? void 0 : i.target) == null ? void 0 : l.closest) == null ? void 0 : c.call(l, "[data-action-disabled='true']"));
    if (!r) return !1;
    const s = String(((u = r.dataset) == null ? void 0 : u.actionReason) ?? a).trim() || a;
    return (d = ui.notifications) == null || d.warn(s), !0;
  }
  async _onToggleCombatMenu(n, i) {
    var r, s, o, l, c, u, d;
    (r = n == null ? void 0 : n.preventDefault) == null || r.call(n), (s = n == null ? void 0 : n.stopPropagation) == null || s.call(n);
    const a = String(
      ((o = i == null ? void 0 : i.dataset) == null ? void 0 : o.combatMenu) ?? ((d = (u = (c = (l = n == null ? void 0 : n.target) == null ? void 0 : l.closest) == null ? void 0 : c.call(l, "[data-combat-menu]")) == null ? void 0 : u.dataset) == null ? void 0 : d.combatMenu) ?? ""
    ).trim();
    a && (De(this, ai, X(this, ai) === a ? null : a), this._renderPreservingScroll(!1));
  }
  async _onToggleStatuses(n, i) {
    var s, o, l, c, u, d, f, m;
    if ((s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n), this._notifyUnavailableAction(i, n, "Statuses are not available right now.") || !this.isEditable) return;
    const a = this.getPersistentActor() ?? this.actor, r = ((l = this.getSheetTokenDocument) == null ? void 0 : l.call(this)) ?? ((u = D.getSnapshot(a, { token: ((c = this.getSheetTokenDocument) == null ? void 0 : c.call(this)) ?? null })) == null ? void 0 : u.tokenDocument) ?? ((f = D.getSnapshot(this.actor, { token: ((d = this.getSheetTokenDocument) == null ? void 0 : d.call(this)) ?? null })) == null ? void 0 : f.tokenDocument) ?? null;
    if (!r) {
      (m = ui.notifications) == null || m.warn("Statuses require a token for this actor on the current scene.");
      return;
    }
    return Yu({ actor: a, token: r });
  }
  _getCombatActionToken(n = ((i) => (i = this.getPersistentActor) == null ? void 0 : i.call(this))() ?? this.actor) {
    var a;
    return ((a = this.getSheetTokenDocument) == null ? void 0 : a.call(this)) ?? D.getCurrentSceneTokenDocument(n) ?? D.getCurrentSceneTokenDocument(this.actor);
  }
  async _onCombatSpend(n, i) {
    return E(this, ae, hi).call(this, n, i, "combatSpend", "Unable to spend action.");
  }
  async _onCombatAction(n, i) {
    return E(this, ae, hi).call(this, n, i, "combatAction", "Unable to perform action.");
  }
  async _onCombatIntent(n, i) {
    return E(this, ae, hi).call(this, n, i, "combatIntent", "Unable to perform action.");
  }
  async _onRemoveActivationAction(n, i) {
    var r, s, o, l, c, u;
    if ((r = n == null ? void 0 : n.preventDefault) == null || r.call(n), (s = n == null ? void 0 : n.stopPropagation) == null || s.call(n), !this.isEditable) return;
    const a = Number(((o = i == null ? void 0 : i.dataset) == null ? void 0 : o.logIndex) ?? -1);
    if (!(!Number.isInteger(a) || a < 0))
      try {
        const d = this.getPersistentActor() ?? this.actor, f = await fM({
          actor: d,
          token: ((l = this.getSheetTokenDocument) == null ? void 0 : l.call(this)) ?? D.getCurrentSceneTokenDocument(d) ?? D.getCurrentSceneTokenDocument(this.actor),
          index: a
        });
        if (!(f != null && f.ok)) {
          (c = ui.notifications) == null || c.warn((f == null ? void 0 : f.reason) ?? "Unable to remove action.");
          return;
        }
        this._renderPreservingScroll({ force: !0 });
      } catch (d) {
        console.error("MWD | Failed to remove activation action", d), (u = ui.notifications) == null || u.error("Unable to remove action.");
      }
  }
  async _onCombatReduceBurn(n, i) {
    return E(this, ae, hi).call(this, n, i, "combatReduceBurn", "Unable to reduce Burn.");
  }
  async _onCombatAssist(n, i) {
    return E(this, ae, hi).call(this, n, i, "combatAssist", "Unable to assist.");
  }
  async _onCombatEvade(n, i) {
    return E(this, ae, hi).call(this, n, i, "combatEvade", "Unable to activate Evade.");
  }
  async _onCombatInterrupt(n, i) {
    return E(this, ae, hi).call(this, n, i, "combatInterrupt", "Unable to interrupt.");
  }
  async _onCombatOverloadCheck(n, i) {
    return E(this, ae, hi).call(this, n, i, "combatOverloadCheck", "Unable to launch overload check.");
  }
  async _onCombatAttack(n, i) {
    return E(this, ae, hi).call(this, n, i, "combatAttack", "Unable to launch attack.");
  }
  async _onCombatFirstAid(n, i) {
    return E(this, ae, hi).call(this, n, i, "combatFirstAid", "Unable to launch First Aid.");
  }
  async _onOpenCombatGadget(n) {
    var o, l, c, u, d, f;
    (o = n == null ? void 0 : n.preventDefault) == null || o.call(n), (l = n == null ? void 0 : n.stopPropagation) == null || l.call(n);
    const i = ((c = this.getPersistentActor) == null ? void 0 : c.call(this)) ?? this.actor, a = ((u = this.getSheetTokenDocument) == null ? void 0 : u.call(this)) ?? D.getCurrentSceneTokenDocument(i) ?? D.getCurrentSceneTokenDocument(this.actor), r = String((a == null ? void 0 : a.uuid) ?? "").trim(), s = (d = game.mwd) == null ? void 0 : d.playerGadget;
    if (!(s != null && s.render)) {
      (f = ui.notifications) == null || f.warn("Player Gadget is not available.");
      return;
    }
    r && s.selectSubject && await s.selectSubject(r), await s.render({ force: !0 }), await this.close();
  }
  async _onPersonalCritRemedy(n, i) {
    var s, o, l, c, u, d, f;
    if ((s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n), !this.isEditable) return;
    const a = String(((l = i == null ? void 0 : i.dataset) == null ? void 0 : l.critId) ?? "").trim(), r = String(((c = i == null ? void 0 : i.dataset) == null ? void 0 : c.remedyKey) ?? "").trim();
    if (a)
      try {
        const m = this.getPersistentActor() ?? this.actor, g = await hM({
          actor: m,
          token: ((u = this.getSheetTokenDocument) == null ? void 0 : u.call(this)) ?? D.getCurrentSceneTokenDocument(m) ?? D.getCurrentSceneTokenDocument(this.actor),
          critId: a,
          remedyKey: r
        });
        if (!(g != null && g.ok)) {
          (d = ui.notifications) == null || d.warn((g == null ? void 0 : g.reason) ?? "Unable to remedy personal critical.");
          return;
        }
        this._renderPreservingScroll({ force: !0 });
      } catch (m) {
        console.error("MWD | Failed to remedy personal critical", m), (f = ui.notifications) == null || f.error("Unable to remedy personal critical.");
      }
  }
  /**
   * Post-render reconciliation hook (stable DOM).
   * Ensures every .csb-tabs group has exactly one active tab/panel:
   * - prefer remembered selection
   * - else use data-default
   * - else use first tab link
   * @override
   */
  _onRender(n, i) {
    var r, s, o;
    (r = super._onRender) == null || r.call(this, n, i), E(this, ae, SM).call(this), E(this, ae, yM).call(this);
    const a = this._getRootElement();
    if (a) {
      for (const l of a.querySelectorAll(".csb-tabs")) {
        const c = l.dataset.group || "default", u = X(this, Rl).get(c), d = l.dataset.default || ((s = l.querySelector(".csb-tab-link[data-tab]")) == null ? void 0 : s.dataset.tab), f = u || d;
        f && E(this, ae, Vf).call(this, l, f);
      }
      for (const l of a.querySelectorAll(".csb-accordion")) {
        const c = l.dataset.group || "default", u = X(this, Da).has(c) ? X(this, Da).get(c) : l.dataset.default || null;
        E(this, ae, Jf).call(this, l, u);
      }
      a.querySelectorAll(".csb-tabs").length && !a.querySelector(".csb-tab-panel.is-active") && console.warn(`${tt} CSB tabs present but no active tab applied. Check element root resolution and CSS .is-active selectors.`, {
        sheet: (o = this.constructor) == null ? void 0 : o.name
      });
      for (const l of a.querySelectorAll('prose-mirror[name="system.biography.history"]'))
        l.addEventListener("change", (c) => {
          c.preventDefault(), this._updateRichTextHistory(l);
        });
      this._restoreScrollPosition();
    }
  }
  async close(n = {}) {
    return E(this, ae, Gf).call(this), E(this, ae, AM).call(this), super.close(n);
  }
  async _updateRichTextHistory(n) {
    if (!this.isEditable || (n == null ? void 0 : n.name) !== "system.biography.history") return;
    const i = String(n.value ?? ""), a = String(foundry.utils.getProperty(this.actor, "system.biography.history") ?? "");
    if (i !== a)
      try {
        await (this.getPersistentActor() ?? this.actor).update({ "system.biography.history": i });
      } catch (r) {
        console.warn("MWD | Rich text history update failed:", r);
      }
  }
  async _commitEditsToActor() {
    var a;
    const n = this._getRootElement();
    if (!n) return;
    const i = F1({
      root: n,
      document: this.actor,
      selector: 'input[name][data-edit-field="staged"], select[name][data-edit-field="staged"], textarea[name][data-edit-field="staged"]',
      clampByPath: this._clampByPath.bind(this),
      skipNames: ["system.biography.history"]
    });
    if (["vehicle", "battlemech"].includes((a = this.actor) == null ? void 0 : a.type) && i["system.movement.ground"] !== void 0 && (i["system.moves"] = i["system.movement.ground"]), !!Object.keys(i).length)
      try {
        await (this.getPersistentActor() ?? this.actor).update(i);
      } catch (r) {
        console.warn("MWD | Commit failed (permissions or validation):", r);
      }
  }
  /** @override */
  async _prepareContext(n) {
    var o, l, c, u, d, f, m, g, p, h, b, y, S, A, T, C, k, w, v, N, P, L;
    console.log(`${tt}BaseActorSheetV2._prepareContext:start`, {
      actorName: (o = this.actor) == null ? void 0 : o.name,
      actorType: (l = this.actor) == null ? void 0 : l.type
    });
    const i = await super._prepareContext(n), a = foundry.utils.deepClone((i == null ? void 0 : i.options) ?? {});
    a.classes = Array.from(((c = this.options) == null ? void 0 : c.classes) ?? []), a.cssClass = a.classes.join(" ");
    const r = foundry.utils.mergeObject(
      i,
      {
        actor: this.actor,
        system: (u = this.actor) == null ? void 0 : u.system,
        editable: this.isEditable,
        owner: ((d = this.document) == null ? void 0 : d.isOwner) ?? !1,
        limited: !(((f = this.document) == null ? void 0 : f.isOwner) ?? !1),
        editing: X(this, ca),
        data: this.actor,
        options: a,
        cssClass: a.cssClass
      },
      { inplace: !1 }
    );
    r.options.owner = r.owner, r.options.limited = r.limited, r.options.editable = r.editable, r.options.editing = r.editing, r.options.viewMode = !r.editing, r.skillsDisplay = ck(((m = this.actor) == null ? void 0 : m.system) ?? {}), r.bio = {
      ...r.bio ?? {},
      fields: {
        history: H1("system.biography.history")
      }
    };
    const s = ["battlemech", "vehicle"].includes(((g = this.actor) == null ? void 0 : g.type) ?? "");
    return r.machineHeader = {
      enabled: s,
      model: String(((b = (h = (p = this.actor) == null ? void 0 : p.system) == null ? void 0 : h.mwd) == null ? void 0 : b.model) ?? "").trim(),
      path: "system.mwd.model",
      placeholder: ((y = this.actor) == null ? void 0 : y.type) === "battlemech" ? "WHM-6R Warhammer" : "Vehicle model",
      cbillCost: Number(((T = (A = (S = this.actor) == null ? void 0 : S.system) == null ? void 0 : A.mwd) == null ? void 0 : T.cbillCost) ?? 0) || 0,
      cbillDisplay: new Intl.NumberFormat("en-US").format(Number(((w = (k = (C = this.actor) == null ? void 0 : C.system) == null ? void 0 : k.mwd) == null ? void 0 : w.cbillCost) ?? 0) || 0),
      cbillPath: "system.mwd.cbillCost"
    }, r.items ?? (r.items = {}), (v = this.actor) != null && v.items && typeof (ot == null ? void 0 : ot.classifyInto) == "function" && (ot.classifyInto(r.items, this.actor.items), r.items.weapon = [
      ...r.items.mechWeapon ?? [],
      ...r.items.personalWeapon ?? []
    ]), r.npcItems = {
      traits: r.items.quality ?? [],
      weapons: r.items.weapon ?? [],
      assetModules: r.items.assetModule ?? [],
      // Legacy partials still read npcItems.inventory, so fold consumables into
      // that alias until every remaining actor surface reads explicit buckets.
      inventory: [
        ...r.items.gear ?? [],
        ...r.items.consumable ?? []
      ]
    }, console.log(`${tt}BaseActorSheetV2._prepareContext:done`, {
      actorType: (N = this.actor) == null ? void 0 : N.type,
      cssClass: r.cssClass,
      itemCount: ((L = (P = this.actor) == null ? void 0 : P.items) == null ? void 0 : L.size) ?? 0,
      editing: X(this, ca)
    }), r;
  }
  /** Clamp certain actor system paths to valid ranges */
  _clampByPath(n, i) {
    return typeof i != "number" ? i : ((/^system\.skills\.[^.]+\.rating$/.test(n) || /^system\.attributes\.[^.]+\.value$/.test(n)) && (i = Math.trunc(i)), /^system\.skills\.[^.]+\.rating$/.test(n) ? Math.clamp(i, 0, 12) : /^system\.attributes\.[^.]+\.value$/.test(n) ? Math.clamp(i, 0, 10) : n === "system.speed" || /^system\.movement\.(ground|flight|jump)$/.test(n) ? Math.max(0, Math.trunc(i)) : i);
  }
  /** Action handler: Condition Monitor set */
  async _onMonitorSet(n, i) {
    var p, h, b, y, S, A;
    if (n.preventDefault(), (p = n.stopPropagation) == null || p.call(n), !this.isEditable) return;
    const a = ((h = i == null ? void 0 : i.closest) == null ? void 0 : h.call(i, "[data-action='monitorSet']")) ?? ((y = (b = n == null ? void 0 : n.target) == null ? void 0 : b.closest) == null ? void 0 : y.call(b, "[data-action='monitorSet']")) ?? i, r = String(((S = a == null ? void 0 : a.dataset) == null ? void 0 : S.monitor) ?? "").trim(), s = Number((A = a == null ? void 0 : a.dataset) == null ? void 0 : A.value);
    if (!r || !Number.isFinite(s)) return;
    this._captureScrollPosition();
    const o = r === "burn" ? "system.burn.value" : `system.monitors.${r}.value`, l = Number(foundry.utils.getProperty(this.actor, o) ?? 0), u = Ta(this.actor) && (r === "armor" || r === "structure") ? FR(l, s) : l === s ? 0 : s, d = this.getPersistentActor() ?? this.actor;
    if (typeof (d == null ? void 0 : d.setMonitorValue) == "function")
      return d.setMonitorValue(r, u, { source: "sheet" });
    const f = `system.monitors.${r}`, m = Number(foundry.utils.getProperty(d, `${f}.max`)) || 0, g = Math.min(Math.max(0, u), Math.max(0, m));
    return d.update({ [`${f}.value`]: g });
  }
  /**
  * Compute -1 penalty per 3 full damage (3,6,9...)
  * Returns 0, -1, -2, ...
  */
  static _mwdPenaltyFromDamage(n) {
    const i = Math.max(0, Number(n) || 0);
    return -Math.floor(i / 3);
  }
  /**
   * Compute resistance = ceil(value / 4), with 0 -> 0
   * 1-4 => 1, 5-8 => 2, ...
   */
  static _mwdResistanceFromValue(n) {
    const i = Math.max(0, Number(n) || 0);
    return i === 0 ? 0 : Math.ceil(i / 4);
  }
};
ca = new WeakMap(), Rl = new WeakMap(), Da = new WeakMap(), Ar = new WeakMap(), ai = new WeakMap(), Oa = new WeakMap(), kr = new WeakMap(), Tr = new WeakMap(), Li = new WeakMap(), ae = new WeakSet(), hi = async function(n, i, a, r = "Unable to perform action.") {
  var o, l, c;
  if ((o = n == null ? void 0 : n.preventDefault) == null || o.call(n), (l = n == null ? void 0 : n.stopPropagation) == null || l.call(n), this._notifyUnavailableAction(i, n, "That combat action is not available right now.") || !this.isEditable) return;
  const s = cM({ handler: a, target: i, event: n });
  if (s)
    try {
      const u = this.getPersistentActor() ?? this.actor, d = await pM({
        actor: u,
        token: this._getCombatActionToken(u),
        payload: s,
        event: n
      });
      if (d != null && d.cancelled) {
        this._renderPreservingScroll(!1);
        return;
      }
      if (!(d != null && d.ok)) {
        (c = ui.notifications) == null || c.warn((d == null ? void 0 : d.reason) ?? r);
        return;
      }
      E(this, ae, jc).call(this, { rerender: !1 }), this._renderPreservingScroll({ force: !0 });
    } catch (u) {
      console.error("MWD | Failed to execute combat intent", u), Yt(u, r);
    }
}, jc = function({ rerender: n = !0 } = {}) {
  X(this, ai) && (De(this, ai, null), n && this._renderPreservingScroll(!1));
}, yM = function() {
  E(this, ae, Gf).call(this), X(this, ai) && (De(this, Oa, (n) => {
    var r;
    const i = this._getRootElement();
    if (!i) return;
    const a = n.target;
    if (a instanceof Node && !((r = a.closest) != null && r.call(a, ".mwd-combat-menu"))) {
      if (!i.contains(a)) {
        E(this, ae, jc).call(this);
        return;
      }
      E(this, ae, jc).call(this);
    }
  }), document.addEventListener("click", X(this, Oa)));
}, Gf = function() {
  X(this, Oa) && (document.removeEventListener("click", X(this, Oa)), De(this, Oa, null));
}, SM = function() {
  var n;
  X(this, Tr).length || !((n = globalThis.Hooks) != null && n.on) || De(this, Tr, [
    ["targetToken", Hooks.on("targetToken", (i) => {
      var a;
      (i == null ? void 0 : i.id) === ((a = game.user) == null ? void 0 : a.id) && E(this, ae, Un).call(this);
    })],
    ["updateToken", Hooks.on("updateToken", (i, a) => {
      E(this, ae, kM).call(this, a) && E(this, ae, qf).call(this, i) && E(this, ae, Un).call(this);
    })],
    ["updateActor", Hooks.on("updateActor", (i) => {
      E(this, ae, Na).call(this, i) && E(this, ae, Un).call(this);
    })],
    ["createActiveEffect", Hooks.on("createActiveEffect", (i) => {
      E(this, ae, Na).call(this, i == null ? void 0 : i.parent) && E(this, ae, Un).call(this);
    })],
    ["updateActiveEffect", Hooks.on("updateActiveEffect", (i) => {
      E(this, ae, Na).call(this, i == null ? void 0 : i.parent) && E(this, ae, Un).call(this);
    })],
    ["deleteActiveEffect", Hooks.on("deleteActiveEffect", (i) => {
      E(this, ae, Na).call(this, i == null ? void 0 : i.parent) && E(this, ae, Un).call(this);
    })],
    ["createItem", Hooks.on("createItem", (i) => {
      E(this, ae, Na).call(this, i == null ? void 0 : i.parent) && E(this, ae, Un).call(this);
    })],
    ["updateItem", Hooks.on("updateItem", (i) => {
      E(this, ae, Na).call(this, i == null ? void 0 : i.parent) && E(this, ae, Un).call(this);
    })],
    ["deleteItem", Hooks.on("deleteItem", (i) => {
      E(this, ae, Na).call(this, i == null ? void 0 : i.parent) && E(this, ae, Un).call(this);
    })],
    ["updateCombatant", Hooks.on("updateCombatant", (i) => {
      E(this, ae, Gc).call(this, i) && E(this, ae, Un).call(this);
    })],
    ["createCombatant", Hooks.on("createCombatant", (i) => {
      E(this, ae, Gc).call(this, i) && E(this, ae, Un).call(this);
    })],
    ["deleteCombatant", Hooks.on("deleteCombatant", (i) => {
      E(this, ae, Gc).call(this, i) && E(this, ae, Un).call(this);
    })],
    ["updateCombat", Hooks.on("updateCombat", () => {
      E(this, ae, Un).call(this);
    })]
  ]);
}, AM = function() {
  for (const [n, i] of X(this, Tr))
    Hooks.off(n, i);
  De(this, Tr, []), X(this, Li) !== null && (clearTimeout(X(this, Li)), De(this, Li, null));
}, Un = function() {
  this.rendered && (X(this, Li) !== null && clearTimeout(X(this, Li)), De(this, Li, setTimeout(() => {
    De(this, Li, null), this.rendered && (this._captureScrollPosition(), this.render({ force: !1 }));
  }, 75)));
}, qf = function(n) {
  var o, l, c, u, d, f, m;
  const i = String((n == null ? void 0 : n.id) ?? ((o = n == null ? void 0 : n.document) == null ? void 0 : o.id) ?? "").trim();
  if (!i) return !1;
  const a = ((l = this.getSheetTokenDocument) == null ? void 0 : l.call(this)) ?? ((d = (u = (c = this.actor) == null ? void 0 : c.getActiveTokens) == null ? void 0 : u.call(c, !0, !0)) == null ? void 0 : d[0]) ?? null, r = String((a == null ? void 0 : a.id) ?? ((f = a == null ? void 0 : a.document) == null ? void 0 : f.id) ?? "").trim();
  return r && i === r ? !0 : new Set(
    Array.from(((m = game.user) == null ? void 0 : m.targets) ?? []).map((g) => {
      var p;
      return String((g == null ? void 0 : g.id) ?? ((p = g == null ? void 0 : g.document) == null ? void 0 : p.id) ?? "").trim();
    }).filter(Boolean)
  ).has(i);
}, Na = function(n) {
  var r, s;
  const i = E(this, ae, Wf).call(this, n), a = String((i == null ? void 0 : i.id) ?? "").trim();
  return a ? a === String(((r = this.actor) == null ? void 0 : r.id) ?? "").trim() ? !0 : Array.from(((s = game.user) == null ? void 0 : s.targets) ?? []).some((o) => {
    var c, u, d;
    const l = String(((c = o == null ? void 0 : o.actor) == null ? void 0 : c.id) ?? ((d = (u = o == null ? void 0 : o.document) == null ? void 0 : u.actor) == null ? void 0 : d.id) ?? "").trim();
    return l && l === a;
  }) : !1;
}, Wf = function(n) {
  return n ? n.documentName === "Actor" || n.type === "character" || n.type === "npc" || n.type === "vehicle" || n.type === "battlemech" ? n : n.actor ? n.actor : n.parent && n.parent !== n ? E(this, ae, Wf).call(this, n.parent) : null : null;
}, Gc = function(n) {
  var a, r, s;
  const i = String((n == null ? void 0 : n.tokenId) ?? ((a = n == null ? void 0 : n.token) == null ? void 0 : a.id) ?? ((s = (r = n == null ? void 0 : n.token) == null ? void 0 : r.document) == null ? void 0 : s.id) ?? "").trim();
  return i ? E(this, ae, qf).call(this, { id: i }) : !1;
}, kM = function(n) {
  return foundry.utils.hasProperty(n, "x") || foundry.utils.hasProperty(n, "y") || foundry.utils.hasProperty(n, "elevation") || foundry.utils.hasProperty(n, "actorId");
}, /**
 * Apply active-state classes within a single .csb-tabs root.
 * Idempotent: safe to call every render.
 */
Vf = function(n, i) {
  n.querySelectorAll(".csb-tab-link").forEach((a) => {
    a.classList.toggle("is-active", a.dataset.tab === i);
  }), n.querySelectorAll(".csb-tab-panel").forEach((a) => {
    a.classList.toggle("is-active", a.dataset.tab === i);
  });
}, Jf = function(n, i) {
  n.dataset.activeSection = i ?? "", n.querySelectorAll(".csb-accordion__section").forEach((a) => {
    const r = a.dataset.section === i;
    a.classList.toggle("is-active", r);
  }), n.querySelectorAll(".csb-accordion__trigger").forEach((a) => {
    const r = a.dataset.section === i;
    a.classList.toggle("is-active", r), a.setAttribute("aria-expanded", r ? "true" : "false");
  }), n.querySelectorAll(".csb-accordion__panel").forEach((a) => {
    const r = a.closest(".csb-accordion__section"), s = (r == null ? void 0 : r.dataset.section) === i;
    a.classList.toggle("is-active", s);
  });
}, // Shared size bounds keep the V2 actor sheets visually consistent while still
// allowing each subclass to request a slightly different preferred size.
F(Re, "MIN_WIDTH", 800), F(Re, "MAX_WIDTH", 950), F(Re, "MIN_HEIGHT", 600), F(Re, "MAX_HEIGHT", 1400), /** @override */
F(Re, "DEFAULT_OPTIONS", foundry.utils.mergeObject(vi(Re, Re, "DEFAULT_OPTIONS"), {
  classes: ["sheet", "actor", I, "appv2", "mwd-sheet", "mwd-character-sheet"],
  position: { width: 760, height: 760 },
  window: { resizable: !0, minimizable: !0 },
  actions: {
    toggleViewMode: Re.prototype._onToggleViewMode,
    tab: Re.prototype._onClickTab,
    accordion: Re.prototype._onClickAccordion,
    roll: Re.prototype._onRollAction,
    monitorSet: Re.prototype._onMonitorSet,
    editImage: Re.prototype._onEditImage,
    createOwnedItem: Re.prototype._onCreateOwnedItem,
    editOwnedItem: Re.prototype._onEditOwnedItem,
    deleteOwnedItem: Re.prototype._onDeleteOwnedItem,
    toggleOwnedItemEquipped: Re.prototype._onToggleOwnedItemEquipped,
    setOwnedItemPrimary: Re.prototype._onSetOwnedItemPrimary,
    toggleInventoryAccordion: Re.prototype._onToggleInventoryAccordion,
    adjustGearQuantity: Re.prototype._onAdjustGearQuantity,
    attackWeapon: Re.prototype._onAttackWeapon,
    toggleCombatMenu: Re.prototype._onToggleCombatMenu,
    toggleStatuses: Re.prototype._onToggleStatuses,
    combatIntent: Re.prototype._onCombatIntent,
    combatSpend: Re.prototype._onCombatSpend,
    combatAction: Re.prototype._onCombatAction,
    combatAssist: Re.prototype._onCombatAssist,
    combatEvade: Re.prototype._onCombatEvade,
    combatInterrupt: Re.prototype._onCombatInterrupt,
    combatFirstAid: Re.prototype._onCombatFirstAid,
    combatReduceBurn: Re.prototype._onCombatReduceBurn,
    combatOverloadCheck: Re.prototype._onCombatOverloadCheck,
    combatAttack: Re.prototype._onCombatAttack,
    personalCritRemedy: Re.prototype._onPersonalCritRemedy,
    removeActivationAction: Re.prototype._onRemoveActivationAction,
    openCombatGadget: Re.prototype._onOpenCombatGadget
  }
}, { inplace: !1 }));
let Al = Re;
function j1(t = {}) {
  switch (t.type) {
    case "stack":
      return "mwd.v2.ui.nodes.stack";
    case "hexabox":
      return "mwd.v2.ui.nodes.hexabox";
    case "panel":
      return "mwd.v2.ui.nodes.panel";
    case "include":
      return "mwd.v2.ui.nodes.include";
    case "tabs":
      return "mwd.v2.ui.nodes.tabs";
    case "accordion":
      return "mwd.v2.ui.nodes.accordion";
    default:
      return "mwd.v2.ui.nodes.unknown";
  }
}
function G1(t) {
  return Array.isArray(t) ? t : typeof t == "string" ? t.split(/\s+/).filter(Boolean) : [];
}
function qc(t) {
  if (!t || typeof t != "object") return t;
  const e = {
    ...t,
    template: t.template ?? j1(t),
    classes: G1(t.classes),
    children: Array.isArray(t.children) ? t.children.map(qc) : []
  };
  return t.type === "tabs" && (e.tabs = Array.isArray(t.tabs) ? t.tabs.map((n) => ({
    ...n,
    children: Array.isArray(n.children) ? n.children.map(qc) : []
  })) : []), t.type === "accordion" && (e.sections = Array.isArray(t.sections) ? t.sections.map((n) => ({
    ...n,
    children: Array.isArray(n.children) ? n.children.map(qc) : []
  })) : []), e;
}
function iS(t = {}) {
  return {
    ...t,
    root: qc(t.root ?? { type: "stack", children: [] })
  };
}
var La, xu, TM;
class Hr {
  static async get(e) {
    if (X(this, La).has(e)) {
      const a = await X(this, La).get(e);
      if (Number((a == null ? void 0 : a.version) ?? 0) > 0) return a;
      X(this, La).delete(e);
    }
    const n = E(this, xu, TM).call(this, e);
    X(this, La).set(e, n);
    const i = await n;
    return Number((i == null ? void 0 : i.version) ?? 0) <= 0 && X(this, La).delete(e), i;
  }
}
La = new WeakMap(), xu = new WeakSet(), TM = async function(e) {
  const n = `systems/${I}/templates/v2/layouts/${e}.layout.json`;
  try {
    const i = await fetch(n);
    if (!i.ok) throw new Error(`HTTP ${i.status} for ${n}`);
    return iS(await i.json());
  } catch (i) {
    return console.error(`${tt}LayoutRegistry.get FAILED`, { layoutId: e, url: n, error: i }), iS({
      id: e,
      version: 0,
      root: { type: "stack", children: [] }
    });
  }
}, Fe(Hr, xu), Fe(Hr, La, /* @__PURE__ */ new Map());
const q1 = 30, wM = Object.freeze({
  stationary: 0,
  moved1: 1,
  moved2: 2,
  moved3Plus: 3
}), W1 = Object.freeze([
  Object.freeze({ min: 0, max: 0, penalty: 0 }),
  Object.freeze({ min: 1, max: 2, penalty: -1 }),
  Object.freeze({ min: 3, max: 4, penalty: -2 }),
  Object.freeze({ min: 5, max: 6, penalty: -3 }),
  Object.freeze({ min: 7, max: 8, penalty: -4 }),
  Object.freeze({ min: 9, max: 1 / 0, penalty: -5 })
]), No = Object.freeze({
  stationary: "Stationary",
  moved1: "Moved 1",
  moved2: "Moved 2",
  moved3Plus: "Moved 3+"
}), V1 = Object.freeze({
  walk: "moved1",
  fly: "moved1",
  jump: "moved1",
  run: "moved2",
  sprint: "moved3Plus"
}), J1 = /* @__PURE__ */ new Set([M.actorTypes.vehicle, M.actorTypes.battlemech]);
function MM(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function Yf(t, e = 0) {
  return Math.max(0, Math.trunc(MM(t, e)));
}
function xt(t = null) {
  return J1.has(t == null ? void 0 : t.type);
}
function Y1(t = 0) {
  return Math.max(0, Math.round(MM(t, 0) / q1));
}
function Q1(t = 0) {
  var n;
  const e = Yf(t, 0);
  return ((n = W1.find((i) => e >= i.min && e <= i.max)) == null ? void 0 : n.penalty) ?? 0;
}
function jr(t = "stationary") {
  const e = String(t ?? "").trim();
  return Object.prototype.hasOwnProperty.call(wM, e) ? e : "stationary";
}
function X1(t = "stationary") {
  return wM[jr(t)] ?? 0;
}
function hs(t = {}, e = "") {
  return t && typeof t == "object" && Object.prototype.hasOwnProperty.call(t, e);
}
function Z1(t = null) {
  var e, n, i;
  return t ? ((e = t.getFlag) == null ? void 0 : e.call(t, "mwd", "personalCombat")) ?? ((i = (n = t.flags) == null ? void 0 : n.mwd) == null ? void 0 : i.personalCombat) ?? {} : {};
}
function vM(t = null, { combat: e = ((n) => (n = globalThis.game) == null ? void 0 : n.combat)() } = {}) {
  var o, l;
  const i = ((l = (o = Z1(t)) == null ? void 0 : o.actionState) == null ? void 0 : l.move) ?? null;
  if (!i || typeof i != "object" || i.moved === !1) return null;
  const a = Number((e == null ? void 0 : e.round) ?? 0), r = Number(i.round ?? 0);
  if (a > 0 && r > 0 && a !== r) return null;
  const s = String(i.movementKind ?? "").trim();
  return {
    targetMotion: V1[s] ?? "moved1",
    jumped: s === "jump",
    movementKind: s
  };
}
function eF(t = null) {
  var i, a;
  if (!xt(t)) return 0;
  const e = $l(((i = t.system) == null ? void 0 : i.movement) ?? {}, {
    actorType: t.type,
    legacyMoves: (a = t.system) == null ? void 0 : a.moves
  }), n = Rr(t);
  return Math.max(
    _r(Yf(e.ground, 0), n.movementPenalty, { immobile: n.immobile }),
    _r(Yf(e.flight, 0), n.movementPenalty, { immobile: n.immobile })
  );
}
function CM(t = {}, { targetCombatant: e = null } = {}) {
  const n = t != null && t.machineMotion && typeof t.machineMotion == "object" ? t.machineMotion : {}, i = vM(e), a = hs(n, "targetMotion") || hs(t, "targetMotion"), r = hs(n, "jumped") || hs(t, "targetJumped");
  return {
    targetMotion: jr(a ? n.targetMotion ?? (t == null ? void 0 : t.targetMotion) : i == null ? void 0 : i.targetMotion),
    jumped: !!(r ? n.jumped ?? (t == null ? void 0 : t.targetJumped) : i != null && i.jumped)
  };
}
function Ph({
  attackerCombatant: t = null,
  targetActor: e = null,
  targetCombatant: n = null,
  payload: i = {},
  suppressAttackerMotion: a = !1
} = {}) {
  const r = CM(i, { targetCombatant: n }), s = i != null && i.machineMotion && typeof i.machineMotion == "object" ? i.machineMotion : {}, o = a ? null : vM(t), l = !a && (hs(s, "attackerMotion") || hs(i, "attackerMotion")), c = a ? "stationary" : jr(l ? s.attackerMotion ?? (i == null ? void 0 : i.attackerMotion) : o == null ? void 0 : o.targetMotion), u = r.targetMotion, d = u !== "stationary", f = eF(e), m = d ? Y1(f) : 0, g = d ? Q1(m) : 0, p = r.jumped ? 1 : 0, h = r.jumped ? -1 : 0, b = X1(u), y = !a && c !== "stationary" ? 1 : 0;
  return {
    attackerMotion: c,
    attackerMotionLabel: No[c] ?? No.stationary,
    attackerMotionDn: y,
    attackerMotionSuppressed: !!a,
    targetMotion: u,
    targetMotionLabel: No[u] ?? No.stationary,
    jumped: r.jumped,
    speedMeters: f,
    trackingHexes: m,
    motionDn: b,
    jumpDn: p,
    dnModifier: y + b + p,
    movementTrackingPenalty: g,
    jumpTrackingPenalty: h,
    trackingPenalty: g + h
  };
}
function tF(t = "close") {
  const e = String(t ?? "").trim().toLowerCase();
  return e === "near" ? -2 : e === "far" ? -3 : e === "extreme" ? -4 : -1;
}
function er(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function Ds(t = 0) {
  const e = er(t, 0);
  return e >= 0 ? `+${e}` : String(e);
}
function Tn({ id: t = "", label: e = "", value: n = 0, title: i = "", source: a = "" } = {}) {
  const r = er(n, 0);
  return {
    id: String(t ?? "").trim(),
    label: String(e ?? "").trim() || "Modifier",
    value: r,
    valueLabel: Ds(r),
    title: String(i ?? "").trim(),
    source: String(a ?? "").trim()
  };
}
function Si(t = []) {
  return t.reduce((e, n) => e + er(n == null ? void 0 : n.value, 0), 0);
}
function nF(t = null) {
  var e;
  return String((t == null ? void 0 : t.id) ?? ((e = t == null ? void 0 : t.document) == null ? void 0 : e.id) ?? (t == null ? void 0 : t.tokenId) ?? "").trim();
}
function iF(t = null) {
  var e;
  return String(((e = t == null ? void 0 : t.document) == null ? void 0 : e.uuid) ?? (t == null ? void 0 : t.uuid) ?? "").trim();
}
function Jl(t = null) {
  var e;
  return (t == null ? void 0 : t.actor) ?? ((e = t == null ? void 0 : t.document) == null ? void 0 : e.actor) ?? null;
}
function aF(t = null) {
  var e, n;
  return String((t == null ? void 0 : t.name) ?? ((e = t == null ? void 0 : t.document) == null ? void 0 : e.name) ?? ((n = Jl(t)) == null ? void 0 : n.name) ?? "Target").trim() || "Target";
}
function rF(t = null, e = null) {
  var i, a, r, s, o;
  const n = (e == null ? void 0 : e.object) ?? e ?? null;
  return n || (((r = (a = (i = globalThis.canvas) == null ? void 0 : i.tokens) == null ? void 0 : a.controlled) == null ? void 0 : r.find((l) => {
    var c;
    return ((c = l == null ? void 0 : l.actor) == null ? void 0 : c.id) === (t == null ? void 0 : t.id);
  })) ?? ((o = (s = t == null ? void 0 : t.getActiveTokens) == null ? void 0 : s.call(t, !0, !0)) == null ? void 0 : o[0]) ?? null);
}
function sF(t = ((e) => (e = globalThis.game) == null ? void 0 : e.user)()) {
  return Array.from((t == null ? void 0 : t.targets) ?? []).map((n) => (n == null ? void 0 : n.object) ?? n).filter((n) => n && Jl(n));
}
function IM(t = null, e = "") {
  var i, a, r, s;
  const n = String(e ?? "").trim();
  return n ? Math.max(0, er(
    ((i = t == null ? void 0 : t.getAttributeValue) == null ? void 0 : i.call(t, n)) ?? ((s = (r = (a = t == null ? void 0 : t.system) == null ? void 0 : a.attributes) == null ? void 0 : r[n]) == null ? void 0 : s.value),
    0
  )) : 0;
}
function oF(t = null) {
  var n;
  const e = ((n = t == null ? void 0 : t.getPersonalCombatLoadout) == null ? void 0 : n.call(t)) ?? null;
  return (e == null ? void 0 : e.activeArmor) ?? null;
}
function lF(t = null) {
  const e = oF(t);
  return !e || e.isDestroyed ? 0 : Math.max(0, er(e.defenseBonus, 0));
}
function EM(t = null, { labelPrefix: e = "Target" } = {}) {
  if (!t) return [];
  const n = xt(t), i = n ? M.actorAttributes.handling : "reflexes", a = IM(t, i);
  return [
    Tn({
      id: n ? `${e.toLowerCase()}.handlingDefense` : `${e.toLowerCase()}.reflexesDefense`,
      label: n ? `${e} Handling + Handling` : `${e} REF + REF`,
      value: a + a
    })
  ];
}
function NM(t = null, { labelPrefix: e = "Target" } = {}) {
  const n = lF(t);
  return n ? [Tn({ id: `${e.toLowerCase()}.armorDefense`, label: "Armor Defense", value: n })] : [];
}
function cF(t = null, { rangeBand: e = "" } = {}) {
  var c, u;
  const n = [], i = [], a = EM(t, { labelPrefix: "Self" }), r = NM(t, { labelPrefix: "Self" });
  if (xt(t)) {
    const d = ll(t, { rangeBand: e });
    d.ar && n.push(Tn({
      id: "machineState.attackAr",
      label: "Machine State",
      value: d.ar
    }));
    const f = ll(t, { rangeBand: e });
    f.dr && i.push(Tn({
      id: "machineState.defenseDr",
      label: "Machine State",
      value: f.dr
    }));
    for (const m of uu(t, {
      payload: { intent: "attack" },
      resolved: { intent: "attack" }
    })) {
      const g = er((c = m.modifies) == null ? void 0 : c.ar, 0), p = er((u = m.modifies) == null ? void 0 : u.dr, 0);
      g && n.push(Tn({
        id: `assetModule.${m.sourceId}.${m.id}.ar`,
        label: m.label,
        value: g,
        source: m.sourceName
      })), p && i.push(Tn({
        id: `assetModule.${m.sourceId}.${m.id}.dr`,
        label: m.label,
        value: p,
        source: m.sourceName
      }));
    }
  }
  const s = Si(n), o = Si(i), l = Si(a) + Si(r) + o;
  return {
    arMods: n,
    arTotal: s,
    arTotalLabel: Ds(s),
    drBase: a,
    armorDefense: r,
    drMods: i,
    drTotal: o,
    drTotalLabel: Ds(o),
    drKnownTotal: l,
    drKnownTotalLabel: Ds(l)
  };
}
function uF(t = null) {
  return EM(t, { labelPrefix: "Target" });
}
function dF(t = null, { rangeBand: e = "" } = {}) {
  var o;
  const n = Jl(t);
  if (!n) return null;
  const i = uF(n), a = NM(n, { labelPrefix: "Target" }), r = [];
  if (xt(n)) {
    const l = ll(n, { rangeBand: e });
    l.dr && r.push(Tn({
      id: "machineState.defenseDr",
      label: "Machine State",
      value: l.dr
    }));
    for (const c of uu(n, {
      payload: { intent: "attack" },
      resolved: { intent: "attack" }
    })) {
      const u = er((o = c.modifies) == null ? void 0 : o.dr, 0);
      u && r.push(Tn({
        id: `assetModule.${c.sourceId}.${c.id}.dr`,
        label: c.label,
        value: u,
        source: c.sourceName
      }));
    }
  }
  const s = Si(i) + Si(a) + Si(r);
  return {
    name: aF(t),
    actorType: String((n == null ? void 0 : n.type) ?? "").trim(),
    actorTypeLabel: ce((n == null ? void 0 : n.type) ?? ""),
    defenseBase: i,
    armorDefense: a,
    drMods: r,
    drTotalKnown: s,
    drTotalKnownLabel: Ds(s)
  };
}
function mF(t = null, e = null, n = null) {
  var k, w, v, N, P, L, _, O;
  if (!xt(t) || !n) return null;
  const i = Jl(n), a = xl(e, n), r = String(((v = (w = (k = globalThis.canvas) == null ? void 0 : k.scene) == null ? void 0 : w.grid) == null ? void 0 : v.units) ?? ((L = (P = (N = globalThis.game) == null ? void 0 : N.system) == null ? void 0 : P.grid) == null ? void 0 : L.units) ?? "").trim(), s = Number.isFinite(a) ? md(a, "close") : "close", o = Hp(s), l = mw(s, 1), c = nF(n), u = iF(n), d = ao(c), f = Pn(e), m = Ph({
    attackerCombatant: f,
    targetActor: i,
    targetCombatant: d,
    payload: {}
  }), g = f && u ? ro(f, u) : "", p = IM(t, M.actorAttributes.system), h = f && u ? Xp(f, u, p, g, (O = (_ = globalThis.game) == null ? void 0 : _.combat) == null ? void 0 : O.round) : null, b = Qp(i, d), y = [];
  b && y.push(Tn({
    id: "ew.trackingPenalty",
    label: "Tracking Penalty",
    value: -b,
    source: "EW"
  })), m.movementTrackingPenalty && y.push(Tn({
    id: "machineMotion.tracking",
    label: `Target Movement (${Number(m.trackingHexes ?? 0)} hex)`,
    value: m.movementTrackingPenalty,
    source: "Motion"
  })), m.jumpTrackingPenalty && y.push(Tn({
    id: "machineMotion.jumpTracking",
    label: "Target Jumped",
    value: m.jumpTrackingPenalty,
    source: "Motion"
  }));
  const S = [
    m.attackerMotionLabel && m.attackerMotion !== "stationary" ? `Attacker: ${m.attackerMotionLabel}` : "",
    m.targetMotionLabel ? `Motion: ${m.targetMotionLabel}` : "",
    Number.isFinite(a) ? `Range: ${Mf(a, r)}` : "",
    m.trackingHexes ? `Tracking: ${m.trackingHexes} hex` : "",
    m.jumped ? "Jumped" : ""
  ].filter(Boolean), A = [Tn({ id: "difficulty.current", label: `Base DN (${o})`, value: l })], T = [
    m.attackerMotionDn ? Tn({ id: "machineMotion.attacker", label: `Attacker Motion (${m.attackerMotionLabel})`, value: m.attackerMotionDn }) : null,
    m.motionDn ? Tn({ id: "machineMotion.actions", label: `Target Motion (${m.targetMotionLabel})`, value: m.motionDn }) : null,
    m.jumpDn ? Tn({ id: "machineMotion.jump", label: "Target Jumped", value: m.jumpDn }) : null
  ].filter(Boolean), C = Si(A) + Si(T);
  return {
    rangeBand: s,
    rangeBandLabel: o,
    distance: a,
    distanceLabel: Number.isFinite(a) ? Mf(a, r) : "",
    rangeDn: A,
    motionDn: T,
    dnTotal: C,
    dnTotalLabel: String(C),
    trackingPenalty: y,
    trackingPenaltyTotal: Si(y),
    trackingPenaltyTotalLabel: Ds(Si(y)),
    detectionState: g,
    detectionStateLabel: g ? wn(g) : "",
    targetingDataSummary: h ? `+${Number(h.value ?? 0)} available` : "None available",
    motionLabels: S
  };
}
function Ed(t = null, {
  sourceToken: e = null,
  targetTokens: n = null,
  user: i = ((a) => (a = globalThis.game) == null ? void 0 : a.user)()
} = {}) {
  const r = Array.isArray(n) ? n.filter((g) => g && Jl(g)) : sF(i), s = r.length === 0 ? "none" : r.length === 1 ? "single" : "multiple", o = rF(t, e), l = s === "single" ? r[0] : null, c = l ? mF(t, o, l) : null, u = (c == null ? void 0 : c.rangeBand) ?? "", d = cF(t, { rangeBand: u }), f = l ? dF(l, { rangeBand: u }) : null, m = [];
  return s === "none" && m.push("Select one target for DN, tracking, and target defense."), s === "multiple" && m.push("Select one target for detailed CQ awareness."), l && xt(t) && !o && m.push("Select or open a scene token for machine range and EW context."), {
    targetState: s,
    self: d,
    target: f,
    machineContext: c,
    warnings: m
  };
}
const Qf = "advancement", vu = Object.freeze([
  "strength",
  "reflexes",
  "guts",
  "intelligence",
  "charisma",
  "edge"
]), Cu = Object.freeze([
  "grit",
  "chaos",
  "insight",
  "rumor",
  "legend",
  "credibility"
]), Me = Object.freeze({
  attribute: "attribute",
  skill: "skill",
  edgePool: "edgePool",
  traitAdd: "traitAdd",
  traitRemove: "traitRemove",
  specializationAdd: "specializationAdd",
  specializationChange: "specializationChange",
  knowledgeSkillAdd: "knowledgeSkillAdd",
  knowledgeSkillRemove: "knowledgeSkillRemove"
}), aS = Object.freeze([
  { id: "green", label: "Green", attributePoints: 5, skillPoints: 10, edgeBonus: 0 },
  { id: "regular", label: "Regular", attributePoints: 8, skillPoints: 14, edgeBonus: 2 },
  { id: "veteran", label: "Veteran", attributePoints: 11, skillPoints: 18, edgeBonus: 4 },
  { id: "elite", label: "Elite", attributePoints: 14, skillPoints: 22, edgeBonus: 6 }
]), rS = new Map(tn.map((t) => [t.code, t])), PM = Object.freeze({
  strength: "Strength",
  reflexes: "Reflexes",
  guts: "Guts",
  intelligence: "Intelligence",
  charisma: "Charisma",
  edge: "Edge"
}), RM = Object.freeze({
  grit: "Grit",
  chaos: "Chaos",
  insight: "Insight",
  rumor: "Rumor",
  legend: "Legend",
  credibility: "Credibility"
});
function fF(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function gF(t, e = 0) {
  return Math.trunc(fF(t, e));
}
function it(t, e = 0) {
  return Math.max(0, gF(t, e));
}
function yi(t = "") {
  return String(t ?? "").trim();
}
function zo(t = []) {
  const e = Array.isArray(t) ? t : typeof t == "string" ? t.split(",") : [], n = /* @__PURE__ */ new Set(), i = [];
  for (const a of e) {
    const r = yi(a), s = r.toLocaleLowerCase();
    !r || n.has(s) || (n.add(s), i.push(r));
  }
  return i;
}
function pF(t = "") {
  const e = yi(t), n = e.includes(".") ? e.split(".").pop() : e;
  return Cu.includes(n) ? n : "";
}
function hF(t = "") {
  return yi(t);
}
function Rh(t = null) {
  const e = t == null ? void 0 : t.items;
  return e ? Array.isArray(e) ? e : Array.isArray(e.contents) ? e.contents : typeof e[Symbol.iterator] == "function" ? Array.from(e) : [] : [];
}
function bF(t = null, e = "") {
  const n = yi(e);
  return n ? Rh(t).find((i) => (i == null ? void 0 : i.uuid) === n || (i == null ? void 0 : i.id) === n || (i == null ? void 0 : i._id) === n) ?? null : null;
}
function yF(t = null) {
  return Rh(t).filter((e) => ((e == null ? void 0 : e.canonicalType) ?? (e == null ? void 0 : e.type)) === "quality");
}
function Xf(t = null) {
  const e = (t == null ? void 0 : t.system) ?? {}, n = yi(e.category);
  return n || (e.positive === !1 ? "negative" : "positive");
}
function Ei({ intent: t, label: e = "", cost: n = 0, updates: i = {}, itemCreates: a = [], itemDeletes: r = [], itemUpdates: s = {}, errors: o = [], warnings: l = [], from: c = null, to: u = null } = {}) {
  return {
    intent: t,
    label: e,
    cost: it(n),
    from: c,
    to: u,
    legal: o.length === 0,
    errors: o,
    warnings: l,
    updates: i,
    itemCreates: a,
    itemDeletes: r,
    itemUpdates: s
  };
}
function Dh(t = {}) {
  var a, r, s, o;
  const e = (t == null ? void 0 : t.system) ?? t ?? {}, n = it((r = (a = e == null ? void 0 : e.counters) == null ? void 0 : a.xp) == null ? void 0 : r.total, 0), i = it((o = (s = e == null ? void 0 : e.counters) == null ? void 0 : s.xp) == null ? void 0 : o.value, 0);
  return {
    earned: n,
    spent: i,
    available: Math.max(0, n - i)
  };
}
function rs(t, e) {
  if (!t.legal) return t;
  const n = Dh(e).available;
  return t.cost > n && (t.legal = !1, t.errors.push(`Not enough XP available (${t.cost} needed, ${n} available).`)), t;
}
function ss(t, e = 0) {
  const n = it(e, 0);
  switch (t) {
    case Me.attribute:
      return n * 5;
    case Me.skill:
      return n * 3;
    case Me.edgePool:
      return n * 2;
    case Me.traitAdd:
    case Me.traitRemove:
      return 6;
    case Me.specializationAdd:
      return 4;
    case Me.specializationChange:
      return 2;
    default:
      return 0;
  }
}
function SF(t, e) {
  return { "system.counters.xp.value": Dh(t).spent + it(e, 0) };
}
function AF(t = {}) {
  return {
    ...t,
    type: yi(t == null ? void 0 : t.type),
    target: yi(t == null ? void 0 : t.target),
    from: t == null ? void 0 : t.from,
    to: t == null ? void 0 : t.to,
    label: yi(t == null ? void 0 : t.label),
    specializationKey: yi((t == null ? void 0 : t.specializationKey) ?? (t == null ? void 0 : t.to)),
    specializationLabel: yi(t == null ? void 0 : t.specializationLabel)
  };
}
function kF(t = {}) {
  var e;
  return t.counters ?? (t.counters = {}), (e = t.counters).xp ?? (e.xp = {}), t.counters.xp.total = it(t.counters.xp.total, 0), t.counters.xp.value = it(t.counters.xp.value, 0), t.knowledgeSkills = zo(t.knowledgeSkills), t;
}
function bc(t = {}) {
  return Dh(t);
}
function TF(t, { tier: e = "" } = {}) {
  var p, h, b, y, S, A, T, C, k;
  const n = (t == null ? void 0 : t.system) ?? t ?? {}, i = aS.find((w) => {
    var v;
    return w.id === yi(e || ((v = n == null ? void 0 : n.biography) == null ? void 0 : v.experienceLevel));
  }) ?? aS[0], a = zo(n.knowledgeSkills), r = it((h = (p = n == null ? void 0 : n.attributes) == null ? void 0 : p.edge) == null ? void 0 : h.value, 1), s = vu.reduce((w, v) => {
    var N, P;
    return w + Math.max(0, it((P = (N = n == null ? void 0 : n.attributes) == null ? void 0 : N[v]) == null ? void 0 : P.value, 1) - 1);
  }, 0), o = tn.reduce((w, v) => {
    var N, P;
    return w + it((P = (N = n == null ? void 0 : n.skills) == null ? void 0 : N[v.code]) == null ? void 0 : P.rating, 0);
  }, Math.max(0, a.length - 1)), l = Cu.reduce((w, v) => {
    var N, P, L;
    return w + Math.max(0, it((L = (P = (N = n == null ? void 0 : n.counters) == null ? void 0 : N.edgePools) == null ? void 0 : P[v]) == null ? void 0 : L.rating, 1) - 1);
  }, 0), c = yF(t), u = c.filter((w) => Xf(w) === "positive").length, d = c.filter((w) => Xf(w) === "negative").length, f = Rh(t).filter((w) => ((w == null ? void 0 : w.canonicalType) ?? (w == null ? void 0 : w.type)) === "lifeModule").length, m = [
    { id: "attributes", label: "Attributes", used: s, budget: i.attributePoints },
    { id: "skills", label: "Skills", used: o, budget: i.skillPoints },
    { id: "edgePools", label: "Edge Pools", used: l, budget: r + i.edgeBonus }
  ].map((w) => ({
    ...w,
    remaining: w.budget - w.used,
    status: w.used > w.budget ? "over" : w.used === w.budget ? "complete" : "under"
  })), g = [];
  for (const w of m)
    w.used > w.budget && g.push(`${w.label} exceed ${i.label} budget by ${w.used - w.budget}.`);
  for (const w of vu)
    it((y = (b = n == null ? void 0 : n.attributes) == null ? void 0 : b[w]) == null ? void 0 : y.value, 1) > 4 && g.push(`${PM[w]} is above the starting cap of 4.`);
  for (const w of tn)
    it((A = (S = n == null ? void 0 : n.skills) == null ? void 0 : S[w.code]) == null ? void 0 : A.rating, 0) > 4 && g.push(`${w.label} is above the starting cap of 4.`);
  for (const w of Cu)
    it((k = (C = (T = n == null ? void 0 : n.counters) == null ? void 0 : T.edgePools) == null ? void 0 : C[w]) == null ? void 0 : k.rating, 1) > r && g.push(`${RM[w]} exceeds the Edge attribute cap.`);
  return a.length === 0 && g.push("Add at least one Knowledge Skill."), u < 1 && g.push("Add at least one positive trait."), d < 1 && g.push("Add at least one negative trait."), f < 4 && g.push("Add four life modules."), {
    tier: { ...i },
    categories: m,
    warnings: g,
    complete: g.length === 0 && m.every((w) => w.status === "complete"),
    knowledgeSkills: a,
    traits: { positive: u, negative: d },
    lifeModules: { count: f, expected: 4 }
  };
}
function Os(t, e = {}, { mode: n = Qf } = {}) {
  var o, l, c, u, d, f, m, g, p, h, b, y, S, A, T, C;
  const i = AF(e);
  if (n !== Qf)
    return Ei({ intent: i, errors: ["XP purchases can only be committed in advancement mode."] });
  const a = (t == null ? void 0 : t.system) ?? {}, r = [], s = [];
  if (i.type === Me.attribute) {
    const k = i.target;
    vu.includes(k) || r.push("Unknown attribute.");
    const w = it((l = (o = a == null ? void 0 : a.attributes) == null ? void 0 : o[k]) == null ? void 0 : l.value, 1), v = it(i.to, w + 1);
    v <= w && r.push("Attribute purchases must increase the rating."), v > 6 && r.push("Attributes cannot be advanced above 6.");
    const N = ss(Me.attribute, v), P = Ei({
      intent: i,
      label: `${PM[k] ?? k} ${w} -> ${v}`,
      cost: N,
      from: w,
      to: v,
      errors: r,
      warnings: s,
      updates: { [`system.attributes.${k}.value`]: v }
    });
    return rs(P, t);
  }
  if (i.type === Me.skill) {
    const k = i.target, w = rS.get(k);
    w || r.push("Unknown skill.");
    const v = it((u = (c = a == null ? void 0 : a.skills) == null ? void 0 : c[k]) == null ? void 0 : u.rating, 0), N = it(i.to, v + 1);
    N <= v && r.push("Skill purchases must increase the rating."), N > 6 && r.push("Skills cannot be advanced above 6.");
    const P = ss(Me.skill, N), L = Ei({
      intent: i,
      label: `${(w == null ? void 0 : w.label) ?? k} ${v} -> ${N}`,
      cost: P,
      from: v,
      to: N,
      errors: r,
      warnings: s,
      updates: { [`system.skills.${k}.rating`]: N }
    });
    return rs(L, t);
  }
  if (i.type === Me.edgePool) {
    const k = pF(i.target);
    k || r.push("Unknown Edge pool.");
    const w = it((f = (d = a == null ? void 0 : a.attributes) == null ? void 0 : d.edge) == null ? void 0 : f.value, 1), v = it((p = (g = (m = a == null ? void 0 : a.counters) == null ? void 0 : m.edgePools) == null ? void 0 : g[k]) == null ? void 0 : p.rating, 1), N = it((y = (b = (h = a == null ? void 0 : a.counters) == null ? void 0 : h.edgePools) == null ? void 0 : b[k]) == null ? void 0 : y.value, 0), P = it(i.to, v + 1);
    P <= v && r.push("Edge pool purchases must increase the rating."), P > w && r.push("Edge pool ratings cannot exceed the Edge attribute.");
    const L = ss(Me.edgePool, P), _ = Ei({
      intent: { ...i, target: k },
      label: `${RM[k] ?? k} ${v} -> ${P}`,
      cost: L,
      from: v,
      to: P,
      errors: r,
      warnings: s,
      updates: {
        [`system.counters.edgePools.${k}.rating`]: P,
        [`system.counters.edgePools.${k}.value`]: Math.min(N, Math.min(P, w))
      }
    });
    return rs(_, t);
  }
  if (i.type === Me.specializationAdd || i.type === Me.specializationChange) {
    const k = i.target, w = rS.get(k);
    w || r.push("Unknown skill."), it((A = (S = a == null ? void 0 : a.skills) == null ? void 0 : S[k]) == null ? void 0 : A.rating, 0) < 2 && r.push("Specializations require skill rating 2+.");
    const N = Xo(((C = (T = a == null ? void 0 : a.skills) == null ? void 0 : T[k]) == null ? void 0 : C.specializations) ?? []), P = N[0] ?? "";
    i.type === Me.specializationAdd && P && r.push("This skill already has a specialization."), i.type === Me.specializationChange && !P && r.push("There is no specialization to change.");
    const _ = Hi(k).find((H) => H.key === i.specializationKey) ?? (i.specializationKey ? { key: i.specializationKey, label: i.specializationLabel || i.specializationKey } : null);
    _ != null && _.key || r.push("Choose a specialization."), _ != null && _.key && _.key === P && r.push("Choose a different specialization.");
    const O = ss(i.type), x = Ei({
      intent: { ...i, specializationKey: (_ == null ? void 0 : _.key) ?? "", specializationLabel: (_ == null ? void 0 : _.label) ?? "" },
      label: `${(w == null ? void 0 : w.label) ?? k}: ${(_ == null ? void 0 : _.label) ?? "Specialization"}`,
      cost: O,
      from: P,
      to: (_ == null ? void 0 : _.key) ?? "",
      errors: r,
      warnings: s,
      updates: { [`system.skills.${k}.specializations`]: _ != null && _.key ? [_.key] : N }
    });
    return rs(x, t);
  }
  if (i.type === Me.traitAdd) {
    const k = i.label || i.target || "Positive Trait", w = ss(Me.traitAdd), v = Ei({
      intent: i,
      label: `Add ${k}`,
      cost: w,
      itemCreates: [{
        name: k,
        type: "quality",
        system: { category: "positive", positive: !0, tier: "minor" }
      }]
    });
    return rs(v, t);
  }
  if (i.type === Me.traitRemove) {
    const k = bF(t, hF(i.target));
    k || r.push("Trait not found."), k && Xf(k) !== "negative" && r.push("Only negative traits can be removed with this purchase.");
    const w = ss(Me.traitRemove), v = Ei({
      intent: i,
      label: `Remove ${(k == null ? void 0 : k.name) ?? "Negative Trait"}`,
      cost: w,
      itemDeletes: k ? [k.id ?? k._id] : [],
      errors: r
    });
    return rs(v, t);
  }
  if (i.type === Me.knowledgeSkillAdd) {
    const k = i.label || i.target, w = zo(a.knowledgeSkills);
    return k || r.push("Knowledge skill name is required."), w.some((v) => v.toLocaleLowerCase() === k.toLocaleLowerCase()) && r.push("That Knowledge Skill already exists."), Ei({
      intent: i,
      label: `Add ${k}`,
      cost: 0,
      errors: r,
      warnings: ["No automated post-creation XP cost is defined for Knowledge Skills."],
      updates: { "system.knowledgeSkills": zo([...w, k]) }
    });
  }
  if (i.type === Me.knowledgeSkillRemove) {
    const k = i.label || i.target, w = zo(a.knowledgeSkills), v = w.filter((N) => N.toLocaleLowerCase() !== k.toLocaleLowerCase());
    return (!k || v.length === w.length) && r.push("Knowledge skill not found."), Ei({
      intent: i,
      label: `Remove ${k}`,
      cost: 0,
      errors: r,
      updates: { "system.knowledgeSkills": v }
    });
  }
  return Ei({ intent: i, errors: ["Unknown advancement purchase type."] });
}
function wF(t) {
  var i, a, r, s, o, l, c;
  const e = (t == null ? void 0 : t.system) ?? {}, n = [];
  for (const u of vu) {
    const d = it((a = (i = e == null ? void 0 : e.attributes) == null ? void 0 : i[u]) == null ? void 0 : a.value, 1);
    d < 6 && n.push(Os(t, { type: Me.attribute, target: u, to: d + 1 }));
  }
  for (const u of tn) {
    const d = it((s = (r = e == null ? void 0 : e.skills) == null ? void 0 : r[u.code]) == null ? void 0 : s.rating, 0);
    d < 6 && n.push(Os(t, { type: Me.skill, target: u.code, to: d + 1 }));
  }
  for (const u of Cu) {
    const d = it((c = (l = (o = e == null ? void 0 : e.counters) == null ? void 0 : o.edgePools) == null ? void 0 : l[u]) == null ? void 0 : c.rating, 1);
    n.push(Os(t, { type: Me.edgePool, target: u, to: d + 1 }));
  }
  return n;
}
async function cr(t, e = {}) {
  if (!(t != null && t.update)) throw new Error("commitPurchase requires an updatable actor.");
  const n = Os(t, e, { mode: Qf });
  if (!n.legal) {
    const a = new Error(n.errors[0] ?? "Advancement purchase is not legal.");
    throw a.preview = n, a;
  }
  if (n.itemCreates.length) {
    if (!t.createEmbeddedDocuments) throw new Error("Actor cannot create trait items.");
    await t.createEmbeddedDocuments("Item", n.itemCreates);
  }
  if (n.itemDeletes.length) {
    if (!t.deleteEmbeddedDocuments) throw new Error("Actor cannot delete trait items.");
    await t.deleteEmbeddedDocuments("Item", n.itemDeletes);
  }
  if (Object.keys(n.itemUpdates).length) {
    if (!t.updateEmbeddedDocuments) throw new Error("Actor cannot update embedded items.");
    await t.updateEmbeddedDocuments("Item", Object.values(n.itemUpdates));
  }
  const i = {
    ...n.updates,
    ...SF(t, n.cost)
  };
  return await t.update(i), {
    ok: !0,
    preview: n,
    updates: i
  };
}
function kl(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function Oh(t, e, n) {
  return Math.min(n, Math.max(e, t));
}
function MF(t = 0) {
  const e = Oh(kl(t, 0), 0, 100);
  return e >= 91 ? "green" : e >= 71 ? "yellow" : e >= 51 ? "orange" : e >= 31 ? "red" : "dark-red";
}
function sS(t, e = {}) {
  const n = Math.max(0, kl(e == null ? void 0 : e.max, 0)), i = Oh(kl(e == null ? void 0 : e.value, 0), 0, n), a = n > 0 ? i / n * 100 : 0;
  return {
    label: t,
    value: String(i),
    tone: MF(a),
    remaining: i,
    max: n,
    percent: a,
    title: `${i}/${n}`
  };
}
function vF({ armor: t = {}, structure: e = {} } = {}) {
  const n = [
    sS("A", t),
    sS("S", e)
  ];
  return {
    parts: n,
    title: `Armor ${n[0].title}; Structure ${n[1].title}`
  };
}
function Iu({
  id: t = "",
  label: e = "",
  kind: n = "wound",
  monitor: i = {},
  editable: a = !1
} = {}) {
  const r = Math.max(0, kl(i == null ? void 0 : i.max, 0)), s = Oh(kl(i == null ? void 0 : i.value, 0), 0, r);
  return {
    id: t,
    label: e,
    kind: n,
    editable: !!a,
    value: s,
    max: r,
    segments: Array.from({ length: r }, (o, l) => {
      const c = l + 1;
      return {
        value: c,
        filled: c <= s
      };
    })
  };
}
function CF(t = {}) {
  const e = String((t == null ? void 0 : t.label) ?? ce((t == null ? void 0 : t.key) ?? "Critical")).trim() || "Critical", n = String((t == null ? void 0 : t.locationLabel) ?? ce((t == null ? void 0 : t.locationKey) ?? "")).trim();
  return n ? `${e} (${n})` : e;
}
function DM(t = []) {
  const e = Array.isArray(t) ? t.filter((i) => i && i.active !== !1) : [], n = e.length;
  return {
    value: n === 0 ? "CLEAR" : n === 1 ? "1 CRIT" : `${n} CRITS`,
    title: e.map(CF).join("; "),
    count: n
  };
}
async function oS(t = "") {
  const e = String(t ?? "").trim();
  if (!e || typeof fromUuid != "function") return null;
  try {
    return await fromUuid(e);
  } catch {
    return null;
  }
}
async function Ut({
  machineActor: t = null,
  operatorActorUuid: e = ""
} = {}) {
  var s, o, l, c, u, d, f, m, g, p, h, b, y;
  const n = await oS(e);
  if (n)
    return { actor: n, uuid: n.uuid ?? e, source: "explicit", reason: "" };
  const i = String(
    ((o = (s = t == null ? void 0 : t.system) == null ? void 0 : s.pilot) == null ? void 0 : o.uuid) ?? ((u = (c = (l = t == null ? void 0 : t.system) == null ? void 0 : l.mwd) == null ? void 0 : c.pilot) == null ? void 0 : u.uuid) ?? ((m = (f = (d = t == null ? void 0 : t.system) == null ? void 0 : d.mwd) == null ? void 0 : f.crew) == null ? void 0 : m.operatorActorUuid) ?? ((h = (p = (g = t == null ? void 0 : t.system) == null ? void 0 : g.mwd) == null ? void 0 : p.crew) == null ? void 0 : h.pilotActorUuid) ?? ""
  ).trim(), a = await oS(i);
  if (a)
    return { actor: a, uuid: a.uuid ?? i, source: "pilot", reason: "" };
  const r = ((y = (b = t == null ? void 0 : t.system) == null ? void 0 : b.mwd) == null ? void 0 : y.crew) ?? {};
  return Number((r == null ? void 0 : r.effectiveCount) ?? (r == null ? void 0 : r.count) ?? 0) > 0 ? {
    actor: null,
    uuid: "",
    source: "crew",
    reason: "Crew exists, but no operator actor is linked."
  } : {
    actor: null,
    uuid: "",
    source: "",
    reason: "No linked operator or pilot actor."
  };
}
function mi(t = null, { sheetToken: e = null } = {}) {
  var s, o, l, c;
  const n = (e == null ? void 0 : e.document) ?? e ?? null;
  if (n) return n;
  const a = (((s = t == null ? void 0 : t.getActiveTokens) == null ? void 0 : s.call(t, !0, !0)) ?? [])[0] ?? null;
  if (a) return (a == null ? void 0 : a.document) ?? a;
  const r = Array.from(((l = (o = globalThis.canvas) == null ? void 0 : o.tokens) == null ? void 0 : l.placeables) ?? []).find((u) => {
    var d;
    return ((d = u == null ? void 0 : u.actor) == null ? void 0 : d.id) && u.actor.id === (t == null ? void 0 : t.id);
  }) ?? null;
  return r ? (r == null ? void 0 : r.document) ?? r : ((c = t == null ? void 0 : t.token) == null ? void 0 : c.document) ?? (t == null ? void 0 : t.token) ?? null;
}
function Zf(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? Math.max(0, Math.trunc(n)) : e;
}
function IF(t) {
  return Array.isArray(t) ? t : Array.isArray(t == null ? void 0 : t.contents) ? t.contents : t && typeof t[Symbol.iterator] == "function" ? Array.from(t) : [];
}
function OM(t = {}) {
  var e;
  return String((t == null ? void 0 : t.type) ?? ((e = t == null ? void 0 : t.actor) == null ? void 0 : e.type) ?? "").trim();
}
function Tl(t = {}) {
  var e;
  return (t == null ? void 0 : t.system) ?? ((e = t == null ? void 0 : t.actor) == null ? void 0 : e.system) ?? {};
}
function EF(t = {}) {
  var n;
  const e = (t == null ? void 0 : t.assetModules) ?? (t == null ? void 0 : t.items) ?? ((n = t == null ? void 0 : t.actor) == null ? void 0 : n.items) ?? [];
  return IF(e).filter((i) => ((i == null ? void 0 : i.canonicalType) ?? (i == null ? void 0 : i.type)) === M.itemType.assetModule);
}
function NF(t = {}) {
  var n;
  const e = Tl(t);
  return Zf((n = e == null ? void 0 : e.movement) == null ? void 0 : n.jump, 0);
}
function PF(t = {}) {
  var i, a, r;
  const e = (t == null ? void 0 : t.statuses) ?? ((i = t == null ? void 0 : t.actor) == null ? void 0 : i.statuses);
  return (a = e == null ? void 0 : e.has) != null && a.call(e, "jumpJetFailure") ? !0 : (Array.isArray(t == null ? void 0 : t.effects) ? t.effects : Array.isArray((r = t == null ? void 0 : t.actor) == null ? void 0 : r.effects) ? t.actor.effects : []).some((s) => {
    var c;
    if (String((s == null ? void 0 : s.statusId) ?? (s == null ? void 0 : s.id) ?? "").trim() === "jumpJetFailure") return !0;
    const l = s == null ? void 0 : s.statuses;
    return Array.isArray(l) ? l.includes("jumpJetFailure") : !!((c = l == null ? void 0 : l.has) != null && c.call(l, "jumpJetFailure"));
  });
}
function RF(t = {}) {
  var n, i;
  return (Array.isArray((i = (n = Tl(t)) == null ? void 0 : n.mwd) == null ? void 0 : i.crits) ? Tl(t).mwd.crits : []).some((a) => a && a.active !== !1 && Array.isArray(a.gates) && a.gates.includes("jump"));
}
function lS(t = "") {
  return {
    enabled: !1,
    available: !1,
    blocked: !1,
    blockedReason: "",
    legacy: !1,
    movement: 0,
    heat: 0,
    attackRatingBonus: 0,
    defenseRatingBonus: 0,
    dfaEnabled: !1,
    sourceIds: [],
    sourceNames: [],
    sourceLabel: t
  };
}
function DF(t = {}) {
  const e = OM(t);
  if (e && e !== M.actorTypes.battlemech)
    return { jumping: lS() };
  const n = EF(t).map((h) => ({
    item: h,
    state: no(h, { installed: !0 }),
    profile: RD(h)
  })).filter((h) => h.state.active && h.profile.enabled), i = NF(t), a = n.length === 0 && i > 0;
  if (!(n.length > 0 || a))
    return { jumping: lS() };
  const s = RF(t) || PF(t), o = s ? "Jump capability is blocked by current machine damage or status effects." : "", l = n.reduce((h, b) => {
    var y, S;
    return {
      movement: h.movement + Zf(b.profile.movement, 0),
      heat: h.heat + Zf(b.profile.heat, 0),
      attackRatingBonus: h.attackRatingBonus + Number(b.profile.attackRatingBonus ?? 0),
      defenseRatingBonus: h.defenseRatingBonus + Number(b.profile.defenseRatingBonus ?? 0),
      dfaEnabled: h.dfaEnabled || !!b.profile.dfaEnabled,
      sourceIds: h.sourceIds.concat((y = b.item) != null && y.id ? [b.item.id] : []),
      sourceNames: h.sourceNames.concat((S = b.item) != null && S.name ? [b.item.name] : [])
    };
  }, {
    movement: 0,
    heat: 0,
    attackRatingBonus: 0,
    defenseRatingBonus: 0,
    dfaEnabled: !1,
    sourceIds: [],
    sourceNames: []
  }), c = a ? ["Legacy Jump Movement"] : l.sourceNames, u = a ? [] : l.sourceIds, d = a ? i : l.movement, f = a ? 0 : l.heat, m = a ? 0 : l.attackRatingBonus, g = a ? 0 : l.defenseRatingBonus, p = a ? !0 : l.dfaEnabled;
  return {
    jumping: {
      enabled: !0,
      available: !s,
      blocked: s,
      blockedReason: o,
      legacy: a,
      movement: d,
      heat: f,
      attackRatingBonus: m,
      defenseRatingBonus: g,
      dfaEnabled: !s && p,
      sourceIds: u,
      sourceNames: c,
      sourceLabel: c.join(", ")
    }
  };
}
function OF(t = {}) {
  return DF(t).jumping;
}
function Yl(t = {}) {
  var i, a, r;
  const e = OF(t);
  if (e != null && e.enabled) return e;
  const n = ((r = (a = (i = Tl(t)) == null ? void 0 : i.mwd) == null ? void 0 : a.mobility) == null ? void 0 : r.jumping) ?? null;
  return n != null && n.enabled ? n : e;
}
function cS(t = null) {
  var e, n, i;
  return t ? OM(t) === M.actorTypes.battlemech ? Yl(t) : FT(((i = (n = (e = Tl(t)) == null ? void 0 : e.mwd) == null ? void 0 : n.mobility) == null ? void 0 : i.jumping) ?? {}) : null;
}
function uS(t = null) {
  var a, r, s, o, l, c;
  if (!t) return !1;
  const e = (a = globalThis.game) == null ? void 0 : a.combat;
  if (!e) return !1;
  const n = Array.from(((s = (r = e.combatants) == null ? void 0 : r.values) == null ? void 0 : s.call(r)) ?? []).find((u) => u.actorId === t.id);
  if (!n) return !1;
  const i = ((c = (l = (o = n.getFlag) == null ? void 0 : o.call(n, "mwd", "personalCombat")) == null ? void 0 : l.actionState) == null ? void 0 : c.move) ?? null;
  return (i == null ? void 0 : i.movementKind) === "jump";
}
const va = Object.freeze({
  walk: Object.freeze({ id: "walk", label: "Walk", cost: 1, heat: 0, mode: "ground" }),
  run: Object.freeze({ id: "run", label: "Run", cost: 2, heat: 1, mode: "ground" }),
  sprint: Object.freeze({ id: "sprint", label: "Sprint", cost: 3, heat: 2, mode: "ground", requiresStabilityRoll: !0 }),
  prone: Object.freeze({ id: "prone", label: "Prone", cost: 1, heat: 0, mode: "posture", statusId: "proneMechFall" }),
  fly: Object.freeze({ id: "fly", label: "Fly", cost: 1, heat: 0, mode: "flight" }),
  jump: Object.freeze({ id: "jump", label: "Jump", cost: 1, heat: 0, mode: "jump" }),
  evasiveManeuver: Object.freeze({ id: "evasiveManeuver", label: "Evasive Maneuver", cost: 2, heat: 0, mode: "posture", statusId: "evasiveWeave", includesMovement: !0 }),
  shield: Object.freeze({ id: "shield", label: "Shield", cost: 2, heat: 0, mode: "posture", statusId: "shielded" })
}), LF = Object.freeze(["braced", "entrenchedHullDown", "evasiveWeave", "shielded"]), $F = Object.freeze({
  walk: 1,
  run: 2,
  sprint: 3
}), _F = Object.freeze(["airborne", "flying", "inFlight", "hovering"]);
function ii(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? Math.max(0, Math.trunc(n)) : e;
}
function BF(t = null, e = "") {
  var n, i;
  return !!((i = (n = t == null ? void 0 : t.statuses) == null ? void 0 : n.has) != null && i.call(n, e));
}
function xF(t = null, e = {}) {
  var i, a, r, s, o, l;
  if (_F.some((c) => BF(t, c)) || ((a = (i = t == null ? void 0 : t.system) == null ? void 0 : i.mwd) == null ? void 0 : a.airborne) === !0 || ((r = t == null ? void 0 : t.system) == null ? void 0 : r.airborne) === !0) return !0;
  const n = String(((o = (s = t == null ? void 0 : t.system) == null ? void 0 : s.mwd) == null ? void 0 : o.movementMode) ?? ((l = t == null ? void 0 : t.system) == null ? void 0 : l.movementMode) ?? "").trim().toLowerCase();
  return ["air", "airborne", "flight", "flying"].includes(n) ? !0 : ii(e.flight, 0) > 0 && ii(e.ground, 0) <= 0;
}
function Lh(t = null) {
  var h, b, y, S;
  const e = $l(((h = t == null ? void 0 : t.system) == null ? void 0 : h.movement) ?? {}, {
    actorType: (t == null ? void 0 : t.type) ?? M.actorTypes.battlemech,
    legacyMoves: (b = t == null ? void 0 : t.system) == null ? void 0 : b.moves
  }), n = Rr(t), i = Yl(t), a = ii(n.movementBonus, 0), r = _r(ii(e.flight, 0) + a, n.movementPenalty, {
    immobile: n.immobile
  }), o = xF(t, e) && ii(e.flight, 0) > 0 ? ii(e.flight, 0) : ii(e.ground, 0), l = _r(o + a, n.movementPenalty, {
    immobile: n.immobile
  }), c = (A) => l * ii($F[A], 1), u = c("walk"), d = c("run"), f = c("sprint"), m = ((S = (y = t == null ? void 0 : t.statuses) == null ? void 0 : y.has) == null ? void 0 : S.call(y, "proneMechFall")) ?? !1, g = n.immobile ? "Machine is immobilized." : "", p = [
    {
      ...va.walk,
      heat: 0,
      distance: u,
      hint: u > 0 ? `${u} m movement` : "Ground movement",
      disabled: !!g,
      reason: g
    },
    {
      ...va.run,
      heat: 1,
      distance: d,
      hint: d > 0 ? `${d} m movement` : "Ground movement",
      disabled: !!g,
      reason: g
    },
    {
      ...va.sprint,
      heat: 2,
      distance: f,
      hint: f > 0 ? `${f} m movement | Stability roll` : "Stability roll",
      disabled: !!(g || n.noSprint),
      reason: g || (n.noSprint ? "Sprint is blocked by current damage or status effects." : "")
    },
    {
      ...va.prone,
      hint: m ? "Already prone" : "Apply Prone status",
      disabled: m,
      reason: m ? "Already prone." : ""
    },
    {
      ...va.evasiveManeuver,
      distance: u,
      hint: u > 0 ? `Withdraw safely — move up to ${u} m, gain Evasive (+3 DR / -2 AR)` : "Withdraw and gain Evasive (+3 DR / -2 AR)",
      disabled: !!g,
      reason: g
    },
    {
      ...va.shield,
      hint: "Guarded stance: +4 DR / −1 AR. First hit absorbed (−2 damage), then Shield ends.",
      disabled: !1,
      reason: ""
    }
  ];
  if (r > 0 && p.push({
    ...va.fly,
    distance: r,
    hint: `${r} m flight movement`,
    disabled: !!g,
    reason: g
  }), i != null && i.enabled && ii(i.movement, 0) > 0) {
    const A = ii(i.heat, 0);
    p.push({
      ...va.jump,
      heat: A,
      distance: ii(i.movement, 0),
      hint: [
        `${ii(i.movement, 0)} m jump movement`,
        A > 0 ? `+${A} Heat` : "",
        i.sourceLabel ?? ""
      ].filter(Boolean).join(" | "),
      disabled: !!(g || n.noJump || i.blocked || i.available === !1),
      reason: g || (n.noJump ? "Jump movement is blocked by current damage or status effects." : "") || (i.blockedReason ?? "")
    });
  }
  return p;
}
async function zF(t, { movementKind: e = "", operatorActorUuid: n = "" } = {}) {
  var c, u, d, f;
  const i = String(e ?? "").trim(), a = Lh(t).find((m) => m.id === i) ?? null;
  if (!a)
    return (c = ui.notifications) == null || c.warn("Unknown movement action."), { ok: !1, reason: "Unknown movement action." };
  if (a.disabled)
    return (u = ui.notifications) == null || u.warn(a.reason || "That movement action is unavailable."), { ok: !1, reason: a.reason || "That movement action is unavailable." };
  const r = mi(t), s = await Ut({
    machineActor: t,
    operatorActorUuid: n
  }), o = (s == null ? void 0 : s.actor) ?? t, l = await D.spendResource(o, {
    token: r,
    resource: "sa",
    cost: a.cost,
    actionId: "move",
    actionLabel: a.label,
    actionCostLabel: `${a.cost} SA`,
    actionCategory: a.cost > 1 ? "complex" : "simple"
  });
  if (!(l != null && l.ok))
    return (d = ui.notifications) == null || d.warn((l == null ? void 0 : l.reason) ?? `Unable to record ${a.label}.`), l;
  if (await D.updateCombatantState(o, {
    token: r,
    mutate: (m, g) => {
      var p, h, b;
      return m.actionState ?? (m.actionState = {}), m.actionState.move = {
        actionId: "move",
        movementKind: a.id,
        label: a.label,
        moved: !["prone", "shield"].includes(a.id),
        round: Number(((p = g == null ? void 0 : g.combat) == null ? void 0 : p.round) ?? 0),
        turn: Number(((h = g == null ? void 0 : g.combat) == null ? void 0 : h.turn) ?? 0),
        combatantId: ((b = g == null ? void 0 : g.combatant) == null ? void 0 : b.id) ?? null
      }, m;
    }
  }), a.heat > 0 && await sd(t, a.heat, { reason: `${a.label} movement` }), ["sprint", "jump"].includes(a.id) && await pd(t, {
    reason: `${a.id}Movement`,
    source: "movement",
    duration: "untilNextActivation",
    token: r
  }), a.statusId) {
    for (const m of LF)
      m !== a.statusId && await mt({ actor: t, statusId: m, active: !1 });
    await mt({
      actor: t,
      statusId: a.statusId,
      active: !0,
      metadata: { notes: "Applied from the Movement quick action." }
    });
  }
  return a.id === "evasiveManeuver" ? await ChatMessage.create({
    content: `<div class="mwd-chat-message"><p><strong>${(t == null ? void 0 : t.name) ?? "BattleMech"}</strong> takes an <strong>Evasive Maneuver</strong>. May move up to normal distance — movement does not trigger opportunity or parting attacks from adjacent enemies. Gains <strong>Evasive</strong> (+3 DR / −2 AR) until the start of next activation. Clear the <em>Evasive Weave</em> status at the start of the next turn.</p></div>`,
    speaker: ChatMessage.getSpeaker({ actor: t }),
    flags: { mwd: { messageType: "postureAction", actionId: "evasiveManeuver" } }
  }) : a.id === "shield" ? await ChatMessage.create({
    content: `<div class="mwd-chat-message"><p><strong>${(t == null ? void 0 : t.name) ?? "BattleMech"}</strong> takes a <strong>Shield</strong> posture. Gains <strong>Shielded</strong> (+4 DR / −1 AR) until the start of next activation. The first hit or graze reduces incoming damage by 2, then Shield ends — apply this manually and clear the <em>Shielded</em> status. Clear at the start of the next turn if unused.</p></div>`,
    speaker: ChatMessage.getSpeaker({ actor: t }),
    flags: { mwd: { messageType: "postureAction", actionId: "shield" } }
  }) : (f = ui.notifications) == null || f.info(`${(t == null ? void 0 : t.name) ?? "BattleMech"}: ${a.label} recorded (${a.cost} SA${a.heat > 0 ? `, +${a.heat} Heat` : ""}${a.requiresStabilityRoll ? ", stability roll required" : ""}).`), { ok: !0, action: a, spend: l };
}
const eg = "machineStandardMelee", Eu = Object.freeze(["close", "near", "far", "extreme"]);
function Ql(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function $h(t = "close", e = "close") {
  const n = String(t ?? "").trim().toLowerCase();
  return Eu.includes(n) ? n : e;
}
function tg(t = "close") {
  const e = Eu.indexOf($h(t));
  return e >= 0 ? e : 0;
}
function FF(t = null) {
  return (t == null ? void 0 : t.type) === M.actorTypes.vehicle || (t == null ? void 0 : t.type) === M.actorTypes.battlemech;
}
function LM(t = null) {
  const e = String((t == null ? void 0 : t.category) ?? (t == null ? void 0 : t.weaponCategory) ?? "").trim().toLowerCase(), n = String((t == null ? void 0 : t.type) ?? "").trim();
  return e === "melee" && (n === M.itemType.mechWeapon || n === "vehicleWeapon" || (t == null ? void 0 : t.isSynthetic));
}
function UF(t = null) {
  var n, i, a, r, s, o;
  const e = Math.max(0, Ql(
    ((i = (n = t == null ? void 0 : t.system) == null ? void 0 : n.mwd) == null ? void 0 : i.tonnage) ?? ((a = t == null ? void 0 : t.system) == null ? void 0 : a.tonnage) ?? ((o = (s = (r = t == null ? void 0 : t.system) == null ? void 0 : r.mwd) == null ? void 0 : s.chassis) == null ? void 0 : o.tonnage),
    0
  ));
  return Math.floor(e / 10);
}
function KF(t = null) {
  var e, n, i, a;
  return Math.max(0, Ql(
    ((e = t == null ? void 0 : t.getAttributeValue) == null ? void 0 : e.call(t, M.actorAttributes.reflexes)) ?? ((a = (i = (n = t == null ? void 0 : t.system) == null ? void 0 : n.attributes) == null ? void 0 : i.reflexes) == null ? void 0 : a.value),
    0
  ));
}
function _h(t = {}) {
  var i;
  const e = ((i = t == null ? void 0 : t.item) == null ? void 0 : i.system) ?? {}, n = e.machineMelee && typeof e.machineMelee == "object" ? e.machineMelee : {};
  return { system: e, config: n };
}
function HF(t = {}) {
  const { system: e, config: n } = _h(t);
  return Ql(n.damageBonus ?? e.damage ?? t.damage, 0);
}
function dS(t = {}, e = "close") {
  var a, r, s, o;
  const { system: n, config: i } = _h(t);
  return Ql(
    ((a = i.attackRatingBand) == null ? void 0 : a[e]) ?? ((r = i.arModifier) == null ? void 0 : r[e]) ?? ((s = n.attackRatingBand) == null ? void 0 : s[e]) ?? ((o = t.attackRatingBand) == null ? void 0 : o[e]),
    0
  );
}
function $M(t = {}) {
  var i, a;
  const { system: e, config: n } = _h(t);
  return $h(
    n.rangeMax ?? n.reach ?? ((i = e.range) == null ? void 0 : i.max) ?? ((a = t.range) == null ? void 0 : a.max) ?? t.defaultRangeBand ?? "close",
    "close"
  );
}
function Bh(t = null) {
  const e = "concussive";
  return {
    id: eg,
    uuid: (t == null ? void 0 : t.uuid) ?? null,
    name: "Standard Melee",
    img: null,
    item: null,
    type: M.itemType.mechWeapon,
    equipped: !0,
    isPrimary: !1,
    isSynthetic: !0,
    category: "melee",
    weaponCategory: "melee",
    skill: "meleeCombat",
    skillDef: null,
    damage: 0,
    ap: 0,
    heat: 0,
    baseDamageType: e,
    baseDamageTypeLabel: En(e),
    damageType: e,
    damageTypeLabel: En(e),
    attackRatingBand: { close: 0, near: 0, far: 0, extreme: 0 },
    range: { max: "close", close: 0, near: 0, far: 0, extreme: 0 },
    defaultRangeBand: "close",
    traits: [],
    effects: {},
    notes: "Baseline unarmed strike / kick / punch",
    resolverKey: "standard",
    capabilityReport: { isTemplated: !1, errors: [] }
  };
}
function _M({
  machineActor: t = null,
  pilotActor: e = null,
  profile: n = null
} = {}) {
  var d, f;
  if (!FF(t) || !LM(n)) return n;
  const i = $M(n), a = tg(i), r = KF(e), s = UF(t), o = HF(n), l = ei((n == null ? void 0 : n.baseDamageType) ?? (n == null ? void 0 : n.damageType), "concussive"), c = ei((n == null ? void 0 : n.damageType) ?? l, l), u = Eu.reduce((m, g, p) => (m[g] = p <= a ? Math.max(0, r + dS(n, g)) : 0, m), {});
  return {
    ...n,
    category: "melee",
    weaponCategory: "melee",
    skill: "meleeCombat",
    damage: Math.max(0, s + o),
    heat: Math.max(0, Ql((n == null ? void 0 : n.heat) ?? ((f = (d = n == null ? void 0 : n.item) == null ? void 0 : d.system) == null ? void 0 : f.heat), 0)),
    baseDamageType: l,
    baseDamageTypeLabel: En(l),
    damageType: c,
    damageTypeLabel: En(c),
    attackRatingBand: u,
    range: {
      ...(n == null ? void 0 : n.range) ?? {},
      max: i
    },
    defaultRangeBand: "close",
    machineMelee: {
      baseDamage: s,
      damageBonus: o,
      pilotReflexes: r,
      rangeCap: i,
      attackRatingModifiers: Eu.reduce((m, g) => (m[g] = dS(n, g), m), {})
    }
  };
}
function mS(t = null, e = "close") {
  if (!LM(t)) return !0;
  const n = $M(t), i = $h(e, "close");
  return tg(i) <= tg(n);
}
function jF() {
  var t, e, n;
  return ((t = game.mwd) == null ? void 0 : t.roll) ?? ((n = (e = game.system) == null ? void 0 : e.mwd) == null ? void 0 : n.roll) ?? null;
}
function Cm(t = {}, e = 0) {
  const n = String((t == null ? void 0 : t.weaponId) ?? "").trim(), i = String((t == null ? void 0 : t.name) ?? "").trim() || z.actor.vehicle.quickActions.unarmed;
  return {
    ...t,
    id: String((t == null ? void 0 : t.id) ?? n ?? `melee-${e + 1}`).trim() || `melee-${e + 1}`,
    name: i,
    weaponId: n || null,
    damage: (t == null ? void 0 : t.damage) ?? 1,
    notes: String((t == null ? void 0 : t.notes) ?? "").trim()
  };
}
function GF(t = null) {
  var e, n, i;
  return ((e = t == null ? void 0 : t.system) == null ? void 0 : e.skill) === "meleeCombat" || ((n = t == null ? void 0 : t.system) == null ? void 0 : n.weaponCategory) === "melee" || ((i = t == null ? void 0 : t.system) == null ? void 0 : i.category) === "melee";
}
function wl(t = null) {
  var a;
  const e = Array.isArray((a = t == null ? void 0 : t.system) == null ? void 0 : a.meleeProfiles) ? t.system.meleeProfiles.map(Cm) : [];
  if (e.length > 0) return e;
  const n = [Cm({
    ..._M({
      machineActor: t,
      pilotActor: null,
      profile: Bh(t)
    }),
    name: z.actor.vehicle.quickActions.unarmed,
    weaponId: null,
    notes: z.actor.vehicle.quickActions.unarmedNotes
  })], i = nd(t, { canonicalType: M.itemType.mechWeapon }).filter(GF);
  return n.push(...i.map((r, s) => {
    var o, l, c, u, d, f;
    return Cm({
      id: r.id,
      name: r.name,
      weaponId: r.id,
      damage: ((l = (o = r.getDamage) == null ? void 0 : o.call(r)) == null ? void 0 : l.value) ?? ((c = r.system) == null ? void 0 : c.damage) ?? 0,
      notes: ((u = r.system) == null ? void 0 : u.description) ?? ((f = (d = r.system) == null ? void 0 : d.references) == null ? void 0 : f.description) ?? ""
    }, s + 1);
  })), n;
}
async function qF(t, {
  profile: e = null,
  profileId: n = "",
  operatorActorUuid: i = ""
} = {}) {
  var u, d;
  const a = wl(t), r = e ?? a.find((f) => String(f.id ?? "").trim() === String(n ?? "").trim()) ?? a[0] ?? null;
  if (!r)
    return (u = ui.notifications) == null || u.warn(z.actor.vehicle.quickActions.errors.noMelee), { ok: !1, reason: z.actor.vehicle.quickActions.errors.noMelee };
  const s = jF();
  if (!(s != null && s.execute))
    return (d = ui.notifications) == null || d.error("MWD roll system not initialized."), { ok: !1, reason: "MWD roll system not initialized." };
  const o = mi(t), l = String(r.weaponId ?? "").trim(), c = String(i ?? "").trim();
  return l ? (await s.execute({
    actor: t,
    payload: {
      intent: "attack",
      sourceType: "mechWeapon",
      sourceId: l,
      weaponId: l,
      edge: { pool: "physical.grit", allowed: ["pre", "post"] },
      tags: ["combat", "attack", "machine"],
      sourceTokenId: (o == null ? void 0 : o.id) ?? null,
      operatorActorUuid: c
    }
  }), { ok: !0, profile: r }) : (await s.execute({
    actor: t,
    payload: {
      intent: "attack",
      sourceType: "mechWeapon",
      syntheticWeapon: Bh(t),
      edge: { allowed: ["pre", "post"] },
      tags: ["machine", "skill", "combat", "attack", "melee"],
      sourceTokenId: (o == null ? void 0 : o.id) ?? null,
      operatorActorUuid: c
    }
  }), { ok: !0, profile: r });
}
const fS = wl, BM = qF;
function Js(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function Im({ chassis: t = 0, forceModifier: e = 0 } = {}) {
  return Math.max(0, Js(t, 0) + Js(e, 0));
}
function WF({ handling: t = 0, piloting: e = 0 } = {}) {
  return Math.max(0, Js(t, 0) + Js(e, 0));
}
function VF({ chassis: t = 0, bonus: e = 0 } = {}) {
  return Math.max(0, Js(t, 0) + Js(e, 0));
}
const xh = "machineChargeAttack", xM = Object.freeze(["impact", "control", "dfa"]), Ys = Object.freeze({
  impact: "Impact Charge",
  control: "Control Charge",
  dfa: "Death From Above"
}), JF = Object.freeze(["prone", "forcedMovement", "skidding", "stalled"]), zM = Object.freeze({
  prone: "Prone",
  forcedMovement: "Forced Movement",
  skidding: "Skidding",
  stalled: "Stalled"
}), YF = Object.freeze({
  // BattleMech
  run: 1,
  fly: 1,
  jump: 1,
  sprint: 2,
  // Vehicle
  reposition: 1,
  redline: 2,
  // Disqualifying — walk and postural actions
  walk: 0,
  move: 0,
  prone: 0,
  hullDown: 0,
  brace: 0,
  flightMove: 1
});
function FM() {
  var t, e, n;
  return ((t = game.mwd) == null ? void 0 : t.roll) ?? ((n = (e = game.system) == null ? void 0 : e.mwd) == null ? void 0 : n.roll) ?? null;
}
function zh(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function gS(t) {
  var e, n, i, a, r, s;
  return Math.max(0, zh(
    ((i = (n = (e = t == null ? void 0 : t.system) == null ? void 0 : e.attributes) == null ? void 0 : n.chassis) == null ? void 0 : i.value) ?? ((r = (a = t == null ? void 0 : t.system) == null ? void 0 : a.mwd) == null ? void 0 : r.chassis) ?? ((s = t == null ? void 0 : t.system) == null ? void 0 : s.chassis),
    0
  ));
}
function QF(t) {
  var e, n, i, a;
  return Math.max(0, zh(
    ((i = (n = (e = t == null ? void 0 : t.system) == null ? void 0 : e.attributes) == null ? void 0 : n.handling) == null ? void 0 : i.value) ?? ((a = t == null ? void 0 : t.system) == null ? void 0 : a.handling),
    0
  ));
}
function XF(t) {
  var e, n, i, a, r, s;
  return Math.max(0, zh(
    ((i = (n = (e = t == null ? void 0 : t.system) == null ? void 0 : e.attributes) == null ? void 0 : n.piloting) == null ? void 0 : i.value) ?? ((s = (r = (a = t == null ? void 0 : t.system) == null ? void 0 : a.skills) == null ? void 0 : r.piloting) == null ? void 0 : s.value),
    0
  ));
}
function UM(t, { token: e = null } = {}) {
  var r, s, o, l;
  const n = ((r = D.getSnapshot) == null ? void 0 : r.call(D, t, { token: e })) ?? null, i = String(((l = (o = (s = n == null ? void 0 : n.state) == null ? void 0 : s.actionState) == null ? void 0 : o.move) == null ? void 0 : l.movementKind) ?? "").trim(), a = YF[i] ?? 0;
  return {
    movementKind: i,
    speedTier: a,
    usedJump: i === "jump",
    hasQualifyingMovement: a > 0 && i !== ""
  };
}
function KM(t, { mode: e, movementState: n }) {
  if (e === "dfa") {
    if ((t == null ? void 0 : t.type) !== M.actorTypes.battlemech)
      return "DFA is only available to BattleMechs.";
    if (!n.usedJump)
      return "DFA requires Jump movement this activation.";
    const i = Yl(t);
    return i != null && i.dfaEnabled ? "" : (i == null ? void 0 : i.blockedReason) ?? "DFA is not available (no jump modules or DFA blocked).";
  }
  return n.hasQualifyingMovement ? "" : `${Ys[e] ?? "Charge"} requires Run or Sprint movement this activation.`;
}
function ZF(t, e, { mode: n, speedTier: i }) {
  const a = gS(t), r = gS(e);
  let s, o, l, c;
  n === "impact" ? (s = VF({ chassis: a, bonus: i }), o = i, l = r, c = Im({ chassis: a, forceModifier: 0 })) : n === "control" ? (s = Math.max(1, a + i - 2), o = Math.min(3, Math.max(1, a + i - r)), l = Math.max(1, r - 1), c = Im({ chassis: a, forceModifier: 1 })) : (s = a + i + 2, o = Math.min(3, Math.max(1, s - r)), l = r + Math.max(1, i - 1), c = null);
  const u = Im({ chassis: r, forceModifier: o });
  return {
    attackerChassis: a,
    defenderChassis: r,
    attackDamage: s,
    forceModifier: o,
    recoilDamage: l,
    defenderKnockdownDn: u,
    missAttackerStabilityDn: c
  };
}
function eU(t, { mode: e, speedTier: n, attackDamage: i, attackerChassis: a }) {
  const r = Ys[e] ?? "Charge", s = "concussive";
  return {
    id: xh,
    uuid: (t == null ? void 0 : t.uuid) ?? null,
    name: r,
    isSynthetic: !0,
    category: "melee",
    weaponCategory: "melee",
    skill: "piloting",
    skillDef: null,
    damage: i,
    ap: 0,
    heat: 0,
    baseDamageType: s,
    baseDamageTypeLabel: En(s),
    damageType: s,
    damageTypeLabel: En(s),
    attackRatingBand: { close: a, near: 0, far: 0, extreme: 0 },
    range: { max: "close", close: 0, near: 0, far: 0, extreme: 0 },
    defaultRangeBand: "close",
    traits: [],
    effects: {},
    notes: `${r} — Chassis ${a}, Speed Tier ${n}`,
    resolverKey: "standard",
    capabilityReport: { isTemplated: !1, errors: [] }
  };
}
async function tU(t, {
  mode: e,
  recoilDamage: n,
  defenderName: i = "target",
  isMiss: a = !1
}) {
  const r = Ys[e] ?? "Charge";
  let s;
  e === "dfa" ? s = a ? `<strong>${(t == null ? void 0 : t.name) ?? "Attacker"}</strong> missed the DFA — <strong>${n} recoil damage</strong> (concussive) to attacker Legs. Attacker falls <em>Prone</em> automatically.` : `<strong>${(t == null ? void 0 : t.name) ?? "Attacker"}</strong> DFA recoil — <strong>${n} damage</strong> (concussive) to attacker. Run a landing check (Piloting + Handling vs Chassis + Jump Tier) to avoid Prone.` : s = `<strong>${r}</strong> recoil — <strong>${n} damage</strong> (concussive) to ${(t == null ? void 0 : t.name) ?? "attacker"} if the attack hits.`, await ChatMessage.create({
    content: `<div class="mwd-chat-message"><p>${s}</p></div>`,
    speaker: ChatMessage.getSpeaker({ actor: t }),
    flags: { mwd: { messageType: "chargeRecoil", mode: e, recoilDamage: n } }
  });
}
async function nU(t, {
  dn: e = 1,
  label: n = "Knockdown Check",
  token: i = null,
  operatorActorUuid: a = ""
} = {}) {
  const r = FM();
  if (!(r != null && r.execute)) return { ok: !1, reason: "MWD roll system not initialized." };
  const s = QF(t), o = XF(t), l = WF({ handling: s, piloting: o });
  return await r.execute({
    actor: t,
    payload: {
      intent: "skill",
      key: "piloting",
      attrKey: "reflexes",
      machineAttributeKey: M.actorAttributes.handling,
      dn: e,
      quickAction: {
        title: `${n} (DN ${e} — Piloting ${o} + Handling ${s} = ${l} dice)`
      },
      edge: { allowed: ["pre", "post"] },
      tags: ["machine", "skill", "knockdown"],
      sourceTokenId: (i == null ? void 0 : i.id) ?? null,
      operatorActorUuid: String(a ?? "").trim()
    }
  }), { ok: !0 };
}
function Fh(t, { token: e = null } = {}) {
  const n = UM(t, { token: e }), i = (t == null ? void 0 : t.type) === M.actorTypes.battlemech;
  return xM.flatMap((a) => {
    if (a === "dfa" && !i) return [];
    const r = KM(t, { mode: a, movementState: n });
    return [{
      id: a,
      label: Ys[a],
      disabled: !!r,
      reason: r
    }];
  });
}
function Uh() {
  return JF.map((t) => ({
    id: t,
    label: zM[t] ?? t
  }));
}
async function Kh(t, {
  mode: e = "impact",
  controlIntent: n = "prone",
  targetActor: i = null,
  token: a = null,
  operatorActorUuid: r = ""
} = {}) {
  var v, N, P, L, _, O, x, H, G, q, ie;
  if (!xM.includes(e)) {
    const Z = `Unknown charge mode: ${e}.`;
    return (v = ui.notifications) == null || v.warn(Z), { ok: !1, reason: Z };
  }
  const s = a ?? mi(t), o = UM(t, { token: s }), l = KM(t, { mode: e, movementState: o });
  if (l)
    return (N = ui.notifications) == null || N.warn(l), { ok: !1, reason: l };
  const c = o.speedTier, u = i ?? ((L = Array.from(((P = game == null ? void 0 : game.user) == null ? void 0 : P.targets) ?? [])[0]) == null ? void 0 : L.actor) ?? null;
  if (!u) {
    const Z = "No target selected. Target a token before using a charge attack.";
    return (_ = ui.notifications) == null || _.warn(Z), { ok: !1, reason: Z };
  }
  const d = FM();
  if (!(d != null && d.execute)) {
    const Z = "MWD roll system not initialized.";
    return (O = ui.notifications) == null || O.error(Z), { ok: !1, reason: Z };
  }
  const f = ZF(t, u, { mode: e, speedTier: c }), m = await Ut({ machineActor: t, operatorActorUuid: r }), g = (m == null ? void 0 : m.actor) ?? t, p = od(t), h = Math.max(0, 2 + Number((p == null ? void 0 : p.extraCost) ?? 0)), b = {
    token: s,
    resource: "sa",
    cost: h,
    actionId: "attack",
    actionLabel: Ys[e],
    actionCostLabel: `${h} SA`,
    actionCategory: "complex"
  }, y = ((x = D.previewResourceSpend) == null ? void 0 : x.call(D, g, b)) ?? { ok: !0 };
  if (!(y != null && y.ok))
    return (H = ui.notifications) == null || H.warn((y == null ? void 0 : y.reason) ?? "Unable to record charge attack action."), y;
  const S = eU(t, {
    mode: e,
    speedTier: c,
    attackDamage: f.attackDamage,
    attackerChassis: f.attackerChassis
  }), A = ["combat", "attack", "machine", "collision", e];
  e === "control" && A.push(`intent:${n}`);
  const T = await d.execute({
    actor: t,
    payload: {
      intent: "attack",
      sourceType: "mechWeapon",
      syntheticWeapon: S,
      machineActionPrecommitted: !0,
      chargeAttack: {
        mode: e,
        controlIntent: e === "control" ? n : null,
        speedTier: c,
        ...f
      },
      edge: { pool: "physical.grit", allowed: ["pre", "post"] },
      tags: A,
      sourceTokenId: (s == null ? void 0 : s.id) ?? null,
      operatorActorUuid: ((G = m == null ? void 0 : m.actor) == null ? void 0 : G.uuid) ?? r
    }
  });
  if (!T || T != null && T.aborted)
    return { ok: !1, cancelled: !0, reason: "Charge attack was cancelled." };
  const C = await D.spendResource(g, b);
  if (!(C != null && C.ok))
    return (q = ui.notifications) == null || q.warn((C == null ? void 0 : C.reason) ?? "Unable to record charge attack action."), C;
  await pd(t, {
    reason: "chargeAttack",
    source: "attack",
    duration: "untilNextActivation",
    token: s
  }), await tU(t, {
    mode: e,
    recoilDamage: f.recoilDamage,
    defenderName: (u == null ? void 0 : u.name) ?? "target"
  });
  const k = Array.from(((ie = game == null ? void 0 : game.user) == null ? void 0 : ie.targets) ?? [])[0] ?? null, w = e === "control" ? `Control Charge Knockdown — ${zM[n] ?? n} on failure` : `${Ys[e]} Knockdown`;
  return await nU(u, {
    dn: f.defenderKnockdownDn,
    label: `${w} (from ${(t == null ? void 0 : t.name) ?? "attacker"})`,
    token: k
  }), { ok: !0, mode: e, speedTier: c, formulas: f };
}
function iU() {
  var t, e, n;
  return ((t = game.mwd) == null ? void 0 : t.roll) ?? ((n = (e = game.system) == null ? void 0 : e.mwd) == null ? void 0 : n.roll) ?? null;
}
function Nu(t = null, { token: e = null } = {}) {
  var s, o, l, c;
  const n = e ?? mi(t), i = ((s = D.getSnapshot) == null ? void 0 : s.call(D, t, { token: n })) ?? null, a = i != null && i.isCurrentTurn ? ((o = D.getUsedWeaponGroupIds) == null ? void 0 : o.call(D, t, { token: n, snapshot: i })) ?? [] : [], r = ((c = (l = t == null ? void 0 : t.system) == null ? void 0 : l.mwd) == null ? void 0 : c.fireMode) ?? Jt;
  return Nw(t, { usedWeaponGroupIds: a, fireMode: r, token: n });
}
function aU(t, {
  sourceToken: e = null,
  operatorActorUuid: n = "",
  fireMode: i = Jt
} = {}) {
  return {
    intent: "attack",
    sourceType: "weaponGroup",
    sourceId: t.id,
    weaponGroupId: t.id,
    fireMode: i,
    machineActionPrecommitted: !0,
    edge: { pool: "physical.grit", allowed: ["pre", "post"] },
    tags: ["combat", "attack", "machine", "groupFire", i],
    sourceTokenId: (e == null ? void 0 : e.id) ?? null,
    operatorActorUuid: String(n ?? "").trim()
  };
}
function rU(t = null) {
  return t ? !t.isAttackLegal || !t.isAvailableThisActivation ? t.disableReason || "That weapon group cannot attack right now." : "" : "That weapon group is no longer available.";
}
function sU({ fireMode: t = Jt, selectedGroup: e = null, groups: n = [] } = {}) {
  var r, s, o;
  const i = n.filter((l) => l.isAttackLegal && l.isAvailableThisActivation);
  if (t === "alphaStrike")
    return {
      ok: i.length > 0,
      reason: i.length ? "" : "No available weapon groups to fire.",
      steps: i.map((l) => ({ group: l }))
    };
  const a = rU(e);
  if (a) return { ok: !1, reason: a, steps: [] };
  if (t === "rapidFire") {
    if (!((r = e.rapidFire) != null && r.eligible))
      return {
        ok: !1,
        reason: ((s = e.rapidFire) == null ? void 0 : s.reason) || "Not rapid-fire capable.",
        steps: []
      };
    const l = Math.max(1, Number(((o = e.rapidFire) == null ? void 0 : o.repeatCount) ?? 1) || 1);
    return {
      ok: !0,
      reason: "",
      steps: Array.from({ length: l }, () => ({ group: e }))
    };
  }
  return {
    ok: !0,
    reason: "",
    steps: [{ group: e }]
  };
}
async function pS(t, {
  group: e = null,
  groupId: n = "",
  token: i = null,
  operatorActorUuid: a = ""
} = {}) {
  var C, k, w, v, N, P, L, _, O, x, H, G;
  const r = Nu(t, { token: i }), s = String(((k = (C = t == null ? void 0 : t.system) == null ? void 0 : C.mwd) == null ? void 0 : k.fireMode) ?? Jt).trim() || Jt, o = tw(s);
  if (o.id !== s || !o.implemented) {
    const q = o.id !== s ? `Unknown BattleMech fire mode: ${s}.` : `${o.label} is not implemented yet.`;
    return (w = ui.notifications) == null || w.warn(q), { ok: !1, reason: q };
  }
  const l = String(n ?? (e == null ? void 0 : e.id) ?? "").trim(), c = e ?? r.find((q) => String((q == null ? void 0 : q.id) ?? "").trim() === l) ?? null, u = sU({ fireMode: o.id, selectedGroup: c, groups: r });
  if (!u.ok) {
    const q = u.reason || (l ? "That weapon group is no longer available." : z.actor.vehicle.quickActions.errors.noRanged);
    return (v = ui.notifications) == null || v.warn(q), { ok: !1, reason: q };
  }
  const d = iU();
  if (!(d != null && d.execute)) {
    const q = "MWD roll system not initialized.";
    return (N = ui.notifications) == null || N.error(q), { ok: !1, reason: q };
  }
  const f = i ?? mi(t), m = await Ut({
    machineActor: t,
    operatorActorUuid: a
  }), g = (m == null ? void 0 : m.actor) ?? t, p = od(t), h = Math.max(0, Number(o.saCost ?? 0) + Number((p == null ? void 0 : p.extraCost) ?? 0)), b = {
    token: f,
    resource: "sa",
    cost: h,
    actionId: "attack",
    actionLabel: "Attack",
    actionCostLabel: `${h} SA`,
    actionCategory: "complex"
  }, y = ((P = D.previewResourceSpend) == null ? void 0 : P.call(D, g, b)) ?? { ok: !0 };
  if (!(y != null && y.ok))
    return (L = ui.notifications) == null || L.warn((y == null ? void 0 : y.reason) ?? "Unable to record attack action."), y;
  const S = /* @__PURE__ */ new Set();
  try {
    for (const q of u.steps) {
      const ie = await d.execute({
        actor: t,
        payload: aU(q.group, {
          sourceToken: f,
          operatorActorUuid: ((_ = m == null ? void 0 : m.actor) == null ? void 0 : _.uuid) ?? a,
          fireMode: o.id
        })
      });
      if (!ie || ie != null && ie.aborted) break;
      S.add(q.group.id);
    }
  } catch (q) {
    throw console.error("MWD | BattleMech fire mode attack failed", {
      actor: t,
      fireMode: o.id,
      completedGroupIds: Array.from(S),
      error: q
    }), (O = ui.notifications) == null || O.error((q == null ? void 0 : q.message) ?? "BattleMech attack failed."), q;
  }
  if (S.size <= 0)
    return { ok: !1, cancelled: !0, reason: "BattleMech attack was cancelled.", fireMode: o.id, completedGroupIds: [] };
  const A = await D.spendResource(g, b);
  if (!(A != null && A.ok))
    return (x = ui.notifications) == null || x.warn((A == null ? void 0 : A.reason) ?? "Unable to record attack action."), A;
  const T = await ((H = D.markWeaponGroupsUsed) == null ? void 0 : H.call(D, t, {
    token: f,
    groupIds: Array.from(S)
  }));
  return T && T.ok === !1 && ((G = ui.notifications) == null || G.warn(T.reason ?? "Unable to record BattleMech weapon-group usage.")), { ok: !0, fireMode: o.id, completedGroupIds: Array.from(S), spend: A };
}
function oU() {
  var t, e, n;
  return ((t = game.mwd) == null ? void 0 : t.roll) ?? ((n = (e = game.system) == null ? void 0 : e.mwd) == null ? void 0 : n.roll) ?? null;
}
function lU(t = null) {
  var e, n, i;
  return ((e = t == null ? void 0 : t.system) == null ? void 0 : e.skill) === "meleeCombat" || ((n = t == null ? void 0 : t.system) == null ? void 0 : n.weaponCategory) === "melee" || ((i = t == null ? void 0 : t.system) == null ? void 0 : i.category) === "melee";
}
function ng(t = null) {
  const e = new Set(
    bi(t).map((n) => String((n == null ? void 0 : n.itemId) ?? "").trim()).filter(Boolean)
  );
  return Array.from((t == null ? void 0 : t.items) ?? []).filter((n) => {
    var a;
    const i = (n == null ? void 0 : n.canonicalType) ?? (n == null ? void 0 : n.type);
    return !(i !== M.itemType.mechWeapon && i !== "vehicleWeapon" || !e.has(String((n == null ? void 0 : n.id) ?? "").trim()) || lU(n) || ((a = n == null ? void 0 : n.isActive) == null ? void 0 : a.call(n)) === !1);
  }).map((n) => {
    var i, a;
    return {
      id: String((n == null ? void 0 : n.id) ?? "").trim(),
      name: String((n == null ? void 0 : n.name) ?? "Weapon").trim() || "Weapon",
      img: (n == null ? void 0 : n.img) ?? "",
      hint: String(((i = n == null ? void 0 : n.system) == null ? void 0 : i.notes) ?? ((a = n == null ? void 0 : n.system) == null ? void 0 : a.description) ?? "").trim()
    };
  });
}
async function HM(t, {
  weaponId: e = "",
  weapon: n = null,
  token: i = null,
  operatorActorUuid: a = ""
} = {}) {
  var y, S, A, T, C, k, w;
  const r = ng(t), s = String(e ?? (n == null ? void 0 : n.id) ?? "").trim(), o = (s ? r.find((v) => v.id === s) : null) ?? r[0] ?? null;
  if (!o) {
    const v = "No mounted ranged weapons available.";
    return (y = ui.notifications) == null || y.warn(v), { ok: !1, reason: v };
  }
  const l = oU();
  if (!(l != null && l.execute)) {
    const v = "MWD roll system not initialized.";
    return (S = ui.notifications) == null || S.error(v), { ok: !1, reason: v };
  }
  const c = i ?? mi(t), u = await Ut({ machineActor: t, operatorActorUuid: a }), d = (u == null ? void 0 : u.actor) ?? t, f = od(t), m = Math.max(0, 1 + Number((f == null ? void 0 : f.extraCost) ?? 0)), g = {
    token: c,
    resource: "sa",
    cost: m,
    actionId: "attack",
    actionLabel: "Attack",
    actionCostLabel: `${m} SA`,
    actionCategory: "complex"
  }, p = ((A = D.previewResourceSpend) == null ? void 0 : A.call(D, d, g)) ?? { ok: !0 };
  if (!(p != null && p.ok))
    return (T = ui.notifications) == null || T.warn((p == null ? void 0 : p.reason) ?? "Unable to record attack action."), p;
  let h = null;
  try {
    h = await l.execute({
      actor: t,
      payload: {
        intent: "attack",
        sourceType: "mechWeapon",
        sourceId: o.id,
        weaponId: o.id,
        machineActionPrecommitted: !0,
        edge: { pool: "physical.grit", allowed: ["pre", "post"] },
        tags: ["combat", "attack", "machine"],
        sourceTokenId: (c == null ? void 0 : c.id) ?? null,
        operatorActorUuid: ((C = u == null ? void 0 : u.actor) == null ? void 0 : C.uuid) ?? a
      }
    });
  } catch (v) {
    throw console.error("MWD | Vehicle ranged attack failed", { actor: t, weaponId: o.id, error: v }), (k = ui.notifications) == null || k.error((v == null ? void 0 : v.message) ?? "Vehicle attack failed."), v;
  }
  if (!h || h != null && h.aborted)
    return { ok: !1, cancelled: !0, reason: "Vehicle attack was cancelled.", weaponId: o.id };
  const b = await D.spendResource(d, g);
  return b != null && b.ok ? { ok: !0, weaponId: o.id, spend: b } : ((w = ui.notifications) == null || w.warn((b == null ? void 0 : b.reason) ?? "Unable to record attack action."), b);
}
function cU(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function uU(t) {
  var e, n;
  return Array.from(t ?? ((n = (e = globalThis.game) == null ? void 0 : e.user) == null ? void 0 : n.targets) ?? []).filter((i) => i == null ? void 0 : i.actor);
}
function dU({ detectionState: t = "blind", canAcquire: e = !1, ceiling: n = "lock" } = {}) {
  return t === "blind" ? "Acquire can establish Contact (DN 1)." : t === "contact" ? "Acquire can upgrade to Track." : t === "track" && e ? "Acquire can upgrade to Lock." : t === "track" && n === "track" ? "ECM prevents advancing beyond Track." : t === "lock" ? "Targeting solution optimized." : "Review target state before acquiring.";
}
function mU({ detectionState: t = "blind", lockGated: e = !1 } = {}) {
  return t === "blind" || t === "contact" ? "Targeting Data unavailable until Track." : t === "track" ? "Targeting Data available." : t === "lock" && e ? "Targeting solution optimized." : "Targeting solution available.";
}
function fU({ canAcquire: t = !1, detectionState: e = "blind", acquireHint: n = "" } = {}) {
  return t ? {
    enabled: !0,
    label: "Acquire",
    title: n
  } : e === "lock" ? {
    enabled: !1,
    label: "Acquire",
    title: "Target is already at Lock."
  } : {
    enabled: !1,
    label: "Acquire",
    title: "Acquire is not available for this target right now."
  };
}
function gU({ canTarget: t = !1, targetHint: e = "" } = {}) {
  return {
    enabled: t,
    label: "Target",
    title: t ? e : "Track or Lock is required before generating targeting data."
  };
}
function hS(t, e) {
  return nd(t, { canonicalType: "mechWeapon" }).filter((n) => {
    var i;
    return (i = n.isActive) == null ? void 0 : i.call(n);
  }).some((n) => Tp(n, e));
}
function Hh(t = null) {
  return {
    tag: hS(t, "tag"),
    narc: hS(t, "narc"),
    c3: JD(t, "c3", ["c3i", "network", "shareTargetingData"])
  };
}
function pU({
  row: t = {},
  detectionState: e = "blind",
  canAcquire: n = !1,
  canTarget: i = !1,
  acquireHint: a = "",
  targetHint: r = "",
  capabilities: s = {}
} = {}) {
  const o = !!s.tag;
  return [
    {
      id: "acquire",
      action: "ewAcquire",
      label: "Acquire",
      icon: "fa-satellite-dish",
      dn: nw(e),
      enabled: n,
      title: n ? a : "Acquire is not available for this target right now."
    },
    {
      id: "target",
      action: "ewTarget",
      label: "Target",
      icon: "fa-crosshairs",
      dn: 2,
      enabled: i,
      title: i ? r : "Track or Lock is required before generating targeting data."
    },
    {
      id: "ecmSpike",
      action: "machineEwAction",
      actionId: "ecmSpike",
      label: "Spike",
      icon: "fa-bolt",
      dn: 1,
      enabled: !0,
      title: "Launch an ECM Spike roll against this target."
    },
    {
      id: "tagTarget",
      action: "machineEwAction",
      actionId: "tagTarget",
      label: "TAG",
      icon: "fa-location-crosshairs",
      dn: 1,
      enabled: o,
      title: o ? "Launch a TAG roll against this target." : "Requires an installed TAG asset module."
    }
  ].map((l) => ({
    ...l,
    targetTokenId: t.targetTokenId,
    targetTokenUuid: t.targetTokenUuid
  }));
}
function hU(t = null, e = null) {
  var r, s, o, l, c, u;
  const n = xl(t, e);
  if (!Number.isFinite(n))
    return {
      distance: null,
      distanceLabel: "",
      band: "",
      bandLabel: "",
      hasRange: !1
    };
  const i = String(((o = (s = (r = globalThis.canvas) == null ? void 0 : r.scene) == null ? void 0 : s.grid) == null ? void 0 : o.units) ?? ((u = (c = (l = globalThis.game) == null ? void 0 : l.system) == null ? void 0 : c.grid) == null ? void 0 : u.units) ?? "").trim(), a = md(n, "close");
  return {
    distance: n,
    distanceLabel: Mf(n, i),
    band: a,
    bandLabel: Hp(a),
    hasRange: !0
  };
}
function bU({
  actor: t = null,
  targetToken: e,
  sourceToken: n = null,
  combatant: i,
  systemAttr: a = 0,
  currentRound: r = null,
  capabilities: s = null
} = {}) {
  var N, P, L;
  if (!(e != null && e.actor)) return null;
  const o = ((N = e.document) == null ? void 0 : N.uuid) ?? e.uuid ?? "", l = String(e.id ?? ((P = e.document) == null ? void 0 : P.id) ?? "").trim(), c = String(((L = n == null ? void 0 : n.document) == null ? void 0 : L.uuid) ?? (n == null ? void 0 : n.uuid) ?? "").trim(), u = hU(n, e), d = {
    rangeBand: u.band,
    friendlyMachineTokenUuid: c
  }, f = bd(i, o, e.actor), m = Mw(e.actor, d), g = ao(l), p = Qp(e.actor, g, d), h = ld(e.actor, d), b = f === "track" || f === "lock" ? _p(a, f) : null, y = f === "track" || f === "lock" ? Xp(i, o, a, f, r) : null, S = f === "blind" || f === "contact" || f === "track" && m === "lock", A = f === "track" || f === "lock", T = f === "lock", C = s ?? Hh(t), k = dU({ detectionState: f, canAcquire: S, ceiling: m }), w = mU({ detectionState: f, lockGated: T }), v = {
    tokenName: e.name ?? "Target",
    targetTokenId: l,
    targetTokenUuid: o,
    detectionState: f,
    detectionStateLabel: wn(f),
    packetValue: (y == null ? void 0 : y.value) ?? 0,
    hasPacket: !!y,
    packetCap: b,
    hasPacketCap: b !== null,
    trackingPenalty: p,
    hasTrackingPenalty: p > 0,
    battleArmorTargetProfile: h,
    battleArmorFriendlyFireRisk: !!(h != null && h.friendlyFireRisk),
    rangeBand: u.band,
    rangeBandLabel: u.bandLabel,
    distance: u.distance,
    distanceLabel: u.distanceLabel,
    hasRange: u.hasRange,
    canAcquire: S,
    canTarget: A,
    acquireHint: k,
    targetHint: w,
    acquireAction: fU({ canAcquire: S, detectionState: f, acquireHint: k }),
    targetAction: gU({ canTarget: A, targetHint: w })
  };
  return v.compactActions = pU({ row: v, detectionState: f, canAcquire: S, canTarget: A, acquireHint: k, targetHint: w, capabilities: C }), v;
}
function jh(t, e = "") {
  const n = Array.isArray(t == null ? void 0 : t.rows) ? t.rows : [], i = String(e ?? "").trim(), a = i === "targeting" || i === "generateFireSolution" ? "canTarget" : "canAcquire";
  return n.find((r) => !!(r != null && r[a])) ?? null;
}
function Ml({
  actor: t,
  token: e,
  targets: n,
  currentRound: i = null
} = {}) {
  var u, d, f, m, g;
  const a = Pn(e), r = cU((f = (d = (u = t == null ? void 0 : t.system) == null ? void 0 : u.attributes) == null ? void 0 : d.system) == null ? void 0 : f.value, 0), s = i ?? ((g = (m = globalThis.game) == null ? void 0 : m.combat) == null ? void 0 : g.round) ?? null, o = Hh(t), l = uU(n).map((p) => bU({
    actor: t,
    targetToken: p,
    sourceToken: e,
    combatant: a,
    systemAttr: r,
    currentRound: s,
    capabilities: o
  })).filter(Boolean), c = l.length > 0;
  return {
    hasTargets: c,
    rows: l,
    capabilities: o,
    canAcquireAny: l.some((p) => p.canAcquire),
    canTargetAny: l.some((p) => p.canTarget),
    emptyState: "Target one or more tokens on the canvas to review EW status.",
    helpText: c ? "Detection state is tracked per targeted machine. Use row buttons for the automated Acquire and Fire Solution actions." : "Select targets on the canvas to review EW state and launch target-specific EW actions."
  };
}
const yU = Object.freeze({
  sensorDegraded: Object.freeze({
    remedyKey: "systemReset",
    skillKey: "computers",
    locationKey: "head",
    effectSummary: "Sensor sweeps and targeting suffer degraded optics and processing until reset."
  }),
  sensorBlind: Object.freeze({
    remedyKey: "systemReset",
    skillKey: "computers",
    locationKey: "head",
    effectSummary: "Sensor and targeting actions are severely impaired until reset."
  }),
  stalled: Object.freeze({
    remedyKey: "emergencyRepair",
    effectSummary: "The machine cannot move normally until the fault is repaired."
  }),
  unstable: Object.freeze({
    remedyKey: "emergencyRepair",
    effectSummary: "Piloting and movement remain unstable until the machine is repaired."
  }),
  limping: Object.freeze({
    remedyKey: "emergencyRepair",
    locationKey: "legs",
    effectSummary: "Mobility remains impaired until the damaged leg assembly is repaired."
  }),
  jumpJetFailure: Object.freeze({
    remedyKey: "emergencyRepair",
    locationKey: "legs",
    effectSummary: "Jump movement is unavailable until the jump system is repaired."
  }),
  overheating: Object.freeze({
    remedyKey: "coolantDump",
    locationKey: "torso",
    effectSummary: "Heat continues to spike until the cooling issue is cleared."
  }),
  reactorInstability: Object.freeze({
    remedyKey: "coolantDump",
    locationKey: "torso",
    effectSummary: "The reactor remains unstable until coolant routing is restored."
  }),
  proneMechFall: Object.freeze({
    remedyKey: "stand",
    effectSummary: "The machine must recover from a prone fall before normal movement resumes."
  })
});
function SU(t = []) {
  return t.map((e) => String(e ?? "").trim()).filter(Boolean);
}
function jM(t, e = "") {
  var s, o;
  const n = String(e ?? "").trim();
  if (!n)
    return {
      locationKey: "",
      conditionLabel: "",
      conditionModifier: 0
    };
  const i = eo(
    foundry.utils.deepClone((t == null ? void 0 : t.system) ?? {}),
    t == null ? void 0 : t.type
  ), a = ((o = (s = i == null ? void 0 : i.mwd) == null ? void 0 : s.locations) == null ? void 0 : o[n]) ?? {}, r = Number((a == null ? void 0 : a.condition) ?? 0) || 0;
  return {
    locationKey: n,
    conditionLabel: Ap(r),
    conditionModifier: kp(r)
  };
}
function GM({ remedySkillKey: t = "", totalDn: e = 0, conditionLabel: n = "" } = {}) {
  var a;
  const i = ((a = Ft(t)) == null ? void 0 : a.label) ?? ce(t);
  return i ? `Reliability + ${i} vs DN ${e}${n ? ` (${n})` : ""}` : "";
}
function AU(t, e = {}) {
  const n = Ya(e.remedyKey), i = $p(e), a = jM(t, e.locationKey), r = String(e.remedySkillKey ?? Zo(e, n) ?? "").trim(), s = el(e, n), o = Math.max(0, s + Number(a.conditionModifier ?? 0));
  return {
    issueKind: "crit",
    issueId: String(e.id ?? "").trim(),
    label: String(e.label ?? ce(e.key)).trim(),
    sortLabel: String(e.label ?? ce(e.key)).trim().toLowerCase(),
    locationLabel: String(e.locationLabel ?? ce(e.locationKey)).trim(),
    detail: SU([
      i.statusLabel ? `Status: ${i.statusLabel}` : "",
      i.scopeSummary,
      i.automationMode === "engine" ? "Automated" : "Reminder Only",
      e.escalationKey ? `Escalates: ${e.escalationKey}` : ""
    ]).join(" | "),
    effectSummary: i.effectText,
    remedyKey: n.key,
    remedyLabel: n.label,
    remedySkillKey: r,
    remedyBaseDn: s,
    totalDn: o,
    remedySummary: GM({
      remedySkillKey: r,
      totalDn: o,
      conditionLabel: a.conditionLabel
    }),
    remediable: n.remediable !== !1,
    statusId: String(e.statusId ?? "").trim()
  };
}
function qM(t, e = "") {
  var u, d;
  const n = String(e ?? "").trim(), i = yU[n] ?? null;
  if (!i || !(((d = (u = t == null ? void 0 : t.statuses) == null ? void 0 : u.has) == null ? void 0 : d.call(u, n)) ?? !1)) return null;
  const a = Ll(n), r = Ya(i.remedyKey), s = jM(t, i.locationKey), o = String(i.skillKey ?? "").trim() || Zo({ remedyKey: i.remedyKey }, r), l = el({ remedyKey: i.remedyKey }, r), c = Math.max(0, l + Number(s.conditionModifier ?? 0));
  return {
    issueKind: "status",
    issueId: n,
    label: String((a == null ? void 0 : a.label) ?? ce(n)).trim() || ce(n),
    sortLabel: String((a == null ? void 0 : a.label) ?? ce(n)).trim().toLowerCase(),
    locationLabel: s.locationKey ? ce(s.locationKey) : "",
    detail: "Status-backed repair issue",
    effectSummary: String(i.effectSummary ?? "").trim(),
    remedyKey: r.key,
    remedyLabel: r.label,
    remedySkillKey: o,
    remedyBaseDn: l,
    totalDn: c,
    remedySummary: GM({
      remedySkillKey: o,
      totalDn: c,
      conditionLabel: s.conditionLabel
    }),
    remediable: r.remediable !== !1,
    statusId: n
  };
}
function kU(t) {
  const e = tr(t).map((a) => AU(t, a)), n = new Set(
    e.map((a) => String(a.statusId ?? "").trim()).filter(Boolean)
  ), i = Array.from((t == null ? void 0 : t.statuses) ?? []).map((a) => String(a ?? "").trim()).filter(Boolean).filter((a) => !n.has(a)).map((a) => qM(t, a)).filter((a) => a !== null && a.remediable !== !1).sort((a, r) => a.sortLabel.localeCompare(r.sortLabel));
  return [...e, ...i];
}
const WM = "MachineIntents.gmMachineRemedyRequest", VM = "MachineIntents.gmMachineRemedyResponse", TU = 1e4, Fo = /* @__PURE__ */ new Map();
let bS = !1;
function yS(t = null) {
  var i, a, r, s;
  const e = /* @__PURE__ */ new Set(), n = (o) => {
    const l = String(o ?? "").trim();
    l && e.add(l);
  };
  return n(t == null ? void 0 : t.id), n(t == null ? void 0 : t._id), n(t == null ? void 0 : t.uuid), n((i = t == null ? void 0 : t.actor) == null ? void 0 : i.id), n((a = t == null ? void 0 : t.actor) == null ? void 0 : a.uuid), n((r = t == null ? void 0 : t.baseActor) == null ? void 0 : r.id), n((s = t == null ? void 0 : t.baseActor) == null ? void 0 : s.uuid), e;
}
function wU(t = null, e = null) {
  if (!t || !e) return !1;
  const n = yS(e);
  for (const i of yS(t))
    if (n.has(i)) return !0;
  return !1;
}
function MU(t = "") {
  var i;
  const e = String(t ?? "").trim();
  if (!e) return null;
  const n = (i = globalThis.game) == null ? void 0 : i.users;
  return typeof (n == null ? void 0 : n.get) == "function" ? n.get(e) ?? null : Array.from(n ?? []).find((a) => String((a == null ? void 0 : a.id) ?? "") === e) ?? null;
}
function vU(t = null, e = null) {
  var n, i;
  if (!t || !e) return !1;
  if (t.isGM) return !0;
  if (typeof e.testUserPermission == "function") {
    const a = ((i = (n = globalThis.CONST) == null ? void 0 : n.DOCUMENT_OWNERSHIP_LEVELS) == null ? void 0 : i.OWNER) ?? 3;
    try {
      if (e.testUserPermission(t, a)) return !0;
    } catch {
    }
  }
  return wU(e, t.character);
}
function Gh(t = {}) {
  return {
    machineActorUuid: String(t.machineActorUuid ?? "").trim(),
    issueKind: String(t.issueKind ?? "").trim(),
    issueId: String(t.issueId ?? "").trim(),
    critId: String(t.critId ?? "").trim(),
    statusId: String(t.statusId ?? "").trim(),
    remedyKey: String(t.remedyKey ?? "").trim(),
    operatorActorUuid: String(t.operatorActorUuid ?? "").trim()
  };
}
function CU(t = {}) {
  var e, n, i, a, r;
  return Gh({
    machineActorUuid: ((e = t.machineActor) == null ? void 0 : e.uuid) ?? "",
    issueKind: t.issueKind ?? "crit",
    issueId: t.issueId ?? ((n = t.crit) == null ? void 0 : n.id) ?? t.statusId ?? "",
    critId: ((i = t.crit) == null ? void 0 : i.id) ?? "",
    statusId: t.statusId ?? "",
    remedyKey: ((a = t.remedy) == null ? void 0 : a.key) ?? "",
    operatorActorUuid: ((r = t.operatorActor) == null ? void 0 : r.uuid) ?? ""
  });
}
function IU(t = {}) {
  return {
    ok: !!(t != null && t.ok),
    skipped: !!(t != null && t.skipped),
    reason: String((t == null ? void 0 : t.reason) ?? "").trim(),
    burnDelta: Math.max(0, Number((t == null ? void 0 : t.burnDelta) ?? 0) || 0),
    finalCost: Math.max(0, Number((t == null ? void 0 : t.finalCost) ?? 0) || 0),
    costLabel: String((t == null ? void 0 : t.costLabel) ?? "").trim()
  };
}
function EU(t = {}) {
  var e, n, i, a, r, s, o, l, c, u, d;
  return {
    ok: !!(t != null && t.ok),
    reason: String((t == null ? void 0 : t.reason) ?? "").trim(),
    passed: !!(t != null && t.passed),
    applied: !!(t != null && t.applied),
    issueKind: String(((e = t == null ? void 0 : t.context) == null ? void 0 : e.issueKind) ?? "").trim(),
    issueId: String(((n = t == null ? void 0 : t.context) == null ? void 0 : n.issueId) ?? (t == null ? void 0 : t.critId) ?? (t == null ? void 0 : t.statusId) ?? "").trim(),
    critId: String((t == null ? void 0 : t.critId) ?? ((a = (i = t == null ? void 0 : t.context) == null ? void 0 : i.crit) == null ? void 0 : a.id) ?? "").trim(),
    statusId: String((t == null ? void 0 : t.statusId) ?? ((r = t == null ? void 0 : t.context) == null ? void 0 : r.statusId) ?? "").trim(),
    remedyKey: String(((s = t == null ? void 0 : t.remedy) == null ? void 0 : s.key) ?? ((l = (o = t == null ? void 0 : t.context) == null ? void 0 : o.remedy) == null ? void 0 : l.key) ?? "").trim(),
    remedyLabel: String(((c = t == null ? void 0 : t.remedy) == null ? void 0 : c.label) ?? ((d = (u = t == null ? void 0 : t.context) == null ? void 0 : u.remedy) == null ? void 0 : d.label) ?? "").trim()
  };
}
function NU() {
  var t, e, n, i, a;
  return ((n = (e = (t = globalThis.foundry) == null ? void 0 : t.utils) == null ? void 0 : e.randomID) == null ? void 0 : n.call(e)) ?? ((a = (i = globalThis.crypto) == null ? void 0 : i.randomUUID) == null ? void 0 : a.call(i)) ?? `${Date.now()}-${Math.random().toString(36).slice(2)}`;
}
function JM() {
  var t;
  return !!((t = globalThis.game) != null && t.user) && !globalThis.game.user.isGM;
}
function PU(t = {}) {
  var a;
  const e = String((t == null ? void 0 : t.requestId) ?? "").trim(), n = String((t == null ? void 0 : t.userId) ?? "").trim();
  if (!e || n !== String(((a = game == null ? void 0 : game.user) == null ? void 0 : a.id) ?? "")) return;
  const i = Fo.get(e);
  i && (clearTimeout(i.timeout), Fo.delete(e), i.resolve(t.result ?? { ok: !1, reason: "GM operation returned no result." }));
}
async function YM(t = "", e = {}) {
  var r;
  const n = NU(), i = String(((r = game == null ? void 0 : game.user) == null ? void 0 : r.id) ?? "").trim();
  return i ? await new Promise((s) => {
    const o = setTimeout(() => {
      Fo.delete(n), s({ ok: !1, reason: "No active GM responded to the machine remedy request." });
    }, TU);
    Fo.set(n, { resolve: s, timeout: o }), ba.call(WM, {
      requestId: n,
      userId: i,
      operation: t,
      payload: e
    }) || (clearTimeout(o), Fo.delete(n), s({ ok: !1, reason: "No remote GM is available for that machine remedy request." }));
  }) : { ok: !1, reason: "No active user for GM operation." };
}
async function RU(t = {}) {
  var a;
  const e = MU(t.userId);
  if (!e) return { ok: !1, reason: "Requesting user could not be resolved." };
  const n = Gh(((a = t.payload) == null ? void 0 : a.intent) ?? {}), i = await Xl(n, { gmOverride: !1 });
  return i.ok ? i.operatorActor ? vU(e, i.operatorActor) ? { ok: !0, intent: n, context: i } : { ok: !1, reason: "You do not control the assigned pilot or operator for this machine." } : { ok: !1, reason: "Machine remedy requires a linked pilot or operator." } : i;
}
async function DU(t = {}) {
  var n, i, a, r;
  if (!((n = game == null ? void 0 : game.user) != null && n.isGM)) return;
  let e;
  try {
    const s = await RU(t);
    if (!s.ok)
      e = { ok: !1, reason: s.reason ?? "Machine remedy request was not authorized." };
    else if (t.operation === "spendCost") {
      const o = await XM(s.context);
      e = IU(o);
    } else if (t.operation === "applyOutcome") {
      const o = await ZM(s.intent, {
        passed: !!((i = t.payload) != null && i.passed),
        gmOverride: !1
      });
      e = EU(o);
    } else
      e = { ok: !1, reason: `Unknown machine remedy GM operation: ${t.operation}` };
  } catch (s) {
    console.error("MWD | GM machine remedy request failed", s), e = { ok: !1, reason: (s == null ? void 0 : s.message) ?? "Machine remedy GM operation failed." };
  }
  (r = (a = game.socket) == null ? void 0 : a.emit) == null || r.call(a, Go, {
    msg: VM,
    data: {
      requestId: t.requestId,
      userId: t.userId,
      result: e
    }
  });
}
async function OU() {
  bS || (bS = !0, await ba.register(WM, {
    condition: (t) => t.isGM,
    multiple: !1,
    callback: (t) => {
      DU(t);
    }
  }), await ba.register(VM, {
    condition: () => !0,
    multiple: !0,
    callback: (t) => PU(t)
  }));
}
async function LU(t, e, n) {
  const i = n.filter((s) => (s == null ? void 0 : s.active) !== !1);
  if (!i.length)
    try {
      await mt({ actor: t, statusId: wT, active: !1 });
    } catch (s) {
      console.warn("MWD | Unable to clear machine critical status", s);
    }
  const a = String((e == null ? void 0 : e.statusId) ?? "").trim();
  if (!(!a || i.some((s) => String((s == null ? void 0 : s.statusId) ?? "").trim() === a)))
    try {
      await mt({ actor: t, statusId: a, active: !1 });
    } catch (s) {
      console.warn(`MWD | Unable to remove crit status "${a}"`, s);
    }
}
async function $U(t = "") {
  const e = String(t ?? "").trim();
  if (!e || typeof fromUuid != "function") return null;
  try {
    return await fromUuid(e);
  } catch {
    return null;
  }
}
function _U(t, { passed: e = !1, actorUuid: n = "", gmOverride: i = !1 } = {}) {
  return e ? {
    ...t,
    active: !1,
    resolvedAt: (/* @__PURE__ */ new Date()).toISOString(),
    resolvedBy: n,
    resolvedByOverride: i && !n,
    lastRemedyAttemptAt: (/* @__PURE__ */ new Date()).toISOString(),
    lastRemedyPassed: !0,
    lastRemedyActorUuid: n,
    lastRemedyOverride: i && !n
  } : {
    ...t,
    lastRemedyAttemptAt: (/* @__PURE__ */ new Date()).toISOString(),
    lastRemedyPassed: !1,
    lastRemedyActorUuid: n,
    lastRemedyOverride: i && !n
  };
}
async function BU(t, e, {
  passed: n = !1,
  actorUuid: i = "",
  gmOverride: a = !1
} = {}) {
  const r = String(e ?? "").trim();
  if (!t || !r) return null;
  const s = foundry.utils.deepClone(t.getFlag(I, "statusRemedyMeta") ?? {});
  return s[r] = {
    ...s[r] ?? {},
    lastRemedyAttemptAt: (/* @__PURE__ */ new Date()).toISOString(),
    lastRemedyPassed: n,
    lastRemedyActorUuid: i,
    lastRemedyOverride: a && !i,
    ...n ? {
      resolvedAt: (/* @__PURE__ */ new Date()).toISOString(),
      resolvedBy: i,
      resolvedByOverride: a && !i
    } : {}
  }, await t.setFlag(I, "statusRemedyMeta", s), s[r];
}
async function Xl(t = {}, e = {}) {
  var S, A, T, C, k, w, v, N, P;
  const n = String(
    t.issueKind ?? (t.statusId ? "status" : "crit")
  ).trim() || "crit", i = String(
    t.issueId ?? (n === "status" ? t.statusId : t.critId) ?? ""
  ).trim(), a = !!(e.gmOverride ?? ((A = (S = globalThis.game) == null ? void 0 : S.user) == null ? void 0 : A.isGM)), r = await $U(t.machineActorUuid);
  if (!r) return { ok: !1, reason: "Machine actor could not be resolved." };
  const s = await Ut({
    machineActor: r,
    operatorActorUuid: t.operatorActorUuid
  });
  if (!s.actor && !a)
    return { ok: !1, reason: s.reason || "No linked operator or pilot actor." };
  if (n === "status") {
    const L = qM(r, i);
    if (!L) return { ok: !1, reason: "That status is not currently repairable." };
    const _ = Ya(t.remedyKey || L.remedyKey);
    return _.remediable === !1 ? { ok: !1, reason: "That status has no field remedy." } : {
      ok: !0,
      issueKind: n,
      issueId: L.issueId,
      machineActor: r,
      statusId: L.statusId,
      issue: L,
      remedy: _,
      operatorActor: s.actor ?? null,
      operator: s,
      gmOverride: a,
      rollingActor: s.actor ?? r,
      locationKey: "",
      locationCondition: 0,
      locationConditionLabel: "",
      locationConditionModifier: 0,
      skillKey: L.remedySkillKey,
      baseDn: L.remedyBaseDn,
      totalDn: L.totalDn,
      remedyEffect: { onSuccess: "clear", onFailure: "noChange" }
    };
  }
  const o = Array.isArray((C = (T = r.system) == null ? void 0 : T.mwd) == null ? void 0 : C.crits) ? r.system.mwd.crits.slice() : [], l = o.findIndex((L) => String((L == null ? void 0 : L.id) ?? "") === i && (L == null ? void 0 : L.active) !== !1);
  if (l < 0) return { ok: !1, reason: "That critical effect is no longer active." };
  const c = o[l], u = Ya(t.remedyKey || c.remedyKey);
  if (u.remediable === !1)
    return { ok: !1, reason: "That critical effect has no field remedy." };
  const d = eo(
    ((v = (w = (k = globalThis.foundry) == null ? void 0 : k.utils) == null ? void 0 : w.deepClone) == null ? void 0 : v.call(w, r.system ?? {})) ?? structuredClone(r.system ?? {}),
    r.type
  ), f = String(c.locationKey ?? "").trim(), m = ((P = (N = d.mwd) == null ? void 0 : N.locations) == null ? void 0 : P[f]) ?? {}, g = Number((m == null ? void 0 : m.condition) ?? 0) || 0, p = kp(g), h = Zo(c, u), b = el(c, u), y = yp(c);
  return {
    ok: !0,
    issueKind: "crit",
    issueId: c.id,
    machineActor: r,
    crit: c,
    critIndex: l,
    remedy: u,
    operatorActor: s.actor ?? null,
    operator: s,
    gmOverride: a,
    rollingActor: s.actor ?? r,
    locationKey: f,
    locationCondition: g,
    locationConditionLabel: Ap(g),
    locationConditionModifier: p,
    skillKey: h,
    baseDn: b,
    totalDn: b + p,
    remedyEffect: y
  };
}
async function QM(t = {}, e = {}) {
  var i, a, r;
  const n = await Xl(t, e);
  return n.ok ? {
    ok: !0,
    actor: n.rollingActor,
    payload: {
      intent: "machineRemedy",
      machineActorUuid: n.machineActor.uuid ?? t.machineActorUuid ?? "",
      issueKind: n.issueKind ?? "crit",
      issueId: n.issueId ?? ((i = n.crit) == null ? void 0 : i.id) ?? n.statusId ?? "",
      critId: ((a = n.crit) == null ? void 0 : a.id) ?? "",
      statusId: n.statusId ?? "",
      remedyKey: n.remedy.key,
      operatorActorUuid: ((r = n.operatorActor) == null ? void 0 : r.uuid) ?? "",
      gmOverride: n.gmOverride,
      tags: ["machine", "remedy"],
      edge: { allowed: ["pre", "post"] }
    },
    context: n
  } : n;
}
async function XM(t = {}, e = {}) {
  if (!(t != null && t.ok) && !(t != null && t.machineActor))
    return { ok: !1, reason: "Machine remedy context is not available." };
  if (!t.operatorActor)
    return { ok: !0, skipped: !0 };
  const n = e.spendResource ?? D.spendResource.bind(D), i = String(t.remedy.resource ?? "sa").trim() || "sa", a = Math.max(0, Number(t.remedy.cost ?? 0) || 0);
  return await n(t.operatorActor, {
    resource: i,
    cost: a,
    actionId: t.remedy.actionId,
    actionLabel: t.remedy.actionLabel,
    actionCostLabel: `${a} ${i.toUpperCase()}`,
    actionCategory: t.remedy.category
  });
}
async function xU(t = {}, e = {}) {
  return JM() && !e.spendResource ? YM("spendCost", {
    intent: CU(t)
  }) : XM(t, e);
}
async function ZM(t = {}, e = {}) {
  var l, c, u, d;
  const n = await Xl(t, e);
  if (!n.ok) return n;
  const i = !!e.passed, a = n.remedyEffect ?? yp(n.crit);
  if (n.issueKind === "status") {
    const f = i && a.onSuccess === "clear", m = await BU(n.machineActor, n.statusId, {
      passed: f,
      actorUuid: ((l = n.operatorActor) == null ? void 0 : l.uuid) ?? "",
      gmOverride: n.gmOverride
    });
    return f && await mt({
      actor: n.machineActor,
      statusId: n.statusId,
      active: !1
    }), {
      ok: !0,
      passed: i,
      applied: f,
      machineActor: n.machineActor,
      operatorActor: n.operatorActor,
      statusId: n.statusId,
      remedy: n.remedy,
      context: n,
      metadata: m
    };
  }
  const r = Array.isArray((u = (c = n.machineActor.system) == null ? void 0 : c.mwd) == null ? void 0 : u.crits) ? n.machineActor.system.mwd.crits.slice() : [], s = r[n.critIndex];
  if (!s || s.active === !1)
    return { ok: !1, reason: "That critical effect is no longer active." };
  const o = _U(s, {
    passed: i && a.onSuccess === "clear",
    actorUuid: ((d = n.operatorActor) == null ? void 0 : d.uuid) ?? "",
    gmOverride: n.gmOverride
  });
  return r[n.critIndex] = o, await n.machineActor.update({ "system.mwd.crits": r }), await LU(n.machineActor, o, r), {
    ok: !0,
    passed: i,
    applied: i && a.onSuccess === "clear",
    machineActor: n.machineActor,
    operatorActor: n.operatorActor,
    crit: o,
    remedy: n.remedy,
    context: n
  };
}
async function ev(t = {}, e = {}) {
  return JM() && !e.forceLocal ? YM("applyOutcome", {
    intent: Gh(t),
    passed: !!e.passed
  }) : ZM(t, e);
}
const ig = Object.freeze({
  wheeled: Object.freeze({
    key: "wheeled",
    label: "Wheeled",
    family: "ground",
    doctrine: "Road repositioning and lane control.",
    favoredTerrain: ["road", "urban"],
    adverseTerrain: ["rough", "forest", "water"],
    affordances: ["reposition"]
  }),
  tracked: Object.freeze({
    key: "tracked",
    label: "Tracked",
    family: "ground",
    doctrine: "Stable support platform in difficult ground.",
    favoredTerrain: ["open", "rough", "urban"],
    adverseTerrain: ["water"],
    affordances: ["stabilizedFire", "hullDown"]
  }),
  hover: Object.freeze({
    key: "hover",
    label: "Hover",
    family: "ground",
    doctrine: "Fast open-ground and water maneuver with clutter instability.",
    favoredTerrain: ["open", "water"],
    adverseTerrain: ["forest", "urban", "rough"],
    affordances: ["reposition", "redline"]
  }),
  vtol: Object.freeze({
    key: "vtol",
    label: "VTOL",
    family: "flight",
    doctrine: "Spotting and mobility dominance with fragile lift systems.",
    favoredTerrain: ["open", "urban"],
    adverseTerrain: ["forest"],
    affordances: ["spotting", "ew", "redline"]
  }),
  drone: Object.freeze({
    key: "drone",
    label: "Drone",
    family: "flight",
    doctrine: "Remote support and networked battlefield presence.",
    favoredTerrain: ["open", "urban"],
    adverseTerrain: [],
    affordances: ["spotting", "ew", "network"]
  }),
  aerospace: Object.freeze({
    key: "aerospace",
    label: "Aerospace",
    family: "flight",
    doctrine: "Fast jet-based passes and future strike hooks.",
    favoredTerrain: ["open"],
    adverseTerrain: ["forest", "urban"],
    affordances: ["fastPass", "redline"]
  })
}), qh = Object.freeze([
  "road",
  "open",
  "rough",
  "forest",
  "urban",
  "water"
]), tv = Object.freeze({
  rotor: "Rotor",
  vector: "Vector"
});
function Pu(t = "") {
  return String(t ?? "").trim().toLowerCase();
}
function SS(t = []) {
  const e = Array.isArray(t) ? t : String(t ?? "").split(",");
  return Array.from(new Set(e.map((n) => Pu(n)).filter((n) => qh.includes(n))));
}
function zU(t = "") {
  const e = Pu(t) || "tracked";
  return ig[e] ?? ig.tracked;
}
function nv(t = {}) {
  var r, s;
  const e = (t == null ? void 0 : t.mwd) ?? {}, n = zU(e.movementProfile), i = Pu(e.flightSubtype), a = i ? tv[i] ?? "" : "";
  return {
    key: n.key,
    label: a && n.key === "vtol" ? `${n.label} (${a})` : n.label,
    family: n.family,
    doctrine: n.doctrine,
    favoredTerrain: SS((r = e.favoredTerrain) != null && r.length ? e.favoredTerrain : n.favoredTerrain),
    adverseTerrain: SS((s = e.adverseTerrain) != null && s.length ? e.adverseTerrain : n.adverseTerrain),
    affordances: Array.from(new Set([...e.affordances ?? [], ...n.affordances].map(Pu).filter(Boolean))),
    terrainClasses: [...qh],
    flightSubtype: i
  };
}
const Ca = Object.freeze({
  move: Object.freeze({ id: "move", label: "Move", cost: 1, strain: 0, mode: "ground" }),
  reposition: Object.freeze({ id: "reposition", label: "Tactical Reposition", cost: 1, strain: 0, mode: "ground" }),
  redline: Object.freeze({ id: "redline", label: "Redline", cost: 2, strain: 1, mode: "ground" }),
  fly: Object.freeze({ id: "fly", label: "Flight Move", cost: 1, strain: 0, mode: "flight" }),
  hullDown: Object.freeze({ id: "hullDown", label: "Hull Down", cost: 1, strain: 0, mode: "posture", statusId: "entrenchedHullDown" }),
  brace: Object.freeze({ id: "brace", label: "Brace", cost: 1, strain: 0, mode: "posture", statusId: "braced" }),
  evasiveManeuver: Object.freeze({ id: "evasiveManeuver", label: "Evasive Maneuver", cost: 2, strain: 0, mode: "posture", statusId: "evasiveWeave", includesMovement: !0 }),
  shield: Object.freeze({ id: "shield", label: "Shield", cost: 2, strain: 0, mode: "posture", statusId: "shielded" })
}), FU = Object.freeze(["braced", "entrenchedHullDown", "evasiveWeave", "shielded"]);
function Em(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? Math.max(0, Math.trunc(n)) : e;
}
function ag(t = null) {
  var d, f;
  const e = $l(((d = t == null ? void 0 : t.system) == null ? void 0 : d.movement) ?? {}, {
    actorType: M.actorTypes.vehicle,
    legacyMoves: (f = t == null ? void 0 : t.system) == null ? void 0 : f.moves
  }), n = Rr(t), i = JT(t), a = nv((t == null ? void 0 : t.system) ?? {}), r = Em(n.movementBonus, 0), s = _r(Em(e.flight, 0) + r, n.movementPenalty, {
    immobile: n.immobile
  }), o = _r(Em(e.ground, 0) + r, n.movementPenalty, {
    immobile: n.immobile
  }), l = n.immobile ? "Vehicle is immobilized." : "", c = l || (n.noSprint ? "Redline is blocked by current damage or status effects." : "") || (i.redlineBlocked ? "Vehicle strain is critical; Redline is blocked until reset or recovery." : ""), u = [
    {
      ...Ca.move,
      distance: o,
      hint: o > 0 ? `${o} m movement | stable operation` : "Stable movement",
      disabled: !!l,
      reason: l
    },
    {
      ...Ca.reposition,
      distance: o,
      hint: `${a.label} positioning | no strain`,
      disabled: !!l,
      reason: l
    },
    {
      ...Ca.redline,
      distance: o,
      hint: o > 0 ? `${o} m movement | +1 Strain` : "+1 Strain",
      disabled: !!c,
      reason: c
    },
    {
      ...Ca.hullDown,
      hint: "Prepared position for vehicle survivability and support fire.",
      disabled: !1,
      reason: ""
    },
    {
      ...Ca.brace,
      hint: "Stabilize the platform for controlled fire. +1 AR, no movement.",
      disabled: !1,
      reason: ""
    },
    {
      ...Ca.evasiveManeuver,
      distance: o,
      hint: o > 0 ? `Withdraw safely — move up to ${o} m, gain Evasive (+3 DR / −2 AR)` : "Withdraw and gain Evasive (+3 DR / −2 AR)",
      disabled: !!l,
      reason: l
    },
    {
      ...Ca.shield,
      hint: "Guarded stance: +4 DR / −1 AR. First hit absorbed (−2 damage), then Shield ends.",
      disabled: !1,
      reason: ""
    }
  ];
  return (s > 0 || a.family === "flight") && u.splice(1, 0, {
    ...Ca.fly,
    distance: s,
    hint: s > 0 ? `${s} m flight movement | ${a.label}` : `${a.label} flight movement`,
    disabled: !!l,
    reason: l
  }), u;
}
async function UU(t, { movementKind: e = "", operatorActorUuid: n = "" } = {}) {
  var c, u, d, f;
  const i = String(e ?? "").trim(), a = ag(t).find((m) => m.id === i) ?? null;
  if (!a)
    return (c = ui.notifications) == null || c.warn("Unknown vehicle movement action."), { ok: !1, reason: "Unknown vehicle movement action." };
  if (a.disabled)
    return (u = ui.notifications) == null || u.warn(a.reason || "That vehicle movement action is unavailable."), { ok: !1, reason: a.reason || "That vehicle movement action is unavailable." };
  const r = mi(t), s = await Ut({ machineActor: t, operatorActorUuid: n }), o = (s == null ? void 0 : s.actor) ?? t, l = await D.spendResource(o, {
    token: r,
    resource: "sa",
    cost: a.cost,
    actionId: "move",
    actionLabel: a.label,
    actionCostLabel: `${a.cost} SA`,
    actionCategory: a.cost > 1 ? "complex" : "simple"
  });
  if (!(l != null && l.ok))
    return (d = ui.notifications) == null || d.warn((l == null ? void 0 : l.reason) ?? `Unable to record ${a.label}.`), l;
  if (await D.updateCombatantState(o, {
    token: r,
    mutate: (m, g) => {
      var p, h, b;
      return m.actionState ?? (m.actionState = {}), m.actionState.move = {
        actionId: "move",
        movementKind: a.id,
        label: a.label,
        moved: !["hullDown", "brace", "shield"].includes(a.id),
        round: Number(((p = g == null ? void 0 : g.combat) == null ? void 0 : p.round) ?? 0),
        turn: Number(((h = g == null ? void 0 : g.combat) == null ? void 0 : h.turn) ?? 0),
        combatantId: ((b = g == null ? void 0 : g.combatant) == null ? void 0 : b.id) ?? null
      }, m;
    }
  }), a.strain > 0 && await eO(t, a.strain, { reason: `${a.label} movement` }), a.id === "redline" && await pd(t, {
    reason: "redlineMovement",
    source: "movement",
    duration: "untilNextActivation",
    token: r
  }), a.statusId) {
    for (const m of FU)
      m !== a.statusId && await mt({ actor: t, statusId: m, active: !1 });
    await mt({
      actor: t,
      statusId: a.statusId,
      active: !0,
      metadata: { notes: `Applied from the ${a.label} vehicle action.` }
    });
  }
  return a.id === "evasiveManeuver" ? await ChatMessage.create({
    content: `<div class="mwd-chat-message"><p><strong>${(t == null ? void 0 : t.name) ?? "Vehicle"}</strong> takes an <strong>Evasive Maneuver</strong>. May move up to normal distance — movement does not trigger opportunity or parting attacks from adjacent enemies. Gains <strong>Evasive</strong> (+3 DR / −2 AR) until the start of next activation. Clear the <em>Evasive Weave</em> status at the start of the next turn.</p></div>`,
    speaker: ChatMessage.getSpeaker({ actor: t }),
    flags: { mwd: { messageType: "postureAction", actionId: "evasiveManeuver" } }
  }) : a.id === "shield" ? await ChatMessage.create({
    content: `<div class="mwd-chat-message"><p><strong>${(t == null ? void 0 : t.name) ?? "Vehicle"}</strong> takes a <strong>Shield</strong> posture. Gains <strong>Shielded</strong> (+4 DR / −1 AR) until the start of next activation. The first hit or graze reduces incoming damage by 2, then Shield ends — apply this manually and clear the <em>Shielded</em> status. Clear at the start of the next turn if unused.</p></div>`,
    speaker: ChatMessage.getSpeaker({ actor: t }),
    flags: { mwd: { messageType: "postureAction", actionId: "shield" } }
  }) : (f = ui.notifications) == null || f.info(`${(t == null ? void 0 : t.name) ?? "Vehicle"}: ${a.label} recorded (${a.cost} SA${a.strain > 0 ? `, +${a.strain} Strain` : ""}).`), { ok: !0, action: a, spend: l };
}
const iv = "MachineActions.gmMachineActionRequest", av = "MachineActions.gmMachineActionResponse", KU = 1e4, Uo = /* @__PURE__ */ new Map();
let AS = !1;
const HU = /* @__PURE__ */ new Set(["sensorSweep", "acquireTarget", "sensorLock", "tagTarget", "narcTarget", "ecmSpike"]);
function Nd() {
  var t, e, n;
  return ((t = game.mwd) == null ? void 0 : t.roll) ?? ((n = (e = game.system) == null ? void 0 : e.mwd) == null ? void 0 : n.roll) ?? null;
}
function Pd() {
  var e;
  const t = "MWD roll system not initialized.";
  return (e = ui.notifications) == null || e.error(t), { ok: !1, reason: t };
}
function jU(t) {
  if (!t) throw new Error("MWD machine action requires actor.");
}
function rv(t = {}) {
  var i;
  if (!t || typeof t != "object")
    throw new Error("MWD machine action request must be an object.");
  const e = String(t.intent ?? ((i = t.payload) == null ? void 0 : i.intent) ?? "").trim(), n = String(t.kind ?? (e === "machineAction" ? "action" : "")).trim();
  if (!n) throw new Error("MWD machine action request requires kind.");
  return { ...t, kind: n };
}
function kS(t, e) {
  if (!t || typeof t != "object" || typeof t.ok != "boolean")
    throw new Error(`MWD machine action "${e}" returned an invalid result.`);
  if (t.ok === !1 && !String(t.reason ?? "").trim())
    throw new Error(`MWD machine action "${e}" returned a failure without reason.`);
  return t;
}
function sv(t = null) {
  return (t == null ? void 0 : t.type) === M.actorTypes.vehicle || (t == null ? void 0 : t.type) === M.actorTypes.battlemech;
}
function TS(t = null) {
  var i, a, r, s;
  const e = /* @__PURE__ */ new Set(), n = (o) => {
    const l = String(o ?? "").trim();
    l && e.add(l);
  };
  return n(t == null ? void 0 : t.id), n(t == null ? void 0 : t._id), n(t == null ? void 0 : t.uuid), n((i = t == null ? void 0 : t.actor) == null ? void 0 : i.id), n((a = t == null ? void 0 : t.actor) == null ? void 0 : a.uuid), n((r = t == null ? void 0 : t.baseActor) == null ? void 0 : r.id), n((s = t == null ? void 0 : t.baseActor) == null ? void 0 : s.uuid), e;
}
function GU(t = null, e = null) {
  if (!t || !e) return !1;
  const n = TS(e);
  for (const i of TS(t))
    if (n.has(i)) return !0;
  return !1;
}
function qU(t = "") {
  var i;
  const e = String(t ?? "").trim();
  if (!e) return null;
  const n = (i = globalThis.game) == null ? void 0 : i.users;
  return typeof (n == null ? void 0 : n.get) == "function" ? n.get(e) ?? null : Array.from(n ?? []).find((a) => String((a == null ? void 0 : a.id) ?? "") === e) ?? null;
}
function WU(t = null, e = null) {
  var n, i;
  if (!t || !e) return !1;
  if (t.isGM) return !0;
  if (typeof e.testUserPermission == "function") {
    const a = ((i = (n = globalThis.CONST) == null ? void 0 : n.DOCUMENT_OWNERSHIP_LEVELS) == null ? void 0 : i.OWNER) ?? 3;
    try {
      if (e.testUserPermission(t, a)) return !0;
    } catch {
    }
  }
  return GU(e, t.character);
}
function VU(t = null, e = ((n) => (n = globalThis.game) == null ? void 0 : n.user)()) {
  var a, r;
  if (!t || !e) return !1;
  if (e.isGM || typeof t.testUserPermission != "function") return !0;
  const i = ((r = (a = globalThis.CONST) == null ? void 0 : a.DOCUMENT_OWNERSHIP_LEVELS) == null ? void 0 : r.OWNER) ?? 3;
  try {
    return t.testUserPermission(e, i);
  } catch {
    return !1;
  }
}
async function JU(t = "") {
  const e = String(t ?? "").trim();
  if (!e || typeof fromUuid != "function") return null;
  try {
    return await fromUuid(e);
  } catch {
    return null;
  }
}
function YU() {
  var t, e, n, i, a;
  return ((n = (e = (t = globalThis.foundry) == null ? void 0 : t.utils) == null ? void 0 : e.randomID) == null ? void 0 : n.call(e)) ?? ((a = (i = globalThis.crypto) == null ? void 0 : i.randomUUID) == null ? void 0 : a.call(i)) ?? `${Date.now()}-${Math.random().toString(36).slice(2)}`;
}
function QU(t = null) {
  var e;
  return String(((e = t == null ? void 0 : t.document) == null ? void 0 : e.id) ?? (t == null ? void 0 : t.id) ?? "").trim();
}
function ov(t = "") {
  var n, i, a, r, s, o, l, c, u, d, f, m, g;
  const e = String(t ?? "").trim();
  return e ? ((a = (i = (n = canvas == null ? void 0 : canvas.scene) == null ? void 0 : n.tokens) == null ? void 0 : i.get) == null ? void 0 : a.call(i, e)) ?? ((o = (s = (r = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : r.get) == null ? void 0 : s.call(r, e)) == null ? void 0 : o.document) ?? ((d = (u = (c = (l = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : l.placeables) == null ? void 0 : c.find) == null ? void 0 : u.call(c, (p) => (p == null ? void 0 : p.id) === e)) == null ? void 0 : d.document) ?? ((g = (m = (f = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : f.placeables) == null ? void 0 : m.find) == null ? void 0 : g.call(m, (p) => (p == null ? void 0 : p.id) === e)) ?? null : null;
}
function Vi(t, e = {}) {
  return e.token ?? ov(e.tokenId) ?? mi(t);
}
function XU(t, e = {}) {
  var i, a, r, s, o, l, c, u, d, f, m;
  const n = {
    kind: String(e.kind ?? "").trim(),
    machineActorUuid: String((t == null ? void 0 : t.uuid) ?? e.machineActorUuid ?? "").trim(),
    operatorActorUuid: String(e.operatorActorUuid ?? "").trim(),
    tokenId: QU(e.token),
    attackKind: String(e.attackKind ?? e.actionId ?? "").trim(),
    movementKind: String(e.movementKind ?? e.actionId ?? "").trim(),
    actionId: String(e.actionId ?? ((i = e.action) == null ? void 0 : i.id) ?? ((a = e.action) == null ? void 0 : a.actionKey) ?? "").trim(),
    mode: String(e.mode ?? "").trim(),
    controlIntent: String(e.controlIntent ?? "").trim(),
    intent: String(e.intent ?? "").trim(),
    sourceType: String(e.sourceType ?? "").trim(),
    sourceId: String(e.sourceId ?? e.itemId ?? e.weaponId ?? "").trim(),
    weaponId: String(e.weaponId ?? e.itemId ?? "").trim(),
    groupId: String(e.groupId ?? ((r = e.group) == null ? void 0 : r.id) ?? "").trim(),
    newMode: String(e.newMode ?? e.fireMode ?? "").trim(),
    profileId: String(e.profileId ?? ((s = e.profile) == null ? void 0 : s.id) ?? "").trim(),
    targetTokenId: String(e.targetTokenId ?? "").trim(),
    targetTokenUuid: String(e.targetTokenUuid ?? "").trim(),
    checkKind: String(e.checkKind ?? e.actionId ?? "").trim(),
    issueKind: String(e.issueKind ?? ((o = e.issue) == null ? void 0 : o.issueKind) ?? "").trim(),
    issueId: String(e.issueId ?? ((l = e.issue) == null ? void 0 : l.issueId) ?? e.critId ?? e.statusId ?? "").trim(),
    critId: String(e.critId ?? (((c = e.issue) == null ? void 0 : c.issueKind) === "crit" ? (u = e.issue) == null ? void 0 : u.issueId : "") ?? "").trim(),
    statusId: String(e.statusId ?? (((d = e.issue) == null ? void 0 : d.issueKind) === "status" ? (f = e.issue) == null ? void 0 : f.issueId : "") ?? "").trim(),
    remedyKey: String(e.remedyKey ?? ((m = e.issue) == null ? void 0 : m.remedyKey) ?? "").trim(),
    source: String(e.source ?? "").trim(),
    reason: String(e.reason ?? "").trim(),
    postDangerCard: e.postDangerCard !== !1,
    _gmRouted: !0
  };
  return !n.sourceId && n.weaponId && (n.sourceId = n.weaponId), n.tokenId || delete n.tokenId, n;
}
function ZU(t = {}) {
  return {
    ok: !!(t != null && t.ok),
    reason: String((t == null ? void 0 : t.reason) ?? "").trim(),
    userMessage: String((t == null ? void 0 : t.userMessage) ?? "").trim(),
    skipped: !!(t != null && t.skipped)
  };
}
function eK(t, e = {}) {
  var n;
  return !!((n = globalThis.game) != null && n.user) && !globalThis.game.user.isGM && sv(t) && !(e != null && e._gmRouted) && !VU(t, globalThis.game.user);
}
function tK(t = {}) {
  var a;
  const e = String((t == null ? void 0 : t.requestId) ?? "").trim(), n = String((t == null ? void 0 : t.userId) ?? "").trim();
  if (!e || n !== String(((a = game == null ? void 0 : game.user) == null ? void 0 : a.id) ?? "")) return;
  const i = Uo.get(e);
  i && (clearTimeout(i.timeout), Uo.delete(e), i.resolve(t.result ?? { ok: !1, reason: "GM operation returned no result." }));
}
async function nK(t, e = {}) {
  var a;
  const n = YU(), i = String(((a = game == null ? void 0 : game.user) == null ? void 0 : a.id) ?? "").trim();
  return i ? new Promise((r) => {
    const s = setTimeout(() => {
      Uo.delete(n), r({ ok: !1, reason: "No active GM responded to the machine action request." });
    }, KU);
    Uo.set(n, { resolve: r, timeout: s }), ba.call(iv, {
      requestId: n,
      userId: i,
      request: XU(t, e)
    }) || (clearTimeout(s), Uo.delete(n), r({ ok: !1, reason: "No remote GM is available for that machine action." }));
  }) : { ok: !1, reason: "No active user for GM operation." };
}
async function iK(t = {}) {
  const e = qU(t.userId);
  if (!e) return { ok: !1, reason: "Requesting user could not be resolved." };
  const n = rv(t.request ?? {}), i = await JU(n.machineActorUuid);
  if (!sv(i)) return { ok: !1, reason: "Machine actor could not be resolved." };
  const a = await Ut({
    machineActor: i,
    operatorActorUuid: n.operatorActorUuid
  });
  return a.actor ? WU(e, a.actor) ? {
    ok: !0,
    actor: i,
    request: {
      ...n,
      operatorActorUuid: a.actor.uuid ?? n.operatorActorUuid ?? "",
      token: ov(n.tokenId),
      _gmRouted: !0
    }
  } : { ok: !1, reason: "You do not control the assigned pilot or operator for this machine." } : { ok: !1, reason: a.reason || "No linked operator or pilot actor." };
}
async function aK(t = {}) {
  var n, i, a;
  if (!((n = game == null ? void 0 : game.user) != null && n.isGM)) return;
  let e;
  try {
    const r = await iK(t);
    r.ok ? e = ZU(await dv(r.actor, r.request)) : e = { ok: !1, reason: r.reason ?? "Machine action request was not authorized." };
  } catch (r) {
    console.error("MWD | GM machine action request failed", r), e = { ok: !1, reason: (r == null ? void 0 : r.message) ?? "Machine action GM operation failed." };
  }
  (a = (i = game.socket) == null ? void 0 : i.emit) == null || a.call(i, Go, {
    msg: av,
    data: {
      requestId: t.requestId,
      userId: t.userId,
      result: e
    }
  });
}
async function rK() {
  AS || (AS = !0, await ba.register(iv, {
    condition: (t) => t.isGM,
    multiple: !1,
    callback: (t) => {
      aK(t);
    }
  }), await ba.register(av, {
    condition: () => !0,
    multiple: !0,
    callback: (t) => tK(t)
  }));
}
async function Zl(t, e, n = null) {
  const i = Nd();
  return i != null && i.execute ? { ok: !0, value: await i.execute({ actor: t, payload: e, event: n }) } : Pd();
}
async function rg(t, e) {
  const n = String(e.attackKind ?? e.actionId ?? "").trim(), i = String(e.operatorActorUuid ?? "").trim(), a = Vi(t, e), r = String(e.sourceType ?? "").trim(), s = String(e.sourceId ?? e.itemId ?? e.weaponId ?? "").trim();
  if (r === "mechWeapon" || s) {
    if (!s) throw new Error("Machine mechWeapon attack requires sourceId.");
    return Zl(t, {
      intent: "attack",
      sourceType: "mechWeapon",
      sourceId: s,
      weaponId: s,
      edge: { pool: "physical.grit", allowed: ["pre", "post"] },
      tags: ["combat", "attack", "machine"],
      sourceTokenId: (a == null ? void 0 : a.id) ?? null,
      operatorActorUuid: i
    }, e.event);
  }
  if (n === "melee")
    return typeof t.rollMeleeAttack == "function" && !e.profile && !e.profileId ? { ok: !0, value: await t.rollMeleeAttack({ operatorActorUuid: i }) } : BM(t, {
      profile: e.profile ?? null,
      profileId: e.profileId ?? "",
      operatorActorUuid: i
    });
  const o = String(e.groupId ?? "").trim();
  if (n === "ranged" || n === "group" || o)
    return (t == null ? void 0 : t.type) === M.actorTypes.battlemech ? pS(t, {
      group: e.group ?? null,
      groupId: o,
      token: a,
      operatorActorUuid: i
    }) : (t == null ? void 0 : t.type) === M.actorTypes.vehicle ? HM(t, {
      weaponId: o,
      token: a,
      operatorActorUuid: i
    }) : typeof t.rollRangedAttack == "function" && !e.group && !e.token ? { ok: !0, value: await t.rollRangedAttack({ groupId: o, operatorActorUuid: i }) } : pS(t, {
      group: e.group ?? null,
      groupId: o,
      token: a,
      operatorActorUuid: i
    });
  throw new Error(`Unknown machine attack kind: ${n || "(empty)"}`);
}
async function lv(t, e) {
  var d, f, m, g, p, h, b;
  if ((t == null ? void 0 : t.type) !== M.actorTypes.battlemech)
    return { ok: !1, reason: "Fire modes are only available to BattleMechs." };
  const n = String(e.newMode ?? e.fireMode ?? "").trim();
  if (!OO.includes(n))
    return { ok: !1, reason: `Unknown BattleMech fire mode: ${n || "(empty)"}.` };
  const i = tw(n);
  if (!i.implemented)
    return { ok: !1, reason: `${i.label} is not implemented yet.` };
  const a = String(((f = (d = t.system) == null ? void 0 : d.mwd) == null ? void 0 : f.fireMode) ?? Jt).trim() || Jt;
  if (n === a) return { ok: !0, unchanged: !0 };
  const r = Vi(t, e), s = await Ut({
    machineActor: t,
    operatorActorUuid: String(e.operatorActorUuid ?? "").trim()
  }), o = (s == null ? void 0 : s.actor) ?? t, l = ((m = D.getSnapshot) == null ? void 0 : m.call(D, o, { token: r })) ?? null;
  if (!(l != null && l.hasCombatant))
    return await t.update({ "system.mwd.fireMode": n }), { ok: !0 };
  if (!l.isCurrentTurn)
    return { ok: !1, reason: "Only available during your activation." };
  if ((p = (g = l.state) == null ? void 0 : g.actionState) != null && p.fireModeChangedThisActivation)
    return { ok: !1, reason: "Fire mode can only be changed once per activation." };
  const c = Number(((h = l.state) == null ? void 0 : h.faRemaining) ?? 0) > 0, u = await D.spendResource(o, {
    token: r,
    resource: c ? "fa" : "sa",
    cost: 1,
    actionId: "changeFireMode",
    actionLabel: "Change Fire Mode",
    actionCostLabel: c ? "Free" : "1 SA",
    actionCategory: "free"
  });
  return u != null && u.ok ? (await ((b = D._applyActionState) == null ? void 0 : b.call(D, o, {
    token: r,
    actionId: "changeFireMode",
    snapshot: u.snapshot
  })), await t.update({ "system.mwd.fireMode": n }), { ok: !0 }) : u;
}
async function cv(t, e) {
  const n = String(e.movementKind ?? e.actionId ?? "").trim();
  if (!n) throw new Error("Machine movement action requires movementKind or actionId.");
  const i = {
    movementKind: n,
    operatorActorUuid: String(e.operatorActorUuid ?? "").trim()
  };
  return (t == null ? void 0 : t.type) === M.actorTypes.vehicle ? UU(t, i) : (t == null ? void 0 : t.type) === M.actorTypes.battlemech ? zF(t, i) : { ok: !1, reason: "actor-not-machine", userMessage: "That actor is not a machine." };
}
async function Wc(t, e) {
  const n = String(e.intent ?? "").trim();
  if (!["acquire", "targeting", "breakLock", "defensiveJink"].includes(n)) return null;
  const i = Vi(t, e), a = Ml({ actor: t, token: i }), r = String(e.targetTokenUuid ?? "").trim(), s = String(e.targetTokenId ?? "").trim(), o = r || s ? (a.rows ?? []).find(
    (c) => r && (c == null ? void 0 : c.targetTokenUuid) === r || s && (c == null ? void 0 : c.targetTokenId) === s
  ) ?? null : n === "breakLock" || n === "defensiveJink" ? fv(a) : jh(a, n);
  return o ? (n === "breakLock" || n === "defensiveJink" ? !0 : n === "targeting" ? o.canTarget : o.canAcquire) ? Zl(t, {
    intent: n,
    actionId: n,
    sourceTokenId: (i == null ? void 0 : i.id) ?? null,
    targetTokenId: o.targetTokenId,
    targetTokenUuid: o.targetTokenUuid,
    operatorActorUuid: String(e.operatorActorUuid ?? "").trim()
  }, e.event) : {
    ok: !1,
    reason: "target-not-eligible",
    userMessage: n === "targeting" ? "That target is not ready for targeting data yet." : "That target cannot advance its detection state right now."
  } : { ok: !1, reason: "missing-target", userMessage: `No targeted token is ready to ${n === "targeting" ? "generate targeting data" : n === "breakLock" ? "break lock" : n === "defensiveJink" ? "jink" : "acquire"}.` };
}
async function uv(t, e) {
  const n = String(e.checkKind ?? e.actionId ?? "").trim();
  if (!["shutdown", "explosion"].includes(n))
    return { ok: !1, reason: "unknown-heat-danger-check", userMessage: "Unknown heat danger check." };
  const i = ma(t);
  if (!i.inDanger || !i.dangerChecks)
    return {
      ok: !1,
      reason: "heat-not-in-danger",
      userMessage: "Heat danger checks are only available while the BattleMech is in Danger heat."
    };
  const a = n === "shutdown" ? Math.max(1, Number(i.dangerChecks.shutdownDN ?? 1) || 1) : Math.max(1, Number(i.dangerChecks.explosionDN ?? 1) || 1), r = Vi(t, e);
  return Zl(t, {
    intent: "heatDangerCheck",
    checkKind: n,
    dn: a,
    tags: ["machine", "heat", "danger", n],
    edge: { allowed: [] },
    sourceTokenId: (r == null ? void 0 : r.id) ?? null
  }, e.event);
}
const sK = Object.freeze({
  walk: "walk",
  safeThrust: "walk",
  run: "run",
  jumpMove: "jump",
  sprint: "sprint",
  dropProne: "prone",
  evasiveManeuver: "evasiveManeuver",
  shield: "shield"
}), oK = Object.freeze({
  walk: "move",
  safeThrust: "move",
  run: "reposition",
  sprint: "redline",
  brace: "brace",
  hullDown: "hullDown",
  evasiveManeuver: "evasiveManeuver",
  shield: "shield"
});
function lK(t, e = "") {
  const n = String(e ?? "").trim();
  return (t == null ? void 0 : t.type) === M.actorTypes.vehicle ? oK[n] ?? "" : (t == null ? void 0 : t.type) === M.actorTypes.battlemech ? sK[n] ?? "" : "";
}
function cK(t, e = "") {
  var i, a;
  const n = String(((i = t == null ? void 0 : t.implementation) == null ? void 0 : i.reason) ?? e ?? "").trim() || `${(t == null ? void 0 : t.label) ?? "That machine action"} is not implemented yet.`;
  return (a = ui.notifications) == null || a.warn(n), { ok: !1, reason: n, userMessage: n, actionId: (t == null ? void 0 : t.key) ?? "" };
}
async function Nm(t, e, n = {}) {
  var s;
  const i = Vi(t, n), a = await Rd(t, e, {
    token: i,
    operatorActorUuid: String(n.operatorActorUuid ?? "").trim()
  });
  if (a && a.ok === !1) return a;
  const r = e.notes || "Action recorded.";
  return (s = ui.notifications) == null || s.info(`${e.label}: ${r}`), {
    ok: !0,
    actionId: e.key,
    costPaid: !!(a && a.ok && !a.skipped),
    rolled: !1,
    stateChanges: [],
    log: { title: e.label, message: r }
  };
}
async function Po(t, e, n = {}) {
  var s;
  const i = String(n.skillKey ?? e.skillKey ?? "").trim(), a = String(n.attributeKey ?? n.attrKey ?? e.attributeKey ?? "").trim();
  if (!i || !a) {
    const o = `${e.label} needs a skill and machine attribute before it can be rolled.`;
    return (s = ui.notifications) == null || s.warn(o), { ok: !1, reason: o, userMessage: o, actionId: e.key };
  }
  const r = Vi(t, n);
  return Zl(t, {
    intent: "skill",
    key: i,
    attrKey: a,
    machineAttributeKey: a,
    operatorActorUuid: String(n.operatorActorUuid ?? "").trim(),
    sourceTokenId: (r == null ? void 0 : r.id) ?? null,
    targetTokenId: String(n.targetTokenId ?? "").trim() || null,
    targetTokenUuid: String(n.targetTokenUuid ?? "").trim() || null,
    machineActionKey: e.key,
    quickAction: { title: e.label },
    edge: { allowed: ["pre", "post"] },
    tags: ["machine", e.resolver, ...e.tags ?? []]
  }, n.event);
}
async function uK(t, e, n = {}) {
  const i = Vi(t, n), a = await Rd(t, e, {
    token: i,
    operatorActorUuid: String(n.operatorActorUuid ?? "").trim()
  });
  return a && a.ok === !1 ? a : {
    ...await uv(t, { ...n, checkKind: "shutdown" }),
    actionId: e.key,
    costPaid: !!(a && a.ok && !a.skipped)
  };
}
async function wS(t, e, n = {}) {
  var a;
  const i = e.key;
  if (i === "swat") return fK(t, e, n);
  if (i === "goDark") {
    const r = Vi(t, n), s = await Rd(t, e, {
      token: r,
      operatorActorUuid: String(n.operatorActorUuid ?? "").trim()
    });
    if (s && s.ok === !1) return s;
    const o = await vL(t, {
      reason: "goDark",
      source: e.key,
      token: r
    }), l = o.suppressed ? "Lifecycle emissions cleared, but suppressed stealth remains offline." : "Signature posture reset.";
    return (a = ui.notifications) == null || a.info(`${e.label}: ${l}`), {
      ...o,
      ok: !0,
      actionId: e.key,
      costPaid: !!(s && s.ok && !s.skipped),
      rolled: !1,
      stateChanges: ["stealthLifecycle"],
      log: { title: e.label, message: l }
    };
  }
  if (i === "acquireTarget" || i === "sensorLock") {
    const r = await Wc(t, { ...n, intent: "acquire" });
    return r ? (await Aw(t, {
      rating: 1,
      reason: i,
      source: "system",
      duration: "untilNextActivation",
      token: Vi(t, n)
    }), r) : Vc(t, {
      actionId: "acquireTarget",
      token: n.token ?? null,
      operatorActorUuid: n.operatorActorUuid
    });
  }
  return i === "generateFireSolution" ? await Wc(t, { ...n, intent: "targeting" }) ?? Vc(t, {
    actionId: "generateFireSolution",
    token: n.token ?? null,
    operatorActorUuid: n.operatorActorUuid
  }) : i === "breakLock" || i === "defensiveJink" ? Wc(t, { ...n, intent: i }) : i === "sensorSweep" || i === "assess" || i === "epmFilter" || i === "tagTarget" || i === "narcTarget" || i === "shareTargetingData" || i === "ecmSpike" || i === "suppressBeacon" ? Vc(t, {
    actionId: i === "assess" ? "sensorSweep" : i,
    token: n.token ?? null,
    operatorActorUuid: n.operatorActorUuid
  }) : Po(t, e, n);
}
function dK(t = {}) {
  var e, n;
  return Array.isArray(t.selectedTokens) ? t.selectedTokens : Array.isArray((n = (e = globalThis.canvas) == null ? void 0 : e.tokens) == null ? void 0 : n.controlled) ? globalThis.canvas.tokens.controlled : [];
}
function mK(t = [], e = {}) {
  const n = String(e.targetTokenUuid ?? "").trim(), i = String(e.targetTokenId ?? "").trim(), a = String(e.targetActorUuid ?? e.actorUuid ?? "").trim();
  return n || i || a ? t.find(
    (r) => {
      var s;
      return n && r.tokenUuid === n || i && r.tokenId === i || a && ((s = r.actor) == null ? void 0 : s.uuid) === a;
    }
  ) ?? null : t.find((r) => r.source === "targeted") ?? t.find((r) => r.source === "selected") ?? t[0] ?? null;
}
async function fK(t, e, n = {}) {
  var s, o, l, c, u, d, f, m;
  const i = Vi(t, n), a = FO(t, {
    machineToken: i,
    targetTokens: n.targetTokens ?? ((o = (s = globalThis.game) == null ? void 0 : s.user) == null ? void 0 : o.targets) ?? null,
    selectedTokens: dK(n)
  }), r = mK(a, n);
  if (!r) {
    const g = "No attached battle armor is eligible for Swat.";
    return (l = ui.notifications) == null || l.warn(g), { ok: !1, reason: g, userMessage: g, actionId: e.key };
  }
  return Zl(t, {
    intent: "skill",
    key: e.skillKey,
    attrKey: e.attributeKey,
    machineAttributeKey: e.attributeKey,
    operatorActorUuid: String(n.operatorActorUuid ?? "").trim(),
    sourceTokenId: (i == null ? void 0 : i.id) ?? null,
    targetTokenId: r.tokenId || null,
    targetTokenUuid: r.tokenUuid || null,
    targetActorUuid: ((c = r.actor) == null ? void 0 : c.uuid) ?? "",
    machineActionKey: e.key,
    quickAction: {
      title: e.label,
      battleArmorSwat: {
        armorItemId: ((u = r.armor) == null ? void 0 : u.id) ?? ((d = r.armor) == null ? void 0 : d.armorId) ?? "",
        armorItemUuid: ((f = r.armor) == null ? void 0 : f.uuid) ?? "",
        attachedToTokenUuid: r.attachedToTokenUuid,
        source: r.source,
        defender: {
          actorUuid: ((m = r.actor) == null ? void 0 : m.uuid) ?? "",
          attrKey: "reflexes",
          skillKey: "athletics"
        },
        success: {
          detach: !0,
          marginTwoPlus: ["prone", "collisionDamage"]
        }
      }
    },
    edge: { allowed: ["pre", "post"] },
    tags: ["machine", "physical", "swat", "battleArmor", "opposed"]
  }, n.event);
}
async function gK(t, e = {}) {
  var r, s, o, l, c;
  const n = String(e.actionId ?? ((r = e.payload) == null ? void 0 : r.actionId) ?? ((s = e.action) == null ? void 0 : s.id) ?? "").trim(), i = _l(n);
  if (i.key === "none" && n && n !== "none") {
    const u = `Unknown machine action: ${n}.`;
    return (o = ui.notifications) == null || o.warn(u), { ok: !1, reason: u, userMessage: u, actionId: n };
  }
  const a = String(((l = i.implementation) == null ? void 0 : l.state) ?? "ready").trim() || "ready";
  if (a === "disabled" || a === "stub")
    return cK(i);
  switch (i.resolver) {
    case "movement": {
      if (i.key === "pilotingCheck")
        return Wh(t, {
          machineAttributeKey: String(e.machineAttributeKey ?? e.attributeKey ?? i.attributeKey ?? M.actorAttributes.handling).trim(),
          skillKey: String(e.skillKey ?? i.skillKey ?? "piloting").trim(),
          operatorActorUuid: String(e.operatorActorUuid ?? "").trim()
        });
      const u = String(e.movementKind ?? "").trim() || lK(t, i.key);
      return u ? cv(t, { ...e, movementKind: u }) : Nm(t, i, e);
    }
    case "attack":
      return i.key === "rangedAttack" ? rg(t, { ...e, attackKind: "ranged" }) : i.key === "physicalAttack" ? rg(t, { ...e, attackKind: "melee" }) : i.key === "chargeAttack" ? Kh(t, { ...e }) : Po(t, i, e);
    case "targeting":
      return wS(t, i, e);
    case "remediation":
      return i.key === "emergencyRepair" && (e.issue || e.issueKind || e.issueId || e.critId || e.statusId || e.remedyKey) ? gv(t, {
        issue: e.issue ?? null,
        issueKind: e.issueKind ?? "",
        issueId: e.issueId ?? "",
        remedyKey: e.remedyKey ?? "",
        operatorActorUuid: e.operatorActorUuid ?? ""
      }) : i.key === "swat" ? wS(t, i, e) : Po(t, i, e);
    case "recovery":
      return i.key === "avoidShutdown" ? uK(t, i, e) : Po(t, i, e);
    case "interaction":
      if (i.key === "selectFireMode") {
        const u = String(e.newMode ?? e.fireMode ?? "").trim();
        if (!u) {
          const d = "Select Fire Mode needs a fire mode value.";
          return (c = ui.notifications) == null || c.warn(d), { ok: !1, reason: d, userMessage: d, actionId: i.key };
        }
        return lv(t, { ...e, newMode: u });
      }
      return Nm(t, i, e);
    case "action":
    default:
      return i.roll || i.skillKey || i.attributeKey ? Po(t, i, e) : Nm(t, i, e);
  }
}
async function dv(t, e = {}) {
  jU(t);
  const n = rv(e);
  if (eK(t, n))
    return kS(await nK(t, n), n.kind);
  let i;
  switch (n.kind) {
    case "action":
      i = await gK(t, n);
      break;
    case "attack":
      i = await rg(t, n);
      break;
    case "changeFireMode":
      i = await lv(t, n);
      break;
    case "movement":
      i = await cv(t, n);
      break;
    case "piloting":
      i = await Wh(t, {
        machineAttributeKey: n.machineAttributeKey,
        skillKey: n.skillKey,
        operatorActorUuid: n.operatorActorUuid
      });
      break;
    case "skillCheck":
      i = await pK(t, {
        machineAttributeKey: n.machineAttributeKey,
        skillKey: n.skillKey,
        operatorActorUuid: n.operatorActorUuid
      });
      break;
    case "ew":
      i = await Wc(t, n) ?? await Vc(t, {
        action: n.action ?? null,
        actionId: n.actionId ?? "",
        token: n.token ?? null,
        operatorActorUuid: n.operatorActorUuid
      });
      break;
    case "repair":
      i = await gv(t, {
        issue: n.issue ?? null,
        issueKind: n.issueKind ?? "",
        issueId: n.issueId ?? "",
        remedyKey: n.remedyKey ?? "",
        operatorActorUuid: n.operatorActorUuid
      });
      break;
    case "heatDangerCheck":
      i = await uv(t, n);
      break;
    case "resolvePendingHeat":
      i = await Lp(t, {
        token: n.token ?? null,
        source: n.source ?? n.reason ?? "sheet control",
        activation: n.activation ?? null,
        postDangerCard: n.postDangerCard ?? !0
      });
      break;
    case "resolvePendingStrain":
      i = await tO(t, {
        reason: n.reason ?? "sheet control"
      });
      break;
    default:
      throw new Error(`Unknown machine action kind: ${n.kind}`);
  }
  return kS(i, n.kind);
}
const MS = Object.freeze({
  execute: dv
});
function mv(t = {}) {
  return t.category === "reaction" ? "Reaction" : t.cost ? `${t.cost} ${String(t.resource ?? "sa").toUpperCase()}` : t.category === "narrative" ? "Narrative" : "Free";
}
function vS(t = {}) {
  return t.category === "complex" ? "Complex" : t.category === "reaction" ? "Reaction" : t.resource === "fa" ? "FA" : mv(t);
}
function ni({
  id: t,
  purpose: e = "",
  targetMode: n = "none",
  execution: i = "skill",
  enabled: a = !0,
  reason: r = "",
  mechanics: s = ""
} = {}) {
  const o = _l(t);
  return {
    id: t,
    actionKey: o.key,
    intent: o.intent || t,
    label: o.label,
    actionType: vS(o),
    attributeKey: o.attributeKey,
    skillKey: o.skillKey,
    targetMode: n,
    execution: i,
    purpose: e || o.notes,
    hint: [
      vS(o),
      o.attributeKey && o.skillKey ? `${o.attributeKey} + ${o.skillKey}` : "",
      e || o.notes,
      s
    ].filter(Boolean).join(" | "),
    disabled: !a,
    reason: r || (a ? "" : "This EW action is not available right now."),
    mechanics: s
  };
}
function fv(t = {}) {
  return Array.isArray(t == null ? void 0 : t.rows) ? t.rows.find((e) => (e == null ? void 0 : e.targetTokenUuid) || (e == null ? void 0 : e.targetTokenId)) ?? null : null;
}
async function Rd(t, e, { token: n = null, operatorActorUuid: i = "" } = {}) {
  var u, d;
  const a = jT(t, e == null ? void 0 : e.key, {
    payload: { actionId: e == null ? void 0 : e.key }
  }), r = a ? {
    ...e,
    cost: Number.isFinite(Number(a.cost)) ? Math.max(0, Number(a.cost)) : e.cost,
    resource: String(a.resource ?? e.resource ?? "sa").trim() || "sa",
    category: String(a.category ?? e.category ?? "simple").trim() || "simple"
  } : e;
  if (!(r != null && r.cost)) return { ok: !0, skipped: !0 };
  const s = await Ut({
    machineActor: t,
    operatorActorUuid: i
  }), o = (s == null ? void 0 : s.actor) ?? t, l = ((u = D.getSnapshot) == null ? void 0 : u.call(D, o, { token: n })) ?? null;
  if (!(l != null && l.hasCombatant)) return { ok: !0, skipped: !0 };
  const c = await D.spendResource(o, {
    token: n,
    resource: r.resource,
    cost: r.cost,
    actionId: r.key,
    actionLabel: r.label,
    actionCostLabel: mv(r),
    actionCategory: r.category
  });
  return c != null && c.ok || (d = ui.notifications) == null || d.warn((c == null ? void 0 : c.reason) ?? `Unable to record ${r.label}.`), c;
}
async function Wh(t, {
  machineAttributeKey: e = M.actorAttributes.handling,
  skillKey: n = "piloting",
  operatorActorUuid: i = ""
} = {}) {
  const a = Nd();
  if (!(a != null && a.execute)) return Pd();
  const r = String(e ?? M.actorAttributes.handling).trim() || M.actorAttributes.handling, s = String(n ?? "piloting").trim() || "piloting";
  return await a.execute({
    actor: t,
    payload: {
      intent: "skill",
      key: s,
      noSkill: s === "none",
      machineAttributeKey: r,
      operatorActorUuid: String(i ?? "").trim(),
      quickAction: { title: z.actor.vehicle.quickActions.pilotingCheck },
      edge: { allowed: ["pre", "post"] },
      tags: ["machine", "skill"]
    }
  }), { ok: !0 };
}
async function pK(t, { machineAttributeKey: e = "", skillKey: n = "", operatorActorUuid: i = "" } = {}) {
  return Wh(t, {
    machineAttributeKey: e,
    skillKey: n,
    operatorActorUuid: i
  });
}
function Gr(t, { token: e = null, includeDisabled: n = !1 } = {}) {
  const i = e ?? mi(t), a = Ml({ actor: t, token: i }), r = !!a.hasTargets, s = a.capabilities ?? Hh(t), o = !!s.tag, l = !!s.narc, c = !!s.c3, u = [
    ni({
      id: "sensorSweep",
      purpose: "General scan: reveal hidden units, detect signatures, identify contacts, and read the battlefield.",
      targetMode: "none",
      execution: "skill"
    }),
    ni({
      id: "acquireTarget",
      purpose: "Improve detection state on the first eligible targeted token.",
      targetMode: "acquire",
      execution: "intent",
      enabled: a.canAcquireAny,
      reason: "No targeted token can currently advance detection state.",
      mechanics: "Automated detection-state update on success."
    }),
    ni({
      id: "generateFireSolution",
      purpose: "Create short-lived targeting data from an existing Track or Lock.",
      targetMode: "targeting",
      execution: "intent",
      enabled: a.canTargetAny,
      reason: "Track or Lock is required before generating targeting data.",
      mechanics: "Automated targeting-data packet on success."
    }),
    ni({
      id: "ecmSpike",
      purpose: "Offensive EW: jam or disrupt a specific target.",
      targetMode: "any",
      execution: "skill",
      enabled: r,
      reason: "Target a token before launching an ECM Spike.",
      mechanics: "Roll only; ECM state effects are not automated yet."
    }),
    ni({
      id: "epmFilter",
      purpose: "Defensive remediation: remove or reduce ECM Jamming.",
      targetMode: "none",
      execution: "skill",
      mechanics: "Roll only unless launched as a critical/status remedy."
    }),
    ni({
      id: "breakLock",
      purpose: "Defensive action: degrade an attacker's detection state.",
      targetMode: "anyOptional",
      execution: "intent",
      mechanics: "Automated on success: selected observer's detection state on this machine degrades one step."
    }),
    ni({
      id: "defensiveJink",
      purpose: "Defensive reaction: reduce an enemy fire-solution packet.",
      targetMode: "anyOptional",
      execution: "intent",
      mechanics: "Automated: selected observer's targetingData packet against this machine is reduced by 1."
    }),
    ni({
      id: "suppressBeacon",
      purpose: "Suppress beacon-based targeting support such as NARC or TAG.",
      targetMode: "any",
      execution: "skill",
      enabled: r,
      reason: "Target a token before suppressing a beacon.",
      mechanics: "Roll only; beacon suppression is not automated yet."
    }),
    ni({
      id: "swat",
      purpose: "Physical removal action for BattleArmor, NARC, or similar attachments.",
      targetMode: "anyOptional",
      execution: "skill",
      mechanics: "Roll only; removal is handled narratively or by status changes."
    }),
    ni({
      id: "tagTarget",
      purpose: "Apply a TAG enabler flag for guided systems.",
      targetMode: "any",
      execution: "skill",
      enabled: r && o,
      reason: o ? "Target a token before using TAG." : "Requires a mounted TAG Designator weapon.",
      mechanics: "Gunnery roll; applies TAG designation to target on completion."
    }),
    ni({
      id: "narcTarget",
      purpose: "Attach a NARC beacon to target for persistent guided-system support.",
      targetMode: "any",
      execution: "skill",
      enabled: r && l,
      reason: l ? "Target a token before using NARC." : "Requires a mounted NARC launcher weapon.",
      mechanics: "Gunnery roll; attaches NARC beacon to target on completion."
    }),
    ni({
      id: "shareTargetingData",
      purpose: "Share best detection state and best eligible packet through C3 or a similar network.",
      targetMode: "none",
      execution: "narrative",
      enabled: c,
      reason: "Requires an installed C3 asset module.",
      mechanics: "Provider-driven; no roll required."
    })
  ];
  return n ? u : u.filter((d) => !d.disabled);
}
async function Vc(t, {
  action: e = null,
  actionId: n = "",
  token: i = null,
  operatorActorUuid: a = ""
} = {}) {
  var y, S, A;
  const r = i ?? mi(t), s = Ml({ actor: t, token: r }), o = Gr(t, { token: r, includeDisabled: !0 }), l = e ?? o.find((T) => String(T.id ?? "").trim() === String(n ?? "").trim()) ?? (o.length === 1 ? o[0] : null);
  if (!l || l.disabled) {
    const T = z.actor.vehicle.quickActions.errors.noSensorSweep;
    return (y = ui.notifications) == null || y.warn((l == null ? void 0 : l.reason) || T), { ok: !1, reason: T };
  }
  const c = { tagTarget: "tag", narcTarget: "narc" }, u = { tagTarget: "tagged", narcTarget: "narced" }, d = c[l.id] ?? null, f = d ? nd(t, { canonicalType: M.itemType.mechWeapon }).filter((T) => {
    var C;
    return (C = T.isActive) == null ? void 0 : C.call(T);
  }).find((T) => Tp(T, d)) ?? null : null;
  let m = null;
  if (l.targetMode === "acquire" || l.targetMode === "targeting" ? m = jh(s, l.intent) : (l.targetMode === "any" || l.targetMode === "anyOptional") && (m = fv(s)), (l.targetMode === "acquire" || l.targetMode === "targeting" || l.targetMode === "any") && !m) {
    const T = "No targeted token is ready for that EW action.";
    return (S = ui.notifications) == null || S.warn(T), { ok: !1, reason: T };
  }
  if (l.execution === "narrative") {
    const T = _l(l.actionKey), C = await Rd(t, T, { token: r, operatorActorUuid: a });
    return C && C.ok === !1 ? C : ((A = ui.notifications) == null || A.info(`${l.label}: ${l.mechanics || "No roll required."}`), { ok: !0, action: l, target: m, narrative: !0 });
  }
  const g = Nd();
  if (!(g != null && g.execute)) return Pd();
  const p = l.execution === "intent" ? {
    intent: l.intent,
    actionId: l.actionKey,
    sourceTokenId: (r == null ? void 0 : r.id) ?? null,
    targetTokenId: (m == null ? void 0 : m.targetTokenId) ?? null,
    targetTokenUuid: (m == null ? void 0 : m.targetTokenUuid) ?? null,
    operatorActorUuid: String(a ?? "").trim(),
    edge: { allowed: ["pre", "post"] },
    tags: ["machine", "ew", l.id]
  } : {
    intent: "skill",
    key: l.skillKey,
    attrKey: l.attributeKey,
    machineAttributeKey: l.attributeKey,
    operatorActorUuid: String(a ?? "").trim(),
    sourceTokenId: (r == null ? void 0 : r.id) ?? null,
    targetTokenId: (m == null ? void 0 : m.targetTokenId) ?? null,
    targetTokenUuid: (m == null ? void 0 : m.targetTokenUuid) ?? null,
    machineActionKey: l.actionKey,
    weaponId: (f == null ? void 0 : f.id) ?? null,
    quickAction: {
      title: l.label,
      ewAction: {
        id: l.id,
        actionType: l.actionType,
        purpose: l.purpose,
        mechanics: l.mechanics
      }
    },
    edge: { allowed: ["pre", "post"] },
    tags: ["machine", "ew", l.id]
  };
  if (!await g.execute({
    actor: t,
    payload: p
  })) return { ok: !1, cancelled: !0, reason: "EW action was cancelled." };
  HU.has(l.id) && await Aw(t, {
    rating: 1,
    reason: l.id,
    source: "system",
    duration: "untilNextActivation",
    token: r
  });
  const b = u[l.id] ?? null;
  if (b && (m != null && m.targetTokenUuid)) {
    const T = await fromUuid(m.targetTokenUuid), C = (T == null ? void 0 : T.actor) ?? T;
    C && await C.toggleStatusEffect(b, { active: !0, overlay: !1 });
  }
  return { ok: !0, action: l, target: m };
}
function Dd(t) {
  return kU(t);
}
async function gv(t, {
  issue: e = null,
  issueKind: n = "",
  issueId: i = "",
  remedyKey: a = "",
  operatorActorUuid: r = ""
} = {}) {
  var u, d, f;
  const s = Dd(t), o = e ?? s.find(
    (m) => String(m.issueKind ?? "").trim() === String(n ?? "").trim() && String(m.issueId ?? "").trim() === String(i ?? "").trim()
  ) ?? (s.length === 1 ? s[0] : null);
  if (!o) {
    const m = "No active criticals or repairable statuses are available.";
    return (u = ui.notifications) == null || u.warn(m), { ok: !1, reason: m };
  }
  const l = await QM({
    machineActorUuid: (t == null ? void 0 : t.uuid) ?? "",
    issueKind: o.issueKind,
    issueId: o.issueId,
    critId: o.issueKind === "crit" ? o.issueId : "",
    statusId: o.issueKind === "status" ? o.issueId : "",
    remedyKey: String(a ?? "").trim() || o.remedyKey,
    operatorActorUuid: String(r ?? "").trim()
  }, {
    gmOverride: !!((d = game.user) != null && d.isGM)
  });
  if (!l.ok)
    return (f = ui.notifications) == null || f.warn(l.reason ?? "Unable to launch the critical repair action."), l;
  const c = Nd();
  return c != null && c.execute ? (await c.execute({
    actor: l.actor,
    payload: l.payload
  }), { ok: !0, issue: o, request: l }) : Pd();
}
function Sn(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function hK(t = null, e = "") {
  var i, a, r, s;
  const n = String(e ?? "").trim();
  return !t || !n ? 0 : Math.max(0, Sn(
    ((i = t.getSkillRating) == null ? void 0 : i.call(t, n)) ?? ((s = (r = (a = t.system) == null ? void 0 : a.skills) == null ? void 0 : r[n]) == null ? void 0 : s.rating),
    0
  ));
}
function bK(t = null) {
  const e = /* @__PURE__ */ new Map([
    ["strength", { label: "Strength", skills: [] }],
    ["reflexes", { label: "Reflexes", skills: [] }],
    ["intelligence", { label: "Intelligence", skills: [] }],
    ["guts", { label: "Guts", skills: [] }],
    ["charisma", { label: "Charisma", skills: [] }]
  ]);
  for (const n of tn) {
    const i = hK(t, n.code);
    if (i <= 0) continue;
    const a = e.get(n.attribute);
    a && a.skills.push({ ...n, rating: i });
  }
  return Array.from(e.values()).filter((n) => n.skills.length);
}
function yK(t) {
  return String(t ?? "").replace(/<[^>]*>/g, " ").replace(/\s+/g, " ").trim();
}
function SK(t, e = 180) {
  const n = yK(t);
  return n ? n.length <= e ? n : `${n.slice(0, Math.max(0, e - 3)).trim()}...` : "";
}
function sg(t = []) {
  return t.map((e) => String(e ?? "").trim()).filter(Boolean);
}
function CS(t = null) {
  const e = t == null ? void 0 : t.items;
  return e ? Array.isArray(e) ? e : Array.isArray(e.contents) ? e.contents : typeof e[Symbol.iterator] == "function" ? Array.from(e) : [] : [];
}
function IS(t = []) {
  return t.filter((e) => e && e.value !== void 0 && e.value !== null && String(e.value).trim() !== "").map((e) => ({
    label: String(e.label ?? "").trim(),
    value: String(e.value ?? "").trim(),
    emphasis: e.emphasis ?? ""
  }));
}
function AK(t = []) {
  return sg(t).map((e) => ({ label: e }));
}
function kK(t = []) {
  return t.filter((e) => e && e.value !== void 0 && e.value !== null && String(e.value).trim() !== "").map((e) => ({
    label: String(e.label ?? "").trim(),
    value: String(e.value ?? "").trim()
  }));
}
const TK = z.mwd.armorMitigationType, wK = z.item.gear.categoryLabels, MK = z.item.consumable.categoryLabels;
function kn(t) {
  return String(t ?? "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#39;");
}
function ES(t) {
  var n, i;
  const e = (i = (n = t == null ? void 0 : t.system) == null ? void 0 : n.quickActions) == null ? void 0 : i.movement;
  return Array.isArray(e) && e.length ? e : Lh(t);
}
function vK() {
  var t, e, n;
  return ((t = game.mwd) == null ? void 0 : t.machineActions) ?? ((n = (e = game.system) == null ? void 0 : e.mwd) == null ? void 0 : n.machineActions) ?? null;
}
async function ur(t, e = {}) {
  const n = vK();
  if (!(n != null && n.execute)) throw new Error("MWD machine action service not initialized.");
  return n.execute(t, e);
}
function og(t = null) {
  var i, a, r, s;
  const e = /* @__PURE__ */ new Set();
  if (!t) return e;
  const n = (o) => {
    const l = String(o ?? "").trim();
    l && e.add(l);
  };
  return n(t.id), n(t.uuid), n((i = t.actor) == null ? void 0 : i.id), n((a = t.actor) == null ? void 0 : a.uuid), n((r = t.baseActor) == null ? void 0 : r.id), n((s = t.baseActor) == null ? void 0 : s.uuid), e;
}
async function CK(t = null) {
  var n, i, a, r, s;
  const e = String(
    ((i = (n = t == null ? void 0 : t.system) == null ? void 0 : n.pilot) == null ? void 0 : i.uuid) ?? ((s = (r = (a = t == null ? void 0 : t.system) == null ? void 0 : a.mwd) == null ? void 0 : r.pilot) == null ? void 0 : s.uuid) ?? ""
  ).trim();
  if (!e || typeof fromUuid != "function") return null;
  try {
    const o = await fromUuid(e);
    return (o == null ? void 0 : o.actor) ?? (o == null ? void 0 : o.baseActor) ?? o ?? null;
  } catch {
    return null;
  }
}
async function IK(t = null, e = null) {
  var r, s, o, l, c;
  const n = String(
    ((s = (r = t == null ? void 0 : t.system) == null ? void 0 : r.pilot) == null ? void 0 : s.uuid) ?? ((c = (l = (o = t == null ? void 0 : t.system) == null ? void 0 : o.mwd) == null ? void 0 : l.pilot) == null ? void 0 : c.uuid) ?? ""
  ).trim();
  if (!e) return !1;
  const i = og(e);
  if (i.has(n)) return !0;
  const a = await CK(t);
  if (!a) return !1;
  for (const u of og(a))
    if (i.has(u)) return !0;
  return !1;
}
function EK() {
  var i, a, r;
  const t = [], e = /* @__PURE__ */ new Set(), n = ({ actor: s = null, uuid: o = "", source: l = "actor" } = {}) => {
    const c = s ?? null, u = String(o ?? (c == null ? void 0 : c.uuid) ?? "").trim();
    !c || !u || e.has(u) || c.type !== "battlemech" && c.type !== "vehicle" || (e.add(u), t.push({ actor: c, uuid: u, source: l }));
  };
  for (const s of ((i = game.actors) == null ? void 0 : i.contents) ?? [])
    n({ actor: s, uuid: s == null ? void 0 : s.uuid, source: "actor" });
  for (const s of ((a = game.scenes) == null ? void 0 : a.contents) ?? []) {
    const o = ((r = s == null ? void 0 : s.tokens) == null ? void 0 : r.contents) ?? (s == null ? void 0 : s.tokens) ?? [];
    for (const l of o)
      l != null && l.isLinked || n({ actor: (l == null ? void 0 : l.actor) ?? null, uuid: l == null ? void 0 : l.uuid, source: "token" });
  }
  return t;
}
async function yc(t = null) {
  var i, a;
  const e = String(((i = t == null ? void 0 : t.dataset) == null ? void 0 : i.mechUuid) ?? "").trim();
  if (e && typeof fromUuid == "function")
    try {
      const r = await fromUuid(e);
      return (r == null ? void 0 : r.actor) ?? (r == null ? void 0 : r.baseActor) ?? r ?? null;
    } catch {
    }
  const n = String(((a = t == null ? void 0 : t.dataset) == null ? void 0 : a.mechId) ?? "").trim();
  return n ? game.actors.get(n) ?? null : null;
}
async function NS({ title: t, label: e, options: n = [], confirmLabel: i = "Select" } = {}) {
  const a = Array.isArray(n) ? n.filter((s) => s == null ? void 0 : s.value) : [];
  if (!a.length) return "";
  if (a.length === 1) return String(a[0].value ?? "").trim();
  const r = `<form class="mwd-quick-select"><div class="mwd-field"><label>${kn(e)}</label><select name="selection">${a.map((s) => `<option value="${kn(s.value)}">${kn(s.label ?? s.value)}</option>`).join("")}</select></div></form>`;
  return await foundry.applications.api.DialogV2.prompt({
    window: { title: t },
    content: r,
    ok: {
      label: i,
      callback: (s, o) => {
        var l, c;
        return String(((l = o.form.elements.selection) == null ? void 0 : l.value) ?? ((c = a[0]) == null ? void 0 : c.value) ?? "").trim();
      }
    }
  });
}
var wr, $a, $s, _a, _s, J, pv, hv, bv, yv, Sv, Av, kv, Tv, wv, Mv, vv, Jc, Cv, Iv, Tt, Ev, Yc, Nv, Pv, Rv, cg, gr, Dv, ug, Ov, dg;
const Je = class Je extends Al {
  constructor() {
    super(...arguments);
    Fe(this, J);
    Fe(this, wr, null);
    Fe(this, $a, /* @__PURE__ */ new Set());
    Fe(this, $s, null);
    Fe(this, _a, null);
    Fe(this, _s, !1);
  }
  /** @override */
  async _prepareContext(n) {
    var C, k, w, v, N, P, L, _, O, x, H, G, q, ie, Z, Se, se, te, pe, K, be, Ie, Oe, Be, R;
    const i = await super._prepareContext(n), a = ((C = this.getSheetTokenDocument) == null ? void 0 : C.call(this)) ?? null;
    i._mwdThemeClass = game.system.mwd.styles.selectCssClass(), i.layout = await Hr.get("character");
    const r = this.actor.getEdgeCap(), s = !!this.isEditable, o = { physical: "Physical", mental: "Mental", social: "Social" }, l = {
      grit: "Grit",
      insight: "Insight",
      legend: "Legend",
      chaos: "Chaos",
      rumor: "Rumor",
      credibility: "Credibility"
    }, c = this.actor.getEdgePoolSummary ? this.actor.getEdgePoolSummary({ groups: Hg }) : { groups: [] };
    i.edgeConsole = {
      cap: r,
      editable: s,
      capPips: Array.from({ length: Math.max(0, r) }, (V, B) => B + 1),
      groups: (c.groups ?? []).map((V) => ({
        id: V.id,
        label: o[V.id] ?? V.id,
        pools: (V.pools ?? []).map((B) => {
          const me = Number(B.effectiveValue ?? 0), oe = Number(B.effectiveMax ?? 0), Ee = Array.from({ length: Math.max(0, oe) }, (bt, yt) => {
            const nn = yt + 1;
            return { n: nn, filled: nn <= me };
          }), Ye = String(B.key ?? "").split(".").pop();
          return {
            key: B.key,
            label: l[Ye] ?? Ye ?? B.key,
            value: me,
            max: oe,
            rating: Number(B.rating ?? 0),
            ratingBonus: Number(B.ratingBonus ?? 0),
            effectiveRating: Number(B.effectiveRating ?? B.rating ?? 0),
            isCapped: Number(B.effectiveRating ?? B.rating ?? 0) > Number(B.cap ?? r),
            pips: Ee,
            // Paths for edit-mode inputs
            pathRating: `system.counters.edgePools.${B.key}.rating`,
            pathValue: `system.counters.edgePools.${B.key}.value`,
            // Keep the roll payload if you want pool-name click to route through BaseActorSheetV2 roll handler
            roll: JSON.stringify({ intent: "edge", pool: B.key })
          };
        })
      }))
    };
    const u = ["grit", "insight", "legend", "chaos", "rumor", "credibility"], d = /* @__PURE__ */ new Map();
    for (const V of i.edgeConsole.groups ?? [])
      for (const B of V.pools ?? []) {
        const me = String(B.key ?? "").split(".").pop();
        me && d.set(me, B), B.domain = V.id;
      }
    i.edgeConsole.poolsOrdered = u.map((V) => d.get(V)).filter(Boolean);
    const f = this.actor.system ?? {};
    i.conditionMonitors = mh(this.actor, {
      editable: this.isEditable
    });
    const m = D.getSnapshot(this.actor, { token: a }), g = Number(((w = (k = this.actor.system) == null ? void 0 : k.burn) == null ? void 0 : w.value) ?? 0), p = 10, h = ((v = m.burn) == null ? void 0 : v.threshold) ?? this.actor.overloadThreshold, b = Math.min(g, p);
    i.burnOverflow = Math.max(0, g - p), i.burnPenalty = Math.floor(g / 2), i.burnPips = Array.from({ length: p }, (V, B) => {
      const me = B + 1;
      return {
        pipValue: me,
        filled: me <= b,
        threshold: me === h
      };
    }), i.combat = {
      roll: {
        initiative: JSON.stringify({ intent: "initiative" }),
        overload: JSON.stringify({ intent: "overload" })
      }
    }, i.burn = {
      value: g,
      penalty: Math.floor(g / 2),
      overflow: Math.max(0, g - 10),
      canOverloadCheck: ((N = m.burn) == null ? void 0 : N.canOverloadCheck) ?? !1,
      overloaded: !!((L = (P = this.actor.system) == null ? void 0 : P.burn) != null && L.overloaded)
    }, i.combatDashboard = fh(m, { actor: this.actor }), i.activePersonalCriticals = ph(this.actor), i.personalSpeed = gh(this.actor), i.battleArmor = Fp(this.actor), i.combatAwarenessPreview = Ed(this.actor, {
      sourceToken: a
    }), i.combatActions = this._buildCombatActionsContext(
      D.buildActionModel(this.actor, m)
    ), i.personalInventory = M0(this.actor, {
      items: i.items,
      isEditable: this.isEditable,
      isExpanded: (V) => X(this, $a).has(V),
      inventoryAccordionId: (V, B) => E(this, J, dg).call(this, V, B),
      armorModifierLabels: TK,
      gearCategoryLabels: wK,
      consumableCategoryLabels: MK
    }), i.bio = {
      fields: ((_ = i.bio) == null ? void 0 : _.fields) ?? {},
      faction: ((O = f.biography) == null ? void 0 : O.faction) ?? "",
      age: ((x = f.biography) == null ? void 0 : x.age) ?? "",
      rank: ((H = f.biography) == null ? void 0 : H.rank) ?? "",
      height: ((G = f.biography) == null ? void 0 : G.height) ?? "",
      weight: ((q = f.biography) == null ? void 0 : q.weight) ?? "",
      xpTotal: ((Z = (ie = f.counters) == null ? void 0 : ie.xp) == null ? void 0 : Z.total) ?? 0,
      xpSpent: ((se = (Se = f.counters) == null ? void 0 : Se.xp) == null ? void 0 : se.value) ?? 0,
      xpAvailable: Math.max(0, Number(((pe = (te = f.counters) == null ? void 0 : te.xp) == null ? void 0 : pe.total) ?? 0) - Number(((be = (K = f.counters) == null ? void 0 : K.xp) == null ? void 0 : be.value) ?? 0)),
      experienceLevel: ((Ie = f.biography) == null ? void 0 : Ie.experienceLevel) ?? "green",
      enrichedHistory: await foundry.applications.ux.TextEditor.implementation.enrichHTML(
        ((Oe = f.biography) == null ? void 0 : Oe.history) ?? "",
        { async: !0, secrets: this.actor.isOwner, relativeTo: this.actor }
      )
    };
    const y = qa(this.actor);
    i.skillsDisplay = ck(((Be = this.actor) == null ? void 0 : Be.system) ?? {}, {
      bonusBySkill: y.bonusBySkill
    }), i.lifeModules = y.slotStates.map((V) => {
      const B = V.state;
      return {
        moduleType: V.moduleType,
        label: V.label,
        hasCatalogEntries: V.availableEntries.length > 0,
        emptyState: V.availableEntries.length > 0 ? `Add ${V.label}` : `No ${V.label} catalog entries configured`,
        item: B ? {
          id: B.itemId,
          name: B.label,
          img: B.item.img,
          bonusLabels: [...B.selectedChoiceLabels ?? []],
          warningLabels: [...B.warningLabels ?? []],
          isActive: B.isActive,
          statusLabel: B.isActive ? "Active" : "Inactive",
          statusReason: B.inactiveReason
        } : null
      };
    });
    const S = ["positive", "negative", "narrative"], A = ["major", "minor"], T = [...((R = i.items) == null ? void 0 : R.quality) ?? []].sort((V, B) => {
      const me = Yn(V.system ?? {}), oe = Yn(B.system ?? {}), Ee = S.indexOf(me.category) - S.indexOf(oe.category);
      if (Ee !== 0) return Ee;
      const Ye = A.indexOf(me.tier) - A.indexOf(oe.tier);
      return Ye !== 0 ? Ye : String(V.name ?? "").localeCompare(String(B.name ?? ""));
    });
    return i.qualityGroups = S.map((V) => ({
      id: V,
      label: vc(V),
      records: T.filter((B) => Yn(B.system ?? {}).category === V).map((B) => {
        var Ee, Ye, bt, yt;
        const me = Yn(B.system ?? {}), oe = E(this, J, dg).call(this, "quality", B.id);
        return {
          id: B.id,
          accordionId: oe,
          isExpanded: X(this, $a).has(oe),
          name: B.name,
          img: B.img,
          subtitle: `${Cc(me.tier)} ${vc(me.category)}`,
          summaryStats: IS([
            { label: "Tier", value: Cc(me.tier), emphasis: "strong" },
            { label: "Activation", value: me.activation || "passive" },
            { label: "Effects", value: String(((Ee = me.effects) == null ? void 0 : Ee.length) ?? 0) }
          ]),
          detailTags: AK([
            me.inactive ? "Inactive" : "",
            ...me.tags ?? []
          ]),
          detailRows: kK([
            { label: "Category", value: vc(me.category) },
            { label: "Tier", value: Cc(me.tier) },
            { label: "Activation", value: me.activation || "passive" },
            { label: "Prerequisites", value: String(((Ye = me.prerequisites) == null ? void 0 : Ye.length) ?? 0) },
            { label: "Effects", value: String(((bt = me.effects) == null ? void 0 : bt.length) ?? 0) },
            { label: "Tags", value: sg(me.tags ?? []).join(", ") }
          ]),
          detailText: SK((yt = B.system) == null ? void 0 : yt.description)
        };
      })
    })), i.knowledgeSkills = (Array.isArray(f.knowledgeSkills) ? f.knowledgeSkills : []).map((V) => String(V ?? "").trim()).filter(Boolean).map((V) => ({ label: V })), i.advancement = E(this, J, pv).call(this), i.assignedMech = await this._buildAssignedMech(), i;
  }
  async _buildAssignedMech() {
    var s;
    const n = ((s = this.getPersistentActor) == null ? void 0 : s.call(this)) ?? this.actor, i = z.mwd.weightClass, a = [];
    for (const o of EK())
      await IK(o.actor, n) && a.push(o);
    const r = a.map(({ actor: o, uuid: l }) => {
      var G, q, ie, Z, Se, se, te, pe, K, be, Ie, Oe;
      const c = o.type === "battlemech", u = ((q = (G = o.system) == null ? void 0 : G.monitors) == null ? void 0 : q.structure) ?? {}, d = ((Z = (ie = o.system) == null ? void 0 : ie.monitors) == null ? void 0 : Z.armor) ?? {}, f = ((se = (Se = o.system) == null ? void 0 : Se.mwd) == null ? void 0 : se.heat) ?? {}, m = ((pe = (te = o.system) == null ? void 0 : te.mwd) == null ? void 0 : pe.heatStatus) ?? {}, g = ((be = (K = o.system) == null ? void 0 : K.mwd) == null ? void 0 : be.crits) ?? [], p = (Be, R, V, B) => {
        var Ee;
        const me = Math.max(0, Sn(B.value, 0)), oe = Math.max(0, Sn(B.max, 0));
        return {
          id: Be,
          label: R,
          kind: V,
          value: me,
          max: oe,
          resistance: Sn((Ee = B.resistance) == null ? void 0 : Ee.default, 0),
          segments: Array.from({ length: oe }, (Ye, bt) => {
            const yt = bt + 1;
            return { value: yt, filled: yt <= me };
          })
        };
      }, h = Math.max(0, Sn(f.current, 0)), b = Math.max(0, Sn(f.trackLength ?? f.max, 0)), y = f.thresholds ?? {}, S = Math.max(
        b,
        h,
        Sn(y.shutdown ?? y.danger, 0)
      ), A = c ? {
        current: h,
        trackLength: b,
        displayMax: S,
        status: m.label ?? m.code ?? "safe",
        segments: Array.from({ length: S }, (Be, R) => {
          const V = R + 1;
          return {
            value: V,
            filled: V <= h,
            breakpoint: sg([
              V === Sn(y.runningHot, 0) ? "runningHot" : "",
              V === Sn(y.overheated, 0) ? "overheated" : "",
              V === Sn(y.shutdown, 0) ? "shutdown" : ""
            ]).join(" ")
          };
        })
      } : null, T = DM(g), C = c ? [p("structure", "Structure", "wound", u), p("armor", "Armor", "armor", d)] : [p("structure", "Structure", "wound", u)], k = Nu(o).some((Be) => Be.isAttackLegal && Be.isAvailableThisActivation), w = c && wl(o).length > 0, N = ES(o).filter((Be) => !Be.disabled), P = c ? Gr(o) : [], L = c ? [
        {
          label: "Movement",
          hint: N.length ? "Move action" : "No moves",
          handler: "mechMovement",
          disabled: N.length === 0,
          dataset: { mechUuid: l, mechId: o.id }
        },
        { label: "Ranged", hint: k ? "Ranged attack" : "No weapons", handler: "mechAttack", disabled: !k, dataset: { attackKind: "ranged", mechUuid: l, mechId: o.id } },
        { label: "Melee", hint: w ? "Melee attack" : "No profiles", handler: "mechAttack", disabled: !w, dataset: { attackKind: "melee", mechUuid: l, mechId: o.id } },
        { label: "Maneuvers", hint: "Charge attack", handler: "mechAttack", disabled: !1, dataset: { attackKind: "charge", mechUuid: l, mechId: o.id } },
        { label: "Piloting Check", hint: "Piloting check", handler: "mechRoll", disabled: !1, dataset: { rollKind: "piloting", mechUuid: l, mechId: o.id } },
        { label: "Electronic Warfare", hint: P.length ? "EW action" : "No EW actions", handler: "mechRoll", disabled: P.length === 0, dataset: { rollKind: "sensor", mechUuid: l, mechId: o.id } },
        { label: "Repair", hint: "Repair action", handler: "mechRoll", disabled: !1, dataset: { rollKind: "repair", mechUuid: l, mechId: o.id } }
      ] : [], _ = Math.max(0, Sn(d.max, 0)), O = Math.min(_, Math.max(0, Sn(d.value, 0))), x = Math.max(0, Sn(u.max, 0)), H = Math.min(x, Math.max(0, Sn(u.value, 0)));
      return {
        id: o.id,
        uuid: l,
        name: o.name,
        typeLabel: c ? "BattleMech" : "Vehicle",
        isMech: c,
        weightLabel: i[(Oe = (Ie = o.system) == null ? void 0 : Ie.mwd) == null ? void 0 : Oe.weightClass] ?? "",
        summaryStats: IS([
          ...c ? [{ label: "Armor", value: `${O} / ${_}` }] : [],
          { label: "Structure", value: `${H} / ${x}` },
          { label: "Heat", value: c ? String(h) : null },
          { label: "Status", value: T.count > 0 ? T.value : "OK" }
        ]),
        conditionMonitors: C,
        heat: A,
        critCount: g.length,
        quickActions: L
      };
    });
    return { mechs: r, hasMech: r.length > 0 };
  }
  async _onOpenAssignedMech(n, i) {
    const a = await yc(i);
    a && a.sheet.render(!0, { focus: !0 });
  }
  async _onMechAttack(n, i) {
    var l, c, u, d, f;
    (l = n == null ? void 0 : n.preventDefault) == null || l.call(n), (c = n == null ? void 0 : n.stopPropagation) == null || c.call(n);
    const a = await yc(i);
    if (!a) return;
    const r = String(((u = i == null ? void 0 : i.dataset) == null ? void 0 : u.attackKind) ?? "").trim(), s = String(((d = i == null ? void 0 : i.dataset) == null ? void 0 : d.groupId) ?? "").trim(), o = ((f = this.actor) == null ? void 0 : f.uuid) ?? "";
    try {
      if (r === "melee") {
        const m = await E(this, J, Mv).call(this, a);
        m && await ur(a, {
          kind: "attack",
          attackKind: "melee",
          profile: m,
          operatorActorUuid: o
        });
      } else if (r === "charge")
        await E(this, J, yv).call(this, a, { operatorActorUuid: o });
      else {
        const m = s ? { id: s } : await E(this, J, kv).call(this, a);
        m != null && m.id && await ur(a, {
          kind: "attack",
          attackKind: "ranged",
          groupId: m.id,
          operatorActorUuid: o
        });
      }
    } catch (m) {
      Yt(m, "Unable to launch BattleMech attack.");
    }
  }
  async _onMechMovement(n, i) {
    var s, o, l;
    (s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n);
    const a = await yc(i);
    if (!a) return;
    const r = await E(this, J, bv).call(this, a);
    if (r)
      try {
        await ur(a, {
          kind: "movement",
          movementKind: r.id,
          operatorActorUuid: ((l = this.actor) == null ? void 0 : l.uuid) ?? ""
        });
      } catch (c) {
        Yt(c, "Unable to record BattleMech movement.");
      }
  }
  async _onMechRoll(n, i) {
    var o, l, c, u;
    (o = n == null ? void 0 : n.preventDefault) == null || o.call(n), (l = n == null ? void 0 : n.stopPropagation) == null || l.call(n);
    const a = await yc(i);
    if (!a) return;
    const r = String(((c = i == null ? void 0 : i.dataset) == null ? void 0 : c.rollKind) ?? "").trim(), s = ((u = this.actor) == null ? void 0 : u.uuid) ?? "";
    try {
      if (r === "piloting") {
        const d = await E(this, J, hv).call(this, a);
        d && await ur(a, { kind: "piloting", operatorActorUuid: s, ...d });
      } else if (r === "sensor") {
        const d = await E(this, J, Tv).call(this, a);
        d && await ur(a, {
          kind: "ew",
          action: d,
          operatorActorUuid: s
        });
      } else if (r === "repair") {
        const d = await E(this, J, wv).call(this, a);
        d && await ur(a, {
          kind: "repair",
          issue: d,
          operatorActorUuid: s
        });
      }
    } catch (d) {
      Yt(d, "Unable to launch BattleMech check.");
    }
  }
  _onRender(n, i) {
    super._onRender(n, i), E(this, J, Iv).call(this), E(this, J, Dv).call(this), E(this, J, vv).call(this);
  }
  async close(n = {}) {
    return E(this, J, ug).call(this), X(this, _a) !== null && (Hooks.off("updateActor", X(this, _a)), De(this, _a, null)), super.close(n);
  }
  requestCombatDashboardRefresh() {
    E(this, J, Tt).call(this, { force: !0 });
  }
  async _onEdgeSet(n, i) {
    var c, u, d;
    if (n.preventDefault(), n.stopPropagation(), !this.isEditable) return;
    const a = ((c = i == null ? void 0 : i.closest) == null ? void 0 : c.call(i, "[data-edge-pool][data-edge-value]")) ?? ((d = (u = n == null ? void 0 : n.target) == null ? void 0 : u.closest) == null ? void 0 : d.call(u, "[data-edge-pool][data-edge-value]"));
    if (!a) return;
    const r = String(a.dataset.edgePool ?? "").trim(), s = Number(a.dataset.edgeValue ?? NaN);
    if (!r || !Number.isFinite(s)) return;
    const o = this.actor.getEdgePool(r);
    if (!(o != null && o.hasPools)) return;
    let l = s;
    return s === o.effectiveValue && (l = s - 1), (n.button === 2 || n.type === "contextmenu") && (l = 0), n.altKey && (l = 0), n.shiftKey && (l = o.effectiveMax), this.actor.setEdgePoolValue(r, l);
  }
  async _onToggleStatuses(n, i) {
    var s, o, l, c, u, d, f, m;
    if ((s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n), E(this, J, Ov).call(this, i, n, "Statuses are not available right now.") || !this.isEditable) return;
    const a = this.getPersistentActor() ?? this.actor, r = ((l = this.getSheetTokenDocument) == null ? void 0 : l.call(this)) ?? ((u = D.getSnapshot(a, { token: ((c = this.getSheetTokenDocument) == null ? void 0 : c.call(this)) ?? null })) == null ? void 0 : u.tokenDocument) ?? ((f = D.getSnapshot(this.actor, { token: ((d = this.getSheetTokenDocument) == null ? void 0 : d.call(this)) ?? null })) == null ? void 0 : f.tokenDocument) ?? null;
    if (!r) {
      (m = ui.notifications) == null || m.warn("Statuses require a token for this actor on the current scene.");
      return;
    }
    return Yu({
      actor: a,
      token: r
    });
  }
  async _onPersonalCritRemedy(n, i) {
    var s, o, l, c, u, d, f;
    if ((s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n), !this.isEditable) return;
    const a = String(((l = i == null ? void 0 : i.dataset) == null ? void 0 : l.critId) ?? "").trim(), r = String(((c = i == null ? void 0 : i.dataset) == null ? void 0 : c.remedyKey) ?? "").trim();
    if (a)
      try {
        const m = this.getPersistentActor() ?? this.actor, g = await hM({
          actor: m,
          token: ((u = this.getSheetTokenDocument) == null ? void 0 : u.call(this)) ?? D.getCurrentSceneTokenDocument(m) ?? D.getCurrentSceneTokenDocument(this.actor),
          critId: a,
          remedyKey: r
        });
        if (!(g != null && g.ok)) {
          (d = ui.notifications) == null || d.warn((g == null ? void 0 : g.reason) ?? "Unable to remedy personal critical.");
          return;
        }
        E(this, J, Tt).call(this, { force: !0 });
      } catch (m) {
        console.error("MWD | Failed to remedy personal critical", m), (f = ui.notifications) == null || f.error("Unable to remedy personal critical.");
      }
  }
  _onOpenAdvancementMode(n) {
    var i, a;
    (i = n == null ? void 0 : n.preventDefault) == null || i.call(n), (a = n == null ? void 0 : n.stopPropagation) == null || a.call(n), De(this, _s, !0), E(this, J, Tt).call(this, { force: !0 });
  }
  _onCloseAdvancementMode(n) {
    var i, a;
    (i = n == null ? void 0 : n.preventDefault) == null || i.call(n), (a = n == null ? void 0 : n.stopPropagation) == null || a.call(n), De(this, _s, !1), E(this, J, Tt).call(this, { force: !0 });
  }
  async _onOpenAdvancementDialog(n, i) {
    var o, l, c, u;
    if ((o = n == null ? void 0 : n.preventDefault) == null || o.call(n), (l = n == null ? void 0 : n.stopPropagation) == null || l.call(n), !this.isEditable) return;
    const a = String(((c = i == null ? void 0 : i.dataset) == null ? void 0 : c.dialog) ?? "").trim();
    if (!a) return;
    const r = ((u = this.getPersistentActor) == null ? void 0 : u.call(this)) ?? this.actor, s = wF(r);
    switch (a) {
      case "attribute":
        return E(this, J, Yc).call(this, r, "Raise Attribute", s.filter((d) => d.intent.type === Me.attribute));
      case "skill":
        return E(this, J, Yc).call(this, r, "Raise Skill", s.filter((d) => d.intent.type === Me.skill));
      case "edgePool":
        return E(this, J, Yc).call(this, r, "Raise Edge Pool", s.filter((d) => d.intent.type === Me.edgePool));
      case "specialization":
        return E(this, J, Nv).call(this, r);
      case "traitAdd":
        return E(this, J, Pv).call(this, r);
      case "traitRemove":
        return E(this, J, Rv).call(this, r);
    }
  }
  async _onCommitAdvancementPurchase(n, i) {
    var s, o, l, c, u, d, f;
    if ((s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n), !this.isEditable) return;
    const a = this.getPersistentActor() ?? this.actor;
    let r = E(this, J, Ev).call(this, i);
    if (r.type) {
      if (r.type === Me.traitAdd) {
        const m = ((l = i == null ? void 0 : i.closest) == null ? void 0 : l.call(i, ".mwd-advancement")) ?? ((u = (c = n == null ? void 0 : n.target) == null ? void 0 : c.closest) == null ? void 0 : u.call(c, ".mwd-advancement")), g = (d = m == null ? void 0 : m.querySelector) == null ? void 0 : d.call(m, "input[name='mwdTraitName']"), p = String((g == null ? void 0 : g.value) ?? "").trim();
        p && (r.target = p, r.label = p);
      }
      try {
        r = await E(this, J, cg).call(this, r), await cr(a, r), E(this, J, Tt).call(this, { force: !0 });
      } catch (m) {
        console.error("MWD | Advancement purchase failed", m), (f = ui.notifications) == null || f.warn((m == null ? void 0 : m.message) ?? "Unable to commit advancement purchase.");
      }
    }
  }
  async _onAddKnowledgeSkill(n, i) {
    var o, l, c, u, d, f, m;
    if ((o = n == null ? void 0 : n.preventDefault) == null || o.call(n), (l = n == null ? void 0 : n.stopPropagation) == null || l.call(n), !this.isEditable) return;
    const a = ((c = i == null ? void 0 : i.closest) == null ? void 0 : c.call(i, ".mwd-knowledge-skills")) ?? ((d = (u = n == null ? void 0 : n.target) == null ? void 0 : u.closest) == null ? void 0 : d.call(u, ".mwd-knowledge-skills")), r = (f = a == null ? void 0 : a.querySelector) == null ? void 0 : f.call(a, "input[name='mwdKnowledgeSkill']"), s = String((r == null ? void 0 : r.value) ?? "").trim();
    if (s)
      try {
        const g = this.getPersistentActor() ?? this.actor;
        await cr(g, {
          type: Me.knowledgeSkillAdd,
          target: s,
          label: s
        }), r && (r.value = ""), E(this, J, Tt).call(this, { force: !0 });
      } catch (g) {
        console.error("MWD | Add Knowledge Skill failed", g), (m = ui.notifications) == null || m.warn((g == null ? void 0 : g.message) ?? "Unable to add Knowledge Skill.");
      }
  }
  async _onRemoveKnowledgeSkill(n, i) {
    var r, s, o, l;
    if ((r = n == null ? void 0 : n.preventDefault) == null || r.call(n), (s = n == null ? void 0 : n.stopPropagation) == null || s.call(n), !this.isEditable) return;
    const a = String(((o = i == null ? void 0 : i.dataset) == null ? void 0 : o.knowledgeSkill) ?? "").trim();
    if (a)
      try {
        const c = this.getPersistentActor() ?? this.actor;
        await cr(c, {
          type: Me.knowledgeSkillRemove,
          target: a,
          label: a
        }), E(this, J, Tt).call(this, { force: !0 });
      } catch (c) {
        console.error("MWD | Remove Knowledge Skill failed", c), (l = ui.notifications) == null || l.warn((c == null ? void 0 : c.message) ?? "Unable to remove Knowledge Skill.");
      }
  }
  async _onAddSkillSpecialization(n, i) {
    var d, f, m, g, p, h, b, y, S;
    if ((d = n == null ? void 0 : n.preventDefault) == null || d.call(n), (f = n == null ? void 0 : n.stopPropagation) == null || f.call(n), !this.isEditable || !this.editing) return;
    const a = String(((m = i == null ? void 0 : i.dataset) == null ? void 0 : m.skillKey) ?? "").trim();
    if (!a) return;
    const r = this.getPersistentActor() ?? this.actor, s = lf(r.system ?? {}, a);
    if (Ts(r.system ?? {}, a).length > 0) {
      (g = ui.notifications) == null || g.warn("A skill can only have one specialization.");
      return;
    }
    if (Number(((b = (h = (p = r.system) == null ? void 0 : p.skills) == null ? void 0 : h[a]) == null ? void 0 : b.rating) ?? 0) < 2) {
      (y = ui.notifications) == null || y.warn("Specializations require skill rating 2+.");
      return;
    }
    const l = Hi(a);
    if (l.length === 0) return;
    let c = ((S = l[0]) == null ? void 0 : S.key) ?? "";
    if (l.length > 1) {
      const A = `<form class="mwd-quick-select"><div class="mwd-field"><label>Specialization</label><select name="specialization">${l.map((T) => `<option value="${T.key}">${T.label}</option>`).join("")}</select></div></form>`;
      c = await foundry.applications.api.DialogV2.prompt({
        window: { title: "Add Skill Specialization" },
        content: A,
        ok: {
          label: "Add",
          callback: (T, C) => {
            var k, w;
            return ((k = C.form.elements.specialization) == null ? void 0 : k.value) ?? ((w = l[0]) == null ? void 0 : w.key) ?? "";
          }
        }
      });
    }
    const u = Xo(
      s.concat([c])
    );
    await r.update({
      [`system.skills.${a}.specializations`]: u
    }), E(this, J, Tt).call(this, { force: !0 });
  }
  async _onRemoveSkillSpecialization(n, i) {
    var l, c, u, d;
    if ((l = n == null ? void 0 : n.preventDefault) == null || l.call(n), (c = n == null ? void 0 : n.stopPropagation) == null || c.call(n), !this.isEditable || !this.editing) return;
    const a = String(((u = i == null ? void 0 : i.dataset) == null ? void 0 : u.skillKey) ?? "").trim(), r = String(((d = i == null ? void 0 : i.dataset) == null ? void 0 : d.specializationKey) ?? "").trim();
    if (!a || !r) return;
    const s = this.getPersistentActor() ?? this.actor, o = Xo(
      lf(s.system ?? {}, a).filter((f) => f !== r)
    );
    await s.update({
      [`system.skills.${a}.specializations`]: o
    }), E(this, J, Tt).call(this, { force: !0 });
  }
  async _onCreateLifeModuleItem(n, i) {
    var d, f, m, g, p;
    if ((d = n == null ? void 0 : n.preventDefault) == null || d.call(n), (f = n == null ? void 0 : n.stopPropagation) == null || f.call(n), !this.isEditable) return;
    const a = String(((m = i == null ? void 0 : i.dataset) == null ? void 0 : m.moduleType) ?? "").trim();
    if (!a) return;
    const r = this.getPersistentActor() ?? this.actor, s = rh(a);
    if (!s.length) {
      (g = ui.notifications) == null || g.warn(`No ${qs(a)} life modules are configured in game settings.`);
      return;
    }
    const o = await NS({
      title: `Choose ${qs(a)} Life Module`,
      label: "Life Module",
      confirmLabel: "Create",
      options: s.map((h) => ({
        value: h.id,
        label: h.label
      }))
    });
    if (!o) return;
    const l = Fa(o);
    if (!l) {
      (p = ui.notifications) == null || p.warn("That life module catalog entry no longer exists.");
      return;
    }
    const c = n0(l, {}), u = {};
    for (const h of c.filter((b) => b.hasMultipleChoices)) {
      const b = await NS({
        title: `Choose Bonus for ${l.label}`,
        label: h.label,
        confirmLabel: "Apply",
        options: h.options.map((y) => ({
          value: y.value,
          label: y.label
        }))
      });
      if (!b) return;
      u[h.id] = b;
    }
    await r.createEmbeddedDocuments("Item", [{
      name: l.label,
      type: "lifeModule",
      system: hl({
        moduleType: a,
        catalogId: l.id,
        selectedGrants: u
      })
    }]), E(this, J, Tt).call(this, { force: !0 });
  }
  async _onCreateOwnedItem(n, i) {
    var l, c, u;
    if ((l = n == null ? void 0 : n.preventDefault) == null || l.call(n), (c = n == null ? void 0 : n.stopPropagation) == null || c.call(n), !this.isEditable) return;
    const a = String(((u = i == null ? void 0 : i.dataset) == null ? void 0 : u.itemType) ?? "").trim();
    if (!a) return;
    const r = this.getPersistentActor() ?? this.actor, s = r.items.filter((d) => d.type === a).length, o = a === "personalWeapon" ? "Personal Weapon" : a === "armor" ? "Armor" : a === "consumable" ? "Consumable" : a.charAt(0).toUpperCase() + a.slice(1);
    await r.createEmbeddedDocuments("Item", [{
      name: `${o} ${s + 1}`,
      type: a
    }]), E(this, J, Tt).call(this, { force: !0 });
  }
  async _onEditOwnedItem(n, i) {
    var r, s, o;
    (r = n == null ? void 0 : n.preventDefault) == null || r.call(n), (s = n == null ? void 0 : n.stopPropagation) == null || s.call(n);
    const a = E(this, J, gr).call(this, i, n);
    (o = a == null ? void 0 : a.sheet) == null || o.render(!0);
  }
  async _onDeleteOwnedItem(n, i) {
    var s, o;
    if ((s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n), !this.isEditable) return;
    const a = E(this, J, gr).call(this, i, n);
    if (!a) return;
    await (this.getPersistentActor() ?? this.actor).deleteEmbeddedDocuments("Item", [a.id]), E(this, J, Tt).call(this, { force: !0 });
  }
  async _onToggleInventoryAccordion(n, i) {
    var r, s, o, l, c, u, d, f, m, g;
    (r = n == null ? void 0 : n.preventDefault) == null || r.call(n), (s = n == null ? void 0 : n.stopPropagation) == null || s.call(n);
    const a = String(
      ((o = i == null ? void 0 : i.dataset) == null ? void 0 : o.accordionId) ?? ((u = (c = (l = i == null ? void 0 : i.closest) == null ? void 0 : l.call(i, "[data-accordion-id]")) == null ? void 0 : c.dataset) == null ? void 0 : u.accordionId) ?? ((g = (m = (f = (d = n == null ? void 0 : n.target) == null ? void 0 : d.closest) == null ? void 0 : f.call(d, "[data-accordion-id]")) == null ? void 0 : m.dataset) == null ? void 0 : g.accordionId) ?? ""
    ).trim();
    a && (X(this, $a).has(a) ? X(this, $a).delete(a) : X(this, $a).add(a), E(this, J, Tt).call(this, !1));
  }
  async _onToggleOwnedItemEquipped(n, i) {
    var s, o, l, c;
    if ((s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n), !this.isEditable) return;
    const a = E(this, J, gr).call(this, i, n);
    if (!a) return;
    const r = this.getPersistentActor() ?? this.actor;
    await ((c = r.setOwnedItemEquipped) == null ? void 0 : c.call(r, a.id, !((l = a.system) != null && l.equipped))), E(this, J, Tt).call(this, { force: !0 });
  }
  async _onSetOwnedItemPrimary(n, i) {
    var s, o, l, c;
    if ((s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n), !this.isEditable) return;
    const a = E(this, J, gr).call(this, i, n);
    if (!a) return;
    const r = this.getPersistentActor() ?? this.actor;
    await ((c = r.setOwnedItemPrimary) == null ? void 0 : c.call(r, a.id, !((l = a.system) != null && l.isPrimary))), E(this, J, Tt).call(this, { force: !0 });
  }
  async _onAdjustGearQuantity(n, i) {
    var c, u, d, f, m, g, p, h, b, y, S;
    if ((c = n == null ? void 0 : n.preventDefault) == null || c.call(n), (u = n == null ? void 0 : n.stopPropagation) == null || u.call(n), !this.isEditable) return;
    const a = E(this, J, gr).call(this, i, n);
    if (!a || !["gear", "consumable"].includes(String(a.canonicalType ?? a.type ?? "").trim())) return;
    const r = Math.trunc(Number(
      ((d = i == null ? void 0 : i.dataset) == null ? void 0 : d.delta) ?? ((g = (m = (f = i == null ? void 0 : i.closest) == null ? void 0 : f.call(i, "[data-delta]")) == null ? void 0 : m.dataset) == null ? void 0 : g.delta) ?? ((y = (b = (h = (p = n == null ? void 0 : n.target) == null ? void 0 : p.closest) == null ? void 0 : h.call(p, "[data-delta]")) == null ? void 0 : b.dataset) == null ? void 0 : y.delta) ?? 0
    ) || 0);
    if (!r) return;
    const o = (this.getPersistentActor() ?? this.actor).items.get(a.id) ?? a, l = Math.max(0, Math.trunc(Number(((S = o.system) == null ? void 0 : S.quantity) ?? 1) || 0) + r);
    await o.update({ "system.quantity": l }), E(this, J, Tt).call(this, { force: !0 });
  }
};
wr = new WeakMap(), $a = new WeakMap(), $s = new WeakMap(), _a = new WeakMap(), _s = new WeakMap(), J = new WeakSet(), pv = function() {
  var o, l;
  const n = ((o = this.getPersistentActor) == null ? void 0 : o.call(this)) ?? this.actor, i = (n == null ? void 0 : n.system) ?? {}, a = bc(n), r = TF(n, {
    tier: ((l = i == null ? void 0 : i.biography) == null ? void 0 : l.experienceLevel) ?? "green"
  }), s = CS(n).filter((c) => ((c == null ? void 0 : c.canonicalType) ?? (c == null ? void 0 : c.type)) === "quality").filter((c) => Yn(c.system ?? {}).category === "negative");
  return {
    open: X(this, _s),
    xp: a,
    build: r,
    negativeTraits: s
  };
}, hv = async function(n) {
  var d, f;
  const i = [
    { key: "handling", label: "Handling" },
    { key: "system", label: "System" },
    { key: "chassis", label: "Chassis" },
    { key: "reliability", label: "Reliability" }
  ], a = bK(this.actor), r = ((d = a.flatMap((m) => m.skills).find((m) => m.code === "piloting")) == null ? void 0 : d.code) ?? "", s = i.map((m) => {
    var p, h, b;
    const g = Sn((b = (h = (p = n == null ? void 0 : n.system) == null ? void 0 : p.attributes) == null ? void 0 : h[m.key]) == null ? void 0 : b.value, 0);
    return `<label class="mwd-pilot-check__attr">
        <input type="radio" name="machineAttr" value="${foundry.utils.escapeHTML(m.key)}" ${m.key === "handling" ? "checked" : ""}>
        <span class="mwd-pilot-check__attr-card">
          <span class="mwd-pilot-check__attr-name">${foundry.utils.escapeHTML(m.label)}</span>
          <span class="mwd-pilot-check__attr-value">${g}</span>
        </span>
      </label>`;
  }).join(""), o = a.map((m) => {
    const g = m.skills.map((p) => `<option value="${foundry.utils.escapeHTML(p.code)}" ${p.code === r ? "selected" : ""}>${foundry.utils.escapeHTML(p.label)} (${p.rating})</option>`).join("");
    return `<optgroup label="${foundry.utils.escapeHTML(m.label)}">${g}</optgroup>`;
  }).join(""), l = `<option value="" ${r ? "" : "selected"}>No Skill (0)</option>`, c = `<form class="mwd-quick-select mwd-skill-check-form mwd-pilot-check">
      <section class="mwd-pilot-check__section">
        <div class="mwd-pilot-check__heading">
          <span>Machine Attribute</span>
          <small>${foundry.utils.escapeHTML((n == null ? void 0 : n.name) ?? "Machine")}</small>
        </div>
        <div class="mwd-pilot-check__attr-grid">${s}</div>
      </section>
      <section class="mwd-pilot-check__section">
        <div class="mwd-pilot-check__heading">
          <span>Skill</span>
          <small>${foundry.utils.escapeHTML(((f = this.actor) == null ? void 0 : f.name) ?? "Pilot")}</small>
        </div>
        <label class="mwd-pilot-check__select">
          <select name="skillKey">${l}${o}</select>
        </label>
      </section>
    </form>`, u = await foundry.applications.api.DialogV2.wait({
    window: { title: z.actor.vehicle.quickActions.pilotingCheck },
    content: c,
    rejectClose: !1,
    buttons: [
      {
        action: "roll",
        label: z.common.roll.button,
        icon: "fa-solid fa-dice",
        default: !0,
        callback: (m, g) => {
          var p, h, b, y;
          return {
            machineAttributeKey: ((h = (p = g.form) == null ? void 0 : p.elements.machineAttr) == null ? void 0 : h.value) ?? "handling",
            skillKey: ((y = (b = g.form) == null ? void 0 : b.elements.skillKey) == null ? void 0 : y.value) ?? r
          };
        }
      }
    ]
  });
  return u ? {
    ...u,
    skillKey: String(u.skillKey ?? "").trim() || "none"
  } : null;
}, bv = async function(n) {
  var l;
  const i = ES(n), a = i.filter((c) => !c.disabled);
  if (!a.length)
    return (l = ui.notifications) == null || l.warn("No movement actions are currently available."), null;
  const r = a[0], s = `<form class="mwd-quick-select">${i.map((c) => `
      <label class="quick-select-option${c.disabled ? " is-disabled" : ""}" title="${foundry.utils.escapeHTML(c.reason || c.hint || "")}">
        <input type="radio" name="movement-action" value="${c.id}" ${c.id === r.id ? "checked" : ""} ${c.disabled ? "disabled" : ""}>
        <span>${foundry.utils.escapeHTML(c.label)}</span>
        <small>${foundry.utils.escapeHTML(`${c.cost} SA${c.heat > 0 ? ` | +${c.heat} Heat` : ""}${c.hint ? ` | ${c.hint}` : ""}`)}</small>
      </label>`).join("")}</form>`, o = await foundry.applications.api.DialogV2.wait({
    window: { title: "Movement" },
    content: s,
    rejectClose: !1,
    buttons: [
      {
        action: "select",
        label: "Move",
        icon: "fa-solid fa-person-running",
        default: !0,
        callback: (c, u) => {
          var d, f;
          return ((f = (d = u.form) == null ? void 0 : d.elements["movement-action"]) == null ? void 0 : f.value) ?? r.id;
        }
      }
    ]
  });
  return a.find((c) => c.id === o) ?? r;
}, yv = async function(n, { operatorActorUuid: i = "" } = {}) {
  var l;
  const a = Fh(n);
  if (!a.filter((c) => !c.disabled).length) {
    (l = ui.notifications) == null || l.warn("No charge modes available. Move with Run, Sprint, or Jump before charging.");
    return;
  }
  const s = await E(this, J, Sv).call(this, a);
  if (!s) return;
  let o = "prone";
  s === "control" && (o = await E(this, J, Av).call(this), !o) || await ur(n, {
    kind: "action",
    actionId: "chargeAttack",
    mode: s,
    controlIntent: o,
    operatorActorUuid: i
  });
}, Sv = async function(n = []) {
  var o;
  const i = n.filter((l) => !l.disabled);
  if (!i.length) return null;
  if (i.length === 1) return i[0].id;
  const a = i[0], r = `<form class="mwd-quick-select">${n.map((l) => `
      <label class="quick-select-option${l.disabled ? " is-disabled" : ""}" title="${foundry.utils.escapeHTML(String(l.reason ?? ""))}">
        <input type="radio" name="charge-mode" value="${foundry.utils.escapeHTML(String(l.id ?? ""))}" ${l.id === a.id ? "checked" : ""} ${l.disabled ? "disabled" : ""}>
        <span>${foundry.utils.escapeHTML(String(l.label ?? ""))}</span>
        ${l.disabled ? `<small>${foundry.utils.escapeHTML(String(l.reason ?? ""))}</small>` : ""}
      </label>`).join("")}</form>`, s = await foundry.applications.api.DialogV2.wait({
    window: { title: "Choose Charge Mode" },
    content: r,
    rejectClose: !1,
    buttons: [
      {
        action: "select",
        label: "Confirm",
        icon: "fa-solid fa-bolt",
        default: !0,
        callback: (l, c) => {
          var u, d;
          return ((d = (u = c.form) == null ? void 0 : u.elements["charge-mode"]) == null ? void 0 : d.value) ?? a.id;
        }
      }
    ]
  });
  return ((o = i.find((l) => l.id === s)) == null ? void 0 : o.id) ?? a.id;
}, Av = async function() {
  var s;
  const n = Uh(), i = n[0] ?? null;
  if (!i) return null;
  const a = `<form class="mwd-quick-select">${n.map((o) => `
      <label class="quick-select-option">
        <input type="radio" name="control-intent" value="${foundry.utils.escapeHTML(String(o.id ?? ""))}" ${o.id === i.id ? "checked" : ""}>
        <span>${foundry.utils.escapeHTML(String(o.label ?? ""))}</span>
      </label>`).join("")}</form>`, r = await foundry.applications.api.DialogV2.wait({
    window: { title: "Control Charge - Intended Outcome" },
    content: a,
    rejectClose: !1,
    buttons: [
      {
        action: "select",
        label: "Confirm",
        icon: "fa-solid fa-bullseye",
        default: !0,
        callback: (o, l) => {
          var c, u;
          return ((u = (c = l.form) == null ? void 0 : c.elements["control-intent"]) == null ? void 0 : u.value) ?? i.id;
        }
      }
    ]
  });
  return ((s = n.find((o) => o.id === r)) == null ? void 0 : s.id) ?? i.id;
}, kv = async function(n) {
  var l;
  const a = Nu(n).filter(
    (c) => Array.isArray(c == null ? void 0 : c.weaponIds) && c.weaponIds.length > 0 && c.isAttackLegal && c.isAvailableThisActivation
  );
  if (!a.length)
    return (l = ui.notifications) == null || l.warn(z.actor.vehicle.quickActions.errors.noRanged), null;
  if (a.length === 1) return a[0];
  const r = a[0], s = `<form class="mwd-quick-select">${a.map((c) => `
      <label class="quick-select-option">
        <input type="radio" name="weapon-group" value="${foundry.utils.escapeHTML(String(c.id ?? ""))}" ${c.id === r.id ? "checked" : ""}>
        <span>${foundry.utils.escapeHTML(String(c.name ?? ""))}</span>
      </label>`).join("")}</form>`, o = await foundry.applications.api.DialogV2.wait({
    window: { title: z.actor.vehicle.quickActions.selectWeaponGroup },
    content: s,
    rejectClose: !1,
    buttons: [
      {
        action: "select",
        label: z.common.roll.button,
        icon: "fa-solid fa-dice",
        default: !0,
        callback: (c, u) => {
          var d, f;
          return ((f = (d = u.form) == null ? void 0 : d.elements["weapon-group"]) == null ? void 0 : f.value) ?? r.id;
        }
      }
    ]
  });
  return a.find((c) => c.id === o) ?? r;
}, Tv = async function(n) {
  var l;
  const i = Gr(n, { includeDisabled: !0 }), a = i.filter((c) => !c.disabled);
  if (!a.length)
    return (l = ui.notifications) == null || l.warn(z.actor.vehicle.quickActions.errors.noSensorSweep), null;
  const r = a[0], s = `<form class="mwd-quick-select">${i.map((c) => `
      <label class="quick-select-option${c.disabled ? " is-disabled" : ""}" title="${foundry.utils.escapeHTML(String(c.reason ?? ""))}">
        <input type="radio" name="ew-action" value="${foundry.utils.escapeHTML(String(c.id ?? ""))}" ${c.id === r.id ? "checked" : ""} ${c.disabled ? "disabled" : ""}>
        <span>${foundry.utils.escapeHTML(String(c.label ?? ""))}</span>
        <small>${foundry.utils.escapeHTML(String(c.disabled ? c.reason : c.hint ?? ""))}</small>
      </label>`).join("")}</form>`, o = await foundry.applications.api.DialogV2.wait({
    window: { title: "Electronic Warfare" },
    content: s,
    rejectClose: !1,
    buttons: [
      {
        action: "select",
        label: z.common.roll.button,
        icon: "fa-solid fa-dice",
        default: !0,
        callback: (c, u) => {
          var d, f;
          return ((f = (d = u.form) == null ? void 0 : d.elements["ew-action"]) == null ? void 0 : f.value) ?? r.id;
        }
      }
    ]
  });
  return a.find((c) => c.id === o) ?? r;
}, wv = async function(n) {
  var o;
  const i = Dd(n);
  if (!i.length)
    return (o = ui.notifications) == null || o.warn("No active criticals or repairable statuses are available."), null;
  if (i.length === 1) return i[0];
  const a = i[0], r = `<form class="mwd-quick-select">${i.map((l) => `
      <label class="quick-select-option">
        <input type="radio" name="repair-issue" value="${foundry.utils.escapeHTML(`${l.issueKind}:${l.issueId}`)}" ${l.issueKind === a.issueKind && l.issueId === a.issueId ? "checked" : ""}>
        <span>${foundry.utils.escapeHTML(String(l.label ?? ""))}</span>
        <small>${foundry.utils.escapeHTML(`${l.remedyLabel ?? ""} | ${l.remedySummary || `DN ${l.totalDn}`}`)}</small>
      </label>`).join("")}</form>`, s = await foundry.applications.api.DialogV2.wait({
    window: { title: z.actor.vehicle.quickActions.emergencyRepair },
    content: r,
    rejectClose: !1,
    buttons: [
      {
        action: "select",
        label: z.common.roll.button,
        icon: "fa-solid fa-dice",
        default: !0,
        callback: (l, c) => {
          var u, d;
          return ((d = (u = c.form) == null ? void 0 : u.elements["repair-issue"]) == null ? void 0 : d.value) ?? `${a.issueKind}:${a.issueId}`;
        }
      }
    ]
  });
  return i.find((l) => `${l.issueKind}:${l.issueId}` === s) ?? a;
}, Mv = async function(n) {
  var o;
  const i = wl(n);
  if (!i.length)
    return (o = ui.notifications) == null || o.warn(z.actor.vehicle.quickActions.errors.noMelee), null;
  if (i.length === 1) return i[0];
  const a = i[0], r = `<form class="mwd-quick-select">${i.map((l) => `
      <label class="quick-select-option">
        <input type="radio" name="melee-profile" value="${foundry.utils.escapeHTML(String(l.id ?? ""))}" ${l.id === a.id ? "checked" : ""}>
        <span>${foundry.utils.escapeHTML(String(l.name ?? ""))}</span>
      </label>`).join("")}</form>`, s = await foundry.applications.api.DialogV2.wait({
    window: { title: z.actor.vehicle.quickActions.selectMeleeProfile },
    content: r,
    rejectClose: !1,
    buttons: [
      {
        action: "select",
        label: z.common.roll.button,
        icon: "fa-solid fa-dice",
        default: !0,
        callback: (l, c) => {
          var u, d;
          return ((d = (u = c.form) == null ? void 0 : u.elements["melee-profile"]) == null ? void 0 : d.value) ?? a.id;
        }
      }
    ]
  });
  return i.find((l) => l.id === s) ?? a;
}, vv = function() {
  var a;
  if (X(this, _a) !== null) return;
  const n = ((a = this.getPersistentActor) == null ? void 0 : a.call(this)) ?? this.actor, i = og(n);
  De(this, _a, Hooks.on("updateActor", (r, s) => {
    var f, m, g, p, h, b, y, S, A, T;
    if (r.type !== "battlemech" && r.type !== "vehicle") return;
    const o = String(((m = (f = s == null ? void 0 : s.system) == null ? void 0 : f.pilot) == null ? void 0 : m.uuid) ?? ((h = (p = (g = s == null ? void 0 : s.system) == null ? void 0 : g.mwd) == null ? void 0 : p.pilot) == null ? void 0 : h.uuid) ?? "").trim(), l = String(((y = (b = r.system) == null ? void 0 : b.pilot) == null ? void 0 : y.uuid) ?? ((T = (A = (S = r.system) == null ? void 0 : S.mwd) == null ? void 0 : A.pilot) == null ? void 0 : T.uuid) ?? "").trim(), c = o !== "", u = i.has(l), d = i.has(o);
    !c && !u || (c || u || d) && this.render();
  }));
}, Jc = function() {
  const n = this._getRootElement();
  return n ? n.querySelector(".mwd-scroll-area") ?? n.querySelector(".csb-tab-panels") : null;
}, Cv = function() {
  const n = E(this, J, Jc).call(this);
  if (!(n instanceof HTMLElement)) {
    De(this, wr, null);
    return;
  }
  De(this, wr, {
    top: n.scrollTop,
    left: n.scrollLeft
  });
}, Iv = function() {
  const n = X(this, wr);
  if (!n) return;
  const i = E(this, J, Jc).call(this);
  i instanceof HTMLElement && (i.scrollTop = n.top, i.scrollLeft = n.left, requestAnimationFrame(() => {
    const a = E(this, J, Jc).call(this);
    a instanceof HTMLElement && (a.scrollTop = n.top, a.scrollLeft = n.left);
  }), De(this, wr, null));
}, Tt = function(n = !1) {
  E(this, J, Cv).call(this), this.render(n);
}, Ev = function(n = null) {
  var i, a, r, s, o, l;
  return {
    type: String(((i = n == null ? void 0 : n.dataset) == null ? void 0 : i.purchaseType) ?? "").trim(),
    target: String(((a = n == null ? void 0 : n.dataset) == null ? void 0 : a.purchaseTarget) ?? "").trim(),
    to: (r = n == null ? void 0 : n.dataset) == null ? void 0 : r.purchaseTo,
    label: String(((s = n == null ? void 0 : n.dataset) == null ? void 0 : s.purchaseLabel) ?? "").trim(),
    specializationKey: String(((o = n == null ? void 0 : n.dataset) == null ? void 0 : o.specializationKey) ?? "").trim(),
    specializationLabel: String(((l = n == null ? void 0 : n.dataset) == null ? void 0 : l.specializationLabel) ?? "").trim()
  };
}, Yc = async function(n, i, a) {
  var c, u;
  if (!a.length) {
    (c = ui.notifications) == null || c.info(`No available purchases for ${i}.`);
    return;
  }
  const r = bc(n), s = a.map((d) => {
    const f = d.legal ? "" : " mwd-adv-dialog__row--disabled", m = d.legal ? "" : " data-initially-disabled disabled";
    return `<label class="mwd-adv-dialog__row${f}" title="${kn(d.errors.join(" "))}">
    <input type="checkbox" name="sel" value="${kn(d.intent.target)}" data-cost="${d.cost}" data-to="${d.intent.to}" data-type="${kn(d.intent.type)}"${m}>
    <span class="mwd-adv-dialog__name">${kn(d.label)}</span>
    <span class="mwd-adv-dialog__cost">${d.cost} XP</span>
   </label>`;
  }).join(""), o = `<div class="mwd-adv-dialog">
   <div class="mwd-adv-dialog__xp-bar">
    <span><strong>${r.available}</strong> XP available</span>
   </div>
   <div class="mwd-adv-dialog__rows">${s}</div>
  </div>`;
  let l;
  try {
    l = await foundry.applications.api.DialogV2.prompt({
      window: { title: i },
      position: { width: 480 },
      content: o,
      ok: {
        label: "Purchase",
        callback: (d, f) => Array.from(f.form.querySelectorAll("input[type=checkbox]:checked")).map((g) => ({ type: g.dataset.type, target: g.value, to: Number(g.dataset.to) }))
      }
    });
  } catch {
    return;
  }
  if (!(!Array.isArray(l) || !l.length)) {
    for (const d of l)
      try {
        await cr(n, d);
      } catch (f) {
        console.error("MWD | Advancement purchase failed", f), (u = ui.notifications) == null || u.warn((f == null ? void 0 : f.message) ?? "Unable to commit advancement purchase.");
        break;
      }
    E(this, J, Tt).call(this, { force: !0 });
  }
}, Nv = async function(n) {
  var c, u, d;
  const i = (n == null ? void 0 : n.system) ?? {}, a = tn.flatMap((f) => {
    var S, A;
    if (Math.max(0, Number(((A = (S = i == null ? void 0 : i.skills) == null ? void 0 : S[f.code]) == null ? void 0 : A.rating) ?? 0) || 0) < 2) return [];
    const g = Ts(i, f.code);
    if (!Hi(f.code).filter((T) => !g.includes(T.key)).length) return [];
    const h = g.length > 0, b = h ? Me.specializationChange : Me.specializationAdd, y = h ? 2 : 4;
    return [{ code: f.code, label: f.label, type: b, cost: y, action: h ? "Change" : "Add" }];
  });
  if (!a.length) {
    (c = ui.notifications) == null || c.info("No skills eligible for specialization. Skills need rating 2+ and available specialization choices.");
    return;
  }
  const s = `<div class="mwd-adv-dialog"><div class="mwd-field"><label>Skill</label><select name="skillCode">${a.map(
    (f) => `<option value="${kn(f.code)}" data-type="${kn(f.type)}">${kn(f.label)} — ${f.action} (${f.cost} XP)</option>`
  ).join("")}</select></div></div>`;
  let o;
  try {
    o = await foundry.applications.api.DialogV2.prompt({
      window: { title: "Manage Specialization" },
      position: { width: 360 },
      content: s,
      ok: {
        label: "Continue",
        callback: (f, m) => {
          var h, b;
          const g = m.form.elements.skillCode, p = (h = g == null ? void 0 : g.options) == null ? void 0 : h[g.selectedIndex];
          return { code: String((g == null ? void 0 : g.value) ?? "").trim(), type: String(((b = p == null ? void 0 : p.dataset) == null ? void 0 : b.type) ?? Me.specializationAdd).trim() };
        }
      }
    });
  } catch {
    return;
  }
  if (!(o != null && o.code)) return;
  let l = { type: o.type, target: o.code };
  try {
    if (l = await E(this, J, cg).call(this, l), !(l != null && l.specializationKey)) return;
    const f = ((u = this.getPersistentActor) == null ? void 0 : u.call(this)) ?? this.actor;
    await cr(f, l), E(this, J, Tt).call(this, { force: !0 });
  } catch (f) {
    console.error("MWD | Specialization purchase failed", f), (d = ui.notifications) == null || d.warn((f == null ? void 0 : f.message) ?? "Unable to commit specialization.");
  }
}, Pv = async function(n) {
  var o;
  const i = bc(n), r = `<div class="mwd-adv-dialog">
   <div class="mwd-adv-dialog__xp-bar">
    <span>Cost: <strong>${Os(n, { type: Me.traitAdd, target: "Positive Trait" }).cost} XP</strong></span>
    <span><strong>${i.available}</strong> XP available</span>
   </div>
   <div class="mwd-field"><label>Trait Name</label><input type="text" name="traitName" placeholder="e.g., Quick Reflexes" autofocus></div>
  </div>`;
  let s;
  try {
    s = await foundry.applications.api.DialogV2.prompt({
      window: { title: "Buy Positive Trait" },
      position: { width: 360 },
      content: r,
      ok: {
        label: "Purchase",
        callback: (l, c) => {
          var u;
          return String(((u = c.form.elements.traitName) == null ? void 0 : u.value) ?? "").trim();
        }
      }
    });
  } catch {
    return;
  }
  if (s)
    try {
      await cr(n, { type: Me.traitAdd, target: s, label: s }), E(this, J, Tt).call(this, { force: !0 });
    } catch (l) {
      console.error("MWD | Trait add failed", l), (o = ui.notifications) == null || o.warn((l == null ? void 0 : l.message) ?? "Unable to buy positive trait.");
    }
}, Rv = async function(n) {
  var l, c;
  const i = CS(n).filter((u) => ((u == null ? void 0 : u.canonicalType) ?? (u == null ? void 0 : u.type)) === "quality").filter((u) => Yn(u.system ?? {}).category === "negative");
  if (!i.length) {
    (l = ui.notifications) == null || l.info("No negative traits to remove.");
    return;
  }
  const a = bc(n), r = i.map((u) => {
    const d = Os(n, { type: Me.traitRemove, target: u.uuid ?? u.id }), f = d.legal ? "" : " mwd-adv-dialog__row--disabled", m = d.legal ? "" : " data-initially-disabled disabled";
    return `<label class="mwd-adv-dialog__row${f}" title="${kn(d.errors.join(" "))}">
    <input type="checkbox" name="sel" value="${kn(u.uuid ?? u.id)}" data-cost="${d.cost}"${m}>
    <span class="mwd-adv-dialog__name">${kn(u.name ?? "Trait")}</span>
    <span class="mwd-adv-dialog__cost">${d.cost} XP</span>
   </label>`;
  }).join(""), s = `<div class="mwd-adv-dialog">
   <div class="mwd-adv-dialog__xp-bar">
    <span><strong>${a.available}</strong> XP available</span>
   </div>
   <div class="mwd-adv-dialog__rows">${r}</div>
  </div>`;
  let o;
  try {
    o = await foundry.applications.api.DialogV2.prompt({
      window: { title: "Remove Negative Trait" },
      position: { width: 420 },
      content: s,
      ok: {
        label: "Remove Selected",
        callback: (u, d) => Array.from(d.form.querySelectorAll("input[type=checkbox]:checked")).map((f) => f.value)
      }
    });
  } catch {
    return;
  }
  if (!(!Array.isArray(o) || !o.length)) {
    for (const u of o)
      try {
        await cr(n, { type: Me.traitRemove, target: u });
      } catch (d) {
        console.error("MWD | Trait remove failed", d), (c = ui.notifications) == null || c.warn((d == null ? void 0 : d.message) ?? "Unable to remove negative trait.");
        break;
      }
    E(this, J, Tt).call(this, { force: !0 });
  }
}, cg = async function(n) {
  if (n.type !== Me.specializationAdd && n.type !== Me.specializationChange) return n;
  const i = this.getPersistentActor() ?? this.actor, a = Ts(i.system ?? {}, n.target), r = Hi(n.target).filter((c) => !a.includes(c.key));
  if (!r.length) return n;
  const s = `<form class="mwd-quick-select"><div class="mwd-field"><label>Specialization</label><select name="specialization">${r.map((c) => `<option value="${kn(c.key)}">${kn(c.label)}</option>`).join("")}</select></div></form>`, o = await foundry.applications.api.DialogV2.prompt({
    window: { title: n.type === Me.specializationChange ? "Change Specialization" : "Add Specialization" },
    content: s,
    ok: {
      label: "Select",
      callback: (c, u) => {
        var d, f;
        return String(((d = u.form.elements.specialization) == null ? void 0 : d.value) ?? ((f = r[0]) == null ? void 0 : f.key) ?? "").trim();
      }
    }
  }), l = r.find((c) => c.key === o) ?? r[0];
  return {
    ...n,
    specializationKey: (l == null ? void 0 : l.key) ?? "",
    specializationLabel: (l == null ? void 0 : l.label) ?? ""
  };
}, gr = function(n, i) {
  var r, s, o, l, c, u, d, f;
  const a = String(
    ((r = n == null ? void 0 : n.dataset) == null ? void 0 : r.itemId) ?? ((l = (o = (s = n == null ? void 0 : n.closest) == null ? void 0 : s.call(n, "[data-item-id]")) == null ? void 0 : o.dataset) == null ? void 0 : l.itemId) ?? ((f = (d = (u = (c = i == null ? void 0 : i.target) == null ? void 0 : c.closest) == null ? void 0 : u.call(c, "[data-item-id]")) == null ? void 0 : d.dataset) == null ? void 0 : f.itemId) ?? ""
  ).trim();
  return a ? this.actor.items.get(a) ?? null : null;
}, Dv = function() {
  var a;
  const n = (a = this._getRootElement) == null ? void 0 : a.call(this);
  if (!n) return;
  E(this, J, ug).call(this);
  const i = new AbortController();
  De(this, $s, i), n.addEventListener("dragstart", (r) => {
    var c, u, d;
    const s = (u = (c = r.target) == null ? void 0 : c.closest) == null ? void 0 : u.call(c, "[data-weapon-attack-uuid]");
    if (!s || !n.contains(s)) return;
    const o = E(this, J, gr).call(this, s, r), l = o ? sM(o) : null;
    if (!l) {
      r.preventDefault();
      return;
    }
    r.stopPropagation(), (d = r.dataTransfer) == null || d.setData("text/plain", JSON.stringify(l)), r.dataTransfer && (r.dataTransfer.effectAllowed = "copy");
  }, { signal: i.signal });
}, ug = function() {
  var n;
  (n = X(this, $s)) == null || n.abort(), De(this, $s, null);
}, Ov = function(n, i, a = "That action is not available right now.") {
  var o, l, c, u, d;
  const r = ((o = n == null ? void 0 : n.closest) == null ? void 0 : o.call(n, "[data-action-disabled='true']")) ?? ((c = (l = i == null ? void 0 : i.target) == null ? void 0 : l.closest) == null ? void 0 : c.call(l, "[data-action-disabled='true']"));
  if (!r) return !1;
  const s = String(((u = r.dataset) == null ? void 0 : u.actionReason) ?? a).trim() || a;
  return (d = ui.notifications) == null || d.warn(s), !0;
}, dg = function(n, i) {
  return `${String(n ?? "").trim()}:${String(i ?? "").trim()}`;
}, F(Je, "PARTS", {
  sheet: {
    get template() {
      return `${ht}/v2/actor/character-sheet.hbs`;
    }
  }
}), F(Je, "DEFAULT_OPTIONS", foundry.utils.mergeObject(vi(Je, Je, "DEFAULT_OPTIONS"), {
  classes: ["character-sheet", I, "actor-sheet-v2", "mwd-character-sheet", "mwd-sheet"],
  window: { minWidth: 450, minHeight: 740, resizable: !0 },
  position: { width: 980, height: 900 },
  actions: {
    ...vi(Je, Je, "DEFAULT_OPTIONS").actions,
    edgeSet: Je.prototype._onEdgeSet,
    personalCritRemedy: Je.prototype._onPersonalCritRemedy,
    createOwnedItem: Je.prototype._onCreateOwnedItem,
    addSkillSpecialization: Je.prototype._onAddSkillSpecialization,
    removeSkillSpecialization: Je.prototype._onRemoveSkillSpecialization,
    createLifeModuleItem: Je.prototype._onCreateLifeModuleItem,
    editOwnedItem: Je.prototype._onEditOwnedItem,
    deleteOwnedItem: Je.prototype._onDeleteOwnedItem,
    toggleInventoryAccordion: Je.prototype._onToggleInventoryAccordion,
    toggleOwnedItemEquipped: Je.prototype._onToggleOwnedItemEquipped,
    setOwnedItemPrimary: Je.prototype._onSetOwnedItemPrimary,
    adjustGearQuantity: Je.prototype._onAdjustGearQuantity,
    openAssignedMech: Je.prototype._onOpenAssignedMech,
    mechAttack: Je.prototype._onMechAttack,
    mechMovement: Je.prototype._onMechMovement,
    mechRoll: Je.prototype._onMechRoll,
    openAdvancementMode: Je.prototype._onOpenAdvancementMode,
    closeAdvancementMode: Je.prototype._onCloseAdvancementMode,
    commitAdvancementPurchase: Je.prototype._onCommitAdvancementPurchase,
    openAdvancementDialog: Je.prototype._onOpenAdvancementDialog,
    addKnowledgeSkill: Je.prototype._onAddKnowledgeSkill,
    removeKnowledgeSkill: Je.prototype._onRemoveKnowledgeSkill
  }
}, { inplace: !1 }));
let lg = Je;
const NK = z.mwd.armorMitigationType, PK = z.item.gear.categoryLabels, RK = z.item.consumable.categoryLabels;
function DK(t = {}, { editing: e = !1 } = {}) {
  if (e) return t;
  const n = (i) => Number((i == null ? void 0 : i.rating) ?? 0) !== 0;
  return {
    ...t,
    left: (t.left ?? []).filter(n),
    right: (t.right ?? []).filter(n)
  };
}
class Lv extends Al {
  static get DEFAULT_OPTIONS() {
    return foundry.utils.mergeObject(super.DEFAULT_OPTIONS, {
      classes: ["npc-sheet", I, "actor-sheet-v2"],
      position: { width: 920, height: 860 }
    }, { inplace: !1 });
  }
  async _prepareContext(e) {
    var s, o;
    const n = await super._prepareContext(e), i = this.actor, a = ((s = this.getSheetTokenDocument) == null ? void 0 : s.call(this)) ?? null;
    if (n.layout = await Hr.get("npc"), Number(((o = n.layout) == null ? void 0 : o.version) ?? 0) <= 0)
      throw new Error("MWD NPC sheet layout failed to load.");
    n.conditionMonitors = mh(i, {
      editable: this.isEditable
    });
    const r = D.getSnapshot(i, { token: a });
    return n.combatDashboard = fh(r, { actor: i }), n.activePersonalCriticals = ph(i), n.personalSpeed = gh(i), n.battleArmor = Fp(i), n.combatActions = this._buildCombatActionsContext(
      D.buildActionModel(i, r)
    ), n.combatAwarenessPreview = Ed(i, {
      sourceToken: a
    }), n.skillsDisplay = DK(n.skillsDisplay, {
      editing: this.editing
    }), n.personalInventory = M0(i, {
      items: n.items,
      isEditable: this.isEditable,
      isExpanded: (l) => this._isInventoryRowExpanded(l),
      inventoryAccordionId: (l, c) => this._inventoryAccordionId(l, c),
      armorModifierLabels: NK,
      gearCategoryLabels: PK,
      consumableCategoryLabels: RK
    }), n.actorSheet = {
      profileFields: [
        O_(i, "system.role", "Role / Archetype")
      ],
      attributeFields: $_(i, [
        { key: "strength", label: "Strength" },
        { key: "reflexes", label: "Reflexes" },
        { key: "intelligence", label: "Intelligence" },
        { key: "guts", label: "Guts" },
        { key: "charisma", label: "Charisma" },
        { key: "edge", label: "Edge" }
      ])
    }, n;
  }
}
F(Lv, "PARTS", {
  sheet: {
    template: `${ht}/v2/actor/npc-sheet.hbs`,
    scrollable: [".sheet-body"]
  }
});
function $v(t) {
  return t && typeof t == "object" ? t : {};
}
function _v(t = "") {
  return String(t ?? "").trim();
}
function OK(t) {
  return (Array.isArray(t) ? t : Object.values($v(t))).map(_v).filter(Boolean);
}
function Vh(t = []) {
  return (Array.isArray(t) ? t : Object.values($v(t))).map((n) => ({
    ...n ?? {},
    weaponIds: OK(n == null ? void 0 : n.weaponIds)
  }));
}
function LK(t = [], e = []) {
  const n = Vh(t), i = new Set(Array.from(e ?? []).map(_v).filter(Boolean));
  let a = !1;
  const r = n.map((s) => {
    const o = s.weaponIds.filter((l) => i.has(l));
    return o.length !== s.weaponIds.length && (a = !0), {
      ...s,
      weaponIds: o
    };
  });
  return {
    changed: a,
    groups: r
  };
}
const { ApplicationV2: $K, HandlebarsApplicationMixin: _K } = foundry.applications.api, Ho = class Ho extends _K($K) {
  static get DEFAULT_OPTIONS() {
    return foundry.utils.mergeObject(super.DEFAULT_OPTIONS, {
      id: "select-actor",
      classes: ["select-actor", "anarchy-dialog"],
      position: { width: 300, height: "auto" },
      window: {
        resizable: !0
      }
    }, { inplace: !1 });
  }
  static async selectActor(e, n, i = async (r) => {
  }, a = async () => {
  }) {
    var o, l, c, u;
    const r = {
      id: `select-actor-${foundry.utils.randomID()}`,
      classes: [((u = (c = (l = (o = game.system) == null ? void 0 : o.mwd) == null ? void 0 : l.styles) == null ? void 0 : c.selectCssClass) == null ? void 0 : u.call(c)) ?? "", ...Ho.DEFAULT_OPTIONS.classes].filter(Boolean),
      window: { title: e }
    };
    return new Ho({ actors: n, onActorSelected: i, onCancel: a }, r).render({ force: !0 });
  }
  constructor(e = {}, n = {}) {
    super(e, n), this.actors = e.actors, this.onActorSelected = e.onActorSelected, this.onCancel = e.onCancel, this._actorSelected = !1;
  }
  async _prepareContext() {
    return { actors: this.actors };
  }
  _onRender(e, n) {
    var i;
    (i = super._onRender) == null || i.call(this, e, n), $(this.element).find(".click-select-actor").on("click", (a) => this.onSelectActor(a)), $(this.element).find('[data-action="cancel"]').on("click", async () => await this.close());
  }
  async onSelectActor(e) {
    const n = $(e.currentTarget).attr("data-actor-id"), i = this.actors.find((a) => a.id == n);
    i && (this._actorSelected = !0, await this.onActorSelected(i), await this.close());
  }
  async close(e) {
    return !this._actorSelected && this.onCancel && await this.onCancel(), super.close(e);
  }
};
F(Ho, "PARTS", {
  body: {
    template: `${ht}/dialog/select-actor.hbs`
  }
});
let mg = Ho;
function xe(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function fg(t = []) {
  return t.map((e) => String(e ?? "").trim()).filter(Boolean);
}
function BK(t = null, e = "") {
  var i, a, r, s;
  const n = String(e ?? "").trim();
  return !t || !n ? 0 : Math.max(0, xe(
    ((i = t.getSkillRating) == null ? void 0 : i.call(t, n)) ?? ((s = (r = (a = t.system) == null ? void 0 : a.skills) == null ? void 0 : r[n]) == null ? void 0 : s.rating),
    0
  ));
}
function xK(t = null) {
  const e = /* @__PURE__ */ new Map([
    ["strength", { label: "Strength", skills: [] }],
    ["reflexes", { label: "Reflexes", skills: [] }],
    ["intelligence", { label: "Intelligence", skills: [] }],
    ["guts", { label: "Guts", skills: [] }],
    ["charisma", { label: "Charisma", skills: [] }]
  ]);
  for (const n of tn) {
    const i = BK(t, n.code);
    if (i <= 0) continue;
    const a = e.get(n.attribute);
    a && a.skills.push({ ...n, rating: i });
  }
  return Array.from(e.values()).filter((n) => n.skills.length);
}
function zK(t) {
  return String(t ?? "").replace(/<[^>]*>/g, " ").replace(/\s+/g, " ").trim();
}
function PS(t, e = 180) {
  const n = zK(t);
  return n ? n.length <= e ? n : `${n.slice(0, Math.max(0, e - 3)).trim()}...` : "";
}
function FK() {
  var t, e, n;
  return ((t = game.mwd) == null ? void 0 : t.machineActions) ?? ((n = (e = game.system) == null ? void 0 : e.mwd) == null ? void 0 : n.machineActions) ?? null;
}
async function Ni(t, e = {}) {
  const n = FK();
  if (!(n != null && n.execute)) throw new Error("MWD machine action service not initialized.");
  return n.execute(t, e);
}
const RS = Object.freeze({
  energy: "ENG",
  ballistic: "BAL",
  missile: "MSL",
  omni: "OMNI"
});
function dr(t = []) {
  return t.map((e) => {
    const n = e ?? {};
    return {
      ...n,
      label: String(n.label ?? "").trim(),
      value: String(n.value ?? "").trim(),
      emphasis: n.emphasis ?? "",
      title: String(n.title ?? "").trim(),
      tone: String(n.tone ?? "").trim(),
      parts: Array.isArray(n.parts) ? n.parts.filter((i) => i && i.value !== void 0 && i.value !== null && String(i.value).trim() !== "").map((i) => ({
        label: String(i.label ?? "").trim(),
        value: String(i.value ?? "").trim(),
        tone: String(i.tone ?? "").trim(),
        title: String(i.title ?? "").trim()
      })) : []
    };
  }).filter((e) => e.value !== "" || e.parts.length).map((e) => ({
    ...e,
    hasParts: e.parts.length > 0
  }));
}
function Pm(t = []) {
  return fg(t).map((e) => ({ label: e }));
}
function os(t = []) {
  return t.filter((e) => e && e.value !== void 0 && e.value !== null && String(e.value).trim() !== "").map((e) => ({
    label: String(e.label ?? "").trim(),
    value: String(e.value ?? "").trim()
  }));
}
function UK(t = "") {
  const e = String(t ?? "").trim().toLowerCase();
  return RS[e] ? RS[e] : ce(e).slice(0, 3).toUpperCase() || "SLOT";
}
function KK(t = {}) {
  return ["close", "near", "far", "extreme", "max"].filter((i) => (t == null ? void 0 : t[i]) !== void 0 && (t == null ? void 0 : t[i]) !== null && String(t[i]).trim() !== "").map((i) => {
    const a = t[i];
    return i === "max" ? `Max ${ce(a)}` : `${ce(i)} ${xe(a, 0)}`;
  }).join(" | ");
}
const HK = Object.freeze({
  handling: "Handling",
  system: "System",
  chassis: "Chassis",
  reliability: "Reliability"
}), Rm = Object.freeze({
  mechWeapon: "Machine Weapon",
  vehicleWeapon: "Vehicle Weapon",
  personalWeapon: "Personal Weapon",
  assetModule: "Asset Module",
  vehicleUpgrade: "Vehicle Upgrade",
  mechEquipment: "Mech Equipment",
  gear: "Gear",
  quality: "Trait",
  skill: "Skill"
});
function DS(t = null) {
  var e, n, i;
  return (t == null ? void 0 : t.type) === "battlemech" ? Yl(t) : ((i = (n = (e = t == null ? void 0 : t.system) == null ? void 0 : e.mwd) == null ? void 0 : n.mobility) == null ? void 0 : i.jumping) ?? null;
}
const OS = Object.freeze({
  battlemech: Object.freeze({
    artPath: "systems/mwd/img/mek/misc/repair/location_mek.png",
    mode: "silhouette",
    positions: Object.freeze({
      head: Object.freeze({ top: "9%", left: "50%" }),
      torso: Object.freeze({ top: "40%", left: "50%" }),
      arms: Object.freeze({ top: "34%", left: "18%" }),
      legs: Object.freeze({ top: "75%", left: "50%" })
    })
  }),
  vehicle: Object.freeze({
    artPath: "",
    mode: "schematic",
    positions: Object.freeze({
      body: Object.freeze({ top: "51%", left: "50%" }),
      turret: Object.freeze({ top: "29%", left: "50%" }),
      mobility: Object.freeze({ top: "77%", left: "50%" })
    })
  })
});
function jK({ condition: t = 0, destroyed: e = !1, stress: n = 0 } = {}) {
  return e ? "dark-red" : Number(t ?? 0) >= 4 ? "red" : Number(t ?? 0) >= 3 ? "orange" : Number(t ?? 0) >= 2 ? "yellow" : Number(n ?? 0) > 0 ? "green" : "";
}
var ua, Bs, xs, Mr, de, Bv, xv, zv, Fv, Uv, Kv, Hv, jv, Gv, Qc, gg, qv, Wv, Ri, pg, Vv, Xc, Jv, Ro, Yv;
const $e = class $e extends Al {
  constructor() {
    super(...arguments);
    Fe(this, de);
    Fe(this, ua, /* @__PURE__ */ new Set());
    Fe(this, Bs, null);
    Fe(this, xs, null);
    Fe(this, Mr, []);
  }
  async close(n = {}) {
    var i, a;
    return E(this, de, Wv).call(this), (i = X(this, xs)) == null || i.abort(), (a = X(this, Bs)) == null || a.abort(), super.close(n);
  }
  async _prepareContext(n) {
    var o, l, c, u, d, f;
    const i = await super._prepareContext(n);
    i._mwdThemeClass = ((u = (c = (l = (o = game.system) == null ? void 0 : o.mwd) == null ? void 0 : l.styles) == null ? void 0 : c.selectCssClass) == null ? void 0 : u.call(c)) ?? "", i.layout = await Hr.get(this.constructor.LAYOUT_ID ?? $e.LAYOUT_ID);
    const a = ((d = this.getPersistentActor) == null ? void 0 : d.call(this)) ?? this.actor, r = ((f = this.getSheetTokenDocument) == null ? void 0 : f.call(this)) ?? this._resolveStatusToken(a), s = tN(a);
    return i.combatAwarenessPreview = Ed(a, {
      sourceToken: r
    }), i.vehicleSheet = {
      summaryStats: this._buildSummaryStats(),
      summaryActions: this._buildSummaryActions(),
      alerts: this._buildAlerts(),
      quickActions: this._buildQuickActions(),
      strain: this._buildStrainModel(),
      movementProfile: this._buildMovementProfilePanel(),
      crewPanel: this._buildCrewPanel(),
      statusAction: {
        label: "Statuses",
        activeStatuses: s,
        summaryTitle: s.map((m) => m.label).join(", "),
        disabled: !this._resolveStatusToken(a),
        reason: "Statuses require a token for this actor on the current scene."
      },
      ewPanel: this._buildEwPanel(),
      signature: this._buildSignaturePanel(a),
      activeCrits: this._buildActiveCrits(),
      attributes: this._buildAttributeCards(),
      movement: this._buildMovementCards(),
      degradation: this._buildDegradationPanel(),
      hardpoints: this._buildMachineHardpoints(),
      hardpointOptions: this._buildHardpointOptions(),
      sections: this._buildVehicleSections(),
      pilotPanel: await this._buildPilotPanel()
    }, i.conditionMonitors = this._buildConditionMonitors(), i;
  }
  async _buildPilotPanel() {
    var r, s, o;
    const n = ((r = this.getPersistentActor) == null ? void 0 : r.call(this)) ?? this.actor, i = String(((o = (s = n.system) == null ? void 0 : s.pilot) == null ? void 0 : o.uuid) ?? "").trim();
    let a = null;
    if (i)
      try {
        a = await fromUuid(i);
      } catch {
      }
    return {
      uuid: i,
      linked: !!a,
      name: (a == null ? void 0 : a.name) ?? null,
      id: (a == null ? void 0 : a.id) ?? null,
      canEdit: !!this.isEditable
    };
  }
  _buildSignaturePanel(n = null) {
    var s, o, l, c, u, d;
    const i = ((o = (s = n == null ? void 0 : n.system) == null ? void 0 : s.mwd) == null ? void 0 : o.stealth) ?? {}, a = gd(n), r = Array.isArray(a.counteredBy) && a.counteredBy.length ? a.counteredBy.join(", ") : "None";
    return {
      enabled: !!i.enabled,
      rating: Math.max(0, xe(i.rating, 0)),
      mode: a.mode,
      modeOptions: ["passive", "active", "suppressed"].map((f) => ({
        value: f,
        label: ce(f),
        selected: a.mode === f
      })),
      detectionCap: String(i.detectionCap ?? ""),
      signature: String(i.signature ?? ""),
      notes: String(i.notes ?? ""),
      effectiveRating: a.effectiveRating,
      baseRating: a.baseRating,
      contributionRating: a.contributionRating,
      revealedLabel: a.revealed ? "Yes" : "No",
      revealPenalty: a.revealPenalty,
      emissionRating: ((l = a.emission) == null ? void 0 : l.effectiveEmissionRating) ?? 0,
      authoredEmissionRating: ((c = a.emission) == null ? void 0 : c.authoredEmissionRating) ?? 0,
      transientEmissionRating: ((u = a.emission) == null ? void 0 : u.transientEmissionRating) ?? 0,
      statusEmissionRating: ((d = a.emission) == null ? void 0 : d.statusEmissionRating) ?? 0,
      counters: r,
      parts: a.parts.map((f) => ({
        label: f.label,
        value: Number(f.value ?? 0),
        displayValue: `${Number(f.value ?? 0) >= 0 ? "+" : ""}${Number(f.value ?? 0) || 0}`
      }))
    };
  }
  async _onAssignPilot(n, i) {
    var s, o;
    if (!this.isEditable) return;
    const a = (((s = game.actors) == null ? void 0 : s.contents) ?? []).filter((l) => l.type === "character");
    if (!a.length) {
      (o = ui.notifications) == null || o.warn("No character actors found in this world.");
      return;
    }
    const r = this.getPersistentActor() ?? this.actor;
    await mg.selectActor(
      "Assign Pilot",
      a,
      async (l) => r.update({ "system.pilot.uuid": l.uuid })
    );
  }
  async _onRemovePilot(n, i) {
    if (!this.isEditable) return;
    await (this.getPersistentActor() ?? this.actor).update({ "system.pilot.uuid": "" });
  }
  async _onOpenPilot(n, i) {
    var o, l, c;
    const a = ((o = this.getPersistentActor) == null ? void 0 : o.call(this)) ?? this.actor, r = String(((c = (l = a.system) == null ? void 0 : l.pilot) == null ? void 0 : c.uuid) ?? "").trim();
    if (!r) return;
    const s = await fromUuid(r).catch(() => null);
    s && s.sheet.render(!0, { focus: !0 });
  }
  async _onDrop(n) {
    var a, r, s;
    if (!this.isEditable) return (a = super._onDrop) == null ? void 0 : a.call(this, n);
    let i;
    try {
      i = (((r = foundry.applications.ux.TextEditor) == null ? void 0 : r.implementation) ?? TextEditor).getDragEventData(n);
    } catch {
    }
    if (!await this._handleHardpointItemDrop(n, i)) {
      if ((i == null ? void 0 : i.type) === "Actor") {
        const o = await fromUuid(i.uuid).catch(() => null);
        if ((o == null ? void 0 : o.type) === "character") {
          await (this.getPersistentActor() ?? this.actor).update({ "system.pilot.uuid": o.uuid });
          return;
        }
      }
      return (s = super._onDrop) == null ? void 0 : s.call(this, n);
    }
  }
  _onRender(n, i) {
    var a;
    (a = super._onRender) == null || a.call(this, n, i), E(this, de, Bv).call(this), E(this, de, xv).call(this), E(this, de, qv).call(this);
  }
  _buildSummaryStats() {
    var r, s, o, l, c, u, d, f, m, g;
    const n = ((r = this.actor.system) == null ? void 0 : r.attributes) ?? {}, i = ((o = (s = this.actor.system) == null ? void 0 : s.monitors) == null ? void 0 : o.structure) ?? {}, a = nT({
      actorType: this.actor.type,
      movement: (l = this.actor.system) == null ? void 0 : l.movement,
      legacyMoves: (c = this.actor.system) == null ? void 0 : c.moves,
      jumpProfile: DS(this.actor),
      movementEffects: Rr(this.actor)
    });
    return dr([
      { label: "Handling", value: xe((u = n.handling) == null ? void 0 : u.value, 0), emphasis: "strong" },
      { label: "Move", parts: a },
      { label: "System", value: xe((d = n.system) == null ? void 0 : d.value, 0) },
      { label: "Chassis", value: xe((f = n.chassis) == null ? void 0 : f.value, 0) },
      { label: "Reliability", value: xe(((m = n.reliability) == null ? void 0 : m.value) ?? ((g = n.condition) == null ? void 0 : g.value), 0) },
      { label: "Structure", value: `${xe(i.value, 0)} / ${xe(i.max, 0)}` }
    ]);
  }
  _buildAlerts() {
    return [];
  }
  _buildSummaryActions() {
    return [];
  }
  _buildQuickActions() {
    var l;
    if (this.actor.type !== "vehicle") return [];
    const n = ((l = this.getPersistentActor) == null ? void 0 : l.call(this)) ?? this.actor, i = ng(n), a = fS(n), s = ag(n).filter((c) => !c.disabled), o = Gr(n, {
      token: this._resolveStatusToken(n)
    });
    return [
      {
        label: "Movement",
        hint: s.length ? "Move action" : "No moves",
        handler: "vehicleMovement",
        disabled: s.length === 0,
        dataset: {}
      },
      {
        label: "Mounted Fire",
        hint: i.length ? "Ranged attack" : "No weapons",
        handler: "vehicleAttack",
        disabled: i.length === 0,
        dataset: {}
      },
      {
        label: "Melee",
        hint: a.length ? "Melee attack" : "No profiles",
        handler: "vehicleMeleeAttack",
        disabled: a.length === 0,
        dataset: {}
      },
      {
        label: "Maneuvers",
        hint: "Charge attack",
        handler: "vehicleChargeAttack",
        disabled: !1,
        dataset: {}
      },
      {
        label: "Piloting Check",
        hint: "Piloting check",
        handler: "vehicleRoll",
        disabled: !1,
        dataset: { rollKind: "piloting" }
      },
      {
        label: "Electronic Warfare",
        hint: o.length ? "EW action" : "No EW actions",
        handler: "vehicleRoll",
        disabled: o.length === 0,
        dataset: { rollKind: "sensor" }
      },
      {
        label: "Repair",
        hint: "Repair action",
        handler: "vehicleRoll",
        disabled: !1,
        dataset: { rollKind: "repair" }
      }
    ];
  }
  _buildStrainModel() {
    if (this.actor.type !== "vehicle") return null;
    const n = ol(this.actor), i = n.thresholds ?? {};
    return {
      ...n,
      editable: !!this.isEditable,
      segments: Array.from({ length: n.max }, (a, r) => {
        const s = r + 1, o = s >= i.critical ? "critical" : s >= i.overstressed ? "overstressed" : s >= i.strained ? "strained" : "normal";
        return {
          value: s,
          filled: s <= n.value,
          current: s === n.value,
          band: o,
          bandLabel: ce(o)
        };
      })
    };
  }
  _buildMovementProfilePanel() {
    if (this.actor.type !== "vehicle") return null;
    const n = nv(this.actor.system ?? {}), i = Object.values(ig).map((s) => ({
      value: s.key,
      label: s.label,
      selected: s.key === n.key
    })), a = Object.entries(tv).map(([s, o]) => ({
      value: s,
      label: o,
      selected: s === n.flightSubtype
    })), r = qh.map((s) => ({
      value: s,
      label: ce(s)
    }));
    return {
      ...n,
      profileOptions: i,
      flightSubtypeOptions: a,
      terrainOptions: r,
      favoredTerrainText: n.favoredTerrain.map(ce).join(", "),
      adverseTerrainText: n.adverseTerrain.map(ce).join(", "),
      affordanceText: n.affordances.map(ce).join(", "),
      profilePath: "system.mwd.movementProfile",
      flightSubtypePath: "system.mwd.flightSubtype",
      favoredTerrainPath: "system.mwd.favoredTerrain",
      adverseTerrainPath: "system.mwd.adverseTerrain"
    };
  }
  _buildCrewPanel() {
    var i, a;
    const n = ((a = (i = this.actor.system) == null ? void 0 : i.mwd) == null ? void 0 : a.crew) ?? {};
    return {
      count: xe(n.count, 1),
      effectiveCount: xe(n.effectiveCount ?? n.count, 1),
      injuryLevel: xe(n.injuryLevel, 0),
      bailedOut: !!n.bailedOut,
      countPath: "system.mwd.crew.count",
      effectiveCountPath: "system.mwd.crew.effectiveCount",
      injuryLevelPath: "system.mwd.crew.injuryLevel",
      bailedOutPath: "system.mwd.crew.bailedOut",
      summary: fg([
        `${xe(n.effectiveCount ?? n.count, 1)} / ${xe(n.count, 1)} effective`,
        xe(n.injuryLevel, 0) > 0 ? `Injury ${xe(n.injuryLevel, 0)}` : "",
        n.bailedOut ? "Bailed Out" : ""
      ]).join(" | ")
    };
  }
  _buildEwPanel() {
    var a;
    const n = ((a = this.getPersistentActor) == null ? void 0 : a.call(this)) ?? this.actor, i = this._resolveStatusToken(n);
    return Ml({ actor: n, token: i });
  }
  _buildAttributeCards() {
    var i;
    const n = ((i = this.actor.system) == null ? void 0 : i.attributes) ?? {};
    return Object.entries(HK).map(([a, r]) => {
      var s;
      return {
        key: a,
        label: r,
        value: xe((s = n == null ? void 0 : n[a]) == null ? void 0 : s.value, 0),
        path: `system.attributes.${a}.value`
      };
    });
  }
  _buildMovementCards() {
    var n, i;
    return tT({
      actorType: this.actor.type,
      movement: (n = this.actor.system) == null ? void 0 : n.movement,
      legacyMoves: (i = this.actor.system) == null ? void 0 : i.moves,
      editing: this.editing,
      jumpProfile: DS(this.actor),
      movementEffects: Rr(this.actor)
    });
  }
  _buildDegradationPanel() {
    var h, b, y, S;
    const n = eo(
      foundry.utils.deepClone(this.actor.system ?? {}),
      this.actor.type
    ), i = n.attributes ?? {}, a = n.mwd ?? {}, r = xe(((h = i.reliability) == null ? void 0 : h.value) ?? ((b = i.condition) == null ? void 0 : b.value), 0), s = fT(r), o = xe((y = a.shock) == null ? void 0 : y.value, 0), l = xe((S = a.reliabilitySpendable) == null ? void 0 : S.value, r), c = pn(this.actor), u = Rr(this.actor), d = OS[this.actor.type] ?? OS.vehicle, f = bR(this.actor.type), m = Object.entries(a.locations ?? {}).sort(([A], [T]) => {
      const C = f.indexOf(A), k = f.indexOf(T), w = C >= 0 ? C : Number.MAX_SAFE_INTEGER, v = k >= 0 ? k : Number.MAX_SAFE_INTEGER;
      return w !== v ? w - v : String(A).localeCompare(String(T));
    }), g = Object.entries(hf).map(([A, T]) => ({
      value: Number(A),
      label: T
    })), p = m.map(([A, T]) => {
      const C = xe(T == null ? void 0 : T.condition, 0), k = xe(T == null ? void 0 : T.stress, 0), w = !!(T != null && T.destroyed), v = jK({ condition: C, destroyed: w, stress: k }), N = d.positions[A] ?? { top: "50%", left: "50%" };
      return {
        key: A,
        label: $i(A),
        stress: k,
        stressPath: `system.mwd.locations.${A}.stress`,
        conditionValue: C,
        conditionPath: `system.mwd.locations.${A}.condition`,
        conditionLabel: Ap(C),
        conditionModifier: kp(C),
        conditionOptions: g.map((P) => ({ ...P, selected: P.value === C })),
        destroyed: w,
        effectSummary: kO(this.actor.type, A, T),
        enabled: (T == null ? void 0 : T.enabled) !== !1,
        tone: v,
        style: `--pin-top:${N.top}; --pin-left:${N.left};`,
        stressLabel: k > 0 ? `Stress ${k}` : "Stress 0"
      };
    });
    return {
      mode: d.mode,
      artPath: d.artPath,
      reliability: r,
      spendable: l,
      shock: o,
      threshold: s,
      shockPath: "system.mwd.shock.value",
      spendablePath: "system.mwd.reliabilitySpendable.value",
      summaryEffects: c.effectTexts ?? [],
      movementEffects: u,
      locations: p
    };
  }
  _buildConditionMonitors() {
    var a, r, s, o, l, c, u;
    const n = ((r = (a = this.actor.system) == null ? void 0 : a.monitors) == null ? void 0 : r.structure) ?? ((l = (o = (s = this.actor.system) == null ? void 0 : s.mwd) == null ? void 0 : o.monitors) == null ? void 0 : l.structure) ?? {}, i = ((u = (c = this.actor.system) == null ? void 0 : c.monitors) == null ? void 0 : u.armor) ?? {};
    return [
      Iu({ id: "structure", label: "Structure", kind: "structure", monitor: n, editable: this.isEditable }),
      Iu({ id: "armor", label: "Armor", kind: "armor", monitor: i, editable: this.isEditable })
    ];
  }
  _buildVehicleSections() {
    const n = ot.classify(this.actor.items);
    return {
      slots: this._buildHardpointSlotSection(),
      upgrades: this._buildRecordSection({
        sectionId: "upgrades",
        itemType: "vehicleUpgrade",
        addLabel: "Add Upgrade",
        emptyLabel: "No vehicle upgrades installed.",
        items: n.vehicleUpgrade ?? []
      }),
      modules: this._buildRecordSection({
        sectionId: "modules",
        itemType: "assetModule",
        addLabel: "Add Module",
        emptyLabel: "No asset modules assigned.",
        items: n.assetModule ?? []
      }),
      gear: this._buildRecordSection({
        sectionId: "gear",
        itemType: "gear",
        addLabel: "Add Gear",
        emptyLabel: "No stored gear or consumables.",
        items: [
          ...n.gear ?? [],
          ...n.consumable ?? []
        ]
      })
    };
  }
  _buildHardpointSlotSection() {
    var r;
    const n = ((r = this.getPersistentActor) == null ? void 0 : r.call(this)) ?? this.actor;
    return {
      sectionId: "slots",
      itemType: "",
      addLabel: "",
      emptyLabel: "No hardpoint slots configured.",
      records: bi(n).map(
        (s, o) => this._buildHardpointSlotRecord(s, o)
      )
    };
  }
  _buildMachineHardpoints() {
    var d, f, m, g;
    const n = ((d = this.getPersistentActor) == null ? void 0 : d.call(this)) ?? this.actor, i = bi(n), a = Array.from(((g = (m = (f = n.system) == null ? void 0 : f.mwd) == null ? void 0 : m.loadout) == null ? void 0 : g.hardpoints) ?? []), r = new Map(this._getHardpointSlottableItems().map((p) => [String((p == null ? void 0 : p.id) ?? "").trim(), p]).filter(([p]) => p)), s = new Map(a.map((p, h) => [
      String((p == null ? void 0 : p.id) ?? `hardpoint-${h + 1}`),
      p
    ])), o = this._getHardpointTypeLabels(), l = this._getHardpointSizeLabels(), c = this._getHardpointLocationLabels(), u = this._buildHardpointOptions();
    return i.map((p, h) => {
      var k;
      const b = String((p == null ? void 0 : p.id) ?? `hardpoint-${h + 1}`), y = s.get(b) ?? {}, S = String((p == null ? void 0 : p.type) ?? (y == null ? void 0 : y.type) ?? "energy").trim() || "energy", A = String((p == null ? void 0 : p.size) ?? (y == null ? void 0 : y.size) ?? "small").trim() || "small", T = String((p == null ? void 0 : p.location) ?? (y == null ? void 0 : y.location) ?? "").trim() || this._getDefaultHardpointLocation(), C = String(
        ((k = r.get(String((p == null ? void 0 : p.itemId) ?? "").trim())) == null ? void 0 : k.name) ?? (y == null ? void 0 : y.occupiedByName) ?? ""
      ).trim();
      return {
        id: b,
        index: h,
        itemId: String((p == null ? void 0 : p.itemId) ?? "").trim(),
        type: S,
        size: A,
        location: T,
        typeLabel: o[S] ?? ce(S),
        sizeLabel: l[A] ?? ce(A),
        locationLabel: c[T] ?? ce(T),
        occupiedByName: C,
        occupancyLabel: C || "Open",
        typeOptions: u.types.map((w) => ({ ...w, selected: w.value === S })),
        sizeOptions: u.sizes.map((w) => ({ ...w, selected: w.value === A })),
        locationOptions: u.locations.map((w) => ({ ...w, selected: w.value === T }))
      };
    });
  }
  _buildHardpointOptions() {
    return {
      types: Object.entries(this._getHardpointTypeLabels()).map(([n, i]) => ({ value: n, label: i })),
      sizes: Object.entries(this._getHardpointSizeLabels()).map(([n, i]) => ({ value: n, label: i })),
      locations: this._getAvailableHardpointLocations().map((n) => ({
        value: n,
        label: this._getHardpointLocationLabels()[n] ?? ce(n)
      }))
    };
  }
  _getHardpointTypeLabels() {
    var n, i, a;
    return ((n = z == null ? void 0 : z.mwd) == null ? void 0 : n.hardpointType) ?? ((a = (i = z == null ? void 0 : z.mwd) == null ? void 0 : i.hardpoint) == null ? void 0 : a.type) ?? {};
  }
  _getHardpointSizeLabels() {
    var n, i, a;
    return ((n = z == null ? void 0 : z.mwd) == null ? void 0 : n.hardpointSize) ?? ((a = (i = z == null ? void 0 : z.mwd) == null ? void 0 : i.hardpoint) == null ? void 0 : a.size) ?? {};
  }
  _getHardpointLocationLabels() {
    var n, i, a;
    return ((n = z == null ? void 0 : z.mwd) == null ? void 0 : n.hardpointLocation) ?? ((a = (i = z == null ? void 0 : z.mwd) == null ? void 0 : i.hardpoint) == null ? void 0 : a.location) ?? {};
  }
  _getAvailableHardpointLocations() {
    var n;
    return ((n = this.actor) == null ? void 0 : n.type) === "battlemech" ? ["arms", "head", "torso"] : ["front", "side", "rear", "turret"];
  }
  async _commitEditsToActor() {
    await super._commitEditsToActor(), await this._persistStagedHardpointFields();
  }
  _getCompatibleHardpointItems(n, { includeAssignedItem: i = !1 } = {}) {
    var o;
    const a = ((o = this.getPersistentActor) == null ? void 0 : o.call(this)) ?? this.actor, r = OR(a), s = String((n == null ? void 0 : n.itemId) ?? "").trim();
    return this._getHardpointSlottableItems().filter((l) => {
      const c = String((l == null ? void 0 : l.id) ?? "").trim();
      return !c || !i && r.has(c) && c !== s ? !1 : wp(n, l);
    });
  }
  async _handleHardpointItemDrop(n, i = null) {
    var m, g, p, h, b, y, S, A, T;
    const a = (g = (m = n == null ? void 0 : n.target) == null ? void 0 : m.closest) == null ? void 0 : g.call(m, ".mwd-record[data-hardpoint-id]");
    if (!a) return !1;
    const r = String(((p = a.dataset) == null ? void 0 : p.hardpointId) ?? "").trim();
    if (!r) return !1;
    const s = await this._persistStagedHardpointFields(), o = this.getPersistentActor() ?? this.actor, l = s.find((C) => C.id === r);
    if (!l) return !1;
    if (String((l == null ? void 0 : l.itemId) ?? "").trim())
      return (h = ui.notifications) == null || h.warn("That hardpoint is already filled."), !0;
    const c = await this._resolveDroppedHardpointItem(i);
    if (!c)
      return String((i == null ? void 0 : i.type) ?? "").trim() === "Item" ? ((b = ui.notifications) == null || b.warn("That item could not be mounted into this hardpoint."), !0) : !1;
    const u = LR(l, c);
    if (u)
      return (y = ui.notifications) == null || y.warn(u), !0;
    let d = String((c == null ? void 0 : c.id) ?? "").trim();
    if (!((c == null ? void 0 : c.parent) === o || (c == null ? void 0 : c.actor) === o)) {
      const C = foundry.utils.deepClone(((S = c.toObject) == null ? void 0 : S.call(c)) ?? c ?? {});
      delete C._id;
      const k = await o.createEmbeddedDocuments("Item", [C]);
      d = String(((A = k == null ? void 0 : k[0]) == null ? void 0 : A.id) ?? "").trim();
    }
    return d ? (await this._setHardpointOccupant(r, d, { hardpoints: s }), this.render({ force: !0 }), !0) : ((T = ui.notifications) == null || T.warn("That item could not be mounted."), !0);
  }
  async _resolveDroppedHardpointItem(n = null) {
    var l, c, u, d;
    if (!n || (n == null ? void 0 : n.type) !== "Item") return null;
    const i = ((l = CONFIG == null ? void 0 : CONFIG.Item) == null ? void 0 : l.documentClass) ?? globalThis.Item ?? null;
    if (typeof (i == null ? void 0 : i.fromDropData) == "function") {
      const f = await i.fromDropData(n).catch(() => null);
      if (f) return f;
    }
    const a = ((c = this.getPersistentActor) == null ? void 0 : c.call(this)) ?? this.actor, r = String((n == null ? void 0 : n.itemId) ?? (n == null ? void 0 : n._id) ?? (n == null ? void 0 : n.id) ?? "").trim(), s = String((n == null ? void 0 : n.actorId) ?? (n == null ? void 0 : n.parentId) ?? "").trim();
    if (r && s && s === String((a == null ? void 0 : a.id) ?? "").trim()) {
      const f = ((d = (u = a == null ? void 0 : a.items) == null ? void 0 : u.get) == null ? void 0 : d.call(u, r)) ?? null;
      if (f) return f;
    }
    const o = n != null && n.uuid ? await fromUuid(n.uuid).catch(() => null) : null;
    return !o && (n != null && n.data) && typeof i == "function" ? new i(n.data, { parent: a }) : o || null;
  }
  async _promptHardpointAssignment(n) {
    const i = this._getCompatibleHardpointItems(n);
    if (!i.length) return "";
    if (i.length === 1) return i[0].id ?? "";
    const a = `<form class="mwd-quick-select">${i.map((r, s) => `
      <label class="quick-select-option">
        <input type="radio" name="slot-item" value="${r.id}" ${s === 0 ? "checked" : ""}>
        <span>${r.name}</span>
      </label>`).join("")}</form>`;
    return await Dialog.prompt({
      title: "Assign Hardpoint Item",
      content: a,
      label: "Assign",
      callback: (r) => r.find('input[name="slot-item"]:checked').val() ?? i[0].id
    });
  }
  async _setHardpointOccupant(n = "", i = "", { hardpoints: a = null } = {}) {
    const r = String(n ?? "").trim();
    if (!r) return;
    const s = this.getPersistentActor() ?? this.actor, o = RR(
      a ?? Ec(s),
      r,
      i,
      { defaultLocation: this._getDefaultHardpointLocation() }
    );
    o.changed && await s.update({ "system.mwd.hardpoints": o.hardpoints }), await this._sanitizeMountedWeaponGroups(s);
  }
  async _deleteMountedHardpointItem(n = "") {
    var l, c;
    const i = String(n ?? "").trim();
    if (!i) return;
    const a = this.getPersistentActor() ?? this.actor, r = bi(a).find((u) => u.id === i), s = String((r == null ? void 0 : r.itemId) ?? "").trim();
    !s || !(((c = (l = a.items) == null ? void 0 : l.get) == null ? void 0 : c.call(l, s)) ?? null) || await a.deleteEmbeddedDocuments("Item", [s]);
  }
  _getHardpointSlottableItems() {
    var i;
    const n = ((i = this.getPersistentActor) == null ? void 0 : i.call(this)) ?? this.actor;
    return Array.from((n == null ? void 0 : n.items) ?? []).filter((a) => String((a == null ? void 0 : a.canonicalType) ?? (a == null ? void 0 : a.type) ?? "").trim() === "mechWeapon");
  }
  _buildHardpointSlotRecord(n, i = 0) {
    var C, k, w, v, N, P, L;
    const a = ((C = this.getPersistentActor) == null ? void 0 : C.call(this)) ?? this.actor, r = String((n == null ? void 0 : n.id) ?? `hardpoint-${i + 1}`).trim(), s = String((n == null ? void 0 : n.type) ?? "").trim(), o = String((n == null ? void 0 : n.size) ?? "").trim(), l = String((n == null ? void 0 : n.location) ?? "").trim() || this._getDefaultHardpointLocation(), c = this._getHardpointTypeLabels(), u = this._getHardpointSizeLabels(), d = this._getHardpointLocationLabels(), f = String((n == null ? void 0 : n.itemId) ?? "").trim(), m = f ? ((w = (k = a == null ? void 0 : a.items) == null ? void 0 : k.get) == null ? void 0 : w.call(k, f)) ?? null : null, g = typeof (m == null ? void 0 : m.getCombatProfile) == "function" ? m.getCombatProfile() : null, p = `slot:${r}`, h = c[s] ?? ce(s), b = u[o] ?? ce(o), y = d[l] ?? ce(l);
    if (!m)
      return {
        id: "",
        hardpointId: r,
        accordionId: p,
        className: "is-hardpoint-slot is-empty-hardpoint",
        mediaClassName: "is-slot-placeholder",
        isExpanded: X(this, ua).has(p),
        isEmptyHardpoint: !0,
        slotTypeCode: UK(s),
        slotSizeCode: b.slice(0, 1).toUpperCase() || "S",
        slotOpenLabel: "Open",
        img: "systems/mwd/img/default/Default_Weapon.svg",
        name: `Empty ${h} ${b} Slot`,
        subtitle: `${y} mount available`,
        summaryStats: dr([
          { label: "Mount", value: `${h} ${b}`, emphasis: "strong" },
          { label: "Status", value: "Open", tone: "green" }
        ]),
        detailTags: Pm(["Open Hardpoint", h, b]),
        detailRows: os([
          { label: "Location", value: y },
          { label: "Accepts", value: `${b} ${h} weapons` },
          { label: "Assigned Weapon", value: "Empty" }
        ]),
        detailText: "Drag a matching mech weapon item onto this slot, or create one directly for this slot.",
        createHardpointItem: {
          hardpointId: r,
          itemType: "mechWeapon",
          label: "Create Weapon for Slot"
        }
      };
    const S = (m == null ? void 0 : m.canonicalType) ?? (m == null ? void 0 : m.type) ?? "", A = Rm[S] ?? ce(S || "item"), T = ((v = m == null ? void 0 : m.system) == null ? void 0 : v.notes) ?? ((N = m == null ? void 0 : m.system) == null ? void 0 : N.description) ?? ((L = (P = m == null ? void 0 : m.system) == null ? void 0 : P.references) == null ? void 0 : L.description) ?? "";
    return {
      id: m.id ?? "",
      hardpointId: r,
      accordionId: p,
      className: "is-hardpoint-slot is-occupied-hardpoint",
      isExpanded: X(this, ua).has(p),
      name: m.name ?? A,
      img: m.img ?? "icons/svg/item-bag.svg",
      subtitle: `${A} | ${y}`,
      summaryStats: dr(g ? [
        { label: "DV", value: xe(g.damage, 0), emphasis: "strong" },
        { label: "AP", value: xe(g.ap, 0) },
        { label: "Type", value: g.damageTypeLabel ?? g.damageType ?? "" }
      ] : [
        { label: "Type", value: A, emphasis: "strong" },
        { label: "Slot", value: u[o] ?? ce(o) }
      ]),
      detailTags: Pm([
        "Mounted",
        h
      ]),
      detailRows: os([
        { label: "Slot Type", value: h },
        { label: "Slot Size", value: b },
        { label: "Location", value: y },
        { label: "Assigned Item", value: m.name ?? A }
      ]),
      detailText: PS(T),
      suppressDeleteButton: !0,
      clearHardpointItem: {
        hardpointId: r,
        label: "Empty Slot",
        hint: "Delete the attached weapon and leave this slot open.",
        icon: "fa-trash-can",
        buttonClassName: "is-danger",
        inlineLabel: "Empty Slot"
      },
      machineAttack: S === "mechWeapon" ? { label: "Attack", itemId: m.id ?? "" } : null
    };
  }
  _buildRecordSection({ sectionId: n = "", itemType: i = "", addLabel: a = "", emptyLabel: r = "", items: s = [] } = {}) {
    return {
      sectionId: n,
      itemType: i,
      addLabel: String(a ?? "").trim(),
      emptyLabel: String(r ?? "Nothing here yet.").trim(),
      records: Array.from(s ?? []).map((o) => this._buildItemRecord(o, { sectionId: n }))
    };
  }
  _buildItemRecord(n, { sectionId: i = "" } = {}) {
    var y, S, A, T, C, k, w, v, N, P, L;
    const a = (n == null ? void 0 : n.system) ?? {}, r = (n == null ? void 0 : n.canonicalType) ?? (n == null ? void 0 : n.type) ?? "", s = typeof (n == null ? void 0 : n.getCombatProfile) == "function" ? n.getCombatProfile() : null, o = r === "assetModule" ? qT(n) : null, l = String(((y = a.activation) == null ? void 0 : y.mode) ?? "passive").trim() || "passive", c = r === "assetModule" && ["toggle", "mode"].includes(l), u = !!((S = a.activation) != null && S.active), d = r === "assetModule" && !a.inactive, f = `${String(i ?? "").trim()}:${String((n == null ? void 0 : n.id) ?? "").trim()}`, m = Rm[r] ?? ce(r || "item"), g = a.notes ?? a.description ?? ((A = a.references) == null ? void 0 : A.description) ?? "", p = a.quantity, h = s ? dr([
      { label: "DV", value: xe(s.damage, 0), emphasis: "strong" },
      { label: "AP", value: xe(s.ap, 0) },
      { label: "Type", value: s.damageTypeLabel ?? s.damageType ?? "" }
    ]) : r === "assetModule" && ((C = (T = a == null ? void 0 : a.mobility) == null ? void 0 : T.jumping) != null && C.enabled) ? dr([
      { label: "Type", value: m },
      { label: "Jump", value: xe(a.mobility.jumping.movement, 0), emphasis: "strong" },
      { label: "Heat", value: xe(a.mobility.jumping.heat, 0) }
    ]) : dr([
      { label: "Type", value: m },
      ...p !== void 0 ? [{ label: "Qty", value: xe(p, 0) }] : []
    ]), b = s ? os([
      { label: "Skill", value: ((k = s.skillDef) == null ? void 0 : k.label) ?? s.skill ?? "" },
      { label: "Category", value: s.category ?? a.weaponCategory ?? a.category ?? "" },
      { label: "Range", value: KK(s.range) }
    ]) : r === "assetModule" && ((v = (w = a == null ? void 0 : a.mobility) == null ? void 0 : w.jumping) != null && v.enabled) ? os([
      { label: "Category", value: a.category ?? m },
      { label: "Activation", value: ce(l) },
      { label: "Heat", value: xe(a.mobility.jumping.heat, 0) },
      { label: "AR Bonus", value: xe(a.mobility.jumping.attackRatingBonus, 0) },
      { label: "DR Bonus", value: xe(a.mobility.jumping.defenseRatingBonus, 0) },
      { label: "DFA", value: a.mobility.jumping.dfaEnabled ? "Enabled" : "Disabled" },
      { label: "Effects", value: (o == null ? void 0 : o.summary) ?? "" }
    ]) : os(r === "assetModule" ? [
      { label: "Category", value: a.category ?? m },
      { label: "Activation", value: ce(l) },
      { label: "Effects", value: (o == null ? void 0 : o.summary) ?? "" }
    ] : [
      { label: "Category", value: a.category ?? m },
      { label: "Quantity", value: p !== void 0 ? xe(p, 0) : "" }
    ]);
    return {
      id: (n == null ? void 0 : n.id) ?? "",
      accordionId: f,
      isExpanded: X(this, ua).has(f),
      name: (n == null ? void 0 : n.name) ?? m,
      img: (n == null ? void 0 : n.img) ?? "icons/svg/item-bag.svg",
      subtitle: ((N = s == null ? void 0 : s.skillDef) == null ? void 0 : N.label) ?? a.category ?? m,
      summaryStats: h,
      detailTags: Pm([
        a.equipped ? "Equipped" : "",
        a.isPrimary ? "Primary" : "",
        r === "assetModule" ? d ? "Ready" : "Inactive" : "",
        c ? u ? "Active" : "Standby" : "",
        (L = (P = a.mobility) == null ? void 0 : P.jumping) != null && L.enabled ? "Jumping" : "",
        a.weaponCategory ?? a.category ?? ""
      ]),
      detailRows: b,
      detailText: PS((o == null ? void 0 : o.summary) || g),
      equipped: !!a.equipped,
      isPrimary: !!a.isPrimary,
      supportsModuleActivation: c,
      moduleActive: u,
      moduleReady: d,
      canAdjustQuantity: !1,
      machineAttack: ["mechWeapon", "vehicleWeapon"].includes(r) ? {
        label: "Attack",
        itemId: (n == null ? void 0 : n.id) ?? ""
      } : null
    };
  }
  async _onCreateOwnedItem(n, i) {
    var l, c, u;
    if ((l = n == null ? void 0 : n.preventDefault) == null || l.call(n), (c = n == null ? void 0 : n.stopPropagation) == null || c.call(n), !this.isEditable) return;
    const a = String(((u = i == null ? void 0 : i.dataset) == null ? void 0 : u.itemType) ?? "").trim();
    if (!a) return;
    const r = this.getPersistentActor() ?? this.actor, s = Rm[a] ?? ce(a), o = r.items.filter((d) => d.type === a).length;
    await r.createEmbeddedDocuments("Item", [{
      name: `${s} ${o + 1}`,
      type: a
    }]), this.render({ force: !0 });
  }
  async _onEditOwnedItem(n, i) {
    var r, s, o;
    (r = n == null ? void 0 : n.preventDefault) == null || r.call(n), (s = n == null ? void 0 : n.stopPropagation) == null || s.call(n);
    const a = E(this, de, Qc).call(this, i, n);
    (o = a == null ? void 0 : a.sheet) == null || o.render(!0);
  }
  async _onDeleteOwnedItem(n, i) {
    var o, l;
    if ((o = n == null ? void 0 : n.preventDefault) == null || o.call(n), (l = n == null ? void 0 : n.stopPropagation) == null || l.call(n), !this.isEditable) return;
    const a = E(this, de, Qc).call(this, i, n);
    if (!a) return;
    const r = this.getPersistentActor() ?? this.actor, s = bi(r).find(
      (c) => String((c == null ? void 0 : c.itemId) ?? "").trim() === String((a == null ? void 0 : a.id) ?? "").trim()
    );
    await r.deleteEmbeddedDocuments("Item", [a.id]), s != null && s.id && await this._setHardpointOccupant(s.id, ""), this.render({ force: !0 });
  }
  async _onToggleAssetModuleActive(n, i) {
    var c, u, d, f, m, g, p, h, b, y;
    if ((c = n == null ? void 0 : n.preventDefault) == null || c.call(n), (u = n == null ? void 0 : n.stopPropagation) == null || u.call(n), !this.isEditable) return;
    const a = E(this, de, Qc).call(this, i, n);
    if (!a || (a.canonicalType ?? a.type) !== "assetModule") return;
    if ((d = a.system) != null && d.inactive) {
      (f = ui.notifications) == null || f.warn("Inactive modules cannot be activated.");
      return;
    }
    const r = this.getPersistentActor() ?? this.actor, s = mi(r), o = ((m = D.getSnapshot) == null ? void 0 : m.call(D, r, { token: s })) ?? null;
    if (o != null && o.hasCombatant) {
      const S = await D.spendResource(r, {
        token: s,
        resource: "fa",
        cost: 1,
        actionId: "assetModuleToggle",
        actionLabel: `Toggle ${a.name}`,
        actionCostLabel: "1 FA",
        actionCategory: "free"
      });
      if (!(S != null && S.ok)) {
        (g = ui.notifications) == null || g.warn((S == null ? void 0 : S.reason) ?? `Unable to toggle ${a.name}.`);
        return;
      }
    }
    await (((h = (p = r.items) == null ? void 0 : p.get) == null ? void 0 : h.call(p, a.id)) ?? a).update({
      "system.activation.active": !((y = (b = a.system) == null ? void 0 : b.activation) != null && y.active)
    }), this.render({ force: !0 });
  }
  async _onToggleInventoryAccordion(n, i) {
    var r, s, o, l, c, u, d, f, m, g;
    (r = n == null ? void 0 : n.preventDefault) == null || r.call(n), (s = n == null ? void 0 : n.stopPropagation) == null || s.call(n);
    const a = String(
      ((o = i == null ? void 0 : i.dataset) == null ? void 0 : o.accordionId) ?? ((u = (c = (l = i == null ? void 0 : i.closest) == null ? void 0 : l.call(i, "[data-accordion-id]")) == null ? void 0 : c.dataset) == null ? void 0 : u.accordionId) ?? ((g = (m = (f = (d = n == null ? void 0 : n.target) == null ? void 0 : d.closest) == null ? void 0 : f.call(d, "[data-accordion-id]")) == null ? void 0 : m.dataset) == null ? void 0 : g.accordionId) ?? ""
    ).trim();
    a && (X(this, ua).has(a) ? X(this, ua).delete(a) : X(this, ua).add(a), this.render({ force: !1 }));
  }
  async _onAddHardpoint(n, i) {
    var l, c, u, d, f, m;
    if ((l = n == null ? void 0 : n.preventDefault) == null || l.call(n), (c = n == null ? void 0 : n.stopPropagation) == null || c.call(n), !this.isEditable) return;
    const a = await this._persistStagedHardpointFields(), r = this.getPersistentActor() ?? this.actor, s = this._buildHardpointOptions(), o = NR(a, {
      id: (d = (u = foundry.utils).randomID) == null ? void 0 : d.call(u),
      type: ((f = s.types[0]) == null ? void 0 : f.value) ?? "energy",
      size: ((m = s.sizes[0]) == null ? void 0 : m.value) ?? "small",
      location: this._getDefaultHardpointLocation()
    }, {
      defaultLocation: this._getDefaultHardpointLocation(),
      idFactory: (g) => {
        var p, h;
        return ((h = (p = foundry.utils).randomID) == null ? void 0 : h.call(p)) ?? `hardpoint-${g + 1}`;
      }
    });
    await r.update({ "system.mwd.hardpoints": o }), this.render({ force: !0 });
  }
  async _onDeleteHardpoint(n, i) {
    var u, d, f, m, g, p, h, b, y, S;
    if ((u = n == null ? void 0 : n.preventDefault) == null || u.call(n), (d = n == null ? void 0 : n.stopPropagation) == null || d.call(n), !this.isEditable) return;
    const a = String(
      ((f = i == null ? void 0 : i.dataset) == null ? void 0 : f.hardpointId) ?? ((p = (g = (m = i == null ? void 0 : i.closest) == null ? void 0 : m.call(i, "[data-hardpoint-id]")) == null ? void 0 : g.dataset) == null ? void 0 : p.hardpointId) ?? ((S = (y = (b = (h = n == null ? void 0 : n.target) == null ? void 0 : h.closest) == null ? void 0 : b.call(h, "[data-hardpoint-id]")) == null ? void 0 : y.dataset) == null ? void 0 : S.hardpointId) ?? ""
    ).trim();
    if (!a) return;
    const r = await this._persistStagedHardpointFields(), s = this.getPersistentActor() ?? this.actor, { hardpoints: o, removed: l } = PR(r, a, {
      defaultLocation: this._getDefaultHardpointLocation()
    });
    if (!l) return;
    const c = String((l == null ? void 0 : l.itemId) ?? "").trim();
    c && await s.deleteEmbeddedDocuments("Item", [c]), await s.update({ "system.mwd.hardpoints": o }), await this._sanitizeMountedWeaponGroups(s), this.render({ force: !0 });
  }
  async _onCreateHardpointItem(n, i) {
    var d, f, m, g, p, h, b, y, S, A;
    if ((d = n == null ? void 0 : n.preventDefault) == null || d.call(n), (f = n == null ? void 0 : n.stopPropagation) == null || f.call(n), !this.isEditable) return;
    const a = String(
      ((m = i == null ? void 0 : i.dataset) == null ? void 0 : m.hardpointId) ?? ((h = (p = (g = i == null ? void 0 : i.closest) == null ? void 0 : g.call(i, "[data-hardpoint-id]")) == null ? void 0 : p.dataset) == null ? void 0 : h.hardpointId) ?? ""
    ).trim();
    if (!a) return;
    const r = await this._persistStagedHardpointFields(), s = this.getPersistentActor() ?? this.actor, o = r.find(
      (T) => String((T == null ? void 0 : T.id) ?? "").trim() === a
    );
    if (!o) return;
    const l = s.items.filter((T) => (T.canonicalType ?? T.type) === "mechWeapon").length, c = ["penetrating", "concussive", "energy"].includes(String((o == null ? void 0 : o.type) ?? "").trim()) ? String(o.type).trim() : "energy", u = await s.createEmbeddedDocuments("Item", [{
      name: `Mech Weapon ${l + 1}`,
      type: "mechWeapon",
      system: {
        size: String((o == null ? void 0 : o.size) ?? "").trim() || "small",
        damageType: c
      }
    }]);
    await this._setHardpointOccupant(a, ((b = u == null ? void 0 : u[0]) == null ? void 0 : b.id) ?? "", { hardpoints: r }), (A = (S = (y = u == null ? void 0 : u[0]) == null ? void 0 : y.sheet) == null ? void 0 : S.render) == null || A.call(S, !0), this.render({ force: !0 });
  }
  async _onAssignHardpointItem(n, i) {
    var l, c, u, d, f, m;
    if ((l = n == null ? void 0 : n.preventDefault) == null || l.call(n), (c = n == null ? void 0 : n.stopPropagation) == null || c.call(n), !this.isEditable) return;
    const a = String(
      ((u = i == null ? void 0 : i.dataset) == null ? void 0 : u.hardpointId) ?? ((m = (f = (d = i == null ? void 0 : i.closest) == null ? void 0 : d.call(i, "[data-hardpoint-id]")) == null ? void 0 : f.dataset) == null ? void 0 : m.hardpointId) ?? ""
    ).trim();
    if (!a) return;
    const r = await this._persistStagedHardpointFields();
    this.getPersistentActor() ?? this.actor;
    const s = r.find((g) => g.id === a);
    if (!s) return;
    const o = await this._promptHardpointAssignment(s);
    o && (await this._setHardpointOccupant(a, o, { hardpoints: r }), this.render({ force: !0 }));
  }
  async _onClearHardpointItem(n, i) {
    var s, o, l, c, u, d;
    if ((s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n), !this.isEditable) return;
    const a = String(
      ((l = i == null ? void 0 : i.dataset) == null ? void 0 : l.hardpointId) ?? ((d = (u = (c = i == null ? void 0 : i.closest) == null ? void 0 : c.call(i, "[data-hardpoint-id]")) == null ? void 0 : u.dataset) == null ? void 0 : d.hardpointId) ?? ""
    ).trim();
    if (!a) return;
    const r = await this._persistStagedHardpointFields();
    await this._deleteMountedHardpointItem(a), await this._setHardpointOccupant(a, "", { hardpoints: r }), this.render({ force: !0 });
  }
  _collectStagedHardpointFields() {
    var i;
    const n = (i = this._getRootElement) == null ? void 0 : i.call(this);
    return n instanceof HTMLElement ? Array.from(n.querySelectorAll("[data-hardpoint-config][data-hardpoint-id]")).map((a) => {
      var o;
      const r = String(((o = a.dataset) == null ? void 0 : o.hardpointId) ?? "").trim();
      if (!r) return null;
      const s = this._collectHardpointRowSettings(a);
      return s ? { id: r, ...s } : null;
    }).filter(Boolean) : [];
  }
  _collectHardpointRowSettings(n = null) {
    var a;
    if (!(n instanceof HTMLElement)) return null;
    const i = {};
    for (const r of n.querySelectorAll("[data-hardpoint-field]")) {
      if (!(r instanceof HTMLInputElement || r instanceof HTMLSelectElement || r instanceof HTMLTextAreaElement) || r.disabled) continue;
      const s = String(((a = r.dataset) == null ? void 0 : a.hardpointField) ?? "").trim();
      ["type", "size", "location"].includes(s) && (i[s] = r.value);
    }
    return Object.keys(i).length ? i : null;
  }
  async _persistStagedHardpointFields() {
    const n = this.getPersistentActor() ?? this.actor, i = Ec(n), a = this._getDefaultHardpointLocation(), r = Aa(i, { defaultLocation: a });
    if (!this.isEditable) return r;
    const s = this._collectStagedHardpointFields();
    if (!s.length) return r;
    const o = ER(i, s, { defaultLocation: a });
    return JSON.stringify(o) === JSON.stringify(r) ? r : (await n.update({ "system.mwd.hardpoints": o }), o);
  }
  async _sanitizeMountedWeaponGroups(n = this.getPersistentActor() ?? this.actor) {
    var o, l;
    const i = n ?? this.actor, a = bi(i).map((c) => String((c == null ? void 0 : c.itemId) ?? "").trim()).filter(Boolean), r = Vh(foundry.utils.deepClone((l = (o = i == null ? void 0 : i.system) == null ? void 0 : o.mwd) == null ? void 0 : l.weaponGroups)), s = LK(r, a);
    s.changed && await i.update({ "system.mwd.weaponGroups": s.groups });
  }
  _buildActiveCrits() {
    var r;
    const n = ((r = this.getPersistentActor) == null ? void 0 : r.call(this)) ?? this.actor, i = this._buildDegradationPanel(), a = new Map((i.locations ?? []).map((s) => [s.key, s]));
    return tr(n).map((s) => {
      var m;
      const o = Ya(s.remedyKey), l = $p(s), c = a.get(String(s.locationKey ?? "").trim()) ?? null, u = String(s.remedySkillKey ?? o.skillKey ?? "").trim(), d = ((m = Ft(u)) == null ? void 0 : m.label) ?? ce(u), f = xe(s.remedyBaseDn ?? o.baseDn, 0) + xe((c == null ? void 0 : c.conditionModifier) ?? 0, 0);
      return {
        id: s.id,
        label: s.label ?? ce(s.key),
        locationLabel: s.locationLabel ?? ce(s.locationKey),
        detail: fg([
          l.statusLabel ? `Status: ${l.statusLabel}` : "",
          l.scopeSummary,
          l.automationMode === "engine" ? "Automated" : "Reminder Only",
          s.escalationKey ? `Escalates: ${s.escalationKey}` : ""
        ]).join(" | "),
        effectSummary: l.effectText,
        remedyLabel: o.label,
        remedySummary: d ? `Reliability + ${d} vs DN ${f}${c ? ` (${c.conditionLabel})` : ""}` : "",
        remedyKey: o.key,
        remediable: o.remediable !== !1,
        machineActorUuid: (n == null ? void 0 : n.uuid) ?? ""
      };
    });
  }
  async _onToggleStatuses(n, i) {
    var s, o, l, c, u, d;
    if ((s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n), ((l = i == null ? void 0 : i.dataset) == null ? void 0 : l.actionDisabled) === "true")
      return (u = ui.notifications) == null || u.warn(((c = i == null ? void 0 : i.dataset) == null ? void 0 : c.actionReason) || "Statuses are not available right now."), !1;
    const a = this.getPersistentActor() ?? this.actor, r = this._resolveStatusToken(a);
    return r ? Yu({
      actor: a,
      token: r
    }) : ((d = ui.notifications) == null || d.warn("Statuses require a token for this actor on the current scene."), !1);
  }
  async _onMachineWeaponAttack(n, i) {
    var c, u, d, f, m, g, p;
    (c = n == null ? void 0 : n.preventDefault) == null || c.call(n), (u = n == null ? void 0 : n.stopPropagation) == null || u.call(n);
    const a = this.getPersistentActor() ?? this.actor, r = String(((d = i == null ? void 0 : i.dataset) == null ? void 0 : d.itemId) ?? "").trim(), s = r ? (m = (f = a.items) == null ? void 0 : f.get) == null ? void 0 : m.call(f, r) : null;
    if (!s)
      return (g = ui.notifications) == null || g.warn("That weapon is no longer available."), !1;
    const o = this._resolveStatusToken(a), l = await Ni(a, {
      kind: "attack",
      sourceType: "mechWeapon",
      sourceId: s.id,
      token: o,
      event: n
    });
    return l != null && l.ok ? !0 : ((l != null && l.userMessage || l != null && l.reason) && ((p = ui.notifications) == null || p.warn(l.userMessage ?? l.reason)), !1);
  }
  async _onMachineCritRemedy(n, i) {
    var s, o, l, c, u, d, f;
    (s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n);
    const a = this.getPersistentActor() ?? this.actor, r = await Ni(a, {
      kind: "repair",
      issueKind: ((l = i == null ? void 0 : i.dataset) == null ? void 0 : l.issueKind) ?? "crit",
      issueId: ((c = i == null ? void 0 : i.dataset) == null ? void 0 : c.critId) ?? "",
      critId: ((u = i == null ? void 0 : i.dataset) == null ? void 0 : u.critId) ?? "",
      remedyKey: ((d = i == null ? void 0 : i.dataset) == null ? void 0 : d.remedyKey) ?? "",
      event: n
    });
    return r != null && r.ok || (f = ui.notifications) == null || f.warn((r == null ? void 0 : r.userMessage) ?? (r == null ? void 0 : r.reason) ?? "Unable to launch that machine remedy."), !!(r != null && r.ok);
  }
  async _onEwAcquire(n, i) {
    var a, r;
    return (a = n == null ? void 0 : n.preventDefault) == null || a.call(n), (r = n == null ? void 0 : n.stopPropagation) == null || r.call(n), E(this, de, gg).call(this, "acquire", n, i);
  }
  async _onVehicleAttack(n, i) {
    var s, o, l;
    (s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n);
    const a = this.getPersistentActor() ?? this.actor, r = this._resolveStatusToken(a);
    try {
      const c = ng(a);
      if (!c.length) {
        (l = ui.notifications) == null || l.warn("No mounted ranged weapons available.");
        return;
      }
      const u = c.length === 1 ? c[0] : await E(this, de, Kv).call(this, c);
      if (!u) return;
      await HM(a, {
        weaponId: u.id,
        token: r
      });
    } catch (c) {
      console.error("MWD | Failed to launch vehicle mounted attack", c), Yt(c, "Unable to launch that vehicle attack.");
    }
  }
  async _onVehicleMeleeAttack(n, i) {
    var r, s, o;
    (r = n == null ? void 0 : n.preventDefault) == null || r.call(n), (s = n == null ? void 0 : n.stopPropagation) == null || s.call(n);
    const a = this.getPersistentActor() ?? this.actor;
    try {
      const l = fS(a);
      if (!l.length) {
        (o = ui.notifications) == null || o.warn(z.actor.vehicle.quickActions.errors.noMelee);
        return;
      }
      const c = l.length === 1 ? l[0] : await E(this, de, Hv).call(this, l);
      if (!c) return;
      await BM(a, { profile: c });
    } catch (l) {
      console.error("MWD | Failed to launch vehicle melee attack", l), Yt(l, "Unable to launch that vehicle melee attack.");
    }
  }
  async _onVehicleChargeAttack(n, i) {
    var r, s, o;
    (r = n == null ? void 0 : n.preventDefault) == null || r.call(n), (s = n == null ? void 0 : n.stopPropagation) == null || s.call(n);
    const a = this.getPersistentActor() ?? this.actor;
    try {
      const l = this._resolveStatusToken(a), c = Fh(a, { token: l });
      if (!c.filter((m) => !m.disabled).length) {
        (o = ui.notifications) == null || o.warn("No charge modes available. Move with Reposition or Redline before charging.");
        return;
      }
      const d = await E(this, de, Fv).call(this, c);
      if (!d) return;
      let f = "prone";
      if (d === "control" && (f = await E(this, de, Uv).call(this), !f))
        return;
      await Kh(a, { mode: d, controlIntent: f, token: l });
    } catch (l) {
      console.error("MWD | Failed to launch vehicle charge attack", l), Yt(l, "Unable to launch that charge attack.");
    }
  }
  async _onVehicleMovement(n, i) {
    var d, f, m;
    (d = n == null ? void 0 : n.preventDefault) == null || d.call(n), (f = n == null ? void 0 : n.stopPropagation) == null || f.call(n);
    const a = this.getPersistentActor() ?? this.actor, r = ag(a), s = r.filter((g) => !g.disabled);
    if (!s.length) {
      (m = ui.notifications) == null || m.warn("No vehicle movement actions are currently available.");
      return;
    }
    const o = s[0], l = `<form class="mwd-quick-select">${r.map((g) => `
      <label class="quick-select-option${g.disabled ? " is-disabled" : ""}" title="${foundry.utils.escapeHTML(g.reason || g.hint || "")}">
        <input type="radio" name="vehicle-movement-action" value="${g.id}" ${g.id === o.id ? "checked" : ""} ${g.disabled ? "disabled" : ""}>
        <span>${foundry.utils.escapeHTML(g.label)}</span>
        <small>${foundry.utils.escapeHTML(`${g.cost} SA${g.strain > 0 ? ` | +${g.strain} Strain` : ""}${g.hint ? ` | ${g.hint}` : ""}`)}</small>
      </label>`).join("")}</form>`, c = await foundry.applications.api.DialogV2.wait({
      window: { title: "Vehicle Movement" },
      content: l,
      rejectClose: !1,
      buttons: [
        {
          action: "select",
          label: "Move",
          icon: "fa-solid fa-gauge-high",
          default: !0,
          callback: (g, p) => {
            var h, b;
            return ((b = (h = p.form) == null ? void 0 : h.elements["vehicle-movement-action"]) == null ? void 0 : b.value) ?? o.id;
          }
        }
      ]
    });
    if (!c) return;
    const u = s.find((g) => g.id === c) ?? o;
    try {
      await Ni(a, {
        kind: "movement",
        movementKind: u.id
      });
    } catch (g) {
      console.error("MWD | Failed to record vehicle movement", g), Yt(g, "Unable to record that vehicle movement.");
    }
  }
  async _onVehicleRoll(n, i) {
    var s, o, l;
    (s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n);
    const a = this.getPersistentActor() ?? this.actor, r = String(((l = i == null ? void 0 : i.dataset) == null ? void 0 : l.rollKind) ?? "").trim();
    try {
      if (r === "piloting") {
        const c = await this._promptMachinePilotCheck(a);
        c && await Ni(a, { kind: "piloting", ...c });
      } else if (r === "sensor") {
        const c = this._resolveStatusToken(a), u = await E(this, de, jv).call(this, a, { token: c });
        u && await Ni(a, {
          kind: "ew",
          action: u,
          token: c
        });
      } else if (r === "repair") {
        const c = await E(this, de, Gv).call(this, a);
        c && await Ni(a, {
          kind: "repair",
          issue: c
        });
      }
    } catch (c) {
      console.error("MWD | Failed to launch vehicle check", c), Yt(c, "Unable to launch that vehicle check.");
    }
  }
  async _promptMachinePilotCheck(n) {
    var m, g;
    const i = [
      { key: "handling", label: "Handling" },
      { key: "system", label: "System" },
      { key: "chassis", label: "Chassis" },
      { key: "reliability", label: "Reliability" }
    ], a = await Ut({ machineActor: n }), r = (a == null ? void 0 : a.actor) ?? null;
    if (!r)
      return (m = ui.notifications) == null || m.warn((a == null ? void 0 : a.reason) || "Machine checks require a linked pilot or operator."), null;
    const s = xK(r), o = ((g = s.flatMap((p) => p.skills).find((p) => p.code === "piloting")) == null ? void 0 : g.code) ?? "", l = i.map((p) => {
      var b, y, S;
      const h = Number(((S = (y = (b = n.system) == null ? void 0 : b.attributes) == null ? void 0 : y[p.key]) == null ? void 0 : S.value) ?? 0);
      return `<label class="mwd-pilot-check__attr">
        <input type="radio" name="machineAttr" value="${foundry.utils.escapeHTML(p.key)}" ${p.key === "handling" ? "checked" : ""}>
        <span class="mwd-pilot-check__attr-card">
          <span class="mwd-pilot-check__attr-name">${foundry.utils.escapeHTML(p.label)}</span>
          <span class="mwd-pilot-check__attr-value">${h}</span>
        </span>
      </label>`;
    }).join(""), c = s.map((p) => {
      const h = p.skills.map((b) => `<option value="${foundry.utils.escapeHTML(b.code)}" ${b.code === o ? "selected" : ""}>${foundry.utils.escapeHTML(b.label)} (${b.rating})</option>`).join("");
      return `<optgroup label="${foundry.utils.escapeHTML(p.label)}">${h}</optgroup>`;
    }).join(""), u = `<option value="" ${o ? "" : "selected"}>No Skill (0)</option>`, d = `<form class="mwd-quick-select mwd-skill-check-form mwd-pilot-check">
      <section class="mwd-pilot-check__section">
        <div class="mwd-pilot-check__heading">
          <span>Machine Attribute</span>
          <small>${foundry.utils.escapeHTML((n == null ? void 0 : n.name) ?? "Machine")}</small>
        </div>
        <div class="mwd-pilot-check__attr-grid">${l}</div>
      </section>
      <section class="mwd-pilot-check__section">
        <div class="mwd-pilot-check__heading">
          <span>Skill</span>
          <small>${foundry.utils.escapeHTML((r == null ? void 0 : r.name) ?? "Pilot")}</small>
        </div>
        <label class="mwd-pilot-check__select">
          <select name="skillKey">${u}${c}</select>
        </label>
      </section>
    </form>`, f = await foundry.applications.api.DialogV2.wait({
      window: { title: z.actor.vehicle.quickActions.pilotingCheck },
      content: d,
      rejectClose: !1,
      buttons: [
        {
          action: "roll",
          label: z.common.roll.button,
          icon: "fa-solid fa-dice",
          default: !0,
          callback: (p, h) => {
            var b, y, S, A;
            return {
              machineAttributeKey: ((y = (b = h.form) == null ? void 0 : b.elements.machineAttr) == null ? void 0 : y.value) ?? "handling",
              skillKey: ((A = (S = h.form) == null ? void 0 : S.elements.skillKey) == null ? void 0 : A.value) ?? o,
              operatorActorUuid: r.uuid ?? (a == null ? void 0 : a.uuid) ?? ""
            };
          }
        }
      ]
    });
    return f ? {
      ...f,
      skillKey: String(f.skillKey ?? "").trim() || "none"
    } : null;
  }
  async _onOpenStrainDialog(n, i) {
    var o;
    if ((o = n == null ? void 0 : n.preventDefault) == null || o.call(n), !this.isEditable) return;
    const a = this.getPersistentActor() ?? this.actor, r = ol(a), s = `
      <form class="mwd-heat-dialog" style="display:grid; gap:0.75rem;">
        <label style="display:grid; gap:0.25rem;">
          <span style="font-weight:700; text-transform:uppercase; letter-spacing:0.08em;">Current Strain</span>
          <input type="number" name="currentStrain" value="${r.value}" min="0" max="${r.max}" step="1" />
        </label>
        <label style="display:grid; gap:0.25rem;">
          <span style="font-weight:700; text-transform:uppercase; letter-spacing:0.08em;">Pending Strain</span>
          <input type="number" name="pendingStrain" value="${r.pendingGenerated}" min="0" step="1" />
        </label>
        <p style="margin:0; opacity:0.8;">Redline adds pending strain. Resolve it here when the vehicle's operational stress should take effect.</p>
      </form>
    `;
    await foundry.applications.api.DialogV2.wait({
      window: { title: `${a.name ?? "Vehicle"} Strain` },
      position: { width: 420 },
      content: s,
      buttons: [
        {
          action: "apply",
          label: "Apply",
          icon: "fa-solid fa-check",
          default: !0,
          callback: async (l, c) => {
            var g, p, h, b, y, S;
            const u = (h = (p = (g = c == null ? void 0 : c.form) == null ? void 0 : g.elements) == null ? void 0 : p.namedItem) == null ? void 0 : h.call(p, "currentStrain"), d = (S = (y = (b = c == null ? void 0 : c.form) == null ? void 0 : b.elements) == null ? void 0 : y.namedItem) == null ? void 0 : S.call(y, "pendingStrain"), f = Math.max(0, Number((u == null ? void 0 : u.value) ?? r.value) || 0), m = Math.max(0, Number((d == null ? void 0 : d.value) ?? r.pendingGenerated) || 0);
            return await a.update({
              "system.mwd.strain.value": f,
              "system.mwd.strain.pendingGenerated": m
            }), !0;
          }
        },
        {
          action: "resolve",
          label: "Resolve Pending",
          icon: "fa-solid fa-gauge-high",
          callback: async (l, c) => {
            var f, m, g, p, h, b;
            const u = (g = (m = (f = c == null ? void 0 : c.form) == null ? void 0 : f.elements) == null ? void 0 : m.namedItem) == null ? void 0 : g.call(m, "currentStrain"), d = (b = (h = (p = c == null ? void 0 : c.form) == null ? void 0 : p.elements) == null ? void 0 : h.namedItem) == null ? void 0 : b.call(h, "pendingStrain");
            return await a.update({
              "system.mwd.strain.value": Math.max(0, Number((u == null ? void 0 : u.value) ?? r.value) || 0),
              "system.mwd.strain.pendingGenerated": Math.max(0, Number((d == null ? void 0 : d.value) ?? r.pendingGenerated) || 0)
            }), await Ni(a, {
              kind: "resolvePendingStrain",
              reason: "strain dialog"
            }), !0;
          }
        },
        {
          action: "cancel",
          label: "Cancel",
          icon: "fa-solid fa-xmark",
          callback: () => !1
        }
      ],
      close: () => !1
    });
  }
  async _onEwTarget(n, i) {
    var a, r;
    return (a = n == null ? void 0 : n.preventDefault) == null || a.call(n), (r = n == null ? void 0 : n.stopPropagation) == null || r.call(n), E(this, de, gg).call(this, "targeting", n, i);
  }
  async _onMachineEwAction(n, i) {
    var s, o, l, c, u, d, f;
    (s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n);
    const a = ((l = this.getPersistentActor) == null ? void 0 : l.call(this)) ?? this.actor, r = String(((c = i == null ? void 0 : i.dataset) == null ? void 0 : c.actionId) ?? "").trim();
    if (!r) return !1;
    try {
      const m = await Ni(a, {
        kind: "ew",
        actionId: r,
        token: this._resolveStatusToken(a),
        targetTokenId: String(((u = i == null ? void 0 : i.dataset) == null ? void 0 : u.targetTokenId) ?? "").trim(),
        targetTokenUuid: String(((d = i == null ? void 0 : i.dataset) == null ? void 0 : d.targetTokenUuid) ?? "").trim(),
        event: n
      });
      return m != null && m.ok ? (E(this, de, Ri).call(this), !0) : ((f = ui.notifications) == null || f.warn((m == null ? void 0 : m.userMessage) ?? (m == null ? void 0 : m.reason) ?? "Unable to launch that EW action."), !1);
    } catch (m) {
      return console.error("MWD | Failed to launch machine EW action", m), Yt(m, "Unable to launch that EW action."), !1;
    }
  }
  async _onSignatureGoDark(n, i) {
    var s, o, l, c;
    (s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n);
    const a = ((l = this.getPersistentActor) == null ? void 0 : l.call(this)) ?? this.actor, r = await Ni(a, {
      kind: "action",
      actionId: "goDark",
      token: this._resolveStatusToken(a),
      event: n
    });
    return r != null && r.ok ? (this.render(!1), !0) : ((c = ui.notifications) == null || c.warn((r == null ? void 0 : r.userMessage) ?? (r == null ? void 0 : r.reason) ?? "Unable to go dark."), !1);
  }
  _resolveStatusToken(n = this.actor) {
    var i;
    return mi(n, {
      sheetToken: ((i = this.getSheetTokenDocument) == null ? void 0 : i.call(this)) ?? null
    });
  }
  _getDefaultHardpointLocation() {
    return this._getAvailableHardpointLocations()[0] ?? "torso";
  }
};
ua = new WeakMap(), Bs = new WeakMap(), xs = new WeakMap(), Mr = new WeakMap(), de = new WeakSet(), Bv = function() {
  var a, r;
  const n = (a = this._getRootElement) == null ? void 0 : a.call(this);
  if (!n) return;
  (r = X(this, Bs)) == null || r.abort();
  const i = new AbortController();
  De(this, Bs, i), n.addEventListener("dragstart", (s) => {
    var u, d, f, m;
    const o = (d = (u = s.target) == null ? void 0 : u.closest) == null ? void 0 : d.call(u, ".mwd-record[data-item-id][draggable='true']");
    if (!o || !n.contains(o)) return;
    const l = this._getOwnedItemFromTarget(o, s), c = ((f = l == null ? void 0 : l.toDragData) == null ? void 0 : f.call(l)) ?? (l != null && l.uuid ? {
      type: "Item",
      uuid: l.uuid
    } : null);
    if (!c) {
      s.preventDefault();
      return;
    }
    s.stopPropagation(), (m = s.dataTransfer) == null || m.setData("text/plain", JSON.stringify(c)), s.dataTransfer && (s.dataTransfer.effectAllowed = "copyMove");
  }, { signal: i.signal }), n.addEventListener("dragover", (s) => {
    var l, c;
    const o = (c = (l = s.target) == null ? void 0 : l.closest) == null ? void 0 : c.call(l, ".mwd-record[data-hardpoint-id]");
    !o || !n.contains(o) || (s.preventDefault(), s.dataTransfer && (s.dataTransfer.dropEffect = "move"));
  }, { signal: i.signal });
}, xv = function() {
  var a, r;
  const n = (a = this._getRootElement) == null ? void 0 : a.call(this);
  if (!n) return;
  (r = X(this, xs)) == null || r.abort();
  const i = new AbortController();
  De(this, xs, i), n.addEventListener("change", (s) => {
    var l, c;
    const o = (c = (l = s.target) == null ? void 0 : l.closest) == null ? void 0 : c.call(l, "[data-hardpoint-field]");
    !o || !n.contains(o) || E(this, de, zv).call(this, o);
  }, { signal: i.signal });
}, zv = async function(n) {
  var l, c;
  if (!this.isEditable) return;
  const i = (l = n == null ? void 0 : n.closest) == null ? void 0 : l.call(n, "[data-hardpoint-config][data-hardpoint-id]"), a = String(((c = i == null ? void 0 : i.dataset) == null ? void 0 : c.hardpointId) ?? "").trim();
  if (!i || !a) return;
  const r = this._collectHardpointRowSettings(i);
  if (!r) return;
  const s = this.getPersistentActor() ?? this.actor, o = DR(
    Ec(s),
    a,
    r,
    { defaultLocation: this._getDefaultHardpointLocation() }
  );
  o.changed && await s.update({ "system.mwd.hardpoints": o.hardpoints });
}, Fv = async function(n) {
  const i = n.filter((s) => !s.disabled);
  if (!i.length) return null;
  if (i.length === 1) return i[0].id;
  const a = i[0], r = `<form class="mwd-quick-select">${n.map((s, o) => `
      <label class="quick-select-option ${s.disabled ? "is-disabled" : ""}" title="${foundry.utils.escapeHTML(s.reason ?? "")}">
        <input type="radio" name="charge-mode" value="${foundry.utils.escapeHTML(s.id)}" ${s.id === a.id ? "checked" : ""} ${s.disabled ? "disabled" : ""}>
        <span>${foundry.utils.escapeHTML(s.label)}</span>
        ${s.disabled ? `<em style="opacity:0.6; font-size:0.85em;">${foundry.utils.escapeHTML(s.reason ?? "")}</em>` : ""}
      </label>`).join("")}</form>`;
  return foundry.applications.api.DialogV2.wait({
    window: { title: "Choose Charge Mode" },
    content: r,
    rejectClose: !1,
    buttons: [
      {
        action: "select",
        label: "Confirm",
        icon: "fa-solid fa-bolt",
        default: !0,
        callback: (s, o) => {
          var l, c;
          return ((c = (l = o.form) == null ? void 0 : l.elements["charge-mode"]) == null ? void 0 : c.value) ?? a.id;
        }
      }
    ]
  });
}, Uv = async function() {
  const n = Uh(), i = `<form class="mwd-quick-select">${n.map((a, r) => `
      <label class="quick-select-option">
        <input type="radio" name="control-intent" value="${foundry.utils.escapeHTML(a.id)}" ${r === 0 ? "checked" : ""}>
        <span>${foundry.utils.escapeHTML(a.label)}</span>
      </label>`).join("")}</form>`;
  return foundry.applications.api.DialogV2.wait({
    window: { title: "Control Charge — Intended Outcome" },
    content: i,
    rejectClose: !1,
    buttons: [
      {
        action: "select",
        label: "Confirm",
        icon: "fa-solid fa-dice",
        default: !0,
        callback: (a, r) => {
          var s, o, l;
          return ((o = (s = r.form) == null ? void 0 : s.elements["control-intent"]) == null ? void 0 : o.value) ?? ((l = n[0]) == null ? void 0 : l.id) ?? null;
        }
      }
    ]
  });
}, Kv = async function(n = []) {
  if (!n.length) return null;
  if (n.length === 1) return n[0];
  const i = n[0], a = `<form class="mwd-quick-select">${n.map((s) => `
      <label class="quick-select-option">
        <input type="radio" name="ranged-weapon" value="${foundry.utils.escapeHTML(String(s.id ?? ""))}" ${s.id === i.id ? "checked" : ""}>
        <span>${foundry.utils.escapeHTML(String(s.name ?? ""))}</span>
        ${s.hint ? `<small>${foundry.utils.escapeHTML(s.hint)}</small>` : ""}
      </label>`).join("")}</form>`, r = await foundry.applications.api.DialogV2.wait({
    window: { title: "Mounted Fire" },
    content: a,
    rejectClose: !1,
    buttons: [
      {
        action: "select",
        label: z.common.roll.button,
        icon: "fa-solid fa-dice",
        default: !0,
        callback: (s, o) => {
          var l, c;
          return ((c = (l = o.form) == null ? void 0 : l.elements["ranged-weapon"]) == null ? void 0 : c.value) ?? i.id;
        }
      }
    ]
  });
  return n.find((s) => s.id === r) ?? i;
}, Hv = async function(n = []) {
  if (!n.length) return null;
  if (n.length === 1) return n[0];
  const i = n[0], a = `<form class="mwd-quick-select">${n.map((s) => `
      <label class="quick-select-option">
        <input type="radio" name="melee-profile" value="${foundry.utils.escapeHTML(String(s.id ?? ""))}" ${s.id === i.id ? "checked" : ""}>
        <span>${foundry.utils.escapeHTML(String(s.name ?? ""))}</span>
      </label>`).join("")}</form>`, r = await foundry.applications.api.DialogV2.wait({
    window: { title: z.actor.vehicle.quickActions.selectMeleeProfile },
    content: a,
    rejectClose: !1,
    buttons: [
      {
        action: "select",
        label: z.common.roll.button,
        icon: "fa-solid fa-dice",
        default: !0,
        callback: (s, o) => {
          var l, c;
          return ((c = (l = o.form) == null ? void 0 : l.elements["melee-profile"]) == null ? void 0 : c.value) ?? i.id;
        }
      }
    ]
  });
  return n.find((s) => s.id === r) ?? i;
}, jv = async function(n, { token: i = null } = {}) {
  var c;
  const a = Gr(n, { token: i, includeDisabled: !0 }), r = a.filter((u) => !u.disabled);
  if (!r.length)
    return (c = ui.notifications) == null || c.warn(z.actor.vehicle.quickActions.errors.noSensorSweep), null;
  const s = r[0], o = `<form class="mwd-quick-select">${a.map((u) => `
      <label class="quick-select-option${u.disabled ? " is-disabled" : ""}" title="${foundry.utils.escapeHTML(String(u.reason ?? ""))}">
        <input type="radio" name="ew-action" value="${foundry.utils.escapeHTML(String(u.id ?? ""))}" ${u.id === s.id ? "checked" : ""} ${u.disabled ? "disabled" : ""}>
        <span>${foundry.utils.escapeHTML(String(u.label ?? ""))}</span>
        <small>${foundry.utils.escapeHTML(String(u.disabled ? u.reason : u.hint ?? ""))}</small>
      </label>`).join("")}</form>`, l = await foundry.applications.api.DialogV2.wait({
    window: { title: "Electronic Warfare" },
    content: o,
    rejectClose: !1,
    buttons: [
      {
        action: "select",
        label: z.common.roll.button,
        icon: "fa-solid fa-dice",
        default: !0,
        callback: (u, d) => {
          var f, m;
          return ((m = (f = d.form) == null ? void 0 : f.elements["ew-action"]) == null ? void 0 : m.value) ?? s.id;
        }
      }
    ]
  });
  return r.find((u) => u.id === l) ?? s;
}, Gv = async function(n) {
  var o;
  const i = Dd(n);
  if (!i.length)
    return (o = ui.notifications) == null || o.warn("No active criticals or repairable statuses are available."), null;
  if (i.length === 1) return i[0];
  const a = i[0], r = `<form class="mwd-quick-select">${i.map((l) => `
      <label class="quick-select-option">
        <input type="radio" name="repair-issue" value="${foundry.utils.escapeHTML(`${l.issueKind}:${l.issueId}`)}" ${l.issueKind === a.issueKind && l.issueId === a.issueId ? "checked" : ""}>
        <span>${foundry.utils.escapeHTML(String(l.label ?? ""))}</span>
        <small>${foundry.utils.escapeHTML(`${l.remedyLabel ?? ""} | ${l.remedySummary || `DN ${l.totalDn}`}`)}</small>
      </label>`).join("")}</form>`, s = await foundry.applications.api.DialogV2.wait({
    window: { title: z.actor.vehicle.quickActions.emergencyRepair },
    content: r,
    rejectClose: !1,
    buttons: [
      {
        action: "select",
        label: z.common.roll.button,
        icon: "fa-solid fa-dice",
        default: !0,
        callback: (l, c) => {
          var u, d;
          return ((d = (u = c.form) == null ? void 0 : u.elements["repair-issue"]) == null ? void 0 : d.value) ?? `${a.issueKind}:${a.issueId}`;
        }
      }
    ]
  });
  return i.find((l) => `${l.issueKind}:${l.issueId}` === s) ?? a;
}, Qc = function(n, i) {
  var r, s, o, l, c, u, d, f;
  const a = String(
    ((r = n == null ? void 0 : n.dataset) == null ? void 0 : r.itemId) ?? ((l = (o = (s = n == null ? void 0 : n.closest) == null ? void 0 : s.call(n, "[data-item-id]")) == null ? void 0 : o.dataset) == null ? void 0 : l.itemId) ?? ((f = (d = (u = (c = i == null ? void 0 : i.target) == null ? void 0 : c.closest) == null ? void 0 : u.call(c, "[data-item-id]")) == null ? void 0 : d.dataset) == null ? void 0 : f.itemId) ?? ""
  ).trim();
  return a ? this.actor.items.get(a) ?? null : null;
}, gg = async function(n, i, a) {
  var f, m, g, p, h, b;
  const r = ((f = this.getPersistentActor) == null ? void 0 : f.call(this)) ?? this.actor, s = this._resolveStatusToken(r), o = Ml({ actor: r, token: s }), l = String(((m = a == null ? void 0 : a.dataset) == null ? void 0 : m.targetTokenUuid) ?? "").trim(), c = String(((g = a == null ? void 0 : a.dataset) == null ? void 0 : g.targetTokenId) ?? "").trim(), u = l || c ? (o.rows ?? []).find(
    (y) => l && (y == null ? void 0 : y.targetTokenUuid) === l || c && (y == null ? void 0 : y.targetTokenId) === c
  ) ?? null : jh(o, n);
  if (!u) {
    const y = n === "targeting" ? "generate targeting data" : "acquire";
    return (p = ui.notifications) == null || p.warn(`No targeted token is ready to ${y}.`), !1;
  }
  if (!(n === "targeting" ? u.canTarget : u.canAcquire))
    return (h = ui.notifications) == null || h.warn(n === "targeting" ? "That target is not ready for targeting data yet." : "That target cannot advance its detection state right now."), !1;
  try {
    const y = await Ni(r, {
      kind: "ew",
      intent: n,
      token: s,
      targetTokenId: u.targetTokenId,
      targetTokenUuid: u.targetTokenUuid,
      event: i
    });
    return y != null && y.ok ? (E(this, de, Ri).call(this), !0) : ((b = ui.notifications) == null || b.warn((y == null ? void 0 : y.userMessage) ?? (y == null ? void 0 : y.reason) ?? "Unable to launch that EW action."), !1);
  } catch (y) {
    return console.error(`MWD | Failed to launch EW ${n}`, y), Yt(y, `Unable to launch ${n === "targeting" ? "targeting" : "acquire"} roll.`), !1;
  }
}, qv = function() {
  X(this, Mr).length || De(this, Mr, [
    ["targetToken", Hooks.on("targetToken", (n) => {
      var i;
      (n == null ? void 0 : n.id) === ((i = game.user) == null ? void 0 : i.id) && E(this, de, Ri).call(this);
    })],
    ["updateToken", Hooks.on("updateToken", (n, i) => {
      E(this, de, Yv).call(this, i) && E(this, de, Vv).call(this, n) && (XO(n, i), E(this, de, Ri).call(this));
    })],
    ["updateCombatant", Hooks.on("updateCombatant", (n, i) => {
      E(this, de, Xc).call(this, n) && E(this, de, Jv).call(this, i) && E(this, de, Ri).call(this);
    })],
    ["createCombatant", Hooks.on("createCombatant", (n) => {
      E(this, de, Xc).call(this, n) && E(this, de, Ri).call(this);
    })],
    ["deleteCombatant", Hooks.on("deleteCombatant", (n) => {
      E(this, de, Xc).call(this, n) && E(this, de, Ri).call(this);
    })],
    ["updateCombat", Hooks.on("updateCombat", (n) => {
      E(this, de, pg).call(this, n) && E(this, de, Ri).call(this);
    })],
    ["deleteCombat", Hooks.on("deleteCombat", (n) => {
      E(this, de, pg).call(this, n) && E(this, de, Ri).call(this);
    })]
  ]);
}, Wv = function() {
  for (const [n, i] of X(this, Mr))
    Hooks.off(n, i);
  De(this, Mr, []);
}, Ri = function() {
  this.rendered && (this._captureScrollPosition(), this.render({ force: !1 }));
}, pg = function(n) {
  var i;
  return !!(n != null && n.id && n.id === ((i = game.combat) == null ? void 0 : i.id));
}, Vv = function(n) {
  var o, l, c, u;
  const i = String((n == null ? void 0 : n.id) ?? ((o = n == null ? void 0 : n.document) == null ? void 0 : o.id) ?? "").trim();
  if (!i) return !1;
  const a = this._resolveStatusToken(((l = this.getPersistentActor) == null ? void 0 : l.call(this)) ?? this.actor), r = String((a == null ? void 0 : a.id) ?? ((c = a == null ? void 0 : a.document) == null ? void 0 : c.id) ?? "").trim();
  return r && i === r ? !0 : new Set(
    Array.from(((u = game.user) == null ? void 0 : u.targets) ?? []).map((d) => {
      var f;
      return String((d == null ? void 0 : d.id) ?? ((f = d == null ? void 0 : d.document) == null ? void 0 : f.id) ?? "").trim();
    }).filter(Boolean)
  ).has(i);
}, Xc = function(n) {
  var o, l, c, u, d, f;
  if (!n) return !1;
  const i = this._resolveStatusToken(((o = this.getPersistentActor) == null ? void 0 : o.call(this)) ?? this.actor), a = String((i == null ? void 0 : i.id) ?? ((l = i == null ? void 0 : i.document) == null ? void 0 : l.id) ?? "").trim(), r = String((n == null ? void 0 : n.tokenId) ?? ((c = n == null ? void 0 : n.token) == null ? void 0 : c.id) ?? ((d = (u = n == null ? void 0 : n.token) == null ? void 0 : u.document) == null ? void 0 : d.id) ?? "").trim();
  return r ? a && r === a ? !0 : new Set(
    Array.from(((f = game.user) == null ? void 0 : f.targets) ?? []).map((m) => {
      var g;
      return String((m == null ? void 0 : m.id) ?? ((g = m == null ? void 0 : m.document) == null ? void 0 : g.id) ?? "").trim();
    }).filter(Boolean)
  ).has(r) : !1;
}, Jv = function(n) {
  return E(this, de, Ro).call(this, n, "flags.mwd.targeting") || E(this, de, Ro).call(this, n, "flags.mwd.ewState") || E(this, de, Ro).call(this, n, "flags.mwd.personalCombat") || E(this, de, Ro).call(this, n, "tokenId");
}, Ro = function(n, i) {
  if (foundry.utils.hasProperty(n, i)) return !0;
  const a = `${i}.`;
  return Object.keys(n ?? {}).some((r) => r === i || r.startsWith(a));
}, Yv = function(n) {
  return foundry.utils.hasProperty(n, "x") || foundry.utils.hasProperty(n, "y") || foundry.utils.hasProperty(n, "elevation");
}, F($e, "LAYOUT_ID", "vehicle"), F($e, "PARTS", {
  sheet: {
    get template() {
      return `${ht}/v2/actor/vehicle-sheet.hbs`;
    }
  }
}), F($e, "DEFAULT_OPTIONS", foundry.utils.mergeObject(vi($e, $e, "DEFAULT_OPTIONS"), {
  classes: ["vehicle-sheet", I, "actor-sheet-v2", "mwd-vehicle-sheet", "mwd-sheet"],
  window: { minWidth: 520, minHeight: 720, resizable: !0 },
  position: { width: 940, height: 900 },
  actions: {
    ...vi($e, $e, "DEFAULT_OPTIONS").actions,
    createOwnedItem: $e.prototype._onCreateOwnedItem,
    editOwnedItem: $e.prototype._onEditOwnedItem,
    deleteOwnedItem: $e.prototype._onDeleteOwnedItem,
    toggleInventoryAccordion: $e.prototype._onToggleInventoryAccordion,
    addHardpoint: $e.prototype._onAddHardpoint,
    deleteHardpoint: $e.prototype._onDeleteHardpoint,
    createHardpointItem: $e.prototype._onCreateHardpointItem,
    assignHardpointItem: $e.prototype._onAssignHardpointItem,
    clearHardpointItem: $e.prototype._onClearHardpointItem,
    machineWeaponAttack: $e.prototype._onMachineWeaponAttack,
    vehicleAttack: $e.prototype._onVehicleAttack,
    vehicleMeleeAttack: $e.prototype._onVehicleMeleeAttack,
    vehicleChargeAttack: $e.prototype._onVehicleChargeAttack,
    vehicleMovement: $e.prototype._onVehicleMovement,
    vehicleRoll: $e.prototype._onVehicleRoll,
    openStrainDialog: $e.prototype._onOpenStrainDialog,
    ewAcquire: $e.prototype._onEwAcquire,
    ewTarget: $e.prototype._onEwTarget,
    machineEwAction: $e.prototype._onMachineEwAction,
    signatureGoDark: $e.prototype._onSignatureGoDark,
    toggleStatuses: $e.prototype._onToggleStatuses,
    machineCritRemedy: $e.prototype._onMachineCritRemedy,
    toggleAssetModuleActive: $e.prototype._onToggleAssetModuleActive,
    assignPilot: $e.prototype._onAssignPilot,
    removePilot: $e.prototype._onRemovePilot,
    openPilot: $e.prototype._onOpenPilot
  }
}, { inplace: !1 }));
let Ru = $e;
const Qv = Object.freeze([
  Object.freeze({ code: "HT-01", tier: "D", trackLength: 8, thresholds: Object.freeze({ runningHot: 2, overheated: 4, shutdown: 6 }) }),
  Object.freeze({ code: "HT-02", tier: "C", trackLength: 10, thresholds: Object.freeze({ runningHot: 3, overheated: 5, shutdown: 7 }) }),
  Object.freeze({ code: "HT-03", tier: "B", trackLength: 12, thresholds: Object.freeze({ runningHot: 3, overheated: 6, shutdown: 8 }) }),
  Object.freeze({ code: "HT-04", tier: "A", trackLength: 12, thresholds: Object.freeze({ runningHot: 4, overheated: 7, shutdown: 9 }) }),
  Object.freeze({ code: "HT-05", tier: "AA", trackLength: 13, thresholds: Object.freeze({ runningHot: 5, overheated: 8, shutdown: 10 }) }),
  Object.freeze({ code: "HT-06", tier: "AAA", trackLength: 14, thresholds: Object.freeze({ runningHot: 5, overheated: 9, shutdown: 11 }) }),
  Object.freeze({ code: "HT-07", tier: "S", trackLength: 15, thresholds: Object.freeze({ runningHot: 6, overheated: 9, shutdown: 12 }) }),
  Object.freeze({ code: "HT-08", tier: "SS", trackLength: 16, thresholds: Object.freeze({ runningHot: 6, overheated: 10, shutdown: 13 }) }),
  Object.freeze({ code: "HT-09", tier: "SSS", trackLength: 18, thresholds: Object.freeze({ runningHot: 7, overheated: 11, shutdown: 15 }) }),
  Object.freeze({ code: "HT-10", tier: "X", trackLength: 20, thresholds: Object.freeze({ runningHot: 8, overheated: 12, shutdown: 16 }) }),
  Object.freeze({ code: "HT-11", tier: "XX", trackLength: 20, thresholds: Object.freeze({ runningHot: 9, overheated: 13, shutdown: 17 }) }),
  Object.freeze({ code: "HT-12", tier: "XXX", trackLength: 24, thresholds: Object.freeze({ runningHot: 10, overheated: 14, shutdown: 19 }) })
]);
function GK(t = "") {
  const e = String(t ?? "").trim();
  return e ? Qv.find((n) => n.code === e) ?? null : null;
}
const LS = {
  light: 4,
  medium: 5,
  heavy: 6,
  assault: 7
}, qK = {
  baseProfile: { name: "Unarmed", damage: "", notes: "" },
  maxWeapons: 0,
  allowedLocations: []
};
function $S() {
  var t, e, n;
  return ((t = ne == null ? void 0 : ne.mwd) == null ? void 0 : t.hardpointType) ?? ((n = (e = ne == null ? void 0 : ne.mwd) == null ? void 0 : e.hardpoint) == null ? void 0 : n.type) ?? {};
}
function _S() {
  var t, e, n;
  return ((t = ne == null ? void 0 : ne.mwd) == null ? void 0 : t.hardpointSize) ?? ((n = (e = ne == null ? void 0 : ne.mwd) == null ? void 0 : e.hardpoint) == null ? void 0 : n.size) ?? {};
}
class WK {
  constructor(e) {
    this.actor = e, this.mwd = e.system.mwd ?? {};
  }
  compute() {
    const e = this.mwd.weightClass ?? "medium", n = LS[e] ?? LS.medium, i = this._normalizeHardpoints(), a = this._normalizeWeaponGroups(), r = [], s = [], o = a.length;
    o > n && r.push(At(ne.mwd.loadout.errors.mountPointsExceeded, {
      used: o,
      total: n
    }));
    const l = this._getWeapons((m) => (m.system.weaponCategory ?? "ranged") !== "melee"), c = new Map(l.map((m) => [m.id, m])), u = /* @__PURE__ */ new Set(), d = i.map((m) => ({ ...m, occupiedBy: null, occupiedByName: void 0 }));
    for (const m of a)
      for (const g of m.weaponIds ?? []) {
        const p = c.get(g);
        if (!p) {
          s.push(At(ne.mwd.loadout.warnings.weaponMissing, { weapon: g }));
          continue;
        }
        const h = ei(p.system.damageType ?? "energy", "energy"), b = Sa(p.system.size ?? "small");
        if (u.has(g)) {
          r.push(At(ne.mwd.loadout.errors.weaponAlreadyGrouped, { weapon: p.name }));
          continue;
        }
        if (u.add(g), (p.system.weaponCategory ?? "ranged") === "melee")
          continue;
        const y = nl(this.actor, p.id), S = d.find((A) => A.id === (y == null ? void 0 : y.id));
        S ? wp(S, p) ? (S.occupiedBy = m.id, S.occupiedByName = m.name) : r.push(At(ne.mwd.loadout.errors.hardpointUnavailable, {
          weapon: p.name,
          type: $S()[h] ?? h,
          size: _S()[b] ?? b
        })) : r.push(At(ne.mwd.loadout.errors.hardpointUnavailable, {
          weapon: p.name,
          type: $S()[h] ?? h,
          size: _S()[b] ?? b
        }));
      }
    const f = this._computeMeleeState(r);
    return {
      mountPoints: {
        total: n,
        used: o,
        remaining: Math.max(0, n - o)
      },
      weightClass: e,
      hardpoints: d,
      weaponGroups: a,
      errors: r,
      warnings: s,
      meleeProfiles: f.profiles,
      meleeLimit: f.limit
    };
  }
  _normalizeWeaponGroups() {
    return this._toCollection(this.mwd.weaponGroups).map((e, n) => ({
      id: e.id ?? `group-${n + 1}`,
      name: e.name || At(ne.common.newName, { type: ne.itemType.singular.weapon }),
      weaponIds: this._asArray(e.weaponIds)
    }));
  }
  _normalizeHardpoints() {
    return this._toCollection(this.mwd.hardpoints).map((e, n) => ({
      id: e.id ?? `hardpoint-${n + 1}`,
      type: to(e.type ?? "energy", "energy"),
      size: Sa(e.size ?? "small"),
      location: e.location ?? "arms",
      itemId: String(e.itemId ?? "").trim()
    }));
  }
  _computeMeleeState(e) {
    var o, l, c;
    const n = foundry.utils.mergeObject(foundry.utils.duplicate(qK), this.mwd.melee ?? {}), i = this._getWeapons((u) => (u.system.weaponCategory ?? "ranged") === "melee"), a = [], r = Number(n.maxWeapons ?? 0);
    i.length > r && e.push(At(ne.mwd.loadout.errors.meleeLimitExceeded, {
      equipped: i.length,
      limit: r
    }));
    const s = this._asArray(n.allowedLocations);
    return a.push({
      name: ((o = n.baseProfile) == null ? void 0 : o.name) || ne.mwd.melee.baseProfile,
      damage: ((l = n.baseProfile) == null ? void 0 : l.damage) ?? "",
      notes: ((c = n.baseProfile) == null ? void 0 : c.notes) ?? ""
    }), i.forEach((u) => {
      var f, m;
      const d = ((f = nl(this.actor, u.id)) == null ? void 0 : f.location) ?? "";
      s.length > 0 && d && !s.includes(d) && e.push(At(ne.mwd.loadout.errors.meleeLocationRestricted, {
        weapon: u.name,
        location: ne.mwd.meleeLocation[d] ?? d
      })), a.push({
        name: u.name,
        damage: u.getDamageCode(),
        notes: ((m = u.system.references) == null ? void 0 : m.description) ?? ""
      });
    }), { profiles: a, limit: r };
  }
  _getWeapons(e) {
    return nd(this.actor, { canonicalType: M.itemType.mechWeapon }).filter((n) => {
      var i;
      return (i = n.isActive) == null ? void 0 : i.call(n);
    }).filter(e);
  }
  _asArray(e) {
    return this._toCollection(e);
  }
  _toCollection(e) {
    return Array.isArray(e) ? e : e == null || e === "" ? [] : typeof e == "object" ? Object.values(e) : [e];
  }
}
function ut(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function Sc(t) {
  var n, i;
  const e = foundry.utils.deepClone((i = (n = t == null ? void 0 : t.system) == null ? void 0 : n.mwd) == null ? void 0 : i.weaponGroups);
  return Vh(e);
}
function VK(t = []) {
  return t.map((e) => String(e ?? "").trim()).filter(Boolean);
}
function Dm(t = []) {
  return t.map((e) => {
    const n = e ?? {};
    return {
      ...n,
      label: String(n.label ?? "").trim(),
      value: String(n.value ?? "").trim(),
      emphasis: n.emphasis ?? "",
      title: String(n.title ?? "").trim(),
      tone: String(n.tone ?? "").trim(),
      parts: Array.isArray(n.parts) ? n.parts.filter((i) => i && i.value !== void 0 && i.value !== null && String(i.value).trim() !== "").map((i) => ({
        label: String(i.label ?? "").trim(),
        value: String(i.value ?? "").trim(),
        tone: String(i.tone ?? "").trim(),
        title: String(i.title ?? "").trim()
      })) : []
    };
  }).filter((e) => e.value !== "" || e.parts.length).map((e) => ({
    ...e,
    hasParts: e.parts.length > 0
  }));
}
function BS(t = []) {
  return VK(t).map((e) => ({ label: e }));
}
function xS(t) {
  var n, i;
  const e = (i = (n = t == null ? void 0 : t.system) == null ? void 0 : n.quickActions) == null ? void 0 : i.movement;
  return Array.isArray(e) && e.length ? e : Lh(t);
}
function JK() {
  var t, e, n;
  return ((t = game.mwd) == null ? void 0 : t.machineActions) ?? ((n = (e = game.system) == null ? void 0 : e.mwd) == null ? void 0 : n.machineActions) ?? null;
}
async function Zi(t, e = {}) {
  const n = JK();
  if (!(n != null && n.execute)) throw new Error("MWD machine action service not initialized.");
  return n.execute(t, e);
}
function zS(t = []) {
  return t.filter((e) => e && e.value !== void 0 && e.value !== null && String(e.value).trim() !== "").map((e) => ({
    label: String(e.label ?? "").trim(),
    value: String(e.value ?? "").trim()
  }));
}
function Xv(t = "") {
  const e = String(t ?? "").trim().toLowerCase();
  return e ? e.charAt(0).toUpperCase() + e.slice(1) : "";
}
function YK(t = {}) {
  return ["close", "near", "far", "extreme"].map((e) => `${Xv(e)} ${ut(t == null ? void 0 : t[e], 0)}`).join(" | ");
}
function Ac(t = "") {
  var n, i;
  const e = ((i = (n = z == null ? void 0 : z.actor) == null ? void 0 : n.vehicle) == null ? void 0 : i.quickActions) ?? {};
  return String((e == null ? void 0 : e[t]) ?? ce(t)).trim() || ce(t);
}
var ze, Zv, eC, Zc, tC, nC, iC, aC, rC, sC, oC, lC, cC, uC, bg, dC;
const qt = class qt extends Ru {
  constructor() {
    super(...arguments);
    Fe(this, ze);
  }
  async _prepareContext(n) {
    var o, l;
    const i = await super._prepareContext(n), a = this.getPersistentActor() ?? this.actor, r = ((o = this.getSheetTokenDocument) == null ? void 0 : o.call(this)) ?? this._resolveStatusToken(a), s = ((l = D.getSnapshot) == null ? void 0 : l.call(D, a, { token: r })) ?? null;
    return i.combatAwarenessPreview = Ed(a, {
      sourceToken: r
    }), i.battlemechSheet = {
      heat: this._buildHeatModel(),
      fireMode: this._buildFireModeSection(a, s),
      quickActions: this._buildQuickActions(),
      weaponGroupSummary: this._buildWeaponGroupSummary(),
      weaponGroups: this._buildWeaponGroups(),
      hardpoints: this._buildHardpoints(),
      chassisFields: this._buildChassisFields()
    }, i;
  }
  _onRender(n, i) {
    super._onRender(n, i);
    const a = this._getRootElement();
    a && (a.querySelectorAll(".mwd-battlemech-groups__name-input").forEach((r) => {
      r.addEventListener("change", (s) => E(this, ze, Zv).call(this, s));
    }), a.querySelectorAll("[data-heat-profile-select]").forEach((r) => {
      r.addEventListener("change", (s) => E(this, ze, eC).call(this, s));
    }));
  }
  _buildChassisFields() {
    var r, s, o, l;
    const n = ut((s = (r = this.actor.system) == null ? void 0 : r.mwd) == null ? void 0 : s.tonnage, 0), i = ((l = (o = this.actor.system) == null ? void 0 : o.mwd) == null ? void 0 : l.weightClass) ?? "medium", a = z.mwd.weightClass;
    return [
      {
        label: "Tonnage",
        path: "system.mwd.tonnage",
        isNumber: !0,
        value: n,
        displayValue: String(n)
      },
      {
        label: a.label,
        path: "system.mwd.weightClass",
        isSelect: !0,
        value: i,
        displayValue: a[i] ?? ce(i),
        options: Object.entries(a).filter(([c]) => c !== "label").map(([c, u]) => ({ value: c, label: u, selected: i === c }))
      }
    ];
  }
  _buildConditionMonitors() {
    var a, r, s, o;
    const n = ((r = (a = this.actor.system) == null ? void 0 : a.monitors) == null ? void 0 : r.structure) ?? {}, i = ((o = (s = this.actor.system) == null ? void 0 : s.monitors) == null ? void 0 : o.armor) ?? {};
    return [
      Iu({ id: "structure", label: "Structure", kind: "structure", monitor: n, editable: this.isEditable }),
      Iu({ id: "armor", label: "Armor", kind: "armor", monitor: i, editable: this.isEditable })
    ];
  }
  _buildSummaryStats() {
    var c, u, d, f, m, g, p, h, b, y, S, A;
    const n = ((u = (c = this.actor.system) == null ? void 0 : c.monitors) == null ? void 0 : u.armor) ?? {}, i = ((f = (d = this.actor.system) == null ? void 0 : d.monitors) == null ? void 0 : f.structure) ?? {}, a = ma(this.actor), r = a.status.toUpperCase(), s = vF({ armor: n, structure: i }), o = DM(((g = (m = this.actor.system) == null ? void 0 : m.mwd) == null ? void 0 : g.crits) ?? []), l = nT({
      actorType: this.actor.type,
      movement: (p = this.actor.system) == null ? void 0 : p.movement,
      legacyMoves: (h = this.actor.system) == null ? void 0 : h.moves,
      jumpProfile: Yl(this.actor),
      movementEffects: Rr(this.actor)
    });
    return Dm([
      { label: "Weight", value: ce(((y = (b = this.actor.system) == null ? void 0 : b.mwd) == null ? void 0 : y.weightClass) ?? "medium"), emphasis: "strong" },
      { label: "Move", parts: l },
      { label: "Tonnage", value: ut((A = (S = this.actor.system) == null ? void 0 : S.mwd) == null ? void 0 : A.tonnage, 0) },
      { label: "Integrity", parts: s.parts, title: s.title },
      { label: "Heat", value: `${a.current} ${r}`, title: a.status },
      { label: "Pending Heat", value: String(a.pendingGenerated), tone: a.pendingGenerated > 0 ? "orange" : "" },
      { label: "Status", value: o.value, title: o.title, tone: o.count > 0 ? "red" : "" }
    ]);
  }
  _buildSummaryActions() {
    return [];
  }
  _buildAlerts() {
    var i, a;
    const n = ((a = (i = this.actor.system) == null ? void 0 : i.mwd) == null ? void 0 : a.loadout) ?? {};
    return [
      ...Array.isArray(n.errors) ? n.errors.map((r) => ({ tone: "danger", text: r })) : [],
      ...Array.isArray(n.warnings) ? n.warnings.map((r) => ({ tone: "warning", text: r })) : []
    ];
  }
  _buildVehicleSections() {
    const n = ot.classify(this.actor.items);
    return {
      slots: this._buildHardpointSlotSection(),
      equipment: this._buildRecordSection({
        sectionId: "equipment",
        itemType: "mechEquipment",
        addLabel: "Add Equipment",
        emptyLabel: "No BattleMech equipment installed.",
        items: n.mechEquipment ?? []
      }),
      modules: this._buildRecordSection({
        sectionId: "modules",
        itemType: "assetModule",
        addLabel: "Add Module",
        emptyLabel: "No asset modules installed.",
        items: n.assetModule ?? []
      }),
      gear: this._buildRecordSection({
        sectionId: "gear",
        itemType: "gear",
        addLabel: "Add Gear",
        emptyLabel: "No stored gear or consumables.",
        items: [
          ...n.gear ?? [],
          ...n.consumable ?? []
        ]
      })
    };
  }
  _buildHeatModel() {
    var r, s, o;
    const n = ma(this.actor), i = n.thresholds ?? {}, a = String(((o = (s = (r = this.actor.system) == null ? void 0 : r.mwd) == null ? void 0 : s.heat) == null ? void 0 : o.profileCode) ?? "").trim();
    return {
      label: "Heat",
      current: n.current,
      trackLength: n.trackLength,
      displayMax: n.displayMax,
      dissipation: n.dissipation,
      effectiveDissipation: n.effectiveDissipation,
      coolingImpaired: n.coolingImpaired,
      pendingGenerated: n.pendingGenerated,
      editable: !!this.isEditable,
      profileCode: a,
      profileOptions: Qv.map((l) => ({
        value: l.code,
        label: l.tier,
        selected: l.code === a,
        trackLength: l.trackLength,
        thresholds: l.thresholds
      })),
      status: n.status,
      thresholds: {
        runningHot: ut(i.runningHot, 0),
        overheated: ut(i.overheated, 0),
        shutdown: ut(i.shutdown, 0),
        hot: ut(i.hot, 0),
        overheat: ut(i.overheat, 0),
        danger: ut(i.danger, 0)
      },
      penalties: {
        movementPenalty: n.penalties.movementPenalty,
        rangedDicePenalty: n.penalties.rangedDicePenalty,
        dangerLevel: n.penalties.dangerLevel
      },
      dangerChecks: n.dangerChecks,
      dangerActions: n.inDanger && n.dangerChecks ? [
        {
          label: "Shutdown Check",
          hint: `Roll ${ut(n.dangerChecks.shutdownPool, 0)}d6 vs DN ${ut(n.dangerChecks.shutdownDN, 0)}`,
          dataset: {
            checkKind: "shutdown",
            dn: ut(n.dangerChecks.shutdownDN, 0)
          }
        },
        {
          label: "Explosion Check",
          hint: `Roll ${ut(n.dangerChecks.explosionPool, 0)}d6 vs DN ${ut(n.dangerChecks.explosionDN, 0)}`,
          dataset: {
            checkKind: "explosion",
            dn: ut(n.dangerChecks.explosionDN, 0)
          }
        }
      ] : [],
      volatile: n.volatile,
      inDanger: n.inDanger,
      segments: Array.from({ length: n.displayMax }, (l, c) => {
        const u = c + 1, d = ut(i.runningHot, 0), f = ut(i.overheated, 0), m = ut(i.shutdown, 0), g = u >= m ? "danger" : u >= f ? "overheat" : u >= d ? "hot" : "safe";
        return {
          value: u,
          filled: u <= n.current,
          current: u === n.current,
          band: g,
          bandLabel: ce(g)
        };
      })
    };
  }
  _buildFireModeSection(n, i = null) {
    var l, c, u, d;
    const a = String(((c = (l = n.system) == null ? void 0 : l.mwd) == null ? void 0 : c.fireMode) ?? Jt).trim() || Jt, r = !!(i != null && i.isCurrentTurn && ((d = (u = i.state) == null ? void 0 : u.actionState) != null && d.fireModeChangedThisActivation));
    let s = null;
    const o = Object.values(ul).map((f) => {
      const m = f.id === a;
      return m && (s = f), {
        ...f,
        isActive: m,
        disabled: !f.implemented || r && !m
      };
    });
    return {
      activeModeId: a,
      activeMode: s ?? ul[Jt],
      modes: o
    };
  }
  async _onOpenHeatDialog(n, i) {
    var r;
    if ((r = n == null ? void 0 : n.preventDefault) == null || r.call(n), !this.isEditable) return;
    const a = this.getPersistentActor() ?? this.actor;
    await E(this, ze, uC).call(this, a);
  }
  _buildQuickActions() {
    var c, u;
    const n = this.getPersistentActor() ?? this.actor, a = this._getPreparedRangedWeaponGroups(n).filter((d) => d.isAttackLegal && d.isAvailableThisActivation), r = n.type === "battlemech" || Array.isArray((c = n.system) == null ? void 0 : c.meleeProfiles) && n.system.meleeProfiles.length > 0, o = xS(n).filter((d) => !d.disabled), l = Gr(n, {
      token: ((u = this.getSheetTokenDocument) == null ? void 0 : u.call(this)) ?? this._resolveStatusToken(n)
    });
    return [
      {
        label: "Movement",
        hint: o.length ? "Move action" : "No moves",
        handler: "mechMovement",
        disabled: o.length === 0,
        dataset: {}
      },
      {
        label: Ac("rangedAttack"),
        hint: a.length > 0 ? "Ranged attack" : "No weapons",
        handler: "mechAttack",
        disabled: a.length === 0,
        dataset: { attackKind: "ranged" }
      },
      {
        label: Ac("meleeAttack"),
        hint: r ? "Melee attack" : "No profiles",
        handler: "mechAttack",
        disabled: !r,
        dataset: { attackKind: "melee" }
      },
      {
        label: "Maneuvers",
        hint: "Charge attack",
        handler: "mechAttack",
        disabled: !1,
        dataset: { attackKind: "charge" }
      },
      {
        label: Ac("pilotingCheck"),
        hint: "Piloting check",
        handler: "mechRoll",
        disabled: !1,
        dataset: { rollKind: "piloting" }
      },
      {
        label: "Electronic Warfare",
        hint: l.length ? "EW action" : "No EW actions",
        handler: "mechRoll",
        disabled: l.length === 0,
        dataset: { rollKind: "sensor" }
      },
      {
        label: Ac("emergencyRepair"),
        hint: "Repair action",
        handler: "mechRoll",
        disabled: !1,
        dataset: { rollKind: "repair" }
      }
    ];
  }
  _buildWeaponGroupSummary() {
    var o, l, c;
    const n = this.getPersistentActor() ?? this.actor, i = new WK(n).compute(), a = (i == null ? void 0 : i.mountPoints) ?? ((c = (l = (o = n.system) == null ? void 0 : o.mwd) == null ? void 0 : l.loadout) == null ? void 0 : c.mountPoints) ?? {}, r = ut(a.total, 0), s = ut(a.used, 0);
    return {
      total: r,
      used: s,
      remaining: Math.max(0, ut(a.remaining, r - s))
    };
  }
  _buildLoadedHardpointChoices(n) {
    var s, o, l;
    const i = ((s = z == null ? void 0 : z.mwd) == null ? void 0 : s.hardpointType) ?? {}, a = ((o = z == null ? void 0 : z.mwd) == null ? void 0 : o.hardpointSize) ?? {}, r = ((l = z == null ? void 0 : z.mwd) == null ? void 0 : l.hardpointLocation) ?? {};
    return bi(n).map((c, u) => {
      var b, y;
      const d = String((c == null ? void 0 : c.itemId) ?? "").trim();
      if (!d) return null;
      const f = ((y = (b = n.items) == null ? void 0 : b.get) == null ? void 0 : y.call(b, d)) ?? null;
      if (!f) return null;
      const m = r[c.location] ?? ce(c.location), g = i[c.type] ?? ce(c.type), p = a[c.size] ?? ce(c.size), h = `${m} ${g} ${p}`.trim();
      return {
        hardpointId: String((c == null ? void 0 : c.id) ?? `hardpoint-${u + 1}`).trim(),
        itemId: d,
        itemName: String((f == null ? void 0 : f.name) ?? "Mounted Weapon").trim() || "Mounted Weapon",
        hardpointName: h,
        chipLabel: `${(f == null ? void 0 : f.name) ?? "Mounted Weapon"} | ${h}`,
        detailLabel: `${h}: ${(f == null ? void 0 : f.name) ?? "Mounted Weapon"}`
      };
    }).filter(Boolean);
  }
  _buildWeaponGroups() {
    var l, c;
    const n = this.getPersistentActor() ?? this.actor, i = this._getPreparedRangedWeaponGroups(n), a = String(((c = (l = n.system) == null ? void 0 : l.mwd) == null ? void 0 : c.fireMode) ?? Jt).trim() || Jt, r = this._buildLoadedHardpointChoices(n), s = new Map(i.map((u) => [u.id, u.name])), o = /* @__PURE__ */ new Map();
    for (const u of i)
      for (const d of Array.from(u.weaponIds ?? [])) {
        const f = String(d ?? "").trim();
        !f || o.has(f) || o.set(f, u.id);
      }
    return i.map((u) => {
      var g, p, h, b, y, S, A, T, C;
      const d = Array.from(u.weaponIds ?? []).map((k) => String(k ?? "").trim()).filter(Boolean), f = r.filter((k) => d.includes(k.itemId)), m = r.map((k) => {
        const w = d.includes(k.itemId), v = o.get(k.itemId) ?? "", N = !!(v && v !== u.id);
        return {
          ...k,
          selected: w,
          assignedElsewhere: N,
          disabled: N,
          title: w ? `Remove ${k.itemName} from ${u.name}` : N ? `${k.itemName} is already bundled in ${s.get(v) ?? "another group"}` : `Add ${k.itemName} to ${u.name}`
        };
      });
      return {
        id: u.id,
        index: u.index,
        name: u.name,
        subtitle: f.length ? f.map((k) => k.itemName).join(", ") : (u.memberWeapons ?? []).map((k) => k.name).join(", "),
        summaryStats: Dm([
          { label: "Weapons", value: Array.isArray(u.memberWeapons) ? u.memberWeapons.length : 0, emphasis: "strong" },
          { label: "Damage", value: ut((g = u.attackSummary) == null ? void 0 : g.damage, 0) },
          { label: "AP", value: ut((p = u.attackSummary) == null ? void 0 : p.ap, 0) },
          { label: "Heat", value: ut((h = u.attackSummary) == null ? void 0 : h.heat, 0) }
        ]),
        detailTags: BS([
          ((b = u.attackSummary) == null ? void 0 : b.damageTypeLabel) ?? "",
          u.isAttackLegal ? u.isAvailableThisActivation ? "Ready" : "Used" : "Blocked"
        ]),
        rapidFire: u.rapidFire,
        detailRows: zS([
          { label: "Weapon Names", value: (u.memberWeapons ?? []).map((k) => k.name).join(", ") },
          { label: "Bundled Hardpoints", value: f.map((k) => k.detailLabel).join(" | ") },
          { label: "Range Cap", value: Xv(((y = u.attackSummary) == null ? void 0 : y.rangeCap) ?? "") },
          { label: "Attack Ratings", value: YK(((S = u.attackSummary) == null ? void 0 : S.attackRatings) ?? {}) },
          { label: "Missing IDs", value: (u.missingWeaponIds ?? []).join(", ") },
          { label: "Warnings", value: (u.compatibilityWarnings ?? []).join(" | ") },
          { label: "Rapid Fire", value: ((A = u.rapidFire) == null ? void 0 : A.reason) ?? "" },
          { label: "Status", value: u.disableReason || (u.isAvailableThisActivation ? "Ready to fire" : "Already fired this activation") }
        ]),
        bundleChoices: m,
        bundleHelp: r.length ? "Bundle mounted slot weapons here. Only attached slot weapons appear." : "Mount weapons into loadout slots first, then bundle the mounted slot weapons here.",
        action: {
          label: u.isAttackLegal && u.isAvailableThisActivation ? "Attack Group" : "Unavailable",
          disabled: !(u.isAttackLegal && u.isAvailableThisActivation),
          title: a === "rapidFire" && !((T = u.rapidFire) != null && T.eligible) ? ((C = u.rapidFire) == null ? void 0 : C.reason) || "Not rapid-fire capable." : u.disableReason || "Attack Group",
          dataset: {
            attackKind: "group",
            groupId: u.id
          }
        }
      };
    });
  }
  _buildHardpoints() {
    var s, o, l, c, u;
    const n = ((o = (s = this.actor.system) == null ? void 0 : s.mwd) == null ? void 0 : o.loadout) ?? {}, i = ((l = z == null ? void 0 : z.mwd) == null ? void 0 : l.hardpointType) ?? {}, a = ((c = z == null ? void 0 : z.mwd) == null ? void 0 : c.hardpointSize) ?? {}, r = ((u = z == null ? void 0 : z.mwd) == null ? void 0 : u.hardpointLocation) ?? {};
    return Array.from(n.hardpoints ?? []).map((d) => ({
      id: d.id,
      name: `${i[d.type] ?? ce(d.type)} ${a[d.size] ?? ce(d.size)}`,
      subtitle: r[d.location] ?? ce(d.location),
      summaryStats: Dm([
        { label: "Type", value: i[d.type] ?? ce(d.type), emphasis: "strong" },
        { label: "Size", value: a[d.size] ?? ce(d.size) }
      ]),
      detailTags: BS([
        d.occupiedByName ? `Occupied by ${d.occupiedByName}` : "Open"
      ]),
      detailRows: zS([
        { label: "Location", value: r[d.location] ?? ce(d.location) },
        { label: "Assigned Group", value: d.occupiedByName ?? "Unassigned" }
      ])
    }));
  }
  async _onMechAttack(n, i) {
    var o, l, c, u, d, f;
    (o = n == null ? void 0 : n.preventDefault) == null || o.call(n), (l = n == null ? void 0 : n.stopPropagation) == null || l.call(n);
    const a = this.getPersistentActor() ?? this.actor, r = String(((c = i == null ? void 0 : i.dataset) == null ? void 0 : c.attackKind) ?? "").trim(), s = String(((u = i == null ? void 0 : i.dataset) == null ? void 0 : u.groupId) ?? "").trim();
    try {
      if (r === "group" && s)
        await E(this, ze, Zc).call(this, a, s);
      else if (r === "ranged") {
        if ((String(((f = (d = a.system) == null ? void 0 : d.mwd) == null ? void 0 : f.fireMode) ?? Jt).trim() || Jt) === "alphaStrike") {
          await E(this, ze, Zc).call(this, a, "");
          return;
        }
        const g = this._getPreparedRangedWeaponGroups(a), p = await E(this, ze, sC).call(this, a, g);
        p != null && p.id && await E(this, ze, Zc).call(this, a, p.id);
      } else if (r === "melee")
        await E(this, ze, tC).call(this, a);
      else if (r === "charge")
        await E(this, ze, nC).call(this, a);
      else
        throw new Error(`MWD | Unrecognised attackKind: "${r}"`);
    } catch (m) {
      console.error("MWD | Failed to launch BattleMech attack", m), Yt(m, "Unable to launch that BattleMech attack.");
    }
  }
  async _onMechMovement(n, i) {
    var s, o;
    (s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n);
    const a = this.getPersistentActor() ?? this.actor, r = await E(this, ze, oC).call(this, a);
    if (r)
      try {
        await Zi(a, {
          kind: "movement",
          movementKind: r.id
        });
      } catch (l) {
        console.error("MWD | Failed to record BattleMech movement", l), Yt(l, "Unable to record that BattleMech movement.");
      }
  }
  async _onMechRoll(n, i) {
    var s, o, l;
    (s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n);
    const a = this.getPersistentActor() ?? this.actor, r = String(((l = i == null ? void 0 : i.dataset) == null ? void 0 : l.rollKind) ?? "").trim();
    try {
      if (r === "piloting") {
        const c = await this._promptMachinePilotCheck(a);
        c && await Zi(a, { kind: "piloting", ...c });
      } else if (r === "sensor") {
        const c = await E(this, ze, lC).call(this, a);
        c && await Zi(a, {
          kind: "ew",
          action: c
        });
      } else if (r === "repair") {
        const c = await E(this, ze, cC).call(this, a);
        c && await Zi(a, {
          kind: "repair",
          issue: c
        });
      }
    } catch (c) {
      console.error("MWD | Failed to launch BattleMech check", c), Yt(c, "Unable to launch that BattleMech check.");
    }
  }
  async _onHeatDangerCheck(n, i) {
    var o, l, c, u, d, f;
    (o = n == null ? void 0 : n.preventDefault) == null || o.call(n), (l = n == null ? void 0 : n.stopPropagation) == null || l.call(n);
    const a = this.getPersistentActor() ?? this.actor, r = String(((c = i == null ? void 0 : i.dataset) == null ? void 0 : c.checkKind) ?? "").trim();
    if (!["shutdown", "explosion"].includes(r)) {
      (u = ui.notifications) == null || u.warn("Unknown heat danger check.");
      return;
    }
    const s = ma(a);
    if (!s.inDanger || !s.dangerChecks) {
      (d = ui.notifications) == null || d.warn("Heat danger checks are only available while the BattleMech is in Danger heat.");
      return;
    }
    try {
      await Zi(a, {
        kind: "heatDangerCheck",
        event: n,
        checkKind: r,
        token: ((f = this.getSheetTokenDocument) == null ? void 0 : f.call(this)) ?? this._resolveStatusToken(a)
      });
    } catch (m) {
      console.error("MWD | Failed to launch BattleMech heat danger check", m), Yt(m, "Unable to launch that heat danger check.");
    }
  }
  async _onSetFireMode(n, i) {
    var s, o, l, c, u;
    (s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n);
    const a = this.getPersistentActor() ?? this.actor, r = String(((l = i == null ? void 0 : i.dataset) == null ? void 0 : l.fireModeId) ?? "").trim();
    if (r)
      try {
        const d = await Zi(a, {
          kind: "changeFireMode",
          newMode: r,
          token: ((c = this.getSheetTokenDocument) == null ? void 0 : c.call(this)) ?? this._resolveStatusToken(a)
        });
        d != null && d.ok || (u = ui.notifications) == null || u.warn((d == null ? void 0 : d.reason) ?? "Could not change fire mode.");
      } catch (d) {
        console.error("MWD | Failed to change BattleMech fire mode", d), Yt(d, "Could not change fire mode.");
      }
  }
  async _onAddWeaponGroup(n, i) {
    var s, o, l, c, u, d;
    if ((s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n), !this.isEditable) return;
    const a = this.getPersistentActor() ?? this.actor, r = Sc(a);
    r.push({
      id: ((c = (l = foundry.utils).randomID) == null ? void 0 : c.call(l)) ?? `group-${r.length + 1}`,
      name: ((d = (u = z == null ? void 0 : z.mwd) == null ? void 0 : u.loadout) == null ? void 0 : d.newGroup) ?? `Weapon Group ${r.length + 1}`,
      weaponIds: []
    }), await a.update({ "system.mwd.weaponGroups": r }), this.render({ force: !0 });
  }
  async _onDeleteWeaponGroup(n, i) {
    var l, c, u, d, f, m, g, p, h, b;
    if ((l = n == null ? void 0 : n.preventDefault) == null || l.call(n), (c = n == null ? void 0 : n.stopPropagation) == null || c.call(n), !this.isEditable) return;
    const a = String(
      ((u = i == null ? void 0 : i.dataset) == null ? void 0 : u.groupId) ?? ((m = (f = (d = i == null ? void 0 : i.closest) == null ? void 0 : d.call(i, "[data-group-id]")) == null ? void 0 : f.dataset) == null ? void 0 : m.groupId) ?? ((b = (h = (p = (g = n == null ? void 0 : n.target) == null ? void 0 : g.closest) == null ? void 0 : p.call(g, "[data-group-id]")) == null ? void 0 : h.dataset) == null ? void 0 : b.groupId) ?? ""
    ).trim();
    if (!a) return;
    const r = this.getPersistentActor() ?? this.actor, s = Sc(r), o = s.findIndex((y) => String((y == null ? void 0 : y.id) ?? "").trim() === a);
    o < 0 || (s.splice(o, 1), await r.update({ "system.mwd.weaponGroups": s }), this.render({ force: !0 }));
  }
  async _onToggleWeaponGroupHardpoint(n, i) {
    var m, g, p, h, b, y, S, A, T, C, k, w, v, N, P, L, _, O, x, H, G, q, ie, Z, Se, se;
    if ((m = n == null ? void 0 : n.preventDefault) == null || m.call(n), (g = n == null ? void 0 : n.stopPropagation) == null || g.call(n), !this.isEditable) return;
    const a = String(
      ((p = i == null ? void 0 : i.dataset) == null ? void 0 : p.groupId) ?? ((y = (b = (h = i == null ? void 0 : i.closest) == null ? void 0 : h.call(i, "[data-group-id]")) == null ? void 0 : b.dataset) == null ? void 0 : y.groupId) ?? ((C = (T = (A = (S = n == null ? void 0 : n.target) == null ? void 0 : S.closest) == null ? void 0 : A.call(S, "[data-group-id]")) == null ? void 0 : T.dataset) == null ? void 0 : C.groupId) ?? ""
    ).trim(), r = String(
      ((k = i == null ? void 0 : i.dataset) == null ? void 0 : k.hardpointId) ?? ((N = (v = (w = i == null ? void 0 : i.closest) == null ? void 0 : w.call(i, "[data-hardpoint-id]")) == null ? void 0 : v.dataset) == null ? void 0 : N.hardpointId) ?? ((O = (_ = (L = (P = n == null ? void 0 : n.target) == null ? void 0 : P.closest) == null ? void 0 : L.call(P, "[data-hardpoint-id]")) == null ? void 0 : _.dataset) == null ? void 0 : O.hardpointId) ?? ""
    ).trim();
    if (!a || !r) return;
    const s = this.getPersistentActor() ?? this.actor, o = bi(s).find((te) => String((te == null ? void 0 : te.id) ?? "").trim() === r) ?? null, l = String((o == null ? void 0 : o.itemId) ?? "").trim();
    if (!l) {
      (x = ui.notifications) == null || x.warn("Only loaded hardpoints can be bundled into a weapon group.");
      return;
    }
    const c = Sc(s), u = c.find((te) => String((te == null ? void 0 : te.id) ?? "").trim() === a) ?? null;
    if (!u) return;
    u.weaponIds = Array.isArray(u.weaponIds) ? u.weaponIds.map((te) => String(te ?? "").trim()).filter(Boolean) : [];
    const d = u.weaponIds.includes(l), f = c.find(
      (te) => String((te == null ? void 0 : te.id) ?? "").trim() !== a && Array.isArray(te == null ? void 0 : te.weaponIds) && te.weaponIds.map((pe) => String(pe ?? "").trim()).includes(l)
    ) ?? null;
    if (!d && f) {
      (ie = ui.notifications) == null || ie.warn(`${((q = (G = (H = s.items) == null ? void 0 : H.get) == null ? void 0 : G.call(H, l)) == null ? void 0 : q.name) ?? "That weapon"} is already bundled in ${f.name ?? "another group"}.`);
      return;
    }
    u.weaponIds = d ? u.weaponIds.filter((te) => te !== l) : [...u.weaponIds, l], this._captureScrollPosition(), await s.update({ "system.mwd.weaponGroups": c }), (se = (Se = (Z = i == null ? void 0 : i.closest) == null ? void 0 : Z.call(i, ".mwd-weapon-group-editor__choice")) == null ? void 0 : Se.classList) == null || se.toggle("is-selected", !d), this.render({ force: !1 });
  }
  _getPreparedRangedWeaponGroups(n) {
    var a;
    const i = ((a = this.getSheetTokenDocument) == null ? void 0 : a.call(this)) ?? this._resolveStatusToken(n);
    return Nu(n, { token: i });
  }
};
ze = new WeakSet(), Zv = async function(n) {
  var c, u, d;
  if (!this.isEditable) return;
  const i = n.target, a = String((i == null ? void 0 : i.value) ?? "").trim();
  if (!a) return;
  const r = String(
    ((d = (u = (c = i.closest) == null ? void 0 : c.call(i, "[data-group-id]")) == null ? void 0 : u.dataset) == null ? void 0 : d.groupId) ?? ""
  ).trim();
  if (!r) return;
  const s = this.getPersistentActor() ?? this.actor, o = Sc(s), l = o.find((f) => String((f == null ? void 0 : f.id) ?? "").trim() === r);
  !l || l.name === a || (l.name = a, await s.update({ "system.mwd.weaponGroups": o }));
}, eC = function(n) {
  if (!this.isEditable) return;
  const i = n == null ? void 0 : n.target;
  if (!(i instanceof HTMLSelectElement)) return;
  const a = GK(i.value);
  if (!a) return;
  const r = this._getRootElement();
  if (!r) return;
  const s = (o, l) => {
    const c = r.querySelector(`[name="${o}"]`);
    c instanceof HTMLInputElement && (c.value = String(l), c.dispatchEvent(new Event("input", { bubbles: !0 })), c.dispatchEvent(new Event("change", { bubbles: !0 })));
  };
  s("system.monitors.heat.max", a.trackLength), s("system.mwd.heat.thresholds.runningHot", a.thresholds.runningHot), s("system.mwd.heat.thresholds.overheated", a.thresholds.overheated), s("system.mwd.heat.thresholds.shutdown", a.thresholds.shutdown);
}, Zc = async function(n, i) {
  var r;
  const a = ((r = this.getSheetTokenDocument) == null ? void 0 : r.call(this)) ?? this._resolveStatusToken(n);
  return Zi(n, {
    kind: "attack",
    attackKind: "group",
    groupId: i,
    token: a
  });
}, tC = async function(n) {
  var r;
  const i = wl(n);
  if (!i.length) {
    (r = ui.notifications) == null || r.warn(z.actor.vehicle.quickActions.errors.noMelee);
    return;
  }
  const a = await E(this, ze, rC).call(this, i);
  a && await Zi(n, {
    kind: "attack",
    attackKind: "melee",
    profile: a
  });
}, nC = async function(n) {
  var c, u, d;
  const i = ((c = this.getSheetTokenDocument) == null ? void 0 : c.call(this)) ?? this._resolveStatusToken(n), a = Fh(n, { token: i });
  if (!a.filter((f) => !f.disabled).length) {
    (u = ui.notifications) == null || u.warn("No charge modes available. Move with Run, Sprint, or Jump before charging.");
    return;
  }
  const s = await E(this, ze, iC).call(this, a);
  if (!s) return;
  let o = "prone";
  if (s === "control" && (o = await E(this, ze, aC).call(this), !o))
    return;
  const l = ((d = this.getSheetTokenDocument) == null ? void 0 : d.call(this)) ?? this._resolveStatusToken(n);
  await Kh(n, { mode: s, controlIntent: o, token: l });
}, iC = async function(n) {
  const i = n.filter((s) => !s.disabled);
  if (!i.length) return null;
  if (i.length === 1) return i[0].id;
  const a = `<form class="mwd-quick-select">${n.map((s, o) => `
      <label class="quick-select-option ${s.disabled ? "is-disabled" : ""}" title="${foundry.utils.escapeHTML(s.reason ?? "")}">
        <input type="radio" name="charge-mode" value="${foundry.utils.escapeHTML(s.id)}" ${o === 0 && !s.disabled ? "checked" : ""} ${s.disabled ? "disabled" : ""}>
        <span>${foundry.utils.escapeHTML(s.label)}</span>
        ${s.disabled ? `<em style="opacity:0.6; font-size:0.85em;">${foundry.utils.escapeHTML(s.reason ?? "")}</em>` : ""}
      </label>`).join("")}</form>`, r = n.find((s) => !s.disabled);
  return foundry.applications.api.DialogV2.wait({
    window: { title: "Choose Charge Mode" },
    content: a,
    rejectClose: !1,
    buttons: [
      {
        action: "select",
        label: "Confirm",
        icon: "fa-solid fa-bolt",
        default: !0,
        callback: (s, o) => {
          var l, c;
          return ((c = (l = o.form) == null ? void 0 : l.elements["charge-mode"]) == null ? void 0 : c.value) ?? (r == null ? void 0 : r.id) ?? null;
        }
      }
    ]
  });
}, aC = async function() {
  const n = Uh(), i = `<form class="mwd-quick-select">${n.map((a, r) => `
      <label class="quick-select-option">
        <input type="radio" name="control-intent" value="${foundry.utils.escapeHTML(a.id)}" ${r === 0 ? "checked" : ""}>
        <span>${foundry.utils.escapeHTML(a.label)}</span>
      </label>`).join("")}</form>`;
  return foundry.applications.api.DialogV2.wait({
    window: { title: "Control Charge — Intended Outcome" },
    content: i,
    rejectClose: !1,
    buttons: [
      {
        action: "select",
        label: "Confirm",
        icon: "fa-solid fa-dice",
        default: !0,
        callback: (a, r) => {
          var s, o, l;
          return ((o = (s = r.form) == null ? void 0 : s.elements["control-intent"]) == null ? void 0 : o.value) ?? ((l = n[0]) == null ? void 0 : l.id) ?? null;
        }
      }
    ]
  });
}, rC = async function(n) {
  const i = Array.isArray(n) ? n : [];
  if (!i.length) return null;
  if (i.length === 1) return i[0];
  const a = i[0], r = `<form class="mwd-quick-select">${i.map((o) => `
      <label class="quick-select-option">
        <input type="radio" name="melee-profile" value="${foundry.utils.escapeHTML(String(o.id ?? ""))}" ${o.id === a.id ? "checked" : ""}>
        <span>${foundry.utils.escapeHTML(String(o.name ?? ""))}</span>
      </label>`).join("")}</form>`, s = await foundry.applications.api.DialogV2.wait({
    window: { title: z.actor.vehicle.quickActions.selectMeleeProfile },
    content: r,
    rejectClose: !1,
    buttons: [
      {
        action: "select",
        label: z.common.roll.button,
        icon: "fa-solid fa-dice",
        default: !0,
        callback: (o, l) => {
          var c, u;
          return ((u = (c = l.form) == null ? void 0 : c.elements["melee-profile"]) == null ? void 0 : u.value) ?? a.id;
        }
      }
    ]
  });
  return i.find((o) => o.id === s) ?? a;
}, sC = async function(n, i = null) {
  const r = (Array.isArray(i) && i.length ? i : this._getPreparedRangedWeaponGroups(n)).filter(
    (c) => Array.isArray(c == null ? void 0 : c.weaponIds) && c.weaponIds.length > 0 && c.isAttackLegal && c.isAvailableThisActivation
  );
  if (!r.length) return null;
  if (r.length === 1) return r[0];
  const s = r[0], o = `<form class="mwd-quick-select">${r.map((c) => `
      <label class="quick-select-option">
        <input type="radio" name="weapon-group" value="${c.id}" ${c.id === s.id ? "checked" : ""}>
        <span>${c.name}</span>
      </label>`).join("")}</form>`, l = await foundry.applications.api.DialogV2.wait({
    window: { title: z.actor.vehicle.quickActions.selectWeaponGroup },
    content: o,
    rejectClose: !1,
    buttons: [
      {
        action: "select",
        label: z.common.roll.button,
        icon: "fa-solid fa-dice",
        default: !0,
        callback: (c, u) => {
          var d, f;
          return ((f = (d = u.form) == null ? void 0 : d.elements["weapon-group"]) == null ? void 0 : f.value) ?? s.id;
        }
      }
    ]
  });
  return r.find((c) => c.id === l) ?? s;
}, oC = async function(n) {
  var l;
  const i = xS(n), a = i.filter((c) => !c.disabled);
  if (!a.length)
    return (l = ui.notifications) == null || l.warn("No movement actions are currently available."), null;
  const r = a[0], s = `<form class="mwd-quick-select">${i.map((c) => `
      <label class="quick-select-option${c.disabled ? " is-disabled" : ""}" title="${foundry.utils.escapeHTML(c.reason || c.hint || "")}">
        <input type="radio" name="movement-action" value="${c.id}" ${c.id === r.id ? "checked" : ""} ${c.disabled ? "disabled" : ""}>
        <span>${foundry.utils.escapeHTML(c.label)}</span>
        <small>${foundry.utils.escapeHTML(`${c.cost} SA${c.heat > 0 ? ` | +${c.heat} Heat` : ""}${c.hint ? ` | ${c.hint}` : ""}`)}</small>
      </label>`).join("")}</form>`, o = await foundry.applications.api.DialogV2.wait({
    window: { title: "Movement" },
    content: s,
    rejectClose: !1,
    buttons: [
      {
        action: "select",
        label: "Move",
        icon: "fa-solid fa-person-running",
        default: !0,
        callback: (c, u) => {
          var d, f;
          return ((f = (d = u.form) == null ? void 0 : d.elements["movement-action"]) == null ? void 0 : f.value) ?? r.id;
        }
      }
    ]
  });
  return a.find((c) => c.id === o) ?? r;
}, lC = async function(n) {
  var c, u;
  const i = ((c = this.getSheetTokenDocument) == null ? void 0 : c.call(this)) ?? this._resolveStatusToken(n), a = Gr(n, { token: i, includeDisabled: !0 }), r = a.filter((d) => !d.disabled);
  if (!r.length)
    return (u = ui.notifications) == null || u.warn(z.actor.vehicle.quickActions.errors.noSensorSweep), null;
  const s = r[0], o = `<form class="mwd-quick-select">${a.map((d) => `
      <label class="quick-select-option${d.disabled ? " is-disabled" : ""}" title="${foundry.utils.escapeHTML(String(d.reason ?? ""))}">
        <input type="radio" name="ew-action" value="${foundry.utils.escapeHTML(String(d.id ?? ""))}" ${d.id === s.id ? "checked" : ""} ${d.disabled ? "disabled" : ""}>
        <span>${foundry.utils.escapeHTML(String(d.label ?? ""))}</span>
        <small>${foundry.utils.escapeHTML(String(d.disabled ? d.reason : d.hint ?? ""))}</small>
      </label>`).join("")}</form>`, l = await foundry.applications.api.DialogV2.wait({
    window: { title: "Electronic Warfare" },
    content: o,
    rejectClose: !1,
    buttons: [
      {
        action: "select",
        label: z.common.roll.button,
        icon: "fa-solid fa-dice",
        default: !0,
        callback: (d, f) => {
          var m, g;
          return ((g = (m = f.form) == null ? void 0 : m.elements["ew-action"]) == null ? void 0 : g.value) ?? s.id;
        }
      }
    ]
  });
  return r.find((d) => d.id === l) ?? s;
}, cC = async function(n) {
  var o;
  const i = Dd(n);
  if (!i.length)
    return (o = ui.notifications) == null || o.warn("No active criticals or repairable statuses are available."), null;
  if (i.length === 1) return i[0];
  const a = i[0], r = `<form class="mwd-quick-select">${i.map((l) => `
      <label class="quick-select-option">
        <input type="radio" name="repair-issue" value="${foundry.utils.escapeHTML(`${l.issueKind}:${l.issueId}`)}" ${l.issueKind === a.issueKind && l.issueId === a.issueId ? "checked" : ""}>
        <span>${foundry.utils.escapeHTML(String(l.label ?? ""))}</span>
        <small>${foundry.utils.escapeHTML(`${l.remedyLabel ?? ""} | ${l.remedySummary || `DN ${l.totalDn}`}`)}</small>
      </label>`).join("")}</form>`, s = await foundry.applications.api.DialogV2.wait({
    window: { title: z.actor.vehicle.quickActions.emergencyRepair },
    content: r,
    rejectClose: !1,
    buttons: [
      {
        action: "select",
        label: z.common.roll.button,
        icon: "fa-solid fa-dice",
        default: !0,
        callback: (l, c) => {
          var u, d;
          return ((d = (u = c.form) == null ? void 0 : u.elements["repair-issue"]) == null ? void 0 : d.value) ?? `${a.issueKind}:${a.issueId}`;
        }
      }
    ]
  });
  return i.find((l) => `${l.issueKind}:${l.issueId}` === s) ?? a;
}, uC = async function(n) {
  const i = ma(n), a = `
      <form class="mwd-heat-dialog" style="display:grid; gap:0.75rem;">
        <label style="display:grid; gap:0.25rem;">
          <span style="font-weight:700; text-transform:uppercase; letter-spacing:0.08em;">Current Heat</span>
          <input type="number" name="currentHeat" value="${i.current}" min="0" step="1" />
        </label>
        <label style="display:grid; gap:0.25rem;">
          <span style="font-weight:700; text-transform:uppercase; letter-spacing:0.08em;">Pending Heat</span>
          <input type="number" name="pendingHeat" value="${i.pendingGenerated}" min="0" step="1" />
        </label>
        <p style="margin:0; opacity:0.8;">Use pending heat for movement surcharges or GM adjustments. Resolve it here when you need an out-of-band heat cycle.</p>
      </form>
    `;
  await foundry.applications.api.DialogV2.wait({
    window: {
      title: `${n.name ?? "BattleMech"} Heat`
    },
    position: {
      width: 420
    },
    content: a,
    buttons: [
      {
        action: "apply",
        label: "Apply",
        icon: "fa-solid fa-check",
        default: !0,
        callback: async (r, s) => {
          var o;
          try {
            return await E(this, ze, bg).call(this, n, s == null ? void 0 : s.form), !0;
          } catch (l) {
            return console.error("MWD | Failed to apply heat dialog changes", l), (o = ui.notifications) == null || o.error("Unable to apply heat changes."), !1;
          }
        }
      },
      {
        action: "resolve",
        label: "Resolve Pending Heat",
        icon: "fa-solid fa-fire",
        callback: async (r, s) => {
          var o;
          try {
            return await E(this, ze, bg).call(this, n, s == null ? void 0 : s.form), await E(this, ze, dC).call(this, n, "heat dialog"), !0;
          } catch (l) {
            return console.error("MWD | Failed to resolve heat from dialog", l), (o = ui.notifications) == null || o.error("Unable to resolve pending heat."), !1;
          }
        }
      },
      {
        action: "cancel",
        label: "Cancel",
        icon: "fa-solid fa-xmark",
        callback: () => !1
      }
    ],
    close: () => !1
  });
}, bg = async function(n, i) {
  var l, c, u, d, f, m, g, p, h, b, y;
  const a = (c = (l = i == null ? void 0 : i.elements) == null ? void 0 : l.namedItem) == null ? void 0 : c.call(l, "currentHeat"), r = (d = (u = i == null ? void 0 : i.elements) == null ? void 0 : u.namedItem) == null ? void 0 : d.call(u, "pendingHeat"), s = Math.max(0, Number((a == null ? void 0 : a.value) ?? ((g = (m = (f = n.system) == null ? void 0 : f.monitors) == null ? void 0 : m.heat) == null ? void 0 : g.value) ?? 0) || 0), o = Math.max(0, Number((r == null ? void 0 : r.value) ?? ((b = (h = (p = n.system) == null ? void 0 : p.mwd) == null ? void 0 : h.heat) == null ? void 0 : b.pendingGenerated) ?? 0) || 0);
  await ((y = n.setMonitorValue) == null ? void 0 : y.call(n, "heat", s)), await Op(n, o, { reason: "heat dialog" });
}, dC = async function(n, i = "sheet control") {
  var o, l;
  const a = ((o = this.getSheetTokenDocument) == null ? void 0 : o.call(this)) ?? this._resolveStatusToken(n), r = ((l = D.getSnapshot) == null ? void 0 : l.call(D, n, { token: a })) ?? null, s = r != null && r.hasCombatant && (r != null && r.isCurrentTurn) ? r.activation : null;
  await Zi(n, {
    kind: "resolvePendingHeat",
    source: i,
    activation: s,
    postDangerCard: !0
  });
}, F(qt, "LAYOUT_ID", "battlemech"), F(qt, "PARTS", {
  sheet: {
    get template() {
      return `${ht}/v2/actor/battlemech-sheet.hbs`;
    }
  }
}), F(qt, "DEFAULT_OPTIONS", foundry.utils.mergeObject(vi(qt, qt, "DEFAULT_OPTIONS"), {
  classes: ["battlemech-sheet", I, "actor-sheet-v2", "mwd-battlemech-sheet", "mwd-sheet"],
  position: { width: 980, height: 940 },
  actions: {
    ...vi(qt, qt, "DEFAULT_OPTIONS").actions,
    mechAttack: qt.prototype._onMechAttack,
    mechMovement: qt.prototype._onMechMovement,
    mechRoll: qt.prototype._onMechRoll,
    heatDangerCheck: qt.prototype._onHeatDangerCheck,
    openHeatDialog: qt.prototype._onOpenHeatDialog,
    addWeaponGroup: qt.prototype._onAddWeaponGroup,
    deleteWeaponGroup: qt.prototype._onDeleteWeaponGroup,
    toggleWeaponGroupHardpoint: qt.prototype._onToggleWeaponGroupHardpoint,
    setFireMode: qt.prototype._onSetFireMode
  }
}, { inplace: !1 }));
let hg = qt;
function QK() {
  console.log(`${tt}Registering Actor sheets (V2)`);
  const { Actors: t } = foundry.documents.collections;
  t.registerSheet(I, lg, {
    types: ["character"],
    makeDefault: !0,
    label: "Character (V2)"
  }), t.registerSheet(I, Lv, {
    types: ["npc"],
    makeDefault: !0,
    label: "NPC (V2)"
  }), t.registerSheet(I, Ru, {
    types: ["vehicle"],
    makeDefault: !0,
    label: "Vehicle (V2)"
  }), t.registerSheet(I, hg, {
    types: ["battlemech"],
    makeDefault: !0,
    label: "BattleMech (V2)"
  });
}
const { HandlebarsApplicationMixin: XK } = foundry.applications.api, { HTMLField: FS, StringField: ZK } = foundry.data.fields, Om = /* @__PURE__ */ new Set(["system.notes", "system.description"]), eH = Object.freeze({
  [M.itemType.personalWeapon]: `${ht}/v2/item/personal-weapon-root.hbs`,
  [M.itemType.mechWeapon]: `${ht}/v2/item/mech-weapon-root.hbs`,
  [M.itemType.armor]: `${ht}/v2/item/armor-root.hbs`
});
function Lm(t, e) {
  const n = new t({ required: !1, blank: !0, initial: "" });
  return n.name = e, n;
}
function tH(t = {}) {
  return {
    ...t,
    sourceReference: t.sourceReference ?? Lm(ZK, "system.sourceReference"),
    notes: t.notes ?? Lm(FS, "system.notes"),
    description: t.description ?? Lm(FS, "system.description")
  };
}
function nH(t = {}) {
  return Object.fromEntries(
    Object.entries(t ?? {}).filter(([, e]) => e !== void 0)
  );
}
var vr, Ba, Cr, Fi, Do, yg;
const Lt = class Lt extends XK(foundry.applications.sheets.ItemSheetV2) {
  constructor() {
    super(...arguments);
    Fe(this, Fi);
    Fe(this, vr, /* @__PURE__ */ new Map());
    Fe(this, Ba, /* @__PURE__ */ new Map());
    Fe(this, Cr, null);
    /** @override */
    F(this, "tabGroups", {
      primary: "main"
      // Default tab
    });
  }
  _getCanonicalItemType() {
    var n, i;
    return ((n = this.item) == null ? void 0 : n.canonicalType) ?? ((i = this.item) == null ? void 0 : i.type);
  }
  _getCanonicalItemTypeFromOptions(n) {
    const i = n == null ? void 0 : n.document;
    return (i == null ? void 0 : i.canonicalType) ?? (i == null ? void 0 : i.type);
  }
  /** @override */
  static get DEFAULT_OPTIONS() {
    return foundry.utils.mergeObject(super.DEFAULT_OPTIONS, {
      classes: ["sheet", "item", I, "appv2", "mwd-sheet", "item-sheet"],
      position: {
        width: 760,
        height: 860
      },
      window: {
        resizable: !0
      },
      actions: {
        editImage: Lt._onEditImage,
        tab: Lt.prototype._onClickTab,
        accordion: Lt.prototype._onClickAccordion,
        checkbarElement: Lt._onClickCheckbar,
        modifierAdd: Lt._onModifierAdd,
        modifierDelete: Lt._onModifierDelete,
        modifierValueChange: Lt._onModifierValueChange,
        modifierConditionChange: Lt._onModifierConditionChange,
        modifierSelectionChange: Lt._onModifierSelectionChange,
        effectCreate: Lt._onEffectCreate,
        effectEdit: Lt._onEffectEdit,
        effectDelete: Lt._onEffectDelete,
        effectToggleDisabled: Lt._onEffectToggleDisabled
      },
      form: {
        submitOnChange: !1,
        closeOnSubmit: !1,
        handler: Lt.prototype._onSubmitForm
      }
    }, { inplace: !1 });
  }
  static get defaultOptions() {
    return this.DEFAULT_OPTIONS;
  }
  _initializeApplicationOptions(n) {
    var s, o, l, c;
    n = super._initializeApplicationOptions(n), n.classes = Array.from(n.classes ?? []);
    const i = this._getCanonicalItemTypeFromOptions(n);
    i && n.classes.push(String(i));
    const a = ((c = (l = (o = (s = game.system) == null ? void 0 : s.mwd) == null ? void 0 : o.styles) == null ? void 0 : l.selectCssClass) == null ? void 0 : c.call(l)) ?? "mwd-theme-default", r = ["mwd-theme-default", "mwd-theme-sra"];
    return n.classes = n.classes.filter((u) => !r.includes(u)), n.classes.push(a), n;
  }
  /* -------------------------------------------- */
  /*  Rendering                                   */
  /* -------------------------------------------- */
  /**
   * Dynamically determine the template based on item type.
   * @param {string} partId - The part identifier
   * @returns {string} The template path
   * @override
   */
  _getPartTemplate(n) {
    var i;
    if (n === "sheet") {
      const a = this._getCanonicalItemType();
      return eH[a] ?? `${ht}/v2/item/${a}.hbs`;
    }
    return ((i = super._getPartTemplate) == null ? void 0 : i.call(this, n)) ?? "";
  }
  /**
   * Override title to show localized item type and name.
   * @override
   */
  get title() {
    const n = this._getCanonicalItemType();
    return `${z.itemType.singular[n] ?? n}: ${this.item.name}`;
  }
  /**
   * Prepare context data for rendering.
   * @param {object} options - Rendering options
   * @returns {Promise<object>} The context object
   * @override
   */
  async _prepareContext(n) {
    var k, w, v, N, P, L, _, O, x;
    const i = await super._prepareContext(n), a = ((w = (k = game.system.mwd.modifiers) == null ? void 0 : k.getEnums) == null ? void 0 : w.call(k)) ?? {}, r = foundry.utils.deepClone((i == null ? void 0 : i.options) ?? {}), s = tH((i == null ? void 0 : i.fields) ?? ((N = (v = this.item.system) == null ? void 0 : v.schema) == null ? void 0 : N.fields) ?? {}), o = ((L = (P = this.item.actor) == null ? void 0 : P.getAttributes) == null ? void 0 : L.call(P, this.item)) ?? [], l = this._getCanonicalItemType(), c = !this.item.actor, u = !!this.item.actor, d = z.itemType.singular[l] ?? l, f = this._getEffectEntries(), m = f.filter((H) => H.syncedCount > 0).length, g = this.constructor.LAYOUT_ID, p = this.item.actor ? (H) => o.includes(H) : (H) => !0, h = l === M.itemType.skill, y = ["mwd", "item-sheet", this.isEditable ? "editable" : "locked"], S = y.join(" ");
    r.classes = y, r.cssClass = S;
    const A = async (H, { secrets: G = this.item.isOwner } = {}) => foundry.applications.ux.TextEditor.implementation.enrichHTML(H ?? "", {
      async: !0,
      secrets: G,
      relativeTo: this.item
    }), T = foundry.utils.expandObject({
      "system.notes": await A(this.item.system.notes ?? ""),
      "system.description": await A(this.item.system.description ?? "")
    }), C = {
      ...i,
      item: this.item,
      data: this.item,
      system: this.item.system,
      // AppV2 prose editors need both raw field definitions and pre-enriched
      // HTML. Keeping both here avoids template-specific enrichment branches.
      fields: s,
      enriched: T,
      enrichedDescription: ((_ = T == null ? void 0 : T.system) == null ? void 0 : _.description) ?? "",
      options: {
        ...r,
        isGM: game.user.isGM,
        limited: !this.document.isOwner,
        owner: this.document.isOwner,
        isOwned: !!this.item.actor,
        editable: this.isEditable,
        cssClass: S,
        viewMode: !1
        // Items don't have view mode like actors do
      },
      ENUMS: {
        ...pt.getEnums(p, h),
        ...a
      },
      MWD: z,
      itemSheet: {
        canonicalType: l,
        typeLabel: d,
        isArmorSheet: l === M.itemType.armor,
        isStandalone: c,
        canUseActorControls: u,
        supportsEffectSync: !!((x = (O = this.item).supportsEquippedEffectSync) != null && x.call(O)),
        effectEntries: f,
        effectCount: f.length,
        syncedEffectCount: m,
        summaryChips: this._getSummaryChips(),
        stateChips: this._getStateChips(f)
      },
      cssClass: S,
      tabs: this._getTabs()
    };
    return g && (C.layout = await Hr.get(g)), C;
  }
  /**
   * Get tab configuration for this item type.
   * Override in subclasses if needed.
   * @returns {object} Tab configuration
   * @protected
   */
  _getTabs() {
    return {
      main: { id: "main", group: "primary", label: "Details" },
      modifiers: { id: "modifiers", group: "primary", label: "Modifiers" },
      effects: { id: "effects", group: "primary", label: "Effects" }
    };
  }
  _getSummaryChips() {
    return [];
  }
  _getStateChips(n = []) {
    var a, r, s;
    const i = [];
    return i.push({ kind: "ownership", label: this.item.actor ? "Owned Item" : "World Item" }), Object.prototype.hasOwnProperty.call(this.item.system ?? {}, "equipped") && i.push({
      kind: "equipment",
      label: (a = this.item.system) != null && a.equipped ? "Equipped" : "Unequipped",
      tone: (r = this.item.system) != null && r.equipped ? "active" : "muted"
    }), Object.prototype.hasOwnProperty.call(this.item.system ?? {}, "isPrimary") && ((s = this.item.system) != null && s.isPrimary) && i.push({ kind: "role", label: "Primary", tone: "accent" }), n.length && i.push({
      kind: "effects",
      label: `${n.length} Effect${n.length === 1 ? "" : "s"}`,
      tone: n.some((o) => o.syncedCount > 0) ? "active" : "muted"
    }), i;
  }
  _getEffectEntries() {
    var a, r, s, o, l, c;
    const n = /* @__PURE__ */ new Map(), i = ((r = (a = this.item).getSyncedActorEffects) == null ? void 0 : r.call(a)) ?? [];
    for (const u of i) {
      const d = (l = (o = (s = u.flags) == null ? void 0 : s[I]) == null ? void 0 : o.equippedItemSync) == null ? void 0 : l.sourceEffectId;
      if (!d) continue;
      const f = n.get(d) ?? [];
      f.push(u), n.set(d, f);
    }
    return Array.from(((c = this.item.effects) == null ? void 0 : c.contents) ?? []).map((u) => {
      var f, m, g, p, h, b, y;
      const d = n.get(u.id) ?? [];
      return {
        id: u.id,
        name: u.name || "New Effect",
        img: u.img || "icons/svg/aura.svg",
        disabled: !!u.disabled,
        transfer: !!u.transfer,
        changesCount: Array.isArray(u.changes) ? u.changes.length : 0,
        statusesCount: Number(((f = u.statuses) == null ? void 0 : f.size) ?? ((m = u.statuses) == null ? void 0 : m.length) ?? 0),
        durationLabel: (g = u.duration) != null && g.seconds ? `${u.duration.seconds}s` : (p = u.duration) != null && p.rounds ? `${u.duration.rounds} rounds` : "Passive",
        syncedCount: d.length,
        syncLabel: this.item.actor ? (b = (h = this.item).supportsEquippedEffectSync) != null && b.call(h) ? (y = this.item.system) != null && y.equipped ? d.length ? `Synced to actor (${d.length})` : "Pending sync" : "Applies when equipped" : "No equip sync" : "World item"
      };
    });
  }
  _getRootElement() {
    var n;
    return this.element instanceof HTMLElement ? this.element : (n = this.element) == null ? void 0 : n[0];
  }
  _onClickTab(n, i) {
    var l, c, u;
    const a = ((l = i == null ? void 0 : i.closest) == null ? void 0 : l.call(i, ".csb-tab-link[data-tab]")) ?? ((u = (c = n == null ? void 0 : n.target) == null ? void 0 : c.closest) == null ? void 0 : u.call(c, ".csb-tab-link[data-tab]"));
    if (!a) return;
    const r = a.closest(".csb-tabs");
    if (!r) return;
    const s = r.dataset.group || "default", o = a.dataset.tab;
    o && (X(this, vr).set(s, o), E(this, Fi, Do).call(this, this._getRootElement(), s, o));
  }
  _onClickAccordion(n, i) {
    var u, d, f;
    const a = ((u = i == null ? void 0 : i.closest) == null ? void 0 : u.call(i, ".csb-accordion__trigger[data-section]")) ?? ((f = (d = n == null ? void 0 : n.target) == null ? void 0 : d.closest) == null ? void 0 : f.call(d, ".csb-accordion__trigger[data-section]"));
    if (!a) return;
    const r = a.dataset.section, s = a.closest(".csb-accordion");
    if (!s || !r) return;
    const o = s.dataset.group || "default", c = (X(this, Ba).has(o) ? X(this, Ba).get(o) : s.dataset.default || null) === r ? null : r;
    X(this, Ba).set(o, c), E(this, Fi, yg).call(this, s, c);
  }
  _onRender(n, i) {
    var s, o, l, c, u;
    (s = super._onRender) == null || s.call(this, n, i), (o = this.window) != null && o.title && (this.window.title.textContent = this.title);
    const a = this._getRootElement();
    if (!a) return;
    a.setAttribute("dir", "ltr"), a.style.direction = "ltr", a.style.unicodeBidi = "isolate", a.style.writingMode = "horizontal-tb";
    const r = a.querySelector('.item-name input[name="name"]');
    r instanceof HTMLInputElement && (r.setAttribute("dir", "ltr"), r.style.direction = "ltr", r.style.unicodeBidi = "isolate", r.style.textAlign = "left", r.style.writingMode = "horizontal-tb");
    for (const d of a.querySelectorAll(".sheet-tabs")) {
      const f = d.dataset.group || "default", m = Array.from(d.querySelectorAll("[data-tab]"));
      if (!m.length) continue;
      for (const b of m)
        b.addEventListener("click", (y) => {
          y.preventDefault(), y.stopPropagation();
          const S = b.dataset.tab;
          S && (X(this, vr).set(f, S), E(this, Fi, Do).call(this, a, f, S));
        });
      const g = X(this, vr).get(f), p = d.dataset.default || ((l = m[0]) == null ? void 0 : l.dataset.tab), h = g || p;
      h && E(this, Fi, Do).call(this, a, f, h);
    }
    for (const d of a.querySelectorAll(".csb-tabs")) {
      const f = d.dataset.group || "default", m = Array.from(d.querySelectorAll(".csb-tab-link[data-tab]"));
      if (!m.length) continue;
      const g = X(this, vr).get(f), p = d.dataset.default || ((c = m[0]) == null ? void 0 : c.dataset.tab), h = g || p;
      h && E(this, Fi, Do).call(this, a, f, h);
    }
    for (const d of a.querySelectorAll(".csb-accordion")) {
      const f = d.dataset.group || "default", m = X(this, Ba).has(f) ? X(this, Ba).get(f) : d.dataset.default || null;
      E(this, Fi, yg).call(this, d, m);
    }
    for (const d of a.querySelectorAll("prose-mirror[name]")) {
      const f = d.getAttribute("name") ?? "";
      Om.has(f) && d.addEventListener("change", (m) => {
        m.preventDefault(), m.stopPropagation(), this._updateRichTextField(d);
      });
    }
    if (this.isEditable) {
      a.querySelectorAll(".mwd-list-picker[data-target-name]").forEach((d) => {
        d.addEventListener("change", (f) => {
          f.preventDefault();
          const m = String(d.value ?? "").trim();
          if (!m) return;
          const g = String(d.dataset.targetName ?? "").trim(), p = g ? Array.from(a.querySelectorAll("input[name], textarea[name]")).find((b) => b.getAttribute("name") === g) : null;
          if (!(p instanceof HTMLInputElement || p instanceof HTMLTextAreaElement)) {
            d.value = "";
            return;
          }
          const h = String(p.value ?? "").split(",").map((b) => b.trim()).filter(Boolean);
          p.value = Array.from(/* @__PURE__ */ new Set([...h, m])).join(", "), d.value = "", this._syncNamedField(p);
        });
      });
      for (const d of a.querySelectorAll("input[name], select[name], textarea[name]"))
        d.closest("prose-mirror") || d.hasAttribute("data-action") || (u = d.classList) != null && u.contains("mwd-list-picker") || d instanceof HTMLElement && d.addEventListener("change", (f) => {
          f.preventDefault(), this._syncNamedField(f.currentTarget ?? d);
        });
    }
    this._restoreScrollPositions();
  }
  async _updateRichTextField(n) {
    var s;
    const i = String(((s = n == null ? void 0 : n.getAttribute) == null ? void 0 : s.call(n, "name")) ?? (n == null ? void 0 : n.name) ?? "");
    if (!this.isEditable || !Om.has(i)) return;
    const a = String(n.value ?? ""), r = String(foundry.utils.getProperty(this.item, i) ?? "");
    if (a !== r)
      try {
        await this.item.update({ [i]: a });
      } catch (o) {
        console.warn("MWD | Rich text item update failed:", o);
      }
  }
  _getNamedFieldUpdate(n) {
    var r, s;
    if (!(n instanceof HTMLElement)) return null;
    const i = String(((r = n.getAttribute) == null ? void 0 : r.call(n, "name")) ?? "").trim();
    if (!i || Om.has(i)) return null;
    if (n instanceof HTMLInputElement) {
      if (n.type === "radio" && !n.checked) return null;
      if (n.type === "checkbox") return { [i]: n.checked };
      if (n.type === "number") {
        const o = Number(n.value);
        return Number.isFinite(o) ? { [i]: o } : null;
      }
    }
    const a = String(((s = n.dataset) == null ? void 0 : s.dtype) ?? "").trim().toLowerCase();
    if (a === "number") {
      const o = Number(n.value);
      return Number.isFinite(o) ? { [i]: o } : null;
    }
    return a === "boolean" ? { [i]: n.value === "true" } : { [i]: String(n.value ?? "") };
  }
  async _syncNamedField(n, i = {}) {
    if (!this.isEditable) return;
    const a = this._getNamedFieldUpdate(n), r = nH({
      ...a ?? {},
      ...i && typeof i == "object" ? i : {}
    });
    if (Object.keys(r).length) {
      this._captureScrollPositions();
      try {
        await this.item.update(r);
      } catch (s) {
        console.warn("MWD | Item field sync failed:", { updates: r, err: s });
      }
    }
  }
  async _onSubmitForm(n, i, a, { updateData: r = null } = {}) {
    if (!this.isEditable || !(i instanceof HTMLFormElement)) return;
    this._captureScrollPositions();
    const s = this._prepareSubmitData(n, i, a, r ?? {});
    await this._processSubmitData(n, i, s);
  }
  _getScrollRestoreSelectors() {
    return [".sheet-body", ".csb-tab-panels"];
  }
  _captureScrollPositions() {
    const n = this._getRootElement();
    if (!n) {
      De(this, Cr, null);
      return;
    }
    const i = [];
    for (const a of this._getScrollRestoreSelectors())
      n.querySelectorAll(a).forEach((r, s) => {
        r instanceof HTMLElement && i.push({
          selector: a,
          index: s,
          top: r.scrollTop,
          left: r.scrollLeft
        });
      });
    De(this, Cr, i.length ? i : null);
  }
  _restoreScrollPositions() {
    const n = X(this, Cr);
    if (!(n != null && n.length)) return;
    const i = () => {
      const a = this._getRootElement();
      if (a)
        for (const r of n) {
          const s = a.querySelectorAll(r.selector).item(r.index);
          s instanceof HTMLElement && (s.scrollTop = r.top, s.scrollLeft = r.left);
        }
    };
    i(), requestAnimationFrame(i), De(this, Cr, null);
  }
  /**
   * Override header buttons to add custom controls.
   * @returns {object[]} Array of header button configurations
   * @override
   */
  _getHeaderControls() {
    return super._getHeaderControls();
  }
  /* -------------------------------------------- */
  /*  Action Handlers                             */
  /* -------------------------------------------- */
  /**
   * Handle clicking a checkbar element (monitor).
   * @param {Event} event - The triggering event
   * @param {HTMLElement} target - The clicked element
   * @returns {Promise<void>}
   * @static
   */
  static async _onClickCheckbar(n, i) {
    const a = this.item;
    if (!a.parent) return;
    const r = i.closest(".checkbar-root");
    if (!r) return;
    const s = r.dataset.monitorCode, o = Number.parseInt(i.dataset.index), l = i.dataset.checked === "true";
    await a.parent.switchMonitorCheck(s, o, l);
  }
  static async _onEditImage(n) {
    var r, s, o;
    if ((r = n == null ? void 0 : n.preventDefault) == null || r.call(n), (s = n == null ? void 0 : n.stopPropagation) == null || s.call(n), !this.isEditable) return;
    const i = foundry.applications.apps.FilePicker.implementation;
    new i({
      type: "image",
      current: ((o = this.item) == null ? void 0 : o.img) ?? "",
      callback: async (l) => {
        l && await this.item.update({ img: l });
      }
    }).render(!0);
  }
  /**
   * Handle adding a new modifier.
   * @param {Event} event - The triggering event
   * @param {HTMLElement} target - The clicked element
   * @returns {Promise<void>}
   * @static
   */
  static async _onModifierAdd(n, i) {
    await this.item.createModifier();
  }
  /**
   * Handle deleting a modifier.
   * @param {Event} event - The triggering event
   * @param {HTMLElement} target - The clicked element
   * @returns {Promise<void>}
   * @static
   */
  static async _onModifierDelete(n, i) {
    const a = i.closest(".define-modifier");
    if (!a) return;
    const r = a.dataset.modifierId;
    r && await this.item.deleteModifier(r);
  }
  /**
   * Handle changing a modifier's value.
   * @param {Event} event - The triggering event
   * @param {HTMLElement} target - The input element
   * @returns {Promise<void>}
   * @static
   */
  static async _onModifierValueChange(n, i) {
    const a = i.closest(".define-modifier");
    if (!a) return;
    const r = a.dataset.modifierId;
    r && await this.item.changeModifierValue(r, i.value);
  }
  /**
   * Handle changing a modifier's condition.
   * @param {Event} event - The triggering event
   * @param {HTMLElement} target - The input element
   * @returns {Promise<void>}
   * @static
   */
  static async _onModifierConditionChange(n, i) {
    const a = i.closest(".define-modifier");
    if (!a) return;
    const r = a.dataset.modifierId;
    r && await this.item.changeModifierCondition(r, i.value);
  }
  /**
   * Handle changing a modifier's selection (dropdown).
   * @param {Event} event - The triggering event
   * @param {HTMLElement} target - The select element
   * @returns {Promise<void>}
   * @static
   */
  static async _onModifierSelectionChange(n, i) {
    const a = i.closest(".define-modifier");
    if (!a) return;
    const r = a.dataset.modifierId, s = i.dataset.modifierSelect;
    r && s && await this.item.changeModifierSelection(r, s, i.value);
  }
  static async _onEffectCreate(n, i) {
    var r, s, o;
    (r = n == null ? void 0 : n.preventDefault) == null || r.call(n), (s = n == null ? void 0 : n.stopPropagation) == null || s.call(n);
    const [a] = await this.item.createEmbeddedDocuments("ActiveEffect", [{
      name: `${this.item.name} Effect`,
      img: this.item.img || "icons/svg/aura.svg",
      disabled: !1,
      transfer: !1,
      changes: []
    }]);
    (o = a == null ? void 0 : a.sheet) == null || o.render(!0);
  }
  static async _onEffectEdit(n, i) {
    var s, o, l, c, u, d, f;
    (s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n);
    const a = ((l = i == null ? void 0 : i.dataset) == null ? void 0 : l.effectId) ?? ((d = (u = (c = i == null ? void 0 : i.closest) == null ? void 0 : c.call(i, "[data-effect-id]")) == null ? void 0 : u.dataset) == null ? void 0 : d.effectId);
    if (!a) return;
    const r = this.item.effects.get(a);
    (f = r == null ? void 0 : r.sheet) == null || f.render(!0);
  }
  static async _onEffectDelete(n, i) {
    var r, s, o, l, c, u;
    (r = n == null ? void 0 : n.preventDefault) == null || r.call(n), (s = n == null ? void 0 : n.stopPropagation) == null || s.call(n);
    const a = ((o = i == null ? void 0 : i.dataset) == null ? void 0 : o.effectId) ?? ((u = (c = (l = i == null ? void 0 : i.closest) == null ? void 0 : l.call(i, "[data-effect-id]")) == null ? void 0 : c.dataset) == null ? void 0 : u.effectId);
    a && await this.item.deleteEmbeddedDocuments("ActiveEffect", [a]);
  }
  static async _onEffectToggleDisabled(n, i) {
    var s, o, l, c, u, d;
    (s = n == null ? void 0 : n.preventDefault) == null || s.call(n), (o = n == null ? void 0 : n.stopPropagation) == null || o.call(n);
    const a = ((l = i == null ? void 0 : i.dataset) == null ? void 0 : l.effectId) ?? ((d = (u = (c = i == null ? void 0 : i.closest) == null ? void 0 : c.call(i, "[data-effect-id]")) == null ? void 0 : u.dataset) == null ? void 0 : d.effectId);
    if (!a) return;
    const r = this.item.effects.get(a);
    r && await r.update({ disabled: !r.disabled });
  }
};
vr = new WeakMap(), Ba = new WeakMap(), Cr = new WeakMap(), Fi = new WeakSet(), Do = function(n, i, a) {
  n && (n.querySelectorAll(`.csb-tabs[data-group="${i}"] .csb-tab-link[data-tab]`).forEach((r) => {
    r.classList.toggle("is-active", r.dataset.tab === a);
  }), n.querySelectorAll(`.csb-tabs[data-group="${i}"] .csb-tab-panel[data-tab]`).forEach((r) => {
    r.classList.toggle("is-active", r.dataset.tab === a);
  }), n.querySelectorAll(".sheet-tabs [data-tab]").forEach((r) => {
    var o;
    (((o = r.closest(".sheet-tabs")) == null ? void 0 : o.dataset.group) || "default") === i && r.classList.toggle("active", r.dataset.tab === a);
  }), n.querySelectorAll(`.tab[data-group="${i}"]`).forEach((r) => {
    r.classList.toggle("active", r.dataset.tab === a);
  }));
}, yg = function(n, i) {
  n.dataset.activeSection = i ?? "", n.querySelectorAll(".csb-accordion__section").forEach((a) => {
    const r = a.dataset.section === i;
    a.classList.toggle("is-active", r);
  }), n.querySelectorAll(".csb-accordion__trigger").forEach((a) => {
    const r = a.dataset.section === i;
    a.classList.toggle("is-active", r), a.setAttribute("aria-expanded", r ? "true" : "false");
  }), n.querySelectorAll(".csb-accordion__panel").forEach((a) => {
    const r = a.closest(".csb-accordion__section"), s = (r == null ? void 0 : r.dataset.section) === i;
    a.classList.toggle("is-active", s);
  });
}, F(Lt, "LAYOUT_ID", null), /** @override */
F(Lt, "PARTS", {
  sheet: {
    template: "",
    // Set dynamically in _getPartTemplate
    scrollable: [".sheet-body"]
  }
}), F(Lt, "TABS", {
  primary: {
    id: "primary",
    group: "primary",
    navSelector: ".sheet-tabs",
    contentSelector: ".sheet-body",
    initial: "main",
    // This must match your tab name
    tabs: [
      { id: "main" },
      { id: "modifiers" }
    ]
  }
});
let wi = Lt;
class Sg extends wi {
}
F(Sg, "LAYOUT_ID", "contact"), F(Sg, "PARTS", {
  sheet: {
    template: `${ht}/v2/item/contact.hbs`,
    scrollable: [".sheet-body"]
  }
});
const iH = Object.freeze([
  { value: "audiovisual", label: "Audiovisual Gear" },
  { value: "communication", label: "Communication Gear" },
  { value: "computing", label: "Computing Gear" },
  { value: "espionage", label: "Espionage Gear" },
  { value: "hostileEnvironment", label: "Hostile Environment Gear" },
  { value: "medical", label: "Medical Gear" },
  { value: "optical", label: "Optical Gear" },
  { value: "power", label: "Power Gear" },
  { value: "repairSalvage", label: "Repair/Salvage Gear" },
  { value: "survival", label: "Survival Gear" },
  { value: "surveillance", label: "Surveillance Gear" }
]), aH = Object.freeze([
  { value: "ammo", label: "Ammunition" },
  { value: "explosive", label: "Explosive" },
  { value: "medical", label: "Medical" },
  { value: "repair", label: "Repair" },
  { value: "fuel", label: "Fuel / Power Cell" },
  { value: "utility", label: "Utility" }
]);
function rH(t) {
  return t === "consumable" ? aH : iH;
}
class Ag extends wi {
  async _prepareContext(e) {
    var s;
    const n = await super._prepareContext(e), i = this._getCanonicalItemType(), a = this.item.system ?? {}, r = rH(i);
    return n.system = {
      ...a,
      quantity: Math.max(0, Math.trunc(Number(a.quantity ?? 1) || 0)),
      rating: Math.max(0, Math.trunc(Number(a.rating ?? 0) || 0)),
      category: String(a.category ?? "").trim(),
      relatedSkill: String(a.relatedSkill ?? "").trim(),
      availability: String(a.availability ?? "").trim(),
      rulesHook: String(a.rulesHook ?? "").trim(),
      tags: Array.isArray(a.tags) ? a.tags.map((o) => String(o ?? "").trim()).filter(Boolean) : []
    }, n.gearEditor = {
      categories: r.map((o) => ({ ...o }))
    }, n.tagsText = n.system.tags.join(", "), n.itemSheet = {
      ...n.itemSheet ?? {},
      summaryChips: [
        { label: "Qty", value: String(n.system.quantity) },
        { label: "Rating", value: String(n.system.rating) },
        {
          label: "Category",
          value: ((s = r.find((o) => o.value === n.system.category)) == null ? void 0 : s.label) ?? "Uncategorized"
        },
        { label: "Skill", value: n.system.relatedSkill || "None" },
        { label: "Avail", value: n.system.availability || "Unlisted" }
      ]
    }, n.layout = await Hr.get(i === "consumable" ? "consumable" : "gear"), n;
  }
}
// One sheet class intentionally backs both gear and consumables so quantity,
// rating, and reference editing never drift into parallel implementations.
F(Ag, "LAYOUT_ID", null), F(Ag, "PARTS", {
  sheet: {
    template: `${ht}/v2/item/gear.hbs`,
    scrollable: [".sheet-body"]
  }
});
class kg extends wi {
  async _prepareContext(e) {
    var s, o;
    const n = await super._prepareContext(e), i = Yn(this.item.system ?? {}), a = $k(), r = Array.isArray((s = n.ENUMS) == null ? void 0 : s.skills) ? n.ENUMS.skills.map((l) => ({
      value: String((l == null ? void 0 : l.value) ?? "").trim(),
      label: String((l == null ? void 0 : l.label) ?? (l == null ? void 0 : l.value) ?? "").trim()
    })).filter((l) => l.value) : [];
    return n.system = {
      ...i,
      effects: (Array.isArray(i.effects) ? i.effects : []).map((l) => ({
        ...l,
        showSkillPicker: Lk(l) || Array.isArray(l.skillKeys) && l.skillKeys.length > 0,
        isEdgeEvent: l.type === "edgeEvent"
      }))
    }, n.traitEditor = {
      ...a,
      skills: r
    }, n.itemSheet = {
      ...n.itemSheet ?? {},
      sheetClass: "mwd-item-sheet--quality",
      summaryChips: [
        { label: "Category", value: vc(i.category) },
        { label: "Tier", value: Cc(i.tier) },
        { label: "Activation", value: String(i.activation ?? "passive").trim() || "Passive" },
        { label: "Effects", value: String(((o = i.effects) == null ? void 0 : o.length) ?? 0) }
      ]
    }, n.tagsText = Array.isArray(i.tags) ? i.tags.join(", ") : "", n;
  }
  _onRender(e, n) {
    var r, s;
    (r = super._onRender) == null || r.call(this, e, n);
    const i = (s = this._getRootElement) == null ? void 0 : s.call(this);
    if (!i) return;
    const a = (o) => {
      var l;
      return (l = this._captureScrollPositions) == null || l.call(this), o();
    };
    i.querySelectorAll(".mwd-quality-prereq-add").forEach((o) => {
      o.addEventListener("click", (l) => {
        l.preventDefault(), l.stopPropagation(), a(() => {
          var c, u;
          return (u = (c = this.item).createQualityPrerequisite) == null ? void 0 : u.call(c);
        });
      });
    }), i.querySelectorAll(".mwd-quality-prereq-delete").forEach((o) => {
      o.addEventListener("click", (l) => {
        l.preventDefault(), l.stopPropagation(), a(() => {
          var c, u;
          return (u = (c = this.item).deleteQualityPrerequisite) == null ? void 0 : u.call(c, o.dataset.prereqId);
        });
      });
    }), i.querySelectorAll(".mwd-quality-prereq-field").forEach((o) => {
      o.addEventListener("change", (l) => {
        l.preventDefault(), l.stopPropagation(), a(() => {
          var c, u;
          return (u = (c = this.item).updateQualityPrerequisite) == null ? void 0 : u.call(
            c,
            o.dataset.prereqId,
            o.dataset.field,
            o.value
          );
        });
      });
    }), i.querySelectorAll(".mwd-quality-effect-add").forEach((o) => {
      o.addEventListener("click", (l) => {
        l.preventDefault(), l.stopPropagation(), a(() => {
          var c, u;
          return (u = (c = this.item).createQualityEffect) == null ? void 0 : u.call(c);
        });
      });
    }), i.querySelectorAll(".mwd-quality-effect-delete").forEach((o) => {
      o.addEventListener("click", (l) => {
        l.preventDefault(), l.stopPropagation(), a(() => {
          var c, u;
          return (u = (c = this.item).deleteQualityEffect) == null ? void 0 : u.call(c, o.dataset.effectId);
        });
      });
    }), i.querySelectorAll(".mwd-quality-effect-field").forEach((o) => {
      o.addEventListener("change", (l) => {
        l.preventDefault(), l.stopPropagation(), a(() => {
          var c, u;
          return (u = (c = this.item).updateQualityEffect) == null ? void 0 : u.call(
            c,
            o.dataset.effectId,
            o.dataset.field,
            o instanceof HTMLInputElement && o.type === "checkbox" ? o.checked : o instanceof HTMLSelectElement && o.multiple ? Array.from(o.selectedOptions).map((d) => d.value) : o.value
          );
        });
      });
    }), i.querySelectorAll(".mwd-quality-effect-skill-toggle").forEach((o) => {
      o.addEventListener("change", (l) => {
        l.preventDefault(), l.stopPropagation();
        const c = o.dataset.effectId, u = Array.from(i.querySelectorAll(`.mwd-quality-effect-skill-toggle[data-effect-id="${c}"]`)).filter((d) => d instanceof HTMLInputElement && d.checked).map((d) => d.value);
        a(() => {
          var d, f;
          return (f = (d = this.item).updateQualityEffect) == null ? void 0 : f.call(
            d,
            c,
            o.dataset.field,
            u
          );
        });
      });
    }), i.querySelectorAll(".mwd-quality-effect-condition-add").forEach((o) => {
      o.addEventListener("click", (l) => {
        l.preventDefault(), l.stopPropagation(), a(() => {
          var c, u;
          return (u = (c = this.item).createQualityEffectCondition) == null ? void 0 : u.call(c, o.dataset.effectId);
        });
      });
    }), i.querySelectorAll(".mwd-quality-effect-condition-delete").forEach((o) => {
      o.addEventListener("click", (l) => {
        l.preventDefault(), l.stopPropagation(), a(() => {
          var c, u;
          return (u = (c = this.item).deleteQualityEffectCondition) == null ? void 0 : u.call(c, o.dataset.effectId, o.dataset.conditionId);
        });
      });
    }), i.querySelectorAll(".mwd-quality-effect-condition-field").forEach((o) => {
      o.addEventListener("change", (l) => {
        l.preventDefault(), l.stopPropagation(), a(() => {
          var c, u;
          return (u = (c = this.item).updateQualityEffectCondition) == null ? void 0 : u.call(
            c,
            o.dataset.effectId,
            o.dataset.conditionId,
            o.dataset.field,
            o.value
          );
        });
      });
    });
  }
}
F(kg, "LAYOUT_ID", "quality"), F(kg, "PARTS", {
  sheet: {
    template: `${ht}/v2/item/quality.hbs`,
    scrollable: [".sheet-body"]
  }
});
class Tg extends wi {
  async _prepareContext(e) {
    var a;
    const n = await super._prepareContext(e), i = qT(this.item);
    return n.itemSheet = {
      ...n.itemSheet ?? {},
      assetModuleEffectsJson: JSON.stringify(((a = this.item.system) == null ? void 0 : a.effects) ?? [], null, 2),
      assetModuleSummary: i.summary,
      assetModuleErrors: i.errors ?? []
    }, n;
  }
  _getNamedFieldUpdate(e) {
    var i, a, r;
    if (String(((i = e == null ? void 0 : e.getAttribute) == null ? void 0 : i.call(e, "name")) ?? "").trim() !== "system.effects") return super._getNamedFieldUpdate(e);
    try {
      const s = JSON.parse(String(e.value ?? "[]"));
      return Array.isArray(s) ? (Np({ effects: s }, { itemName: this.item.name, itemId: this.item.id }), { "system.effects": s }) : ((a = ui.notifications) == null || a.error("AssetModule effects JSON must be an array."), null);
    } catch (s) {
      const o = s instanceof Ep ? s.userMessage : "AssetModule effects JSON is invalid.";
      return (r = ui.notifications) == null || r.error(o), console.error("MWD | Invalid AssetModule effects JSON", s), null;
    }
  }
}
F(Tg, "LAYOUT_ID", "asset-module"), F(Tg, "PARTS", {
  sheet: {
    template: `${ht}/v2/item/assetModule.hbs`,
    scrollable: [".sheet-body"]
  }
});
class wg extends wi {
  static get DEFAULT_OPTIONS() {
    return foundry.utils.mergeObject(super.DEFAULT_OPTIONS, {
      position: {
        width: 640,
        height: 620
      },
      window: {
        ...super.DEFAULT_OPTIONS.window,
        minWidth: 560,
        minHeight: 480
      }
    }, { inplace: !1 });
  }
  _getTabs() {
    return {
      main: { id: "main", group: "primary", label: "Details" }
    };
  }
  _getSummaryChips() {
    const e = hl(this.item.system ?? {}), n = Fa(e.catalogId), a = Td(n, e.selectedGrants, { legacySelectedSkill: e.selectedSkill }).map((s) => s.choice).filter(Boolean).map((s) => pl(s, { includeBonusText: !0 })).join(", "), r = this.item.actor ? qa(this.item.actor).stateByItemId.get(this.item.id) ?? null : null;
    return [
      { label: "Slot", value: qs(e.moduleType) },
      { label: "Module", value: (n == null ? void 0 : n.label) ?? "Unlinked" },
      { label: "Bonuses", value: a || "Pending choice" },
      r ? { label: "Status", value: r.isActive ? "Active" : "Inactive" } : null
    ].filter(Boolean);
  }
  async _prepareContext(e) {
    const n = await super._prepareContext(e), i = hl(this.item.system ?? {}), a = i.moduleType, r = Fa(i.catalogId), s = a ? rh(a) : [], o = n0(r, i.selectedGrants, { legacySelectedSkill: i.selectedSkill }), l = this.item.actor ? qa(this.item.actor).stateByItemId.get(this.item.id) ?? null : null;
    return n.lifeModuleEditor = {
      moduleType: a,
      moduleTypeLabel: qs(a),
      moduleTypes: Qw().map((c) => ({
        ...c,
        selected: c.value === a
      })),
      availableEntries: s.map((c) => ({
        id: c.id,
        label: c.label,
        selected: c.id === i.catalogId
      })),
      hasAvailableEntries: s.length > 0,
      selectedEntry: r,
      selectedGrants: i.selectedGrants,
      grantFields: o,
      requiresAnyLabels: ((r == null ? void 0 : r.requiresAny) ?? []).map((c) => {
        var u;
        return ((u = Fa(c)) == null ? void 0 : u.label) ?? c;
      }),
      excludesAnyLabels: ((r == null ? void 0 : r.excludesAny) ?? []).map((c) => {
        var u;
        return ((u = Fa(c)) == null ? void 0 : u.label) ?? c;
      }),
      actorState: l,
      warningLabels: [...(l == null ? void 0 : l.warningLabels) ?? []],
      isOwned: !!this.item.actor,
      statusLabel: l ? l.isActive ? "Active" : "Inactive" : r ? "Configured" : "Unlinked",
      statusReason: (l == null ? void 0 : l.inactiveReason) ?? ""
    }, n;
  }
}
F(wg, "LAYOUT_ID", "life-module"), F(wg, "PARTS", {
  sheet: {
    template: `${ht}/v2/item/lifeModule.hbs`,
    scrollable: [".sheet-body"]
  }
});
class Mg extends wi {
}
F(Mg, "LAYOUT_ID", "skill"), F(Mg, "PARTS", {
  sheet: {
    template: `${ht}/v2/item/skill.hbs`,
    scrollable: [".sheet-body"]
  }
});
const sH = Object.freeze([
  "firearms",
  "projectileWeapons",
  "heavyWeapons",
  "meleeCombat"
]), oH = Object.freeze([
  { value: "melee", label: "Melee" },
  { value: "ranged", label: "Ranged" }
]), US = Object.freeze([
  { value: "quantity", label: "Quantity" },
  { value: "durability.current", label: "Durability" },
  { value: "", label: "Custom Path" }
]), lH = Object.freeze(/* @__PURE__ */ new Set(["consumable", "gear"]));
function cH(t) {
  return String((t == null ? void 0 : t.canonicalType) ?? (t == null ? void 0 : t.type) ?? "item").trim().replace(/([a-z])([A-Z])/g, "$1 $2").replace(/^./, (n) => n.toUpperCase());
}
function mC(t) {
  return lH.has(String((t == null ? void 0 : t.canonicalType) ?? (t == null ? void 0 : t.type) ?? "").trim());
}
function uH(t) {
  var e;
  try {
    return (((e = foundry.applications.ux.TextEditor) == null ? void 0 : e.implementation) ?? TextEditor).getDragEventData(t);
  } catch {
    return null;
  }
}
function $m(t) {
  return t ? typeof t.values == "function" ? Array.from(t.values()) : Array.isArray(t) ? t : Array.isArray(t.contents) ? t.contents : [] : [];
}
function Du(t) {
  var e;
  return Math.max(0, Number(((e = t == null ? void 0 : t.system) == null ? void 0 : e.quantity) ?? 0) || 0);
}
function ra(t) {
  return ku((t == null ? void 0 : t.system) ?? {}, { name: t == null ? void 0 : t.name }).payloadKey || wt(t == null ? void 0 : t.name);
}
function KS(t, { activeId: e = "" } = {}) {
  var i, a, r;
  const n = /* @__PURE__ */ new Map();
  for (const s of t) {
    const o = (s == null ? void 0 : s.item) ?? null;
    if (!o) continue;
    const l = ra(o), c = n.get(l) ?? {
      key: l,
      item: o,
      source: s.source,
      owned: !!s.owned,
      count: 0,
      quantity: 0,
      totalQuantity: 0,
      families: Array.isArray((i = o.system) == null ? void 0 : i.families) ? o.system.families : [],
      tags: Array.isArray((a = o.system) == null ? void 0 : a.tags) ? o.system.tags : [],
      items: []
    }, u = Du(o);
    c.items.push(o), c.count += 1, c.totalQuantity += u, c.owned = c.owned || !!s.owned, c.source = c.owned ? "Actor" : s.source, (l === e || String(o.uuid ?? "") === e || ra(c.item) !== e && String(((r = c.item) == null ? void 0 : r.uuid) ?? "") !== e && u > Du(c.item)) && (c.item = o, c.quantity = u), n.set(l, c);
  }
  return Array.from(n.values()).sort((s, o) => {
    var l, c;
    return s.owned !== o.owned ? s.owned ? -1 : 1 : String(((l = s.item) == null ? void 0 : l.name) ?? "").localeCompare(String(((c = o.item) == null ? void 0 : c.name) ?? ""));
  });
}
function dH(t, e = "") {
  var i;
  const n = String(e ?? "").trim();
  return Array.from(((i = t == null ? void 0 : t.actor) == null ? void 0 : i.items) ?? []).filter((a) => {
    const r = String((a == null ? void 0 : a.id) ?? "").trim();
    return !r || r === (t == null ? void 0 : t.id) ? !1 : r === n || mC(a);
  }).sort((a, r) => String((a == null ? void 0 : a.name) ?? "").localeCompare(String((r == null ? void 0 : r.name) ?? ""))).map((a) => ({
    value: a.id,
    label: `${a.name || "Unnamed Item"} (${cH(a)})`
  }));
}
function vg(t, e, n) {
  const i = String(e ?? "").trim();
  return !i || t.some((a) => a.value === i) ? t : t.concat({ value: i, label: n(i) });
}
function mH(t, e) {
  var d, f, m, g, p, h, b;
  const n = si(e), i = dH(t, (d = n.link) == null ? void 0 : d.itemId), a = yE({
    source: n,
    actor: (t == null ? void 0 : t.actor) ?? null
  }), r = ((p = (m = (f = t == null ? void 0 : t.actor) == null ? void 0 : f.items) == null ? void 0 : m.get) == null ? void 0 : p.call(m, ((g = n.link) == null ? void 0 : g.itemId) ?? "")) ?? null, s = vg(
    [...US],
    (h = n.link) == null ? void 0 : h.itemPath,
    (y) => `Custom (${y})`
  ), o = new Set(US.map((y) => String(y.value ?? "").trim())), l = String(((b = n.link) == null ? void 0 : b.itemPath) ?? "").trim(), c = !!(t != null && t.actor);
  let u = "";
  return n.kind === "itemRef" && (c ? i.length ? r ? mC(r) ? l ? u = a.isTracked ? `Linked to ${r.name} | Available ${Number(a.current ?? 0)}` : `Linked to ${r.name} | Path not resolving to a tracked value yet.` : u = `Linked to ${r.name}. Pick which field should be consumed.` : u = `Linked to ${r.name} | Legacy non-inventory source. Repoint this to Gear or Consumable when convenient.` : u = "Pick an owned item to consume from." : u = "Add an owned Gear or Consumable item to the actor, then link this weapon to it." : u = "Embed this weapon in an actor to link it to owned inventory."), {
    ...n,
    resolvedState: a,
    ui: {
      ownedItemOptions: i,
      itemPathOptions: s,
      hasOwnedActor: c,
      linkedItemName: (r == null ? void 0 : r.name) ?? "",
      showCustomItemPath: n.kind === "itemRef" && !o.has(l),
      preview: u
    }
  };
}
class vl extends wi {
  /** @override */
  static get DEFAULT_OPTIONS() {
    return foundry.utils.mergeObject(super.DEFAULT_OPTIONS, {
      actions: {
        ...super.DEFAULT_OPTIONS.actions,
        weaponSkillChange: vl._onWeaponSkillChange,
        weaponCategoryChange: vl._onWeaponCategoryChange
      }
    }, { inplace: !1 });
  }
  _getTabs() {
    return {
      ...super._getTabs(),
      modifiers: { id: "modifiers", group: "primary", label: "Roll Modifiers" },
      effects: { id: "effects", group: "primary", label: "Active Effects" }
    };
  }
  /**
   * Prepare context data, adding weapon-specific enums.
   * @param {object} options - Rendering options
   * @returns {Promise<object>} The prepared context
   * @override
   */
  async _prepareContext(e) {
    var k, w, v, N, P, L, _, O, x, H, G, q, ie, Z, Se, se, te, pe;
    const n = await super._prepareContext(e), i = this._getCanonicalItemType();
    n.ENUMS = {
      ...n.ENUMS ?? {},
      defenses: _t.getDefenses()
    };
    const a = Array.isArray((k = n.ENUMS) == null ? void 0 : k.skills) ? n.ENUMS.skills : [], r = (w = this.item.system) == null ? void 0 : w.skill, s = (v = this.item.system) == null ? void 0 : v.damageType, o = i === "personalWeapon" ? vg(
      a.filter((K) => sH.includes(K.value)),
      r,
      (K) => {
        var be;
        return ((be = a.find((Ie) => Ie.value === K)) == null ? void 0 : be.label) ?? K;
      }
    ) : a, l = String(((N = this.item.system) == null ? void 0 : N.weaponCategory) ?? ((P = this.item.system) == null ? void 0 : P.category) ?? "ranged").trim().toLowerCase() === "melee" ? "melee" : "ranged", c = ((L = z == null ? void 0 : z.mwd) == null ? void 0 : L.hardpointSize) ?? ((O = (_ = z == null ? void 0 : z.mwd) == null ? void 0 : _.hardpoint) == null ? void 0 : O.size) ?? {}, u = i === "mechWeapon" ? [...kR] : [...Vo], d = i === "mechWeapon" ? En : In;
    n.weaponProfile = ((H = (x = this.item).getCombatProfile) == null ? void 0 : H.call(x)) ?? null;
    const f = ((G = this.item.system) == null ? void 0 : G.payloadCompatibility) ?? {}, m = ((ie = (q = this.item).getPayloadState) == null ? void 0 : ie.call(q)) ?? ((Z = n.weaponProfile) == null ? void 0 : Z.payloadState) ?? null, g = Array.isArray(m == null ? void 0 : m.payloads) ? m.payloads.filter((K) => String((K == null ? void 0 : K.sourceType) ?? "").trim() === lh) : [], p = String((m == null ? void 0 : m.activePayloadId) ?? "").trim(), h = Fr((Se = this.item.system) == null ? void 0 : Se.payloadSourceAssignments), b = Array.isArray((se = this.item.system) == null ? void 0 : se.consumptionSources) ? this.item.system.consumptionSources.map((K) => si(K)) : [], y = b.find((K) => String((K == null ? void 0 : K.kind) ?? "").trim() === "untracked") ?? null, S = String((y == null ? void 0 : y.label) ?? "").trim() || "Untracked", A = [
      { value: "", label: S },
      ...b.filter((K) => String((K == null ? void 0 : K.kind) ?? "").trim() !== "untracked").map((K) => ({ value: K.id, label: K.label || K.id }))
    ], T = new Map(b.map((K) => [String((K == null ? void 0 : K.id) ?? "").trim(), K])), C = KS(g.map((K) => {
      var be, Ie, Oe;
      return {
        item: K.itemId ? (Oe = (Ie = (be = this.item.actor) == null ? void 0 : be.items) == null ? void 0 : Ie.get) == null ? void 0 : Oe.call(Ie, K.itemId) : null,
        source: "Actor",
        owned: !0
      };
    }).filter((K) => K.item), { activeId: p });
    return n.weaponEditor = {
      skills: o,
      categories: i === "mechWeapon" ? [...oH] : [
        { value: "melee", label: "Melee" },
        { value: "projectile", label: "Projectile" },
        { value: "ranged", label: "Ranged" },
        { value: "smallArms", label: "Small Arms" },
        { value: "support", label: "Support" },
        { value: "thrown", label: "Thrown" },
        { value: "other", label: "Other" }
      ],
      damageTypes: vg(
        u,
        s,
        (K) => d(K)
      ),
      ranges: li.RANGE_ORDER.map((K) => ({
        value: K,
        label: i === "personalWeapon" ? pu(K) : K.charAt(0).toUpperCase() + K.slice(1)
      })),
      rangeBandLabels: Object.fromEntries(li.RANGE_ORDER.map((K) => [
        K,
        i === "personalWeapon" ? pu(K) : K.charAt(0).toUpperCase() + K.slice(1)
      ])),
      standardTraits: [...nE],
      payloadCapabilityOptions: OA,
      ammoDamageTypes: [{ value: "", label: "Use Weapon Default" }, ...Vo],
      payloadTemplateShapes: Wg,
      payloadTemplatePlacements: Vg,
      areaEffectKinds: [
        { value: kt.none, label: "None" },
        { value: kt.discrete, label: "Discrete" },
        { value: kt.persistent, label: "Persistent Hazard" }
      ],
      exposureTiers: [
        { value: Te.minor, label: "Minor" },
        { value: Te.major, label: "Major" },
        { value: Te.full, label: "Full" }
      ],
      resolverKeys: [
        { value: "standard", label: "Standard" },
        { value: "template", label: "Template" }
      ],
      payloadSourceKinds: [
        { value: "untracked", label: "Untracked" },
        { value: "internal", label: "Internal" },
        { value: "actorResource", label: "Actor Resource" },
        { value: "itemRef", label: "Linked Item" }
      ],
      hardpointSizes: Object.entries(c).map(([K, be]) => ({ value: K, label: be })),
      consumptionSources: Array.isArray((te = this.item.system) == null ? void 0 : te.consumptionSources) ? this.item.system.consumptionSources.map((K) => mH(this.item, K)) : [],
      untrackedSourceLabel: S,
      payloadCompatibilityText: {
        families: Array.isArray(f.families) ? f.families.join(", ") : String(f.families ?? ""),
        tagsAll: Array.isArray(f.tagsAll) ? f.tagsAll.join(", ") : String(f.tagsAll ?? "")
      },
      payloadOptions: (Array.isArray(m == null ? void 0 : m.payloads) ? m.payloads : []).map((K) => ({
        value: K.id,
        label: K.label || "Payload",
        selected: K.id === p
      })),
      payloadFamilyOptions: Au(sh()),
      payloadTagOptions: Au(oh()),
      actorPayloads: C.map((K) => {
        var B, me;
        const be = ra(K.item), Ie = g.find((oe) => oe.payloadKey === be || oe.id === be) ?? {}, Oe = h[be] ?? { sourceId: null }, Be = String((Oe == null ? void 0 : Oe.sourceId) ?? "").trim(), R = Be ? T.get(Be) : null, V = Be && !R ? A.concat({ value: Be, label: `Missing source (${Be})` }) : A;
        return {
          id: Ie.id || be,
          payloadKey: be,
          itemId: ((B = K.item) == null ? void 0 : B.id) || "",
          label: Ie.label || ((me = K.item) == null ? void 0 : me.name) || "Payload",
          families: K.families.join(", "),
          tags: K.tags.join(", "),
          quantity: K.totalQuantity || K.quantity,
          stackCount: K.count,
          stackLabel: K.count > 1 ? `${K.count} stacks` : "",
          sourceId: Be,
          sourceOptions: V,
          sourceLabel: (R == null ? void 0 : R.label) ?? S,
          loadedPayloadKey: wt(R == null ? void 0 : R.loadedPayloadKey),
          loaded: wt(R == null ? void 0 : R.loadedPayloadKey) === be,
          selected: be === p
        };
      }),
      activePayloadLabel: (m == null ? void 0 : m.payloadLabel) || "Unloaded",
      hasActorPayloads: C.length > 0
    }, n.itemSheet = {
      ...n.itemSheet ?? {},
      isCompactWeaponSheet: !0,
      weaponSheetVariant: i === "mechWeapon" ? "mech" : "personal",
      mechDerivedSkillLabel: l === "melee" ? "Melee Combat" : "Gunnery",
      mechIsMelee: i === "mechWeapon" && l === "melee"
    }, n.itemSheet.stateChips = (n.itemSheet.stateChips ?? []).filter(
      (K) => !["ownership", "equipment", "role"].includes(K.kind)
    ), n.itemSheet.currentPayloadLabel = ((pe = n.weaponProfile) == null ? void 0 : pe.payloadLabel) ?? "", n;
  }
  /**
   * Handle changing the weapon skill, which updates the defense attribute.
   * @param {Event} event - The triggering event
   * @param {HTMLElement} target - The changed select element
   * @static
   * @async
   */
  _onRender(e, n) {
    var i;
    (i = super._onRender) == null || i.call(this, e, n), this._bindWeaponPayloadDropTarget(), this._bindPayloadEditorControls(), this._bindWeaponStandardTraitControls();
  }
  _bindWeaponPayloadDropTarget() {
    var n;
    const e = (n = this._getRootElement) == null ? void 0 : n.call(this);
    e && e.addEventListener("dragover", (i) => {
      i.preventDefault(), i.dataTransfer && (i.dataTransfer.dropEffect = "copy");
    });
  }
  async _onDrop(e) {
    var i, a;
    if (!this.isEditable) return (i = super._onDrop) == null ? void 0 : i.call(this, e);
    const n = uH(e);
    if (!await this._handleWeaponPayloadDrop(n))
      return (a = super._onDrop) == null ? void 0 : a.call(this, e);
  }
  async _handleWeaponPayloadDrop(e = null) {
    var r, s, o;
    if (String((e == null ? void 0 : e.type) ?? "").trim() !== "Item") return !1;
    const n = await this._resolveDroppedItem(e);
    if (!us(n)) return !1;
    if (!(this.item.actor ?? null))
      return (r = ui.notifications) == null || r.warn("Reusable payloads must be owned by the same actor as the weapon. Add this weapon to an actor before assigning payload items."), !0;
    const a = ((s = this.item.system) == null ? void 0 : s.payloadCompatibility) ?? {};
    return $c(a, n.system ?? {}) ? (await this._assignWeaponPayloadItem(n), !0) : ((o = ui.notifications) == null || o.warn(`${n.name ?? "That payload"} is not compatible with ${this.item.name ?? "this weapon"}. Check the weapon's accepted families and tag filters.`), !0);
  }
  async _assignWeaponPayloadItem(e) {
    var l, c, u, d, f;
    const n = this.item.actor ?? null;
    if (!n || !us(e)) return !1;
    let i = e;
    const a = (e == null ? void 0 : e.parent) === n || (e == null ? void 0 : e.actor) === n, r = ra(e), s = $m(n.items).find((m) => (m == null ? void 0 : m.id) !== (e == null ? void 0 : e.id) && us(m) && ra(m) === r);
    if (s) {
      const m = Du(s), g = Math.max(1, Du(e));
      await s.update({ "system.quantity": m + g }), a && (e != null && e.id) && await n.deleteEmbeddedDocuments("Item", [e.id]), i = s;
    } else if (!a) {
      const m = foundry.utils.deepClone(((l = e.toObject) == null ? void 0 : l.call(e)) ?? e ?? {});
      m.system ?? (m.system = {}), m.system.payloadKey = r, delete m._id;
      const g = await n.createEmbeddedDocuments("Item", [m]);
      i = (g == null ? void 0 : g[0]) ?? null;
    }
    const o = ra(i);
    return o ? (await ((d = (u = this.item).setActivePayload) == null ? void 0 : d.call(u, o)), (f = ui.notifications) == null || f.info(`${i.name ?? "Payload"} assigned to ${this.item.name ?? "weapon"}.`), this.render({ force: !0 }), !0) : ((c = ui.notifications) == null || c.warn("That payload could not be assigned to this weapon."), !1);
  }
  async _promptReusablePayloadSelection() {
    var f, m, g, p, h, b, y;
    const e = this.item.actor ?? null;
    if (!e) {
      (f = ui.notifications) == null || f.warn("Reusable payloads must be owned by the same actor as the weapon. Add this weapon to an actor before assigning payload items.");
      return;
    }
    const n = ((m = this.item.system) == null ? void 0 : m.payloadCompatibility) ?? {}, i = $m(e.items).filter((S) => us(S) && $c(n, S.system ?? {})).map((S) => ({ item: S, source: "Actor", owned: !0 })), a = new Set(i.map((S) => ra(S.item))), r = $m(game.items).filter((S) => us(S) && $c(n, S.system ?? {})).filter((S) => !a.has(ra(S))).map((S) => ({ item: S, source: "World", owned: !1 })), s = String(((h = (p = (g = this.item).getPayloadState) == null ? void 0 : p.call(g)) == null ? void 0 : h.activePayloadId) ?? "").trim(), o = KS([...i, ...r], { activeId: s });
    if (!o.length) {
      (b = ui.notifications) == null || b.warn("No payload items match this weapon's accepted families and required tags.");
      return;
    }
    const l = o.map((S, A) => {
      const T = S.item, C = [
        S.source,
        S.families.length ? S.families.join(", ") : "",
        S.tags.length ? S.tags.join(", ") : "",
        `Qty ${S.quantity}`,
        S.count > 1 ? `${S.count} stacks, ${S.totalQuantity} total` : ""
      ].filter(Boolean).join(" | "), k = ra(T) === s || !s && A === 0;
      return `
        <option value="${foundry.utils.escapeHTML(S.key)}" ${k ? "selected" : ""}>
          ${foundry.utils.escapeHTML(`${T.name || "Payload"} (${C})`)}
        </option>`;
    }).join(""), c = o.map((S) => {
      var T;
      const A = [
        S.source,
        S.families.length ? `Families: ${S.families.join(", ")}` : "",
        S.tags.length ? `Tags: ${S.tags.join(", ")}` : "",
        `Qty ${S.quantity}`,
        S.count > 1 ? `${S.count} matching stacks, ${S.totalQuantity} total` : ""
      ].filter(Boolean).join(" | ");
      return `<li><strong>${foundry.utils.escapeHTML(((T = S.item) == null ? void 0 : T.name) || "Payload")}</strong><br><small>${foundry.utils.escapeHTML(A)}</small></li>`;
    }).join(""), u = await foundry.applications.api.DialogV2.prompt({
      window: { title: "Select Weapon Payload" },
      content: `
        <form class="mwd-quick-select mwd-payload-select-dialog">
          <div class="mwd-field">
            <label>Payload</label>
            <select name="payload-uuid">${l}</select>
          </div>
          <ul>${c}</ul>
        </form>`,
      rejectClose: !1,
      ok: {
        label: "Select",
        icon: "fa-solid fa-check",
        callback: (S, A) => {
          var T, C, k;
          return ((C = (T = A.form) == null ? void 0 : T.elements["payload-uuid"]) == null ? void 0 : C.value) ?? ((k = o[0]) == null ? void 0 : k.key) ?? "";
        }
      }
    });
    if (!u) return;
    const d = ((y = o.find((S) => S.key === u)) == null ? void 0 : y.item) ?? null;
    d && await this._assignWeaponPayloadItem(d);
  }
  async _resolveDroppedItem(e = null) {
    var o, l, c;
    if (!e || (e == null ? void 0 : e.type) !== "Item") return null;
    const n = ((o = CONFIG == null ? void 0 : CONFIG.Item) == null ? void 0 : o.documentClass) ?? globalThis.Item ?? null;
    if (typeof (n == null ? void 0 : n.fromDropData) == "function") {
      const u = await n.fromDropData(e).catch(() => null);
      if (u) return u;
    }
    const i = this.item.actor ?? null, a = String((e == null ? void 0 : e.itemId) ?? (e == null ? void 0 : e._id) ?? (e == null ? void 0 : e.id) ?? "").trim(), r = String((e == null ? void 0 : e.actorId) ?? (e == null ? void 0 : e.parentId) ?? "").trim();
    if (a && r && r === String((i == null ? void 0 : i.id) ?? "").trim()) {
      const u = ((c = (l = i == null ? void 0 : i.items) == null ? void 0 : l.get) == null ? void 0 : c.call(l, a)) ?? null;
      if (u) return u;
    }
    const s = e != null && e.uuid ? await fromUuid(e.uuid).catch(() => null) : null;
    return !s && (e != null && e.data) && typeof n == "function" ? new n(e.data, { parent: i }) : s ?? null;
  }
  _bindWeaponStandardTraitControls() {
    var i;
    const e = (i = this._getRootElement) == null ? void 0 : i.call(this);
    if (!e) return;
    const n = (a) => {
      var r;
      return (r = this._captureScrollPositions) == null || r.call(this), a();
    };
    e.querySelectorAll(".mwd-weapon-standard-trait-add").forEach((a) => {
      a.addEventListener("click", (r) => {
        r.preventDefault(), n(() => {
          var s, o;
          return (o = (s = this.item).createWeaponStandardTrait) == null ? void 0 : o.call(s);
        });
      });
    }), e.querySelectorAll(".mwd-weapon-standard-trait-delete").forEach((a) => {
      a.addEventListener("click", (r) => {
        r.preventDefault(), n(() => {
          var s, o;
          return (o = (s = this.item).deleteWeaponStandardTrait) == null ? void 0 : o.call(s, a.dataset.traitId);
        });
      });
    }), e.querySelectorAll(".mwd-weapon-standard-trait-field").forEach((a) => {
      a.addEventListener("change", (r) => {
        r.preventDefault(), n(() => {
          var s, o;
          return (o = (s = this.item).updateWeaponStandardTrait) == null ? void 0 : o.call(
            s,
            a.dataset.traitId,
            a.dataset.field,
            a.value
          );
        });
      });
    });
  }
  _bindPayloadEditorControls() {
    var i;
    const e = (i = this._getRootElement) == null ? void 0 : i.call(this);
    if (!e) return;
    const n = (a) => {
      var r;
      return (r = this._captureScrollPositions) == null || r.call(this), a();
    };
    e.querySelectorAll(".mwd-payload-add").forEach((a) => {
      a.addEventListener("click", (r) => {
        r.preventDefault(), n(() => this._promptReusablePayloadSelection());
      });
    }), e.querySelectorAll(".mwd-active-payload-select").forEach((a) => {
      a.addEventListener("change", (r) => {
        var o;
        r.preventDefault();
        const s = String(((o = r.currentTarget) == null ? void 0 : o.value) ?? "").trim();
        s && n(() => {
          var l, c;
          return (c = (l = this.item).setActivePayload) == null ? void 0 : c.call(l, s);
        });
      });
    }), e.querySelectorAll(".mwd-payload-select").forEach((a) => {
      a.addEventListener("click", (r) => {
        r.preventDefault();
        const s = String(a.dataset.payloadId ?? "").trim();
        s && n(() => {
          var o, l;
          return (l = (o = this.item).setActivePayload) == null ? void 0 : l.call(o, s);
        });
      });
    }), e.querySelectorAll(".mwd-payload-delete").forEach((a) => {
      a.addEventListener("click", (r) => {
        r.preventDefault(), n(() => {
          var s, o;
          return (o = (s = this.item).deletePayload) == null ? void 0 : o.call(s, a.dataset.payloadId);
        });
      });
    }), e.querySelectorAll(".mwd-payload-item-remove").forEach((a) => {
      a.addEventListener("click", (r) => {
        var l;
        r.preventDefault();
        const s = String(a.dataset.itemId ?? "").trim();
        if (!s) return;
        const o = this.item.actor ?? null;
        o && ((l = this._captureScrollPositions) == null || l.call(this), (async () => (await o.deleteEmbeddedDocuments("Item", [s]), this.render({ force: !0 })))());
      });
    }), e.querySelectorAll(".mwd-payload-field").forEach((a) => {
      a.addEventListener("change", (r) => {
        r.preventDefault(), n(() => {
          var s, o;
          return (o = (s = this.item).updatePayloadField) == null ? void 0 : o.call(
            s,
            a.dataset.payloadId,
            a.dataset.field,
            a.value
          );
        });
      });
    }), e.querySelectorAll(".mwd-payload-source-field").forEach((a) => {
      a.addEventListener("change", (r) => {
        r.preventDefault(), n(() => {
          var s, o;
          return (o = (s = this.item).updatePayloadSourceAssignment) == null ? void 0 : o.call(
            s,
            a.dataset.payloadKey,
            a.value
          );
        });
      });
    }), e.querySelectorAll(".mwd-source-add").forEach((a) => {
      a.addEventListener("click", (r) => {
        r.preventDefault(), n(() => {
          var s, o;
          return (o = (s = this.item).createConsumptionSource) == null ? void 0 : o.call(s);
        });
      });
    }), e.querySelectorAll(".mwd-source-delete").forEach((a) => {
      a.addEventListener("click", (r) => {
        r.preventDefault(), n(() => {
          var s, o;
          return (o = (s = this.item).deleteConsumptionSource) == null ? void 0 : o.call(s, a.dataset.sourceId);
        });
      });
    }), e.querySelectorAll(".mwd-source-field").forEach((a) => {
      a.addEventListener("change", (r) => {
        r.preventDefault();
        const s = a.type === "checkbox" ? a.checked : a.value;
        n(() => {
          var o, l;
          return (l = (o = this.item).updateConsumptionSourceField) == null ? void 0 : l.call(
            o,
            a.dataset.sourceId,
            a.dataset.field,
            s
          );
        });
      });
    }), e.querySelectorAll(".mwd-capability-picker").forEach((a) => {
      a.addEventListener("change", (r) => {
        r.preventDefault();
        const s = String(a.value ?? "").trim();
        if (!s) return;
        const o = String(a.dataset.values ?? "").split(",").map((d) => d.trim()).filter(Boolean), l = Array.from(/* @__PURE__ */ new Set([...o, s]));
        a.value = "";
        const c = String(a.dataset.payloadId ?? "").trim(), u = String(a.dataset.field ?? "").trim();
        if (u) {
          if (c) {
            n(() => {
              var d, f;
              return (f = (d = this.item).updatePayloadField) == null ? void 0 : f.call(d, c, u, l.join(", "));
            });
            return;
          }
          n(() => this.item.update({ [u]: l }));
        }
      });
    });
  }
  static async _onWeaponSkillChange(e, n) {
    var r, s;
    const i = n.value, a = (s = (r = game.system.mwd.skills) == null ? void 0 : r.get) == null ? void 0 : s.call(r, i);
    await this._syncNamedField(n, {
      ...a != null && a.defense ? { "system.defense": a.defense } : {}
    });
  }
  static async _onWeaponCategoryChange(e, n) {
    var r, s, o;
    const i = String((n == null ? void 0 : n.value) ?? "").trim().toLowerCase() === "melee" ? "melee" : "ranged";
    if ((((r = this._getCanonicalItemType) == null ? void 0 : r.call(this)) ?? ((s = this.item) == null ? void 0 : s.canonicalType) ?? ((o = this.item) == null ? void 0 : o.type)) === "mechWeapon") {
      const l = {
        "system.category": i,
        "system.weaponCategory": i,
        "system.skill": i === "melee" ? "meleeCombat" : "gunnery"
      };
      i === "melee" && (l["system.range.max"] = "close", l["system.range.close"] = 0, l["system.range.near"] = 0, l["system.range.far"] = 0, l["system.range.extreme"] = 0), await this._syncNamedField(n, l);
      return;
    }
    await this._syncNamedField(n);
  }
}
const Ss = class Ss extends vl {
  static get DEFAULT_OPTIONS() {
    return foundry.utils.mergeObject(super.DEFAULT_OPTIONS, {
      position: {
        width: 680,
        height: 720
      },
      window: {
        ...super.DEFAULT_OPTIONS.window,
        minWidth: 680,
        minHeight: 480,
        maxWidth: 960
      },
      actions: {
        ...super.DEFAULT_OPTIONS.actions,
        attackWeapon: Ss._onAttackWeapon,
        reloadWeaponPayload: Ss._onReloadWeaponPayload
      }
    }, { inplace: !1 });
  }
  async _prepareContext(e) {
    var s, o, l;
    const n = await super._prepareContext(e), i = this.item.actor ?? null, a = n.weaponProfile ?? null, r = !!(i && typeof i.isCharacterLike == "function" && i.isCharacterLike() && ((o = (s = this.item).isPersonalWeapon) != null && o.call(s)));
    return n.itemSheet = {
      ...n.itemSheet ?? {},
      canAttack: r,
      attackDisabled: !r || !((l = this.item.system) != null && l.equipped)
    }, n.itemSheet.summaryChips = this._getSummaryChips(a), n.itemSheet.reloadState = this._getReloadDisplayState(a), n;
  }
  _getReloadDisplayState(e = ((n) => ((i) => (i = (n = this.item).getCombatProfile) == null ? void 0 : i.call(n))())() ?? null) {
    var d, f, m, g, p;
    const a = ((f = (d = this.item).canReloadActivePayload) == null ? void 0 : f.call(d, { detailed: !0 })) ?? { canReload: !1, reason: "" }, r = !!((m = e == null ? void 0 : e.sourceState) != null && m.isTracked), s = String((e == null ? void 0 : e.payloadLabel) ?? (a == null ? void 0 : a.payloadLabel) ?? "").trim() || "Unloaded", o = Number(((g = e == null ? void 0 : e.sourceState) == null ? void 0 : g.current) ?? (a == null ? void 0 : a.current) ?? 0) || 0, l = Number(((p = e == null ? void 0 : e.sourceState) == null ? void 0 : p.max) ?? (a == null ? void 0 : a.max) ?? 0) || 0, c = r ? `${s} ${o}/${l}` : s, u = a.canReload ? "Click to reload" : String(a.reason ?? "").trim() || "Payload read-only";
    return {
      canReload: !!a.canReload,
      disabled: !a.canReload,
      value: c,
      hint: u,
      title: a.canReload ? `Reload ${s}` : u
    };
  }
  _getSummaryChips(e = ((n) => ((i) => (i = (n = this.item).getCombatProfile) == null ? void 0 : i.call(n))())() ?? null) {
    var r, s, o;
    if (!e) return [];
    const a = [
      { label: "Category", value: String(e.category ?? "").trim() || "Ranged" },
      { label: "Skill", value: ((r = e.skillDef) == null ? void 0 : r.label) ?? e.skill ?? "Firearms" },
      { label: "DV", value: String(Number(e.damage ?? 0)) },
      { label: "AP", value: String(Number(e.ap ?? 0)) },
      { label: "Type", value: In(e.damageType) || "Penetrating" },
      { label: "Track", value: e.damageTrack === "fatigue" ? "Fatigue" : "Physical" },
      { label: "Range", value: String(((s = e.range) == null ? void 0 : s.max) ?? "near").trim() || "Near" }
    ];
    return String(e.category ?? "").trim().toLowerCase() !== "melee" && a.push((o = e == null ? void 0 : e.sourceState) != null && o.isTracked ? { label: "Payload", value: `${(e == null ? void 0 : e.payloadLabel) || "Unloaded"} (${Number(e.sourceState.current ?? 0)}/${Number(e.sourceState.max ?? 0)})` } : { label: "Payload", value: (e == null ? void 0 : e.payloadLabel) || "Unloaded" }), String(e.availability ?? "").trim() && a.push({ label: "Avail", value: String(e.availability).trim() }), a;
  }
  static async _onAttackWeapon(e) {
    var i, a, r, s;
    (i = e == null ? void 0 : e.preventDefault) == null || i.call(e), (a = e == null ? void 0 : e.stopPropagation) == null || a.call(e), !(!(this.item.actor ?? null) || !((s = (r = this.item).isPersonalWeapon) != null && s.call(r))) && await Vl({ weapon: this.item, event: e });
  }
  static async _onReloadWeaponPayload(e) {
    var i, a, r, s, o, l, c, u;
    if ((i = e == null ? void 0 : e.preventDefault) == null || i.call(e), (a = e == null ? void 0 : e.stopPropagation) == null || a.call(e), !((s = (r = this.item) == null ? void 0 : r.isPersonalWeapon) != null && s.call(r))) return;
    (o = this._captureScrollPositions) == null || o.call(this);
    const n = await ((c = (l = this.item).reloadActivePayload) == null ? void 0 : c.call(l));
    n != null && n.ok || (u = ui.notifications) == null || u.warn((n == null ? void 0 : n.reason) ?? "Unable to reload that weapon.");
  }
  _onRender(e, n) {
    var a, r;
    (a = super._onRender) == null || a.call(this, e, n), (r = this._getRootElement) == null || r.call(this);
  }
};
F(Ss, "LAYOUT_ID", "personal-weapon"), F(Ss, "PARTS", {
  sheet: {
    template: `${ht}/v2/item/personal-weapon-root.hbs`,
    scrollable: [".sheet-body"]
  }
});
let Cg = Ss;
class Ig extends vl {
}
F(Ig, "LAYOUT_ID", "mech-weapon"), F(Ig, "PARTS", {
  sheet: {
    template: `${ht}/v2/item/mech-weapon-root.hbs`,
    scrollable: [".sheet-body"]
  }
});
class fC extends wi {
  async _prepareContext(e) {
    const n = await super._prepareContext(e), i = this.item.system ?? {}, a = i.profile ?? {};
    return n.payloadEditor = {
      familiesText: Array.isArray(i.families) ? i.families.join(", ") : String(i.families ?? ""),
      tagsText: Array.isArray(i.tags) ? i.tags.join(", ") : String(i.tags ?? ""),
      traitsText: Array.isArray(a.traits) ? a.traits.join(", ") : String(a.traits ?? ""),
      keywordsText: Array.isArray(a.keywords) ? a.keywords.join(", ") : String(a.keywords ?? ""),
      familyOptions: Au(sh()),
      tagOptions: Au(oh()),
      damageTypes: [
        { value: "", label: "Weapon Default" },
        ...Vo.map((r) => ({
          value: r.value,
          label: In(r.value)
        }))
      ],
      capabilityOptions: OA,
      resolverKeys: [
        { value: "standard", label: "Standard" },
        { value: "template", label: "Template" }
      ],
      templateShapes: Wg,
      templatePlacements: Vg,
      areaEffectKinds: [
        { value: kt.none, label: "None" },
        { value: kt.discrete, label: "Discrete" },
        { value: kt.persistent, label: "Persistent" }
      ],
      exposureTiers: [
        { value: Te.none, label: "None" },
        { value: Te.minor, label: "Minor" },
        { value: Te.major, label: "Major" },
        { value: Te.full, label: "Full" }
      ]
    }, n.itemSheet = {
      ...n.itemSheet ?? {},
      summaryChips: [
        { label: "Qty", value: String(i.quantity ?? 0) },
        { label: "Families", value: n.payloadEditor.familiesText || "None" },
        { label: "Tags", value: n.payloadEditor.tagsText || "None" }
      ]
    }, n;
  }
}
F(fC, "PARTS", {
  sheet: {
    template: `${ht}/v2/item/weapon-payload.hbs`,
    scrollable: [".sheet-body"]
  }
});
const fH = {
  penetrating: "Penetrating",
  concussive: "Concussive",
  energy: "Energy",
  thermal: "Thermal",
  electrical: "Electrical"
};
function HS(t) {
  const e = Number(t ?? 0) || 0;
  return e > 0 ? `+${e}` : `${e}`;
}
function gH({ defenseBonus: t = 0, mitigationByType: e = {} } = {}) {
  const n = [], i = Number(t ?? 0) || 0;
  i !== 0 && n.push(`Defense ${HS(i)}`);
  const a = Ki(e);
  for (const [r, s] of Object.entries(fH)) {
    const o = Number((a == null ? void 0 : a[r]) ?? 0) || 0;
    o !== 0 && n.push(`${s} ${HS(o)}`);
  }
  return n.join(" | ");
}
function _m(t = 0, e = 0) {
  return `${Number(t ?? 0)}/${Number(e ?? 0)}`;
}
function pH(t = "") {
  const e = String(t ?? "").trim();
  return e ? e.charAt(0).toUpperCase() + e.slice(1) : "";
}
class Eg extends wi {
  static get DEFAULT_OPTIONS() {
    return foundry.utils.mergeObject(super.DEFAULT_OPTIONS, {
      position: {
        width: 960,
        height: 860
      },
      window: {
        ...super.DEFAULT_OPTIONS.window,
        minWidth: 920,
        minHeight: 760
      }
    }, { inplace: !1 });
  }
  async _prepareContext(e) {
    var c, u, d, f, m, g, p, h, b, y, S, A, T, C, k, w, v, N, P;
    const n = await super._prepareContext(e), i = this.item, a = i.actor ?? null, r = ((c = a == null ? void 0 : a.getPersonalCombatLoadout) == null ? void 0 : c.call(a)) ?? null, s = ((u = r == null ? void 0 : r.activeArmor) == null ? void 0 : u.id) ?? null, o = ((d = r == null ? void 0 : r.activeArmor) == null ? void 0 : d.id) === i.id ? r.activeArmor : (f = i.getArmorProfile) == null ? void 0 : f.call(i, { actor: a }), l = Ai((o == null ? void 0 : o.battleArmor) ?? ((m = i.system) == null ? void 0 : m.battleArmor));
    return n.armorState = o, n.isActiveArmor = s === i.id, n.battleArmorEnabled = ki(l), n.effectiveDurabilityCurrent = Number(
      ((g = o == null ? void 0 : o.durability) == null ? void 0 : g.current) ?? ((h = (p = i.system) == null ? void 0 : p.durability) == null ? void 0 : h.current) ?? ((y = (b = i.system) == null ? void 0 : b.durability) == null ? void 0 : y.max) ?? ((S = i.system) == null ? void 0 : S.rating) ?? 0
    ), n.effectiveDurabilityMax = Number(
      ((A = o == null ? void 0 : o.durability) == null ? void 0 : A.max) ?? ((T = i.system) == null ? void 0 : T.rating) ?? 0
    ), n.effectiveArmorRating = Number(
      (o == null ? void 0 : o.currentArmorRating) ?? (o == null ? void 0 : o.ratingCurrent) ?? Math.min(
        Number(((C = i.system) == null ? void 0 : C.rating) ?? 0),
        Number(((w = (k = i.system) == null ? void 0 : k.durability) == null ? void 0 : w.current) ?? ((N = (v = i.system) == null ? void 0 : v.durability) == null ? void 0 : N.max) ?? ((P = i.system) == null ? void 0 : P.rating) ?? 0)
      )
    ), n.effectiveArmorResist = Number(
      (o == null ? void 0 : o.baseMitigation) ?? (o == null ? void 0 : o.baseResistance) ?? 0
    ), n.armorModifierSummary = this._getArmorModifierSummary(o), n.armorHeroStats = this._getHeroStats(o, l), n.itemSheet = { ...n.itemSheet ?? {} }, n.itemSheet.summaryChips = this._getSummaryChips(o, l), n.armorEditor = {
      standardTraits: [...aE]
    }, n;
  }
  _getBattleArmorStats(e = {}) {
    var i, a, r, s;
    const n = Ai(e);
    return [
      {
        label: "Structure",
        value: _m((i = n.structure) == null ? void 0 : i.value, (a = n.structure) == null ? void 0 : a.max)
      },
      {
        label: "Resist",
        value: String(ml(n))
      },
      {
        label: "Armor Pool",
        value: _m((r = n.armorPool) == null ? void 0 : r.value, (s = n.armorPool) == null ? void 0 : s.max)
      },
      {
        label: "State",
        value: pH(n.state)
      }
    ];
  }
  _getHeroStats(e = null, n = null) {
    var r, s, o, l, c;
    const i = this.item.system ?? {}, a = Ai(n ?? (e == null ? void 0 : e.battleArmor) ?? i.battleArmor);
    return ki(a) ? this._getBattleArmorStats(a) : [
      {
        label: "Rating",
        value: String(Number(
          (e == null ? void 0 : e.ratingCurrent) ?? (e == null ? void 0 : e.currentArmorRating) ?? Math.min(
            Number(i.rating ?? 0),
            Number(((r = i.durability) == null ? void 0 : r.current) ?? ((s = i.durability) == null ? void 0 : s.max) ?? i.rating ?? 0)
          )
        ))
      },
      {
        label: "Resist",
        value: String(Number((e == null ? void 0 : e.baseMitigation) ?? (e == null ? void 0 : e.baseResistance) ?? 0))
      },
      { label: "Defense", value: String(Number(i.defenseBonus ?? 0)) },
      {
        label: "Durability",
        value: _m(
          ((o = e == null ? void 0 : e.durability) == null ? void 0 : o.current) ?? ((l = i.durability) == null ? void 0 : l.current) ?? i.rating ?? 0,
          ((c = e == null ? void 0 : e.durability) == null ? void 0 : c.max) ?? i.rating ?? 0
        )
      }
    ];
  }
  _getSummaryChips(e = null, n = null) {
    const i = this.item.system ?? {}, a = this._getHeroStats(e, n), r = String(i.availability ?? "").trim();
    return r && a.push({ label: "Avail", value: r }), a;
  }
  _getArmorModifierSummary(e = null) {
    const n = this.item.system ?? {};
    return gH({
      defenseBonus: (e == null ? void 0 : e.defenseBonus) ?? n.defenseBonus ?? 0,
      mitigationByType: (e == null ? void 0 : e.mitigationByType) ?? (e == null ? void 0 : e.typedMitigation) ?? n.mitigationByType ?? {}
    });
  }
  _onRender(e, n) {
    var a, r;
    (a = super._onRender) == null || a.call(this, e, n);
    const i = (r = this._getRootElement) == null ? void 0 : r.call(this);
    i && (i.querySelectorAll(".mwd-armor-standard-trait-add").forEach((s) => {
      s.addEventListener("click", (o) => {
        var l, c;
        o.preventDefault(), (c = (l = this.item).createArmorStandardTrait) == null || c.call(l);
      });
    }), i.querySelectorAll(".mwd-armor-standard-trait-delete").forEach((s) => {
      s.addEventListener("click", (o) => {
        var l, c;
        o.preventDefault(), (c = (l = this.item).deleteArmorStandardTrait) == null || c.call(l, s.dataset.traitId);
      });
    }), i.querySelectorAll(".mwd-armor-standard-trait-field").forEach((s) => {
      s.addEventListener("change", (o) => {
        var l, c;
        o.preventDefault(), (c = (l = this.item).updateArmorStandardTrait) == null || c.call(
          l,
          s.dataset.traitId,
          s.dataset.field,
          s.value
        );
      });
    }));
  }
}
F(Eg, "LAYOUT_ID", "armor"), F(Eg, "PARTS", {
  sheet: {
    template: `${ht}/v2/item/armor-root.hbs`,
    scrollable: [".sheet-body"]
  }
});
function hH() {
  console.log(`${tt}Registering Item sheets (V2)`);
  const { Items: t } = foundry.documents.collections;
  t.registerSheet(I, Sg, { types: ["contact"], makeDefault: !0, label: "Contact (V2)" }), t.registerSheet(I, Ag, { types: ["gear", "consumable"], makeDefault: !0, label: "Gear / Consumable (V2)" }), t.registerSheet(I, kg, { types: ["quality"], makeDefault: !0, label: "Quality (V2)" }), t.registerSheet(I, Tg, { types: ["assetModule"], makeDefault: !0, label: "Asset Module (V2)" }), t.registerSheet(I, wg, { types: ["lifeModule"], makeDefault: !0, label: "Life Module (V2)" }), t.registerSheet(I, Mg, { types: ["skill"], makeDefault: !0, label: "Skill (V2)" }), t.registerSheet(I, Cg, { types: ["personalWeapon", "weapon"], makeDefault: !0, label: "Personal Weapon (V2)" }), t.registerSheet(I, Ig, { types: ["mechWeapon"], makeDefault: !0, label: "Mech Weapon (V2)" }), t.registerSheet(I, fC, { types: ["weaponPayload"], makeDefault: !0, label: "Weapon Payload (V2)" }), t.registerSheet(I, Eg, { types: ["armor"], makeDefault: !0, label: "Armor (V2)" });
}
const jS = [
  // UI (CSB render entry point + node types)
  `systems/${I}/templates/v2/ui/layout-root.hbs`,
  `systems/${I}/templates/v2/ui/nodes/hexabox.hbs`,
  `systems/${I}/templates/v2/ui/nodes/stack.hbs`,
  `systems/${I}/templates/v2/ui/nodes/panel.hbs`,
  `systems/${I}/templates/v2/ui/nodes/include.hbs`,
  `systems/${I}/templates/v2/ui/nodes/tabs.hbs`,
  `systems/${I}/templates/v2/ui/nodes/accordion.hbs`,
  `systems/${I}/templates/v2/ui/nodes/unknown.hbs`,
  `systems/${I}/templates/common/actor-reference.hbs`,
  `systems/${I}/templates/common/view-mode.hbs`,
  `systems/${I}/templates/common/label.hbs`,
  `systems/${I}/templates/common/enum-value-label.hbs`,
  `systems/${I}/templates/common/damage-code.hbs`,
  `systems/${I}/templates/common/damage-armor.hbs`,
  `systems/${I}/templates/v2/ui/mod-stepper.hbs`,
  `systems/${I}/templates/v2/ui/condition-monitors.hbs`,
  `systems/${I}/templates/v2/ui/combat-awareness-preview.hbs`,
  `systems/${I}/templates/v2/roll/_mwd-roll-card.hbs`,
  `systems/${I}/templates/v2/roll/_mwd-damage-application-card.hbs`,
  `systems/${I}/templates/v2/roll/_mwd-hazard-card.hbs`,
  `systems/${I}/templates/v2/components/checkbox.hbs`,
  `systems/${I}/templates/v2/components/radio.hbs`,
  // Character UI
  `systems/${I}/templates/v2/ui/character/attributes.hbs`,
  `systems/${I}/templates/v2/ui/character/combat-actions.hbs`,
  `systems/${I}/templates/v2/ui/character/battle-armor-panel.hbs`,
  `systems/${I}/templates/v2/ui/character/skills-column.hbs`,
  `systems/${I}/templates/v2/ui/character/skill-row.hbs`,
  `systems/${I}/templates/v2/ui/character/edge-console.hbs`,
  `systems/${I}/templates/v2/ui/character/burn-monitor.hbs`,
  `systems/${I}/templates/v2/ui/character/status-dashboard.hbs`,
  `systems/${I}/templates/v2/ui/character/active-criticals.hbs`,
  `systems/${I}/templates/v2/ui/character/inventory-section.hbs`,
  `systems/${I}/templates/v2/ui/character/inventory-record.hbs`,
  `systems/${I}/templates/v2/ui/character/bio-identity.hbs`,
  `systems/${I}/templates/v2/ui/character/bio-history.hbs`,
  `systems/${I}/templates/v2/ui/character/advancement.hbs`,
  `systems/${I}/templates/v2/ui/character/knowledge-skills.hbs`,
  `systems/${I}/templates/v2/ui/vehicle/summary-bar.hbs`,
  `systems/${I}/templates/v2/ui/vehicle/quick-actions.hbs`,
  `systems/${I}/templates/v2/ui/vehicle/attributes-grid.hbs`,
  `systems/${I}/templates/v2/ui/vehicle/movement-grid.hbs`,
  `systems/${I}/templates/v2/ui/vehicle/movement-profile.hbs`,
  `systems/${I}/templates/v2/ui/vehicle/signature-panel.hbs`,
  `systems/${I}/templates/v2/ui/vehicle/strain-track.hbs`,
  `systems/${I}/templates/v2/ui/vehicle/degradation-grid.hbs`,
  `systems/${I}/templates/v2/ui/vehicle/active-crits.hbs`,
  `systems/${I}/templates/v2/ui/vehicle/record-section.hbs`,
  `systems/${I}/templates/v2/ui/vehicle/ew-panel.hbs`,
  `systems/${I}/templates/v2/ui/battlemech/chassis-fields.hbs`,
  `systems/${I}/templates/v2/ui/battlemech/fire-mode.hbs`,
  `systems/${I}/templates/v2/ui/battlemech/heat-track.hbs`,
  `systems/${I}/templates/v2/ui/battlemech/quick-actions.hbs`,
  `systems/${I}/templates/v2/ui/battlemech/weapon-groups.hbs`,
  `systems/${I}/templates/v2/ui/battlemech/hardpoints.hbs`,
  `systems/${I}/templates/v2/ui/battlemech/loadout-hardpoints.hbs`,
  `systems/${I}/templates/v2/ui/battlemech/ew-status.hbs`,
  `systems/${I}/templates/v2/ui/vehicle/pilot-panel.hbs`,
  `systems/${I}/templates/v2/ui/vehicle/crew-panel.hbs`,
  `systems/${I}/templates/v2/ui/character/assigned-mech.hbs`,
  // Sheet wrapper
  `systems/${I}/templates/v2/actor/_sheet-root.hbs`,
  `systems/${I}/templates/v2/actor/npc-sheet.hbs`,
  `systems/${I}/templates/v2/actor/vehicle-sheet.hbs`,
  `systems/${I}/templates/v2/actor/battlemech-sheet.hbs`,
  // Placeholders
  `systems/${I}/templates/v2/ui/placeholders/assigned-systems.hbs`,
  `systems/${I}/templates/v2/ui/placeholders/inventory-gear.hbs`,
  `systems/${I}/templates/v2/ui/placeholders/inventory-consumables.hbs`,
  `systems/${I}/templates/v2/ui/placeholders/inventory-armor.hbs`,
  `systems/${I}/templates/v2/ui/placeholders/inventory-weapons.hbs`,
  `systems/${I}/templates/v2/ui/placeholders/bio-description.hbs`,
  `systems/${I}/templates/v2/ui/placeholders/bio-traits.hbs`,
  `systems/${I}/templates/v2/ui/placeholders/bio-life-modules.hbs`,
  `systems/${I}/templates/v2/ui/actor/field-grid.hbs`,
  `systems/${I}/templates/v2/ui/actor/owned-item-list.hbs`,
  `systems/${I}/templates/v2/ui/actor/action-buttons.hbs`,
  `systems/${I}/templates/v2/ui/actor/notes-editor.hbs`,
  `systems/${I}/templates/v2/ui/actor/hardpoint-list.hbs`,
  `systems/${I}/templates/v2/ui/actor/weapon-group-list.hbs`,
  // V2 item partials
  `systems/${I}/templates/v2/item/_item-sheet-root.hbs`,
  `systems/${I}/templates/v2/item/contact.hbs`,
  `systems/${I}/templates/v2/item/gear.hbs`,
  `systems/${I}/templates/v2/item/consumable.hbs`,
  `systems/${I}/templates/v2/item/assetModule.hbs`,
  `systems/${I}/templates/v2/item/skill.hbs`,
  `systems/${I}/templates/v2/item/lifeModule.hbs`,
  `systems/${I}/templates/v2/item/quality.hbs`,
  `systems/${I}/templates/v2/item/personal-weapon-root.hbs`,
  `systems/${I}/templates/v2/item/mech-weapon-root.hbs`,
  `systems/${I}/templates/v2/item/weapon-payload.hbs`,
  `systems/${I}/templates/v2/item/armor-root.hbs`,
  `systems/${I}/templates/v2/item/parts/itemname.hbs`,
  `systems/${I}/templates/v2/item/parts/inactive.hbs`,
  `systems/${I}/templates/v2/item/parts/references.hbs`,
  `systems/${I}/templates/v2/item/parts/gear-main.hbs`,
  `systems/${I}/templates/v2/item/parts/consumable-main.hbs`,
  `systems/${I}/templates/v2/item/parts/asset-module-main.hbs`,
  `systems/${I}/templates/v2/item/parts/skill-main.hbs`,
  `systems/${I}/templates/v2/item/parts/life-module-main.hbs`,
  `systems/${I}/templates/v2/item/parts/quality-main.hbs`,
  `systems/${I}/templates/v2/item/parts/quality-limits.hbs`,
  `systems/${I}/templates/v2/item/parts/quality-prerequisites.hbs`,
  `systems/${I}/templates/v2/item/parts/quality-effects.hbs`,
  `systems/${I}/templates/v2/item/parts/modifier.hbs`,
  `systems/${I}/templates/v2/item/parts/modifiers.hbs`,
  `systems/${I}/templates/v2/item/parts/weapon-main.hbs`,
  `systems/${I}/templates/v2/item/parts/weapon-compact-main.hbs`,
  `systems/${I}/templates/v2/item/parts/weapon-summary-hero.hbs`,
  `systems/${I}/templates/v2/item/parts/weapon-combat-essentials.hbs`,
  `systems/${I}/templates/v2/item/parts/weapon-standard-traits.hbs`,
  `systems/${I}/templates/v2/item/parts/weapon-usage-bands.hbs`,
  `systems/${I}/templates/v2/item/parts/weapon-ammo-profiles.hbs`,
  `systems/${I}/templates/v2/item/parts/weapon-consumption-sources.hbs`,
  `systems/${I}/templates/v2/item/parts/weapon-notes-reference.hbs`,
  `systems/${I}/templates/v2/item/parts/armor-main.hbs`,
  `systems/${I}/templates/v2/item/parts/armor-summary-hero.hbs`,
  `systems/${I}/templates/v2/item/parts/armor-combat-essentials.hbs`,
  `systems/${I}/templates/v2/item/parts/armor-usage-protection.hbs`,
  `systems/${I}/templates/v2/item/parts/armor-resistance-modifiers.hbs`,
  `systems/${I}/templates/v2/item/parts/armor-identity-fields.hbs`,
  `systems/${I}/templates/v2/item/parts/armor-standard-traits.hbs`,
  `systems/${I}/templates/v2/item/parts/armor-notes-reference.hbs`,
  `systems/${I}/templates/v2/item/parts/item-effects.hbs`,
  // Actors
  `systems/${I}/templates/v2/actor/character-sheet.hbs`,
  `systems/${I}/templates/v2/actor/vehicle-sheet.hbs`,
  `systems/${I}/templates/v2/actor/battlemech-sheet.hbs`
];
function bH(t) {
  const e = String(t).replaceAll("\\", "/"), n = `systems/${I}/templates/`, i = e.indexOf(n);
  return `mwd.${(i >= 0 ? e.slice(i + n.length) : e).replace(/\.hbs$/i, "").split("/").filter(Boolean).map((o) => o.replace(/^_+/, "")).join(".")}`;
}
function yH() {
  var t, e;
  return ((e = (t = foundry == null ? void 0 : foundry.applications) == null ? void 0 : t.handlebars) == null ? void 0 : e.Handlebars) ?? Handlebars;
}
async function SH() {
  var e, n;
  const t = yH();
  try {
    const i = {};
    for (const r of jS)
      i[bH(r)] = r, i[r] = r;
    await foundry.applications.handlebars.loadTemplates(i);
    const a = "mwd.v2.ui.layout-root";
    if (!((e = Handlebars.partials) != null && e[a])) {
      const r = Object.keys(Handlebars.partials ?? {});
      throw console.error("Missing required partial:", a), console.error("Closest matches:", r.filter((s) => s.includes("layout-root"))), new Error(`Template preload failed: ${a} not registered`);
    }
    if (t !== Handlebars) {
      for (const [r, s] of Object.entries(t.partials ?? {}))
        if (!((n = Handlebars.partials) != null && n[r]))
          try {
            Handlebars.registerPartial(r, s);
          } catch {
          }
    }
    console.log(`${tt}preloadTemplatesV2 OK`, { loaded: jS.length });
  } catch (i) {
    throw console.error(`${tt}preloadTemplatesV2 FAILED`, i), i;
  }
}
function GS(t) {
  const e = Math.max(0, Number(t) || 0);
  return -Math.floor(e / 3);
}
function AH(t) {
  const e = Math.max(0, Number(t) || 0);
  return e === 0 ? 0 : Math.ceil(e / 4);
}
function qS(t = {}) {
  const e = t.physical ?? {}, n = t.fatigue ?? {}, i = t.armor ?? {}, a = Number(e.value) || 0, r = Number(n.value) || 0, s = Math.max(Number(i.value) || 0, Number(i.max) || 0);
  return {
    physical: { penalty: GS(a) },
    fatigue: { penalty: GS(r) },
    armor: { resistance: AH(s) }
  };
}
const Bm = {
  penaltyPer3Damage: (t) => {
    const e = Math.max(0, Number(t) || 0);
    return -Math.floor(e / 3);
  },
  resistancePerQuarter: (t) => {
    const e = Math.max(0, Number(t) || 0);
    return e === 0 ? 0 : Math.ceil(e / 4);
  }
  // heatPenaltyCurve: ...
};
function kH(t, e, n, i) {
  const a = t.system ?? {}, r = `monitors.${e}`, s = Number(foundry.utils.getProperty(a, `${r}.max`)) || 0, o = Number(foundry.utils.getProperty(a, `${r}.value`)) || 0;
  switch (n) {
    case "value":
      return i;
    case "armorPersonalBase":
      return i;
    case "mechArmorBase":
      return Math.max(i, s, o);
    case "vehicleArmorBase":
      return Math.max(i, s, o);
    default:
      return i;
  }
}
function TH(t = {}) {
  return Object.entries(Ki(t)).filter(([, e]) => Number(e) > 0).map(([e, n]) => `${e} +${n}`).join(", ");
}
class wH extends Actor {
  /** @override */
  async _preCreate(e, n, i) {
    var o;
    super._preCreate && await super._preCreate(e, n, i);
    const a = await g0("Actor", (e == null ? void 0 : e.type) ?? this.type), r = {};
    a.system && Object.keys(a.system).length && (r.system = foundry.utils.mergeObject(
      foundry.utils.deepClone(a.system),
      foundry.utils.deepClone((e == null ? void 0 : e.system) ?? this.system ?? {}),
      { inplace: !1, recursive: !0, overwrite: !0 }
    )), a.prototypeToken && (r.prototypeToken = foundry.utils.mergeObject(
      foundry.utils.deepClone(a.prototypeToken),
      foundry.utils.deepClone((e == null ? void 0 : e.prototypeToken) ?? this.prototypeToken ?? {}),
      { inplace: !1, recursive: !0, overwrite: !0 }
    )), Object.keys(r).length && this.updateSource(r);
    const s = (e == null ? void 0 : e.type) ?? this.type;
    Ta(s) && !((o = this.getFlag) != null && o.call(this, "mwd", zb)) && this.updateSource({ [`flags.mwd.${zb}`]: xR });
  }
  /* -------------------------------------------- */
  /* Base & Derived Data                           */
  /* -------------------------------------------- */
  /** @override */
  prepareBaseData() {
    var e, n;
    if (super.prepareBaseData(), this.isCharacterLike()) {
      const i = this.system ?? {};
      if (LE(i), this.type === "character" && kF(i), i.speed = Math.max(0, Math.trunc(Number(i.speed ?? 12) || 12)), (e = i.skills) != null && e.skills && typeof i.skills.skills == "object") {
        for (const [a, r] of Object.entries(i.skills.skills))
          (n = i.skills)[a] ?? (n[a] = r);
        delete i.skills.skills;
      }
    }
    this._prepareEdgePoolsBase();
  }
  /** @override */
  prepareDerivedData() {
    super.prepareDerivedData(), this._prepareEdgePoolsDerived(), this._prepareMonitors(), this._preparePersonalCombatDerived();
  }
  /**
   * Base-data prep for Edge pools:
   * - Ensure numeric rating/value where present
   * - Initialize value ONLY if missing/invalid (NOT if 0)
   * - Remove legacy keys (max)
   * - No clamping, no "start full" behavior
   */
  _prepareEdgePoolsBase() {
    var n, i;
    if (this.type !== "character") return;
    const e = (i = (n = this.system) == null ? void 0 : n.counters) == null ? void 0 : i.edgePools;
    if (!(!e || typeof e != "object"))
      for (const a of Object.values(e)) {
        if (!a || typeof a != "object") continue;
        a.rating = Math.max(0, Number(a.rating ?? 0));
        const r = Object.prototype.hasOwnProperty.call(a, "value"), s = Number(a.value);
        (!r || !Number.isFinite(s)) && (a.value = a.rating), "max" in a && delete a.max;
      }
  }
  /**
   * Derived-data prep for Edge pools:
   * - Computes cap/effectiveMax/effectiveValue
   * - Stores in a non-persisted cache on the actor instance
   * - No writes to system data (prevents UI snap-back / loops)
   */
  _prepareEdgePoolsDerived() {
    var i, a;
    this._mwdDerived ?? (this._mwdDerived = {}), this._mwdDerived.edgePools = null;
    const e = this.getEdgeCap(), n = this.type === "character" ? qa(this).bonusByEdgePool ?? {} : {};
    if (this.type === "character" && this.hasEdgePools()) {
      const r = ((a = (i = this.system) == null ? void 0 : i.counters) == null ? void 0 : a.edgePools) ?? {}, s = {};
      for (const [o, l] of Object.entries(r)) {
        const c = Math.max(0, Number((l == null ? void 0 : l.rating) ?? 0)), u = Math.max(0, Number((l == null ? void 0 : l.value) ?? 0)), d = Math.max(0, Number((n == null ? void 0 : n[o]) ?? 0)), f = c + d, m = Math.min(f, e), g = Math.min(u, m);
        s[o] = {
          key: o,
          rating: c,
          ratingBonus: d,
          effectiveRating: f,
          value: u,
          cap: e,
          effectiveMax: m,
          effectiveValue: g,
          hasPools: !0,
          isEmpty: g <= 0,
          isCapped: f > e
        };
      }
      this._mwdDerived.edgePools = { cap: e, pools: s };
      return;
    }
    this._mwdDerived.edgePools = { cap: e, pools: {} };
  }
  /* -------------------------------------------- */
  /* Capabilities                                  */
  /* -------------------------------------------- */
  isCharacterLike() {
    return this.type === "character" || this.type === "npc";
  }
  hasSkills() {
    return this.type === "character" || this.type === "npc";
  }
  hasEdgePools() {
    var e, n;
    return this.type === "character" && !!((n = (e = this.system) == null ? void 0 : e.counters) != null && n.edgePools);
  }
  getAttributeValue(e) {
    var r, s, o, l, c, u, d;
    const n = Number(((o = (s = (r = this.system) == null ? void 0 : r.attributes) == null ? void 0 : s[e]) == null ? void 0 : o.value) ?? 0) || 0, i = this.isCharacterLike() && Number(((l = Zg(this).attributeModifiers) == null ? void 0 : l[e]) ?? 0) || 0, a = this.isCharacterLike() && e === "strength" && Number(((d = (u = (c = this.system) == null ? void 0 : c.derived) == null ? void 0 : u.battleArmor) == null ? void 0 : d.enhancedStrengthBonus) ?? 0) || 0;
    return Math.max(0, n + i + a);
  }
  getSkillRating(e) {
    var n, i, a;
    return Math.max(0, Number(((a = (i = (n = this.system) == null ? void 0 : n.skills) == null ? void 0 : i[e]) == null ? void 0 : a.rating) ?? 0));
  }
  getOwnedItem(e) {
    var n, i;
    return ((i = (n = this.items) == null ? void 0 : n.get) == null ? void 0 : i.call(n, e)) ?? null;
  }
  async setCheckbarValue(e, n) {
    return await this.update({ [e]: n });
  }
  getPersonalCombatLoadout({ refresh: e = !1 } = {}) {
    var i;
    if (!e) {
      const a = (i = this._mwdDerived) == null ? void 0 : i.personalCombat;
      if (a) return a;
    }
    const n = this._computePersonalCombatLoadout();
    return this._mwdDerived ?? (this._mwdDerived = {}), this._mwdDerived.personalCombat = n, n;
  }
  _computePersonalCombatLoadout() {
    const e = [], n = this.items.filter((m) => {
      var g;
      return ((g = m.isPersonalWeapon) == null ? void 0 : g.call(m)) ?? m.type === M.itemType.personalWeapon;
    }).map((m) => {
      var g;
      return ((g = m.getCombatProfile) == null ? void 0 : g.call(m)) ?? null;
    }).filter(Boolean), i = this.items.filter((m) => {
      var g;
      return ((g = m.isArmor) == null ? void 0 : g.call(m)) ?? m.type === M.itemType.armor;
    }).map((m) => {
      var g;
      return ((g = m.getArmorProfile) == null ? void 0 : g.call(m, { actor: this })) ?? null;
    }).filter(Boolean), a = n.filter((m) => m.equipped), r = i.filter((m) => m.equipped), s = a.filter((m) => m.isPrimary), o = r.filter((m) => m.isPrimary);
    let l = null, c = null, u = !1;
    s.length === 1 ? (c = s[0], l = c) : s.length > 1 ? (e.push("Multiple equipped primary weapons found; attack selection requires a chooser."), u = !0) : a.length === 1 ? l = a[0] : a.length > 1 ? u = !0 : l = li.buildDefaultUnarmedProfile(this);
    let d = null, f = null;
    return o.length === 1 ? (d = o[0], f = this._buildActiveArmorState(d)) : o.length > 1 ? (e.push("Multiple equipped primary armor items found; using the first equipped armor."), f = r[0] ? this._buildActiveArmorState(r[0]) : null) : r.length === 1 ? f = this._buildActiveArmorState(r[0]) : r.length > 1 && (e.push("Multiple equipped armor items found without a single primary; using the first equipped armor."), f = this._buildActiveArmorState(r[0])), {
      weapons: n,
      equippedWeapons: a,
      primaryWeapon: c,
      defaultWeapon: l,
      weaponChoiceRequired: u,
      armor: i,
      equippedArmor: r,
      primaryArmor: d,
      activeArmor: f,
      warnings: e
    };
  }
  _buildActiveArmorState(e) {
    var u, d;
    if (!e) return null;
    const n = Math.max(0, Number(((u = e == null ? void 0 : e.durability) == null ? void 0 : u.max) ?? (e == null ? void 0 : e.rating) ?? 0)), i = Math.min(
      n,
      Math.max(0, Number(((d = e == null ? void 0 : e.durability) == null ? void 0 : d.current) ?? (e == null ? void 0 : e.remainingDurability) ?? (e == null ? void 0 : e.currentArmorRating) ?? n))
    ), a = Math.max(0, Number((e == null ? void 0 : e.rating) ?? 0)), r = Math.min(a, i), s = Ki(e == null ? void 0 : e.mitigationByType), o = ep(r), l = Ai(e == null ? void 0 : e.battleArmor), c = ki(l);
    return {
      ...e,
      armorId: e.id,
      remainingDurability: i,
      currentArmorRating: c ? 0 : r,
      baseMitigation: c ? 0 : o,
      baseResistance: c ? 0 : o,
      mitigationByType: c ? Ki({}) : s,
      typedMitigation: c ? Ki({}) : s,
      ratingCurrent: c ? 0 : r,
      isDestroyed: c ? l.state === "wrecked" : i <= 0,
      durability: {
        current: i,
        max: n
      },
      traitEffects: e.traitEffects,
      effects: e.effects,
      battleArmor: l
    };
  }
  async setOwnedItemEquipped(e, n) {
    var a, r, s;
    const i = this.getOwnedItem(e);
    return !i || !((a = i.isPersonalWeapon) != null && a.call(i) || (r = i.isArmor) != null && r.call(i)) ? null : this.updateEmbeddedDocuments("Item", [{
      _id: i.id,
      "system.equipped": !!n,
      "system.isPrimary": n ? !!((s = i.system) != null && s.isPrimary) : !1
    }]);
  }
  async setOwnedItemPrimary(e, n) {
    var s, o, l, c;
    const i = this.getOwnedItem(e);
    if (!i || !((s = i.isPersonalWeapon) != null && s.call(i) || (o = i.isArmor) != null && o.call(i))) return null;
    const a = [], r = !!n;
    if (r)
      for (const u of this.items.filter((d) => d.type === i.type && d.id !== i.id))
        (l = u.system) != null && l.isPrimary && a.push({ _id: u.id, "system.isPrimary": !1 });
    return a.push({
      _id: i.id,
      "system.isPrimary": r,
      "system.equipped": r ? !0 : !!((c = i.system) != null && c.equipped)
    }), this.updateEmbeddedDocuments("Item", a);
  }
  /* -------------------------------------------- */
  /* Edge API (cap + pools w/ rating + current)    */
  /* -------------------------------------------- */
  getEdgeCap() {
    var e, n, i;
    return Math.max(0, Number(((i = (n = (e = this.system) == null ? void 0 : e.attributes) == null ? void 0 : n.edge) == null ? void 0 : i.value) ?? 0));
  }
  getEdgePoolRaw(e) {
    var n, i, a;
    return ((a = (i = (n = this.system) == null ? void 0 : n.counters) == null ? void 0 : i.edgePools) == null ? void 0 : a[e]) ?? null;
  }
  /**
   * Canonical pool accessor.
   * - Character: returns raw + effective values (effective is clamped by cap)
   * - NPC: no pools; Edge attribute acts as a single “pool” (effective max/value = cap)
   * - Vehicle/Mech: safe zeros
   */
  getEdgePool(e) {
    var d, f, m, g;
    const n = this.getEdgeCap();
    if (this.type === "npc" && !this.hasEdgePools()) {
      const p = n, h = n;
      return {
        key: e,
        value: h,
        rating: p,
        effectiveValue: h,
        effectiveMax: p,
        cap: n,
        hasPools: !1
      };
    }
    if (!this.hasEdgePools())
      return {
        key: e,
        value: 0,
        rating: 0,
        effectiveValue: 0,
        effectiveMax: 0,
        cap: n,
        hasPools: !1
      };
    const i = (m = (f = (d = this._mwdDerived) == null ? void 0 : d.edgePools) == null ? void 0 : f.pools) == null ? void 0 : m[e];
    if (i)
      return {
        key: i.key,
        value: i.value,
        rating: i.rating,
        ratingBonus: i.ratingBonus,
        effectiveRating: i.effectiveRating,
        effectiveValue: i.effectiveValue,
        effectiveMax: i.effectiveMax,
        cap: i.cap,
        hasPools: !0
      };
    const a = this.getEdgePoolRaw(e), r = Math.max(0, Number((a == null ? void 0 : a.rating) ?? 0)), s = Math.max(0, Number((a == null ? void 0 : a.value) ?? 0)), o = Math.max(0, Number(((g = qa(this).bonusByEdgePool) == null ? void 0 : g[e]) ?? 0)), l = r + o, c = Math.min(l, n), u = Math.min(s, c);
    return {
      key: e,
      value: s,
      rating: r,
      ratingBonus: o,
      effectiveRating: l,
      effectiveValue: u,
      effectiveMax: c,
      cap: n,
      hasPools: !0
    };
  }
  getEdgePoolValue(e) {
    return this.getEdgePool(e).effectiveValue;
  }
  getEdgePoolMax(e) {
    return this.getEdgePool(e).effectiveMax;
  }
  /**
   * Set the CURRENT value for a pool (admin/adjustment or spend).
   * - Characters only (six pools).
   * - Clamps to [0, effectiveMax] where effectiveMax = min(rating, edgeCap).
   * - Does not modify rating.
   */
  async setEdgePoolValue(e, n) {
    var s;
    if (!this.hasEdgePools()) return;
    const i = Math.max(0, Number(((s = this.getEdgePool(e)) == null ? void 0 : s.effectiveMax) ?? 0)), a = Number(n ?? 0), r = Math.max(0, Math.min(a, i));
    return this.update({
      [`system.counters.edgePools.${e}.value`]: r
    });
  }
  /**
   * Convenience: adjust CURRENT value by delta (e.g., spend -1, award +1).
   */
  async adjustEdgePoolValue(e, n) {
    var r;
    if (!this.hasEdgePools()) return;
    const i = Math.max(0, Number(((r = this.getEdgePoolRaw(e)) == null ? void 0 : r.value) ?? 0)), a = Number(n ?? 0);
    return this.setEdgePoolValue(e, i + a);
  }
  /**
   * Set the RATING (advancement ceiling) for a pool.
   * Stores true rating (not cap-clamped), then clamps current value to new effective max.
   */
  async setEdgePoolRating(e, n) {
    var c, u;
    if (!this.hasEdgePools()) return;
    const i = this.getEdgeCap(), a = Math.max(0, Number(n ?? 0)), r = Math.max(0, Number(((c = qa(this).bonusByEdgePool) == null ? void 0 : c[e]) ?? 0)), s = Math.min(a + r, i), o = Math.max(0, Number(((u = this.getEdgePoolRaw(e)) == null ? void 0 : u.value) ?? 0)), l = Math.min(o, s);
    return this.update({
      [`system.counters.edgePools.${e}.rating`]: a,
      [`system.counters.edgePools.${e}.value`]: l
    });
  }
  /**
   * Sheet-facing summary for rendering.
   * If `groups` is provided, returns grouped pool arrays.
   */
  getEdgePoolSummary({ groups: e } = {}) {
    var i, a, r, s;
    const n = this.getEdgeCap();
    if (this.hasEdgePools()) {
      const o = ((a = (i = this._mwdDerived) == null ? void 0 : i.edgePools) == null ? void 0 : a.pools) ?? {};
      if (e && typeof e == "object") {
        const c = Object.entries(e).map(([u, d]) => {
          const f = (d ?? []).map((m) => {
            const g = o[m] ?? this.getEdgePool(m);
            return {
              ...g,
              isEmpty: (g.effectiveValue ?? 0) <= 0,
              isCapped: (g.effectiveRating ?? g.rating ?? 0) > (g.cap ?? n)
            };
          });
          return { id: u, pools: f };
        });
        return { cap: n, hasPools: !0, groups: c, pools: [] };
      }
      const l = Object.keys(((s = (r = this.system) == null ? void 0 : r.counters) == null ? void 0 : s.edgePools) ?? {}).map((c) => {
        const u = o[c] ?? this.getEdgePool(c);
        return {
          ...u,
          isEmpty: (u.effectiveValue ?? 0) <= 0,
          isCapped: (u.effectiveRating ?? u.rating ?? 0) > (u.cap ?? n)
        };
      });
      return { cap: n, hasPools: !0, groups: [], pools: l };
    }
    return { cap: n, hasPools: !1, groups: [], pools: [] };
  }
  /**
   * Spend Edge from a pool (decrement current value).
   * - Characters only (six pools)
   * - Amount defaults to 1
   * - Safe no-op if pool missing
   */
  async spendEdge(e, n = 1, i = {}) {
    if (!this.hasEdgePools()) return;
    const a = Math.max(0, Number(n ?? 1));
    if (!a) return;
    let r = a;
    if (!i.skipTraitHooks) {
      const o = i.runtime ?? {}, l = {
        poolKey: e,
        amount: a,
        source: String(i.source ?? "").trim(),
        eventKey: String(i.eventKey ?? "").trim()
      }, c = zt({
        actor: this,
        phase: "onEdgeSpend",
        facts: mf({ actor: this, packet: l, phase: "onEdgeSpend", runtime: o }),
        packet: l,
        options: { runtime: o, consumeUsage: !0 }
      });
      await ga({ actor: this, mutations: c.mutations, runtime: o }), r = Math.max(0, Number(c.packet.amount ?? a) || 0);
    }
    const s = r;
    if (s)
      return this.adjustEdgePoolValue(e, -s);
  }
  async gainEdge(e, n = 1, i = {}) {
    if (!this.hasEdgePools()) return;
    const a = Number(n ?? 0);
    if (!a) return;
    let r = a;
    if (!i.skipTraitHooks) {
      const s = i.runtime ?? {}, o = {
        poolKey: e,
        amount: a,
        source: String(i.source ?? "").trim(),
        eventKey: String(i.eventKey ?? "").trim()
      }, l = zt({
        actor: this,
        phase: "onEdgeGain",
        facts: mf({ actor: this, packet: o, phase: "onEdgeGain", runtime: s }),
        packet: o,
        options: { runtime: s, consumeUsage: !0 }
      });
      await ga({ actor: this, mutations: l.mutations, runtime: s }), r = Number(l.packet.amount ?? a) || 0;
    }
    return this.adjustEdgePoolValue(e, r);
  }
  /* -------------------------------------------- */
  /* Document Lifecycle                            */
  /* -------------------------------------------- */
  /** @override */
  async _onUpdate(e, n, i) {
    await super._onUpdate(e, n, i), game.userId === i && (n != null && n.mwdSyncOverloadedFromEffect || foundry.utils.hasProperty(e, "system.burn.overloaded") && await this._syncOverloadedEffect(!!e.system.burn.overloaded));
  }
  _onCreateDescendantDocuments(e, n, i, a, r, s) {
    super._onCreateDescendantDocuments(e, n, i, a, r, s), n === "effects" && (this._syncOverloadedFieldFromEffects(), i.some((l) => {
      var c, u;
      return (u = (c = l.statuses) == null ? void 0 : c.has) == null ? void 0 : u.call(c, "shutdown");
    }) && game.user.isGM && (this.type === "battlemech" || this.type === "vehicle") && this._onGainShutdownStatus());
  }
  _onUpdateDescendantDocuments(e, n, i, a, r, s) {
    super._onUpdateDescendantDocuments(e, n, i, a, r, s), n === "effects" && this._syncOverloadedFieldFromEffects();
  }
  _onDeleteDescendantDocuments(e, n, i, a, r, s) {
    super._onDeleteDescendantDocuments(e, n, i, a, r, s), n === "effects" && this._syncOverloadedFieldFromEffects();
  }
  async _onGainShutdownStatus() {
    for (const e of this.getActiveTokens(!0)) {
      const n = Pn(e);
      n && await HL(n);
    }
  }
  get overloadThreshold() {
    return 6 + oi(this, "overloadThresholdMod");
  }
  async _syncOverloadedEffect(e) {
    await this.toggleStatusEffect("overloaded", { active: e, overlay: !1 });
  }
  async _syncOverloadedFieldFromEffects() {
    var i, a, r, s;
    const e = ((a = (i = this.statuses) == null ? void 0 : i.has) == null ? void 0 : a.call(i, "overloaded")) ?? !1, n = !!((s = (r = this.system) == null ? void 0 : r.burn) != null && s.overloaded);
    e !== n && await this.update(
      { "system.burn.overloaded": e },
      { mwdSyncOverloadedFromEffect: !0 }
    );
  }
  /* -------------------------------------------- */
  /* Condition Monitors                            */
  /* -------------------------------------------- */
  async setMonitorValue(e, n, { source: i = "unknown" } = {}) {
    var d, f, m;
    if (e === "burn") {
      const g = Math.max(0, Number(n) || 0);
      return this.update({ "system.burn.value": g });
    }
    if (e === "heat" && this.type === "battlemech") {
      const g = Number(foundry.utils.getProperty(this, "system.monitors.heat.max")), p = Number(foundry.utils.getProperty(this, "system.mwd.heat.max")), h = Number(foundry.utils.getProperty(this, "system.mwd.heat.hardMax")), b = Math.max(
        0,
        Number.isFinite(g) && g > 0 ? g : Number.isFinite(p) && p > 0 ? p : Number.isFinite(h) ? h : 0
      ), y = Math.max(0, Number(n) || 0);
      return this.update({
        "system.monitors.heat.value": y,
        "system.monitors.heat.max": b,
        "system.mwd.heat.current": y,
        "system.mwd.heat.max": b
      });
    }
    if (e === "armor" && this.isCharacterLike()) {
      const g = this.getPersonalCombatLoadout({ refresh: !0 }), p = g == null ? void 0 : g.activeArmor, h = (p == null ? void 0 : p.armorId) ?? (p == null ? void 0 : p.id) ?? null, b = h ? this.items.get(h) : null;
      if (!(b != null && b.id)) return null;
      if (ki(p == null ? void 0 : p.battleArmor)) {
        const T = Math.max(0, Number(((d = p.battleArmor.armorPool) == null ? void 0 : d.max) ?? 0)), C = Math.min(Math.max(0, Number(n) || 0), T);
        return this.updateEmbeddedDocuments("Item", [{
          _id: b.id,
          "system.battleArmor.armorPool.value": C
        }]);
      }
      const S = Math.max(0, Number(((f = b.system) == null ? void 0 : f.rating) ?? 0) || 0), A = Math.min(Math.max(0, Number(n) || 0), S);
      return this.updateEmbeddedDocuments("Item", [{
        _id: b.id,
        "system.durability.max": S,
        "system.durability.current": A
      }]);
    }
    const a = `system.monitors.${e}`, r = Number(foundry.utils.getProperty(this, `${a}.max`)) || 0, s = Math.max(0, r), o = Math.min(Math.max(0, Number(n) || 0), s), l = { [`${a}.value`]: o }, c = this.type, u = (m = Ud == null ? void 0 : Ud[c]) == null ? void 0 : m[e];
    if (u != null && u.derived)
      for (const [g, p] of Object.entries(u.derived)) {
        const h = Bm == null ? void 0 : Bm[p.fn];
        if (typeof h != "function") continue;
        const b = kH(this, e, p.source, o);
        l[`${a}.derived.${g}`] = h(b);
      }
    return this.update(l);
  }
  _prepareMonitors() {
    var g, p, h, b, y, S, A, T, C, k, w, v, N, P, L, _, O, x;
    const e = this.system.monitors ?? {};
    let n = null;
    if (this.isCharacterLike()) {
      n = io(this);
      const H = xO(n == null ? void 0 : n.battleArmor);
      (g = this.system).derived ?? (g.derived = {}), this.system.derived.battleArmor = { enhancedStrengthBonus: H };
      const G = this.getAttributeValue("strength"), q = Math.max(0, Number(((b = (h = (p = this.system) == null ? void 0 : p.attributes) == null ? void 0 : h.guts) == null ? void 0 : b.value) ?? 0));
      e.physical ?? (e.physical = {}), e.fatigue ?? (e.fatigue = {}), e.physical.max = G === 0 ? 0 : Ym + G, e.fatigue.max = q === 0 ? 0 : Ym + q;
    }
    const i = qS(e), a = Number(((y = i == null ? void 0 : i.physical) == null ? void 0 : y.penalty) ?? 0), r = Number(((S = i == null ? void 0 : i.fatigue) == null ? void 0 : S.penalty) ?? 0), s = {
      physicalValue: Math.max(
        0,
        (Number(((A = e.physical) == null ? void 0 : A.value) ?? 0) || 0) + oi(this, "conditionPhysicalValueMod")
      ),
      fatigueValue: Math.max(
        0,
        (Number(((T = e.fatigue) == null ? void 0 : T.value) ?? 0) || 0) + oi(this, "conditionFatigueValueMod")
      ),
      physicalPenalty: a,
      fatiguePenalty: r,
      totalPenalty: a + r
    };
    s.physicalPenalty += oi(this, "conditionPhysicalPenaltyMod"), s.fatiguePenalty += oi(this, "conditionFatiguePenaltyMod");
    const o = zt({
      actor: this,
      phase: "onConditionPenaltyResolved",
      facts: jk({ actor: this, packet: s, runtime: {} }),
      packet: s,
      options: { consumeUsage: !1 }
    }), l = foundry.utils.deepClone(e);
    l.physical ?? (l.physical = {}), l.fatigue ?? (l.fatigue = {}), l.physical.value = Math.max(0, Number(o.packet.physicalValue ?? s.physicalValue) || 0), l.fatigue.value = Math.max(0, Number(o.packet.fatigueValue ?? s.fatigueValue) || 0);
    const c = qS(l);
    (C = this.system).derived ?? (C.derived = {}), this.system.derived.monitors = c;
    let u = Number(o.packet.physicalPenalty) !== a ? Number(o.packet.physicalPenalty ?? 0) : Number(((k = c == null ? void 0 : c.physical) == null ? void 0 : k.penalty) ?? 0), d = Number(o.packet.fatiguePenalty) !== r ? Number(o.packet.fatiguePenalty ?? 0) : Number(((w = c == null ? void 0 : c.fatigue) == null ? void 0 : w.penalty) ?? 0);
    ow(n == null ? void 0 : n.battleArmor) && ((N = (v = n == null ? void 0 : n.battleArmor) == null ? void 0 : v.systems) != null && N.medicalSuppression) && (u = 0, d = 0);
    const f = Number(((P = c == null ? void 0 : c.armor) == null ? void 0 : P.resistance) ?? 0), m = u + d;
    e.physical ?? (e.physical = {}), (L = e.physical).derived ?? (L.derived = {}), e.physical.derived.penalty = u, e.fatigue ?? (e.fatigue = {}), (_ = e.fatigue).derived ?? (_.derived = {}), e.fatigue.derived.penalty = d, e.armor ?? (e.armor = {}), (O = e.armor).derived ?? (O.derived = {}), e.armor.derived.resistance = f, (x = this.system.derived).condition ?? (x.condition = {}), this.system.derived.condition.physicalPenalty = u, this.system.derived.condition.fatiguePenalty = d, this.system.derived.condition.totalPenalty = m, this.system.derived.conditionPenalty = m;
  }
  _preparePersonalCombatDerived() {
    var o, l, c, u, d, f, m, g, p;
    if (!this.isCharacterLike()) return;
    (o = this.system).derived ?? (o.derived = {}), (l = this.system.derived).personalCombat ?? (l.personalCombat = {}), this.system.derived.personalCombat.speed = Fw(this);
    const e = this.getPersonalCombatLoadout({ refresh: !0 }), n = (u = (c = this.system) == null ? void 0 : c.monitors) == null ? void 0 : u.armor;
    if (!n) return;
    const i = e.activeArmor, a = ki(i == null ? void 0 : i.battleArmor) ? i.battleArmor : null, r = a ? Math.max(0, Number(((d = a.armorPool) == null ? void 0 : d.max) ?? 0)) : Math.max(0, Number(((f = i == null ? void 0 : i.durability) == null ? void 0 : f.max) ?? 0)), s = a ? Math.max(0, Number(((m = a.armorPool) == null ? void 0 : m.value) ?? 0)) : Math.max(0, Number((i == null ? void 0 : i.remainingDurability) ?? ((g = i == null ? void 0 : i.durability) == null ? void 0 : g.current) ?? 0));
    n.max = r, n.value = Math.min(r, s), n.resistance = {
      default: a ? ml(a) : Number((i == null ? void 0 : i.baseMitigation) ?? (i == null ? void 0 : i.baseResistance) ?? 0),
      byType: {}
    }, n.resistanceBonusByType = a || i != null && i.isDestroyed ? {} : (i == null ? void 0 : i.mitigationByType) ?? (i == null ? void 0 : i.typedMitigation) ?? {}, n.derived ?? (n.derived = {}), n.derived.resistance = a ? ml(a) : Number((i == null ? void 0 : i.baseMitigation) ?? (i == null ? void 0 : i.baseResistance) ?? 0), n.effect = a ? `Battle Armor ${a.state}` : i != null && i.isDestroyed ? "Destroyed" : i ? TH(i.mitigationByType ?? i.typedMitigation) : "", this.system.derived.personalCombat = {
      ...this.system.derived.personalCombat ?? {},
      defaultWeaponId: ((p = e.defaultWeapon) == null ? void 0 : p.id) ?? null,
      activeArmorId: (i == null ? void 0 : i.id) ?? null,
      battleArmor: Fp(this),
      warnings: [...e.warnings ?? []]
    };
  }
}
class MH extends Combat {
  async rollInitiative(e, n = {}) {
    for (const i of e) {
      const a = this.combatants.get(i);
      a != null && a.actor && await game.mwd.roll.execute({
        actor: a.actor,
        payload: { intent: "initiative" }
      });
    }
    return this;
  }
  _sortCombatants(e, n) {
    var o, l, c, u, d, f, m, g;
    const i = Number.isNumeric(e.initiative) ? e.initiative : -1 / 0, a = Number.isNumeric(n.initiative) ? n.initiative : -1 / 0;
    if (i !== a) return a - i;
    const r = !e.isNPC, s = !n.isNPC;
    if (r !== s) return r ? -1 : 1;
    if (r) {
      const p = Number(((u = (c = (l = (o = e.actor) == null ? void 0 : o.system) == null ? void 0 : l.attributes) == null ? void 0 : c.edge) == null ? void 0 : u.value) ?? 0), h = Number(((g = (m = (f = (d = n.actor) == null ? void 0 : d.system) == null ? void 0 : f.attributes) == null ? void 0 : m.edge) == null ? void 0 : g.value) ?? 0);
      if (p !== h) return h - p;
    }
    return 0;
  }
  async nextRound() {
    await this.resetAll();
    const e = await super.nextRound();
    return await this.rollAll(), e;
  }
}
function vH(t = null) {
  return (t == null ? void 0 : t.type) === M.actorTypes.vehicle || (t == null ? void 0 : t.type) === M.actorTypes.battlemech;
}
function CH(t = "") {
  return String(t ?? "").replace(/([a-z0-9])([A-Z])/g, "$1 $2").replace(/[_-]+/g, " ").replace(/\s+/g, " ").trim().replace(/\b\w/g, (e) => e.toUpperCase());
}
async function IH({ actor: t, payload: e } = {}) {
  var O, x, H, G, q, ie, Z, Se, se, te;
  if (!t) throw new Error("resolveSkill requires actor");
  const n = String((e == null ? void 0 : e.key) ?? "").trim(), i = !!(e != null && e.noSkill) || n === "none", a = i ? "none" : n, r = i ? { label: "No Skill", attribute: "" } : Ft(a);
  if (!r) throw new Error(`Unknown skill: ${a}`);
  const s = vH(t), o = s ? await Ut({
    machineActor: t,
    operatorActorUuid: String((e == null ? void 0 : e.operatorActorUuid) ?? "").trim()
  }) : null, l = (o == null ? void 0 : o.actor) ?? t, c = l.system ?? {}, u = String((e == null ? void 0 : e.machineAttributeKey) ?? "").trim(), d = String(
    u || (e == null ? void 0 : e.attrKey) || (s && a === "piloting" ? M.actorAttributes.handling : r.attribute) || ""
  ).trim();
  if (!d) throw new Error(`Skill ${a} missing attribute key`);
  const f = u || s && a === "piloting" ? t : l, m = f.system ?? {}, g = Number(((O = f.getAttributeValue) == null ? void 0 : O.call(f, d)) ?? ((H = (x = m == null ? void 0 : m.attributes) == null ? void 0 : x[d]) == null ? void 0 : H.value) ?? 0), p = i ? 0 : Number(((q = (G = c == null ? void 0 : c.skills) == null ? void 0 : G[a]) == null ? void 0 : q.rating) ?? 0), h = i ? 0 : Number(((Z = (ie = c == null ? void 0 : c.skills) == null ? void 0 : ie[a]) == null ? void 0 : Z.bonus) ?? 0), b = i ? /* @__PURE__ */ new Set() : new Set(Ts(c, a)), y = i ? null : rp(a, e == null ? void 0 : e.specializationKey), S = y && b.has(y.key) ? y : null, A = S ? np : 0, T = Array.from(/* @__PURE__ */ new Set([
    ...Array.isArray(e == null ? void 0 : e.domains) ? e.domains : r.domains ?? [],
    `skill.${a}`
  ])), C = Number.isFinite(Number(e == null ? void 0 : e.diceTarget)) ? Number(e.diceTarget) : Number.isFinite(Number(e == null ? void 0 : e.target)) ? Number(e.target) : 5, k = tM(e), w = k ? k.dn : Number.isFinite(Number(e == null ? void 0 : e.dn)) ? Number(e.dn) : 1, v = s && a === "piloting" ? lO(t) : 0, N = w + v, P = CH(d), L = f !== l && (f != null && f.name) ? ` (${f.name})` : "", _ = s && (l != null && l.name) ? ` (${l.name})` : "";
  return {
    intent: "skill",
    rollType: "simple",
    title: (k == null ? void 0 : k.title) ?? `${r.label} (${P})`,
    subtitle: t.name ?? "Actor",
    domains: T,
    // Per-die threshold (cs>=X). Kept separate from dnHits.
    diceTarget: C,
    // DN = hits needed for success
    difficulty: { dn: N },
    dn: {
      parts: [
        (k == null ? void 0 : k.dnPart) ?? { id: "difficulty.base", label: "Base DN", value: w, tags: ["base"] },
        ...v ? [{ id: "machineCrit.unstable", label: "Unstable", value: v, tags: ["machineCrit"] }] : []
      ],
      total: N
    },
    edge: {
      earn: { enabled: !0, rate: 4, maxPerRoll: 1 }
    },
    pool: { attribute: g, skill: p, bonus: h, specialization: A },
    breakdown: [
      { id: "attribute", label: `${P}${L}`, value: g },
      { id: "skill", label: `${r.label}${_}`, value: p },
      { id: "bonus", label: "Bonus", value: h },
      ...S ? [{
        id: "specialization",
        label: `Specialization (${S.label})`,
        value: A
      }] : []
    ],
    specialization: S ? {
      key: S.key,
      label: S.label,
      value: A,
      skillKey: a
    } : null,
    // optional extra metadata (safe to stash)
    data: {
      skillKey: a,
      attrKey: d,
      machineActorUuid: s ? t.uuid ?? "" : "",
      operatorActorUuid: ((Se = o == null ? void 0 : o.actor) == null ? void 0 : Se.uuid) ?? "",
      label: `${d}+${r.label}`,
      specializationKey: (S == null ? void 0 : S.key) ?? "",
      specializationLabel: (S == null ? void 0 : S.label) ?? ""
    },
    rollActor: l,
    machineActor: s ? t : null,
    operator: o ? {
      actorUuid: ((se = o.actor) == null ? void 0 : se.uuid) ?? "",
      name: ((te = o.actor) == null ? void 0 : te.name) ?? "",
      source: o.source ?? "",
      reason: o.reason ?? ""
    } : null
  };
}
const EH = /* @__PURE__ */ new Set([
  "grit",
  "chaos",
  "insight",
  "rumor",
  "legend",
  "credibility"
]), NH = {
  grit: "physical",
  chaos: "physical",
  insight: "mental",
  rumor: "mental",
  legend: "social",
  credibility: "social"
};
async function PH({ actor: t, payload: e } = {}) {
  if (!t) throw new Error("resolveEdge requires actor");
  const n = String((e == null ? void 0 : e.pool) ?? "").trim();
  if (!EH.has(n)) throw new Error(`Invalid edge pool: ${n}`);
  const i = t.getEdgePool(n), a = Math.max(0, Number((i == null ? void 0 : i.effectiveValue) ?? 0));
  return {
    intent: "edge",
    title: `Edge — ${n}`,
    subtitle: t.name ?? "Actor",
    domains: [NH[n] ?? "unknown"],
    // drop "edge" tag unless you truly want it
    // Make it directly rollable by the core roll pipeline
    target: 5,
    poolTotal: a,
    breakdown: [
      { id: "current", label: "Current", value: Number((i == null ? void 0 : i.value) ?? 0) },
      { id: "rating", label: "Rating", value: Number((i == null ? void 0 : i.rating) ?? 0) },
      { id: "cap", label: "Edge Cap", value: Number((i == null ? void 0 : i.cap) ?? 0) },
      { id: "usable", label: "Usable", value: a }
    ],
    data: { poolKey: n }
  };
}
async function RH({ actor: t, payload: e, event: n } = {}) {
  return {
    title: "Attribute",
    intent: "attribute",
    domainTags: ["general"],
    pool: { attribute: 0, skill: 0, bonus: 0 },
    breakdown: [],
    mods: []
  };
}
async function DH({ actor: t, payload: e } = {}) {
  if (!t) throw new Error("resolveCommon requires actor");
  const n = String((e == null ? void 0 : e.id) ?? "").trim();
  if (!n) throw new Error("Common rolls require payload.id");
  const i = cp(n);
  if (!i) throw new Error(`Unknown common check: ${n}`);
  const a = Array.isArray(i.formula) ? i.formula : [];
  if (a.length !== 2)
    throw new Error(`Common check ${n} must define exactly two attributes.`);
  const r = a.map((c) => {
    var d, f, m, g;
    const u = cN(c);
    if (!u) throw new Error(`Common check ${n} uses unsupported attribute code: ${c}`);
    return {
      code: String(c).trim().toUpperCase(),
      key: u,
      label: uN(c),
      value: Number(((d = t.getAttributeValue) == null ? void 0 : d.call(t, u)) ?? ((g = (m = (f = t.system) == null ? void 0 : f.attributes) == null ? void 0 : m[u]) == null ? void 0 : g.value) ?? 0)
    };
  }), s = r.reduce((c, u) => c + Number(u.value ?? 0), 0), o = Array.isArray(i.tags) ? [...i.tags] : [], l = Array.isArray(i.domains) ? [...i.domains] : [];
  return {
    intent: "common",
    rollType: "simple",
    title: String(i.label ?? n).trim() || n,
    subtitle: t.name ?? "Actor",
    domains: l,
    tags: o,
    formula: dN(a),
    difficulty: {
      dn: Number.isFinite(Number(e == null ? void 0 : e.dn)) ? Number(e.dn) : 1
    },
    edge: {
      earn: { enabled: !0, rate: 4, maxPerRoll: 1 }
    },
    pool: {
      attribute: s,
      skill: 0,
      bonus: 0,
      specialization: 0
    },
    breakdown: r.map((c) => ({
      id: `attribute.${c.code.toLowerCase()}`,
      label: c.label,
      value: c.value
    })),
    data: {
      commonCheckId: n,
      label: String(i.label ?? n).trim() || n,
      formulaCodes: a,
      tags: o,
      attributes: r
    }
  };
}
const OH = 90;
var EA;
const LH = Number(((EA = CONST == null ? void 0 : CONST.REGION_VISIBILITY) == null ? void 0 : EA.ALWAYS) ?? 2) || 2;
function Ou() {
  var t, e;
  return Number(((t = canvas.grid) == null ? void 0 : t.size) ?? ((e = canvas.dimensions) == null ? void 0 : e.size) ?? 100) || 100;
}
function $H(t = {}) {
  return Math.max(0, Number((t == null ? void 0 : t.size) ?? 0) || 0);
}
function oo(t) {
  var n, i, a, r;
  return ((i = (n = canvas.tokens) == null ? void 0 : n.controlled) == null ? void 0 : i.find((s) => {
    var o;
    return ((o = s.actor) == null ? void 0 : o.id) === (t == null ? void 0 : t.id);
  })) ?? null ?? ((r = (a = t == null ? void 0 : t.getActiveTokens) == null ? void 0 : a.call(t, !0, !0)) == null ? void 0 : r[0]) ?? null;
}
function WS(t) {
  var e, n;
  return Number(
    ((e = t == null ? void 0 : t.document) == null ? void 0 : e.disposition) ?? (t == null ? void 0 : t.disposition) ?? ((n = CONST == null ? void 0 : CONST.TOKEN_DISPOSITIONS) == null ? void 0 : n.NEUTRAL) ?? 0
  );
}
function Lu(t) {
  var s, o, l, c, u;
  const e = (t == null ? void 0 : t.center) ?? ((s = t == null ? void 0 : t.object) == null ? void 0 : s.center);
  if (e) return { x: Number(e.x ?? 0), y: Number(e.y ?? 0) };
  const n = Number((t == null ? void 0 : t.x) ?? ((o = t == null ? void 0 : t.document) == null ? void 0 : o.x) ?? 0), i = Number((t == null ? void 0 : t.y) ?? ((l = t == null ? void 0 : t.document) == null ? void 0 : l.y) ?? 0), a = Number((t == null ? void 0 : t.w) ?? (t == null ? void 0 : t.width) ?? ((c = t == null ? void 0 : t.document) == null ? void 0 : c.width) ?? 1) * Ou(), r = Number((t == null ? void 0 : t.h) ?? (t == null ? void 0 : t.height) ?? ((u = t == null ? void 0 : t.document) == null ? void 0 : u.height) ?? 1) * Ou();
  return { x: n + a / 2, y: i + r / 2 };
}
function _H(t) {
  var i, a, r, s;
  const e = Number((t == null ? void 0 : t.w) ?? ((i = t == null ? void 0 : t.object) == null ? void 0 : i.w) ?? 0) || Number(((a = t == null ? void 0 : t.document) == null ? void 0 : a.width) ?? 1) * Ou(), n = Number((t == null ? void 0 : t.h) ?? ((r = t == null ? void 0 : t.object) == null ? void 0 : r.h) ?? 0) || Number(((s = t == null ? void 0 : t.document) == null ? void 0 : s.height) ?? 1) * Ou();
  return Math.max(e, n) / 2;
}
function BH() {
  var n;
  const t = ((n = canvas == null ? void 0 : canvas.stage) == null ? void 0 : n.pivot) ?? null, e = (canvas == null ? void 0 : canvas.dimensions) ?? {};
  return {
    x: Number((t == null ? void 0 : t.x) ?? e.width / 2 ?? 0) || 0,
    y: Number((t == null ? void 0 : t.y) ?? e.height / 2 ?? 0) || 0
  };
}
function xH() {
  var t;
  return Array.from(((t = game.user) == null ? void 0 : t.targets) ?? []).find((e) => e == null ? void 0 : e.actor) ?? null;
}
function zH(t, e) {
  return {
    x: (Number((t == null ? void 0 : t.x) ?? 0) + Number((e == null ? void 0 : e.x) ?? 0)) / 2,
    y: (Number((t == null ? void 0 : t.y) ?? 0) + Number((e == null ? void 0 : e.y) ?? 0)) / 2
  };
}
function FH({ template: t = {}, actor: e = null } = {}) {
  const n = String((t == null ? void 0 : t.placement) ?? "").trim().toLowerCase(), i = oo(e), a = xH(), r = i ? Lu(i) : null, s = a ? Lu(a) : null;
  return n === "origin" && r ? r : n === "targeted" && s ? s : n === "placed" && r && s ? zH(r, s) : BH();
}
function UH({ attack: t = {}, actor: e = null } = {}) {
  const n = (t == null ? void 0 : t.template) ?? null, i = String((n == null ? void 0 : n.shape) ?? "").trim().toLowerCase();
  if (!i) return null;
  const a = FH({ template: n, actor: e });
  return vt({
    shape: i,
    x: a.x,
    y: a.y,
    direction: 0,
    distance: $H(n),
    angle: i === "cone" ? OH : null,
    width: i === "line" ? 1 : null,
    placementMode: (n == null ? void 0 : n.placement) ?? null
  });
}
function KH() {
  var e, n;
  const t = new PIXI.Container();
  return t.eventMode = "none", t.sortableChildren = !0, (n = (e = canvas.stage) == null ? void 0 : e.addChild) == null || n.call(e, t), t;
}
function HH(t) {
  var e;
  t != null && t.parent && t.parent.removeChild(t), (e = t == null ? void 0 : t.destroy) == null || e.call(t, { children: !0 });
}
function jH() {
  const t = KH(), e = new PIXI.Container();
  e.eventMode = "none", e.zIndex = 5;
  const n = new PIXI.Container();
  return n.eventMode = "none", n.zIndex = 10, t.addChild(e), t.addChild(n), { root: t, templateLayer: e, markerLayer: n };
}
function GH(t) {
  HH((t == null ? void 0 : t.root) ?? t);
}
function qH() {
  var n;
  const t = String(((n = game.user) == null ? void 0 : n.color) ?? "#ff6400").replace("#", "").trim(), e = Number.parseInt(t, 16);
  return Number.isFinite(e) ? e : 16737280;
}
function gC(t) {
  var e;
  (e = t == null ? void 0 : t.removeChildren) == null || e.call(t).forEach((n) => {
    var i;
    return (i = n.destroy) == null ? void 0 : i.call(n, { children: !0 });
  });
}
function Ng(t) {
  var c, u, d;
  const e = ((c = canvas == null ? void 0 : canvas.app) == null ? void 0 : c.view) ?? null, n = ((u = canvas == null ? void 0 : canvas.app) == null ? void 0 : u.renderer) ?? null, i = (canvas == null ? void 0 : canvas.stage) ?? null;
  if (!e || !i) return null;
  const a = Number((t == null ? void 0 : t.clientX) ?? NaN), r = Number((t == null ? void 0 : t.clientY) ?? NaN);
  if (!Number.isFinite(a) || !Number.isFinite(r)) return null;
  const s = e.getBoundingClientRect();
  if (a < s.left || a > s.right || r < s.top || r > s.bottom) return null;
  const o = new PIXI.Point();
  if (typeof ((d = n == null ? void 0 : n.events) == null ? void 0 : d.mapPositionToPoint) == "function")
    n.events.mapPositionToPoint(o, a, r);
  else {
    const f = Number((n == null ? void 0 : n.resolution) ?? window.devicePixelRatio ?? 1) || 1;
    o.x = (a - s.left) * f, o.y = (r - s.top) * f;
  }
  const l = i.toLocal(o);
  return {
    x: Number((l == null ? void 0 : l.x) ?? 0) || 0,
    y: Number((l == null ? void 0 : l.y) ?? 0) || 0
  };
}
function WH(t, e) {
  const n = Number((e == null ? void 0 : e.x) ?? 0) - Number((t == null ? void 0 : t.x) ?? 0), i = Number((e == null ? void 0 : e.y) ?? 0) - Number((t == null ? void 0 : t.y) ?? 0);
  return n === 0 && i === 0 ? 0 : Math.atan2(i, n) * 180 / Math.PI;
}
function xm(t = 0) {
  var i, a, r, s, o;
  const e = Number(((i = canvas.grid) == null ? void 0 : i.size) ?? ((a = canvas.dimensions) == null ? void 0 : a.size) ?? 100) || 100, n = Number(((s = (r = canvas.scene) == null ? void 0 : r.grid) == null ? void 0 : s.distance) ?? ((o = canvas.dimensions) == null ? void 0 : o.distance) ?? 1) || 1;
  return Number(t ?? 0) * (e / n);
}
function VH({ geometry: t = null, pointer: e = null, attack: n = {}, actor: i = null } = {}) {
  var l;
  const a = vt(t);
  if (!a) return null;
  const r = Ui(a) ?? null;
  if (!r || !e) return r;
  const o = String(((l = n == null ? void 0 : n.template) == null ? void 0 : l.placement) ?? r.placementMode ?? "").trim().toLowerCase() !== "origin";
  if (o && (r.x = e.x, r.y = e.y), ["line", "cone", "rect"].includes(String(r.shape ?? "").trim().toLowerCase())) {
    const c = oo(i), u = c ? Lu(c) : null, d = o ? u ?? { x: Number(a.x ?? 0), y: Number(a.y ?? 0) } : { x: Number(r.x ?? 0), y: Number(r.y ?? 0) };
    r.direction = WH(d, e);
  }
  return vt(r);
}
function JH(t, e = null) {
  if (!t) return;
  gC(t);
  const n = vt(e);
  if (!n) return;
  const i = qH(), a = new PIXI.Graphics();
  switch (a.lineStyle(3, i, 0.95), a.beginFill(i, 0.18), String(n.shape ?? "").trim().toLowerCase()) {
    case "blast": {
      a.drawCircle(
        Number(n.x ?? 0),
        Number(n.y ?? 0),
        xm(n.distance ?? 0)
      );
      break;
    }
    case "rect": {
      const r = xm(n.width ?? 0), s = xm(n.height ?? 0);
      a.position.set(Number(n.x ?? 0), Number(n.y ?? 0)), a.rotation = Number(n.direction ?? 0) * Math.PI / 180, a.drawRect(
        -(Number(n.anchorX ?? 0) || 0) * r,
        -(Number(n.anchorY ?? 0) || 0) * s,
        r,
        s
      );
      break;
    }
    default: {
      const [r] = Ku(n);
      (r == null ? void 0 : r.type) === "polygon" && Array.isArray(r.points) && r.points.length >= 3 && a.drawPolygon(r.points.flatMap((s) => [Number((s == null ? void 0 : s.x) ?? 0), Number((s == null ? void 0 : s.y) ?? 0)]));
      break;
    }
  }
  a.endFill(), t.addChild(a);
}
function YH(t = Te.none) {
  return t === Te.full ? 14042437 : t === Te.major ? 15174447 : t === Te.minor ? 15782993 : 10134706;
}
function QH(t, e = []) {
  if (t) {
    gC(t);
    for (const n of e) {
      const i = Lu(n.token), a = Math.max(20, _H(n.token) + 12), r = YH(n.exposureTier), s = new PIXI.Graphics();
      s.lineStyle(4, r, 0.95), s.beginFill(r, 0.14), s.drawCircle(i.x, i.y, a), s.endFill(), s.zIndex = 10;
      const o = new PIXI.Text(ci(n.exposureTier), {
        fontFamily: "MWD UI",
        fontSize: 18,
        fontWeight: "700",
        fill: r,
        stroke: 1118481,
        strokeThickness: 4,
        align: "center"
      });
      o.anchor.set(0.5, 1), o.position.set(i.x, i.y - a - 6), o.zIndex = 11, t.addChild(s), t.addChild(o);
    }
  }
}
function pC(t, e = {}) {
  var r, s, o, l, c, u, d, f, m, g, p, h, b, y, S, A;
  const n = (t == null ? void 0 : t.actor) ?? null;
  if (!n) return null;
  const i = ((r = n == null ? void 0 : n.getPersonalCombatLoadout) == null ? void 0 : r.call(n)) ?? null, a = (i == null ? void 0 : i.activeArmor) ?? null;
  return {
    tokenId: (t == null ? void 0 : t.id) ?? null,
    tokenUuid: ((s = t == null ? void 0 : t.document) == null ? void 0 : s.uuid) ?? null,
    actorId: n.id,
    actorUuid: n.uuid,
    name: n.name ?? (t == null ? void 0 : t.name) ?? "Target",
    attributes: {
      reflexes: Number(((c = (l = (o = n == null ? void 0 : n.system) == null ? void 0 : o.attributes) == null ? void 0 : l.reflexes) == null ? void 0 : c.value) ?? 0) || 0
    },
    skills: {
      tactics: {
        rating: Number(((f = (d = (u = n == null ? void 0 : n.system) == null ? void 0 : u.skills) == null ? void 0 : d.tactics) == null ? void 0 : f.rating) ?? 0) || 0
      }
    },
    activeArmor: a ? {
      armorId: a.id,
      rating: Number(a.ratingCurrent ?? a.rating ?? 0),
      currentArmorRating: Number(a.currentArmorRating ?? ((m = a.durability) == null ? void 0 : m.current) ?? 0),
      remainingDurability: Number(a.remainingDurability ?? ((g = a.durability) == null ? void 0 : g.current) ?? 0),
      baseMitigation: Number(a.baseMitigation ?? a.baseResistance ?? 0),
      baseResistance: Number(a.baseMitigation ?? a.baseResistance ?? 0),
      mitigationByType: { ...a.mitigationByType ?? a.typedMitigation ?? {} },
      tags: [...a.tags ?? []],
      isDestroyed: !!a.isDestroyed,
      defenseBonus: Number(a.defenseBonus ?? 0)
    } : null,
    exposure: Va({
      tier: ((p = e == null ? void 0 : e.exposure) == null ? void 0 : p.initialTier) ?? ((h = e == null ? void 0 : e.exposure) == null ? void 0 : h.tier) ?? (e == null ? void 0 : e.exposureTier) ?? Te.none,
      appliedTier: ((b = e == null ? void 0 : e.exposure) == null ? void 0 : b.finalTier) ?? ((y = e == null ? void 0 : e.exposure) == null ? void 0 : y.appliedTier) ?? (e == null ? void 0 : e.exposureTier) ?? Te.none,
      evadeUsed: !!((S = e == null ? void 0 : e.exposure) != null && S.evadeUsed),
      evadeLocked: !!((A = e == null ? void 0 : e.exposure) != null && A.evadeLocked)
    }),
    areaEffect: e != null && e.areaEffect ? foundry.utils.deepClone(e.areaEffect) : null
  };
}
function XH({ attack: t = {}, geometry: e = null, attacker: n = null } = {}) {
  var o;
  const i = (t == null ? void 0 : t.template) ?? null, a = vt(e);
  if (!i || !a) return [];
  const r = oo(n), s = (r == null ? void 0 : r.id) ?? null;
  return (((o = canvas.tokens) == null ? void 0 : o.placeables) ?? []).filter((l) => l == null ? void 0 : l.actor).filter((l) => l.id !== s || (i == null ? void 0 : i.placement) === "origin").filter((l) => Uu(a, l)).map((l) => {
    const c = HA({ geometry: a, token: l });
    return pC(l, {
      exposureTier: c,
      areaEffect: {
        templateShape: (i == null ? void 0 : i.shape) ?? "",
        templatePlacement: (i == null ? void 0 : i.placement) ?? "",
        templateGeometry: Ui(a)
      }
    });
  }).filter(Boolean);
}
function ZH({ attack: t = {}, geometry: e = null, attacker: n = null } = {}) {
  var o;
  const i = (t == null ? void 0 : t.template) ?? null, a = oo(n), r = (a == null ? void 0 : a.id) ?? null, s = vt(e);
  return !i || !s ? [] : (((o = canvas.tokens) == null ? void 0 : o.placeables) ?? []).filter((l) => l == null ? void 0 : l.actor).filter((l) => l.id !== r || (i == null ? void 0 : i.placement) === "origin").filter((l) => Uu(s, l)).map((l) => ({
    token: l,
    exposureTier: HA({ geometry: s, token: l })
  }));
}
function ej({ geometry: t = null, attack: e = {}, attacker: n = null } = {}) {
  var f, m, g, p;
  const i = (e == null ? void 0 : e.template) ?? null, a = vt(t);
  if (!i || !a) return [];
  const r = oo(n), s = (r == null ? void 0 : r.id) ?? null, o = Number(((f = CONST == null ? void 0 : CONST.TOKEN_DISPOSITIONS) == null ? void 0 : f.HOSTILE) ?? -1), l = Number(((m = CONST == null ? void 0 : CONST.TOKEN_DISPOSITIONS) == null ? void 0 : m.FRIENDLY) ?? 1), c = Number(((g = CONST == null ? void 0 : CONST.TOKEN_DISPOSITIONS) == null ? void 0 : g.NEUTRAL) ?? 0), u = WS(r), d = (h) => {
    const b = WS(h);
    return r ? u === l ? b === o : u === o ? b === l : u === c ? b === o : b !== u : !0;
  };
  return (((p = canvas.tokens) == null ? void 0 : p.placeables) ?? []).filter((h) => h == null ? void 0 : h.actor).filter((h) => h.id !== s || (i == null ? void 0 : i.placement) === "origin").filter((h) => Uu(a, h)).filter(d).map((h) => String(h.id ?? "").trim()).filter(Boolean);
}
function tj(t = {}) {
  var i;
  const e = String(((i = t == null ? void 0 : t.template) == null ? void 0 : i.shape) ?? "template").trim().toLowerCase();
  return `${e ? `${e.slice(0, 1).toUpperCase()}${e.slice(1)}` : "Template"} placement: left-click to place, right-click or Esc to cancel, Enter or Space to confirm.`;
}
async function nj({ attack: t = {} } = {}) {
  var n, i;
  const e = tj(t);
  return e && ((i = (n = ui.notifications) == null ? void 0 : n.info) == null || i.call(n, e)), new Promise((a) => {
    let r = !1;
    const s = () => {
      window.removeEventListener("pointerdown", u, !0), window.removeEventListener("keydown", d, !0), window.removeEventListener("contextmenu", f, !0);
    }, o = (m = !1) => {
      r || (r = !0, s(), a(!!m));
    }, l = (m) => {
      var g, p, h;
      (g = m == null ? void 0 : m.preventDefault) == null || g.call(m), (p = m == null ? void 0 : m.stopPropagation) == null || p.call(m), (h = m == null ? void 0 : m.stopImmediatePropagation) == null || h.call(m);
    }, c = (m) => {
      if (!(m instanceof HTMLElement)) return !1;
      const g = String(m.tagName ?? "").trim().toUpperCase();
      return m.isContentEditable || ["INPUT", "TEXTAREA", "SELECT"].includes(g);
    }, u = (m) => {
      const g = Number((m == null ? void 0 : m.button) ?? 0), p = Ng(m);
      if (g === 2 && p) {
        l(m), o(!1);
        return;
      }
      g !== 0 || !p || (l(m), o(!0));
    }, d = (m) => {
      const g = String((m == null ? void 0 : m.key) ?? ""), p = String((m == null ? void 0 : m.code) ?? "");
      if (g === "Escape") {
        l(m), o(!1);
        return;
      }
      c((m == null ? void 0 : m.target) ?? document.activeElement) || (g === "Enter" || g === "NumpadEnter" || g === " " || g === "Spacebar" || p === "Space") && (l(m), o(!0));
    }, f = (m) => {
      Ng(m) && l(m);
    };
    window.addEventListener("pointerdown", u, !0), window.addEventListener("keydown", d, !0), window.addEventListener("contextmenu", f, !0);
  });
}
async function ij({ actor: t = null, attack: e = {}, templateGeometry: n = null } = {}) {
  var o, l, c, u, d, f;
  if (!(canvas != null && canvas.scene) || zA((e == null ? void 0 : e.areaEffect) ?? ((o = e == null ? void 0 : e.payload) == null ? void 0 : o.areaEffect) ?? {})) return null;
  const i = vt(n, {
    template: e == null ? void 0 : e.template,
    placement: e == null ? void 0 : e.templatePlacement
  });
  if (!i) return null;
  const a = Ku(i);
  if (!a.length) return null;
  const r = `${String(((l = e == null ? void 0 : e.weapon) == null ? void 0 : l.name) ?? (e == null ? void 0 : e.name) ?? "Template").trim() || "Template"} Template`, [s] = await canvas.scene.createEmbeddedDocuments("Region", [{
    name: r,
    color: String(((c = game.user) == null ? void 0 : c.color) ?? "#ff6400").trim() || "#ff6400",
    visibility: LH,
    locked: !1,
    shapes: a,
    flags: {
      mwd: {
        templateIndicator: {
          sourceActorUuid: (t == null ? void 0 : t.uuid) ?? null,
          sourceItemUuid: ((u = e == null ? void 0 : e.weapon) == null ? void 0 : u.uuid) ?? null,
          payloadId: ((d = e == null ? void 0 : e.payloadState) == null ? void 0 : d.activePayloadId) ?? ((f = e == null ? void 0 : e.payload) == null ? void 0 : f.id) ?? "",
          label: r,
          templateGeometry: Ui(i),
          templatePlacement: foundry.utils.deepClone((e == null ? void 0 : e.templatePlacement) ?? null),
          template: foundry.utils.deepClone((e == null ? void 0 : e.template) ?? null)
        }
      }
    }
  }]);
  return s ?? null;
}
async function aj({ actor: t, attack: e } = {}) {
  if (!(canvas != null && canvas.scene))
    throw dt("Templated attacks require an active scene canvas.", { severity: "warn" });
  const n = (e == null ? void 0 : e.template) ?? null;
  if (!(n != null && n.shape) || !(Number(n == null ? void 0 : n.size) > 0))
    throw dt("Templated attack is missing valid template configuration.", { severity: "warn" });
  if (!CI.includes(n.shape))
    throw dt(`Template shape "${n.shape}" is normalized but not supported in v1.`, { severity: "warn" });
  const i = oo(t);
  if (n.placement === "origin" && !i)
    throw dt("Origin-placed templated attacks require the attacker to have a token on the current scene.", { severity: "warn" });
  const a = UH({ attack: e, actor: t });
  if (!a)
    throw dt("Unable to initialize template placement for this attack.", { severity: "warn" });
  const r = jH();
  let s = Ui(a), o = "";
  const l = (d = null) => JSON.stringify({
    shape: (d == null ? void 0 : d.shape) ?? "",
    x: Number((d == null ? void 0 : d.x) ?? 0),
    y: Number((d == null ? void 0 : d.y) ?? 0),
    direction: Number((d == null ? void 0 : d.direction) ?? 0),
    distance: Number((d == null ? void 0 : d.distance) ?? 0),
    angle: Number((d == null ? void 0 : d.angle) ?? 0),
    width: Number((d == null ? void 0 : d.width) ?? 0),
    height: Number((d == null ? void 0 : d.height) ?? 0),
    anchorX: Number((d == null ? void 0 : d.anchorX) ?? 0),
    anchorY: Number((d == null ? void 0 : d.anchorY) ?? 0),
    placementMode: (d == null ? void 0 : d.placementMode) ?? ""
  }), c = () => {
    JH(r.templateLayer, s), QH(r.markerLayer, ZH({ attack: e, geometry: s, attacker: t }));
  }, u = (d) => {
    const f = Ng(d);
    if (!f) return;
    const m = VH({
      geometry: s,
      pointer: f,
      attack: e,
      actor: t
    });
    if (!m) return;
    const g = l(m);
    g !== o && (s = m, o = g, c());
  };
  try {
    if (o = l(s), c(), window.addEventListener("pointermove", u), !await nj({
      attack: {
        ...e,
        actor: t
      }
    })) return null;
    const f = Ui(s);
    if (!f) return null;
    const m = GI(f, n), g = XH({
      attack: e,
      geometry: f,
      attacker: t
    });
    return {
      templateGeometry: Ui(f),
      placement: (m == null ? void 0 : m.placement) ?? null,
      autoTargetTokenIds: ej({
        geometry: f,
        attack: e,
        attacker: t
      }),
      targetSnapshots: g
    };
  } finally {
    window.removeEventListener("pointermove", u), GH(r);
  }
}
function rj(t) {
  return Array.isArray(t) ? t : Array.isArray(t == null ? void 0 : t.contents) ? t.contents : t && typeof t[Symbol.iterator] == "function" ? Array.from(t) : [];
}
function sj(t = {}) {
  var e;
  return String((t == null ? void 0 : t.type) ?? ((e = t == null ? void 0 : t.actor) == null ? void 0 : e.type) ?? "").trim();
}
function oj(t = {}) {
  var n;
  const e = (t == null ? void 0 : t.assetModules) ?? (t == null ? void 0 : t.items) ?? ((n = t == null ? void 0 : t.actor) == null ? void 0 : n.items) ?? [];
  return rj(e).filter((i) => ((i == null ? void 0 : i.canonicalType) ?? (i == null ? void 0 : i.type)) === M.itemType.assetModule);
}
function zm() {
  return {
    active: !1,
    diceModifier: 0,
    targetNumberModifier: 0,
    sourceIds: [],
    sourceNames: [],
    sourceLabel: ""
  };
}
function lj(t = {}, e = {}) {
  const n = sj(t);
  if (n && n !== M.actorTypes.battlemech && n !== M.actorTypes.vehicle)
    return { clustering: zm() };
  const i = oj(t).map((o) => ({
    item: o,
    state: no(o, { installed: !0 }),
    profile: DD(o)
  })).filter(
    (o) => {
      var l, c;
      return o.state.active && (Number(((l = o.profile) == null ? void 0 : l.diceModifier) ?? 0) !== 0 || Number(((c = o.profile) == null ? void 0 : c.targetNumberModifier) ?? 0) !== 0);
    }
  ), a = WD((t == null ? void 0 : t.actor) ?? t, e);
  if (!i.length && !a.diceModifier && !a.targetNumberModifier)
    return { clustering: zm() };
  const r = i.reduce((o, l) => {
    var c, u, d, f;
    return {
      active: !0,
      diceModifier: o.diceModifier + (Number(((c = l.profile) == null ? void 0 : c.diceModifier) ?? 0) || 0),
      targetNumberModifier: o.targetNumberModifier + (Number(((u = l.profile) == null ? void 0 : u.targetNumberModifier) ?? 0) || 0),
      sourceIds: (d = l.item) != null && d.id ? o.sourceIds.concat([l.item.id]) : o.sourceIds,
      sourceNames: (f = l.item) != null && f.name ? o.sourceNames.concat([l.item.name]) : o.sourceNames
    };
  }, zm()), s = {
    active: !0,
    diceModifier: r.diceModifier + Number(a.diceModifier ?? 0),
    targetNumberModifier: r.targetNumberModifier + Number(a.targetNumberModifier ?? 0),
    sourceIds: r.sourceIds.concat(a.sourceIds ?? []),
    sourceNames: r.sourceNames.concat(a.sourceNames ?? [])
  };
  return {
    clustering: {
      ...s,
      sourceLabel: s.sourceNames.join(", ")
    }
  };
}
function cj(t = {}, e = {}) {
  return lj(t, e).clustering;
}
function Ko(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? Math.trunc(n) : e;
}
function hC(t, e = 5) {
  return Ko(t, e);
}
function uj({
  clusteringDice: t = 0,
  clusteringTargetNumber: e = 5,
  diceModifier: n = 0,
  targetNumberModifier: i = 0
} = {}) {
  const a = Math.max(0, Ko(t, 0)), r = hC(e, 5), o = a > 0 ? Ko(n, 0) : 0, l = Math.max(0, a + o), c = l > 0, u = c ? Ko(i, 0) : 0;
  return {
    active: c,
    baseDice: a,
    diceModifier: o,
    dice: l,
    baseTargetNumber: r,
    targetNumberModifier: u,
    targetNumber: r + u
  };
}
async function dj({
  clusteringDice: t = 0,
  clusteringTargetNumber: e = 5,
  RollClass: n = globalThis.Roll
} = {}) {
  const i = Math.max(0, Ko(t, 0)), a = hC(e, 5), r = i > 0 ? `${i}d6cs>=${a}` : "";
  if (!i || typeof n != "function")
    return {
      rolled: !1,
      dice: i,
      targetNumber: a,
      hits: 0,
      formula: r,
      results: [],
      roll: null
    };
  const s = await new n(r).evaluate(), o = Array.isArray(s == null ? void 0 : s.dice) ? s.dice[0] ?? null : null, l = Array.isArray(o == null ? void 0 : o.results) ? o.results.map((c, u) => ({
    index: u,
    result: Number((c == null ? void 0 : c.result) ?? 0) || 0,
    success: !!(c != null && c.success)
  })) : [];
  return {
    rolled: !0,
    dice: i,
    targetNumber: a,
    hits: l.filter((c) => c.success).length,
    formula: r,
    results: l,
    roll: typeof (s == null ? void 0 : s.toJSON) == "function" ? s.toJSON() : null
  };
}
const Fm = Object.freeze({
  direct: "direct",
  exposure: "exposure"
});
function mj({ areaEffect: t = null, requiresTemplatedWorkflow: e = !1 } = {}) {
  return !e || (t == null ? void 0 : t.kind) === kt.persistent ? Fm.direct : Fm.exposure;
}
function fj(t = {}) {
  var e;
  return Array.isArray(t == null ? void 0 : t.targetSnapshots) ? t.targetSnapshots : Array.from(((e = game.user) == null ? void 0 : e.targets) ?? []).map(pC).filter(Boolean);
}
function Cl(t, e = {}) {
  var a, r, s, o, l, c, u, d, f;
  const n = String((e == null ? void 0 : e.sourceTokenId) ?? "").trim();
  if (n) {
    const m = ((r = (a = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : a.get) == null ? void 0 : r.call(a, n)) ?? ((l = (o = (s = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : s.placeables) == null ? void 0 : o.find) == null ? void 0 : l.call(o, (g) => (g == null ? void 0 : g.id) === n)) ?? null;
    if (m) return m;
  }
  return ((u = (c = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : c.controlled) == null ? void 0 : u.find((m) => {
    var g;
    return ((g = m.actor) == null ? void 0 : g.id) === (t == null ? void 0 : t.id);
  })) ?? null ?? ((f = (d = t == null ? void 0 : t.getActiveTokens) == null ? void 0 : d.call(t, !0, !0)) == null ? void 0 : f[0]) ?? null;
}
function bC(t = {}) {
  var n, i, a, r, s;
  const e = String((t == null ? void 0 : t.tokenId) ?? "").trim();
  return e ? ((i = (n = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : n.get) == null ? void 0 : i.call(n, e)) ?? ((s = (r = (a = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : a.placeables) == null ? void 0 : r.find) == null ? void 0 : s.call(r, (o) => (o == null ? void 0 : o.id) === e)) ?? null : null;
}
function gj(t = {}) {
  var e;
  return ((e = bC(t)) == null ? void 0 : e.actor) ?? null;
}
function pj(t, e) {
  var s, o, l, c;
  const n = canvas == null ? void 0 : canvas.grid, i = (t == null ? void 0 : t.center) ?? ((s = t == null ? void 0 : t.object) == null ? void 0 : s.center) ?? null, a = (e == null ? void 0 : e.center) ?? ((o = e == null ? void 0 : e.object) == null ? void 0 : o.center) ?? null;
  if (!n || !i || !a) return null;
  if (typeof n.measurePath == "function")
    try {
      const u = n.measurePath([i, a], { gridSpaces: !0 }), d = Number(
        (u == null ? void 0 : u.distance) ?? (u == null ? void 0 : u.cost) ?? (u == null ? void 0 : u.totalDistance) ?? (u == null ? void 0 : u.totalCost) ?? NaN
      );
      if (Number.isFinite(d)) return d;
    } catch {
    }
  const r = ((c = (l = foundry == null ? void 0 : foundry.canvas) == null ? void 0 : l.geometry) == null ? void 0 : c.Ray) ?? globalThis.Ray;
  if (typeof n.measureDistances == "function" && typeof r == "function")
    try {
      const u = n.measureDistances([{ ray: new r(i, a) }], { gridSpaces: !0 }), d = Number(Array.isArray(u) ? u[0] : NaN);
      if (Number.isFinite(d)) return d;
    } catch {
      return null;
    }
  return null;
}
function hj({ actor: t, payload: e, weapon: n, targets: i = [] } = {}) {
  const a = String((e == null ? void 0 : e.rangeBand) ?? "").trim().toLowerCase(), r = ((n == null ? void 0 : n.type) === "personalWeapon" || (n == null ? void 0 : n.isSynthetic)) && i.length === 1, s = xt(t) && i.length === 1;
  if (!r && !s)
    return a || String((n == null ? void 0 : n.defaultRangeBand) ?? "close").trim() || "close";
  const o = Cl(t, e), l = bC(i[0]), c = pj(o, l);
  if (s) {
    const d = md(c, (n == null ? void 0 : n.defaultRangeBand) ?? "close");
    return d === "outOfRange" ? d : a ? mS(n, a) ? a : "outOfRange" : mS(n, d) ? d : "outOfRange";
  }
  const u = nL(c, (n == null ? void 0 : n.range) ?? {}, (n == null ? void 0 : n.defaultRangeBand) ?? "close");
  return u === "outOfRange" ? u : a || ((n == null ? void 0 : n.type) !== "personalWeapon" && !(n != null && n.isSynthetic) || i.length !== 1 ? String((n == null ? void 0 : n.defaultRangeBand) ?? "close").trim() || "close" : u);
}
function yC(t) {
  return ["mechWeapon", "vehicleWeapon"].includes((t == null ? void 0 : t.canonicalType) ?? (t == null ? void 0 : t.type));
}
function bj(t = "") {
  return String(t ?? "").replace(/([a-z0-9])([A-Z])/g, "$1 $2").replace(/[_-]+/g, " ").replace(/\s+/g, " ").trim().replace(/\b\w/g, (e) => e.toUpperCase());
}
function yj(t = {}, e = {}) {
  return {
    close: Number(t.close ?? 0) + Number(e.close ?? 0),
    near: Number(t.near ?? 0) + Number(e.near ?? 0),
    far: Number(t.far ?? 0) + Number(e.far ?? 0),
    extreme: Number(t.extreme ?? 0) + Number(e.extreme ?? 0)
  };
}
function Sj(t, e) {
  var m, g, p, h;
  const n = String((e == null ? void 0 : e.sourceId) ?? (e == null ? void 0 : e.weaponGroupId) ?? ((m = e == null ? void 0 : e.machineWeaponGroup) == null ? void 0 : m.id) ?? "").trim();
  if (!n) return null;
  const i = Array.from(((g = t.system) == null ? void 0 : g.weaponGroups) ?? ((h = (p = t.system) == null ? void 0 : p.mwd) == null ? void 0 : h.weaponGroupDetails) ?? []).find((b) => String((b == null ? void 0 : b.id) ?? "").trim() === n) ?? null, a = Array.isArray(i == null ? void 0 : i.weaponIds) ? i.weaponIds : Array.isArray(i == null ? void 0 : i.weapons) ? i.weapons.map((b) => b == null ? void 0 : b.id).filter(Boolean) : [], r = a.map((b) => {
    var y, S;
    return (S = (y = t.items) == null ? void 0 : y.get) == null ? void 0 : S.call(y, b);
  }).filter((b) => b && yC(b));
  if (!i || !r.length) return null;
  const s = r.map((b) => {
    var y;
    return ((y = b.getCombatProfile) == null ? void 0 : y.call(b)) ?? null;
  }).filter(Boolean), o = s[0] ?? {}, l = s.reduce((b, y) => yj(b, y.attackRatingBand), {}), c = s.reduce((b, y) => b + (Number(y.damage ?? 0) || 0), 0), u = s.reduce((b, y) => b + Math.max(0, Number(y.clusteringDice ?? 0) || 0), 0), d = Math.max(0, ...s.map((b) => Number(b.ap ?? 0) || 0)), f = String(o.skill ?? "gunnery").trim() || "gunnery";
  return {
    id: i.id,
    uuid: t.uuid ?? null,
    name: i.name || "Weapon Group",
    img: o.img,
    type: "mechWeaponGroup",
    machineWeaponGroup: {
      id: i.id,
      weaponIds: a,
      weaponNames: r.map((b) => b.name)
    },
    category: o.category ?? "ranged",
    skill: f,
    skillDef: Ft(f),
    damage: c,
    clusteringDice: u,
    ap: d,
    baseDamageType: o.baseDamageType ?? o.damageType ?? "energy",
    baseDamageTypeLabel: o.baseDamageTypeLabel ?? o.damageTypeLabel ?? "Energy",
    damageType: o.damageType ?? o.baseDamageType ?? "energy",
    damageTypeLabel: o.damageTypeLabel ?? o.baseDamageTypeLabel ?? "Energy",
    attackRatingBand: l,
    range: o.range ?? {},
    defaultRangeBand: o.defaultRangeBand ?? "near",
    effects: {},
    notes: s.map((b) => b.notes).filter(Boolean).join(`
`)
  };
}
function Aj(t, e) {
  var s, o, l, c, u, d, f, m, g, p, h, b, y, S, A;
  const n = String((e == null ? void 0 : e.sourceType) ?? "").trim(), i = String((e == null ? void 0 : e.sourceId) ?? "").trim();
  if (xt(t) && ((s = e == null ? void 0 : e.syntheticWeapon) == null ? void 0 : s.id) === eg)
    return {
      ...Bh(t),
      ...e.syntheticWeapon,
      id: eg,
      damage: 0,
      attackRatingBand: {
        close: 0,
        near: 0,
        far: 0,
        extreme: 0,
        ...((o = e.syntheticWeapon) == null ? void 0 : o.attackRatingBand) ?? {}
      },
      isSynthetic: !0,
      defaultRangeBand: "close"
    };
  if (xt(t) && ((l = e == null ? void 0 : e.syntheticWeapon) == null ? void 0 : l.id) === xh)
    return { ...e.syntheticWeapon, isSynthetic: !0 };
  if (((c = e == null ? void 0 : e.syntheticWeapon) == null ? void 0 : c.id) === "unarmed") {
    const T = li.buildDefaultUnarmedProfile(t);
    return {
      ...T,
      ...e.syntheticWeapon,
      damage: T.damage,
      attackRatingBand: {
        ...((u = e.syntheticWeapon) == null ? void 0 : u.attackRatingBand) ?? T.attackRatingBand,
        close: T.attackRatingBand.close
      },
      isSynthetic: !0,
      defaultRangeBand: "close"
    };
  }
  if (xt(t)) {
    if ((t == null ? void 0 : t.type) === M.actorTypes.battlemech && (n === "weaponGroup" || e != null && e.weaponGroupId)) {
      const w = Cl(t, e), v = ((d = D.getSnapshot) == null ? void 0 : d.call(D, t, { token: w })) ?? null, N = v != null && v.isCurrentTurn ? ((f = D.getUsedWeaponGroupIds) == null ? void 0 : f.call(D, t, { token: w, snapshot: v })) ?? [] : [], P = ZL(t, i || e.weaponGroupId, {
        usedWeaponGroupIds: N
      });
      if (!(P != null && P.ok))
        throw dt((P == null ? void 0 : P.reason) || "That weapon group cannot attack right now.", {
          severity: "warn"
        });
      return P.profile;
    }
    const T = Sj(t, e);
    if (T) return T;
    const C = n === "mechWeapon" ? i : String((e == null ? void 0 : e.weaponId) ?? "").trim(), k = ((g = (m = t.items) == null ? void 0 : m.get) == null ? void 0 : g.call(m, C ?? "")) ?? null;
    if (!k || !yC(k))
      throw new Error("Machine attack requires an owned vehicle or BattleMech weapon.");
    return ((p = k.getCombatProfile) == null ? void 0 : p.call(k)) ?? null;
  }
  const a = n === "personalWeapon" ? i : String((e == null ? void 0 : e.weaponId) ?? "").trim(), r = ((b = (h = t.items) == null ? void 0 : h.get) == null ? void 0 : b.call(h, a ?? "")) ?? null;
  if (!r || !(((y = r.isPersonalWeapon) == null ? void 0 : y.call(r)) ?? r.type === "personalWeapon") || !((S = r.system) != null && S.equipped))
    throw new Error("Attack requires an equipped personal weapon.");
  return ((A = r.getCombatProfile) == null ? void 0 : A.call(r, { payloadId: e == null ? void 0 : e.payloadId })) ?? null;
}
function kj({ actor: t, payload: e, targets: n = [] } = {}) {
  var d;
  if (n.length !== 1) return null;
  const i = (game == null ? void 0 : game.combat) ?? null, a = Cl(t, e), r = Pn(a), s = ao((d = n[0]) == null ? void 0 : d.tokenId), o = D.getMoveActionCountForCombatant(r, { combat: i }), l = D.getMoveActionCountForCombatant(s, { combat: i }), c = o > 0 && !oi(t, "suppressAttackerMotionDN") ? 1 : 0, u = l === 0 ? -1 : l >= 2 ? 1 : 0;
  return {
    attackerMoves: o,
    targetMoves: l,
    attackerMotionDn: c,
    targetMotionDn: u,
    attackerMotionLabel: o > 0 ? "Attacker Moved" : "Attacker Stationary",
    targetMotionLabel: l === 0 ? "Target Stationary" : l >= 2 ? "Target Moved 2+" : "Target Moved",
    dnModifier: c + u
  };
}
async function Tj({ actor: t, payload: e } = {}) {
  var Be, R, V, B, me, oe, Ee, Ye, bt, yt, nn, Dn, On, Ln, $n, _n, cn, Bn, xn, U, Y, Ae, fe, we, Pe, Le, lt, qe, un, et, an, We, ke, Qe, zn;
  if (!t) throw new Error("resolveAttack requires actor");
  const n = Aj(t, e);
  if (!n) throw new Error("Unable to resolve weapon profile.");
  const i = xt(t) ? await Ut({
    machineActor: t,
    operatorActorUuid: String((e == null ? void 0 : e.operatorActorUuid) ?? "").trim()
  }) : null, a = (i == null ? void 0 : i.actor) ?? t, r = xt(t) ? _M({ machineActor: t, pilotActor: a, profile: n }) : n, s = xt(t) ? cj(t, {
    payload: e,
    resolved: {
      intent: "attack",
      attack: { weapon: r }
    }
  }) : null, o = uj({
    clusteringDice: Number((r == null ? void 0 : r.clusteringDice) ?? 0) || 0,
    clusteringTargetNumber: Number((r == null ? void 0 : r.clusteringTargetNumber) ?? 5) || 5,
    diceModifier: Number((s == null ? void 0 : s.diceModifier) ?? 0) || 0,
    targetNumberModifier: Number((s == null ? void 0 : s.targetNumberModifier) ?? 0) || 0
  }), l = {
    ...r,
    clusteringDice: o.dice,
    clusteringTargetNumber: o.targetNumber,
    clusteringBaseDice: o.baseDice,
    clusteringBaseTargetNumber: o.baseTargetNumber,
    clusteringModifiers: {
      diceModifier: o.diceModifier,
      targetNumberModifier: o.targetNumberModifier,
      sourceIds: Array.isArray(s == null ? void 0 : s.sourceIds) ? [...s.sourceIds] : [],
      sourceNames: Array.isArray(s == null ? void 0 : s.sourceNames) ? [...s.sourceNames] : [],
      sourceLabel: String((s == null ? void 0 : s.sourceLabel) ?? "").trim()
    }
  };
  if (xt(t)) {
    const ct = PO(t, {
      weaponGroupId: e == null ? void 0 : e.weaponGroupId,
      weaponId: e == null ? void 0 : e.weaponId,
      weapon: l
    });
    if (ct.blocked)
      throw dt(ct.reason || "That weapon group cannot attack right now.", {
        severity: "warn"
      });
    const It = fO(t, {
      weaponGroupId: e == null ? void 0 : e.weaponGroupId,
      weaponId: e == null ? void 0 : e.weaponId,
      weapon: l
    });
    if (It.blocked)
      throw dt(It.reason || "The machine's current state prevents this attack.", {
        severity: "warn"
      });
  }
  if (Array.isArray((Be = l == null ? void 0 : l.capabilityReport) == null ? void 0 : Be.errors) && l.capabilityReport.errors.length > 0)
    throw dt(
      ((R = l.capabilityReport.errors[0]) == null ? void 0 : R.message) ?? "Weapon capability data is invalid for this attack.",
      { severity: "warn" }
    );
  const c = Ft(l.skill) ?? {
    code: l.skill,
    label: l.skill || "Attack",
    attribute: "reflexes",
    domains: ["physical"]
  }, u = String(c.attribute ?? "reflexes").trim() || "reflexes", d = bj(u), f = ((V = a.getAttributeValue) == null ? void 0 : V.call(a, u)) ?? Number(((oe = (me = (B = a.system) == null ? void 0 : B.attributes) == null ? void 0 : me[u]) == null ? void 0 : oe.value) ?? 0), m = ((Ee = a.getSkillRating) == null ? void 0 : Ee.call(a, l.skill)) ?? Number(((yt = (bt = (Ye = a.system) == null ? void 0 : Ye.skills) == null ? void 0 : bt[l.skill]) == null ? void 0 : yt.rating) ?? 0), g = Number(((On = (Dn = (nn = a.system) == null ? void 0 : nn.skills) == null ? void 0 : Dn[l.skill]) == null ? void 0 : On.bonus) ?? 0), p = new Set(Ts(a.system ?? {}, l.skill)), h = rp(l.skill, e == null ? void 0 : e.specializationKey), b = h && p.has(h.key) ? h : null, y = b ? np : 0, S = Number(((Ln = l == null ? void 0 : l.effects) == null ? void 0 : Ln.accuracyMod) ?? 0) || 0, A = g + S, T = fj(e), C = hj({ actor: t, payload: e, weapon: l, targets: T }), k = (l == null ? void 0 : l.type) === "personalWeapon" || l != null && l.isSynthetic ? dd(C) : Hp(C), w = Number((($n = l == null ? void 0 : l.attackRatingBand) == null ? void 0 : $n[C]) ?? 0) || 0, v = !!((_n = l == null ? void 0 : l.capabilityReport) != null && _n.isTemplated), N = Gi((l == null ? void 0 : l.areaEffect) ?? {}), P = mj({ areaEffect: N, requiresTemplatedWorkflow: v }), _ = (l == null ? void 0 : l.type) === "personalWeapon" || (l == null ? void 0 : l.isSynthetic) ? kj({ actor: t, payload: e, targets: T }) : null, O = ((_ == null ? void 0 : _.attackerMoves) ?? 0) > 0, x = !!((cn = e == null ? void 0 : e.aim) != null && cn.active) && !v && T.length === 1 && !O && l.category !== "melee", H = x ? Math.max(0, ((Bn = a.getSkillRating) == null ? void 0 : Bn.call(a, "perception")) ?? Number(((Y = (U = (xn = a.system) == null ? void 0 : xn.skills) == null ? void 0 : U.perception) == null ? void 0 : Y.rating) ?? 0)) : 0, G = (Ae = e == null ? void 0 : e.aim) != null && Ae.active ? {
    active: !0,
    eligible: x,
    ineligibleReason: v ? "Aim cannot apply to template attacks." : T.length !== 1 ? "Aim cannot apply to multi-target attacks." : O ? "Aim is spoiled after moving this activation." : l.category === "melee" ? "Aim does not apply to melee attacks." : "",
    skillCode: l.skill,
    skillLabel: c.label ?? l.skill ?? "Attack Skill",
    bonusSkillCode: "perception",
    bonusSkillLabel: "Perception",
    bonusValue: H
  } : null;
  if (!v && T.length === 0)
    throw dt("Target at least one token to attack.", { severity: "warn" });
  const q = Number(l.ap ?? 0) + Number(((fe = l == null ? void 0 : l.effects) == null ? void 0 : fe.ap) ?? 0), ie = e != null && e.attackOptions && typeof e.attackOptions == "object" ? e.attackOptions : {};
  if (xt(t) && ie.losBlocked && !ie.indirectAttack)
    throw dt("Line of sight is fully blocked. Use Indirect Attack or sensor-enabled fire.", { severity: "warn" });
  const Z = l.category === "melee" ? "attack.melee" : "attack.ranged", Se = [
    l.damageType,
    l.baseDamageType,
    l.weaponType,
    l.weaponCategory
  ].map((ct) => String(ct ?? "").trim().toLowerCase()).filter(Boolean).map((ct) => `attack.${ct}`), se = xt(t) && T.length === 1 ? Ph({
    attackerCombatant: Pn(Cl(t, e)),
    targetActor: gj(T[0]),
    targetCombatant: ao((we = T[0]) == null ? void 0 : we.tokenId),
    payload: e,
    suppressAttackerMotion: !!(e != null && e.chargeAttack) || String(((Pe = e == null ? void 0 : e.syntheticWeapon) == null ? void 0 : Pe.id) ?? "") === xh
  }) : null, te = (l == null ? void 0 : l.type) === "personalWeapon" || l != null && l.isSynthetic ? tL(C, 1) : mw(C, 1), pe = Number.isFinite(Number(e == null ? void 0 : e.dn)), K = [{
    id: "difficulty.current",
    label: (l == null ? void 0 : l.type) === "personalWeapon" || l != null && l.isSynthetic ? `Base DN (${k})` : `Base DN (${k})`,
    value: te,
    tags: ["base", "range"]
  }];
  se != null && se.attackerMotionDn && K.push({
    id: "machineMotion.attacker",
    label: `Attacker Motion (${se.attackerMotionLabel})`,
    value: se.attackerMotionDn,
    tags: ["motion"]
  }), se != null && se.motionDn && K.push({
    id: "machineMotion.actions",
    label: `Target Motion (${se.targetMotionLabel})`,
    value: se.motionDn,
    tags: ["motion"]
  }), se != null && se.jumpDn && K.push({
    id: "machineMotion.jump",
    label: "Target Jumped",
    value: se.jumpDn,
    tags: ["motion", "jump"]
  }), _ != null && _.attackerMotionDn && K.push({
    id: "personalMotion.attacker",
    label: _.attackerMotionLabel,
    value: _.attackerMotionDn,
    tags: ["motion"]
  }), _ != null && _.targetMotionDn && K.push({
    id: "personalMotion.target",
    label: _.targetMotionLabel,
    value: _.targetMotionDn,
    tags: ["motion"]
  });
  const be = K.reduce((ct, It) => ct + Number(It.value ?? 0), 0), Ie = pe ? Number(e.dn) : be;
  if (xt(t) && bO(t) && C !== "close")
    throw dt("Sensor Blind limits attacks to Close range.", { severity: "warn" });
  let Oe = null;
  if (xt(t) && T.length > 0) {
    const ct = T[0], It = String((ct == null ? void 0 : ct.tokenUuid) ?? "").trim(), Mi = Cl(t, e), Fn = Pn(Mi), Et = (lt = (Le = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : Le.get) == null ? void 0 : lt.call(Le, It), sr = (Et == null ? void 0 : Et.visible) ?? !0 ? bd(Fn, It, Et == null ? void 0 : Et.actor) : "blind";
    if (Fn && sr === "blind")
      throw dt("No targeting solution. Acquire contact first.", { severity: "warn" });
    const ec = Number(((et = (un = (qe = t == null ? void 0 : t.system) == null ? void 0 : qe.attributes) == null ? void 0 : un.system) == null ? void 0 : et.value) ?? 0) || 0, Qi = Xp(Fn, It, ec, sr, (an = game.combat) == null ? void 0 : an.round);
    Oe = {
      detectionState: sr,
      detectionStateLabel: wn(sr),
      targetTokenUuid: It,
      attackerCombatantId: (Fn == null ? void 0 : Fn.id) ?? null,
      activePacketId: (Qi == null ? void 0 : Qi.id) ?? null,
      targetingDataValue: (Qi == null ? void 0 : Qi.value) ?? 0
    };
  }
  return {
    intent: "attack",
    rollType: "simple",
    title: `${l.name} Attack`,
    subtitle: t.name ?? "Actor",
    domains: Array.isArray(c.domains) && c.domains.length ? c.domains : ["physical"],
    domainTags: Array.from(/* @__PURE__ */ new Set(["combat", "attack", Z, ...Se])),
    diceTarget: Number.isFinite(Number(e == null ? void 0 : e.diceTarget)) ? Number(e.diceTarget) : 5,
    difficulty: { dn: Ie },
    dn: {
      parts: pe ? [{
        id: "difficulty.override",
        label: "DN Override",
        value: Ie,
        tags: ["override"]
      }] : K,
      total: Ie
    },
    edge: {
      earn: { enabled: !0, rate: 4, maxPerRoll: 1 }
    },
    pool: { attribute: f, skill: m, bonus: A + H, specialization: y },
    breakdown: [
      {
        id: "attribute",
        label: (We = i == null ? void 0 : i.actor) != null && We.name ? `${d} (${i.actor.name})` : d,
        value: f
      },
      { id: "skill", label: (ke = i == null ? void 0 : i.actor) != null && ke.name ? `${c.label} (${i.actor.name})` : c.label, value: m },
      { id: "bonus", label: "Skill Bonus", value: g },
      ...b ? [{
        id: "specialization",
        label: `Specialization (${b.label})`,
        value: y
      }] : [],
      { id: "weaponAccuracy", label: "Weapon Accuracy", value: S },
      ...H ? [{ id: "aim.perception", label: "Aim (Perception)", value: H }] : [],
      { id: "damage", label: "Damage", value: Number(l.damage ?? 0) || 0 },
      ...o.active ? [
        { id: "clusterDice", label: "Cluster Dice", value: o.dice },
        { id: "clusterTarget", label: "Cluster TN", value: o.targetNumber }
      ] : [],
      { id: "ap", label: "AP", value: q },
      { id: "attackRating", label: `Attack Rating (${k})`, value: w }
    ],
    attack: {
      rangeBand: C,
      weapon: l,
      payload: (l == null ? void 0 : l.payload) ?? null,
      payloadState: (l == null ? void 0 : l.payloadState) ?? null,
      source: (l == null ? void 0 : l.source) ?? null,
      sourceState: (l == null ? void 0 : l.sourceState) ?? null,
      template: (l == null ? void 0 : l.template) ?? null,
      areaEffect: N,
      damageScaling: P,
      templateGeometry: (e == null ? void 0 : e.templateGeometry) ?? null,
      templatePlacement: (e == null ? void 0 : e.templatePlacement) ?? null,
      resolution: (l == null ? void 0 : l.resolution) ?? null,
      resolverKey: (l == null ? void 0 : l.resolverKey) ?? "standard",
      fireModes: (l == null ? void 0 : l.fireModes) ?? null,
      keywords: (l == null ? void 0 : l.keywords) ?? [],
      capabilityReport: (l == null ? void 0 : l.capabilityReport) ?? null,
      skill: {
        code: c.code ?? l.skill,
        label: c.label ?? l.skill,
        attribute: u,
        specialization: b ? {
          key: b.key,
          label: b.label,
          value: y
        } : null
      },
      targets: T,
      aim: G,
      totalAp: q,
      ewContext: Oe,
      machineMotion: se,
      personalMotion: _,
      operator: i ? {
        actorUuid: ((Qe = i.actor) == null ? void 0 : Qe.uuid) ?? "",
        name: ((zn = i.actor) == null ? void 0 : zn.name) ?? "",
        source: i.source ?? "",
        reason: i.reason ?? ""
      } : null,
      attackOptions: {
        indirectAttack: !!ie.indirectAttack,
        losBlocked: !!ie.losBlocked
      }
    },
    rollActor: a,
    specialization: b ? {
      key: b.key,
      label: b.label,
      value: y,
      skillKey: c.code ?? l.skill
    } : null
  };
}
async function wj({ actor: t, payload: e, event: n } = {}) {
  return {
    title: "Defense",
    intent: "defense",
    domainTags: ["combat"],
    pool: { attribute: 0, skill: 0, bonus: 0 },
    breakdown: [],
    mods: []
  };
}
async function Mj({ actor: t, payload: e, event: n } = {}) {
  const i = vj(e), a = Zg(t), r = Cj(i, a), s = [];
  return r > 0 && s.push({ label: "Sealed Armor", value: r }), {
    title: "Resistance",
    intent: "resistance",
    domainTags: ["combat"],
    pool: { attribute: 0, skill: 0, bonus: r },
    breakdown: s,
    mods: []
  };
}
function vj(t = {}) {
  const e = t && typeof t == "object" ? t : {}, n = [
    e.damageType,
    e.resistanceType,
    e.hazardType,
    e.type,
    e.category,
    ...Array.isArray(e.tags) ? e.tags : [],
    ...Array.isArray(e.keywords) ? e.keywords : [],
    ...Array.isArray(e.damageTags) ? e.damageTags : [],
    ...Array.isArray(e.hazardTags) ? e.hazardTags : []
  ];
  return new Set(n.map((i) => String(i ?? "").trim().toLowerCase()).filter(Boolean));
}
function Cj(t, e = {}) {
  var a, r;
  const n = (t.has("gas") || t.has("gases")) && Number(((a = e == null ? void 0 : e.resistanceDice) == null ? void 0 : a.gas) ?? 0) || 0, i = (t.has("chemical") || t.has("chemicals")) && Number(((r = e == null ? void 0 : e.resistanceDice) == null ? void 0 : r.chemical) ?? 0) || 0;
  return Math.max(0, n, i);
}
function SC(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function VS(t, e) {
  var n, i, a;
  return Math.max(0, SC((a = (i = (n = t == null ? void 0 : t.system) == null ? void 0 : n.attributes) == null ? void 0 : i[e]) == null ? void 0 : a.value, 0));
}
function Ij(t) {
  return (t == null ? void 0 : t.type) === M.actorTypes.vehicle || (t == null ? void 0 : t.type) === M.actorTypes.battlemech;
}
function Ej({ machineActor: t = null, pilotActor: e = null } = {}) {
  var l, c, u, d;
  const n = VS(t, M.actorAttributes.handling), i = VS(t, M.actorAttributes.system), a = Math.max(0, SC(
    ((l = e == null ? void 0 : e.getAttributeValue) == null ? void 0 : l.call(e, M.actorAttributes.reflexes)) ?? ((d = (u = (c = e == null ? void 0 : e.system) == null ? void 0 : c.attributes) == null ? void 0 : u[M.actorAttributes.reflexes]) == null ? void 0 : d.value),
    0
  )), r = i > n ? M.actorAttributes.system : M.actorAttributes.handling, s = i > n ? "System" : "Handling", o = Math.max(n, i);
  return {
    handling: n,
    system: i,
    machineAttributeKey: r,
    machineAttributeLabel: s,
    machineAttributeValue: o,
    pilotReflexes: a,
    totalBonus: o + a
  };
}
async function Nj({ actor: t } = {}) {
  var i, a, r, s, o, l, c, u;
  if (Ij(t)) {
    const d = await Ut({ machineActor: t }), f = Ej({
      machineActor: t,
      pilotActor: d.actor
    }), m = (i = d.actor) != null && i.name ? `Pilot REF (${d.actor.name})` : "Pilot REF";
    return {
      intent: "initiative",
      title: "Initiative",
      rollType: "sum",
      domains: ["combat", "machine"],
      sum: {
        formula: "2d6 + @machine + @pilotReflexes",
        data: {
          machine: f.machineAttributeValue,
          pilotReflexes: f.pilotReflexes
        }
      },
      breakdown: [
        { id: "base", label: "2d6", value: 0 },
        {
          id: `machine.${f.machineAttributeKey}`,
          label: `Best Machine (${f.machineAttributeLabel})`,
          value: f.machineAttributeValue
        },
        {
          id: "pilot.reflexes",
          label: m,
          value: f.pilotReflexes,
          title: d.reason
        }
      ],
      pool: { attribute: 0, skill: 0, bonus: 0 }
    };
  }
  const e = Number(((a = t.getAttributeValue) == null ? void 0 : a.call(t, "reflexes")) ?? ((o = (s = (r = t.system) == null ? void 0 : r.attributes) == null ? void 0 : s.reflexes) == null ? void 0 : o.value) ?? 0), n = Number(((u = (c = (l = t.system) == null ? void 0 : l.attributes) == null ? void 0 : c.edge) == null ? void 0 : u.value) ?? 0);
  return {
    intent: "initiative",
    title: "Initiative",
    rollType: "sum",
    // <- tells executor how to roll
    domains: ["combat"],
    // <- for organizational/filtering purposes
    sum: {
      formula: "2d6 + @ref + @edge",
      data: { ref: e, edge: n }
    },
    breakdown: [
      { id: "base", label: "2d6", value: 0 },
      { id: "ref", label: "REF", value: e },
      { id: "edge", label: "EDGE", value: n }
    ],
    // keep pool numeric to satisfy normalizeResolvedContext (if still used)
    pool: { attribute: 0, skill: 0, bonus: 0 }
  };
}
async function Pj({ actor: t }) {
  var r, s, o, l, c;
  const e = Number(((s = (r = t.system) == null ? void 0 : r.burn) == null ? void 0 : s.value) ?? 0), n = t.overloadThreshold;
  e < n && ui.notifications.warn(`Overload check is only required at Burn ${n}+.`);
  const i = Number(((c = (l = (o = t.system) == null ? void 0 : o.attributes) == null ? void 0 : l.guts) == null ? void 0 : c.value) ?? 0), a = oi(t, "overloadDNMod");
  return {
    intent: "overload",
    title: "Overload Check",
    domains: ["mental"],
    pool: {
      attribute: i,
      skill: i,
      bonus: 0
    },
    difficulty: {
      dn: Math.max(0, e - (n - 1) + a)
    },
    breakdown: [
      { id: "guts1", label: "Guts", value: i },
      { id: "guts2", label: "Guts", value: i }
    ]
  };
}
function Pg(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function Rj(t = null, e = "") {
  var n, i, a, r, s, o;
  return {
    rating: Pg((a = (i = (n = t == null ? void 0 : t.system) == null ? void 0 : n.skills) == null ? void 0 : i[e]) == null ? void 0 : a.rating, 0),
    bonus: Pg((o = (s = (r = t == null ? void 0 : t.system) == null ? void 0 : r.skills) == null ? void 0 : s[e]) == null ? void 0 : o.bonus, 0)
  };
}
function JS(t = "", e = null) {
  const n = String(t ?? "").trim(), i = String((e == null ? void 0 : e.name) ?? "").trim();
  return i ? `${n} (${i})` : n;
}
async function Dj({ actor: t, payload: e } = {}) {
  var c, u, d, f, m, g, p, h, b, y;
  const n = await QM(e, {
    gmOverride: !!(e != null && e.gmOverride)
  });
  if (!n.ok)
    throw new Error(n.reason ?? "Machine remedy could not be prepared.");
  const i = await Xl(e, {
    gmOverride: !!(e != null && e.gmOverride)
  });
  if (!i.ok)
    throw new Error(i.reason ?? "Machine remedy could not be resolved.");
  const a = Ft(i.skillKey);
  if (!a)
    throw new Error(`Machine remedy is missing a valid skill: ${i.skillKey}`);
  const r = n.actor ?? t ?? i.operatorActor ?? i.machineActor, s = Pg((f = (d = (u = (c = i.machineActor) == null ? void 0 : c.system) == null ? void 0 : u.attributes) == null ? void 0 : d.reliability) == null ? void 0 : f.value, 0), o = Rj(r, i.skillKey), l = i.crit ?? i.issue ?? {};
  return {
    intent: "machineRemedy",
    rollType: "simple",
    title: `${i.remedy.label}: ${l.label ?? "Critical Remedy"}`,
    subtitle: `${((m = i.machineActor) == null ? void 0 : m.name) ?? "Machine"} | ${l.locationLabel ?? i.locationKey}`,
    domains: ["mental"],
    diceTarget: 5,
    difficulty: { dn: i.totalDn },
    pool: {
      attribute: s,
      skill: o.rating,
      bonus: o.bonus,
      specialization: 0
    },
    breakdown: [
      { id: "attribute", label: JS("Reliability", i.machineActor), value: s },
      { id: "skill", label: JS(a.label, i.operatorActor), value: o.rating },
      ...o.bonus ? [{ id: "bonus", label: "Skill Bonus", value: o.bonus }] : []
    ],
    specialization: null,
    machineRemedy: {
      machineActorUuid: ((g = i.machineActor) == null ? void 0 : g.uuid) ?? "",
      operatorActorUuid: ((p = i.operatorActor) == null ? void 0 : p.uuid) ?? "",
      critId: ((h = i.crit) == null ? void 0 : h.id) ?? "",
      critLabel: l.label ?? "",
      locationKey: i.locationKey,
      locationLabel: l.locationLabel ?? i.locationKey,
      remedyKey: i.remedy.key,
      remedyLabel: i.remedy.label,
      skillKey: i.skillKey,
      skillLabel: a.label,
      baseDn: i.baseDn,
      conditionModifier: i.locationConditionModifier,
      conditionLabel: i.locationConditionLabel,
      conditionValue: i.locationCondition,
      totalDn: i.totalDn,
      gmOverride: i.gmOverride,
      remedyEffect: i.remedyEffect,
      cost: i.remedy.cost
    },
    data: {
      skillKey: i.skillKey,
      attrKey: "reliability",
      machineActorUuid: ((b = i.machineActor) == null ? void 0 : b.uuid) ?? "",
      operatorActorUuid: ((y = i.operatorActor) == null ? void 0 : y.uuid) ?? "",
      label: `reliability+${a.label}`
    },
    rollActor: r,
    machineActor: i.machineActor
  };
}
function Oj(t) {
  return (t == null ? void 0 : t.type) === M.actorTypes.vehicle || (t == null ? void 0 : t.type) === M.actorTypes.battlemech;
}
function Lj(t) {
  var i, a, r, s, o;
  const e = String((t == null ? void 0 : t.targetTokenUuid) ?? "").trim();
  if (e)
    return ((a = (i = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : i.placeables) == null ? void 0 : a.find((l) => {
      var c;
      return (((c = l.document) == null ? void 0 : c.uuid) ?? l.uuid) === e;
    })) ?? null;
  const n = String((t == null ? void 0 : t.targetTokenId) ?? "").trim();
  return n ? ((s = (r = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : r.get) == null ? void 0 : s.call(r, n)) ?? null : Array.from(((o = game.user) == null ? void 0 : o.targets) ?? []).find((l) => l.actor) ?? null;
}
function $j(t, e) {
  var i, a, r, s, o, l, c, u;
  const n = String((e == null ? void 0 : e.sourceTokenId) ?? "").trim();
  return n ? ((a = (i = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : i.get) == null ? void 0 : a.call(i, n)) ?? ((s = (r = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : r.placeables) == null ? void 0 : s.find((d) => d.id === n)) ?? null : ((l = (o = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : o.controlled) == null ? void 0 : l.find((d) => {
    var f;
    return ((f = d.actor) == null ? void 0 : f.id) === (t == null ? void 0 : t.id);
  })) ?? ((u = (c = t == null ? void 0 : t.getActiveTokens) == null ? void 0 : c.call(t, !0, !0)) == null ? void 0 : u[0]) ?? null;
}
function _j(t, e = "Target") {
  var n;
  return String((t == null ? void 0 : t.name) ?? ((n = t == null ? void 0 : t.actor) == null ? void 0 : n.name) ?? e).trim() || e;
}
function YS(t = "", e = null) {
  const n = String(t ?? "").trim(), i = String((e == null ? void 0 : e.name) ?? "").trim();
  return i ? `${n} (${i})` : n;
}
async function QS({ actor: t, payload: e } = {}) {
  var k, w, v, N, P, L, _, O, x, H, G, q, ie, Z, Se;
  if (!t) throw new Error("resolveAcquire requires actor");
  if (!Oj(t))
    throw dt("Acquire is a machine action.", { severity: "warn" });
  if (gO(t))
    throw dt("Sensor actions are blocked by the machine's current state.", { severity: "warn" });
  const n = Lj(e);
  if (!(n != null && n.actor))
    throw dt("Target a token to acquire.", { severity: "warn" });
  const i = n.actor, a = ((k = n.document) == null ? void 0 : k.uuid) ?? n.uuid ?? "", r = _j(n), s = $j(t, e), o = Pn(s), l = ro(o, a);
  if (l === "lock")
    throw dt("Target is already at lock state.", { severity: "info" });
  const c = Mw(i);
  if (l === "track" && c === "track")
    throw dt("ECM prevents advancing beyond Track on this target.", { severity: "warn" });
  const u = await Ut({
    machineActor: t,
    operatorActorUuid: String((e == null ? void 0 : e.operatorActorUuid) ?? "").trim()
  }), d = u.actor ?? t, f = "system", m = "perception", g = Math.max(0, Number(((N = (v = (w = t == null ? void 0 : t.system) == null ? void 0 : w.attributes) == null ? void 0 : v[f]) == null ? void 0 : N.value) ?? 0) || 0), p = Ft("perception"), h = Number(((_ = (L = (P = d == null ? void 0 : d.system) == null ? void 0 : P.skills) == null ? void 0 : L[m]) == null ? void 0 : _.rating) ?? 0) || 0, b = Number(((H = (x = (O = d == null ? void 0 : d.system) == null ? void 0 : O.skills) == null ? void 0 : x[m]) == null ? void 0 : H.bonus) ?? 0) || 0, y = nw(l), S = zL(i, {
    attacker: t,
    payload: e,
    resolved: {
      intent: "acquire",
      acquire: { currentState: l }
    }
  }), A = EL(t, i, {
    intent: "acquire",
    currentState: l,
    payload: e,
    attackerToken: s,
    targetToken: n,
    rangeBand: e == null ? void 0 : e.rangeBand
  }), T = A.reduce((se, te) => se + (Number(te.value ?? 0) || 0), 0), C = y + S + T;
  return {
    intent: "acquire",
    rollType: "simple",
    title: "Acquire Target",
    subtitle: r,
    domains: ["mental"],
    domainTags: ["sensor", "sensor.acquire", "skill.perception"],
    diceTarget: 5,
    difficulty: { dn: C },
    dn: {
      parts: [
        { id: "difficulty.base", label: "Base DN", value: y, tags: ["base"] },
        ...S ? [{ id: "ew.ecmDn", label: "ECM Modifier", value: S, tags: ["ew"] }] : [],
        ...A
      ],
      total: C
    },
    edge: { allowed: ["pre", "post"], earn: { enabled: !0, rate: 4, maxPerRoll: 1 } },
    pool: {
      attribute: g,
      skill: h,
      bonus: b,
      specialization: 0
    },
    breakdown: [
      { id: "attribute", label: YS("System", t), value: g },
      { id: "skill", label: YS((p == null ? void 0 : p.label) ?? "Perception", u.actor), value: h },
      ...b ? [{ id: "bonus", label: "Perception Bonus", value: b }] : []
    ],
    specialization: null,
    data: {
      skillKey: m,
      attrKey: f,
      machineActorUuid: t.uuid ?? "",
      operatorActorUuid: ((G = u.actor) == null ? void 0 : G.uuid) ?? "",
      label: `${f}+${(p == null ? void 0 : p.label) ?? "Perception"}`
    },
    acquire: {
      machineActorUuid: t.uuid ?? "",
      operatorActorUuid: ((q = u.actor) == null ? void 0 : q.uuid) ?? "",
      operatorName: ((ie = u.actor) == null ? void 0 : ie.name) ?? "",
      attackerTokenId: (s == null ? void 0 : s.id) ?? ((Z = s == null ? void 0 : s.document) == null ? void 0 : Z.id) ?? "",
      attackerTokenUuid: ((Se = s == null ? void 0 : s.document) == null ? void 0 : Se.uuid) ?? (s == null ? void 0 : s.uuid) ?? "",
      attackerCombatantId: (o == null ? void 0 : o.id) ?? "",
      targetTokenUuid: a,
      targetTokenId: (n == null ? void 0 : n.id) ?? "",
      targetName: r,
      currentState: l,
      currentStateLabel: wn(l),
      ceiling: c
    },
    rollActor: d,
    machineActor: t
  };
}
function Bj(t) {
  return (t == null ? void 0 : t.type) === M.actorTypes.vehicle || (t == null ? void 0 : t.type) === M.actorTypes.battlemech;
}
function xj(t) {
  var i, a, r, s, o;
  const e = String((t == null ? void 0 : t.targetTokenUuid) ?? "").trim();
  if (e)
    return ((a = (i = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : i.placeables) == null ? void 0 : a.find((l) => {
      var c;
      return (((c = l.document) == null ? void 0 : c.uuid) ?? l.uuid) === e;
    })) ?? null;
  const n = String((t == null ? void 0 : t.targetTokenId) ?? "").trim();
  return n ? ((s = (r = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : r.get) == null ? void 0 : s.call(r, n)) ?? null : Array.from(((o = game.user) == null ? void 0 : o.targets) ?? []).find((l) => l.actor) ?? null;
}
function zj(t, e) {
  var i, a, r, s, o, l, c, u;
  const n = String((e == null ? void 0 : e.sourceTokenId) ?? "").trim();
  return n ? ((a = (i = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : i.get) == null ? void 0 : a.call(i, n)) ?? ((s = (r = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : r.placeables) == null ? void 0 : s.find((d) => d.id === n)) ?? null : ((l = (o = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : o.controlled) == null ? void 0 : l.find((d) => {
    var f;
    return ((f = d.actor) == null ? void 0 : f.id) === (t == null ? void 0 : t.id);
  })) ?? ((u = (c = t == null ? void 0 : t.getActiveTokens) == null ? void 0 : c.call(t, !0, !0)) == null ? void 0 : u[0]) ?? null;
}
function Fj(t, e = "Target") {
  var n;
  return String((t == null ? void 0 : t.name) ?? ((n = t == null ? void 0 : t.actor) == null ? void 0 : n.name) ?? e).trim() || e;
}
function XS(t = "", e = null) {
  const n = String(t ?? "").trim(), i = String((e == null ? void 0 : e.name) ?? "").trim();
  return i ? `${n} (${i})` : n;
}
const Um = 2;
async function ZS({ actor: t, payload: e } = {}) {
  var b, y, S, A, T, C, k, w, v, N, P, L, _, O, x;
  if (!t) throw new Error("resolveTargeting requires actor");
  if (!Bj(t))
    throw dt("Targeting is a machine action.", { severity: "warn" });
  if (pO(t))
    throw dt("Targeting Data cannot be generated in the machine's current state.", { severity: "warn" });
  const n = xj(e);
  if (!(n != null && n.actor))
    throw dt("Target a token to generate targeting data.", { severity: "warn" });
  const i = ((b = n.document) == null ? void 0 : b.uuid) ?? n.uuid ?? "", a = Fj(n), r = zj(t, e), s = Pn(r), o = bd(s, i, n.actor);
  if (o !== "track" && o !== "lock")
    throw dt("Track or Lock is required to generate targeting data.", { severity: "warn" });
  const l = await Ut({
    machineActor: t,
    operatorActorUuid: String((e == null ? void 0 : e.operatorActorUuid) ?? "").trim()
  }), c = l.actor ?? t, u = "system", d = "gunnery", f = Math.max(0, Number(((A = (S = (y = t == null ? void 0 : t.system) == null ? void 0 : y.attributes) == null ? void 0 : S[u]) == null ? void 0 : A.value) ?? 0) || 0), m = Ft(d), g = Number(((k = (C = (T = c == null ? void 0 : c.system) == null ? void 0 : T.skills) == null ? void 0 : C[d]) == null ? void 0 : k.rating) ?? 0) || 0, p = Number(((N = (v = (w = c == null ? void 0 : c.system) == null ? void 0 : w.skills) == null ? void 0 : v[d]) == null ? void 0 : N.bonus) ?? 0) || 0, h = _p(f, o);
  return {
    intent: "targeting",
    rollType: "simple",
    title: "Generate Targeting Data",
    subtitle: a,
    domains: ["mental"],
    domainTags: ["sensor", "sensor.targeting", "skill.gunnery"],
    diceTarget: 5,
    difficulty: { dn: Um },
    dn: {
      parts: [{ id: "difficulty.base", label: "DN (Targeting)", value: Um, tags: ["base"] }],
      total: Um
    },
    edge: { allowed: ["pre", "post"], earn: { enabled: !0, rate: 4, maxPerRoll: 1 } },
    pool: {
      attribute: f,
      skill: g,
      bonus: p,
      specialization: 0
    },
    breakdown: [
      { id: "attribute", label: XS("System", t), value: f },
      { id: "skill", label: XS((m == null ? void 0 : m.label) ?? "Gunnery", l.actor), value: g },
      ...p ? [{ id: "bonus", label: "Gunnery Bonus", value: p }] : []
    ],
    specialization: null,
    data: {
      skillKey: d,
      attrKey: u,
      machineActorUuid: t.uuid ?? "",
      operatorActorUuid: ((P = l.actor) == null ? void 0 : P.uuid) ?? "",
      label: `${u}+${(m == null ? void 0 : m.label) ?? "Gunnery"}`
    },
    targeting: {
      machineActorUuid: t.uuid ?? "",
      operatorActorUuid: ((L = l.actor) == null ? void 0 : L.uuid) ?? "",
      operatorName: ((_ = l.actor) == null ? void 0 : _.name) ?? "",
      attackerTokenId: (r == null ? void 0 : r.id) ?? ((O = r == null ? void 0 : r.document) == null ? void 0 : O.id) ?? "",
      attackerTokenUuid: ((x = r == null ? void 0 : r.document) == null ? void 0 : x.uuid) ?? (r == null ? void 0 : r.uuid) ?? "",
      attackerCombatantId: (s == null ? void 0 : s.id) ?? "",
      targetTokenUuid: i,
      targetTokenId: (n == null ? void 0 : n.id) ?? "",
      targetName: a,
      detectionState: o,
      detectionStateLabel: wn(o),
      cap: h
    },
    rollActor: c,
    machineActor: t
  };
}
const AC = Object.freeze({
  blind: 1,
  contact: 1,
  track: 2,
  lock: 3
}), kC = Object.freeze({
  open: -1,
  exposed: -1,
  typical: 0,
  battlefield: 0,
  covered: 1,
  terrain: 1,
  woods: 1,
  urban: 1,
  ridge: 1,
  ecm: 1
});
function Uj(t) {
  return (t == null ? void 0 : t.type) === M.actorTypes.vehicle || (t == null ? void 0 : t.type) === M.actorTypes.battlemech;
}
function So(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function Kj(t = "") {
  const e = String(t ?? "").trim().toLowerCase().replace(/[^a-z0-9]+/g, "");
  return e === "openground" || e === "fullyexposed" ? "open" : e === "typicalbattlefield" ? "typical" : e === "ridgeline" ? "ridge" : Object.prototype.hasOwnProperty.call(kC, e) ? e : "typical";
}
function Rg(t = "", e = "contact") {
  const n = String(t ?? "").trim().toLowerCase();
  return Object.prototype.hasOwnProperty.call(AC, n) ? n : e;
}
function TC(t = "") {
  var n, i, a, r, s, o;
  const e = String(t ?? "").trim();
  return e ? ((a = (i = (n = globalThis.canvas) == null ? void 0 : n.tokens) == null ? void 0 : i.get) == null ? void 0 : a.call(i, e)) ?? ((o = (s = (r = globalThis.canvas) == null ? void 0 : r.tokens) == null ? void 0 : s.placeables) == null ? void 0 : o.find((l) => {
    var c;
    return l.id === e || ((c = l.document) == null ? void 0 : c.id) === e;
  })) ?? null : null;
}
function wC(t = "") {
  var n, i, a;
  const e = String(t ?? "").trim();
  return e ? ((a = (i = (n = globalThis.canvas) == null ? void 0 : n.tokens) == null ? void 0 : i.placeables) == null ? void 0 : a.find((r) => {
    var s;
    return (((s = r.document) == null ? void 0 : s.uuid) ?? r.uuid) === e;
  })) ?? null : null;
}
function Hj(t, e = {}) {
  var n, i, a;
  return wC(e == null ? void 0 : e.sourceTokenUuid) ?? TC(e == null ? void 0 : e.sourceTokenId) ?? ((i = (n = t == null ? void 0 : t.getActiveTokens) == null ? void 0 : n.call(t, !0, !0)) == null ? void 0 : i[0]) ?? ((a = t == null ? void 0 : t.token) == null ? void 0 : a.object) ?? (t == null ? void 0 : t.token) ?? null;
}
function jj(t = {}) {
  return TC(t == null ? void 0 : t.targetTokenId) ?? wC(t == null ? void 0 : t.targetTokenUuid) ?? null;
}
function Gj(t = null) {
  var e;
  return String(((e = t == null ? void 0 : t.document) == null ? void 0 : e.uuid) ?? (t == null ? void 0 : t.uuid) ?? "").trim();
}
function eA(t = null, e = "") {
  var n, i;
  return !!((i = (n = t == null ? void 0 : t.statuses) == null ? void 0 : n.has) != null && i.call(n, e));
}
function qj(t = "") {
  return String(t ?? "").trim().toLowerCase().replace(/[^a-z0-9]+/g, "");
}
function Km(t) {
  return (Array.isArray(t) ? t : typeof t == "string" ? t.split(",") : []).map((n) => String(n ?? "").trim()).filter(Boolean);
}
function Wj(t = null) {
  var n, i, a, r;
  const e = /* @__PURE__ */ new Set(["activeprobe", "probe", "probeking", "advancedsensors"]);
  for (const s of Array.from((t == null ? void 0 : t.items) ?? []))
    if ([
      s == null ? void 0 : s.id,
      s == null ? void 0 : s.name,
      (n = s == null ? void 0 : s.system) == null ? void 0 : n.category,
      ...Km((i = s == null ? void 0 : s.system) == null ? void 0 : i.tags),
      ...Km((a = s == null ? void 0 : s.system) == null ? void 0 : a.capabilities),
      ...Km((r = s == null ? void 0 : s.system) == null ? void 0 : r.keywords)
    ].some((l) => e.has(qj(l)))) return !0;
  return !1;
}
function Vj(t = "contact") {
  const e = Rg(t), n = AC[e] ?? 1;
  return { currentState: e, nextState: e === "lock" ? "track" : e === "track" ? "contact" : "blind", dn: n };
}
function Jj(t = "typical") {
  return t === "open" || t === "exposed" ? "Open Ground" : t === "covered" || t === "terrain" || t === "woods" || t === "urban" || t === "ridge" || t === "ecm" ? "Favorable Cover / ECM" : "Typical Battlefield";
}
function Yj(t = {}) {
  const e = Kj((t == null ? void 0 : t.breakLockSituation) ?? (t == null ? void 0 : t.situation)), n = kC[e] ?? 0;
  return n ? {
    id: "breakLock.situation",
    label: Jj(e),
    value: n,
    tags: ["breakLock", "situation"]
  } : null;
}
function Qj(t, e, n = {}, i = "contact") {
  const a = Vj(i), r = [{
    id: "breakLock.detectionState",
    label: `${wn(a.currentState)} -> ${wn(a.nextState)}`,
    value: a.dn,
    tags: ["base", "breakLock", "detectionState"]
  }];
  return (eA(t, "tagged") || eA(t, "narced")) && r.push({
    id: "breakLock.beacon",
    label: "TAG / NARC Beacon",
    value: 1,
    tags: ["breakLock", "beacon"]
  }), fd(t, {
    intent: "breakLock",
    payload: n
  }).effectiveEmissionRating > 0 && r.push({
    id: "breakLock.highEmission",
    label: "High Emission",
    value: 1,
    tags: ["breakLock", "emission"]
  }), Wj(e) && r.push({
    id: "breakLock.activeProbe",
    label: "Active Probe",
    value: 1,
    tags: ["breakLock", "activeProbe"]
  }), r;
}
function Xj({ observerToken: t = null, sourceToken: e = null, payload: n = {} } = {}) {
  const i = Rg((n == null ? void 0 : n.breakLockDetectionState) ?? (n == null ? void 0 : n.currentState) ?? (n == null ? void 0 : n.detectionState), "contact"), a = String((n == null ? void 0 : n.breakLockTargetTokenUuid) ?? "").trim() || Gj(e), r = Pn(t);
  return r && a ? Rg(ro(r, a), "blind") : i;
}
function tA(t = "", e = null) {
  const n = String(t ?? "").trim(), i = String((e == null ? void 0 : e.name) ?? "").trim();
  return i ? `${n} (${i})` : n;
}
async function Zj({ actor: t, payload: e } = {}) {
  var b, y, S, A, T, C, k, w, v, N, P, L, _, O, x, H, G;
  if (!t) throw new Error("resolveBreakLock requires actor");
  if (!Uj(t))
    throw dt("Break Lock is a machine action.", { severity: "warn" });
  const n = Hj(t, e), i = jj(e), a = await Ut({
    machineActor: t,
    operatorActorUuid: String((e == null ? void 0 : e.operatorActorUuid) ?? "").trim()
  }), r = a.actor ?? t, s = M.actorAttributes.handling, o = "stealth", l = Ft(o), c = Math.max(0, So(((b = t == null ? void 0 : t.getAttributeValue) == null ? void 0 : b.call(t, s)) ?? ((A = (S = (y = t == null ? void 0 : t.system) == null ? void 0 : y.attributes) == null ? void 0 : S[s]) == null ? void 0 : A.value), 0)), u = Math.max(0, So((k = (C = (T = r == null ? void 0 : r.system) == null ? void 0 : T.skills) == null ? void 0 : C[o]) == null ? void 0 : k.rating, 0)), d = Math.max(0, So((N = (v = (w = r == null ? void 0 : r.system) == null ? void 0 : w.skills) == null ? void 0 : v[o]) == null ? void 0 : N.bonus, 0)), f = Xj({ observerToken: i, sourceToken: n, payload: e }), m = Yj(e), g = So(m == null ? void 0 : m.value, 0), p = Qj(t, (i == null ? void 0 : i.actor) ?? null, e, f), h = Math.max(1, p.reduce((q, ie) => q + So(ie.value, 0), 0));
  return {
    intent: "breakLock",
    rollType: "simple",
    title: "Break Lock",
    subtitle: (i == null ? void 0 : i.name) ?? ((P = i == null ? void 0 : i.actor) == null ? void 0 : P.name) ?? "Observer",
    domains: ["physical", "skill.stealth", "sensor", "ew"],
    domainTags: ["sensor", "sensor.breakLock", "skill.stealth"],
    diceTarget: 5,
    difficulty: { dn: h },
    dn: { parts: p, total: h },
    edge: { allowed: ["pre", "post"], earn: { enabled: !0, rate: 4, maxPerRoll: 1 } },
    pool: {
      attribute: c,
      skill: u,
      bonus: d + g,
      specialization: 0
    },
    breakdown: [
      { id: "attribute", label: tA("Handling", t), value: c },
      { id: "skill", label: tA((l == null ? void 0 : l.label) ?? "Stealth", a.actor), value: u },
      ...d ? [{ id: "bonus", label: "Stealth Bonus", value: d }] : [],
      ...m ? [{ id: m.id, label: m.label, value: m.value }] : []
    ],
    specialization: null,
    data: {
      skillKey: o,
      attrKey: s,
      machineActorUuid: t.uuid ?? "",
      operatorActorUuid: ((L = a.actor) == null ? void 0 : L.uuid) ?? "",
      label: `${s}+${(l == null ? void 0 : l.label) ?? "Stealth"}`
    },
    breakLock: {
      machineActorUuid: t.uuid ?? "",
      operatorActorUuid: ((_ = a.actor) == null ? void 0 : _.uuid) ?? "",
      sourceTokenId: (n == null ? void 0 : n.id) ?? ((O = n == null ? void 0 : n.document) == null ? void 0 : O.id) ?? "",
      sourceTokenUuid: ((x = n == null ? void 0 : n.document) == null ? void 0 : x.uuid) ?? (n == null ? void 0 : n.uuid) ?? "",
      observerTokenId: (i == null ? void 0 : i.id) ?? ((H = i == null ? void 0 : i.document) == null ? void 0 : H.id) ?? "",
      observerTokenUuid: ((G = i == null ? void 0 : i.document) == null ? void 0 : G.uuid) ?? (i == null ? void 0 : i.uuid) ?? "",
      currentState: f,
      currentStateLabel: wn(f)
    },
    rollActor: r,
    machineActor: t
  };
}
const Hm = 2;
function eG(t) {
  return (t == null ? void 0 : t.type) === M.actorTypes.vehicle || (t == null ? void 0 : t.type) === M.actorTypes.battlemech;
}
function jm(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function MC(t = "") {
  var n, i, a, r, s, o;
  const e = String(t ?? "").trim();
  return e ? ((a = (i = (n = globalThis.canvas) == null ? void 0 : n.tokens) == null ? void 0 : i.get) == null ? void 0 : a.call(i, e)) ?? ((o = (s = (r = globalThis.canvas) == null ? void 0 : r.tokens) == null ? void 0 : s.placeables) == null ? void 0 : o.find((l) => {
    var c;
    return l.id === e || ((c = l.document) == null ? void 0 : c.id) === e;
  })) ?? null : null;
}
function vC(t = "") {
  var n, i, a;
  const e = String(t ?? "").trim();
  return e ? ((a = (i = (n = globalThis.canvas) == null ? void 0 : n.tokens) == null ? void 0 : i.placeables) == null ? void 0 : a.find((r) => {
    var s;
    return (((s = r.document) == null ? void 0 : s.uuid) ?? r.uuid) === e;
  })) ?? null : null;
}
function tG(t, e = {}) {
  var n, i, a;
  return vC(e == null ? void 0 : e.sourceTokenUuid) ?? MC(e == null ? void 0 : e.sourceTokenId) ?? ((i = (n = t == null ? void 0 : t.getActiveTokens) == null ? void 0 : n.call(t, !0, !0)) == null ? void 0 : i[0]) ?? ((a = t == null ? void 0 : t.token) == null ? void 0 : a.object) ?? (t == null ? void 0 : t.token) ?? null;
}
function nG(t = {}) {
  return MC(t == null ? void 0 : t.targetTokenId) ?? vC(t == null ? void 0 : t.targetTokenUuid) ?? null;
}
function nA(t = "", e = null) {
  const n = String(t ?? "").trim(), i = String((e == null ? void 0 : e.name) ?? "").trim();
  return i ? `${n} (${i})` : n;
}
async function iG({ actor: t, payload: e } = {}) {
  var f, m, g, p, h, b, y, S, A, T, C, k, w, v, N, P, L;
  if (!t) throw new Error("resolveDefensiveJink requires actor");
  if (!eG(t))
    throw dt("Defensive Jink is a machine action.", { severity: "warn" });
  const n = tG(t, e), i = nG(e), a = await Ut({
    machineActor: t,
    operatorActorUuid: String((e == null ? void 0 : e.operatorActorUuid) ?? "").trim()
  }), r = a.actor ?? t, s = M.actorAttributes.handling, o = "piloting", l = Ft(o), c = Math.max(0, jm(((f = t == null ? void 0 : t.getAttributeValue) == null ? void 0 : f.call(t, s)) ?? ((p = (g = (m = t == null ? void 0 : t.system) == null ? void 0 : m.attributes) == null ? void 0 : g[s]) == null ? void 0 : p.value), 0)), u = Math.max(0, jm((y = (b = (h = r == null ? void 0 : r.system) == null ? void 0 : h.skills) == null ? void 0 : b[o]) == null ? void 0 : y.rating, 0)), d = Math.max(0, jm((T = (A = (S = r == null ? void 0 : r.system) == null ? void 0 : S.skills) == null ? void 0 : A[o]) == null ? void 0 : T.bonus, 0));
  return {
    intent: "defensiveJink",
    rollType: "simple",
    title: "Defensive Jink",
    subtitle: (i == null ? void 0 : i.name) ?? ((C = i == null ? void 0 : i.actor) == null ? void 0 : C.name) ?? "Observer",
    domains: ["physical", "skill.piloting", "sensor", "ew"],
    domainTags: ["sensor", "sensor.jink", "skill.piloting"],
    diceTarget: 5,
    difficulty: { dn: Hm },
    dn: {
      parts: [{ id: "jink.base", label: "Defensive Jink", value: Hm, tags: ["base", "jink"] }],
      total: Hm
    },
    edge: { allowed: ["pre", "post"], earn: { enabled: !0, rate: 4, maxPerRoll: 1 } },
    pool: {
      attribute: c,
      skill: u,
      bonus: d,
      specialization: 0
    },
    breakdown: [
      { id: "attribute", label: nA("Handling", t), value: c },
      { id: "skill", label: nA((l == null ? void 0 : l.label) ?? "Piloting", a.actor), value: u },
      ...d ? [{ id: "bonus", label: "Piloting Bonus", value: d }] : []
    ],
    specialization: null,
    data: {
      skillKey: o,
      attrKey: s,
      machineActorUuid: t.uuid ?? "",
      operatorActorUuid: ((k = a.actor) == null ? void 0 : k.uuid) ?? "",
      label: `${s}+${(l == null ? void 0 : l.label) ?? "Piloting"}`
    },
    defensiveJink: {
      machineActorUuid: t.uuid ?? "",
      operatorActorUuid: ((w = a.actor) == null ? void 0 : w.uuid) ?? "",
      sourceTokenId: (n == null ? void 0 : n.id) ?? ((v = n == null ? void 0 : n.document) == null ? void 0 : v.id) ?? "",
      sourceTokenUuid: ((N = n == null ? void 0 : n.document) == null ? void 0 : N.uuid) ?? (n == null ? void 0 : n.uuid) ?? "",
      observerTokenId: (i == null ? void 0 : i.id) ?? ((P = i == null ? void 0 : i.document) == null ? void 0 : P.id) ?? "",
      observerTokenUuid: ((L = i == null ? void 0 : i.document) == null ? void 0 : L.uuid) ?? (i == null ? void 0 : i.uuid) ?? ""
    },
    rollActor: r,
    machineActor: t
  };
}
function aG(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function xi(t, e = 0) {
  return Math.max(e, aG(t, e));
}
function rG(t = "") {
  const e = String(t ?? "").trim().toLowerCase();
  return e === "shutdown" || e === "explosion" ? e : "";
}
function sG(t, e) {
  const n = (e == null ? void 0 : e.dangerChecks) ?? {};
  return t === "shutdown" ? {
    title: "Shutdown Check",
    pool: xi(n.shutdownPool, 0),
    dn: xi(n.shutdownDN, 1),
    formula: "Chassis + Reliability",
    successText: "The BattleMech stays online.",
    failureText: "On failure, compare the margin to the pilot's System Operations for an override; otherwise the BattleMech shuts down."
  } : {
    title: "Explosion Check",
    pool: xi(n.explosionPool, 1),
    dn: xi(n.explosionDN, 1),
    formula: "Chassis + Reliability - Danger",
    successText: "Volatile systems remain contained.",
    failureText: "Failure detonates volatile components."
  };
}
function oG(t = null, e = "") {
  var r, s, o, l;
  const n = String(e ?? "").trim();
  if (!t || !n) return null;
  const i = (r = t.getSkillRating) == null ? void 0 : r.call(t, n);
  if (Number.isFinite(Number(i))) return xi(i, 0);
  const a = (l = (o = (s = t.system) == null ? void 0 : s.skills) == null ? void 0 : o[n]) == null ? void 0 : l.rating;
  return Number.isFinite(Number(a)) ? xi(a, 0) : null;
}
async function lG({ actor: t, payload: e } = {}) {
  var g, p, h, b, y, S, A;
  if (!t) throw new Error("resolveHeatDangerCheck requires actor");
  if (t.type !== "battlemech") throw new Error("Heat danger checks require a BattleMech actor");
  const n = rG((e == null ? void 0 : e.checkKind) ?? (e == null ? void 0 : e.kind));
  if (!n) throw new Error("Heat danger checks require payload.checkKind of shutdown or explosion");
  const i = ma(t);
  if (!i.inDanger || !i.dangerChecks)
    throw new Error("Heat danger checks are only available while the BattleMech is in Danger heat");
  const a = await Ut({
    machineActor: t,
    operatorActorUuid: e == null ? void 0 : e.operatorActorUuid
  }), r = (a == null ? void 0 : a.actor) ?? null, s = oG(r, "systemOps"), o = sG(n, i), l = Number.isFinite(Number(e == null ? void 0 : e.dn)) ? xi(e.dn, 1) : o.dn, c = xi((h = (p = (g = t.system) == null ? void 0 : g.attributes) == null ? void 0 : p.chassis) == null ? void 0 : h.value, 0), u = xi((S = (y = (b = t.system) == null ? void 0 : b.attributes) == null ? void 0 : y.reliability) == null ? void 0 : S.value, 0), d = xi((A = i.penalties) == null ? void 0 : A.dangerLevel, 0), f = n === "explosion" ? -d : 0, m = n === "explosion" ? o.pool - (c + u + f) : 0;
  return {
    intent: "heatDangerCheck",
    rollType: "simple",
    title: o.title,
    subtitle: t.name ?? "BattleMech",
    domains: ["machine", "heat"],
    tags: ["machine", "heat", "danger", n],
    formula: o.formula,
    machineActor: t,
    difficulty: { dn: l },
    dn: {
      parts: [
        {
          id: `heat.${n}.dn`,
          label: n === "shutdown" ? "Danger Level" : "Explosion DN",
          value: l,
          tags: ["heat", "danger"]
        }
      ],
      total: l
    },
    edge: { earn: { enabled: !1 } },
    pool: {
      attribute: c,
      skill: u,
      bonus: f,
      specialization: m
    },
    breakdown: [
      { id: "attribute", label: "Chassis", value: c },
      { id: "skill", label: "Reliability", value: u },
      ...f ? [{ id: "bonus", label: `Danger Level ${d}`, value: f }] : [],
      ...m ? [{ id: "specialization", label: "Minimum Pool", value: m }] : []
    ],
    heatDangerCheck: {
      kind: n,
      label: o.title,
      dangerLevel: d,
      volatile: !!i.volatile,
      pool: o.pool,
      dn: l,
      operatorName: (r == null ? void 0 : r.name) ?? "",
      operatorActorUuid: (a == null ? void 0 : a.uuid) ?? (r == null ? void 0 : r.uuid) ?? "",
      operatorSource: (a == null ? void 0 : a.source) ?? "",
      operatorReason: (a == null ? void 0 : a.reason) ?? "",
      systemOpsRating: s,
      successText: o.successText,
      failureText: o.failureText
    },
    data: {
      checkKind: n,
      dangerLevel: d,
      volatile: !!i.volatile
    }
  };
}
const cG = {
  skill: IH,
  edge: PH,
  attribute: RH,
  common: DH,
  attack: Tj,
  defense: wj,
  resistance: Mj,
  initiative: Nj,
  overload: Pj,
  machineRemedy: Dj,
  heatDangerCheck: lG,
  acquire: QS,
  acquireTarget: QS,
  targeting: ZS,
  generateFireSolution: ZS,
  breakLock: Zj,
  defensiveJink: iG
};
async function Gm({ actor: t, payload: e, event: n } = {}) {
  if (!t) throw new Error("resolveIntent requires actor");
  const i = String((e == null ? void 0 : e.intent) ?? "").trim();
  if (!i) throw new Error("resolveIntent requires payload.intent");
  const a = cG[i];
  if (!a) throw new Error(`Unsupported roll intent: ${i}`);
  const r = await a({ actor: t, payload: e, event: n });
  return uG(r, { intent: i });
}
function uG(t, { intent: e } = {}) {
  (!t || typeof t != "object") && (t = {}), t.intent = t.intent ?? e ?? "unknown", t.title = String(t.title ?? "Roll"), t.domains = Array.isArray(t.domains) ? t.domains : [], t.breakdown = Array.isArray(t.breakdown) ? t.breakdown : [], t.mods = Array.isArray(t.mods) ? t.mods : [];
  const n = t.pool && typeof t.pool == "object" ? t.pool : {}, i = Number(n.attribute ?? n.base ?? 0), a = Number(n.skill ?? n.rating ?? 0), r = Number(n.bonus ?? 0), s = Number(n.specialization ?? 0);
  if (![i, a, r, s].every(Number.isFinite))
    throw console.error("MWD | Invalid pool parts after intent resolution", { intent: e, ctx: t }), new Error("MWD.roll: pool parts must be numeric (attribute/skill/bonus/specialization).");
  return t.pool = {
    attribute: i,
    skill: a,
    bonus: r,
    specialization: s,
    totalBase: i + a + r + s
  }, t.rollType = t.rollType ?? "simple", t.diceTarget = Number.isFinite(t.diceTarget) ? t.diceTarget : Number(t.target ?? 5), t.difficulty && typeof t.difficulty == "object" ? t.difficulty.dn = Number(t.difficulty.dn ?? 0) : Number.isFinite(t.dn) && (t.difficulty = { dn: Number(t.dn) }), t.breakdown.length || (t.breakdown = [
    { id: "attribute", label: "Attribute", value: i },
    { id: "skill", label: "Skill", value: a },
    { id: "bonus", label: "Bonus", value: r },
    ...s ? [{ id: "specialization", label: "Specialization", value: s }] : []
  ]), t;
}
var zs;
class dG {
  constructor() {
    Fe(this, zs, /* @__PURE__ */ new Map());
  }
  register(e) {
    if (!(e != null && e.id) || typeof e.collect != "function")
      throw new Error("Invalid ModifierProvider: missing id or collect()");
    X(this, zs).has(e.id) || X(this, zs).set(e.id, e);
  }
  async collectAll(e) {
    const n = [];
    for (const i of X(this, zs).values()) {
      const a = await i.collect(e);
      if (a != null && a.length)
        for (const r of a)
          r && typeof r.label == "string" && typeof r.value == "number" && typeof r.source == "string" ? n.push(r) : console.warn("MWD | DROPPED MOD (bad shape)", i.id, r);
    }
    return n;
  }
}
zs = new WeakMap();
const dn = new dG();
function mG(t) {
  if (t == null || t === "" || t === "—" || t === "–") return 0;
  const e = Number(t);
  return Number.isFinite(e) ? e : null;
}
function fG(t) {
  const e = mG(t == null ? void 0 : t.value);
  return e === null ? null : { ...t, value: e };
}
function gG(...t) {
  const e = /* @__PURE__ */ new Set(), n = [];
  for (const i of t) {
    const a = Array.isArray(i) ? i : typeof i == "string" ? i.split(",") : [];
    for (const r of a) {
      const s = String(r ?? "").trim();
      !s || e.has(s) || (e.add(s), n.push(s));
    }
  }
  return n;
}
async function iA({
  actor: t,
  rollActor: e = null,
  machineActor: n = null,
  rollType: i,
  skillId: a,
  domains: r,
  // NEW (optional)
  payload: s,
  resolved: o,
  context: l
} = {}) {
  const c = gG(r, o == null ? void 0 : o.domains, o == null ? void 0 : o.domainTags, s == null ? void 0 : s.domains), u = { actor: t, rollActor: e, machineActor: n, rollType: i, skillId: a, domains: c, payload: s, resolved: o, context: l }, d = await dn.collectAll(u);
  let f = [];
  for (const g of d ?? []) {
    const p = fG(g);
    if (!p) {
      console.warn("MWD | Dropping invalid modifier value", g);
      continue;
    }
    f.push(p);
  }
  c.length && (f = f.filter((g) => !g.domain || c.includes(g.domain)));
  const m = f.reduce((g, p) => g + p.value, 0);
  return { mods: f, total: m };
}
function pG({
  actor: t,
  payload: e,
  ctx: n,
  roll: i,
  target: a,
  pool: r,
  mods: s = [],
  modTotal: o = 0,
  hits: l = null,
  ones: c = null,
  edge: u = null,
  outcomeModel: d = null
} = {}) {
  var N, P, L, _;
  if (!t) throw new Error("buildResolved requires actor");
  if (!(e != null && e.intent)) throw new Error("buildResolved requires payload.intent");
  if (!i) throw new Error("buildResolved requires roll");
  const f = foundry.utils.randomID(), m = (N = i.dice) == null ? void 0 : N[0], g = Array.isArray(m == null ? void 0 : m.results) ? m.results : [], p = ["", "one", "two", "three", "four", "five", "six"], h = g.map((O, x) => {
    const H = `pool:${x}`, G = Number(O.result), q = !!O.success, ie = p[G] ?? "one";
    return {
      ref: H,
      face: G,
      fasClass: q ? `fas fa-dice-${ie}` : `far fa-dice-${ie}`,
      isSuccess: q,
      isFailure: !q,
      tooltip: q ? `Die ${x + 1}: ${G} (Success vs TN ${Number(a ?? 5)})` : `Die ${x + 1}: ${G} (Failure vs TN ${Number(a ?? 5)})`
    };
  }), b = h.filter((O) => O.isFailure).map((O) => O.ref), y = [{
    id: "pool",
    label: "Pool",
    faces: 6,
    termIndex: 0,
    // informational only
    dice: h
  }], S = (Array.isArray(s) ? s : []).map((O, x) => {
    const H = Number(O.value ?? 0), G = `mod:${bG(O.label ?? "mod")}:${x}`;
    return {
      id: O.id ?? G,
      label: O.label ?? "Modifier",
      value: H,
      domain: O.domain ?? null,
      source: O.source ?? null,
      tooltip: O.tooltip ?? `${O.label ?? "Modifier"} ${aA(H)}`
    };
  }), A = S.map((O) => O.id), C = (Array.isArray(n == null ? void 0 : n.breakdown) ? n.breakdown : []).map((O) => ({
    id: `pool.${O.id ?? foundry.utils.randomID()}`,
    label: O.label ?? O.id ?? "Row",
    value: Number(O.value ?? 0),
    tooltip: `Contribution from ${O.label ?? O.id}: ${Number(O.value ?? 0)}`
  }));
  C.push({
    id: "mods.total",
    label: "Mods",
    value: Number(o ?? 0),
    modIds: A,
    tooltip: S.length ? S.map((O) => `${O.label}: ${aA(O.value)}`).join(`
`) : "No roll-time modifiers."
  }), C.push({
    id: "pool.final",
    label: "Final Pool",
    value: Number(r ?? 0),
    tooltip: `Final dice pool rolled: ${Number(r ?? 0)}d6`
  });
  const k = Number.isFinite(Number(l)) ? Number(l) : h.filter((O) => O.isSuccess).length, w = Number.isFinite(Number(c)) ? Number(c) : h.filter((O) => O.face === 1).length, v = hG(u, { payload: e });
  return {
    version: 2,
    id: f,
    actorUuid: t.uuid,
    // Re-entry
    originPayload: e,
    // Render header
    title: (n == null ? void 0 : n.title) ?? "Roll",
    subtitle: (n == null ? void 0 : n.subtitle) ?? t.name ?? "Actor",
    formula: String((n == null ? void 0 : n.formula) ?? "").trim(),
    intent: (n == null ? void 0 : n.intent) ?? e.intent,
    domains: Array.isArray(n == null ? void 0 : n.domains) ? n.domains : [],
    attack: (n == null ? void 0 : n.attack) ?? null,
    machineRemedy: (n == null ? void 0 : n.machineRemedy) ?? null,
    heatDangerCheck: (n == null ? void 0 : n.heatDangerCheck) ?? null,
    specialization: (n == null ? void 0 : n.specialization) ?? null,
    dn: (n == null ? void 0 : n.dn) ?? (((P = n == null ? void 0 : n.difficulty) == null ? void 0 : P.dn) !== void 0 ? {
      parts: [{
        id: "difficulty.current",
        label: "DN",
        value: Number(n.difficulty.dn ?? 0),
        tags: ["manual"]
      }],
      total: Number(n.difficulty.dn ?? 0)
    } : null),
    // Minimal context snapshot so chat-actions can recompute interpretation
    // after post-spend rerolls mutate hits.
    ctxSnapshot: {
      rollType: (n == null ? void 0 : n.rollType) ?? "simple",
      difficulty: (n == null ? void 0 : n.difficulty) ?? null,
      dn: (n == null ? void 0 : n.dn) ?? null,
      opposed: (n == null ? void 0 : n.opposed) ?? null,
      net: (n == null ? void 0 : n.net) ?? null,
      edge: {
        pool: ((L = n == null ? void 0 : n.edge) == null ? void 0 : L.pool) ?? null,
        earn: ((_ = n == null ? void 0 : n.edge) == null ? void 0 : _.earn) ?? null
      }
    },
    // Roll + dice
    roll: {
      json: i.toJSON(),
      formula: i.formula,
      target: Number(a ?? 5),
      pool: Number(r ?? 0),
      diceGroups: y,
      failureDiceRefs: b
    },
    // Outcome numbers
    outcome: {
      hits: k,
      ones: w
    },
    // New: engine-interpreted outcome (margin/net/converted/edgeEarned/etc
    outcomeModel: d,
    // Breakdown + modifiers
    breakdownRows: C,
    modifiers: {
      applied: S,
      total: Number(o ?? 0)
    },
    areaEffectPreviewState: foundry.utils.deepClone((e == null ? void 0 : e.areaEffectPreviewState) ?? {}),
    // Edge snapshot / affordances
    edge: v
  };
}
function hG(t, { payload: e } = {}) {
  var g, p, h, b, y, S, A, T, C, k, w, v, N, P;
  const n = !!((g = e == null ? void 0 : e.edge) != null && g.enabled), i = (t == null ? void 0 : t.domain) ?? null, a = (t == null ? void 0 : t.pools) ?? null, r = ((p = t == null ? void 0 : t.pre) == null ? void 0 : p.poolKey) ?? ((b = (h = e == null ? void 0 : e.edge) == null ? void 0 : h.pre) == null ? void 0 : b.poolKey) ?? (n ? ((y = e == null ? void 0 : e.edge) == null ? void 0 : y.poolKey) ?? null : null), s = Number(((S = t == null ? void 0 : t.pre) == null ? void 0 : S.spent) ?? ((T = (A = e == null ? void 0 : e.edge) == null ? void 0 : A.pre) == null ? void 0 : T.spent) ?? (n ? 1 : 0)) ? 1 : 0, o = ((C = t == null ? void 0 : t.post) == null ? void 0 : C.poolKey) ?? ((w = (k = e == null ? void 0 : e.edge) == null ? void 0 : k.post) == null ? void 0 : w.poolKey) ?? null, l = Number(((v = t == null ? void 0 : t.post) == null ? void 0 : v.spent) ?? ((P = (N = e == null ? void 0 : e.edge) == null ? void 0 : N.post) == null ? void 0 : P.spent) ?? 0) ? 1 : 0, c = (a == null ? void 0 : a.a) ?? null, u = (a == null ? void 0 : a.b) ?? null, d = [c, u].filter(Boolean);
  let f = [c, u].filter(Boolean);
  s && r && (f = f.filter((L) => L !== r));
  const m = {
    canSpendPre: d.length > 0 && !s,
    // spending pre after roll is not a thing
    canSpendPost: f.length > 0 && !l,
    canPostRerollFailures: f.length > 0 && !l
  };
  return {
    domain: i,
    pools: a ? { a: c, b: u } : null,
    pre: { poolKey: r, spent: s },
    post: { poolKey: o, spent: l },
    allowed: {
      prePools: d,
      postPools: f
    },
    availableActions: m
  };
}
function aA(t) {
  const e = Number(t ?? 0);
  return e >= 0 ? `+${e}` : `${e}`;
}
function bG(t) {
  return String(t).toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, "");
}
function Ct(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? Math.trunc(n) : e;
}
function qm(t, e = 0, n = Number.POSITIVE_INFINITY) {
  const i = Ct(t, e);
  return Math.max(e, Math.min(n, i));
}
function CC(t, e = 1) {
  var i;
  const n = Ct((i = t == null ? void 0 : t.difficulty) == null ? void 0 : i.dn, Ct(e, 1));
  return Math.max(0, n);
}
function yG(t, e) {
  return Math.max(0, Ct(t, 0) - Ct(e, 0));
}
function SG({ convert: t, remainder: e, rate: n = 4 } = {}) {
  const i = Math.max(0, Ct(e, 0)), a = Math.max(1, Ct(n, 4)), r = Math.max(0, Ct(t, 0)), s = Math.floor(r / a) * a;
  return Math.min(i, s);
}
function AG(t, { rate: e = 4, maxPerRoll: n = Number.POSITIVE_INFINITY } = {}) {
  const i = Math.max(1, Ct(e, 4)), a = Math.floor(Math.max(0, Ct(t, 0)) / i), r = Number.isFinite(n) ? Math.max(0, Ct(n, 0)) : Number.POSITIVE_INFINITY;
  return { amount: Math.min(a, r), rate: i };
}
function kG(t) {
  var i;
  const e = ((i = t == null ? void 0 : t.edge) == null ? void 0 : i.earn) ?? {};
  return {
    enabled: !!(e != null && e.enabled),
    rate: Math.max(1, Ct(e == null ? void 0 : e.rate, 4)),
    maxPerRoll: (e == null ? void 0 : e.maxPerRoll) ?? Number.POSITIVE_INFINITY
  };
}
function $u(t) {
  var n;
  const e = (n = t == null ? void 0 : t.edge) == null ? void 0 : n.pool;
  return e ? String(e) : null;
}
function TG(t) {
  let e = 0, n = 0;
  const i = (a) => {
    if (!a) return;
    const r = a == null ? void 0 : a.results;
    if (Array.isArray(r))
      for (const o of r) {
        if ((o == null ? void 0 : o.active) === !1) continue;
        const l = Number(o == null ? void 0 : o.result);
        Number.isFinite(l) && (e += 1, l === 1 && (n += 1));
      }
    const s = a == null ? void 0 : a.terms;
    if (Array.isArray(s))
      for (const o of s) i(o);
    if (Array.isArray(a))
      for (const o of a) i(o);
  };
  return i(t), { dice: e, ones: n };
}
function Jh(t, e) {
  if (Ct(t, 0) !== 0) return !1;
  const { dice: n, ones: i } = TG(e);
  return n <= 0 ? !1 : i >= Math.ceil(n / 2);
}
function wG(t, e, n = 4) {
  return !!(t && Ct(e, 0) > Ct(n, 4));
}
function rA(t, e) {
  const n = Ct(e == null ? void 0 : e.successes, 0), i = CC(t, 1), a = n >= i, r = n - i, s = wG(a, r, 4), o = Jh(n, e == null ? void 0 : e.raw), l = $u(t);
  let c = null;
  return r > 4 ? c = { amount: 1, pool: l, reason: "criticalSuccess", details: { margin: r } } : o && (c = { amount: 1, pool: l, reason: "criticalFailure", details: {} }), {
    rollType: "simple",
    passed: a,
    successes: n,
    difficulty: { dn: i },
    margin: r,
    criticalSuccess: s,
    criticalFailure: o,
    tier: s ? "criticalSuccess" : o ? "criticalFailure" : a ? "success" : "failure",
    edgeEarned: c
  };
}
function MG(t, e, n) {
  var f, m;
  const i = Ct(e == null ? void 0 : e.successes, 0), a = Ct(n == null ? void 0 : n.successes, 0), r = !!((f = t == null ? void 0 : t.opposed) != null && f.net), s = String(((m = t == null ? void 0 : t.opposed) == null ? void 0 : m.dnTies) ?? "stalemate");
  let o = null, l = !1;
  r ? (o = i - a, o > 0 ? l = !0 : o < 0 ? l = !1 : s === "attackerWins" ? l = !0 : l = !1) : i > a ? l = !0 : i < a ? l = !1 : s === "attackerWins" ? l = !0 : l = !1;
  const c = Jh(i, e == null ? void 0 : e.raw), u = $u(t);
  let d = null;
  return r && typeof o == "number" && o > 4 ? d = { amount: 1, pool: u, reason: "criticalSuccess", details: { netHits: o } } : c && (d = { amount: 1, pool: u, reason: "criticalFailure", details: {} }), {
    rollType: "opposed",
    passed: l,
    successes: i,
    criticalFailure: c,
    opposed: {
      attacker: i,
      defender: a,
      netEnabled: r,
      netHits: r ? o : void 0,
      tiePolicy: s
    },
    edgeEarned: d
  };
}
function vG(t, e) {
  var h, b, y;
  const n = Ct(e == null ? void 0 : e.successes, 0), i = CC(t, 1), a = n >= i, r = Jh(n, e == null ? void 0 : e.raw), s = yG(n, i), o = ((h = t == null ? void 0 : t.net) == null ? void 0 : h.convert) ?? ((b = t == null ? void 0 : t.allocation) == null ? void 0 : b.convert) ?? 0, l = kG(t), c = l.rate, u = SG({ convert: o, remainder: s, rate: c }), d = s - u, f = l.enabled && u >= c ? (() => {
    const { amount: S } = AG(u, { rate: c, maxPerRoll: l.maxPerRoll }), A = $u(t);
    return S > 0 ? { amount: S, pool: A, reason: "convert4", details: { converted: u, rate: c } } : null;
  })() : null, m = r ? { amount: 1, pool: $u(t), reason: "critFail", details: { onesRule: "halfOrMoreOnes" } } : null, g = [];
  f && g.push(f), m && g.push(m);
  const p = g.length === 0 ? null : g.length === 1 ? g[0] : {
    amount: g.reduce((S, A) => S + (Number(A == null ? void 0 : A.amount) || 0), 0),
    pool: ((y = g[0]) == null ? void 0 : y.pool) ?? null,
    reason: "multiple",
    details: { awards: g }
  };
  return {
    rollType: "net",
    passed: a,
    successes: n,
    difficulty: { dn: i },
    criticalFailure: r,
    tier: r ? "criticalFailure" : a ? "success" : "failure",
    net: {
      remainder: s,
      convertRequested: Ct(o, 0),
      converted: u,
      value: d,
      // the actual net effect magnitude you apply (+net dice, etc.)
      rate: c,
      canConvert: s >= c
    },
    edgeEarned: p
  };
}
function CG(t, e) {
  var o, l, c, u;
  const n = Ct(e == null ? void 0 : e.successes, 0), i = qm((o = t == null ? void 0 : t.extended) == null ? void 0 : o.target, 1, 1e4), a = qm((l = t == null ? void 0 : t.extended) == null ? void 0 : l.accumulated, 0, 1e4), r = qm(a + n, 0, 1e4), s = r >= i;
  return {
    rollType: "extended",
    passed: s,
    successes: n,
    extended: {
      target: i,
      accumulated: a,
      nextAccumulated: r,
      remaining: Math.max(0, i - r),
      completed: s,
      interval: ((c = t == null ? void 0 : t.extended) == null ? void 0 : c.interval) ?? null,
      failureLimit: ((u = t == null ? void 0 : t.extended) == null ? void 0 : u.failureLimit) ?? null
    },
    // NOTE: Edge earn rules for extended are intentionally omitted per your earlier constraint.
    edgeEarned: null
  };
}
function IC(t, e, n = null) {
  var a;
  switch (String((t == null ? void 0 : t.rollType) ?? "simple")) {
    case "simple":
      return rA(t, e);
    case "opposed":
      return MG(t, e, n);
    case "net":
      return vG(t, e);
    case "extended":
      return CG(t, e);
    default: {
      const r = {
        ...t,
        difficulty: { dn: Number(((a = t == null ? void 0 : t.difficulty) == null ? void 0 : a.dn) ?? 1) || 1 }
      };
      return rA(r, e);
    }
  }
}
const { ApplicationV2: IG, HandlebarsApplicationMixin: EG } = foundry.applications.api, NG = Object.freeze([
  Object.freeze({ id: "attackOption.lightObscuring", label: "Light obscuring terrain", value: -1 }),
  Object.freeze({ id: "attackOption.heavyObscuring", label: "Heavy obscuring terrain", value: -2 }),
  Object.freeze({ id: "attackOption.multipleObscurants", label: "Multiple/intervening obscurants", value: -3 }),
  Object.freeze({ id: "attackOption.sensorHaze", label: "Smoke / ECM / sensor haze", value: -1 }),
  Object.freeze({ id: "attackOption.advantageousPosition", label: "Advantageous Position", value: 1 })
]);
function PG(t, e = -3, n = 3) {
  const i = [], a = "../img/dice";
  for (let r = e; r <= n; r++) {
    const s = Math.abs(r), o = s === 0 ? `${a}/BlankDice.webp` : `${a}/D6_${s}.svg`;
    i.push({
      value: r,
      abs: s,
      icon: o,
      active: r === t,
      neg: r < 0,
      pos: r > 0,
      zero: r === 0,
      title: r === 0 ? "0 (neutral)" : r < 0 ? `${r} penalty` : `+${r} bonus`
    });
  }
  return i;
}
function sA(t) {
  return (Array.isArray(t) ? t : []).map((n) => ({
    id: (n == null ? void 0 : n.id) ?? foundry.utils.randomID(),
    label: typeof (n == null ? void 0 : n.label) == "string" ? n.label : "Manual",
    value: Number((n == null ? void 0 : n.value) ?? 0),
    enabled: (n == null ? void 0 : n.enabled) !== !1,
    source: String((n == null ? void 0 : n.source) ?? "Manual").trim() || "Manual",
    optional: (n == null ? void 0 : n.optional) === !0,
    traitItemId: String((n == null ? void 0 : n.traitItemId) ?? "").trim(),
    traitEffectId: String((n == null ? void 0 : n.traitEffectId) ?? "").trim()
  }));
}
function EC(t = {}) {
  return t.attackOptions && typeof t.attackOptions == "object" ? t.attackOptions : {};
}
function Dg(t = {}, e = {}) {
  t.attackOptions = {
    ...EC(t),
    ...e
  };
}
function Og(t = {}, e = {}) {
  t.machineMotion = {
    ...t.machineMotion && typeof t.machineMotion == "object" ? t.machineMotion : {},
    ...e
  };
}
function oA(t = {}, e = "") {
  const n = t.machineMotion && typeof t.machineMotion == "object" ? t.machineMotion : {};
  return Object.prototype.hasOwnProperty.call(n, e);
}
function RG(t = {}, e = null) {
  var s, o, l, c;
  const n = (s = e == null ? void 0 : e.attack) == null ? void 0 : s.machineMotion, i = ((c = (l = (o = e == null ? void 0 : e.attack) == null ? void 0 : o.targets) == null ? void 0 : l[0]) == null ? void 0 : c.tokenId) ?? null;
  let a = null;
  if (n && typeof n == "object")
    a = {
      targetMotion: jr(n.targetMotion),
      jumped: !!n.jumped
    };
  else {
    const u = ao(i);
    a = CM(t, { targetCombatant: u });
  }
  if (!a || typeof a != "object") return t;
  const r = {};
  return oA(t, "targetMotion") || (r.targetMotion = jr(a.targetMotion)), oA(t, "jumped") || (r.jumped = !!a.jumped), Object.keys(r).length && Og(t, r), t;
}
function Lg(t = [], e = "") {
  return t.some((n) => (n == null ? void 0 : n.id) === e);
}
function NC(t = [], e = {}) {
  const n = String((e == null ? void 0 : e.id) ?? "").trim();
  if (!n) return t;
  const i = t.findIndex((r) => (r == null ? void 0 : r.id) === n), a = {
    id: n,
    label: String((e == null ? void 0 : e.label) ?? "Manual").trim() || "Manual",
    value: Number((e == null ? void 0 : e.value) ?? 0)
  };
  return i >= 0 ? t[i] = a : t.push(a), t;
}
function DG(t = [], e = "") {
  return t.filter((n) => (n == null ? void 0 : n.id) !== e);
}
function OG({ manual: t = [], attack: e = null, payload: n = {} } = {}) {
  if (!e) return null;
  const i = EC(n), a = tF(e.rangeBand), r = !!i.indirectAttack || Lg(t, "attackOption.indirectAttack");
  r && (NC(t, {
    id: "attackOption.indirectAttack",
    label: "Indirect Attack",
    value: a
  }), Dg(n, { indirectAttack: !0 }));
  const s = [
    ...NG,
    { id: "attackOption.indirectAttack", label: "Indirect Attack", value: a, flag: "indirectAttack" }
  ].map((c) => ({
    ...c,
    valueLabel: c.value >= 0 ? `+${c.value}` : String(c.value),
    active: c.id === "attackOption.indirectAttack" ? r : Lg(t, c.id)
  })), o = n.machineMotion && typeof n.machineMotion == "object" ? n.machineMotion : {}, l = jr(o.targetMotion);
  return {
    motionChoices: Object.entries(No).map(([c, u]) => ({
      key: c,
      label: u,
      selected: c === l
    })),
    jumped: !!o.jumped,
    options: s,
    losBlocked: !!i.losBlocked
  };
}
function Wm(t, e) {
  const n = t == null ? void 0 : t.toggles;
  return n && typeof n == "object" && e in n ? !!n[e] : !!(t != null && t[e]);
}
function LG(t, e) {
  t.useEdge = !!e.useEdge, t.takeRisks = !!e.takeRisks, t.opponentRoll = !!e.opponentRoll, t.toggles = t.toggles && typeof t.toggles == "object" ? t.toggles : {}, t.toggles.useEdge = !!e.useEdge, t.toggles.takeRisks = !!e.takeRisks, t.toggles.opponentRoll = !!e.opponentRoll;
}
function lA(t, e, n) {
  const i = String(n ?? "").trim(), a = i ? RE(e, i) : "";
  if (i && a) {
    t.specializationKey = i, t.specializationLabel = a;
    return;
  }
  delete t.specializationKey, delete t.specializationLabel;
}
function $G(t) {
  const e = Array.isArray(t == null ? void 0 : t.breakdown) ? t.breakdown : [], n = (i) => {
    var a;
    return Number(((a = e.find((r) => (r == null ? void 0 : r.id) === i)) == null ? void 0 : a.value) ?? 0);
  };
  return {
    attribute: n("attribute"),
    skill: n("skill"),
    bonus: n("bonus"),
    specialization: n("specialization")
  };
}
var qn;
const rt = class rt extends EG(IG) {
  constructor({ actor: n, rollActor: i = null, baseContext: a, initialState: r = null, options: s = {} }) {
    var u, d;
    super(s);
    Fe(this, qn, null);
    /** @type {{ baseContext: any, state: any }} */
    F(this, "_mwd", { baseContext: null, state: null });
    this.actor = n, this.rollActor = i ?? n, this._mwd.baseContext = a ?? {};
    const o = foundry.utils.deepClone(this._mwd.baseContext.payload ?? {}), l = sA(o.manualModifiers);
    this._mwd.state = foundry.utils.mergeObject(
      {
        payload: o,
        manual: l,
        toggles: {
          useEdge: Wm(o, "useEdge"),
          takeRisks: Wm(o, "takeRisks"),
          opponentRoll: Wm(o, "opponentRoll")
        }
      },
      r ?? {},
      { inplace: !1, insertKeys: !0, insertValues: !0, overwrite: !0 }
    );
    const c = String(((d = (u = o == null ? void 0 : o.edge) == null ? void 0 : u.pre) == null ? void 0 : d.poolKey) ?? "").trim() || null;
    this._mwd.state.edge = {
      prePoolKey: c
    };
  }
  async wait() {
    return new Promise((n) => {
      De(this, qn, n), this.render(!0);
    });
  }
  async close(n = {}) {
    if (X(this, qn)) {
      const i = X(this, qn);
      De(this, qn, null), i(null);
    }
    return super.close(n);
  }
  /* --------------------------- */
  /* Prepare Context             */
  /* --------------------------- */
  async _prepareContext(n) {
    var L, _, O, x, H, G, q, ie, Z, Se, se, te, pe, K, be, Ie, Oe, Be, R, V, B, me, oe, Ee, Ye, bt, yt, nn, Dn, On, Ln, $n, _n, cn, Bn, xn, U, Y, Ae, fe, we, Pe, Le, lt, qe, un, et, an, We;
    const i = this._mwd.baseContext ?? {}, a = this._mwd.state ?? {}, r = Number.isFinite(Number((L = a == null ? void 0 : a.payload) == null ? void 0 : L.dn)) ? Number(a.payload.dn) : Number.isFinite(Number((O = (_ = i == null ? void 0 : i.resolved) == null ? void 0 : _.dn) == null ? void 0 : O.total)) ? Number(i.resolved.dn.total) : Number.isFinite(Number(i == null ? void 0 : i.dn)) ? Number(i.dn) : Number.isFinite(Number((H = (x = i == null ? void 0 : i.resolved) == null ? void 0 : x.difficulty) == null ? void 0 : H.dn)) ? Number(i.resolved.difficulty.dn) : 1, s = (i == null ? void 0 : i.intent) ?? "skill";
    let o, l;
    const c = Array.isArray(a.manual) ? a.manual.reduce((ke, Qe) => ke + ((Qe == null ? void 0 : Qe.enabled) === !1 ? 0 : Number((Qe == null ? void 0 : Qe.value) || 0)), 0) : 0;
    if (s === "edge") {
      const ke = (i == null ? void 0 : i.resolved) ?? {}, Qe = Array.isArray(ke.breakdown) ? ke.breakdown : [], zn = (It) => {
        var Mi;
        return Number(((Mi = Qe.find((Fn) => Fn.id === It)) == null ? void 0 : Mi.value) ?? 0);
      }, ct = Number(((G = ke == null ? void 0 : ke.pool) == null ? void 0 : G.attribute) ?? 0);
      o = {
        pool: ct,
        rating: zn("rating"),
        cap: zn("cap"),
        modifiers: Number(((q = i == null ? void 0 : i.dice) == null ? void 0 : q.modifiers) ?? 0)
      }, l = Math.max(0, ct + o.modifiers + c);
    } else {
      o = {
        attribute: Number(((ie = i == null ? void 0 : i.dice) == null ? void 0 : ie.attribute) ?? 0),
        attributeLabel: String(((Z = i == null ? void 0 : i.dice) == null ? void 0 : Z.attributeLabel) ?? "Attribute").trim() || "Attribute",
        skill: Number(((Se = i == null ? void 0 : i.dice) == null ? void 0 : Se.skill) ?? 0),
        skillLabel: String(((se = i == null ? void 0 : i.dice) == null ? void 0 : se.skillLabel) ?? "Skill").trim() || "Skill",
        bonus: Number(((te = i == null ? void 0 : i.dice) == null ? void 0 : te.bonus) ?? 0),
        bonusLabel: String(((pe = i == null ? void 0 : i.dice) == null ? void 0 : pe.bonusLabel) ?? "Bonus").trim() || "Bonus",
        specialization: Number(((K = i == null ? void 0 : i.dice) == null ? void 0 : K.specialization) ?? 0),
        specializationLabel: String(((be = i == null ? void 0 : i.dice) == null ? void 0 : be.specializationLabel) ?? "Specialization").trim() || "Specialization",
        modifiers: Number(((Ie = i == null ? void 0 : i.dice) == null ? void 0 : Ie.modifiers) ?? 0)
      };
      const ke = o.modifiers + c, Qe = o.attribute + o.skill + o.bonus + o.specialization;
      l = Math.max(0, Qe + ke);
    }
    const u = Array.isArray((Oe = i == null ? void 0 : i.resolved) == null ? void 0 : Oe.domains) ? i.resolved.domains : [], d = u.includes("physical") ? "physical" : u.includes("mental") ? "mental" : u.includes("social") ? "social" : null, m = (d === "physical" ? ["grit", "chaos"] : d === "mental" ? ["insight", "rumor"] : d === "social" ? ["legend", "credibility"] : []).map((ke) => {
      var Qe, zn, ct, It;
      return {
        key: ke,
        label: ke.charAt(0).toUpperCase() + ke.slice(1),
        available: Number(((ct = (zn = (Qe = this.rollActor) == null ? void 0 : Qe.getEdgePool) == null ? void 0 : zn.call(Qe, ke)) == null ? void 0 : ct.effectiveValue) ?? 0),
        selected: ke === (((It = a.edge) == null ? void 0 : It.prePoolKey) ?? null)
      };
    }), g = m.find((ke) => ke.selected), p = (g == null ? void 0 : g.label) ?? null, h = ((Be = i == null ? void 0 : i.resolved) == null ? void 0 : Be.attack) ?? null, b = String(
      ((R = h == null ? void 0 : h.skill) == null ? void 0 : R.code) ?? ((B = (V = i == null ? void 0 : i.resolved) == null ? void 0 : V.specialization) == null ? void 0 : B.skillKey) ?? ((oe = (me = i == null ? void 0 : i.resolved) == null ? void 0 : me.data) == null ? void 0 : oe.skillKey) ?? ((Ee = i == null ? void 0 : i.payload) == null ? void 0 : Ee.key) ?? ""
    ).trim(), y = b ? lk(((Ye = this.rollActor) == null ? void 0 : Ye.system) ?? {}, b) : [], S = String(((bt = a == null ? void 0 : a.payload) == null ? void 0 : bt.specializationKey) ?? "").trim(), A = y.find((ke) => ke.key === S) ?? null;
    if (s !== "edge") {
      o.specialization = A ? Number(((nn = (yt = i == null ? void 0 : i.resolved) == null ? void 0 : yt.specialization) == null ? void 0 : nn.value) ?? 2) : 0;
      const ke = o.modifiers + c, Qe = o.attribute + o.skill + o.bonus + o.specialization;
      l = Math.max(0, Qe + ke);
    }
    const T = Array.isArray((Dn = h == null ? void 0 : h.payloadState) == null ? void 0 : Dn.payloads) ? h.payloadState.payloads : [], C = String(((On = h == null ? void 0 : h.weapon) == null ? void 0 : On.category) ?? "").trim().toLowerCase() !== "melee" && T.length > 0, k = String(((Ln = a == null ? void 0 : a.payload) == null ? void 0 : Ln.payloadId) ?? (($n = h == null ? void 0 : h.payloadState) == null ? void 0 : $n.activePayloadId) ?? "").trim(), w = T.find((ke) => ke.id === k) ?? null, v = ((Bn = (cn = (_n = h == null ? void 0 : h.weapon) == null ? void 0 : _n.item) == null ? void 0 : cn.getPayloadState) == null ? void 0 : Bn.call(cn, { payloadId: k })) ?? null, N = (i == null ? void 0 : i.machineActor) ?? this.actor, P = s === "attack" && h && xt(N) ? OG({ manual: a.manual ?? [], attack: h, payload: a.payload ?? {} }) : null;
    return {
      header: {
        left: ((xn = i == null ? void 0 : i.header) == null ? void 0 : xn.left) ?? "Roll",
        right: ((U = i == null ? void 0 : i.header) == null ? void 0 : U.right) ?? ((Y = this.actor) == null ? void 0 : Y.name) ?? ""
      },
      formula: String((i == null ? void 0 : i.formula) ?? ((Ae = i == null ? void 0 : i.resolved) == null ? void 0 : Ae.formula) ?? "").trim(),
      dice: o,
      modifiers: Array.isArray(i.modifiers) ? i.modifiers : [],
      manual: (a.manual ?? []).map((ke) => ({
        ...ke,
        sourceLabel: String(ke.source ?? "Manual").trim() || "Manual",
        steps: PG(Number(ke.value ?? 0), Math.min(-4, Number(ke.value ?? 0), -3), 3)
      })),
      edge: {
        domain: d,
        choices: m,
        selectedLabel: p
      },
      toggles: s === "edge" ? { useEdge: !1, takeRisks: !1, opponentRoll: !1 } : a.toggles,
      totalPool: l,
      intent: s,
      dn: r,
      specialization: y.length ? {
        skillCode: b,
        options: y.map((ke) => ({
          key: ke.key,
          label: ke.label,
          selected: ke.key === S
        })),
        selectedKey: S,
        selectedLabel: (A == null ? void 0 : A.label) ?? ""
      } : null,
      attack: h ? {
        weaponName: ((fe = h == null ? void 0 : h.weapon) == null ? void 0 : fe.name) ?? "Weapon",
        rangeBand: ((we = h == null ? void 0 : h.weapon) == null ? void 0 : we.type) === "personalWeapon" || (Pe = h == null ? void 0 : h.weapon) != null && Pe.isSynthetic ? dd((h == null ? void 0 : h.rangeBand) ?? "") : (h == null ? void 0 : h.rangeBand) ?? "",
        damageType: ((Le = w == null ? void 0 : w.modifies) == null ? void 0 : Le.damageType) || ((lt = h == null ? void 0 : h.weapon) == null ? void 0 : lt.damageTypeLabel) || ((qe = h == null ? void 0 : h.weapon) == null ? void 0 : qe.damageType) || "",
        usesPayloads: C,
        source: (v == null ? void 0 : v.sourceState) ?? (h == null ? void 0 : h.sourceState) ?? null,
        payloads: T.map((ke) => {
          var Qe;
          return {
            id: ke.id,
            name: ke.label,
            damageType: (Qe = ke.modifies) == null ? void 0 : Qe.damageType,
            selected: ke.id === k
          };
        }),
        selectedPayloadId: k,
        selectedPayloadLabel: (v == null ? void 0 : v.payloadLabel) ?? (w == null ? void 0 : w.label) ?? ((un = h == null ? void 0 : h.payload) == null ? void 0 : un.label) ?? ((et = h == null ? void 0 : h.weapon) == null ? void 0 : et.payloadLabel) ?? "",
        selectedSourceLabel: ((an = v == null ? void 0 : v.sourceState) == null ? void 0 : an.label) ?? ((We = h == null ? void 0 : h.sourceState) == null ? void 0 : We.label) ?? ""
      } : null,
      machineAttackOptions: P
    };
  }
  /* --------------------------- */
  /* Actions                     */
  /* --------------------------- */
  async _onCancel(n) {
    if (n == null || n.preventDefault(), X(this, qn)) {
      const i = X(this, qn);
      De(this, qn, null), i(null);
    }
    return this.close();
  }
  async _onSubmit(n) {
    var a, r, s, o, l, c, u, d, f, m, g, p, h;
    n == null || n.preventDefault();
    const i = this._mwd.state;
    if (i.payload.manualModifiers = (i.manual ?? []).filter((b) => {
      var y;
      return b && (((y = b.label) == null ? void 0 : y.trim()) || Number(b.value));
    }).map((b) => {
      var y;
      return {
        id: b.id,
        label: ((y = b.label) == null ? void 0 : y.trim()) || "Manual",
        value: Number(b.value ?? 0),
        enabled: b.enabled !== !1,
        source: String(b.source ?? "Manual").trim() || "Manual",
        optional: b.optional === !0,
        traitItemId: String(b.traitItemId ?? "").trim(),
        traitEffectId: String(b.traitEffectId ?? "").trim()
      };
    }), LG(i.payload, i.toggles ?? {}), lA(
      i.payload,
      ((a = i.payload) == null ? void 0 : a.intent) === "attack" ? ((r = i.payload) == null ? void 0 : r.skillKey) ?? ((c = (l = (o = (s = this._mwd.baseContext) == null ? void 0 : s.resolved) == null ? void 0 : o.attack) == null ? void 0 : l.skill) == null ? void 0 : c.code) : ((u = i.payload) == null ? void 0 : u.key) ?? ((m = (f = (d = this._mwd.baseContext) == null ? void 0 : d.resolved) == null ? void 0 : f.data) == null ? void 0 : m.skillKey),
      (g = i.payload) == null ? void 0 : g.specializationKey
    ), X(this, qn)) {
      const b = X(this, qn);
      De(this, qn, null), b({ payload: i.payload });
    }
    if (i.payload.edge = i.payload.edge && typeof i.payload.edge == "object" ? i.payload.edge : {}, i.payload.edge.pre = i.payload.edge.pre && typeof i.payload.edge.pre == "object" ? i.payload.edge.pre : {}, (p = i.toggles) != null && p.useEdge) {
      const b = String(((h = i.edge) == null ? void 0 : h.prePoolKey) ?? "").trim() || null;
      i.payload.edge.pre.poolKey = b, i.payload.edge.pre.spent = b ? 1 : 0;
    } else
      i.payload.edge.pre.poolKey = null, i.payload.edge.pre.spent = 0;
    return this.close();
  }
  async _onAddManual(n) {
    return n == null || n.preventDefault(), this._mwd.state.manual.push({
      id: foundry.utils.randomID(),
      label: "Manual",
      value: 0,
      enabled: !0,
      source: "Manual"
    }), this.render(!1);
  }
  async _onRemoveManual(n, i) {
    var r;
    n == null || n.preventDefault();
    const a = (r = i == null ? void 0 : i.dataset) == null ? void 0 : r.id;
    if (a)
      return this._mwd.state.manual = this._mwd.state.manual.filter((s) => s.id !== a), this.render(!1);
  }
  async _onSetManualValue(n, i) {
    var o, l;
    n == null || n.preventDefault();
    const a = (o = i == null ? void 0 : i.dataset) == null ? void 0 : o.id, r = (l = i == null ? void 0 : i.dataset) == null ? void 0 : l.field;
    if (!a || !r) return;
    const s = this._mwd.state.manual.find((c) => c.id === a);
    if (s)
      return r === "label" && (s.label = String(i.value ?? "")), r === "value" && (s.value = Number(i.value ?? 0)), this.render(!1);
  }
  async _onSetManualStepper(n, i) {
    var o, l;
    n == null || n.preventDefault();
    const a = (o = i == null ? void 0 : i.dataset) == null ? void 0 : o.id, r = Number((l = i == null ? void 0 : i.dataset) == null ? void 0 : l.value);
    if (!a || Number.isNaN(r)) return;
    const s = this._mwd.state.manual.find((c) => c.id === a);
    if (s)
      return s.value = r, this.render(!1);
  }
  async _onToggleManual(n, i) {
    var s;
    n == null || n.preventDefault();
    const a = (s = i == null ? void 0 : i.dataset) == null ? void 0 : s.id;
    if (!a) return;
    const r = this._mwd.state.manual.find((o) => o.id === a);
    if (r)
      return r.enabled = !!i.checked, this.render(!1);
  }
  async _onSetEdgePrePool(n, i) {
    var r;
    n == null || n.preventDefault();
    const a = String(((r = i == null ? void 0 : i.dataset) == null ? void 0 : r.poolKey) ?? "").trim();
    if (a)
      return this._mwd.state.edge = this._mwd.state.edge ?? {}, this._mwd.state.edge.prePoolKey = a, this._mwd.state.toggles.useEdge = !0, this.render(!1);
  }
  async _onToggleCheckbox(n, i) {
    var r;
    n == null || n.preventDefault();
    const a = (r = i == null ? void 0 : i.dataset) == null ? void 0 : r.key;
    if (a)
      return this._mwd.state.toggles[a] = !!i.checked, this.render(!1);
  }
  async _onSetMachineTargetMotion(n, i) {
    var r;
    n == null || n.preventDefault();
    const a = jr((r = i == null ? void 0 : i.dataset) == null ? void 0 : r.motion);
    return Og(this._mwd.state.payload, { targetMotion: a }), this.render(!1);
  }
  async _onToggleMachineTargetJumped(n, i) {
    return n == null || n.preventDefault(), Og(this._mwd.state.payload, { jumped: !!(i != null && i.checked) }), this.render(!1);
  }
  async _onToggleAttackOption(n, i) {
    var l, c, u, d;
    n == null || n.preventDefault();
    const a = String(((l = i == null ? void 0 : i.dataset) == null ? void 0 : l.optionId) ?? "").trim();
    if (!a) return;
    const r = this._mwd.state.manual ?? [], s = Lg(r, a);
    this._mwd.state.manual = s ? DG(r, a) : NC(r, {
      id: a,
      label: ((c = i == null ? void 0 : i.dataset) == null ? void 0 : c.label) ?? "Attack Option",
      value: Number(((u = i == null ? void 0 : i.dataset) == null ? void 0 : u.value) ?? 0)
    });
    const o = String(((d = i == null ? void 0 : i.dataset) == null ? void 0 : d.flag) ?? "").trim();
    return o && Dg(this._mwd.state.payload, { [o]: !s }), this.render(!1);
  }
  async _onToggleAttackFlag(n, i) {
    var r;
    n == null || n.preventDefault();
    const a = String(((r = i == null ? void 0 : i.dataset) == null ? void 0 : r.flag) ?? "").trim();
    if (a)
      return Dg(this._mwd.state.payload, { [a]: !!(i != null && i.checked) }), this.render(!1);
  }
  async _onSetDn(n, i) {
    n == null || n.preventDefault();
    const a = String((i == null ? void 0 : i.value) ?? "").trim(), r = a === "" ? null : Number(a);
    return this._mwd.state.payload.dn = Number.isFinite(r) ? Math.max(0, Math.trunc(r)) : null, this.render(!1);
  }
  async _onSetPayload(n, i) {
    return n == null || n.preventDefault(), this._mwd.state.payload.payloadId = String((i == null ? void 0 : i.value) ?? "").trim(), this.render(!1);
  }
  async _onSetSpecialization(n, i) {
    var s;
    n == null || n.preventDefault();
    const a = String(((s = i == null ? void 0 : i.dataset) == null ? void 0 : s.skillCode) ?? "").trim(), r = String((i == null ? void 0 : i.value) ?? "").trim();
    if (a)
      return lA(this._mwd.state.payload, a, r), this.render(!1);
  }
  _onRender(n, i) {
    var r, s;
    (r = super._onRender) == null || r.call(this, n, i);
    const a = this.element instanceof HTMLElement ? this.element : (s = this.element) == null ? void 0 : s[0];
    a && (a.querySelectorAll("[data-action='setPayload']").forEach((o) => {
      o.addEventListener("change", (l) => {
        this._onSetPayload(l, l.currentTarget);
      });
    }), a.querySelectorAll("[data-action='setSpecialization']").forEach((o) => {
      o.addEventListener("change", (l) => {
        this._onSetSpecialization(l, l.currentTarget);
      });
    }), a.querySelectorAll("[data-action='setDn']").forEach((o) => {
      o.addEventListener("change", (l) => {
        this._onSetDn(l, l.currentTarget);
      });
    }), a.querySelectorAll("[data-action='toggleMachineTargetJumped']").forEach((o) => {
      o.addEventListener("change", (l) => {
        this._onToggleMachineTargetJumped(l, l.currentTarget);
      });
    }), a.querySelectorAll("[data-action='toggleAttackFlag']").forEach((o) => {
      o.addEventListener("change", (l) => {
        this._onToggleAttackFlag(l, l.currentTarget);
      });
    }));
  }
  /**
   * Open the roll dialog as a payload editor and return an updated payload.
   * Cancel returns null.
   *
   * IMPORTANT:
   *  - Prefer passing explicit dice parts via args.diceParts (attribute/skill/bonus).
   *  - This avoids scraping resolved.breakdown.
   */
  static async prompt({ actor: n, rollActor: i = null, basePayload: a, resolved: r, diceParts: s = null, mods: o = [], modTotal: l = 0 } = {}) {
    var h, b, y, S, A, T, C, k, w, v, N, P, L, _;
    const c = foundry.utils.deepClone(a ?? {});
    RG(c, r);
    try {
      const O = (r == null ? void 0 : r.rollType) ?? "simple", x = String((c == null ? void 0 : c.intent) ?? (r == null ? void 0 : r.intent) ?? "").trim().toLowerCase();
      if (O === "simple" && x !== "attack" && (c == null ? void 0 : c.dn) == null) {
        const H = Number(game.settings.get(game.system.id, "gmNextDn"));
        Number.isFinite(H) && (c.dn = Math.max(0, Math.trunc(H)));
      }
    } catch (O) {
      console.warn("MWD: failed to default DN from GM Gadget", O);
    }
    const u = {
      left: (r == null ? void 0 : r.title) ?? "Roll",
      right: (n == null ? void 0 : n.name) ?? ""
    }, d = s ?? $G(r), f = {
      attribute: Number((d == null ? void 0 : d.attribute) ?? 0),
      attributeLabel: ((y = (b = (h = r == null ? void 0 : r.breakdown) == null ? void 0 : h.find) == null ? void 0 : b.call(h, (O) => (O == null ? void 0 : O.id) === "attribute")) == null ? void 0 : y.label) ?? "Attribute",
      skill: Number((d == null ? void 0 : d.skill) ?? 0),
      skillLabel: ((T = (A = (S = r == null ? void 0 : r.breakdown) == null ? void 0 : S.find) == null ? void 0 : A.call(S, (O) => (O == null ? void 0 : O.id) === "skill")) == null ? void 0 : T.label) ?? "Skill",
      bonus: Number((d == null ? void 0 : d.bonus) ?? 0),
      bonusLabel: ((w = (k = (C = r == null ? void 0 : r.breakdown) == null ? void 0 : C.find) == null ? void 0 : k.call(C, (O) => (O == null ? void 0 : O.id) === "bonus")) == null ? void 0 : w.label) ?? "Bonus",
      specialization: Number((d == null ? void 0 : d.specialization) ?? 0),
      specializationLabel: ((P = (N = (v = r == null ? void 0 : r.breakdown) == null ? void 0 : v.find) == null ? void 0 : N.call(v, (O) => (O == null ? void 0 : O.id) === "specialization")) == null ? void 0 : P.label) ?? "Specialization",
      modifiers: Number(l ?? 0)
    }, m = (Array.isArray(o) ? o : []).map((O) => ({
      label: O.label ?? "Modifier",
      source: O.source ?? "",
      value: Number(O.value ?? 0)
    }));
    c.manualModifiers = sA(c.manualModifiers);
    const p = await new rt({
      actor: n,
      rollActor: i,
      baseContext: {
        intent: (r == null ? void 0 : r.intent) ?? "skill",
        header: u,
        formula: String((r == null ? void 0 : r.formula) ?? "").trim(),
        dice: f,
        modifiers: m,
        payload: c,
        resolved: r,
        // keep full resolved for edge display
        rollActor: i,
        machineActor: n,
        dn: Number((c == null ? void 0 : c.dn) ?? ((L = r == null ? void 0 : r.dn) == null ? void 0 : L.total) ?? ((_ = r == null ? void 0 : r.difficulty) == null ? void 0 : _.dn) ?? 1)
      }
    }).wait();
    return (p == null ? void 0 : p.payload) ?? null;
  }
};
qn = new WeakMap(), F(rt, "DEFAULT_OPTIONS", foundry.utils.mergeObject(
  vi(rt, rt, "DEFAULT_OPTIONS"),
  {
    id: "mwd-roll-dialog",
    classes: [
      ...vi(rt, rt, "DEFAULT_OPTIONS").classes ?? [],
      "mwd",
      "mwd-roll-dialog"
    ],
    window: { title: "Roll", resizable: !1 },
    position: { width: 520, height: "auto" },
    actions: {
      submit: rt.prototype._onSubmit,
      cancel: rt.prototype._onCancel,
      addManual: rt.prototype._onAddManual,
      removeManual: rt.prototype._onRemoveManual,
      setManualValue: rt.prototype._onSetManualValue,
      setManualStepper: rt.prototype._onSetManualStepper,
      toggleManual: rt.prototype._onToggleManual,
      setEdgePrePool: rt.prototype._onSetEdgePrePool,
      toggleCheckbox: rt.prototype._onToggleCheckbox,
      setMachineTargetMotion: rt.prototype._onSetMachineTargetMotion,
      toggleMachineTargetJumped: rt.prototype._onToggleMachineTargetJumped,
      toggleAttackOption: rt.prototype._onToggleAttackOption,
      toggleAttackFlag: rt.prototype._onToggleAttackFlag,
      setDn: rt.prototype._onSetDn,
      setPayload: rt.prototype._onSetPayload,
      setSpecialization: rt.prototype._onSetSpecialization
    }
  },
  { inplace: !1 }
)), F(rt, "PARTS", {
  body: { template: "systems/mwd/templates/v2/roll/mwd-roll-dialog.hbs" }
});
let $g = rt;
const { ApplicationV2: _G, HandlebarsApplicationMixin: BG } = foundry.applications.api, jo = class jo extends BG(_G) {
  static get DEFAULT_OPTIONS() {
    return foundry.utils.mergeObject(super.DEFAULT_OPTIONS, {
      id: "select-item",
      classes: ["select-item", "anarchy-dialog"],
      position: { width: 360, height: "auto" },
      window: {
        resizable: !0
      }
    }, { inplace: !1 });
  }
  static async selectItem(e, n) {
    const i = {
      id: `select-item-${foundry.utils.randomID()}`,
      classes: [game.system.anarchy.styles.selectCssClass(), ...jo.DEFAULT_OPTIONS.classes],
      window: { title: e }
    };
    return new jo({ items: n }, i).wait();
  }
  constructor(e = {}, n = {}) {
    super(e, n), this.items = Array.isArray(e.items) ? e.items : [], this._selected = !1, this._resolve = null;
  }
  async _prepareContext() {
    return { items: this.items };
  }
  _onRender(e, n) {
    var i;
    (i = super._onRender) == null || i.call(this, e, n), $(this.element).find(".click-select-item").on("click", (a) => this.onSelectItem(a)), $(this.element).find('[data-action="cancel"]').on("click", async () => {
      if (this._resolve) {
        const a = this._resolve;
        this._resolve = null, a(null);
      }
      await this.close();
    });
  }
  wait() {
    return new Promise((e) => {
      this._resolve = e, this.render({ force: !0 });
    });
  }
  async onSelectItem(e) {
    const n = $(e.currentTarget).attr("data-item-id"), i = this.items.find((a) => a.id === n) ?? null;
    if (this._selected = !0, this._resolve) {
      const a = this._resolve;
      this._resolve = null, a(i);
    }
    await this.close();
  }
  async close(e) {
    if (!this._selected && this._resolve) {
      const n = this._resolve;
      this._resolve = null, n(null);
    }
    return super.close(e);
  }
};
F(jo, "PARTS", {
  body: {
    template: `${ht}/dialog/select-item.hbs`
  }
});
let _g = jo;
function oa(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function xG(t) {
  const e = String(t ?? "").trim().toLowerCase();
  if (e === "onfire") return { kind: "onFire" };
  if (e === "tagged" || e === "tag") return { kind: "status", statusId: "tagged" };
  if (e === "narced" || e === "narc") return { kind: "status", statusId: "narced" };
  const n = e.match(/^(burn|heat)\+(\d+(?:\.\d+)?)$/);
  if (!n) return null;
  const i = Math.max(0, Number(n[2]) || 0);
  return i > 0 ? { kind: n[1], amount: i } : null;
}
function zG(t = null) {
  return !1;
}
function FG(t = {}) {
  return String((t == null ? void 0 : t.damageScaling) ?? "direct").trim().toLowerCase() === "exposure";
}
function UG(t = {}) {
  var i, a, r, s;
  const e = Array.isArray((i = t == null ? void 0 : t.attack) == null ? void 0 : i.targets) ? t.attack.targets : [], n = Gi(((a = t == null ? void 0 : t.attack) == null ? void 0 : a.areaEffect) ?? ((s = (r = t == null ? void 0 : t.attack) == null ? void 0 : r.payload) == null ? void 0 : s.areaEffect) ?? {});
  if (!e.length && n.kind !== kt.persistent)
    throw new Error("Attack requires at least one target.");
  return e;
}
async function PC(t = {}) {
  if (!(t != null && t.actorUuid)) return null;
  try {
    return await fromUuid(t.actorUuid);
  } catch (e) {
    return console.warn("MWD | Unable to resolve attack target actor for CQ", t, e), null;
  }
}
function KG(t = {}, e = null, n = "") {
  var i, a, r, s, o;
  return Math.max(0, oa(
    ((i = t == null ? void 0 : t.attributes) == null ? void 0 : i[n]) ?? ((a = e == null ? void 0 : e.getAttributeValue) == null ? void 0 : a.call(e, n)) ?? ((o = (s = (r = e == null ? void 0 : e.system) == null ? void 0 : r.attributes) == null ? void 0 : s[n]) == null ? void 0 : o.value),
    0
  ));
}
function HG(t = {}, e = null, n = "") {
  var i, a, r, s, o, l;
  return Math.max(0, oa(
    ((a = (i = t == null ? void 0 : t.skills) == null ? void 0 : i[n]) == null ? void 0 : a.rating) ?? ((r = e == null ? void 0 : e.getSkillRating) == null ? void 0 : r.call(e, n)) ?? ((l = (o = (s = e == null ? void 0 : e.system) == null ? void 0 : s.skills) == null ? void 0 : o[n]) == null ? void 0 : l.rating),
    0
  ));
}
function kc(t = []) {
  return t.reduce((e, n) => e + oa(n == null ? void 0 : n.value, 0), 0);
}
async function jG({ attacker: t = null, ctx: e = {}, target: n = {} } = {}) {
  var O, x, H, G, q, ie, Z, Se, se, te, pe, K, be, Ie, Oe, Be, R, V, B, me, oe, Ee, Ye, bt, yt, nn, Dn, On, Ln, $n, _n, cn, Bn, xn, U, Y, Ae, fe;
  const i = await PC(n), a = Math.max(0, Number(((G = (x = (O = e == null ? void 0 : e.attack) == null ? void 0 : O.weapon) == null ? void 0 : x.attackRatingBand) == null ? void 0 : G[(H = e == null ? void 0 : e.attack) == null ? void 0 : H.rangeBand]) ?? 0) || 0), r = Fs(i), s = r ? M.actorAttributes.handling : "reflexes", o = KG(n, i, s), l = o + o, c = String(((ie = (q = e == null ? void 0 : e.attack) == null ? void 0 : q.skill) == null ? void 0 : ie.code) ?? ((Se = (Z = e == null ? void 0 : e.attack) == null ? void 0 : Z.weapon) == null ? void 0 : Se.skill) ?? "").trim(), u = String(((te = (se = e == null ? void 0 : e.attack) == null ? void 0 : se.skill) == null ? void 0 : te.label) ?? c ?? "Attack Skill").trim() || "Attack Skill", d = c ? Math.max(0, oa(((pe = t == null ? void 0 : t.getSkillRating) == null ? void 0 : pe.call(t, c)) ?? ((Ie = (be = (K = t == null ? void 0 : t.system) == null ? void 0 : K.skills) == null ? void 0 : be[c]) == null ? void 0 : Ie.rating), 0)) : 0, f = r ? "piloting" : "tactics", m = r ? "Piloting" : "Tactics", g = HG(n, i, f), p = d - g, h = Math.abs(p), b = Math.max(0, Number(((Oe = n == null ? void 0 : n.activeArmor) == null ? void 0 : Oe.defenseBonus) ?? 0) || 0), y = String(((Be = e == null ? void 0 : e.attack) == null ? void 0 : Be.rangeBand) ?? "").trim() || "range", A = [{
    id: "weapon.attackRating",
    label: `Weapon AR (${((V = (R = e == null ? void 0 : e.attack) == null ? void 0 : R.weapon) == null ? void 0 : V.type) === "personalWeapon" || (me = (B = e == null ? void 0 : e.attack) == null ? void 0 : B.weapon) != null && me.isSynthetic ? dd(y) : y})`,
    value: a
  }], T = [{
    id: r ? "target.handlingDefense" : "target.reflexesDefense",
    label: r ? "Target Handling + Handling" : "Target REF + REF",
    value: l
  }];
  if (p > 0 ? A.push({
    id: "skill.attackVsTactics",
    label: `${u} over Tactics`,
    value: h
  }) : p < 0 && T.push({
    id: "target.tacticsAdvantage",
    label: `${m} over ${u}`,
    value: h
  }), (Ee = (oe = e == null ? void 0 : e.attack) == null ? void 0 : oe.aim) != null && Ee.eligible) {
    const we = String(((bt = (Ye = e == null ? void 0 : e.attack) == null ? void 0 : Ye.aim) == null ? void 0 : bt.bonusSkillCode) ?? "perception").trim() || "perception", Pe = String(((nn = (yt = e == null ? void 0 : e.attack) == null ? void 0 : yt.aim) == null ? void 0 : nn.bonusSkillLabel) ?? "Perception").trim() || "Perception", Le = Math.max(0, oa(
      ((Dn = t == null ? void 0 : t.getSkillRating) == null ? void 0 : Dn.call(t, we)) ?? (($n = (Ln = (On = t == null ? void 0 : t.system) == null ? void 0 : On.skills) == null ? void 0 : Ln[we]) == null ? void 0 : $n.rating),
      0
    ));
    A.push({
      id: "state.aim",
      label: `Aim (${Pe})`,
      value: Le
    });
  }
  if (T.push({
    id: "target.armorDefense",
    label: "Armor Defense",
    value: b
  }), Fs(t)) {
    const we = ll(t, {
      rangeBand: (_n = e == null ? void 0 : e.attack) == null ? void 0 : _n.rangeBand
    });
    we.ar && A.push({
      id: "machineState.attackAr",
      label: "Machine State",
      value: we.ar
    });
    for (const Pe of uu(t, { resolved: e, payload: ((cn = e == null ? void 0 : e.attack) == null ? void 0 : cn.payload) ?? {} })) {
      const Le = oa((Bn = Pe.modifies) == null ? void 0 : Bn.ar, 0);
      Le && A.push({
        id: `assetModule.${Pe.sourceId}.${Pe.id}.ar`,
        label: Pe.label,
        value: Le
      });
    }
    if (uS(t)) {
      const Pe = oa((xn = cS(t)) == null ? void 0 : xn.attackRatingBonus, 0);
      Pe && A.push({ id: "jumping.attackRatingBonus", label: "Jump Maneuver", value: Pe });
    }
  }
  if (r) {
    const we = ll(i, {
      rangeBand: (U = e == null ? void 0 : e.attack) == null ? void 0 : U.rangeBand
    });
    we.dr && T.push({
      id: "machineState.defenseDr",
      label: "Machine State",
      value: we.dr
    });
    for (const Pe of uu(i, { resolved: e, payload: ((Y = e == null ? void 0 : e.attack) == null ? void 0 : Y.payload) ?? {} })) {
      const Le = oa((Ae = Pe.modifies) == null ? void 0 : Ae.dr, 0);
      Le && T.push({
        id: `assetModule.${Pe.sourceId}.${Pe.id}.dr`,
        label: Pe.label,
        value: Le
      });
    }
    if (uS(i)) {
      const Pe = oa((fe = cS(i)) == null ? void 0 : fe.defenseRatingBonus, 0);
      Pe && T.push({ id: "jumping.defenseRatingBonus", label: "Jump Maneuver", value: Pe });
    }
  }
  const C = oi(t, "attackRatingMod");
  C && A.push({
    id: "activeEffect.attackRatingMod",
    label: "Active Effect AR",
    value: C
  });
  const k = { total: kc(A) }, w = zt({
    actor: t,
    phase: "onAttackRatingResolved",
    facts: tP({ actor: t, packet: k, runtime: {} }),
    packet: k,
    options: { consumeUsage: !1 }
  });
  for (const we of w.modifiers)
    A.push({
      id: we.id,
      label: we.label,
      value: Number(we.value ?? 0)
    });
  const v = Number(w.packet.total ?? kc(A)) || 0, N = oi(i, "defenseRatingMod");
  N && T.push({
    id: "activeEffect.defenseRatingMod",
    label: "Active Effect DR",
    value: N
  });
  const P = { total: kc(T) }, L = zt({
    actor: i,
    phase: "onDefenseRatingResolved",
    facts: Kk({
      actor: i,
      packet: P,
      runtime: {}
    }),
    packet: P,
    options: { consumeUsage: !1 }
  });
  for (const we of L.modifiers)
    T.push({
      id: we.id,
      label: we.label,
      value: Number(we.value ?? 0)
    });
  const _ = Number(L.packet.total ?? kc(T)) || 0;
  return {
    ar: {
      parts: A,
      total: v
    },
    dr: {
      parts: T,
      total: _
    },
    comparison: {
      attackSkillCode: c,
      attackSkillLabel: u,
      attackerSkill: d,
      defenderSkillCode: f,
      defenderSkillLabel: m,
      defenderSkill: g,
      delta: p,
      advantage: h,
      winner: p > 0 ? "attacker" : p < 0 ? "defender" : "none"
    },
    value: v - _
  };
}
async function GG(t = {}, e = {}) {
  var P, L, _, O, x, H, G, q, ie, Z, Se, se, te, pe, K, be;
  const n = (t == null ? void 0 : t.attack) ?? {}, i = String(((L = (P = n == null ? void 0 : n.payload) == null ? void 0 : P.modifies) == null ? void 0 : L.damageType) ?? "").trim(), a = RO(t == null ? void 0 : t.attacker, {
    weaponGroupId: ((O = (_ = n == null ? void 0 : n.weapon) == null ? void 0 : _.machineWeaponGroup) == null ? void 0 : O.id) ?? ((x = n == null ? void 0 : n.payload) == null ? void 0 : x.weaponGroupId),
    weaponId: (H = n == null ? void 0 : n.payload) == null ? void 0 : H.weaponId,
    weapon: n == null ? void 0 : n.weapon
  }), r = Rp(t == null ? void 0 : t.attacker), s = tl(((G = n == null ? void 0 : n.weapon) == null ? void 0 : G.baseDamageType) ?? ((q = n == null ? void 0 : n.weapon) == null ? void 0 : q.damageType)), o = Math.max(0, (Number(((ie = n == null ? void 0 : n.weapon) == null ? void 0 : ie.damage) ?? 0) || 0) + a + (s ? Number(r.energyAttackDamage ?? 0) : 0)), l = Math.max(0, Number(((Z = n == null ? void 0 : n.weapon) == null ? void 0 : Z.clusteringDice) ?? 0) || 0), c = Number(((Se = n == null ? void 0 : n.weapon) == null ? void 0 : Se.clusteringTargetNumber) ?? 5) || 5, u = e.outcome !== "miss" && l > 0 ? await dj({
    clusteringDice: l,
    clusteringTargetNumber: c
  }) : {
    rolled: !1,
    dice: l,
    targetNumber: c,
    hits: 0,
    formula: l > 0 ? `${l}d6cs>=${c}` : "",
    results: [],
    roll: null
  }, d = e.outcome === "miss" ? 0 : Number(u.hits ?? 0), f = !!(t != null && t.targetIsMachine), m = i || ((se = n == null ? void 0 : n.weapon) == null ? void 0 : se.damageType), g = f ? String(m ?? "kinetic").trim() || "kinetic" : Xn(m, "concussive"), p = Math.max(0, Number((n == null ? void 0 : n.totalAp) ?? ((te = n == null ? void 0 : n.weapon) == null ? void 0 : te.ap) ?? 0) || 0), h = e.outcome === "graze" ? 0.5 : 1, b = e.outcome === "miss" ? 0 : o, y = zG(n == null ? void 0 : n.weapon) ? Number(e.netHits ?? 0) : 0, S = b * h, A = d * h, T = y * h, C = S + A + T, k = Xg((n == null ? void 0 : n.currentExposure) ?? Va({
    tier: ((pe = n == null ? void 0 : n.currentExposure) == null ? void 0 : pe.initialTier) ?? ((K = n == null ? void 0 : n.currentExposure) == null ? void 0 : K.tier) ?? "none"
  }), {
    active: !!(n != null && n.evadeActive),
    locked: !!(n != null && n.evadeLocked)
  }), w = Gi((n == null ? void 0 : n.areaEffect) ?? ((be = n == null ? void 0 : n.payload) == null ? void 0 : be.areaEffect) ?? {}), v = FG(n), N = v ? $r(C, k.finalTier) : C;
  return {
    baseDamage: o,
    effectiveWeaponDamage: S,
    clustering: {
      ...u,
      damageBonus: A
    },
    netHits: Number(e.netHits ?? 0),
    netDamageBonus: T,
    attackQuality: e.outcome === "graze" ? "graze" : e.outcome === "hit" && Number(e.netHits ?? 0) >= 4 ? "highMargin" : e.outcome === "hit" ? "hit" : "",
    incoming: C,
    scaledIncoming: N,
    ap: p,
    damageType: g,
    damageTypeLabel: f ? g : In(g),
    exposure: k,
    usesExposureScaling: v,
    areaEffect: w
  };
}
function qG(t = {}) {
  return String((t == null ? void 0 : t.tokenUuid) ?? (t == null ? void 0 : t.actorUuid) ?? (t == null ? void 0 : t.tokenId) ?? (t == null ? void 0 : t.actorId) ?? (t == null ? void 0 : t.name) ?? foundry.utils.randomID()).trim();
}
function cA(t, e) {
  var r, s;
  const n = ((s = (r = t == null ? void 0 : t.system) == null ? void 0 : r.monitors) == null ? void 0 : s[e]) ?? {}, i = Math.max(0, Number(n.max ?? 0) || 0);
  return Math.min(i, Math.max(0, Number(n.value ?? 0) || 0));
}
function uA({ attacker: t, ctx: e, damage: n, targetActor: i = null, hitLocation: a = null } = {}) {
  var o, l, c, u, d, f, m, g, p, h, b, y, S, A, T, C, k, w, v, N, P, L, _, O, x, H, G, q;
  const r = ((c = (l = (o = e == null ? void 0 : e.attack) == null ? void 0 : o.weapon) == null ? void 0 : l.resolution) == null ? void 0 : c.onHitEffect) ?? null, s = Fs(t) ? "machine" : String(((d = (u = e == null ? void 0 : e.attack) == null ? void 0 : u.weapon) == null ? void 0 : d.scale) ?? "").trim() || "personal";
  return Fs(i) ? {
    mode: "machineAttackDamage",
    damage: (n == null ? void 0 : n.incoming) ?? 0,
    attackQuality: (n == null ? void 0 : n.attackQuality) ?? "",
    outcome: (n == null ? void 0 : n.attackQuality) === "highMargin" ? "hit" : (n == null ? void 0 : n.attackQuality) ?? "",
    netHits: (n == null ? void 0 : n.netHits) ?? 0,
    damageType: n == null ? void 0 : n.damageType,
    ap: (n == null ? void 0 : n.ap) ?? 0,
    hitLocation: a,
    onHitEffect: r,
    effects: ((m = (f = e == null ? void 0 : e.attack) == null ? void 0 : f.weapon) == null ? void 0 : m.effects) ?? {},
    sourceScale: s,
    chaosCriticalSelected: !1,
    reliabilitySpendSelections: [],
    previewRevision: 0,
    requirePreparedCriticalRecords: !0,
    source: `${(t == null ? void 0 : t.name) ?? "Attacker"}: ${((p = (g = e == null ? void 0 : e.attack) == null ? void 0 : g.weapon) == null ? void 0 : p.name) ?? "Attack"}`,
    sourceData: {
      attackerUuid: (t == null ? void 0 : t.uuid) ?? "",
      weaponName: ((b = (h = e == null ? void 0 : e.attack) == null ? void 0 : h.weapon) == null ? void 0 : b.name) ?? "Attack",
      weaponUuid: ((S = (y = e == null ? void 0 : e.attack) == null ? void 0 : y.weapon) == null ? void 0 : S.uuid) ?? ""
    },
    attackDamage: {
      effectiveWeaponDamage: (n == null ? void 0 : n.effectiveWeaponDamage) ?? 0,
      clustering: (n == null ? void 0 : n.clustering) ?? null,
      netDamageBonus: (n == null ? void 0 : n.netDamageBonus) ?? 0,
      incoming: (n == null ? void 0 : n.incoming) ?? 0
    },
    notes: ""
  } : {
    mode: "attackDamage",
    track: ((T = (A = e == null ? void 0 : e.attack) == null ? void 0 : A.weapon) == null ? void 0 : T.damageTrack) === M.monitors.fatigue ? M.monitors.fatigue : M.monitors.physical,
    damage: (n == null ? void 0 : n.scaledIncoming) ?? 0,
    sourceScale: s,
    netHits: 0,
    critNetHits: (n == null ? void 0 : n.netHits) ?? 0,
    critSeverity: zw((n == null ? void 0 : n.netHits) ?? 0),
    outcome: ["hit", "highMargin"].includes(String((n == null ? void 0 : n.attackQuality) ?? "")) ? "hit" : (n == null ? void 0 : n.attackQuality) ?? "",
    previewRevision: 0,
    requirePreparedCriticalRecords: !0,
    weaponUuid: ((k = (C = e == null ? void 0 : e.attack) == null ? void 0 : C.weapon) == null ? void 0 : k.uuid) ?? "",
    weaponId: ((v = (w = e == null ? void 0 : e.attack) == null ? void 0 : w.weapon) == null ? void 0 : v.id) ?? ((P = (N = e == null ? void 0 : e.attack) == null ? void 0 : N.payload) == null ? void 0 : P.weaponId) ?? "",
    weaponName: ((_ = (L = e == null ? void 0 : e.attack) == null ? void 0 : L.weapon) == null ? void 0 : _.name) ?? "",
    damageType: n == null ? void 0 : n.damageType,
    ap: (n == null ? void 0 : n.ap) ?? 0,
    effects: ((x = (O = e == null ? void 0 : e.attack) == null ? void 0 : O.weapon) == null ? void 0 : x.effects) ?? {},
    onHitEffect: r,
    source: `${(t == null ? void 0 : t.name) ?? "Attacker"}: ${((G = (H = e == null ? void 0 : e.attack) == null ? void 0 : H.weapon) == null ? void 0 : G.name) ?? "Attack"}`,
    attackDamage: {
      effectiveWeaponDamage: (n == null ? void 0 : n.effectiveWeaponDamage) ?? 0,
      clustering: (n == null ? void 0 : n.clustering) ?? null,
      netDamageBonus: (n == null ? void 0 : n.netDamageBonus) ?? 0,
      incoming: (n == null ? void 0 : n.incoming) ?? 0,
      scaledIncoming: (n == null ? void 0 : n.scaledIncoming) ?? (n == null ? void 0 : n.incoming) ?? 0
    },
    notes: n != null && n.usesExposureScaling && ((q = n == null ? void 0 : n.exposure) != null && q.initialTier) ? `Exposure ${ci(n.exposure.initialTier)}${n.exposure.evadeUsed ? ` -> ${ci(n.exposure.finalTier)}` : ""}` : ""
  };
}
function Bg(t) {
  var e;
  return typeof foundry < "u" && ((e = foundry == null ? void 0 : foundry.utils) != null && e.deepClone) ? foundry.utils.deepClone(t) : JSON.parse(JSON.stringify(t ?? null));
}
function WG({ target: t = {}, payload: e = {}, hitLocation: n = null, preview: i = {} } = {}) {
  var s;
  const a = Array.isArray((s = i == null ? void 0 : i.critical) == null ? void 0 : s.records) ? Bg(i.critical.records) : [], r = Math.max(0, Math.trunc(Number((e == null ? void 0 : e.previewRevision) ?? 0) || 0));
  return a.length && (e.preparedCriticalRecords = a.map((o) => ({
    ...o,
    previewRevision: r
  }))), {
    id: foundry.utils.randomID(),
    type: "machineAttackDamage",
    targetActorUuid: (t == null ? void 0 : t.actorUuid) ?? null,
    targetTokenUuid: (t == null ? void 0 : t.tokenUuid) ?? null,
    target: {
      name: (t == null ? void 0 : t.name) ?? "Target",
      actorUuid: (t == null ? void 0 : t.actorUuid) ?? null,
      tokenUuid: (t == null ? void 0 : t.tokenUuid) ?? null
    },
    hitLocation: (i == null ? void 0 : i.hitLocation) ?? n,
    damagePreview: (i == null ? void 0 : i.damagePreview) ?? null,
    critical: (i == null ? void 0 : i.critical) ?? null,
    preparedCriticalRecords: e.preparedCriticalRecords ?? [],
    reliabilityOptions: (i == null ? void 0 : i.reliabilityOptions) ?? null,
    previewRevision: r,
    applied: !1,
    payload: e,
    preview: i
  };
}
function bs(t, e = {}, n = {}, { queued: i = !1, applied: a = !1, skipped: r = !1, reason: s = "" } = {}) {
  return r ? {
    ok: !0,
    skipped: !0,
    queued: !1,
    applied: !1,
    reason: s || "Missed target."
  } : t != null && t.ok ? {
    ok: !0,
    queued: !!i,
    applied: !!a,
    preview: !!t.dryRun,
    actorName: t.actorName ?? (e == null ? void 0 : e.name) ?? "Target",
    sourceType: t.sourceType ?? null,
    mode: t.mode ?? "attackDamage",
    track: t.track ?? M.monitors.physical,
    requestedDelta: Number(t.requestedDelta ?? 0),
    appliedDelta: Number(t.appliedDelta ?? 0),
    usedArmor: !!t.usedArmor,
    damageType: t.damageType ?? (n == null ? void 0 : n.damageType) ?? "",
    effectiveAp: Number(t.effectiveAp ?? (n == null ? void 0 : n.ap) ?? 0),
    hitLocation: t.hitLocation ?? null,
    critical: t.critical ?? null,
    damagePreview: t.damagePreview ?? null,
    reliabilityOptions: t.reliabilityOptions ?? null,
    previewRevision: Number(t.previewRevision ?? 0) || 0,
    machine: t.machine ?? null,
    battleArmor: t.battleArmor ?? null,
    degradation: t.degradation ?? null,
    mitigation: t.mitigation ? {
      baseMitigation: Number(t.mitigation.baseMitigation ?? 0),
      typeMitigationMod: Number(t.mitigation.typeMitigationMod ?? 0),
      netResistance: Number(t.mitigation.netResistance ?? 0),
      armorBefore: Number(t.mitigation.armorBefore ?? 0),
      armorAfter: Number(t.mitigation.armorAfter ?? 0),
      reinforcedBefore: Number(t.mitigation.reinforcedBefore ?? 0),
      reinforcedAfter: Number(t.mitigation.reinforcedAfter ?? 0),
      reinforcedMax: Number(t.mitigation.reinforcedMax ?? 0)
    } : null,
    damageIncoming: Number(t.damageIncoming ?? 0),
    adjustedIncoming: Number(t.adjustedIncoming ?? 0),
    finalDamage: Number(t.finalDamage ?? 0),
    sourceScale: String(t.sourceScale ?? "").trim(),
    targetScale: String(t.targetScale ?? "").trim(),
    scaleConversion: t.scaleConversion ?? null,
    attackDamage: t.attackDamage ?? (n == null ? void 0 : n.attackDamage) ?? {
      effectiveWeaponDamage: (n == null ? void 0 : n.effectiveWeaponDamage) ?? 0,
      clustering: (n == null ? void 0 : n.clustering) ?? null,
      netDamageBonus: (n == null ? void 0 : n.netDamageBonus) ?? 0,
      incoming: (n == null ? void 0 : n.incoming) ?? t.damageIncoming ?? 0,
      scaledIncoming: (n == null ? void 0 : n.scaledIncoming) ?? t.adjustedIncoming ?? t.damageIncoming ?? 0
    },
    beforeLabel: String(t.beforeLabel ?? "").trim(),
    afterLabel: String(t.afterLabel ?? "").trim(),
    source: String(t.source ?? "").trim(),
    notes: String(t.notes ?? "").trim()
  } : {
    ok: !1,
    queued: !1,
    applied: !1,
    reason: (t == null ? void 0 : t.reason) ?? s ?? "Unable to preview attack damage."
  };
}
async function VG({ attacker: t, ctx: e, target: n, outcome: i, damage: a } = {}) {
  var d, f, m;
  if ((i == null ? void 0 : i.outcome) === "miss")
    return bs(null, n, a, { skipped: !0, reason: "Missed target." });
  if (((d = a == null ? void 0 : a.areaEffect) == null ? void 0 : d.kind) === kt.persistent)
    return {
      ok: !0,
      queued: !0,
      applied: !1,
      preview: !0,
      actorName: (n == null ? void 0 : n.name) ?? "Target",
      mode: "hazardEntry",
      reason: ""
    };
  let r = null, s = null;
  try {
    r = n != null && n.tokenUuid ? await fromUuid(n.tokenUuid) : null, s = n != null && n.actorUuid ? await fromUuid(n.actorUuid) : null;
  } catch (g) {
    return console.warn("MWD | Unable to resolve target for queued attack damage", n, g), bs(null, n, a, { reason: "Unable to resolve attack target." });
  }
  const l = Fs(s) ? lT({
    actor: s,
    rollTotal: oT(),
    armorBefore: cA(s, M.monitors.armor),
    structureBefore: cA(s, M.monitors.structure)
  }) : null, c = uA({ attacker: t, ctx: e, damage: a, targetActor: s, hitLocation: l }), u = await Mn.apply({
    actor: s,
    token: r,
    payload: c,
    options: {
      actorId: (s == null ? void 0 : s.id) ?? "",
      dryRun: !0,
      logToChat: !1
    }
  });
  if (u != null && u.ok) {
    const g = bs(u, n, a, { queued: !0, applied: !1 }), p = uA({ attacker: t, ctx: e, damage: a, targetActor: s, hitLocation: l }), h = p.mode === "machineAttackDamage" ? WG({ target: n, payload: p, hitLocation: l, preview: g }) : {
      id: foundry.utils.randomID(),
      type: "attackDamage",
      targetActorUuid: (n == null ? void 0 : n.actorUuid) ?? null,
      targetTokenUuid: (n == null ? void 0 : n.tokenUuid) ?? null,
      applied: !1,
      target: {
        name: (n == null ? void 0 : n.name) ?? "Target",
        actorUuid: (n == null ? void 0 : n.actorUuid) ?? null,
        tokenUuid: (n == null ? void 0 : n.tokenUuid) ?? null
      },
      critical: (g == null ? void 0 : g.critical) ?? null,
      preparedCriticalRecords: Array.isArray((f = g == null ? void 0 : g.critical) == null ? void 0 : f.records) ? Bg(g.critical.records).map((b) => ({
        ...b,
        previewRevision: Math.max(0, Math.trunc(Number(p.previewRevision ?? 0) || 0))
      })) : [],
      previewRevision: Math.max(0, Math.trunc(Number(p.previewRevision ?? 0) || 0)),
      payload: {
        ...p,
        criticalPreview: (g == null ? void 0 : g.critical) ?? null,
        preparedCriticalRecords: Array.isArray((m = g == null ? void 0 : g.critical) == null ? void 0 : m.records) ? Bg(g.critical.records).map((b) => ({
          ...b,
          previewRevision: Math.max(0, Math.trunc(Number(p.previewRevision ?? 0) || 0))
        })) : []
      },
      hitLocation: l,
      preview: g
    };
    return {
      ...g,
      queuedMutation: h
    };
  }
  return bs(u, n, a, { reason: "Unable to preview attack damage." });
}
async function JG({ attacker: t, ctx: e, outcomeModel: n, target: i, previewState: a = {} } = {}) {
  var y, S;
  const r = await jG({ attacker: t, ctx: e, target: i }), s = await PC(i), o = Number((n == null ? void 0 : n.margin) ?? 0), l = Number(r.value ?? 0), c = o;
  let u = l > 0 ? o >= 0 ? "hit" : "miss" : l === 0 ? o >= 1 ? "hit" : o === 0 ? "graze" : "miss" : l >= -3 ? o >= 2 ? "hit" : o === 1 ? "graze" : "miss" : o >= 3 ? "hit" : o >= 1 ? "graze" : "miss";
  String(((y = e == null ? void 0 : e.attack) == null ? void 0 : y.rangeBand) ?? "").trim().toLowerCase() === "outofrange" && u === "hit" && (u = "graze");
  const d = u === "hit" ? Math.max(0, c) : 0, f = (e == null ? void 0 : e.attack) ?? {}, m = qG(i), g = (a == null ? void 0 : a[m]) ?? {}, p = (i == null ? void 0 : i.exposure) ?? Va({ tier: "none" }), h = await GG({
    ...e,
    attacker: t,
    targetIsMachine: Fs(s),
    attack: {
      ...f,
      currentExposure: p,
      areaEffect: (f == null ? void 0 : f.areaEffect) ?? ((S = f == null ? void 0 : f.payload) == null ? void 0 : S.areaEffect) ?? null,
      evadeActive: !!(g != null && g.evadeActive),
      evadeLocked: !!(p != null && p.evadeLocked)
    }
  }, { outcome: u, netHits: d }), b = await VG({
    attacker: t,
    ctx: e,
    target: i,
    outcome: { outcome: u },
    damage: h
  });
  return {
    target: {
      name: (i == null ? void 0 : i.name) ?? "Target",
      actorUuid: (i == null ? void 0 : i.actorUuid) ?? null,
      tokenUuid: (i == null ? void 0 : i.tokenUuid) ?? null
    },
    previewKey: m,
    exposure: p,
    evadeActive: !!(g != null && g.evadeActive),
    evadeEdgePoolKey: String((g == null ? void 0 : g.edgePoolKey) ?? "").trim() || null,
    cq: r,
    margin: o,
    rawNetHits: c,
    netHits: d,
    outcome: u,
    damage: h,
    damageResult: b,
    queuedMutation: (b == null ? void 0 : b.queuedMutation) ?? null
  };
}
function YG(t = []) {
  const e = { hits: 0, grazes: 0, misses: 0 };
  for (const n of t)
    (n == null ? void 0 : n.outcome) === "hit" ? e.hits += 1 : (n == null ? void 0 : n.outcome) === "graze" ? e.grazes += 1 : e.misses += 1;
  return {
    ...e,
    overallOutcome: e.hits > 0 ? "hit" : e.grazes > 0 ? "graze" : "miss"
  };
}
async function RC({ attacker: t, ctx: e, outcomeModel: n, previewState: i = {}, existingAttackResult: a = null } = {}) {
  var c, u, d, f, m, g;
  const r = UG(e), s = [];
  for (const p of r)
    s.push(await JG({ attacker: t, ctx: e, outcomeModel: n, target: p, previewState: i }));
  const o = Gi(((c = e == null ? void 0 : e.attack) == null ? void 0 : c.areaEffect) ?? ((d = (u = e == null ? void 0 : e.attack) == null ? void 0 : u.payload) == null ? void 0 : d.areaEffect) ?? {});
  let l = String((a == null ? void 0 : a.persistentRegionUuid) ?? "").trim() || null;
  if (o.kind === kt.persistent && !l) {
    const p = await AP({
      attacker: t,
      attack: (e == null ? void 0 : e.attack) ?? {},
      targetResult: s[0] ?? null
    });
    l = (p == null ? void 0 : p.uuid) ?? null;
  }
  return {
    targetCount: r.length,
    results: s,
    summary: YG(s),
    areaEffect: o,
    templateGeometry: Ui(vt(
      (f = e == null ? void 0 : e.attack) == null ? void 0 : f.templateGeometry,
      {
        template: (m = e == null ? void 0 : e.attack) == null ? void 0 : m.template,
        placement: (g = e == null ? void 0 : e.attack) == null ? void 0 : g.templatePlacement
      }
    )),
    persistentRegionUuid: l
  };
}
function Od(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function zi(t, e = 0) {
  return Math.max(e, Od(t, e));
}
function QG(t = "") {
  const e = String(t ?? "").trim().toLowerCase();
  return e === "shutdown" || e === "explosion" ? e : "";
}
function XG({ check: t, result: e }) {
  return {
    scope: "Heat Danger",
    severity: `DN ${Number((t == null ? void 0 : t.dn) ?? 0)}`,
    notes: `Failed shutdown check by ${Number((e == null ? void 0 : e.failureMargin) ?? 0)}.`,
    tags: ["heat", "danger", "shutdown"]
  };
}
async function ZG({ actor: t, check: e, outcomeModel: n }) {
  var m;
  const i = zi((e == null ? void 0 : e.dn) ?? ((m = n == null ? void 0 : n.difficulty) == null ? void 0 : m.dn), 1), a = zi(n == null ? void 0 : n.successes, 0), r = Od(n == null ? void 0 : n.margin, a - i), s = zi(i - a, 0), o = Number.isFinite(Number(e == null ? void 0 : e.systemOpsRating)) ? zi(e.systemOpsRating, 0) : null, l = String((e == null ? void 0 : e.operatorName) ?? "").trim(), c = o !== null && s < o, u = {
    kind: "shutdown",
    passed: !1,
    hits: a,
    dn: i,
    margin: r,
    failureMargin: s,
    operatorName: l,
    systemOpsRating: o,
    overrideAvailable: c,
    statusId: "shutdown",
    statusApplied: !1,
    statusAlreadyActive: qi(t, "shutdown")
  };
  if (c)
    return {
      ...u,
      outcome: "overrideAvailable",
      effectText: `Shutdown check failed by ${s}, below ${l ? `${l}'s ` : "the pilot's "}System Operations ${o}.`,
      nextStep: "Pilot may override the shutdown. Apply Shutdown only if the override is declined."
    };
  const d = await mt({
    actor: t,
    statusId: "shutdown",
    active: !0,
    metadata: XG({ check: e, result: u })
  }), f = o === null ? "No linked pilot System Operations rating was found, so no override is available." : `Failure margin ${s} is not below System Operations ${o}.`;
  return {
    ...u,
    statusApplied: !!d,
    statusAlreadyActive: u.statusAlreadyActive || qi(t, "shutdown"),
    outcome: "shutdownApplied",
    effectText: u.statusAlreadyActive ? "The BattleMech was already Shutdown." : "The BattleMech shuts down.",
    nextStep: `${f} Shutdown status is active until cleared through the appropriate restart or recovery procedure.`
  };
}
function eq({ check: t, outcomeModel: e }) {
  var o;
  const n = zi((t == null ? void 0 : t.dn) ?? ((o = e == null ? void 0 : e.difficulty) == null ? void 0 : o.dn), 1), i = zi(e == null ? void 0 : e.successes, 0), a = Od(e == null ? void 0 : e.margin, i - n), r = zi(n - i, 0), s = !!(t != null && t.volatile);
  return e != null && e.passed ? {
    kind: "explosion",
    passed: !0,
    hits: i,
    dn: n,
    margin: a,
    failureMargin: 0,
    volatile: s,
    explosionTriggered: !1,
    outcome: "contained",
    effectText: "Volatile systems remain contained.",
    nextStep: s ? "No detonation is triggered by this check." : "No volatile components were detected, so no explosion is applied."
  } : s ? {
    kind: "explosion",
    passed: !1,
    hits: i,
    dn: n,
    margin: a,
    failureMargin: r,
    volatile: s,
    explosionTriggered: !0,
    outcome: "explosionTriggered",
    effectText: "Volatile components detonate.",
    nextStep: "Resolve the volatile component detonation immediately, then apply any resulting damage or destruction."
  } : {
    kind: "explosion",
    passed: !1,
    hits: i,
    dn: n,
    margin: a,
    failureMargin: r,
    volatile: s,
    explosionTriggered: !1,
    outcome: "noVolatileComponents",
    effectText: "Explosion check failed, but no volatile components are present.",
    nextStep: "No detonation is applied. Continue resolving heat effects."
  };
}
async function tq({ actor: t, ctx: e, outcomeModel: n } = {}) {
  var l;
  const i = (e == null ? void 0 : e.heatDangerCheck) ?? null, a = QG(i == null ? void 0 : i.kind);
  if (!t || !i || !a || !n) return null;
  if (a === "explosion")
    return eq({ check: i, outcomeModel: n });
  const r = zi((i == null ? void 0 : i.dn) ?? ((l = n == null ? void 0 : n.difficulty) == null ? void 0 : l.dn), 1), s = zi(n == null ? void 0 : n.successes, 0), o = Od(n == null ? void 0 : n.margin, s - r);
  return n.passed ? {
    kind: "shutdown",
    passed: !0,
    hits: s,
    dn: r,
    margin: o,
    failureMargin: 0,
    operatorName: String((i == null ? void 0 : i.operatorName) ?? "").trim(),
    systemOpsRating: Number.isFinite(Number(i == null ? void 0 : i.systemOpsRating)) ? zi(i.systemOpsRating, 0) : null,
    overrideAvailable: !1,
    statusId: "shutdown",
    statusApplied: !1,
    statusAlreadyActive: qi(t, "shutdown"),
    outcome: "staysOnline",
    effectText: "The BattleMech stays online.",
    nextStep: "No shutdown is applied."
  } : ZG({ actor: t, check: i, outcomeModel: n });
}
function Ld(t) {
  var n, i;
  const e = String(t ?? "").trim();
  return e ? ((i = (n = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : n.placeables) == null ? void 0 : i.find((a) => {
    var r;
    return (((r = a.document) == null ? void 0 : r.uuid) ?? a.uuid) === e;
  })) ?? null : null;
}
function $d(t) {
  var n, i, a, r;
  const e = String(t ?? "").trim();
  return e ? ((i = (n = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : n.get) == null ? void 0 : i.call(n, e)) ?? ((r = (a = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : a.placeables) == null ? void 0 : r.find((s) => {
    var o;
    return (s.id ?? ((o = s.document) == null ? void 0 : o.id)) === e;
  })) ?? null : null;
}
function nq(t) {
  return t ? Array.isArray(t) ? t : Array.isArray(t.contents) ? t.contents : typeof t[Symbol.iterator] == "function" ? Array.from(t) : [] : [];
}
function DC(t) {
  var i, a, r;
  const e = String(t ?? "").trim();
  if (!e) return null;
  const n = (a = (i = globalThis.game) == null ? void 0 : i.combat) == null ? void 0 : a.combatants;
  return ((r = n == null ? void 0 : n.get) == null ? void 0 : r.call(n, e)) ?? nq(n).find((s) => {
    const o = Array.isArray(s) ? s[1] : s;
    return (o == null ? void 0 : o.id) === e;
  }) ?? null;
}
function OC(t = {}) {
  return DC(t == null ? void 0 : t.attackerCombatantId) ?? Pn(Ld(t == null ? void 0 : t.attackerTokenUuid)) ?? Pn($d(t == null ? void 0 : t.attackerTokenId));
}
function LC(t = null) {
  var e;
  return String(((e = t == null ? void 0 : t.document) == null ? void 0 : e.uuid) ?? (t == null ? void 0 : t.uuid) ?? "").trim();
}
function iq(t = "blind") {
  const e = vn.indexOf(t);
  return e <= 0 ? "blind" : vn[e - 1] ?? "blind";
}
function $C(t = {}) {
  const e = String((t == null ? void 0 : t.targetCombatantId) ?? "").trim();
  if (e) return DC(e);
  const n = String((t == null ? void 0 : t.targetTokenId) ?? "").trim();
  if (n)
    return ao(n) ?? Pn($d(n));
  const i = String((t == null ? void 0 : t.targetTokenUuid) ?? "").trim();
  return i ? Pn(Ld(i)) : null;
}
function _C(t = null, e = {}) {
  var n, i, a;
  return Ld(e == null ? void 0 : e.sourceTokenUuid) ?? $d(e == null ? void 0 : e.sourceTokenId) ?? ((i = (n = t == null ? void 0 : t.getActiveTokens) == null ? void 0 : n.call(t, !0, !0)) == null ? void 0 : i[0]) ?? ((a = t == null ? void 0 : t.token) == null ? void 0 : a.object) ?? (t == null ? void 0 : t.token) ?? null;
}
async function BC({ attacker: t, ctx: e, outcomeModel: n } = {}) {
  var m;
  const i = e == null ? void 0 : e.acquire;
  if (!i) return { ok: !1, reason: "Missing acquire context." };
  const a = Number((n == null ? void 0 : n.successes) ?? (n == null ? void 0 : n.hits) ?? 0), r = Number(((m = e == null ? void 0 : e.difficulty) == null ? void 0 : m.dn) ?? 1);
  if (!(a >= r))
    return {
      ok: !1,
      reason: "Acquire roll failed.",
      previousState: i.currentState,
      newState: i.currentState,
      hits: a,
      dn: r
    };
  const o = i.ceiling ?? "lock", l = $O(i.currentState), c = vn.indexOf(o), d = vn.indexOf(l) <= c ? l : o;
  Hooks.callAll("mwd.beforeAcquireStateChange", {
    attacker: t,
    targetTokenUuid: i.targetTokenUuid,
    currentState: i.currentState,
    newState: d,
    ctx: e
  });
  const f = OC(i);
  if (!f) {
    const g = "Acquire succeeded, but no attacker combatant was found; detection state was not persisted.";
    return console.warn(`MWD | EW acquire: ${g}`), {
      ok: !1,
      reason: g,
      persistenceFailed: !0,
      previousState: i.currentState,
      newState: i.currentState,
      attemptedState: d,
      ceiling: o,
      hits: a,
      dn: r,
      hitCeiling: d !== l
    };
  }
  return await bu(f, i.targetTokenUuid, d), {
    ok: !0,
    previousState: i.currentState,
    newState: d,
    ceiling: o,
    hits: a,
    dn: r,
    hitCeiling: d !== l
  };
}
async function xC({ attacker: t, ctx: e, outcomeModel: n } = {}) {
  var f, m;
  const i = e == null ? void 0 : e.targeting;
  if (!i) return { ok: !1, reason: "Missing targeting context." };
  const a = Number((n == null ? void 0 : n.successes) ?? (n == null ? void 0 : n.hits) ?? 0), r = Number(((f = e == null ? void 0 : e.difficulty) == null ? void 0 : f.dn) ?? 2);
  if (!(a >= r))
    return {
      ok: !1,
      reason: "Targeting roll failed.",
      rawHits: a,
      dn: r
    };
  const o = a, l = Math.min(o, i.cap), c = Ld(i.attackerTokenUuid) ?? $d(i.attackerTokenId), u = jL({
    value: l,
    sourceToken: c,
    round: ((m = game.combat) == null ? void 0 : m.round) ?? null
  }), d = OC(i);
  if (!d) {
    const g = "Targeting roll succeeded, but no attacker combatant was found; targeting data was not persisted.";
    return console.warn(`MWD | EW targeting: ${g}`), {
      ok: !1,
      reason: g,
      persistenceFailed: !0,
      rawHits: o,
      dn: r
    };
  }
  return await FL(d, i.targetTokenUuid, u), {
    ok: !0,
    packetValue: l,
    rawHits: o,
    dn: r,
    packetId: u.id,
    hitCap: l < o
  };
}
async function zC({ attacker: t, payload: e = {}, ctx: n = {}, outcomeModel: i } = {}) {
  var m, g;
  if (String((e == null ? void 0 : e.machineActionKey) ?? (e == null ? void 0 : e.actionId) ?? ((m = n == null ? void 0 : n.data) == null ? void 0 : m.actionId) ?? "").trim() !== "breakLock") return null;
  const r = Number((i == null ? void 0 : i.successes) ?? (i == null ? void 0 : i.hits) ?? 0), s = Number(((g = n == null ? void 0 : n.difficulty) == null ? void 0 : g.dn) ?? 1), o = r >= s, l = _C(t, e), c = String((e == null ? void 0 : e.breakLockTargetTokenUuid) ?? "").trim() || LC(l), u = $C(e);
  if (!o)
    return {
      ok: !1,
      reason: "Break Lock roll failed.",
      hits: r,
      dn: s,
      targetTokenUuid: c
    };
  if (!c)
    return {
      ok: !1,
      reason: "Break Lock succeeded, but no acting machine token was found.",
      persistenceFailed: !0,
      hits: r,
      dn: s
    };
  if (!u)
    return {
      ok: !1,
      reason: "Break Lock succeeded, but no observing combatant was found; detection state was not persisted.",
      persistenceFailed: !0,
      hits: r,
      dn: s,
      targetTokenUuid: c
    };
  const d = ro(u, c), f = iq(d);
  return f === d ? {
    ok: !1,
    reason: "Observer has no detection state to reduce.",
    previousState: d,
    newState: f,
    hits: r,
    dn: s,
    targetTokenUuid: c,
    observerCombatantId: u.id ?? ""
  } : (Hooks.callAll("mwd.beforeBreakLockStateChange", {
    attacker: t,
    observerCombatant: u,
    targetTokenUuid: c,
    previousState: d,
    newState: f,
    ctx: n
  }), await bu(u, c, f), {
    ok: !0,
    previousState: d,
    newState: f,
    hits: r,
    dn: s,
    targetTokenUuid: c,
    observerCombatantId: u.id ?? ""
  });
}
async function aq({ defender: t, payload: e = {} } = {}) {
  const n = String((e == null ? void 0 : e.machineActionKey) ?? (e == null ? void 0 : e.actionId) ?? "").trim();
  if (n && n !== "defensiveJink") return null;
  const i = _C(t, e), a = String((e == null ? void 0 : e.jinkTargetTokenUuid) ?? (e == null ? void 0 : e.breakLockTargetTokenUuid) ?? "").trim() || LC(i), r = $C(e);
  return a ? r ? UL(r, a, 1, {
    packetId: String((e == null ? void 0 : e.targetingPacketId) ?? (e == null ? void 0 : e.packetId) ?? "").trim(),
    source: "defensiveJink"
  }) : {
    ok: !1,
    reason: "Defensive Jink could not find the observing combatant.",
    persistenceFailed: !0,
    targetTokenUuid: a
  } : {
    ok: !1,
    reason: "Defensive Jink could not find the defending machine token.",
    persistenceFailed: !0
  };
}
async function FC({ defender: t, payload: e = {}, ctx: n = {}, outcomeModel: i } = {}) {
  var c, u;
  if (String((e == null ? void 0 : e.machineActionKey) ?? (e == null ? void 0 : e.actionId) ?? ((c = n == null ? void 0 : n.data) == null ? void 0 : c.actionId) ?? "").trim() !== "defensiveJink") return null;
  const r = Number((i == null ? void 0 : i.successes) ?? (i == null ? void 0 : i.hits) ?? 0), s = Number(((u = n == null ? void 0 : n.difficulty) == null ? void 0 : u.dn) ?? 1);
  return r >= s ? {
    ...await aq({ defender: t, payload: e }),
    hits: r,
    dn: s
  } : {
    ok: !1,
    reason: "Defensive Jink roll failed.",
    hits: r,
    dn: s
  };
}
const dA = { execute: Eq, recomputeResolvedOutcomeAndAttack: UC, applyPostRerollFailures: gq }, rq = {
  physical: ["grit", "chaos"],
  mental: ["insight", "rumor"],
  social: ["legend", "credibility"]
}, sq = /* @__PURE__ */ new Set(["criticalSuccess", "criticalFailure", "critFail"]), oq = /* @__PURE__ */ new Set([
  "acquire",
  "acquireTarget",
  "generateFireSolution",
  "heatDangerCheck",
  "machineRemedy",
  "targeting"
]);
function lq(t = []) {
  return Array.from(new Set(t.map((e) => String(e ?? "").trim()).filter(Boolean)));
}
function cq(t) {
  var e;
  return (t == null ? void 0 : t.reason) === "multiple" && Array.isArray((e = t == null ? void 0 : t.details) == null ? void 0 : e.awards) ? t.details.awards.filter((n) => Number((n == null ? void 0 : n.amount) ?? 0) > 0) : Number((t == null ? void 0 : t.amount) ?? 0) > 0 ? [t] : [];
}
function uq(t = {}) {
  var i, a, r;
  const e = (t == null ? void 0 : t.ctxSnapshot) ?? {}, n = Number(((i = t == null ? void 0 : t.dn) == null ? void 0 : i.total) ?? ((a = e == null ? void 0 : e.dn) == null ? void 0 : a.total) ?? ((r = e == null ? void 0 : e.difficulty) == null ? void 0 : r.dn) ?? 1);
  return {
    intent: (t == null ? void 0 : t.intent) ?? "unknown",
    rollType: (e == null ? void 0 : e.rollType) ?? "simple",
    difficulty: {
      ...e != null && e.difficulty && typeof e.difficulty == "object" ? e.difficulty : {},
      dn: Number.isFinite(n) ? n : 1
    },
    dn: (t == null ? void 0 : t.dn) ?? (e == null ? void 0 : e.dn) ?? null,
    opposed: (e == null ? void 0 : e.opposed) ?? null,
    net: (e == null ? void 0 : e.net) ?? null,
    edge: (e == null ? void 0 : e.edge) ?? null,
    domains: Array.isArray(t == null ? void 0 : t.domains) ? t.domains : [],
    attack: (t == null ? void 0 : t.attack) ?? null,
    machineRemedy: (t == null ? void 0 : t.machineRemedy) ?? null,
    acquire: (t == null ? void 0 : t.acquire) ?? null,
    targeting: (t == null ? void 0 : t.targeting) ?? null
  };
}
async function UC(t = {}, e = null) {
  var s, o, l, c, u, d, f, m, g, p, h, b;
  const n = uq(t), i = Number(((s = t == null ? void 0 : t.outcome) == null ? void 0 : s.hits) ?? 0) || 0, a = ((o = t == null ? void 0 : t.outcomeModel) == null ? void 0 : o.edgeEarned) ?? null;
  t.outcomeModel = IC(n, { successes: i, raw: (l = t == null ? void 0 : t.roll) == null ? void 0 : l.json }, null);
  const r = Number(((u = (c = t == null ? void 0 : t.edge) == null ? void 0 : c.pre) == null ? void 0 : u.spent) ?? 0) > 0 || Number(((f = (d = t == null ? void 0 : t.edge) == null ? void 0 : d.post) == null ? void 0 : f.spent) ?? 0) > 0;
  return t.outcomeModel.edgeEarned = r ? null : a, n.intent === "attack" && e && n.attack ? t.attackResult = await RC({
    attacker: e,
    ctx: n,
    outcomeModel: t.outcomeModel,
    previewState: t.areaEffectPreviewState ?? {},
    existingAttackResult: t.attackResult ?? null
  }) : n.intent === "machineRemedy" ? t.machineRemedyResult = {
    ...await ev(t.originPayload ?? {}, {
      gmOverride: !!((m = t == null ? void 0 : t.originPayload) != null && m.gmOverride),
      passed: !!((g = t.outcomeModel) != null && g.passed)
    }),
    spend: ((p = t.machineRemedyResult) == null ? void 0 : p.spend) ?? null,
    context: ((h = t.machineRemedyResult) == null ? void 0 : h.context) ?? null
  } : n.intent === "acquire" && e && n.acquire ? t.ewAcquireResult = await BC({
    attacker: e,
    ctx: n,
    outcomeModel: t.outcomeModel
  }) : n.intent === "targeting" && e && n.targeting ? t.ewTargetingResult = await xC({
    attacker: e,
    ctx: n,
    outcomeModel: t.outcomeModel
  }) : (n.intent === "breakLock" || n.intent === "skill" && ((b = t.originPayload) == null ? void 0 : b.machineActionKey) === "breakLock") && e ? t.ewBreakLockResult = await zC({
    attacker: e,
    payload: t.originPayload,
    ctx: n,
    outcomeModel: t.outcomeModel
  }) : n.intent === "defensiveJink" && e && (t.ewJinkResult = await FC({
    defender: e,
    payload: t.originPayload,
    ctx: n,
    outcomeModel: t.outcomeModel
  })), t;
}
function dq(t = {}) {
  var n;
  return (Array.isArray((n = t == null ? void 0 : t.attackResult) == null ? void 0 : n.results) ? t.attackResult.results : []).some((i) => {
    var a;
    return !!((a = i == null ? void 0 : i.queuedMutation) != null && a.applied);
  });
}
function KC(t = null) {
  if (Array.isArray(t == null ? void 0 : t.appliedAwards))
    return t.appliedAwards.map((i) => ({
      pool: String((i == null ? void 0 : i.pool) ?? "").trim(),
      amount: Math.max(0, Number((i == null ? void 0 : i.amount) ?? 0))
    })).filter((i) => i.pool && i.amount > 0);
  const e = String((t == null ? void 0 : t.pool) ?? "").trim(), n = Math.max(0, Number((t == null ? void 0 : t.amount) ?? 0));
  return t != null && t.applied && e && n > 0 ? [{ pool: e, amount: n }] : [];
}
function mq(t, e = {}, n = "") {
  var r, s, o;
  const i = Number(((r = t == null ? void 0 : t.getEdgePoolValue) == null ? void 0 : r.call(t, n)) ?? ((s = t == null ? void 0 : t.getRemainingEdge) == null ? void 0 : s.call(t, n)) ?? 0), a = KC((o = e == null ? void 0 : e.outcomeModel) == null ? void 0 : o.edgeEarned).filter((l) => l.pool === n).reduce((l, c) => l + Number(c.amount ?? 0), 0);
  return Math.max(0, i - a);
}
async function fq(t, e = {}) {
  var i, a;
  const n = KC((i = e == null ? void 0 : e.outcomeModel) == null ? void 0 : i.edgeEarned);
  if (n.length) {
    for (const r of n)
      await ((a = t.spendEdge) == null ? void 0 : a.call(t, r.pool, r.amount, {
        skipTraitHooks: !0,
        source: "postEdgeRevokesEarnedEdge"
      }));
    e != null && e.outcomeModel && (e.outcomeModel.edgeEarned = null);
  }
}
async function gq({ message: t = null, poolKey: e = "" } = {}) {
  var g, p, h, b, y, S, A, T, C, k, w;
  const n = String(e ?? "").trim();
  if (!t) throw new Error("Post-reroll requires a chat message.");
  if (!n) throw new Error("Post-reroll requires poolKey.");
  const i = foundry.utils.deepClone((p = (g = t == null ? void 0 : t.flags) == null ? void 0 : g.mwd) == null ? void 0 : p.resolved);
  if (!i) return { ok: !1, reason: "missing-resolved", userMessage: "Roll data is no longer available." };
  if (dq(i))
    return {
      ok: !1,
      reason: "attack-damage-applied",
      userMessage: "Post-roll Edge is disabled after attack damage has been applied."
    };
  if ((h = i == null ? void 0 : i.firstAidResult) != null && h.applied)
    return {
      ok: !1,
      reason: "first-aid-applied",
      userMessage: "Post-roll Edge is disabled after First Aid has been applied."
    };
  if (Number(((y = (b = i == null ? void 0 : i.edge) == null ? void 0 : b.post) == null ? void 0 : y.spent) ?? 0) === 1)
    return { ok: !1, reason: "already-spent", userMessage: "Post-roll Edge has already been spent." };
  if (!(Array.isArray((A = (S = i == null ? void 0 : i.edge) == null ? void 0 : S.allowed) == null ? void 0 : A.postPools) ? i.edge.allowed.postPools : []).includes(n))
    return {
      ok: !1,
      reason: "pool-not-allowed",
      userMessage: `Post-spend pool not allowed: ${n}`
    };
  const r = Array.isArray((T = i == null ? void 0 : i.roll) == null ? void 0 : T.failureDiceRefs) ? i.roll.failureDiceRefs : [];
  if (r.length <= 0)
    return { ok: !1, reason: "no-failures", userMessage: "No failures to reroll." };
  const s = await fromUuid(i.rollActorUuid ?? i.actorUuid);
  if (!s) return { ok: !1, reason: "actor-not-found", userMessage: "Actor not found for this roll." };
  const o = await fromUuid(i.actorUuid) ?? s;
  if (mq(s, i, n) <= 0)
    return {
      ok: !1,
      reason: "edge-unavailable",
      userMessage: `No ${n} Edge available for post-spend.`
    };
  await fq(s, i), await ((C = s.spendEdge) == null ? void 0 : C.call(s, n, 1));
  const l = Number(((k = i == null ? void 0 : i.roll) == null ? void 0 : k.target) ?? 5), u = (w = (await new Roll(`${r.length}d6cs>=${l}`).evaluate()).dice) == null ? void 0 : w[0], d = Array.isArray(u == null ? void 0 : u.results) ? u.results : [], f = d.filter((v) => v.success).length;
  i.outcome = i.outcome ?? {}, i.outcome.hits = Number(i.outcome.hits ?? 0) + f, i.edge = i.edge ?? {}, i.edge.post = { poolKey: n, spent: 1 }, i.edge.availableActions = {
    ...i.edge.availableActions ?? {},
    canSpendPost: !1,
    canPostRerollFailures: !1
  }, i.roll = i.roll ?? {}, i.roll.diceGroups = Array.isArray(i.roll.diceGroups) ? i.roll.diceGroups : [], i.roll.diceGroups.push({
    id: "post",
    label: "Post Reroll",
    faces: 6,
    termIndex: null,
    dice: d.map((v, N) => {
      const P = Number(v.result), L = !!v.success;
      return {
        ref: `post:${N}`,
        face: P,
        isSuccess: L,
        isFailure: !L,
        tooltip: L ? `Post die ${N + 1}: ${P} (Success vs TN ${l})` : `Post die ${N + 1}: ${P} (Failure vs TN ${l})`
      };
    })
  }), await UC(i, o);
  const m = await ar({ resolved: i });
  return {
    ok: !0,
    resolved: i,
    content: m,
    updateData: {
      content: m,
      "flags.mwd.resolved": i,
      "flags.mwd.payload.edge.post": { poolKey: n, spent: 1 }
    }
  };
}
function HC(t) {
  return sq.has(String((t == null ? void 0 : t.reason) ?? ""));
}
function pq(t, e) {
  var r;
  const n = rq[e] ?? [];
  let i = null, a = -1;
  for (const s of n) {
    const o = (r = t.getEdgePool) == null ? void 0 : r.call(t, s), l = Number((o == null ? void 0 : o.effectiveMax) ?? (o == null ? void 0 : o.rating) ?? 0), c = Number((o == null ? void 0 : o.effectiveValue) ?? (o == null ? void 0 : o.value) ?? 0), u = Math.max(0, l - c);
    u > a && (a = u, i = s);
  }
  return i ?? n[0] ?? null;
}
function hq(t, e) {
  return (e == null ? void 0 : e.domain) ?? GC(t == null ? void 0 : t.domains);
}
function bq(t, e, n) {
  if (HC(e)) {
    const i = pq(t, n);
    return i ? [i] : [];
  }
  return e != null && e.pool ? [String(e.pool)] : [];
}
async function yq({ actor: t, ctx: e, edgeInfo: n, earned: i } = {}) {
  var c;
  if (!(t != null && t.gainEdge) || !(i != null && i.amount)) return null;
  const a = hq(e, n), r = cq(i), s = [], o = [];
  for (const u of r) {
    const d = Math.max(0, Number((u == null ? void 0 : u.amount) ?? 0));
    if (!d) continue;
    const f = bq(t, u, a);
    for (const m of f) {
      const g = ((c = t.getEdgePool) == null ? void 0 : c.call(t, m)) ?? {}, p = Number(g.effectiveValue ?? g.value ?? 0), h = Number(g.effectiveMax ?? g.rating ?? 0), b = Number.isFinite(h) ? Math.max(0, Math.min(d, h - p)) : d;
      b <= 0 || (await t.gainEdge(m, d, {
        source: HC(u) ? "criticalOutcome" : "rollOutcome"
      }), o.push(m), s.push({
        pool: m,
        amount: b,
        reason: String((u == null ? void 0 : u.reason) ?? i.reason ?? "rollOutcome")
      }));
    }
  }
  if (!s.length) return null;
  const l = lq(o);
  return {
    ...i,
    pool: l.length === 1 ? l[0] : null,
    pools: l,
    targetLabel: l.join(", "),
    applied: !0,
    appliedAwards: s
  };
}
function Sq(t) {
  const n = (Array.isArray(t == null ? void 0 : t.manualModifiers) ? t.manualModifiers : []).filter((a) => (a == null ? void 0 : a.enabled) !== !1).map((a) => ({
    id: a.id ?? foundry.utils.randomID(),
    label: (a.label ?? "Manual").trim() || "Manual",
    value: Number(a.value ?? 0),
    source: String(a.source ?? "Manual").trim() || "Manual"
  })).filter((a) => Number.isFinite(a.value) && a.value !== 0), i = n.reduce((a, r) => a + r.value, 0);
  return { mods: n, total: i };
}
function Aq(...t) {
  const e = /* @__PURE__ */ new Set();
  return t.filter((n) => {
    if (!n) return !1;
    const i = n.uuid ?? n.id ?? n;
    return e.has(i) ? !1 : (e.add(i), !0);
  });
}
function mA(t = {}) {
  const e = t.toggles ?? {}, n = String((t == null ? void 0 : t.payloadId) ?? (t == null ? void 0 : t.ammoTypeId) ?? "").trim();
  return {
    ...t,
    ...n ? { payloadId: n } : {},
    toggles: {
      useEdge: !!e.useEdge,
      takeRisks: !!e.takeRisks,
      opponentRoll: !!e.opponentRoll
    },
    manualModifiers: jC(t.manualModifiers)
  };
}
function kq(t = {}) {
  var i;
  const e = String((t == null ? void 0 : t.intent) ?? "").trim();
  if (oq.has(e) || e === "initiative" || e === "skill" && (t != null && t.machineActionKey)) return !0;
  if (e !== "attack") return !1;
  const n = String((t == null ? void 0 : t.sourceType) ?? "").trim();
  return n === "weaponGroup" || n === "mechWeapon" || n === "vehicleWeapon" || !!(t != null && t.weaponGroupId) || !!((i = t == null ? void 0 : t.machineWeaponGroup) != null && i.id);
}
function Tq(t, e = {}) {
  var i;
  if (!t || Nt(t) || !kq(e)) return t;
  const n = ((i = D.resolveActivationUnit) == null ? void 0 : i.call(D, { actor: t })) ?? null;
  return n != null && n.platformActor && (n != null && n.operatorActor) ? n.platformActor : t;
}
async function wq({ actor: t, payload: e } = {}) {
  var c, u, d, f, m, g, p, h, b, y, S, A, T, C;
  if ((e == null ? void 0 : e.intent) !== "attack") return e;
  const n = foundry.utils.deepClone(e ?? {}), i = ((c = t.getPersonalCombatLoadout) == null ? void 0 : c.call(t, { refresh: !0 })) ?? null, a = Nt(t), r = String((n == null ? void 0 : n.sourceType) ?? "").trim(), s = String((n == null ? void 0 : n.sourceId) ?? "").trim(), o = String(((u = n == null ? void 0 : n.syntheticWeapon) == null ? void 0 : u.id) ?? "").trim(), l = (k) => {
    var v, N, P, L, _;
    const w = ((N = (v = t.items) == null ? void 0 : v.get) == null ? void 0 : N.call(v, k)) ?? null;
    return !w || !(((P = w.isPersonalWeapon) == null ? void 0 : P.call(w)) ?? w.type === M.itemType.personalWeapon) || !((L = w.system) != null && L.equipped) ? null : ((_ = w.getCombatProfile) == null ? void 0 : _.call(w, { payloadId: n == null ? void 0 : n.payloadId })) ?? null;
  };
  if (!r || !s) {
    const k = String((n == null ? void 0 : n.weaponGroupId) ?? ((d = n == null ? void 0 : n.machineWeaponGroup) == null ? void 0 : d.id) ?? "").trim(), w = String((n == null ? void 0 : n.weaponId) ?? "").trim();
    k ? (n.sourceType = "weaponGroup", n.sourceId = k) : w && (n.sourceType = a ? "mechWeapon" : "personalWeapon", n.sourceId = w);
  }
  if (String((n == null ? void 0 : n.sourceType) ?? "").trim() === "weaponGroup") {
    if (n.weaponGroupId = String((n == null ? void 0 : n.sourceId) ?? (n == null ? void 0 : n.weaponGroupId) ?? "").trim(), n.sourceId = n.weaponGroupId, !n.weaponGroupId)
      throw new Error("Attack requires a valid weapon group source.");
    return n;
  }
  if (String((n == null ? void 0 : n.sourceType) ?? "").trim() === "mechWeapon") {
    if (n.weaponId = String((n == null ? void 0 : n.sourceId) ?? (n == null ? void 0 : n.weaponId) ?? "").trim(), n.sourceId = n.weaponId, !n.weaponId && a && o && (n.weaponId = o, n.sourceId = o), !n.weaponId)
      throw new Error("Attack requires a valid machine weapon source.");
    return n;
  }
  if (String((n == null ? void 0 : n.sourceType) ?? "").trim() === "personalWeapon" && s && (n.weaponId = s), n.weaponId) {
    const k = l(n.weaponId);
    if (!k)
      throw new Error("Attack requires an owned equipped personal weapon.");
    return n.sourceType = "personalWeapon", n.sourceId = n.weaponId, n.payloadId = n.payloadId ?? ((f = k == null ? void 0 : k.payloadState) == null ? void 0 : f.activePayloadId) ?? "", n;
  }
  if (n.mode === "auto") {
    if (i != null && i.weaponChoiceRequired) {
      const k = await _g.selectItem(
        "Choose Weapon",
        i.equippedWeapons ?? []
      );
      return k ? (n.weaponId = k.id, n.sourceType = "personalWeapon", n.sourceId = k.id, n.payloadId = n.payloadId ?? ((m = k == null ? void 0 : k.payloadState) == null ? void 0 : m.activePayloadId) ?? "", delete n.mode, n) : null;
    }
    if ((g = i == null ? void 0 : i.defaultWeapon) != null && g.isSynthetic || ((p = i == null ? void 0 : i.defaultWeapon) == null ? void 0 : p.id) === "unarmed")
      return n.syntheticWeapon = foundry.utils.deepClone(i.defaultWeapon ?? li.buildDefaultUnarmedProfile(t)), n.weaponId = n.syntheticWeapon.id, n.sourceType = "personalWeapon", n.sourceId = n.syntheticWeapon.id, n.payloadId = n.payloadId ?? ((b = (h = n.syntheticWeapon) == null ? void 0 : h.payloadState) == null ? void 0 : b.activePayloadId) ?? "", delete n.mode, n;
    if ((y = i == null ? void 0 : i.defaultWeapon) != null && y.id)
      return n.weaponId = i.defaultWeapon.id, n.sourceType = "personalWeapon", n.sourceId = i.defaultWeapon.id, n.payloadId = n.payloadId ?? ((A = (S = i.defaultWeapon) == null ? void 0 : S.payloadState) == null ? void 0 : A.activePayloadId) ?? "", delete n.mode, n;
  }
  if (n.fallback === "unarmed")
    return n.syntheticWeapon = foundry.utils.deepClone(li.buildDefaultUnarmedProfile(t)), n.weaponId = n.syntheticWeapon.id, n.sourceType = "personalWeapon", n.sourceId = n.syntheticWeapon.id, n.payloadId = n.payloadId ?? ((C = (T = n.syntheticWeapon) == null ? void 0 : T.payloadState) == null ? void 0 : C.activePayloadId) ?? "", delete n.mode, n;
  throw new Error("Attack could not resolve a usable weapon.");
}
function jC(t) {
  return Array.isArray(t) ? t.map((e) => ({
    id: (e == null ? void 0 : e.id) ?? foundry.utils.randomID(),
    label: typeof (e == null ? void 0 : e.label) == "string" ? e.label : "Manual",
    value: Number((e == null ? void 0 : e.value) ?? 0),
    enabled: (e == null ? void 0 : e.enabled) !== !1,
    source: String((e == null ? void 0 : e.source) ?? "Manual").trim() || "Manual",
    optional: (e == null ? void 0 : e.optional) === !0,
    traitItemId: String((e == null ? void 0 : e.traitItemId) ?? "").trim(),
    traitEffectId: String((e == null ? void 0 : e.traitEffectId) ?? "").trim()
  })) : [];
}
function Mq(t = {}, e = []) {
  const n = jC(t.manualModifiers);
  if (!Array.isArray(e) || !e.length)
    return { ...t, manualModifiers: n };
  const i = new Set(n.map((r) => String(r.id ?? "").trim()).filter(Boolean)), a = e.map((r) => ({
    id: String((r == null ? void 0 : r.id) ?? foundry.utils.randomID()).trim() || foundry.utils.randomID(),
    label: String((r == null ? void 0 : r.label) ?? "Trait").trim() || "Trait",
    value: Number((r == null ? void 0 : r.value) ?? 0),
    enabled: (r == null ? void 0 : r.enabled) === !0,
    source: String((r == null ? void 0 : r.source) ?? "Trait").trim() || "Trait",
    optional: !0,
    traitItemId: String((r == null ? void 0 : r.traitItemId) ?? "").trim(),
    traitEffectId: String((r == null ? void 0 : r.traitEffectId) ?? "").trim()
  })).filter((r) => Number.isFinite(r.value) && r.value !== 0 && !i.has(r.id));
  return {
    ...t,
    manualModifiers: [...n, ...a]
  };
}
async function vq(t = []) {
  var n, i, a, r, s, o, l, c, u;
  const e = Array.from(new Set(
    (Array.isArray(t) ? t : []).map((d) => String(d ?? "").trim()).filter(Boolean)
  ));
  if (typeof ((n = game.user) == null ? void 0 : n.updateTokenTargets) == "function") {
    await game.user.updateTokenTargets(e);
    return;
  }
  for (const d of Array.from(((i = game.user) == null ? void 0 : i.targets) ?? []))
    (a = d == null ? void 0 : d.setTarget) == null || a.call(d, !1, { releaseOthers: !1, user: game.user });
  for (const d of e) {
    const f = ((s = (r = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : r.get) == null ? void 0 : s.call(r, d)) ?? ((c = (l = (o = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : o.placeables) == null ? void 0 : l.find) == null ? void 0 : c.call(l, (m) => (m == null ? void 0 : m.id) === d)) ?? null;
    (u = f == null ? void 0 : f.setTarget) == null || u.call(f, !0, { releaseOthers: !1, user: game.user });
  }
}
function Yh(t, e = {}) {
  var i, a, r, s, o, l, c, u, d, f, m;
  const n = String((e == null ? void 0 : e.sourceTokenId) ?? "").trim();
  if (n) {
    const g = ((a = (i = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : i.get) == null ? void 0 : a.call(i, n)) ?? ((o = (s = (r = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : r.placeables) == null ? void 0 : s.find) == null ? void 0 : o.call(s, (p) => (p == null ? void 0 : p.id) === n)) ?? null;
    if (g) return g.document ?? g;
  }
  return ((l = t == null ? void 0 : t.token) == null ? void 0 : l.document) ?? (t == null ? void 0 : t.token) ?? ((d = (u = (c = t == null ? void 0 : t.getActiveTokens) == null ? void 0 : c.call(t, !0, !0)) == null ? void 0 : u[0]) == null ? void 0 : d.document) ?? ((m = (f = t == null ? void 0 : t.getActiveTokens) == null ? void 0 : f.call(t, !0, !0)) == null ? void 0 : m[0]) ?? null;
}
async function Cq(t, e = {}, { rollActor: n = null } = {}) {
  var u, d, f, m;
  if (!Nt(t) || e != null && e.machineActionPrecommitted) return;
  const i = Yh(t, e), a = n ?? t, r = ((u = D.getSnapshot) == null ? void 0 : u.call(D, a, { token: i })) ?? null;
  if (!(r != null && r.hasCombatant)) return;
  const s = od(t), o = (t == null ? void 0 : t.type) === M.actorTypes.battlemech && String((e == null ? void 0 : e.sourceType) ?? "").trim() === "weaponGroup" && String((e == null ? void 0 : e.sourceId) ?? (e == null ? void 0 : e.weaponGroupId) ?? "").trim(), l = o ? 1 + Number((s == null ? void 0 : s.extraCost) ?? 0) : Number((s == null ? void 0 : s.totalCost) ?? 0), c = await D.spendResource(a, {
    token: i,
    resource: "sa",
    cost: l,
    actionId: "attack",
    actionLabel: "Attack",
    actionCostLabel: `${l} SA`,
    actionCategory: "complex"
  });
  if (!(c != null && c.ok)) {
    (d = ui.notifications) == null || d.warn((c == null ? void 0 : c.reason) ?? "Unable to record attack action.");
    return;
  }
  if (o) {
    const g = await ((f = D.markWeaponGroupUsed) == null ? void 0 : f.call(D, t, {
      token: i,
      groupId: String((e == null ? void 0 : e.sourceId) ?? (e == null ? void 0 : e.weaponGroupId) ?? "").trim()
    }));
    g != null && g.ok || (m = ui.notifications) == null || m.warn((g == null ? void 0 : g.reason) ?? "Unable to record BattleMech weapon-group usage.");
  }
}
function Iq(t = {}, e = null) {
  return e ? {
    ...t,
    cost: Number.isFinite(Number(e.cost)) ? Math.max(0, Number(e.cost)) : t.cost,
    resource: String(e.resource ?? t.resource ?? "sa").trim() || "sa",
    category: String(e.category ?? t.category ?? "simple").trim() || "simple"
  } : t;
}
async function Ao(t, e = "", n = {}, { rollActor: i = null, resolved: a = null } = {}) {
  var f, m, g, p, h, b, y, S, A;
  if (!Nt(t)) return;
  const r = _l(e), s = jT(t, r.key, {
    payload: n,
    resolved: a,
    context: {
      targetState: ((f = a == null ? void 0 : a.acquire) == null ? void 0 : f.currentState) ?? "",
      detectionState: ((m = a == null ? void 0 : a.targeting) == null ? void 0 : m.detectionState) ?? ((g = a == null ? void 0 : a.acquire) == null ? void 0 : g.currentState) ?? ""
    }
  }), o = Iq(r, s), l = Yh(t, n), c = i ?? t, u = ((p = D.getSnapshot) == null ? void 0 : p.call(D, c, { token: l })) ?? null;
  if (o != null && o.cost && (o != null && o.resource) && (u != null && u.hasCombatant)) {
    const T = await D.spendResource(c, {
      token: l,
      resource: o.resource,
      cost: o.cost,
      actionId: o.key,
      actionLabel: o.label,
      actionCostLabel: `${o.cost} ${String(o.resource).toUpperCase()}`,
      actionCategory: o.category
    });
    if (!(T != null && T.ok)) {
      (h = ui.notifications) == null || h.warn((T == null ? void 0 : T.reason) ?? `Unable to record ${o.label}.`);
      return;
    }
  }
  const d = YD(t, o.key, {
    payload: n,
    resolved: a,
    context: {
      targetState: ((b = a == null ? void 0 : a.acquire) == null ? void 0 : b.currentState) ?? "",
      detectionState: ((y = a == null ? void 0 : a.targeting) == null ? void 0 : y.detectionState) ?? ((S = a == null ? void 0 : a.acquire) == null ? void 0 : S.currentState) ?? ""
    }
  });
  if (d.heat && (t == null ? void 0 : t.type) === M.actorTypes.battlemech && await sd(t, d.heat, { reason: `${o.label} module cost` }), (A = d.stress) != null && A.length) {
    const T = {};
    for (const C of d.stress) {
      const k = String(C.location ?? "").trim();
      if (!k) continue;
      const w = `system.mwd.locations.${k}.stress`, v = Number(foundry.utils.getProperty(t, w) ?? 0) || 0;
      T[w] = Math.max(0, v + (Number(C.value ?? 0) || 0));
    }
    Object.keys(T).length && await t.update(T);
  }
}
async function Eq({ actor: t, payload: e, event: n, uiState: i = null } = {}) {
  var se, te, pe, K, be, Ie, Oe, Be, R, V, B, me, oe, Ee, Ye, bt, yt, nn, Dn, On, Ln, $n, _n, cn, Bn, xn, U, Y, Ae, fe, we, Pe, Le, lt, qe, un, et, an, We, ke, Qe, zn, ct, It, Mi, Fn, Et, zd, sr, ec, Qi, ab, rb, sb, ob;
  if (t != null && t.actor && (t = t.actor), (se = t == null ? void 0 : t.document) != null && se.actor && (t = t.document.actor), !t) throw new Error("MWD.roll.execute requires actor");
  if (!(e != null && e.intent)) throw new Error("MWD.roll.execute requires payload.intent");
  const a = Z_(i);
  if (a && (e = eB(e, { subject: (i == null ? void 0 : i.subject) ?? null })), e = mA(e), t = Tq(t, e), e = await wq({ actor: t, payload: e }), !e) return null;
  let r = await Gm({ actor: t, payload: e, event: n }), s = (r == null ? void 0 : r.rollActor) ?? t;
  if (e.intent === "attack" && ((pe = (te = r == null ? void 0 : r.attack) == null ? void 0 : te.capabilityReport) != null && pe.isTemplated)) {
    const le = await aj({
      actor: t,
      attack: r.attack
    });
    if (!le) return null;
    try {
      await ij({
        actor: t,
        attack: r.attack,
        templateGeometry: le.templateGeometry ?? null
      });
    } catch (je) {
      console.warn("MWD | Unable to create visual template indicator", je);
    }
    if (await vq(le.autoTargetTokenIds ?? []), !zA(((K = r == null ? void 0 : r.attack) == null ? void 0 : K.areaEffect) ?? ((Ie = (be = r == null ? void 0 : r.attack) == null ? void 0 : be.payload) == null ? void 0 : Ie.areaEffect) ?? {}) && (!Array.isArray(le.targetSnapshots) || le.targetSnapshots.length === 0))
      return (Oe = ui.notifications) == null || Oe.warn("Template placement did not affect any targets."), null;
    e.targetSnapshots = Array.isArray(le.targetSnapshots) ? le.targetSnapshots : [], e.templateGeometry = le.templateGeometry ?? null, e.templatePlacement = le.placement, r = await Gm({ actor: t, payload: e, event: n }), s = (r == null ? void 0 : r.rollActor) ?? t;
  } else e.intent === "attack" && (delete e.targetSnapshots, delete e.templatePlacement, delete e.templateGeometry);
  e = Mq(
    e,
    qk({ actor: t, rollActor: s, resolved: r, payload: e })
  );
  let o = await iA({
    actor: t,
    rollActor: s,
    machineActor: (r == null ? void 0 : r.machineActor) ?? null,
    rollType: e.intent,
    skillId: e.key,
    domains: r.domains,
    payload: e,
    resolved: r,
    context: { event: n }
  }), l;
  if (e.intent === "initiative")
    l = e;
  else if (l = await $g.prompt({
    actor: t,
    rollActor: s,
    basePayload: e,
    resolved: r,
    diceParts: {
      attribute: ((Be = r == null ? void 0 : r.pool) == null ? void 0 : Be.attribute) ?? 0,
      skill: ((R = r == null ? void 0 : r.pool) == null ? void 0 : R.skill) ?? 0,
      bonus: ((V = r == null ? void 0 : r.pool) == null ? void 0 : V.bonus) ?? 0,
      specialization: ((B = r == null ? void 0 : r.pool) == null ? void 0 : B.specialization) ?? 0
    },
    mods: o.mods,
    modTotal: o.total
  }), !l) return null;
  if (a && (i != null && i.subject) && await tB(i.subject), e = mA(l), r = await Gm({ actor: t, payload: e, event: n }), s = (r == null ? void 0 : r.rollActor) ?? t, e.intent === "attack" && !((oe = (me = r == null ? void 0 : r.attack) == null ? void 0 : me.capabilityReport) != null && oe.isTemplated) && (delete e.targetSnapshots, delete e.templatePlacement, delete e.templateGeometry), e.intent === "attack" && e.weaponId) {
    const le = ((Ye = (Ee = t.items) == null ? void 0 : Ee.get) == null ? void 0 : Ye.call(Ee, e.weaponId)) ?? null;
    if ((bt = le == null ? void 0 : le.isWeapon) != null && bt.call(le) && typeof le.canConsumePayload == "function") {
      const je = String(e.payloadId ?? "").trim(), lo = String(((yt = le.system) == null ? void 0 : yt.selectedPayloadKey) || ((nn = le.system) == null ? void 0 : nn.selectedPayloadUuid) || ((Dn = le.system) == null ? void 0 : Dn.selectedPayloadId) || "").trim();
      je && je !== lo && await ((On = le.setActivePayload) == null ? void 0 : On.call(le, je));
      const gi = (Ln = le.canConsumePayload) == null ? void 0 : Ln.call(le, { payloadId: je, detailed: !0 });
      if (!(gi != null && gi.ok)) {
        const wa = ($n = le.getPayloadState) == null ? void 0 : $n.call(le, { payloadId: je }), sI = wa != null && wa.payloadLabel ? ` (${wa.payloadLabel})` : "";
        return (_n = ui.notifications) == null || _n.warn((gi == null ? void 0 : gi.reason) || `Not enough payload${sI} for ${le.name}.`), null;
      }
    }
  }
  o = await iA({
    actor: t,
    rollActor: s,
    machineActor: (r == null ? void 0 : r.machineActor) ?? null,
    rollType: e.intent,
    skillId: e.key,
    domains: r.domains,
    payload: e,
    resolved: r,
    context: { event: n }
  });
  const { mods: c, total: u } = o, { mods: d, total: f } = Sq(e);
  let m = [...c, ...d], g = Number(u ?? 0) + Number(f ?? 0);
  const p = Number(((cn = r == null ? void 0 : r.pool) == null ? void 0 : cn.attribute) ?? 0) + Number(((Bn = r == null ? void 0 : r.pool) == null ? void 0 : Bn.skill) ?? 0) + Number(((xn = r == null ? void 0 : r.pool) == null ? void 0 : xn.bonus) ?? 0) + Number(((U = r == null ? void 0 : r.pool) == null ? void 0 : U.specialization) ?? 0), h = Math.max(0, p + Number(g ?? 0)), b = e.intent !== "initiative", y = b ? Nq({ actor: s, ctx: r, payload: e }) : null, S = (Y = y == null ? void 0 : y.pre) != null && Y.spent ? 4 : Number(r.diceTarget ?? r.target ?? 5), A = {
    snapshot: ((we = (fe = (Ae = game.mwd) == null ? void 0 : Ae.personalCombat) == null ? void 0 : fe.getSnapshot) == null ? void 0 : we.call(fe, t)) ?? null
  };
  let T = null, C = null;
  if (r.intent === "machineRemedy") {
    if (C = await Xl(e, {
      gmOverride: !!(e != null && e.gmOverride)
    }), !C.ok)
      return (Pe = ui.notifications) == null || Pe.warn(C.reason ?? "Unable to resolve the machine remedy."), null;
    if (T = await xU(C), !(T != null && T.ok))
      return (Le = ui.notifications) == null || Le.warn((T == null ? void 0 : T.reason) ?? "Unable to spend the remedy action."), null;
  }
  for (const le of Aq(s, t)) {
    const je = {
      snapshot: ((un = (qe = (lt = game.mwd) == null ? void 0 : lt.personalCombat) == null ? void 0 : qe.getSnapshot) == null ? void 0 : un.call(qe, le)) ?? null
    }, lo = zt({
      actor: le,
      phase: "onBuildRoll",
      facts: Xu({ actor: le, resolved: r, payload: e, runtime: je }),
      packet: {},
      options: { runtime: je, consumeUsage: !0 }
    });
    await ga({ actor: le, mutations: lo.mutations, runtime: je });
  }
  b && ((et = y == null ? void 0 : y.pre) != null && et.spent) && ((an = y == null ? void 0 : y.pre) != null && an.poolKey) && await ((We = s.spendEdge) == null ? void 0 : We.call(s, y.pre.poolKey, 1));
  let k, w = 0, v = 0;
  if (r.rollType === "sum" && ((ke = r.sum) != null && ke.formula))
    k = await new Roll(r.sum.formula, r.sum.data ?? {}).evaluate(), w = Number(k.total ?? 0) + Number(g ?? 0);
  else {
    k = await new Roll(`${h}d6cs>=${S}`).evaluate();
    const le = (Qe = k.dice) == null ? void 0 : Qe[0];
    w = Array.isArray(le == null ? void 0 : le.results) ? le.results.filter((je) => je.success).length : 0, v = Array.isArray(le == null ? void 0 : le.results) ? le.results.filter((je) => je.result === 1).length : 0;
  }
  if (r.intent === "initiative" && (k == null ? void 0 : k.total) != null) {
    const le = { total: Number(k.total ?? 0) + Number(g ?? 0) }, je = zt({
      actor: t,
      phase: "onInitiativeResolved",
      facts: zk({ actor: t, packet: le, runtime: A }),
      packet: le,
      options: { runtime: A, consumeUsage: !0 }
    });
    if (await ga({ actor: t, mutations: je.mutations, runtime: A }), je.modifiers.length) {
      const lo = je.modifiers.reduce((gi, wa) => gi + Number(wa.value ?? 0), 0);
      m = m.concat(je.modifiers), g += lo, w = Number(je.packet.total ?? 0), await fA({ actor: t, total: je.packet.total ?? k.total }), r.breakdown = (r.breakdown ?? []).concat(je.modifiers.map((gi, wa) => ({
        id: `traitInitiative${wa + 1}`,
        label: gi.label,
        value: Number(gi.value ?? 0)
      })));
    } else
      w = Number(le.total ?? 0), await fA({ actor: t, total: le.total });
  }
  const N = IC(
    r,
    { successes: w, raw: (zn = k == null ? void 0 : k.toJSON) == null ? void 0 : zn.call(k) },
    null
    // opposed rolls can pass defender result later
  ), P = N == null ? void 0 : N.edgeEarned;
  !((ct = y == null ? void 0 : y.pre) != null && ct.spent) && !((It = y == null ? void 0 : y.post) != null && It.spent) && (P == null ? void 0 : P.amount) > 0 ? N.edgeEarned = await yq({
    actor: s,
    ctx: r,
    edgeInfo: y,
    earned: P
  }) : (P == null ? void 0 : P.amount) > 0 && (N.edgeEarned = null), r.intent === "overload" && await Rq({ actor: t, passed: N.passed });
  let _ = null;
  r.intent === "heatDangerCheck" && (_ = await tq({ actor: t, ctx: r, outcomeModel: N }));
  let O = null, x = null;
  r.intent === "attack" ? O = await RC({
    attacker: t,
    ctx: r,
    outcomeModel: N
  }) : r.intent === "machineRemedy" && (x = await ev(e, {
    gmOverride: !!(e != null && e.gmOverride),
    passed: !!(N != null && N.passed)
  }));
  let H = null, G = null, q = null, ie = null;
  if (r.intent === "acquire" && (H = await BC({ attacker: t, ctx: r, outcomeModel: N })), r.intent === "targeting" && (G = await xC({ attacker: t, ctx: r, outcomeModel: N })), (r.intent === "breakLock" || r.intent === "skill" && e.machineActionKey === "breakLock") && (q = await zC({ attacker: t, payload: e, ctx: r, outcomeModel: N })), r.intent === "defensiveJink" && (ie = await FC({ defender: t, payload: e, ctx: r, outcomeModel: N })), r.intent === "attack" && ((Fn = (Mi = r.attack) == null ? void 0 : Mi.ewContext) != null && Fn.activePacketId)) {
    const le = Yh(t, e), je = Pn(le);
    je && (Hooks.callAll("mwd.beforeTargetingPacketConsume", {
      attacker: t,
      targetTokenUuid: r.attack.ewContext.targetTokenUuid,
      packetId: r.attack.ewContext.activePacketId,
      ctx: r
    }), await KL(je, r.attack.ewContext.targetTokenUuid, r.attack.ewContext.activePacketId));
  }
  const Z = pG({
    actor: t,
    payload: e,
    ctx: r,
    roll: k,
    target: S,
    pool: h,
    mods: m,
    modTotal: g,
    hits: w,
    ones: v,
    edge: y,
    outcomeModel: N
  });
  s && s.uuid !== t.uuid && (Z.rollActorUuid = s.uuid), O && (Z.attackResult = O), _ && (Z.heatDangerResult = _), r.intent === "machineRemedy" && (Z.machineRemedy = r.machineRemedy ?? null, Z.machineRemedyResult = {
    ...x ?? { ok: !1, reason: "Machine remedy result missing." },
    spend: T,
    context: C
  }), H && (Z.ewAcquireResult = H), G && (Z.ewTargetingResult = G), q && (Z.ewBreakLockResult = q), ie && (Z.ewJinkResult = ie), r.acquire && (Z.acquire = r.acquire), r.targeting && (Z.targeting = r.targeting);
  const Se = await ar({ resolved: Z });
  if (e.intent === "attack" && e.weaponId) {
    const le = ((zd = (Et = t.items) == null ? void 0 : Et.get) == null ? void 0 : zd.call(Et, e.weaponId)) ?? null;
    (sr = le == null ? void 0 : le.isWeapon) != null && sr.call(le) && typeof le.consumePayload == "function" && (await ((ec = le.consumePayload) == null ? void 0 : ec.call(le, { payloadId: e.payloadId })) || (Qi = ui.notifications) == null || Qi.warn(`Payload could not be consumed for ${le.name}.`));
  }
  if (r.intent === "attack" && t.type === "battlemech") {
    const le = Array.from(/* @__PURE__ */ new Set([
      ...(((sb = (rb = (ab = r == null ? void 0 : r.attack) == null ? void 0 : ab.weapon) == null ? void 0 : rb.machineWeaponGroup) == null ? void 0 : sb.weaponIds) ?? []).map((je) => String(je ?? "").trim()).filter(Boolean),
      ...e != null && e.weaponId ? [String(e.weaponId).trim()] : []
    ]));
    try {
      await XT(t, {
        weaponIds: le,
        attackProfile: ((ob = r == null ? void 0 : r.attack) == null ? void 0 : ob.weapon) ?? null,
        reason: "attack resolution"
      });
    } catch (je) {
      console.warn("MWD | Unable to record BattleMech attack heat", je);
    }
  }
  return r.intent === "attack" ? (await Cq(t, e, { rollActor: s }), await pd(t, {
    reason: "weaponAttack",
    source: "attack",
    duration: "untilNextActivation"
  })) : r.intent === "acquire" ? await Ao(t, "acquireTarget", e, { rollActor: s, resolved: r }) : r.intent === "targeting" ? await Ao(t, "generateFireSolution", e, { rollActor: s, resolved: r }) : r.intent === "breakLock" ? await Ao(t, "breakLock", e, { rollActor: s, resolved: r }) : r.intent === "defensiveJink" ? await Ao(t, "defensiveJink", e, { rollActor: s, resolved: r }) : r.intent === "skill" && e.machineActionKey && await Ao(t, e.machineActionKey, e, { rollActor: s, resolved: r }), ChatMessage.create({
    speaker: ChatMessage.getSpeaker({ actor: t }),
    content: Se,
    rolls: k ? [k] : [],
    flags: {
      mwd: {
        payload: e,
        resolved: Z
      }
    }
  });
}
function Nq({ actor: t, ctx: e, payload: n }) {
  var g, p, h, b, y, S, A;
  const i = GC(e == null ? void 0 : e.domains), a = Pq[i] ?? null, r = (a == null ? void 0 : a.a) ?? null, s = (a == null ? void 0 : a.b) ?? null, o = [r, s].filter(Boolean), l = !!((g = n == null ? void 0 : n.toggles) != null && g.useEdge) || !!(n != null && n.useEdge);
  let c = String(((h = (p = n == null ? void 0 : n.edge) == null ? void 0 : p.pre) == null ? void 0 : h.poolKey) ?? "").trim() || null;
  c && !o.includes(c) && (c = null);
  const u = l && c ? 1 : 0;
  let d = [...o];
  u && c && (d = d.filter((T) => T !== c));
  let f = String(((y = (b = n == null ? void 0 : n.edge) == null ? void 0 : b.post) == null ? void 0 : y.poolKey) ?? "").trim() || null;
  f && !d.includes(f) && (f = null);
  const m = Number(((A = (S = n == null ? void 0 : n.edge) == null ? void 0 : S.post) == null ? void 0 : A.spent) ?? 0) ? 1 : 0;
  return {
    domain: i,
    pools: a ? { a: r, b: s } : null,
    pre: { poolKey: c, spent: u },
    post: { poolKey: f, spent: m },
    allowed: { prePools: o, postPools: d }
  };
}
function GC(t) {
  return Array.isArray(t) ? t.includes("physical") ? "physical" : t.includes("mental") ? "mental" : t.includes("social") ? "social" : null : null;
}
const Pq = {
  physical: { a: "grit", b: "chaos" },
  mental: { a: "insight", b: "rumor" },
  social: { a: "legend", b: "credibility" }
};
async function fA({ actor: t, total: e }) {
  var l, c, u, d, f, m;
  const n = ((l = D.resolveActivationUnit) == null ? void 0 : l.call(D, { actor: t })) ?? null;
  if (n != null && n.combatant) {
    await n.combatant.update({ initiative: Number(e) });
    return;
  }
  const i = (u = (c = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : c.controlled) == null ? void 0 : u.find((g) => {
    var p;
    return ((p = g.actor) == null ? void 0 : p.id) === t.id;
  }), a = ((f = (d = t.getActiveTokens) == null ? void 0 : d.call(t, !0, !0)) == null ? void 0 : f[0]) ?? null, r = i ?? a;
  if (!r) {
    (m = ui.notifications) == null || m.warn("Initiative requires a token on the current scene.");
    return;
  }
  let s = game.combat;
  s || (s = await Combat.create({
    scene: canvas.scene.id,
    active: !0
  }));
  let o = s.combatants.find((g) => g.tokenId === r.id);
  if (!o) {
    const g = await s.createEmbeddedDocuments("Combatant", [{
      tokenId: r.id,
      actorId: t.id,
      sceneId: canvas.scene.id
    }]);
    o = g == null ? void 0 : g[0];
  }
  o && await o.update({ initiative: Number(e) });
}
async function Rq({ actor: t, passed: e }) {
  e || await t.update({ "system.burn.overloaded": !0 });
}
const Dq = /* @__PURE__ */ new Set(["physical", "mental", "social"]);
function Oq(t) {
  if (t == null || t === "" || t === "—" || t === "–") return 0;
  const e = Number(t);
  return Number.isFinite(e) ? e : null;
}
function Lq(t) {
  if (!t) return;
  const e = String(t).trim().toLowerCase();
  return Dq.has(e) ? e : void 0;
}
function $q(...t) {
  const e = /* @__PURE__ */ new Set();
  return t.filter((n) => {
    if (!n) return !1;
    const i = n.uuid ?? n.id ?? n;
    return e.has(i) ? !1 : (e.add(i), !0);
  });
}
class _q {
  constructor() {
    F(this, "id", "mwd.itemModifiers");
    F(this, "label", "Item Modifiers");
  }
  collect(e) {
    var i, a;
    const n = [];
    for (const r of $q(e == null ? void 0 : e.actor, e == null ? void 0 : e.rollActor))
      for (const s of r.items ?? []) {
        const o = (a = (i = s.flags) == null ? void 0 : i.mwd) == null ? void 0 : a.modifiers;
        if (!(!Array.isArray(o) || o.length === 0))
          for (const l of o) {
            if (!l) continue;
            const c = Oq(l.value);
            if (c === null) {
              console.warn("MWD | Dropping item modifier with invalid value", {
                actor: r.name,
                item: s.name,
                modifier: l
              });
              continue;
            }
            n.push({
              label: l.label ?? s.name,
              value: c,
              source: s.name,
              domain: Lq(l.domain)
            });
          }
      }
    return n;
  }
}
function Bq(...t) {
  const e = /* @__PURE__ */ new Set();
  return t.filter((n) => {
    if (!n) return !1;
    const i = n.uuid ?? n.id ?? n;
    return e.has(i) ? !1 : (e.add(i), !0);
  });
}
class xq {
  constructor() {
    F(this, "id", "mwd.statusEffects");
    F(this, "label", "Status Effects");
  }
  collect({ actor: e, rollActor: n, domains: i } = {}) {
    var s;
    const a = [], r = Array.isArray(i) ? i.map((o) => String(o ?? "").trim()).filter(Boolean) : [];
    for (const o of Bq(e, n)) {
      const l = o == null ? void 0 : o.statuses;
      if (l)
        for (const c of l) {
          const u = Ll(c), d = u ? Wu(u, o) ? u.modifierKey : "" : c, f = ws == null ? void 0 : ws[d];
          if ((s = f == null ? void 0 : f.mods) != null && s.length)
            for (const m of f.mods) {
              const g = Array.isArray(m.domains) ? m.domains : [], p = m.value, h = g.find((b) => r.includes(b)) ?? g[0] ?? "";
              r.length && g.length && !h || r.length && g.length && !g.some((b) => r.includes(b)) || a.push({
                label: f.label ?? c,
                value: p,
                source: "Status",
                ...h ? { domain: h } : {}
              });
            }
        }
    }
    return a;
  }
}
class zq {
  constructor() {
    F(this, "id", "mwd.baseRollModifiers");
    F(this, "label", "Roll (Base)");
  }
  collect({ payload: e } = {}) {
    var s, o, l;
    const n = [], i = (s = e == null ? void 0 : e.modifiers) == null ? void 0 : s.manual;
    if (Array.isArray(i) && i.length) {
      for (const c of i) {
        if (!c) continue;
        const u = Number(c.value);
        !Number.isFinite(u) || u === 0 || n.push({
          id: c.id ?? void 0,
          label: c.label ?? "Manual modifier",
          value: u,
          source: "Manual",
          domain: c.domain ?? void 0
        });
      }
      return n;
    }
    const a = ((o = e == null ? void 0 : e.dialog) == null ? void 0 : o.otherMods) ?? ((l = e == null ? void 0 : e.modifiers) == null ? void 0 : l.otherMods) ?? (e == null ? void 0 : e.otherMods) ?? 0, r = Number(a);
    return Number.isFinite(r) && r !== 0 && n.push({
      id: "otherMods",
      label: "Other modifiers",
      value: r,
      source: "Roll"
    }), n;
  }
}
class Fq {
  constructor() {
    F(this, "id", "mwd.condition");
    F(this, "label", "Condition");
  }
  collect({ actor: e, rollActor: n, rollType: i } = {}) {
    var c, u, d, f, m, g, p;
    const a = n ?? e;
    if (!a) return [];
    if (i === "edge") return [];
    const r = ((c = a.system) == null ? void 0 : c.derived) ?? {}, s = Number(
      ((u = r == null ? void 0 : r.condition) == null ? void 0 : u.physicalPenalty) ?? ((f = (d = r == null ? void 0 : r.monitors) == null ? void 0 : d.physical) == null ? void 0 : f.penalty) ?? 0
    ), o = Number(
      ((m = r == null ? void 0 : r.condition) == null ? void 0 : m.fatiguePenalty) ?? ((p = (g = r == null ? void 0 : r.monitors) == null ? void 0 : g.fatigue) == null ? void 0 : p.penalty) ?? 0
    ), l = [];
    return Number.isFinite(s) && s !== 0 && l.push({
      id: "conditionPhysical",
      label: "Physical Penalty",
      value: s,
      source: "Physical Track"
      // domain: "physical" // optional; leave unset to apply to all domains
    }), Number.isFinite(o) && o !== 0 && l.push({
      id: "conditionFatigue",
      label: "Fatigue Penalty",
      value: o,
      source: "Fatigue Track"
      // domain: "physical" // optional
    }), l;
  }
}
const Uq = {
  id: "burn",
  async collect(t) {
    var a, r;
    const e = t.rollActor ?? t.actor;
    if (!e) return [];
    const n = Number(((r = (a = e.system) == null ? void 0 : a.burn) == null ? void 0 : r.value) ?? 0), i = Math.floor(n / 2);
    return i <= 0 ? [] : [{
      id: "burn",
      label: "Burn",
      value: -i,
      source: "Burn Track",
      domain: null
    }];
  }
};
function Kq(...t) {
  const e = /* @__PURE__ */ new Set();
  return t.filter((n) => {
    if (!n) return !1;
    const i = n.uuid ?? n.id ?? n;
    return e.has(i) ? !1 : (e.add(i), !0);
  });
}
class Hq {
  constructor() {
    F(this, "id", "mwd.lifeModules");
    F(this, "label", "Life Modules");
  }
  collect({ actor: e, rollActor: n, resolved: i } = {}) {
    return Kq(n, e).flatMap((a) => l_({ actor: a, resolved: i }));
  }
}
function jq(...t) {
  const e = /* @__PURE__ */ new Set();
  return t.filter((n) => {
    if (!n) return !1;
    const i = n.uuid ?? n.id ?? n;
    return e.has(i) ? !1 : (e.add(i), !0);
  });
}
class Gq {
  constructor() {
    F(this, "id", "mwd.traits");
    F(this, "label", "Traits");
  }
  collect({ actor: e, rollActor: n, resolved: i, payload: a } = {}) {
    return jq(n, e).flatMap((r) => {
      var o, l, c;
      const s = {
        snapshot: ((c = (l = (o = game.mwd) == null ? void 0 : o.personalCombat) == null ? void 0 : l.getSnapshot) == null ? void 0 : c.call(l, r)) ?? null
      };
      return zt({
        actor: r,
        phase: "onBuildRoll",
        facts: Xu({ actor: r, resolved: i, payload: a, runtime: s }),
        packet: {},
        options: { runtime: s, consumeUsage: !1 }
      }).modifiers;
    });
  }
}
const qq = Object.freeze({
  attackCQPenalty: { value: -1, intents: ["attack"], label: "Attack CQ Penalty" },
  sensorPenalty: { value: -1, skills: ["perception", "technician"], label: "Sensor Penalty" },
  pilotingPenalty: { value: -1, skills: ["piloting"], label: "Piloting Penalty" }
});
class Wq {
  constructor() {
    F(this, "id", "mwd.machineCriticals");
    F(this, "label", "Machine Criticals");
  }
  collect({ actor: e, resolved: n, payload: i } = {}) {
    var l, c, u;
    const a = tr(e);
    if (!a.length) return [];
    const r = String((n == null ? void 0 : n.intent) ?? (i == null ? void 0 : i.intent) ?? "").trim(), s = String(((c = (l = n == null ? void 0 : n.attack) == null ? void 0 : l.skill) == null ? void 0 : c.code) ?? ((u = n == null ? void 0 : n.skill) == null ? void 0 : u.code) ?? (i == null ? void 0 : i.key) ?? "").trim(), o = [];
    for (const d of a)
      for (const f of d.mods ?? []) {
        const m = qq[f];
        m && (m.intents && !m.intents.includes(r) || m.skills && !m.skills.includes(s) || o.push({
          id: `machineCrit.${d.id}.${f}`,
          label: `${d.label ?? "Machine Critical"}: ${m.label}`,
          value: m.value,
          source: "Machine Critical"
        }));
      }
    return o;
  }
}
function Vq(t = null) {
  return (t == null ? void 0 : t.type) === M.actorTypes.vehicle || (t == null ? void 0 : t.type) === M.actorTypes.battlemech;
}
function Jq(t = {}, e = {}) {
  var r, s, o;
  const n = ((r = t == null ? void 0 : t.attack) == null ? void 0 : r.weapon) ?? {}, i = String((n == null ? void 0 : n.category) ?? (n == null ? void 0 : n.attackKind) ?? (e == null ? void 0 : e.attackKind) ?? "").trim().toLowerCase(), a = String(((o = (s = t == null ? void 0 : t.attack) == null ? void 0 : s.skill) == null ? void 0 : o.code) ?? (n == null ? void 0 : n.skill) ?? (e == null ? void 0 : e.key) ?? "").trim().toLowerCase();
  return i !== "melee" && a !== "meleecombat" && a !== "melee";
}
function Vm(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function Yq(t = null) {
  var s, o, l, c;
  const e = ((o = (s = t == null ? void 0 : t.system) == null ? void 0 : s.monitors) == null ? void 0 : o.heat) ?? {}, n = ((c = (l = t == null ? void 0 : t.system) == null ? void 0 : l.mwd) == null ? void 0 : c.heat) ?? {}, i = Math.max(0, Vm(e.value ?? n.current, 0)), a = Math.max(0, Vm(e.max ?? n.max ?? n.hardMax, 0)), r = td(n.thresholds ?? {}, a);
  return Math.max(0, Vm(ed(i, r).rangedDicePenalty, 0));
}
class Qq {
  constructor() {
    F(this, "id", "mwd.machineStateEffects");
    F(this, "label", "Machine State Effects");
  }
  collect({ actor: e, resolved: n, payload: i } = {}) {
    var s, o, l, c, u;
    if (!Vq(e)) return [];
    const a = String((n == null ? void 0 : n.intent) ?? (i == null ? void 0 : i.intent) ?? "").trim(), r = [];
    if (a === "attack") {
      const d = mO(e, {
        weaponGroupId: ((l = (o = (s = n == null ? void 0 : n.attack) == null ? void 0 : s.weapon) == null ? void 0 : o.machineWeaponGroup) == null ? void 0 : l.id) ?? (i == null ? void 0 : i.weaponGroupId),
        weaponId: i == null ? void 0 : i.weaponId,
        weapon: (c = n == null ? void 0 : n.attack) == null ? void 0 : c.weapon,
        rangeBand: ((u = n == null ? void 0 : n.attack) == null ? void 0 : u.rangeBand) ?? (i == null ? void 0 : i.rangeBand)
      });
      if (d && r.push({
        id: "machineState.attackDice",
        label: "Machine State",
        value: d,
        source: "Machine State"
      }), e.type === M.actorTypes.battlemech && Jq(n, i)) {
        const f = Yq(e);
        f && r.push({
          id: "battlemechHeat.rangedDice",
          label: "Overheat",
          value: -f,
          source: "Heat"
        });
      }
    }
    if (a === "acquire") {
      const d = uO(e);
      d && r.push({
        id: "machineState.acquireDice",
        label: "Sensors",
        value: d,
        source: "Machine State"
      });
    }
    if (a === "targeting") {
      const d = dO(e);
      d && r.push({
        id: "machineState.targetingDice",
        label: "Fire Solution",
        value: d,
        source: "Machine State"
      });
    }
    if (a === "skill" && String((i == null ? void 0 : i.key) ?? "").trim() === "piloting") {
      const d = cO(e);
      d && r.push({
        id: "machineState.pilotingDice",
        label: "Piloting State",
        value: d,
        source: "Machine State"
      });
    }
    return r;
  }
}
function Xq(t) {
  return (t == null ? void 0 : t.type) === M.actorTypes.vehicle || (t == null ? void 0 : t.type) === M.actorTypes.battlemech;
}
class Zq {
  constructor() {
    F(this, "id", "mwd.ewTrackingPenalty");
    F(this, "label", "EW Tracking Penalty");
  }
  collect({ actor: e, resolved: n, payload: i } = {}) {
    var g, p, h, b, y;
    if (!Xq(e)) return [];
    const a = String((n == null ? void 0 : n.intent) ?? (i == null ? void 0 : i.intent) ?? "").trim();
    if (a !== "attack") return [];
    const r = ((g = n == null ? void 0 : n.attack) == null ? void 0 : g.targets) ?? [];
    if (!r.length) return [];
    const s = r[0], o = String((s == null ? void 0 : s.tokenId) ?? "").trim();
    if (!o) return [];
    const l = ((h = (p = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : p.get) == null ? void 0 : h.call(p, o)) ?? null, c = (l == null ? void 0 : l.actor) ?? null, u = [], d = Qp(c, null);
    d && u.push({
      id: "ew.trackingPenalty",
      label: "Tracking Penalty",
      value: -d,
      source: "EW"
    });
    const f = NL(e, c, {
      intent: a,
      payload: i,
      resolved: n,
      rangeBand: (b = n == null ? void 0 : n.attack) == null ? void 0 : b.rangeBand,
      targetToken: l
    });
    f && u.push(f);
    const m = ((y = n == null ? void 0 : n.attack) == null ? void 0 : y.machineMotion) ?? Ph({ targetActor: c, payload: i });
    return m != null && m.movementTrackingPenalty && u.push({
      id: "machineMotion.tracking",
      label: `Target Movement (${Number(m.trackingHexes ?? 0)} hex)`,
      value: m.movementTrackingPenalty,
      source: "Motion"
    }), m != null && m.jumpTrackingPenalty && u.push({
      id: "machineMotion.jumpTracking",
      label: "Target Jumped",
      value: m.jumpTrackingPenalty,
      source: "Motion"
    }), u;
  }
}
function eW(t) {
  return (t == null ? void 0 : t.type) === M.actorTypes.vehicle || (t == null ? void 0 : t.type) === M.actorTypes.battlemech;
}
class tW {
  constructor() {
    F(this, "id", "mwd.ewTargetingData");
    F(this, "label", "EW Targeting Data");
  }
  collect({ actor: e, resolved: n, payload: i } = {}) {
    var s, o;
    if (!eW(e)) return [];
    if (String((n == null ? void 0 : n.intent) ?? (i == null ? void 0 : i.intent) ?? "").trim() !== "attack") return [];
    const r = Number(((o = (s = n == null ? void 0 : n.attack) == null ? void 0 : s.ewContext) == null ? void 0 : o.targetingDataValue) ?? 0);
    return r ? [{
      id: "ew.targetingData",
      label: "Targeting Data",
      value: r,
      source: "EW"
    }] : [];
  }
}
function nW(t) {
  return (t == null ? void 0 : t.type) === M.actorTypes.vehicle || (t == null ? void 0 : t.type) === M.actorTypes.battlemech;
}
function iW(t = {}) {
  var a, r, s, o, l, c, u, d, f;
  const e = ((a = t == null ? void 0 : t.acquire) == null ? void 0 : a.targetActor) ?? (t == null ? void 0 : t.targetActor) ?? null;
  if (e) return e;
  const n = String(((r = t == null ? void 0 : t.acquire) == null ? void 0 : r.targetTokenId) ?? "").trim();
  if (n) {
    const m = ((l = (o = (s = globalThis.canvas) == null ? void 0 : s.tokens) == null ? void 0 : o.get) == null ? void 0 : l.call(o, n)) ?? null;
    if (m != null && m.actor) return m.actor;
  }
  const i = String(((c = t == null ? void 0 : t.acquire) == null ? void 0 : c.targetTokenUuid) ?? "").trim();
  if (i) {
    const m = ((f = (d = (u = globalThis.canvas) == null ? void 0 : u.tokens) == null ? void 0 : d.placeables) == null ? void 0 : f.find(
      (g) => {
        var p;
        return (((p = g.document) == null ? void 0 : p.uuid) ?? g.uuid) === i;
      }
    )) ?? null;
    if (m != null && m.actor) return m.actor;
  }
  return null;
}
class aW {
  constructor() {
    F(this, "id", "mwd.stealthEmissionAcquire");
    F(this, "label", "Stealth Emission Acquire");
  }
  collect({ actor: e, resolved: n, payload: i } = {}) {
    var l;
    if (!nW(e)) return [];
    const a = String((n == null ? void 0 : n.intent) ?? (i == null ? void 0 : i.intent) ?? "").trim(), r = String((i == null ? void 0 : i.actionId) ?? ((l = n == null ? void 0 : n.data) == null ? void 0 : l.actionId) ?? "").trim();
    if (a !== "acquire" && r !== "acquireTarget") return [];
    const s = iW(n), o = PL(e, s, {
      intent: "acquire",
      payload: i,
      resolved: n
    });
    return o ? [o] : [];
  }
}
function rW(t = null) {
  return (t == null ? void 0 : t.type) === M.actorTypes.vehicle || (t == null ? void 0 : t.type) === M.actorTypes.battlemech;
}
class sW {
  constructor() {
    F(this, "id", "mwd.assetModuleEffects");
    F(this, "label", "Asset Module Effects");
  }
  collect({ actor: e, resolved: n, payload: i, context: a } = {}) {
    if (!rW(e)) return [];
    const { effects: r } = Ji(e, { resolved: n, payload: i, context: a });
    return r.filter((s) => {
      var o;
      return Number(((o = s.modifies) == null ? void 0 : o.dice) ?? 0);
    }).map((s) => {
      var o;
      return {
        id: `assetModule.${s.sourceId}.${s.id}.dice`,
        label: s.label,
        value: Number(((o = s.modifies) == null ? void 0 : o.dice) ?? 0) || 0,
        source: s.sourceName || "Asset Module"
      };
    });
  }
}
class oW {
  constructor() {
    F(this, "id", "mwd.firstAidModifiers");
    F(this, "label", "First Aid Modifiers");
  }
  collect(e = {}) {
    return nz(e);
  }
}
const gA = [
  M.itemType.assetModule,
  M.itemType.mechWeapon,
  M.itemType.personalWeapon,
  "weapon"
];
class ft {
  constructor() {
    this.modifiers = {
      groups: pt.mapObjetToKeyValue(ne.modifier.group, "key", "label"),
      roll: ft._buildGroupOptions("roll"),
      attribute: ft._buildGroupOptions("attribute"),
      monitor: ft._buildGroupOptions("monitor"),
      other: ft._buildGroupOptions("other")
    }, Hooks.once("ready", () => this.onReady());
  }
  static _buildGroupOptions(e) {
    switch (e) {
      case "attribute":
        return {
          label: ne.modifier.group[e],
          effects: pt.hbsAttributes.map((n) => ({ key: n.value, label: n.labelkey })),
          categories: []
        };
    }
    return {
      label: ne.modifier.group[e],
      effects: pt.mapObjetToKeyValue(ne.modifier[e].effect, "key", "label"),
      categories: pt.mapObjetToKeyValue(ne.modifier[e].category, "key", "label")
    };
  }
  async onReady() {
    Handlebars.registerHelper("modifierHasSubCategory", (e, n, i) => this.hasSubCategory(e, n, i)), Handlebars.registerHelper("modifierSelectOption", (e, n) => this.getSelectOptions(e, n));
  }
  hasSubCategory(e, n, i) {
    switch (e) {
      case "roll":
        return !0;
      case "monitor":
        return i === "resistanceByType";
    }
    return !1;
  }
  getSelectOptions(e, n) {
    var i, a;
    switch (e) {
      case "group":
        return this.modifiers.groups;
      case "effect":
        return (i = this.modifiers[n.hash.group]) == null ? void 0 : i.effects;
      case "category":
        return (a = this.modifiers[n.hash.group]) == null ? void 0 : a.categories;
      case "subCategory":
        switch (n.hash.group) {
          case "roll":
            return this.getSelectRollSubCategories(n.hash.category);
          case "monitor": {
            switch (n.hash.category) {
              case "resistanceByType":
                return pt.getDamageTypes().map((r) => ({ key: r.value, label: r.labelkey }));
            }
            return [];
          }
        }
        return [];
    }
    return [];
  }
  getSelectRollSubCategories(e) {
    switch (e) {
      case "attribute":
        return pt.getAttributes().map((i) => ({ key: i.value, label: i.labelkey }));
      case "skill":
        return game.system.anarchy.skills.getSkills().map((i) => ({ key: i.code, label: i.labelkey }));
      case "attributeAction":
        const n = _t.all().map((i) => ({ key: i.code, label: i.labelkey }));
        return ot.distinct(n.map((i) => i.key)).map((i) => n.find((a) => a.key == i));
    }
    return [];
  }
  getEnums() {
    return { modifiers: this.modifiers };
  }
  static buildRollModifiersFilter(e, n) {
    return (i) => {
      var a;
      if (i.group == "roll" && i.effect == n)
        switch (i.category) {
          case "attribute":
            return [e.attribute1, e.attribute2].includes(i.subCategory);
          case "skill":
            return i.subCategory == ((a = e.skill) == null ? void 0 : a.system.code);
          case "attributeAction":
            return i.subCategory == e.attributeAction || i.subCategory == _t.getDefenseAttributeAction(e.defenseAction);
        }
      return !1;
    };
  }
  static computeRollModifiers(e, n, i) {
    const a = ft.buildRollModifiersFilter(n, i), r = (c) => c.group == "roll" && c.effect == i && a(c), s = ft._activeItems(e).map((c) => ft.itemModifiers(c, r)).reduce((c, u) => c.concat(u), []).sort(ot.descending((c) => c.modifier.value)), o = ft.$sumAssetModuleModifiers(s.filter((c) => gA.includes(c.item.type)).map((c) => c.modifier.value)), l = ot.sumValues(s.filter((c) => !gA.includes(c.item.type)).map((c) => c.modifier.value));
    return {
      value: o + l,
      sources: s
    };
  }
  static $sumAssetModuleModifiers(e) {
    const n = e.find((r) => r > 3) ?? 0, i = ot.sumValues(e.filter((r) => r < 0)), a = Math.min(3, ot.sumValues(e.filter((r) => r > 0 && r <= 3)));
    return i + Math.max(a, n);
  }
  static computeModifiers(e, n, i = void 0, a = void 0) {
    const r = ft._createFilter(n, i, a), s = ft._activeItems(e).map((l) => ft.itemModifiers(l, r)).reduce((l, c) => l.concat(c), []);
    return {
      value: ot.sumValues(s, (l) => l.modifier.value),
      sources: s
    };
  }
  static sumMonitorModifiers(e, n, i, a = void 0) {
    return ft.sumModifiers(ft._activeItems(e), "monitor", n, i, a);
  }
  static sumModifiers(e, n, i, a, r = void 0) {
    const s = ft._createFilter(n, i, a, r), o = ft._activeItems(e).map((l) => ft.itemModifiers(l, s)).reduce((l, c) => l.concat(c), []);
    return ot.sumValues(o, (l) => l.modifier.value);
  }
  static _createFilter(e, n, i, a = void 0) {
    return (r) => r.group == e && r.effect == (n ?? r.effect) && r.category == (i ?? r.category) && (a == null ? !0 : r.subCategory == a);
  }
  static countModifiers(e, n, i = void 0, a = void 0) {
    const r = ft._createFilter(n, i, a);
    return ft._activeItems(e).map((o) => ft.itemModifiers(o, r)).reduce((o, l) => o.concat(l), []).count;
  }
  static itemModifiers(e, n) {
    return ft._listItemModifiers(e, n).map((i) => ft._itemModifier(e, i));
  }
  static _listItemModifiers(e, n = (i) => !0) {
    return (e.system.modifiers ?? []).filter(n);
  }
  static _itemModifier(e, n) {
    return {
      item: e,
      modifier: n
    };
  }
  static _activeItems(e) {
    return e;
  }
}
function Or(t = {}) {
  var e;
  return (t == null ? void 0 : t.type) === "machineAttackDamage" || ((e = t == null ? void 0 : t.payload) == null ? void 0 : e.mode) === "machineAttackDamage";
}
function xg(t = {}) {
  var e;
  return !Or(t) && ((t == null ? void 0 : t.type) === "attackDamage" || ((e = t == null ? void 0 : t.payload) == null ? void 0 : e.mode) === "attackDamage");
}
function Il(t = {}) {
  var e;
  return (t == null ? void 0 : t.targetActorUuid) ?? ((e = t == null ? void 0 : t.target) == null ? void 0 : e.actorUuid) ?? null;
}
function El(t = {}) {
  var e;
  return (t == null ? void 0 : t.targetTokenUuid) ?? ((e = t == null ? void 0 : t.target) == null ? void 0 : e.tokenUuid) ?? null;
}
function qC(t = {}, e = {}) {
  var a, r, s;
  if (!Or(t)) return t;
  const n = Math.max(0, Math.trunc(Number(((a = t == null ? void 0 : t.payload) == null ? void 0 : a.previewRevision) ?? (t == null ? void 0 : t.previewRevision) ?? (e == null ? void 0 : e.previewRevision) ?? 0) || 0)), i = Array.isArray((r = e == null ? void 0 : e.critical) == null ? void 0 : r.records) ? foundry.utils.deepClone(e.critical.records).map((o) => ({ ...o, previewRevision: n })) : [];
  return t.type = "machineAttackDamage", t.targetActorUuid = Il(t), t.targetTokenUuid = El(t), t.hitLocation = (e == null ? void 0 : e.hitLocation) ?? t.hitLocation ?? ((s = t.payload) == null ? void 0 : s.hitLocation) ?? null, t.damagePreview = (e == null ? void 0 : e.damagePreview) ?? null, t.critical = (e == null ? void 0 : e.critical) ?? null, t.preparedCriticalRecords = i, t.reliabilityOptions = (e == null ? void 0 : e.reliabilityOptions) ?? null, t.previewRevision = n, t.preview = e, t.payload ?? (t.payload = {}), t.payload.previewRevision = n, t.payload.hitLocation = t.hitLocation, t.payload.preparedCriticalRecords = i, t;
}
function lW(t = {}, e = {}) {
  var a, r;
  if (!xg(t)) return t;
  const n = Math.max(0, Math.trunc(Number(((a = t == null ? void 0 : t.payload) == null ? void 0 : a.previewRevision) ?? (t == null ? void 0 : t.previewRevision) ?? (e == null ? void 0 : e.previewRevision) ?? 0) || 0)), i = Array.isArray((r = e == null ? void 0 : e.critical) == null ? void 0 : r.records) ? foundry.utils.deepClone(e.critical.records).map((s) => ({ ...s, previewRevision: n })) : [];
  return t.type = "attackDamage", t.targetActorUuid = Il(t), t.targetTokenUuid = El(t), t.critical = (e == null ? void 0 : e.critical) ?? null, t.preparedCriticalRecords = i, t.previewRevision = n, t.preview = e, t.payload ?? (t.payload = {}), t.payload.previewRevision = n, t.payload.criticalPreview = (e == null ? void 0 : e.critical) ?? null, t.payload.preparedCriticalRecords = i, t;
}
async function cW({ mutation: t = {}, result: e = {}, targetActor: n = null, targetToken: i = null } = {}) {
  var s;
  if (!Or(t)) return { ok: !1, reason: "not-machine-damage" };
  t.previewRevision = Math.max(0, Math.trunc(Number(t.previewRevision ?? ((s = t.payload) == null ? void 0 : s.previewRevision) ?? 0) || 0)) + 1, t.payload ?? (t.payload = {}), t.payload.previewRevision = t.previewRevision, delete t.payload.preparedCriticalRecords, t.preparedCriticalRecords = [], t.damagePreview = null, t.critical = null, t.reliabilityOptions = null;
  const a = await Mn.apply({
    actor: n,
    token: i,
    payload: t.payload,
    options: {
      actorId: (n == null ? void 0 : n.id) ?? "",
      dryRun: !0,
      logToChat: !1
    }
  }), r = bs(
    a,
    (e == null ? void 0 : e.target) ?? t.target ?? {},
    (e == null ? void 0 : e.damage) ?? {},
    { queued: !0, applied: !1 }
  );
  return qC(t, r), { ok: !0, summary: r };
}
async function uW({ machineActor: t = null, operatorActorUuid: e = "" } = {}) {
  var r, s, o, l, c, u, d, f;
  const n = await Ut({ machineActor: t, operatorActorUuid: e });
  if (!n.actor)
    return (r = game.user) != null && r.isGM ? { ok: !0, gmOverride: !0 } : { ok: !1, reason: n.reason || "No linked operator or pilot actor for Chaos Edge." };
  const i = M.counters.edgePools.chaos, a = Number(((o = (s = n.actor).getRemainingEdge) == null ? void 0 : o.call(s, i)) ?? ((c = (l = n.actor).getEdgePoolValue) == null ? void 0 : c.call(l, i)) ?? 0);
  return a <= 0 && !((u = game.user) != null && u.isGM) ? { ok: !1, reason: `${n.actor.name ?? "Operator"} has no Chaos Edge remaining.` } : (a > 0 && await ((f = (d = n.actor).spendEdge) == null ? void 0 : f.call(d, i, 1, { source: "machineChaosCritical" })), { ok: !0, operatorActor: n.actor });
}
function dW(t) {
  return (t == null ? void 0 : t.type) === M.actorTypes.character || (t == null ? void 0 : t.type) === M.actorTypes.npc;
}
function mW(t) {
  return (t == null ? void 0 : t.type) === M.actorTypes.battlemech || (t == null ? void 0 : t.type) === M.actorTypes.vehicle;
}
async function fW({ targetActor: t, targetToken: e, onHitEffect: n, source: i = "" } = {}) {
  if (!n || !t) return;
  const a = xG(n);
  if (a) {
    if (a.kind === "onFire") {
      await Mn.apply({
        actor: t,
        token: e,
        payload: { mode: "status", statusId: "onFire", active: !0, source: i },
        options: { logToChat: !1 }
      });
      return;
    }
    if (a.kind === "status" && a.statusId) {
      await Mn.apply({
        actor: t,
        token: e,
        payload: { mode: "status", statusId: a.statusId, active: !0, source: i },
        options: { logToChat: !1 }
      });
      return;
    }
    a.kind === "burn" && dW(t) ? await Mn.apply({
      actor: t,
      token: e,
      payload: { mode: "burnDelta", delta: a.amount, source: i },
      options: { logToChat: !1 }
    }) : a.kind === "heat" && mW(t) && await sd(t, a.amount, { reason: i || "weapon hit effect" });
  }
}
async function WC(t, e) {
  var c, u, d, f, m, g, p, h;
  const n = ((u = (c = t == null ? void 0 : t.attackResult) == null ? void 0 : c.results) == null ? void 0 : u[e]) ?? null, i = (n == null ? void 0 : n.queuedMutation) ?? null;
  if (!i)
    return { ok: !1, reason: "no-queued-damage", userMessage: "No queued attack damage to apply." };
  if (i.applied)
    return { ok: !0, skipped: !0, reason: "already-applied", userMessage: "That attack damage has already been applied." };
  let a = null, r = null;
  if (a = Il(i) ? await fromUuid(Il(i)) : null, r = El(i) ? await fromUuid(El(i)) : null, n != null && n.evadeActive && a) {
    const b = await D.commitReactionSpend(a, {
      token: r,
      actionId: "evade",
      actionLabel: "Evade",
      actionCategory: "reaction",
      logLabel: `Evade: ${((d = i.target) == null ? void 0 : d.name) ?? ((f = n == null ? void 0 : n.target) == null ? void 0 : f.name) ?? "Target"}`,
      edgePoolKey: String((n == null ? void 0 : n.evadeEdgePoolKey) ?? "").trim()
    });
    if (!(b != null && b.ok))
      return { ok: !1, reason: "evade-spend-failed", userMessage: (b == null ? void 0 : b.reason) ?? "Unable to spend the Evade reaction." };
    await D.clearPendingReaction(a, { token: r });
  }
  if (Or(i) && ((m = i.payload) != null && m.chaosCriticalSelected)) {
    const b = await uW({
      machineActor: a,
      operatorActorUuid: (g = i.payload) == null ? void 0 : g.operatorActorUuid
    });
    if (!b.ok) return b;
  }
  Or(i) && (i.payload ?? (i.payload = {}), i.payload.applied = !!i.applied, i.payload.previewRevision = Math.max(0, Math.trunc(Number(i.previewRevision ?? i.payload.previewRevision ?? 0) || 0)), i.payload.preparedCriticalRecords = Array.isArray(i.preparedCriticalRecords) ? foundry.utils.deepClone(i.preparedCriticalRecords) : Array.isArray(i.payload.preparedCriticalRecords) ? i.payload.preparedCriticalRecords : []), xg(i) && (i.payload ?? (i.payload = {}), i.payload.applied = !!i.applied, i.payload.previewRevision = Math.max(0, Math.trunc(Number(i.previewRevision ?? i.payload.previewRevision ?? 0) || 0)), i.payload.preparedCriticalRecords = Array.isArray(i.preparedCriticalRecords) ? foundry.utils.deepClone(i.preparedCriticalRecords) : Array.isArray(i.payload.preparedCriticalRecords) ? i.payload.preparedCriticalRecords : []);
  const s = await Mn.apply({
    actor: a,
    token: r,
    payload: i.payload ?? {},
    options: {
      actorId: (a == null ? void 0 : a.id) ?? "",
      logToChat: !1
    }
  }), o = bs(
    s,
    (n == null ? void 0 : n.target) ?? i.target ?? {},
    (n == null ? void 0 : n.damage) ?? {},
    { queued: !1, applied: !!(s != null && s.ok) }
  );
  if (!(s != null && s.ok))
    return { ok: !1, reason: "harm-apply-failed", userMessage: o.reason ?? "Unable to apply attack damage." };
  const l = ((p = i.payload) == null ? void 0 : p.onHitEffect) ?? null;
  if (l)
    try {
      await fW({
        targetActor: a,
        targetToken: r,
        onHitEffect: l,
        source: ((h = i.payload) == null ? void 0 : h.source) ?? ""
      });
    } catch (b) {
      console.warn("MWD | Failed to apply on-hit effect", l, b);
    }
  return i.applied = !0, i.appliedResult = o, Or(i) && (i.payload.applied = !0, i.payload.appliedResult = o, qC(i, o), i.applied = !0, i.appliedResult = o), xg(i) && (i.payload.applied = !0, i.payload.appliedResult = o, lW(i, o), i.applied = !0, i.appliedResult = o), n.queuedMutation = i, n.damageResult = o, n.evadeApplied = !!n.evadeActive, t.edge ?? (t.edge = {}), t.edge.availableActions = {
    ...t.edge.availableActions ?? {},
    canSpendPost: !1,
    canPostRerollFailures: !1
  }, {
    ok: !0,
    applied: !0,
    summary: o,
    targetActor: a,
    targetToken: r
  };
}
async function gW({ message: t = null, resultIndex: e } = {}) {
  var s, o;
  if (!t) throw new Error("Queued attack damage requires a chat message.");
  const n = Number(e);
  if (!Number.isInteger(n) || n < 0)
    throw new Error(`Invalid queued attack damage result index: ${e}`);
  const i = foundry.utils.deepClone((o = (s = t == null ? void 0 : t.flags) == null ? void 0 : s.mwd) == null ? void 0 : o.resolved);
  if (!i) return { ok: !1, reason: "missing-resolved", userMessage: "Roll data is no longer available." };
  const a = await WC(i, n);
  if (!a.ok || a.skipped) return a;
  const r = await ar({ resolved: i });
  return {
    ...a,
    resolved: i,
    content: r,
    updateData: {
      content: r,
      "flags.mwd.resolved": i
    }
  };
}
async function pW({ message: t = null } = {}) {
  var l, c, u;
  if (!t) throw new Error("Queued attack damage requires a chat message.");
  const e = foundry.utils.deepClone((c = (l = t == null ? void 0 : t.flags) == null ? void 0 : l.mwd) == null ? void 0 : c.resolved);
  if (!e) return { ok: !1, reason: "missing-resolved", userMessage: "Roll data is no longer available." };
  const i = (Array.isArray((u = e == null ? void 0 : e.attackResult) == null ? void 0 : u.results) ? e.attackResult.results : []).map((d, f) => ({ result: d, index: f })).filter(({ result: d }) => (d == null ? void 0 : d.queuedMutation) && !d.queuedMutation.applied).map(({ index: d }) => d);
  if (!i.length)
    return { ok: !1, reason: "none-pending", userMessage: "No queued attack damage remains to apply." };
  let a = 0;
  const r = [], s = [];
  for (const d of i) {
    const f = await WC(e, d);
    f.ok && f.applied ? (a += 1, s.push(f)) : f.ok || r.push(f.userMessage ?? f.reason ?? `Target ${d + 1} failed.`);
  }
  if (a <= 0)
    return { ok: !1, reason: "none-applied", userMessage: r[0] ?? "Unable to apply queued attack damage." };
  const o = await ar({ resolved: e });
  return {
    ok: !0,
    applied: a,
    failures: r,
    appliedResults: s,
    resolved: e,
    content: o,
    updateData: {
      content: o,
      "flags.mwd.resolved": e
    }
  };
}
const hW = Object.freeze({
  applyQueuedAttackDamageFromMessage: gW,
  applyAllQueuedAttackDamageFromMessage: pW,
  rebuildQueuedAttackDamagePreview: cW,
  isMachineDamageMutation: Or,
  getMutationTargetActorUuid: Il,
  getMutationTargetTokenUuid: El
});
function zg(t, e = 0, n = 1) {
  const i = Number(t);
  return Number.isFinite(i) ? Math.min(n, Math.max(e, i)) : e;
}
function bW(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function ko(t, e = 0) {
  return Math.max(e, bW(t, e));
}
function pA(t, e, n) {
  return n <= e ? t >= n ? 1 : 0 : zg((t - e) / (n - e), 0, 1);
}
function yW(t = {}) {
  return !!(t && typeof t == "object" && "current" in t && "thresholds" in t);
}
function SW(t = null) {
  var e;
  return !t || t != null && t.type && t.type !== "battlemech" ? null : yW(t) ? t : (e = t == null ? void 0 : t.actor) != null && e.type && t.actor.type !== "battlemech" ? null : ma(t);
}
function AW(t = null) {
  var u;
  const e = SW(t);
  if (!e) return null;
  const n = ko(e.current, 0), i = e.thresholds ?? {}, a = ko(i.runningHot ?? i.hot, 0), r = Math.max(a, ko(i.overheated ?? i.overheat, a)), s = Math.max(r, ko(i.shutdown ?? i.danger, r || 1)), o = ko((u = e == null ? void 0 : e.penalties) == null ? void 0 : u.dangerLevel, n >= s ? n - s + 1 : 0), l = zg(s > 0 ? n / s : 0, 0, 1);
  if (n < a || a <= 0)
    return {
      active: !1,
      heat: n,
      normalized: l,
      band: "safe",
      warmth: 0,
      glow: 0,
      shimmer: 0,
      blur: 0,
      pulseAmplitude: 0,
      pulseSpeed: 0,
      animated: !1
    };
  if (n < r) {
    const d = pA(n, a, Math.max(a + 1, r - 1));
    return {
      active: !0,
      heat: n,
      normalized: l,
      band: "hot",
      warmth: 0.12 + d * 0.16,
      glow: 0.04 + d * 0.16,
      shimmer: 0.02 + d * 0.1,
      blur: 0,
      pulseAmplitude: 0,
      pulseSpeed: 0,
      animated: !1
    };
  }
  if (n < s) {
    const d = pA(n, r, Math.max(r + 1, s - 1));
    return {
      active: !0,
      heat: n,
      normalized: l,
      band: "overheat",
      warmth: 0.3 + d * 0.24,
      glow: 0.18 + d * 0.24,
      shimmer: 0.16 + d * 0.25,
      blur: d * 0.18,
      pulseAmplitude: 0.2 + d * 0.15,
      pulseSpeed: 0,
      animated: !0
    };
  }
  const c = zg((o - 1) / 4, 0, 1);
  return {
    active: !0,
    heat: n,
    normalized: l,
    band: "danger",
    warmth: 0.58 + c * 0.1,
    glow: 0.42 + c * 0.25,
    shimmer: 0.42 + c * 0.5,
    blur: 0.18 + c * 0.1,
    pulseAmplitude: 0.3 + c * 0.15,
    pulseSpeed: 0.95 + c * 0.55,
    animated: !0
  };
}
const kW = Object.freeze({
  uTime: 0,
  uHeat: 0,
  uShimmer: 0,
  uGlow: 0,
  uPulse: 0,
  uTopBias: 1.75
}), TW = `
attribute vec2 aVertexPosition;
attribute vec2 aTextureCoord;

uniform mat3 projectionMatrix;

varying vec2 vTextureCoord;

void main(void) {
  vTextureCoord = aTextureCoord;
  gl_Position = vec4((projectionMatrix * vec3(aVertexPosition, 1.0)).xy, 0.0, 1.0);
}
`, wW = `
varying vec2 vTextureCoord;

uniform sampler2D uSampler;
uniform float uTime;
uniform float uHeat;
uniform float uShimmer;
uniform float uGlow;
uniform float uPulse;
uniform float uTopBias;

float hash(vec2 p) {
  return fract(sin(dot(p, vec2(127.1, 311.7))) * 43758.5453123);
}

void main(void) {
  vec2 uv = vTextureCoord;
  float topBias = pow(clamp(1.0 - uv.y, 0.0, 1.0), max(0.001, uTopBias));
  float wave = sin((uv.y * 46.0) + (uTime * 3.1)) * 0.0025 * uShimmer * (0.3 + topBias);
  float jitter = (hash(vec2(floor(uv.y * 64.0), floor((uTime * 8.0) + (uv.x * 24.0)))) - 0.5)
    * 0.0020
    * uShimmer
    * (0.2 + topBias);
  vec2 sampleUv = uv + vec2(wave + jitter, 0.0);

  vec4 base = texture2D(uSampler, sampleUv);
  float pulse = 1.0 + (sin(uTime * 4.0) * uPulse);
  float glowFactor = uGlow * pulse * (0.35 + (topBias * 0.65));

  vec3 warmed = base.rgb;
  warmed.r += (uHeat * 0.18) + (glowFactor * 0.18);
  warmed.g += (uHeat * 0.04) + (glowFactor * 0.04);
  warmed.b = max(0.0, warmed.b - (uHeat * 0.08));

  vec3 finalColor = mix(base.rgb, warmed, clamp((uHeat * 0.85) + (glowFactor * 0.55), 0.0, 1.0));
  finalColor += vec3(1.0, 0.38, 0.08) * glowFactor * 0.10 * base.a;

  gl_FragColor = vec4(finalColor, base.a);
}
`;
class MW {
  constructor(e = "", n = "", i = {}) {
    this.uniforms = { ...i }, this.enabled = !0, this.padding = 0, this.autoFit = !0;
  }
}
function vW() {
  var t, e, n, i, a;
  return ((i = (n = (e = (t = globalThis.foundry) == null ? void 0 : t.canvas) == null ? void 0 : e.rendering) == null ? void 0 : n.filters) == null ? void 0 : i.AbstractBaseFilter) ?? ((a = globalThis.PIXI) == null ? void 0 : a.Filter) ?? MW;
}
const CW = vW();
class IW extends CW {
  constructor(e = {}) {
    super(TW, wW, {
      ...kW,
      ...e
    }), this.padding = 6, this.autoFit = !0;
  }
  static create(e = {}) {
    return new this(e);
  }
  setVisualState(e = {}, { photosensitive: n = !1 } = {}) {
    const i = this.uniforms ?? {};
    i.uHeat = Number((e == null ? void 0 : e.normalized) ?? 0) || 0, i.uGlow = Number((e == null ? void 0 : e.glow) ?? 0) || 0, i.uTopBias = 1.75, i.uShimmer = n ? 0 : Number((e == null ? void 0 : e.shimmer) ?? 0) || 0, i.uPulse = n ? 0 : Number((e == null ? void 0 : e.pulseAmplitude) ?? 0) || 0;
  }
  setTime(e = 0) {
    const n = this.uniforms ?? {};
    n.uTime = Number(e) || 0;
  }
}
function hA(t = {}) {
  return IW.create(t);
}
const bA = "systems/mwd/img/mek/units/DamageDecals/FireMulti/SmokeFireMulti.png";
function la(t, e) {
  var n, i;
  return (i = (n = globalThis.foundry) == null ? void 0 : n.utils) != null && i.hasProperty ? foundry.utils.hasProperty(t, e) : String(e ?? "").split(".").filter(Boolean).reduce((a, r) => a == null ? void 0 : a[r], t) !== void 0;
}
function EW() {
  var t, e, n;
  return ((t = globalThis.PIXI) == null ? void 0 : t.ColorMatrixFilter) ?? ((n = (e = globalThis.PIXI) == null ? void 0 : e.filters) == null ? void 0 : n.ColorMatrixFilter) ?? null;
}
function NW() {
  var t, e, n;
  return ((t = globalThis.PIXI) == null ? void 0 : t.BlurFilter) ?? ((n = (e = globalThis.PIXI) == null ? void 0 : e.filters) == null ? void 0 : n.BlurFilter) ?? null;
}
function PW() {
  var t;
  return ((t = globalThis.PIXI) == null ? void 0 : t.Sprite) ?? null;
}
function RW() {
  var t;
  return ((t = globalThis.PIXI) == null ? void 0 : t.Texture) ?? null;
}
function DW(t = "SCREEN") {
  var e, n;
  return ((n = (e = globalThis.PIXI) == null ? void 0 : e.BLEND_MODES) == null ? void 0 : n[t]) ?? String(t).toLowerCase();
}
function yA(t) {
  var e;
  return String(((e = t == null ? void 0 : t.document) == null ? void 0 : e.uuid) ?? (t == null ? void 0 : t.id) ?? "").trim();
}
function VC(t) {
  return Array.isArray(t) ? t : [];
}
function Fg(t = null) {
  return t ? Array.isArray(t) ? t : t.contents && Array.isArray(t.contents) ? t.contents : typeof t.values == "function" ? Array.from(t.values()) : typeof t[Symbol.iterator] == "function" ? Array.from(t) : [] : [];
}
function Ug(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? Math.max(e, n) : e;
}
function OW(t, e) {
  var n, i, a;
  return !t || !e ? !1 : ((n = t.actor) == null ? void 0 : n.id) === e.id ? !0 : String(((a = (i = t == null ? void 0 : t.document) == null ? void 0 : i.baseActor) == null ? void 0 : a.id) ?? "").trim() === String(e.id ?? "").trim();
}
function pr(t = null) {
  return (t == null ? void 0 : t.type) === "battlemech" || (t == null ? void 0 : t.type) === "vehicle";
}
function JC(t = null) {
  var a, r, s, o, l;
  const e = /* @__PURE__ */ new Set(), n = (c) => {
    const u = String(c ?? "").trim();
    u && e.add(u);
  }, i = t == null ? void 0 : t.statuses;
  return (i != null && i.forEach || Array.isArray(i)) && i.forEach(n), n((r = (a = t == null ? void 0 : t.getFlag) == null ? void 0 : a.call(t, I, "status")) == null ? void 0 : r.id), n((l = (o = (s = t == null ? void 0 : t.flags) == null ? void 0 : s[I]) == null ? void 0 : o.status) == null ? void 0 : l.id), n(t == null ? void 0 : t.statusId), e;
}
function LW(t = null, e = "") {
  var r;
  const n = String(e ?? "").trim();
  if (!t || !n) return !1;
  const i = t.statuses;
  return (r = i == null ? void 0 : i.has) != null && r.call(i, n) || Array.isArray(i) && i.includes(n) ? !0 : [
    ...Fg(t.effects),
    ...Fg(t.appliedEffects)
  ].some((s) => JC(s).has(n));
}
function $W(t = null, e = "") {
  var s, o, l, c;
  const n = String(e ?? "").trim();
  if (!t || !n) return !1;
  const i = ((s = t.document) == null ? void 0 : s.statuses) ?? t.statuses;
  if ((o = i == null ? void 0 : i.has) != null && o.call(i, n) || Array.isArray(i) && i.includes(n)) return !0;
  const a = (((l = globalThis.CONFIG) == null ? void 0 : l.statusEffects) ?? []).find((u) => String((u == null ? void 0 : u.id) ?? "").trim() === n), r = new Set([
    n,
    String((a == null ? void 0 : a.img) ?? "").trim(),
    String((a == null ? void 0 : a.icon) ?? "").trim()
  ].filter(Boolean));
  return Fg(((c = t.document) == null ? void 0 : c.effects) ?? t.effects).map((u) => String(u ?? "").trim()).some((u) => r.has(u));
}
function _W(t = null, e = null) {
  var n, i, a;
  return pr(t) ? LW(t, "destroyed") || $W(e, "destroyed") ? !0 : String(((a = (i = (n = t == null ? void 0 : t.system) == null ? void 0 : n.mwd) == null ? void 0 : i.status) == null ? void 0 : a.state) ?? "").trim().toLowerCase() === "destroyed" : !1;
}
function SA(t = [], e = []) {
  return VC(t).filter((n) => n && !e.includes(n));
}
function AA(t = 0) {
  const e = Ug(t, 0);
  return [
    1 + e * 0.34,
    e * 0.05,
    0,
    0,
    0,
    e * 0.01,
    1 + e * 0.07,
    0,
    0,
    0,
    0,
    e * 0.03,
    Math.max(0.62, 1 - e * 0.24),
    0,
    0,
    0,
    0,
    0,
    1,
    0
  ];
}
function BW(t, e) {
  var n;
  (n = t == null ? void 0 : t.add) == null || n.call(t, e);
}
function xW(t, e) {
  var n;
  (n = t == null ? void 0 : t.remove) == null || n.call(t, e);
}
function zW(t = {}) {
  return la(t, "system.monitors.heat.value") || la(t, "system.mwd.heat.current") || la(t, "system.mwd.heat.thresholds") || la(t, "system.mwd.heat.thresholds.runningHot") || la(t, "system.mwd.heat.thresholds.overheated") || la(t, "system.mwd.heat.thresholds.shutdown");
}
function FW(t = {}) {
  return la(t, "system.mwd.status.state");
}
class UW {
  constructor({
    settingKey: e = DA,
    tickerAdd: n = BW,
    tickerRemove: i = xW
  } = {}) {
    this.settingKey = e, this._records = /* @__PURE__ */ new Map(), this._animatedKeys = /* @__PURE__ */ new Set(), this._tickerAdd = n, this._tickerRemove = i, this._tickerFn = null;
  }
  init() {
    Hooks.on("canvasReady", () => {
      this.refreshAll(), this._queueRefreshAll(250);
    }), Hooks.on("refreshToken", (e) => this._onRefreshToken(e)), Hooks.on("updateToken", (e, n) => this._onUpdateTokenDocument(e, n)), Hooks.on("updateActor", (e, n) => this._onUpdateActor(e, n)), Hooks.on("createActiveEffect", (e) => this._onActiveEffectChange(e)), Hooks.on("updateActiveEffect", (e) => this._onActiveEffectChange(e)), Hooks.on("deleteActiveEffect", (e) => this._onActiveEffectChange(e)), Hooks.on("canvasTearDown", () => this.onCanvasTearDown()), Hooks.on("updateSetting", (e) => this._onUpdateSetting(e));
  }
  isEnabled() {
    try {
      return game.settings.get(I, this.settingKey) !== !1;
    } catch {
      return !0;
    }
  }
  syncActor(e) {
    var n;
    if (!(!e || !pr(e) || !(canvas != null && canvas.ready)))
      for (const i of ((n = canvas.tokens) == null ? void 0 : n.placeables) ?? [])
        OW(i, e) && this.syncToken(i);
  }
  refreshAll() {
    var e;
    if (canvas != null && canvas.ready)
      for (const n of ((e = canvas.tokens) == null ? void 0 : e.placeables) ?? [])
        pr(n == null ? void 0 : n.actor) ? this.syncToken(n) : this.clearToken(n);
  }
  syncToken(e) {
    if (!e) return null;
    if (!this.isEnabled())
      return this.clearToken(e), null;
    const n = e.actor ?? null, i = (n == null ? void 0 : n.type) === "battlemech" ? AW(n) : null, a = _W(n, e);
    if (!(i != null && i.active) && !a)
      return this.clearToken(e), i;
    const r = this.ensure(e);
    return r && (this._applyHeatState(r, i, {
      photosensitive: !!(canvas != null && canvas.photosensitiveMode)
    }), this._applyRuinedState(r, { ruined: a })), i;
  }
  ensure(e) {
    const n = this._getDisplayObject(e);
    if (!n) return null;
    const i = yA(e);
    if (!i) return null;
    let a = this._records.get(i) ?? null;
    if (a && a.display !== n && (this._detachRecord(a), this._records.delete(i), this._animatedKeys.delete(i), a = null), a)
      a.token = e, a.display = n;
    else {
      const r = this._createColorFilter(), s = this._createBlurFilter(), o = hA();
      a = {
        key: i,
        token: e,
        display: n,
        color: r,
        blur: s,
        shimmer: o,
        ruinedDecal: null,
        ruinedFilters: null,
        ruinedBaseScale: 1,
        time: 0,
        animated: !1,
        decalAnimated: !1
      }, this._records.set(i, a);
    }
    return this._attachRecord(a), a;
  }
  clearToken(e) {
    const n = yA(e);
    if (!n) return;
    const i = this._records.get(n) ?? null;
    i && (this._detachRecord(i), this._records.delete(n), this._animatedKeys.delete(n), this._teardownTickerIfIdle());
  }
  onCanvasTearDown() {
    for (const e of Array.from(this._records.keys())) {
      const n = this._records.get(e);
      n && this._detachRecord(n);
    }
    this._records.clear(), this._animatedKeys.clear(), this._teardownTicker(!0);
  }
  _onRefreshToken(e) {
    if (!pr(e == null ? void 0 : e.actor)) {
      this.clearToken(e);
      return;
    }
    this.syncToken(e);
  }
  _onUpdateActor(e, n) {
    pr(e) && (!zW(n) && !FW(n) || this.syncActor(e));
  }
  _onUpdateTokenDocument(e, n = {}) {
    var a, r, s;
    if (!la(n, "effects") && !la(n, "overlayEffect")) return;
    const i = ((r = (a = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : a.get) == null ? void 0 : r.call(a, e == null ? void 0 : e.id)) ?? (((s = canvas == null ? void 0 : canvas.tokens) == null ? void 0 : s.placeables) ?? []).find((o) => (o == null ? void 0 : o.document) === e) ?? null;
    !i || !pr(i.actor) || this._queueTokenSync(i);
  }
  _onActiveEffectChange(e) {
    const n = (e == null ? void 0 : e.parent) ?? null;
    pr(n) && JC(e).has("destroyed") && this._queueActorSync(n);
  }
  _queueRefreshAll(e = 0) {
    const n = () => this.refreshAll(), i = Math.max(0, Number(e) || 0);
    i && typeof setTimeout == "function" ? setTimeout(n, i) : typeof queueMicrotask == "function" ? queueMicrotask(n) : Promise.resolve().then(n);
  }
  _queueActorSync(e) {
    const n = () => this.syncActor(e);
    typeof queueMicrotask == "function" ? queueMicrotask(n) : Promise.resolve().then(n);
  }
  _queueTokenSync(e) {
    const n = () => this.syncToken(e);
    typeof queueMicrotask == "function" ? queueMicrotask(n) : Promise.resolve().then(n);
  }
  _onUpdateSetting(e) {
    if (String((e == null ? void 0 : e.key) ?? "") === `${I}.${this.settingKey}`) {
      if (!this.isEnabled()) {
        this.onCanvasTearDown();
        return;
      }
      this.refreshAll();
    }
  }
  _getDisplayObject(e) {
    return (e == null ? void 0 : e.mesh) ?? null;
  }
  _createColorFilter() {
    const e = EW();
    return e ? new e() : null;
  }
  _createBlurFilter() {
    if (typeof (canvas == null ? void 0 : canvas.createBlurFilter) == "function")
      return canvas.createBlurFilter(1e-4, 2);
    const e = NW();
    return e ? new e() : null;
  }
  _createRuinedDecal() {
    const e = PW();
    if (!e) return null;
    const n = RW(), i = n != null && n.from ? n.from(bA) : bA, a = new e(i);
    return a.name = "mwd-machine-ruined-decal", a.visible = !1, a.alpha = 0.92, a.eventMode = "none", a.interactive = !1, a.zIndex = 95, a;
  }
  _ownedFilters(e) {
    return [e.shimmer, e.color, e.blur].filter(Boolean);
  }
  _attachRecord(e) {
    var r;
    const n = VC((r = e.display) == null ? void 0 : r.filters), i = this._ownedFilters(e), a = SA(n, i).concat(i);
    e.display.filters = a.length ? a : null;
  }
  _detachRecord(e) {
    var i, a;
    const n = e.display;
    if (n) {
      const r = SA(n.filters, this._ownedFilters(e));
      n.filters = r.length ? r : null;
    }
    this._destroyRuinedDecal(e), e.blur && ((a = (i = canvas == null ? void 0 : canvas.blurFilters) == null ? void 0 : i.delete) == null || a.call(i, e.blur));
  }
  _applyHeatState(e, n, { photosensitive: i = !1 } = {}) {
    if (!(n != null && n.active)) {
      this._disableHeatFilters(e);
      return;
    }
    if (e.color && (typeof e.color.reset == "function" && e.color.reset(), e.color.matrix = AA(n.warmth), e.color.alpha = 1, e.color.enabled = !0), e.blur) {
      const r = Ug(n.blur, 0);
      e.blur.enabled = r > 1e-3, "blur" in e.blur && (e.blur.blur = r), "strength" in e.blur && (e.blur.strength = r), "blurX" in e.blur && (e.blur.blurX = r), "blurY" in e.blur && (e.blur.blurY = r);
    }
    e.shimmer && (e.shimmer.enabled = !0, e.shimmer.setVisualState(n, { photosensitive: i }), i ? e.shimmer.setTime(0) : e.shimmer.setTime(e.time));
    const a = !!n.animated && !i;
    e.animated = a, !a && !e.decalAnimated && (e.time = 0), this._syncAnimatedKey(e);
  }
  _disableHeatFilters(e) {
    e.color && (e.color.enabled = !1), e.blur && (e.blur.enabled = !1), e.shimmer && (e.shimmer.enabled = !1), e.animated = !1, e.decalAnimated || (e.time = 0), this._syncAnimatedKey(e);
  }
  _applyRuinedState(e, { ruined: n = !1, photosensitive: i = !!(canvas != null && canvas.photosensitiveMode) } = {}) {
    var s, o, l;
    if (!n) {
      e.decalAnimated = !1, this._destroyRuinedDecal(e), this._syncAnimatedKey(e);
      return;
    }
    e.ruinedDecal || (e.ruinedDecal = this._createRuinedDecal());
    const a = e.ruinedDecal;
    if (!a) return;
    const r = this._getDecalParent(e);
    a.parent !== r && ((o = (s = a.parent) == null ? void 0 : s.removeChild) == null || o.call(s, a), (l = r == null ? void 0 : r.addChild) == null || l.call(r, a)), a.visible = !0, a.blendMode = DW("SCREEN"), this._fitRuinedDecal(e, a), this._applyRuinedDecalFilters(e, { photosensitive: i }), e.decalAnimated = !i, this._animateRuinedDecal(e, 0), this._syncAnimatedKey(e);
  }
  _getDecalParent(e) {
    var n;
    return e.token ?? ((n = e.display) == null ? void 0 : n.parent) ?? null;
  }
  _fitRuinedDecal(e, n) {
    var s, o, l, c;
    const i = this._getTokenVisualBounds(e), a = i.width || 1, r = i.height || a;
    if ((s = n.anchor) != null && s.set && n.anchor.set(0.5, 0.5), (o = n.scale) != null && o.set) {
      const u = Math.max(1, Number(((l = n.texture) == null ? void 0 : l.width) ?? n.width ?? a) || 1), d = Math.max(1, Number(((c = n.texture) == null ? void 0 : c.height) ?? n.height ?? r) || 1), f = Math.max(a / u, r / d) * 1.05;
      e.ruinedBaseScale = f, n.scale.set(f, f);
    } else
      n.width = a * 1.05, n.height = r * 1.05, e.ruinedBaseScale = 1;
    n.x = i.x + a / 2, n.y = i.y + r / 2;
  }
  _applyRuinedDecalFilters(e, { photosensitive: n = !1 } = {}) {
    var l;
    const i = e.ruinedDecal;
    if (!i) return;
    e.ruinedFilters || (e.ruinedFilters = {
      color: this._createColorFilter(),
      blur: this._createBlurFilter(),
      shimmer: hA()
    });
    const { shimmer: a, color: r, blur: s } = e.ruinedFilters;
    a != null && a.setVisualState && (a.enabled = !0, a.setVisualState({
      active: !0,
      normalized: 0.85,
      glow: 0.45,
      shimmer: 0.55,
      pulseAmplitude: 0.18
    }, { photosensitive: n }), (l = a.setTime) == null || l.call(a, e.time)), r && (typeof r.reset == "function" && r.reset(), r.matrix = AA(0.78), r.alpha = 0.95, r.enabled = !0), s && (s.enabled = !n, "blur" in s && (s.blur = 0.35), "strength" in s && (s.strength = 0.35), "blurX" in s && (s.blurX = 0.18), "blurY" in s && (s.blurY = 0.38));
    const o = [a, r, s].filter(Boolean);
    i.filters = o.length ? o : null;
  }
  _destroyRuinedDecal(e) {
    var n, i, a, r, s, o;
    if (e.ruinedDecal) {
      (i = (n = e.ruinedDecal.parent) == null ? void 0 : n.removeChild) == null || i.call(n, e.ruinedDecal), e.ruinedDecal.filters = null, (r = (a = e.ruinedDecal).destroy) == null || r.call(a, { children: !0 }), e.ruinedDecal = null;
      for (const l of Object.values(e.ruinedFilters ?? {}))
        (o = (s = canvas == null ? void 0 : canvas.blurFilters) == null ? void 0 : s.delete) == null || o.call(s, l);
      e.ruinedFilters = null, e.decalAnimated = !1;
    }
  }
  _animateRuinedDecal(e, n = 0) {
    var l, c;
    const i = e.ruinedDecal;
    if (!(i != null && i.visible)) return;
    const a = Number(n) || 0, r = Math.sin(a * 3.4) * 0.035 + Math.sin(a * 7.1) * 0.018, s = Math.sin(a * 1.7) * 0.018, o = e.ruinedBaseScale * (1 + r + s);
    i.alpha = 0.78 + Math.sin(a * 4.6) * 0.08 + Math.sin(a * 9.3) * 0.025, i.rotation = Math.sin(a * 0.8) * 0.018, (l = i.scale) != null && l.set && i.scale.set(o, o);
    for (const u of Object.values(e.ruinedFilters ?? {}))
      (c = u == null ? void 0 : u.setTime) == null || c.call(u, a);
  }
  _syncAnimatedKey(e) {
    e != null && e.key && (e.animated || e.decalAnimated ? (this._animatedKeys.add(e.key), this._ensureTicker()) : (this._animatedKeys.delete(e.key), this._teardownTickerIfIdle()));
  }
  _getTokenVisualBounds(e) {
    var s, o;
    const n = e.token ?? {}, i = e.display ?? {}, a = Number(n.w ?? ((s = n.document) == null ? void 0 : s.widthPx) ?? i.width ?? n.width ?? 0) || 1, r = Number(n.h ?? ((o = n.document) == null ? void 0 : o.heightPx) ?? i.height ?? n.height ?? a) || a;
    return {
      x: Number(i.x ?? 0) || 0,
      y: Number(i.y ?? 0) || 0,
      width: a,
      height: r
    };
  }
  _ensureTicker() {
    var e;
    this._tickerFn || !((e = canvas == null ? void 0 : canvas.app) != null && e.ticker) || (this._tickerFn = (n) => this._onTick(n), this._tickerAdd(canvas.app.ticker, this._tickerFn));
  }
  _teardownTicker(e = !1) {
    var n;
    !this._tickerFn || !((n = canvas == null ? void 0 : canvas.app) != null && n.ticker) || !e && this._animatedKeys.size > 0 || (this._tickerRemove(canvas.app.ticker, this._tickerFn), this._tickerFn = null);
  }
  _teardownTickerIfIdle() {
    this._teardownTicker(!1);
  }
  _onTick(e) {
    var i, a;
    if (canvas != null && canvas.photosensitiveMode) return;
    const n = Ug(((e == null ? void 0 : e.deltaMS) ?? 16.6667) / 1e3, 0);
    for (const r of Array.from(this._animatedKeys)) {
      const s = this._records.get(r);
      if (!(s != null && s.display) || s.display.destroyed) {
        this._animatedKeys.delete(r), this._records.delete(r);
        continue;
      }
      s.time += n, s.animated && ((a = (i = s.shimmer) == null ? void 0 : i.setTime) == null || a.call(i, s.time)), s.decalAnimated && this._animateRuinedDecal(s, s.time);
    }
    this._teardownTickerIfIdle();
  }
}
const Kg = "pilotVisionOwnership";
let kA = !1;
function YC() {
  var e;
  const t = ((e = globalThis.CONST) == null ? void 0 : e.DOCUMENT_OWNERSHIP_LEVELS) ?? {};
  return {
    none: Number(t.NONE ?? 0),
    observer: Number(t.OBSERVER ?? 2),
    owner: Number(t.OWNER ?? 3)
  };
}
function TA(t = null) {
  var i, a, r, s;
  const e = /* @__PURE__ */ new Set(), n = (o) => {
    const l = String(o ?? "").trim();
    l && e.add(l);
  };
  return n(t == null ? void 0 : t.id), n(t == null ? void 0 : t._id), n(t == null ? void 0 : t.uuid), n((i = t == null ? void 0 : t.actor) == null ? void 0 : i.id), n((a = t == null ? void 0 : t.actor) == null ? void 0 : a.uuid), n((r = t == null ? void 0 : t.baseActor) == null ? void 0 : r.id), n((s = t == null ? void 0 : t.baseActor) == null ? void 0 : s.uuid), e;
}
function KW(t = null, e = null) {
  if (!t || !e) return !1;
  const n = TA(e);
  for (const i of TA(t))
    if (n.has(i)) return !0;
  return !1;
}
function HW(t = null, e = null) {
  if (!t || !e || t.isGM) return !1;
  if (KW(e, t.character)) return !0;
  if (typeof e.testUserPermission != "function") return !1;
  try {
    return e.testUserPermission(t, YC().owner);
  } catch {
    return !1;
  }
}
async function jW(t = null) {
  var n, i, a, r, s, o, l, c, u, d, f;
  const e = String(
    ((i = (n = t == null ? void 0 : t.system) == null ? void 0 : n.pilot) == null ? void 0 : i.uuid) ?? ((s = (r = (a = t == null ? void 0 : t.system) == null ? void 0 : a.mwd) == null ? void 0 : r.pilot) == null ? void 0 : s.uuid) ?? ((c = (l = (o = t == null ? void 0 : t.system) == null ? void 0 : o.mwd) == null ? void 0 : l.crew) == null ? void 0 : c.operatorActorUuid) ?? ((f = (d = (u = t == null ? void 0 : t.system) == null ? void 0 : u.mwd) == null ? void 0 : d.crew) == null ? void 0 : f.pilotActorUuid) ?? ""
  ).trim();
  if (!e || typeof fromUuid != "function") return null;
  try {
    return await fromUuid(e);
  } catch {
    return null;
  }
}
function GW() {
  var e;
  const t = (e = globalThis.game) == null ? void 0 : e.users;
  return t ? typeof t.values == "function" ? Array.from(t.values()) : Array.from(t) : [];
}
function qW(t = null) {
  var n, i, a;
  const e = ((n = t == null ? void 0 : t.getFlag) == null ? void 0 : n.call(t, I, Kg)) ?? ((a = (i = t == null ? void 0 : t.flags) == null ? void 0 : i[I]) == null ? void 0 : a[Kg]) ?? {};
  return e && typeof e == "object" && !Array.isArray(e) ? { ...e } : {};
}
async function QC(t = null) {
  var m, g;
  if (!((g = (m = globalThis.game) == null ? void 0 : m.user) != null && g.isGM)) return { ok: !0, skipped: !0 };
  if (!Ta(t)) return { ok: !0, skipped: !0 };
  if (typeof (t == null ? void 0 : t.update) != "function") return { ok: !1, reason: "Machine actor cannot be updated." };
  const { observer: e, none: n } = YC(), i = { ...t.ownership ?? {} }, a = qW(t), r = { ...a }, s = { ...i };
  let o = !1;
  const l = await jW(t), c = new Set(
    l ? GW().filter((p) => HW(p, l)).map((p) => String(p.id ?? "").trim()).filter(Boolean) : []
  );
  for (const p of c) {
    const h = Number(i[p] ?? n) || n;
    h >= e || (s[p] = e, r[p] = h, o = !0);
  }
  for (const [p, h] of Object.entries(a)) {
    if (c.has(p)) continue;
    const b = Math.max(n, Number(h ?? n) || n);
    b > n ? s[p] = b : delete s[p], delete r[p], o = !0;
  }
  const u = {};
  o && (u.ownership = s);
  const d = JSON.stringify(a), f = JSON.stringify(r);
  return d !== f && (u[`flags.${I}.${Kg}`] = r), Object.keys(u).length ? (await t.update(u), { ok: !0, changed: !0 }) : { ok: !0, changed: !1 };
}
async function WW() {
  var e, n, i;
  if (!((n = (e = globalThis.game) == null ? void 0 : e.user) != null && n.isGM)) return;
  const t = Array.from(((i = game.actors) == null ? void 0 : i.contents) ?? game.actors ?? []);
  for (const a of t)
    Ta(a) && await QC(a);
}
function VW() {
  kA || (kA = !0, Hooks.on("updateActor", (t, e = {}) => {
    var n, i;
    !((i = (n = globalThis.game) == null ? void 0 : n.user) != null && i.isGM) || !Ta(t) || (foundry.utils.hasProperty(e, "system.pilot.uuid") || foundry.utils.hasProperty(e, "system.mwd.pilot.uuid") || foundry.utils.hasProperty(e, "system.mwd.crew.operatorActorUuid") || foundry.utils.hasProperty(e, "system.mwd.crew.pilotActorUuid")) && QC(t);
  }));
}
let wA = null;
function Qs(t, e = 0) {
  const n = Number(t);
  return Number.isFinite(n) ? n : e;
}
function MA(t) {
  return (Array.isArray(t) ? t : typeof t == "string" ? t.split(",") : []).map((n) => String(n ?? "").trim()).filter(Boolean);
}
function JW(t = {}, e = {}) {
  var r;
  const n = MA(t.appliesTo);
  if (!n.length) return !0;
  const i = String(e.damageType ?? "").trim(), a = MA((r = e.payload) == null ? void 0 : r.tags);
  return n.includes(i) || n.some((s) => a.includes(s)) || n.some((s) => a.includes(`damage.${s}`));
}
function YW(t = {}) {
  const e = t.actor ?? null;
  if (!e) return;
  let n = Math.max(0, Qs(t.adjustedIncoming ?? t.damageIncoming, 0));
  for (const i of Up(e, { kind: "incomingDamageReduction" })) {
    if (!JW(i, t)) continue;
    const a = Math.max(0, Qs(i.value, 0));
    a <= 0 || (n = Math.max(0, n - a), t.contributions ?? (t.contributions = []), t.contributions.push({
      kind: i.kind,
      sourceId: i.sourceId,
      sourceName: i.sourceName,
      value: -a
    }));
  }
  t.adjustedIncoming = n;
}
function QW(t = {}) {
  const e = t.actor ?? t.source ?? null;
  if (e)
    for (const n of Up(e, { kind: "currentHeatAdjustment" })) {
      const i = String(n.timing ?? "dissipation").trim();
      if (i !== "dissipation" && i !== "activation") continue;
      const a = Qs(n.value, 0);
      a && (t.generated = Math.max(0, Qs(t.generated, 0) + a));
    }
}
function XW(t = {}) {
  var i;
  const e = ((i = t.combatant) == null ? void 0 : i.actor) ?? null;
  if (!e) return;
  const n = Up(e, { kind: "targetingPacket" }).filter((a) => !a.hook || a.hook === t.hook).sort((a, r) => Qs(r.value, 0) - Qs(a.value, 0));
  n.length && (t.bestPacket = n[0]);
}
function ZW() {
  var t;
  !((t = globalThis.Hooks) != null && t.on) || wA === globalThis.Hooks || (wA = globalThis.Hooks, Hooks.on(Rt.harm.beforeMachineDamagePreview, YW), Hooks.on(Rt.heat.beforeHeatDissipation, QW), Hooks.on(Rt.ew.beforeAttackTargeting, (e) => {
    e.hook = Rt.ew.beforeAttackTargeting, XW(e);
  }));
}
const Ls = "mwdSensor", e2 = Object.freeze(["contact", "track", "lock"]);
var NA;
const t2 = Number(((NA = Kp("close")) == null ? void 0 : NA.max) ?? 59);
function Lr(t = null) {
  return t ? Array.isArray(t) ? t : Array.isArray(t.contents) ? t.contents : typeof t.values == "function" ? Array.from(t.values()) : typeof t[Symbol.iterator] == "function" ? Array.from(t) : [] : [];
}
function Xs(t = null) {
  var e;
  return (t == null ? void 0 : t.object) ?? (t == null ? void 0 : t.token) ?? ((e = t == null ? void 0 : t.document) == null ? void 0 : e.object) ?? t;
}
function n2(t = null) {
  return (t == null ? void 0 : t.document) ?? t ?? null;
}
function _d(t = null) {
  var e, n;
  return (t == null ? void 0 : t.actor) ?? ((e = t == null ? void 0 : t.document) == null ? void 0 : e.actor) ?? ((n = t == null ? void 0 : t.actorLink) == null ? void 0 : n.actor) ?? null;
}
function i2(t = null) {
  var e;
  return String(((e = t == null ? void 0 : t.document) == null ? void 0 : e.uuid) ?? (t == null ? void 0 : t.uuid) ?? "").trim();
}
function a2(t = null) {
  var e, n;
  return Number(
    ((e = t == null ? void 0 : t.document) == null ? void 0 : e.disposition) ?? (t == null ? void 0 : t.disposition) ?? ((n = t == null ? void 0 : t.data) == null ? void 0 : n.disposition) ?? 0
  );
}
function r2() {
  var t, e;
  return Number(((e = (t = globalThis.CONST) == null ? void 0 : t.TOKEN_DISPOSITIONS) == null ? void 0 : e.HOSTILE) ?? -1);
}
function vA(t = null, e = "") {
  var a, r, s, o;
  const n = String(e ?? "").trim();
  if (!t || !n) return !1;
  const i = t.statuses ?? ((a = t.document) == null ? void 0 : a.statuses);
  if ((r = i == null ? void 0 : i.has) != null && r.call(i, n) || Array.isArray(i) && i.includes(n)) return !0;
  for (const l of Lr(t.effects ?? t.appliedEffects)) {
    const c = l == null ? void 0 : l.statuses;
    if ((s = c == null ? void 0 : c.has) != null && s.call(c, n) || Array.isArray(c) && c.includes(n) || String((l == null ? void 0 : l.statusId) ?? "").trim() === n) return !0;
  }
  return Lr(((o = t.document) == null ? void 0 : o.effects) ?? []).map((l) => String(l ?? "").trim()).includes(n);
}
function s2(t = null, e = null) {
  const n = _d(t);
  if (!vA(n, "sensorBlind") && !vA(t, "sensorBlind")) return !1;
  const i = xl(t, e);
  return Number.isFinite(i) ? i > t2 : !0;
}
function XC() {
  var t, e, n;
  return ((n = (e = (t = globalThis.foundry) == null ? void 0 : t.canvas) == null ? void 0 : e.perception) == null ? void 0 : n.DetectionMode) ?? globalThis.DetectionMode ?? class {
  };
}
function o2() {
  return XC().DETECTION_TYPES ?? { OTHER: 3, SIGHT: 1 };
}
function ZC(t = null) {
  var e;
  return Xs(
    (t == null ? void 0 : t.object) ?? (t == null ? void 0 : t.token) ?? ((e = t == null ? void 0 : t.document) == null ? void 0 : e.object) ?? (t == null ? void 0 : t.source) ?? t
  );
}
function eI(t = null) {
  return Xs(t);
}
function l2(t = null, e = null, n = null) {
  var o;
  const i = Number((e == null ? void 0 : e.range) ?? ((o = e == null ? void 0 : e.data) == null ? void 0 : o.range) ?? 0);
  if (!Number.isFinite(i) || i <= 0) return !0;
  const a = ZC(t), r = eI(n), s = xl(a, r);
  return Number.isFinite(s) && s <= i;
}
function Bd(t = null) {
  const e = n2(t), n = (e == null ? void 0 : e.detectionModes) ?? (t == null ? void 0 : t.detectionModes) ?? [];
  return Array.isArray(n) ? n : Lr(n);
}
function c2(t = {}) {
  var e, n;
  return typeof (t == null ? void 0 : t.toObject) == "function" ? t.toObject() : typeof (t == null ? void 0 : t.toJSON) == "function" ? t.toJSON() : (n = (e = globalThis.foundry) == null ? void 0 : e.utils) != null && n.deepClone ? foundry.utils.deepClone(t) : JSON.parse(JSON.stringify(t ?? {}));
}
function Qh(t = null, e = null) {
  const n = Xs(t), i = Xs(e), a = i2(i);
  if (!n || !a) return "blind";
  const r = Pn(n);
  return r ? ro(r, a) : "blind";
}
function Xh(t = null, e = null) {
  const n = Xs(t), i = Xs(e);
  if (!n || !i) return !1;
  const a = _d(i);
  return !Ta(a) || a2(i) !== r2() || s2(n, i) ? !1 : e2.includes(Qh(n, i));
}
class u2 extends XC() {
  _canDetect(e, n) {
    return Xh(ZC(e), eI(n));
  }
  _testLOS() {
    return !0;
  }
  _testRange(e, n, i) {
    return l2(e, n, i);
  }
}
function Zh(t = []) {
  const e = Bd({ detectionModes: t }).map(c2), n = e.findIndex((i) => String((i == null ? void 0 : i.id) ?? "").trim() === Ls);
  if (n >= 0) {
    const i = e[n] ?? {}, a = { ...i, id: Ls, enabled: !0 }, r = JSON.stringify(i) !== JSON.stringify(a);
    return e[n] = a, { changed: r, modes: e };
  }
  return {
    changed: !0,
    modes: e.concat([{ id: Ls, enabled: !0, range: 0 }])
  };
}
async function d2(t = null) {
  const e = _d(t);
  if (!Ta(e)) return !1;
  const { changed: n, modes: i } = Zh(Bd(t));
  return n ? typeof (t == null ? void 0 : t.update) == "function" ? (await t.update({ detectionModes: i }), !0) : (t.detectionModes = i, !0) : !1;
}
async function m2(t = null) {
  if (!Ta(t)) return !1;
  const e = t.prototypeToken ?? t.token ?? null, { changed: n, modes: i } = Zh(Bd(e));
  return n ? typeof t.update == "function" ? (await t.update({ "prototypeToken.detectionModes": i }), !0) : (e && (e.detectionModes = i), !0) : !1;
}
async function f2(t = null) {
  const e = Lr(t == null ? void 0 : t.tokens), n = [];
  let i = 0;
  for (const a of e) {
    const r = _d(a);
    if (!Ta(r)) continue;
    const { changed: s, modes: o } = Zh(Bd(a));
    s && (typeof (a == null ? void 0 : a.update) == "function" ? await a.update({ detectionModes: o }) : n.push({ _id: a.id, detectionModes: o }), i += 1);
  }
  return n.length && typeof (t == null ? void 0 : t.updateEmbeddedDocuments) == "function" && await t.updateEmbeddedDocuments("Token", n), i;
}
async function CA({
  actors: t = ((a) => (a = globalThis.game) == null ? void 0 : a.actors)(),
  scenes: e = ((r) => (r = globalThis.game) == null ? void 0 : r.scenes)(),
  canvasTokens: n = ((o) => (o = ((s) => (s = globalThis.canvas) == null ? void 0 : s.tokens)()) == null ? void 0 : o.placeables)(),
  requireGM: i = !0
} = {}) {
  var c, u;
  if (i && !((u = (c = globalThis.game) == null ? void 0 : c.user) != null && u.isGM)) return { changed: 0, skipped: !0 };
  let l = 0;
  for (const d of Lr(t))
    await m2(d) && (l += 1);
  for (const d of Lr(e))
    l += await f2(d);
  for (const d of Lr(n))
    await d2((d == null ? void 0 : d.document) ?? d) && (l += 1);
  return { changed: l, skipped: !1 };
}
function g2() {
  var n;
  const t = (n = globalThis.canvas) == null ? void 0 : n.perception;
  if (typeof (t == null ? void 0 : t.update) != "function") return !1;
  const e = [
    { sight: { refresh: !0 }, visibility: { refresh: !0 } },
    { sight: !0, visibility: !0 },
    { initializeVision: !0, refreshVision: !0 }
  ];
  for (const i of e)
    try {
      return t.update(i, !0), !0;
    } catch {
    }
  return !1;
}
function p2() {
  var n;
  const t = (n = globalThis.CONFIG) == null ? void 0 : n.Canvas;
  if (!t) return !1;
  if (t.detectionModes ?? (t.detectionModes = {}), t.detectionModes[Ls]) return !0;
  const e = o2();
  return t.detectionModes[Ls] = new u2({
    id: Ls,
    label: "MWD Sensor",
    type: e.OTHER ?? e.SIGHT ?? 3,
    walls: !1
  }), !0;
}
function h2(t = "blind", e = "blind") {
  const n = vn.indexOf(t), i = vn.indexOf(e);
  return (n < 0 ? 0 : n) - (i < 0 ? 0 : i);
}
const _u = "mwd-sensor-overlay", b2 = Object.freeze({
  contact: Object.freeze({
    state: "contact",
    label: "HOSTILE CONTACT",
    color: 5306111,
    alpha: 0.58,
    weight: 1
  }),
  track: Object.freeze({
    state: "track",
    label: "HOSTILE TRACK",
    color: 6291381,
    alpha: 0.78,
    weight: 2
  }),
  lock: Object.freeze({
    state: "lock",
    label: "HOSTILE LOCK",
    color: 16765015,
    alpha: 0.92,
    weight: 3
  })
});
let IA = !1, Jm = !1;
const Nl = /* @__PURE__ */ new Map();
function qr(t = null) {
  return t ? Array.isArray(t) ? t : Array.isArray(t.contents) ? t.contents : typeof t.values == "function" ? Array.from(t.values()) : typeof t[Symbol.iterator] == "function" ? Array.from(t) : [] : [];
}
function eb(t = null) {
  var e, n;
  return String(((e = t == null ? void 0 : t.document) == null ? void 0 : e.uuid) ?? (t == null ? void 0 : t.uuid) ?? (t == null ? void 0 : t.id) ?? ((n = t == null ? void 0 : t.document) == null ? void 0 : n.id) ?? "").trim();
}
function y2() {
  var e, n;
  const t = qr((n = (e = globalThis.canvas) == null ? void 0 : e.tokens) == null ? void 0 : n.controlled).filter((i) => i == null ? void 0 : i.actor);
  return t.length ? t : [];
}
function tI() {
  var t, e;
  return qr((e = (t = globalThis.canvas) == null ? void 0 : t.tokens) == null ? void 0 : e.placeables).filter((n) => n == null ? void 0 : n.actor);
}
function S2() {
  var t, e, n, i;
  return {
    Container: ((t = globalThis.PIXI) == null ? void 0 : t.Container) ?? null,
    Graphics: ((e = globalThis.PIXI) == null ? void 0 : e.Graphics) ?? null,
    Text: ((n = globalThis.PIXI) == null ? void 0 : n.Text) ?? null,
    TextStyle: ((i = globalThis.PIXI) == null ? void 0 : i.TextStyle) ?? null
  };
}
function A2(t = null) {
  var i, a;
  const e = Number((t == null ? void 0 : t.w) ?? ((i = t == null ? void 0 : t.document) == null ? void 0 : i.widthPx) ?? (t == null ? void 0 : t.width) ?? 0) || 1, n = Number((t == null ? void 0 : t.h) ?? ((a = t == null ? void 0 : t.document) == null ? void 0 : a.heightPx) ?? (t == null ? void 0 : t.height) ?? e) || e;
  return {
    width: e,
    height: n,
    centerX: e / 2,
    centerY: n / 2
  };
}
function k2(t = null, e = 0.5, n = 0.5) {
  var i;
  (i = t == null ? void 0 : t.anchor) != null && i.set && t.anchor.set(e, n);
}
function T2(t, e, n, i) {
  if (!t) return null;
  const a = e ? new e({
    align: "center",
    fill: n.color,
    fontFamily: "MWD Numeric, monospace",
    fontSize: 13,
    fontWeight: "700",
    letterSpacing: 0,
    stroke: 132874,
    strokeThickness: 3
  }) : {
    align: "center",
    fill: n.color,
    fontSize: 13,
    fontWeight: "700",
    stroke: 132874,
    strokeThickness: 3
  }, r = new t(n.label, a);
  return r.name = `${_u}-label`, r.x = 0, r.y = i, r.alpha = n.alpha, k2(r, 0.5, 0.5), r;
}
function w2(t, e, n) {
  const i = Math.max(4, Math.min(e.width, e.height) * 0.075);
  t.lineStyle(1, n.color, n.alpha), t.beginFill(n.color, 0.32), t.drawCircle(e.centerX, Math.max(i + 2, e.height * 0.12), i), t.endFill();
}
function M2(t, e, n) {
  const i = Math.max(4, Math.min(e.width, e.height) * 0.08), a = Math.max(10, Math.min(e.width, e.height) * 0.22), r = i, s = e.width - i, o = i, l = e.height - i;
  t.lineStyle(2, n.color, n.alpha), t.moveTo(r + a, o).lineTo(r, o).lineTo(r, o + a), t.moveTo(s - a, o).lineTo(s, o).lineTo(s, o + a), t.moveTo(r, l - a).lineTo(r, l).lineTo(r + a, l), t.moveTo(s, l - a).lineTo(s, l).lineTo(s - a, l);
}
function v2(t, e, n) {
  const i = Math.max(14, Math.min(e.width, e.height) * 0.34);
  t.lineStyle(2, n.color, n.alpha), t.drawCircle(e.centerX, e.centerY, i), t.moveTo(e.centerX - i - 8, e.centerY).lineTo(e.centerX - i + 6, e.centerY), t.moveTo(e.centerX + i - 6, e.centerY).lineTo(e.centerX + i + 8, e.centerY), t.moveTo(e.centerX, e.centerY - i - 8).lineTo(e.centerX, e.centerY - i + 6), t.moveTo(e.centerX, e.centerY + i - 6).lineTo(e.centerX, e.centerY + i + 8);
}
function tb(t = null, e = null, n = null) {
  var d;
  if (!t || !e) return null;
  const i = eb(t);
  if (!i) return null;
  const { Container: a, Graphics: r, Text: s, TextStyle: o } = S2();
  if (!a || !r)
    return t._mwdSensorOverlayModel = e, Nl.set(i, { token: t, model: e, overlay: null }), e;
  Bu(t);
  const l = A2(t), c = new a();
  c.name = _u, c.eventMode = "none", c.interactive = !1, c.zIndex = 120;
  const u = new r();
  if (u.name = `${_u}-graphics`, n(u, l, e), c.addChild(u), e.state !== "contact") {
    const f = T2(s, o, e, Math.max(12, -8));
    f && (f.x = l.centerX, c.addChild(f));
  }
  return (d = t.addChild) == null || d.call(t, c), !c.parent && Array.isArray(t.children) && t.children.push(c), t._mwdSensorOverlayModel = e, Nl.set(i, { token: t, model: e, overlay: c }), c;
}
function Bu(t = null) {
  var a, r, s;
  const e = eb(t), n = e ? Nl.get(e) : null, i = qr(t == null ? void 0 : t.children).filter((o) => (o == null ? void 0 : o.name) === _u);
  for (const o of [n == null ? void 0 : n.overlay, ...i].filter(Boolean)) {
    if ((r = (a = o.parent) == null ? void 0 : a.removeChild) == null || r.call(a, o), Array.isArray(t == null ? void 0 : t.children)) {
      const l = t.children.indexOf(o);
      l >= 0 && t.children.splice(l, 1);
    }
    (s = o.destroy) == null || s.call(o, { children: !0 });
  }
  e && Nl.delete(e), t && Object.prototype.hasOwnProperty.call(t, "_mwdSensorOverlayModel") && delete t._mwdSensorOverlayModel;
}
function xd(t = "") {
  const e = String(t ?? "").trim(), n = b2[e] ?? null;
  return n ? { ...n } : null;
}
function C2(t = [], e = null) {
  let n = "";
  for (const i of qr(t)) {
    if (i === e || !Xh(i, e)) continue;
    const a = Qh(i, e);
    (!n || h2(a, n) > 0) && (n = a);
  }
  return xd(n);
}
function nI(t, e = xd("contact")) {
  return tb(t, e, w2);
}
function iI(t, e = xd("track")) {
  return tb(t, e, M2);
}
function aI(t, e = xd("lock")) {
  return tb(t, e, v2);
}
function rI(t = tI()) {
  const e = qr(t);
  if (e.length) {
    for (const n of e) Bu(n);
    return;
  }
  for (const n of Array.from(Nl.values()))
    Bu(n.token);
}
function nb({
  observerTokens: t = y2(),
  targetTokens: e = tI()
} = {}) {
  const n = qr(t), i = qr(e), a = /* @__PURE__ */ new Map();
  for (const r of i) {
    const s = C2(n, r);
    if (a.set(eb(r), s), !s) {
      Bu(r);
      continue;
    }
    s.state === "lock" ? aI(r, s) : s.state === "track" ? iI(r, s) : nI(r, s);
  }
  return a;
}
function pi({ requestPerception: t = !0 } = {}) {
  if (t && g2(), Jm) return;
  Jm = !0;
  const e = () => {
    Jm = !1, nb();
  };
  typeof queueMicrotask == "function" ? queueMicrotask(e) : Promise.resolve().then(e);
}
function I2() {
  return IA || !globalThis.Hooks ? !1 : (IA = !0, Hooks.on("canvasReady", () => pi({ requestPerception: !1 })), Hooks.on("sightRefresh", () => nb()), Hooks.on("refreshToken", () => pi({ requestPerception: !1 })), Hooks.on("controlToken", () => pi()), Hooks.on("targetToken", () => pi()), Hooks.on("updateToken", () => pi()), Hooks.on("updateActor", () => pi()), Hooks.on("updateCombatant", () => pi()), Hooks.on("createCombatant", () => pi()), Hooks.on("deleteCombatant", () => pi()), Hooks.on("updateCombat", () => pi()), Hooks.on("deleteCombat", () => pi()), Hooks.on("canvasTearDown", () => rI()), !0);
}
function E2() {
  Object.assign(CONFIG.fontDefinitions, {
    "MWD UI": {
      editor: !0,
      fonts: [
        { urls: ["systems/mwd/fonts/Exo2/Exo2-Regular.woff2"], weight: 400, style: "normal" },
        { urls: ["systems/mwd/fonts/Exo2/Exo2-Italic.woff2"], weight: 400, style: "italic" },
        { urls: ["systems/mwd/fonts/Exo2/Exo2-Bold.woff2"], weight: 700, style: "normal" }
      ]
    },
    "MWD Display": {
      editor: !1,
      fonts: [
        { urls: ["systems/mwd/fonts/btclassic/BattletechOldStyle.woff2"], weight: 400, style: "normal" }
      ]
    },
    "MWD Body": {
      editor: !0,
      fonts: [
        { urls: ["systems/mwd/fonts/bitter/Bitter-Regular.woff2"], weight: 400, style: "normal" },
        { urls: ["systems/mwd/fonts/bitter/Bitter-Bold.woff2"], weight: 700, style: "normal" }
      ]
    },
    "MWD Numeric": {
      editor: !1,
      fonts: [
        { urls: ["systems/mwd/fonts/anta/Anta-Regular.woff2"], weight: 400, style: "normal" }
      ]
    },
    "Material Symbols Rounded": {
      editor: !1,
      fonts: [
        { urls: ["systems/mwd/fonts/Icons/MaterialSymbolsRounded.woff2"], weight: 400, style: "normal" }
      ]
    },
    "MWD Logo": {
      editor: !1,
      fonts: [
        { urls: ["systems/mwd/fonts/btclassic/BTLogo_old.woff2"], weight: 400, style: "normal" }
      ]
    }
  });
}
function N2() {
  return {
    get(t) {
      return Ft(t);
    },
    getSkills({ withKnowledge: t = !1 } = {}) {
      return Qo();
    },
    list() {
      return Qo();
    }
  };
}
function P2() {
  return {
    get(t) {
      return Fa(t);
    },
    list() {
      return kd();
    },
    listByType(t) {
      return rh(t);
    },
    getTypeLabel(t) {
      return qs(t);
    },
    evaluate(t) {
      return qa(t);
    }
  };
}
function R2() {
  return {
    normalizeQualitySystem(t) {
      return Yn(t);
    },
    getEditorConfig() {
      return $k();
    },
    evaluatePhase(t) {
      return zt(t);
    },
    applyMutations(t) {
      return ga(t);
    },
    buildRollFacts(t) {
      return Xu(t);
    },
    buildActionCostFacts(t) {
      return df(t);
    },
    buildActivationBudgetFacts(t) {
      return Hk(t);
    },
    buildBurnFacts(t) {
      return Ic(t);
    },
    buildConditionPenaltyFacts(t) {
      return jk(t);
    },
    buildInitiativeFacts(t) {
      return zk(t);
    },
    buildDamageFacts(t) {
      return Fk(t);
    },
    buildDefenseRatingFacts(t) {
      return Kk(t);
    },
    buildDerivedPersonalCombatFacts(t) {
      return Uk(t);
    },
    buildEdgeFacts(t) {
      return mf(t);
    },
    buildEndOfActivationFacts(t) {
      return Gk(t);
    },
    buildOptionalManualModifiers(t) {
      return qk(t);
    },
    getActiveEffectModifier(t, e) {
      return oi(t, e);
    }
  };
}
function D2() {
  return {
    normalizeCarrier(t, e) {
      return Qu(t, e);
    },
    evaluatePhase(t) {
      return uf(t);
    },
    prepareUsageCommit(t) {
      return DN(t);
    },
    commitUsage(t) {
      return ON(t);
    },
    buildAppliedSummary(t) {
      return LN(t);
    }
  };
}
class ib {
  static start() {
    const e = new ib();
    Hooks.once("init", () => e.onInit()), Hooks.once("ready", () => e.onReady());
  }
  async onInit() {
    console.log(tt + "AnarchySystem.onInit"), game.system.mwd = this, game.system.anarchy = this, game.mwd ?? (game.mwd = {}), E2(), p2(), I2(), Iz(), lx("mwd"), dB("mwd"), game.mwd.roll = dA, game.mwd.attacks = eS, game.mwd.personalCombat = D, game.mwd.personalCombatActions = $1, game.mwd.playerGadget = mB({ systemId: I }), game.mwd.combat = {
      resolveActivationUnit: (...e) => D.resolveActivationUnit(...e),
      resolveCombatantForActor: (...e) => D.resolveCombatantForActor(...e),
      getActionEconomyActorForCombatant: (...e) => D.getActionEconomyActorForCombatant(...e),
      getPlatformActorForCombatant: (...e) => D.getPlatformActorForCombatant(...e)
    }, game.mwd.machineActions = MS, game.mwd.machineSensors = {
      canDetect: Xh,
      getDetectionState: Qh,
      syncTokenDetectionModes: CA,
      refreshSensorOverlays: nb,
      renderContactOverlay: nI,
      renderTrackOverlay: iI,
      renderLockOverlay: aI,
      clearSensorOverlays: rI
    }, game.mwd.harm = Object.assign(Mn, hW), game.mwd.machineHeat = {
      adjustPendingHeat: sd,
      buildModel: ma,
      recordAttackHeat: XT,
      resolvePendingHeat: Lp,
      setPendingHeat: Op
    }, game.mwd.tokenHeatFx = new UW(), game.mwd.tokenHeatFx.init(), this.roll = dA, this.attacks = eS, this.personalCombat = D, this.combat = game.mwd.combat, this.machineActions = MS, this.harm = game.mwd.harm, this.machineHeat = game.mwd.machineHeat, this.tokenHeatFx = game.mwd.tokenHeatFx, this.skills = N2(), this.lifeModules = P2(), this.rules = D2(), this.traits = R2(), this.remoteCall = new ba(), rK(), OU(), VW(), ZW(), game.system.mwd.skills = this.skills, game.system.mwd.lifeModules = this.lifeModules, game.system.mwd.rules = this.rules, game.system.mwd.traits = this.traits, game.mwd.skills = this.skills, game.mwd.lifeModules = this.lifeModules, game.mwd.rules = this.rules, game.mwd.traits = this.traits, pt.init(), this.modifiers = new ft(), dn.register(new _q()), dn.register(new xq()), dn.register(new zq()), dn.register(new Fq()), dn.register(Uq), dn.register(new Hq()), dn.register(new Gq()), dn.register(new Wq()), dn.register(new Qq()), dn.register(new sW()), dn.register(new oW()), dn.register(new Zq()), dn.register(new tW()), dn.register(new aW()), dn.register(new DB()), _t.init(), Handlebars.registerHelper("mwdClassList", (e) => Array.isArray(e) ? e.join(" ") : typeof e == "string" ? e : ""), this.hooks = new Cd(), this.styles = new kB(), this.handlebarsManager = new yh(), D.init(), Qx.register(), Hooks.on("updateSetting", (e) => {
      (e == null ? void 0 : e.key) === `${I}.statusConditionCatalog` && pb();
    }), console.log(tt + "AnarchySystem.onInit | loading system"), CONFIG.ANARCHY = z, CONFIG.Combat.documentClass = MH, CONFIG.Combat.initiative = { formula: "2d6" }, pb(), CONFIG.Actor.documentClass = wH, CONFIG.Item.documentClass = Vs, Vs.init(), lM(), rN(), QK(), hH(), await SH(), console.log(tt + "AnarchySystem.onInit | done");
  }
  async onReady() {
    var n, i, a;
    if (console.log(tt + "AnarchySystem.onReady"), await D.onReady(), (a = (i = (n = game.mwd) == null ? void 0 : n.tokenHeatFx) == null ? void 0 : i.refreshAll) == null || a.call(i), !game.user.isGM) {
      fB({ systemId: I });
      return;
    }
    await n_(), await jE(), await hP(), await WW(), await CA();
    const e = game.settings.get(I, "enableGMGadget");
    if (!e) {
      console.log(`${tt}GMManager render skipped (enableGMGadget=false)`);
      return;
    }
    game.mwd = game.mwd ?? {}, game.mwd.gmGadget = () => cx({ systemId: I }).render({ force: !0 }), e && game.mwd.gmGadget();
  }
}
ib.start();
//# sourceMappingURL=index.mjs.map
