{{!--
templates/common/enum-value-label.hbs
Purpose: Handlebars template for enum-value-label. Used to render UI in the system.
--}}

{{!
  data is:
  {
    // the entries to propose as options
    entries: [
      { "value": "k1", "label": "label1"},
      { "value": "k2", "labelkey": "ANARCHY.(...).label2"},
    ]
  }
}}
{{#unless noEmpty}}
<option value=""></option>
{{/unless}}
{{#each entries as |entry|}}
<option value="{{entry.value}}">{{> 'systems/mwd/templates/common/label.hbs' entry}}</option>
{{/each}}
